import Mathlib.Algebra.BigOperators.Group.Finset.Basic
import Mathlib.Algebra.Order.BigOperators.Group.Finset
import Mathlib.Analysis.Matrix.Spectrum
import Mathlib.Data.Real.Basic
import Mathlib.LinearAlgebra.Matrix.Rank
import Mathlib.Tactic.FieldSimp
import Mathlib.Tactic.Linarith
import Mathlib.Tactic.Ring
import NumStability.Algorithms.LinearSystems.LeastSquares.Basic
import NumStability.Algorithms.LinearSystems.LeastSquares.NormalEquations
import NumStability.Algorithms.LinearSystems.LeastSquares.QRSolve
import NumStability.Algorithms.LinearSystems.LeastSquares.RankGeometry
import NumStability.Algorithms.LinearSystems.LeastSquares.Refinement
import NumStability.Algorithms.LinearSystems.LeastSquares.StoredQR
import NumStability.Algorithms.LinearSystems.QR.Householder.PanelApplication
import NumStability.Algorithms.LinearSystems.QR.Householder.StoredQR
import NumStability.Algorithms.LinearSystems.QR.HouseholderSpec
import NumStability.Algorithms.LinearSystems.QR.Householder.TrailingPanels
import NumStability.Algorithms.LinearSystems.QR.QRSolve
import NumStability.Algorithms.LinearSystems.Triangular.BackSubstitution
import NumStability.Algorithms.LinearSystems.Triangular.DiagonalDominance
import NumStability.Algorithms.LinearSystems.Triangular.InverseBounds
import NumStability.Analysis.MatrixAlgebra
import NumStability.Analysis.Perturbation.LeastSquares.NormalEquations
import NumStability.Analysis.PerturbationTheory
import NumStability.Analysis.Rounding
import NumStability.FloatingPoint.Model

namespace NumStability

open scoped BigOperators
open scoped BigOperators Matrix.Norms.Frobenius

/-!
# Basic

Canonical reusable module extracted without change from Higham20Algorithms, Higham20Equations, Higham20Refinement, Higham20RowSorting, LSPerturbation, LSQRSolve.
-/

/-- **Augmented system for the LS problem** (Higham eq 20.3).

    The LS solution x and residual r = b − Ax satisfy the
    (m+n)×(m+n) augmented system:

    [I  A ][r]   [b]
    [Aᵀ 0 ][x] = [0]

    This is equivalent to the normal equations AᵀAx = Aᵀb.

    The inverse of the augmented system matrix has blocks
    involving A⁺ and (AᵀA)⁻¹ (eq 19.6), enabling componentwise
    perturbation analysis.

    We capture the componentwise perturbation result (Theorem 20.2)
    in terms of the n×n Gram inverse (AᵀA)⁻¹, which is representable
    in the library's square-matrix framework. -/
structure LSAugmentedPerturbation (n : ℕ)
    (ATA ATA_inv : Fin n → Fin n → ℝ)
    (x y : Fin n → ℝ) (eps : ℝ)
    (bound_vec : Fin n → ℝ) : Prop where
  /-- (AᵀA)⁻¹ is the inverse of the Gram matrix. -/
  gram_inv : IsInverse n ATA ATA_inv
  /-- The perturbation ε is nonneg. -/
  eps_nonneg : 0 ≤ eps
  /-- Componentwise bound on the solution perturbation (eq 19.8):
      |y_i − x_i| ≤ ε · bound_vec_i
      where bound_vec captures the |(AᵀA)⁻¹|-weighted perturbation. -/
  solution_bound : ∀ i, |y i - x i| ≤ eps * bound_vec i
/-- Higham, 2nd ed., Chapter 20, equation (20.5): source componentwise
    perturbation model `|DeltaA| <= eps E`, `|Deltab| <= eps f`.

    The nonnegativity fields record the implicit majorant side conditions used
    when (20.5) is propagated through the componentwise bounds (20.7)-(20.10). -/
def LSComponentwisePerturbation {m n : ℕ}
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (eps : ℝ) : Prop :=
  0 ≤ eps ∧
  (∀ i j, 0 ≤ E i j) ∧
  (∀ i, 0 ≤ f i) ∧
  (∀ i j, |DeltaA i j| ≤ eps * E i j) ∧
  (∀ i, |Deltab i| ≤ eps * f i)
/-- The componentwise data majorant `f + E |y|` used in (20.7)-(20.8). -/
noncomputable def lsComponentwiseDataMajorant {m n : ℕ}
    (E : Fin m → Fin n → ℝ) (f : Fin m → ℝ) (y : Fin n → ℝ) :
    Fin m → ℝ :=
  fun i => f i + rectMatMulVec E (absVec n y) i
/-- The componentwise transposed perturbation majorant `E^T |s|` used in
    (20.7)-(20.8). -/
noncomputable def lsComponentwiseTransposeMajorant {m n : ℕ}
    (E : Fin m → Fin n → ℝ) (s : Fin m → ℝ) : Fin n → ℝ :=
  fun j => ∑ i : Fin m, E i j * |s i|
/-- Unit-roundoff threshold that implies the half-radius guard for the
    conservative gamma-factor RHS package in Theorem 20.4. -/
noncomputable def theorem20_4GammaFactorRhsSmallnessThreshold (n k : ℕ) : ℝ :=
  1 / (2 *
    (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n : ℝ))
/-- The gamma-factor RHS smallness threshold is positive for every nonempty
    panel. -/
theorem theorem20_4GammaFactorRhsSmallnessThreshold_pos {n k : ℕ}
    (hn : 0 < n) :
    0 < theorem20_4GammaFactorRhsSmallnessThreshold n k := by
  have hidx_nat :
      0 < householderQRRhsPanelGammaClosedGrowthIndex (n + k) n :=
    theorem20_4GammaFactorRhsIndex_pos (n := n) (k := k) hn
  have hidx : 0 < (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n : ℝ) := by
    exact_mod_cast hidx_nat
  unfold theorem20_4GammaFactorRhsSmallnessThreshold
  exact one_div_pos.mpr (mul_pos (by norm_num) hidx)
/-- Source-facing smallness lemma for the current Theorem 20.4 conservative
    gamma-factor RHS package.  It replaces the raw half-radius guard
    `idx * fp.u <= 1/2` by a named unit-roundoff threshold. -/
theorem theorem20_4GammaFactorRhs_half_of_unit_roundoff_lt_smallnessThreshold
    (fp : FPModel) {n k : ℕ} (hn : 0 < n)
    (hu : fp.u < theorem20_4GammaFactorRhsSmallnessThreshold n k) :
    ((householderQRRhsPanelGammaClosedGrowthIndex (n + k) n : ℝ) *
        fp.u ≤ 1 / 2) := by
  let idx : ℕ := householderQRRhsPanelGammaClosedGrowthIndex (n + k) n
  have hidx_nat : 0 < idx := by
    simpa [idx] using theorem20_4GammaFactorRhsIndex_pos (n := n) (k := k) hn
  have hidx : 0 < (idx : ℝ) := by
    exact_mod_cast hidx_nat
  have hmul_lt :
      (idx : ℝ) * fp.u <
        (idx : ℝ) * theorem20_4GammaFactorRhsSmallnessThreshold n k :=
    mul_lt_mul_of_pos_left hu hidx
  have hright :
      (idx : ℝ) * theorem20_4GammaFactorRhsSmallnessThreshold n k = 1 / 2 := by
    have hthreshold :
        theorem20_4GammaFactorRhsSmallnessThreshold n k =
          1 / (2 * (idx : ℝ)) := by
      dsimp [idx, theorem20_4GammaFactorRhsSmallnessThreshold]
    rw [hthreshold]
    field_simp [ne_of_gt hidx]
  rw [hright] at hmul_lt
  exact le_of_lt hmul_lt
/-- Source-shaped bounded perturbation wrapper for Higham, 2nd ed.,
    Chapter 20, Theorem 20.3.  It does not prove the Householder QR
    perturbations; rather, it packages the exact final step once a concrete QR
    path supplies perturbed normal equations and data-perturbation radii. -/
theorem exists_perturbed_ls_minimizer_of_rectLSNormalEquations_normBound
    {m n : ℕ} (A ΔA : Fin m → Fin n → ℝ) (b Δb : Fin m → ℝ)
    (x_hat : Fin n → ℝ) (cA cb : ℝ)
    (hNE : RectLSNormalEquations
      (fun i j => A i j + ΔA i j) (fun i => b i + Δb i) x_hat)
    (hΔA : frobNorm ΔA ≤ cA) (hΔb : vecNorm2 Δb ≤ cb) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤ cA ∧ vecNorm2 Δb' ≤ cb ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i) x_hat := by
  exact ⟨ΔA, Δb, hΔA, hΔb, hNE.perturbed_isLeastSquaresMinimizer⟩

-- ============================================================
-- §20.3  Exact augmented-MGS least-squares algebra
-- ============================================================
/-- Componentwise domination of a square top-block perturbation gives the
    corresponding rectangular Frobenius bound after embedding as `[ΔR; 0]`. -/
theorem rectTopBlock_frobNorm_perturb_bound {m n : ℕ}
    (c : ℝ) (R ΔR : Fin n → Fin n → ℝ)
    (hc : 0 ≤ c)
    (hΔ : ∀ i j : Fin n, |ΔR i j| ≤ c * |R i j|) :
    frobNormRect (rectTopBlock (m := m) ΔR) ≤
      c * frobNormRect (rectTopBlock (m := m) R) := by
  have hentry : ∀ (i : Fin m) (j : Fin n),
      |rectTopBlock (m := m) ΔR i j| ≤
        c * |rectTopBlock (m := m) R i j| := by
    intro i j
    by_cases hi : i.val < n
    · calc
        |rectTopBlock (m := m) ΔR i j|
            = |ΔR ⟨i.val, hi⟩ j| := by
                rw [rectTopBlock_top ΔR i j hi]
        _ ≤ c * |R ⟨i.val, hi⟩ j| := hΔ ⟨i.val, hi⟩ j
        _ = c * |rectTopBlock (m := m) R i j| := by
                rw [rectTopBlock_top R i j hi]
    · have hle : n ≤ i.val := le_of_not_gt hi
      rw [rectTopBlock_bottom ΔR i j hle, rectTopBlock_bottom R i j hle,
        abs_zero, mul_zero]
  have hnonneg : ∀ (i : Fin m) (j : Fin n),
      0 ≤ c * |rectTopBlock (m := m) R i j| := by
    intro i j
    exact mul_nonneg hc (abs_nonneg _)
  have hmono :=
    frobNormRect_le_of_entry_abs_le
      (rectTopBlock (m := m) ΔR)
      (fun i j => c * |rectTopBlock (m := m) R i j|)
      hnonneg hentry
  calc
    frobNormRect (rectTopBlock (m := m) ΔR)
        ≤ frobNormRect
            (fun i j => c * |rectTopBlock (m := m) R i j|) := hmono
    _ = |c| * frobNormRect
            (fun i j => |rectTopBlock (m := m) R i j|) := by
          rw [frobNormRect_smul]
    _ = c * frobNormRect (rectTopBlock (m := m) R) := by
          rw [abs_of_nonneg hc, frobNormRect_abs]
/-- The top-block Frobenius budget specialized to the repository's
    floating-point `gamma` constant. -/
theorem rectTopBlock_frobNorm_perturb_bound_of_gamma {m n : ℕ}
    (fp : FPModel) (R ΔR : Fin n → Fin n → ℝ)
    (hγ : gammaValid fp n)
    (hΔ : ∀ i j : Fin n, |ΔR i j| ≤ gamma fp n * |R i j|) :
    frobNormRect (rectTopBlock (m := m) ΔR) ≤
      gamma fp n * frobNormRect (rectTopBlock (m := m) R) :=
  rectTopBlock_frobNorm_perturb_bound
    (m := m) (n := n) (gamma fp n) R ΔR (gamma_nonneg fp hγ) hΔ
/-- Columnwise top-block perturbation budget from entrywise relative control. -/
theorem rectTopBlock_col_vecNorm2_perturb_bound {m n : ℕ} {c : ℝ}
    (R ΔR : Fin n → Fin n → ℝ) (hc : 0 ≤ c)
    (hΔ : ∀ i j : Fin n, |ΔR i j| ≤ c * |R i j|) (j : Fin n) :
    vecNorm2 (fun i : Fin m => rectTopBlock (m := m) ΔR i j) ≤
      c * vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j) := by
  have hentry : ∀ i : Fin m,
      |rectTopBlock (m := m) ΔR i j| ≤
        c * |rectTopBlock (m := m) R i j| := by
    intro i
    by_cases hi : i.val < n
    · have hR := hΔ ⟨i.val, hi⟩ j
      simpa [rectTopBlock_top, hi] using hR
    · have hle : n ≤ i.val := le_of_not_gt hi
      simp [rectTopBlock_bottom, hle]
  have hnorm :=
    vecNorm2_le_of_abs_le
      (fun i : Fin m => rectTopBlock (m := m) ΔR i j)
      (fun i : Fin m => c * |rectTopBlock (m := m) R i j|) hentry
  calc
    vecNorm2 (fun i : Fin m => rectTopBlock (m := m) ΔR i j)
        ≤ vecNorm2 (fun i : Fin m => c * |rectTopBlock (m := m) R i j|) :=
          hnorm
    _ = c * vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j) := by
          rw [vecNorm2_smul, abs_of_nonneg hc, vecNorm2_abs]
/-- Columnwise top-block perturbation budget specialized to the repository's
    floating-point `gamma` constant. -/
theorem rectTopBlock_col_vecNorm2_perturb_bound_of_gamma {m n : ℕ}
    (fp : FPModel) (R ΔR : Fin n → Fin n → ℝ)
    (hγ : gammaValid fp n)
    (hΔ : ∀ i j : Fin n, |ΔR i j| ≤ gamma fp n * |R i j|) (j : Fin n) :
    vecNorm2 (fun i : Fin m => rectTopBlock (m := m) ΔR i j) ≤
      gamma fp n * vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j) :=
  rectTopBlock_col_vecNorm2_perturb_bound
    (m := m) (n := n) R ΔR (gamma_nonneg fp hγ) hΔ j
/-- Pull a rounded top-block solve back through a common orthogonal QR factor.

    Suppose transformed rectangular QR data are represented with one common
    orthogonal factor,
    `A_hat = Qᵀ(A + ΔA)` and `b_hat = Qᵀ(b + Δb)`, and `A_hat` has top
    block `R` with zero lower block.  The local floating-point back-substitution
    theorem perturbs the top block to `R + ΔR`; this result expresses that
    perturbation as an original-data perturbation
    `ΔA + Q [ΔR;0]` and proves the corresponding original rectangular normal
    equations. -/
theorem RectLSNormalEquations.exists_original_perturbation_of_commonQ_topBlock_fl_backSub
    {m n : ℕ} (fp : FPModel)
    (A ΔA : Fin m → Fin n → ℝ) (b Δb : Fin m → ℝ)
    (A_hat : Fin m → Fin n → ℝ) (b_hat : Fin m → ℝ)
    (Q : Fin m → Fin m → ℝ)
    (R : Fin n → Fin n → ℝ) (c : Fin n → ℝ)
    (hQ : IsOrthogonal m Q)
    (hAhat : ∀ i j, A_hat i j =
      matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j)
    (hbhat : ∀ i, b_hat i =
      matMulVec m (matTranspose Q) (fun a => b a + Δb a) i)
    (hA_top : ∀ (i : Fin m) (j : Fin n) (hi : i.val < n),
      A_hat i j = R ⟨i.val, hi⟩ j)
    (hA_bottom : ∀ (i : Fin m) (j : Fin n), n ≤ i.val → A_hat i j = 0)
    (hb_top : ∀ (i : Fin m) (hi : i.val < n),
      b_hat i = c ⟨i.val, hi⟩)
    (hdiag : ∀ i : Fin n, R i i ≠ 0)
    (hupper : ∀ i j : Fin n, j.val < i.val → R i j = 0)
    (hγ : gammaValid fp n) :
    ∃ (ΔR : Fin n → Fin n → ℝ) (ΔA_total : Fin m → Fin n → ℝ),
      (∀ i j : Fin n, |ΔR i j| ≤ gamma fp n * |R i j|) ∧
      RectLSNormalEquations
        (fun i j => A i j + ΔA_total i j) (fun i => b i + Δb i)
        (fl_backSub fp n R c) ∧
      frobNormRect ΔA_total ≤
        frobNormRect ΔA + frobNormRect (rectTopBlock (m := m) ΔR) := by
  rcases RectLSNormalEquations.exists_topBlock_of_fl_backSub
      (m := m) (n := n) fp R c b_hat hdiag hupper hγ hb_top with
    ⟨ΔR, hΔR, hNEtop⟩
  let topΔR : Fin m → Fin n → ℝ := rectTopBlock (m := m) ΔR
  let ΔA_total : Fin m → Fin n → ℝ :=
    fun i j => ΔA i j + matMulRectLeft Q topΔR i j
  have hQTQ : matMul m (matTranspose Q) Q = idMatrix m :=
    funext fun i => funext fun j => hQ.left_inv i j
  have hpull :
      matMulRectLeft (matTranspose Q) (matMulRectLeft Q topΔR) = topΔR := by
    rw [← matMulRectLeft_assoc, hQTQ, matMulRectLeft_id]
  have hAeq :
      rectTopBlock (m := m) (fun i j => R i j + ΔR i j) =
        matMulRectLeft (matTranspose Q)
          (fun i j => A i j + ΔA_total i j) := by
    ext i j
    have hsplit :
        (fun a b => A a b + ΔA_total a b) =
          fun a b => (A a b + ΔA a b) + matMulRectLeft Q topΔR a b := by
      ext a b
      simp [ΔA_total]
      ring
    have hcalc :
        matMulRectLeft (matTranspose Q)
            (fun a b => A a b + ΔA_total a b) i j =
          matMulRectLeft (matTranspose Q)
              (fun a b => A a b + ΔA a b) i j + topΔR i j := by
      calc
        matMulRectLeft (matTranspose Q)
            (fun a b => A a b + ΔA_total a b) i j
            = matMulRectLeft (matTranspose Q)
                (fun a b => (A a b + ΔA a b) + matMulRectLeft Q topΔR a b) i j := by
                rw [hsplit]
        _ = matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j +
              matMulRectLeft (matTranspose Q) (matMulRectLeft Q topΔR) i j := by
                exact congr_fun
                  (congr_fun
                    (matMulRectLeft_add_right (matTranspose Q)
                      (fun a b => A a b + ΔA a b)
                      (matMulRectLeft Q topΔR)) i) j
        _ = matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j +
              topΔR i j := by
                rw [congr_fun (congr_fun hpull i) j]
    have hshape :
        A_hat i j + topΔR i j =
          rectTopBlock (fun i j => R i j + ΔR i j) i j := by
      by_cases hi : i.val < n
      · simp [topΔR, rectTopBlock_top, hi, hA_top i j hi]
      · have hle : n ≤ i.val := le_of_not_gt hi
        simp [topΔR, rectTopBlock_bottom, hle, hA_bottom i j hle]
    calc
      rectTopBlock (m := m) (fun i j => R i j + ΔR i j) i j
          = A_hat i j + topΔR i j := hshape.symm
      _ = matMulRectLeft (matTranspose Q)
              (fun a b => A a b + ΔA a b) i j + topΔR i j := by
            rw [hAhat i j]
      _ = matMulRectLeft (matTranspose Q)
              (fun a b => A a b + ΔA_total a b) i j := hcalc.symm
  have hbEq : b_hat = matMulVec m (matTranspose Q) (fun a => b a + Δb a) :=
    funext hbhat
  have hNEorig :
      RectLSNormalEquations
        (fun i j => A i j + ΔA_total i j) (fun i => b i + Δb i)
        (fl_backSub fp n R c) :=
    RectLSNormalEquations.of_orthogonal_left
      (matTranspose Q) (fun i j => A i j + ΔA_total i j)
      (rectTopBlock (m := m) (fun i j => R i j + ΔR i j))
      (fun i => b i + Δb i) b_hat (fl_backSub fp n R c)
      hQ.transpose hAeq hbEq hNEtop
  have hbound :
      frobNormRect ΔA_total ≤
        frobNormRect ΔA + frobNormRect (rectTopBlock (m := m) ΔR) := by
    calc
      frobNormRect ΔA_total
          = frobNormRect (fun i j => ΔA i j + matMulRectLeft Q topΔR i j) := rfl
      _ ≤ frobNormRect ΔA + frobNormRect (matMulRectLeft Q topΔR) :=
            frobNormRect_add_le ΔA (matMulRectLeft Q topΔR)
      _ = frobNormRect ΔA + frobNormRect topΔR := by
            rw [frobNormRect_orthogonal_left Q topΔR hQ]
      _ = frobNormRect ΔA + frobNormRect (rectTopBlock ΔR) := rfl
  exact ⟨ΔR, ΔA_total, hΔR, hNEorig, hbound⟩
/-- The pulled-back rounded top-block theorem with the embedded triangular
    perturbation norm discharged by the componentwise `gamma` budget. -/
theorem RectLSNormalEquations.exists_original_perturbation_of_commonQ_topBlock_fl_backSub_gamma_bound
    {m n : ℕ} (fp : FPModel)
    (A ΔA : Fin m → Fin n → ℝ) (b Δb : Fin m → ℝ)
    (A_hat : Fin m → Fin n → ℝ) (b_hat : Fin m → ℝ)
    (Q : Fin m → Fin m → ℝ)
    (R : Fin n → Fin n → ℝ) (c : Fin n → ℝ)
    (hQ : IsOrthogonal m Q)
    (hAhat : ∀ i j, A_hat i j =
      matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j)
    (hbhat : ∀ i, b_hat i =
      matMulVec m (matTranspose Q) (fun a => b a + Δb a) i)
    (hA_top : ∀ (i : Fin m) (j : Fin n) (hi : i.val < n),
      A_hat i j = R ⟨i.val, hi⟩ j)
    (hA_bottom : ∀ (i : Fin m) (j : Fin n), n ≤ i.val → A_hat i j = 0)
    (hb_top : ∀ (i : Fin m) (hi : i.val < n),
      b_hat i = c ⟨i.val, hi⟩)
    (hdiag : ∀ i : Fin n, R i i ≠ 0)
    (hupper : ∀ i j : Fin n, j.val < i.val → R i j = 0)
    (hγ : gammaValid fp n) :
    ∃ (ΔR : Fin n → Fin n → ℝ) (ΔA_total : Fin m → Fin n → ℝ),
      (∀ i j : Fin n, |ΔR i j| ≤ gamma fp n * |R i j|) ∧
      RectLSNormalEquations
        (fun i j => A i j + ΔA_total i j) (fun i => b i + Δb i)
        (fl_backSub fp n R c) ∧
      frobNormRect ΔA_total ≤
        frobNormRect ΔA +
          gamma fp n * frobNormRect (rectTopBlock (m := m) R) := by
  rcases
    RectLSNormalEquations.exists_original_perturbation_of_commonQ_topBlock_fl_backSub
      (m := m) (n := n) fp A ΔA b Δb A_hat b_hat Q R c
      hQ hAhat hbhat hA_top hA_bottom hb_top hdiag hupper hγ with
    ⟨ΔR, ΔA_total, hΔR, hNE, hbound⟩
  have htop :
      frobNormRect (rectTopBlock (m := m) ΔR) ≤
        gamma fp n * frobNormRect (rectTopBlock (m := m) R) :=
    rectTopBlock_frobNorm_perturb_bound_of_gamma
      (m := m) (n := n) fp R ΔR hγ hΔR
  refine ⟨ΔR, ΔA_total, hΔR, hNE, ?_⟩
  exact hbound.trans (add_le_add_right htop (frobNormRect ΔA))
/-- Induced right-hand-side perturbation from a rectangular data perturbation. -/
noncomputable def rectLSRhsPerturbation {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (ΔA : Fin m → Fin n → ℝ) (Δb : Fin m → ℝ) : Fin n → ℝ :=
  fun j =>
    rectLSRhs (fun i l => A i l + ΔA i l) (fun i => b i + Δb i) j -
      rectLSRhs A b j
/-- Entrywise budget for the right-hand-side perturbation induced by
    rectangular data perturbations. -/
noncomputable def rectLSRhsPerturbationEntryBudget {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (ΔA : Fin m → Fin n → ℝ) (Δb : Fin m → ℝ) : Fin n → ℝ :=
  fun j => ∑ i : Fin m,
    (|A i j| * |Δb i| + |ΔA i j| * |b i| + |ΔA i j| * |Δb i|)
/-- Coarse right-hand-side perturbation budget from
    `‖ΔA‖_F ≤ cA` and `‖Δb‖₂ ≤ cb`. -/
noncomputable def rectLSRhsPerturbationNormBudget {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (cA cb : ℝ) : Fin n → ℝ :=
  fun j => ∑ i : Fin m, (|A i j| * cb + cA * |b i| + cA * cb)
theorem rectLSRhsPerturbationEntryBudget_nonneg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (ΔA : Fin m → Fin n → ℝ) (Δb : Fin m → ℝ) :
    ∀ j : Fin n, 0 ≤ rectLSRhsPerturbationEntryBudget A b ΔA Δb j := by
  intro j
  unfold rectLSRhsPerturbationEntryBudget
  apply Finset.sum_nonneg
  intro i _
  exact add_nonneg
    (add_nonneg
      (mul_nonneg (abs_nonneg _) (abs_nonneg _))
      (mul_nonneg (abs_nonneg _) (abs_nonneg _)))
    (mul_nonneg (abs_nonneg _) (abs_nonneg _))
theorem rectLSRhsPerturbationNormBudget_nonneg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) {cA cb : ℝ}
    (hcA : 0 ≤ cA) (hcb : 0 ≤ cb) :
    ∀ j : Fin n, 0 ≤ rectLSRhsPerturbationNormBudget A b cA cb j := by
  intro j
  unfold rectLSRhsPerturbationNormBudget
  apply Finset.sum_nonneg
  intro i _
  exact add_nonneg
    (add_nonneg
      (mul_nonneg (abs_nonneg _) hcb)
      (mul_nonneg hcA (abs_nonneg _)))
    (mul_nonneg hcA hcb)
/-- A nonnegative RHS norm budget is bounded by the sum of all RHS norm
    budgets.  This packages the scalar `c_g` used by `LSQRSolveBackwardError`
    without requiring a separate user-supplied domination hypothesis. -/
theorem rectLSRhsPerturbationNormBudget_le_sum {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) {cA cb : ℝ}
    (hcA : 0 ≤ cA) (hcb : 0 ≤ cb) (j : Fin n) :
    rectLSRhsPerturbationNormBudget A b cA cb j ≤
      ∑ k : Fin n, rectLSRhsPerturbationNormBudget A b cA cb k := by
  exact Finset.single_le_sum
    (fun k _ => rectLSRhsPerturbationNormBudget_nonneg A b hcA hcb k)
    (Finset.mem_univ j)
/-- Final rectangular data-perturbation radius used by the QR least-squares
    handoff: accumulated common-`Q` data error plus rounded triangular
    top-block error. -/
noncomputable def qrSolveFinalDataPerturbationBudget {m n : ℕ}
    (fp : FPModel) (A : Fin m → Fin n → ℝ) (R : Fin n → Fin n → ℝ)
    (cStep : ℝ) : ℝ :=
  ((1 + cStep) ^ n - 1) * frobNormRect A +
    gamma fp n * frobNormRect (rectTopBlock (m := m) R)
/-- Final RHS perturbation radius used by the QR least-squares handoff. -/
noncomputable def qrSolveFinalRhsPerturbationBudget {m n : ℕ}
    (b : Fin m → ℝ) (cStep : ℝ) : ℝ :=
  ((1 + cStep) ^ n - 1) * vecNorm2 b
theorem qrSolveFinalDataPerturbationBudget_nonneg {m n : ℕ}
    (fp : FPModel) (A : Fin m → Fin n → ℝ) (R : Fin n → Fin n → ℝ)
    {cStep : ℝ} (hcStep : 0 ≤ cStep) (hγ : gammaValid fp n) :
    0 ≤ qrSolveFinalDataPerturbationBudget fp A R cStep := by
  unfold qrSolveFinalDataPerturbationBudget
  exact add_nonneg
    (mul_nonneg (qrSolveGeometricFactor_nonneg (n := n) hcStep)
      (frobNormRect_nonneg A))
    (mul_nonneg (gamma_nonneg fp hγ)
      (frobNormRect_nonneg (rectTopBlock (m := m) R)))
theorem qrSolveFinalRhsPerturbationBudget_nonneg {m n : ℕ}
    (b : Fin m → ℝ) {cStep : ℝ} (hcStep : 0 ≤ cStep) :
    0 ≤ qrSolveFinalRhsPerturbationBudget (n := n) b cStep := by
  unfold qrSolveFinalRhsPerturbationBudget
  exact mul_nonneg (qrSolveGeometricFactor_nonneg (n := n) hcStep)
    (vecNorm2_nonneg b)
/-- Explicit final Gram radius induced by the QR least-squares data budget. -/
noncomputable def qrSolveFinalGramBudget {m n : ℕ}
    (fp : FPModel) (A : Fin m → Fin n → ℝ) (R : Fin n → Fin n → ℝ)
    (cStep : ℝ) : ℝ :=
  frobNorm
    (rectLSGramPerturbationNormBudget A
      (qrSolveFinalDataPerturbationBudget fp A R cStep))
/-- Explicit final RHS radius induced by the QR least-squares data budget. -/
noncomputable def qrSolveFinalRhsBudget {m n : ℕ}
    (fp : FPModel) (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (R : Fin n → Fin n → ℝ) (cStep : ℝ) : ℝ :=
  ∑ j : Fin n,
    rectLSRhsPerturbationNormBudget A b
      (qrSolveFinalDataPerturbationBudget fp A R cStep)
      (qrSolveFinalRhsPerturbationBudget (n := n) b cStep) j
/-- Expansion of the induced rectangular right-hand-side perturbation. -/
theorem rectLSRhsPerturbation_eq_sum {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (ΔA : Fin m → Fin n → ℝ) (Δb : Fin m → ℝ) (j : Fin n) :
    rectLSRhsPerturbation A b ΔA Δb j =
      ∑ i : Fin m, (A i j * Δb i + ΔA i j * b i + ΔA i j * Δb i) := by
  unfold rectLSRhsPerturbation rectLSRhs
  rw [← Finset.sum_sub_distrib]
  apply Finset.sum_congr rfl
  intro i _
  ring
/-- The induced right-hand-side perturbation is bounded entrywise by its exact
    absolute-value expansion budget. -/
theorem rectLSRhsPerturbation_abs_le_entryBudget {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (ΔA : Fin m → Fin n → ℝ) (Δb : Fin m → ℝ) :
    ∀ j : Fin n,
      |rectLSRhsPerturbation A b ΔA Δb j| ≤
        rectLSRhsPerturbationEntryBudget A b ΔA Δb j := by
  intro j
  rw [rectLSRhsPerturbation_eq_sum]
  unfold rectLSRhsPerturbationEntryBudget
  calc
    |∑ i : Fin m, (A i j * Δb i + ΔA i j * b i + ΔA i j * Δb i)|
        ≤ ∑ i : Fin m, |A i j * Δb i + ΔA i j * b i + ΔA i j * Δb i| := by
          exact Finset.abs_sum_le_sum_abs _ _
    _ ≤ ∑ i : Fin m,
          (|A i j| * |Δb i| + |ΔA i j| * |b i| + |ΔA i j| * |Δb i|) := by
        apply Finset.sum_le_sum
        intro i _
        calc
          |A i j * Δb i + ΔA i j * b i + ΔA i j * Δb i|
              ≤ |A i j * Δb i| + |ΔA i j * b i| + |ΔA i j * Δb i| := by
                exact abs_add_three _ _ _
          _ = |A i j| * |Δb i| + |ΔA i j| * |b i| + |ΔA i j| * |Δb i| := by
                rw [abs_mul, abs_mul, abs_mul]
/-- The induced right-hand-side perturbation is bounded entrywise by the
    coarse data-perturbation budget. -/
theorem rectLSRhsPerturbation_abs_le_normBudget {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (ΔA : Fin m → Fin n → ℝ) (Δb : Fin m → ℝ) {cA cb : ℝ}
    (hΔA : frobNorm ΔA ≤ cA) (hΔb : vecNorm2 Δb ≤ cb) :
    ∀ j : Fin n,
      |rectLSRhsPerturbation A b ΔA Δb j| ≤
        rectLSRhsPerturbationNormBudget A b cA cb j := by
  intro j
  have hentry := rectLSRhsPerturbation_abs_le_entryBudget A b ΔA Δb j
  calc
    |rectLSRhsPerturbation A b ΔA Δb j|
        ≤ rectLSRhsPerturbationEntryBudget A b ΔA Δb j := hentry
    _ ≤ rectLSRhsPerturbationNormBudget A b cA cb j := by
        unfold rectLSRhsPerturbationEntryBudget rectLSRhsPerturbationNormBudget
        apply Finset.sum_le_sum
        intro i _
        have hΔAij : |ΔA i j| ≤ cA :=
          (abs_entry_le_frobNorm ΔA i j).trans hΔA
        have hΔbi : |Δb i| ≤ cb :=
          (abs_coord_le_vecNorm2 Δb i).trans hΔb
        exact add_le_add
          (add_le_add
            (mul_le_mul_of_nonneg_left hΔbi (abs_nonneg _))
            (mul_le_mul_of_nonneg_right hΔAij (abs_nonneg _)))
          (mul_le_mul hΔAij hΔbi (abs_nonneg _) (le_trans (abs_nonneg _) hΔAij))
/-- A perturbed rectangular normal equation induces the corresponding
    perturbed Gram system for the unperturbed data.  This is the deterministic
    adapter needed to connect a future rectangular QR backward-error theorem to
    the repository's existing `LSQRSolveBackwardError`-based RandNLA solver
    transfer. -/
theorem rectLSNormalEquations_perturbed_to_gram_system {m n : ℕ}
    (A ΔA : Fin m → Fin n → ℝ) (b Δb : Fin m → ℝ)
    (x_hat : Fin n → ℝ)
    (hNE : RectLSNormalEquations
      (fun i j => A i j + ΔA i j) (fun i => b i + Δb i) x_hat) :
    ∀ j : Fin n,
      matMulVec n
          (fun a c => rectLSGram A a c + rectLSGramPerturbation A ΔA a c)
          x_hat j =
        rectLSRhs A b j + rectLSRhsPerturbation A b ΔA Δb j := by
  intro j
  have hj := hNE j
  simpa [RectLSNormalEquations, rectLSGramPerturbation,
    rectLSRhsPerturbation, matMulVec, sub_eq_add_neg, add_assoc,
    add_comm, add_left_comm] using hj
/-- Source-facing rectangular exact-minimizer conclusion for the common-`Q`
    transformed QR route.

    This is the direct Theorem 20.3-style conclusion behind
    `LSQRSolveBackwardError.of_commonQ_topBlock_fl_backSub_gamma_bound_normBudget`:
    the rounded top-block back substitution is an exact least-squares
    minimizer for explicitly bounded rectangular perturbations of the original
    data.  The remaining hypotheses are exactly the common transformed data,
    final `[R;0]` shape, and triangular-solve domain assumptions. -/
theorem exists_perturbed_ls_minimizer_of_commonQ_topBlock_fl_backSub_gamma_bound
    {m n : ℕ} (fp : FPModel)
    (A ΔA : Fin m → Fin n → ℝ) (b Δb : Fin m → ℝ)
    (A_hat : Fin m → Fin n → ℝ) (b_hat : Fin m → ℝ)
    (Q : Fin m → Fin m → ℝ)
    (R : Fin n → Fin n → ℝ) (cTop : Fin n → ℝ)
    (cA0 cb : ℝ)
    (hQ : IsOrthogonal m Q)
    (hAhat : ∀ i j, A_hat i j =
      matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j)
    (hbhat : ∀ i, b_hat i =
      matMulVec m (matTranspose Q) (fun a => b a + Δb a) i)
    (hA_top : ∀ (i : Fin m) (j : Fin n) (hi : i.val < n),
      A_hat i j = R ⟨i.val, hi⟩ j)
    (hA_bottom : ∀ (i : Fin m) (j : Fin n), n ≤ i.val → A_hat i j = 0)
    (hb_top : ∀ (i : Fin m) (hi : i.val < n),
      b_hat i = cTop ⟨i.val, hi⟩)
    (hdiag : ∀ i : Fin n, R i i ≠ 0)
    (hupper : ∀ i j : Fin n, j.val < i.val → R i j = 0)
    (hγ : gammaValid fp n)
    (hΔA : frobNormRect ΔA ≤ cA0)
    (hΔb : vecNorm2 Δb ≤ cb) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        cA0 + gamma fp n * frobNormRect (rectTopBlock (m := m) R) ∧
      vecNorm2 Δb' ≤ cb ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n R cTop) := by
  rcases
    RectLSNormalEquations.exists_original_perturbation_of_commonQ_topBlock_fl_backSub_gamma_bound
      (m := m) (n := n) fp A ΔA b Δb A_hat b_hat Q R cTop
      hQ hAhat hbhat hA_top hA_bottom hb_top hdiag hupper hγ with
    ⟨_ΔR, ΔA_total, _hΔR, hNE, hΔA_total_rect⟩
  have hΔA_total :
      frobNorm ΔA_total ≤
        cA0 + gamma fp n * frobNormRect (rectTopBlock (m := m) R) := by
    rw [← frobNormRect_eq_frobNormFn]
    calc
      frobNormRect ΔA_total
          ≤ frobNormRect ΔA +
              gamma fp n * frobNormRect (rectTopBlock (m := m) R) :=
            hΔA_total_rect
      _ ≤ cA0 + gamma fp n * frobNormRect (rectTopBlock (m := m) R) := by
            exact add_le_add hΔA (le_refl _)
  exact ⟨ΔA_total, Δb, hΔA_total, hΔb, hNE.perturbed_isLeastSquaresMinimizer⟩
/-- Source-facing columnwise exact-minimizer conclusion for the common-`Q`
    transformed QR route.

    This preserves the columnwise perturbation data needed by Higham,
    Chapter 20, Theorem 20.3.  If the preliminary rectangular perturbation has
    per-column radii `cA0`, then pulling back the rounded triangular-solve
    perturbation gives per-column radii
    `cA0_j + gamma_n * (||a_j||_2 + cA0_j)`. -/
theorem exists_perturbed_ls_minimizer_of_commonQ_topBlock_fl_backSub_gamma_bound_columnwise
    {m n : ℕ} (fp : FPModel)
    (A ΔA : Fin m → Fin n → ℝ) (b Δb : Fin m → ℝ)
    (A_hat : Fin m → Fin n → ℝ) (b_hat : Fin m → ℝ)
    (Q : Fin m → Fin m → ℝ)
    (R : Fin n → Fin n → ℝ) (cTop : Fin n → ℝ)
    (cA0 : Fin n → ℝ) (cb : ℝ)
    (hQ : IsOrthogonal m Q)
    (hAhat : ∀ i j, A_hat i j =
      matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j)
    (hbhat : ∀ i, b_hat i =
      matMulVec m (matTranspose Q) (fun a => b a + Δb a) i)
    (hA_top : ∀ (i : Fin m) (j : Fin n) (hi : i.val < n),
      A_hat i j = R ⟨i.val, hi⟩ j)
    (hA_bottom : ∀ (i : Fin m) (j : Fin n), n ≤ i.val → A_hat i j = 0)
    (hb_top : ∀ (i : Fin m) (hi : i.val < n),
      b_hat i = cTop ⟨i.val, hi⟩)
    (hdiag : ∀ i : Fin n, R i i ≠ 0)
    (hupper : ∀ i j : Fin n, j.val < i.val → R i j = 0)
    (hγ : gammaValid fp n)
    (hΔA_cols : ∀ j : Fin n,
      vecNorm2 (fun i : Fin m => ΔA i j) ≤ cA0 j)
    (hΔb : vecNorm2 Δb ≤ cb) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          cA0 j + gamma fp n *
            (vecNorm2 (fun i : Fin m => A i j) + cA0 j)) ∧
      vecNorm2 Δb' ≤ cb ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n R cTop) := by
  rcases RectLSNormalEquations.exists_topBlock_of_fl_backSub
      (m := m) (n := n) fp R cTop b_hat hdiag hupper hγ hb_top with
    ⟨ΔR, hΔR, hNEtop⟩
  let topΔR : Fin m → Fin n → ℝ := rectTopBlock (m := m) ΔR
  let ΔA_total : Fin m → Fin n → ℝ :=
    fun i j => ΔA i j + matMulRectLeft Q topΔR i j
  have hQTQ : matMul m (matTranspose Q) Q = idMatrix m :=
    funext fun i => funext fun j => hQ.left_inv i j
  have hpull :
      matMulRectLeft (matTranspose Q) (matMulRectLeft Q topΔR) = topΔR := by
    rw [← matMulRectLeft_assoc, hQTQ, matMulRectLeft_id]
  have hAeq :
      rectTopBlock (m := m) (fun i j => R i j + ΔR i j) =
        matMulRectLeft (matTranspose Q)
          (fun i j => A i j + ΔA_total i j) := by
    ext i j
    have hsplit :
        (fun a b => A a b + ΔA_total a b) =
          fun a b => (A a b + ΔA a b) + matMulRectLeft Q topΔR a b := by
      ext a b
      simp [ΔA_total]
      ring
    have hcalc :
        matMulRectLeft (matTranspose Q)
            (fun a b => A a b + ΔA_total a b) i j =
          matMulRectLeft (matTranspose Q)
              (fun a b => A a b + ΔA a b) i j + topΔR i j := by
      calc
        matMulRectLeft (matTranspose Q)
            (fun a b => A a b + ΔA_total a b) i j
            = matMulRectLeft (matTranspose Q)
                (fun a b => (A a b + ΔA a b) + matMulRectLeft Q topΔR a b) i j := by
                rw [hsplit]
        _ = matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j +
              matMulRectLeft (matTranspose Q) (matMulRectLeft Q topΔR) i j := by
                exact congr_fun
                  (congr_fun
                    (matMulRectLeft_add_right (matTranspose Q)
                      (fun a b => A a b + ΔA a b)
                      (matMulRectLeft Q topΔR)) i) j
        _ = matMulRectLeft (matTranspose Q) (fun a b => A a b + ΔA a b) i j +
              topΔR i j := by
                rw [congr_fun (congr_fun hpull i) j]
    have hshape :
        A_hat i j + topΔR i j =
          rectTopBlock (fun i j => R i j + ΔR i j) i j := by
      by_cases hi : i.val < n
      · simp [topΔR, rectTopBlock_top, hi, hA_top i j hi]
      · have hle : n ≤ i.val := le_of_not_gt hi
        simp [topΔR, rectTopBlock_bottom, hle, hA_bottom i j hle]
    calc
      rectTopBlock (m := m) (fun i j => R i j + ΔR i j) i j
          = A_hat i j + topΔR i j := hshape.symm
      _ = matMulRectLeft (matTranspose Q)
              (fun a b => A a b + ΔA a b) i j + topΔR i j := by
            rw [hAhat i j]
      _ = matMulRectLeft (matTranspose Q)
              (fun a b => A a b + ΔA_total a b) i j := hcalc.symm
  have hbEq : b_hat = matMulVec m (matTranspose Q) (fun a => b a + Δb a) :=
    funext hbhat
  have hNEorig :
      RectLSNormalEquations
        (fun i j => A i j + ΔA_total i j) (fun i => b i + Δb i)
        (fl_backSub fp n R cTop) :=
    RectLSNormalEquations.of_orthogonal_left
      (matTranspose Q) (fun i j => A i j + ΔA_total i j)
      (rectTopBlock (m := m) (fun i j => R i j + ΔR i j))
      (fun i => b i + Δb i) b_hat (fl_backSub fp n R cTop)
      hQ.transpose hAeq hbEq hNEtop
  have hAhat_eq_topR : ∀ i j,
      A_hat i j = rectTopBlock (m := m) R i j := by
    intro i j
    by_cases hi : i.val < n
    · simp [rectTopBlock_top, hi, hA_top i j hi]
    · have hle : n ≤ i.val := le_of_not_gt hi
      simp [rectTopBlock_bottom, hle, hA_bottom i j hle]
  have hγ_nonneg : 0 ≤ gamma fp n := gamma_nonneg fp hγ
  have hcol : ∀ j : Fin n,
      vecNorm2 (fun i : Fin m => ΔA_total i j) ≤
        cA0 j + gamma fp n *
          (vecNorm2 (fun i : Fin m => A i j) + cA0 j) := by
    intro j
    have htopΔ :
        vecNorm2 (fun i : Fin m => topΔR i j) ≤
          gamma fp n *
            vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j) := by
      simpa [topΔR] using
        rectTopBlock_col_vecNorm2_perturb_bound_of_gamma
          (m := m) (n := n) fp R ΔR hγ hΔR j
    have hQtop :
        vecNorm2 (fun i : Fin m => matMulRectLeft Q topΔR i j) =
          vecNorm2 (fun i : Fin m => topΔR i j) := by
      simpa [matMulRectLeft, matMulVec] using
        vecNorm2_orthogonal Q (fun i : Fin m => topΔR i j) hQ
    have hRcol :
        vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j) ≤
          vecNorm2 (fun i : Fin m => A i j) + cA0 j := by
      have htop_eq :
          (fun i : Fin m => rectTopBlock (m := m) R i j) =
            matMulVec m (matTranspose Q)
              (fun i : Fin m => A i j + ΔA i j) := by
        ext i
        rw [← hAhat_eq_topR i j, hAhat i j]
        rfl
      calc
        vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j)
            = vecNorm2
                (matMulVec m (matTranspose Q)
                  (fun i : Fin m => A i j + ΔA i j)) := by rw [htop_eq]
        _ = vecNorm2 (fun i : Fin m => A i j + ΔA i j) :=
              vecNorm2_orthogonal (matTranspose Q)
                (fun i : Fin m => A i j + ΔA i j) hQ.transpose
        _ ≤ vecNorm2 (fun i : Fin m => A i j) +
              vecNorm2 (fun i : Fin m => ΔA i j) :=
              vecNorm2_add_le _ _
        _ ≤ vecNorm2 (fun i : Fin m => A i j) + cA0 j :=
              add_le_add_right (hΔA_cols j) (vecNorm2 (fun i : Fin m => A i j))
    calc
      vecNorm2 (fun i : Fin m => ΔA_total i j)
          = vecNorm2
              (fun i : Fin m => ΔA i j + matMulRectLeft Q topΔR i j) := rfl
      _ ≤ vecNorm2 (fun i : Fin m => ΔA i j) +
            vecNorm2 (fun i : Fin m => matMulRectLeft Q topΔR i j) :=
            vecNorm2_add_le _ _
      _ = vecNorm2 (fun i : Fin m => ΔA i j) +
            vecNorm2 (fun i : Fin m => topΔR i j) := by rw [hQtop]
      _ ≤ cA0 j +
            gamma fp n * vecNorm2 (fun i : Fin m => rectTopBlock (m := m) R i j) :=
            add_le_add (hΔA_cols j) htopΔ
      _ ≤ cA0 j + gamma fp n *
            (vecNorm2 (fun i : Fin m => A i j) + cA0 j) :=
            add_le_add_right
              (mul_le_mul_of_nonneg_left hRcol hγ_nonneg) (cA0 j)
  exact ⟨ΔA_total, Δb, hcol, hΔb, hNEorig.perturbed_isLeastSquaresMinimizer⟩
/-- Source-facing rectangular exact-minimizer conclusion for a supplied
    orthogonal-transformation QR sequence followed by rounded top-block back
    substitution. -/
theorem exists_perturbed_ls_minimizer_of_rect_orthogonal_sequence_topBlock_fl_backSub_gamma_bound
    {m n r : ℕ} (fp : FPModel)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (P ΔP : ℕ → Fin m → Fin m → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (R : Fin n → Fin n → ℝ) (cTop : Fin n → ℝ)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hP : ∀ k, k < r → IsOrthogonal m (P k))
    (hΔP : ∀ k, k < r → frobNorm (ΔP k) ≤ cStep)
    (hNextA : ∀ k, k < r →
      A_hat (k + 1) = matMulRectLeft (fun a b => P k a b + ΔP k a b) (A_hat k))
    (hNextb : ∀ k, k < r →
      b_hat (k + 1) = matMulVec m (fun a b => P k a b + ΔP k a b) (b_hat k))
    (hA_top : ∀ (i : Fin m) (j : Fin n) (hi : i.val < n),
      A_hat r i j = R ⟨i.val, hi⟩ j)
    (hA_bottom : ∀ (i : Fin m) (j : Fin n), n ≤ i.val → A_hat r i j = 0)
    (hb_top : ∀ (i : Fin m) (hi : i.val < n),
      b_hat r i = cTop ⟨i.val, hi⟩)
    (hdiag : ∀ i : Fin n, R i i ≠ 0)
    (hupper : ∀ i j : Fin n, j.val < i.val → R i j = 0)
    (hγ : gammaValid fp n) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ r - 1) * frobNormRect A +
          gamma fp n * frobNormRect (rectTopBlock (m := m) R) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ r - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n R cTop) := by
  rcases
    rect_orthogonal_matrix_vector_sequence_geometric
      m n r A b A_hat b_hat P ΔP cStep hcStep hInitA hInitb
      hP hΔP hNextA hNextb with
    ⟨Q, ΔA, Δb, hQ, hArep, hbrep, hΔA, hΔb⟩
  exact
    exists_perturbed_ls_minimizer_of_commonQ_topBlock_fl_backSub_gamma_bound
      (m := m) (n := n) fp A ΔA b Δb (A_hat r) (b_hat r) Q R cTop
      (((1 + cStep) ^ r - 1) * frobNormRect A)
      (((1 + cStep) ^ r - 1) * vecNorm2 b)
      hQ hArep hbrep hA_top hA_bottom hb_top hdiag hupper hγ hΔA hΔb
/-- Source-facing rectangular exact-minimizer conclusion for the compact
    rounded Householder sequence route.

    This exposes the Chapter 20 Theorem 20.3 shape directly: under the compact
    Householder step recurrences, the final `[R;0]` shape, and the visible
    triangular nonbreakdown assumptions, the computed back-substitution vector
    is an exact LS minimizer for bounded rectangular perturbations of `A` and
    `b`. -/
theorem exists_perturbed_ls_minimizer_of_compact_householder_sequence_topBlock_fl_backSub_gamma_bound
    {m n r : ℕ} (fp : FPModel)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (v : ℕ → Fin m → ℝ) (β : ℕ → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (R : Fin n → Fin n → ℝ) (cTop : Fin n → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k, k < r →
      A_hat (k + 1) =
        fl_householderApplyCompactPanel fp m n (v k) (β k) (A_hat k))
    (hStepb : ∀ k, k < r →
      b_hat (k + 1) =
        fl_householderApplyCompact fp m (v k) (β k) (b_hat k))
    (horth : ∀ k, k < r →
      IsOrthogonal m (householder m (v k) (β k)))
    (hA_budget : ∀ k, k < r → ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        householderCompactComponentBudget fp m (v k) (β k)
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k, k < r →
      vecNorm2 (fun i : Fin m =>
        householderCompactComponentBudget fp m (v k) (β k) (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hA_top : ∀ (i : Fin m) (j : Fin n) (hi : i.val < n),
      A_hat r i j = R ⟨i.val, hi⟩ j)
    (hA_bottom : ∀ (i : Fin m) (j : Fin n), n ≤ i.val → A_hat r i j = 0)
    (hb_top : ∀ (i : Fin m) (hi : i.val < n),
      b_hat r i = cTop ⟨i.val, hi⟩)
    (hdiag : ∀ i : Fin n, R i i ≠ 0)
    (hupper : ∀ i j : Fin n, j.val < i.val → R i j = 0) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ r - 1) * frobNormRect A +
          gamma fp n * frobNormRect (rectTopBlock (m := m) R) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ r - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n R cTop) := by
  let η : ℝ := (1 + cStep) ^ r - 1
  have hη_nonneg : 0 ≤ η := by
    unfold η
    have hbase : (1 : ℝ) ≤ 1 + cStep := by linarith
    exact sub_nonneg.mpr (one_le_pow₀ hbase)
  rcases
    fl_householderApplyCompactPanel_rect_orthogonal_columnwise_vector_sequence_geometric
      fp m n r v β A b A_hat b_hat cStep hcStep hm
      hInitA hInitb hStepA hStepb horth hA_budget hb_budget with
    ⟨Q, ΔA, Δb, hQ, hArep, hbrep, hΔA_cols, hΔb⟩
  have hΔA : frobNormRect ΔA ≤ η * frobNormRect A := by
    apply frobNormRect_le_of_col_vecNorm2_le
    · exact hη_nonneg
    · intro j
      simpa [η] using hΔA_cols j
  exact
    exists_perturbed_ls_minimizer_of_commonQ_topBlock_fl_backSub_gamma_bound
      (m := m) (n := n) fp A ΔA b Δb (A_hat r) (b_hat r) Q R cTop
      (η * frobNormRect A) (η * vecNorm2 b) hQ hArep hbrep
      hA_top hA_bottom hb_top hdiag hupper hγ hΔA (by simpa [η] using hΔb)
/-- Source-facing rectangular exact-minimizer conclusion for the stored
    trailing Householder QR loop.

    This is the direct Theorem 20.3-style counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_normBudget`:
    it reads `R` and the transformed right-hand side from the final stored
    outputs and concludes that the computed back-substitution vector is an
    exact least-squares minimizer for bounded rectangular perturbations of the
    original data. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let η : ℝ := (1 + cStep) ^ n - 1
  let R : Fin n → Fin n → ℝ :=
    fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
  let cTop : Fin n → ℝ :=
    fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩
  rcases
    fl_householderStoredTrailingPanel_higham_columnwise_factorization
      fp hmn A b A_hat b_hat alpha cStep hcStep hm
      hInitA hInitb hStepA hStepb halpha hden hA_budget hb_budget with
    ⟨Q, ΔA, Δb, hQ, hArep, hbrep, hΔA_cols, hΔb,
      hA_top, hA_bottom, hb_top, hupper⟩
  have hη_nonneg : 0 ≤ η := by
    unfold η
    have hbase : (1 : ℝ) ≤ 1 + cStep := by linarith
    exact sub_nonneg.mpr (one_le_pow₀ hbase)
  have hΔA : frobNormRect ΔA ≤ η * frobNormRect A := by
    apply frobNormRect_le_of_col_vecNorm2_le
    · exact hη_nonneg
    · intro j
      simpa [η] using hΔA_cols j
  have hmain :=
    exists_perturbed_ls_minimizer_of_commonQ_topBlock_fl_backSub_gamma_bound
      (m := m) (n := n) fp A ΔA b Δb (A_hat n) (b_hat n) Q R cTop
      (η * frobNormRect A) (η * vecNorm2 b)
      hQ hArep hbrep hA_top hA_bottom hb_top
      (by intro i; simpa [R] using hdiag i)
      (by simpa [R] using hupper)
      hγ hΔA (by simpa [η] using hΔb)
  simpa [η, R, cTop] using hmain
/-- Source-facing columnwise exact-minimizer conclusion for the stored trailing
    Householder QR loop.

    This keeps the columnwise perturbation radii from the stored Householder QR
    factorization all the way through the rounded triangular solve.  The final
    radius `theta = (1 + cStep)^n * (1 + gamma fp n) - 1` is a compact
    source-shaped envelope for the accumulated Householder perturbation and the
    rounded back-substitution perturbation. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0) :
    let theta := (1 + cStep) ^ n * (1 + gamma fp n) - 1
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          theta * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ theta * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let η : ℝ := (1 + cStep) ^ n - 1
  let theta : ℝ := (1 + cStep) ^ n * (1 + gamma fp n) - 1
  let R : Fin n → Fin n → ℝ :=
    fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
  let cTop : Fin n → ℝ :=
    fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩
  rcases
    fl_householderStoredTrailingPanel_higham_columnwise_factorization
      fp hmn A b A_hat b_hat alpha cStep hcStep hm
      hInitA hInitb hStepA hStepb halpha hden hA_budget hb_budget with
    ⟨Q, ΔA, Δb, hQ, hArep, hbrep, hΔA_cols, hΔb,
      hA_top, hA_bottom, hb_top, hupper⟩
  have hη_nonneg : 0 ≤ η := by
    unfold η
    have hbase : (1 : ℝ) ≤ 1 + cStep := by linarith
    exact sub_nonneg.mpr (one_le_pow₀ hbase)
  have hγ_nonneg : 0 ≤ gamma fp n := gamma_nonneg fp hγ
  have htheta_eq : theta = η + gamma fp n * (1 + η) := by
    unfold theta η
    ring
  have htheta_ge_eta : η ≤ theta := by
    rw [htheta_eq]
    have hone_eta : 0 ≤ 1 + η := by linarith
    exact le_add_of_nonneg_right (mul_nonneg hγ_nonneg hone_eta)
  have hmain :=
    exists_perturbed_ls_minimizer_of_commonQ_topBlock_fl_backSub_gamma_bound_columnwise
      (m := m) (n := n) fp A ΔA b Δb (A_hat n) (b_hat n) Q R cTop
      (fun j => η * vecNorm2 (fun i : Fin m => A i j)) (η * vecNorm2 b)
      hQ hArep hbrep hA_top hA_bottom hb_top
      (by intro i; simpa [R] using hdiag i)
      (by simpa [R] using hupper)
      hγ (by intro j; simpa [η] using hΔA_cols j)
      (by simpa [η] using hΔb)
  rcases hmain with ⟨ΔA', Δb', hcols, hbnd, hls⟩
  refine ⟨ΔA', Δb', ?_, ?_, hls⟩
  · intro j
    have hcol := hcols j
    let aNorm : ℝ := vecNorm2 (fun i : Fin m => A i j)
    calc
      vecNorm2 (fun i : Fin m => ΔA' i j)
          ≤ η * aNorm + gamma fp n * (aNorm + η * aNorm) := by
            simpa [aNorm] using hcol
      _ = theta * aNorm := by
            rw [htheta_eq]
            ring
  · exact hbnd.trans
      (mul_le_mul_of_nonneg_right htheta_ge_eta (vecNorm2_nonneg b))
/-- Source-style gamma-tilde envelope for the stored trailing Householder QR
    exact-minimizer theorem.

    The stored-loop theorem above computes the accumulated radius as
    `(1 + cStep)^n * (1 + gamma fp n) - 1`.  This wrapper exposes the printed
    theorem's single `gammaTilde` radius and isolates the remaining source
    constant closure in the hypothesis `hgammaTilde`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gammaTilde_bound_columnwise
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep gammaTilde : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0)
    (hgammaTilde : (1 + cStep) ^ n * (1 + gamma fp n) - 1 ≤ gammaTilde) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gammaTilde * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gammaTilde * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let theta : ℝ := (1 + cStep) ^ n * (1 + gamma fp n) - 1
  have htheta_le : theta ≤ gammaTilde := by
    simpa [theta] using hgammaTilde
  rcases
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise
      (m := m) (n := n) fp hmn A b A_hat b_hat alpha cStep hcStep
      hm hγ hInitA hInitb hStepA hStepb halpha hden hA_budget hb_budget hdiag with
    ⟨ΔA', Δb', hcols, hbnd, hls⟩
  refine ⟨ΔA', Δb', ?_, ?_, hls⟩
  · intro j
    exact (hcols j).trans
      (mul_le_mul_of_nonneg_right htheta_le
        (vecNorm2_nonneg (fun i : Fin m => A i j)))
  · exact hbnd.trans
      (mul_le_mul_of_nonneg_right htheta_le (vecNorm2_nonneg b))
/-- Stored trailing Householder QR solve certificate with the source-style
    accumulated radius made concrete.

    If the local stored-Householder column and right-hand-side update budget
    satisfies `cStep ≤ gamma fp stepOps`, then the combined stored-QR plus
    triangular-solve backward-error radius from the gamma-tilde wrapper is
    bounded by the single Higham factor `gamma fp (n*stepOps+n)`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_cStep_le_gamma
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hcStep_gamma : cStep ≤ gamma fp stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hγn : gammaValid fp n :=
    gammaValid_mono fp (Nat.le_add_left n (n * stepOps)) hγtotal
  have hgammaTilde :
      (1 + cStep) ^ n * (1 + gamma fp n) - 1 ≤
        gamma fp (n * stepOps + n) :=
    NumStability.one_add_pow_mul_one_add_gamma_sub_one_le_gamma_sum_of_le_gamma
      fp n stepOps hcStep hcStep_gamma hγtotal
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gammaTilde_bound_columnwise
      (m := m) (n := n) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (cStep := cStep)
      (gammaTilde := gamma fp (n * stepOps + n))
      (hcStep := hcStep) (hm := hm) (hγ := hγn)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (halpha := halpha) (hden := hden)
      (hA_budget := hA_budget) (hb_budget := hb_budget)
      (hdiag := hdiag) (hgammaTilde := hgammaTilde)
/-- Stored trailing Householder QR solve certificate with the deterministic
    compact sequence budget instantiated.

    This is the source-facing sequence-budget version of the concrete gamma
    wrapper above: the one-step column/RHS budget hypotheses are supplied by
    the stored compact Householder sequence budget, and only the comparison
    from that computed sequence budget to `gamma fp stepOps` remains visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sequenceBudget_le_gamma
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hseq_gamma :
      storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha ≤
        gamma fp stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let cStep : ℝ := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
  have hcStep : 0 ≤ cStep := by
    simpa [cStep] using
      storedQRCompactSequenceRelativeBudget_nonneg hmn fp A_hat b_hat alpha hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_cStep_le_gamma
      (m := m) (n := n) (stepOps := stepOps) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (cStep := cStep) (hcStep := hcStep)
      (hm := hm) (hγtotal := hγtotal)
      (hcStep_gamma := by simpa [cStep] using hseq_gamma)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (halpha := halpha) (hden := hden)
      (hA_budget := fun k hk j => by
        simpa [cStep] using
          storedQRCompactSequenceRelativeBudget_column_bound
            hmn fp A_hat b_hat alpha hm k hk j)
      (hb_budget := fun k hk => by
        simpa [cStep] using
          storedQRCompactSequenceRelativeBudget_rhs_bound
            hmn fp A_hat b_hat alpha hm k hk)
      (hdiag := hdiag)
/-- Stored trailing Householder QR solve certificate with deterministic
    one-step compact budgets.

    This keeps the sharp concrete radius from the `cStep` wrapper, but replaces
    the abstract column/RHS compact-update hypotheses by the computed stored
    one-step panel budget.  If every stored Householder step has relative
    compact budget at most `gamma fp stepOps`, then the QR-loop and triangular
    solve combine to the source-shaped radius `gamma fp (n*stepOps+n)`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_stepBudget_le_gamma
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγstep : gammaValid fp stepOps)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hStepBudget : ∀ k : Fin n,
      storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤
        gamma fp stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let cStep : ℝ := gamma fp stepOps
  have hcStep : 0 ≤ cStep := by
    simpa [cStep] using gamma_nonneg fp hγstep
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_cStep_le_gamma
      (m := m) (n := n) (stepOps := stepOps) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (cStep := cStep) (hcStep := hcStep)
      (hm := hm) (hγtotal := hγtotal)
      (hcStep_gamma := by simp [cStep])
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (halpha := halpha) (hden := hden)
      (hA_budget := fun k hk j => by
        let kf : Fin n := ⟨k, hk⟩
        let stepBudget : ℝ :=
          storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha kf
        have hlocal :
            vecNorm2 (fun i : Fin m =>
              if j.val < k then 0
              else householderCompactComponentBudget fp m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
                (householderBetaSpec m
                  (householderTrailingActiveVector m
                    ⟨k, lt_of_lt_of_le hk hmn⟩
                    (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
                (fun a => A_hat k a j) i) ≤
              stepBudget * vecNorm2 (fun i : Fin m => A_hat k i j) := by
          simpa [stepBudget, storedQRCompactStepRelativeBudget, kf] using
            householderCompactPanelRelativeBudget_stored_column_bound
              fp m n k
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (A_hat k) (b_hat k) hm j
        have hstep : stepBudget ≤ cStep := by
          simpa [stepBudget, cStep] using hStepBudget kf
        exact hlocal.trans
          (mul_le_mul_of_nonneg_right hstep
            (vecNorm2_nonneg (fun i : Fin m => A_hat k i j))))
      (hb_budget := fun k hk => by
        let kf : Fin n := ⟨k, hk⟩
        let stepBudget : ℝ :=
          storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha kf
        have hlocal :
            vecNorm2 (fun i : Fin m =>
              if i.val < k then 0
              else householderCompactComponentBudget fp m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
                (householderBetaSpec m
                  (householderTrailingActiveVector m
                    ⟨k, lt_of_lt_of_le hk hmn⟩
                    (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
                (b_hat k) i) ≤
              stepBudget * vecNorm2 (b_hat k) := by
          simpa [stepBudget, storedQRCompactStepRelativeBudget, kf] using
            householderCompactPanelRelativeBudget_stored_rhs_bound
              fp m n k
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (A_hat k) (b_hat k) hm
        have hstep : stepBudget ≤ cStep := by
          simpa [stepBudget, cStep] using hStepBudget kf
        exact hlocal.trans
          (mul_le_mul_of_nonneg_right hstep (vecNorm2_nonneg (b_hat k))))
      (hdiag := hdiag)
/-- Stored trailing Householder QR solve certificate with the per-step compact
    budget comparison discharged by an explicit operation-count index.

    This is the source-facing successor of the one-step-budget wrapper: the
    hypothesis `storedQRCompactStepRelativeBudget ... <= gamma fp stepOps` is
    derived from the source nonzero-denominator condition for each signed
    Householder stage and the displayed conservative operation count
    `31 * (n + 1) * m <= stepOps`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγstep : gammaValid fp stepOps)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hstepOps : 31 * (n + 1) * m ≤ stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_stepBudget_le_gamma
      (m := m) (n := n) (stepOps := stepOps) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hm := hm) (hγstep := hγstep)
      (hγtotal := hγtotal)
      (hStepBudget := fun k =>
        storedQRCompactStepRelativeBudget_le_gamma_of_source_den_ne_zero_operation_count
          hmn fp A_hat b_hat alpha hm hγstep hstepOps k
          (by simpa using hden k.val k.isLt))
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (halpha := halpha) (hden := hden) (hdiag := hdiag)
/-- Stored trailing Householder QR solve certificate with concrete
    operation-count γ bounds and a pivot-error final-diagonal bridge.

    This removes the final `Rᵢᵢ ≠ 0` hypothesis from the source-denominator
    operation-count wrapper.  The remaining local nonbreakdown assumptions are
    the source denominator condition used to bound each compact step and the
    per-pivot diagonal component budget `... < |alpha_k|` used by the QR
    module to prove the final stored top block has nonzero diagonal. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps_pivot_error_lt_abs_alpha
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγstep : gammaValid fp stepOps)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hstepOps : 31 * (n + 1) * m ≤ stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k|) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0 :=
    fl_householderStoredTrailingPanel_sequence_diag_nonzero_of_budget_lt_abs_alpha
      fp hmn A_hat alpha hm hStepA halpha hden hbudgetDiag
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps
      (m := m) (n := n) (stepOps := stepOps) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hm := hm) (hγstep := hγstep)
      (hγtotal := hγtotal) (hstepOps := hstepOps)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (halpha := halpha) (hden := hden) (hdiag := hdiag)
/-- Stored trailing Householder QR solve certificate with concrete
    operation-count γ bounds from the standard signed Householder scalar.

    This is a source-facing wrapper for the operation-count concrete-gamma
    route: the signed-`alpha` definition and positive trailing norm prove the
    exact scalar identity and source denominator nonbreakdown, while the
    square-root diagonal budget proves the final stored diagonal bridge. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps_signed_alpha_trailingNorm_pos_and_sqrt_budget
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγstep : gammaValid fp stepOps)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hstepOps : 31 * (n + 1) * m ≤ stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0 := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_mul_pivot_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 := by
    intro k hk
    simpa using
      householderTrailingActiveVector_inner_self_ne_zero_of_trailingNorm2Sq_pos_mul_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
        (halpha k hk) (htrailingPos k hk) (hsign k hk)
  have hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k| := by
    intro k hk
    exact
      budget_lt_abs_alpha_of_lt_sqrt_trailingNorm2Sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
        (householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩)
        (halpha k hk) (hbudgetSqrt k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps_pivot_error_lt_abs_alpha
      (m := m) (n := n) (stepOps := stepOps) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hm := hm) (hγstep := hγstep)
      (hγtotal := hγtotal) (hstepOps := hstepOps)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (halpha := halpha) (hden := hden) (hbudgetDiag := hbudgetDiag)
/-- Stored trailing Householder QR solve certificate with concrete
    operation-count γ bounds and only the final total γ-validity guard.

    For a nonempty least-squares problem, `stepOps <= n * stepOps + n`.
    Together with the displayed operation-count dominance, this derives the
    local `gammaValid fp stepOps` and `gammaValid fp m` guards required by the
    signed-alpha operation-count wrapper from the single final radius guard. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps_signed_alpha_trailingNorm_pos_and_sqrt_budget_of_total_gammaValid
    {m n stepOps : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγtotal : gammaValid fp (n * stepOps + n))
    (hstepOps : 31 * (n + 1) * m ≤ stepOps)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * stepOps + n) * vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * stepOps + n) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hstep_le_total : stepOps ≤ n * stepOps + n := by
    have hstep_le_nstep : stepOps ≤ n * stepOps := by
      simpa [one_mul] using
        Nat.mul_le_mul_right stepOps (Nat.succ_le_of_lt hnpos)
    exact le_trans hstep_le_nstep (Nat.le_add_right (n * stepOps) n)
  have hγstep : gammaValid fp stepOps :=
    gammaValid_mono fp hstep_le_total hγtotal
  have hm_le_step : m ≤ stepOps := by
    have hcoeff : 1 ≤ 31 * (n + 1) := by omega
    have hm_le_ops : m ≤ 31 * (n + 1) * m := by
      simpa using Nat.mul_le_mul_right m hcoeff
    exact le_trans hm_le_ops hstepOps
  have hm : gammaValid fp m :=
    gammaValid_mono fp (le_trans hm_le_step hstep_le_total) hγtotal
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps_signed_alpha_trailingNorm_pos_and_sqrt_budget
      (m := m) (n := n) (stepOps := stepOps) (fp := fp) (hmn := hmn)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hm := hm) (hγstep := hγstep)
      (hγtotal := hγtotal) (hstepOps := hstepOps)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (htrailingPos := htrailingPos)
      (hbudgetSqrt := hbudgetSqrt)
/-- Stored trailing Householder QR solve certificate with the conservative
    operation count instantiated explicitly.

    This is the source-facing specialization of the total-gamma-validity
    wrapper with `stepOps = 31 * (n + 1) * m`.  It removes the separate
    `stepOps` parameter and the displayed dominance proof while keeping the
    remaining source-side signed-alpha, positive trailing-norm, square-root
    budget, and final total gamma-validity guards visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_trailingNorm_pos_and_sqrt_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγtotal : gammaValid fp (n * (31 * (n + 1) * m) + n))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_stepOps_signed_alpha_trailingNorm_pos_and_sqrt_budget_of_total_gammaValid
      (m := m) (n := n) (stepOps := 31 * (n + 1) * m)
      (fp := fp) (hmn := hmn) (hnpos := hnpos)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hγtotal := hγtotal) (hstepOps := le_rfl)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (htrailingPos := htrailingPos)
      (hbudgetSqrt := hbudgetSqrt)
/-- Stored trailing Householder QR solve certificate with the conservative
    operation count and an explicit unit-roundoff smallness guard.

    This variant removes the abstract `gammaValid` hypothesis from
    `..._sourceDen_conservativeOperationCount_signed_alpha_trailingNorm_pos_and_sqrt_budget`.
    The displayed condition is exactly the validity condition for the final
    radius `gamma fp (n * (31 * (n + 1) * m) + n)`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_trailingNorm_pos_and_sqrt_budget_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hγtotal : gammaValid fp (n * (31 * (n + 1) * m) + n) := by
    simpa [gammaValid] using hγsmall
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_trailingNorm_pos_and_sqrt_budget
      (m := m) (n := n) (fp := fp) (hmn := hmn) (hnpos := hnpos)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hγtotal := hγtotal)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (htrailingPos := htrailingPos)
      (hbudgetSqrt := hbudgetSqrt)
/-- Stored trailing Householder QR solve certificate where trailing-norm
    positivity is derived from the strict square-root budget.

    The compact component budget is nonnegative under the final unit-roundoff
    smallness guard.  Therefore `budget < sqrt(trailingNorm2Sq)` already
    implies `0 < trailingNorm2Sq`, so the explicit `htrailingPos` hypothesis in
    the previous wrapper is redundant. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_sqrt_budget_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hγtotal : gammaValid fp (n * (31 * (n + 1) * m) + n) := by
    simpa [gammaValid] using hγsmall
  have hm_le_total : m ≤ n * (31 * (n + 1) * m) + n := by
    have hcoeff : 1 ≤ 31 * (n + 1) := by omega
    have hm_le_step : m ≤ 31 * (n + 1) * m := by
      simpa using Nat.mul_le_mul_right m hcoeff
    have hstep_le_total :
        31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) + n := by
      have hstep_le_nstep :
          31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) := by
        simpa [one_mul] using
          Nat.mul_le_mul_right (31 * (n + 1) * m)
            (Nat.succ_le_of_lt hnpos)
      exact le_trans hstep_le_nstep
        (Nat.le_add_right (n * (31 * (n + 1) * m)) n)
    exact le_trans hm_le_step hstep_le_total
  have hm : gammaValid fp m :=
    gammaValid_mono fp hm_le_total hγtotal
  have htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    have hbudget_nonneg :
        0 ≤
          householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩ :=
      householderCompactComponentBudget_nonneg fp m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
        (householderBetaSpec m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
        (fun a => A_hat k a ⟨k, hk⟩) hm
        ⟨k, lt_of_lt_of_le hk hmn⟩
    have hsqrt_pos :
        0 <
          Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)) :=
      lt_of_le_of_lt hbudget_nonneg (hbudgetSqrt k hk)
    exact Real.sqrt_pos.mp hsqrt_pos
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_trailingNorm_pos_and_sqrt_budget_no_gammaValid
      (m := m) (n := n) (fp := fp) (hmn := hmn) (hnpos := hnpos)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hγsmall := hγsmall)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (htrailingPos := htrailingPos)
      (hbudgetSqrt := hbudgetSqrt)
/-- Stored trailing Householder QR solve certificate where the square-root
    budget is derived from a dimensioned norm-square margin.

    The hypothesis
    `m * budget^2 < householderTrailingNorm2Sq` is the algebraic margin used by
    several stored-QR nonbreakdown routes.  Nonnegativity of the compact budget
    converts it into the strict square-root budget required by the preceding
    source-denominator concrete-`gamma` wrapper. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_normSqBudget_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩)) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hγtotal : gammaValid fp (n * (31 * (n + 1) * m) + n) := by
    simpa [gammaValid] using hγsmall
  have hm_le_total : m ≤ n * (31 * (n + 1) * m) + n := by
    have hcoeff : 1 ≤ 31 * (n + 1) := by omega
    have hm_le_step : m ≤ 31 * (n + 1) * m := by
      simpa using Nat.mul_le_mul_right m hcoeff
    have hstep_le_total :
        31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) + n := by
      have hstep_le_nstep :
          31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) := by
        simpa [one_mul] using
          Nat.mul_le_mul_right (31 * (n + 1) * m)
            (Nat.succ_le_of_lt hnpos)
      exact le_trans hstep_le_nstep
        (Nat.le_add_right (n * (31 * (n + 1) * m)) n)
    exact le_trans hm_le_step hstep_le_total
  have hm : gammaValid fp m :=
    gammaValid_mono fp hm_le_total hγtotal
  have hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    have hmargin :
        (m : ℝ) * budget ^ 2 <
          householderTrailingNorm2Sq m p
            (fun a => A_hat k a ⟨k, hk⟩) := by
      simpa [budget, beta, v, p] using hbudgetNormSq k hk
    simpa [budget, beta, v, p] using
      budget_lt_sqrt_householderTrailingNorm2Sq_of_dim_mul_budget_sq_lt_trailingNorm2Sq
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg hmargin
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_sqrt_budget_no_gammaValid
      (m := m) (n := n) (fp := fp) (hmn := hmn) (hnpos := hnpos)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hγsmall := hγsmall)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (hbudgetSqrt := hbudgetSqrt)
/-- Stored trailing Householder QR solve certificate whose norm-square budget is
    derived from nonsingular leading blocks and a local inverse-norm budget.

    This source-denominator conservative route removes the visible
    `m * budget^2 < trailingNorm2Sq` hypothesis of
    `..._signed_alpha_normSqBudget_no_gammaValid`.  The stored prefix lower-zero
    invariant and nonsingular leading blocks produce the prefix-span witness; the
    local `kappaInf`/dual-budget assumptions produce the norm-square margin. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let v : ℕ → Fin m → ℝ := fun k =>
    if hk : k < n then
      householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    else 0
  let β : ℕ → ℝ := fun k => householderBetaSpec m (v k)
  have hStep : ∀ k, k < n →
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k (v k) (β k) (A_hat k) := by
    intro k hk
    simpa [v, β, hk] using hStepA k hk
  have hprefix :=
    fl_householderStoredPanel_sequence_prefix_lower_zero
      fp v β A_hat hStep
  have hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    let hkm : k + 1 ≤ m :=
      Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)
    let S : Fin (k + 1) → Fin (k + 1) → ℝ :=
      qrLeadingBlock (A_hat k) hkm hk
    let C : Fin (k + 1) → Fin (k + 1) → ℝ :=
      nonsingInv (k + 1) S
    let budget : ℝ :=
      householderCompactComponentBudget fp m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
        (householderBetaSpec m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
        (fun a => A_hat k a ⟨k, hk⟩)
        ⟨k, lt_of_lt_of_le hk hmn⟩
    have hupper : ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) hkm hk i j = 0 := by
      intro i j hji
      have hjk : j.val < k := by omega
      exact
        hprefix k (Nat.le_of_lt hk)
          (qrLeadingRow m k hkm i)
          (qrLeadingColumn n k hk j)
          (by simpa [qrLeadingColumn] using hjk)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
    have hlowerPrev : ∀ (i : Fin m) (j : Fin k), k ≤ i.val →
        A_hat k i (qrPreviousColumn n k hk j) = 0 := by
      intro i j hi
      exact
        hprefix k (Nat.le_of_lt hk) i (qrPreviousColumn n k hk j)
          (by simp [qrPreviousColumn])
          (lt_of_lt_of_le j.isLt hi)
    have hprefixSpan : qrPrefixSupportSpannedByPreviousColumns (A_hat k) hk :=
      qrPrefixSupportSpannedByPreviousColumns_of_leadingBlock_upper_det_ne_zero
        (A_hat k) hkm hk hupper (by simpa [hkm] using hdetLead k hk)
        hlowerPrev
    have hC : IsLeftInverse (k + 1) S C := by
      exact
        (isInverse_nonsingInv_of_det_ne_zero (k + 1) S
          (by simpa [S, hkm] using hdetLead k hk)).1
    have hCinf : ((k + 1 : ℕ) : ℝ) * infNorm C ^ 2 ≤ K k := by
      simpa [S, C, hkm] using
        infNorm_sq_budget_of_kappaInf_le_and_det_ne_zero
          (k + 1) (Nat.succ_pos k) S C (κ k) (K k)
          (by simpa [S, hkm] using hdetLead k hk)
          (by simpa [S, C, hkm] using hκ k hk)
          (by simpa [S, hkm] using hκbudget k hk)
    simpa [budget, S, C, hkm] using
      dim_mul_budget_sq_lt_trailingNorm2Sq_of_leadingBlock_leftInverse_infNorm_budget
        (A_hat k) hkm hk C hC hprefixSpan (K k) budget
        (hK k hk) hCinf (by simpa [budget] using hbudgetDual k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_normSqBudget_no_gammaValid
      (m := m) (n := n) (fp := fp) (hmn := hmn) (hnpos := hnpos)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hγsmall := hγsmall)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (hbudgetNormSq := hbudgetNormSq)
/-- Source-facing stored trailing Householder exact-minimizer theorem with a
    concrete pivot-error nonbreakdown condition.

    This removes the final `Rᵢᵢ ≠ 0` hypothesis from
    `exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound`
    using the same QR diagonal-nonzero bridge as the Gram-certificate theorem
    above. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_pivot_error_lt_abs_alpha
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k|) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hdiag : ∀ i : Fin n,
      A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ i ≠ 0 :=
    fl_householderStoredTrailingPanel_sequence_diag_nonzero_of_budget_lt_abs_alpha
      fp hmn A_hat alpha hm hStepA halpha hden hbudgetDiag
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hden hA_budget hb_budget hdiag
/-- Source-facing stored trailing Householder exact-minimizer theorem with
    concrete pivot-value and pivot-error nonbreakdown conditions.

    This removes the explicit Householder denominator hypothesis from the
    previous direct minimizer wrapper by deriving it from
    `A_hat[k,k] != alpha_k`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_pivot_ne_alpha_and_pivot_error_lt_abs_alpha
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hpivotNe : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ alpha k)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k|) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 := by
    intro k hk
    simpa using
      householderTrailingActiveVector_inner_self_ne_zero_of_pivot_ne_alpha
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
        (hpivotNe k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_pivot_error_lt_abs_alpha
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hden hA_budget hb_budget
      hbudgetDiag
/-- Source-facing stored trailing Householder exact-minimizer theorem using
    the standard Householder sign convention for denominator nonbreakdown. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_trailingNorm_pos_mul_nonpos_and_pivot_error_lt_abs_alpha
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k|) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hpivotNe : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ alpha k := by
    intro k hk
    simpa using
      householder_pivot_ne_alpha_of_trailingNorm2Sq_pos_mul_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
        (halpha k hk) (htrailingPos k hk) (hsign k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_pivot_ne_alpha_and_pivot_error_lt_abs_alpha
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hpivotNe hA_budget hb_budget
      hbudgetDiag
/-- Source-facing stored trailing Householder exact-minimizer theorem with the
    standard signed Householder scalar and square-root diagonal-update budget.
-/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_signed_alpha_trailingNorm_pos_and_sqrt_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0 := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_mul_pivot_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k| := by
    intro k hk
    exact
      budget_lt_abs_alpha_of_lt_sqrt_trailingNorm2Sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
        (householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩)
        (halpha k hk) (hbudgetSqrt k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_trailingNorm_pos_mul_nonpos_and_pivot_error_lt_abs_alpha
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha htrailingPos hsign
      hA_budget hb_budget hbudgetDiag
/-- Source-facing stored trailing Householder exact-minimizer theorem from
    nonsingular local leading blocks and a square-root trailing-norm pivot
    budget.

    The determinant hypotheses supply positive active trailing-column norms;
    the square-root compact-update budget supplies the pivot-error inequality
    used by the direct Theorem 20.3 minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_leading_block_det_ne_zero_sqrt_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun i => A_hat k i ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun i => A_hat k i ⟨k, hk⟩) := by
    intro k hk
    exact
      householderTrailingNorm2Sq_pos_of_leading_block_det_ne_zero
        (A_hat k) (lt_of_lt_of_le hk hmn) hk
        (hdetPrev k hk) (hdetLead k hk) (hlowerPrev k hk)
  have hbudgetDiag : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ < |alpha k| := by
    intro k hk
    exact
      budget_lt_abs_alpha_of_lt_sqrt_trailingNorm2Sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun i => A_hat k i ⟨k, hk⟩) (alpha k)
        (householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩)
        (halpha k hk) (hbudgetSqrt k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_trailingNorm_pos_mul_nonpos_and_pivot_error_lt_abs_alpha
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha htrailingPos hsign
      hA_budget hb_budget hbudgetDiag
/-- Source-facing stored trailing Householder exact-minimizer theorem through
    explicit triangular-principal-minor nonbreakdown data.

    This is the direct minimizer counterpart of the solver-facing triangular
    leading-block square-root-budget route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_triangular_leading_blocks_sqrt_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hupper : ∀ k (_hk : k < n) (i : Fin m) (j : Fin n),
      j.val < i.val → A_hat k i j = 0)
    (hdiagPrev : ∀ k (hk : k < n) (r : Fin k),
      A_hat k
        (qrPrefixRow m k (le_of_lt (lt_of_lt_of_le hk hmn)) r)
        (qrPreviousColumn n k hk r) ≠ 0)
    (hdiagLead : ∀ k (hk : k < n) (r : Fin (k + 1)),
      A_hat k
        (qrLeadingRow m k
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) r)
        (qrLeadingColumn n k hk r) ≠ 0)
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun i => A_hat k i ⟨k, hk⟩))) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0 := by
    intro k hk
    exact
      qrPreviousLeadingBlockTranspose_det_ne_zero_of_upper_triangular_diag_ne_zero
        (A_hat k) (le_of_lt (lt_of_lt_of_le hk hmn)) hk
        (hupper k hk) (hdiagPrev k hk)
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      qrLeadingBlock_det_ne_zero_of_upper_triangular_diag_ne_zero
        (A_hat k) (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk
        (hupper k hk) (hdiagLead k hk)
  have hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0 := by
    intro k hk i j hki
    exact hupper k hk i (qrPreviousColumn n k hk j)
      (Nat.lt_of_lt_of_le j.isLt hki)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_leading_block_det_ne_zero_sqrt_budget
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hdetPrev hdetLead hlowerPrev
      hsign hA_budget hb_budget hbudgetSqrt
/-- Source-facing stored trailing Householder exact-minimizer theorem from
    local leading-block determinants and self-norm `κ∞` budgets.

    This is the direct minimizer counterpart of the solver-facing
    `..._of_leading_blocks_det_ne_zero_kappaInf_selfNorm_budget` route: the
    determinant of each previous block supplies prefix span, while the local
    condition-number budget and displayed dual budget imply the square-root
    pivot budget consumed by the direct Theorem 20.3 minimizer certificate. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_leading_blocks_det_ne_zero_kappaInf_selfNorm_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    have hmargin :
        (m : ℝ) * budget ^ 2 <
          householderTrailingNorm2Sq m p
            (fun a => A_hat k a ⟨k, hk⟩) := by
      let hkm : k + 1 ≤ m :=
        Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)
      let S : Fin (k + 1) → Fin (k + 1) → ℝ :=
        qrLeadingBlock (A_hat k) hkm hk
      let C : Fin (k + 1) → Fin (k + 1) → ℝ :=
        nonsingInv (k + 1) S
      have hprefixSpan : qrPrefixSupportSpannedByPreviousColumns (A_hat k) hk :=
        qrPrefixSupportSpannedByPreviousColumns_of_det_ne_zero_previousLeadingBlockTranspose
          (A_hat k) (le_of_lt (lt_of_lt_of_le hk hmn)) hk
          (hdetPrev k hk) (hlowerPrev k hk)
      have hC : IsLeftInverse (k + 1) S C := by
        exact
          (isInverse_nonsingInv_of_det_ne_zero (k + 1) S
            (by simpa [S, hkm] using hdetLead k hk)).1
      have hCinf : ((k + 1 : ℕ) : ℝ) * infNorm C ^ 2 ≤ K k := by
        simpa [S, C, hkm] using
          infNorm_sq_budget_of_kappaInf_le_and_det_ne_zero
            (k + 1) (Nat.succ_pos k) S C (κ k) (K k)
            (by simpa [S, hkm] using hdetLead k hk)
            (by simpa [S, C, hkm] using hκ k hk)
            (by simpa [S, hkm] using hκbudget k hk)
      simpa [budget, beta, v, p, S, C, hkm] using
        dim_mul_budget_sq_lt_trailingNorm2Sq_of_leadingBlock_leftInverse_infNorm_budget
          (A_hat k) hkm hk C hC hprefixSpan (K k) budget
          (hK k hk) hCinf
          (by simpa [budget, beta, v, p] using hbudgetDual k hk)
    simpa [budget, beta, v, p] using
      budget_lt_sqrt_householderTrailingNorm2Sq_of_dim_mul_budget_sq_lt_trailingNorm2Sq
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg hmargin
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_leading_block_det_ne_zero_sqrt_budget
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hdetPrev hdetLead hlowerPrev
      hsign hA_budget hb_budget hbudgetSqrt
/-- Source-facing stored trailing Householder exact-minimizer theorem from
    triangular local leading blocks and self-norm `κ∞` budgets.

    This is the direct minimizer counterpart of the solver-facing triangular
    condition-number route.  The triangular shape and nonzero local diagonals
    supply the determinant and lower-zero hypotheses consumed by
    `..._of_leading_blocks_det_ne_zero_kappaInf_selfNorm_budget`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_triangular_leading_blocks_kappaInf_selfNorm_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hupper : ∀ k (_hk : k < n) (i : Fin m) (j : Fin n),
      j.val < i.val → A_hat k i j = 0)
    (hdiagPrev : ∀ k (hk : k < n) (r : Fin k),
      A_hat k
        (qrPrefixRow m k (le_of_lt (lt_of_lt_of_le hk hmn)) r)
        (qrPreviousColumn n k hk r) ≠ 0)
    (hdiagLead : ∀ k (hk : k < n) (r : Fin (k + 1)),
      A_hat k
        (qrLeadingRow m k
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) r)
        (qrLeadingColumn n k hk r) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0 := by
    intro k hk
    exact
      qrPreviousLeadingBlockTranspose_det_ne_zero_of_upper_triangular_diag_ne_zero
        (A_hat k) (le_of_lt (lt_of_lt_of_le hk hmn)) hk
        (hupper k hk) (hdiagPrev k hk)
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      qrLeadingBlock_det_ne_zero_of_upper_triangular_diag_ne_zero
        (A_hat k) (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk
        (hupper k hk) (hdiagLead k hk)
  have hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0 := by
    intro k hk i j hki
    exact hupper k hk i (qrPreviousColumn n k hk j)
      (Nat.lt_of_lt_of_le j.isLt hki)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_leading_blocks_det_ne_zero_kappaInf_selfNorm_budget
      fp hmn A b A_hat b_hat alpha κ K cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hdetPrev hdetLead hlowerPrev
      hK hκ hκbudget hsign hA_budget hb_budget hbudgetDual
/-- Source-facing stored trailing Householder exact-minimizer theorem from the
    computed prefix lower-zero shape, triangular local diagonal nonzeros, and
    self-norm `κ∞` budgets.

    The stored panel recurrence supplies exactly the triangular entries needed
    for the previous/current local determinant bridges; the conclusion remains
    the direct perturbed rectangular least-squares minimizer statement. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_prefix_lower_zero_triangular_leading_blocks_kappaInf_selfNorm_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdiagPrev : ∀ k (hk : k < n) (r : Fin k),
      A_hat k
        (qrPrefixRow m k (le_of_lt (lt_of_lt_of_le hk hmn)) r)
        (qrPreviousColumn n k hk r) ≠ 0)
    (hdiagLead : ∀ k (hk : k < n) (r : Fin (k + 1)),
      A_hat k
        (qrLeadingRow m k
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) r)
        (qrLeadingColumn n k hk r) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let vStep : ℕ → Fin m → ℝ := fun k =>
    if hk : k < n then
      householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    else
      0
  let βStep : ℕ → ℝ := fun k =>
    if hk : k < n then
      householderBetaSpec m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
    else
      0
  have hStepA_shape : ∀ k, k < n →
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k (vStep k) (βStep k) (A_hat k) := by
    intro k hk
    simpa [vStep, βStep, hk] using hStepA k hk
  have hprefixLower :
      ∀ k, k ≤ n →
        ∀ (i : Fin m) (j : Fin n),
          j.val < k → j.val < i.val → A_hat k i j = 0 :=
    fl_householderStoredPanel_sequence_prefix_lower_zero
      (m := m) (n := n) fp vStep βStep A_hat hStepA_shape
  have hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0 := by
    intro k hk
    apply
      qrPreviousLeadingBlockTranspose_det_ne_zero_of_local_lower_triangular_diag_ne_zero
        (A_hat k) (le_of_lt (lt_of_lt_of_le hk hmn)) hk
    · intro i j hij
      simpa [qrPreviousLeadingBlockTranspose] using
        hprefixLower k (Nat.le_of_lt hk)
          (qrPrefixRow m k (le_of_lt (lt_of_lt_of_le hk hmn)) j)
          (qrPreviousColumn n k hk i) i.isLt
          (by simpa [qrPrefixRow, qrPreviousColumn] using hij)
    · exact hdiagPrev k hk
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    apply
      qrLeadingBlock_det_ne_zero_of_local_upper_triangular_diag_ne_zero
        (A_hat k) (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk
    · intro i j hji
      have hjk : j.val < k := Nat.lt_of_lt_of_le hji (Nat.le_of_lt_succ i.isLt)
      simpa [qrLeadingBlock] using
        hprefixLower k (Nat.le_of_lt hk)
          (qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)
          (by simpa [qrLeadingColumn] using hjk)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
    · exact hdiagLead k hk
  have hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0 := by
    intro k hk i j hki
    simpa [qrPreviousColumn] using
      hprefixLower k (Nat.le_of_lt hk) i
        (qrPreviousColumn n k hk j) j.isLt
        (Nat.lt_of_lt_of_le j.isLt hki)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_leading_blocks_det_ne_zero_kappaInf_selfNorm_budget
      fp hmn A b A_hat b_hat alpha κ K cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hdetPrev hdetLead hlowerPrev
      hK hκ hκbudget hsign hA_budget hb_budget hbudgetDual
/-- Source-facing computed-prefix triangular self-norm QR exact-minimizer
    certificate with the standard signed Householder scalar made explicit.

    This direct minimizer wrapper replaces the independent squared-norm and
    sign hypotheses in the prefix-lower-zero `κ∞` route by the concrete source
    convention for `alpha_k`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_signed_alpha_prefix_lower_zero_triangular_leading_blocks_kappaInf_selfNorm_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdiagPrev : ∀ k (hk : k < n) (r : Fin k),
      A_hat k
        (qrPrefixRow m k (le_of_lt (lt_of_lt_of_le hk hmn)) r)
        (qrPreviousColumn n k hk r) ≠ 0)
    (hdiagLead : ∀ k (hk : k < n) (r : Fin (k + 1)),
      A_hat k
        (qrLeadingRow m k
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) r)
        (qrLeadingColumn n k hk r) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0 := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_mul_pivot_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_prefix_lower_zero_triangular_leading_blocks_kappaInf_selfNorm_budget
      fp hmn A b A_hat b_hat alpha κ K cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb halpha hdiagPrev hdiagLead
      hK hκ hκbudget hsign hA_budget hb_budget hbudgetDual
/-- Source-facing computed-prefix triangular self-norm QR exact-minimizer
    certificate with current-pivot nonzero hypotheses.

    This direct minimizer wrapper derives the earlier local diagonal nonzeros
    from the stored loop, signed Householder `alpha`, and square-root budget,
    leaving only the current local pivot nonzero condition visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_signed_alpha_prefix_lower_zero_pivot_nonzero_kappaInf_selfNorm_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (cStep : ℝ) (hcStep : 0 ≤ cStep)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)))
    (hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hA_budget : ∀ k (hk : k < n), ∀ j : Fin n,
      vecNorm2 (fun i : Fin m =>
        if j.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a j) i) ≤
        cStep * vecNorm2 (fun i : Fin m => A_hat k i j))
    (hb_budget : ∀ k (hk : k < n),
      vecNorm2 (fun i : Fin m =>
        if i.val < k then 0
        else householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) i) ≤
        cStep * vecNorm2 (b_hat k))
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    simpa [budget, beta, v, p] using
      householderTrailingNorm2Sq_pos_of_nonneg_budget_lt_sqrt
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg (hbudgetSqrt k hk)
  have hdiagPrefix :
      ∀ k (hk : k ≤ n) (i : Fin k),
        A_hat k
          ⟨i.val, lt_of_lt_of_le i.isLt (le_trans hk hmn)⟩
          ⟨i.val, lt_of_lt_of_le i.isLt hk⟩ ≠ 0 :=
    fl_householderStoredTrailingPanel_sequence_prefix_diag_nonzero_of_signed_alpha_trailingNorm_pos_sqrt_budget
      fp hmn A_hat alpha hm hStepA hAlphaDef htrailingPos hbudgetSqrt
  have hdiagPrev : ∀ k (hk : k < n) (r : Fin k),
      A_hat k
        (qrPrefixRow m k (le_of_lt (lt_of_lt_of_le hk hmn)) r)
        (qrPreviousColumn n k hk r) ≠ 0 := by
    intro k hk r
    simpa [qrPrefixRow, qrPreviousColumn] using
      hdiagPrefix k (Nat.le_of_lt hk) r
  have hdiagLead : ∀ k (hk : k < n) (r : Fin (k + 1)),
      A_hat k
        (qrLeadingRow m k
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) r)
        (qrLeadingColumn n k hk r) ≠ 0 := by
    intro k hk r
    by_cases hr : r.val < k
    · let rp : Fin k := ⟨r.val, hr⟩
      have hp := hdiagPrefix k (Nat.le_of_lt hk) rp
      simpa [qrLeadingRow, qrLeadingColumn, qrPrefixRow, qrPreviousColumn, rp] using hp
    · have hr_eq : r.val = k := by omega
      simpa [qrLeadingRow, qrLeadingColumn, hr_eq] using hdiagPivot k hk
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_signed_alpha_prefix_lower_zero_triangular_leading_blocks_kappaInf_selfNorm_budget
      fp hmn A b A_hat b_hat alpha κ K cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb hAlphaDef hdiagPrev hdiagLead
      hK hκ hκbudget hA_budget hb_budget hbudgetDual
/-- Source-facing explicit compact-budget stored Householder exact-minimizer
    certificate for the prefix-local signed-alpha pivot-nonzero route.

    This removes the separate compact panel/RHS budget-domination hypotheses by
    choosing the deterministic sequence budget
    `storedQRCompactSequenceRelativeBudget`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_pivot_nonzero_kappaInf_selfNorm_budget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)))
    (hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
  have hcStep : 0 ≤ cStep := by
    simpa [cStep] using
      storedQRCompactSequenceRelativeBudget_nonneg
        hmn fp A_hat b_hat alpha hm
  change
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_signed_alpha_prefix_lower_zero_pivot_nonzero_kappaInf_selfNorm_budget
      fp hmn A b A_hat b_hat alpha κ K cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb hAlphaDef hbudgetSqrt
      hdiagPivot hK hκ hκbudget
      (fun k hk j => by
        simpa [cStep] using
          storedQRCompactSequenceRelativeBudget_column_bound
            hmn fp A_hat b_hat alpha hm k hk j)
      (fun k hk => by
        simpa [cStep] using
          storedQRCompactSequenceRelativeBudget_rhs_bound
            hmn fp A_hat b_hat alpha hm k hk)
      hbudgetDual
/-- Source-facing explicit compact-budget stored Householder exact-minimizer
    certificate with the pivot budget stated as a dimensioned trailing
    norm-square margin.

    This direct minimizer bridge mirrors the solver-facing
    `..._normSqBudget` theorem: the visible hypothesis
    `m * budget_k^2 < ||A_k(k:m,k)||_2^2` implies the square-root pivot budget
    consumed by the existing explicit compact-budget certificate. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_pivot_nonzero_kappaInf_selfNorm_normSqBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    have hmargin :
        (m : ℝ) * budget ^ 2 <
          householderTrailingNorm2Sq m p
            (fun a => A_hat k a ⟨k, hk⟩) := by
      simpa [budget, beta, v, p] using hbudgetNormSq k hk
    simpa [budget, beta, v, p] using
      budget_lt_sqrt_householderTrailingNorm2Sq_of_dim_mul_budget_sq_lt_trailingNorm2Sq
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg hmargin
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_pivot_nonzero_kappaInf_selfNorm_budget
      fp hmn A b A_hat b_hat alpha κ K hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hbudgetSqrt hdiagPivot hK hκ hκbudget hbudgetDual
/-- Source-facing explicit compact-budget exact-minimizer certificate from
    structured nonsingular leading blocks and norm-square pivot margins.

    The stored lower-zero invariant and nonsingular displayed leading blocks
    supply the current pivot nonzero condition needed by triangular back
    substitution; the norm-square margin then feeds the direct compact-budget
    pivot route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_leadingBlock_det_ne_zero_kappaInf_selfNorm_normSqBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  have hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0 :=
    fl_householderStoredTrailingPanel_sequence_current_pivot_ne_zero_of_leadingBlock_det_ne_zero
      fp hmn A_hat alpha hStepA hdetLead
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_pivot_nonzero_kappaInf_selfNorm_normSqBudget
      fp hmn A b A_hat b_hat alpha κ K hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hbudgetNormSq hdiagPivot hK hκ hκbudget hbudgetDual
/-- Source-facing explicit compact-budget exact-minimizer certificate from
    structured nonsingular leading blocks and local `κ∞`/dual budgets.

    This is the direct minimizer companion of the solver-facing dual-budget
    theorem: the local leading-block inverse budget and the compact-update
    dual budget imply the dimensioned trailing-norm-square margin internally.
-/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (κ K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let v : ℕ → Fin m → ℝ := fun k =>
    if hk : k < n then
      householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    else 0
  let β : ℕ → ℝ := fun k => householderBetaSpec m (v k)
  have hStep : ∀ k, k < n →
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k (v k) (β k) (A_hat k) := by
    intro k hk
    simpa [v, β, hk] using hStepA k hk
  have hprefix :=
    fl_householderStoredPanel_sequence_prefix_lower_zero
      fp v β A_hat hStep
  have hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    let hkm : k + 1 ≤ m :=
      Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)
    let S : Fin (k + 1) → Fin (k + 1) → ℝ :=
      qrLeadingBlock (A_hat k) hkm hk
    let C : Fin (k + 1) → Fin (k + 1) → ℝ :=
      nonsingInv (k + 1) S
    let budget : ℝ :=
      householderCompactComponentBudget fp m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
        (householderBetaSpec m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
        (fun a => A_hat k a ⟨k, hk⟩)
        ⟨k, lt_of_lt_of_le hk hmn⟩
    have hupper : ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) hkm hk i j = 0 := by
      intro i j hji
      have hjk : j.val < k := by omega
      exact
        hprefix k (Nat.le_of_lt hk)
          (qrLeadingRow m k hkm i)
          (qrLeadingColumn n k hk j)
          (by simpa [qrLeadingColumn] using hjk)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
    have hlowerPrev : ∀ (i : Fin m) (j : Fin k), k ≤ i.val →
        A_hat k i (qrPreviousColumn n k hk j) = 0 := by
      intro i j hi
      exact
        hprefix k (Nat.le_of_lt hk) i (qrPreviousColumn n k hk j)
          (by simp [qrPreviousColumn])
          (lt_of_lt_of_le j.isLt hi)
    have hprefixSpan : qrPrefixSupportSpannedByPreviousColumns (A_hat k) hk :=
      qrPrefixSupportSpannedByPreviousColumns_of_leadingBlock_upper_det_ne_zero
        (A_hat k) hkm hk hupper (by simpa [hkm] using hdetLead k hk)
        hlowerPrev
    have hC : IsLeftInverse (k + 1) S C := by
      exact
        (isInverse_nonsingInv_of_det_ne_zero (k + 1) S
          (by simpa [S, hkm] using hdetLead k hk)).1
    have hCinf : ((k + 1 : ℕ) : ℝ) * infNorm C ^ 2 ≤ K k := by
      simpa [S, C, hkm] using
        infNorm_sq_budget_of_kappaInf_le_and_det_ne_zero
          (k + 1) (Nat.succ_pos k) S C (κ k) (K k)
          (by simpa [S, hkm] using hdetLead k hk)
          (by simpa [S, C, hkm] using hκ k hk)
          (by simpa [S, hkm] using hκbudget k hk)
    simpa [budget, S, C, hkm] using
      dim_mul_budget_sq_lt_trailingNorm2Sq_of_leadingBlock_leftInverse_infNorm_budget
        (A_hat k) hkm hk C hC hprefixSpan (K k) budget
        (hK k hk) hCinf (by simpa [budget] using hbudgetDual k hk)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_leadingBlock_det_ne_zero_kappaInf_selfNorm_normSqBudget
      fp hmn A b A_hat b_hat alpha κ K hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hbudgetNormSq hdetLead hK hκ hκbudget hbudgetDual
/-- Source-facing explicit compact-budget exact-minimizer certificate from
    nonsingular stored leading blocks and a visible inverse-∞ budget.

    This direct minimizer route removes the local `κ∞`/self-norm hypotheses
    from the previous wrapper.  The stored prefix lower-zero invariant and
    `det(S_k) ≠ 0` give a local prefix-span witness; the displayed inverse-∞
    budget and compact-update dual budget give the trailing-norm-square margin
    that implies the square-root Householder pivot budget. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_leadingBlock_det_ne_zero_invNorm_dualBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hCinf : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          infNorm
            (nonsingInv (k + 1)
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
  have hcStep : 0 ≤ cStep := by
    simpa [cStep] using
      storedQRCompactSequenceRelativeBudget_nonneg
        hmn fp A_hat b_hat alpha hm
  let v : ℕ → Fin m → ℝ := fun k =>
    if hk : k < n then
      householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    else 0
  let β : ℕ → ℝ := fun k => householderBetaSpec m (v k)
  have hStep : ∀ k, k < n →
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k (v k) (β k) (A_hat k) := by
    intro k hk
    simpa [v, β, hk] using hStepA k hk
  have hprefix :=
    fl_householderStoredPanel_sequence_prefix_lower_zero
      fp v β A_hat hStep
  have hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    let hkm : k + 1 ≤ m :=
      Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)
    let S : Fin (k + 1) → Fin (k + 1) → ℝ :=
      qrLeadingBlock (A_hat k) hkm hk
    let C : Fin (k + 1) → Fin (k + 1) → ℝ :=
      nonsingInv (k + 1) S
    let budget : ℝ :=
      householderCompactComponentBudget fp m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
        (householderBetaSpec m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
        (fun a => A_hat k a ⟨k, hk⟩)
        ⟨k, lt_of_lt_of_le hk hmn⟩
    have hupper : ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) hkm hk i j = 0 := by
      intro i j hji
      have hjk : j.val < k := by omega
      exact
        hprefix k (Nat.le_of_lt hk)
          (qrLeadingRow m k hkm i)
          (qrLeadingColumn n k hk j)
          (by simpa [qrLeadingColumn] using hjk)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
    have hlowerPrev : ∀ (i : Fin m) (j : Fin k), k ≤ i.val →
        A_hat k i (qrPreviousColumn n k hk j) = 0 := by
      intro i j hi
      exact
        hprefix k (Nat.le_of_lt hk) i (qrPreviousColumn n k hk j)
          (by simp [qrPreviousColumn])
          (lt_of_lt_of_le j.isLt hi)
    have hprefixSpan : qrPrefixSupportSpannedByPreviousColumns (A_hat k) hk :=
      qrPrefixSupportSpannedByPreviousColumns_of_leadingBlock_upper_det_ne_zero
        (A_hat k) hkm hk hupper (by simpa [hkm] using hdetLead k hk)
        hlowerPrev
    have hC : IsLeftInverse (k + 1) S C := by
      exact
        (isInverse_nonsingInv_of_det_ne_zero (k + 1) S
          (by simpa [S, hkm] using hdetLead k hk)).1
    have hCinf_local : ((k + 1 : ℕ) : ℝ) * infNorm C ^ 2 ≤ K k := by
      simpa [S, C, hkm] using hCinf k hk
    simpa [budget, S, C, hkm] using
      dim_mul_budget_sq_lt_trailingNorm2Sq_of_leadingBlock_leftInverse_infNorm_budget
        (A_hat k) hkm hk C hC hprefixSpan (K k) budget
        (hK k hk) hCinf_local (by simpa [budget] using hbudgetDual k hk)
  have hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    have hmargin :
        (m : ℝ) * budget ^ 2 <
          householderTrailingNorm2Sq m p
            (fun a => A_hat k a ⟨k, hk⟩) := by
      simpa [budget, beta, v, p] using hbudgetNormSq k hk
    simpa [budget, beta, v, p] using
      budget_lt_sqrt_householderTrailingNorm2Sq_of_dim_mul_budget_sq_lt_trailingNorm2Sq
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg hmargin
  have htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    simpa [budget, beta, v, p] using
      householderTrailingNorm2Sq_pos_of_nonneg_budget_lt_sqrt
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg (hbudgetSqrt k hk)
  change
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_of_signed_alpha_trailingNorm_pos_and_sqrt_budget
      fp hmn A b A_hat b_hat alpha cStep hcStep hm hγ
      hInitA hInitb hStepA hStepb hAlphaDef htrailingPos
      (fun k hk j => by
        simpa [cStep] using
          storedQRCompactSequenceRelativeBudget_column_bound
            hmn fp A_hat b_hat alpha hm k hk j)
      (fun k hk => by
        simpa [cStep] using
          storedQRCompactSequenceRelativeBudget_rhs_bound
            hmn fp A_hat b_hat alpha hm k hk)
      hbudgetSqrt
/-- Source-facing explicit compact-budget exact-minimizer certificate from
    diagonally dominant nonsingular stored leading blocks.

    This diagonal-dominance route specializes
    `..._leadingBlock_det_ne_zero_invNorm_dualBudget` by using the local
    Higham triangular inverse bound for `IsDiagDominantUpper` leading blocks
    to derive the inverse-∞ budget internally.  The compact-update dual budget
    remains visible as the quantitative smallness condition that supplies the
    Householder pivot margin. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_dualBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hKbound : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (2 ^ k *
            (1 / Finset.inf' Finset.univ
              ⟨(⟨k, Nat.lt_succ_self k⟩ : Fin (k + 1)), Finset.mem_univ _⟩
              (fun r : Fin (k + 1) =>
                |qrLeadingBlock (A_hat k)
                    (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk r r|))) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  refine
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_leadingBlock_det_ne_zero_invNorm_dualBudget
      fp hmn A b A_hat b_hat alpha K hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hK ?_ hbudgetDual
  intro k hk
  exact
    triInv_infNorm_sq_budget_of_diagDominantUpper_det_ne_zero (k + 1)
      (qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
      ⟨k, Nat.lt_succ_self k⟩
      (K k)
      (hDD k hk)
      (by simpa using hdetLead k hk)
      (by simpa using hKbound k hk)
/-- Source-facing diagonal-dominant exact-minimizer certificate with the
    auxiliary inverse budget chosen concretely.

    This removes the arbitrary `K_k` parameter from
    `..._diagDominant_leadingBlock_det_ne_zero_dualBudget` by choosing
    `K_k = 2 * D_k`, where `D_k` is the displayed diagonal-dominant triangular
    inverse budget.  The remaining hypothesis is the direct smallness condition
    `m * B_k^2 < 1 / (2D_k)` for the compact Householder component budget. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hbudgetConcrete : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 /
          (2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
              ⟨k, Nat.lt_succ_self k⟩)) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let K : ℕ → ℝ := fun k =>
    if hk : k < n then
      2 *
        diagDominantUpperInvBudgetExpr (k + 1)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          ⟨k, Nat.lt_succ_self k⟩
    else 1
  refine
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_dualBudget
      fp hmn A b A_hat b_hat alpha K hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hDD ?_ ?_ ?_
  · intro k hk
    have hDpos :
        0 <
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ :=
      diagDominantUpperInvBudgetExpr_pos (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        ⟨k, Nat.lt_succ_self k⟩
        (hDD k hk)
    simp [K, hk, hDpos]
  · intro k hk
    have hDnonneg :
        0 ≤
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ :=
      le_of_lt
        (diagDominantUpperInvBudgetExpr_pos (k + 1)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          ⟨k, Nat.lt_succ_self k⟩
          (hDD k hk))
    have hle :
        diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ ≤
          2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
              ⟨k, Nat.lt_succ_self k⟩ := by
      linarith
    simpa [K, hk, diagDominantUpperInvBudgetExpr] using hle
  · intro k hk
    simpa [K, hk] using hbudgetConcrete k hk
/-- Product-form companion to the source-facing concrete diagonal-dominant
    exact-minimizer route.

    This replaces the direct denominator hypothesis `m * B_k^2 < 1 / (2D_k)`
    by the product smallness condition `2D_k * (m * B_k^2) < 1`.  Positivity of
    `D_k` is derived locally from diagonal dominance. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualProductBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hbudgetProduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  refine
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualBudget
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hDD ?_
  intro k hk
  exact
    mul_sq_lt_inv_two_mul_of_two_mul_mul_sq_lt_one
      (diagDominantUpperInvBudgetExpr_pos (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        ⟨k, Nat.lt_succ_self k⟩
        (hDD k hk))
      (hbudgetProduct k hk)
/-- Source-facing stored-loop sequence-budget version of the product-form
    diagonal-dominant exact-minimizer route.

    This replaces the raw compact component product condition by the displayed
    stored QR sequence budget times the current pivot-column norm.  The local
    compact component bound then supplies the product condition consumed by
    `..._concreteDualProductBudget`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualProductSequenceBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hbudgetSequenceProduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  refine
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualProductBudget
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hDD ?_
  intro k hk
  let D :=
    diagDominantUpperInvBudgetExpr (k + 1)
      (qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
      ⟨k, Nat.lt_succ_self k⟩
  let budget :=
    householderCompactComponentBudget fp m
      (householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
      (householderBetaSpec m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
      (fun a => A_hat k a ⟨k, hk⟩)
      ⟨k, lt_of_lt_of_le hk hmn⟩
  let B :=
    storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
      vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)
  have hD_nonneg : 0 ≤ D := by
    exact le_of_lt
      (diagDominantUpperInvBudgetExpr_pos (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        ⟨k, Nat.lt_succ_self k⟩
        (hDD k hk))
  have hm_nonneg : 0 ≤ (m : ℝ) := Nat.cast_nonneg m
  have hbudget_nonneg : 0 ≤ budget := by
    simpa [budget] using
      householderCompactComponentBudget_nonneg fp m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
        (householderBetaSpec m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
        (fun a => A_hat k a ⟨k, hk⟩) hm
        ⟨k, lt_of_lt_of_le hk hmn⟩
  have hbudget_le : budget ≤ B := by
    simpa [budget, B] using
      storedQRCompactPivotBudget_le_sequence_column_norm
        hmn fp A_hat b_hat alpha hm k hk
  exact
    two_mul_mul_sq_lt_one_of_nonneg_le
      hD_nonneg hm_nonneg hbudget_nonneg hbudget_le
      (by simpa [D, B] using hbudgetSequenceProduct k hk)
/-- Universal-form route elimination for the Cox--Higham row-budget split:
    upper triangular nonsingular leading blocks, positive active-block mass,
    and an off-diagonal row-growth budget still do not imply the matching
    diagonal lower-bound field.

    The same `[[1,2],[0,1]]` witness has a valid row budget `2` for the strict
    upper entry, but the first displayed diagonal has magnitude `1`.  Therefore
    the row-growth upper budget and the diagonal lower-bound/nonbreakdown field
    are independent source obligations in the current QR bottleneck. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_offdiagBudget_implies_rowBudget_diag :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (rowBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k) →
      (∀ k (hk : k < 2), ∀ i : Fin (k + 1), i.val < k →
        rowBudget k ≤
          |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i i|)) := by
  intro h
  let A_hat : ℕ → Fin 2 → Fin 2 → ℝ := fun _ => diagDominanceCounterexample2
  let rowBudget : ℕ → ℝ := fun _ => 2
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    have hk_cases : k = 0 ∨ k = 1 := by omega
    rcases hk_cases with rfl | rfl
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (1 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          diagDominanceCounterexample2]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
    · simpa [A_hat] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, rowBudget, qrLeadingBlock, qrLeadingRow,
            qrLeadingColumn, diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hdiag := h A_hat rowBudget hupper hdetLead hactive hoffdiagBudget
  have hbad :=
    hdiag 1 (by norm_num) (⟨0, by norm_num⟩ : Fin (1 + 1)) (by norm_num)
  norm_num [A_hat, rowBudget, qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
    diagDominanceCounterexample2] at hbad
/-- Norm-square nonbreakdown from the concrete diagonal-dominant product-sequence
    route.

This factors the reusable margin hidden inside the concrete-dual stored-QR
solver theorem: local diagonal dominance supplies the displayed triangular
inverse budget, while the finite sequence product budget controls the actual
compact Householder pivot budget through
`storedQRCompactPivotBudget_le_sequence_column_norm`. -/
theorem storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_diagDominant_concreteDualProductSequenceBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hbudgetSequenceProduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
  classical
  let v : ℕ → Fin m → ℝ := fun k =>
    if hk : k < n then
      householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    else 0
  let β : ℕ → ℝ := fun k => householderBetaSpec m (v k)
  have hStep : ∀ k, k < n →
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k (v k) (β k) (A_hat k) := by
    intro k hk
    simpa [v, β, hk] using hStepA k hk
  have hprefixLower :=
    fl_householderStoredPanel_sequence_prefix_lower_zero
      fp v β A_hat hStep
  intro k hk
  let hkm : k + 1 ≤ m :=
    Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)
  let S : Fin (k + 1) → Fin (k + 1) → ℝ :=
    qrLeadingBlock (A_hat k) hkm hk
  let C : Fin (k + 1) → Fin (k + 1) → ℝ :=
    nonsingInv (k + 1) S
  let D : ℝ :=
    diagDominantUpperInvBudgetExpr (k + 1) S
      ⟨k, Nat.lt_succ_self k⟩
  let budget : ℝ :=
    householderCompactComponentBudget fp m
      (householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
      (householderBetaSpec m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
      (fun a => A_hat k a ⟨k, hk⟩)
      ⟨k, lt_of_lt_of_le hk hmn⟩
  let B : ℝ :=
    storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
      vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)
  have hupper : ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) hkm hk i j = 0 := by
    intro i j hji
    have hjk : j.val < k := by omega
    exact
      hprefixLower k (Nat.le_of_lt hk)
        (qrLeadingRow m k hkm i)
        (qrLeadingColumn n k hk j)
        (by simpa [qrLeadingColumn] using hjk)
        (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hlowerPrev : ∀ (i : Fin m) (j : Fin k), k ≤ i.val →
      A_hat k i (qrPreviousColumn n k hk j) = 0 := by
    intro i j hi
    exact
      hprefixLower k (Nat.le_of_lt hk) i (qrPreviousColumn n k hk j)
        (by simp [qrPreviousColumn])
        (lt_of_lt_of_le j.isLt hi)
  have hprefixSpan : qrPrefixSupportSpannedByPreviousColumns (A_hat k) hk :=
    qrPrefixSupportSpannedByPreviousColumns_of_leadingBlock_upper_det_ne_zero
      (A_hat k) hkm hk hupper (by simpa [S, hkm] using hdetLead k hk)
      hlowerPrev
  have hC : IsLeftInverse (k + 1) S C := by
    exact
      (isInverse_nonsingInv_of_det_ne_zero (k + 1) S
        (by simpa [S, hkm] using hdetLead k hk)).1
  have hCinf : ((k + 1 : ℕ) : ℝ) * infNorm C ^ 2 ≤ 2 * D := by
    simpa [S, C, D, hkm] using
      triInv_infNorm_sq_budget_of_diagDominantUpper_det_ne_zero_twice_budget
        (k + 1) S ⟨k, Nat.lt_succ_self k⟩ (hDD k hk)
        (by simpa [S, hkm] using hdetLead k hk)
  have hDpos : 0 < D := by
    simpa [D, S, hkm] using
      diagDominantUpperInvBudgetExpr_pos (k + 1) S
        ⟨k, Nat.lt_succ_self k⟩ (hDD k hk)
  have hbudget_nonneg : 0 ≤ budget := by
    simpa [budget] using
      householderCompactComponentBudget_nonneg fp m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
        (householderBetaSpec m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
        (fun a => A_hat k a ⟨k, hk⟩) hm
        ⟨k, lt_of_lt_of_le hk hmn⟩
  have hbudget_le : budget ≤ B := by
    simpa [budget, B] using
      storedQRCompactPivotBudget_le_sequence_column_norm
        hmn fp A_hat b_hat alpha hm k hk
  have hsmall_local :
      2 * D * ((m : ℝ) * budget ^ 2) < 1 := by
    exact
      two_mul_mul_sq_lt_one_of_nonneg_le
        (le_of_lt hDpos) (Nat.cast_nonneg m) hbudget_nonneg hbudget_le
        (by simpa [D, B, S, hkm] using hbudgetSequenceProduct k hk)
  have hdual : (m : ℝ) * budget ^ 2 < 1 / (2 * D) :=
    mul_sq_lt_inv_two_mul_of_two_mul_mul_sq_lt_one hDpos hsmall_local
  have hKpos : 0 < 2 * D := by nlinarith
  simpa [budget, S, C, D, hkm] using
    dim_mul_budget_sq_lt_trailingNorm2Sq_of_leadingBlock_leftInverse_infNorm_budget
      (A_hat k) hkm hk C hC hprefixSpan (2 * D) budget
      hKpos hCinf hdual
/-- The stored recurrence and signed-alpha nonbreakdown budget supply the
    previous diagonal entries in each displayed leading block.

    Compared with
    `StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diag_offdiag_product`,
    this theorem no longer asks for all displayed local diagonals as one
    hypothesis.  The already-written prefix diagonals come from the repository's
    prefix-diagonal stored-QR theorem; only the current pivot nonzero condition
    remains visible. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_pivot_sqrtBudget_offdiag_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)))
    (hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    simpa [budget, beta, v, p] using
      householderTrailingNorm2Sq_pos_of_nonneg_budget_lt_sqrt
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg (hbudgetSqrt k hk)
  have hdiagPrefix :
      ∀ k (hk : k ≤ n) (i : Fin k),
        A_hat k
          ⟨i.val, lt_of_lt_of_le i.isLt (le_trans hk hmn)⟩
          ⟨i.val, lt_of_lt_of_le i.isLt hk⟩ ≠ 0 :=
    fl_householderStoredTrailingPanel_sequence_prefix_diag_nonzero_of_signed_alpha_trailingNorm_pos_sqrt_budget
      fp hmn A_hat alpha hm hStepA hAlphaDef htrailingPos hbudgetSqrt
  have hdiag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i ≠ 0 := by
    intro k hk i
    by_cases hi : i.val < k
    · let ip : Fin k := ⟨i.val, hi⟩
      have hp := hdiagPrefix k (Nat.le_of_lt hk) ip
      simpa [qrLeadingBlock, qrLeadingRow, qrLeadingColumn, ip] using hp
    · have hi_eq : i.val = k := by omega
      simpa [qrLeadingBlock, qrLeadingRow, qrLeadingColumn, hi_eq] using
        hdiagPivot k hk
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diag_offdiag_product
      hmn fp A_hat b_hat alpha hStepA hdiag hoffdiag hproduct
/-- The stored recurrence, signed-alpha nonbreakdown budget, and nonsingular
    displayed leading blocks supply the diagonal part of
    `StoredQRSourceOffDiagonalControl`.

    Compared with
    `StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_pivot_sqrtBudget_offdiag_product`,
    this theorem no longer asks for the current pivot nonzero condition
    directly.  The repository's structured no-pivot determinant bridge derives
    that current pivot condition from the local leading-block determinant and
    the stored lower-zero shape.  Thus the remaining visible obligations are
    local leading-block nonsingularity, square-root nonbreakdown, row-wise
    off-diagonal domination, and compact-product smallness. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_sqrtBudget_offdiag_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0 :=
    fl_householderStoredTrailingPanel_sequence_current_pivot_ne_zero_of_leadingBlock_det_ne_zero
      fp hmn A_hat alpha hStepA hdetLead
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_pivot_sqrtBudget_offdiag_product
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetSqrt
      hdiagPivot hoffdiag hproduct
/-- Stored trailing Householder QR source control from a dimensioned
    trailing-norm-square budget.

    This is the norm-square version of
    `..._leadingBlock_det_ne_zero_sqrtBudget_offdiag_product`: the repository
    bridge
    `budget_lt_sqrt_householderTrailingNorm2Sq_of_dim_mul_budget_sq_lt_trailingNorm2Sq`
    converts the dimensioned margin `m * budget^2 < ||active column||_2^2`
    into the square-root nonbreakdown budget consumed by the existing
    source-shaped off-diagonal-control theorem. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_offdiag_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)) := by
    intro k hk
    let p : Fin m := ⟨k, lt_of_lt_of_le hk hmn⟩
    let v :=
      householderTrailingActiveVector m p
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    let beta := householderBetaSpec m v
    let budget :=
      householderCompactComponentBudget fp m v beta
        (fun a => A_hat k a ⟨k, hk⟩) p
    have hbudget_nonneg : 0 ≤ budget := by
      simpa [budget, beta, v, p] using
        householderCompactComponentBudget_nonneg fp m v beta
          (fun a => A_hat k a ⟨k, hk⟩) hm p
    simpa [budget, beta, v, p] using
      budget_lt_sqrt_householderTrailingNorm2Sq_of_dim_mul_budget_sq_lt_trailingNorm2Sq
        m p (fun a => A_hat k a ⟨k, hk⟩) budget
        hbudget_nonneg (hbudgetNormSq k hk)
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_sqrtBudget_offdiag_product
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetSqrt
      hdetLead hoffdiag hproduct
/-- Stored trailing Householder QR source control from row-growth budgets and
    diagonal lower bounds.

    This decomposes the row-wise off-diagonal domination field into two
    source-facing obligations: a row budget controls each displayed upper
    off-diagonal entry, and that row budget is no larger than the corresponding
    displayed diagonal magnitude.  It is designed for the Cox--Higham
    pivoted/sorted route, where the upper bound comes from row-growth and the
    lower bound must come from a nonbreakdown/diagonal-growth invariant. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_offdiag_product
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
      hdetLead ?_ hproduct
  intro k hk i j hij
  exact (hoffdiagBudget k hk i j hij).trans (hrowBudget_diag k hk i)
/-- Stored trailing Householder QR source control from row-growth budgets and
    diagonal lower bounds on only the rows that have displayed upper entries.

    This is a sharper version of
    `..._normSqBudget_rowBudget_product`: the row `i = k` of the displayed
    `(k+1) × (k+1)` leading block has no strict upper off-diagonal entry, so the
    row-budget-to-diagonal comparison is needed only for `i < k`. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product_of_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_offdiag_product
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
      hdetLead ?_ hproduct
  intro k hk i j hij
  have hj_le_k : j.val ≤ k := Nat.le_of_lt_succ j.isLt
  have hi_lt_k : i.val < k := lt_of_lt_of_le hij hj_le_k
  exact (hoffdiagBudget k hk i j hij).trans
    (hrowBudget_diag k hk i hi_lt_k)
/-- Canonical finite row budget for strict upper entries of a displayed
    leading block.

For rows `i < k`, this is the maximum absolute value among the strict upper
entries `(i,j)`, `i < j`, of the `(k+1) × (k+1)` displayed leading block.  The
last row has no strict upper entry, so its value is irrelevant to
`StoredQRDisplayedRowBudgetControl` and is set to zero. -/
noncomputable def qrLeadingStrictUpperRowMaxBudget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (k : ℕ) (hk : k < n) (i : Fin (k + 1)) : ℝ :=
  if hi : i.val < k then
    Finset.sup'
      (Finset.univ.filter (fun j : Fin (k + 1) => i.val < j.val))
      (by
        refine ⟨⟨k, Nat.lt_succ_self k⟩, ?_⟩
        simp only [Finset.mem_filter, Finset.mem_univ, true_and]
        exact hi)
      (fun j =>
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j|)
  else 0
theorem qrLeadingStrictUpperRowMaxBudget_entry_le
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    {k : ℕ} (hk : k < n) {i j : Fin (k + 1)}
    (hij : i.val < j.val) :
    |qrLeadingBlock (A_hat k)
      (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
    qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i := by
  classical
  have hi_lt_k : i.val < k :=
    lt_of_lt_of_le hij (Nat.le_of_lt_succ j.isLt)
  rw [qrLeadingStrictUpperRowMaxBudget, dif_pos hi_lt_k]
  have hmem :
      j ∈ Finset.univ.filter (fun j : Fin (k + 1) => i.val < j.val) := by
    simp only [Finset.mem_filter, Finset.mem_univ, true_and]
    exact hij
  exact
    Finset.le_sup'
      (s := Finset.univ.filter (fun j : Fin (k + 1) => i.val < j.val))
      (f := fun j =>
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j|)
      hmem
theorem qrLeadingStrictUpperRowMaxBudget_le_diag_of_offdiag
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    {k : ℕ} (hk : k < n) {i : Fin (k + 1)}
    (hi : i.val < k)
    (hoffdiag : ∀ j : Fin (k + 1), i.val < j.val →
      |qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
      |qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|) :
    qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤
      |qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| := by
  classical
  rw [qrLeadingStrictUpperRowMaxBudget, dif_pos hi]
  apply Finset.sup'_le
  intro j hj
  have hij : i.val < j.val := by
    exact (Finset.mem_filter.mp hj).2
  exact hoffdiag j hij
/-- Finite scalar defect for the route-1 row-max/diagonal contraction field.

This is the maximum over all displayed rows that have strict upper entries of
`rowMax - |diag|`.  It is zero when no such displayed row exists.  Thus the
single scalar condition `storedQRRowMaxDiagDefectBudget <= 0` is exactly the
finite check needed for the `ρ = 1` row-max contraction branch. -/
noncomputable def storedQRRowMaxDiagDefectBudget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ) : ℝ :=
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  if hs : s.Nonempty then
    Finset.sup' s hs (fun p =>
      if hi : p.2.val < p.1.val then
        let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
        qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i -
          |qrLeadingBlock (A_hat p.1.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn)) p.1.isLt
            i i|
      else 0)
  else 0
/-- Each displayed row-max/diagonal defect is bounded by the finite scalar
    defect budget. -/
theorem storedQRRowMaxDiagDefect_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    {k : ℕ} (hk : k < n) (i : Fin (k + 1)) (hi : i.val < k) :
    qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i -
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| ≤
      storedQRRowMaxDiagDefectBudget hmn A_hat := by
  classical
  let p : Fin n × Fin n := (⟨k, hk⟩, ⟨i.val, lt_trans hi hk⟩)
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  have hmem : p ∈ s := by
    simp [s, p, hi]
  have hs : s.Nonempty := ⟨p, hmem⟩
  unfold storedQRRowMaxDiagDefectBudget
  rw [dif_pos hs]
  have hsup :
      (if hp : p.2.val < p.1.val then
        let i' : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hp⟩
        qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i' -
          |qrLeadingBlock (A_hat p.1.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn)) p.1.isLt
            i' i'|
      else 0) ≤
        Finset.sup' s hs (fun p =>
          if hi : p.2.val < p.1.val then
            let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
            qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i -
              |qrLeadingBlock (A_hat p.1.val)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
                p.1.isLt i i|
          else 0) :=
    Finset.le_sup' (s := s)
      (f := fun p =>
        if hi : p.2.val < p.1.val then
          let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
          qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i -
            |qrLeadingBlock (A_hat p.1.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
              p.1.isLt i i|
        else 0)
      hmem
  convert hsup using 1
  simp [p, hi]
/-- Local diagonal dominance makes the canonical scalar row-max/diagonal defect
    nonpositive.

This bridges the diagonal-dominant route and the row-max defect route: if every
displayed leading block is `IsDiagDominantUpper`, then every strict-upper row
maximum is bounded by the matching displayed diagonal, so the finite maximum of
`rowMax - |diag|` is at most zero. -/
theorem storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) :
    storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0 := by
  classical
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  unfold storedQRRowMaxDiagDefectBudget
  by_cases hs : s.Nonempty
  · rw [dif_pos hs]
    apply Finset.sup'_le
    intro p hp
    by_cases hi : p.2.val < p.1.val
    · let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
      have hrow :
          qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i ≤
            |qrLeadingBlock (A_hat p.1.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
              p.1.isLt i i| := by
        refine
          qrLeadingStrictUpperRowMaxBudget_le_diag_of_offdiag
            hmn A_hat p.1.isLt hi ?_
        intro j hij
        exact (hDD p.1.val p.1.isLt).2.2 i j hij
      have hterm :
          qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i -
              |qrLeadingBlock (A_hat p.1.val)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
                p.1.isLt i i| ≤ 0 := by
        linarith
      simpa [hi, i] using hterm
    · simp [hi]
  · rw [dif_neg hs]
/-- Finite scalar defect for the uniform Cox--Higham stage-budget diagonal
    lower-bound field.

This is the maximum over displayed rows that have strict upper entries of
`stageBudget k - |S_k(i,i)|`.  Thus the single scalar condition
`storedQRStageDiagLowerDefectBudget <= 0` is exactly the finite check needed
to discharge the active/prefix route's diagonal lower-bound hypothesis
`stageBudget k <= |S_k(i,i)|` for rows `i < k`. -/
noncomputable def storedQRStageDiagLowerDefectBudget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ) : ℝ :=
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  if hs : s.Nonempty then
    Finset.sup' s hs (fun p =>
      if hi : p.2.val < p.1.val then
        let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
        stageBudget p.1.val -
          |qrLeadingBlock (A_hat p.1.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn)) p.1.isLt
            i i|
      else 0)
  else 0
/-- Each displayed stage-budget/diagonal defect is bounded by the finite scalar
    diagonal-lower defect budget. -/
theorem storedQRStageDiagLowerDefect_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    {k : ℕ} (hk : k < n) (i : Fin (k + 1)) (hi : i.val < k) :
    stageBudget k -
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| ≤
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget := by
  classical
  let p : Fin n × Fin n := (⟨k, hk⟩, ⟨i.val, lt_trans hi hk⟩)
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  have hmem : p ∈ s := by
    simp [s, p, hi]
  have hs : s.Nonempty := ⟨p, hmem⟩
  unfold storedQRStageDiagLowerDefectBudget
  rw [dif_pos hs]
  have hsup :
      (if hp : p.2.val < p.1.val then
        let i' : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hp⟩
        stageBudget p.1.val -
          |qrLeadingBlock (A_hat p.1.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
            p.1.isLt i' i'|
      else 0) ≤
        Finset.sup' s hs (fun p =>
          if hi : p.2.val < p.1.val then
            let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
            stageBudget p.1.val -
              |qrLeadingBlock (A_hat p.1.val)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
                p.1.isLt i i|
          else 0) :=
    Finset.le_sup' (s := s)
      (f := fun p =>
        if hi : p.2.val < p.1.val then
          let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
          stageBudget p.1.val -
            |qrLeadingBlock (A_hat p.1.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
              p.1.isLt i i|
        else 0)
      hmem
  convert hsup using 1
  simp [p, hi]
/-- A nonpositive finite stage-diagonal defect budget supplies the diagonal
    lower-bound field for all displayed rows with strict upper entries. -/
theorem storedQRStageBudget_le_diag_of_stageDiagLowerDefectBudget_nonpos
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hdefect : storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0) :
    ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| := by
  intro k hk i hi
  have hle :=
    storedQRStageDiagLowerDefect_le_budget hmn A_hat stageBudget hk i hi
  have hnonpos :
      stageBudget k -
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| ≤ 0 :=
    hle.trans hdefect
  linarith
/-- Pointwise diagonal lower bounds supply the scalar finite stage-diagonal
    defect condition.

This is the converse packaging theorem for
`storedQRStageBudget_le_diag_of_stageDiagLowerDefectBudget_nonpos`: the
finite scalar maximum is nonpositive exactly when every displayed
stage-budget/diagonal defect is nonpositive. -/
theorem storedQRStageDiagLowerDefectBudget_nonpos_of_stageBudget_le_diag
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hdiag : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|) :
    storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 := by
  classical
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  unfold storedQRStageDiagLowerDefectBudget
  by_cases hs : s.Nonempty
  · rw [dif_pos hs]
    apply Finset.sup'_le
    intro p hp
    by_cases hi : p.2.val < p.1.val
    · let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
      have hdiag_pi := hdiag p.1.val p.1.isLt i hi
      have hterm :
          stageBudget p.1.val -
            |qrLeadingBlock (A_hat p.1.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
              p.1.isLt i i| ≤ 0 := by
        linarith
      simpa [hi, i] using hterm
    · simp [hi]
  · rw [dif_neg hs]
/-- Row-max scalar control also supplies the stage-diagonal scalar condition
    when the uniform stage budget is no larger than the displayed row maximum.

This adapter links the row-max branch and the active/prefix stage-budget
branch without re-opening the finite-maximum proof: if `rowMax - |diag|` is
nonpositive everywhere and `stageBudget k <= rowMax(k,i)` on each displayed
off-diagonal row, then `stageBudget k - |diag|` is nonpositive everywhere. -/
theorem storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageBudget_le_rowMax
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) :
    storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 := by
  classical
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  unfold storedQRStageDiagLowerDefectBudget
  by_cases hs : s.Nonempty
  · rw [dif_pos hs]
    apply Finset.sup'_le
    intro p hp
    by_cases hi : p.2.val < p.1.val
    · let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
      have hstage :
          stageBudget p.1.val ≤
            qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i :=
        hstage_le_rowMax p.1.val p.1.isLt i hi
      have hrow_le :=
        storedQRRowMaxDiagDefect_le_budget hmn A_hat p.1.isLt i hi
      have hterm :
          stageBudget p.1.val -
              |qrLeadingBlock (A_hat p.1.val)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le p.1.isLt hmn))
                p.1.isLt i i| ≤ 0 := by
        linarith
      simpa [hi, i] using hterm
    · simp [hi]
  · rw [dif_neg hs]
/-- Local diagonal dominance gives the stage-diagonal scalar condition whenever
    the uniform stage budget is bounded by the displayed row maximum.

This is the diagonal-dominance specialization of
`storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageBudget_le_rowMax`. -/
theorem storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageBudget_le_rowMax
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) :
    storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
  storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageBudget_le_rowMax
    hmn A_hat stageBudget
    (storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant hmn A_hat hDD)
    hstage_le_rowMax
/-- Finite scalar defect for the active/prefix comparison
    `stageBudget <= rowMax`.

This is the maximum over displayed rows that have strict upper entries of
`stageBudget k - rowMax(k,i)`.  Its nonpositivity is exactly the finite scalar
form of the displayed-row comparison consumed by the row-max bridge. -/
noncomputable def storedQRStageRowMaxComparisonDefectBudget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ) : ℝ :=
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  if hs : s.Nonempty then
    Finset.sup' s hs (fun p =>
      if hi : p.2.val < p.1.val then
        let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
        stageBudget p.1.val -
          qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i
      else 0)
  else 0
/-- Each displayed stage-budget/row-max comparison defect is bounded by the
    finite scalar comparison defect budget. -/
theorem storedQRStageRowMaxComparisonDefect_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    {k : ℕ} (hk : k < n) (i : Fin (k + 1)) (hi : i.val < k) :
    stageBudget k - qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget := by
  classical
  let p : Fin n × Fin n := (⟨k, hk⟩, ⟨i.val, lt_trans hi hk⟩)
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  have hmem : p ∈ s := by
    simp [s, p, hi]
  have hs : s.Nonempty := ⟨p, hmem⟩
  unfold storedQRStageRowMaxComparisonDefectBudget
  rw [dif_pos hs]
  have hsup :
      (if hp : p.2.val < p.1.val then
        let i' : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hp⟩
        stageBudget p.1.val -
          qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i'
      else 0) ≤
        Finset.sup' s hs (fun p =>
          if hi : p.2.val < p.1.val then
            let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
            stageBudget p.1.val -
              qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i
          else 0) :=
    Finset.le_sup' (s := s)
      (f := fun p =>
        if hi : p.2.val < p.1.val then
          let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
          stageBudget p.1.val -
            qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i
        else 0)
      hmem
  convert hsup using 1
  simp [p, hi]
/-- A nonpositive finite stage-budget/row-max comparison defect supplies the
    displayed comparison field used by the row-max bridge. -/
theorem storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hdefect :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0) :
    ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i := by
  intro k hk i hi
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget hmn A_hat stageBudget hk i hi
  have hnonpos :
      stageBudget k -
          qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤ 0 :=
    hle.trans hdefect
  linarith
/-- Pointwise displayed stage-budget/row-max comparisons supply the scalar
    finite comparison defect condition. -/
theorem storedQRStageRowMaxComparisonDefectBudget_nonpos_of_stageBudget_le_rowMax
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) :
    storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0 := by
  classical
  let s : Finset (Fin n × Fin n) :=
    Finset.univ.filter (fun p : Fin n × Fin n => p.2.val < p.1.val)
  unfold storedQRStageRowMaxComparisonDefectBudget
  by_cases hs : s.Nonempty
  · rw [dif_pos hs]
    apply Finset.sup'_le
    intro p hp
    by_cases hi : p.2.val < p.1.val
    · let i : Fin (p.1.val + 1) := ⟨p.2.val, Nat.lt_succ_of_lt hi⟩
      have hcomparison :
          stageBudget p.1.val ≤
            qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i :=
        hstage_le_rowMax p.1.val p.1.isLt i hi
      have hterm :
          stageBudget p.1.val -
              qrLeadingStrictUpperRowMaxBudget hmn A_hat p.1.val p.1.isLt i ≤
            0 := by
        linarith
      simpa [hi, i] using hterm
    · simp [hi]
  · rw [dif_neg hs]
/-- Scalar row-max defect plus scalar stage-budget/row-max comparison imply the
    scalar stage-diagonal condition. -/
theorem storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageRowMaxComparisonDefectBudget_nonpos
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0) :
    storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
  storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageBudget_le_rowMax
    hmn A_hat stageBudget hrowDefect
    (storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
      hmn A_hat stageBudget hcomparison)
/-- Local diagonal dominance plus scalar stage-budget/row-max comparison imply
    the scalar stage-diagonal condition. -/
theorem storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageRowMaxComparisonDefectBudget_nonpos
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0) :
    storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
  storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageRowMaxComparisonDefectBudget_nonpos
    hmn A_hat stageBudget
    (storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant hmn A_hat hDD)
    hcomparison
/-- Stored trailing Householder QR source control from Cox--Higham stage
    budgets and diagonal lower bounds.

    This is the solver-facing composition of the row-growth bridge in
    `HouseholderQR.lean` with the row-budget decomposition above.  The theorem
    discharges the displayed upper off-diagonal field from per-entry stage
    budgets, while keeping the genuinely source-specific obligations visible:
    local leading-block nonsingularity, norm-square nonbreakdown, row-budget
    domination by displayed diagonal magnitudes, and compact-product smallness.
    The stage recurrence is parameterized by the panel vectors `v, beta`; in
    concrete signed-pivot uses these are instantiated by the chosen loop. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (v : ℕ → Fin m → ℝ) (beta : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStageStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t (v t) (beta t) (A_hat t))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m (householder m (v t) (beta t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m (v t) (beta t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m (householder m (v t) (beta t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i :=
    fl_householderStoredPanel_sequence_leadingBlock_offdiag_budget_of_exact_stage_budgets_factor
      hmn fp A_hat v beta c rowBudget entryBudget hm hStageStep
      hinit hpivot hbudget hexact hrowBudget
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product
      hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hoffdiagBudget hrowBudget_diag hproduct
/-- Source-shaped off-diagonal-control certificate from Cox--Higham stage
    budgets, with diagonal lower bounds required only on rows that have
    displayed upper entries.

    This is the stage-budget version of
    `..._rowBudget_product_of_offdiag_rows`: the QR row-growth bridge is
    unchanged, but the downstream diagonal comparison is restricted to
    `i.val < k`. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product_of_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (v : ℕ → Fin m → ℝ) (beta : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStageStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t (v t) (beta t) (A_hat t))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m (householder m (v t) (beta t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m (v t) (beta t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m (householder m (v t) (beta t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i :=
    fl_householderStoredPanel_sequence_leadingBlock_offdiag_budget_of_exact_stage_budgets_factor
      hmn fp A_hat v beta c rowBudget entryBudget hm hStageStep
      hinit hpivot hbudget hexact hrowBudget
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product_of_offdiag_rows
      hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hoffdiagBudget hrowBudget_diag hproduct
/-- Source-shaped off-diagonal-control certificate from Cox--Higham stage
    budgets for the concrete signed stored-QR stages.

    This specializes
    `StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product`
    by instantiating the abstract stage vectors with
    `storedQRSignedStageVector` and `storedQRSignedStageBeta`.  It is still a
    scoped bottleneck dependency: the Cox--Higham stage-budget recurrence,
    pivot-column zeroing equations, row-budget/diagonal domination, local
    nonsingularity, and compact-product smallness remain visible fields. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m
              (householder m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hSignedStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t
          (storedQRSignedStageVector hmn A_hat alpha t)
          (storedQRSignedStageBeta hmn A_hat alpha t)
          (A_hat t) := by
    intro t ht
    simpa [storedQRSignedStageVector, storedQRSignedStageBeta, ht]
      using hStepA t ht
  have hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i :=
    fl_householderStoredPanel_sequence_leadingBlock_offdiag_budget_of_signed_stage_budgets_factor
      hmn fp A_hat alpha c rowBudget entryBudget hm hSignedStep
      hinit hpivot hbudget hexact hrowBudget
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product
      hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hoffdiagBudget hrowBudget_diag hproduct
/-- Source-shaped off-diagonal-control certificate from concrete signed-stage
    budgets, with diagonal lower bounds restricted to rows with displayed
    upper entries. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m
              (householder m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hSignedStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t
          (storedQRSignedStageVector hmn A_hat alpha t)
          (storedQRSignedStageBeta hmn A_hat alpha t)
          (A_hat t) := by
    intro t ht
    simpa [storedQRSignedStageVector, storedQRSignedStageBeta, ht]
      using hStepA t ht
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product_of_offdiag_rows
      hmn fp A_hat b_hat alpha
      (storedQRSignedStageVector hmn A_hat alpha)
      (storedQRSignedStageBeta hmn A_hat alpha)
      c rowBudget entryBudget hm hStepA hSignedStep hAlphaDef hbudgetNormSq
      hdetLead hinit hpivot hbudget hexact hrowBudget hrowBudget_diag hproduct
/-- Source-shaped off-diagonal control from signed-stage budgets, with
    pivot-column zeroing derived from the norm-square nonbreakdown budget.

    Compared with
    `StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product`,
    this theorem removes the explicit `hpivot` field.  The only remaining
    Cox--Higham fields are the stage-budget recurrence, exact same-reflector
    bounds, row-budget/diagonal domination, determinant nonbreakdown, and
    compact-product smallness. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product
      hmn fp A_hat b_hat alpha c rowBudget entryBudget hm hStepA
      hAlphaDef hbudgetNormSq hdetLead hinit
      (storedQRSignedStage_pivot_zeroing_field_of_normSqBudget
        hmn fp A_hat alpha hAlphaDef hbudgetNormSq)
      hbudget hexact hrowBudget hrowBudget_diag hproduct
/-- Source-shaped off-diagonal control from signed-stage budgets, with
    pivot-column zeroing derived from the norm-square nonbreakdown budget and
    diagonal lower bounds restricted to rows with displayed upper entries. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_offdiag_rows
      hmn fp A_hat b_hat alpha c rowBudget entryBudget hm hStepA
      hAlphaDef hbudgetNormSq hdetLead hinit
      (storedQRSignedStage_pivot_zeroing_field_of_normSqBudget
        hmn fp A_hat alpha hAlphaDef hbudgetNormSq)
      hbudget hexact hrowBudget hrowBudget_diag hproduct
/-- Source-shaped off-diagonal control from a single monotone signed-stage
    Cox--Higham budget sequence.

    This specializes the row-budget handoff by taking every entry budget at
    stage `t` to be `stageBudget t` and every displayed row budget for the
    `k`th leading block to be `stageBudget k`.  Monotonicity then supplies the
    terminal row-budget domination field because
    `qrLeadingOffdiagStop j <= k`. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget
      hmn fp A_hat b_hat alpha c
      (fun k _hk _i => stageBudget k)
      (fun _k _hk _i _j _hij t => stageBudget t)
      hm hStepA hAlphaDef hbudgetNormSq hdetLead hinit hbudget hexact
      (fun k _hk _i j _hij =>
        hBudget_mono (qrLeadingOffdiagStop j) k (qrLeadingOffdiagStop_le j))
      hBudget_diag hproduct
/-- Uniform signed-stage source-control handoff with the diagonal lower-bound
    field restricted to rows that have displayed upper entries. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget_offdiag_rows
      hmn fp A_hat b_hat alpha c
      (fun k _hk _i => stageBudget k)
      (fun _k _hk _i _j _hij t => stageBudget t)
      hm hStepA hAlphaDef hbudgetNormSq hdetLead hinit hbudget hexact
      (fun k _hk _i j _hij =>
        hBudget_mono (qrLeadingOffdiagStop j) k (qrLeadingOffdiagStop_le j))
      hBudget_diag hproduct
/-- Source-shaped off-diagonal control from a single monotone signed-stage
    Cox--Higham budget sequence, deriving the exact-reflector field from
    concrete signed-stage row/column bounds.

    This composes
    `storedQRSignedStage_exact_same_reflector_bound_of_prefix_or_active_stage_bounds`
    with the uniform-budget source-control handoff.  It removes the abstract
    `hexact` hypothesis from the previous theorem.  The remaining hypotheses
    are source-visible: stage entry bounds, pivot maximality, norm-square
    nonbreakdown, diagonal lower bounds, and compact-product smallness. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          |A_hat t
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
            stageBudget t)
    (hstageColBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ r : Fin m, t ≤ r.val →
          |A_hat t r (qrLeadingColumn n k hk j)| ≤ stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget
      hmn fp A_hat b_hat alpha
      (coxHighamActiveRowGrowthFactor m) stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hbudget
      (fun k hk i j hij t ht => by
        let row : Fin m :=
          qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i
        let col : Fin n := qrLeadingColumn n k hk j
        have ht_le_k : t ≤ k := by
          exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
        have htn : t < n := lt_of_le_of_lt ht_le_k hk
        have hcolActive : t ≤ col.val := by
          by_cases hjk : j.val < k
          · have htj : t ≤ j.val := by
              exact Nat.lt_succ_iff.mp
                (by simpa [qrLeadingOffdiagStop, hjk] using ht)
            simpa [col, qrLeadingColumn] using htj
          · have hle : j.val ≤ k := Nat.le_of_lt_succ j.isLt
            have heq : j.val = k := le_antisymm hle (le_of_not_gt hjk)
            have htk : t < k := by
              simpa [qrLeadingOffdiagStop, hjk, heq] using ht
            have htj : t ≤ j.val := by omega
            simpa [col, qrLeadingColumn] using htj
        have hnormPos :
            0 <
              householderTrailingColumnNorm2Sq
                (m := m) (n := n) ⟨t, lt_of_lt_of_le htn hmn⟩
                (A_hat t) ⟨t, htn⟩ := by
          have hnonneg :
              0 ≤
                (m : ℝ) *
                  (householderCompactComponentBudget fp m
                    (householderTrailingActiveVector m
                      ⟨t, lt_of_lt_of_le htn hmn⟩
                      (fun a => A_hat t a ⟨t, htn⟩) (alpha t))
                    (householderBetaSpec m
                      (householderTrailingActiveVector m
                        ⟨t, lt_of_lt_of_le htn hmn⟩
                        (fun a => A_hat t a ⟨t, htn⟩) (alpha t)))
                    (fun a => A_hat t a ⟨t, htn⟩)
                    ⟨t, lt_of_lt_of_le htn hmn⟩) ^ 2 := by
            exact mul_nonneg (Nat.cast_nonneg m) (sq_nonneg _)
          have hpos :=
            lt_of_le_of_lt hnonneg (hbudgetNormSq t htn)
          simpa [householderTrailingColumnNorm2Sq] using hpos
        exact
          storedQRSignedStage_exact_same_reflector_bound_of_prefix_or_active_stage_bounds
            hmn A_hat alpha t htn row col (stageBudget t)
            (hAlphaDef t htn) hcolActive (hBudget_nonneg t)
            (fun _hprefix =>
              hstageRowBound k hk i j hij t ht col hcolActive)
            hnormPos
            (fun l hl => hpivotMax t htn l hl)
            (fun l hl => hstageRowBound k hk i j hij t ht l hl)
            (fun r hr => hstageColBound k hk i j hij t ht r hr))
      hBudget_mono hBudget_diag hproduct
/-- Uniform signed-stage source-control handoff with the exact-reflector field
    derived locally and the diagonal lower-bound field restricted to rows with
    displayed upper entries. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          |A_hat t
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
            stageBudget t)
    (hstageColBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ r : Fin m, t ≤ r.val →
          |A_hat t r (qrLeadingColumn n k hk j)| ≤ stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_offdiag_rows
      hmn fp A_hat b_hat alpha
      (coxHighamActiveRowGrowthFactor m) stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hbudget
      (fun k hk i j hij t ht => by
        let row : Fin m :=
          qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i
        let col : Fin n := qrLeadingColumn n k hk j
        have ht_le_k : t ≤ k := by
          exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
        have htn : t < n := lt_of_le_of_lt ht_le_k hk
        have hcolActive : t ≤ col.val := by
          by_cases hjk : j.val < k
          · have htj : t ≤ j.val := by
              exact Nat.lt_succ_iff.mp
                (by simpa [qrLeadingOffdiagStop, hjk] using ht)
            simpa [col, qrLeadingColumn] using htj
          · have hle : j.val ≤ k := Nat.le_of_lt_succ j.isLt
            have heq : j.val = k := le_antisymm hle (le_of_not_gt hjk)
            have htk : t < k := by
              simpa [qrLeadingOffdiagStop, hjk, heq] using ht
            have htj : t ≤ j.val := by omega
            simpa [col, qrLeadingColumn] using htj
        have hnormPos :
            0 <
              householderTrailingColumnNorm2Sq
                (m := m) (n := n) ⟨t, lt_of_lt_of_le htn hmn⟩
                (A_hat t) ⟨t, htn⟩ := by
          have hnonneg :
              0 ≤
                (m : ℝ) *
                  (householderCompactComponentBudget fp m
                    (householderTrailingActiveVector m
                      ⟨t, lt_of_lt_of_le htn hmn⟩
                      (fun a => A_hat t a ⟨t, htn⟩) (alpha t))
                    (householderBetaSpec m
                      (householderTrailingActiveVector m
                        ⟨t, lt_of_lt_of_le htn hmn⟩
                        (fun a => A_hat t a ⟨t, htn⟩) (alpha t)))
                    (fun a => A_hat t a ⟨t, htn⟩)
                    ⟨t, lt_of_lt_of_le htn hmn⟩) ^ 2 := by
            exact mul_nonneg (Nat.cast_nonneg m) (sq_nonneg _)
          have hpos :=
            lt_of_le_of_lt hnonneg (hbudgetNormSq t htn)
          simpa [householderTrailingColumnNorm2Sq] using hpos
        exact
          storedQRSignedStage_exact_same_reflector_bound_of_prefix_or_active_stage_bounds
            hmn A_hat alpha t htn row col (stageBudget t)
            (hAlphaDef t htn) hcolActive (hBudget_nonneg t)
            (fun _hprefix =>
              hstageRowBound k hk i j hij t ht col hcolActive)
            hnormPos
            (fun l hl => hpivotMax t htn l hl)
            (fun l hl => hstageRowBound k hk i j hij t ht l hl)
            (fun r hr => hstageColBound k hk i j hij t ht r hr))
      hBudget_mono hBudget_diag hproduct
/-- Uniform signed-stage source-control handoff from an active-suffix block
    budget plus a prefix-row budget.

    This is a statement-correction adapter for the rectangular QR bottleneck:
    active rows and columns are controlled by one suffix block invariant, while
    the only separate row-growth obligation is for displayed rows that have
    already moved into the prefix before the current stage. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_stage_entry_bounds_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageActiveBlockBound : ∀ t, t < n →
      ∀ r : Fin m, t ≤ r.val →
        ∀ l : Fin n, t ≤ l.val → |A_hat t r l| ≤ stageBudget t)
    (hstagePrefixRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          (qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i).val < t →
            |A_hat t
              (qrLeadingRow m k
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
              stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hbudget hBudget_nonneg hBudget_mono
      hBudget_diag hpivotMax ?_ ?_ hproduct
  · intro k hk i j hij t ht l hl
    let row : Fin m :=
      qrLeadingRow m k
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i
    have ht_le_k : t ≤ k := by
      exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
    have htn : t < n := lt_of_le_of_lt ht_le_k hk
    by_cases hrow : t ≤ row.val
    · simpa [row] using hstageActiveBlockBound t htn row hrow l hl
    · have hprefix : row.val < t := Nat.lt_of_not_ge hrow
      simpa [row] using
        hstagePrefixRowBound k hk i j hij t ht l hl hprefix
  · intro k hk i j hij t ht r hr
    let col : Fin n := qrLeadingColumn n k hk j
    have ht_le_k : t ≤ k := by
      exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
    have htn : t < n := lt_of_le_of_lt ht_le_k hk
    have hcolActive : t ≤ col.val := by
      by_cases hjk : j.val < k
      · have htj : t ≤ j.val := by
          exact Nat.lt_succ_iff.mp
            (by simpa [qrLeadingOffdiagStop, hjk] using ht)
        simpa [col, qrLeadingColumn] using htj
      · have hle : j.val ≤ k := Nat.le_of_lt_succ j.isLt
        have heq : j.val = k := le_antisymm hle (le_of_not_gt hjk)
        have htk : t < k := by
          simpa [qrLeadingOffdiagStop, hjk, heq] using ht
        have htj : t ≤ j.val := by omega
        simpa [col, qrLeadingColumn] using htj
    simpa [col] using hstageActiveBlockBound t htn r hr col hcolActive
/-- Active-suffix stage bounds from the rounded signed-pivot active-block
    recurrence.

    This factors out the reusable part of the rectangular QR bottleneck: the
    Cox--Higham stored-panel sequence theorem propagates one uniform bound over
    the active trailing block, provided the signed pivot stages use a positive
    nonbreakdown norm, pivot-maximal columns, completed-column preservation,
    pivot-column zeroing, and the one-step active-block budget. -/
theorem storedQRSignedStage_active_block_bound_of_signed_stage_budget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hactiveBudget : ∀ t : ℕ, t + 1 < n →
      ∀ r : Fin m, t + 1 ≤ r.val →
        ∀ l : Fin n, t + 1 ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hcompleted : ∀ t (_ht : t < n), ∀ j : Fin n, j.val < t →
      ∀ i : Fin m,
        matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a j) i = A_hat t i j) :
    ∀ t (_ht : t < n), ∀ row : Fin m, t ≤ row.val →
      ∀ l : Fin n, t ≤ l.val → |A_hat t row l| ≤ stageBudget t := by
  classical
  intro t ht row hrow l hl
  have hnpos : 0 < n := lt_of_le_of_lt (Nat.zero_le t) ht
  have hmpos : 0 < m := lt_of_lt_of_le hnpos hmn
  let p : ℕ → Fin m := fun s =>
    if hs : s < n then ⟨s, lt_of_lt_of_le hs hmn⟩ else ⟨0, hmpos⟩
  let pivotCol : ℕ → Fin n := fun s =>
    if hs : s < n then ⟨s, hs⟩ else ⟨0, hnpos⟩
  have hactiveSeq :
      ∀ s : ℕ, s ≤ t →
        ∀ row' : Fin m, (p s).val ≤ row'.val →
          ∀ l' : Fin n, (pivotCol s).val ≤ l'.val →
            |A_hat s row' l'| ≤ stageBudget s := by
    exact
      coxHigham_storedPanel_sequence_active_block_bound_of_signed_pivot_stage_bounds
        fp t A_hat p pivotCol stageBudget hm hinitBlock
        (fun s _hs => hBudget_nonneg s)
        (fun s hs => by
          have hsn : s < n := Nat.lt_trans hs ht
          have halpha := hAlphaDef s hsn
          simpa [p, pivotCol, hsn, signedPivotHouseholderVector,
            signedPivotHouseholderBeta, storedQRSignedStageVector,
            storedQRSignedStageBeta, halpha, householderTrailingColumnNorm2Sq]
            using hStepA s hsn)
        (fun u s hus hst => by
          have hsn : s < n := lt_of_le_of_lt hst ht
          have hun : u < n := lt_of_le_of_lt hus hsn
          simpa [p, hun, hsn] using hus)
        (fun u s hus hst => by
          have hsn : s < n := lt_of_le_of_lt hst ht
          have hun : u < n := lt_of_le_of_lt hus hsn
          simpa [pivotCol, hun, hsn] using hus)
        (fun s hs => by
          have hsn : s < n := Nat.lt_trans hs ht
          have hleft_nonneg :
              0 ≤
                (m : ℝ) *
                  (householderCompactComponentBudget fp m
                    (householderTrailingActiveVector m
                      ⟨s, lt_of_lt_of_le hsn hmn⟩
                      (fun a => A_hat s a ⟨s, hsn⟩) (alpha s))
                    (householderBetaSpec m
                      (householderTrailingActiveVector m
                        ⟨s, lt_of_lt_of_le hsn hmn⟩
                        (fun a => A_hat s a ⟨s, hsn⟩) (alpha s)))
                    (fun a => A_hat s a ⟨s, hsn⟩)
                    ⟨s, lt_of_lt_of_le hsn hmn⟩) ^ 2 := by
            exact mul_nonneg (Nat.cast_nonneg m) (sq_nonneg _)
          have hpos := lt_of_le_of_lt hleft_nonneg (hbudgetNormSq s hsn)
          simpa [p, pivotCol, hsn, householderTrailingColumnNorm2Sq] using
            hpos)
        (fun s hs l' hl' => by
          have hsn : s < n := Nat.lt_trans hs ht
          have hl_s : s ≤ l'.val := by
            simpa [pivotCol, hsn] using hl'
          simpa [p, pivotCol, hsn] using hpivotMax s hsn l' hl_s)
        (fun s hs j hj i => by
          have hsn : s < n := Nat.lt_trans hs ht
          have halpha := hAlphaDef s hsn
          simpa [p, pivotCol, hsn, signedPivotHouseholderVector,
            signedPivotHouseholderBeta, storedQRSignedStageVector,
            storedQRSignedStageBeta, halpha, householderTrailingColumnNorm2Sq]
            using hcompleted s hsn j (by simpa [pivotCol, hsn] using hj) i)
        (fun s hs j hj i hi => by
          have hsn : s < n := Nat.lt_trans hs ht
          have halpha := hAlphaDef s hsn
          have hleft_nonneg :
              0 ≤
                (m : ℝ) *
                  (householderCompactComponentBudget fp m
                    (householderTrailingActiveVector m
                      ⟨s, lt_of_lt_of_le hsn hmn⟩
                      (fun a => A_hat s a ⟨s, hsn⟩) (alpha s))
                    (householderBetaSpec m
                      (householderTrailingActiveVector m
                        ⟨s, lt_of_lt_of_le hsn hmn⟩
                        (fun a => A_hat s a ⟨s, hsn⟩) (alpha s)))
                    (fun a => A_hat s a ⟨s, hsn⟩)
                    ⟨s, lt_of_lt_of_le hsn hmn⟩) ^ 2 := by
            exact mul_nonneg (Nat.cast_nonneg m) (sq_nonneg _)
          have hpos := lt_of_le_of_lt hleft_nonneg (hbudgetNormSq s hsn)
          have hcol : j = ⟨s, hsn⟩ := by
            apply Fin.ext
            simpa [pivotCol, hsn] using hj
          subst hcol
          have hzero :=
            storedQRSignedStage_pivot_column_zero_below_of_trailingNorm_pos
              hmn A_hat alpha s hsn (hAlphaDef s hsn) hpos i
              (by simpa [pivotCol, hsn] using hi)
          simpa [p, pivotCol, hsn, signedPivotHouseholderVector,
            signedPivotHouseholderBeta, storedQRSignedStageVector,
            storedQRSignedStageBeta, halpha, householderTrailingColumnNorm2Sq]
            using hzero)
        (fun s hs row' l' hrowNext hcolNext => by
          have hsn : s < n := Nat.lt_trans hs ht
          have hsuccn : s + 1 < n := lt_of_le_of_lt (Nat.succ_le_iff.mpr hs) ht
          have halpha := hAlphaDef s hsn
          have hrowNext' : s + 1 ≤ row'.val := by
            simpa [p, hsuccn] using hrowNext
          have hcolNext' : s + 1 ≤ l'.val := by
            simpa [pivotCol, hsuccn] using hcolNext
          simpa [p, pivotCol, hsn, hsuccn, signedPivotHouseholderVector,
            signedPivotHouseholderBeta, storedQRSignedStageVector,
            storedQRSignedStageBeta, halpha, householderTrailingColumnNorm2Sq]
            using hactiveBudget s hsuccn row' hrowNext' l' hcolNext')
  have hpt : (p t).val ≤ row.val := by
    simpa [p, ht] using hrow
  have hct : (pivotCol t).val ≤ l.val := by
    simpa [pivotCol, ht] using hl
  exact hactiveSeq t le_rfl row hpt l hct
/-- Prefix-row stage bounds from the active-block invariant and a displayed-row
    recurrence.

    This is the next dependency in the rectangular QR/preconditioner bottleneck.
    Once a row has moved into the displayed prefix, its later entries are
    controlled by the same exact-reflector row-growth theorem used for active
    rows, followed by the concrete stored-panel floating-point component
    budget.  The hypothesis `hprefixBudget` is exactly that one-step recurrence
    for rows with `row.val < t + 1`. -/
theorem storedQRSignedStage_prefix_row_bound_of_active_block_and_prefix_budget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageActiveBlockBound : ∀ t, t < n →
      ∀ r : Fin m, t ≤ r.val →
        ∀ l : Fin n, t ≤ l.val → |A_hat t r l| ≤ stageBudget t)
    (hprefixBudget : ∀ t (_ht : t < n),
      ∀ r : Fin m, r.val < t + 1 →
        ∀ l : Fin n, t ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1)) :
    ∀ t (_ht : t < n), ∀ r : Fin m, r.val < t →
      ∀ l : Fin n, t ≤ l.val → |A_hat t r l| ≤ stageBudget t := by
  classical
  intro t
  induction t with
  | zero =>
      intro _ht r hr
      exact (Nat.not_lt_zero r.val hr).elim
  | succ t ih =>
      intro ht r hr l hl
      have htn : t < n := Nat.lt_trans (Nat.lt_succ_self t) ht
      have hcolStep : t ≤ l.val := le_trans (Nat.le_succ t) hl
      have hcolStrict : t < l.val := Nat.lt_of_succ_le hl
      have hstep := congrFun (congrFun (hStepA t htn) r) l
      rw [hstep]
      have hnorm :
          0 <
            householderTrailingColumnNorm2Sq
              (m := m) (n := n) ⟨t, lt_of_lt_of_le htn hmn⟩
              (A_hat t) ⟨t, htn⟩ := by
        have hleft_nonneg :
            0 ≤
              (m : ℝ) *
                (householderCompactComponentBudget fp m
                  (householderTrailingActiveVector m
                    ⟨t, lt_of_lt_of_le htn hmn⟩
                    (fun a => A_hat t a ⟨t, htn⟩) (alpha t))
                  (householderBetaSpec m
                    (householderTrailingActiveVector m
                      ⟨t, lt_of_lt_of_le htn hmn⟩
                      (fun a => A_hat t a ⟨t, htn⟩) (alpha t)))
                  (fun a => A_hat t a ⟨t, htn⟩)
                  ⟨t, lt_of_lt_of_le htn hmn⟩) ^ 2 := by
          exact mul_nonneg (Nat.cast_nonneg m) (sq_nonneg _)
        have hpos := lt_of_le_of_lt hleft_nonneg (hbudgetNormSq t htn)
        simpa [householderTrailingColumnNorm2Sq] using hpos
      have hexact :
          |matMulVec m
            (householder m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t))
            (fun a => A_hat t a l) r| ≤
            coxHighamActiveRowGrowthFactor m * stageBudget t := by
        exact
          storedQRSignedStage_exact_same_reflector_bound_of_prefix_or_active_stage_bounds
            hmn A_hat alpha t htn r l (stageBudget t)
            (hAlphaDef t htn) hcolStep (hBudget_nonneg t)
            (fun hprefix => ih htn r hprefix l hcolStep)
            hnorm
            (fun l' hl' => hpivotMax t htn l' hl')
            (fun l' hl' => by
              by_cases hprefix : r.val < t
              · exact ih htn r hprefix l' hl'
              · have hactive : t ≤ r.val := le_of_not_gt hprefix
                exact hstageActiveBlockBound t htn r hactive l' hl')
            (fun r' hr' =>
              hstageActiveBlockBound t htn r' hr' l hcolStep)
      have hstepBound :
          |fl_householderStoredPanelStep fp m n t
            (householderTrailingActiveVector m
              ⟨t, lt_of_lt_of_le htn hmn⟩
              (fun a => A_hat t a ⟨t, htn⟩) (alpha t))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨t, lt_of_lt_of_le htn hmn⟩
                (fun a => A_hat t a ⟨t, htn⟩) (alpha t)))
            (A_hat t) r l| ≤
            coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r := by
        have hraw :=
          coxHigham_storedPanelStep_active_entry_bound_of_exact_stage_budget_factor
            fp m n t
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t)
            (coxHighamActiveRowGrowthFactor m) (stageBudget t)
            (A_hat t) hm r l hcolStep
            (fun hlt _i => False.elim ((Nat.not_lt_of_ge hcolStep) hlt))
            (fun heq _i _hi => False.elim ((Nat.ne_of_gt hcolStrict) heq))
            hexact
        simpa [storedQRSignedStageVector, storedQRSignedStageBeta, htn]
          using hraw
      exact hstepBound.trans (hprefixBudget t htn r hr l hcolStep)
/-- Finite global compact-update budget for one signed stored-QR stage.

    For a fixed stage `t < n`, this is the maximum, over all displayed matrix
    entries, of the floating-point compact-update component budget generated by
    the signed stored-QR reflector at that stage. -/
noncomputable def storedQRSignedStageGlobalCompactBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (t : ℕ) (ht : t < n) : ℝ :=
  Finset.sup' (Finset.univ : Finset (Fin m × Fin n))
    (Finset.univ_nonempty_iff.mpr
      ⟨((⟨t, lt_of_lt_of_le ht hmn⟩ : Fin m), (⟨t, ht⟩ : Fin n))⟩)
    (fun rc =>
      householderCompactComponentBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha t)
        (storedQRSignedStageBeta hmn A_hat alpha t)
        (fun a => A_hat t a rc.2) rc.1)
/-- Every matrix-entry compact component is bounded by the finite global
    compact-update budget for the signed stored-QR stage. -/
theorem storedQRSignedStage_compact_component_le_globalBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (t : ℕ) (ht : t < n)
    (r : Fin m) (l : Fin n) :
    householderCompactComponentBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha t)
        (storedQRSignedStageBeta hmn A_hat alpha t)
        (fun a => A_hat t a l) r ≤
      storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht := by
  classical
  unfold storedQRSignedStageGlobalCompactBudget
  exact
    Finset.le_sup' (s := (Finset.univ : Finset (Fin m × Fin n)))
      (f := fun rc =>
        householderCompactComponentBudget fp m
          (storedQRSignedStageVector hmn A_hat alpha t)
          (storedQRSignedStageBeta hmn A_hat alpha t)
          (fun a => A_hat t a rc.2) rc.1)
      (Finset.mem_univ (r, l))
/-- The finite global compact-update budget is nonnegative.

This packages component-budget nonnegativity for later stage-recurrence
arguments: the finite maximum contains the pivot entry, and every compact
component budget is nonnegative under `gammaValid fp m`. -/
theorem storedQRSignedStageGlobalCompactBudget_nonneg
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (t : ℕ) (ht : t < n)
    (hm : gammaValid fp m) :
    0 ≤ storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht := by
  classical
  let r : Fin m := ⟨t, lt_of_lt_of_le ht hmn⟩
  let l : Fin n := ⟨t, ht⟩
  have hcomp :
      0 ≤
        householderCompactComponentBudget fp m
          (storedQRSignedStageVector hmn A_hat alpha t)
          (storedQRSignedStageBeta hmn A_hat alpha t)
          (fun a => A_hat t a l) r := by
    exact
      householderCompactComponentBudget_nonneg fp m
        (storedQRSignedStageVector hmn A_hat alpha t)
        (storedQRSignedStageBeta hmn A_hat alpha t)
        (fun a => A_hat t a l) hm r
  exact
    hcomp.trans
      (storedQRSignedStage_compact_component_le_globalBudget
        hmn fp A_hat alpha t ht r l)
/-- A signed-stage global compact-budget recurrence propagates nonnegativity
    from the initial stage budget across the QR horizon. -/
theorem storedQRSignedStageBudget_nonneg_on_stages_of_globalCompactBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0) :
    ∀ t : ℕ, t ≤ n → 0 ≤ stageBudget t := by
  intro t ht
  induction t with
  | zero =>
      simpa using hBudget0_nonneg
  | succ t ih =>
      have ht_le : t ≤ n := Nat.le_of_succ_le ht
      have ht_lt : t < n := Nat.lt_of_succ_le ht
      have hprev_nonneg : 0 ≤ stageBudget t := ih ht_le
      have hscale_nonneg :
          0 ≤ coxHighamActiveRowGrowthFactor m * stageBudget t :=
        mul_nonneg (coxHighamActiveRowGrowthFactor_nonneg m) hprev_nonneg
      have hcompact_nonneg :
          0 ≤ storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht_lt :=
        storedQRSignedStageGlobalCompactBudget_nonneg
          hmn fp A_hat alpha t ht_lt hm
      exact
        (add_nonneg hscale_nonneg hcompact_nonneg).trans
          (hglobalBudget t ht_lt)
/-- A signed-stage global compact-budget recurrence is monotone on QR stages
    once the initial stage budget is nonnegative. -/
theorem storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget_of_initial_nonneg
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0) :
    ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b := by
  have hnonneg_on :
      ∀ t : ℕ, t ≤ n → 0 ≤ stageBudget t :=
    storedQRSignedStageBudget_nonneg_on_stages_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
  have hstepMono : ∀ b : ℕ, b < n → stageBudget b ≤ stageBudget (b + 1) := by
    intro b hb_lt
    have hb_le : b ≤ n := Nat.le_of_lt hb_lt
    have hscale :
        stageBudget b ≤ coxHighamActiveRowGrowthFactor m * stageBudget b := by
      simpa [one_mul] using
        mul_le_mul_of_nonneg_right
          (one_le_coxHighamActiveRowGrowthFactor m) (hnonneg_on b hb_le)
    have hcompact_nonneg :
        0 ≤ storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha b hb_lt :=
      storedQRSignedStageGlobalCompactBudget_nonneg
        hmn fp A_hat alpha b hb_lt hm
    exact
      hscale.trans
        ((le_add_of_nonneg_right hcompact_nonneg).trans
          (hglobalBudget b hb_lt))
  intro a b hab hb
  induction b generalizing a with
  | zero =>
      have ha0 : a = 0 := Nat.eq_zero_of_le_zero hab
      subst a
      rfl
  | succ b ih =>
      rcases Nat.eq_or_lt_of_le hab with heq | hlt
      · subst a
        rfl
      · have hab' : a ≤ b := Nat.lt_succ_iff.mp hlt
        have hb_le : b ≤ n := Nat.le_of_succ_le hb
        have hb_lt : b < n := Nat.lt_of_succ_le hb
        exact (ih a hab' hb_le).trans (hstepMono b hb_lt)
/-- A signed-stage global compact-budget recurrence is monotone on QR stages.

If the stage budgets are nonnegative and each successor budget dominates
`coxHighamActiveRowGrowthFactor m * B_t` plus the finite compact-update budget,
then the budget sequence is monotone for all indices up to the QR horizon `n`.
This removes the need to prove a separate monotonicity invariant on the actual
algorithmic stage range. -/
theorem storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t) :
    ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b := by
  have hstepMono : ∀ b : ℕ, b < n → stageBudget b ≤ stageBudget (b + 1) := by
    intro b hb_lt
    have hscale :
        stageBudget b ≤ coxHighamActiveRowGrowthFactor m * stageBudget b := by
      simpa [one_mul] using
        mul_le_mul_of_nonneg_right
          (one_le_coxHighamActiveRowGrowthFactor m) (hBudget_nonneg b)
    have hcompact_nonneg :
        0 ≤ storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha b hb_lt :=
      storedQRSignedStageGlobalCompactBudget_nonneg
        hmn fp A_hat alpha b hb_lt hm
    have hstep :
        stageBudget b ≤ stageBudget (b + 1) :=
      hscale.trans
        ((le_add_of_nonneg_right hcompact_nonneg).trans
          (hglobalBudget b hb_lt))
    exact hstep
  intro a b hab hb
  induction b generalizing a with
  | zero =>
      have ha0 : a = 0 := Nat.eq_zero_of_le_zero hab
      subst a
      rfl
  | succ b ih =>
      rcases Nat.eq_or_lt_of_le hab with heq | hlt
      · subst a
        rfl
      · have hab' : a ≤ b := Nat.lt_succ_iff.mp hlt
        have hb_le : b ≤ n := Nat.le_of_succ_le hb
        have hb_lt : b < n := Nat.lt_of_succ_le hb
        exact (ih a hab' hb_le).trans (hstepMono b hb_lt)
/-- Clamp a stage-budget sequence after the QR horizon.

The QR source-control theorems only compare stages `b <= n`, but some older
surfaces asked for monotonicity on all natural indices.  This extension keeps
the original budget on the algorithmic horizon and freezes it afterward. -/
def qrStageHorizonBudget (n : ℕ) (stageBudget : ℕ → ℝ) : ℕ → ℝ :=
  fun t => if t ≤ n then stageBudget t else stageBudget n
theorem qrStageHorizonBudget_eq_of_le
    (n : ℕ) (stageBudget : ℕ → ℝ) {t : ℕ} (ht : t ≤ n) :
    qrStageHorizonBudget n stageBudget t = stageBudget t := by
  simp [qrStageHorizonBudget, ht]
theorem qrStageHorizonBudget_nonneg
    (n : ℕ) (stageBudget : ℕ → ℝ)
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t) :
    ∀ t : ℕ, 0 ≤ qrStageHorizonBudget n stageBudget t := by
  intro t
  by_cases ht : t ≤ n
  · simpa [qrStageHorizonBudget, ht] using hBudget_nonneg t
  · simpa [qrStageHorizonBudget, ht] using hBudget_nonneg n
/-- The QR-horizon clamp is globally nonnegative when the original stage budget
    is nonnegative on the finite algorithmic horizon. -/
theorem qrStageHorizonBudget_nonneg_of_nonneg_on_stages
    (n : ℕ) (stageBudget : ℕ → ℝ)
    (hBudget_nonneg_on_stages :
      ∀ t : ℕ, t ≤ n → 0 ≤ stageBudget t) :
    ∀ t : ℕ, 0 ≤ qrStageHorizonBudget n stageBudget t := by
  intro t
  by_cases ht : t ≤ n
  · simpa [qrStageHorizonBudget, ht] using
      hBudget_nonneg_on_stages t ht
  · simpa [qrStageHorizonBudget, ht] using
      hBudget_nonneg_on_stages n le_rfl
theorem qrStageHorizonBudget_mono_of_mono_on_stages
    (n : ℕ) (stageBudget : ℕ → ℝ)
    (hBudget_mono_on_stages :
      ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b) :
    ∀ a b : ℕ, a ≤ b →
      qrStageHorizonBudget n stageBudget a ≤
        qrStageHorizonBudget n stageBudget b := by
  intro a b hab
  by_cases hb : b ≤ n
  · have ha : a ≤ n := le_trans hab hb
    simpa [qrStageHorizonBudget, ha, hb] using
      hBudget_mono_on_stages a b hab hb
  · by_cases ha : a ≤ n
    · have hle : stageBudget a ≤ stageBudget n :=
        hBudget_mono_on_stages a n ha le_rfl
      simpa [qrStageHorizonBudget, ha, hb] using hle
    · simp [qrStageHorizonBudget, ha, hb]
/-- The scalar active/prefix row-max comparison defect is stable under the
    QR-horizon clamp.

Only stages `k < n` appear in the finite comparison maximum, so replacing the
budget by `qrStageHorizonBudget n stageBudget` preserves the displayed
comparison certificate consumed by the source-control handoff. -/
theorem storedQRStageRowMaxComparisonDefectBudget_nonpos_of_horizonBudget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (stageBudget : ℕ → ℝ)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0) :
    storedQRStageRowMaxComparisonDefectBudget hmn A_hat
        (qrStageHorizonBudget n stageBudget) ≤ 0 := by
  apply storedQRStageRowMaxComparisonDefectBudget_nonpos_of_stageBudget_le_rowMax
  intro k hk i hi
  have hstage :=
    storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
      hmn A_hat stageBudget hcomparison k hk i hi
  have hk_le : k ≤ n := Nat.le_of_lt hk
  simpa [qrStageHorizonBudget, hk_le] using hstage
/-- A single finite global compact-step recurrence supplies both one-step
    budget fields used by the active/prefix QR certificate.

    This closes the bookkeeping part of the red QR bottleneck: instead of
    assuming separate active-row and prefix-row one-step inequalities, it is
    enough to show that `stageBudget (t+1)` dominates the Cox--Higham exact
    factor plus the finite maximum compact component at stage `t`. -/
theorem storedQRSignedStage_active_prefix_budgets_of_globalCompactBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1)) :
    (∀ t : ℕ, t + 1 < n →
      ∀ r : Fin m, t + 1 ≤ r.val →
        ∀ l : Fin n, t + 1 ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1)) ∧
    (∀ t (_ht : t < n),
      ∀ r : Fin m, r.val < t + 1 →
        ∀ l : Fin n, t ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1)) := by
  classical
  constructor
  · intro t ht r _hr l _hl
    have htn : t < n := Nat.lt_trans (Nat.lt_succ_self t) ht
    have hcomp :
        householderCompactComponentBudget fp m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t)
            (fun a => A_hat t a l) r ≤
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t htn :=
      storedQRSignedStage_compact_component_le_globalBudget
        hmn fp A_hat alpha t htn r l
    have hstep :
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a l) r ≤
          coxHighamActiveRowGrowthFactor m * stageBudget t +
            storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t htn := by
      simpa [add_comm] using
        (add_le_add_left hcomp
          (coxHighamActiveRowGrowthFactor m * stageBudget t))
    exact hstep.trans (hglobalBudget t htn)
  · intro t ht r _hr l _hl
    have hcomp :
        householderCompactComponentBudget fp m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t)
            (fun a => A_hat t a l) r ≤
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht :=
      storedQRSignedStage_compact_component_le_globalBudget
        hmn fp A_hat alpha t ht r l
    have hstep :
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a l) r ≤
          coxHighamActiveRowGrowthFactor m * stageBudget t +
            storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht := by
      simpa [add_comm] using
        (add_le_add_left hcomp
          (coxHighamActiveRowGrowthFactor m * stageBudget t))
    exact hstep.trans (hglobalBudget t ht)
/-- Uniform signed-stage source-control handoff deriving the active-suffix
    block budget from the rounded signed-pivot active-block recurrence.

    The only stage-entry field left outside the recurrence is the prefix
    displayed-row budget: active rows and active columns are supplied by the
    Cox--Higham active-block sequence theorem already present in the QR
    library. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hactiveBudget : ∀ t : ℕ, t + 1 < n →
      ∀ r : Fin m, t + 1 ≤ r.val →
        ∀ l : Fin n, t + 1 ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hcompleted : ∀ t (_ht : t < n), ∀ j : Fin n, j.val < t →
      ∀ i : Fin m,
        matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a j) i = A_hat t i j)
    (hstagePrefixRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          (qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i).val < t →
            |A_hat t
              (qrLeadingRow m k
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
              stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_stage_entry_bounds_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hbudget hBudget_nonneg hBudget_mono
      hBudget_diag hpivotMax ?_ hstagePrefixRowBound hproduct
  exact
    storedQRSignedStage_active_block_bound_of_signed_stage_budget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hinitBlock hactiveBudget hBudget_nonneg hpivotMax
      hcompleted
/-- Uniform signed-stage source-control handoff deriving both pieces of the
    active/prefix stage-entry split from one-step recurrences.

    Active suffix entries are propagated by
    `storedQRSignedStage_active_block_bound_of_signed_stage_budget`; prefix
    displayed rows are then propagated by
    `storedQRSignedStage_prefix_row_bound_of_active_block_and_prefix_budget`.
    Thus the source-control invariant no longer assumes a raw prefix-row bound;
    it assumes only the displayed-row one-step budget. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_prefixRowRecurrence_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hactiveBudget : ∀ t : ℕ, t + 1 < n →
      ∀ r : Fin m, t + 1 ≤ r.val →
        ∀ l : Fin n, t + 1 ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1))
    (hprefixBudget : ∀ t (_ht : t < n),
      ∀ r : Fin m, r.val < t + 1 →
        ∀ l : Fin n, t ≤ l.val →
          coxHighamActiveRowGrowthFactor m * stageBudget t +
              householderCompactComponentBudget fp m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t)
                (fun a => A_hat t a l) r ≤
            stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hcompleted : ∀ t (_ht : t < n), ∀ j : Fin n, j.val < t →
      ∀ i : Fin m,
        matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a j) i = A_hat t i j)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hactiveBlock :
      ∀ t (_ht : t < n), ∀ row : Fin m, t ≤ row.val →
        ∀ l : Fin n, t ≤ l.val → |A_hat t row l| ≤ stageBudget t :=
    storedQRSignedStage_active_block_bound_of_signed_stage_budget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hinitBlock hactiveBudget hBudget_nonneg hpivotMax
      hcompleted
  have hprefixRows :
      ∀ t (_ht : t < n), ∀ row : Fin m, row.val < t →
        ∀ l : Fin n, t ≤ l.val → |A_hat t row l| ≤ stageBudget t :=
    storedQRSignedStage_prefix_row_bound_of_active_block_and_prefix_budget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hBudget_nonneg hpivotMax hactiveBlock hprefixBudget
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hbudget hactiveBudget
      hBudget_nonneg hBudget_mono hBudget_diag hpivotMax hcompleted ?_
      hproduct
  intro k hk i j hij t ht l hl hrow
  have ht_le_k : t ≤ k := by
    exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
  have htn : t < n := lt_of_le_of_lt ht_le_k hk
  exact
    hprefixRows t htn
      (qrLeadingRow m k
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
      hrow l hl
/-- Uniform signed-stage source-control handoff with all one-step matrix-entry
    budgets generated by one finite global compact-step budget.

    This composes the active/prefix recurrence theorem with
    `storedQRSignedStageGlobalCompactBudget`: the off-diagonal, active-block,
    and prefix-row one-step inequalities all follow from the same finite
    maximum compact component at each stage. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hcompleted : ∀ t (_ht : t < n), ∀ j : Fin n, j.val < t →
      ∀ i : Fin m,
        matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a j) i = A_hat t i j)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  have hbudgets :=
    storedQRSignedStage_active_prefix_budgets_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hglobalBudget
  have hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1) := by
    intro k hk i j _hij t ht
    have ht_le_k : t ≤ k := by
      exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
    have htn : t < n := lt_of_le_of_lt ht_le_k hk
    have hcomp :
        householderCompactComponentBudget fp m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t)
            (fun a => A_hat t a (qrLeadingColumn n k hk j))
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) ≤
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t htn :=
      storedQRSignedStage_compact_component_le_globalBudget
        hmn fp A_hat alpha t htn
        (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
        (qrLeadingColumn n k hk j)
    have hstep :
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) ≤
          coxHighamActiveRowGrowthFactor m * stageBudget t +
            storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t htn := by
      simpa [add_comm] using
        (add_le_add_left hcomp
          (coxHighamActiveRowGrowthFactor m * stageBudget t))
    exact hstep.trans (hglobalBudget t htn)
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_prefixRowRecurrence_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hbudget hbudgets.1
      hbudgets.2 hBudget_nonneg hBudget_mono hBudget_diag hpivotMax
      hcompleted hproduct
/-- Uniform signed-stage source-control handoff whose completed-column
    preservation field is derived from the stored QR lower-zero invariant.

    This removes the explicit completed-column hypothesis from the global
    compact-step budget route.  The remaining source-facing hypotheses are the
    finite global budget recurrence, pivot maximality, norm-square
    nonbreakdown, diagonal lower bounds, compact-product smallness, and
    leading-block determinant assumptions. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hBudget_diag hpivotMax
      (storedQRSignedStage_completed_column_preservation
        hmn fp A_hat alpha hStepA)
      hproduct
/-- Finite global compact-product budget for the stored QR sequence.

    This is the maximum, over all pivots, of the per-pivot compact-product
    expression used by the off-diagonal-control route.  For `n = 0` the product
    side condition is vacuous, so the definition returns `0`. -/
noncomputable def storedQRCompactSequenceProductBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ) : ℝ :=
  if hn : 0 < n then
    Finset.sup' (Finset.univ : Finset (Fin n))
      (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩)
      (fun k => storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha k)
  else 0
/-- Each per-pivot compact-product expression is bounded by the finite global
    compact-product budget. -/
theorem storedQRCompactSequenceProductExpr_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (k : Fin n) :
    storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha k ≤
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha := by
  classical
  unfold storedQRCompactSequenceProductBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
        (f := fun k =>
          storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha k)
        (Finset.mem_univ k)
  · have hnpos : 0 < n := lt_of_le_of_lt (Nat.zero_le k.val) k.isLt
    exact False.elim (hn hnpos)
/-- The finite global compact-product budget is strictly below `1` whenever
    every per-pivot compact-product expression is strictly below `1`.

    This is the converse bookkeeping adapter to
    `storedQRCompactSequenceProductExpr_le_budget`: it does not prove the
    per-pivot inequalities, but it closes the finite-maximum step needed to
    move between older per-pivot smallness hypotheses and the newer scalar
    global product condition. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_forall_expr_lt
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hprod : ∀ k : Fin n,
      storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha k < 1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  classical
  unfold storedQRCompactSequenceProductBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      (Finset.sup'_lt_iff
        (s := (Finset.univ : Finset (Fin n)))
        (H := Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩)
        (f := fun k =>
          storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha k)
        (a := (1 : ℝ))).mpr
        (fun k _hk => hprod k)
  · rw [dif_neg hn]
    norm_num
/-- Natural-indexed version of
    `storedQRCompactSequenceProductBudget_lt_one_of_forall_expr_lt`.

    This matches the product-smallness hypotheses used by the older
    source-control wrappers. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_forall_pivot_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  refine
    storedQRCompactSequenceProductBudget_lt_one_of_forall_expr_lt
      hmn fp A_hat b_hat alpha ?_
  intro k
  simpa [storedQRCompactSequenceProductExpr] using hproduct k.val k.isLt
/-- The stored compact-product side condition is automatic in the zero
    compact-update regime.

If the common stored compact relative budget is exactly zero, every per-pivot
product expression is zero, so the finite product maximum is strictly below
`1`.  This closes the exact/zero-compact product-smallness dependency without
claiming anything about the genuinely rounded floating-point case, where the
relative budget must still be bounded. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_relativeBudget_eq_zero
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hzero :
      storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha = 0) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  refine
    storedQRCompactSequenceProductBudget_lt_one_of_forall_expr_lt
      hmn fp A_hat b_hat alpha ?_
  intro k
  simp [storedQRCompactSequenceProductExpr, hzero]
/-- Scalar sufficient condition for the global compact-product smallness
    hypothesis.

    If the diagonal-dominant inverse-budget factor is bounded by `Dmax` at
    every pivot and the displayed pivot-column norm is bounded by `Nmax`, then
    the single scalar inequality using `Dmax` and `Nmax` implies
    `storedQRCompactSequenceProductBudget < 1`.  This is still a visible
    product-smallness assumption, but it reduces the finite family of product
    obligations to one source-facing scalar budget. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_factor_norm_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDmax : 0 ≤ Dmax)
    (hNmax : 0 ≤ Nmax)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDle : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hNle : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              Nmax) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  classical
  refine
    storedQRCompactSequenceProductBudget_lt_one_of_forall_expr_lt
      hmn fp A_hat b_hat alpha ?_
  intro k
  let D : ℝ :=
    diagDominantUpperInvBudgetExpr (k.val + 1)
      (qrLeadingBlock (A_hat k.val)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
      ⟨k.val, Nat.lt_succ_self k.val⟩
  let c : ℝ :=
    storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
  let r : ℝ := vecNorm2 (fun i : Fin m => A_hat k.val i k)
  have hD_nonneg : 0 ≤ D := by
    exact le_of_lt (diagDominantUpperInvBudgetExpr_pos (k.val + 1)
      (qrLeadingBlock (A_hat k.val)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
      ⟨k.val, Nat.lt_succ_self k.val⟩ (hDD k))
  have hD_bound : D ≤ Dmax := by
    simpa [D] using hDle k
  have hc_nonneg : 0 ≤ c := by
    simpa [c] using
      storedQRCompactSequenceRelativeBudget_nonneg
        hmn fp A_hat b_hat alpha hm
  have hr_nonneg : 0 ≤ r := by
    simpa [r] using vecNorm2_nonneg (fun i : Fin m => A_hat k.val i k)
  have hr_bound : r ≤ Nmax := by
    simpa [r] using hNle k
  have hcr_bound : c * r ≤ c * Nmax :=
    mul_le_mul_of_nonneg_left hr_bound hc_nonneg
  have hcr_nonneg : 0 ≤ c * r := mul_nonneg hc_nonneg hr_nonneg
  have hcN_nonneg : 0 ≤ c * Nmax := mul_nonneg hc_nonneg hNmax
  have hsq_bound : (c * r) ^ 2 ≤ (c * Nmax) ^ 2 := by
    nlinarith [hcr_bound, hcr_nonneg, hcN_nonneg]
  have hm_nonneg : 0 ≤ (m : ℝ) := Nat.cast_nonneg m
  have hright_bound :
      (m : ℝ) * (c * r) ^ 2 ≤ (m : ℝ) * (c * Nmax) ^ 2 :=
    mul_le_mul_of_nonneg_left hsq_bound hm_nonneg
  have hright_nonneg : 0 ≤ (m : ℝ) * (c * r) ^ 2 :=
    mul_nonneg hm_nonneg (sq_nonneg _)
  have hleft_bound : 2 * D ≤ 2 * Dmax := by
    nlinarith
  have hleft_nonneg : 0 ≤ 2 * Dmax := by
    nlinarith
  have hprod_bound :
      2 * D * ((m : ℝ) * (c * r) ^ 2) ≤
        2 * Dmax * ((m : ℝ) * (c * Nmax) ^ 2) :=
    mul_le_mul hleft_bound hright_bound hright_nonneg hleft_nonneg
  exact lt_of_le_of_lt (by
    simpa [storedQRCompactSequenceProductExpr, D, c, r] using hprod_bound)
    hsmall
/-- Finite maximum of the diagonal-dominant inverse-budget factors used in the
    stored QR compact-product condition.  The value is `0` when there are no
    pivots. -/
noncomputable def storedQRDiagDominantInvFactorBudget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ) : ℝ :=
  if hn : 0 < n then
    Finset.sup' (Finset.univ : Finset (Fin n))
      (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩)
      (fun k =>
        diagDominantUpperInvBudgetExpr (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
          ⟨k.val, Nat.lt_succ_self k.val⟩)
  else 0
/-- Each diagonal-dominant inverse-budget factor is bounded by its finite
    maximum. -/
theorem storedQRDiagDominantInvFactor_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (k : Fin n) :
    diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤
      storedQRDiagDominantInvFactorBudget hmn A_hat := by
  classical
  unfold storedQRDiagDominantInvFactorBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
        (f := fun k : Fin n =>
          diagDominantUpperInvBudgetExpr (k.val + 1)
            (qrLeadingBlock (A_hat k.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
            ⟨k.val, Nat.lt_succ_self k.val⟩)
        (Finset.mem_univ k)
  · have hnpos : 0 < n := lt_of_le_of_lt (Nat.zero_le k.val) k.isLt
    exact False.elim (hn hnpos)
/-- The finite diagonal-dominant inverse-factor budget is nonnegative when all
    displayed leading blocks are diagonally dominant. -/
theorem storedQRDiagDominantInvFactorBudget_nonneg
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)) :
    0 ≤ storedQRDiagDominantInvFactorBudget hmn A_hat := by
  classical
  unfold storedQRDiagDominantInvFactorBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    let k0 : Fin n := ⟨0, hn⟩
    have hk0_pos :
        0 <
          diagDominantUpperInvBudgetExpr (k0.val + 1)
            (qrLeadingBlock (A_hat k0.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le k0.isLt hmn)) k0.isLt)
            ⟨k0.val, Nat.lt_succ_self k0.val⟩ := by
      exact
        diagDominantUpperInvBudgetExpr_pos (k0.val + 1)
          (qrLeadingBlock (A_hat k0.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le k0.isLt hmn)) k0.isLt)
          ⟨k0.val, Nat.lt_succ_self k0.val⟩ (hDD k0)
    exact
      le_trans (le_of_lt hk0_pos)
        (Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
          (f := fun k =>
            diagDominantUpperInvBudgetExpr (k.val + 1)
              (qrLeadingBlock (A_hat k.val)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
              ⟨k.val, Nat.lt_succ_self k.val⟩)
          (Finset.mem_univ k0))
  · rw [dif_neg hn]
/-- If every diagonal-dominant inverse-budget factor is bounded by a displayed
    scalar, then the canonical finite maximum is bounded by that scalar.

    The nonnegativity hypothesis is only needed for the zero-pivot case, where
    the finite maximum is defined to be `0` and the pointwise hypothesis is
    vacuous. -/
theorem storedQRDiagDominantInvFactorBudget_le_of_forall_le
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (Dmax : ℝ)
    (hDmax_nonneg : 0 ≤ Dmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax) :
    storedQRDiagDominantInvFactorBudget hmn A_hat ≤ Dmax := by
  classical
  unfold storedQRDiagDominantInvFactorBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.sup'_le
        (H := (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩))
        (f := fun k : Fin n =>
          diagDominantUpperInvBudgetExpr (k.val + 1)
            (qrLeadingBlock (A_hat k.val)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
            ⟨k.val, Nat.lt_succ_self k.val⟩)
        (by
          intro k _hk
          exact hD k)
  · rw [dif_neg hn]
    exact hDmax_nonneg
/-- Finite maximum of the displayed pivot-column norms used in the stored QR
    compact-product condition.  The value is `0` when there are no pivots. -/
noncomputable def storedQRPivotColumnNormBudget
    {m n : ℕ} (_hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ) : ℝ :=
  if hn : 0 < n then
    Finset.sup' (Finset.univ : Finset (Fin n))
      (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩)
      (fun k => vecNorm2 (fun i : Fin m => A_hat k.val i k))
  else 0
/-- Each displayed pivot-column norm is bounded by its finite maximum. -/
theorem storedQRPivotColumnNorm_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (k : Fin n) :
    vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤
      storedQRPivotColumnNormBudget hmn A_hat := by
  classical
  unfold storedQRPivotColumnNormBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
        (f := fun k : Fin n =>
          vecNorm2 (fun i : Fin m => A_hat k.val i k))
        (Finset.mem_univ k)
  · have hnpos : 0 < n := lt_of_le_of_lt (Nat.zero_le k.val) k.isLt
    exact False.elim (hn hnpos)
/-- The finite pivot-column norm budget is nonnegative. -/
theorem storedQRPivotColumnNormBudget_nonneg
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ) :
    0 ≤ storedQRPivotColumnNormBudget hmn A_hat := by
  classical
  unfold storedQRPivotColumnNormBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    let k0 : Fin n := ⟨0, hn⟩
    exact
      le_trans (vecNorm2_nonneg (fun i : Fin m => A_hat k0.val i k0))
        (Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
          (f := fun k => vecNorm2 (fun i : Fin m => A_hat k.val i k))
          (Finset.mem_univ k0))
  · rw [dif_neg hn]
/-- If every displayed pivot-column norm is bounded by a scalar, then the
    canonical finite maximum of the pivot-column norms is bounded by that
    scalar.

    The nonnegativity hypothesis is only needed for the zero-pivot case. -/
theorem storedQRPivotColumnNormBudget_le_of_forall_le
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (Nmax : ℝ)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax) :
    storedQRPivotColumnNormBudget hmn A_hat ≤ Nmax := by
  classical
  unfold storedQRPivotColumnNormBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.sup'_le
        (H := (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩))
        (f := fun k : Fin n =>
          vecNorm2 (fun i : Fin m => A_hat k.val i k))
        (by
          intro k _hk
          exact hN k)
  · rw [dif_neg hn]
    exact hNmax_nonneg
/-- Canonical finite-maximum version of the scalar compact-product smallness
    adapter.

    This removes the caller's separate `Dmax`/`Nmax` bound fields by choosing
    them as finite maxima over the stored sequence.  The remaining assumption
    is the single scalar smallness inequality for those canonical budgets. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_factor_norm_bounds
      hmn fp A_hat b_hat alpha
      (storedQRDiagDominantInvFactorBudget hmn A_hat)
      (storedQRPivotColumnNormBudget hmn A_hat)
      hm
      (storedQRDiagDominantInvFactorBudget_nonneg hmn A_hat hDD)
      (storedQRPivotColumnNormBudget_nonneg hmn A_hat)
      hDD
      (storedQRDiagDominantInvFactor_le_budget hmn A_hat)
      (storedQRPivotColumnNorm_le_budget hmn A_hat)
      hsmall
/-- Canonical finite-maximum compact-product smallness from an explicit
    relative-budget cap.

    This is the positive-budget sibling of the zero-relative-budget endpoint.
    If the stored compact relative budget is bounded by a nonnegative scalar
    `cmax`, then it is enough to check the canonical scalar product condition
    with `cmax`.  The theorem still exposes a genuine numerical smallness
    assumption: it does not prove the cap or the scalar inequality from the
    stored QR recurrence. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_relativeBudget_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hcmax_nonneg : 0 ≤ cmax)
    (hc_le :
      storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha ≤ cmax)
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (cmax * storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  let c : ℝ :=
    storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
  let D : ℝ := storedQRDiagDominantInvFactorBudget hmn A_hat
  let N : ℝ := storedQRPivotColumnNormBudget hmn A_hat
  have hD_nonneg : 0 ≤ D := by
    simpa [D] using storedQRDiagDominantInvFactorBudget_nonneg hmn A_hat hDD
  have hN_nonneg : 0 ≤ N := by
    simpa [N] using storedQRPivotColumnNormBudget_nonneg hmn A_hat
  have hc_nonneg : 0 ≤ c := by
    simpa [c] using
      storedQRCompactSequenceRelativeBudget_nonneg
        hmn fp A_hat b_hat alpha hm
  have hc_bound : c ≤ cmax := by
    simpa [c] using hc_le
  have hcN_bound : c * N ≤ cmax * N :=
    mul_le_mul_of_nonneg_right hc_bound hN_nonneg
  have hcN_nonneg : 0 ≤ c * N := mul_nonneg hc_nonneg hN_nonneg
  have hcmaxN_nonneg : 0 ≤ cmax * N := mul_nonneg hcmax_nonneg hN_nonneg
  have hsq_bound : (c * N) ^ 2 ≤ (cmax * N) ^ 2 := by
    nlinarith [hcN_bound, hcN_nonneg, hcmaxN_nonneg]
  have hm_nonneg : 0 ≤ (m : ℝ) := Nat.cast_nonneg m
  have hright_bound :
      (m : ℝ) * (c * N) ^ 2 ≤ (m : ℝ) * (cmax * N) ^ 2 :=
    mul_le_mul_of_nonneg_left hsq_bound hm_nonneg
  have hright_nonneg : 0 ≤ (m : ℝ) * (c * N) ^ 2 :=
    mul_nonneg hm_nonneg (sq_nonneg _)
  have hleft_nonneg : 0 ≤ 2 * D := by
    nlinarith
  have hprod_bound :
      2 * D * ((m : ℝ) * (c * N) ^ 2) ≤
        2 * D * ((m : ℝ) * (cmax * N) ^ 2) :=
    mul_le_mul_of_nonneg_left hright_bound hleft_nonneg
  have hsmall_actual :
      2 * D * ((m : ℝ) * (c * N) ^ 2) < 1 :=
    lt_of_le_of_lt hprod_bound (by simpa [D, N] using hsmall)
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp A_hat b_hat alpha hm hDD
      (by simpa [c, D, N] using hsmall_actual)
/-- Canonical finite-maximum compact-product smallness from a uniform per-step
    compact-panel relative budget.

    If every stored QR step has compact relative budget at most `cStep`, then
    the sequence budget is at most `n * cStep`.  Therefore the positive-budget
    product-smallness condition can be checked with the explicit cap
    `n * cStep`.  This still leaves the per-step cap and scalar inequality as
    visible numerical obligations. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_stepBudget_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cStep : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hcStep_nonneg : 0 ≤ cStep)
    (hStep : ∀ k : Fin n,
      storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤ cStep)
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) * cStep) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  have hcmax_nonneg : 0 ≤ (n : ℝ) * cStep :=
    mul_nonneg (Nat.cast_nonneg n) hcStep_nonneg
  have hseq_le :
      storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha ≤
        (n : ℝ) * cStep :=
    storedQRCompactSequenceRelativeBudget_le_mul_of_step_le
      hmn fp A_hat b_hat alpha cStep hStep
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_relativeBudget_le
      hmn fp A_hat b_hat alpha ((n : ℝ) * cStep) hm hDD
      hcmax_nonneg hseq_le hsmall
/-- Canonical finite-maximum compact-product smallness from vector-level
    compact Householder column and RHS budget caps.

    This is the solver-facing form of the one-step panel-cap reduction: if each
    stored stage's panel-column compact relative budget is at most `cCol` and
    each RHS compact relative budget is at most `cRhs`, then the one-step panel
    budget is at most `n * cCol + cRhs`, and the sequence cap is
    `n * (n * cCol + cRhs)`. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_columnRhsBudget_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cCol cRhs : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hcCol_nonneg : 0 ≤ cCol)
    (hcRhs_nonneg : 0 ≤ cRhs)
    (hCol : ∀ k : Fin n, ∀ j : Fin n,
      householderCompactRelativeBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha k.val)
        (storedQRSignedStageBeta hmn A_hat alpha k.val)
        (fun i : Fin m => A_hat k.val i j) ≤ cCol)
    (hRhs : ∀ k : Fin n,
      householderCompactRelativeBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha k.val)
        (storedQRSignedStageBeta hmn A_hat alpha k.val)
        (b_hat k.val) ≤ cRhs)
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * cCol + cRhs)) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  let cStep : ℝ := (n : ℝ) * cCol + cRhs
  have hcStep_nonneg : 0 ≤ cStep := by
    exact add_nonneg
      (mul_nonneg (Nat.cast_nonneg n) hcCol_nonneg)
      hcRhs_nonneg
  have hStep : ∀ k : Fin n,
      storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤
        cStep := by
    intro k
    exact
      storedQRCompactStepRelativeBudget_le_mul_add_of_column_rhs_le
        hmn fp A_hat b_hat alpha k cCol cRhs
        (hCol k) (hRhs k)
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_stepBudget_le
      hmn fp A_hat b_hat alpha cStep hm hDD hcStep_nonneg hStep
      (by simpa [cStep] using hsmall)
/-- Canonical finite-maximum compact-product smallness from primitive
    vector-level compact Householder norm-budget caps.

    Compared with
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_columnRhsBudget_le`,
    the vector hypotheses are stated before division by the column/RHS norms:
    each compact norm budget is at most `cCol * ‖A_k(:,j)‖₂` or
    `cRhs * ‖b_k‖₂`.  The theorem first converts those hypotheses into
    relative-budget caps and then uses the same explicit sequence cap
    `n * (n * cCol + cRhs)`. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_columnRhsNormBudget_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cCol cRhs : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hcCol_nonneg : 0 ≤ cCol)
    (hcRhs_nonneg : 0 ≤ cRhs)
    (hCol : ∀ k : Fin n, ∀ j : Fin n,
      householderCompactNormBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha k.val)
        (storedQRSignedStageBeta hmn A_hat alpha k.val)
        (fun i : Fin m => A_hat k.val i j) ≤
          cCol * vecNorm2 (fun i : Fin m => A_hat k.val i j))
    (hRhs : ∀ k : Fin n,
      householderCompactNormBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha k.val)
        (storedQRSignedStageBeta hmn A_hat alpha k.val)
        (b_hat k.val) ≤ cRhs * vecNorm2 (b_hat k.val))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * cCol + cRhs)) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  let cStep : ℝ := (n : ℝ) * cCol + cRhs
  have hcStep_nonneg : 0 ≤ cStep := by
    exact add_nonneg
      (mul_nonneg (Nat.cast_nonneg n) hcCol_nonneg)
      hcRhs_nonneg
  have hStep : ∀ k : Fin n,
      storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤
        cStep := by
    intro k
    exact
      storedQRCompactStepRelativeBudget_le_mul_add_of_normBudget_le_mul
        hmn fp A_hat b_hat alpha k cCol cRhs
        hcCol_nonneg hcRhs_nonneg
        (hCol k) (hRhs k)
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_stepBudget_le
      hmn fp A_hat b_hat alpha cStep hm hDD hcStep_nonneg hStep
      (by simpa [cStep] using hsmall)
/-- Canonical finite-maximum compact-product smallness from componentwise
    compact Householder column/RHS caps.

    This is the componentwise primitive-facing version of
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_columnRhsNormBudget_le`:
    each explicit compact component budget is bounded by `cCol * |A_k(i,j)|`
    or `cRhs * |b_k(i)|`, and the local vector-norm adapter turns those
    inequalities into the norm-budget caps consumed by the product theorem. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_columnRhsComponentBudget_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cCol cRhs : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hcCol_nonneg : 0 ≤ cCol)
    (hcRhs_nonneg : 0 ≤ cRhs)
    (hCol : ∀ k : Fin n, ∀ j : Fin n, ∀ i : Fin m,
      householderCompactComponentBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha k.val)
        (storedQRSignedStageBeta hmn A_hat alpha k.val)
        (fun a : Fin m => A_hat k.val a j) i ≤
          cCol * |A_hat k.val i j|)
    (hRhs : ∀ k : Fin n, ∀ i : Fin m,
      householderCompactComponentBudget fp m
        (storedQRSignedStageVector hmn A_hat alpha k.val)
        (storedQRSignedStageBeta hmn A_hat alpha k.val)
        (b_hat k.val) i ≤ cRhs * |b_hat k.val i|)
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * cCol + cRhs)) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  let cStep : ℝ := (n : ℝ) * cCol + cRhs
  have hcStep_nonneg : 0 ≤ cStep := by
    exact add_nonneg
      (mul_nonneg (Nat.cast_nonneg n) hcCol_nonneg)
      hcRhs_nonneg
  have hStep : ∀ k : Fin n,
      storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤
        cStep := by
    intro k
    exact
      storedQRCompactStepRelativeBudget_le_mul_add_of_componentBudget_le_mul_abs
        hmn fp A_hat b_hat alpha k cCol cRhs
        hm hcCol_nonneg hcRhs_nonneg
        (hCol k) (hRhs k)
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_stepBudget_le
      hmn fp A_hat b_hat alpha cStep hm hDD hcStep_nonneg hStep
      (by simpa [cStep] using hsmall)
/-- Canonical finite-maximum compact-product smallness from the explicit
    reflector-dependent compact norm coefficient.

    This avoids assuming entrywise relative caps of the form
    `budget_i <= c * |input_i|`.  Instead each stored stage uses the valid
    normwise coefficient produced by the compact dot/scale/subtract arithmetic,
    and the caller only supplies a uniform bound `cHH` on those finite stage
    coefficients plus the resulting scalar smallness inequality. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeff_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (cHH : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hcHH_nonneg : 0 ≤ cHH)
    (hCoeff : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ cHH)
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * cHH + cHH)) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  let cStep : ℝ := (n : ℝ) * cHH + cHH
  have hcStep_nonneg : 0 ≤ cStep := by
    exact add_nonneg
      (mul_nonneg (Nat.cast_nonneg n) hcHH_nonneg)
      hcHH_nonneg
  have hStep : ∀ k : Fin n,
      storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤
        cStep := by
    intro k
    have hlocal :
        storedQRCompactStepRelativeBudget hmn fp A_hat b_hat alpha k ≤
          (n : ℝ) *
              storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k +
            storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k :=
      storedQRCompactStepRelativeBudget_le_mul_add_normBudgetCoeff
        hmn fp A_hat b_hat alpha hm k
    have hcap :
        (n : ℝ) *
              storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k +
            storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤
          cStep := by
      exact add_le_add
        (mul_le_mul_of_nonneg_left (hCoeff k) (Nat.cast_nonneg n))
        (hCoeff k)
    exact hlocal.trans hcap
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_stepBudget_le
      hmn fp A_hat b_hat alpha cStep hm hDD hcStep_nonneg hStep
      (by simpa [cStep] using hsmall)
/-- Canonical finite maximum of the reflector-dependent compact norm
    coefficients over the stored QR stages.  The value is `0` when there are no
    pivots. -/
noncomputable def storedQRCompactStepNormBudgetCoeffBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ) : ℝ :=
  if hn : 0 < n then
    Finset.sup' (Finset.univ : Finset (Fin n))
      (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩)
      (fun k => storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k)
  else 0
/-- Each stored-stage reflector-dependent compact norm coefficient is bounded
    by its canonical finite maximum. -/
theorem storedQRCompactStepNormBudgetCoeff_le_budget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ) (k : Fin n) :
    storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤
      storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha := by
  classical
  unfold storedQRCompactStepNormBudgetCoeffBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
        (f := fun k => storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k)
        (Finset.mem_univ k)
  · have hnpos : 0 < n := lt_of_le_of_lt (Nat.zero_le k.val) k.isLt
    exact False.elim (hn hnpos)
/-- The canonical finite maximum of stored-stage compact norm coefficients is
    nonnegative. -/
theorem storedQRCompactStepNormBudgetCoeffBudget_nonneg
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ) (hm : gammaValid fp m) :
    0 ≤ storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha := by
  classical
  unfold storedQRCompactStepNormBudgetCoeffBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    let k0 : Fin n := ⟨0, hn⟩
    exact
      le_trans
        (storedQRCompactStepNormBudgetCoeff_nonneg hmn fp A_hat alpha hm k0)
        (Finset.le_sup' (s := (Finset.univ : Finset (Fin n)))
          (f := fun k =>
            storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k)
          (Finset.mem_univ k0))
  · rw [dif_neg hn]
/-- If every stored-stage reflector-dependent compact norm coefficient is
    bounded by a scalar, then the canonical finite maximum is bounded by that
    scalar.

    The nonnegativity hypothesis is only needed for the zero-pivot case. -/
theorem storedQRCompactStepNormBudgetCoeffBudget_le_of_forall_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Cmax : ℝ)
    (hCmax_nonneg : 0 ≤ Cmax)
    (hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax) :
    storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha ≤ Cmax := by
  classical
  unfold storedQRCompactStepNormBudgetCoeffBudget
  by_cases hn : 0 < n
  · rw [dif_pos hn]
    exact
      Finset.sup'_le
        (H := (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩))
        (f := fun k : Fin n =>
          storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k)
        (by
          intro k _hk
          exact hC k)
  · rw [dif_neg hn]
    exact hCmax_nonneg
/-- The canonical finite maximum of stored-stage compact norm coefficients is
    bounded by the explicit Householder scalar factor once each signed stage
    satisfies a pivot-local `|β_k| * ‖v_k‖₂^2` bound.

    This is the coefficient-estimate handoff for the compact QR product route:
    it reduces the abstract `Cmax` hypothesis to a uniform bound on the
    reflector normalization quantity. -/
theorem storedQRCompactStepNormBudgetCoeffBudget_le_of_forall_abs_beta_norm_sq_le
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Bmax : ℝ)
    (hm : gammaValid fp m)
    (hBmax_nonneg : 0 ≤ Bmax)
    (hB : ∀ k : Fin n,
      |storedQRSignedStageBeta hmn A_hat alpha k.val| *
          vecNorm2 (storedQRSignedStageVector hmn A_hat alpha k.val) ^ 2 ≤
        Bmax) :
    storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha ≤
      fp.u + Bmax * householderCompactNormBudgetCoeffFactor fp m := by
  have hfactor_nonneg :
      0 ≤ householderCompactNormBudgetCoeffFactor fp m :=
    householderCompactNormBudgetCoeffFactor_nonneg fp m hm
  have hCmax_nonneg :
      0 ≤ fp.u + Bmax * householderCompactNormBudgetCoeffFactor fp m :=
    add_nonneg fp.u_nonneg (mul_nonneg hBmax_nonneg hfactor_nonneg)
  exact
    storedQRCompactStepNormBudgetCoeffBudget_le_of_forall_le
      hmn fp A_hat alpha
      (fp.u + Bmax * householderCompactNormBudgetCoeffFactor fp m)
      hCmax_nonneg
      (storedQRCompactStepNormBudgetCoeff_le_of_forall_abs_beta_norm_sq_le
        hmn fp A_hat alpha Bmax hm hB)
/-- Canonical finite-maximum coefficient bound from the source-shaped
    nonbreakdown hypothesis for the signed Householder stages.

    This closes the `Bmax` dependency of the compact-product route for the
    exact Householder normalization: each nonzero denominator gives
    `|β_k| ‖v_k‖₂² = 2`, hence the displayed coefficient maximum is bounded by
    `u + 2 * householderCompactNormBudgetCoeffFactor fp m`. -/
theorem storedQRCompactStepNormBudgetCoeffBudget_le_of_forall_source_den_ne_zero
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) :
    storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha ≤
      fp.u + 2 * householderCompactNormBudgetCoeffFactor fp m := by
  have hfactor_nonneg :
      0 ≤ householderCompactNormBudgetCoeffFactor fp m :=
    householderCompactNormBudgetCoeffFactor_nonneg fp m hm
  have hCmax_nonneg :
      0 ≤ fp.u + 2 * householderCompactNormBudgetCoeffFactor fp m :=
    add_nonneg fp.u_nonneg (mul_nonneg (by linarith) hfactor_nonneg)
  exact
    storedQRCompactStepNormBudgetCoeffBudget_le_of_forall_le
      hmn fp A_hat alpha
      (fp.u + 2 * householderCompactNormBudgetCoeffFactor fp m)
      hCmax_nonneg
      (storedQRCompactStepNormBudgetCoeff_le_of_forall_source_den_ne_zero
        hmn fp A_hat alpha hm hden)
/-- Canonical finite-maximum compact-product smallness from the explicit
    reflector-dependent compact norm coefficient.

    This is the finite-max version of
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeff_le`.
    Instead of asking the caller for an arbitrary uniform coefficient `cHH` and
    a proof that every stage coefficient is below it, it chooses `cHH` to be the
    finite maximum of the stored-stage coefficients.  The only remaining
    numerical compact-product obligation is the scalar smallness inequality
    using that displayed finite maximum. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                  storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                  storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeff_le
      hmn fp A_hat b_hat alpha
      (storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)
      hm hDD
      (storedQRCompactStepNormBudgetCoeffBudget_nonneg hmn fp A_hat alpha hm)
      (storedQRCompactStepNormBudgetCoeff_le_budget hmn fp A_hat alpha)
      hsmall
/-- Monotonicity certificate for the canonical norm-coefficient scalar
    smallness condition.

    If the diagonal-dominant inverse budget, the explicit compact Householder
    coefficient maximum, and the pivot-column norm budget are each bounded by
    route constants `Dmax`, `Cmax`, and `Nmax`, then the exact
    scalar condition used by the canonical coefficient theorem follows from
    the same condition with those route constants.

    This is only scalar algebra.  It does not prove the three bounds, diagonal
    dominance, or the numerical smallness of the displayed route constants.
    Nonnegativity of those constants follows from the domination hypotheses. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax :
      storedQRDiagDominantInvFactorBudget hmn A_hat ≤ Dmax)
    (hCmax :
      storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha ≤ Cmax)
    (hNmax :
      storedQRPivotColumnNormBudget hmn A_hat ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  let D : ℝ := storedQRDiagDominantInvFactorBudget hmn A_hat
  let C : ℝ := storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha
  let N : ℝ := storedQRPivotColumnNormBudget hmn A_hat
  have hD_nonneg : 0 ≤ D := by
    simpa [D] using storedQRDiagDominantInvFactorBudget_nonneg hmn A_hat hDD
  have hC_nonneg : 0 ≤ C := by
    simpa [C] using
      storedQRCompactStepNormBudgetCoeffBudget_nonneg hmn fp A_hat alpha hm
  have hN_nonneg : 0 ≤ N := by
    simpa [N] using storedQRPivotColumnNormBudget_nonneg hmn A_hat
  have hD_bound : D ≤ Dmax := by
    simpa [D] using hDmax
  have hC_bound : C ≤ Cmax := by
    simpa [C] using hCmax
  have hN_bound : N ≤ Nmax := by
    simpa [N] using hNmax
  have hDmax_nonneg : 0 ≤ Dmax := le_trans hD_nonneg hD_bound
  have hCmax_nonneg : 0 ≤ Cmax := le_trans hC_nonneg hC_bound
  have hNmax_nonneg : 0 ≤ Nmax := le_trans hN_nonneg hN_bound
  have hn_nonneg : 0 ≤ (n : ℝ) := Nat.cast_nonneg n
  have hm_nonneg : 0 ≤ (m : ℝ) := Nat.cast_nonneg m
  have hCsum_bound : (n : ℝ) * C + C ≤ (n : ℝ) * Cmax + Cmax := by
    have hmul : (n : ℝ) * C ≤ (n : ℝ) * Cmax :=
      mul_le_mul_of_nonneg_left hC_bound hn_nonneg
    linarith
  have hCsum_nonneg : 0 ≤ (n : ℝ) * C + C := by
    nlinarith [mul_nonneg hn_nonneg hC_nonneg, hC_nonneg]
  have hCmaxsum_nonneg : 0 ≤ (n : ℝ) * Cmax + Cmax := by
    nlinarith [mul_nonneg hn_nonneg hCmax_nonneg, hCmax_nonneg]
  have hseq_bound :
      (n : ℝ) * ((n : ℝ) * C + C) ≤
        (n : ℝ) * ((n : ℝ) * Cmax + Cmax) :=
    mul_le_mul_of_nonneg_left hCsum_bound hn_nonneg
  have hseq_nonneg : 0 ≤ (n : ℝ) * ((n : ℝ) * C + C) :=
    mul_nonneg hn_nonneg hCsum_nonneg
  have hseqmax_nonneg : 0 ≤ (n : ℝ) * ((n : ℝ) * Cmax + Cmax) :=
    mul_nonneg hn_nonneg hCmaxsum_nonneg
  have hseqN_bound :
      ((n : ℝ) * ((n : ℝ) * C + C)) * N ≤
        ((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax :=
    mul_le_mul hseq_bound hN_bound hN_nonneg hseqmax_nonneg
  have hseqN_nonneg : 0 ≤ ((n : ℝ) * ((n : ℝ) * C + C)) * N :=
    mul_nonneg hseq_nonneg hN_nonneg
  have hseqNmax_nonneg :
      0 ≤ ((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax :=
    mul_nonneg hseqmax_nonneg hNmax_nonneg
  have hsq_bound :
      (((n : ℝ) * ((n : ℝ) * C + C)) * N) ^ 2 ≤
        (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2 := by
    nlinarith [hseqN_bound, hseqN_nonneg, hseqNmax_nonneg]
  have hright_bound :
      (m : ℝ) * (((n : ℝ) * ((n : ℝ) * C + C)) * N) ^ 2 ≤
        (m : ℝ) *
          (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2 :=
    mul_le_mul_of_nonneg_left hsq_bound hm_nonneg
  have hright_nonneg :
      0 ≤ (m : ℝ) * (((n : ℝ) * ((n : ℝ) * C + C)) * N) ^ 2 :=
    mul_nonneg hm_nonneg (sq_nonneg _)
  have hleft_bound : 2 * D ≤ 2 * Dmax := by nlinarith
  have hleftmax_nonneg : 0 ≤ 2 * Dmax := by nlinarith
  have hprod_bound :
      2 * D * ((m : ℝ) * (((n : ℝ) * ((n : ℝ) * C + C)) * N) ^ 2) ≤
        2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) :=
    mul_le_mul hleft_bound hright_bound hright_nonneg hleftmax_nonneg
  have hactual :
      2 * D * ((m : ℝ) * (((n : ℝ) * ((n : ℝ) * C + C)) * N) ^ 2) < 1 :=
    lt_of_le_of_lt hprod_bound hsmall
  simpa [D, C, N] using hactual
/-- Canonical finite-maximum compact-product smallness from explicit route
    bounds on the three displayed maxima.

    This packages `storedQRCompactNormBudgetCoeffSmallness_of_bounds` with the
    existing coefficient-maximum product theorem.  It reduces the scalar
    obligation to route constants `Dmax`, `Cmax`, and `Nmax` that dominate the
    canonical budgets; it does not prove those bounds or local diagonal
    dominance. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax :
      storedQRDiagDominantInvFactorBudget hmn A_hat ≤ Dmax)
    (hCmax :
      storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha ≤ Cmax)
    (hNmax :
      storedQRPivotColumnNormBudget hmn A_hat ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_bounds
        hmn fp A_hat alpha Dmax Cmax Nmax hm hDD
        hDmax hCmax hNmax hsmall)
/-- Pointwise route bounds for the canonical norm-coefficient scalar smallness
    condition.

    If every diagonal-dominant inverse factor, every stored compact
    norm-coefficient, and every displayed pivot-column norm are bounded by the
    displayed constants `Dmax`, `Cmax`, and `Nmax`, then the exact canonical
    scalar condition follows from the same cleaner inequality in those
    constants.  Nonnegativity of the displayed constants is only needed for the
    zero-pivot edge case of the finite maxima. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hCmax_nonneg : 0 ≤ Cmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  exact
    storedQRCompactNormBudgetCoeffSmallness_of_bounds
      hmn fp A_hat alpha Dmax Cmax Nmax hm hDD
      (storedQRDiagDominantInvFactorBudget_le_of_forall_le
        hmn A_hat Dmax hDmax_nonneg hD)
      (storedQRCompactStepNormBudgetCoeffBudget_le_of_forall_le
        hmn fp A_hat alpha Cmax hCmax_nonneg hC)
      (storedQRPivotColumnNormBudget_le_of_forall_le
        hmn A_hat Nmax hNmax_nonneg hN)
      hsmall
/-- Canonical finite-maximum compact-product smallness from pointwise route
    bounds on the three scalar budget families.

    This theorem is the product-budget version of
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds`: it reduces
    the global compact-product obligation to per-pivot upper bounds and one
    displayed scalar inequality.  It still does not prove those per-pivot
    estimates from a concrete QR recurrence. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget_pointwise_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hCmax_nonneg : 0 ≤ Cmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds
        hmn fp A_hat alpha Dmax Cmax Nmax hm hDD
        hDmax_nonneg hCmax_nonneg hNmax_nonneg hD hC hN hsmall)
/-- Pointwise route bounds with the compact Householder coefficient estimated
    from `|β_k| * ‖v_k‖₂^2`.

    This specializes
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds` by taking
    `Cmax = u + Bmax * householderCompactNormBudgetCoeffFactor fp m`.
    It reduces the compact-coefficient route to three visible per-pivot fields:
    diagonal-dominant inverse-factor bounds, signed-stage
    `|β_k| * ‖v_k‖₂^2` bounds, and pivot-column norm bounds. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_absBetaNormSq_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Bmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hBmax_nonneg : 0 ≤ Bmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hB : ∀ k : Fin n,
      |storedQRSignedStageBeta hmn A_hat alpha k.val| *
          vecNorm2 (storedQRSignedStageVector hmn A_hat alpha k.val) ^ 2 ≤
        Bmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                    (fp.u +
                      Bmax * householderCompactNormBudgetCoeffFactor fp m) +
                  (fp.u +
                    Bmax * householderCompactNormBudgetCoeffFactor fp m))) *
              Nmax) ^ 2) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  let Cmax : ℝ :=
    fp.u + Bmax * householderCompactNormBudgetCoeffFactor fp m
  have hCmax_nonneg : 0 ≤ Cmax := by
    dsimp [Cmax]
    exact add_nonneg fp.u_nonneg
      (mul_nonneg hBmax_nonneg
        (householderCompactNormBudgetCoeffFactor_nonneg fp m hm))
  have hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax := by
    intro k
    dsimp [Cmax]
    exact
      storedQRCompactStepNormBudgetCoeff_le_of_abs_beta_norm_sq_le
        hmn fp A_hat alpha Bmax hm k (hB k)
  have hsmallC :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1 := by
    simpa [Cmax, add_assoc] using hsmall
  exact
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds
      hmn fp A_hat alpha Dmax Cmax Nmax hm hDD
      hDmax_nonneg hCmax_nonneg hNmax_nonneg hD hC hN hsmallC
/-- Pointwise route bounds with the compact Householder coefficient specialized
    to exact Householder normalization and source-shaped nonbreakdown.

    This is the concrete `Bmax = 2` version of the preceding beta-norm route.
    It removes the abstract pointwise `|β_k| ‖v_k‖₂² ≤ Bmax` assumption by
    reusing the exact `β = 2/(vᵀv)` normalization theorem for each nonzero
    signed-stage denominator. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                    (fp.u +
                      2 * householderCompactNormBudgetCoeffFactor fp m) +
                  (fp.u +
                    2 * householderCompactNormBudgetCoeffFactor fp m))) *
              Nmax) ^ 2) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  let Cmax : ℝ :=
    fp.u + 2 * householderCompactNormBudgetCoeffFactor fp m
  have hfactor_nonneg :
      0 ≤ householderCompactNormBudgetCoeffFactor fp m :=
    householderCompactNormBudgetCoeffFactor_nonneg fp m hm
  have hCmax_nonneg : 0 ≤ Cmax := by
    dsimp [Cmax]
    exact add_nonneg fp.u_nonneg (mul_nonneg (by linarith) hfactor_nonneg)
  have hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax :=
    storedQRCompactStepNormBudgetCoeff_le_of_forall_source_den_ne_zero
      hmn fp A_hat alpha hm hden
  have hsmallC :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1 := by
    simpa [Cmax, add_assoc] using hsmall
  exact
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds
      hmn fp A_hat alpha Dmax Cmax Nmax hm hDD
      hDmax_nonneg hCmax_nonneg hNmax_nonneg hD hC hN hsmallC
/-- Source-denominator coefficient route with the scalar smallness written in
    the compact form `n * (n + 1) * (u + 2F_m)`.

    This is only an algebraic normalization of
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_bounds`.
    It keeps the numerical scalar-smallness condition explicit, but presents
    the exact-normalization coefficient branch in the source-facing form used
    by the route ledger. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_simple_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) *
                    (fp.u +
                      2 * householderCompactNormBudgetCoeffFactor fp m)) *
                Nmax) ^ 2)) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  refine
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_bounds
      hmn fp A_hat alpha Dmax Nmax hm hDD
      hDmax_nonneg hNmax_nonneg hD hden hN ?_
  ring_nf at hsmall ⊢
  simpa using hsmall
/-- Source-denominator coefficient route with explicit caps on the unit roundoff
    and the Householder coefficient factor.

    This is a scalar-smallness dependency for the red LS.2g route: if the
    route constants `Dcap` and `Ncap` bound the local diagonal-dominant inverse
    factors and pivot-column norms, and if `Ucap` and `Fcap` bound `fp.u` and
    `householderCompactNormBudgetCoeffFactor fp m`, then the source-facing
    smallness condition follows from the same displayed inequality with those
    caps.  The theorem does not prove the `Dcap`/`Ncap` pointwise estimates,
    diagonal dominance, source nonbreakdown, or the numerical cap inequality. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_cap_bounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap Fcap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hFcap_nonneg : 0 ≤ Fcap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (hF : householderCompactNormBudgetCoeffFactor fp m ≤ Fcap)
    (hsmall :
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  refine
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_simple_bounds
      hmn fp A_hat alpha Dcap Ncap hm hDD
      hDcap_nonneg hNcap_nonneg hD hden hN ?_
  let F : ℝ := householderCompactNormBudgetCoeffFactor fp m
  let scale : ℝ := (n : ℝ) * ((n : ℝ) + 1)
  have hF_nonneg : 0 ≤ F := by
    simpa [F] using householderCompactNormBudgetCoeffFactor_nonneg fp m hm
  have hround_bound : fp.u + 2 * F ≤ Ucap + 2 * Fcap := by
    nlinarith [hu, hF]
  have hround_nonneg : 0 ≤ fp.u + 2 * F := by
    nlinarith [fp.u_nonneg, hF_nonneg]
  have hroundcap_nonneg : 0 ≤ Ucap + 2 * Fcap := by
    nlinarith [hUcap_nonneg, hFcap_nonneg]
  have hn_nonneg : 0 ≤ (n : ℝ) := Nat.cast_nonneg n
  have hn1_nonneg : 0 ≤ ((n : ℝ) + 1) := by
    nlinarith [hn_nonneg]
  have hscale_nonneg : 0 ≤ scale := by
    dsimp [scale]
    exact mul_nonneg hn_nonneg hn1_nonneg
  have hscale_bound :
      scale * (fp.u + 2 * F) ≤ scale * (Ucap + 2 * Fcap) :=
    mul_le_mul_of_nonneg_left hround_bound hscale_nonneg
  have hscale_actual_nonneg : 0 ≤ scale * (fp.u + 2 * F) :=
    mul_nonneg hscale_nonneg hround_nonneg
  have hscale_cap_nonneg : 0 ≤ scale * (Ucap + 2 * Fcap) :=
    mul_nonneg hscale_nonneg hroundcap_nonneg
  have hscaledN_bound :
      (scale * (fp.u + 2 * F)) * Ncap ≤
        (scale * (Ucap + 2 * Fcap)) * Ncap :=
    mul_le_mul_of_nonneg_right hscale_bound hNcap_nonneg
  have hscaledN_actual_nonneg : 0 ≤ (scale * (fp.u + 2 * F)) * Ncap :=
    mul_nonneg hscale_actual_nonneg hNcap_nonneg
  have hscaledN_cap_nonneg : 0 ≤ (scale * (Ucap + 2 * Fcap)) * Ncap :=
    mul_nonneg hscale_cap_nonneg hNcap_nonneg
  have hsq_bound :
      ((scale * (fp.u + 2 * F)) * Ncap) ^ 2 ≤
        ((scale * (Ucap + 2 * Fcap)) * Ncap) ^ 2 := by
    nlinarith [hscaledN_bound, hscaledN_actual_nonneg,
      hscaledN_cap_nonneg]
  have hm_nonneg : 0 ≤ (m : ℝ) := Nat.cast_nonneg m
  have hright_bound :
      (m : ℝ) * ((scale * (fp.u + 2 * F)) * Ncap) ^ 2 ≤
        (m : ℝ) * ((scale * (Ucap + 2 * Fcap)) * Ncap) ^ 2 :=
    mul_le_mul_of_nonneg_left hsq_bound hm_nonneg
  have hright_nonneg :
      0 ≤ (m : ℝ) * ((scale * (fp.u + 2 * F)) * Ncap) ^ 2 :=
    mul_nonneg hm_nonneg (sq_nonneg _)
  have hleft_nonneg : 0 ≤ 2 * Dcap := by
    nlinarith [hDcap_nonneg]
  have hprod_bound :
      2 * Dcap *
          ((m : ℝ) * ((scale * (fp.u + 2 * F)) * Ncap) ^ 2) ≤
        2 * Dcap *
          ((m : ℝ) * ((scale * (Ucap + 2 * Fcap)) * Ncap) ^ 2) :=
    mul_le_mul_of_nonneg_left hright_bound hleft_nonneg
  have hactual :
      2 * Dcap *
          ((m : ℝ) * ((scale * (fp.u + 2 * F)) * Ncap) ^ 2) < 1 :=
    lt_of_le_of_lt hprod_bound (by
      simpa [scale, mul_assoc] using hsmall)
  simpa [F, scale, mul_assoc] using hactual
/-- Source-denominator coefficient route with the Householder factor cap
    derived directly from a unit-roundoff cap.

    Compared with
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_cap_bounds`,
    this theorem removes the separate hypothesis
    `householderCompactNormBudgetCoeffFactor fp m ≤ Fcap`.  The displayed
    `Fcap` is the explicit polynomial in `Ucap` and `Gcap`, while the remaining
    route obligations are the primitive unit-roundoff cap, the rational gamma
    cap, pointwise `Dcap`/`Ncap` bounds, source nonbreakdown, and the scalar
    cap-smallness inequality. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uGammaCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap Gcap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hGcap : ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap) ≤ Gcap)
    (hsmall :
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  let Fcap : ℝ :=
    Ucap * (1 + Gcap) * (1 + Ucap) +
      Ucap * (1 + Gcap) +
      Gcap +
      Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
  have hF : householderCompactNormBudgetCoeffFactor fp m ≤ Fcap := by
    simpa [Fcap] using
      householderCompactNormBudgetCoeffFactor_le_of_u_cap_gamma_cap
        fp m Ucap Gcap hUcap_nonneg hu huCap hGcap
  have hFcap_nonneg : 0 ≤ Fcap :=
    le_trans (householderCompactNormBudgetCoeffFactor_nonneg fp m hm) hF
  exact
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_cap_bounds
      hmn fp A_hat alpha Dcap Ncap Ucap Fcap hm hDD
      hDcap_nonneg hNcap_nonneg hUcap_nonneg hFcap_nonneg
      hD hden hN hu hF (by simpa [Fcap] using hsmall)
/-- Source-denominator coefficient route with the rational gamma cap chosen
    explicitly from the unit-roundoff cap.

    Compared with
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uGammaCapBounds`,
    this theorem removes the separate `Gcap` parameter and rational-domination
    hypothesis by setting
    `Gcap = (m * Ucap) / (1 - m * Ucap)`.  The remaining numerical obligation is
    the same displayed scalar-smallness inequality with that choice substituted.
    It still leaves the primitive unit-roundoff cap, pointwise `Dcap`/`Ncap`
    bounds, source nonbreakdown, and scalar cap-smallness visible. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uRationalGammaCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  let Gcap : ℝ := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
  exact
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uGammaCapBounds
      hmn fp A_hat alpha Dcap Ncap Ucap Gcap hm hDD
      hDcap_nonneg hNcap_nonneg hUcap_nonneg
      hD hden hN hu huCap (le_refl Gcap) (by simpa [Gcap] using hsmall)
/-- Canonical finite-maximum specialization of the rational gamma cap route.

    This removes the separate `Dcap`/`Ncap` pointwise-domination fields by
    choosing them to be the repository's canonical finite maxima:
    `storedQRDiagDominantInvFactorBudget` and `storedQRPivotColumnNormBudget`.
    It still leaves source nonbreakdown, local diagonal dominance, the
    unit-roundoff cap, and the displayed scalar-smallness inequality visible. -/
theorem storedQRCompactNormBudgetCoeffSmallness_of_source_den_ne_zero_uRationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
        ((m : ℝ) *
          (((n : ℝ) *
              ((n : ℝ) *
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
            storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
      1 := by
  let Dcap : ℝ := storedQRDiagDominantInvFactorBudget hmn A_hat
  let Ncap : ℝ := storedQRPivotColumnNormBudget hmn A_hat
  exact
    storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uRationalGammaCapBounds
      hmn fp A_hat alpha Dcap Ncap Ucap hm hDD
      (by simpa [Dcap] using storedQRDiagDominantInvFactorBudget_nonneg hmn A_hat hDD)
      (by simpa [Ncap] using storedQRPivotColumnNormBudget_nonneg hmn A_hat)
      hUcap_nonneg
      (by
        intro k
        simpa [Dcap] using storedQRDiagDominantInvFactor_le_budget hmn A_hat k)
      hden
      (by
        intro k
        simpa [Ncap] using storedQRPivotColumnNorm_le_budget hmn A_hat k)
      hu huCap
      (by simpa [Dcap, Ncap] using hsmall)
/-- Compact-product smallness from pointwise route bounds where the compact
    Householder coefficient is estimated through
    `|β_k| * ‖v_k‖₂^2`.

    This theorem replaces the displayed `Cmax` field of the previous
    pointwise-bound route with the explicit scalar
    `u + Bmax * householderCompactNormBudgetCoeffFactor fp m`. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_absBetaNormSqPointwiseBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Bmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hBmax_nonneg : 0 ≤ Bmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hB : ∀ k : Fin n,
      |storedQRSignedStageBeta hmn A_hat alpha k.val| *
          vecNorm2 (storedQRSignedStageVector hmn A_hat alpha k.val) ^ 2 ≤
        Bmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                    (fp.u +
                      Bmax * householderCompactNormBudgetCoeffFactor fp m) +
                  (fp.u +
                    Bmax * householderCompactNormBudgetCoeffFactor fp m))) *
              Nmax) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_absBetaNormSq_bounds
        hmn fp A_hat alpha Dmax Bmax Nmax hm hDD
        hDmax_nonneg hBmax_nonneg hNmax_nonneg hD hB hN hsmall)
/-- Compact-product smallness from pointwise route bounds and source-shaped
    signed-stage nonbreakdown.

    This is the concrete exact-normalization version of
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_absBetaNormSqPointwiseBounds`,
    with `Bmax` specialized to `2`. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenPointwiseBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                    (fp.u +
                      2 * householderCompactNormBudgetCoeffFactor fp m) +
                  (fp.u +
                    2 * householderCompactNormBudgetCoeffFactor fp m))) *
              Nmax) ^ 2) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_bounds
        hmn fp A_hat alpha Dmax Nmax hm hDD
        hDmax_nonneg hNmax_nonneg hD hden hN hsmall)
/-- Compact-product smallness from source-shaped nonbreakdown and the compact
    scalar condition `n * (n + 1) * (u + 2F_m)`.

    This is the source-facing scalar-smallness sibling of
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenPointwiseBounds`.
    It is still conditional on the displayed numerical inequality, local
    diagonal dominance, and the pointwise `Dmax`/`Nmax` route bounds. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenSimpleBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) *
                    (fp.u +
                      2 * householderCompactNormBudgetCoeffFactor fp m)) *
                Nmax) ^ 2)) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_simple_bounds
        hmn fp A_hat alpha Dmax Nmax hm hDD
        hDmax_nonneg hNmax_nonneg hD hden hN hsmall)
/-- Compact-product smallness from source-shaped nonbreakdown and explicit caps
    on the unit roundoff and Householder coefficient factor.

    This is the product-budget sibling of
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_cap_bounds`.
    It closes the scalar bookkeeping once the caller supplies cap estimates for
    `fp.u` and `householderCompactNormBudgetCoeffFactor fp m`, while leaving
    the pointwise inverse-factor and pivot-column bounds visible. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap Fcap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hFcap_nonneg : 0 ≤ Fcap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (hF : householderCompactNormBudgetCoeffFactor fp m ≤ Fcap)
    (hsmall :
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_cap_bounds
        hmn fp A_hat alpha Dcap Ncap Ucap Fcap hm hDD
        hDcap_nonneg hNcap_nonneg hUcap_nonneg hFcap_nonneg
        hD hden hN hu hF hsmall)
/-- Compact-product smallness from source-shaped nonbreakdown and a
    unit-roundoff-derived Householder factor cap.

    This product-budget sibling of
    `storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uGammaCapBounds`
    removes the independent `Fcap` proof obligation from the product route. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenUGammaCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap Gcap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hGcap : ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap) ≤ Gcap)
    (hsmall :
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uGammaCapBounds
        hmn fp A_hat alpha Dcap Ncap Ucap Gcap hm hDD
        hDcap_nonneg hNcap_nonneg hUcap_nonneg
        hD hden hN hu huCap hGcap hsmall)
/-- Compact-product smallness from source-shaped nonbreakdown with the rational
    gamma cap chosen explicitly from the unit-roundoff cap.

    This product-budget sibling of
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenUGammaCapBounds`
    removes the separate `Gcap` parameter and rational-domination hypothesis by
    setting `Gcap = (m * Ucap) / (1 - m * Ucap)`. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenURationalGammaCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uRationalGammaCapBounds
        hmn fp A_hat alpha Dcap Ncap Ucap hm hDD
        hDcap_nonneg hNcap_nonneg hUcap_nonneg
        hD hden hN hu huCap hsmall)
/-- Compact-product sibling of
    `storedQRCompactNormBudgetCoeffSmallness_of_source_den_ne_zero_uRationalGammaCanonicalBounds`.

    The route uses the canonical finite maxima for the diagonal-dominant inverse
    factors and pivot-column norms, so no external `Dcap`/`Ncap` pointwise
    domination certificates are exposed. -/
theorem storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_source_den_ne_zero_uRationalGammaCanonicalBounds
        hmn fp A_hat alpha Ucap hm hDD
        hUcap_nonneg hden hu huCap hsmall)
/-- Build the packaged off-diagonal-control invariant from the two remaining
    visible route-1 fields: local diagonal dominance and the canonical
    finite-maximum compact-product smallness inequality.

    This is a structure-level handoff.  It does not prove diagonal dominance or
    the scalar finite-max inequality from the stored QR recurrence; it only
    derives the packaged invariant once those source/domain facts are supplied. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSmallness
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  refine ⟨?_, hDD, ?_⟩
  · intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  · intro k hk
    let kk : Fin n := ⟨k, hk⟩
    have hDDfin : ∀ kk : Fin n,
        IsDiagDominantUpper (kk.val + 1)
          (qrLeadingBlock (A_hat kk.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
      intro kk
      exact hDD kk.val kk.isLt
    have hbudget :
        storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
      storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
        hmn fp A_hat b_hat alpha hm hDDfin hsmall
    have hle :
        storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha kk ≤
          storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha :=
      storedQRCompactSequenceProductExpr_le_budget hmn fp A_hat b_hat alpha kk
    exact lt_of_le_of_lt (by
      simpa [storedQRCompactSequenceProductExpr, kk] using hle) hbudget
/-- Build the packaged off-diagonal-control invariant from local diagonal
    dominance and the canonical finite maximum of the explicit compact
    Householder norm coefficient.

    Compared with
    `StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSmallness`,
    this theorem does not ask for a scalar condition involving the already
    assembled `storedQRCompactSequenceRelativeBudget`.  It uses the displayed
    coefficient maximum `storedQRCompactStepNormBudgetCoeffBudget` instead. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                  storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha +
                  storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha)) *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  refine ⟨?_, hDD, ?_⟩
  · intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  · intro k hk
    let kk : Fin n := ⟨k, hk⟩
    have hDDfin : ∀ kk : Fin n,
        IsDiagDominantUpper (kk.val + 1)
          (qrLeadingBlock (A_hat kk.val)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
      intro kk
      exact hDD kk.val kk.isLt
    have hbudget :
        storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
      storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
        hmn fp A_hat b_hat alpha hm hDDfin hsmall
    have hle :
        storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha kk ≤
          storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha :=
      storedQRCompactSequenceProductExpr_le_budget hmn fp A_hat b_hat alpha kk
    exact lt_of_le_of_lt (by
      simpa [storedQRCompactSequenceProductExpr, kk] using hle) hbudget
/-- Build the packaged off-diagonal-control invariant from local diagonal
    dominance and explicit route bounds on the three canonical scalar budgets.

    Compared with
    `StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness`,
    this theorem lets the caller certify the scalar condition using displayed
    constants `Dmax`, `Cmax`, and `Nmax` that dominate the canonical
    diagonal-dominant inverse factor, compact Householder coefficient maximum,
    and pivot-column norm budget. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffBoundedSmallness
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax :
      storedQRDiagDominantInvFactorBudget hmn A_hat ≤ Dmax)
    (hCmax :
      storedQRCompactStepNormBudgetCoeffBudget hmn fp A_hat alpha ≤ Cmax)
    (hNmax :
      storedQRPivotColumnNormBudget hmn A_hat ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_bounds
        hmn fp A_hat alpha Dmax Cmax Nmax hm hDDfin
        hDmax hCmax hNmax hsmall)
/-- Build the packaged off-diagonal-control invariant from local diagonal
    dominance and pointwise route bounds on the three scalar budget families.

    This is the pointwise-bound sibling of
    `StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffBoundedSmallness`.
    It closes the finite-maximum bookkeeping once each pivot's inverse factor,
    compact norm coefficient, and pivot-column norm have explicit displayed
    bounds.  It still does not prove diagonal dominance or those pointwise
    estimates from an arbitrary stored QR loop. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffPointwiseBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hCmax_nonneg : 0 ≤ Cmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_bounds
        hmn fp A_hat alpha Dmax Cmax Nmax hm hDDfin
        hDmax_nonneg hCmax_nonneg hNmax_nonneg hD hC hN hsmall)
/-- Build the packaged off-diagonal-control invariant from local diagonal
    dominance, pointwise inverse/pivot-column bounds, and pointwise
    `|β_k| * ‖v_k‖₂^2` bounds for the signed Householder stages.

    This is the invariant-level handoff for the compact coefficient estimate:
    the displayed coefficient maximum is specialized to
    `u + Bmax * householderCompactNormBudgetCoeffFactor fp m`.  It still does
    not prove local diagonal dominance, the pointwise estimates, or the scalar
    smallness inequality from an arbitrary QR recurrence. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxAbsBetaNormSqPointwiseBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Bmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hBmax_nonneg : 0 ≤ Bmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hB : ∀ k : Fin n,
      |storedQRSignedStageBeta hmn A_hat alpha k.val| *
          vecNorm2 (storedQRSignedStageVector hmn A_hat alpha k.val) ^ 2 ≤
        Bmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                    (fp.u +
                      Bmax * householderCompactNormBudgetCoeffFactor fp m) +
                  (fp.u +
                    Bmax * householderCompactNormBudgetCoeffFactor fp m))) *
              Nmax) ^ 2) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_absBetaNormSq_bounds
        hmn fp A_hat alpha Dmax Bmax Nmax hm hDDfin
        hDmax_nonneg hBmax_nonneg hNmax_nonneg hD hB hN hsmall)
/-- Build the packaged off-diagonal-control invariant from local diagonal
    dominance, pointwise inverse/pivot-column bounds, and source-shaped
    nonbreakdown of the signed Householder denominators.

    This is the concrete exact-normalization handoff for the compact
    coefficient route.  It replaces the abstract `Bmax` beta-norm hypothesis by
    the proved identity `|β_k| ‖v_k‖₂² = 2` for every nonzero signed-stage
    denominator. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenPointwiseBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                    (fp.u +
                      2 * householderCompactNormBudgetCoeffFactor fp m) +
                  (fp.u +
                    2 * householderCompactNormBudgetCoeffFactor fp m))) *
              Nmax) ^ 2) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_bounds
        hmn fp A_hat alpha Dmax Nmax hm hDDfin
        hDmax_nonneg hNmax_nonneg hD hden hN hsmall)
/-- Source-denominator off-diagonal-control handoff using the compact scalar
    condition `n * (n + 1) * (u + 2F_m)`.

    This theorem exposes the same remaining route fields as
    `StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenPointwiseBounds`,
    but with the scalar-smallness side condition in the simplified
    source-facing form. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenSimpleBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) *
                    (fp.u +
                      2 * householderCompactNormBudgetCoeffFactor fp m)) *
                Nmax) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_simple_bounds
        hmn fp A_hat alpha Dmax Nmax hm hDDfin
        hDmax_nonneg hNmax_nonneg hD hden hN hsmall)
/-- Source-denominator off-diagonal-control handoff from explicit caps on the
    unit roundoff and Householder coefficient factor.

    This packages the scalar cap bridge into the invariant used by the
    rectangular QR/preconditioner route.  The remaining visible obligations are
    exactly the source/domain fields that the cap bridge does not prove: local
    diagonal dominance, pointwise inverse-factor and pivot-column bounds,
    source nonbreakdown, and the displayed cap-smallness inequality. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap Fcap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hFcap_nonneg : 0 ≤ Fcap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (hF : householderCompactNormBudgetCoeffFactor fp m ≤ Fcap)
    (hsmall :
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_cap_bounds
        hmn fp A_hat alpha Dcap Ncap Ucap Fcap hm hDDfin
        hDcap_nonneg hNcap_nonneg hUcap_nonneg hFcap_nonneg
        hD hden hN hu hF hsmall)
/-- Source-denominator off-diagonal-control handoff from a unit-roundoff-derived
    compact Householder coefficient-factor cap.

    This removes the independent `Fcap` proof field from the invariant surface
    by composing the source-denominator cap route with the Householder
    coefficient-factor cap and the gamma cap from `Ucap`. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenUGammaCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap Gcap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hGcap : ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap) ≤ Gcap)
    (hsmall :
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uGammaCapBounds
        hmn fp A_hat alpha Dcap Ncap Ucap Gcap hm hDDfin
        hDcap_nonneg hNcap_nonneg hUcap_nonneg
        hD hden hN hu huCap hGcap hsmall)
/-- Source-denominator off-diagonal-control handoff from a unit-roundoff cap
    with the rational gamma cap chosen explicitly.

    Compared with
    `StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenUGammaCapBounds`,
    this theorem removes the separate `Gcap` parameter and rational-domination
    hypothesis by setting `Gcap = (m * Ucap) / (1 - m * Ucap)`. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenURationalGammaCapBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dcap Ncap Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDcap_nonneg : 0 ≤ Dcap)
    (hNcap_nonneg : 0 ≤ Ncap)
    (hUcap_nonneg : 0 ≤ Ucap)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dcap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Ncap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_pointwise_source_den_ne_zero_uRationalGammaCapBounds
        hmn fp A_hat alpha Dcap Ncap Ucap hm hDDfin
        hDcap_nonneg hNcap_nonneg hUcap_nonneg
        hD hden hN hu huCap hsmall)
/-- Source-denominator off-diagonal-control handoff with rational gamma and
    canonical finite maxima for the inverse-factor and pivot-column budgets.

    This is the packaged invariant sibling of
    `storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenURationalGammaCanonicalBounds`.
    It removes the separate `Dcap`/`Ncap` pointwise-domination fields while
    leaving the scalar cap-smallness, source nonbreakdown, local diagonal
    dominance, and unit-roundoff cap as visible route assumptions. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hDDfin : ∀ kk : Fin n,
      IsDiagDominantUpper (kk.val + 1)
        (qrLeadingBlock (A_hat kk.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le kk.isLt hmn)) kk.isLt) := by
    intro kk
    exact hDD kk.val kk.isLt
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxNormBudgetCoeffSmallness
      hmn fp A_hat b_hat alpha hm hDD
      (storedQRCompactNormBudgetCoeffSmallness_of_source_den_ne_zero_uRationalGammaCanonicalBounds
        hmn fp A_hat alpha Ucap hm hDDfin
        hUcap_nonneg hden hu huCap hsmall)
/-- Canonical rational-gamma off-diagonal-control handoff with source
    denominator nonbreakdown derived from signed-alpha/trailing-norm data.

    Compared with
    `StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds`,
    this wrapper no longer exposes the raw source-denominator hypothesis.
    It keeps the positive trailing-norm and sign-choice fields visible because
    those are the genuine source-domain facts needed to prove denominator
    nonbreakdown. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_trailingNormPos_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
      (storedQRSourceDenominator_ne_zero_of_trailingNorm2Sq_pos_mul_nonpos
        hmn A_hat alpha halpha htrailingPos hsign)
      hu huCap hsmall
/-- Canonical rational-gamma off-diagonal-control handoff with source
    denominator nonbreakdown derived from leading-block determinant data.

    This is the determinant/rank specialization of
    `StoredQROffDiagonalControlInvariant.of_diagDominant_trailingNormPos_finiteMaxSourceDenURationalGammaCanonicalBounds`.
    The previous and current leading principal blocks, together with the
    existing lower-zero shape, imply positive active trailing norms via
    `householderTrailingNorm2Sq_pos_of_leading_block_det_ne_zero`; the signed
    alpha data then gives the Householder source denominator nonbreakdown. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_leadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    exact
      householderTrailingNorm2Sq_pos_of_leading_block_det_ne_zero
        (A_hat k) (lt_of_lt_of_le hk hmn) hk
        (hdetPrev k hk) (hdetLead k hk) (hlowerPrev k hk)
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_trailingNormPos_finiteMaxSourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
      halpha htrailingPos hsign hu huCap hsmall
/-- Canonical rational-gamma off-diagonal-control handoff with both
    source-nonbreakdown inputs derived from the signed Householder definition.

    This is the algorithm-facing sibling of
    `StoredQROffDiagonalControlInvariant.of_diagDominant_leadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds`:
    callers provide the concrete signed-alpha formula used by the stored QR
    loop, and the theorem derives the two scalar signed-alpha facts internally.
    The remaining visible fields are the genuine route-1 assumptions: local
    diagonal dominance, previous/current leading-block nonsingularity,
    lower-zero shape, a unit-roundoff cap, and the canonical scalar smallness
    inequality. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_leadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0 := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_mul_pivot_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_leadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
      halpha hdetPrev hdetLead hlowerPrev hsign hu huCap hsmall
/-- Canonical rational-gamma off-diagonal-control handoff with the current
    leading-block determinant derived from local diagonal dominance.

    Compared with
    `StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_leadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds`,
    this wrapper keeps only the previous transposed leading-block determinant
    field visible.  The determinant of the current leading block follows from
    `IsDiagDominantUpper`, which already includes upper-triangular shape and
    nonzero diagonal entries. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_previousLeadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0)
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_leadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
      hAlphaDef hdetPrev hdetLead hlowerPrev hu huCap hsmall
/-- Canonical rational-gamma off-diagonal-control handoff with both local
    determinant fields derived from local diagonal dominance.

    Compared with
    `StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_previousLeadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds`,
    this wrapper removes the previous transposed leading-block determinant
    field as well.  The previous determinant follows because the previous block
    is the transpose orientation of the top-left part of the same
    diagonally-dominant current leading block.  The lower-zero field remains
    visible because it is still consumed by the trailing-norm/nonbreakdown
    bridge. -/
theorem StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_lowerPrev_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha := by
  have hdetPrev : ∀ k (hk : k < n),
      Matrix.det
        (qrPreviousLeadingBlockTranspose (A_hat k)
          (le_of_lt (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin k) (Fin k) ℝ) ≠ 0 := by
    intro k hk
    exact
      qrPreviousLeadingBlockTranspose_det_ne_zero_of_diagDominant_leadingBlock
        (A_hat k) (le_of_lt (lt_of_lt_of_le hk hmn))
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk
        (hDD k hk)
  exact
    StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_previousLeadingBlock_det_ne_zero_finiteMaxSourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
      hAlphaDef hdetPrev hlowerPrev hu huCap hsmall
/-- Route-elimination witness for the canonical rational-gamma scalar
    smallness condition.

    Even in the `1 x 1` diagonally dominant, source-nonbreakdown case with
    `fp.u <= Ucap` and `(m : ℝ) * Ucap < 1`, the displayed canonical scalar
    inequality need not hold.  The witness takes the abstract exact
    `FPModel.exactWithUnitRoundoff (1/2)`, the constant matrix `A = [1]`, and
    `Ucap = 1/2`; the canonical scalar expression evaluates to a concrete
    number greater than `1`.

    This is a theorem-statement correction for the QR bottleneck: the scalar
    inequality is a genuine numerical/domain assumption unless future work
    proves stronger scale, conditioning, or machine-model hypotheses. -/
theorem not_forall_diagDominant_sourceDen_uCap_implies_uRationalGammaCanonicalSmallness :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 1 → Fin 1 → ℝ)
      (alpha : ℕ → ℝ)
      (Ucap : ℝ),
      gammaValid fp 1 →
      (∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt)) →
      0 ≤ Ucap →
      (∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      fp.u ≤ Ucap →
      (1 : ℝ) * Ucap < 1 →
      (let Dcap := storedQRDiagDominantInvFactorBudget (by norm_num : 1 ≤ 1) A_hat
       let Ncap := storedQRPivotColumnNormBudget (by norm_num : 1 ≤ 1) A_hat
       let Gcap := ((1 : ℝ) * Ucap) / (1 - (1 : ℝ) * Ucap)
       let Fcap :=
          Ucap * (1 + Gcap) * (1 + Ucap) +
            Ucap * (1 + Gcap) +
            Gcap +
            Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
       2 * Dcap *
            ((1 : ℝ) *
              ((((1 : ℝ) * ((1 : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                  Ncap) ^ 2)) <
          1)) := by
  intro h
  let fp : FPModel := FPModel.exactWithUnitRoundoff ((1 : ℝ) / 2) (by norm_num)
  let A_hat : ℕ → Fin 1 → Fin 1 → ℝ := fun _ _ _ => 1
  let alpha : ℕ → ℝ := fun _ => 0
  have hm : gammaValid fp 1 := by
    norm_num [fp, FPModel.exactWithUnitRoundoff, gammaValid]
  have hDD : ∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt) := by
    intro k
    fin_cases k
    refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      fin_cases j
      omega
    · intro i
      fin_cases i
      norm_num [A_hat, qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i
      fin_cases j
      omega
  have hden : ∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 := by
    intro k hk
    interval_cases k
    norm_num [A_hat, alpha, householderTrailingActiveVector,
      householderActiveVector, householderTrailingPart]
  have hbad := h fp A_hat alpha ((1 : ℝ) / 2) hm hDD
    (by norm_num) hden (by norm_num [fp, FPModel.exactWithUnitRoundoff])
    (by norm_num)
  norm_num [storedQRDiagDominantInvFactorBudget,
    storedQRPivotColumnNormBudget, diagDominantUpperInvBudgetExpr,
    qrLeadingBlock, qrLeadingRow, qrLeadingColumn, vecNorm2, vecNorm2Sq,
    A_hat] at hbad
  ring_nf at hbad
  have hnot :
      ¬ (441 / 2 + 1 * Nat.rawCast 567 + 1 ^ 2 * (729 / 2) : ℝ) < 1 := by
    intro hlt
    norm_num at hlt
    exact (by norm_num : ¬ ((1152 : ℝ) < 1)) hlt
  exact hnot hbad
/-- Route-elimination witness for the canonical rational-gamma scalar
    smallness condition when the cap is the actual unit roundoff.

    The preceding route-elimination theorem shows that a visible cap `Ucap`
    does not by itself imply the displayed finite-max scalar smallness
    inequality.  This sibling removes the cap notation entirely: even if one
    plugs `fp.u` directly into the rational-gamma expression, diagonal
    dominance and source nonbreakdown do not force the scalar inequality. -/
theorem not_forall_diagDominant_sourceDen_actualU_implies_uRationalGammaCanonicalSmallness :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 1 → Fin 1 → ℝ)
      (alpha : ℕ → ℝ),
      gammaValid fp 1 →
      (∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt)) →
      (∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (1 : ℝ) * fp.u < 1 →
      (let Dcap := storedQRDiagDominantInvFactorBudget (by norm_num : 1 ≤ 1) A_hat
       let Ncap := storedQRPivotColumnNormBudget (by norm_num : 1 ≤ 1) A_hat
       let Gcap := ((1 : ℝ) * fp.u) / (1 - (1 : ℝ) * fp.u)
       let Fcap :=
          fp.u * (1 + Gcap) * (1 + fp.u) +
            fp.u * (1 + Gcap) +
            Gcap +
            fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
       2 * Dcap *
            ((1 : ℝ) *
              ((((1 : ℝ) * ((1 : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                  Ncap) ^ 2)) <
          1)) := by
  intro h
  let fp : FPModel := FPModel.exactWithUnitRoundoff ((1 : ℝ) / 2) (by norm_num)
  let A_hat : ℕ → Fin 1 → Fin 1 → ℝ := fun _ _ _ => 1
  let alpha : ℕ → ℝ := fun _ => 0
  have hm : gammaValid fp 1 := by
    norm_num [fp, FPModel.exactWithUnitRoundoff, gammaValid]
  have hDD : ∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt) := by
    intro k
    fin_cases k
    refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      fin_cases j
      omega
    · intro i
      fin_cases i
      norm_num [A_hat, qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i
      fin_cases j
      omega
  have hden : ∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 := by
    intro k hk
    interval_cases k
    norm_num [A_hat, alpha, householderTrailingActiveVector,
      householderActiveVector, householderTrailingPart]
  have hbad := h fp A_hat alpha hm hDD hden
    (by norm_num [fp, FPModel.exactWithUnitRoundoff])
  norm_num [storedQRDiagDominantInvFactorBudget,
    storedQRPivotColumnNormBudget, diagDominantUpperInvBudgetExpr,
    qrLeadingBlock, qrLeadingRow, qrLeadingColumn, vecNorm2, vecNorm2Sq,
    A_hat, fp, FPModel.exactWithUnitRoundoff] at hbad
  ring_nf at hbad
  have hnot : ¬ (((12 : ℝ) ^ 2) * 8 < 1) := by norm_num
  exact hnot hbad
/-- Route-elimination witness for the actual-unit scalar-smallness condition
    with the concrete signed Householder alpha formula visible.

    The signed-alpha source equation and source denominator nonbreakdown do not
    force the canonical finite-max scalar smallness inequality.  The same
    `1 x 1` exact-with-unit-roundoff witness as
    `not_forall_diagDominant_sourceDen_actualU_implies_uRationalGammaCanonicalSmallness`
    satisfies the signed-alpha equation (`alpha = -1` for the unit pivot), but
    the rational-gamma finite-max expression at `fp.u = 1/2` is still larger
    than `1`. -/
theorem not_forall_diagDominant_signedAlphaDef_sourceDen_actualU_implies_uRationalGammaCanonicalSmallness :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 1 → Fin 1 → ℝ)
      (alpha : ℕ → ℝ),
      gammaValid fp 1 →
      (∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt)) →
      (∀ k (hk : k < 1),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (1 : ℝ) * fp.u < 1 →
      (let Dcap := storedQRDiagDominantInvFactorBudget (by norm_num : 1 ≤ 1) A_hat
       let Ncap := storedQRPivotColumnNormBudget (by norm_num : 1 ≤ 1) A_hat
       let Gcap := ((1 : ℝ) * fp.u) / (1 - (1 : ℝ) * fp.u)
       let Fcap :=
          fp.u * (1 + Gcap) * (1 + fp.u) +
            fp.u * (1 + Gcap) +
            Gcap +
            fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
       2 * Dcap *
            ((1 : ℝ) *
              ((((1 : ℝ) * ((1 : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                  Ncap) ^ 2)) <
          1)) := by
  intro h
  let fp : FPModel := FPModel.exactWithUnitRoundoff ((1 : ℝ) / 2) (by norm_num)
  let A_hat : ℕ → Fin 1 → Fin 1 → ℝ := fun _ _ _ => 1
  let alpha : ℕ → ℝ := fun _ => -1
  have hm : gammaValid fp 1 := by
    norm_num [fp, FPModel.exactWithUnitRoundoff, gammaValid]
  have hDD : ∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt) := by
    intro k
    fin_cases k
    refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      fin_cases j
      omega
    · intro i
      fin_cases i
      norm_num [A_hat, qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i
      fin_cases j
      omega
  have hAlphaDef : ∀ k (hk : k < 1),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩ ⟨k, hk⟩) := by
    intro k hk
    interval_cases k
    norm_num [A_hat, alpha, signedHouseholderAlpha,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
  have hden : ∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 := by
    intro k hk
    interval_cases k
    norm_num [A_hat, alpha, householderTrailingActiveVector,
      householderActiveVector, householderTrailingPart]
  have hbad := h fp A_hat alpha hm hDD hAlphaDef hden
    (by norm_num [fp, FPModel.exactWithUnitRoundoff])
  norm_num [storedQRDiagDominantInvFactorBudget,
    storedQRPivotColumnNormBudget, diagDominantUpperInvBudgetExpr,
    qrLeadingBlock, qrLeadingRow, qrLeadingColumn, vecNorm2, vecNorm2Sq,
    A_hat, fp, FPModel.exactWithUnitRoundoff] at hbad
  ring_nf at hbad
  have hnot : ¬ (((12 : ℝ) ^ 2) * 8 < 1) := by norm_num
  exact hnot hbad
/-- Route-elimination witness for the actual-unit scalar-smallness condition
    after replacing `gammaValid` by the scalar unit-roundoff guard.

    This is the no-`gammaValid` sibling of
    `not_forall_diagDominant_signedAlphaDef_sourceDen_actualU_implies_uRationalGammaCanonicalSmallness`.
    Even with local diagonal dominance, the concrete signed-alpha equation,
    source denominator nonbreakdown, and `(1 : ℝ) * fp.u < 1`, the canonical
    finite-max rational-gamma scalar smallness condition is not automatic. -/
theorem not_forall_diagDominant_signedAlphaDef_sourceDen_actualU_no_gammaValid_implies_uRationalGammaCanonicalSmallness :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 1 → Fin 1 → ℝ)
      (alpha : ℕ → ℝ),
      (∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt)) →
      (∀ k (hk : k < 1),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 1),
        (∑ i : Fin 1,
          householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (1 : ℝ) * fp.u < 1 →
      (let Dcap := storedQRDiagDominantInvFactorBudget (by norm_num : 1 ≤ 1) A_hat
       let Ncap := storedQRPivotColumnNormBudget (by norm_num : 1 ≤ 1) A_hat
       let Gcap := ((1 : ℝ) * fp.u) / (1 - (1 : ℝ) * fp.u)
       let Fcap :=
          fp.u * (1 + Gcap) * (1 + fp.u) +
            fp.u * (1 + Gcap) +
            Gcap +
            fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
       2 * Dcap *
            ((1 : ℝ) *
              ((((1 : ℝ) * ((1 : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                  Ncap) ^ 2)) <
          1)) := by
  intro h
  exact
    not_forall_diagDominant_signedAlphaDef_sourceDen_actualU_implies_uRationalGammaCanonicalSmallness
      (by
        intro fp A_hat alpha _hm hDD hAlphaDef hden huSmall
        exact h fp A_hat alpha hDD hAlphaDef hden huSmall)
/-- Stored-recurrence route-elimination witness for the actual-unit
    scalar-smallness condition.

    After the stored-lower source-denominator reduction, the raw
    source-denominator field is no longer the right boundary.  This theorem
    rules out the next tempting shortcut: even if the actual stored panel
    recurrence, local diagonal dominance, the signed-alpha equation, and
    `(1 : ℝ) * fp.u < 1` are available, the canonical rational-gamma
    finite-max scalar-smallness inequality is not automatic. -/
theorem not_forall_diagDominant_signedAlphaDef_stored_trailing_sequence_actualU_no_gammaValid_implies_uRationalGammaCanonicalSmallness :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 1 → Fin 1 → ℝ)
      (alpha : ℕ → ℝ),
      (∀ k (hk : k < 1),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 1 1 k
            (householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 1
              (householderTrailingActiveVector 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt)) →
      (∀ k (hk : k < 1),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩ ⟨k, hk⟩)) →
      (1 : ℝ) * fp.u < 1 →
      (let Dcap := storedQRDiagDominantInvFactorBudget (by norm_num : 1 ≤ 1) A_hat
       let Ncap := storedQRPivotColumnNormBudget (by norm_num : 1 ≤ 1) A_hat
       let Gcap := ((1 : ℝ) * fp.u) / (1 - (1 : ℝ) * fp.u)
       let Fcap :=
          fp.u * (1 + Gcap) * (1 + fp.u) +
            fp.u * (1 + Gcap) +
            Gcap +
            fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
       2 * Dcap *
            ((1 : ℝ) *
              ((((1 : ℝ) * ((1 : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                  Ncap) ^ 2)) <
          1)) := by
  intro h
  let fp : FPModel := FPModel.exactWithUnitRoundoff ((1 : ℝ) / 2) (by norm_num)
  let alpha : ℕ → ℝ := fun _ => -1
  let A0 : Fin 1 → Fin 1 → ℝ := fun _ _ => 1
  let Astep : Fin 1 → Fin 1 → ℝ :=
    fl_householderStoredPanelStep fp 1 1 0
      (householderTrailingActiveVector 1 ⟨0, by norm_num⟩
        (fun a => A0 a ⟨0, by norm_num⟩) (alpha 0))
      (householderBetaSpec 1
        (householderTrailingActiveVector 1 ⟨0, by norm_num⟩
          (fun a => A0 a ⟨0, by norm_num⟩) (alpha 0)))
      A0
  let A_hat : ℕ → Fin 1 → Fin 1 → ℝ := fun t => if t = 0 then A0 else Astep
  have hStepA : ∀ k (hk : k < 1),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 1 1 k
            (householderTrailingActiveVector 1
              ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 1
              (householderTrailingActiveVector 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (A_hat k) := by
    intro k hk
    interval_cases k
    ext i j
    simp [A_hat, Astep]
  have hDD : ∀ k : Fin 1,
        IsDiagDominantUpper (k.val + 1)
          (qrLeadingBlock (A_hat k.val)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le k.isLt (by norm_num : 1 ≤ 1))) k.isLt) := by
    intro k
    fin_cases k
    refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      fin_cases j
      omega
    · intro i
      fin_cases i
      norm_num [A_hat, A0, qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i
      fin_cases j
      omega
  have hAlphaDef : ∀ k (hk : k < 1),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 1
                ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩
                (fun a => A_hat k a ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by norm_num : 1 ≤ 1)⟩ ⟨k, hk⟩) := by
    intro k hk
    interval_cases k
    norm_num [A_hat, A0, alpha, signedHouseholderAlpha,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
  have hbad := h fp A_hat alpha hStepA hDD hAlphaDef
    (by norm_num [fp, FPModel.exactWithUnitRoundoff])
  norm_num [storedQRDiagDominantInvFactorBudget,
    storedQRPivotColumnNormBudget, diagDominantUpperInvBudgetExpr,
    qrLeadingBlock, qrLeadingRow, qrLeadingColumn, vecNorm2, vecNorm2Sq,
    A_hat, A0, fp, FPModel.exactWithUnitRoundoff] at hbad
  ring_nf at hbad
  have hnot : ¬ (((12 : ℝ) ^ 2) * 8 < 1) := by norm_num
  exact hnot hbad
/-- Dimensioned norm-square nonbreakdown for the signed stored-QR stage from
    the repository's leading-block inverse-budget route.

    This is the reusable foundation behind the newer source-control wrappers:
    nonsingular displayed leading blocks plus local `κ∞`/self-norm and dual
    compact-budget hypotheses imply the norm-square pivot margin consumed by
    the signed-stage row-growth route. -/
theorem storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha κ K : ℕ → ℝ)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k) :
    ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
  classical
  let v : ℕ → Fin m → ℝ := fun k =>
    if hk : k < n then
      householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k)
    else 0
  let β : ℕ → ℝ := fun k => householderBetaSpec m (v k)
  have hStep : ∀ k, k < n →
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k (v k) (β k) (A_hat k) := by
    intro k hk
    simpa [v, β, hk] using hStepA k hk
  have hprefixLower :=
    fl_householderStoredPanel_sequence_prefix_lower_zero
      fp v β A_hat hStep
  intro k hk
  let hkm : k + 1 ≤ m :=
    Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)
  let S : Fin (k + 1) → Fin (k + 1) → ℝ :=
    qrLeadingBlock (A_hat k) hkm hk
  let C : Fin (k + 1) → Fin (k + 1) → ℝ :=
    nonsingInv (k + 1) S
  let budget : ℝ :=
    householderCompactComponentBudget fp m
      (householderTrailingActiveVector m
        ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
      (householderBetaSpec m
        (householderTrailingActiveVector m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
      (fun a => A_hat k a ⟨k, hk⟩)
      ⟨k, lt_of_lt_of_le hk hmn⟩
  have hupper : ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) hkm hk i j = 0 := by
    intro i j hji
    have hjk : j.val < k := by omega
    exact
      hprefixLower k (Nat.le_of_lt hk)
        (qrLeadingRow m k hkm i)
        (qrLeadingColumn n k hk j)
        (by simpa [qrLeadingColumn] using hjk)
        (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hlowerPrev : ∀ (i : Fin m) (j : Fin k), k ≤ i.val →
      A_hat k i (qrPreviousColumn n k hk j) = 0 := by
    intro i j hi
    exact
      hprefixLower k (Nat.le_of_lt hk) i (qrPreviousColumn n k hk j)
        (by simp [qrPreviousColumn])
        (lt_of_lt_of_le j.isLt hi)
  have hprefixSpan : qrPrefixSupportSpannedByPreviousColumns (A_hat k) hk :=
    qrPrefixSupportSpannedByPreviousColumns_of_leadingBlock_upper_det_ne_zero
      (A_hat k) hkm hk hupper (by simpa [S, hkm] using hdetLead k hk)
      hlowerPrev
  have hC : IsLeftInverse (k + 1) S C := by
    exact
      (isInverse_nonsingInv_of_det_ne_zero (k + 1) S
        (by simpa [S, hkm] using hdetLead k hk)).1
  have hCinf : ((k + 1 : ℕ) : ℝ) * infNorm C ^ 2 ≤ K k := by
    simpa [S, C, hkm] using
      infNorm_sq_budget_of_kappaInf_le_and_det_ne_zero
        (k + 1) (Nat.succ_pos k) S C (κ k) (K k)
        (by simpa [S, hkm] using hdetLead k hk)
        (by simpa [S, C, hkm] using hκ k hk)
        (by simpa [S, hkm] using hκbudget k hk)
  simpa [budget, S, C, hkm] using
    dim_mul_budget_sq_lt_trailingNorm2Sq_of_leadingBlock_leftInverse_infNorm_budget
      (A_hat k) hkm hk C hC hprefixSpan (K k) budget
      (hK k hk) hCinf (by simpa [budget] using hbudgetDual k hk)
/-- Stored trailing Householder QR source control from local diagonal dominance
    and the finite global compact-product budget.

    This closes the off-diagonal/diagonal-lower-bound branch when each local
    displayed leading block is already diagonally dominant: the repository
    `IsDiagDominantUpper` hypothesis gives the rowwise upper off-diagonal
    domination required by the primitive source-control theorem, while the
    finite maximum `storedQRCompactSequenceProductBudget` packages the
    per-pivot compact-product smallness field. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_diagDominant_globalProduct
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_offdiag_product
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
      hdetLead ?_ ?_
  · intro k hk i j hij
    exact (hDD k hk).2.2 i j hij
  · intro k hk
    have hle :=
      storedQRCompactSequenceProductExpr_le_budget
        hmn fp A_hat b_hat alpha (⟨k, hk⟩ : Fin n)
    exact lt_of_le_of_lt (by
      simpa [storedQRCompactSequenceProductExpr] using hle) hglobalProduct
/-- Diagonal-dominant global-product source-control handoff with the
    norm-square margin derived from local `κ∞`/dual-budget data.

    This is the diagonal-dominance companion to the active/prefix
    `kappaInf_dualBudget` branch: it uses local diagonal dominance to supply
    the off-diagonal domination field, and reuses the leading-block
    inverse-budget route for nonbreakdown. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_diagDominant_globalProduct
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_diagDominant_globalProduct
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
      hdetLead hDD hglobalProduct
/-- Diagonal-dominant source-control handoff with product smallness supplied by
    the canonical finite-maximum scalar condition.

    This is the route-1 narrowing used by the equation (8) bottleneck: instead
    of carrying the raw finite global product condition, it is enough to prove
    the single scalar inequality using the canonical maxima of the local
    diagonal-dominant inverse factors and displayed pivot-column norms. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_diagDominant_finiteMaxSmallness
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp A_hat b_hat alpha hm
      (fun k => hDD k.val k.isLt) hsmall
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_diagDominant_globalProduct
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
      hdetLead hDD hglobalProduct
/-- Diagonal-dominant finite-max source-control handoff with the norm-square
    margin derived from local `κ∞`/dual-budget data. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_diagDominant_finiteMaxSmallness
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_diagDominant_finiteMaxSmallness
      hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
      hdetLead hDD hsmall
/-- Global-product source-control handoff for the active/prefix global-budget
    route.

    The per-pivot compact-product hypotheses are supplied by a single finite
    global product budget.  This does not prove product smallness from machine
    parameters; it packages the exact remaining smallness obligation as the
    scalar condition `storedQRCompactSequenceProductBudget < 1`. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hBudget_diag hpivotMax ?_
  intro k hk
  have hle :=
    storedQRCompactSequenceProductExpr_le_budget
      hmn fp A_hat b_hat alpha (⟨k, hk⟩ : Fin n)
  exact lt_of_le_of_lt (by
    simpa [storedQRCompactSequenceProductExpr] using hle) hglobalProduct
/-- Completed-column global-product source-control handoff without a separate
    global stage-budget monotonicity hypothesis.

The finite global compact-step recurrence implies monotonicity on the QR
horizon.  We clamp the budget after `n` and reuse the existing global-product
handoff with a genuinely global monotone extension; all QR-stage hypotheses
are unchanged because every algorithmic stage is `<= n`. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_offdiag_rows_of_horizonBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  let stageBudget' := qrStageHorizonBudget n stageBudget
  have hBudget_mono_on_stages :
      ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b :=
    storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget_nonneg
  have hBudget'_mono :
      ∀ a b : ℕ, a ≤ b → stageBudget' a ≤ stageBudget' b := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_mono_of_mono_on_stages
        n stageBudget hBudget_mono_on_stages
  have hBudget'_nonneg : ∀ t : ℕ, 0 ≤ stageBudget' t := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_nonneg n stageBudget hBudget_nonneg
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget' t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget' (t + 1) := by
    intro t ht
    have ht_le : t ≤ n := Nat.le_of_lt ht
    have hsucc_le : t + 1 ≤ n := Nat.succ_le_iff.mpr ht
    simpa [stageBudget', qrStageHorizonBudget, ht_le, hsucc_le] using
      hglobalBudget t ht
  have hinit' : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
            (qrLeadingColumn n k hk j)| ≤
          stageBudget' 0 := by
    intro k hk i j hij
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using
      hinit k hk i j hij
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget' 0 := by
    intro r l
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using hinitBlock r l
  have hBudget_diag' : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget' k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| := by
    intro k hk i hi
    have hk_le : k ≤ n := Nat.le_of_lt hk
    simpa [stageBudget', qrStageHorizonBudget, hk_le] using
      hBudget_diag k hk i hi
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget' hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit' hinitBlock' hglobalBudget'
      hBudget'_nonneg hBudget'_mono hBudget_diag' hpivotMax hglobalProduct
/-- Global-product source-control handoff with the norm-square pivot margin
    derived from local leading-block `κ∞`/dual-budget data.

    This reuses
    `storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget`
    so the newest active/prefix global-product route no longer needs a raw
    `hbudgetNormSq` hypothesis. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_completedColumns_globalProduct_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget hBudget_nonneg
      hBudget_mono hBudget_diag hpivotMax hglobalProduct
/-- Global-product active/prefix source-control handoff with the diagonal
    lower-bound field supplied by one finite stage-diagonal defect budget.

This packages the remaining Cox--Higham diagonal lower-bound obligation as the
single scalar condition `storedQRStageDiagLowerDefectBudget <= 0`.  It does not
prove the diagonal lower bound from the pivoted loop; it makes the exact finite
source/domain check explicit before applying the active/prefix global-budget
route. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget hBudget_nonneg
      hBudget_mono
      (storedQRStageBudget_le_diag_of_stageDiagLowerDefectBudget_nonpos
        hmn A_hat stageBudget hstageDiagDefect)
      hpivotMax hglobalProduct
/-- Kappa/dual-budget active-prefix source-control handoff with the diagonal
    lower-bound field supplied by one finite stage-diagonal defect budget. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget hBudget_nonneg
      hBudget_mono hstageDiagDefect hpivotMax hglobalProduct
/-- Active-max-pivot source-control sibling of the stage-diagonal defect route.

This replaces the raw pivot-maximality field in
`..._stageDiagDefect_offdiag_rows` by the source-shaped policy equation that
the displayed pivot column is the finite active max-pivot selector. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget hBudget_nonneg
      hBudget_mono hstageDiagDefect
      (storedQRActiveMaxPivotColumn_pivotMax hmn A_hat hpivotChoice)
      hglobalProduct
/-- Kappa/dual-budget active-max-pivot source-control sibling of the
    stage-diagonal defect route. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hstageDiagDefect hpivotChoice
      hglobalProduct
/-- Diagonal-dominant scalar-comparison source-control sibling for the
    active-max-pivot route.

Local diagonal dominance supplies the displayed leading-block determinants and,
together with the scalar stage-budget/row-max comparison defect, supplies the
stage-diagonal scalar condition.  The remaining visible fields are the
diagonal-dominance invariant, comparison scalar, conditioning/dual-budget data,
active-pivot policy, and compact-product smallness. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  have hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
    storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageRowMaxComparisonDefectBudget_nonpos
      hmn A_hat stageBudget hDD hcomparison
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
      hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hstageDiagDefect hpivotChoice hglobalProduct
/-- Diagonal-dominant scalar-comparison source-control sibling whose compact
    product hypothesis is supplied by the canonical finite-max smallness
    inequality.

This closes the raw global-product field for the active-max-pivot
scalar-comparison branch, while keeping the finite-max scalar inequality,
local diagonal dominance, comparison scalar, conditioning/dual-budget data,
and active-pivot policy visible. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp A_hat b_hat alpha hm (fun k => hDD k.val k.isLt) hsmall
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageRowMaxComparisonDefect_offdiag_rows
      hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
      hDD hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hcomparison hpivotChoice hglobalProduct
/-- Diagonal-dominant scalar-comparison active-pivot source-control sibling using
    the concrete dual-budget route.

Compared with the `κ∞`/dual-budget finite-max theorem, this wrapper does not
expose auxiliary `κ`/`K` sequences or a separate dual compact-budget field.  The
canonical finite-max product scalar supplies the per-pivot product budget, and
`storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_diagDominant_concreteDualProductSequenceBudget`
turns that budget into the norm-square nonbreakdown margin consumed by the
active source-control theorem. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp A_hat b_hat alpha hm (fun k => hDD k.val k.isLt) hsmall
  have hbudgetSequenceProduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1 := by
    intro k hk
    exact
      lt_of_le_of_lt
        (by
          have hle :=
            storedQRCompactSequenceProductExpr_le_budget
              hmn fp A_hat b_hat alpha (⟨k, hk⟩ : Fin n)
          simpa [storedQRCompactSequenceProductExpr] using hle)
        hglobalProduct
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_diagDominant_concreteDualProductSequenceBudget
      hmn fp A_hat b_hat alpha hm hStepA hdetLead hDD hbudgetSequenceProduct
  have hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
    storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageRowMaxComparisonDefectBudget_nonpos
      hmn A_hat stageBudget hDD hcomparison
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget hBudget_nonneg
      hBudget_mono hstageDiagDefect hpivotChoice hglobalProduct
/-- Actual-unit-roundoff sibling of the active finite-max concrete-dual
    source-control handoff.

The source-control theorem no longer exposes `gammaValid fp m`; it derives that
validity guard from `(m : ℝ) * fp.u < 1`.  The real QR-domain fields remain
visible: local diagonal dominance, signed-stage recurrence budgets, active-pivot
choice, scalar comparison defect, and finite-max product smallness. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef hDD hinit
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hcomparison
      hpivotChoice hsmall
/-- Diagonal-dominant scalar-comparison active-pivot source-control sibling
    using the canonical source-denominator/rational-gamma compact-product cap.

Compared with
`StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows`,
this wrapper no longer asks for the finite-max scalar written in terms of the
assembled sequence-relative budget.  Instead it derives the raw compact-product
field from source-shaped denominator nonbreakdown, a unit-roundoff cap, and the
canonical rational-gamma cap-smallness inequality.  The genuinely open active
route fields remain visible: local diagonal dominance, the signed-stage budget
recurrence, active-pivot choice, and the scalar comparison defect. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm (fun k => hDD k.val k.isLt)
      hUcap_nonneg hden hu huCap hsmall
  have hbudgetSequenceProduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1 := by
    intro k hk
    exact
      lt_of_le_of_lt
        (by
          have hle :=
            storedQRCompactSequenceProductExpr_le_budget
              hmn fp A_hat b_hat alpha (⟨k, hk⟩ : Fin n)
          simpa [storedQRCompactSequenceProductExpr] using hle)
        hglobalProduct
  have hbudgetNormSq :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_diagDominant_concreteDualProductSequenceBudget
      hmn fp A_hat b_hat alpha hm hStepA hdetLead hDD hbudgetSequenceProduct
  have hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
    storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageRowMaxComparisonDefectBudget_nonpos
      hmn A_hat stageBudget hDD hcomparison
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_activeBlockRecurrence_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hinit hinitBlock hglobalBudget hBudget_nonneg
      hBudget_mono hstageDiagDefect hpivotChoice hglobalProduct
/-- Horizon-clamped source-denominator/cap source-control handoff.

The finite global compact-step recurrence already supplies monotonicity on the
algorithmic QR horizon.  This wrapper clamps the stage budget after `n`, proves
the clamped comparison defect from the displayed scalar comparison, and reuses
the source-denominator/cap handoff whose older surface required global
monotonicity. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  let stageBudget' := qrStageHorizonBudget n stageBudget
  have hBudget_mono_on_stages :
      ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b :=
    storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget_nonneg
  have hBudget'_mono :
      ∀ a b : ℕ, a ≤ b → stageBudget' a ≤ stageBudget' b := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_mono_of_mono_on_stages
        n stageBudget hBudget_mono_on_stages
  have hBudget'_nonneg : ∀ t : ℕ, 0 ≤ stageBudget' t := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_nonneg n stageBudget hBudget_nonneg
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget' t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget' (t + 1) := by
    intro t ht
    have ht_le : t ≤ n := Nat.le_of_lt ht
    have hsucc_le : t + 1 ≤ n := Nat.succ_le_iff.mpr ht
    simpa [stageBudget', qrStageHorizonBudget, ht_le, hsucc_le] using
      hglobalBudget t ht
  have hinit' : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
            (qrLeadingColumn n k hk j)| ≤
          stageBudget' 0 := by
    intro k hk i j hij
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using
      hinit k hk i j hij
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget' 0 := by
    intro r l
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using hinitBlock r l
  have hcomparison' :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget' ≤ 0 := by
    simpa [stageBudget'] using
      storedQRStageRowMaxComparisonDefectBudget_nonpos_of_horizonBudget
        hmn A_hat stageBudget hcomparison
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget' Ucap hm hStepA hAlphaDef
      hDD hinit' hinitBlock' hglobalBudget' hBudget'_nonneg
      hBudget'_mono hcomparison' hpivotChoice hUcap_nonneg hden hu huCap
      hsmall
/-- Horizon-clamped source-denominator/cap source-control handoff from only
    initial stage-budget nonnegativity.

The finite global compact-step recurrence propagates nonnegativity and
monotonicity on the QR horizon.  After clamping the budget after `n`, that
finite-horizon information is enough for the older globally quantified
source-control surface. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget_of_initial_nonneg
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  let stageBudget' := qrStageHorizonBudget n stageBudget
  have hBudget_nonneg_on_stages :
      ∀ t : ℕ, t ≤ n → 0 ≤ stageBudget t :=
    storedQRSignedStageBudget_nonneg_on_stages_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
  have hBudget_mono_on_stages :
      ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b :=
    storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget_of_initial_nonneg
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
  have hBudget'_mono :
      ∀ a b : ℕ, a ≤ b → stageBudget' a ≤ stageBudget' b := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_mono_of_mono_on_stages
        n stageBudget hBudget_mono_on_stages
  have hBudget'_nonneg : ∀ t : ℕ, 0 ≤ stageBudget' t := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_nonneg_of_nonneg_on_stages
        n stageBudget hBudget_nonneg_on_stages
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget' t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget' (t + 1) := by
    intro t ht
    have ht_le : t ≤ n := Nat.le_of_lt ht
    have hsucc_le : t + 1 ≤ n := Nat.succ_le_iff.mpr ht
    simpa [stageBudget', qrStageHorizonBudget, ht_le, hsucc_le] using
      hglobalBudget t ht
  have hinit' : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
            (qrLeadingColumn n k hk j)| ≤
          stageBudget' 0 := by
    intro k hk i j hij
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using
      hinit k hk i j hij
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget' 0 := by
    intro r l
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using hinitBlock r l
  have hcomparison' :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget' ≤ 0 := by
    simpa [stageBudget'] using
      storedQRStageRowMaxComparisonDefectBudget_nonpos_of_horizonBudget
        hmn A_hat stageBudget hcomparison
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget' Ucap hm hStepA hAlphaDef
      hDD hinit' hinitBlock' hglobalBudget' hBudget'_nonneg
      hBudget'_mono hcomparison' hpivotChoice hUcap_nonneg hden hu huCap
      hsmall
/-- Actual-unit-roundoff sibling of the diagonal-dominant scalar-comparison
    source-denominator/cap source-control theorem.

This removes the abstract `gammaValid fp m` and auxiliary cap fields from the
local source-control surface by specializing `Ucap = fp.u`; the displayed
actual-unit scalar guard derives the validity condition internally. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows
      hmn fp A_hat b_hat alpha stageBudget fp.u hm hStepA hAlphaDef hDD
      hinit hinitBlock hglobalBudget hBudget_nonneg hBudget_mono
      hcomparison hpivotChoice fp.u_nonneg hden (le_rfl : fp.u ≤ fp.u)
      huSmall hsmall
/-- Actual-unit active source-denominator theorem with denominator nonbreakdown
    derived from the stored loop.

This is the stored-lower sibling of
`..._finiteMaxSourceDenURationalGammaCanonicalBounds_..._of_actualUnitRoundoff_no_gammaValid`:
the raw source denominator field is no longer visible.  It is derived from the
stored recurrence, signed-alpha definition, and local diagonal dominance. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
      hmn fp A_hat b_hat alpha stageBudget huSmall hStepA hAlphaDef hDD
      hinit hinitBlock hglobalBudget hBudget_nonneg hBudget_mono
      hcomparison hpivotChoice
      (storedQRSourceDenominator_ne_zero_of_diagDominant_signedAlphaDef_stored_trailing_sequence
        fp hmn A_hat alpha hStepA hAlphaDef hDD)
      hsmall
/-- Actual-unit active source-control theorem with denominator nonbreakdown and
    stage-budget monotonicity derived from stored-loop source fields.

This is the horizon-budget sibling of
`..._diagDominant_storedLower_..._of_actualUnitRoundoff_no_gammaValid`: the
raw source denominator and global `hBudget_mono` fields are both discharged by
the stored recurrence, signed-alpha definition, diagonal dominance, and finite
global compact-budget recurrence. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid_of_horizonBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget
      hmn fp A_hat b_hat alpha stageBudget fp.u hm hStepA hAlphaDef hDD
      hinit hinitBlock hglobalBudget hBudget_nonneg hcomparison
      hpivotChoice fp.u_nonneg
      (storedQRSourceDenominator_ne_zero_of_diagDominant_signedAlphaDef_stored_trailing_sequence
        fp hmn A_hat alpha hStepA hAlphaDef hDD)
      (le_rfl : fp.u ≤ fp.u) huSmall hsmall
/-- Kappa/dual-budget active-max-pivot source-control sibling using the
    row-max scalar defect plus an explicit stage-budget/row-max comparison.

This is the visible-assumption form of the LS.2g-gi bridge: instead of asking
for the scalar stage-diagonal defect directly, the theorem consumes a
nonpositive row-max defect and the comparison
`stageBudget k <= qrLeadingStrictUpperRowMaxBudget ... k hk i` on displayed
off-diagonal rows, derives `storedQRStageDiagLowerDefectBudget <= 0`, and then
uses the existing active-pivot stage-diagonal route. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
      hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono
      (storedQRStageDiagLowerDefectBudget_nonpos_of_rowMaxDiagDefectBudget_nonpos_stageBudget_le_rowMax
        hmn A_hat stageBudget hrowDefect hstage_le_rowMax)
      hpivotChoice hglobalProduct
/-- Source-control active-max-pivot row-max QR certificate with the
    operation-validity guard displayed as `(m : ℝ) * fp.u < 1`.

This is the actual-unit-roundoff sibling of the visible row-max source-control
theorem above.  It derives the local `gammaValid fp m` obligation internally,
while keeping the row-max scalar defect, stage-budget/row-max comparison,
determinant/conditioning, dual compact-budget, and global compact-product
hypotheses visible. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows
      hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
      hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hrowDefect hstage_le_rowMax
      hpivotChoice hglobalProduct
/-- Source-control active-max-pivot row-max QR certificate using two finite
    scalar defects.

This scalar-comparison sibling replaces the displayed pointwise comparison
`stageBudget <= qrLeadingStrictUpperRowMaxBudget` by the finite maximum
condition `storedQRStageRowMaxComparisonDefectBudget <= 0`, then applies the
existing visible row-max source-control theorem. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows
      hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
      hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hrowDefect
      (storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
        hmn A_hat stageBudget hcomparison)
      hpivotChoice hglobalProduct
/-- Actual-unit-roundoff sibling of the scalar-comparison active-max-pivot
    row-max source-control theorem. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
      hmn fp A_hat b_hat alpha κ K stageBudget huSmall hStepA hAlphaDef
      hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
      hBudget_nonneg hBudget_mono hrowDefect
      (storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
        hmn A_hat stageBudget hcomparison)
      hpivotChoice hglobalProduct
/-- Source-facing exact-minimizer certificate from the packaged
    off-diagonal-control invariant.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_offDiagonalControl`.
    The invariant supplies local nonsingularity, diagonal dominance, and the
    stored-sequence product budget consumed by the concrete diagonal-dominant
    route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_offDiagonalControl
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hoff :
      StoredQROffDiagonalControlInvariant hmn fp A_hat b_hat alpha) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualProductSequenceBudget
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      hoff.leadingBlock_det_ne_zero
      hoff.leadingBlock_diagDominant
      hoff.compact_sequence_product_small
/-- Source-facing exact-minimizer certificate from source-shaped
    off-diagonal-control data.

    This expands the packaged invariant into local triangular shape, nonzero
    diagonals, row off-diagonal domination, and stored-sequence product
    smallness, then reuses the packaged off-diagonal-control minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hoff :
      StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_offDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQROffDiagonalControlInvariant.of_sourceOffDiagonalControl
        hmn fp A_hat b_hat alpha hoff)
/-- Source-facing finite-max diagonal-dominant exact-minimizer certificate with
    explicit stored leading-block nonsingularity.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_diagDominant_finiteMaxSmallness_concreteDual`.
    The canonical finite-max scalar smallness condition supplies the
    per-pivot product-sequence budget consumed by the concrete
    diagonal-dominant exact-minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_diagDominant_finiteMaxSmallness_concreteDual
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hproduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp A_hat b_hat alpha hm (fun k => hDD k.val k.isLt) hsmall
  refine
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualProductSequenceBudget
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hDD ?_
  intro k hk
  exact
    lt_of_le_of_lt
      (storedQRCompactSequenceProductExpr_le_budget hmn fp A_hat b_hat alpha
        (⟨k, hk⟩ : Fin n))
      hproduct
/-- Source-facing finite-max diagonal-dominant exact-minimizer certificate with
    stored leading-block nonsingularity derived from diagonal dominance. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_finiteMaxSmallness_concreteDual
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_diagDominant_finiteMaxSmallness_concreteDual
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (fun k hk =>
        det_ne_zero_of_diagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (hDD k hk))
      hDD hsmall
/-- Source-facing canonical rational-gamma finite-max exact-minimizer
    certificate with raw source-denominator nonbreakdown.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds`.
    The canonical rational-gamma scalar smallness condition supplies the
    packaged off-diagonal-control invariant consumed by the direct minimizer
    route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_offDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQROffDiagonalControlInvariant.of_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds
        hmn fp A_hat b_hat alpha Ucap hm hDD
        hUcap_nonneg hden hu huCap hsmall)
/-- Columnwise source-denominator exact-minimizer certificate from the
    canonical diagonal-dominant rational-gamma finite-max route.

    This is the columnwise, conservative-operation-count counterpart of
    `..._explicitCompactBudget_of_signed_alpha_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds`.
    The canonical source-denominator scalar condition supplies the finite
    sequence product budget, diagonal dominance supplies nonsingular displayed
    leading blocks, and the concrete-dual norm-square theorem supplies the
    Householder nonbreakdown margin consumed by the source-denominator wrapper.
    No separate `gammaValid fp m`, `κ∞`, inverse-budget, or dual compact-budget
    hypotheses remain visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hγtotal : gammaValid fp (n * (31 * (n + 1) * m) + n) := by
    simpa [gammaValid] using hγsmall
  have hm_le_total : m ≤ n * (31 * (n + 1) * m) + n := by
    have hcoeff : 1 ≤ 31 * (n + 1) := by omega
    have hm_le_step : m ≤ 31 * (n + 1) * m := by
      simpa using Nat.mul_le_mul_right m hcoeff
    have hstep_le_total :
        31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) + n := by
      have hstep_le_nstep :
          31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) := by
        simpa [one_mul] using
          Nat.mul_le_mul_right (31 * (n + 1) * m)
            (Nat.succ_le_of_lt hnpos)
      exact le_trans hstep_le_nstep
        (Nat.le_add_right (n * (31 * (n + 1) * m)) n)
    exact le_trans hm_le_step hstep_le_total
  have hm : gammaValid fp m :=
    gammaValid_mono fp hm_le_total hγtotal
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_sourceDenURationalGammaCanonicalBounds
      hmn fp A_hat b_hat alpha Ucap hm (fun k => hDD k.val k.isLt)
      hUcap_nonneg hden hu huCap hsmall
  have hbudgetSequenceProduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1 := by
    intro k hk
    exact
      lt_of_le_of_lt
        (storedQRCompactSequenceProductExpr_le_budget
          hmn fp A_hat b_hat alpha (⟨k, hk⟩ : Fin n))
        hglobalProduct
  have hbudgetNormSq :
      ∀ k (hk : k < n),
        (m : ℝ) *
            (householderCompactComponentBudget fp m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
          householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_diagDominant_concreteDualProductSequenceBudget
      hmn fp A_hat b_hat alpha hm hStepA hdetLead hDD hbudgetSequenceProduct
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_normSqBudget_no_gammaValid
      (m := m) (n := n) (fp := fp) (hmn := hmn) (hnpos := hnpos)
      (A := A) (b := b) (A_hat := A_hat) (b_hat := b_hat)
      (alpha := alpha) (hγsmall := hγsmall)
      (hInitA := hInitA) (hInitb := hInitb)
      (hStepA := hStepA) (hStepb := hStepb)
      (hAlphaDef := hAlphaDef) (hbudgetNormSq := hbudgetNormSq)
/-- Columnwise source-denominator exact-minimizer certificate with source
    denominator nonbreakdown derived from signed-alpha trailing-norm data.

    This strengthens
    `..._diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid`
    by replacing the raw `vᵀv ≠ 0` hypothesis with the positive active
    trailing-norm field and the standard signed-Householder `alpha` definition.
    The source denominator and sign facts are derived internally before the
    canonical finite-max source-denominator wrapper is applied. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_trailingNormPos_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0 := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_mul_pivot_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 :=
    storedQRSourceDenominator_ne_zero_of_trailingNorm2Sq_pos_mul_nonpos
      hmn A_hat alpha halpha htrailingPos hsign
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid
      fp hmn hnpos A b A_hat b_hat alpha Ucap hγsmall hInitA hInitb
      hStepA hStepb hAlphaDef hDD hUcap_nonneg hden hu huCap hsmall
/-- Columnwise source-denominator exact-minimizer certificate with source
    denominator nonbreakdown derived from the stored panel recurrence.

    This removes the visible positive trailing-norm hypothesis from the
    columnwise conservative-operation-count route.  Local diagonal dominance
    gives the leading-block determinant facts, the stored recurrence gives the
    previous-column lower-zero shape, and the signed-alpha formula supplies the
    scalar sign/square facts used by the source denominator helper. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hUcap_nonneg : 0 ≤ Ucap := le_trans fp.u_nonneg hu
  have hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0 :=
    storedQRSourceDenominator_ne_zero_of_diagDominant_signedAlphaDef_stored_trailing_sequence
      fp hmn A_hat alpha hStepA hAlphaDef hDD
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid
      fp hmn hnpos A b A_hat b_hat alpha Ucap hγsmall hInitA hInitb
      hStepA hStepb hAlphaDef hDD hUcap_nonneg hden hu huCap hsmall
/-- Columnwise source-denominator exact-minimizer certificate for the actual
    unit roundoff.

    This specializes the stored-lower canonical finite-max route to
    `Ucap = fp.u`.  The displayed final-radius smallness condition is strong
    enough to imply the local `(m : ℝ) * fp.u < 1` cap required by the compact
    Householder coefficient bounds. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let totalOps : ℕ := n * (31 * (n + 1) * m) + n
  have hm_le_total : m ≤ totalOps := by
    have hcoeff : 1 ≤ 31 * (n + 1) := by omega
    have hm_le_step : m ≤ 31 * (n + 1) * m := by
      simpa using Nat.mul_le_mul_right m hcoeff
    have hstep_le_total :
        31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) + n := by
      have hstep_le_nstep :
          31 * (n + 1) * m ≤ n * (31 * (n + 1) * m) := by
        simpa [one_mul] using
          Nat.mul_le_mul_right (31 * (n + 1) * m)
            (Nat.succ_le_of_lt hnpos)
      exact le_trans hstep_le_nstep
        (Nat.le_add_right (n * (31 * (n + 1) * m)) n)
    exact le_trans hm_le_step hstep_le_total
  have huCap : (m : ℝ) * fp.u < 1 := by
    have hm_cast : (m : ℝ) ≤ (totalOps : ℝ) := by
      exact_mod_cast hm_le_total
    have hprod_le : (m : ℝ) * fp.u ≤ (totalOps : ℝ) * fp.u :=
      mul_le_mul_of_nonneg_right hm_cast fp.u_nonneg
    exact lt_of_le_of_lt hprod_le (by simpa [totalOps] using hγsmall)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_no_gammaValid
      fp hmn hnpos A b A_hat b_hat alpha fp.u hγsmall hInitA hInitb
      hStepA hStepb hAlphaDef hDD (le_rfl : fp.u ≤ fp.u) huCap hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, stored-Householder QR
    source-facing columnwise wrapper.

    The conclusion matches the printed theorem's exact least-squares solution
    form with columnwise perturbation bounds and the RHS perturbation bound for
    the computed `fl_backSub` result.  This is still a structured no-pivot
    route: local diagonal dominance of the stored leading blocks and the
    displayed finite-max smallness condition remain visible. -/
theorem theorem20_3_householder_qr_ls_backward_error_columnwise_of_diagDominant_storedLower_finiteMax_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hnpos : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hγsmall : (((n * (31 * (n + 1) * m) + n : ℕ) : ℝ) * fp.u < 1))
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      (∀ j : Fin n,
        vecNorm2 (fun i : Fin m => ΔA' i j) ≤
          gamma fp (n * (31 * (n + 1) * m) + n) *
            vecNorm2 (fun i : Fin m => A i j)) ∧
      vecNorm2 Δb' ≤ gamma fp (n * (31 * (n + 1) * m) + n) *
          vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_columnwise_of_sourceDen_conservativeOperationCount_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_of_actualUnitRoundoff_no_gammaValid
      fp hmn hnpos A b A_hat b_hat alpha hγsmall hInitA hInitb
      hStepA hStepb hAlphaDef hDD hsmall
/-- Source-facing canonical rational-gamma finite-max exact-minimizer
    certificate with source denominator nonbreakdown derived from signed-alpha
    trailing-norm data. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_trailingNormPos_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (htrailingPos : ∀ k (hk : k < n),
      0 < householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have halpha : ∀ k (hk : k < n),
      alpha k * alpha k =
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩) := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_sq
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  have hsign : ∀ k (hk : k < n),
      alpha k * A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≤ 0 := by
    intro k hk
    rw [hAlphaDef k hk]
    exact
      signedHouseholderAlpha_sqrt_trailingNorm2Sq_mul_pivot_nonpos
        m ⟨k, lt_of_lt_of_le hk hmn⟩
        (fun a => A_hat k a ⟨k, hk⟩)
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_offDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQROffDiagonalControlInvariant.of_diagDominant_trailingNormPos_finiteMaxSourceDenURationalGammaCanonicalBounds
        hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
        halpha htrailingPos hsign hu huCap hsmall)
/-- Source-facing canonical rational-gamma finite-max exact-minimizer
    certificate with lower-zero data supplied explicitly.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_lowerPrev_finiteMaxSourceDenURationalGammaCanonicalBounds`.
    Diagonal dominance supplies the leading-block determinant data, while the
    explicit previous-column lower-zero field is still visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_lowerPrev_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_offDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQROffDiagonalControlInvariant.of_diagDominant_signedAlphaDef_lowerPrev_finiteMaxSourceDenURationalGammaCanonicalBounds
        hmn fp A_hat b_hat alpha Ucap hm hDD hUcap_nonneg
        hAlphaDef hlowerPrev hu huCap hsmall)
/-- Source-facing canonical rational-gamma finite-max exact-minimizer
    certificate with lower-zero data derived from the stored panel recurrence. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hlowerPrev : ∀ k (hk : k < n) (i : Fin m) (j : Fin k),
      k ≤ i.val → A_hat k i (qrPreviousColumn n k hk j) = 0 :=
    storedQRPreviousColumnLowerZero_of_stored_trailing_householder_sequence
      fp hmn A_hat alpha hStepA
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_lowerPrev_finiteMaxSourceDenURationalGammaCanonicalBounds
      fp hmn A b A_hat b_hat alpha Ucap hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hDD hUcap_nonneg hlowerPrev hu huCap hsmall
/-- Source-facing stored-lower canonical rational-gamma exact-minimizer
    certificate whose gamma-validity guards are derived from a unit-roundoff
    cap. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_of_uCap_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Ucap : ℝ)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m Ucap hu huCap
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  have hUcap_nonneg : 0 ≤ Ucap := le_trans fp.u_nonneg hu
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds
      fp hmn A b A_hat b_hat alpha Ucap hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hDD hUcap_nonneg hu huCap hsmall
/-- Source-facing stored-lower canonical rational-gamma exact-minimizer
    certificate at the actual unit roundoff, with operation validity stated as
    `(m : ℝ) * fp.u < 1`. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_finiteMaxSourceDenURationalGammaCanonicalBounds_of_uCap_no_gammaValid
      fp hmn A b A_hat b_hat alpha fp.u hInitA hInitb hStepA hStepb
      hAlphaDef hDD (le_rfl : fp.u ≤ fp.u) huSmall hsmall
/-- Source-facing pointwise-bound diagonal-dominant exact-minimizer certificate
    with explicit stored leading-block nonsingularity.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_diagDominant_finiteMaxNormBudgetCoeffPointwiseBounds_concreteDual`.
    The displayed pointwise bounds `Dmax`, `Cmax`, and `Nmax` close the
    compact-product side condition consumed by the concrete diagonal-dominant
    exact-minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_diagDominant_finiteMaxNormBudgetCoeffPointwiseBounds_concreteDual
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hCmax_nonneg : 0 ≤ Cmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hproduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget_pointwise_bounds
      hmn fp A_hat b_hat alpha Dmax Cmax Nmax hm
      (fun k => hDD k.val k.isLt)
      hDmax_nonneg hCmax_nonneg hNmax_nonneg hD hC hN hsmall
  refine
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_prefix_lower_zero_diagDominant_leadingBlock_det_ne_zero_concreteDualProductSequenceBudget
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hDD ?_
  intro k hk
  exact
    lt_of_le_of_lt
      (storedQRCompactSequenceProductExpr_le_budget hmn fp A_hat b_hat alpha
        (⟨k, hk⟩ : Fin n))
      hproduct
/-- Source-facing pointwise-bound diagonal-dominant exact-minimizer certificate
    with stored leading-block nonsingularity derived from diagonal dominance. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_finiteMaxNormBudgetCoeffPointwiseBounds_concreteDual
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (Dmax Cmax Nmax : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hDmax_nonneg : 0 ≤ Dmax)
    (hCmax_nonneg : 0 ≤ Cmax)
    (hNmax_nonneg : 0 ≤ Nmax)
    (hD : ∀ k : Fin n,
      diagDominantUpperInvBudgetExpr (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt)
        ⟨k.val, Nat.lt_succ_self k.val⟩ ≤ Dmax)
    (hC : ∀ k : Fin n,
      storedQRCompactStepNormBudgetCoeff hmn fp A_hat alpha k ≤ Cmax)
    (hN : ∀ k : Fin n,
      vecNorm2 (fun i : Fin m => A_hat k.val i k) ≤ Nmax)
    (hsmall :
      2 * Dmax *
          ((m : ℝ) *
            (((n : ℝ) * ((n : ℝ) * Cmax + Cmax)) * Nmax) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_diagDominant_finiteMaxNormBudgetCoeffPointwiseBounds_concreteDual
      fp hmn A b A_hat b_hat alpha Dmax Cmax Nmax hm hγ hInitA hInitb
      hStepA hStepb hAlphaDef
      (fun k hk =>
        det_ne_zero_of_diagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (hDD k hk))
      hDD hDmax_nonneg hCmax_nonneg hNmax_nonneg hD hC hN hsmall
/-- Source-facing exact-minimizer certificate with triangular shape derived from
    the stored recurrence and source-shaped diagonal/off-diagonal/product data.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diag_offdiag_product`.
    The stored recurrence supplies the lower-zero leading-block shape; the
    caller supplies the visible nonzero diagonal, row-wise off-diagonal
    domination, and compact-product smallness fields. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diag_offdiag_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdiag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diag_offdiag_product
        hmn fp A_hat b_hat alpha hStepA hdiag hoffdiag hproduct)
/-- Source-facing exact-minimizer certificate with stored triangular shape and
    previous displayed diagonal nonzeros derived from the signed-alpha
    nonbreakdown budget.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_pivot_sqrtBudget_offdiag_product`.
    The theorem keeps the current pivot nonzero, row-wise off-diagonal
    domination, and compact-product smallness fields visible, while deriving
    the preceding diagonal entries from the stored recurrence and signed-alpha
    square-root budget. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_pivot_sqrtBudget_offdiag_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)))
    (hdiagPivot : ∀ k (hk : k < n),
      A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩ ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_pivot_sqrtBudget_offdiag_product
        hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetSqrt
        hdiagPivot hoffdiag hproduct)
/-- Source-facing exact-minimizer certificate with current pivot nonzeroness
    derived from nonsingular displayed leading blocks.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_sqrtBudget_offdiag_product`.
    It exposes the determinant-shaped local nonsingularity condition, the
    square-root nonbreakdown budget, row-wise off-diagonal domination, and
    compact-product smallness, then derives the source off-diagonal-control
    package used by the direct minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_sqrtBudget_offdiag_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetSqrt : ∀ k (hk : k < n),
      householderCompactComponentBudget fp m
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (fun a => A_hat k a ⟨k, hk⟩)
          ⟨k, lt_of_lt_of_le hk hmn⟩ <
        Real.sqrt
          (householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩)))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_sqrtBudget_offdiag_product
        hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetSqrt
        hdetLead hoffdiag hproduct)
/-- Source-facing exact-minimizer certificate from source-shaped off-diagonal
    control with the square-root nonbreakdown budget supplied by a dimensioned
    trailing-norm-square margin.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_offdiag_product`.
    It exposes the algebraic margin
    `m * budget^2 < ||active column||_2^2`, determinant-shaped local
    nonsingularity, row-wise off-diagonal domination, and compact-product
    smallness, then derives the source off-diagonal-control package used by
    the direct minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_offdiag_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiag : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_offdiag_product
        hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
        hdetLead hoffdiag hproduct)
/-- Source-facing exact-minimizer certificate with off-diagonal control supplied
    through row-growth budgets and diagonal lower bounds.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product`.
    The row budget controls each displayed strict upper entry, while the
    diagonal lower-bound field shows that this budget is dominated by the
    corresponding displayed diagonal magnitude. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product
        hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hoffdiagBudget hrowBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate with row-growth budgets whose
    diagonal lower-bound obligation is restricted to rows with displayed upper
    entries.

    This is the rectangular-data counterpart of
    `LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product_of_offdiag_rows`.
    It removes the unused final-row diagonal comparison from the theorem
    surface while preserving the direct perturbed least-squares minimizer
    conclusion. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product_of_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hoffdiagBudget : ∀ k (hk : k < n),
      ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product_of_offdiag_rows
        hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hoffdiagBudget hrowBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate from Cox--Higham stage budgets
    and row budgets.

This is the rectangular-data counterpart of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product`.
It keeps the stage recurrence, pivot-zeroing, exact same-reflector bounds,
row-budget domination, diagonal lower bounds, and compact-product smallness
visible on the final perturbed least-squares minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (v : ℕ → Fin m → ℝ) (beta : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStageStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t (v t) (beta t) (A_hat t))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m (householder m (v t) (beta t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m (v t) (beta t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m (householder m (v t) (beta t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product
        hmn fp A_hat b_hat alpha v beta c rowBudget entryBudget hm
        hStepA hStageStep hAlphaDef hbudgetNormSq hdetLead hinit hpivot
        hbudget hexact hrowBudget hrowBudget_diag hproduct)
/-- Source-facing stage-budget exact-minimizer certificate whose diagonal
    lower-bound field is restricted to rows with displayed strict-upper data.

This is the rectangular-data counterpart of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product_of_offdiag_rows`.
It preserves the corrected offdiag-row-only diagonal lower-bound surface while
returning the final perturbed least-squares minimizer conclusion. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product_of_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (v : ℕ → Fin m → ℝ) (beta : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStageStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t (v t) (beta t) (A_hat t))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m (householder m (v t) (beta t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m (v t) (beta t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m (householder m (v t) (beta t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product_of_offdiag_rows
        hmn fp A_hat b_hat alpha v beta c rowBudget entryBudget hm
        hStepA hStageStep hAlphaDef hbudgetNormSq hdetLead hinit hpivot
        hbudget hexact hrowBudget hrowBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate from concrete signed-stage
    Cox--Higham row budgets.

This is the signed-stage specialization of the stage-budget handoff: the
abstract stage vectors are fixed to the signed stored-QR stage vector and beta,
while the final theorem still returns the rectangular perturbed least-squares
minimizer conclusion. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m
              (householder m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hSignedStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t
          (storedQRSignedStageVector hmn A_hat alpha t)
          (storedQRSignedStageBeta hmn A_hat alpha t)
          (A_hat t) := by
    intro t ht
    simpa [storedQRSignedStageVector, storedQRSignedStageBeta, ht]
      using hStepA t ht
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product
      fp hmn A b A_hat b_hat alpha
      (storedQRSignedStageVector hmn A_hat alpha)
      (storedQRSignedStageBeta hmn A_hat alpha)
      c rowBudget entryBudget hm hγ hInitA hInitb hStepA hSignedStep
      hStepb hAlphaDef hbudgetNormSq hdetLead hinit hpivot hbudget hexact
      hrowBudget hrowBudget_diag hproduct
/-- Source-facing exact-minimizer certificate from concrete signed-stage
    row budgets with offdiag-row-only diagonal lower-bound obligations. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hpivot : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        (qrLeadingColumn n k hk j).val = t →
          ∀ a : Fin m, t < a.val →
            matMulVec m
              (householder m
                (storedQRSignedStageVector hmn A_hat alpha t)
                (storedQRSignedStageBeta hmn A_hat alpha t))
              (fun r => A_hat t r (qrLeadingColumn n k hk j)) a = 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hSignedStep : ∀ t, t < n →
      A_hat (t + 1) =
        fl_householderStoredPanelStep fp m n t
          (storedQRSignedStageVector hmn A_hat alpha t)
          (storedQRSignedStageBeta hmn A_hat alpha t)
          (A_hat t) := by
    intro t ht
    simpa [storedQRSignedStageVector, storedQRSignedStageBeta, ht]
      using hStepA t ht
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_stageBudget_rowBudget_product_of_offdiag_rows
      fp hmn A b A_hat b_hat alpha
      (storedQRSignedStageVector hmn A_hat alpha)
      (storedQRSignedStageBeta hmn A_hat alpha)
      c rowBudget entryBudget hm hγ hInitA hInitb hStepA hSignedStep
      hStepb hAlphaDef hbudgetNormSq hdetLead hinit hpivot hbudget hexact
      hrowBudget hrowBudget_diag hproduct
/-- Source-facing exact-minimizer certificate from concrete signed-stage
    row budgets where pivot-column zeroing is derived from the norm-square
    nonbreakdown budget. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget
        hmn fp A_hat b_hat alpha c rowBudget entryBudget hm hStepA
        hAlphaDef hbudgetNormSq hdetLead hinit hbudget hexact
        hrowBudget hrowBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate from concrete signed-stage
    row budgets with norm-square-derived pivot-column zeroing and offdiag-row
    diagonal lower-bound obligations. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hrowBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageBudget_rowBudget_product_of_normSqBudget_offdiag_rows
        hmn fp A_hat b_hat alpha c rowBudget entryBudget hm hStepA
        hAlphaDef hbudgetNormSq hdetLead hinit hbudget hexact
        hrowBudget hrowBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate from a single monotone
    signed-stage budget sequence. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget
        hmn fp A_hat b_hat alpha c stageBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hinit hbudget hexact hBudget_mono
        hBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate from a single monotone
    signed-stage budget sequence with offdiag-row diagonal obligations. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (c : ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_offdiag_rows
        hmn fp A_hat b_hat alpha c stageBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hinit hbudget hexact hBudget_mono
        hBudget_diag hproduct)
/-- Source-facing exact-minimizer certificate from a monotone signed-stage
    budget sequence whose exact-reflector field is derived from stage entry
    bounds. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1),
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          |A_hat t
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
            stageBudget t)
    (hstageColBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ r : Fin m, t ≤ r.val →
          |A_hat t r (qrLeadingColumn n k hk j)| ≤ stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds
        hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hinit hbudget hBudget_nonneg hBudget_mono
        hBudget_diag hpivotMax hstageRowBound hstageColBound hproduct)
/-- Source-facing exact-minimizer certificate from concrete stage entry bounds
    with offdiag-row diagonal lower-bound obligations. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          |A_hat t
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
            stageBudget t)
    (hstageColBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ r : Fin m, t ≤ r.val →
          |A_hat t r (qrLeadingColumn n k hk j)| ≤ stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_stage_entry_bounds_offdiag_rows
        hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hinit hbudget hBudget_nonneg hBudget_mono
        hBudget_diag hpivotMax hstageRowBound hstageColBound hproduct)
/-- Source-facing exact-minimizer certificate from an active-suffix block
    budget plus prefix-row bounds for the monotone signed-stage route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_stage_entry_bounds_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩)
    (hstageActiveBlockBound : ∀ t, t < n →
      ∀ r : Fin m, t ≤ r.val →
        ∀ l : Fin n, t ≤ l.val → |A_hat t r l| ≤ stageBudget t)
    (hstagePrefixRowBound : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val, ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        ∀ l : Fin n, t ≤ l.val →
          (qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i).val < t →
            |A_hat t
              (qrLeadingRow m k
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) l| ≤
              stageBudget t)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_signedStageUniformBudget_product_of_normSqBudget_activePrefix_stage_entry_bounds_offdiag_rows
        hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hinit hbudget hBudget_nonneg hBudget_mono
        hBudget_diag hpivotMax hstageActiveBlockBound
        hstagePrefixRowBound hproduct)
/-- Source-facing exact-minimizer sibling of the active global-product
    `kappaInf`/dual-budget scalar-comparison theorem.

This keeps the same diagonal-dominant, active-pivot, signed-stage, comparison
defect, and compact global-product surface as the solver-facing theorem above,
but exposes the rectangular perturbed least-squares conclusion directly. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageRowMaxComparisonDefect_offdiag_rows
        hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
        hDD hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
        hBudget_nonneg hBudget_mono hcomparison hpivotChoice hglobalProduct)
/-- Source-facing exact-minimizer sibling of the active finite-max
    `kappaInf`/dual-budget scalar-comparison theorem.

This keeps the same diagonal-dominant, active-pivot, signed-stage, and
finite-max smallness surface as the solver-facing theorem above, but exposes
the rectangular perturbed least-squares conclusion directly. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_offdiag_rows
        hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
        hDD hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
        hBudget_nonneg hBudget_mono hcomparison hpivotChoice hsmall)
/-- Source-facing exact-minimizer sibling of the active finite-max concrete-dual
    scalar-comparison theorem.

This has the same diagonal-dominant, signed-stage, active-pivot,
scalar-comparison, and finite-max concrete-dual surface as the solver-facing
theorem above, but exposes the rectangular perturbed least-squares minimizer
conclusion directly. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows
        hmn fp A_hat b_hat alpha stageBudget hm hStepA hAlphaDef hDD hinit
        hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hcomparison
        hpivotChoice hsmall)
/-- Source-facing exact-minimizer sibling of the active finite-max concrete-dual
    scalar-comparison theorem.

This lifts the actual-unit-roundoff active finite-max source-control route from
the solver-facing Gram certificate to the direct rectangular perturbation
conclusion. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSmallness_stageRowMaxComparisonDefect_concreteDual_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
        hmn fp A_hat b_hat alpha stageBudget huSmall hStepA hAlphaDef
        hDD hinit hinitBlock hglobalBudget hBudget_nonneg hBudget_mono
        hcomparison hpivotChoice hsmall)
/-- Source-facing exact-minimizer sibling of the active source-denominator
    scalar-comparison theorem.

This keeps the non-actual `Ucap`, source denominator nonbreakdown, and
canonical rational-gamma scalar-smallness fields visible while lifting the
solver-facing Gram certificate to the direct rectangular perturbed
least-squares minimizer conclusion. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows
        hmn fp A_hat b_hat alpha stageBudget Ucap hm hStepA hAlphaDef hDD
        hinit hinitBlock hglobalBudget hBudget_nonneg hBudget_mono
        hcomparison hpivotChoice hUcap_nonneg hden hu huCap hsmall)
/-- Source-facing exact-minimizer theorem for the horizon-budget
    source-denominator/cap route.

This is the direct rectangular perturbation conclusion for the active finite
max source-denominator branch after deriving stage-budget monotonicity from
the finite QR recurrence through horizon clamping. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (Ucap : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hUcap_nonneg : 0 ≤ Ucap)
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hu : fp.u ≤ Ucap)
    (huCap : (m : ℝ) * Ucap < 1)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * Ucap) / (1 - (m : ℝ) * Ucap)
      let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget
        hmn fp A_hat b_hat alpha stageBudget Ucap hm hStepA hAlphaDef hDD
        hinit hinitBlock hglobalBudget hBudget_nonneg hcomparison
        hpivotChoice hUcap_nonneg hden hu huCap hsmall)
/-- Source-facing exact-minimizer sibling of the active source-denominator
    scalar-comparison actual-unit-roundoff theorem.

This lifts the active finite-max source-denominator route from the
solver-facing Gram certificate to the direct rectangular perturbation
conclusion while keeping the source denominator nonbreakdown field visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
        hmn fp A_hat b_hat alpha stageBudget huSmall hStepA hAlphaDef hDD
        hinit hinitBlock hglobalBudget hBudget_nonneg hBudget_mono
        hcomparison hpivotChoice hden hsmall)
/-- Source-facing actual-unit-roundoff exact-minimizer theorem with
    horizon-clamped stage-budget monotonicity.

This is the active finite-max source-denominator exact-minimizer route with
actual unit roundoff and no exposed global `hBudget_mono` field. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid_of_horizonBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hden : ∀ k (hk : k < n),
      (∑ i : Fin m,
        householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
          householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0)
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget
      fp hmn A b A_hat b_hat alpha stageBudget fp.u hm hγ hInitA hInitb
      hStepA hStepb hAlphaDef hDD hinit hinitBlock hglobalBudget
      hBudget_nonneg hcomparison hpivotChoice fp.u_nonneg hden
      (le_rfl : fp.u ≤ fp.u) huSmall hsmall
/-- Source-facing exact-minimizer sibling of the stored-lower active-pivot
    finite-max source-control theorem.

    This lifts the solver-facing actual-unit-roundoff stored-lower
    source-denominator route to the direct rectangular perturbation conclusion:
    the same stored-loop `fl_backSub` vector is an exact least-squares
    minimizer for perturbed data with the explicit compact-sequence
    Frobenius/RHS budgets. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
        hmn fp A_hat b_hat alpha stageBudget huSmall hStepA hAlphaDef hDD
        hinit hinitBlock hglobalBudget hBudget_nonneg hBudget_mono
        hcomparison hpivotChoice hsmall)
/-- Source-facing exact-minimizer stored-lower active-pivot route with
    actual unit roundoff and horizon-clamped stage-budget monotonicity.

This is the direct rectangular perturbation conclusion after deriving both
source denominator nonbreakdown and global stage-budget monotonicity from
stored-loop source fields. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid_of_horizonBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha
      (gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall)
      (gammaValid_mono fp hmn
        (gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall))
      hInitA hInitb hStepA hStepb hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid_of_horizonBudget
        hmn fp A_hat b_hat alpha stageBudget huSmall hStepA hAlphaDef hDD
        hinit hinitBlock hglobalBudget hBudget_nonneg hcomparison
        hpivotChoice hsmall)
/-- Canonical Frobenius-starting stage budget for the concrete stored
    Householder QR loop.

The budget starts at `||A||_F` and then follows the same signed-stage global
compact recurrence used by the Chapter 20, Theorem 20.3 stored-loop wrappers.
This packages the recurrence as data instead of asking callers to supply a
separate proof for it. -/
noncomputable def storedHouseholderQRFrobStageBudget (fp : FPModel)
    {m n : ℕ} (hmn : n ≤ m) (A : Fin m → Fin n → ℝ) : ℕ → ℝ
  | 0 => frobNormRect A
  | t + 1 =>
      coxHighamActiveRowGrowthFactor m *
          storedHouseholderQRFrobStageBudget fp hmn A t +
        if ht : t < n then
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht
        else
          0
@[simp] theorem storedHouseholderQRFrobStageBudget_zero (fp : FPModel)
    {m n : ℕ} (hmn : n ≤ m) (A : Fin m → Fin n → ℝ) :
    storedHouseholderQRFrobStageBudget fp hmn A 0 = frobNormRect A := rfl
theorem storedHouseholderQRFrobStageBudget_succ_of_lt (fp : FPModel)
    {m n : ℕ} (hmn : n ≤ m) (A : Fin m → Fin n → ℝ)
    (t : ℕ) (ht : t < n) :
    storedHouseholderQRFrobStageBudget fp hmn A (t + 1) =
      coxHighamActiveRowGrowthFactor m *
          storedHouseholderQRFrobStageBudget fp hmn A t +
        storedQRSignedStageGlobalCompactBudget hmn fp
          (storedHouseholderQRMatrixSeq fp hmn A)
          (storedHouseholderQRAlphaSeq fp hmn A) t ht := by
  simp [storedHouseholderQRFrobStageBudget, ht]
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, stored-Householder QR
    source-facing compact-budget wrapper.

    This packages the strongest stored-lower active-pivot route currently
    available: actual unit roundoff is used, the source denominator
    nonbreakdown and global stage-budget monotonicity are derived internally,
    and the computed `fl_backSub` vector is an exact least-squares minimizer
    for perturbed data with the explicit compact-sequence Frobenius/RHS
    budgets.  Local diagonal dominance, active-pivot selection,
    stage-budget comparison, and finite-max smallness hypotheses remain
    visible, so this is still a conditional Theorem 20.3 endgame step rather
    than the unconditional printed theorem. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_storedLower_activePivot_actualUnitRoundoff_horizonBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_diagDominant_storedLower_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid_of_horizonBudget
      fp hmn A b A_hat b_hat alpha stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hDD hinit hinitBlock hglobalBudget
      hBudget_nonneg hcomparison hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, stored-Householder QR
    source-facing compact-budget wrapper with initial stage-budget
    nonnegativity.

    This strengthens
    `theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_storedLower_activePivot_actualUnitRoundoff_horizonBudget`
    by deriving finite-horizon nonnegativity from `0 <= stageBudget 0` and the
    global compact-step recurrence.  The direct rectangular perturbation
    conclusion and the remaining genuine stored-Householder side conditions are
    unchanged. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_storedLower_activePivot_actualUnitRoundoff_initialBudget_horizonBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_diagDominant_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_finiteMaxSourceDenURationalGammaCanonicalBounds_stageRowMaxComparisonDefect_offdiag_rows_of_horizonBudget_of_initial_nonneg
        hmn fp A_hat b_hat alpha stageBudget fp.u hm hStepA hAlphaDef hDD
        hinit hinitBlock hglobalBudget hBudget0_nonneg hcomparison
        hpivotChoice fp.u_nonneg
        (storedQRSourceDenominator_ne_zero_of_diagDominant_signedAlphaDef_stored_trailing_sequence
          fp hmn A_hat alpha hStepA hAlphaDef hDD)
        (le_rfl : fp.u ≤ fp.u) huSmall hsmall)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, stored-Householder QR
    concrete-loop compact-budget wrapper.

This specializes
`theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_storedLower_activePivot_actualUnitRoundoff_initialBudget_horizonBudget`
to the concrete recursive stored-Householder matrix, right-hand-side, and
signed-`alpha` sequences.  The loop recurrence, initial equations, and signed
`alpha` definition are discharged by the sequence definitions; the genuine
diagonal-dominance, active-pivot, scalar-comparison, initial-budget, and
finite-max smallness side conditions remain visible. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_initialBudget_horizonBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      let A_hat := storedHouseholderQRMatrixSeq fp hmn A
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let A_hat : ℕ → Fin m → Fin n → ℝ := storedHouseholderQRMatrixSeq fp hmn A
  let b_hat : ℕ → Fin m → ℝ := storedHouseholderQRRhsSeq fp hmn A b
  let alpha : ℕ → ℝ := storedHouseholderQRAlphaSeq fp hmn A
  have hInitA : A_hat 0 = A := by
    simp [A_hat]
  have hInitb : b_hat 0 = b := by
    simp [b_hat]
  have hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k) := by
    intro k hk
    simpa [A_hat, alpha] using
      storedHouseholderQRMatrixSeq_succ_of_lt fp hmn A k hk
  have hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) := by
    intro k hk
    simpa [A_hat, b_hat, alpha] using
      storedHouseholderQRRhsSeq_succ_of_lt fp hmn A b k hk
  have hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩) := by
    intro k hk
    simpa [A_hat, alpha] using
      storedHouseholderQRAlphaSeq_eq_signed fp hmn A k hk
  have hDD' : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk) := by
    intro k hk
    simpa [A_hat] using hDD k hk
  have hinit' : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
            (qrLeadingColumn n k hk j)| ≤
          stageBudget 0 := by
    intro k hk i j hij
    simpa [A_hat] using hinit k hk i j hij
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0 := by
    intro r l
    simpa [A_hat] using hinitBlock r l
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    simpa [A_hat, alpha] using hglobalBudget t ht
  have hcomparison' :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0 := by
    simpa [A_hat] using hcomparison
  have hpivotChoice' : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t) := by
    intro t ht
    simpa [A_hat] using hpivotChoice t ht
  have hsmall' :
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1 := by
    simpa [A_hat, b_hat, alpha] using hsmall
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_storedLower_activePivot_actualUnitRoundoff_initialBudget_horizonBudget
      fp hmn A b A_hat b_hat alpha stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hDD' hinit' hinitBlock' hglobalBudget'
      hBudget0_nonneg hcomparison' hpivotChoice' hsmall'
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-Householder QR
    wrapper with nonempty source dimensions.

For a nonempty least-squares problem, the full initial block bound
`|A r l| <= stageBudget 0` supplies both initial side conditions consumed by
the concrete stored-lower route: the displayed strict-upper initial field and
`0 <= stageBudget 0`. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_horizonBudget_of_nonempty
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      let A_hat := storedHouseholderQRMatrixSeq fp hmn A
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm_pos : 0 < m := lt_of_lt_of_le hn hmn
  let r0 : Fin m := ⟨0, hm_pos⟩
  let l0 : Fin n := ⟨0, hn⟩
  have hBudget0_nonneg : 0 ≤ stageBudget 0 :=
    le_trans (abs_nonneg (A r0 l0)) (hinitBlock r0 l0)
  have hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val,
        |A
          (qrLeadingRow m k
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0 := by
    intro k hk i j _hij
    exact hinitBlock
      (qrLeadingRow m k
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
      (qrLeadingColumn n k hk j)
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_initialBudget_horizonBudget
      fp hmn A b stageBudget huSmall hDD hinit hinitBlock hglobalBudget
      hBudget0_nonneg hcomparison hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-Householder QR
    wrapper with a Frobenius-size initial budget.

For nonempty source dimensions, the single normwise assumption
`frobNormRect A <= stageBudget 0` implies the full entrywise initial block
bound required by the concrete stored-lower route. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_horizonBudget_of_nonempty_frobInitial
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hinitFrob : frobNormRect A ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      let A_hat := storedHouseholderQRMatrixSeq fp hmn A
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0 := by
    intro r l
    exact (abs_entry_le_frobNormRect A r l).trans hinitFrob
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_horizonBudget_of_nonempty
      fp hmn hn A b stageBudget huSmall hDD hinitBlock hglobalBudget
      hcomparison hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-Householder QR
    wrapper using the canonical Frobenius-starting stage budget.

The public surface no longer asks for an initial entrywise block budget or a
separate proof of the signed-stage compact recurrence: both are supplied by
`storedHouseholderQRFrobStageBudget`. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_horizonBudget_of_nonempty_frobStageBudget
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRFrobStageBudget fp hmn A) ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      let A_hat := storedHouseholderQRMatrixSeq fp hmn A
      let Dcap := storedQRDiagDominantInvFactorBudget hmn A_hat
      let Ncap := storedQRPivotColumnNormBudget hmn A_hat
      let Gcap := ((m : ℝ) * fp.u) / (1 - (m : ℝ) * fp.u)
      let Fcap :=
        fp.u * (1 + Gcap) * (1 + fp.u) +
          fp.u * (1 + Gcap) +
          Gcap +
          fp.u * (1 + Gcap) * (1 + fp.u) ^ 2
      2 * Dcap *
          ((m : ℝ) *
            ((((n : ℝ) * ((n : ℝ) + 1) * (fp.u + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let stageBudget : ℕ → ℝ := storedHouseholderQRFrobStageBudget fp hmn A
  have hinitFrob : frobNormRect A ≤ stageBudget 0 := by
    simp [stageBudget]
  have hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    change
      coxHighamActiveRowGrowthFactor m *
            storedHouseholderQRFrobStageBudget fp hmn A t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        storedHouseholderQRFrobStageBudget fp hmn A (t + 1)
    rw [storedHouseholderQRFrobStageBudget_succ_of_lt fp hmn A t ht]
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_diagDominant_concreteStoredLower_activePivot_actualUnitRoundoff_horizonBudget_of_nonempty_frobInitial
      fp hmn hn A b stageBudget huSmall hDD hinitFrob hglobalBudget
      hcomparison hpivotChoice hsmall
/-- Source-facing exact-minimizer sibling of the active-max-pivot row-max
    theorem with an explicit stage-budget/row-max comparison.

This keeps the determinant, `kappaInf` self-norm, dual-budget, row-max diagonal
defect, active-pivot, stage-budget comparison, and compact global-product
fields visible, but exposes the rectangular perturbed least-squares conclusion
directly. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows
        hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
        hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock
        hglobalBudget hBudget_nonneg hBudget_mono hrowDefect
        hstage_le_rowMax hpivotChoice hglobalProduct)
/-- Source-facing exact-minimizer sibling of the actual-unit-roundoff
    active-max-pivot row-max theorem with an explicit stage-budget/row-max
    comparison. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hstage_le_rowMax : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      stageBudget k ≤ qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows
      fp hmn A b A_hat b_hat alpha κ K stageBudget hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock
      hglobalBudget hBudget_nonneg hBudget_mono hrowDefect
      hstage_le_rowMax hpivotChoice hglobalProduct
/-- Source-facing exact-minimizer sibling of the active-max-pivot
    stage-diagonal-defect QR route.

This is the rectangular-data lift of the solver/source-control theorem with
`storedQRStageDiagLowerDefectBudget <= 0`.  It removes the separate row-max
diagonal-defect and stage-budget/row-max comparison fields from the final
least-squares minimizer surface; the single stage-diagonal finite check is the
visible diagonal lower-bound certificate. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
        hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
        hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
        hBudget_nonneg hBudget_mono hstageDiagDefect hpivotChoice
        hglobalProduct)
/-- Actual-unit-roundoff sibling of the active-max-pivot stage-diagonal
    exact-minimizer theorem. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows
      fp hmn A b A_hat b_hat alpha κ K stageBudget hm hγ hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual hinit
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hstageDiagDefect
      hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, stage-diagonal/dual-budget
    source-facing compact-budget wrapper.

This is the strongest active-pivot route currently exposed at the final
least-squares minimizer surface.  Compared with the row-max wrapper below, the
row-max diagonal defect and scalar stage-budget/row-max comparison are replaced
by the single finite stage-diagonal lower-bound defect
`storedQRStageDiagLowerDefectBudget <= 0`.  The determinant/conditioning data,
dual compact-budget data, active-pivot policy, and compact global-product
smallness remain visible. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_stageDiagDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
      fp hmn A b A_hat b_hat alpha κ K stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual hinit
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hstageDiagDefect
      hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, row-max/dual-budget
    source-facing compact-budget wrapper.

This packages the actual-unit-roundoff active-pivot row-max route under a
Theorem 20.3 name.  Compared with the diagonal-dominance finite-max wrapper,
it does not expose local diagonal dominance or the `Dcap`/`Ncap` finite-max
smallness inequality.  The remaining visible obligations are the stored QR
recurrence, signed `alpha`, leading-block determinant/conditioning data,
dual compact-budget data, row-max diagonal defect, stage-budget/row-max
scalar comparison, active-pivot selection, and compact global-product
smallness. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageBudgetLeRowMax_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
      fp hmn A b A_hat b_hat alpha κ K stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual hinit
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hrowDefect
      (storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
        hmn A_hat stageBudget hcomparison)
      hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, row-max/dual-budget
    horizon-budget compact wrapper.

This is the row-max analogue of the stage-diagonal horizon-budget wrapper:
the global compact recurrence and the initial scalar budget produce the
globally monotone/nonnegative horizon-clamped budget internally, while the full
initial-block bound supplies the displayed strict-upper initial entries. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_horizonBudget_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let stageBudget' : ℕ → ℝ := qrStageHorizonBudget n stageBudget
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hBudget_nonneg_on_stages :
      ∀ t : ℕ, t ≤ n → 0 ≤ stageBudget t :=
    storedQRSignedStageBudget_nonneg_on_stages_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
  have hBudget_mono_on_stages :
      ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b :=
    storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget_of_initial_nonneg
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
  have hBudget'_nonneg : ∀ t : ℕ, 0 ≤ stageBudget' t := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_nonneg_of_nonneg_on_stages
        n stageBudget hBudget_nonneg_on_stages
  have hBudget'_mono : ∀ a b : ℕ, a ≤ b → stageBudget' a ≤ stageBudget' b := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_mono_of_mono_on_stages
        n stageBudget hBudget_mono_on_stages
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget' t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget' (t + 1) := by
    intro t ht
    have ht_le : t ≤ n := Nat.le_of_lt ht
    have hsucc_le : t + 1 ≤ n := Nat.succ_le_iff.mpr ht
    simpa [stageBudget', qrStageHorizonBudget, ht_le, hsucc_le] using
      hglobalBudget t ht
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget' 0 := by
    intro r l
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using hinitBlock r l
  have hinit' : ∀ k (hk : k < n), ∀ i j : Fin (k + 1),
      ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
            (qrLeadingColumn n k hk j)| ≤
          stageBudget' 0 := by
    intro k hk i j _hij
    exact hinitBlock'
      (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
      (qrLeadingColumn n k hk j)
  have hcomparison' :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget' ≤ 0 := by
    simpa [stageBudget'] using
      storedQRStageRowMaxComparisonDefectBudget_nonpos_of_horizonBudget
        hmn A_hat stageBudget hcomparison
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_actualUnitRoundoff
      fp hmn A b A_hat b_hat alpha κ K stageBudget' huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual
      hinit' hinitBlock' hglobalBudget' hBudget'_nonneg hBudget'_mono
      hrowDefect hcomparison' hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    active-pivot row-max/dual-budget horizon wrapper.

This specializes the row-max horizon-budget route to the concrete stored
Householder matrix, right-hand-side, and signed-alpha sequences.  The stored
recurrence, initialization, and signed-alpha hypotheses are discharged
definitionally; determinant/conditioning, dual-budget, row-max, scalar
comparison, active-pivot, initial-block, recurrence-budget, and compact-product
assumptions remain visible. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRRhsSeq fp hmn A b)
        (storedHouseholderQRAlphaSeq fp hmn A) < 1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let A_hat : ℕ → Fin m → Fin n → ℝ := storedHouseholderQRMatrixSeq fp hmn A
  let b_hat : ℕ → Fin m → ℝ := storedHouseholderQRRhsSeq fp hmn A b
  let alpha : ℕ → ℝ := storedHouseholderQRAlphaSeq fp hmn A
  have hInitA : A_hat 0 = A := by
    simp [A_hat]
  have hInitb : b_hat 0 = b := by
    simp [b_hat]
  have hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k) := by
    intro k hk
    simpa [A_hat, alpha] using
      storedHouseholderQRMatrixSeq_succ_of_lt fp hmn A k hk
  have hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) := by
    intro k hk
    simpa [A_hat, b_hat, alpha] using
      storedHouseholderQRRhsSeq_succ_of_lt fp hmn A b k hk
  have hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩) := by
    intro k hk
    simpa [A_hat, alpha] using
      storedHouseholderQRAlphaSeq_eq_signed fp hmn A k hk
  have hdetLead' : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    simpa [A_hat] using hdetLead k hk
  have hκ' : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k := by
    intro k hk
    simpa [A_hat] using hκ k hk
  have hκbudget' : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k := by
    intro k hk
    simpa [A_hat] using hκbudget k hk
  have hbudgetDual' : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k := by
    intro k hk
    simpa [A_hat, alpha] using hbudgetDual k hk
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0 := by
    intro r l
    simpa [A_hat] using hinitBlock r l
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    simpa [A_hat, alpha] using hglobalBudget t ht
  have hrowDefect' : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0 := by
    simpa [A_hat] using hrowDefect
  have hcomparison' :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0 := by
    simpa [A_hat] using hcomparison
  have hpivotChoice' : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t) := by
    intro t ht
    simpa [A_hat] using hpivotChoice t ht
  have hglobalProduct' :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
    simpa [A_hat, b_hat, alpha] using hglobalProduct
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_horizonBudget_actualUnitRoundoff
      fp hmn A b A_hat b_hat alpha κ K stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead' hK hκ' hκbudget'
      hbudgetDual' hinitBlock' hglobalBudget' hBudget0_nonneg hrowDefect'
      hcomparison' hpivotChoice' hglobalProduct'
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    row-max/dual-budget horizon wrapper with nonempty source dimensions.

For a nonempty least-squares problem, the full initial-block bound already
implies `0 <= stageBudget 0`; this removes the separate initial-budget
nonnegativity hypothesis from the concrete row-max route. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff_of_nonempty
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRRhsSeq fp hmn A b)
        (storedHouseholderQRAlphaSeq fp hmn A) < 1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm_pos : 0 < m := lt_of_lt_of_le hn hmn
  let r0 : Fin m := ⟨0, hm_pos⟩
  let l0 : Fin n := ⟨0, hn⟩
  have hBudget0_nonneg : 0 ≤ stageBudget 0 :=
    le_trans (abs_nonneg (A r0 l0)) (hinitBlock r0 l0)
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff
      fp hmn A b κ K stageBudget huSmall hdetLead hK hκ hκbudget
      hbudgetDual hinitBlock hglobalBudget hBudget0_nonneg hrowDefect
      hcomparison hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    row-max/dual-budget horizon wrapper with a Frobenius initial budget.

This replaces the entrywise initial-block bound in the nonempty concrete
row-max route by the normwise source-facing assumption
`frobNormRect A <= stageBudget 0`. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff_of_nonempty_frobInitial
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitFrob : frobNormRect A ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRRhsSeq fp hmn A b)
        (storedHouseholderQRAlphaSeq fp hmn A) < 1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0 := by
    intro r l
    exact (abs_entry_le_frobNormRect A r l).trans hinitFrob
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff_of_nonempty
      fp hmn hn A b κ K stageBudget huSmall hdetLead hK hκ hκbudget
      hbudgetDual hinitBlock hglobalBudget hrowDefect hcomparison
      hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    row-max/dual-budget horizon wrapper with finite-max smallness.

This is the Frobenius-initial row-max route with the compact product and
leading-block determinant side conditions discharged from local diagonal
dominance plus the canonical finite-max smallness inequality.  The remaining
visible obligations are the conditioning/dual-budget data, the row-max and
stage-budget comparison defects, the active-pivot policy, and the finite
compact-step budget recurrence. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty_frobInitial
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitFrob : frobNormRect A ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRRhsSeq fp hmn A b)
        (storedHouseholderQRAlphaSeq fp hmn A) < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp (storedHouseholderQRMatrixSeq fp hmn A)
      (storedHouseholderQRRhsSeq fp hmn A b)
      (storedHouseholderQRAlphaSeq fp hmn A) hm
      (fun k => hDD k.val k.isLt) hsmall
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff_of_nonempty_frobInitial
      fp hmn hn A b κ K stageBudget huSmall hdetLead hK hκ hκbudget
      hbudgetDual hinitFrob hglobalBudget hrowDefect hcomparison
      hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    row-max/dual-budget wrapper using the canonical Frobenius stage budget.

This specializes the row-max finite-smallness route to
`storedHouseholderQRFrobStageBudget`, removing the public Frobenius-initial
budget and compact-stage recurrence hypotheses.  The remaining row-max
obligations are the scalar comparison defect for this canonical budget,
row-max diagonal defect, active-pivot policy, conditioning/dual-budget data,
local diagonal dominance, and finite-max smallness. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_frobStageBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRFrobStageBudget fp hmn A) ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let stageBudget : ℕ → ℝ := storedHouseholderQRFrobStageBudget fp hmn A
  have hinitFrob : frobNormRect A ≤ stageBudget 0 := by
    simp [stageBudget]
  have hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    change
      coxHighamActiveRowGrowthFactor m *
            storedHouseholderQRFrobStageBudget fp hmn A t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        storedHouseholderQRFrobStageBudget fp hmn A (t + 1)
    rw [storedHouseholderQRFrobStageBudget_succ_of_lt fp hmn A t ht]
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty_frobInitial
      fp hmn hn A b κ K stageBudget huSmall hDD hK hκ hκbudget
      hbudgetDual hinitFrob hglobalBudget hrowDefect hcomparison
      hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    row-max/dual-budget wrapper using the canonical Frobenius stage budget and
    the finite maximum of stored compact norm coefficients.

Local diagonal dominance now supplies the row-max diagonal-defect side condition
internally.  The compact global-product hypothesis is derived from the finite
norm-coefficient maximum rather than from the exact sequence-relative budget, so
callers can keep the numerical smallness check on the displayed finite maxima. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_rowMax_dualBudget_concreteStored_frobStageBudget_normCoeffBudgetSmallness_actualUnitRoundoff_of_nonempty
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRFrobStageBudget fp hmn A) ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (((n : ℝ) *
                ((n : ℝ) *
                  storedQRCompactStepNormBudgetCoeffBudget hmn fp
                    (storedHouseholderQRMatrixSeq fp hmn A)
                    (storedHouseholderQRAlphaSeq fp hmn A) +
                  storedQRCompactStepNormBudgetCoeffBudget hmn fp
                    (storedHouseholderQRMatrixSeq fp hmn A)
                    (storedHouseholderQRAlphaSeq fp hmn A))) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let A_hat : ℕ → Fin m → Fin n → ℝ := storedHouseholderQRMatrixSeq fp hmn A
  let b_hat : ℕ → Fin m → ℝ := storedHouseholderQRRhsSeq fp hmn A b
  let alpha : ℕ → ℝ := storedHouseholderQRAlphaSeq fp hmn A
  let stageBudget : ℕ → ℝ := storedHouseholderQRFrobStageBudget fp hmn A
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hDD_fin : ∀ k : Fin n,
      IsDiagDominantUpper (k.val + 1)
        (qrLeadingBlock (A_hat k.val)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le k.isLt hmn)) k.isLt) := by
    intro k
    simpa [A_hat] using hDD k.val k.isLt
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  have hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0 := by
    exact
      storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant hmn A_hat
        (by
          intro k hk
          simpa [A_hat] using hDD k hk)
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 := by
    exact
      storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_normBudgetCoeffBudget
        hmn fp A_hat b_hat alpha hm hDD_fin
        (by simpa [A_hat, alpha] using hsmall)
  have hinitFrob : frobNormRect A ≤ stageBudget 0 := by
    simp [stageBudget]
  have hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    change
      coxHighamActiveRowGrowthFactor m *
            storedHouseholderQRFrobStageBudget fp hmn A t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        storedHouseholderQRFrobStageBudget fp hmn A (t + 1)
    rw [storedHouseholderQRFrobStageBudget_succ_of_lt fp hmn A t ht]
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_rowMax_dualBudget_concreteStored_horizonBudget_actualUnitRoundoff_of_nonempty_frobInitial
      fp hmn hn A b κ K stageBudget huSmall hdetLead hK hκ hκbudget
      hbudgetDual hinitFrob hglobalBudget
      (by simpa [A_hat] using hrowDefect)
      (by simpa [stageBudget] using hcomparison)
      hpivotChoice
      (by simpa [A_hat, b_hat, alpha] using hglobalProduct)
/-- Source-facing exact-minimizer sibling of the scalar-comparison
    active-max-pivot row-max theorem.

This exposes the finite scalar comparison defect
`storedQRStageRowMaxComparisonDefectBudget <= 0` directly, while keeping the
determinant, `kappaInf` self-norm, dual-budget, active-pivot, row-max diagonal
defect, and compact global-product fields visible at the rectangular
least-squares minimizer surface. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageRowMaxComparisonDefect_offdiag_rows
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageRowMaxComparisonDefect_offdiag_rows
        hmn fp A_hat b_hat alpha κ K stageBudget hm hStepA hAlphaDef
        hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
        hBudget_nonneg hBudget_mono hrowDefect hcomparison hpivotChoice
        hglobalProduct)
/-- Source-facing exact-minimizer sibling of the actual-unit-roundoff
    scalar-comparison active-max-pivot row-max theorem. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hrowDefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_signedStageUniformBudget_globalCompactBudget_activeMaxPivot_completedColumns_globalProduct_rowMaxDiagDefect_stageRowMaxComparisonDefect_offdiag_rows_of_actualUnitRoundoff_no_gammaValid
        hmn fp A_hat b_hat alpha κ K stageBudget huSmall hStepA hAlphaDef
        hdetLead hK hκ hκbudget hbudgetDual hinit hinitBlock hglobalBudget
        hBudget_nonneg hBudget_mono hrowDefect hcomparison hpivotChoice
        hglobalProduct)
/-- **Forward error bound for QR-based LS solve**.

    From the backward error (Theorem 19.3), the forward error satisfies
    |x̂ − x| ≤ |(AᵀA)⁻¹| · (|ΔG| · |x̂| + |Δg|)

    Since QR factorization avoids forming AᵀA explicitly, the effective
    conditioning is κ₂(A) (not κ₂(A)²) when the residual is small.
    This is the key advantage of QR over the normal equations method.

    The proof follows the same pattern as `ls_normal_equations_forward_error`
    in LSNormalEquations.lean: extract perturbations from the structure,
    form the residual equation, and apply the inverse bound. -/
theorem ls_qr_forward_error (n : ℕ)
    (ATA ATA_inv : Fin n → Fin n → ℝ)
    (hInv : IsInverse n ATA ATA_inv)
    (ATb x x_hat : Fin n → ℝ)
    (hExact : ∀ i, matMulVec n ATA x i = ATb i)
    (ΔG : Fin n → Fin n → ℝ) (Δg : Fin n → ℝ)
    (hPerturbed : ∀ i, matMulVec n (fun a b => ATA a b + ΔG a b) x_hat i =
      ATb i + Δg i) :
    ∀ i : Fin n, |x_hat i - x i| ≤
      ∑ j : Fin n, |ATA_inv i j| *
        (∑ k : Fin n, |ΔG j k| * |x_hat k| + |Δg j|) := by
  -- This is a direct application of Theorem 7.2 (normwise_perturbation_bound)
  -- with A := AᵀA, ΔA := ΔG, Δb := Δg.
  have hExact' : ∀ i, ∑ j, ATA i j * x j = ATb i := by
    intro i; exact hExact i
  have hPerturbed' : ∀ i, ∑ j, (ATA i j + ΔG i j) * x_hat j = ATb i + Δg i := by
    intro i; exact hPerturbed i
  intro i
  rw [abs_sub_comm]
  exact normwise_perturbation_bound n ATA ATA_inv x x_hat ATb ΔG Δg
    hInv.1 hExact' hPerturbed' i

-- ============================================================
-- §19.2  QR vs Normal Equations comparison
-- ============================================================
/-- **QR vs Normal Equations: normwise forward error comparison**.

    For the Gram system AᵀAx = Aᵀb, the condition number is
    κ(AᵀA) = κ₂(A)². Hence:
    - Normal equations: forward error ≲ κ₂(A)² · u  (always)
    - QR factorization: forward error ≲ κ₂(A) · u   (when residual small)

    The QR method achieves the lower bound because it works with R
    (condition number κ₂(A)) rather than AᵀA (condition number κ₂(A)²).

    We formalize a normwise version: if |ΔG_{ij}| ≤ ε_G uniformly
    and |Δg_i| ≤ ε_g uniformly, then the forward error is bounded
    by |(AᵀA)⁻¹| · (ε_G · ∑|x̂| + ε_g) componentwise. -/
theorem gram_forward_error_normwise (n : ℕ)
    (ATA ATA_inv : Fin n → Fin n → ℝ)
    (hInv : IsInverse n ATA ATA_inv)
    (ATb x x_hat : Fin n → ℝ)
    (hExact : ∀ i, matMulVec n ATA x i = ATb i)
    (ΔG : Fin n → Fin n → ℝ) (Δg : Fin n → ℝ)
    (hPerturbed : ∀ i, matMulVec n (fun a b => ATA a b + ΔG a b) x_hat i =
      ATb i + Δg i)
    (hΔG_bound : ∀ i j, |ΔG i j| ≤ ε_G)
    (hΔg_bound : ∀ i, |Δg i| ≤ ε_g)
    (_hε_G : 0 ≤ ε_G) (_hε_g : 0 ≤ ε_g) :
    ∀ i : Fin n, |x_hat i - x i| ≤
      ∑ j : Fin n, |ATA_inv i j| *
        (ε_G * ∑ k : Fin n, |x_hat k| + ε_g) := by
  have hFwd := ls_qr_forward_error n ATA ATA_inv hInv ATb x x_hat hExact ΔG Δg hPerturbed
  intro i
  calc |x_hat i - x i|
      ≤ ∑ j : Fin n, |ATA_inv i j| *
          (∑ k : Fin n, |ΔG j k| * |x_hat k| + |Δg j|) := hFwd i
    _ ≤ ∑ j : Fin n, |ATA_inv i j| *
          (ε_G * ∑ k : Fin n, |x_hat k| + ε_g) := by
        apply Finset.sum_le_sum; intro j _
        apply mul_le_mul_of_nonneg_left _ (abs_nonneg _)
        have hSum : ∑ k : Fin n, |ΔG j k| * |x_hat k| ≤
            ε_G * ∑ k : Fin n, |x_hat k| := by
          rw [Finset.mul_sum]
          apply Finset.sum_le_sum; intro k _
          exact mul_le_mul_of_nonneg_right (hΔG_bound j k) (abs_nonneg _)
        linarith [hΔg_bound j]
/-- The exact quadratic-and-higher part of `gamma fp k`.

This is a rational expression, not asymptotic notation.  Under
`gammaValid fp k`, `gamma fp k = k*u + higham20GammaRemainder fp k`. -/
noncomputable def higham20GammaRemainder (fp : FPModel) (k : ℕ) : ℝ :=
  (((k : ℝ) * fp.u) ^ 2) / (1 - (k : ℝ) * fp.u)
/-- Translating the correction problem by the current iterate preserves the
least-squares residual exactly. -/
theorem higham20_directLSRefinement_residual_translation {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (x d : Fin n → ℝ) :
    lsResidual A (higham20DirectLSRefinementResidual A b x) d =
      lsResidual A b (higham20DirectLSRefinementUpdate x d) := by
  ext i
  unfold lsResidual higham20DirectLSRefinementResidual
    higham20DirectLSRefinementUpdate lsResidualHigham
  rw [congrFun (rectMatMulVec_add A x d) i]
  ring
theorem higham20_gamma_eq_linear_add_remainder (fp : FPModel) (k : ℕ)
    (hk : gammaValid fp k) :
    gamma fp k = (k : ℝ) * fp.u + higham20GammaRemainder fp k := by
  simpa [higham20GammaRemainder] using
    gamma_eq_linear_plus_quadratic_remainder fp k hk
/-- The correction objective in step 2 is the original objective evaluated at
the updated iterate. -/
theorem higham20_directLSRefinement_objective_translation {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (x d : Fin n → ℝ) :
    lsObjective A (higham20DirectLSRefinementResidual A b x) d =
      lsObjective A b (higham20DirectLSRefinementUpdate x d) := by
  unfold lsObjective
  rw [higham20_directLSRefinement_residual_translation A b x d]
/-- An exact solve of the correction least-squares problem makes the updated
iterate an exact minimizer of the original problem. -/
theorem higham20_directLSRefinement_update_isLeastSquaresMinimizer {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (x d : Fin n → ℝ)
    (hd : IsLeastSquaresMinimizer
      A (higham20DirectLSRefinementResidual A b x) d) :
    IsLeastSquaresMinimizer A b (higham20DirectLSRefinementUpdate x d) := by
  intro y
  let e : Fin n → ℝ := fun j => y j - x j
  have hupdate_e : higham20DirectLSRefinementUpdate x e = y := by
    ext j
    simp [higham20DirectLSRefinementUpdate, e]
  calc
    lsObjective A b (higham20DirectLSRefinementUpdate x d) =
        lsObjective A (higham20DirectLSRefinementResidual A b x) d :=
      (higham20_directLSRefinement_objective_translation A b x d).symm
    _ ≤ lsObjective A (higham20DirectLSRefinementResidual A b x) e := hd e
    _ = lsObjective A b (higham20DirectLSRefinementUpdate x e) :=
      higham20_directLSRefinement_objective_translation A b x e
    _ = lsObjective A b y := by rw [hupdate_e]
theorem Higham20DirectLSRefinementStep.updated_isLeastSquaresMinimizer
    {m n : ℕ} {A : Fin m → Fin n → ℝ} {b : Fin m → ℝ}
    {x : Fin n → ℝ} {r : Fin m → ℝ} {d y : Fin n → ℝ}
    (h : Higham20DirectLSRefinementStep A b x r d y) :
    IsLeastSquaresMinimizer A b y := by
  rw [h.update_eq]
  apply higham20_directLSRefinement_update_isLeastSquaresMinimizer A b x d
  simpa [h.residual_eq] using h.correction_minimizer
theorem Higham20DirectLSRefinementRun.successor_isLeastSquaresMinimizer
    {m n steps : ℕ} {A : Fin m → Fin n → ℝ} {b : Fin m → ℝ}
    (run : Higham20DirectLSRefinementRun A b steps) (k : Fin steps) :
    IsLeastSquaresMinimizer A b (run.iterate k.succ) :=
  (run.step k).updated_isLeastSquaresMinimizer

/-! ## Section 20.5: augmented-system iterative refinement -/
/-- The exact four-step CSNE update is an exact least-squares minimizer. -/
theorem Higham20CorrectedSeminormalEquationsStep.updated_isLeastSquaresMinimizer
    {m n : ℕ} {A : Fin m → Fin n → ℝ} {b : Fin m → ℝ}
    {R : Fin n → Fin n → ℝ} {z x : Fin n → ℝ}
    {r : Fin m → ℝ} {t w y : Fin n → ℝ}
    (h : Higham20CorrectedSeminormalEquationsStep A b R z x r t w y)
    (hGram : ∀ j k : Fin n,
      rectLSGram A j k = ∑ i : Fin n, R i j * R i k) :
    IsLeastSquaresMinimizer A b y := by
  have hcorr : IsLeastSquaresMinimizer A r w :=
    h.correction_sne.isLeastSquaresMinimizer hGram
  rw [h.update_eq]
  apply higham20_directLSRefinement_update_isLeastSquaresMinimizer A b x w
  simpa [h.residual_eq] using hcorr
/-- The exact componentwise defect majorant obtained by moving the two
Theorem 20.4 matrix perturbations and the two right-hand-side perturbations
back to the unperturbed augmented system. -/
noncomputable def higham20Eq20_4CorrectionMajorant {m n : ℕ}
    (E1 E2 : Fin m → Fin n → ℝ)
    (Deltaf : Fin m → ℝ) (Deltag : Fin n → ℝ)
    (rhat : Fin m → ℝ) (x : Fin n → ℝ) : Fin (m + n) → ℝ :=
  Fin.append
    (fun i => |Deltaf i| + ∑ j : Fin n, |E1 i j| * |x j|)
    (fun j => |Deltag j| + ∑ i : Fin m, |E2 i j| * |rhat i|)
/-- The actual residual block returned by the Householder-QR augmented solve
of Theorem 20.4. -/
noncomputable def higham20Eq20_4CorrectionResidual {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ) : Fin (n + k) → ℝ :=
  matMulVec (n + k) (higham20Eq20_4Q fp A)
    (Fin.append (higham20Eq20_4ForwardBlock fp A g)
      (fun i : Fin k =>
        higham20Eq20_4TransformedRhs fp A f (Fin.natAdd n i)))
namespace Higham20RowSorting

/-- Scaled form of the scalar Cox--Higham `sqrt 2` lemma. -/
lemma abs_two_div_mul_le_sqrt_two_mul_of_abs_le_phi_mul_sqrt_mul_sqrt
    {S T inner phi : ℝ}
    (hS : 0 ≤ S) (hT : 0 ≤ T) (hden : 2 * T ≤ S)
    (hphi : 0 ≤ phi)
    (hinner : |inner| ≤ phi * (Real.sqrt S * Real.sqrt T)) :
    |(2 / S) * inner| ≤ Real.sqrt 2 * phi := by
  by_cases hphi0 : phi = 0
  · have hinner0 : inner = 0 := by
      apply abs_eq_zero.mp
      apply le_antisymm
      · simpa [hphi0] using hinner
      · exact abs_nonneg _
    simp [hphi0, hinner0]
  · have hphipos : 0 < phi := lt_of_le_of_ne hphi (Ne.symm hphi0)
    have hscaled : |inner / phi| ≤ Real.sqrt S * Real.sqrt T := by
      rw [abs_div, abs_of_pos hphipos, div_le_iff₀ hphipos]
      simpa [mul_assoc, mul_left_comm, mul_comm] using hinner
    have hcore : |(2 / S) * (inner / phi)| ≤ Real.sqrt 2 :=
      abs_two_div_mul_le_sqrt_two_of_abs_le_sqrt_mul_sqrt
        hS hT hden hscaled
    have hin : inner = phi * (inner / phi) := by
      field_simp [hphi0]
    have hmul : (2 / S) * inner =
        phi * ((2 / S) * (inner / phi)) := by
      calc
        (2 / S) * inner = (2 / S) * (phi * (inner / phi)) :=
          congrArg (fun z : ℝ => (2 / S) * z) hin
        _ = phi * ((2 / S) * (inner / phi)) := by ring
    calc
      |(2 / S) * inner| = phi * |(2 / S) * (inner / phi)| := by
        rw [hmul]
        rw [abs_mul, abs_of_pos hphipos]
      _ ≤ phi * Real.sqrt 2 := mul_le_mul_of_nonneg_left hcore hphi
      _ = Real.sqrt 2 * phi := by ring
/-- A signed Householder update coefficient is at most `sqrt 2 * phi` when
the transformed vector's active tail is at most `phi` times the pivot-column
active norm.  This is the RHS analogue of Cox--Higham Lemma 2.1. -/
theorem abs_householderBeta_mul_inner_trailingPart_le_sqrt_two_mul
    (n : ℕ) (p : Fin n) (x y : Fin n → ℝ) (alpha phi : ℝ)
    (halpha : alpha * alpha = householderTrailingNorm2Sq n p x)
    (hsign : alpha * x p ≤ 0) (hphi : 0 ≤ phi)
    (hy : vecNorm2 (householderTrailingPart n p y) ≤
      phi * Real.sqrt (householderTrailingNorm2Sq n p x)) :
    |householderBetaSpec n (householderTrailingActiveVector n p x alpha) *
        (∑ i : Fin n,
          householderTrailingActiveVector n p x alpha i *
            householderTrailingPart n p y i)| ≤
      Real.sqrt 2 * phi := by
  let v := householderTrailingActiveVector n p x alpha
  let yTail := householderTrailingPart n p y
  let T := householderTrailingNorm2Sq n p x
  let inner := ∑ i : Fin n, v i * yTail i
  let S := ∑ i : Fin n, v i * v i
  have hS_nonneg : 0 ≤ S := by
    simpa [S, vecNorm2Sq, pow_two] using (vecNorm2Sq_nonneg v)
  have hT_nonneg : 0 ≤ T := by
    simpa [T, householderTrailingNorm2Sq] using
      (vecNorm2Sq_nonneg (householderTrailingPart n p x))
  have hden : 2 * T ≤ S := by
    simpa [S, T, v] using
      householderTrailingActiveVector_inner_self_ge_two_trailingNorm2Sq_of_mul_nonpos
        n p x alpha halpha hsign
  have hcs : |inner| ≤ Real.sqrt S * vecNorm2 yTail := by
    simpa [inner, S, v, yTail, vecNorm2, vecNorm2Sq, pow_two] using
      (abs_vecInnerProduct_le_vecNorm2_mul v yTail)
  have hinner : |inner| ≤ phi * (Real.sqrt S * Real.sqrt T) := by
    calc
      |inner| ≤ Real.sqrt S * vecNorm2 yTail := hcs
      _ ≤ Real.sqrt S * (phi * Real.sqrt T) :=
        mul_le_mul_of_nonneg_left (by simpa [T, yTail] using hy)
          (Real.sqrt_nonneg _)
      _ = phi * (Real.sqrt S * Real.sqrt T) := by ring
  simpa [householderBetaSpec, S, inner, v, yTail] using
    abs_two_div_mul_le_sqrt_two_mul_of_abs_le_phi_mul_sqrt_mul_sqrt
      hS_nonneg hT_nonneg hden hphi hinner

end Higham20RowSorting

end NumStability
