import Mathlib.Algebra.Order.GroupWithZero.Finset
import Mathlib.LinearAlgebra.Basis.VectorSpace
import Mathlib.LinearAlgebra.Charpoly.ToMatrix
import Mathlib.LinearAlgebra.Eigenspace.Charpoly
import Mathlib.LinearAlgebra.Finsupp.Pi
import Mathlib.LinearAlgebra.Matrix.FiniteDimensional
import Mathlib.LinearAlgebra.Matrix.Irreducible.Defs
import NumStability.Algorithms.NormEstimation.OneNorm.PowerMethod
import NumStability.Analysis.Asymptotics.Bounds
import NumStability.Analysis.Conditioning.DistanceToSingularity
import NumStability.Analysis.MatrixAlgebra
import NumStability.Analysis.MatrixNorms.Basic
import NumStability.Analysis.MatrixNorms.SpectralRadius
import NumStability.Analysis.OperatorNorms.Basic
import NumStability.Analysis.PerturbationTheory
import NumStability.Analysis.SingularValues.Realification
import NumStability.Analysis.Summation.Signs
import NumStability.Analysis.VectorNorms.Basic

-- Analysis/HighamChapter7.lean
--
-- Source-facing wrappers for Higham Chapter 7.
--
-- The heavy perturbation arguments live in `PerturbationTheory.lean`; this file
-- records Chapter 7 statements whose exact source shape is a relative
-- infinity-norm or practical-error corollary of those componentwise results.

















namespace NumStability

open scoped BigOperators

-- ============================================================
-- Chapter 7 forward-error kernels
-- ============================================================














































-- ============================================================
-- Problem 7.1: local Neumann contraction infrastructure
-- ============================================================

































































































































































































































































































































































































































































































-- ============================================================
-- Theorem 7.1: arbitrary subordinate-norm Rigal-Gaches wrappers
-- ============================================================




























































































































































































































































































-- ============================================================
-- Equations (7.11), (7.13), and (7.14): condition numbers
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.5: SVD-projected perturbation bound
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.6(a): row-wise relative perturbations
-- ============================================================





















































































































































































































-- ============================================================
-- Problem 7.6(b): columnwise relative perturbations
-- ============================================================












































































































































































































































-- ============================================================
-- Chapter 7 finite sign helpers
-- ============================================================
















-- ============================================================
-- Problem 7.9: linearized scalar-output componentwise condition
-- ============================================================






































































































































































































































































































































































































































































-- ------------------------------------------------------------
-- Problem 7.9: finite normwise scalar-output formula
-- ------------------------------------------------------------














































































































































































































































































































































































-- ------------------------------------------------------------
-- Problem 7.9: first-order source-radius condition values
-- ------------------------------------------------------------






















































































































































































































































































































-- ============================================================
-- Problem 7.11: linearized inverse componentwise perturbations
-- ============================================================















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.9: exact componentwise scalar-output bridge
-- ============================================================



























































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.9: exact normwise scalar-output bridge
-- ============================================================













































































































































































































































































































































































































































































































































-- ============================================================
-- Equation (7.26): distance to singularity
-- ============================================================





















-- ============================================================
-- Equation (7.15) / Problem 7.3 row-equilibration infrastructure
-- ============================================================










































































































































































































































































































































































-- ============================================================
-- Equation (7.17): Kahan's symbolic 3 × 3 example
-- ============================================================












































































































































































































































































































































































































-- ============================================================
-- Problem 7.4: unit-diagonal SPD scaling core bound
-- ============================================================






















































































































































-- ============================================================
-- Theorem 7.5 dependency: Euclidean column-equilibration bounds
-- ============================================================
















































































































































































































































































































































































































































































































































































/-- A strictly positive finite family over a nonempty `Fin n` has a strictly
positive lower bound. -/
lemma ch7_exists_pos_le_all_fin {n : ℕ} (hn : 0 < n)
    (f : Fin n → ℝ) (hpos : ∀ i : Fin n, 0 < f i) :
    ∃ c : ℝ, 0 < c ∧ ∀ i : Fin n, c ≤ f i := by
  have hnonempty : (Finset.univ : Finset (Fin n)).Nonempty :=
    Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩
  rcases Finset.exists_min_image (Finset.univ : Finset (Fin n)) f hnonempty with
    ⟨k, _hk, hkmin⟩
  exact ⟨f k, hpos k, fun i => hkmin i (Finset.mem_univ i)⟩

















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Corollary 7.6 dependency: SPD diagonal scaling via Cholesky factors
-- ============================================================


























































































































-- ============================================================
-- Theorem 7.7: Stewart-Sun Frobenius right scaling
-- ============================================================
























































































































































































































































































-- ============================================================
-- Theorem 7.8 / Problem 7.10: Bauer two-sided scaling algebraic core
-- ============================================================








































































































































































































































































































































































































lemma ch7_infNormVec_const_mul_of_nonneg {n : ℕ} {a : ℝ}
    (ha : 0 ≤ a) (x : Fin n → ℝ) :
    infNormVec (fun i : Fin n => a * x i) = a * infNormVec x := by
  unfold infNormVec
  have hfun : (fun i : Fin n => a * x i) = a • x := by
    ext i
    simp [Pi.smul_apply, smul_eq_mul]
  rw [hfun, norm_smul, Real.norm_eq_abs, abs_of_nonneg ha]

lemma ch7_infNormVec_pos_of_pos {n : ℕ} (hn : 0 < n)
    (x : Fin n → ℝ) (hx : ∀ i : Fin n, 0 < x i) :
    0 < infNormVec x := by
  let i0 : Fin n := ⟨0, hn⟩
  have hle := abs_le_infNormVec x i0
  exact lt_of_lt_of_le (abs_pos.mpr (ne_of_gt (hx i0))) hle

































lemma ch7_perronScalar_nonneg_of_nonneg_eigenvector {n : ℕ} (hn : 0 < n)
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hx : ∀ i : Fin n, 0 < x i)
    (heig : ∀ i : Fin n, matMulVec n A x i = ρ * x i) :
    0 ≤ ρ := by
  let i0 : Fin n := ⟨0, hn⟩
  have hAx_nonneg : 0 ≤ matMulVec n A x i0 := by
    unfold matMulVec
    exact Finset.sum_nonneg fun j _ => mul_nonneg (hA i0 j) (le_of_lt (hx j))
  have hprod : 0 ≤ ρ * x i0 := by
    simpa [heig i0] using hAx_nonneg
  nlinarith [hx i0, hprod]

lemma ch7_exists_pos_entry_of_nonzero_nonneg {n : ℕ} (x : Fin n → ℝ)
    (hx_nonneg : ∀ i : Fin n, 0 ≤ x i)
    (hx_ne : x ≠ 0) :
    ∃ i : Fin n, 0 < x i := by
  by_contra hnone
  apply hx_ne
  ext i
  have hi_le : x i ≤ 0 := le_of_not_gt (fun hi => hnone ⟨i, hi⟩)
  exact le_antisymm hi_le (hx_nonneg i)

lemma ch7_perronScalar_nonneg_of_nonzero_nonneg_eigenvector {n : ℕ}
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hx_nonneg : ∀ i : Fin n, 0 ≤ x i)
    (hx_ne : x ≠ 0)
    (heig : ∀ i : Fin n, matMulVec n A x i = ρ * x i) :
    0 ≤ ρ := by
  obtain ⟨i0, hxi0⟩ :=
    ch7_exists_pos_entry_of_nonzero_nonneg x hx_nonneg hx_ne
  have hAx_nonneg : 0 ≤ matMulVec n A x i0 := by
    unfold matMulVec
    exact Finset.sum_nonneg fun j _ => mul_nonneg (hA i0 j) (hx_nonneg j)
  have hprod : 0 ≤ ρ * x i0 := by
    simpa [heig i0] using hAx_nonneg
  nlinarith [hxi0, hprod]

/-- A positive right eigenvector for a nonnegative matrix with at least one
positive entry has a positive eigenvalue.  This is the strict-positivity
version needed by Bauer's irreducible `Cx` transfer; it is not a
Perron-Frobenius existence theorem. -/
lemma ch7_perronScalar_pos_of_nonneg_eigenvector_entry_pos {n : ℕ}
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hx : ∀ i : Fin n, 0 < x i)
    (heig : ∀ i : Fin n, matMulVec n A x i = ρ * x i)
    (hentry : ∃ i j : Fin n, 0 < A i j) :
    0 < ρ := by
  obtain ⟨i, j, hij⟩ := hentry
  have hterms_nonneg :
      ∀ k : Fin n, 0 ≤ A i k * x k := by
    intro k
    exact mul_nonneg (hA i k) (le_of_lt (hx k))
  have hterm_pos : 0 < A i j * x j := mul_pos hij (hx j)
  have hAx_pos : 0 < matMulVec n A x i := by
    unfold matMulVec
    exact Finset.sum_pos' (fun k _ => hterms_nonneg k)
      ⟨j, Finset.mem_univ j, hterm_pos⟩
  have hprod : 0 < ρ * x i := by
    simpa [heig i] using hAx_pos
  nlinarith [hx i, hprod]

/-- If a nonnegative matrix has a positive right eigenvector with eigenvalue
`ρ`, then `ρ` is bounded by the infinity norm.  This is the finite-dimensional
norm half used in Bauer's scaling proof; it does not assert Perron-Frobenius
existence. -/
theorem ch7_infNorm_ge_of_nonneg_right_eigenvector {n : ℕ} (hn : 0 < n)
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hx : ∀ i : Fin n, 0 < x i)
    (heig : ∀ i : Fin n, matMulVec n A x i = ρ * x i) :
    ρ ≤ infNorm A := by
  have hρ : 0 ≤ ρ :=
    ch7_perronScalar_nonneg_of_nonneg_eigenvector hn A ρ x hA hx heig
  have hxnorm_pos : 0 < infNormVec x := ch7_infNormVec_pos_of_pos hn x hx
  have hAx :
      matMulVec n A x = fun i : Fin n => ρ * x i := by
    ext i
    exact heig i
  have hnorm := infNormVec_matMulVec_le hn A x
  rw [hAx, ch7_infNormVec_const_mul_of_nonneg hρ x] at hnorm
  nlinarith [hxnorm_pos, hnorm]

/-- Local finite-dimensional eigenvalue-radius predicate for the Chapter 7
Bauer/Perron row.  It records that `ρ` is attained by a complexified
eigenvector and bounds the modulus of every complex eigenvalue certificate.
This is not Mathlib's Banach-algebra `spectralRadius`; it is the finite
eigenvalue-radius bridge needed before that API identification. -/
def ch7IsComplexEigenvalueRadius {n : ℕ}
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) : Prop :=
  0 ≤ ρ ∧
    (∃ z : CVec n, z ≠ 0 ∧
      ∀ i : Fin n, complexMatrixVecMul (realRectToCMatrix A) z i = (ρ : ℂ) * z i) ∧
    ∀ (mu : ℂ) (z : CVec n), z ≠ 0 →
      (∀ i : Fin n, complexMatrixVecMul (realRectToCMatrix A) z i = mu * z i) →
        ‖mu‖ ≤ ρ

/-- The finite set-like value surface for the local Chapter 7 spectral-radius
bridge: all moduli of complex eigenvalue certificates of the complexification
of a real matrix.  This is a local finite-dimensional model of spectral
radius, not Mathlib's Banach-algebra `spectralRadius`. -/
def ch7ComplexEigenvalueModulusSet {n : ℕ}
    (A : Fin n → Fin n → ℝ) : Set ℝ :=
  {r : ℝ | ∃ mu : ℂ, ∃ z : CVec n,
    z ≠ 0 ∧
      (∀ i : Fin n, complexMatrixVecMul (realRectToCMatrix A) z i = mu * z i) ∧
      r = ‖mu‖}

/-- The local radius predicate says exactly that `ρ` is a greatest element of
the finite complex-eigenvalue-modulus value set. -/
theorem ch7_complexEigenvalueModulusSet_isGreatest_of_isComplexEigenvalueRadius
    {n : ℕ} {A : Fin n → Fin n → ℝ} {ρ : ℝ}
    (hρ : ch7IsComplexEigenvalueRadius A ρ) :
    IsGreatest (ch7ComplexEigenvalueModulusSet A) ρ := by
  rcases hρ with ⟨hρ_nonneg, ⟨z, hz_ne, hz_eig⟩, hbound⟩
  constructor
  · refine ⟨(ρ : ℂ), z, hz_ne, ?_, ?_⟩
    · simpa using hz_eig
    · exact (Complex.norm_of_nonneg hρ_nonneg).symm
  · intro r hr
    rcases hr with ⟨mu, z, hz_ne, hz_eig, rfl⟩
    exact hbound mu z hz_ne hz_eig

/-- Local finite-dimensional spectral-radius equality in `sSup` form:
`ρ` is the supremum of the complex eigenvalue moduli whenever
`ch7IsComplexEigenvalueRadius A ρ` holds.  This is the source-shaped bridge
toward Higham's `ρ(A)`; the `toLin_spectralRadius` lemmas below identify the
same maximum with Mathlib's Banach-algebra `spectralRadius`. -/
theorem ch7_complexEigenvalueModulusSet_sSup_eq_of_isComplexEigenvalueRadius
    {n : ℕ} {A : Fin n → Fin n → ℝ} {ρ : ℝ}
    (hρ : ch7IsComplexEigenvalueRadius A ρ) :
    sSup (ch7ComplexEigenvalueModulusSet A) = ρ := by
  have hgreatest :=
    ch7_complexEigenvalueModulusSet_isGreatest_of_isComplexEigenvalueRadius
      (A := A) (ρ := ρ) hρ
  exact le_antisymm
    (csSup_le ⟨ρ, hgreatest.1⟩ hgreatest.2)
    (le_csSup ⟨ρ, hgreatest.2⟩ hgreatest.1)

/-- Chapter 7's local real-matrix eigenvalue-modulus carrier is exactly the
generic Split 1 complex-matrix carrier after real complexification. -/
theorem ch7_complexEigenvalueModulusSet_eq_complexMatrixEigenvalueModulusSet
    {n : ℕ} (A : Fin n → Fin n → ℝ) :
    ch7ComplexEigenvalueModulusSet A =
      ComplexMatrixEigenvalueModulusSet (realRectToCMatrix A) := by
  ext r
  constructor
  · intro hr
    rcases hr with ⟨mu, z, hz_ne, hz_eig, rfl⟩
    refine ⟨mu, z, hz_ne, ?_, rfl⟩
    ext i
    simpa [complexVecSMul] using hz_eig i
  · intro hr
    rcases hr with ⟨mu, z, hz_ne, hz_eig, rfl⟩
    refine ⟨mu, z, hz_ne, ?_, rfl⟩
    intro i
    exact congrFun hz_eig i

/-- Chapter 7's local real-matrix eigenvalue-modulus carrier is also exactly
the moduli of Mathlib spectrum elements of the complexified `toLin'` matrix. -/
theorem ch7_complexEigenvalueModulusSet_eq_toLin_spectrum_modulusSet
    {n : ℕ} (A : Fin n → Fin n → ℝ) :
    ch7ComplexEigenvalueModulusSet A =
      {r : ℝ | ∃ lam : ℂ,
        lam ∈ spectrum ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix A)) ∧
        r = ‖lam‖} := by
  rw [ch7_complexEigenvalueModulusSet_eq_complexMatrixEigenvalueModulusSet A,
    complexMatrixEigenvalueModulusSet_eq_toLin_spectrum_modulusSet]

/-- A Chapter 7 local finite spectral-radius certificate exports to the generic
Split 1 maximum-eigenvalue-modulus interface for the complexified matrix. -/
theorem ch7_isMaxComplexMatrixEigenvalueModulus_of_isComplexEigenvalueRadius
    {n : ℕ} {A : Fin n → Fin n → ℝ} {ρ : ℝ}
    (hρ : ch7IsComplexEigenvalueRadius A ρ) :
    IsMaxComplexMatrixEigenvalueModulus (realRectToCMatrix A) ρ := by
  have hgreatest :=
    ch7_complexEigenvalueModulusSet_isGreatest_of_isComplexEigenvalueRadius
      (A := A) (ρ := ρ) hρ
  constructor
  · simpa [ch7_complexEigenvalueModulusSet_eq_complexMatrixEigenvalueModulusSet A]
      using hgreatest.1
  · intro r hr
    exact hgreatest.2
      (by
        simpa [ch7_complexEigenvalueModulusSet_eq_complexMatrixEigenvalueModulusSet A]
          using hr)

/-- A Chapter 7 local finite spectral-radius certificate also gives a greatest
element of the Mathlib-spectrum-modulus set for the complexified `toLin'`
matrix. -/
theorem ch7_toLin_spectrum_modulusSet_isGreatest_of_isComplexEigenvalueRadius
    {n : ℕ} {A : Fin n → Fin n → ℝ} {ρ : ℝ}
    (hρ : ch7IsComplexEigenvalueRadius A ρ) :
    IsGreatest
      {r : ℝ | ∃ lam : ℂ,
        lam ∈ spectrum ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix A)) ∧
        r = ‖lam‖} ρ := by
  have hgreatest :=
    ch7_complexEigenvalueModulusSet_isGreatest_of_isComplexEigenvalueRadius
      (A := A) (ρ := ρ) hρ
  simpa [ch7_complexEigenvalueModulusSet_eq_toLin_spectrum_modulusSet A]
    using hgreatest






























































































































/-- Finite complex matrix endomorphisms have a nonzero complex eigenpair whose
eigenvalue attains Mathlib's algebraic `spectralRadius`.  This closes the
finite-dimensional complex spectral-radius existence half of the Chapter 7
Bauer/Perron bridge; it does not supply a nonnegative real Perron vector. -/
theorem ch7_toLin_exists_spectralRadius_attaining_eigenpair {n : ℕ}
    (hn : 0 < n) (A : Matrix (Fin n) (Fin n) ℂ) :
    ∃ lam : ℂ, ∃ z : Fin n → ℂ,
      z ≠ 0 ∧ Matrix.toLin' A z = lam • z ∧
      (nnnorm lam : ENNReal) = spectralRadius ℂ (Matrix.toLin' A) := by
  classical
  haveI : Nonempty (Fin n) := ⟨⟨0, hn⟩⟩
  have hs_nonempty : (spectrum ℂ (Matrix.toLin' A)).Nonempty := by
    exact spectrum.nonempty_of_isAlgClosed_of_finiteDimensional ℂ (Matrix.toLin' A)
  have hs_finite : (spectrum ℂ (Matrix.toLin' A)).Finite := by
    exact Module.End.finite_spectrum (Matrix.toLin' A)
  have hmem := hs_nonempty.ciSup_mem_image
    (s := spectrum ℂ (Matrix.toLin' A))
    (f := fun lam : ℂ => (nnnorm lam : ENNReal)) hs_finite
  rcases hmem with ⟨lam, hlam, hEq⟩
  have heig : Module.End.HasEigenvalue (Matrix.toLin' A) lam :=
    Module.End.HasEigenvalue.of_mem_spectrum hlam
  rcases Module.End.HasEigenvalue.exists_hasEigenvector heig with ⟨z, hz⟩
  refine ⟨lam, z, (Module.End.hasEigenvector_iff.mp hz).2, ?_, ?_⟩
  · exact Module.End.HasEigenvector.apply_eq_smul hz
  · simpa [spectralRadius] using hEq

/-- Taking componentwise moduli of a complex eigenvector of a nonnegative real
matrix gives a nonzero nonnegative real subeigenvector.  This is the
source-facing Collatz-Wielandt/Perron-Frobenius bridge used by the Chapter 7
Bauer row: it proves the elementary inequality step, while the remaining PF
gate is the upgrade from subeigenvector to a positive real Perron eigenvector
under irreducibility. -/
lemma ch7_abs_complex_eigenvector_subeigenvector_of_nonneg_matrix {n : ℕ}
    (A : Fin n → Fin n → ℝ) (mu : ℂ) (z : CVec n)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hz_ne : z ≠ 0)
    (heig : ∀ i : Fin n,
      complexMatrixVecMul (realRectToCMatrix A) z i = mu * z i) :
    let x : Fin n → ℝ := fun i : Fin n => ‖z i‖
    x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      ∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n A x i := by
  classical
  let x : Fin n → ℝ := fun i : Fin n => ‖z i‖
  have hx_ne : x ≠ 0 := by
    intro hx_zero
    apply hz_ne
    ext i
    have hi := congrFun hx_zero i
    dsimp [x] at hi
    exact norm_eq_zero.mp hi
  refine ⟨hx_ne, ?_, ?_⟩
  · intro i
    exact norm_nonneg (z i)
  · intro i
    have htri :
        ‖mu * z i‖ ≤ ∑ j : Fin n, ‖(A i j : ℂ) * z j‖ := by
      calc
        ‖mu * z i‖ = ‖complexMatrixVecMul (realRectToCMatrix A) z i‖ := by
          rw [heig i]
        _ ≤ ∑ j : Fin n, ‖(A i j : ℂ) * z j‖ := by
          unfold complexMatrixVecMul realRectToCMatrix
          simpa using
            norm_sum_le Finset.univ (fun j : Fin n => (A i j : ℂ) * z j)
    have hterms_eq :
        (∑ j : Fin n, ‖(A i j : ℂ) * z j‖) =
          ∑ j : Fin n, A i j * x j := by
      apply Finset.sum_congr rfl
      intro j _
      rw [norm_mul]
      have hAnorm : ‖(A i j : ℂ)‖ = A i j := by
        rw [Complex.norm_of_nonneg (hA i j)]
      rw [hAnorm]
    calc
      ‖mu‖ * x i = ‖mu * z i‖ := by
        rw [norm_mul]
      _ ≤ ∑ j : Fin n, ‖(A i j : ℂ) * z j‖ := htri
      _ = ∑ j : Fin n, A i j * x j := hterms_eq
      _ = matMulVec n A x i := by rfl

/-- A nonnegative real matrix has a complex spectral-radius-attaining
eigenpair whose componentwise modulus vector is a nonzero nonnegative
subeigenvector.  This packages finite-dimensional spectral-radius attainment
with the elementary absolute-value inequality needed before the final
Perron-Frobenius positivity upgrade. -/
theorem ch7_exists_spectralRadius_attaining_nonneg_subeigenvector {n : ℕ}
    (hn : 0 < n) (A : Fin n → Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix A) z i = mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix A)) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n A x i) := by
  let M : Matrix (Fin n) (Fin n) ℂ :=
    (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix A)
  obtain ⟨mu, z, hz_ne, hz_lin, hrad⟩ :=
    ch7_toLin_exists_spectralRadius_attaining_eigenpair hn M
  have heig : ∀ i : Fin n,
      complexMatrixVecMul (realRectToCMatrix A) z i = mu * z i := by
    intro i
    have hi := congrFun hz_lin i
    simpa [M, complexMatrixVecMul, Matrix.toLin'_apply, Matrix.mulVec,
      dotProduct, Pi.smul_apply, smul_eq_mul] using hi
  let x : Fin n → ℝ := fun i : Fin n => ‖z i‖
  have hsub :=
    ch7_abs_complex_eigenvector_subeigenvector_of_nonneg_matrix
      A mu z hA hz_ne heig
  rcases hsub with ⟨hx_ne, hx_nonneg, hsubeig⟩
  refine ⟨mu, z, x, hz_ne, hx_ne, hx_nonneg, heig, ?_, hsubeig⟩
  simpa [M] using hrad
















/-- A positive real eigenvector for a nonnegative real matrix bounds every
complex eigenvalue certificate by the same real scalar.  This is the elementary
finite-dimensional spectral-radius half of the Bauer/Perron-Frobenius bridge:
choose an index maximizing `‖zᵢ‖ / xᵢ` and compare `|Az|` with `Ax`. -/
theorem ch7_complex_eigenvalue_norm_le_of_positive_real_eigenvector {n : ℕ}
    (hn : 0 < n)
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ) (mu : ℂ) (z : CVec n)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hx : ∀ i : Fin n, 0 < x i)
    (heig_real : ∀ i : Fin n, matMulVec n A x i = ρ * x i)
    (hz_ne : z ≠ 0)
    (heig_complex : ∀ i : Fin n,
      complexMatrixVecMul (realRectToCMatrix A) z i = mu * z i) :
    ‖mu‖ ≤ ρ := by
  classical
  let ratios : Fin n → ℝ := fun i => ‖z i‖ / x i
  let R : ℝ := Finset.univ.sup'
    (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩) ratios
  obtain ⟨imax, _himem, hR_eq⟩ :=
    Finset.exists_mem_eq_sup' (s := (Finset.univ : Finset (Fin n)))
      (Finset.univ_nonempty_iff.mpr ⟨⟨0, hn⟩⟩) ratios
  have hratio_le : ∀ j : Fin n, ratios j ≤ R := by
    intro j
    exact Finset.le_sup' (s := (Finset.univ : Finset (Fin n))) (f := ratios)
      (Finset.mem_univ j)
  have hz_bound : ∀ j : Fin n, ‖z j‖ ≤ R * x j := by
    intro j
    have h := hratio_le j
    dsimp [ratios] at h
    exact (div_le_iff₀ (hx j)).mp h
  have hz_exists : ∃ i : Fin n, z i ≠ 0 := by
    by_contra hnone
    apply hz_ne
    ext i
    by_contra hzi
    exact hnone ⟨i, hzi⟩
  have hR_pos : 0 < R := by
    obtain ⟨i, hzi⟩ := hz_exists
    have hpos_ratio : 0 < ratios i := div_pos (norm_pos_iff.mpr hzi) (hx i)
    exact lt_of_lt_of_le hpos_ratio (hratio_le i)
  have hleft : ‖mu‖ * ‖z imax‖ ≤ R * (ρ * x imax) := by
    have htri :
        ‖mu * z imax‖ ≤ ∑ j : Fin n, ‖(A imax j : ℂ) * z j‖ := by
      calc
        ‖mu * z imax‖ =
            ‖complexMatrixVecMul (realRectToCMatrix A) z imax‖ := by
          rw [heig_complex imax]
        _ ≤ ∑ j : Fin n, ‖(A imax j : ℂ) * z j‖ := by
          unfold complexMatrixVecMul realRectToCMatrix
          simpa using
            norm_sum_le Finset.univ (fun j : Fin n => (A imax j : ℂ) * z j)
    have hterms_eq :
        (∑ j : Fin n, ‖(A imax j : ℂ) * z j‖) =
          ∑ j : Fin n, A imax j * ‖z j‖ := by
      apply Finset.sum_congr rfl
      intro j _
      rw [norm_mul]
      have hAnorm : ‖(A imax j : ℂ)‖ = A imax j := by
        rw [Complex.norm_of_nonneg (hA imax j)]
      rw [hAnorm]
    have hsum_bound :
        (∑ j : Fin n, A imax j * ‖z j‖) ≤
          ∑ j : Fin n, A imax j * (R * x j) := by
      exact Finset.sum_le_sum fun j _ =>
        mul_le_mul_of_nonneg_left (hz_bound j) (hA imax j)
    have hsum_eq :
        (∑ j : Fin n, A imax j * (R * x j)) =
          R * (ρ * x imax) := by
      calc
        (∑ j : Fin n, A imax j * (R * x j)) =
            R * (∑ j : Fin n, A imax j * x j) := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro j _
          ring
        _ = R * matMulVec n A x imax := by rfl
        _ = R * (ρ * x imax) := by rw [heig_real imax]
    calc
      ‖mu‖ * ‖z imax‖ = ‖mu * z imax‖ := by rw [norm_mul]
      _ ≤ ∑ j : Fin n, ‖(A imax j : ℂ) * z j‖ := htri
      _ = ∑ j : Fin n, A imax j * ‖z j‖ := hterms_eq
      _ ≤ ∑ j : Fin n, A imax j * (R * x j) := hsum_bound
      _ = R * (ρ * x imax) := hsum_eq
  have hright_eq : R * (ρ * x imax) = ρ * (R * x imax) := by ring
  have hleft_eq : ‖z imax‖ = R * x imax := by
    have hratio_eq : ratios imax = R := hR_eq.symm
    dsimp [ratios] at hratio_eq
    exact (div_eq_iff (ne_of_gt (hx imax))).mp hratio_eq
  have hcancel : ‖mu‖ * (R * x imax) ≤ ρ * (R * x imax) := by
    simpa [hleft_eq, hright_eq] using hleft
  have hden_pos : 0 < R * x imax := mul_pos hR_pos (hx imax)
  nlinarith [hden_pos, hcancel]

lemma ch7_real_positive_eigenvector_complexified {n : ℕ}
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (heig_real : ∀ i : Fin n, matMulVec n A x i = ρ * x i) :
    ∀ i : Fin n,
      complexMatrixVecMul (realRectToCMatrix A) (fun j : Fin n => (x j : ℂ)) i =
        (ρ : ℂ) * (x i : ℂ) := by
  intro i
  have h := congrArg (fun r : ℝ => (r : ℂ)) (heig_real i)
  simpa [complexMatrixVecMul, realRectToCMatrix, matMulVec, Complex.ofReal_sum,
    Complex.ofReal_mul] using h

/-- A positive real eigenvector for a nonnegative real matrix makes `ρ` the
attained finite complex-eigenvalue radius in the local Chapter 7 sense.  Full
Perron-Frobenius existence of such a vector remains a separate source-facing
target. -/
theorem ch7_isComplexEigenvalueRadius_of_positive_real_eigenvector {n : ℕ}
    (hn : 0 < n)
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hx : ∀ i : Fin n, 0 < x i)
    (heig_real : ∀ i : Fin n, matMulVec n A x i = ρ * x i) :
    ch7IsComplexEigenvalueRadius A ρ := by
  refine ⟨?_, ?_, ?_⟩
  · exact ch7_perronScalar_nonneg_of_nonneg_eigenvector
      hn A ρ x hA hx heig_real
  · let z : CVec n := fun j : Fin n => (x j : ℂ)
    refine ⟨z, ?_, ?_⟩
    · intro hz
      have h0 := congrFun hz ⟨0, hn⟩
      have hx0_ne : (x ⟨0, hn⟩ : ℂ) ≠ 0 := by
        exact_mod_cast ne_of_gt (hx ⟨0, hn⟩)
      exact hx0_ne h0
    · simpa [z] using
        ch7_real_positive_eigenvector_complexified A ρ x heig_real
  · intro mu z hz_ne heig_complex
    exact ch7_complex_eigenvalue_norm_le_of_positive_real_eigenvector
      hn A ρ x mu z hA hx heig_real hz_ne heig_complex




































































































































































































































































































































































































































































































































































































































































































































































































lemma ch7_matrix_mulVec_eq_matMulVec {n : ℕ}
    (M : Fin n → Fin n → ℝ) (x : Fin n → ℝ) :
    Matrix.mulVec (Matrix.of M : Matrix (Fin n) (Fin n) ℝ) x =
      matMulVec n M x := by
  ext i
  simp [Matrix.mulVec, dotProduct, matMulVec]

lemma ch7_matrix_pow_mulVec_eigen {n : ℕ}
    (M : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (heig : ∀ i : Fin n, matMulVec n M x i = ρ * x i) :
    ∀ k : ℕ,
      Matrix.mulVec ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x =
        fun i : Fin n => ρ ^ k * x i := by
  intro k
  induction k with
  | zero =>
      ext i
      simp
  | succ k ih =>
      ext i
      rw [pow_succ']
      rw [← Matrix.mulVec_mulVec]
      rw [ih]
      have hvec : (fun i : Fin n => ρ ^ k * x i) = (ρ ^ k) • x := by
        ext j
        simp [Pi.smul_apply, smul_eq_mul]
      rw [hvec]
      rw [Matrix.mulVec_smul]
      simp [ch7_matrix_mulVec_eq_matMulVec, heig, Pi.smul_apply, smul_eq_mul]
      ring

/-- A positive right eigenvector for a nonnegative irreducible matrix has a
positive eigenvalue.  This is still certificate-level Bauer infrastructure: it
uses irreducibility to prove strict positivity of an already supplied
eigenvalue/eigenvector pair, but does not assert Perron-Frobenius existence. -/
lemma ch7_perronScalar_pos_of_nonneg_irreducible_eigenvector {n : ℕ}
    (hn : 0 < n)
    (A : Fin n → Fin n → ℝ) (ρ : ℝ) (x : Fin n → ℝ)
    (hA : ∀ i j : Fin n, 0 ≤ A i j)
    (hIrred : Matrix.IsIrreducible
      (Matrix.of A : Matrix (Fin n) (Fin n) ℝ))
    (hx : ∀ i : Fin n, 0 < x i)
    (heig : ∀ i : Fin n, matMulVec n A x i = ρ * x i) :
    0 < ρ := by
  classical
  have hρ_nonneg : 0 ≤ ρ :=
    ch7_perronScalar_nonneg_of_nonneg_eigenvector hn A ρ x hA hx heig
  let i0 : Fin n := ⟨0, hn⟩
  have hA_nonneg : ∀ i j : Fin n,
      0 ≤ (Matrix.of A : Matrix (Fin n) (Fin n) ℝ) i j := hA
  have hpow_exists : ∀ i j : Fin n,
      ∃ k > 0, 0 < ((Matrix.of A : Matrix (Fin n) (Fin n) ℝ) ^ k) i j :=
    (Matrix.isIrreducible_iff_exists_pow_pos hA_nonneg).mp hIrred
  obtain ⟨k, hk_pos, hpow_i0⟩ := hpow_exists i0 i0
  have hterms_nonneg : ∀ l : Fin n,
      0 ≤ ((Matrix.of A : Matrix (Fin n) (Fin n) ℝ) ^ k) i0 l * x l := by
    intro l
    exact mul_nonneg (Matrix.pow_apply_nonneg hA_nonneg k i0 l) (le_of_lt (hx l))
  have hterm_pos :
      0 < ((Matrix.of A : Matrix (Fin n) (Fin n) ℝ) ^ k) i0 i0 * x i0 :=
    mul_pos hpow_i0 (hx i0)
  have hmul_pos :
      0 <
        Matrix.mulVec
          ((Matrix.of A : Matrix (Fin n) (Fin n) ℝ) ^ k) x i0 := by
    unfold Matrix.mulVec dotProduct
    exact Finset.sum_pos' (fun l _ => hterms_nonneg l)
      ⟨i0, Finset.mem_univ i0, hterm_pos⟩
  have hpow_eig := congrFun (ch7_matrix_pow_mulVec_eigen A ρ x heig k) i0
  have hprod_pos : 0 < ρ ^ k * x i0 := by
    simpa [hpow_eig] using hmul_pos
  have hρ_pow_pos : 0 < ρ ^ k := by
    nlinarith [hx i0, hprod_pos]
  have hρ_ne : ρ ≠ 0 := by
    intro hzero
    subst ρ
    simp [Nat.ne_of_gt hk_pos] at hρ_pow_pos
  exact lt_of_le_of_ne hρ_nonneg (Ne.symm hρ_ne)



















































/-- Irreducibility propagates any nonzero nonnegative vector to every
coordinate after some positive matrix power.  This is the reusable
support-propagation dependency for the Perron-Frobenius route behind
Theorem 7.8 / Problem 7.10; it does not assert existence of a Perron
eigenvector. -/
lemma ch7_irreducible_pow_mulVec_pos_of_nonzero_nonneg {n : ℕ}
    (M : Fin n → Fin n → ℝ) (x : Fin n → ℝ)
    (hM : Matrix.IsIrreducible (Matrix.of M : Matrix (Fin n) (Fin n) ℝ))
    (hx_nonneg : ∀ i : Fin n, 0 ≤ x i)
    (hx_ne : x ≠ 0) :
    ∀ i : Fin n, ∃ k > 0,
      0 <
        Matrix.mulVec
          ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x i := by
  classical
  have hA_nonneg : ∀ i j : Fin n,
      0 ≤ (Matrix.of M : Matrix (Fin n) (Fin n) ℝ) i j := hM.1
  have hpow_exists : ∀ i j : Fin n,
      ∃ k > 0, 0 < ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) i j :=
    (Matrix.isIrreducible_iff_exists_pow_pos hA_nonneg).mp hM
  obtain ⟨j, hxj⟩ :=
    ch7_exists_pos_entry_of_nonzero_nonneg x hx_nonneg hx_ne
  intro i
  obtain ⟨k, hk_pos, hpow_ij⟩ := hpow_exists i j
  have hterms_nonneg : ∀ l : Fin n,
      0 ≤ ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) i l * x l := by
    intro l
    exact mul_nonneg (Matrix.pow_apply_nonneg hA_nonneg k i l) (hx_nonneg l)
  have hterm_pos :
      0 < ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) i j * x j :=
    mul_pos hpow_ij hxj
  refine ⟨k, hk_pos, ?_⟩
  unfold Matrix.mulVec dotProduct
  exact Finset.sum_pos' (fun l _ => hterms_nonneg l)
    ⟨j, Finset.mem_univ j, hterm_pos⟩

/-- Powers of a nonnegative matrix send nonnegative vectors to nonnegative
vectors.  This is kept in Chapter 7 because the current Perron-Frobenius
closure uses it only for the Bauer-scaling irreducibility path. -/
lemma ch7_matrix_pow_mulVec_nonneg_of_nonneg {n : ℕ}
    (M : Fin n → Fin n → ℝ) (x : Fin n → ℝ)
    (hM_nonneg : ∀ i j : Fin n,
      0 ≤ (Matrix.of M : Matrix (Fin n) (Fin n) ℝ) i j)
    (hx_nonneg : ∀ i : Fin n, 0 ≤ x i) :
    ∀ k : ℕ, ∀ i : Fin n,
      0 ≤
        Matrix.mulVec
          ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x i := by
  intro k i
  unfold Matrix.mulVec dotProduct
  exact Finset.sum_nonneg fun j _ =>
    mul_nonneg (Matrix.pow_apply_nonneg hM_nonneg k i j) (hx_nonneg j)

/-- Nonnegative matrices are monotone for componentwise vector order.  This is
the local order adapter needed to iterate the nonnegative subeigenvector
inequality on the Perron-Frobenius route. -/
lemma ch7_matrix_mulVec_mono_of_nonneg {n : ℕ}
    (A : Matrix (Fin n) (Fin n) ℝ) {u v : Fin n → ℝ}
    (hA_nonneg : ∀ i j : Fin n, 0 ≤ A i j)
    (huv : ∀ j : Fin n, u j ≤ v j) :
    ∀ i : Fin n, Matrix.mulVec A u i ≤ Matrix.mulVec A v i := by
  intro i
  unfold Matrix.mulVec
  exact dotProduct_le_dotProduct_of_nonneg_left
    (u := u) (v := v) (w := fun j : Fin n => A i j)
    (show u ≤ v from fun j => huv j)
    (show 0 ≤ fun j : Fin n => A i j from fun j => hA_nonneg i j)

/-- A componentwise subeigenvector inequality for a nonnegative matrix
iterates through every matrix power.  This is a dependency for the
spectral-radius-attaining nonnegative subeigenvector stage of the
Perron-Frobenius route in Theorem 7.8 / Problem 7.10. -/
lemma ch7_matrix_pow_mulVec_subeigen_le_of_nonneg {n : ℕ}
    (A : Matrix (Fin n) (Fin n) ℝ) (lam : ℝ) (x : Fin n → ℝ)
    (hA_nonneg : ∀ i j : Fin n, 0 ≤ A i j)
    (hlam : 0 ≤ lam)
    (hsub : ∀ i : Fin n, lam * x i ≤ Matrix.mulVec A x i) :
    ∀ k : ℕ, ∀ i : Fin n,
      lam ^ k * x i ≤ Matrix.mulVec (A ^ k) x i := by
  intro k
  induction k with
  | zero =>
      intro i
      simp
  | succ k ih =>
      intro i
      have hlamk : 0 ≤ lam ^ k := pow_nonneg hlam k
      have hscaled :
          lam ^ k * (lam * x i) ≤ lam ^ k * Matrix.mulVec A x i :=
        mul_le_mul_of_nonneg_left (hsub i) hlamk
      have hleft : lam ^ Nat.succ k * x i = lam ^ k * (lam * x i) := by
        rw [pow_succ]
        ring
      have hscale_mulVec :
          Matrix.mulVec A (fun j : Fin n => lam ^ k * x j) i =
            lam ^ k * Matrix.mulVec A x i := by
        have hvec : (fun j : Fin n => lam ^ k * x j) = (lam ^ k) • x := by
          ext j
          simp [Pi.smul_apply, smul_eq_mul]
        rw [hvec, Matrix.mulVec_smul]
        simp [Pi.smul_apply, smul_eq_mul]
      have hmono :
          Matrix.mulVec A (fun j : Fin n => lam ^ k * x j) i ≤
            Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i :=
        ch7_matrix_mulVec_mono_of_nonneg A hA_nonneg (fun j => ih j) i
      have hpow_mulVec :
          Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i =
            Matrix.mulVec (A ^ Nat.succ k) x i := by
        rw [pow_succ']
        rw [← Matrix.mulVec_mulVec]
      calc
        lam ^ Nat.succ k * x i = lam ^ k * (lam * x i) := hleft
        _ ≤ lam ^ k * Matrix.mulVec A x i := hscaled
        _ = Matrix.mulVec A (fun j : Fin n => lam ^ k * x j) i :=
          hscale_mulVec.symm
        _ ≤ Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i := hmono
        _ = Matrix.mulVec (A ^ Nat.succ k) x i := hpow_mulVec

/-- A nonempty finite complex vector attains its supremum norm at a coordinate. -/
lemma ch7_complexVec_norm_eq_coord_of_nonempty {n : ℕ} (hn : 0 < n)
    (x : Fin n → ℂ) :
    ∃ i : Fin n, ‖x‖ = ‖x i‖ := by
  classical
  have huniv : (Finset.univ : Finset (Fin n)).Nonempty :=
    ⟨⟨0, hn⟩, Finset.mem_univ _⟩
  obtain ⟨i, _hi, hsup⟩ :=
    Finset.exists_mem_eq_sup
      (s := (Finset.univ : Finset (Fin n))) huniv
      (fun i : Fin n => ‖x i‖₊)
  refine ⟨i, ?_⟩
  have hnn : ‖x‖₊ = ‖x i‖₊ := by
    simpa [Pi.nnnorm_def] using hsup
  exact congrArg (fun a : NNReal => (a : ℝ)) hnn

/-- Complexification commutes with matrix powers for Chapter 7 square real
matrices. -/
lemma ch7_realRectToCMatrix_matrix_pow {n : ℕ}
    (M : Fin n → Fin n → ℝ) :
    ∀ k : ℕ,
      (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M) ^ k =
        fun i j => (((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) i j : ℂ) := by
  intro k
  induction k with
  | zero =>
      ext i j
      by_cases hij : i = j
      · subst j
        simp
      · simp [hij]
  | succ k ih =>
      rw [pow_succ', pow_succ', ih]
      ext i j
      simp [Matrix.mul_apply, realRectToCMatrix, Complex.ofReal_sum,
        Complex.ofReal_mul]

/-- Applying a complexified real matrix power to a complexified real vector is
the complexification of the corresponding real powered matrix-vector product. -/
lemma ch7_realRectToCMatrix_pow_mulVec_complexified {n : ℕ}
    (M : Fin n → Fin n → ℝ) (x : Fin n → ℝ) (k : ℕ) :
    Matrix.mulVec
        ((show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M) ^ k)
        (fun i : Fin n => (x i : ℂ)) =
      fun i : Fin n =>
        ((Matrix.mulVec ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x i : ℝ) : ℂ) := by
  ext i
  rw [ch7_realRectToCMatrix_matrix_pow M k]
  simp [Matrix.mulVec, dotProduct, Complex.ofReal_sum, Complex.ofReal_mul]

section Ch7MatrixOperatorNorm

open scoped Matrix.Norms.Operator

/-- A positive real subeigenvector gives a lower bound on every power's
matrix operator norm.  The norm is Mathlib's scoped `L∞` operator norm on
finite matrices, used only internally to invoke Gelfand's formula. -/
lemma ch7_matrix_pow_opNorm_ge_pow_subeigen {n : ℕ} (hn : 0 < n)
    (M : Fin n → Fin n → ℝ) (lam : ℝ) (x : Fin n → ℝ)
    (hM_nonneg : ∀ i j : Fin n, 0 ≤ M i j)
    (hlam : 0 ≤ lam)
    (hx : ∀ i : Fin n, 0 < x i)
    (hsub : ∀ i : Fin n, lam * x i ≤ matMulVec n M x i) :
    ∀ k : ℕ,
      lam ^ k ≤
        ‖(show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M) ^ k‖ := by
  classical
  intro k
  let A : Matrix (Fin n) (Fin n) ℝ :=
    (Matrix.of M : Matrix (Fin n) (Fin n) ℝ)
  let C : Matrix (Fin n) (Fin n) ℂ :=
    (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)
  let xc : Fin n → ℂ := fun i => (x i : ℂ)
  obtain ⟨i0, hnorm_eq⟩ := ch7_complexVec_norm_eq_coord_of_nonempty hn xc
  have hxnorm_eq : ‖xc‖ = x i0 := by
    rw [hnorm_eq]
    simp [xc, Complex.norm_real, abs_of_nonneg (le_of_lt (hx i0))]
  have hA_nonneg : ∀ i j : Fin n, 0 ≤ A i j := by
    intro i j
    simpa [A, Matrix.of] using hM_nonneg i j
  have hsub_matrix : ∀ i : Fin n, lam * x i ≤ Matrix.mulVec A x i := by
    intro i
    simpa [A, ch7_matrix_mulVec_eq_matMulVec M x] using hsub i
  have hpow_sub :
      lam ^ k * x i0 ≤ Matrix.mulVec (A ^ k) x i0 :=
    ch7_matrix_pow_mulVec_subeigen_le_of_nonneg
      A lam x hA_nonneg hlam hsub_matrix k i0
  have hx_nonneg : ∀ i : Fin n, 0 ≤ x i := fun i => le_of_lt (hx i)
  have hpow_nonneg :
      0 ≤ Matrix.mulVec (A ^ k) x i0 := by
    simpa [A] using
      ch7_matrix_pow_mulVec_nonneg_of_nonneg M x hA_nonneg hx_nonneg k i0
  have hcoord :
      ‖Matrix.mulVec (C ^ k) xc i0‖ = Matrix.mulVec (A ^ k) x i0 := by
    have happ_i := congrFun
      (ch7_realRectToCMatrix_pow_mulVec_complexified M x k) i0
    simpa [C, A, xc, Complex.norm_real, abs_of_nonneg hpow_nonneg]
      using congrArg norm happ_i
  have hcoord_le_vec :
      ‖Matrix.mulVec (C ^ k) xc i0‖ ≤ ‖Matrix.mulVec (C ^ k) xc‖ :=
    norm_le_pi_norm (Matrix.mulVec (C ^ k) xc) i0
  have hop : ‖Matrix.mulVec (C ^ k) xc‖ ≤ ‖C ^ k‖ * ‖xc‖ :=
    Matrix.linfty_opNorm_mulVec (C ^ k) xc
  have hscaled : lam ^ k * x i0 ≤ ‖C ^ k‖ * x i0 := by
    calc
      lam ^ k * x i0 ≤ Matrix.mulVec (A ^ k) x i0 := hpow_sub
      _ = ‖Matrix.mulVec (C ^ k) xc i0‖ := hcoord.symm
      _ ≤ ‖Matrix.mulVec (C ^ k) xc‖ := hcoord_le_vec
      _ ≤ ‖C ^ k‖ * ‖xc‖ := hop
      _ = ‖C ^ k‖ * x i0 := by rw [hxnorm_eq]
  have hxpos := hx i0
  have hnorm_nonneg : 0 ≤ ‖C ^ k‖ := norm_nonneg _
  nlinarith



















































































/-- Collatz/Gelfand lower bound: a positive right subeigenvector of a
nonnegative finite real matrix forces its scalar below the Mathlib
spectral radius of the complexified matrix algebra. -/
theorem ch7_matrix_spectralRadius_ge_of_positive_right_subeigenvector {n : ℕ}
    (hn : 0 < n) (M : Fin n → Fin n → ℝ) (lam : ℝ) (x : Fin n → ℝ)
    (hM_nonneg : ∀ i j : Fin n, 0 ≤ M i j)
    (hlam : 0 ≤ lam)
    (hx : ∀ i : Fin n, 0 < x i)
    (hsub : ∀ i : Fin n, lam * x i ≤ matMulVec n M x i) :
    ENNReal.ofReal lam ≤
      spectralRadius ℂ
        (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M) := by
  classical
  let C : Matrix (Fin n) (Fin n) ℂ :=
    (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)
  by_cases hlam_zero : lam = 0
  · simp [hlam_zero]
  have hpow_bound :
      ∀ k : ℕ, lam ^ k ≤ ‖C ^ k‖ := by
    intro k
    simpa [C] using
      ch7_matrix_pow_opNorm_ge_pow_subeigen
        hn M lam x hM_nonneg hlam hx hsub k
  letI : CompleteSpace (Matrix (Fin n) (Fin n) ℂ) :=
    FiniteDimensional.complete ℂ _
  have hcomplete_check : CompleteSpace (Matrix (Fin n) (Fin n) ℂ) := inferInstance
  have hlim :=
    @spectrum.pow_norm_pow_one_div_tendsto_nhds_spectralRadius
      (Matrix (Fin n) (Fin n) ℂ) inferInstance inferInstance hcomplete_check C
  refine ge_of_tendsto hlim ?_
  refine Filter.eventually_atTop.2 ⟨1, ?_⟩
  intro k hk
  have hk_pos_nat : 0 < k := Nat.pos_of_ne_zero (Nat.ne_zero_of_lt hk)
  have hk_pos : 0 < (k : ℝ) := Nat.cast_pos.mpr hk_pos_nat
  have hpow_nonneg : 0 ≤ lam ^ k := pow_nonneg hlam k
  have hroot :=
    Real.rpow_le_rpow hpow_nonneg (hpow_bound k)
      (le_of_lt (one_div_pos.mpr hk_pos))
  have hroot' :
      (lam ^ k) ^ (1 / (k : ℝ)) ≤
        ‖C ^ k‖ ^ (1 / (k : ℝ)) := by
    simpa [one_div] using hroot
  have hleft : (lam ^ k) ^ (1 / (k : ℝ)) = lam := by
    rw [← Real.rpow_natCast lam k]
    rw [← Real.rpow_mul hlam]
    have hmul : (k : ℝ) * (1 / (k : ℝ)) = 1 := by
      field_simp [ne_of_gt hk_pos]
    rw [hmul, Real.rpow_one]
  have hleft' : (lam ^ k) ^ ((k : ℝ)⁻¹) = lam := by
    simpa [one_div] using hleft
  have hreal : lam ≤ ‖C ^ k‖ ^ (1 / (k : ℝ)) := by
    simpa [one_div, hleft'] using hroot'
  exact ENNReal.ofReal_le_ofReal hreal

























































end Ch7MatrixOperatorNorm

/-- The algebraic spectral radius is unchanged by Mathlib's finite
`Matrix.toLin'` algebra equivalence. -/
theorem ch7_toLin_spectralRadius_eq_matrix_spectralRadius {n : ℕ}
    (C : Matrix (Fin n) (Fin n) ℂ) :
    spectralRadius ℂ (Matrix.toLin' C) = spectralRadius ℂ C := by
  unfold spectralRadius
  rw [Matrix.spectrum_toLin' C]
















































































































/-- `toLin'`-facing form of the Collatz/Gelfand lower bound used to close the
Perron-Frobenius equality gate. -/
theorem ch7_toLin_spectralRadius_ge_of_positive_right_subeigenvector {n : ℕ}
    (hn : 0 < n) (M : Fin n → Fin n → ℝ) (lam : ℝ) (x : Fin n → ℝ)
    (hM_nonneg : ∀ i j : Fin n, 0 ≤ M i j)
    (hlam : 0 ≤ lam)
    (hx : ∀ i : Fin n, 0 < x i)
    (hsub : ∀ i : Fin n, lam * x i ≤ matMulVec n M x i) :
    ENNReal.ofReal lam ≤
      spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)) := by
  rw [ch7_toLin_spectralRadius_eq_matrix_spectralRadius]
  exact ch7_matrix_spectralRadius_ge_of_positive_right_subeigenvector
    hn M lam x hM_nonneg hlam hx hsub










































































































































































/-- A one-step subeigenvector inequality propagates from `x` to every powered
iterate `A^k x` when `A` is nonnegative. -/
lemma ch7_matrix_pow_mulVec_subeigen_step_of_nonneg {n : ℕ}
    (A : Matrix (Fin n) (Fin n) ℝ) (lam : ℝ) (x : Fin n → ℝ)
    (hA_nonneg : ∀ i j : Fin n, 0 ≤ A i j)
    (hsub : ∀ i : Fin n, lam * x i ≤ Matrix.mulVec A x i) :
    ∀ k : ℕ, ∀ i : Fin n,
      lam * Matrix.mulVec (A ^ k) x i ≤
        Matrix.mulVec (A ^ Nat.succ k) x i := by
  intro k
  induction k with
  | zero =>
      intro i
      simpa using hsub i
  | succ k ih =>
      intro i
      have hmono :
          Matrix.mulVec A
              (fun j : Fin n => lam * Matrix.mulVec (A ^ k) x j) i ≤
            Matrix.mulVec A (Matrix.mulVec (A ^ Nat.succ k) x) i :=
        ch7_matrix_mulVec_mono_of_nonneg A hA_nonneg (fun j => ih j) i
      have hleft :
          Matrix.mulVec A
              (fun j : Fin n => lam * Matrix.mulVec (A ^ k) x j) i =
            lam * Matrix.mulVec (A ^ Nat.succ k) x i := by
        have hvec :
            (fun j : Fin n => lam * Matrix.mulVec (A ^ k) x j) =
              lam • Matrix.mulVec (A ^ k) x := by
          ext j
          simp [Pi.smul_apply, smul_eq_mul]
        have hpow_mulVec :
            Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i =
              Matrix.mulVec (A ^ Nat.succ k) x i := by
          calc
            Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i =
                Matrix.mulVec (A * A ^ k) x i := by
                  rw [← Matrix.mulVec_mulVec]
            _ = Matrix.mulVec (A ^ Nat.succ k) x i := by
                  rw [← pow_succ']
        calc
          Matrix.mulVec A
              (fun j : Fin n => lam * Matrix.mulVec (A ^ k) x j) i =
              (lam • Matrix.mulVec A (Matrix.mulVec (A ^ k) x)) i := by
                rw [hvec, Matrix.mulVec_smul]
          _ = lam * Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i := by
                simp [Pi.smul_apply, smul_eq_mul]
          _ = lam * Matrix.mulVec (A ^ Nat.succ k) x i := by
                rw [hpow_mulVec]
      have hright :
          Matrix.mulVec A (Matrix.mulVec (A ^ Nat.succ k) x) i =
            Matrix.mulVec (A ^ Nat.succ (Nat.succ k)) x i := by
        calc
          Matrix.mulVec A (Matrix.mulVec (A ^ Nat.succ k) x) i =
              Matrix.mulVec (A * A ^ Nat.succ k) x i := by
                rw [← Matrix.mulVec_mulVec]
          _ = Matrix.mulVec (A ^ Nat.succ (Nat.succ k)) x i := by
                rw [← pow_succ']
      calc
        lam * Matrix.mulVec (A ^ Nat.succ k) x i =
            Matrix.mulVec A
              (fun j : Fin n => lam * Matrix.mulVec (A ^ k) x j) i :=
              hleft.symm
        _ ≤ Matrix.mulVec A (Matrix.mulVec (A ^ Nat.succ k) x) i := hmono
        _ = Matrix.mulVec (A ^ Nat.succ (Nat.succ k)) x i := hright











































/-- A finite sum of positive powers of an irreducible nonnegative matrix sends
any nonzero nonnegative vector to a strictly positive vector.  This uniformizes
the coordinate-by-coordinate support-propagation lemma and is a local
dependency for the remaining Perron-Frobenius existence route in Theorem 7.8 /
Problem 7.10. -/
lemma ch7_exists_irreducible_pow_sum_mulVec_pos_of_nonzero_nonneg {n : ℕ}
    (M : Fin n → Fin n → ℝ) (x : Fin n → ℝ)
    (hM : Matrix.IsIrreducible (Matrix.of M : Matrix (Fin n) (Fin n) ℝ))
    (hx_nonneg : ∀ i : Fin n, 0 ≤ x i)
    (hx_ne : x ≠ 0) :
    ∃ s : Finset ℕ, (∀ k ∈ s, 0 < k) ∧
      ∀ i : Fin n,
        0 <
          ∑ k ∈ s,
            Matrix.mulVec
              ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x i := by
  classical
  have hcoord :
      ∀ i : Fin n, ∃ k > 0,
        0 <
          Matrix.mulVec
            ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x i :=
    ch7_irreducible_pow_mulVec_pos_of_nonzero_nonneg M x hM hx_nonneg hx_ne
  let kOf : Fin n → ℕ := fun i => Classical.choose (hcoord i)
  have hk_pos : ∀ i : Fin n, 0 < kOf i := by
    intro i
    exact (Classical.choose_spec (hcoord i)).1
  have hk_mulVec_pos : ∀ i : Fin n,
      0 <
        Matrix.mulVec
          ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ kOf i) x i := by
    intro i
    exact (Classical.choose_spec (hcoord i)).2
  refine ⟨Finset.univ.image kOf, ?_, ?_⟩
  · intro k hk
    rcases Finset.mem_image.mp hk with ⟨i, _hi, hik⟩
    simpa [← hik] using hk_pos i
  · intro i
    have hterms_nonneg : ∀ k : ℕ, k ∈ Finset.univ.image kOf →
        0 ≤
          Matrix.mulVec
            ((Matrix.of M : Matrix (Fin n) (Fin n) ℝ) ^ k) x i := by
      intro k _hk
      exact ch7_matrix_pow_mulVec_nonneg_of_nonneg M x hM.1 hx_nonneg k i
    have hk_mem : kOf i ∈ Finset.univ.image kOf :=
      Finset.mem_image.mpr ⟨i, Finset.mem_univ i, rfl⟩
    exact Finset.sum_pos' hterms_nonneg
      ⟨kOf i, hk_mem, hk_mulVec_pos i⟩








































/-- Irreducibility upgrades a nonzero nonnegative subeigenvector into a
strictly positive subeigenvector by summing positive powered iterates.  This is
a local Perron-Frobenius route dependency; it still does not assert existence
of an eigenvector or equality at the spectral radius. -/
lemma ch7_exists_positive_subeigenvector_of_irreducible_nonzero_nonneg_subeigen
    {n : ℕ}
    (M : Fin n → Fin n → ℝ) (lam : ℝ) (x : Fin n → ℝ)
    (hM : Matrix.IsIrreducible (Matrix.of M : Matrix (Fin n) (Fin n) ℝ))
    (hx_nonneg : ∀ i : Fin n, 0 ≤ x i)
    (hx_ne : x ≠ 0)
    (hsub : ∀ i : Fin n, lam * x i ≤ matMulVec n M x i) :
    ∃ y : Fin n → ℝ,
      (∀ i : Fin n, 0 < y i) ∧
        ∀ i : Fin n, lam * y i ≤ matMulVec n M y i := by
  classical
  let A : Matrix (Fin n) (Fin n) ℝ :=
    (Matrix.of M : Matrix (Fin n) (Fin n) ℝ)
  obtain ⟨s, _hs_pos, hs_mulVec_pos⟩ :=
    ch7_exists_irreducible_pow_sum_mulVec_pos_of_nonzero_nonneg
      M x hM hx_nonneg hx_ne
  let y : Fin n → ℝ :=
    fun i => ∑ k ∈ s, Matrix.mulVec (A ^ k) x i
  have hsub_matrix : ∀ i : Fin n, lam * x i ≤ Matrix.mulVec A x i := by
    intro i
    simpa [A, ch7_matrix_mulVec_eq_matMulVec M x] using hsub i
  have hstep : ∀ k : ℕ, ∀ i : Fin n,
      lam * Matrix.mulVec (A ^ k) x i ≤
        Matrix.mulVec (A ^ Nat.succ k) x i :=
    ch7_matrix_pow_mulVec_subeigen_step_of_nonneg A lam x hM.1 hsub_matrix
  refine ⟨y, ?_, ?_⟩
  · intro i
    simpa [y, A] using hs_mulVec_pos i
  · intro i
    have hleft :
        lam * y i =
          ∑ k ∈ s, lam * Matrix.mulVec (A ^ k) x i := by
      simp [y, Finset.mul_sum]
    have hright :
        Matrix.mulVec A y i =
          ∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) x i := by
      have hy_sum : y = ∑ k ∈ s, Matrix.mulVec (A ^ k) x := by
        ext j
        simp [y]
      rw [hy_sum, Matrix.mulVec_sum]
      rw [Finset.sum_apply]
      apply Finset.sum_congr rfl
      intro k _hk
      calc
        Matrix.mulVec A (Matrix.mulVec (A ^ k) x) i =
            Matrix.mulVec (A * A ^ k) x i := by
              rw [← Matrix.mulVec_mulVec]
        _ = Matrix.mulVec (A ^ Nat.succ k) x i := by
              rw [← pow_succ']
    have hmat :
        lam * y i ≤ Matrix.mulVec A y i := by
      calc
        lam * y i =
            ∑ k ∈ s, lam * Matrix.mulVec (A ^ k) x i := hleft
        _ ≤ ∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) x i :=
            Finset.sum_le_sum fun k _hk => hstep k i
        _ = Matrix.mulVec A y i := hright.symm
    simpa [A, ch7_matrix_mulVec_eq_matMulVec M y] using hmat

/-- Strict Collatz step for the remaining Perron-Frobenius closure: if a
strictly positive subeigenvector has a strict defect in at least one
coordinate, irreducibility propagates that defect and yields a strictly larger
positive subeigenvalue.  This is still below the full spectral-radius
contradiction, but removes the local propagation bottleneck. -/
lemma ch7_exists_stronger_positive_subeigenvector_of_strict_subeigen
    {n : ℕ} (hn : 0 < n)
    (M : Fin n → Fin n → ℝ) (lam : ℝ) (y : Fin n → ℝ)
    (hM : Matrix.IsIrreducible (Matrix.of M : Matrix (Fin n) (Fin n) ℝ))
    (hy_pos : ∀ i : Fin n, 0 < y i)
    (hsub : ∀ i : Fin n, lam * y i ≤ matMulVec n M y i)
    (hstrict : ∃ i : Fin n, lam * y i < matMulVec n M y i) :
    ∃ eps : ℝ, ∃ v : Fin n → ℝ,
      0 < eps ∧
      (∀ i : Fin n, 0 < v i) ∧
      ∀ i : Fin n, (lam + eps) * v i ≤ matMulVec n M v i := by
  classical
  let A : Matrix (Fin n) (Fin n) ℝ :=
    (Matrix.of M : Matrix (Fin n) (Fin n) ℝ)
  let w : Fin n → ℝ := fun i => Matrix.mulVec A y i - lam * y i
  have hsub_matrix :
      ∀ i : Fin n, lam * y i ≤ Matrix.mulVec A y i := by
    intro i
    simpa [A, ch7_matrix_mulVec_eq_matMulVec M y] using hsub i
  have hw_nonneg : ∀ i : Fin n, 0 ≤ w i := by
    intro i
    exact sub_nonneg.mpr (hsub_matrix i)
  obtain ⟨i0, hstrict_i0⟩ := hstrict
  have hw_pos_i0 : 0 < w i0 := by
    dsimp [w]
    exact sub_pos.mpr (by
      simpa [A, ch7_matrix_mulVec_eq_matMulVec M y] using hstrict_i0)
  have hw_ne : w ≠ 0 := by
    intro hw_eq
    have : w i0 = 0 := by simp [hw_eq]
    linarith
  obtain ⟨s, _hs_pos, hsum_w_pos⟩ :=
    ch7_exists_irreducible_pow_sum_mulVec_pos_of_nonzero_nonneg
      M w hM hw_nonneg hw_ne
  let ysum : Fin n → ℝ := ∑ k ∈ s, Matrix.mulVec (A ^ k) y
  let v : Fin n → ℝ := y + ysum
  have hy_nonneg : ∀ i : Fin n, 0 ≤ y i := fun i => le_of_lt (hy_pos i)
  have hysum_nonneg : ∀ i : Fin n, 0 ≤ ysum i := by
    intro i
    dsimp [ysum]
    rw [Finset.sum_apply]
    exact Finset.sum_nonneg fun k _hk =>
      ch7_matrix_pow_mulVec_nonneg_of_nonneg M y hM.1 hy_nonneg k i
  have hv_pos : ∀ i : Fin n, 0 < v i := by
    intro i
    dsimp [v]
    nlinarith [hy_pos i, hysum_nonneg i]
  have hpow_defect : ∀ k : ℕ, ∀ i : Fin n,
      Matrix.mulVec (A ^ k) w i =
        Matrix.mulVec (A ^ Nat.succ k) y i -
          lam * Matrix.mulVec (A ^ k) y i := by
    intro k i
    have hw_vec : w = Matrix.mulVec A y - lam • y := by
      ext j
      simp [w, Pi.smul_apply, smul_eq_mul]
    have hpow_mul :
        Matrix.mulVec (A ^ k) (Matrix.mulVec A y) i =
          Matrix.mulVec (A ^ Nat.succ k) y i := by
      calc
        Matrix.mulVec (A ^ k) (Matrix.mulVec A y) i =
            Matrix.mulVec (A ^ k * A) y i := by
              rw [← Matrix.mulVec_mulVec]
        _ = Matrix.mulVec (A ^ Nat.succ k) y i := by
              rw [← pow_succ]
    calc
      Matrix.mulVec (A ^ k) w i =
          Matrix.mulVec (A ^ k) (Matrix.mulVec A y - lam • y) i := by
            rw [hw_vec]
      _ = Matrix.mulVec (A ^ k) (Matrix.mulVec A y) i -
            Matrix.mulVec (A ^ k) (lam • y) i := by
            rw [Matrix.mulVec_sub]
            rfl
      _ = Matrix.mulVec (A ^ Nat.succ k) y i -
            lam * Matrix.mulVec (A ^ k) y i := by
            rw [hpow_mul, Matrix.mulVec_smul]
            simp [Pi.smul_apply, smul_eq_mul]
  have hAv_sum : ∀ i : Fin n,
      Matrix.mulVec A ysum i =
        ∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) y i := by
    intro i
    dsimp [ysum]
    rw [Matrix.mulVec_sum]
    rw [Finset.sum_apply]
    apply Finset.sum_congr rfl
    intro k _hk
    calc
      Matrix.mulVec A (Matrix.mulVec (A ^ k) y) i =
          Matrix.mulVec (A * A ^ k) y i := by
            rw [← Matrix.mulVec_mulVec]
      _ = Matrix.mulVec (A ^ Nat.succ k) y i := by
            rw [← pow_succ']
  have hlam_ysum : ∀ i : Fin n,
      lam * ysum i =
        ∑ k ∈ s, lam * Matrix.mulVec (A ^ k) y i := by
    intro i
    dsimp [ysum]
    rw [Finset.sum_apply]
    rw [Finset.mul_sum]
  have hsum_defect : ∀ i : Fin n,
      (∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) y i) -
          (∑ k ∈ s, lam * Matrix.mulVec (A ^ k) y i) =
        ∑ k ∈ s, Matrix.mulVec (A ^ k) w i := by
    intro i
    rw [← Finset.sum_sub_distrib]
    apply Finset.sum_congr rfl
    intro k _hk
    exact (hpow_defect k i).symm
  have hdefect_eq : ∀ i : Fin n,
      Matrix.mulVec A v i - lam * v i =
        w i + ∑ k ∈ s, Matrix.mulVec (A ^ k) w i := by
    intro i
    have hAv :
        Matrix.mulVec A v i =
          Matrix.mulVec A y i +
            ∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) y i := by
      dsimp [v]
      rw [Matrix.mulVec_add]
      rw [Pi.add_apply]
      rw [hAv_sum i]
    have hlamv :
        lam * v i =
          lam * y i + ∑ k ∈ s, lam * Matrix.mulVec (A ^ k) y i := by
      dsimp [v]
      rw [mul_add]
      rw [hlam_ysum i]
    calc
      Matrix.mulVec A v i - lam * v i =
          (Matrix.mulVec A y i +
              ∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) y i) -
            (lam * y i +
              ∑ k ∈ s, lam * Matrix.mulVec (A ^ k) y i) := by
            rw [hAv, hlamv]
      _ = (Matrix.mulVec A y i - lam * y i) +
            ((∑ k ∈ s, Matrix.mulVec (A ^ Nat.succ k) y i) -
              (∑ k ∈ s, lam * Matrix.mulVec (A ^ k) y i)) := by
            ring
      _ = w i + ∑ k ∈ s, Matrix.mulVec (A ^ k) w i := by
            rw [hsum_defect i]
  have hdefect_pos : ∀ i : Fin n,
      0 < Matrix.mulVec A v i - lam * v i := by
    intro i
    rw [hdefect_eq i]
    exact add_pos_of_nonneg_of_pos (hw_nonneg i) (hsum_w_pos i)
  let ratio : Fin n → ℝ :=
    fun i => (Matrix.mulVec A v i - lam * v i) / v i
  have hratio_pos : ∀ i : Fin n, 0 < ratio i := by
    intro i
    exact div_pos (hdefect_pos i) (hv_pos i)
  obtain ⟨c, hc_pos, hc_le⟩ := ch7_exists_pos_le_all_fin hn ratio hratio_pos
  let eps : ℝ := c / 2
  have heps_pos : 0 < eps := by
    dsimp [eps]
    linarith
  refine ⟨eps, v, heps_pos, hv_pos, ?_⟩
  intro i
  have heps_le_ratio : eps ≤ ratio i := by
    dsimp [eps]
    linarith [hc_pos, hc_le i]
  have heps_mul_le :
      eps * v i ≤ Matrix.mulVec A v i - lam * v i := by
    exact (le_div_iff₀ (hv_pos i)).mp heps_le_ratio
  have hmat :
      (lam + eps) * v i ≤ Matrix.mulVec A v i := by
    calc
      (lam + eps) * v i = lam * v i + eps * v i := by ring
      _ ≤ lam * v i + (Matrix.mulVec A v i - lam * v i) :=
          add_le_add le_rfl heps_mul_le
      _ = Matrix.mulVec A v i := by ring
  simpa [A, ch7_matrix_mulVec_eq_matMulVec M v] using hmat

/-- Dichotomy form of the strict Collatz step: a strictly positive
subeigenvector of an irreducible nonnegative matrix is either already a right
eigenvector, or it can be upgraded to a positive subeigenvector for a strictly
larger scalar. -/
lemma ch7_positive_subeigenvector_eigen_or_exists_stronger
    {n : ℕ} (hn : 0 < n)
    (M : Fin n → Fin n → ℝ) (lam : ℝ) (y : Fin n → ℝ)
    (hM : Matrix.IsIrreducible (Matrix.of M : Matrix (Fin n) (Fin n) ℝ))
    (hy_pos : ∀ i : Fin n, 0 < y i)
    (hsub : ∀ i : Fin n, lam * y i ≤ matMulVec n M y i) :
    (∀ i : Fin n, matMulVec n M y i = lam * y i) ∨
      ∃ eps : ℝ, ∃ v : Fin n → ℝ,
        0 < eps ∧
        (∀ i : Fin n, 0 < v i) ∧
        ∀ i : Fin n, (lam + eps) * v i ≤ matMulVec n M v i := by
  by_cases heig : ∀ i : Fin n, matMulVec n M y i = lam * y i
  · exact Or.inl heig
  · right
    apply ch7_exists_stronger_positive_subeigenvector_of_strict_subeigen
      hn M lam y hM hy_pos hsub
    by_contra hnostrict
    apply heig
    intro i
    have hnot_lt : ¬ lam * y i < matMulVec n M y i := by
      intro hlt
      exact hnostrict ⟨i, hlt⟩
    exact le_antisymm (not_lt.mp hnot_lt) (hsub i)

/-- A nonnegative irreducible matrix has a spectral-radius-attaining complex
eigenpair whose absolute-value subeigenvector can be upgraded to a strictly
positive subeigenvector.  This is the positive subeigenvector stage of the
Bauer/Perron route; it still does not assert the real eigenvector equation. -/
theorem ch7_exists_spectralRadius_attaining_positive_subeigenvector
    {n : ℕ} (hn : 0 < n) (M : Fin n → Fin n → ℝ)
    (hM : Matrix.IsIrreducible
      (Matrix.of M : Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix M) z i = mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n M x i) ∧
      (∀ i : Fin n, ‖mu‖ * y i ≤ matMulVec n M y i) := by
  have hM_nonneg : ∀ i j : Fin n, 0 ≤ M i j := by
    intro i j
    simpa [Matrix.of] using hM.1 i j
  obtain ⟨mu, z, x, hz_ne, hx_ne, hx_nonneg, heig, hrad, hsub⟩ :=
    ch7_exists_spectralRadius_attaining_nonneg_subeigenvector hn M hM_nonneg
  obtain ⟨y, hy_pos, hy_sub⟩ :=
    ch7_exists_positive_subeigenvector_of_irreducible_nonzero_nonneg_subeigen
      M ‖mu‖ x hM hx_nonneg hx_ne hsub
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad, hsub, hy_sub⟩

/-- Nonnegative irreducible matrices have a strictly positive real
right-eigenvector at the complexified Mathlib spectral radius.  The proof
combines the spectral-radius-attaining complex eigenpair, absolute-value
subeigenvector construction, irreducible positivity propagation, the strict
Collatz improvement step, and the Gelfand lower bound above. -/
theorem ch7_exists_spectralRadius_attaining_positive_eigenvector
    {n : ℕ} (hn : 0 < n) (M : Fin n → Fin n → ℝ)
    (hM : Matrix.IsIrreducible
      (Matrix.of M : Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix M) z i = mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n M x i) ∧
      (∀ i : Fin n, matMulVec n M y i = ‖mu‖ * y i) := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby⟩ :=
    ch7_exists_spectralRadius_attaining_positive_subeigenvector hn M hM
  rcases ch7_positive_subeigenvector_eigen_or_exists_stronger
      hn M ‖mu‖ y hM hy_pos hsuby with heig_real | hstrong
  · exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
      hsubx, heig_real⟩
  · rcases hstrong with ⟨eps, v, heps_pos, hv_pos, hsub_strong⟩
    have hM_nonneg : ∀ i j : Fin n, 0 ≤ M i j := by
      intro i j
      simpa [Matrix.of] using hM.1 i j
    have hlam_strong : 0 ≤ ‖mu‖ + eps :=
      add_nonneg (norm_nonneg mu) (le_of_lt heps_pos)
    have hle_radius :
        ENNReal.ofReal (‖mu‖ + eps) ≤
          spectralRadius ℂ
            (Matrix.toLin'
              (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)) :=
      ch7_toLin_spectralRadius_ge_of_positive_right_subeigenvector
        hn M (‖mu‖ + eps) v hM_nonneg hlam_strong hv_pos hsub_strong
    have hle_ofReal :
        ENNReal.ofReal (‖mu‖ + eps) ≤ ENNReal.ofReal ‖mu‖ := by
      calc
        ENNReal.ofReal (‖mu‖ + eps) ≤
            spectralRadius ℂ
              (Matrix.toLin'
                (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix M)) :=
          hle_radius
        _ = (nnnorm mu : ENNReal) := hrad.symm
        _ = ENNReal.ofReal ‖mu‖ := by
          simp [ENNReal.coe_nnreal_eq]
    have hreal_le : ‖mu‖ + eps ≤ ‖mu‖ :=
      (ENNReal.ofReal_le_ofReal_iff (norm_nonneg mu)).mp hle_ofReal
    exfalso
    nlinarith















































































































































































































/-- A strictly positive square matrix is primitive in Mathlib's matrix
connectivity API. -/
lemma ch7_matrix_isPrimitive_of_pos_entries {n : ℕ}
    (M : Fin n → Fin n → ℝ)
    (hM : ∀ i j : Fin n, 0 < M i j) :
    Matrix.IsPrimitive (Matrix.of M : Matrix (Fin n) (Fin n) ℝ) := by
  refine ⟨fun i j => le_of_lt (hM i j), 1, by norm_num, ?_⟩
  intro i j
  simpa using hM i j

/-- Strictly positive entries imply the irreducibility side condition used in
Higham Theorem 7.8 and Problem 7.10(d). -/
lemma ch7_matrix_isIrreducible_of_pos_entries {n : ℕ}
    (M : Fin n → Fin n → ℝ)
    (hM : ∀ i j : Fin n, 0 < M i j) :
    Matrix.IsIrreducible (Matrix.of M : Matrix (Fin n) (Fin n) ℝ) :=
  (ch7_matrix_isPrimitive_of_pos_entries M hM).isIrreducible

























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.10(e): one-norm Bauer transpose reductions
-- ============================================================























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.15: Hadamard product algebra under two-sided scaling
-- ============================================================




























































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Theorem 7.4, relative infinity-norm form of (7.10)
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.2: residual/error sandwich in the infinity norm
-- ============================================================

































































































































































































-- ============================================================
-- Theorem 7.2: arbitrary subordinate-norm form of (7.4)
-- ============================================================






























































































































-- ============================================================
-- Equation (7.5): generic normwise condition number
-- ============================================================

























































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.7: dropping the right-hand-side perturbation budget
-- ============================================================
















































































































































































































































































































































































-- ============================================================
-- Problem 7.8: rectangular Frobenius backward error
-- ============================================================


































































































































































































































































































































































-- ============================================================
-- Lemma 7.9: practical error bounds
-- ============================================================






























































































-- ============================================================
-- Equations (7.30) and (7.31): computed residual bounds
-- ============================================================




































































































-- ============================================================
-- Problem 7.13: sparse computed residual bounds
-- ============================================================





































































































































































































































































































































-- ============================================================
-- Equation (7.32): perturbation theory by calculus
-- ============================================================


















































































































































-- ============================================================
-- Equation (7.33): stochastic matrices
-- ============================================================














/-! ## Theorem 7.8 (Bauer) — source-labeled aliases

Higham 2nd ed., Chapter 7, §7.3, Theorem 7.8 (p. 127, eq. (7.24)): for
nonsingular `A` with `|A||A⁻¹|` and `|A⁻¹||A|` irreducible,
`min_{D₁,D₂ ∈ 𝒟ₙ} κ_∞(D₁AD₂) = ρ(|A||A⁻¹|)`. The book's proof is delegated
to Problem 7.10, so the full formalization lives in the `problem7_10*`
family (with `B = |A|`, `C = |A⁻¹|`); these thin aliases give the primary
source label its own traceable surface. -/




















end NumStability
