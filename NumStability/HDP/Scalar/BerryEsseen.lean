import NumStability.HDP.Scalar.CentralLimit
import NumStability.HDP.Scalar.GaussianTails
import Mathlib.Analysis.Calculus.SmoothSeries
import Mathlib.Analysis.Calculus.Taylor
import Mathlib.Analysis.Fourier.Convolution
import Mathlib.Analysis.Fourier.Inversion
import Mathlib.Analysis.Fourier.RiemannLebesgueLemma
import Mathlib.Analysis.Normed.Group.Tannery
import Mathlib.Analysis.Real.Pi.Bounds
import Mathlib.Analysis.SpecialFunctions.Integrals.Basic
import Mathlib.Analysis.SpecialFunctions.Trigonometric.Cotangent
import Mathlib.Analysis.SpecialFunctions.Trigonometric.Bounds
import Mathlib.Analysis.SpecialFunctions.Trigonometric.Sinc
import Mathlib.MeasureTheory.Function.LocallyIntegrable
import Mathlib.MeasureTheory.Function.LpSeminorm.LpNorm
import Mathlib.MeasureTheory.Integral.Gamma
import Mathlib.MeasureTheory.Measure.IntegralCharFun
import Mathlib.MeasureTheory.Measure.Lebesgue.Integral
import Mathlib.MeasureTheory.Measure.WithDensity
import Mathlib.Probability.CDF

/-!
# Berry--Esseen foundations

Reusable analytic and distribution-function foundations for quantitative
central-limit estimates. This module is kept separate from `CentralLimit` so
incremental Berry--Esseen work does not invalidate the completed Chapter 1
central-limit audits.
-/

noncomputable section

open MeasureTheory ProbabilityTheory Set Filter Function
open scoped Convolution ENNReal FourierTransform MeasureTheory NNReal Topology

namespace NumStability.HDP.Scalar.BerryEsseen

/-- The pointwise gap between the cumulative distribution functions of two
real measures. -/
noncomputable def cdfGap (μ ν : Measure ℝ) (x : ℝ) : ℝ :=
  |cdf μ x - cdf ν x|

/-- Signed upper CDF error.  Unlike `cdfGap`, this keeps cancellations under
averaging and convolution. -/
noncomputable def cdfUpperError (mu nu : Measure ℝ) (x : ℝ) : ℝ :=
  cdf mu x - cdf nu x

/-- Signed lower CDF error. -/
noncomputable def cdfLowerError (mu nu : Measure ℝ) (x : ℝ) : ℝ :=
  cdf nu x - cdf mu x

theorem cdfLowerError_eq_upper_swap (mu nu : Measure ℝ) (x : ℝ) :
    cdfLowerError mu nu x = cdfUpperError nu mu x := rfl

theorem cdfLowerError_eq_neg_upper (mu nu : Measure ℝ) (x : ℝ) :
    cdfLowerError mu nu x = -cdfUpperError mu nu x := by
  unfold cdfLowerError cdfUpperError
  ring

theorem abs_cdfLowerError_eq_abs_cdfUpperError
    (mu nu : Measure ℝ) (x : ℝ) :
    |cdfLowerError mu nu x| = |cdfUpperError mu nu x| := by
  rw [cdfLowerError_eq_neg_upper, abs_neg]

/-- Algebraic telescoping of a signed CDF error through an intermediate
convolution law. -/
theorem cdfUpperError_conv_telescope
    (mu nu kappa lambda : Measure ℝ) (x : ℝ) :
    cdfUpperError (kappa ∗ mu) (lambda ∗ nu) x =
      cdfUpperError (kappa ∗ mu) (kappa ∗ nu) x +
        cdfUpperError (kappa ∗ nu) (lambda ∗ nu) x := by
  unfold cdfUpperError
  ring

/-- Algebraic telescoping of a signed lower CDF error through an intermediate
convolution law. -/
theorem cdfLowerError_conv_telescope
    (mu nu kappa lambda : Measure ℝ) (x : ℝ) :
    cdfLowerError (kappa ∗ mu) (lambda ∗ nu) x =
      cdfLowerError (kappa ∗ mu) (kappa ∗ nu) x +
        cdfLowerError (kappa ∗ nu) (lambda ∗ nu) x := by
  unfold cdfLowerError
  ring

/-- Kolmogorov distance, presented as the supremum of pointwise CDF gaps. -/
noncomputable def kolmogorovDistance (μ ν : Measure ℝ) : ℝ :=
  sSup (Set.range (cdfGap μ ν))

/-- Supremum of the signed upper CDF error.  Keeping this direction separate
prevents the two-endpoint loss caused by taking an absolute value before
averaging. -/
noncomputable def upperCDFDistance (mu nu : Measure ℝ) : ℝ :=
  sSup (Set.range (cdfUpperError mu nu))

/-- Supremum of the signed lower CDF error. -/
noncomputable def lowerCDFDistance (mu nu : Measure ℝ) : ℝ :=
  sSup (Set.range (cdfLowerError mu nu))

/-- A pointwise CDF bound. Keeping this predicate alongside the supremum form
makes later smoothing estimates convenient to apply. -/
def CDFBound (μ ν : Measure ℝ) (ε : ℝ) : Prop :=
  ∀ x, cdfGap μ ν x ≤ ε

theorem cdfGap_nonneg (μ ν : Measure ℝ) (x : ℝ) :
    0 ≤ cdfGap μ ν x :=
  abs_nonneg _

theorem cdfGap_le_one (μ ν : Measure ℝ) (x : ℝ) :
    cdfGap μ ν x ≤ 1 := by
  rw [cdfGap, abs_le]
  constructor
  · linarith [cdf_nonneg μ x, cdf_le_one ν x]
  · linarith [cdf_nonneg ν x, cdf_le_one μ x]

theorem cdfUpperError_le_one (mu nu : Measure ℝ) (x : ℝ) :
    cdfUpperError mu nu x ≤ 1 := by
  unfold cdfUpperError
  linarith [cdf_nonneg nu x, cdf_le_one mu x]

theorem cdfLowerError_le_one (mu nu : Measure ℝ) (x : ℝ) :
    cdfLowerError mu nu x ≤ 1 := by
  unfold cdfLowerError
  linarith [cdf_nonneg mu x, cdf_le_one nu x]

theorem bddAbove_range_cdfGap (μ ν : Measure ℝ) :
    BddAbove (Set.range (cdfGap μ ν)) := by
  exact ⟨1, by rintro _ ⟨x, rfl⟩; exact cdfGap_le_one μ ν x⟩

theorem bddAbove_range_cdfUpperError (mu nu : Measure ℝ) :
    BddAbove (Set.range (cdfUpperError mu nu)) := by
  exact ⟨1, by rintro _ ⟨x, rfl⟩; exact cdfUpperError_le_one mu nu x⟩

theorem bddAbove_range_cdfLowerError (mu nu : Measure ℝ) :
    BddAbove (Set.range (cdfLowerError mu nu)) := by
  exact ⟨1, by rintro _ ⟨x, rfl⟩; exact cdfLowerError_le_one mu nu x⟩

theorem cdfGap_le_kolmogorovDistance (μ ν : Measure ℝ) (x : ℝ) :
    cdfGap μ ν x ≤ kolmogorovDistance μ ν :=
  le_csSup (bddAbove_range_cdfGap μ ν) ⟨x, rfl⟩

theorem cdfUpperError_le_upperCDFDistance
    (mu nu : Measure ℝ) (x : ℝ) :
    cdfUpperError mu nu x ≤ upperCDFDistance mu nu :=
  le_csSup (bddAbove_range_cdfUpperError mu nu) ⟨x, rfl⟩

theorem cdfLowerError_le_lowerCDFDistance
    (mu nu : Measure ℝ) (x : ℝ) :
    cdfLowerError mu nu x ≤ lowerCDFDistance mu nu :=
  le_csSup (bddAbove_range_cdfLowerError mu nu) ⟨x, rfl⟩

theorem upperCDFDistance_le_kolmogorovDistance (mu nu : Measure ℝ) :
    upperCDFDistance mu nu ≤ kolmogorovDistance mu nu := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  calc
    cdfUpperError mu nu x ≤ cdfGap mu nu x := by
      unfold cdfUpperError cdfGap
      exact le_abs_self _
    _ ≤ kolmogorovDistance mu nu := cdfGap_le_kolmogorovDistance mu nu x

theorem lowerCDFDistance_le_kolmogorovDistance (mu nu : Measure ℝ) :
    lowerCDFDistance mu nu ≤ kolmogorovDistance mu nu := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  calc
    cdfLowerError mu nu x ≤ cdfGap mu nu x := by
      unfold cdfLowerError cdfGap
      simpa only [neg_sub] using neg_le_abs (cdf mu x - cdf nu x)
    _ ≤ kolmogorovDistance mu nu := cdfGap_le_kolmogorovDistance mu nu x

theorem cdfGap_eq_max_signed (mu nu : Measure ℝ) (x : ℝ) :
    cdfGap mu nu x =
      max (cdfUpperError mu nu x) (cdfLowerError mu nu x) := by
  unfold cdfGap cdfUpperError cdfLowerError
  rw [abs_eq_max_neg]
  congr 1
  ring

/-- The maximum of two real numbers, expressed through their sum and signed
difference. -/
theorem max_eq_add_add_abs_sub_div_two (a b : ℝ) :
    max a b = (a + b + |a - b|) / 2 := by
  rcases le_total a b with hab | hba
  · rw [max_eq_right hab, abs_of_nonpos (sub_nonpos.mpr hab)]
    ring
  · rw [max_eq_left hba, abs_of_nonneg (sub_nonneg.mpr hba)]
    ring

/-- Kolmogorov distance is exactly the maximum of its two signed one-sided
CDF distances. -/
theorem kolmogorovDistance_eq_max_signed (mu nu : Measure ℝ) :
    kolmogorovDistance mu nu =
      max (upperCDFDistance mu nu) (lowerCDFDistance mu nu) := by
  apply le_antisymm
  · apply csSup_le (Set.range_nonempty _)
    rintro _ ⟨x, rfl⟩
    rw [cdfGap_eq_max_signed]
    exact max_le_max
      (cdfUpperError_le_upperCDFDistance mu nu x)
      (cdfLowerError_le_lowerCDFDistance mu nu x)
  · exact max_le
      (upperCDFDistance_le_kolmogorovDistance mu nu)
      (lowerCDFDistance_le_kolmogorovDistance mu nu)

/-- Probability laws with matching first and second moments integrate every
quadratic polynomial equally. -/
theorem integral_quadratic_eq_of_moments
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu)
    (hmean : (∫ y, y ∂mu) = ∫ y, y ∂nu)
    (hsecond : (∫ y, y ^ 2 ∂mu) = ∫ y, y ^ 2 ∂nu)
    (a0 a1 a2 : ℝ) :
    (∫ y, a0 + a1 * y + a2 * y ^ 2 ∂mu) =
      ∫ y, a0 + a1 * y + a2 * y ^ 2 ∂nu := by
  have hmuLin : Integrable (fun y : ℝ => a0 + a1 * y) mu :=
    (integrable_const a0).add (hmu1.const_mul a1)
  have hnuLin : Integrable (fun y : ℝ => a0 + a1 * y) nu :=
    (integrable_const a0).add (hnu1.const_mul a1)
  have hmuCalc :
      (∫ y, a0 + a1 * y + a2 * y ^ 2 ∂mu) =
        a0 + a1 * (∫ y, y ∂mu) + a2 * (∫ y, y ^ 2 ∂mu) := by
    calc
      (∫ y, a0 + a1 * y + a2 * y ^ 2 ∂mu) =
          (∫ y, a0 + a1 * y ∂mu) + ∫ y, a2 * y ^ 2 ∂mu :=
        integral_add hmuLin (hmu2.const_mul a2)
      _ = ((∫ _y : ℝ, a0 ∂mu) + ∫ y, a1 * y ∂mu) +
          ∫ y, a2 * y ^ 2 ∂mu := by
        rw [integral_add (integrable_const a0) (hmu1.const_mul a1)]
      _ = _ := by rw [integral_const, integral_const_mul, integral_const_mul]; simp
  have hnuCalc :
      (∫ y, a0 + a1 * y + a2 * y ^ 2 ∂nu) =
        a0 + a1 * (∫ y, y ∂nu) + a2 * (∫ y, y ^ 2 ∂nu) := by
    calc
      (∫ y, a0 + a1 * y + a2 * y ^ 2 ∂nu) =
          (∫ y, a0 + a1 * y ∂nu) + ∫ y, a2 * y ^ 2 ∂nu :=
        integral_add hnuLin (hnu2.const_mul a2)
      _ = ((∫ _y : ℝ, a0 ∂nu) + ∫ y, a1 * y ∂nu) +
          ∫ y, a2 * y ^ 2 ∂nu := by
        rw [integral_add (integrable_const a0) (hnu1.const_mul a1)]
      _ = _ := by rw [integral_const, integral_const_mul, integral_const_mul]; simp
  calc
    (∫ y, a0 + a1 * y + a2 * y ^ 2 ∂mu) =
        a0 + a1 * (∫ y, y ∂mu) + a2 * (∫ y, y ^ 2 ∂mu) := hmuCalc
    _ = a0 + a1 * (∫ y, y ∂nu) + a2 * (∫ y, y ^ 2 ∂nu) := by
      rw [hmean, hsecond]
    _ = ∫ y, a0 + a1 * y + a2 * y ^ 2 ∂nu := hnuCalc.symm

/-- If a common integrable approximation has the same integral under two
laws, a cubic pointwise remainder controls the difference of the target
integrals. -/
theorem abs_integral_sub_integral_le_of_common_approximation
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (f p : ℝ → ℝ)
    (hfmu : Integrable f mu) (hfnu : Integrable f nu)
    (hpmu : Integrable p mu) (hpnu : Integrable p nu)
    (hpEq : (∫ y, p y ∂mu) = ∫ y, p y ∂nu)
    {C : ℝ}
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu)
    (hrem : ∀ y, |f y - p y| ≤ C * |y| ^ 3) :
    |(∫ y, f y ∂mu) - ∫ y, f y ∂nu| ≤
      C * ((∫ y, |y| ^ 3 ∂mu) + ∫ y, |y| ^ 3 ∂nu) := by
  have hrmu : Integrable (fun y => f y - p y) mu := hfmu.sub hpmu
  have hrnu : Integrable (fun y => f y - p y) nu := hfnu.sub hpnu
  calc
    |(∫ y, f y ∂mu) - ∫ y, f y ∂nu| =
        |(∫ y, f y - p y ∂mu) - ∫ y, f y - p y ∂nu| := by
      rw [integral_sub hfmu hpmu, integral_sub hfnu hpnu, hpEq]
      ring
    _ ≤ |∫ y, f y - p y ∂mu| + |∫ y, f y - p y ∂nu| :=
      abs_sub _ _
    _ ≤ (∫ y, |f y - p y| ∂mu) + ∫ y, |f y - p y| ∂nu :=
      add_le_add abs_integral_le_integral_abs abs_integral_le_integral_abs
    _ ≤ (∫ y, C * |y| ^ 3 ∂mu) + ∫ y, C * |y| ^ 3 ∂nu := by
      exact add_le_add
        (integral_mono hrmu.abs (hmu3.const_mul C) hrem)
        (integral_mono hrnu.abs (hnu3.const_mul C) hrem)
    _ = C * ((∫ y, |y| ^ 3 ∂mu) + ∫ y, |y| ^ 3 ∂nu) := by
      rw [integral_const_mul, integral_const_mul]
      ring

/-- Equal first and second moments cancel every quadratic Taylor term, so a
third-order remainder is the only contribution to the integral comparison. -/
theorem abs_integral_sub_integral_le_of_quadratic_moment_match
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (f : ℝ → ℝ)
    (hfmu : Integrable f mu) (hfnu : Integrable f nu)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu)
    (hmean : (∫ y, y ∂mu) = ∫ y, y ∂nu)
    (hsecond : (∫ y, y ^ 2 ∂mu) = ∫ y, y ^ 2 ∂nu)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu)
    (a0 a1 a2 M : ℝ)
    (hrem : ∀ y,
      |f y - (a0 + a1 * y + a2 * y ^ 2)| ≤ (M / 6) * |y| ^ 3) :
    |(∫ y, f y ∂mu) - ∫ y, f y ∂nu| ≤
      (M / 6) * ((∫ y, |y| ^ 3 ∂mu) + ∫ y, |y| ^ 3 ∂nu) := by
  let p : ℝ → ℝ := fun y => a0 + a1 * y + a2 * y ^ 2
  have hpmu : Integrable p mu :=
    ((integrable_const a0).add (hmu1.const_mul a1)).add (hmu2.const_mul a2)
  have hpnu : Integrable p nu :=
    ((integrable_const a0).add (hnu1.const_mul a1)).add (hnu2.const_mul a2)
  apply abs_integral_sub_integral_le_of_common_approximation
    mu nu f p hfmu hfnu hpmu hpnu
  · exact integral_quadratic_eq_of_moments
      mu nu hmu1 hnu1 hmu2 hnu2 hmean hsecond a0 a1 a2
  · exact hmu3
  · exact hnu3
  · exact hrem

theorem integrable_standardNormal_id :
    Integrable (fun y : ℝ => y)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  simpa only [id_eq] using
    ((ProbabilityTheory.memLp_id_gaussianReal
      (μ := (0 : ℝ)) (v := (1 : ℝ≥0)) 1).integrable (by norm_num))

theorem integrable_standardNormal_sq :
    Integrable (fun y : ℝ => y ^ 2)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  simpa only [id_eq] using
    (ProbabilityTheory.memLp_id_gaussianReal
      (μ := (0 : ℝ)) (v := (1 : ℝ≥0)) 2).integrable_sq

theorem integrable_standardNormal_abs_cube :
    Integrable (fun y : ℝ => |y| ^ 3)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  simpa only [id_eq, Real.norm_eq_abs] using
    (ProbabilityTheory.memLp_id_gaussianReal
      (μ := (0 : ℝ)) (v := (1 : ℝ≥0)) 3).integrable_norm_pow (by norm_num)

theorem integral_standardNormal_id :
    (∫ y : ℝ, y
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) = 0 := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  exact ProbabilityTheory.integral_id_gaussianReal

theorem integral_standardNormal_sq :
    (∫ y : ℝ, y ^ 2
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) = 1 := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  have hv := @ProbabilityTheory.variance_id_gaussianReal (0 : ℝ) (1 : ℝ≥0)
  change Var[(fun y : ℝ => y); ProbabilityTheory.gaussianReal 0 1] =
    (1 : ℝ≥0) at hv
  rw [variance_of_integral_eq_zero measurable_id'.aemeasurable (by simp)] at hv
  norm_num at hv ⊢
  exact hv

/-- Standard-normal specialization of quadratic moment cancellation. -/
theorem abs_integral_sub_standardNormal_le_of_quadratic_remainder
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (f : ℝ → ℝ)
    (hfmu : Integrable f mu)
    (hfGaussian : Integrable f
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (a0 a1 a2 M : ℝ)
    (hrem : ∀ y,
      |f y - (a0 + a1 * y + a2 * y ^ 2)| ≤ (M / 6) * |y| ^ 3) :
    |(∫ y, f y ∂mu) -
        ∫ y, f y
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw| ≤
      (M / 6) * ((∫ y, |y| ^ 3 ∂mu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  apply abs_integral_sub_integral_le_of_quadratic_moment_match
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw f
    hfmu hfGaussian hmu1 integrable_standardNormal_id hmu2
    integrable_standardNormal_sq
  · rw [hmean, integral_standardNormal_id]
  · rw [hsecond, integral_standardNormal_sq]
  · exact hmu3
  · exact integrable_standardNormal_abs_cube
  · exact hrem

theorem kolmogorovDistance_nonneg (μ ν : Measure ℝ) :
    0 ≤ kolmogorovDistance μ ν :=
  (cdfGap_nonneg μ ν 0).trans (cdfGap_le_kolmogorovDistance μ ν 0)

theorem kolmogorovDistance_le_one (μ ν : Measure ℝ) :
    kolmogorovDistance μ ν ≤ 1 := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  exact cdfGap_le_one μ ν x

theorem kolmogorovDistance_le_iff (μ ν : Measure ℝ) (ε : ℝ) :
    kolmogorovDistance μ ν ≤ ε ↔ CDFBound μ ν ε := by
  constructor
  · intro h x
    exact (cdfGap_le_kolmogorovDistance μ ν x).trans h
  · intro h
    apply csSup_le (Set.range_nonempty _)
    rintro _ ⟨x, rfl⟩
    exact h x

theorem kolmogorovDistance_comm (μ ν : Measure ℝ) :
    kolmogorovDistance μ ν = kolmogorovDistance ν μ := by
  apply le_antisymm
  · apply (kolmogorovDistance_le_iff μ ν _).2
    intro x
    simpa only [cdfGap, abs_sub_comm] using
      cdfGap_le_kolmogorovDistance ν μ x
  · apply (kolmogorovDistance_le_iff ν μ _).2
    intro x
    simpa only [cdfGap, abs_sub_comm] using
      cdfGap_le_kolmogorovDistance μ ν x

theorem kolmogorovDistance_triangle (μ ν ρ : Measure ℝ) :
    kolmogorovDistance μ ρ ≤
      kolmogorovDistance μ ν + kolmogorovDistance ν ρ := by
  apply (kolmogorovDistance_le_iff μ ρ _).2
  intro x
  change |cdf μ x - cdf ρ x| ≤ _
  calc
    |cdf μ x - cdf ρ x| =
        |(cdf μ x - cdf ν x) + (cdf ν x - cdf ρ x)| := by
      congr 1
      ring
    _ ≤ |cdf μ x - cdf ν x| + |cdf ν x - cdf ρ x| :=
      abs_add_le _ _
    _ ≤ kolmogorovDistance μ ν + kolmogorovDistance ν ρ := by
      exact add_le_add
        (cdfGap_le_kolmogorovDistance μ ν x)
        (cdfGap_le_kolmogorovDistance ν ρ x)

@[simp] theorem kolmogorovDistance_self (μ : Measure ℝ) :
    kolmogorovDistance μ μ = 0 := by
  apply le_antisymm
  · apply (kolmogorovDistance_le_iff μ μ 0).2
    intro x
    simp [cdfGap]
  · exact kolmogorovDistance_nonneg μ μ

theorem kolmogorovDistance_eq_zero_iff
    (μ ν : Measure ℝ)
    [IsProbabilityMeasure μ] [IsProbabilityMeasure ν] :
    kolmogorovDistance μ ν = 0 ↔ μ = ν := by
  constructor
  · intro h
    apply MeasureTheory.Measure.eq_of_cdf μ ν
    ext x
    have hx : cdfGap μ ν x ≤ 0 := by
      rw [← h]
      exact cdfGap_le_kolmogorovDistance μ ν x
    have habs : |cdf μ x - cdf ν x| = 0 :=
      le_antisymm hx (abs_nonneg _)
    exact sub_eq_zero.mp (abs_eq_zero.mp habs)
  · rintro rfl
    exact kolmogorovDistance_self μ

theorem cdf_map_eq_real_preimage
    {Ω : Type*} [MeasurableSpace Ω]
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : Measurable X) (x : ℝ) :
    cdf (μ.map X) x = μ.real (X ⁻¹' Iic x) := by
  letI : IsProbabilityMeasure (μ.map X) :=
    Measure.isProbabilityMeasure_map hX.aemeasurable
  rw [cdf_eq_real, map_measureReal_apply hX measurableSet_Iic]

/-- The CDF of a positive scalar pushforward is evaluated at the inversely
scaled argument. -/
theorem cdf_map_mul
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {c : ℝ} (hc : 0 < c) (x : ℝ) :
    cdf (Measure.map (fun y : ℝ => c * y) mu) x = cdf mu (x / c) := by
  letI : IsProbabilityMeasure (Measure.map (fun y : ℝ => c * y) mu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  rw [cdf_eq_real, map_measureReal_apply (by fun_prop) measurableSet_Iic,
    cdf_eq_real]
  congr 1
  ext y
  simp only [Set.mem_preimage, mem_Iic]
  rw [le_div_iff₀ hc]
  simp only [mul_comm]

theorem cdfGap_map_mul
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) (x : ℝ) :
    cdfGap
        (Measure.map (fun y : ℝ => c * y) mu)
        (Measure.map (fun y : ℝ => c * y) nu) x =
      cdfGap mu nu (x / c) := by
  unfold cdfGap
  rw [cdf_map_mul mu hc, cdf_map_mul nu hc]

/-- Kolmogorov distance is invariant under a common positive scalar
pushforward. -/
theorem kolmogorovDistance_map_mul_eq
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) :
    kolmogorovDistance
        (Measure.map (fun y : ℝ => c * y) mu)
        (Measure.map (fun y : ℝ => c * y) nu) =
      kolmogorovDistance mu nu := by
  letI : IsProbabilityMeasure (Measure.map (fun y : ℝ => c * y) mu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure (Measure.map (fun y : ℝ => c * y) nu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  apply le_antisymm
  · apply (kolmogorovDistance_le_iff _ _ _).2
    intro x
    rw [cdfGap_map_mul mu nu hc]
    exact cdfGap_le_kolmogorovDistance mu nu (x / c)
  · apply (kolmogorovDistance_le_iff _ _ _).2
    intro x
    calc
      cdfGap mu nu x = cdfGap
          (Measure.map (fun y : ℝ => c * y) mu)
          (Measure.map (fun y : ℝ => c * y) nu) (c * x) := by
        rw [cdfGap_map_mul mu nu hc]
        field_simp
      _ ≤ kolmogorovDistance
          (Measure.map (fun y : ℝ => c * y) mu)
          (Measure.map (fun y : ℝ => c * y) nu) :=
        cdfGap_le_kolmogorovDistance _ _ (c * x)

/-- Scaling a common-convolution replacement by the same positive factor
does not change its Kolmogorov distance; the conclusion is oriented for the
Gaussian replacement bounds below. -/
theorem kolmogorovDistance_map_mul_conv_eq
    (kappa mu nu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) :
    kolmogorovDistance
        (Measure.map (fun y : ℝ => c * y) (kappa ∗ mu))
        (Measure.map (fun y : ℝ => c * y) (kappa ∗ nu)) =
      kolmogorovDistance (mu ∗ kappa) (nu ∗ kappa) := by
  rw [kolmogorovDistance_map_mul_eq (kappa ∗ mu) (kappa ∗ nu) hc,
    Measure.conv_comm kappa mu, Measure.conv_comm kappa nu]

theorem cdfUpperError_map_mul
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) (x : ℝ) :
    cdfUpperError
        (Measure.map (fun y : ℝ => c * y) mu)
        (Measure.map (fun y : ℝ => c * y) nu) x =
      cdfUpperError mu nu (x / c) := by
  unfold cdfUpperError
  rw [cdf_map_mul mu hc, cdf_map_mul nu hc]

theorem cdfLowerError_map_mul
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) (x : ℝ) :
    cdfLowerError
        (Measure.map (fun y : ℝ => c * y) mu)
        (Measure.map (fun y : ℝ => c * y) nu) x =
      cdfLowerError mu nu (x / c) := by
  simpa only [cdfLowerError_eq_upper_swap] using
    cdfUpperError_map_mul nu mu hc x

/-- The spatial `L¹` norm of the upper signed CDF error scales linearly under
a common positive scalar pushforward. -/
theorem integral_abs_cdfUpperError_map_mul_eq
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) :
    (∫ x, |cdfUpperError
      (Measure.map (fun y : ℝ => c * y) mu)
      (Measure.map (fun y : ℝ => c * y) nu) x|) =
      c * ∫ x, |cdfUpperError mu nu x| := by
  calc
    (∫ x, |cdfUpperError
        (Measure.map (fun y : ℝ => c * y) mu)
        (Measure.map (fun y : ℝ => c * y) nu) x|) =
        ∫ x, |cdfUpperError mu nu (c⁻¹ * x)| := by
      apply integral_congr_ae
      exact ae_of_all _ fun x => by
        change |cdfUpperError
          (Measure.map (fun y : ℝ => c * y) mu)
          (Measure.map (fun y : ℝ => c * y) nu) x| =
            |cdfUpperError mu nu (c⁻¹ * x)|
        rw [cdfUpperError_map_mul mu nu hc]
        congr 2
        field_simp
    _ = |(c⁻¹)⁻¹| * ∫ x, |cdfUpperError mu nu x| := by
      simpa only [smul_eq_mul] using
        (Measure.integral_comp_mul_left
          (fun x => |cdfUpperError mu nu x|) c⁻¹)
    _ = c * ∫ x, |cdfUpperError mu nu x| := by
      rw [inv_inv, abs_of_pos hc]

/-- Lower signed-error companion to
`integral_abs_cdfUpperError_map_mul_eq`. -/
theorem integral_abs_cdfLowerError_map_mul_eq
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) :
    (∫ x, |cdfLowerError
      (Measure.map (fun y : ℝ => c * y) mu)
      (Measure.map (fun y : ℝ => c * y) nu) x|) =
      c * ∫ x, |cdfLowerError mu nu x| := by
  simpa only [cdfLowerError_eq_upper_swap] using
    integral_abs_cdfUpperError_map_mul_eq nu mu hc

/-- The second spatially weighted `L¹` norm of the upper signed CDF error
scales cubically under a common positive scalar pushforward. -/
theorem integral_abs_sq_mul_abs_cdfUpperError_map_mul_eq
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) :
    (∫ x, |x| ^ 2 * |cdfUpperError
      (Measure.map (fun y : ℝ => c * y) mu)
      (Measure.map (fun y : ℝ => c * y) nu) x|) =
      c ^ 3 * ∫ x, |x| ^ 2 * |cdfUpperError mu nu x| := by
  calc
    (∫ x, |x| ^ 2 * |cdfUpperError
        (Measure.map (fun y : ℝ => c * y) mu)
        (Measure.map (fun y : ℝ => c * y) nu) x|) =
        ∫ x, |x| ^ 2 * |cdfUpperError mu nu (c⁻¹ * x)| := by
      apply integral_congr_ae
      exact ae_of_all _ fun x => by
        change |x| ^ 2 * |cdfUpperError
          (Measure.map (fun y : ℝ => c * y) mu)
          (Measure.map (fun y : ℝ => c * y) nu) x| =
            |x| ^ 2 * |cdfUpperError mu nu (c⁻¹ * x)|
        rw [cdfUpperError_map_mul mu nu hc]
        congr 3
        field_simp
    _ = |(c⁻¹)⁻¹| *
        ∫ x, |c * x| ^ 2 * |cdfUpperError mu nu x| := by
      convert (Measure.integral_comp_mul_left
        (fun x => |c * x| ^ 2 * |cdfUpperError mu nu x|) c⁻¹) using 1
      apply integral_congr_ae
      exact ae_of_all _ fun x => by
        congr 3
        field_simp
    _ = c ^ 3 * ∫ x, |x| ^ 2 * |cdfUpperError mu nu x| := by
      rw [inv_inv, abs_of_pos hc]
      simp_rw [abs_mul, abs_of_pos hc, mul_pow]
      rw [show (fun x => c ^ 2 * |x| ^ 2 * |cdfUpperError mu nu x|) =
          fun x => c ^ 2 * (|x| ^ 2 * |cdfUpperError mu nu x|) by
        funext x
        ring]
      rw [integral_const_mul]
      ring

/-- Lower signed-error companion to the cubic weighted scaling identity. -/
theorem integral_abs_sq_mul_abs_cdfLowerError_map_mul_eq
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {c : ℝ} (hc : 0 < c) :
    (∫ x, |x| ^ 2 * |cdfLowerError
      (Measure.map (fun y : ℝ => c * y) mu)
      (Measure.map (fun y : ℝ => c * y) nu) x|) =
      c ^ 3 * ∫ x, |x| ^ 2 * |cdfLowerError mu nu x| := by
  simpa only [cdfLowerError_eq_upper_swap] using
    integral_abs_sq_mul_abs_cdfUpperError_map_mul_eq nu mu hc

/-- Centered Gaussian variances add under convolution, specialized to the
successive natural variances used by the leave-one-out recurrence. -/
theorem gaussianReal_nat_succ_conv_standardNormalLaw (N : ℕ) :
    ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0) ∗
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw =
      ProbabilityTheory.gaussianReal 0 (N + 2 : ℝ≥0) := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  convert (ProbabilityTheory.gaussianReal_conv_gaussianReal
      (m₁ := 0) (m₂ := 0)
      (v₁ := (N + 1 : ℝ≥0)) (v₂ := (1 : ℝ≥0))) using 1 <;>
    norm_num [Nat.cast_add, add_assoc]

/-- Scaling a centered Gaussian of natural variance `N + 1` by
`1 / sqrt(N + 1)` gives the standard normal law. -/
theorem map_gaussianReal_nat_succ_eq_standardNormalLaw (N : ℕ) :
    Measure.map (fun y : ℝ => (Real.sqrt (N + 1 : ℝ))⁻¹ * y)
        (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0)) =
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  rw [ProbabilityTheory.gaussianReal_map_const_mul,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  congr 2
  · simp
  · rw [← NNReal.coe_inj]
    simp only [NNReal.coe_mul, NNReal.coe_mk, NNReal.coe_one]
    have hs : Real.sqrt (N + 1 : ℝ) ≠ 0 := by positivity
    rw [inv_pow, Real.sq_sqrt (by positivity)]
    field_simp
    norm_cast

/-- The measure of the strict lower ray is the left limit of the CDF. -/
theorem measureReal_Iio_eq_leftLim_cdf
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (t : ℝ) :
    mu.real (Iio t) = leftLim (cdf mu) t := by
  calc
    mu.real (Iio t) = (cdf mu).measure.real (Iio t) := by
      rw [measure_cdf mu]
    _ = leftLim (cdf mu) t := by
      rw [Measure.real, (cdf mu).measure_Iio (tendsto_cdf_atBot mu)]
      simp only [sub_zero]
      rw [ENNReal.toReal_ofReal]
      exact (cdf_nonneg mu (t - 1)).trans
        ((monotone_cdf mu).le_leftLim (sub_lt_self t zero_lt_one))

/-- Kolmogorov distance controls strict lower-tail probabilities as well as
the closed lower-tail probabilities used to define the CDF. -/
theorem measureReal_Iio_gap_le_kolmogorovDistance
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (t : ℝ) :
    |mu.real (Iio t) - nu.real (Iio t)| ≤ kolmogorovDistance mu nu := by
  rw [measureReal_Iio_eq_leftLim_cdf mu t,
    measureReal_Iio_eq_leftLim_cdf nu t]
  have hlim : Tendsto (fun x : ℝ => |cdf mu x - cdf nu x|)
      (nhdsWithin t (Iio t))
      (nhds |leftLim (cdf mu) t - leftLim (cdf nu) t|) :=
    ((monotone_cdf mu).tendsto_leftLim t |>.sub
      ((monotone_cdf nu).tendsto_leftLim t)).abs
  exact le_of_tendsto hlim (Eventually.of_forall fun x =>
    cdfGap_le_kolmogorovDistance mu nu x)

/-- The midpoint CDF averages the closed lower-tail probability with its
strict-lower counterpart.  It is the atom-symmetric coordinate naturally
exposed by the off-diagonal Prawitz imbalance. -/
noncomputable def midpointCDF (mu : Measure ℝ) (x : ℝ) : ℝ :=
  (cdf mu x + mu.real (Iio x)) / 2

/-- Signed two-law difference in the midpoint-CDF coordinate. -/
noncomputable def midpointCDFDifference
    (mu nu : Measure ℝ) (x : ℝ) : ℝ :=
  midpointCDF mu x - midpointCDF nu x

theorem midpointCDFDifference_eq
    (mu nu : Measure ℝ) (x : ℝ) :
    midpointCDFDifference mu nu x =
      ((cdf mu x - cdf nu x) +
        (mu.real (Iio x) - nu.real (Iio x))) / 2 := by
  unfold midpointCDFDifference midpointCDF
  ring

/-- Kolmogorov distance controls the atom-symmetric midpoint-CDF coordinate
with coefficient one. -/
theorem abs_midpointCDFDifference_le_kolmogorovDistance
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (x : ℝ) :
    |midpointCDFDifference mu nu x| ≤ kolmogorovDistance mu nu := by
  rw [midpointCDFDifference_eq, abs_div,
    abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
  have hClosed : |cdf mu x - cdf nu x| ≤
      kolmogorovDistance mu nu := by
    simpa only [cdfGap] using cdfGap_le_kolmogorovDistance mu nu x
  have hOpen : |mu.real (Iio x) - nu.real (Iio x)| ≤
      kolmogorovDistance mu nu :=
    measureReal_Iio_gap_le_kolmogorovDistance mu nu x
  have hAdd := abs_add_le
    (cdf mu x - cdf nu x)
    (mu.real (Iio x) - nu.real (Iio x))
  linarith

/-- A singleton mass is the jump between the closed CDF and its strict
lower-tail value. -/
theorem measureReal_singleton_eq_cdf_sub_Iio
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (x : ℝ) :
    mu.real {x} = cdf mu x - mu.real (Iio x) := by
  rw [cdf_eq_real]
  have hsubset : Iio x ⊆ Iic x := Iio_subset_Iic_self
  have hdiff : Iic x \ Iio x = {x} := by
    ext y
    simp only [mem_diff, mem_Iic, mem_Iio, mem_singleton_iff]
    constructor
    · rintro ⟨hyx, hnlt⟩
      exact le_antisymm hyx (not_lt.mp hnlt)
    · rintro rfl
      exact ⟨le_rfl, lt_irrefl _⟩
  rw [← measureReal_diff hsubset measurableSet_Iio, hdiff]

/-- The signed difference of two atom masses separates into the closed-CDF
difference minus the strict-lower-tail difference. -/
theorem measureReal_singleton_sub_eq_cdf_sub_sub_Iio
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (x : ℝ) :
    mu.real {x} - nu.real {x} =
      (cdf mu x - cdf nu x) -
        (mu.real (Iio x) - nu.real (Iio x)) := by
  rw [measureReal_singleton_eq_cdf_sub_Iio mu x,
    measureReal_singleton_eq_cdf_sub_Iio nu x]
  ring

/-- The atom-mass difference is bounded by one closed-CDF gap and one
strict-lower-tail gap. -/
theorem abs_measureReal_singleton_sub_le_cdfGap_add_IioGap
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (x : ℝ) :
    |mu.real {x} - nu.real {x}| ≤
      cdfGap mu nu x +
        |mu.real (Iio x) - nu.real (Iio x)| := by
  rw [measureReal_singleton_sub_eq_cdf_sub_sub_Iio mu nu x, cdfGap]
  exact abs_sub _ _

/-- Consequently Kolmogorov distance controls atom-mass differences with the
sharp coefficient supplied by the two CDF sides. -/
theorem abs_measureReal_singleton_sub_le_two_mul_kolmogorovDistance
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (x : ℝ) :
    |mu.real {x} - nu.real {x}| ≤
      2 * kolmogorovDistance mu nu := by
  have hAtom :=
    abs_measureReal_singleton_sub_le_cdfGap_add_IioGap mu nu x
  have hClosed := cdfGap_le_kolmogorovDistance mu nu x
  have hOpen := measureReal_Iio_gap_le_kolmogorovDistance mu nu x
  linarith

/-- Kolmogorov distance controls the closed upper-tail probabilities printed
in the source, without any atomlessness assumption on the first measure. -/
theorem measureReal_Ici_gap_le_kolmogorovDistance
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (t : ℝ) :
    |mu.real (Ici t) - nu.real (Ici t)| ≤ kolmogorovDistance mu nu := by
  have hmu : mu.real (Ici t) = 1 - mu.real (Iio t) := by
    rw [← compl_Iio, measureReal_compl measurableSet_Iio, probReal_univ]
  have hnu : nu.real (Ici t) = 1 - nu.real (Iio t) := by
    rw [← compl_Iio, measureReal_compl measurableSet_Iio, probReal_univ]
  rw [hmu, hnu]
  simpa only [sub_sub_sub_cancel_left, abs_sub_comm] using
    measureReal_Iio_gap_le_kolmogorovDistance mu nu t

/-- A first absolute moment gives the characteristic function a quantitative
modulus at the origin. This removes the apparent `1 / |t|` singularity in the
truncated integral occurring in Esseen smoothing. -/
theorem norm_charFun_sub_one_le_firstMoment
    {μ : Measure ℝ} [IsProbabilityMeasure μ]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ) (t : ℝ) :
    ‖MeasureTheory.charFun μ t - 1‖ ≤
      |t| * ∫ x, |x| ∂μ := by
  have hExp : Integrable
      (fun x : ℝ => Complex.exp (((t * x : ℝ) : ℂ) * Complex.I)) μ := by
    refine Integrable.of_bound (by fun_prop) 1 ?_
    exact ae_of_all μ fun x => by
      simp only [Complex.norm_exp_ofReal_mul_I]
      norm_num
  have hOne : Integrable (fun _ : ℝ => (1 : ℂ)) μ := integrable_const 1
  have hSub :
      (∫ x : ℝ, Complex.exp (((t * x : ℝ) : ℂ) * Complex.I) - 1 ∂μ) =
        (∫ x : ℝ, Complex.exp (((t * x : ℝ) : ℂ) * Complex.I) ∂μ) - 1 := by
    rw [integral_sub hExp hOne]
    simp [integral_const]
  rw [MeasureTheory.charFun_apply_real]
  simp_rw [← Complex.ofReal_mul]
  rw [← hSub]
  calc
    ‖∫ x : ℝ, Complex.exp (((t * x : ℝ) : ℂ) * Complex.I) - 1 ∂μ‖ ≤
        ∫ x : ℝ, |t| * |x| ∂μ :=
      norm_integral_le_of_norm_le (hμ1.const_mul |t|) <|
        ae_of_all μ fun x => by
          simpa only [mul_comm ((t * x : ℝ) : ℂ), Real.norm_eq_abs,
            abs_mul] using
            (Real.norm_exp_I_mul_ofReal_sub_one_le (x := t * x))
    _ = |t| * ∫ x : ℝ, |x| ∂μ := by rw [integral_const_mul]

/-- The characteristic functions of two laws with finite first moments differ
by `O(|t|)` at the origin. -/
theorem norm_charFun_sub_charFun_le_firstMoments
    {μ ν : Measure ℝ} [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ)
    (hν1 : Integrable (fun x : ℝ => |x|) ν) (t : ℝ) :
    ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ ≤
      |t| * ((∫ x, |x| ∂μ) + ∫ x, |x| ∂ν) := by
  calc
    ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ =
        ‖(MeasureTheory.charFun μ t - 1) +
          (1 - MeasureTheory.charFun ν t)‖ := by
      congr 1
      ring
    _ ≤ ‖MeasureTheory.charFun μ t - 1‖ +
        ‖1 - MeasureTheory.charFun ν t‖ := norm_add_le _ _
    _ = ‖MeasureTheory.charFun μ t - 1‖ +
        ‖MeasureTheory.charFun ν t - 1‖ := by
      rw [← norm_neg (1 - MeasureTheory.charFun ν t)]
      congr 2
      ring
    _ ≤ |t| * (∫ x, |x| ∂μ) + |t| * ∫ x, |x| ∂ν :=
      add_le_add
        (norm_charFun_sub_one_le_firstMoment hμ1 t)
        (norm_charFun_sub_one_le_firstMoment hν1 t)
    _ = |t| * ((∫ x, |x| ∂μ) + ∫ x, |x| ∂ν) := by ring

/-- The discrepancy quotient used in Esseen's truncated Fourier integral,
defined to be zero at the removable singularity. -/
noncomputable def charFunDiscrepancyQuotient
    (μ ν : Measure ℝ) (t : ℝ) : ℝ :=
  if t = 0 then 0
  else ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ / |t|

theorem charFunDiscrepancyQuotient_nonneg (μ ν : Measure ℝ) (t : ℝ) :
    0 ≤ charFunDiscrepancyQuotient μ ν t := by
  rw [charFunDiscrepancyQuotient]
  split_ifs
  · exact le_rfl
  · positivity

theorem norm_charFun_sub_eq_abs_mul_charFunDiscrepancyQuotient
    (mu nu : Measure ℝ) {t : ℝ} (ht : t ≠ 0) :
    ‖MeasureTheory.charFun mu t - MeasureTheory.charFun nu t‖ =
      |t| * charFunDiscrepancyQuotient mu nu t := by
  rw [charFunDiscrepancyQuotient, if_neg ht]
  field_simp [abs_ne_zero.mpr ht]

/-- Universal large-frequency bound obtained only from the unit norm of
probability characteristic functions. -/
theorem charFunDiscrepancyQuotient_le_two_div_abs
    (μ ν : Measure ℝ) [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (t : ℝ) :
    charFunDiscrepancyQuotient μ ν t ≤ 2 / |t| := by
  by_cases ht : t = 0
  · subst t
    simp [charFunDiscrepancyQuotient]
  · rw [charFunDiscrepancyQuotient, if_neg ht]
    apply div_le_div_of_nonneg_right _ (abs_nonneg t)
    calc
      ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ ≤
          ‖MeasureTheory.charFun μ t‖ + ‖MeasureTheory.charFun ν t‖ :=
        norm_sub_le _ _
      _ ≤ 1 + 1 := add_le_add
        (MeasureTheory.norm_charFun_le_one t)
        (MeasureTheory.norm_charFun_le_one t)
      _ = 2 := by norm_num

theorem stronglyMeasurable_charFunDiscrepancyQuotient
    (μ ν : Measure ℝ) [IsFiniteMeasure μ] [IsFiniteMeasure ν] :
    StronglyMeasurable (charFunDiscrepancyQuotient μ ν) := by
  unfold charFunDiscrepancyQuotient
  exact (Measurable.ite (measurableSet_singleton 0) measurable_const
    (by fun_prop)).stronglyMeasurable

theorem charFunDiscrepancyQuotient_le_firstMoments
    {μ ν : Measure ℝ} [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ)
    (hν1 : Integrable (fun x : ℝ => |x|) ν) (t : ℝ) :
    charFunDiscrepancyQuotient μ ν t ≤
      (∫ x, |x| ∂μ) + ∫ x, |x| ∂ν := by
  rw [charFunDiscrepancyQuotient]
  split_ifs with ht
  · exact add_nonneg (integral_nonneg fun _ => abs_nonneg _)
      (integral_nonneg fun _ => abs_nonneg _)
  · rw [div_le_iff₀ (abs_pos.mpr ht)]
    simpa only [mul_comm] using
      norm_charFun_sub_charFun_le_firstMoments hμ1 hν1 t

/-- Under finite first moments the discrepancy quotient is integrable on
every bounded interval, so the truncated Esseen integral is well-defined. -/
theorem intervalIntegrable_charFunDiscrepancyQuotient
    {μ ν : Measure ℝ} [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ)
    (hν1 : Integrable (fun x : ℝ => |x|) ν) (a b : ℝ) :
    IntervalIntegrable (charFunDiscrepancyQuotient μ ν) volume a b := by
  let M : ℝ := (∫ x, |x| ∂μ) + ∫ x, |x| ∂ν
  have hM : IntervalIntegrable (fun _ : ℝ => M) volume a b :=
    intervalIntegrable_const
  refine hM.mono_fun' ?_ ?_
  · exact (stronglyMeasurable_charFunDiscrepancyQuotient μ ν).aestronglyMeasurable
  · exact ae_of_all (volume.restrict (uIoc a b)) fun t => by
      change |charFunDiscrepancyQuotient μ ν t| ≤ M
      rw [abs_of_nonneg (charFunDiscrepancyQuotient_nonneg μ ν t)]
      simpa only [M] using
        charFunDiscrepancyQuotient_le_firstMoments hμ1 hν1 t

/-- Exact standard-normal CDF increment as an interval integral of its
density. -/
theorem standardNormalCDF_sub_eq_integral
    {a b : ℝ} (hab : a ≤ b) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw b -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a =
      ∫ x in a..b, ProbabilityTheory.gaussianPDFReal 0 1 x := by
  open NumStability.HDP.Scalar.LimitTheorems in
  rw [cdf_eq_real, cdf_eq_real]
  have hsubset : Iic a ⊆ Iic b := Iic_subset_Iic.mpr hab
  rw [← measureReal_diff hsubset measurableSet_Iic]
  have hdiff : Iic b \ Iic a = Ioc a b := by
    ext x
    simp only [mem_diff, mem_Iic, mem_Ioc, not_le]
    tauto
  rw [hdiff, standardNormalLaw, Measure.real_def,
    gaussianReal_apply_eq_integral 0 (by norm_num : (1 : NNReal) ≠ 0) (Ioc a b)]
  rw [ENNReal.toReal_ofReal
    (integral_nonneg fun x => gaussianPDFReal_nonneg 0 1 x)]
  exact (intervalIntegral.integral_of_le hab).symm

theorem standardNormalCDF_eq_add_integral (a b : ℝ) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw b =
      cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a +
        ∫ x in a..b, ProbabilityTheory.gaussianPDFReal 0 1 x := by
  rcases le_total a b with hab | hba
  · have h := standardNormalCDF_sub_eq_integral hab
    linarith
  · have h := standardNormalCDF_sub_eq_integral hba
    rw [intervalIntegral.integral_symm]
    linarith

theorem hasDerivAt_standardNormalCDF (x : ℝ) :
    HasDerivAt
      (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (ProbabilityTheory.gaussianPDFReal 0 1 x) x := by
  have hfun :
      cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw =
        fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x +
          ∫ t in x..u, ProbabilityTheory.gaussianPDFReal 0 1 t := by
    funext u
    exact standardNormalCDF_eq_add_integral x u
  rw [hfun]
  convert (hasDerivAt_const x
    (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)).add
      (intervalIntegral.integral_hasDerivAt_right
        (ProbabilityTheory.integrable_gaussianPDFReal 0 1).intervalIntegrable
        (ProbabilityTheory.stronglyMeasurable_gaussianPDFReal 0 1).stronglyMeasurableAtFilter
        (by rw [ProbabilityTheory.gaussianPDFReal_def]; fun_prop)) using 1 <;> simp

/-- The standard-normal density exactly cancels the integrating factor in
the Stein equation. -/
theorem sqrt_two_pi_mul_exp_half_sq_mul_standardNormalPDF (z : ℝ) :
    Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
        ProbabilityTheory.gaussianPDFReal 0 1 z = 1 := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
  have hsqrt : Real.sqrt (2 * Real.pi) ≠ 0 := by positivity
  calc
    Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
        ((Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(z ^ 2) / 2)) =
      (Real.sqrt (2 * Real.pi) * (Real.sqrt (2 * Real.pi))⁻¹) *
        (Real.exp (z ^ 2 / 2) * Real.exp (-(z ^ 2) / 2)) := by ring
    _ = 1 := by
      rw [mul_inv_cancel₀ hsqrt, ← Real.exp_add,
        show z ^ 2 / 2 + -(z ^ 2) / 2 = 0 by ring, Real.exp_zero]
      norm_num

/-- Lower branch of the standard-normal Stein solution for the half-line
test at threshold `x`. -/
noncomputable def standardNormalSteinLower (x z : ℝ) : ℝ :=
  Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z *
    (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)

/-- Upper branch of the standard-normal Stein solution for the half-line
test at threshold `x`. -/
noncomputable def standardNormalSteinUpper (x z : ℝ) : ℝ :=
  Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x *
    (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z)

theorem standardNormalSteinLower_eq_upper (x : ℝ) :
    standardNormalSteinLower x x = standardNormalSteinUpper x x := by
  unfold standardNormalSteinLower standardNormalSteinUpper
  ring

/-- The lower branch solves the half-line Stein equation. -/
theorem hasDerivAt_standardNormalSteinLower (x z : ℝ) :
    HasDerivAt (standardNormalSteinLower x)
      (z * standardNormalSteinLower x z +
        (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)) z := by
  have hinner : HasDerivAt (fun y : ℝ => y ^ 2 / 2) z z := by
    convert ((hasDerivAt_id z).pow 2).div_const 2 using 1 <;> simp <;> ring
  have hexp : HasDerivAt (fun y : ℝ => Real.exp (y ^ 2 / 2))
      (Real.exp (z ^ 2 / 2) * z) z := by
    convert (Real.hasDerivAt_exp (z ^ 2 / 2)).comp z hinner using 1 <;> simp
  have h :=
    (((hexp.const_mul (Real.sqrt (2 * Real.pi))).mul
      (hasDerivAt_standardNormalCDF z)).mul_const
        (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x))
  convert h using 1
  · rw [sqrt_two_pi_mul_exp_half_sq_mul_standardNormalPDF]
    unfold standardNormalSteinLower
    ring

/-- The upper branch solves the complementary half-line Stein equation. -/
theorem hasDerivAt_standardNormalSteinUpper (x z : ℝ) :
    HasDerivAt (standardNormalSteinUpper x)
      (z * standardNormalSteinUpper x z -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) z := by
  have hinner : HasDerivAt (fun y : ℝ => y ^ 2 / 2) z z := by
    convert ((hasDerivAt_id z).pow 2).div_const 2 using 1 <;> simp <;> ring
  have hexp : HasDerivAt (fun y : ℝ => Real.exp (y ^ 2 / 2))
      (Real.exp (z ^ 2 / 2) * z) z := by
    convert (Real.hasDerivAt_exp (z ^ 2 / 2)).comp z hinner using 1 <;> simp
  have honeSub := (hasDerivAt_const z 1).sub (hasDerivAt_standardNormalCDF z)
  have h :=
    (((hexp.const_mul (Real.sqrt (2 * Real.pi))).mul_const
      (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)).mul honeSub)
  convert h using 1
  unfold standardNormalSteinUpper
  have hcancel := sqrt_two_pi_mul_exp_half_sq_mul_standardNormalPDF z
  calc
    z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x *
          (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z)) -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
      z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x *
          (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z)) -
        (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          ProbabilityTheory.gaussianPDFReal 0 1 z) *
          cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
        rw [hcancel]
        ring
    _ = _ := by
      simp only [Pi.sub_apply, Pi.one_apply]
      ring

theorem continuous_standardNormalCDF :
    Continuous
      (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  rw [continuous_iff_continuousAt]
  exact fun z => (hasDerivAt_standardNormalCDF z).continuousAt

/-- The continuous half-line Stein solution, obtained by joining the two
closed-form branches at the threshold. -/
noncomputable def standardNormalSteinSolution (x z : ℝ) : ℝ :=
  if z ≤ x then standardNormalSteinLower x z else standardNormalSteinUpper x z

theorem standardNormalSteinSolution_of_le {x z : ℝ} (hz : z ≤ x) :
    standardNormalSteinSolution x z = standardNormalSteinLower x z := by
  simp only [standardNormalSteinSolution, if_pos hz]

theorem standardNormalSteinSolution_of_lt {x z : ℝ} (hz : x < z) :
    standardNormalSteinSolution x z = standardNormalSteinUpper x z := by
  simp only [standardNormalSteinSolution, if_neg (not_le.mpr hz)]

theorem continuous_standardNormalSteinSolution (x : ℝ) :
    Continuous (standardNormalSteinSolution x) := by
  have hlower : Continuous (standardNormalSteinLower x) := by
    unfold standardNormalSteinLower
    exact (((continuous_const.mul (by fun_prop : Continuous
      (fun z : ℝ => Real.exp (z ^ 2 / 2)))).mul
        continuous_standardNormalCDF).mul continuous_const)
  have hupper : Continuous (standardNormalSteinUpper x) := by
    unfold standardNormalSteinUpper
    exact (((continuous_const.mul (by fun_prop : Continuous
      (fun z : ℝ => Real.exp (z ^ 2 / 2)))).mul continuous_const).mul
        (continuous_const.sub continuous_standardNormalCDF))
  unfold standardNormalSteinSolution
  apply continuous_if_le continuous_id continuous_const
    hlower.continuousOn hupper.continuousOn
  intro z hz
  have hzx : z = x := by simpa using hz
  subst z
  exact standardNormalSteinLower_eq_upper x

/-- Away from the threshold, the joined solution satisfies the lower branch
of the half-line Stein equation. -/
theorem hasDerivAt_standardNormalSteinSolution_of_lt
    {x z : ℝ} (hz : z < x) :
    HasDerivAt (standardNormalSteinSolution x)
      (z * standardNormalSteinSolution x z +
        (1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)) z := by
  have heq : standardNormalSteinSolution x =ᶠ[𝓝 z]
      standardNormalSteinLower x := by
    filter_upwards [eventually_le_nhds hz] with y hy
    exact standardNormalSteinSolution_of_le hy
  have h := (hasDerivAt_standardNormalSteinLower x z).congr_of_eventuallyEq heq
  simpa only [standardNormalSteinSolution_of_le hz.le] using h

/-- Away from the threshold, the joined solution satisfies the upper branch
of the half-line Stein equation. -/
theorem hasDerivAt_standardNormalSteinSolution_of_gt
    {x z : ℝ} (hz : x < z) :
    HasDerivAt (standardNormalSteinSolution x)
      (z * standardNormalSteinSolution x z -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) z := by
  have heq : standardNormalSteinSolution x =ᶠ[𝓝 z]
      standardNormalSteinUpper x := by
    filter_upwards [eventually_gt_nhds hz] with y hy
    exact standardNormalSteinSolution_of_lt hy
  have h := (hasDerivAt_standardNormalSteinUpper x z).congr_of_eventuallyEq heq
  simpa only [standardNormalSteinSolution_of_lt hz] using h

/-- Centered half-line test appearing on the right side of the standard-normal
Stein equation. -/
noncomputable def standardNormalHalfLineSteinTarget (x z : ℝ) : ℝ :=
  if z ≤ x then
    1 - cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x
  else
    -cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x

theorem measurable_standardNormalHalfLineSteinTarget (x : ℝ) :
    Measurable (standardNormalHalfLineSteinTarget x) := by
  unfold standardNormalHalfLineSteinTarget
  exact Measurable.ite measurableSet_Iic measurable_const measurable_const

theorem abs_standardNormalHalfLineSteinTarget_le_one (x z : ℝ) :
    |standardNormalHalfLineSteinTarget x z| ≤ 1 := by
  unfold standardNormalHalfLineSteinTarget
  split_ifs
  · rw [abs_of_nonneg (sub_nonneg.mpr
      (cdf_le_one NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x))]
    linarith [cdf_nonneg
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x]
  · rw [abs_neg, abs_of_nonneg (cdf_nonneg
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)]
    exact cdf_le_one NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x

theorem standardNormalSteinSolution_nonneg (x z : ℝ) :
    0 ≤ standardNormalSteinSolution x z := by
  unfold standardNormalSteinSolution
  split_ifs
  · unfold standardNormalSteinLower
    exact mul_nonneg
      (mul_nonneg
        (mul_nonneg (Real.sqrt_nonneg _) (Real.exp_pos _).le)
        (cdf_nonneg NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z))
      (sub_nonneg.mpr
        (cdf_le_one NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x))
  · unfold standardNormalSteinUpper
    exact mul_nonneg
      (mul_nonneg
        (mul_nonneg (Real.sqrt_nonneg _) (Real.exp_pos _).le)
        (cdf_nonneg NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x))
      (sub_nonneg.mpr
        (cdf_le_one NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z))

/-- Pointwise Stein identity away from the half-line threshold. -/
theorem deriv_standardNormalSteinSolution_sub_mul
    {x z : ℝ} (hz : z ≠ x) :
    deriv (standardNormalSteinSolution x) z -
        z * standardNormalSteinSolution x z =
      standardNormalHalfLineSteinTarget x z := by
  rcases lt_or_gt_of_ne hz with hzx | hxz
  · rw [(hasDerivAt_standardNormalSteinSolution_of_lt hzx).deriv]
    simp only [standardNormalHalfLineSteinTarget, if_pos hzx.le]
    ring
  · rw [(hasDerivAt_standardNormalSteinSolution_of_gt hxz).deriv]
    simp only [standardNormalHalfLineSteinTarget, if_neg (not_le.mpr hxz)]
    ring

/-- Almost-everywhere Stein identity in the Lebesgue form used for integration
and leave-one-out arguments. -/
theorem ae_deriv_standardNormalSteinSolution_sub_mul (x : ℝ) :
    ∀ᵐ z ∂volume,
      deriv (standardNormalSteinSolution x) z -
          z * standardNormalSteinSolution x z =
        standardNormalHalfLineSteinTarget x z := by
  filter_upwards [(volume : Measure ℝ).ae_ne x] with z hz
  exact deriv_standardNormalSteinSolution_sub_mul hz

/-- Total derivative representative for Stein expectations.  Unlike the
analytic derivative, this definition also fixes the value at the threshold,
where an arbitrary comparison law may have an atom. -/
noncomputable def standardNormalSteinDerivative (x z : ℝ) : ℝ :=
  z * standardNormalSteinSolution x z + standardNormalHalfLineSteinTarget x z

theorem standardNormalSteinDerivative_eq_deriv
    {x z : ℝ} (hz : z ≠ x) :
    standardNormalSteinDerivative x z =
      deriv (standardNormalSteinSolution x) z := by
  have h := deriv_standardNormalSteinSolution_sub_mul hz
  unfold standardNormalSteinDerivative
  linarith

theorem standardNormalHalfLineSteinTarget_eq_derivative_sub_mul (x z : ℝ) :
    standardNormalHalfLineSteinTarget x z =
      standardNormalSteinDerivative x z -
        z * standardNormalSteinSolution x z := by
  unfold standardNormalSteinDerivative
  ring

theorem integrable_standardNormalHalfLineSteinTarget
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (x : ℝ) :
    Integrable (standardNormalHalfLineSteinTarget x) nu := by
  apply Integrable.of_bound
    (measurable_standardNormalHalfLineSteinTarget x).aestronglyMeasurable 1
  exact ae_of_all nu fun z => by
    rw [Real.norm_eq_abs]
    exact abs_standardNormalHalfLineSteinTarget_le_one x z

/-- The expectation of the centered half-line Stein target under an arbitrary
probability law is exactly its CDF gap from the standard normal. -/
theorem integral_standardNormalHalfLineSteinTarget_eq_cdf_sub
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (x : ℝ) :
    (∫ z, standardNormalHalfLineSteinTarget x z ∂nu) =
      cdf nu x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
  have hInd : Integrable
      ((Iic x).indicator (fun _ : ℝ => (1 : ℝ))) nu :=
    (integrable_const (1 : ℝ)).indicator measurableSet_Iic
  have hfun : standardNormalHalfLineSteinTarget x = fun z =>
      (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) z -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
    funext z
    unfold standardNormalHalfLineSteinTarget
    simp only [Set.indicator_apply, mem_Iic]
    by_cases hz : z ≤ x <;> simp [hz]
  have hIntInd :
      (∫ z, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) z ∂nu) =
        nu.real (Iic x) := integral_indicator_one measurableSet_Iic
  have hCDFnu : cdf nu x = nu.real (Iic x) := cdf_eq_real nu x
  have hCDFnormal :
      cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw.real (Iic x) :=
    cdf_eq_real NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x
  rw [hfun, integral_sub hInd (integrable_const _),
    hIntInd, integral_const,
    probReal_univ, one_smul, hCDFnu, hCDFnormal]

section SteinBounds

open NumStability.HDP.Scalar.LimitTheorems

/-- Standard-normal reflection identifies its lower CDF with the opposite
closed upper tail. -/
theorem standardNormalCDF_eq_tail_neg (z : ℝ) :
    cdf standardNormalLaw z = standardNormalLaw.real (Ici (-z)) := by
  rw [cdf_eq_real]
  have hmap : standardNormalLaw.map (fun y : ℝ => -y) = standardNormalLaw := by
    simpa [standardNormalLaw] using
      (ProbabilityTheory.gaussianReal_map_neg (μ := (0 : ℝ)) (v := (1 : ℝ≥0)))
  calc
    standardNormalLaw.real (Iic z) =
        (standardNormalLaw.map (fun y : ℝ => -y)).real (Iic z) := by rw [hmap]
    _ = standardNormalLaw.real (Ici (-z)) := by
      simp only [Measure.real_def,
        Measure.map_apply (by fun_prop : Measurable (fun y : ℝ => -y)) measurableSet_Iic,
        neg_preimage, neg_Iic]

/-- The upper Mills-ratio estimate cancels the standard-normal integrating
factor outside the unit interval. -/
theorem sqrt_two_pi_mul_exp_mul_standardNormalTail_le_one
    (t : ℝ) (ht : 1 ≤ t) :
    Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ici t) ≤ 1 := by
  have htail :=
    NumStability.HDP.Scalar.GaussianTails.standardNormalTail_le_density t ht
  have hfactor : 0 ≤ Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) := by
    positivity
  calc
    Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ici t) ≤
      Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        ((Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(t ^ 2) / 2)) :=
      mul_le_mul_of_nonneg_left htail hfactor
    _ = 1 := by
      have hsqrt : Real.sqrt (2 * Real.pi) ≠ 0 := by positivity
      rw [mul_mul_mul_comm, mul_inv_cancel₀ hsqrt, one_mul, ← Real.exp_add]
      have hzero : t ^ 2 / 2 + -(t ^ 2) / 2 = 0 := by ring
      rw [hzero, Real.exp_zero]

/-- CDF-complement version of the upper Mills-ratio cancellation bound. -/
theorem sqrt_two_pi_mul_exp_mul_one_sub_standardNormalCDF_le_one
    (t : ℝ) (ht : 1 ≤ t) :
    Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        (1 - cdf standardNormalLaw t) ≤ 1 := by
  have hopen : 1 - cdf standardNormalLaw t =
      standardNormalLaw.real (Ioi t) := by
    rw [cdf_eq_real, ← probReal_compl_eq_one_sub measurableSet_Iic, compl_Iic]
  rw [hopen]
  calc
    Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ioi t) ≤
      Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ici t) := by
      apply mul_le_mul_of_nonneg_left
      · exact measureReal_mono Ioi_subset_Ici_self
      · positivity
    _ ≤ 1 := sqrt_two_pi_mul_exp_mul_standardNormalTail_le_one t ht

/-- Reflected lower-tail version of the Mills-ratio cancellation bound. -/
theorem sqrt_two_pi_mul_exp_mul_standardNormalCDF_le_one
    (t : ℝ) (ht : t ≤ -1) :
    Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        cdf standardNormalLaw t ≤ 1 := by
  rw [standardNormalCDF_eq_tail_neg]
  simpa only [neg_sq] using
    sqrt_two_pi_mul_exp_mul_standardNormalTail_le_one (-t) (by linarith)

/-- The sharp Mills-ratio cancellation with the extra positive location
factor.  Unlike the coarser tail bound above, this holds all the way down to
zero. -/
theorem mul_sqrt_two_pi_mul_exp_mul_standardNormalTail_le_one
    (t : ℝ) (ht : 0 < t) :
    t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ici t)) ≤ 1 := by
  have htail :=
    (NumStability.HDP.Scalar.GaussianTails.standardNormalTail_bounds t ht).2
  have hsqrt : Real.sqrt (2 * Real.pi) ≠ 0 := by positivity
  have ht0 : t ≠ 0 := ne_of_gt ht
  have hfactor : 0 ≤ t *
      (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2)) := by positivity
  calc
    t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ici t)) =
      (t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2))) *
        standardNormalLaw.real (Ici t) := by ring
    _ ≤ (t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2))) *
        ((Real.sqrt (2 * Real.pi))⁻¹ *
          ((1 / t) * Real.exp (-(t ^ 2) / 2))) :=
      mul_le_mul_of_nonneg_left htail hfactor
    _ = 1 := by
      rw [one_div]
      calc
        (t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2))) *
            ((Real.sqrt (2 * Real.pi))⁻¹ *
              (t⁻¹ * Real.exp (-(t ^ 2) / 2))) =
          (t * t⁻¹) *
            (Real.sqrt (2 * Real.pi) * (Real.sqrt (2 * Real.pi))⁻¹) *
            (Real.exp (t ^ 2 / 2) * Real.exp (-(t ^ 2) / 2)) := by ring
        _ = 1 := by
          rw [mul_inv_cancel₀ ht0, mul_inv_cancel₀ hsqrt, ← Real.exp_add]
          rw [show t ^ 2 / 2 + -(t ^ 2) / 2 = 0 by ring, Real.exp_zero]
          norm_num

/-- CDF-complement form of the sharp positive-location cancellation. -/
theorem mul_sqrt_two_pi_mul_exp_mul_one_sub_standardNormalCDF_le_one
    (t : ℝ) (ht : 0 < t) :
    t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        (1 - cdf standardNormalLaw t)) ≤ 1 := by
  have hopen : 1 - cdf standardNormalLaw t =
      standardNormalLaw.real (Ioi t) := by
    rw [cdf_eq_real, ← probReal_compl_eq_one_sub measurableSet_Iic, compl_Iic]
  rw [hopen]
  calc
    t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ioi t)) ≤
      t * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        standardNormalLaw.real (Ici t)) := by
      apply mul_le_mul_of_nonneg_left
      · apply mul_le_mul_of_nonneg_left
        · exact measureReal_mono Ioi_subset_Ici_self
        · positivity
      · exact ht.le
    _ ≤ 1 := mul_sqrt_two_pi_mul_exp_mul_standardNormalTail_le_one t ht

/-- Reflected sharp Mills-ratio cancellation for a negative location. -/
theorem neg_mul_sqrt_two_pi_mul_exp_mul_standardNormalCDF_le_one
    (t : ℝ) (ht : t < 0) :
    (-t) * (Real.sqrt (2 * Real.pi) * Real.exp (t ^ 2 / 2) *
        cdf standardNormalLaw t) ≤ 1 := by
  rw [standardNormalCDF_eq_tail_neg]
  simpa only [neg_sq] using
    mul_sqrt_two_pi_mul_exp_mul_standardNormalTail_le_one (-t) (by linarith)

/-- At a negative location, the magnitude of the linear part of the Stein
solution is controlled by the upper tail at the test threshold. -/
theorem neg_mul_standardNormalSteinSolution_le_one_sub_cdf
    (x z : ℝ) (hz : z < 0) :
    (-z) * standardNormalSteinSolution x z ≤
      1 - cdf standardNormalLaw x := by
  have hcancel :=
    neg_mul_sqrt_two_pi_mul_exp_mul_standardNormalCDF_le_one z hz
  unfold standardNormalSteinSolution
  split_ifs with hzx
  · unfold standardNormalSteinLower
    calc
      (-z) * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z * (1 - cdf standardNormalLaw x)) =
        ((-z) * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z)) * (1 - cdf standardNormalLaw x) := by ring
      _ ≤ 1 * (1 - cdf standardNormalLaw x) :=
        mul_le_mul_of_nonneg_right hcancel
          (sub_nonneg.mpr (cdf_le_one standardNormalLaw x))
      _ = 1 - cdf standardNormalLaw x := one_mul _
  · have hxz : x < z := lt_of_not_ge hzx
    have hmono : cdf standardNormalLaw x ≤ cdf standardNormalLaw z :=
      monotone_cdf standardNormalLaw hxz.le
    have hfactor : 0 ≤ (-z) *
        (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2)) :=
      mul_nonneg (neg_nonneg.mpr hz.le) (by positivity)
    have hcancel' :
        ((-z) * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2))) *
          cdf standardNormalLaw z ≤ 1 := by
      simpa only [mul_assoc] using hcancel
    have htail : 0 ≤ 1 - cdf standardNormalLaw z :=
      sub_nonneg.mpr (cdf_le_one standardNormalLaw z)
    unfold standardNormalSteinUpper
    calc
      (-z) * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw x * (1 - cdf standardNormalLaw z)) =
        (((-z) * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2))) *
          cdf standardNormalLaw x) * (1 - cdf standardNormalLaw z) := by ring
      _ ≤ (((-z) * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2))) *
          cdf standardNormalLaw z) * (1 - cdf standardNormalLaw z) := by
        exact mul_le_mul_of_nonneg_right
          (mul_le_mul_of_nonneg_left hmono hfactor) htail
      _ ≤ 1 * (1 - cdf standardNormalLaw z) :=
        mul_le_mul_of_nonneg_right hcancel' htail
      _ ≤ 1 * (1 - cdf standardNormalLaw x) := by linarith
      _ = 1 - cdf standardNormalLaw x := one_mul _

/-- At a positive location, the linear part of the Stein solution is
controlled by the lower probability at the test threshold. -/
theorem mul_standardNormalSteinSolution_le_cdf
    (x z : ℝ) (hz : 0 < z) :
    z * standardNormalSteinSolution x z ≤ cdf standardNormalLaw x := by
  have hcancel :=
    mul_sqrt_two_pi_mul_exp_mul_one_sub_standardNormalCDF_le_one z hz
  unfold standardNormalSteinSolution
  split_ifs with hzx
  · have hmono : cdf standardNormalLaw z ≤ cdf standardNormalLaw x :=
      monotone_cdf standardNormalLaw hzx
    have hfactor : 0 ≤ z *
        (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2)) := by positivity
    have hp : 0 ≤ cdf standardNormalLaw z := cdf_nonneg standardNormalLaw z
    have hcancel' :
        (z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2))) *
          (1 - cdf standardNormalLaw z) ≤ 1 := by
      simpa only [mul_assoc] using hcancel
    have htail : 1 - cdf standardNormalLaw x ≤
        1 - cdf standardNormalLaw z := by linarith
    unfold standardNormalSteinLower
    calc
      z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z * (1 - cdf standardNormalLaw x)) =
        cdf standardNormalLaw z *
          ((z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2))) *
            (1 - cdf standardNormalLaw x)) := by ring
      _ ≤ cdf standardNormalLaw z *
          ((z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2))) *
            (1 - cdf standardNormalLaw z)) := by
        exact mul_le_mul_of_nonneg_left
          (mul_le_mul_of_nonneg_left htail hfactor) hp
      _ ≤ cdf standardNormalLaw z * 1 :=
        mul_le_mul_of_nonneg_left hcancel' hp
      _ ≤ cdf standardNormalLaw x := by simpa using hmono
  · have hp : 0 ≤ cdf standardNormalLaw x := cdf_nonneg standardNormalLaw x
    unfold standardNormalSteinUpper
    calc
      z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw x * (1 - cdf standardNormalLaw z)) =
        cdf standardNormalLaw x *
          (z * (Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
            (1 - cdf standardNormalLaw z))) := by ring
      _ ≤ cdf standardNormalLaw x * 1 :=
        mul_le_mul_of_nonneg_left hcancel hp
      _ = cdf standardNormalLaw x := mul_one _

/-- The total half-line Stein derivative has the classical sharp uniform
bound, including at the threshold where its value was fixed explicitly. -/
theorem abs_standardNormalSteinDerivative_le_one (x z : ℝ) :
    |standardNormalSteinDerivative x z| ≤ 1 := by
  unfold standardNormalSteinDerivative standardNormalHalfLineSteinTarget
  by_cases hzx : z ≤ x
  · simp only [if_pos hzx]
    by_cases hz0 : z = 0
    · subst z
      simp only [zero_mul, zero_add]
      rw [abs_of_nonneg (sub_nonneg.mpr (cdf_le_one standardNormalLaw x))]
      linarith [cdf_nonneg standardNormalLaw x]
    · rcases lt_or_gt_of_ne hz0 with hz | hz
      · have hlin := neg_mul_standardNormalSteinSolution_le_one_sub_cdf x z hz
        have hf := standardNormalSteinSolution_nonneg x z
        have hnonneg : 0 ≤ z * standardNormalSteinSolution x z +
            (1 - cdf standardNormalLaw x) := by
          nlinarith
        rw [abs_of_nonneg hnonneg]
        have hmul : z * standardNormalSteinSolution x z ≤ 0 :=
          mul_nonpos_of_nonpos_of_nonneg hz.le hf
        linarith [cdf_nonneg standardNormalLaw x]
      · have hlin := mul_standardNormalSteinSolution_le_cdf x z hz
        have hf := standardNormalSteinSolution_nonneg x z
        have hnonneg : 0 ≤ z * standardNormalSteinSolution x z +
            (1 - cdf standardNormalLaw x) :=
          add_nonneg (mul_nonneg hz.le hf)
            (sub_nonneg.mpr (cdf_le_one standardNormalLaw x))
        rw [abs_of_nonneg hnonneg]
        linarith
  · simp only [if_neg hzx]
    by_cases hz0 : z = 0
    · subst z
      simp only [zero_mul, zero_add, abs_neg]
      rw [abs_of_nonneg (cdf_nonneg standardNormalLaw x)]
      exact cdf_le_one standardNormalLaw x
    · rcases lt_or_gt_of_ne hz0 with hz | hz
      · have hlin := neg_mul_standardNormalSteinSolution_le_one_sub_cdf x z hz
        have hf := standardNormalSteinSolution_nonneg x z
        have hnonpos : z * standardNormalSteinSolution x z +
            -cdf standardNormalLaw x ≤ 0 := by
          have hmul : z * standardNormalSteinSolution x z ≤ 0 :=
            mul_nonpos_of_nonpos_of_nonneg hz.le hf
          linarith [cdf_nonneg standardNormalLaw x]
        rw [abs_of_nonpos hnonpos]
        nlinarith
      · have hlin := mul_standardNormalSteinSolution_le_cdf x z hz
        have hf := standardNormalSteinSolution_nonneg x z
        have hnonpos : z * standardNormalSteinSolution x z +
            -cdf standardNormalLaw x ≤ 0 := by linarith
        rw [abs_of_nonpos hnonpos]
        have hmul : 0 ≤ z * standardNormalSteinSolution x z :=
          mul_nonneg hz.le hf
        linarith [cdf_le_one standardNormalLaw x]

/-- A coarse universal bound for the half-line Stein solution.  Sharpness is
not needed here; this bound is chosen to make first-moment integrability
immediate. -/
theorem standardNormalSteinSolution_le (x z : ℝ) :
    standardNormalSteinSolution x z ≤
      Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) := by
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  have hC : 1 ≤ C := by
    dsimp [C]
    have hsqrt : 1 ≤ Real.sqrt (2 * Real.pi) := by
      rw [Real.one_le_sqrt]
      nlinarith [Real.two_le_pi]
    have hexp : 1 ≤ Real.exp (1 / 2 : ℝ) := Real.one_le_exp (by norm_num)
    nlinarith [mul_le_mul hsqrt hexp (by norm_num : (0 : ℝ) ≤ 1)
      (Real.sqrt_nonneg (2 * Real.pi))]
  have hsolution : standardNormalSteinSolution x z ≤
      Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
        cdf standardNormalLaw z * (1 - cdf standardNormalLaw z) := by
    unfold standardNormalSteinSolution
    split_ifs with hzx
    · unfold standardNormalSteinLower
      have hmono := monotone_cdf standardNormalLaw hzx
      have hpref : 0 ≤ Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z :=
        mul_nonneg (mul_nonneg (Real.sqrt_nonneg _) (Real.exp_pos _).le)
          (cdf_nonneg _ _)
      exact mul_le_mul_of_nonneg_left (by linarith) hpref
    · unfold standardNormalSteinUpper
      have hmono := monotone_cdf standardNormalLaw (le_of_not_ge hzx)
      have hfactor : 0 ≤ Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) := by
        positivity
      have hprefix := mul_le_mul_of_nonneg_left hmono hfactor
      exact mul_le_mul_of_nonneg_right hprefix
        (sub_nonneg.mpr (cdf_le_one standardNormalLaw z))
  apply hsolution.trans
  by_cases hzlo : z ≤ -1
  · calc
      Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z * (1 - cdf standardNormalLaw z) ≤
        Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z * 1 := by
        apply mul_le_mul_of_nonneg_left
        · linarith [cdf_nonneg standardNormalLaw z]
        · exact mul_nonneg
            (mul_nonneg (Real.sqrt_nonneg _) (Real.exp_pos _).le)
            (cdf_nonneg _ _)
      _ = Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z := by ring
      _ ≤ 1 := sqrt_two_pi_mul_exp_mul_standardNormalCDF_le_one z hzlo
      _ ≤ C := hC
  · by_cases hzhi : 1 ≤ z
    · calc
        Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
            cdf standardNormalLaw z * (1 - cdf standardNormalLaw z) ≤
          Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
            1 * (1 - cdf standardNormalLaw z) := by
          apply mul_le_mul_of_nonneg_right
          · apply mul_le_mul_of_nonneg_left
            exact cdf_le_one standardNormalLaw z
            positivity
          · exact sub_nonneg.mpr (cdf_le_one standardNormalLaw z)
        _ = Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
            (1 - cdf standardNormalLaw z) := by ring
        _ ≤ 1 :=
          sqrt_two_pi_mul_exp_mul_one_sub_standardNormalCDF_le_one z hzhi
        _ ≤ C := hC
    · have hzlower : -1 ≤ z := by linarith
      have hzupper : z ≤ 1 := by linarith
      have hzsq : z ^ 2 ≤ 1 := by
        nlinarith [mul_nonneg (by linarith : 0 ≤ z + 1) (by linarith : 0 ≤ 1 - z)]
      have hexp : Real.exp (z ^ 2 / 2) ≤ Real.exp (1 / 2 : ℝ) :=
        Real.exp_le_exp.mpr (by linarith)
      have hA : 0 ≤ Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) := by
        positivity
      have hPnonneg : 0 ≤ cdf standardNormalLaw z := cdf_nonneg _ _
      have hQnonneg : 0 ≤ 1 - cdf standardNormalLaw z :=
        sub_nonneg.mpr (cdf_le_one _ _)
      have hPle : cdf standardNormalLaw z ≤ 1 := cdf_le_one _ _
      have hQle : 1 - cdf standardNormalLaw z ≤ 1 := by
        linarith [cdf_nonneg standardNormalLaw z]
      have hAP : Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
          cdf standardNormalLaw z ≤
          Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) * 1 :=
        mul_le_mul_of_nonneg_left hPle hA
      calc
        Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) *
            cdf standardNormalLaw z * (1 - cdf standardNormalLaw z) ≤
          Real.sqrt (2 * Real.pi) * Real.exp (z ^ 2 / 2) * 1 * 1 :=
            mul_le_mul hAP hQle hQnonneg (mul_nonneg hA zero_le_one)
        _ ≤ Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) * 1 * 1 := by
          gcongr
        _ = C := by simp [C]

theorem integrable_standardNormalSteinSolution
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (x : ℝ) :
    Integrable (standardNormalSteinSolution x) nu := by
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  apply Integrable.of_bound
    (continuous_standardNormalSteinSolution x).aestronglyMeasurable C
  exact ae_of_all nu fun z => by
    rw [Real.norm_eq_abs, abs_of_nonneg (standardNormalSteinSolution_nonneg x z)]
    exact standardNormalSteinSolution_le x z

theorem measurable_standardNormalSteinDerivative (x : ℝ) :
    Measurable (standardNormalSteinDerivative x) := by
  unfold standardNormalSteinDerivative
  exact (measurable_id.mul
    (continuous_standardNormalSteinSolution x).measurable).add
      (measurable_standardNormalHalfLineSteinTarget x)

theorem integrable_mul_standardNormalSteinSolution
    (nu : Measure ℝ) (x : ℝ)
    (hfirst : Integrable (fun z : ℝ => |z|) nu) :
    Integrable (fun z => z * standardNormalSteinSolution x z) nu := by
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  have hmajor : Integrable (fun z : ℝ => C * |z|) nu := hfirst.const_mul C
  apply hmajor.mono'
    (measurable_id.mul
      (continuous_standardNormalSteinSolution x).measurable).aestronglyMeasurable
  exact ae_of_all nu fun z => by
    rw [Real.norm_eq_abs, abs_mul,
      abs_of_nonneg (standardNormalSteinSolution_nonneg x z)]
    dsimp [C]
    nlinarith [mul_le_mul_of_nonneg_left
      (standardNormalSteinSolution_le x z) (abs_nonneg z)]

theorem integrable_standardNormalSteinDerivative
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (x : ℝ)
    (hfirst : Integrable (fun z : ℝ => |z|) nu) :
    Integrable (standardNormalSteinDerivative x) nu := by
  unfold standardNormalSteinDerivative
  exact (integrable_mul_standardNormalSteinSolution nu x hfirst).add
    (integrable_standardNormalHalfLineSteinTarget nu x)

/-- The sharp derivative bound makes every measurable pullback integrable on
a probability space, without a moment hypothesis. -/
theorem integrable_standardNormalSteinDerivative_comp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (Y : Omega → ℝ) (hY : AEMeasurable Y mu) (x : ℝ) :
    Integrable (fun omega => standardNormalSteinDerivative x (Y omega)) mu := by
  apply Integrable.of_bound
    ((measurable_standardNormalSteinDerivative x).comp_aemeasurable hY
      |>.aestronglyMeasurable) 1
  exact ae_of_all mu fun omega => by
    rw [Real.norm_eq_abs]
    exact abs_standardNormalSteinDerivative_le_one x (Y omega)

/-- Integrated Stein identity for an arbitrary probability law with finite
first absolute moment.  The total derivative representative makes the formula
valid even when the law has an atom at the threshold. -/
theorem integral_standardNormalSteinDerivative_sub_mul_eq_cdf_sub
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (x : ℝ)
    (hfirst : Integrable (fun z : ℝ => |z|) nu) :
    (∫ z, standardNormalSteinDerivative x z ∂nu) -
        ∫ z, z * standardNormalSteinSolution x z ∂nu =
      cdf nu x - cdf standardNormalLaw x := by
  have hDeriv := integrable_standardNormalSteinDerivative nu x hfirst
  have hMul := integrable_mul_standardNormalSteinSolution nu x hfirst
  calc
    (∫ z, standardNormalSteinDerivative x z ∂nu) -
        ∫ z, z * standardNormalSteinSolution x z ∂nu =
      ∫ z, standardNormalSteinDerivative x z -
        z * standardNormalSteinSolution x z ∂nu :=
      (integral_sub hDeriv hMul).symm
    _ = ∫ z, standardNormalHalfLineSteinTarget x z ∂nu := by
      apply integral_congr_ae
      exact ae_of_all nu fun z =>
        (standardNormalHalfLineSteinTarget_eq_derivative_sub_mul x z).symm
    _ = cdf nu x - cdf standardNormalLaw x :=
      integral_standardNormalHalfLineSteinTarget_eq_cdf_sub nu x

/-- Pull the integrated Stein identity for the law of a random variable back
to its original probability space. -/
theorem integral_comp_standardNormalSteinDerivative_sub_mul_eq_cdf_sub
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (Y : Omega → ℝ) (hY : AEMeasurable Y mu)
    (hfirst : Integrable (fun omega => |Y omega|) mu) (x : ℝ) :
    (∫ omega, standardNormalSteinDerivative x (Y omega) ∂mu) -
        ∫ omega, Y omega * standardNormalSteinSolution x (Y omega) ∂mu =
      cdf (probabilityLaw Y hY) x - cdf standardNormalLaw x := by
  let law : Measure ℝ := probabilityLaw Y hY
  letI : IsProbabilityMeasure law := inferInstance
  have hLawFirst : Integrable (fun z : ℝ => |z|) law := by
    change Integrable (fun z : ℝ => |z|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY]
    simpa only [Function.comp_apply] using hfirst
  have h := integral_standardNormalSteinDerivative_sub_mul_eq_cdf_sub
    law x hLawFirst
  have hDerivMeas := measurable_standardNormalSteinDerivative x
  have hMulMeas : Measurable
      (fun z : ℝ => z * standardNormalSteinSolution x z) :=
    measurable_id.mul (continuous_standardNormalSteinSolution x).measurable
  change (∫ z, standardNormalSteinDerivative x z ∂Measure.map Y mu) -
      ∫ z, z * standardNormalSteinSolution x z ∂Measure.map Y mu =
    cdf (Measure.map Y mu) x - cdf standardNormalLaw x at h
  change (∫ omega, standardNormalSteinDerivative x (Y omega) ∂mu) -
      ∫ omega, Y omega * standardNormalSteinSolution x (Y omega) ∂mu =
    cdf (Measure.map Y mu) x - cdf standardNormalLaw x
  rw [integral_map hY hDerivMeas.aestronglyMeasurable,
    integral_map hY hMulMeas.aestronglyMeasurable] at h
  simpa only [Function.comp_apply] using h

/-- Exact Stein expectation representation for a normalized centered iid sum.
This is the entry point for the summand-by-summand leave-one-out expansion. -/
theorem integral_normalizedCenteredIidSum_standardNormalStein_eq_cdf_sub
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (N : ℕ) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    (∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        ∫ omega, W omega * standardNormalSteinSolution x (W omega) ∂mu =
      cdf (probabilityLaw W
          (normalizedCenteredIidSum_memLp X hX N).aemeasurable) x -
        cdf standardNormalLaw x := by
  let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hW : MemLp W 2 mu := normalizedCenteredIidSum_memLp X hX N
  have hfirst : Integrable (fun omega => |W omega|) mu := by
    simpa only [Real.norm_eq_abs] using (hW.integrable (by norm_num)).norm
  exact integral_comp_standardNormalSteinDerivative_sub_mul_eq_cdf_sub
    W hW.aemeasurable hfirst x

/-- The normalized iid sum with one indexed summand removed. -/
noncomputable def normalizedCenteredIidLeaveOneOut
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1)) :
    Omega → ℝ :=
  fun omega => (Real.sqrt (N + 1 : ℝ))⁻¹ *
    ∑ j ∈ (Finset.range (N + 1)).erase i.1, X j omega

theorem normalizedCenteredIidSum_eq_leaveOneOut_add
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1))
    (omega : Omega) :
    normalizedCenteredIidSum X N omega =
      normalizedCenteredIidLeaveOneOut X N i omega +
        (Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega := by
  unfold normalizedCenteredIidSum normalizedCenteredIidLeaveOneOut
  have hsum : (∑ j : Fin (N + 1), X j.1 omega) =
      ∑ j ∈ Finset.range (N + 1), X j omega := by
    simpa only [Finset.sum_apply] using
      (Fin.sum_univ_eq_sum_range (fun j => X j omega) (N + 1))
  rw [hsum, ← Finset.sum_erase_add _ _ (Finset.mem_range.2 i.isLt)]
  ring

theorem normalizedCenteredIidLeaveOneOut_memLp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (N : ℕ) (i : Fin (N + 1)) :
    MemLp (normalizedCenteredIidLeaveOneOut X N i) 2 mu := by
  have hs : MemLp
      (fun omega => ∑ j ∈ (Finset.range (N + 1)).erase i.1, X j omega) 2 mu :=
    memLp_finset_sum ((Finset.range (N + 1)).erase i.1) (fun j _ => hX j)
  exact hs.const_mul (Real.sqrt (N + 1 : ℝ))⁻¹

theorem indepFun_normalizedCenteredIidLeaveOneOut
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega}
    (X : ℕ → Omega → ℝ) (hX : ∀ j, AEMeasurable (X j) mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) :
    IndepFun (X i.1) (normalizedCenteredIidLeaveOneOut X N i) mu := by
  have hi : i.1 ∉ (Finset.range (N + 1)).erase i.1 := by simp
  have hind :=
    (hIndep.indepFun_finset_sum_of_notMem₀ hX hi).symm
  have hcomp := hind.comp measurable_id
    (show Measurable (fun y : ℝ => (Real.sqrt (N + 1 : ℝ))⁻¹ * y) by fun_prop)
  change IndepFun (X i.1)
    (fun omega => (Real.sqrt (N + 1 : ℝ))⁻¹ *
      ∑ j ∈ (Finset.range (N + 1)).erase i.1, X j omega) mu
  simpa [Function.comp_def, Finset.sum_apply] using hcomp

/-- On a probability space, a unit second moment bounds the first absolute
moment by one. -/
theorem integral_abs_le_one_of_secondMoment_eq_one
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    {Y : Omega → ℝ} (hY : MemLp Y 2 mu)
    (hSecond : ∫ omega, (Y omega) ^ 2 ∂mu = 1) :
    ∫ omega, |Y omega| ∂mu ≤ 1 := by
  have h := expectedAbs_le_sqrt_secondMoment hY
  rw [hSecond] at h
  simpa using h

/-- Identically distributed real random variables have the same third
absolute moment. -/
theorem integral_abs_cube_eq_of_identDistrib
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} {Y Z : Omega → ℝ}
    (hIdent : IdentDistrib Y Z mu mu) :
    (∫ omega, |Y omega| ^ 3 ∂mu) = ∫ omega, |Z omega| ^ 3 ∂mu := by
  have h := hIdent.comp
    (by fun_prop : Measurable fun y : ℝ => |y| ^ 3)
  simpa only [Function.comp_apply] using h.integral_eq

/-- The exact second moment of a normalized iid sum with one summand
removed.  There are `N` surviving unit-variance terms but the normalization
still uses `sqrt (N + 1)`. -/
theorem integral_sq_normalizedCenteredIidLeaveOneOut_eq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu)
    (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (i : Fin (N + 1)) :
    (∫ omega, (normalizedCenteredIidLeaveOneOut X N i omega) ^ 2 ∂mu) =
      (N : ℝ) / (N + 1 : ℝ) := by
  let s : Finset ℕ := (Finset.range (N + 1)).erase i.1
  let S : Omega → ℝ := fun omega => ∑ j ∈ s, X j omega
  have hS : MemLp S 2 mu := by
    dsimp only [S]
    exact memLp_finset_sum s (fun j _ => hX j)
  have hSMean : ∫ omega, S omega ∂mu = 0 := by
    dsimp only [S]
    rw [integral_finset_sum]
    · simp only [hMean, Finset.sum_const_zero]
    · intro j _
      exact (hX j).integrable (by norm_num)
  have hPair : Set.Pairwise (↑s : Set ℕ)
      (fun j k => IndepFun (X j) (X k) mu) := by
    intro j hj k hk hjk
    exact hIndep.indepFun hjk
  have hVarSum : Var[S; mu] = ∑ j ∈ s, Var[X j; mu] := by
    have h := ProbabilityTheory.IndepFun.variance_sum
      (s := s) (X := X) (fun j _ => hX j) hPair
    have hfun : S = ∑ j ∈ s, X j := by
      funext omega
      simp only [S, Finset.sum_apply]
    rw [hfun]
    exact h
  have hVarEach : ∀ j, Var[X j; mu] = 1 := by
    intro j
    rw [variance_of_integral_eq_zero (hX j).aemeasurable (hMean j)]
    exact hSecond j
  have hCard : s.card = N := by
    dsimp only [s]
    rw [Finset.card_erase_of_mem (Finset.mem_range.2 i.isLt), Finset.card_range]
    omega
  have hVarS : Var[S; mu] = (N : ℝ) := by
    rw [hVarSum]
    simp_rw [hVarEach]
    simp [hCard]
  have hWMean :
      ∫ omega, normalizedCenteredIidLeaveOneOut X N i omega ∂mu = 0 := by
    unfold normalizedCenteredIidLeaveOneOut
    change ∫ omega, (Real.sqrt (N + 1 : ℝ))⁻¹ * S omega ∂mu = 0
    rw [integral_const_mul, hSMean, mul_zero]
  have hVarW : Var[normalizedCenteredIidLeaveOneOut X N i; mu] =
      (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (N : ℝ) := by
    unfold normalizedCenteredIidLeaveOneOut
    rw [variance_const_mul]
    change (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * Var[S; mu] = _
    rw [hVarS]
  calc
    (∫ omega, (normalizedCenteredIidLeaveOneOut X N i omega) ^ 2 ∂mu) =
        Var[normalizedCenteredIidLeaveOneOut X N i; mu] :=
      (variance_of_integral_eq_zero
        (normalizedCenteredIidLeaveOneOut_memLp X hX N i).aemeasurable
        hWMean).symm
    _ = (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (N : ℝ) := hVarW
    _ = (N : ℝ) / (N + 1 : ℝ) := by
      have hsqrt : Real.sqrt (N + 1 : ℝ) ^ 2 = (N + 1 : ℝ) :=
        Real.sq_sqrt (by positivity)
      rw [inv_pow, hsqrt]
      ring

/-- The normalized leave-one-out sum has first absolute moment at most one. -/
theorem integral_abs_normalizedCenteredIidLeaveOneOut_le_one
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu)
    (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (i : Fin (N + 1)) :
    ∫ omega, |normalizedCenteredIidLeaveOneOut X N i omega| ∂mu ≤ 1 := by
  have h := expectedAbs_le_sqrt_secondMoment
    (normalizedCenteredIidLeaveOneOut_memLp X hX N i)
  rw [integral_sq_normalizedCenteredIidLeaveOneOut_eq
    X hX hIndep hMean hSecond N i] at h
  exact h.trans (by
    rw [Real.sqrt_le_one]
    exact (div_le_one (by positivity : (0 : ℝ) < N + 1)).2 (by norm_num))

/-- Adding a deterministic nonnegative Stein-envelope constant preserves the
unit first-moment bound for the leave-one-out sum. -/
theorem integral_abs_normalizedCenteredIidLeaveOneOut_add_const_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu)
    (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (i : Fin (N + 1)) (C : ℝ) :
    (∫ omega,
        |normalizedCenteredIidLeaveOneOut X N i omega| + C ∂mu) ≤ 1 + C := by
  have hWi := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hAbs : Integrable
      (fun omega => |normalizedCenteredIidLeaveOneOut X N i omega|) mu := by
    simpa only [Real.norm_eq_abs] using
      (hWi.integrable (by norm_num)).norm
  rw [integral_add hAbs (integrable_const C)]
  simpa using add_le_add_right
    (integral_abs_normalizedCenteredIidLeaveOneOut_le_one
      X hX hIndep hMean hSecond N i) C

/-- `Fin.succAbove i` enumerates the finite range with `i` erased, so the two
forms of a leave-one-out sum agree exactly. -/
theorem sum_range_erase_eq_sum_succAbove
    {Omega : Type*} (X : ℕ → Omega → ℝ)
    (N : ℕ) (i : Fin (N + 1)) (omega : Omega) :
    (∑ j ∈ (Finset.range (N + 1)).erase i.1, X j omega) =
      ∑ j : Fin N, X (i.succAbove j).1 omega := by
  have hRange : (∑ j : Fin (N + 1), X j.1 omega) =
      ∑ j ∈ Finset.range (N + 1), X j omega := by
    simpa only [Finset.sum_apply] using
      (Fin.sum_univ_eq_sum_range (fun j => X j omega) (N + 1))
  calc
    (∑ j ∈ (Finset.range (N + 1)).erase i.1, X j omega) =
        (∑ j ∈ Finset.range (N + 1), X j omega) - X i.1 omega := by
      rw [← Finset.sum_erase_add _ _ (Finset.mem_range.2 i.isLt)]
      ring
    _ = (∑ j : Fin (N + 1), X j.1 omega) - X i.1 omega := by
      rw [hRange]
    _ = ∑ j : Fin N, X (i.succAbove j).1 omega := by
      rw [Fin.sum_univ_succAbove (fun j : Fin (N + 1) => X j.1 omega) i]
      ring

/-- Any `N`-element subfamily selected from an iid sequence has the same sum
law as the first `N` variables. -/
theorem map_sum_succAbove_eq_map_sum_fin
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, AEMeasurable (X j) mu)
    (hIndep : iIndepFun X mu)
    (hIdent : ∀ j, IdentDistrib (X j) (X 0) mu mu)
    (N : ℕ) (i : Fin (N + 1)) :
    Measure.map (∑ j : Fin N, X (i.succAbove j).1) mu =
      Measure.map (∑ j : Fin N, X j.1) mu := by
  cases N with
  | zero => simp
  | succ N =>
      let j0 : Fin (N + 1) := ⟨0, Nat.succ_pos N⟩
      let Y : Fin (N + 1) → Omega → ℝ :=
        fun j => X (i.succAbove j).1
      let Z : Fin (N + 1) → Omega → ℝ := fun j => X j.1
      have hY : ∀ j, AEMeasurable (Y j) mu := fun j => hX _
      have hZ : ∀ j, AEMeasurable (Z j) mu := fun j => hX _
      have hYIndep : iIndepFun Y mu := by
        exact hIndep.precomp
          (Fin.val_injective.comp i.succAbove_right_injective)
      have hZIndep : iIndepFun Z mu := hIndep.precomp Fin.val_injective
      have hYIdent : ∀ j, IdentDistrib (Y j) (Y j0) mu mu := by
        intro j
        exact (hIdent (i.succAbove j).1).trans
          (hIdent (i.succAbove j0).1).symm
      have hZIdent : ∀ j, IdentDistrib (Z j) (Z j0) mu mu := by
        intro j
        exact (hIdent j.1).trans (hIdent j0.1).symm
      have hYZ : IdentDistrib (Y j0) (Z j0) mu mu :=
        (hIdent (i.succAbove j0).1).trans (hIdent j0.1).symm
      apply Measure.ext_of_charFun
      funext t
      calc
        MeasureTheory.charFun (Measure.map (∑ j, Y j) mu) t =
            MeasureTheory.charFun (Measure.map (Y j0) mu) t ^ (N + 1) :=
          by simpa using charFun_map_iid_sum_eq_pow Y j0 hY hYIndep hYIdent t
        _ = MeasureTheory.charFun (Measure.map (Z j0) mu) t ^ (N + 1) := by
          rw [hYZ.map_eq]
        _ = MeasureTheory.charFun (Measure.map (∑ j, Z j) mu) t :=
          by simpa using
            (charFun_map_iid_sum_eq_pow Z j0 hZ hZIndep hZIdent t).symm

/-- For `N > 0`, every leave-one-out law is the common `N`-summand
standardized iid law, contracted by `sqrt N / sqrt (N + 1)`. -/
theorem map_normalizedCenteredIidLeaveOneOut_eq_map_mul_normalizedPrefix
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, AEMeasurable (X j) mu)
    (hIndep : iIndepFun X mu)
    (hIdent : ∀ j, IdentDistrib (X j) (X 0) mu mu)
    {N : ℕ} (hN : 0 < N) (i : Fin (N + 1)) :
    Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu =
      Measure.map
        (fun y : ℝ => Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N - 1)) mu) := by
  let U : Omega → ℝ := fun omega =>
    ∑ j : Fin N, X (i.succAbove j).1 omega
  let V : Omega → ℝ := fun omega => ∑ j : Fin N, X j.1 omega
  let P := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
    X (N - 1)
  let a : ℝ := (Real.sqrt (N + 1 : ℝ))⁻¹
  let c : ℝ := Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ)
  have hU : AEMeasurable U mu := by
    dsimp only [U]
    fun_prop
  have hV : AEMeasurable V mu := by
    dsimp only [V]
    fun_prop
  have hP : AEMeasurable P mu := by
    dsimp only [P]
    unfold NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
    fun_prop
  have hMapUV : Measure.map U mu = Measure.map V mu := by
    have hUFun : U = ∑ j : Fin N, X (i.succAbove j).1 := by
      funext omega
      simp only [U, Finset.sum_apply]
    have hVFun : V = ∑ j : Fin N, X j.1 := by
      funext omega
      simp only [V, Finset.sum_apply]
    rw [hUFun, hVFun]
    exact map_sum_succAbove_eq_map_sum_fin X hX hIndep hIdent N i
  have haMeas : AEMeasurable (fun y : ℝ => a * y) (Measure.map U mu) := by
    fun_prop
  have haMeasV : AEMeasurable (fun y : ℝ => a * y) (Measure.map V mu) := by
    fun_prop
  have hcMeas : AEMeasurable (fun y : ℝ => c * y) (Measure.map P mu) := by
    fun_prop
  have hCoeff : c * (Real.sqrt (N : ℝ))⁻¹ = a := by
    dsimp only [c, a]
    field_simp [show Real.sqrt (N : ℝ) ≠ 0 by positivity,
      show Real.sqrt (N + 1 : ℝ) ≠ 0 by positivity]
  have hWiFun : normalizedCenteredIidLeaveOneOut X N i =
      fun omega => a * U omega := by
    funext omega
    unfold normalizedCenteredIidLeaveOneOut
    dsimp only [a, U]
    rw [sum_range_erase_eq_sum_succAbove X N i omega]
  have hPPoint : ∀ omega, P omega =
      (Real.sqrt (N : ℝ))⁻¹ * V omega := by
    intro omega
    dsimp only [P, V]
    unfold NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
    rw [show N - 1 + 1 = N by omega]
    rw [show ((N - 1 : ℕ) : ℝ) + 1 = (N : ℝ) by
      norm_cast
      omega]
  have hPFun : (fun omega => a * V omega) = fun omega => c * P omega := by
    funext omega
    rw [hPPoint]
    rw [← hCoeff]
    ring
  calc
    Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu =
        Measure.map (fun omega => a * U omega) mu := by rw [hWiFun]
    _ = Measure.map (fun y : ℝ => a * y) (Measure.map U mu) := by
      simpa only [Function.comp_apply] using
        (haMeas.map_map_of_aemeasurable hU).symm
    _ = Measure.map (fun y : ℝ => a * y) (Measure.map V mu) := by
      rw [hMapUV]
    _ = Measure.map (fun omega => a * V omega) mu := by
      simpa only [Function.comp_apply] using
        haMeasV.map_map_of_aemeasurable hV
    _ = Measure.map (fun omega => c * P omega) mu := by rw [hPFun]
    _ = Measure.map (fun y : ℝ => c * y) (Measure.map P mu) := by
      simpa only [Function.comp_apply] using
        (hcMeas.map_map_of_aemeasurable hP).symm

/-- The leave-one-out Kolmogorov error is bounded by the preceding normalized
iid error plus the standard-normal defect caused by the exact contraction. -/
theorem kolmogorovDistance_normalizedCenteredIidLeaveOneOut_le_prefix_add_scalingDefect
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, AEMeasurable (X j) mu)
    (hIndep : iIndepFun X mu)
    (hIdent : ∀ j, IdentDistrib (X j) (X 0) mu mu)
    {N : ℕ} (hN : 0 < N) (i : Fin (N + 1)) :
    kolmogorovDistance
        (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
              X (N - 1)) mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        kolmogorovDistance
          (Measure.map
            (fun y : ℝ =>
              Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let P := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
    X (N - 1)
  let c : ℝ := Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ)
  let nu : Measure ℝ := Measure.map P mu
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  have hP : AEMeasurable P mu := by
    dsimp only [P]
    unfold NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
    fun_prop
  letI : IsProbabilityMeasure nu := Measure.isProbabilityMeasure_map hP
  letI : IsProbabilityMeasure gamma := inferInstance
  have hc : 0 < c := by
    dsimp only [c]
    positivity
  have hLaw :=
    map_normalizedCenteredIidLeaveOneOut_eq_map_mul_normalizedPrefix
      X hX hIndep hIdent hN i
  change kolmogorovDistance
      (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu) gamma ≤
    kolmogorovDistance nu gamma +
      kolmogorovDistance (Measure.map (fun y : ℝ => c * y) gamma) gamma
  rw [hLaw]
  calc
    kolmogorovDistance (Measure.map (fun y : ℝ => c * y) nu) gamma ≤
        kolmogorovDistance
            (Measure.map (fun y : ℝ => c * y) nu)
            (Measure.map (fun y : ℝ => c * y) gamma) +
          kolmogorovDistance (Measure.map (fun y : ℝ => c * y) gamma) gamma :=
      kolmogorovDistance_triangle _ _ _
    _ = kolmogorovDistance nu gamma +
          kolmogorovDistance (Measure.map (fun y : ℝ => c * y) gamma) gamma := by
      rw [kolmogorovDistance_map_mul_eq nu gamma hc]

/-- Fubini formula for an arbitrary integrable real function of two
independent random variables.  This complements the product-only integration
lemmas in Mathlib and is the interface needed for random interval endpoints. -/
theorem integral_comp_pair_eq_integral_integral_of_indepFun
    {Omega A B : Type*} [MeasurableSpace Omega]
    [MeasurableSpace A] [MeasurableSpace B]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (Y : Omega → A) (X : Omega → B)
    (hY : AEMeasurable Y mu) (hX : AEMeasurable X mu)
    (hIndep : IndepFun Y X mu) (F : A × B → ℝ)
    (hF : Integrable F ((Measure.map Y mu).prod (Measure.map X mu))) :
    (∫ omega, F (Y omega, X omega) ∂mu) =
      ∫ y, ∫ x, F (y, x) ∂Measure.map X mu ∂Measure.map Y mu := by
  have hmap : Measure.map (fun omega => (Y omega, X omega)) mu =
      (Measure.map Y mu).prod (Measure.map X mu) :=
    (indepFun_iff_map_prod_eq_prod_map_map hY hX).mp hIndep
  have hFmap : AEStronglyMeasurable F
      (Measure.map (fun omega => (Y omega, X omega)) mu) := by
    rw [hmap]
    exact hF.aestronglyMeasurable
  calc
    (∫ omega, F (Y omega, X omega) ∂mu) =
        ∫ p, F p ∂Measure.map (fun omega => (Y omega, X omega)) mu :=
      (integral_map (hY.prodMk hX) hFmap).symm
    _ = ∫ p, F p ∂(Measure.map Y mu).prod (Measure.map X mu) := by rw [hmap]
    _ = ∫ y, ∫ x, F (y, x) ∂Measure.map X mu ∂Measure.map Y mu :=
      integral_prod F hF

/-- Independence and centering cancel the contribution in which the removed
summand is multiplied by the Stein solution evaluated at the leave-one-out
sum. -/
theorem integral_mul_standardNormalSteinSolution_leaveOneOut_eq_zero
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ)
    (hMean : ∫ omega, X i.1 omega ∂mu = 0) :
    (∫ omega, X i.1 omega *
        standardNormalSteinSolution x
          (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) = 0 := by
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hfact := hind.integral_comp_mul_comp
    (hX i.1).aemeasurable hLeave.aemeasurable
    aestronglyMeasurable_id
    (continuous_standardNormalSteinSolution x).aestronglyMeasurable
  simpa [Function.comp_def, hMean] using hfact

theorem integrable_mul_standardNormalSteinSolution_comp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega}
    (Y Z : Omega → ℝ) (hY : Integrable Y mu) (hZ : AEMeasurable Z mu)
    (x : ℝ) :
    Integrable (fun omega =>
      Y omega * standardNormalSteinSolution x (Z omega)) mu := by
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  have hmajor : Integrable (fun omega => C * |Y omega|) mu := by
    simpa only [Real.norm_eq_abs] using hY.norm.const_mul C
  apply hmajor.mono'
    (hY.aestronglyMeasurable.mul
      ((continuous_standardNormalSteinSolution x).measurable.comp_aemeasurable hZ
        |>.aestronglyMeasurable))
  exact ae_of_all mu fun omega => by
    change |Y omega * standardNormalSteinSolution x (Z omega)| ≤ C * |Y omega|
    rw [abs_mul,
      abs_of_nonneg (standardNormalSteinSolution_nonneg x (Z omega))]
    dsimp [C]
    nlinarith [mul_le_mul_of_nonneg_left
      (standardNormalSteinSolution_le x (Z omega)) (abs_nonneg (Y omega))]

/-- Exact leave-one-out expansion of the drift term in the normalized-sum
Stein identity. -/
theorem integral_normalizedCenteredIidSum_mul_steinSolution_eq_leaveOneOut
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (N : ℕ) (x : ℝ) :
    let W := normalizedCenteredIidSum X N
    (∫ omega, W omega * standardNormalSteinSolution x (W omega) ∂mu) =
      (Real.sqrt (N + 1 : ℝ))⁻¹ *
        ∑ i : Fin (N + 1), ∫ omega,
          X i.1 omega *
            (standardNormalSteinSolution x (W omega) -
              standardNormalSteinSolution x
                (normalizedCenteredIidLeaveOneOut X N i omega)) ∂mu := by
  let W := normalizedCenteredIidSum X N
  change (∫ omega, W omega * standardNormalSteinSolution x (W omega) ∂mu) =
    (Real.sqrt (N + 1 : ℝ))⁻¹ *
      ∑ i : Fin (N + 1), ∫ omega,
        X i.1 omega *
          (standardNormalSteinSolution x (W omega) -
            standardNormalSteinSolution x
              (normalizedCenteredIidLeaveOneOut X N i omega)) ∂mu
  have hW : MemLp W 2 mu := normalizedCenteredIidSum_memLp X hX N
  have hXiW : ∀ i : Fin (N + 1), Integrable (fun omega =>
      X i.1 omega * standardNormalSteinSolution x (W omega)) mu := by
    intro i
    exact integrable_mul_standardNormalSteinSolution_comp
      (X i.1) W ((hX i.1).integrable (by norm_num)) hW.aemeasurable x
  have hXiLeave : ∀ i : Fin (N + 1), Integrable (fun omega =>
      X i.1 omega * standardNormalSteinSolution x
        (normalizedCenteredIidLeaveOneOut X N i omega)) mu := by
    intro i
    exact integrable_mul_standardNormalSteinSolution_comp
      (X i.1) (normalizedCenteredIidLeaveOneOut X N i)
      ((hX i.1).integrable (by norm_num))
      (normalizedCenteredIidLeaveOneOut_memLp X hX N i).aemeasurable x
  have hterm : ∀ i : Fin (N + 1),
      (∫ omega, X i.1 omega * standardNormalSteinSolution x (W omega) ∂mu) =
        ∫ omega, X i.1 omega *
          (standardNormalSteinSolution x (W omega) -
            standardNormalSteinSolution x
              (normalizedCenteredIidLeaveOneOut X N i omega)) ∂mu := by
    intro i
    have hzero :=
      integral_mul_standardNormalSteinSolution_leaveOneOut_eq_zero
        X hX hIndep N i x (hMean i.1)
    calc
      (∫ omega, X i.1 omega * standardNormalSteinSolution x (W omega) ∂mu) =
          (∫ omega, X i.1 omega * standardNormalSteinSolution x (W omega) ∂mu) -
            ∫ omega, X i.1 omega * standardNormalSteinSolution x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu := by rw [hzero, sub_zero]
      _ = ∫ omega,
          X i.1 omega * standardNormalSteinSolution x (W omega) -
            X i.1 omega * standardNormalSteinSolution x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu :=
        (integral_sub (hXiW i) (hXiLeave i)).symm
      _ = ∫ omega, X i.1 omega *
          (standardNormalSteinSolution x (W omega) -
            standardNormalSteinSolution x
              (normalizedCenteredIidLeaveOneOut X N i omega)) ∂mu := by
        apply integral_congr_ae
        exact ae_of_all mu fun omega => by ring
  have hpoint : (fun omega =>
      W omega * standardNormalSteinSolution x (W omega)) =
      fun omega => (Real.sqrt (N + 1 : ℝ))⁻¹ *
        ∑ i : Fin (N + 1),
          X i.1 omega * standardNormalSteinSolution x (W omega) := by
    funext omega
    dsimp only [W, normalizedCenteredIidSum]
    rw [mul_assoc, Finset.sum_mul]
  rw [hpoint, integral_const_mul, integral_finset_sum]
  · congr 1
    apply Finset.sum_congr rfl
    intro i _
    exact hterm i
  · intro i _
    exact hXiW i

/-- The normalized-sum Stein identity after the drift term has been expanded
summand by summand around the corresponding leave-one-out sums. -/
theorem integral_normalizedCenteredIidSum_stein_eq_leaveOneOut
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (N : ℕ) (x : ℝ) :
    let W := normalizedCenteredIidSum X N
    (∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        (Real.sqrt (N + 1 : ℝ))⁻¹ *
          ∑ i : Fin (N + 1), ∫ omega,
            X i.1 omega *
              (standardNormalSteinSolution x (W omega) -
                standardNormalSteinSolution x
                  (normalizedCenteredIidLeaveOneOut X N i omega)) ∂mu =
      cdf (probabilityLaw W
          (normalizedCenteredIidSum_memLp X hX N).aemeasurable) x -
        cdf standardNormalLaw x := by
  have h := integral_normalizedCenteredIidSum_standardNormalStein_eq_cdf_sub
    X hX N x
  have hDrift :=
    integral_normalizedCenteredIidSum_mul_steinSolution_eq_leaveOneOut
      X hX hIndep hMean N x
  dsimp only at h hDrift ⊢
  rw [hDrift] at h
  exact h

theorem antitone_standardNormalHalfLineSteinTarget (x : ℝ) :
    Antitone (standardNormalHalfLineSteinTarget x) := by
  intro a b hab
  unfold standardNormalHalfLineSteinTarget
  by_cases ha : a ≤ x
  · by_cases hb : b ≤ x
    · simp [ha, hb]
    · simp [ha, hb]
  · have hb : ¬b ≤ x := fun hbx => ha (hab.trans hbx)
    simp [ha, hb]

/-- The discontinuous part of the Stein derivative changes only when the
unoriented interval between the two arguments contains the threshold. -/
theorem abs_standardNormalHalfLineSteinTarget_sub_le_crossing
    (x a b : ℝ) :
    |standardNormalHalfLineSteinTarget x b -
        standardNormalHalfLineSteinTarget x a| ≤
      (uIcc a b).indicator (fun _ : ℝ => (1 : ℝ)) x := by
  classical
  unfold standardNormalHalfLineSteinTarget
  by_cases ha : a ≤ x
  · by_cases hb : b ≤ x
    · simp only [if_pos ha, if_pos hb, sub_self, abs_zero]
      exact Set.indicator_nonneg (fun _ _ => zero_le_one) x
    · have hmem : x ∈ uIcc a b :=
        Set.mem_uIcc.mpr (Or.inl ⟨ha, (le_of_not_ge hb)⟩)
      rw [if_neg hb, if_pos ha, Set.indicator_of_mem hmem]
      rw [show -cdf standardNormalLaw x -
          (1 - cdf standardNormalLaw x) = (-1 : ℝ) by ring]
      norm_num
  · by_cases hb : b ≤ x
    · have hmem : x ∈ uIcc a b :=
        Set.mem_uIcc.mpr (Or.inr ⟨hb, (le_of_not_ge ha)⟩)
      rw [if_pos hb, if_neg ha, Set.indicator_of_mem hmem]
      rw [show (1 - cdf standardNormalLaw x) -
          -cdf standardNormalLaw x = (1 : ℝ) by ring]
      norm_num
    · simp only [if_neg ha, if_neg hb, sub_self, abs_zero]
      exact Set.indicator_nonneg (fun _ _ => zero_le_one) x

/-- If adding `y` crosses the threshold `x`, then the starting point lies in
the symmetric interval of radius `|y|` around that threshold. -/
theorem mem_Icc_sub_abs_add_abs_of_mem_uIcc_add
    {x w y : ℝ} (h : x ∈ uIcc w (w + y)) :
    w ∈ Icc (x - |y|) (x + |y|) := by
  rcases Set.mem_uIcc.mp h with h | h
  · constructor <;> nlinarith [le_abs_self y, neg_le_abs y]
  · constructor <;> nlinarith [le_abs_self y, neg_le_abs y]

/-- Indicator form of the deterministic crossing containment. -/
theorem indicator_uIcc_add_le_indicator_Icc_sub_abs_add_abs
    (x w y : ℝ) :
    (uIcc w (w + y)).indicator (fun _ : ℝ => (1 : ℝ)) x ≤
      (Icc (x - |y|) (x + |y|)).indicator
        (fun _ : ℝ => (1 : ℝ)) w := by
  classical
  by_cases h : x ∈ uIcc w (w + y)
  · rw [Set.indicator_of_mem h,
      Set.indicator_of_mem (mem_Icc_sub_abs_add_abs_of_mem_uIcc_add h)]
  · rw [Set.indicator_of_notMem h]
    exact Set.indicator_nonneg (fun _ _ => zero_le_one) w

/-- For a nonnegative increment, a change in the half-line target forces the
starting point into the half-open interval `(x - y, x]`. -/
theorem abs_standardNormalHalfLineSteinTarget_add_sub_le_indicator_Ioc_of_nonneg
    (x w y : ℝ) (hy : 0 ≤ y) :
    |standardNormalHalfLineSteinTarget x (w + y) -
        standardNormalHalfLineSteinTarget x w| ≤
      (Ioc (x - y) x).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  classical
  unfold standardNormalHalfLineSteinTarget
  by_cases hw : w ≤ x
  · by_cases hwy : w + y ≤ x
    · simp only [if_pos hw, if_pos hwy, sub_self, abs_zero]
      exact Set.indicator_nonneg (fun _ _ => zero_le_one) w
    · have hmem : w ∈ Ioc (x - y) x := ⟨by linarith, hw⟩
      rw [if_neg hwy, if_pos hw, Set.indicator_of_mem hmem]
      rw [show -cdf standardNormalLaw x -
          (1 - cdf standardNormalLaw x) = (-1 : ℝ) by ring]
      norm_num
  · have hwy : ¬w + y ≤ x := by
      intro h
      exact hw (by linarith)
    simp only [if_neg hw, if_neg hwy, sub_self, abs_zero]
    exact Set.indicator_nonneg (fun _ _ => zero_le_one) w

/-- For a nonpositive increment, a change in the half-line target forces the
starting point into the half-open interval `(x, x - y]`. -/
theorem abs_standardNormalHalfLineSteinTarget_add_sub_le_indicator_Ioc_of_nonpos
    (x w y : ℝ) (hy : y ≤ 0) :
    |standardNormalHalfLineSteinTarget x (w + y) -
        standardNormalHalfLineSteinTarget x w| ≤
      (Ioc x (x - y)).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  classical
  unfold standardNormalHalfLineSteinTarget
  by_cases hw : w ≤ x
  · have hwy : w + y ≤ x := by linarith
    simp only [if_pos hw, if_pos hwy, sub_self, abs_zero]
    exact Set.indicator_nonneg (fun _ _ => zero_le_one) w
  · by_cases hwy : w + y ≤ x
    · have hmem : w ∈ Ioc x (x - y) := ⟨lt_of_not_ge hw, by linarith⟩
      rw [if_pos hwy, if_neg hw, Set.indicator_of_mem hmem]
      rw [show (1 - cdf standardNormalLaw x) -
          -cdf standardNormalLaw x = (1 : ℝ) by ring]
      norm_num
    · simp only [if_neg hw, if_neg hwy, sub_self, abs_zero]
      exact Set.indicator_nonneg (fun _ _ => zero_le_one) w

theorem indicator_Ioc_sub_le_of_nonneg
    (x w s y : ℝ) (hs : 0 ≤ s) (hsy : s ≤ y) :
    (Ioc (x - s) x).indicator (fun _ : ℝ => (1 : ℝ)) w ≤
      (Ioc (x - y) x).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  classical
  by_cases hmem : w ∈ Ioc (x - s) x
  · have hmem' : w ∈ Ioc (x - y) x := ⟨by linarith [hmem.1], hmem.2⟩
    rw [Set.indicator_of_mem hmem, Set.indicator_of_mem hmem']
  · rw [Set.indicator_of_notMem hmem]
    exact Set.indicator_nonneg (fun _ _ => zero_le_one) w

theorem indicator_Ioc_sub_le_of_nonpos
    (x w y s : ℝ) (hys : y ≤ s) (hs : s ≤ 0) :
    (Ioc x (x - s)).indicator (fun _ : ℝ => (1 : ℝ)) w ≤
      (Ioc x (x - y)).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  classical
  by_cases hmem : w ∈ Ioc x (x - s)
  · have hmem' : w ∈ Ioc x (x - y) := ⟨hmem.1, by linarith [hmem.2]⟩
    rw [Set.indicator_of_mem hmem, Set.indicator_of_mem hmem']
  · rw [Set.indicator_of_notMem hmem]
    exact Set.indicator_nonneg (fun _ _ => zero_le_one) w

/-- Measurable two-variable event encoding the direction-sensitive half-open
interval crossed by an additive displacement `scale * v`. -/
def standardNormalSteinCrossingSet (x scale : ℝ) : Set (ℝ × ℝ) :=
  {p | (0 ≤ p.2 ∧ p.1 ∈ Ioc (x - scale * p.2) x) ∨
    (p.2 ≤ 0 ∧ p.1 ∈ Ioc x (x - scale * p.2))}

theorem measurableSet_standardNormalSteinCrossingSet (x scale : ℝ) :
    MeasurableSet (standardNormalSteinCrossingSet x scale) := by
  unfold standardNormalSteinCrossingSet
  measurability

theorem intervalIntegrable_standardNormalSteinDerivative
    (x a b : ℝ) :
    IntervalIntegrable (standardNormalSteinDerivative x) volume a b := by
  unfold standardNormalSteinDerivative
  exact ((continuous_id.mul (continuous_standardNormalSteinSolution x)).intervalIntegrable a b).add
    ((antitone_standardNormalHalfLineSteinTarget x).intervalIntegrable)

/-- Right derivative of the joined solution, using the upper branch at the
threshold. -/
noncomputable def standardNormalSteinRightDerivative (x z : ℝ) : ℝ :=
  if z < x then
    z * standardNormalSteinSolution x z + (1 - cdf standardNormalLaw x)
  else
    z * standardNormalSteinSolution x z - cdf standardNormalLaw x

theorem standardNormalSteinRightDerivative_eq_total
    {x z : ℝ} (hz : z ≠ x) :
    standardNormalSteinRightDerivative x z =
      standardNormalSteinDerivative x z := by
  rcases lt_or_gt_of_ne hz with hzx | hxz
  · unfold standardNormalSteinRightDerivative standardNormalSteinDerivative
    simp [standardNormalHalfLineSteinTarget, hzx, hzx.le]
  · unfold standardNormalSteinRightDerivative standardNormalSteinDerivative
    simp [standardNormalHalfLineSteinTarget, not_lt_of_ge hxz.le,
      not_le.mpr hxz, sub_eq_add_neg]

theorem intervalIntegrable_standardNormalSteinRightDerivative
    (x a b : ℝ) :
    IntervalIntegrable (standardNormalSteinRightDerivative x) volume a b := by
  apply (intervalIntegrable_standardNormalSteinDerivative x a b).congr_ae
  filter_upwards [(volume.restrict (uIoc a b)).ae_ne x] with z hz
  exact (standardNormalSteinRightDerivative_eq_total hz).symm

theorem hasDerivWithinAt_standardNormalSteinSolution_right
    (x z : ℝ) :
    HasDerivWithinAt (standardNormalSteinSolution x)
      (standardNormalSteinRightDerivative x z) (Ioi z) z := by
  rcases lt_trichotomy z x with hzx | hzx | hxz
  · simpa [standardNormalSteinRightDerivative, hzx] using
      (hasDerivAt_standardNormalSteinSolution_of_lt hzx).hasDerivWithinAt
  · subst z
    have hu : HasDerivWithinAt (standardNormalSteinUpper x)
        (x * standardNormalSteinUpper x x - cdf standardNormalLaw x)
        (Ioi x) x :=
      (hasDerivAt_standardNormalSteinUpper x x).hasDerivWithinAt
    have hj := hu.congr
      (fun y hy => standardNormalSteinSolution_of_lt hy)
      (by rw [standardNormalSteinSolution_of_le le_rfl,
        standardNormalSteinLower_eq_upper])
    have hjoin : standardNormalSteinSolution x x = standardNormalSteinUpper x x := by
      rw [standardNormalSteinSolution_of_le le_rfl,
        standardNormalSteinLower_eq_upper]
    simpa [standardNormalSteinRightDerivative, hjoin] using hj
  · simpa [standardNormalSteinRightDerivative, not_lt_of_ge hxz.le] using
      (hasDerivAt_standardNormalSteinSolution_of_gt hxz).hasDerivWithinAt

theorem intervalIntegral_standardNormalSteinRightDerivative_eq_sub
    (x a b : ℝ) :
    (∫ z in a..b, standardNormalSteinRightDerivative x z) =
      standardNormalSteinSolution x b - standardNormalSteinSolution x a := by
  apply intervalIntegral.integral_eq_sub_of_hasDeriv_right
  · exact (continuous_standardNormalSteinSolution x).continuousOn
  · intro z _
    exact hasDerivWithinAt_standardNormalSteinSolution_right x z
  · exact intervalIntegrable_standardNormalSteinRightDerivative x a b

/-- Exact increment formula for the joined Stein solution.  The total
representative differs from the one-sided analytic derivative only at the
threshold, which has Lebesgue measure zero. -/
theorem intervalIntegral_standardNormalSteinDerivative_eq_sub
    (x a b : ℝ) :
    (∫ z in a..b, standardNormalSteinDerivative x z) =
      standardNormalSteinSolution x b - standardNormalSteinSolution x a := by
  calc
    (∫ z in a..b, standardNormalSteinDerivative x z) =
        ∫ z in a..b, standardNormalSteinRightDerivative x z := by
      apply intervalIntegral.integral_congr_ae
      filter_upwards [(volume : Measure ℝ).ae_ne x] with z hz _
      exact (standardNormalSteinRightDerivative_eq_total hz).symm
    _ = standardNormalSteinSolution x b - standardNormalSteinSolution x a :=
      intervalIntegral_standardNormalSteinRightDerivative_eq_sub x a b

/-- The first-order interval remainder is bounded by the integral of the
derivative increment from the starting point. -/
theorem abs_mul_standardNormalSteinDerivative_sub_intervalIntegral_le
    (x a b : ℝ) :
    |(b - a) * standardNormalSteinDerivative x a -
        (∫ z in a..b, standardNormalSteinDerivative x z)| ≤
      ∫ z in uIoc a b,
        |standardNormalSteinDerivative x z -
          standardNormalSteinDerivative x a| := by
  have hD := intervalIntegrable_standardNormalSteinDerivative x a b
  have hconst : IntervalIntegrable
      (fun _ : ℝ => standardNormalSteinDerivative x a) volume a b :=
    intervalIntegrable_const
  calc
    |(b - a) * standardNormalSteinDerivative x a -
        (∫ z in a..b, standardNormalSteinDerivative x z)| =
      ‖∫ z in a..b, (standardNormalSteinDerivative x a -
        standardNormalSteinDerivative x z)‖ := by
      rw [intervalIntegral.integral_sub hconst hD,
        intervalIntegral.integral_const, Real.norm_eq_abs]
      congr 1
    _ = ‖∫ z in uIoc a b, (standardNormalSteinDerivative x a -
        standardNormalSteinDerivative x z)‖ :=
      intervalIntegral.norm_intervalIntegral_eq
        (fun z => standardNormalSteinDerivative x a -
          standardNormalSteinDerivative x z) a b volume
    _ ≤ ∫ z in uIoc a b,
        ‖standardNormalSteinDerivative x a -
          standardNormalSteinDerivative x z‖ :=
      norm_integral_le_integral_norm _
    _ = ∫ z in uIoc a b,
        |standardNormalSteinDerivative x z -
          standardNormalSteinDerivative x a| := by
      apply integral_congr_ae
      exact ae_of_all _ fun z => by
        change |standardNormalSteinDerivative x a -
          standardNormalSteinDerivative x z| =
            |standardNormalSteinDerivative x z -
              standardNormalSteinDerivative x a|
        exact abs_sub_comm _ _

theorem abs_standardNormalSteinSolution_sub_le_integral_abs_derivative
    (x a b : ℝ) :
    |standardNormalSteinSolution x b - standardNormalSteinSolution x a| ≤
      ∫ z in uIoc a b, |standardNormalSteinDerivative x z| := by
  calc
    |standardNormalSteinSolution x b - standardNormalSteinSolution x a| =
        ‖∫ z in a..b, standardNormalSteinDerivative x z‖ := by
      rw [intervalIntegral_standardNormalSteinDerivative_eq_sub,
        Real.norm_eq_abs]
    _ = ‖∫ z in uIoc a b, standardNormalSteinDerivative x z‖ :=
      intervalIntegral.norm_intervalIntegral_eq
        (standardNormalSteinDerivative x) a b volume
    _ ≤ ∫ z in uIoc a b, ‖standardNormalSteinDerivative x z‖ :=
      norm_integral_le_integral_norm _
    _ = ∫ z in uIoc a b, |standardNormalSteinDerivative x z| := by
      simp only [Real.norm_eq_abs]

/-- The sharp global Lipschitz estimate for the half-line Stein solution. -/
theorem abs_standardNormalSteinSolution_sub_le (x a b : ℝ) :
    |standardNormalSteinSolution x b - standardNormalSteinSolution x a| ≤
      |b - a| := by
  have hbound : ∀ z ∈ uIoc a b,
      ‖standardNormalSteinDerivative x z‖ ≤ 1 := by
    intro z _
    simpa only [Real.norm_eq_abs] using
      abs_standardNormalSteinDerivative_le_one x z
  have hnorm := intervalIntegral.norm_integral_le_of_norm_le_const hbound
  rw [intervalIntegral_standardNormalSteinDerivative_eq_sub,
    Real.norm_eq_abs] at hnorm
  simpa using hnorm

/-- The continuous drift part of the total Stein derivative has a local
increment bound in terms of the starting point. -/
theorem abs_mul_standardNormalSteinSolution_sub_le
    (x a b : ℝ) :
    |b * standardNormalSteinSolution x b -
        a * standardNormalSteinSolution x a| ≤
      (|a| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) * |b - a| := by
  have hsplit :
      b * standardNormalSteinSolution x b -
          a * standardNormalSteinSolution x a =
        a * (standardNormalSteinSolution x b -
            standardNormalSteinSolution x a) +
          (b - a) * standardNormalSteinSolution x b := by ring
  rw [hsplit]
  apply (abs_add_le _ _).trans
  calc
    |a * (standardNormalSteinSolution x b -
          standardNormalSteinSolution x a)| +
        |(b - a) * standardNormalSteinSolution x b| =
      |a| * |standardNormalSteinSolution x b -
          standardNormalSteinSolution x a| +
        |b - a| * standardNormalSteinSolution x b := by
      rw [abs_mul, abs_mul,
        abs_of_nonneg (standardNormalSteinSolution_nonneg x b)]
    _ ≤ |a| * |b - a| +
        |b - a| *
          (Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) := by
      exact add_le_add
        (mul_le_mul_of_nonneg_left
          (abs_standardNormalSteinSolution_sub_le x a b) (abs_nonneg a))
        (mul_le_mul_of_nonneg_left
          (standardNormalSteinSolution_le x b) (abs_nonneg (b - a)))
    _ = (|a| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
        |b - a| := by ring

/-- Increment bound for the total Stein derivative.  Its continuous drift
part is controlled by the one-Lipschitz solution, while the only discontinuous
contribution is the explicit threshold-crossing indicator. -/
theorem abs_standardNormalSteinDerivative_sub_le_crossing
    (x a b : ℝ) :
    |standardNormalSteinDerivative x b -
        standardNormalSteinDerivative x a| ≤
      (|a| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) * |b - a| +
        (uIcc a b).indicator (fun _ : ℝ => (1 : ℝ)) x := by
  have hsplit :
      standardNormalSteinDerivative x b -
          standardNormalSteinDerivative x a =
        (a * (standardNormalSteinSolution x b -
            standardNormalSteinSolution x a) +
          (b - a) * standardNormalSteinSolution x b) +
        (standardNormalHalfLineSteinTarget x b -
          standardNormalHalfLineSteinTarget x a) := by
    unfold standardNormalSteinDerivative
    ring
  rw [hsplit]
  apply (abs_add_le _ _).trans
  apply add_le_add
  · calc
      |a * (standardNormalSteinSolution x b -
            standardNormalSteinSolution x a) +
          (b - a) * standardNormalSteinSolution x b| ≤
        |a * (standardNormalSteinSolution x b -
            standardNormalSteinSolution x a)| +
          |(b - a) * standardNormalSteinSolution x b| := abs_add_le _ _
      _ = |a| * |standardNormalSteinSolution x b -
            standardNormalSteinSolution x a| +
          |b - a| * standardNormalSteinSolution x b := by
        rw [abs_mul, abs_mul,
          abs_of_nonneg (standardNormalSteinSolution_nonneg x b)]
      _ ≤ |a| * |b - a| +
          |b - a| *
            (Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) := by
        exact add_le_add
          (mul_le_mul_of_nonneg_left
            (abs_standardNormalSteinSolution_sub_le x a b) (abs_nonneg a))
          (mul_le_mul_of_nonneg_left
            (standardNormalSteinSolution_le x b) (abs_nonneg (b - a)))
      _ = (|a| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          |b - a| := by ring
  · exact abs_standardNormalHalfLineSteinTarget_sub_le_crossing x a b

/-- Directional derivative-increment bound for a nonnegative displacement;
the atomic crossing term is a half-open interval of length exactly `y`. -/
theorem abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonneg
    (x w y : ℝ) (hy : 0 ≤ y) :
    |standardNormalSteinDerivative x (w + y) -
        standardNormalSteinDerivative x w| ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) * |y| +
        (Ioc (x - y) x).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  have hsplit :
      standardNormalSteinDerivative x (w + y) -
          standardNormalSteinDerivative x w =
        ((w + y) * standardNormalSteinSolution x (w + y) -
          w * standardNormalSteinSolution x w) +
        (standardNormalHalfLineSteinTarget x (w + y) -
          standardNormalHalfLineSteinTarget x w) := by
    unfold standardNormalSteinDerivative
    ring
  rw [hsplit]
  exact (abs_add_le _ _).trans (add_le_add
    (by simpa only [add_sub_cancel_left] using
      abs_mul_standardNormalSteinSolution_sub_le x w (w + y))
    (abs_standardNormalHalfLineSteinTarget_add_sub_le_indicator_Ioc_of_nonneg
      x w y hy))

/-- Directional derivative-increment bound for a nonpositive displacement;
the atomic crossing term is again a half-open interval of length `|y|`. -/
theorem abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonpos
    (x w y : ℝ) (hy : y ≤ 0) :
    |standardNormalSteinDerivative x (w + y) -
        standardNormalSteinDerivative x w| ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) * |y| +
        (Ioc x (x - y)).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  have hsplit :
      standardNormalSteinDerivative x (w + y) -
          standardNormalSteinDerivative x w =
        ((w + y) * standardNormalSteinSolution x (w + y) -
          w * standardNormalSteinSolution x w) +
        (standardNormalHalfLineSteinTarget x (w + y) -
          standardNormalHalfLineSteinTarget x w) := by
    unfold standardNormalSteinDerivative
    ring
  rw [hsplit]
  exact (abs_add_le _ _).trans (add_le_add
    (by simpa only [add_sub_cancel_left] using
      abs_mul_standardNormalSteinSolution_sub_le x w (w + y))
    (abs_standardNormalHalfLineSteinTarget_add_sub_le_indicator_Ioc_of_nonpos
      x w y hy))

/-- Positive-displacement integral of the derivative increment.  The smooth
part is quadratic in the displacement and all intermediate jumps are absorbed
by the single endpoint crossing event. -/
theorem integral_abs_standardNormalSteinDerivative_sub_le_of_nonneg
    (x w y : ℝ) (hy : 0 ≤ y) :
    (∫ z in uIoc w (w + y),
      |standardNormalSteinDerivative x z -
        standardNormalSteinDerivative x w|) ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (y ^ 2 / 2) +
        y * (Ioc (x - y) x).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  let A : ℝ := |w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  let K : ℝ := (Ioc (x - y) x).indicator (fun _ : ℝ => (1 : ℝ)) w
  have hDiffII : IntervalIntegrable (fun z =>
      standardNormalSteinDerivative x z - standardNormalSteinDerivative x w)
      volume w (w + y) :=
    (intervalIntegrable_standardNormalSteinDerivative x w (w + y)).sub
      intervalIntegrable_const
  have hLeft : IntegrableOn (fun z =>
      |standardNormalSteinDerivative x z - standardNormalSteinDerivative x w|)
      (uIoc w (w + y)) volume := by
    simpa only [Real.norm_eq_abs] using hDiffII.norm.def'
  have hContII : IntervalIntegrable (fun z : ℝ => A * |z - w|)
      volume w (w + y) := by
    exact ((continuous_const : Continuous (fun _ : ℝ => A)).mul
      ((continuous_id.sub continuous_const).abs)).intervalIntegrable w (w + y)
  have hConstII : IntervalIntegrable (fun _ : ℝ => K) volume w (w + y) :=
    intervalIntegrable_const
  have hCont := hContII.def'
  have hConst := hConstII.def'
  have hMajor := hCont.add hConst
  have hpoint : ∀ z ∈ uIoc w (w + y),
      |standardNormalSteinDerivative x z - standardNormalSteinDerivative x w| ≤
        A * |z - w| + K := by
    intro z hz
    have hwy : w ≤ w + y := by linarith
    rw [uIoc_of_le hwy] at hz
    have hs : 0 ≤ z - w := sub_nonneg.mpr hz.1.le
    have hsy : z - w ≤ y := by linarith [hz.2]
    have hD :=
      abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonneg
        x w (z - w) hs
    rw [show w + (z - w) = z by ring] at hD
    exact hD.trans (by
      simpa only [A, K] using add_le_add_right
        (indicator_Ioc_sub_le_of_nonneg x w (z - w) y hs hsy)
        ((|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          |z - w|))
  have hmono := setIntegral_mono_on hLeft hMajor measurableSet_uIoc hpoint
  have hpow : (∫ z in uIoc w (w + y), |z - w|) = y ^ 2 / 2 := by
    have h := integral_pow_abs_sub_uIoc (a := w) (b := w + y) (n := 1)
    norm_num [abs_of_nonneg hy] at h
    simpa using h
  have hconst : (∫ _z in uIoc w (w + y), K) = y * K := by
    have hvol : volume.real (uIoc w (w + y)) = y := by
      simp [MeasureTheory.measureReal_def, hy]
    rw [setIntegral_const, hvol, smul_eq_mul]
  calc
    (∫ z in uIoc w (w + y),
      |standardNormalSteinDerivative x z -
        standardNormalSteinDerivative x w|) ≤
      ∫ z in uIoc w (w + y), (A * |z - w| + K) := hmono
    _ = A * (∫ z in uIoc w (w + y), |z - w|) +
        ∫ _z in uIoc w (w + y), K := by
      rw [integral_add hCont hConst, integral_const_mul]
    _ = A * (y ^ 2 / 2) + y * K := by rw [hpow, hconst]
    _ = _ := by rfl

/-- Negative-displacement integral of the derivative increment.  Reversing the
interval changes only the directional crossing event and replaces its length
by `-y = |y|`. -/
theorem integral_abs_standardNormalSteinDerivative_sub_le_of_nonpos
    (x w y : ℝ) (hy : y ≤ 0) :
    (∫ z in uIoc w (w + y),
      |standardNormalSteinDerivative x z -
        standardNormalSteinDerivative x w|) ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (y ^ 2 / 2) +
        (-y) * (Ioc x (x - y)).indicator (fun _ : ℝ => (1 : ℝ)) w := by
  let A : ℝ := |w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  let K : ℝ := (Ioc x (x - y)).indicator (fun _ : ℝ => (1 : ℝ)) w
  have hDiffII : IntervalIntegrable (fun z =>
      standardNormalSteinDerivative x z - standardNormalSteinDerivative x w)
      volume w (w + y) :=
    (intervalIntegrable_standardNormalSteinDerivative x w (w + y)).sub
      intervalIntegrable_const
  have hLeft : IntegrableOn (fun z =>
      |standardNormalSteinDerivative x z - standardNormalSteinDerivative x w|)
      (uIoc w (w + y)) volume := by
    simpa only [Real.norm_eq_abs] using hDiffII.norm.def'
  have hContII : IntervalIntegrable (fun z : ℝ => A * |z - w|)
      volume w (w + y) := by
    exact ((continuous_const : Continuous (fun _ : ℝ => A)).mul
      ((continuous_id.sub continuous_const).abs)).intervalIntegrable w (w + y)
  have hConstII : IntervalIntegrable (fun _ : ℝ => K) volume w (w + y) :=
    intervalIntegrable_const
  have hCont := hContII.def'
  have hConst := hConstII.def'
  have hMajor := hCont.add hConst
  have hpoint : ∀ z ∈ uIoc w (w + y),
      |standardNormalSteinDerivative x z - standardNormalSteinDerivative x w| ≤
        A * |z - w| + K := by
    intro z hz
    have hwy : w + y ≤ w := by linarith
    rw [uIoc_of_ge hwy] at hz
    have hys : y ≤ z - w := by linarith [hz.1]
    have hs : z - w ≤ 0 := sub_nonpos.mpr hz.2
    have hD :=
      abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonpos
        x w (z - w) hs
    rw [show w + (z - w) = z by ring] at hD
    exact hD.trans (by
      simpa only [A, K] using add_le_add_right
        (indicator_Ioc_sub_le_of_nonpos x w y (z - w) hys hs)
        ((|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          |z - w|))
  have hmono := setIntegral_mono_on hLeft hMajor measurableSet_uIoc hpoint
  have hpow : (∫ z in uIoc w (w + y), |z - w|) = y ^ 2 / 2 := by
    have h := integral_pow_abs_sub_uIoc (a := w) (b := w + y) (n := 1)
    norm_num [abs_of_nonpos hy] at h
    convert h using 1 <;> ring
  have hconst : (∫ _z in uIoc w (w + y), K) = (-y) * K := by
    have hvol : volume.real (uIoc w (w + y)) = -y := by
      simp [MeasureTheory.measureReal_def, hy]
    rw [setIntegral_const, hvol, smul_eq_mul]
  calc
    (∫ z in uIoc w (w + y),
      |standardNormalSteinDerivative x z -
        standardNormalSteinDerivative x w|) ≤
      ∫ z in uIoc w (w + y), (A * |z - w| + K) := hmono
    _ = A * (∫ z in uIoc w (w + y), |z - w|) +
        ∫ _z in uIoc w (w + y), K := by
      rw [integral_add hCont hConst, integral_const_mul]
    _ = A * (y ^ 2 / 2) + (-y) * K := by rw [hpow, hconst]
    _ = _ := by rfl

/-- Sign-free derivative-increment integral bound, with the directional atomic
term represented by the measurable crossing set. -/
theorem integral_abs_standardNormalSteinDerivative_sub_le_crossingSet
    (x w y : ℝ) :
    (∫ z in uIoc w (w + y),
      |standardNormalSteinDerivative x z -
        standardNormalSteinDerivative x w|) ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (y ^ 2 / 2) +
        |y| * (standardNormalSteinCrossingSet x 1).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (w, y) := by
  by_cases hy : 0 ≤ y
  · rcases eq_or_lt_of_le hy with hzero | hpos
    · subst y
      simp [standardNormalSteinCrossingSet]
    · have hnpos : ¬ y ≤ 0 := not_le_of_gt hpos
      simpa [standardNormalSteinCrossingSet, Set.indicator_apply, hy,
        hnpos, abs_of_pos hpos] using
        integral_abs_standardNormalSteinDerivative_sub_le_of_nonneg x w y hy
  · have hneg : y < 0 := lt_of_not_ge hy
    have hy' : y ≤ 0 := hneg.le
    simpa [standardNormalSteinCrossingSet, Set.indicator_apply, hy,
      hy', abs_of_neg hneg] using
      integral_abs_standardNormalSteinDerivative_sub_le_of_nonpos x w y hy'

/-- First-order interval remainder bound with a quadratic continuous term and
the direction-sensitive threshold-crossing term exposed explicitly. -/
theorem abs_mul_standardNormalSteinDerivative_sub_intervalIntegral_le_crossingSet
    (x w y : ℝ) :
    |y * standardNormalSteinDerivative x w -
        (∫ z in w..w + y, standardNormalSteinDerivative x z)| ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (y ^ 2 / 2) +
        |y| * (standardNormalSteinCrossingSet x 1).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (w, y) := by
  have h := abs_mul_standardNormalSteinDerivative_sub_intervalIntegral_le
    x w (w + y)
  have h' :
      |y * standardNormalSteinDerivative x w -
          (∫ z in w..w + y, standardNormalSteinDerivative x z)| ≤
        ∫ z in uIoc w (w + y),
          |standardNormalSteinDerivative x z -
            standardNormalSteinDerivative x w| := by
    convert h using 1 <;> ring
  exact h'.trans
    (integral_abs_standardNormalSteinDerivative_sub_le_crossingSet x w y)

/-- After multiplying the first-order remainder by the displacement, the
continuous term is cubic and the crossing term carries the squared
displacement. -/
theorem abs_sq_mul_standardNormalSteinDerivative_sub_mul_intervalIntegral_le_crossingSet
    (x w y : ℝ) :
    |y ^ 2 * standardNormalSteinDerivative x w -
        y * (∫ z in w..w + y, standardNormalSteinDerivative x z)| ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (|y| ^ 3 / 2) +
        y ^ 2 * (standardNormalSteinCrossingSet x 1).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (w, y) := by
  have h :=
    abs_mul_standardNormalSteinDerivative_sub_intervalIntegral_le_crossingSet
      x w y
  have hmul := mul_le_mul_of_nonneg_left h (abs_nonneg y)
  calc
    |y ^ 2 * standardNormalSteinDerivative x w -
        y * (∫ z in w..w + y, standardNormalSteinDerivative x z)| =
      |y| * |y * standardNormalSteinDerivative x w -
        (∫ z in w..w + y, standardNormalSteinDerivative x z)| := by
          rw [← abs_mul]
          congr 1
          ring
    _ ≤ |y| *
        ((|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
            (y ^ 2 / 2) +
          |y| * (standardNormalSteinCrossingSet x 1).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ)) (w, y)) := hmul
    _ = (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (|y| ^ 3 / 2) +
        y ^ 2 * (standardNormalSteinCrossingSet x 1).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (w, y) := by
      rw [← sq_abs y]
      ring

/-- A nonnegative scale can be moved from the displacement coordinate into the
scale parameter of the crossing set. -/
theorem mem_standardNormalSteinCrossingSet_one_mul_iff
    (x scale w v : ℝ) (hscale : 0 ≤ scale) :
    (w, scale * v) ∈ standardNormalSteinCrossingSet x 1 ↔
      (w, v) ∈ standardNormalSteinCrossingSet x scale := by
  by_cases hzero : scale = 0
  · subst scale
    simp [standardNormalSteinCrossingSet]
  · have hpos : 0 < scale := lt_of_le_of_ne hscale (Ne.symm hzero)
    have hnonneg : 0 ≤ scale * v ↔ 0 ≤ v :=
      mul_nonneg_iff_of_pos_left hpos
    have hnonpos : scale * v ≤ 0 ↔ v ≤ 0 := by
      simpa using
        (mul_le_mul_iff_right₀ hpos : scale * v ≤ scale * 0 ↔ v ≤ 0)
    simp [standardNormalSteinCrossingSet, hnonneg, hnonpos]

theorem standardNormalSteinCrossingSet_indicator_one_mul_eq
    (x scale w v : ℝ) (hscale : 0 ≤ scale) :
    (standardNormalSteinCrossingSet x 1).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (w, scale * v) =
      (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (w, v) := by
  classical
  have hmem := mem_standardNormalSteinCrossingSet_one_mul_iff
    x scale w v hscale
  by_cases h : (w, scale * v) ∈ standardNormalSteinCrossingSet x 1
  · rw [Set.indicator_of_mem h, Set.indicator_of_mem (hmem.mp h)]
  · have h' : (w, v) ∉ standardNormalSteinCrossingSet x scale :=
      fun hv => h (hmem.mpr hv)
    rw [Set.indicator_of_notMem h, Set.indicator_of_notMem h']

/-- Scaled version of the cubic interval-remainder bound, arranged in the
normalization used by the leave-one-out sum. -/
theorem abs_scale_sq_mul_standardNormalSteinDerivative_sub_scale_mul_intervalIntegral_le
    (x w scale v : ℝ) (hscale : 0 ≤ scale) :
    |scale ^ 2 * v ^ 2 * standardNormalSteinDerivative x w -
        scale * v *
          (∫ z in w..w + scale * v, standardNormalSteinDerivative x z)| ≤
      (|w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (scale ^ 3 * |v| ^ 3 / 2) +
        scale ^ 2 * v ^ 2 *
          (standardNormalSteinCrossingSet x scale).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ)) (w, v) := by
  have h :=
    abs_sq_mul_standardNormalSteinDerivative_sub_mul_intervalIntegral_le_crossingSet
      x w (scale * v)
  rw [standardNormalSteinCrossingSet_indicator_one_mul_eq
    x scale w v hscale] at h
  simpa [mul_pow, abs_mul, abs_of_nonneg hscale, mul_assoc] using h

/-- Pointwise interval-remainder estimate for one normalized summand in the
leave-one-out decomposition. -/
theorem abs_normalizedSummand_steinIntervalRemainder_le
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1))
    (x : ℝ) (omega : Omega) :
    let W := normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    |scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)| ≤
      (|Wi omega| +
          Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (scale ^ 3 * |X i.1 omega| ^ 3 / 2) +
        scale ^ 2 * (X i.1 omega) ^ 2 *
          (standardNormalSteinCrossingSet x scale).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ))
            (Wi omega, X i.1 omega) := by
  dsimp only
  rw [normalizedCenteredIidSum_eq_leaveOneOut_add]
  exact
    abs_scale_sq_mul_standardNormalSteinDerivative_sub_scale_mul_intervalIntegral_le
      x (normalizedCenteredIidLeaveOneOut X N i omega)
        (Real.sqrt (N + 1 : ℝ))⁻¹ (X i.1 omega)
        (inv_nonneg.mpr (Real.sqrt_nonneg _))

/-- Pointwise leave-one-out specialization of the derivative-increment
bound, with the normalized removed summand exposed explicitly. -/
theorem abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le_crossing
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1))
    (x : ℝ) (omega : Omega) :
    let W := normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ≤
      (|Wi omega| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (scale * |X i.1 omega|) +
        (uIcc (Wi omega) (W omega)).indicator
          (fun _ : ℝ => (1 : ℝ)) x := by
  dsimp only
  have h := abs_standardNormalSteinDerivative_sub_le_crossing x
    (normalizedCenteredIidLeaveOneOut X N i omega)
    (normalizedCenteredIidSum X N omega)
  have hscale : 0 ≤ (Real.sqrt (N + 1 : ℝ))⁻¹ :=
    inv_nonneg.mpr (Real.sqrt_nonneg _)
  have hdiff :
      |normalizedCenteredIidSum X N omega -
          normalizedCenteredIidLeaveOneOut X N i omega| =
        (Real.sqrt (N + 1 : ℝ))⁻¹ * |X i.1 omega| := by
    rw [normalizedCenteredIidSum_eq_leaveOneOut_add,
      add_sub_cancel_left, abs_mul, abs_of_nonneg hscale]
  rw [hdiff] at h
  exact h

/-- Positive removed-summand specialization with a half-open crossing
interval of the exact normalized summand length. -/
theorem abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le_of_nonneg
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1))
    (x : ℝ) (omega : Omega) (hXi : 0 ≤ X i.1 omega) :
    let W := normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ≤
      (|Wi omega| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (scale * |X i.1 omega|) +
        (Ioc (x - scale * X i.1 omega) x).indicator
          (fun _ : ℝ => (1 : ℝ)) (Wi omega) := by
  dsimp only
  rw [normalizedCenteredIidSum_eq_leaveOneOut_add]
  have hscale : 0 ≤ (Real.sqrt (N + 1 : ℝ))⁻¹ :=
    inv_nonneg.mpr (Real.sqrt_nonneg _)
  have h := abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonneg
    x (normalizedCenteredIidLeaveOneOut X N i omega)
      ((Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega)
      (mul_nonneg hscale hXi)
  rw [abs_mul, abs_of_nonneg hscale] at h
  exact h

/-- Negative removed-summand specialization with a half-open crossing
interval of the exact normalized summand length. -/
theorem abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le_of_nonpos
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1))
    (x : ℝ) (omega : Omega) (hXi : X i.1 omega ≤ 0) :
    let W := normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ≤
      (|Wi omega| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (scale * |X i.1 omega|) +
        (Ioc x (x - scale * X i.1 omega)).indicator
          (fun _ : ℝ => (1 : ℝ)) (Wi omega) := by
  dsimp only
  rw [normalizedCenteredIidSum_eq_leaveOneOut_add]
  have hscale : 0 ≤ (Real.sqrt (N + 1 : ℝ))⁻¹ :=
    inv_nonneg.mpr (Real.sqrt_nonneg _)
  have h := abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonpos
    x (normalizedCenteredIidLeaveOneOut X N i omega)
      ((Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega)
      (mul_nonpos_of_nonneg_of_nonpos hscale hXi)
  rw [abs_mul, abs_of_nonneg hscale] at h
  exact h

/-- Unified pointwise leave-one-out increment bound.  The measurable crossing
set records the sign split without placing a decidable conditional in the
statement. -/
theorem abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le_crossingSet
    {Omega : Type*} (X : ℕ → Omega → ℝ) (N : ℕ) (i : Fin (N + 1))
    (x : ℝ) (omega : Omega) :
    let W := normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ≤
      (|Wi omega| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
          (scale * |X i.1 omega|) +
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega) := by
  dsimp only
  rw [normalizedCenteredIidSum_eq_leaveOneOut_add]
  have hscale : 0 ≤ (Real.sqrt (N + 1 : ℝ))⁻¹ :=
    inv_nonneg.mpr (Real.sqrt_nonneg _)
  by_cases hzero : X i.1 omega = 0
  · rw [hzero]
    simp [standardNormalSteinCrossingSet]
  · rcases lt_or_gt_of_ne hzero with hneg | hpos
    · have h :=
        abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonpos
          x (normalizedCenteredIidLeaveOneOut X N i omega)
          ((Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega)
          (mul_nonpos_of_nonneg_of_nonpos hscale hneg.le)
      rw [abs_mul, abs_of_nonneg hscale] at h
      simpa [standardNormalSteinCrossingSet, Set.indicator_apply,
        hneg.le, not_le_of_gt hneg] using h
    · have h :=
        abs_standardNormalSteinDerivative_add_sub_le_indicator_Ioc_of_nonneg
          x (normalizedCenteredIidLeaveOneOut X N i omega)
          ((Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega)
          (mul_nonneg hscale hpos.le)
      rw [abs_mul, abs_of_nonneg hscale] at h
      simpa [standardNormalSteinCrossingSet, Set.indicator_apply,
        hpos.le, not_le_of_gt hpos] using h

theorem abs_standardNormalSteinDerivative_le_linear (x z : ℝ) :
    |standardNormalSteinDerivative x z| ≤
      Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) * |z| + 1 := by
  unfold standardNormalSteinDerivative
  calc
    |z * standardNormalSteinSolution x z +
        standardNormalHalfLineSteinTarget x z| ≤
      |z * standardNormalSteinSolution x z| +
        |standardNormalHalfLineSteinTarget x z| := abs_add_le _ _
    _ = |z| * standardNormalSteinSolution x z +
        |standardNormalHalfLineSteinTarget x z| := by
      rw [abs_mul, abs_of_nonneg (standardNormalSteinSolution_nonneg x z)]
    _ ≤ |z| * (Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) + 1 :=
      add_le_add
        (mul_le_mul_of_nonneg_left (standardNormalSteinSolution_le x z) (abs_nonneg z))
        (abs_standardNormalHalfLineSteinTarget_le_one x z)
    _ = Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) * |z| + 1 := by ring

/-- A location-dependent Lipschitz bound obtained from the exact increment
formula and the coarse linear bound for the total Stein derivative. -/
theorem abs_standardNormalSteinSolution_sub_le_linear
    (x a b : ℝ) :
    |standardNormalSteinSolution x b - standardNormalSteinSolution x a| ≤
      (Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) *
          max |a| |b| + 1) * |b - a| := by
  have hC : 0 ≤ Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) := by
    positivity
  have hbound : ∀ z ∈ uIoc a b,
      ‖standardNormalSteinDerivative x z‖ ≤
        Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) *
          max |a| |b| + 1 := by
    intro z hz
    have hzabs : |z| ≤ max |a| |b| := by
      rcases le_total a b with hab | hba
      · rw [uIoc_of_le hab] at hz
        exact abs_le_max_abs_abs (le_of_lt hz.1) hz.2
      · rw [uIoc_of_ge hba] at hz
        simpa only [max_comm] using abs_le_max_abs_abs (le_of_lt hz.1) hz.2
    rw [Real.norm_eq_abs]
    exact (abs_standardNormalSteinDerivative_le_linear x z).trans
      (by linarith [mul_le_mul_of_nonneg_left hzabs hC])
  have hnorm := intervalIntegral.norm_integral_le_of_norm_le_const hbound
  rw [intervalIntegral_standardNormalSteinDerivative_eq_sub,
    Real.norm_eq_abs] at hnorm
  exact hnorm

/-- Exact normalized-sum leave-one-out identity with every Stein-solution
increment rewritten as an oriented integral of the total derivative. -/
theorem integral_normalizedCenteredIidSum_stein_eq_leaveOneOutInterval
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (N : ℕ) (x : ℝ) :
    let W := normalizedCenteredIidSum X N
    (∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        (Real.sqrt (N + 1 : ℝ))⁻¹ *
          ∑ i : Fin (N + 1), ∫ omega,
            X i.1 omega *
              (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..W omega,
                standardNormalSteinDerivative x z) ∂mu =
      cdf (probabilityLaw W
          (normalizedCenteredIidSum_memLp X hX N).aemeasurable) x -
        cdf standardNormalLaw x := by
  have h := integral_normalizedCenteredIidSum_stein_eq_leaveOneOut
    X hX hIndep hMean N x
  dsimp only at h ⊢
  simpa only [intervalIntegral_standardNormalSteinDerivative_eq_sub] using h

/-- Independence and unit variance identify the quadratic summand factor with
the derivative expectation at the corresponding leave-one-out sum. -/
theorem integral_sq_mul_standardNormalSteinDerivative_leaveOneOut_eq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ)
    (hSecond : ∫ omega, (X i.1 omega) ^ 2 ∂mu = 1) :
    (∫ omega, (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
        (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) =
      ∫ omega, standardNormalSteinDerivative x
        (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu := by
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hfact := hind.integral_comp_mul_comp
    (hX i.1).aemeasurable hLeave.aemeasurable
    (show AEStronglyMeasurable (fun y : ℝ => y ^ 2)
        (Measure.map (X i.1) mu) by fun_prop)
    (measurable_standardNormalSteinDerivative x).aestronglyMeasurable
  simpa [Function.comp_def, hSecond] using hfact

/-- Exact variance-cancellation decomposition of the normalized-sum Stein
identity.  The first bracket compares derivative expectations, while the
second is the summandwise interval remainder before any estimate. -/
theorem integral_normalizedCenteredIidSum_stein_varianceCancellation
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (x : ℝ) :
    let W := normalizedCenteredIidSum X N
    let a := (Real.sqrt (N + 1 : ℝ))⁻¹
    ((∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        a ^ 2 * ∑ i : Fin (N + 1), ∫ omega,
          standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) +
      (a ^ 2 * ∑ i : Fin (N + 1), ∫ omega,
          (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu -
        a * ∑ i : Fin (N + 1), ∫ omega,
          X i.1 omega *
            (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..W omega,
              standardNormalSteinDerivative x z) ∂mu) =
      cdf (probabilityLaw W
          (normalizedCenteredIidSum_memLp X hX N).aemeasurable) x -
        cdf standardNormalLaw x := by
  have h := integral_normalizedCenteredIidSum_stein_eq_leaveOneOutInterval
    X hX hIndep hMean N x
  have hVar :
      (∑ i : Fin (N + 1), ∫ omega,
          (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) =
        ∑ i : Fin (N + 1), ∫ omega,
          standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu := by
    apply Finset.sum_congr rfl
    intro i _
    exact integral_sq_mul_standardNormalSteinDerivative_leaveOneOut_eq
      X hX hIndep N i x (hSecond i.1)
  dsimp only at h ⊢
  rw [hVar]
  linarith

end SteinBounds

theorem hasDerivAt_standardNormalPDF (x : ℝ) :
    HasDerivAt (ProbabilityTheory.gaussianPDFReal 0 1)
      (-x * ProbabilityTheory.gaussianPDFReal 0 1 x) x := by
  have hinner : HasDerivAt (fun y : ℝ => -(y - 0) ^ 2 / (2 * (1 : ℝ))) (-x) x := by
    convert (((hasDerivAt_id x).sub_const 0).pow 2).neg.div_const (2 * 1) using 1 <;>
      simp <;> ring
  have hexp : HasDerivAt
      (fun y : ℝ => Real.exp (-(y - 0) ^ 2 / (2 * (1 : ℝ))))
      (Real.exp (-(x - 0) ^ 2 / (2 * (1 : ℝ))) * (-x)) x :=
    (Real.hasDerivAt_exp _).comp x hinner
  have hmul := hexp.const_mul (Real.sqrt (2 * Real.pi * (1 : ℝ)))⁻¹
  simpa only [ProbabilityTheory.gaussianPDFReal_def, NNReal.coe_one,
    mul_one, sub_zero, neg_mul, mul_neg, mul_comm, mul_left_comm, mul_assoc] using hmul

theorem hasDerivAt_standardNormalPDF_deriv (x : ℝ) :
    HasDerivAt
      (fun y => -y * ProbabilityTheory.gaussianPDFReal 0 1 y)
      ((x ^ 2 - 1) * ProbabilityTheory.gaussianPDFReal 0 1 x) x := by
  convert (hasDerivAt_id x).neg.mul (hasDerivAt_standardNormalPDF x) using 1 <;>
    simp <;> ring

theorem contDiff_three_standardNormalCDF :
    ContDiff ℝ 3
      (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  rw [show (3 : WithTop ℕ∞) = 2 + 1 by norm_num, contDiff_succ_iff_deriv]
  refine ⟨fun x => (hasDerivAt_standardNormalCDF x).differentiableAt, by norm_num, ?_⟩
  have hderiv :
      deriv (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
        ProbabilityTheory.gaussianPDFReal 0 1 := by
    funext x
    exact (hasDerivAt_standardNormalCDF x).deriv
  rw [hderiv, ProbabilityTheory.gaussianPDFReal_def]
  fun_prop

theorem iteratedDeriv_three_standardNormalCDF (x : ℝ) :
    iteratedDeriv 3
        (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x =
      (x ^ 2 - 1) * ProbabilityTheory.gaussianPDFReal 0 1 x := by
  rw [show 3 = 2 + 1 by norm_num, iteratedDeriv_succ']
  rw [show 2 = 1 + 1 by norm_num, iteratedDeriv_succ]
  rw [show 1 = 0 + 1 by norm_num, iteratedDeriv_succ']
  simp only [iteratedDeriv_zero]
  have hderivCDF :
      deriv (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
        ProbabilityTheory.gaussianPDFReal 0 1 := by
    funext y
    exact (hasDerivAt_standardNormalCDF y).deriv
  rw [hderivCDF]
  have hderivPDF :
      deriv (ProbabilityTheory.gaussianPDFReal 0 1) =
        fun y => -y * ProbabilityTheory.gaussianPDFReal 0 1 y := by
    funext y
    exact (hasDerivAt_standardNormalPDF y).deriv
  rw [hderivPDF]
  exact (hasDerivAt_standardNormalPDF_deriv x).deriv

theorem sq_mul_exp_neg_half_le_two (x : ℝ) :
    x ^ 2 * Real.exp (-(x ^ 2) / 2) ≤ 2 := by
  have hmain := Real.mul_exp_neg_le_exp_neg_one (x ^ 2 / 2)
  have hone : Real.exp (-1) ≤ 1 := Real.exp_le_one_iff.mpr (by norm_num)
  have h := hmain.trans hone
  have hexp : Real.exp (-(x ^ 2) / 2) = Real.exp (-(x ^ 2 / 2)) := by
    congr 1
    ring
  rw [hexp]
  nlinarith

theorem abs_iteratedDeriv_three_standardNormalCDF_le (x : ℝ) :
    |iteratedDeriv 3
        (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x| ≤
      3 * (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [iteratedDeriv_three_standardNormalCDF,
    ProbabilityTheory.gaussianPDFReal_def]
  simp only [NNReal.coe_one, mul_one, sub_zero]
  have hC : 0 ≤ (Real.sqrt (2 * Real.pi))⁻¹ :=
    inv_nonneg.mpr (Real.sqrt_nonneg _)
  have habs : |x ^ 2 - 1| ≤ x ^ 2 + 1 := by
    rw [abs_le]
    constructor <;> nlinarith [sq_nonneg x]
  have hexp : Real.exp (-(x ^ 2) / 2) ≤ 1 :=
    Real.exp_le_one_iff.mpr (by nlinarith [sq_nonneg x])
  have hpoly :
      |x ^ 2 - 1| * Real.exp (-(x ^ 2) / 2) ≤ 3 := by
    calc
      |x ^ 2 - 1| * Real.exp (-(x ^ 2) / 2) ≤
          (x ^ 2 + 1) * Real.exp (-(x ^ 2) / 2) :=
        mul_le_mul_of_nonneg_right habs (Real.exp_pos _).le
      _ = x ^ 2 * Real.exp (-(x ^ 2) / 2) +
          Real.exp (-(x ^ 2) / 2) := by ring
      _ ≤ 2 + 1 := add_le_add (sq_mul_exp_neg_half_le_two x) hexp
      _ = 3 := by norm_num
  rw [abs_mul, abs_mul, abs_of_nonneg hC, abs_of_pos (Real.exp_pos _)]
  nlinarith [mul_le_mul_of_nonneg_left hpoly hC]

/-- The standard-normal density is twice continuously differentiable. -/
theorem contDiff_two_standardNormalPDF :
    ContDiff ℝ 2 (ProbabilityTheory.gaussianPDFReal 0 1) := by
  rw [ProbabilityTheory.gaussianPDFReal_def]
  fun_prop

theorem iteratedDeriv_one_affine_standardNormalPDF (a y t : ℝ) :
    iteratedDeriv 1
        (fun u => ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u)) t =
      y * (-(a + y * t) *
        ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t)) := by
  rw [show 1 = 0 + 1 by norm_num, iteratedDeriv_succ', iteratedDeriv_zero]
  have hinner : HasDerivAt (fun u : ℝ => a + y * u) y t := by
    convert (hasDerivAt_const t a).add ((hasDerivAt_id t).const_mul y) using 1 <;>
      simp <;> ring
  convert ((hasDerivAt_standardNormalPDF (a + y * t)).comp t hinner).deriv using 1 <;>
    ring

theorem iteratedDeriv_two_affine_standardNormalPDF (a y t : ℝ) :
    iteratedDeriv 2
        (fun u => ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u)) t =
      y ^ 2 * (((a + y * t) ^ 2 - 1) *
        ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t)) := by
  rw [show 2 = 1 + 1 by norm_num, iteratedDeriv_succ]
  have hfirst :
      iteratedDeriv 1
          (fun u => ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u)) =
        fun u => y * (-(a + y * u) *
          ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u)) := by
    funext u
    exact iteratedDeriv_one_affine_standardNormalPDF a y u
  rw [hfirst]
  have hinner : HasDerivAt (fun u : ℝ => a + y * u) y t := by
    convert (hasDerivAt_const t a).add ((hasDerivAt_id t).const_mul y) using 1 <;>
      simp <;> ring
  have hpdf :=
    (hasDerivAt_standardNormalPDF_deriv (a + y * t)).comp t hinner
  convert hpdf.const_mul y |>.deriv using 1 <;> ring

theorem abs_iteratedDeriv_two_affine_standardNormalPDF_le (a y t : ℝ) :
    |iteratedDeriv 2
        (fun u => ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u)) t| ≤
      (3 * (Real.sqrt (2 * Real.pi))⁻¹) * |y| ^ 2 := by
  rw [iteratedDeriv_two_affine_standardNormalPDF, abs_mul, abs_pow]
  have h := abs_iteratedDeriv_three_standardNormalCDF_le (a + y * t)
  rw [iteratedDeriv_three_standardNormalCDF] at h
  nlinarith [mul_le_mul_of_nonneg_left h (pow_nonneg (abs_nonneg y) 2)]

theorem taylorWithinEval_one_affine_standardNormalPDF (a y : ℝ) :
    taylorWithinEval
        (fun t => ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t))
        1 (Icc 0 1) 0 1 =
      ProbabilityTheory.gaussianPDFReal 0 1 a +
        y * (-a * ProbabilityTheory.gaussianPDFReal 0 1 a) := by
  let g : ℝ → ℝ := fun t =>
    ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t)
  have hg : ContDiff ℝ 2 g := contDiff_two_standardNormalPDF.comp (by fun_prop)
  have hu : UniqueDiffOn ℝ (Icc (0 : ℝ) 1) := uniqueDiffOn_Icc zero_lt_one
  have hzero : (0 : ℝ) ∈ Icc (0 : ℝ) 1 := by norm_num
  have hwithin0 : iteratedDerivWithin 0 g (Icc 0 1) 0 = iteratedDeriv 0 g 0 :=
    iteratedDerivWithin_eq_iteratedDeriv hu (hg.of_le (by norm_num)).contDiffAt hzero
  have hwithin1 : iteratedDerivWithin 1 g (Icc 0 1) 0 = iteratedDeriv 1 g 0 :=
    iteratedDerivWithin_eq_iteratedDeriv hu (hg.of_le (by norm_num)).contDiffAt hzero
  change taylorWithinEval g 1 (Icc 0 1) 0 1 = _
  rw [taylor_within_apply]
  simp [Finset.sum_range_succ, hwithin0, hwithin1, iteratedDeriv_zero,
    g, iteratedDeriv_one_affine_standardNormalPDF, Nat.factorial]

/-- The standard-normal density admits a linear Taylor polynomial with the
uniform second-order remainder needed to exploit two signed spatial
cancellations. -/
theorem standardNormalPDF_linear_remainder (a y : ℝ) :
    |ProbabilityTheory.gaussianPDFReal 0 1 (a + y) -
        (ProbabilityTheory.gaussianPDFReal 0 1 a +
          y * (-a * ProbabilityTheory.gaussianPDFReal 0 1 a))| ≤
      ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) * |y| ^ 2 := by
  let g : ℝ → ℝ := fun t =>
    ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t)
  have hg : ContDiff ℝ 2 g := contDiff_two_standardNormalPDF.comp (by fun_prop)
  obtain ⟨t, _ht, hrem⟩ :=
    taylor_mean_remainder_lagrange_iteratedDeriv
      (f := g) (n := 1) (x₀ := 0) (x := 1) zero_lt_one hg.contDiffOn
  have hpoly := taylorWithinEval_one_affine_standardNormalPDF a y
  have hbound := abs_iteratedDeriv_two_affine_standardNormalPDF_le a y t
  have hdiv : |iteratedDeriv 2 g t| / 2 ≤
      ((3 * (Real.sqrt (2 * Real.pi))⁻¹) * |y| ^ 2) / 2 :=
    div_le_div_of_nonneg_right hbound (by norm_num)
  calc
    |ProbabilityTheory.gaussianPDFReal 0 1 (a + y) -
        (ProbabilityTheory.gaussianPDFReal 0 1 a +
          y * (-a * ProbabilityTheory.gaussianPDFReal 0 1 a))| =
        |g 1 - taylorWithinEval g 1 (Icc 0 1) 0 1| := by
      rw [hpoly]
      simp only [g, mul_one]
    _ = |iteratedDeriv 2 g t| / 2 := by
      rw [hrem]
      norm_num [Nat.factorial]
      rw [abs_div, abs_mul]
      change |iteratedDeriv 2 g t| * |(1 : ℝ)| / |(2 : ℝ)| =
        |iteratedDeriv 2 g t| / 2
      simp
    _ ≤ ((3 * (Real.sqrt (2 * Real.pi))⁻¹) * |y| ^ 2) / 2 := hdiv
    _ = ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) * |y| ^ 2 := by ring

/-- The linear density remainder after scaling the standard normal by a
positive standard deviation.  The second-order coefficient scales exactly
as the inverse cube of the standard deviation. -/
theorem scaledStandardNormalPDF_linear_remainder
    {σ : ℝ} (hσ : 0 < σ) (a y : ℝ) :
    |σ⁻¹ *
      (ProbabilityTheory.gaussianPDFReal 0 1 ((a + y) / σ) -
        (ProbabilityTheory.gaussianPDFReal 0 1 (a / σ) +
          (y / σ) * (-(a / σ) *
            ProbabilityTheory.gaussianPDFReal 0 1 (a / σ))))| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3) * |y| ^ 2 := by
  have hrem := standardNormalPDF_linear_remainder (a / σ) (y / σ)
  rw [show a / σ + y / σ = (a + y) / σ by ring] at hrem
  rw [abs_mul, abs_of_pos (inv_pos.mpr hσ)]
  calc
    σ⁻¹ *
        |ProbabilityTheory.gaussianPDFReal 0 1 ((a + y) / σ) -
          (ProbabilityTheory.gaussianPDFReal 0 1 (a / σ) +
            (y / σ) * (-(a / σ) *
              ProbabilityTheory.gaussianPDFReal 0 1 (a / σ)))| ≤
        σ⁻¹ * (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
          |y / σ| ^ 2) :=
      mul_le_mul_of_nonneg_left hrem (inv_nonneg.mpr hσ.le)
    _ = (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3) *
        |y| ^ 2 := by
      rw [abs_div, abs_of_pos hσ]
      field_simp

/-- A positive scaled-normal density converts two exact spatial
cancellations into a second-order weighted `L¹ -> L∞` estimate. -/
theorem abs_integral_scaledStandardNormalPDF_mul_le_of_cancellations
    (e : ℝ → ℝ) (he : Integrable e)
    (he1 : Integrable (fun z => z * e z))
    (he2 : Integrable (fun z => |z| ^ 2 * |e z|))
    (hzero : (∫ z, e z) = 0)
    (hone : (∫ z, z * e z) = 0)
    {σ : ℝ} (hσ : 0 < σ) (x : ℝ) :
    |∫ z, σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ) * e z| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3) *
        ∫ z, |z| ^ 2 * |e z| := by
  let K : ℝ → ℝ := fun z =>
    σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ)
  let Q : ℝ → ℝ := fun z => σ⁻¹ *
    (ProbabilityTheory.gaussianPDFReal 0 1 (x / σ) +
      (-z / σ) * (-(x / σ) *
        ProbabilityTheory.gaussianPDFReal 0 1 (x / σ)))
  let C : ℝ := ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3
  let A : ℝ := σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 (x / σ)
  let D : ℝ := σ⁻¹ * σ⁻¹ * (x / σ) *
    ProbabilityTheory.gaussianPDFReal 0 1 (x / σ)
  have hC : 0 ≤ C := by
    dsimp only [C]
    positivity
  have hrem (z : ℝ) : |K z - Q z| ≤ C * |z| ^ 2 := by
    have h := scaledStandardNormalPDF_linear_remainder hσ x (-z)
    dsimp only [K, Q, C]
    calc
      |σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ) -
          σ⁻¹ * (ProbabilityTheory.gaussianPDFReal 0 1 (x / σ) +
            (-z / σ) * (-(x / σ) *
              ProbabilityTheory.gaussianPDFReal 0 1 (x / σ)))| =
          |σ⁻¹ *
            (ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ) -
              (ProbabilityTheory.gaussianPDFReal 0 1 (x / σ) +
                (-z / σ) * (-(x / σ) *
                  ProbabilityTheory.gaussianPDFReal 0 1 (x / σ))))| := by
        congr 1
        ring
      _ ≤ (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3) *
          |z| ^ 2 := by
        simpa only [sub_eq_add_neg, add_neg_cancel_right, neg_div,
          abs_neg] using h
  have hQfun : (fun z => Q z * e z) =
      fun z => A * e z + D * (z * e z) := by
    funext z
    dsimp only [Q, A, D]
    ring
  have hQProd : Integrable (fun z => Q z * e z) := by
    rw [hQfun]
    exact (he.const_mul A).add (he1.const_mul D)
  have hQInt : (∫ z, Q z * e z) = 0 := by
    rw [hQfun, integral_add (he.const_mul A) (he1.const_mul D),
      integral_const_mul, integral_const_mul, hzero, hone]
    ring
  have hmajor : Integrable (fun z => C * (|z| ^ 2 * |e z|)) :=
    he2.const_mul C
  have hRProd : Integrable (fun z => (K z - Q z) * e z) := by
    apply hmajor.mono' (by fun_prop)
    exact ae_of_all _ fun z => by
      change ‖(K z - Q z) * e z‖ ≤ C * (|z| ^ 2 * |e z|)
      rw [Real.norm_eq_abs, abs_mul]
      simpa only [mul_assoc] using
        mul_le_mul_of_nonneg_right (hrem z) (abs_nonneg (e z))
  have hKfun : (fun z => K z * e z) =
      fun z => (K z - Q z) * e z + Q z * e z := by
    funext z
    ring
  calc
    |∫ z, σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ) * e z| =
        |∫ z, K z * e z| := by rfl
    _ = |(∫ z, (K z - Q z) * e z) + ∫ z, Q z * e z| := by
      rw [hKfun, integral_add hRProd hQProd]
    _ = |∫ z, (K z - Q z) * e z| := by rw [hQInt, add_zero]
    _ ≤ ∫ z, |(K z - Q z) * e z| := by
      simpa only [Real.norm_eq_abs] using
        (norm_integral_le_integral_norm (fun z => (K z - Q z) * e z))
    _ ≤ ∫ z, C * (|z| ^ 2 * |e z|) := by
      apply integral_mono hRProd.norm hmajor
      intro z
      change ‖(K z - Q z) * e z‖ ≤ C * (|z| ^ 2 * |e z|)
      rw [Real.norm_eq_abs, abs_mul]
      simpa only [mul_assoc] using
        mul_le_mul_of_nonneg_right (hrem z) (abs_nonneg (e z))
    _ = C * ∫ z, |z| ^ 2 * |e z| := by rw [integral_const_mul]

/-- Change of variables from averaging against a positively scaled standard
normal law to the corresponding translated scaled-density convolution. -/
theorem integral_comp_sub_map_mul_standardNormalLaw_eq_integral_scaledPDF
    (e : ℝ → ℝ) (he : Measurable e) {σ : ℝ} (hσ : 0 < σ) (x : ℝ) :
    (∫ y, e (x - y) ∂Measure.map (fun z : ℝ => σ * z)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
      ∫ z, σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ) * e z := by
  have hfm : AEStronglyMeasurable (fun y : ℝ => e (x - y))
      (Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    (he.comp (measurable_const.sub measurable_id)).aestronglyMeasurable
  have hmap := MeasureTheory.integral_map
    (μ := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
    (φ := fun z : ℝ => σ * z) (f := fun y : ℝ => e (x - y))
    (by fun_prop) hfm
  rw [hmap,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
    ProbabilityTheory.integral_gaussianReal_eq_integral_smul
      (by norm_num : (1 : ℝ≥0) ≠ 0)]
  simp only [smul_eq_mul]
  let F : ℝ → ℝ := fun z =>
    σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 ((x - z) / σ) * e z
  let G : ℝ → ℝ := fun y =>
    σ⁻¹ * ProbabilityTheory.gaussianPDFReal 0 1 (y / σ) * e (x - y)
  have htranslate : (∫ z, F z) = ∫ y, G y := by
    calc
      (∫ z, F z) = ∫ z, F (z + x) :=
        (MeasureTheory.integral_add_right_eq_self F x).symm
      _ = ∫ z, F (-z + x) := by
        rw [MeasureTheory.integral_neg_eq_self (fun z => F (z + x)) volume]
      _ = ∫ y, G y := by
        apply integral_congr_ae
        exact ae_of_all _ fun y => by
          dsimp only [F, G]
          rw [show x - (-y + x) = y by ring,
            show -y + x = x - y by ring]
  have hscale : (∫ y, G y) =
      ∫ y, ProbabilityTheory.gaussianPDFReal 0 1 y * e (x - σ * y) := by
    have hcomp := Measure.integral_comp_mul_left G σ
    have habs : |σ⁻¹| = σ⁻¹ := abs_of_pos (inv_pos.mpr hσ)
    rw [habs, smul_eq_mul] at hcomp
    calc
      (∫ y, G y) = σ * (σ⁻¹ * ∫ y, G y) := by
        field_simp
      _ = σ * ∫ y, G (σ * y) := by rw [hcomp]
      _ = ∫ y, σ * G (σ * y) := by rw [integral_const_mul]
      _ = ∫ y, ProbabilityTheory.gaussianPDFReal 0 1 y *
          e (x - σ * y) := by
        apply integral_congr_ae
        exact ae_of_all _ fun y => by
          dsimp only [G]
          field_simp
  exact hscale.symm.trans htranslate.symm

theorem iteratedDeriv_one_affine_standardNormalCDF (a y t : ℝ) :
    iteratedDeriv 1
        (fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * u)) t =
      y * ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t) := by
  rw [show 1 = 0 + 1 by norm_num, iteratedDeriv_succ', iteratedDeriv_zero]
  have hinner : HasDerivAt (fun u : ℝ => a + y * u) y t := by
    convert (hasDerivAt_const t a).add ((hasDerivAt_id t).const_mul y) using 1 <;>
      simp <;> ring
  convert ((hasDerivAt_standardNormalCDF (a + y * t)).comp t hinner).deriv using 1 <;>
    ring

theorem iteratedDeriv_two_affine_standardNormalCDF (a y t : ℝ) :
    iteratedDeriv 2
        (fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * u)) t =
      y ^ 2 * (-(a + y * t) *
        ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t)) := by
  rw [show 2 = 1 + 1 by norm_num, iteratedDeriv_succ]
  have hfirst :
      iteratedDeriv 1
          (fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * u)) =
        fun u => y * ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u) := by
    funext u
    exact iteratedDeriv_one_affine_standardNormalCDF a y u
  rw [hfirst]
  have hinner : HasDerivAt (fun u : ℝ => a + y * u) y t := by
    convert (hasDerivAt_const t a).add ((hasDerivAt_id t).const_mul y) using 1 <;>
      simp <;> ring
  have hpdf := (hasDerivAt_standardNormalPDF (a + y * t)).comp t hinner
  convert hpdf.const_mul y |>.deriv using 1 <;> ring

theorem iteratedDeriv_three_affine_standardNormalCDF (a y t : ℝ) :
    iteratedDeriv 3
        (fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * u)) t =
      y ^ 3 * (((a + y * t) ^ 2 - 1) *
        ProbabilityTheory.gaussianPDFReal 0 1 (a + y * t)) := by
  rw [show 3 = 2 + 1 by norm_num, iteratedDeriv_succ]
  have hsecond :
      iteratedDeriv 2
          (fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * u)) =
        fun u => y ^ 2 * (-(a + y * u) *
          ProbabilityTheory.gaussianPDFReal 0 1 (a + y * u)) := by
    funext u
    exact iteratedDeriv_two_affine_standardNormalCDF a y u
  rw [hsecond]
  have hinner : HasDerivAt (fun u : ℝ => a + y * u) y t := by
    convert (hasDerivAt_const t a).add ((hasDerivAt_id t).const_mul y) using 1 <;>
      simp <;> ring
  have hpdf :=
    (hasDerivAt_standardNormalPDF_deriv (a + y * t)).comp t hinner
  convert hpdf.const_mul (y ^ 2) |>.deriv using 1 <;> ring

theorem abs_iteratedDeriv_three_affine_standardNormalCDF_le (a y t : ℝ) :
    |iteratedDeriv 3
        (fun u => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * u)) t| ≤
      (3 * (Real.sqrt (2 * Real.pi))⁻¹) * |y| ^ 3 := by
  rw [iteratedDeriv_three_affine_standardNormalCDF, abs_mul, abs_pow]
  have h := abs_iteratedDeriv_three_standardNormalCDF_le (a + y * t)
  rw [iteratedDeriv_three_standardNormalCDF] at h
  nlinarith [mul_le_mul_of_nonneg_left h (pow_nonneg (abs_nonneg y) 3)]

theorem taylorWithinEval_two_affine_standardNormalCDF (a y : ℝ) :
    taylorWithinEval
        (fun t => cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * t))
        2 (Icc 0 1) 0 1 =
      cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a +
        y * ProbabilityTheory.gaussianPDFReal 0 1 a +
        (y ^ 2 / 2) * (-a * ProbabilityTheory.gaussianPDFReal 0 1 a) := by
  let g : ℝ → ℝ := fun t =>
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * t)
  have hg : ContDiff ℝ 3 g :=
    contDiff_three_standardNormalCDF.comp
      (by fun_prop)
  have hu : UniqueDiffOn ℝ (Icc (0 : ℝ) 1) := uniqueDiffOn_Icc zero_lt_one
  have hzero : (0 : ℝ) ∈ Icc (0 : ℝ) 1 := by norm_num
  have hwithin0 : iteratedDerivWithin 0 g (Icc 0 1) 0 = iteratedDeriv 0 g 0 :=
    iteratedDerivWithin_eq_iteratedDeriv hu (hg.of_le (by norm_num)).contDiffAt hzero
  have hwithin1 : iteratedDerivWithin 1 g (Icc 0 1) 0 = iteratedDeriv 1 g 0 :=
    iteratedDerivWithin_eq_iteratedDeriv hu (hg.of_le (by norm_num)).contDiffAt hzero
  have hwithin2 : iteratedDerivWithin 2 g (Icc 0 1) 0 = iteratedDeriv 2 g 0 :=
    iteratedDerivWithin_eq_iteratedDeriv hu (hg.of_le (by norm_num)).contDiffAt hzero
  change taylorWithinEval g 2 (Icc 0 1) 0 1 = _
  rw [taylor_within_apply]
  simp [Finset.sum_range_succ, hwithin0, hwithin1, hwithin2, iteratedDeriv_zero,
    g, iteratedDeriv_one_affine_standardNormalCDF,
    iteratedDeriv_two_affine_standardNormalCDF, Nat.factorial]
  ring_nf

/-- The standard-normal CDF admits a quadratic Taylor polynomial with a
uniform cubic remainder.  Parameterizing the displacement on `[0,1]` makes
the estimate valid for positive and negative `y` without separate endpoint
arguments. -/
theorem standardNormalCDF_quadratic_remainder (a y : ℝ) :
    |cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y) -
        (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a +
          y * ProbabilityTheory.gaussianPDFReal 0 1 a +
          (y ^ 2 / 2) * (-a * ProbabilityTheory.gaussianPDFReal 0 1 a))| ≤
      ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 6) * |y| ^ 3 := by
  let g : ℝ → ℝ := fun t =>
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y * t)
  have hg : ContDiff ℝ 3 g :=
    contDiff_three_standardNormalCDF.comp (by fun_prop)
  obtain ⟨t, _ht, hrem⟩ :=
    taylor_mean_remainder_lagrange_iteratedDeriv
      (f := g) (n := 2) (x₀ := 0) (x := 1) zero_lt_one hg.contDiffOn
  have hpoly := taylorWithinEval_two_affine_standardNormalCDF a y
  have hbound := abs_iteratedDeriv_three_affine_standardNormalCDF_le a y t
  have hdiv : |iteratedDeriv 3 g t| / 6 ≤
      ((3 * (Real.sqrt (2 * Real.pi))⁻¹) * |y| ^ 3) / 6 :=
    div_le_div_of_nonneg_right hbound (by norm_num)
  calc
    |cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (a + y) -
        (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a +
          y * ProbabilityTheory.gaussianPDFReal 0 1 a +
          (y ^ 2 / 2) * (-a * ProbabilityTheory.gaussianPDFReal 0 1 a))| =
        |g 1 - taylorWithinEval g 2 (Icc 0 1) 0 1| := by
      rw [hpoly]
      simp only [g, mul_one]
    _ = |iteratedDeriv 3 g t| / 6 := by
      rw [hrem]
      norm_num [Nat.factorial]
      rw [abs_div, abs_mul]
      change |iteratedDeriv 3 g t| * |(1 : ℝ)| / |(6 : ℝ)| =
        |iteratedDeriv 3 g t| / 6
      simp
    _ ≤ ((3 * (Real.sqrt (2 * Real.pi))⁻¹) * |y| ^ 3) / 6 := hdiv
    _ = ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 6) * |y| ^ 3 := by ring

/-- The standard-normal CDF remainder transported through a positive scalar
pushforward. -/
theorem scaledStandardNormalCDF_quadratic_remainder
    {σ : ℝ} (hσ : 0 < σ) (a y : ℝ) :
    |cdf (Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a + y) -
        (cdf (Measure.map (fun z : ℝ => σ * z)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) a +
          (y / σ) * ProbabilityTheory.gaussianPDFReal 0 1 (a / σ) +
          ((y / σ) ^ 2 / 2) *
            (-(a / σ) * ProbabilityTheory.gaussianPDFReal 0 1 (a / σ)))| ≤
      ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 6) * |y / σ| ^ 3 := by
  rw [cdf_map_mul NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hσ,
    cdf_map_mul NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hσ]
  convert standardNormalCDF_quadratic_remainder (a / σ) (y / σ) using 1 <;>
    field_simp [hσ.ne'] <;> ring

/-- Canonical-polynomial form of the scaled standard-normal CDF remainder.
The cubic constant scales as the inverse cube of the smoothing standard
deviation. -/
theorem scaledStandardNormalCDF_quadratic_remainder_canonical
    {σ : ℝ} (hσ : 0 < σ) (a y : ℝ) :
    |cdf (Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a + y) -
        (cdf (Measure.map (fun z : ℝ => σ * z)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) a +
          (ProbabilityTheory.gaussianPDFReal 0 1 (a / σ) / σ) * y +
          ((-(a / σ) * ProbabilityTheory.gaussianPDFReal 0 1 (a / σ)) /
            (2 * σ ^ 2)) * y ^ 2)| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 := by
  have h := scaledStandardNormalCDF_quadratic_remainder hσ a y
  convert h using 1
  · field_simp [hσ.ne'] <;> ring
  · rw [abs_div, abs_of_pos hσ]
    field_simp [hσ.ne']

theorem standardNormalPDF_le (x : ℝ) :
    ProbabilityTheory.gaussianPDFReal 0 1 x ≤
      (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
  change (Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(x ^ 2) / 2) ≤
    (Real.sqrt (2 * Real.pi))⁻¹
  have hexp : Real.exp (-(x ^ 2) / 2) ≤ 1 :=
    Real.exp_le_one_iff.mpr
      (div_nonpos_of_nonpos_of_nonneg (neg_nonpos.mpr (sq_nonneg x)) (by norm_num))
  simpa only [mul_one] using mul_le_mul_of_nonneg_left hexp
    (inv_nonneg.mpr (Real.sqrt_nonneg (2 * Real.pi)))

theorem abs_standardNormalPDF_deriv_le (x : ℝ) :
    |-x * ProbabilityTheory.gaussianPDFReal 0 1 x| ≤
      2 * (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
  have hC : 0 ≤ (Real.sqrt (2 * Real.pi))⁻¹ :=
    inv_nonneg.mpr (Real.sqrt_nonneg _)
  have hexp : Real.exp (-(x ^ 2) / 2) ≤ 1 :=
    Real.exp_le_one_iff.mpr (by nlinarith [sq_nonneg x])
  have hxexp : |x| * Real.exp (-(x ^ 2) / 2) ≤ 2 := by
    by_cases hx : |x| ≤ 1
    · nlinarith [Real.exp_pos (-(x ^ 2) / 2)]
    · have hxle : |x| ≤ x ^ 2 := by
        rw [← sq_abs]
        nlinarith [abs_nonneg x]
      calc
        |x| * Real.exp (-(x ^ 2) / 2) ≤
            x ^ 2 * Real.exp (-(x ^ 2) / 2) :=
          mul_le_mul_of_nonneg_right hxle (Real.exp_pos _).le
        _ ≤ 2 := sq_mul_exp_neg_half_le_two x
  rw [abs_mul, abs_mul, abs_of_nonneg hC, abs_of_pos (Real.exp_pos _), abs_neg]
  nlinarith [mul_le_mul_of_nonneg_left hxexp hC]

/-- The standard-normal CDF has the sharp global density Lipschitz constant.
This is the target regularity term used by Esseen smoothing. -/
theorem standardNormalCDF_sub_le
    {a b : ℝ} (hab : a ≤ b) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw b -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * (b - a) := by
  open NumStability.HDP.Scalar.LimitTheorems in
  rw [cdf_eq_real, cdf_eq_real]
  have hsubset : Iic a ⊆ Iic b := Iic_subset_Iic.mpr hab
  rw [← measureReal_diff hsubset measurableSet_Iic]
  have hdiff : Iic b \ Iic a = Ioc a b := by
    ext x
    simp only [mem_diff, mem_Iic, mem_Ioc, not_le]
    tauto
  rw [hdiff, standardNormalLaw, Measure.real_def,
    gaussianReal_apply_eq_integral 0 (by norm_num : (1 : NNReal) ≠ 0) (Ioc a b)]
  rw [ENNReal.toReal_ofReal
    (integral_nonneg fun x => gaussianPDFReal_nonneg 0 1 x)]
  rw [standardNormalLaw_pdf]
  have hpdf : Integrable (fun x : ℝ =>
      (Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(x ^ 2) / 2)) volume := by
    rw [← standardNormalLaw_pdf]
    exact ProbabilityTheory.integrable_gaussianPDFReal 0 1
  calc
    (∫ x in Ioc a b,
        (Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(x ^ 2) / 2)) ≤
        ∫ _x in Ioc a b, (Real.sqrt (2 * Real.pi))⁻¹ := by
      apply setIntegral_mono_on
        hpdf.integrableOn (integrableOn_const measure_Ioc_lt_top.ne) measurableSet_Ioc
      intro x _
      have hexp : Real.exp (-(x ^ 2) / 2) ≤ 1 :=
        Real.exp_le_one_iff.mpr (by nlinarith [sq_nonneg x])
      simpa only [mul_one] using mul_le_mul_of_nonneg_left hexp
        (inv_nonneg.mpr (Real.sqrt_nonneg (2 * Real.pi)))
    _ = (Real.sqrt (2 * Real.pi))⁻¹ * (b - a) := by
      simp [MeasureTheory.integral_const, hab]
      ring

theorem standardNormalCDF_lipschitz (a b : ℝ) :
    |cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw b -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw a| ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * |b - a| := by
  rcases le_total a b with hab | hba
  · rw [abs_of_nonneg (sub_nonneg.mpr <|
      monotone_cdf
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hab),
      abs_of_nonneg (sub_nonneg.mpr hab)]
    exact standardNormalCDF_sub_le hab
  · rw [abs_of_nonpos (sub_nonpos.mpr <|
      monotone_cdf
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hba),
      abs_of_nonpos (sub_nonpos.mpr hba)]
    simpa only [neg_sub] using standardNormalCDF_sub_le hba

/-- Probability of a half-open interval as a CDF increment. -/
theorem measureReal_Ioc_eq_cdf_sub
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {a b : ℝ} (hab : a ≤ b) :
    mu.real (Ioc a b) = cdf mu b - cdf mu a := by
  conv_lhs => rw [← ProbabilityTheory.measure_cdf mu]
  rw [Measure.real_def,
    StieltjesFunction.measure_Ioc, ENNReal.toReal_ofReal]
  exact sub_nonneg.mpr (monotone_cdf mu hab)

/-- Every interval probability is bounded by its standard-normal length term
plus twice the Kolmogorov discrepancy. -/
theorem measureReal_Ioc_le_standardNormal_length_add_kolmogorov
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {a b : ℝ} (hab : a ≤ b) :
    mu.real (Ioc a b) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * (b - a) +
        2 * kolmogorovDistance mu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let normal : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let D : ℝ := kolmogorovDistance mu normal
  have ha := cdfGap_le_kolmogorovDistance mu normal a
  have hb := cdfGap_le_kolmogorovDistance mu normal b
  have hnormal := standardNormalCDF_lipschitz a b
  rw [cdfGap] at ha hb
  have ha' : cdf normal a - cdf mu a ≤ D := by
    calc
      cdf normal a - cdf mu a ≤ |cdf normal a - cdf mu a| := le_abs_self _
      _ = |cdf mu a - cdf normal a| := abs_sub_comm _ _
      _ ≤ D := by simpa only [normal, D] using ha
  have hb' : cdf mu b - cdf normal b ≤ D := by
    calc
      cdf mu b - cdf normal b ≤ |cdf mu b - cdf normal b| := le_abs_self _
      _ ≤ D := by simpa only [normal, D] using hb
  have hnormal' : cdf normal b - cdf normal a ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * (b - a) :=
    (le_abs_self _).trans (by
      simpa only [normal, abs_of_nonneg (sub_nonneg.mpr hab)] using hnormal)
  rw [measureReal_Ioc_eq_cdf_sub mu hab]
  dsimp only [normal, D] at ha' hb' hnormal' ⊢
  linarith

/-- Lévy concentration function, using half-open intervals of length `h`. -/
noncomputable def concentrationFunction (mu : Measure ℝ) (h : ℝ) : ℝ :=
  sSup (Set.range fun x : ℝ => mu.real (Ioc x (x + h)))

theorem bddAbove_range_concentrationInterval
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (h : ℝ) :
    BddAbove (Set.range fun x : ℝ => mu.real (Ioc x (x + h))) := by
  refine ⟨1, ?_⟩
  rintro _ ⟨x, rfl⟩
  calc
    mu.real (Ioc x (x + h)) ≤ mu.real Set.univ :=
      measureReal_mono (subset_univ _)
    _ = 1 := by simp

theorem measureReal_Ioc_le_concentrationFunction
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (h x : ℝ) :
    mu.real (Ioc x (x + h)) ≤ concentrationFunction mu h := by
  apply le_csSup (bddAbove_range_concentrationInterval mu h)
  exact ⟨x, rfl⟩

/-- Interval mass under multiplication by a positive scalar, with the
endpoints divided by that scalar. -/
theorem measureReal_map_mul_Ioc
    (mu : Measure ℝ) {c : ℝ} (hc : 0 < c) (x h : ℝ) :
    (Measure.map (fun y : ℝ => c * y) mu).real (Ioc x (x + h)) =
      mu.real (Ioc (x / c) (x / c + h / c)) := by
  rw [map_measureReal_apply (by fun_prop) measurableSet_Ioc]
  congr 1
  ext y
  simp only [Set.mem_preimage, mem_Ioc]
  rw [← add_div]
  constructor
  · rintro ⟨hy₁, hy₂⟩
    constructor
    · rw [div_lt_iff₀ hc]
      simpa [mul_comm] using hy₁
    · rw [le_div_iff₀ hc]
      simpa [mul_comm] using hy₂
  · rintro ⟨hy₁, hy₂⟩
    constructor
    · rw [div_lt_iff₀ hc] at hy₁
      simpa [mul_comm] using hy₁
    · rw [le_div_iff₀ hc] at hy₂
      simpa [mul_comm] using hy₂

/-- Positive scaling transports the concentration function by dividing the
interval length by the scale.  The inequality direction is the one needed
for normalized partial sums. -/
theorem concentrationFunction_map_mul_le
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {c : ℝ} (hc : 0 < c) (h : ℝ) :
    concentrationFunction (Measure.map (fun y : ℝ => c * y) mu) h ≤
      concentrationFunction mu (h / c) := by
  letI : IsProbabilityMeasure (Measure.map (fun y : ℝ => c * y) mu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  change (Measure.map (fun y : ℝ => c * y) mu).real (Ioc x (x + h)) ≤ _
  rw [measureReal_map_mul_Ioc mu hc]
  exact measureReal_Ioc_le_concentrationFunction mu (h / c) (x / c)

/-- The concentration function transforms exactly under a positive scalar
map. -/
theorem concentrationFunction_map_mul_eq
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {c : ℝ} (hc : 0 < c) (h : ℝ) :
    concentrationFunction (Measure.map (fun y : ℝ => c * y) mu) h =
      concentrationFunction mu (h / c) := by
  apply le_antisymm (concentrationFunction_map_mul_le mu hc h)
  letI : IsProbabilityMeasure (Measure.map (fun y : ℝ => c * y) mu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  calc
    mu.real (Ioc x (x + h / c)) =
        (Measure.map (fun y : ℝ => c * y) mu).real
          (Ioc (c * x) (c * x + h)) := by
      rw [measureReal_map_mul_Ioc mu hc]
      simp [hc.ne']
    _ ≤ concentrationFunction (Measure.map (fun y : ℝ => c * y) mu) h :=
      measureReal_Ioc_le_concentrationFunction
        (Measure.map (fun y : ℝ => c * y) mu) h (c * x)

/-- The law of the first `N + 1` independent summands is the convolution of
the first `N`-summand law with the law of the last summand. -/
theorem map_sum_range_succ_eq_conv
    {Ω : Type*} [MeasurableSpace Ω]
    {mu : Measure Ω} [IsProbabilityMeasure mu]
    (X : ℕ → Ω → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) :
    Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu =
      Measure.map (∑ i ∈ Finset.range N, X i) mu ∗
        Measure.map (X N) mu := by
  have hi := hIndep.indepFun_sum_range_succ₀ hX N
  have hs : AEMeasurable (∑ i ∈ Finset.range N, X i) mu := by
    fun_prop
  have hm := hi.map_add_eq_map_conv_map₀ hs (hX N)
  rw [Finset.sum_range_succ]
  exact hm

/-- The normalized centered iid law is the positive scalar pushforward of
the corresponding unnormalized range-sum law. -/
theorem map_normalizedCenteredIidSum_eq_map_mul_sum_range
    {Ω : Type*} [MeasurableSpace Ω]
    {mu : Measure Ω} [IsProbabilityMeasure mu]
    (X : ℕ → Ω → ℝ) (hX : ∀ i, AEMeasurable (X i) mu) (N : ℕ) :
    Measure.map
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu =
      Measure.map (fun y : ℝ => (Real.sqrt (N + 1 : ℝ))⁻¹ * y)
        (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu) := by
  have hs : AEMeasurable (∑ i ∈ Finset.range (N + 1), X i) mu := by
    fun_prop
  have hc : AEMeasurable
      (fun y : ℝ => (Real.sqrt (N + 1 : ℝ))⁻¹ * y)
      (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu) := by
    fun_prop
  rw [hc.map_map_of_aemeasurable hs]
  congr 1
  funext ω
  simp only [NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum,
    Function.comp_apply]
  congr 1
  simpa only [Finset.sum_apply] using
    (Fin.sum_univ_eq_sum_range (fun i => X i ω) (N + 1))

/-- Standard-normal comparison bound for the Lévy concentration function. -/
theorem concentrationFunction_le_standardNormal_length_add_kolmogorov
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction mu h ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * h +
        2 * kolmogorovDistance mu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  simpa only [add_sub_cancel_left] using
    measureReal_Ioc_le_standardNormal_length_add_kolmogorov
      mu (show x ≤ x + h by linarith)

/-- Each fixed-displacement section of the Stein crossing set has mass at
most the concentration function at the exact displacement length. -/
theorem integral_standardNormalSteinCrossingSet_section_le_concentrationFunction
    (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (x scale v : ℝ) (hscale : 0 ≤ scale) :
    (∫ w, (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (w, v) ∂nu) ≤
      concentrationFunction nu (scale * |v|) := by
  by_cases hv0 : v = 0
  · subst v
    have h := measureReal_Ioc_le_concentrationFunction nu 0 x
    simpa [standardNormalSteinCrossingSet] using h
  · rcases lt_or_gt_of_ne hv0 with hv | hv
    · have hfun :
        (fun w => (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (w, v)) =
        (Ioc x (x - scale * v)).indicator (fun _ : ℝ => (1 : ℝ)) := by
        funext w
        simp [standardNormalSteinCrossingSet, Set.indicator_apply,
          hv.le, not_le_of_gt hv]
      rw [hfun]
      have hint :
          (∫ w, (Ioc x (x - scale * v)).indicator
            (fun _ : ℝ => (1 : ℝ)) w ∂nu) =
            nu.real (Ioc x (x - scale * v)) :=
        integral_indicator_one measurableSet_Ioc
      rw [hint]
      have h := measureReal_Ioc_le_concentrationFunction nu
        (scale * (-v)) x
      simpa [abs_of_neg hv] using h
    · have hfun :
        (fun w => (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (w, v)) =
        (Ioc (x - scale * v) x).indicator (fun _ : ℝ => (1 : ℝ)) := by
        funext w
        simp [standardNormalSteinCrossingSet, Set.indicator_apply,
          hv.le, not_le_of_gt hv]
      rw [hfun]
      have hint :
          (∫ w, (Ioc (x - scale * v) x).indicator
            (fun _ : ℝ => (1 : ℝ)) w ∂nu) =
            nu.real (Ioc (x - scale * v) x) :=
        integral_indicator_one measurableSet_Ioc
      rw [hint]
      have h := measureReal_Ioc_le_concentrationFunction nu
        (scale * v) (x - scale * v)
      simpa [abs_of_pos hv] using h

/-- Independence turns the random Stein crossing event into fixed interval
sections.  Standard-normal comparison then bounds its expectation by the
scaled first absolute moment plus twice the leave-one-out Kolmogorov gap. -/
theorem integral_standardNormalSteinCrossingSet_comp_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (Y X : Omega → ℝ) (hY : AEMeasurable Y mu) (hX : AEMeasurable X mu)
    (hIndep : IndepFun Y X mu)
    (hXfirst : Integrable (fun v : ℝ => |v|) (Measure.map X mu))
    (x scale : ℝ) (hscale : 0 ≤ scale) :
    (∫ omega, (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Y omega, X omega) ∂mu) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ v, |v| ∂Measure.map X mu) +
        2 * kolmogorovDistance (Measure.map Y mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let nuY : Measure ℝ := Measure.map Y mu
  let nuX : Measure ℝ := Measure.map X mu
  let D : ℝ := kolmogorovDistance nuY
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let F : ℝ × ℝ → ℝ := fun p =>
    (standardNormalSteinCrossingSet x scale).indicator
      (fun _ : ℝ × ℝ => (1 : ℝ)) (p.2, p.1)
  letI : IsProbabilityMeasure nuY := Measure.isProbabilityMeasure_map hY
  letI : IsProbabilityMeasure nuX := Measure.isProbabilityMeasure_map hX
  have hFmeas : Measurable F := by
    dsimp only [F]
    exact (measurable_const.indicator
      (measurableSet_standardNormalSteinCrossingSet x scale)).comp measurable_swap
  have hFint : Integrable F (nuX.prod nuY) := by
    apply Integrable.of_bound hFmeas.aestronglyMeasurable 1
    exact ae_of_all _ fun p => by
      dsimp only [F]
      by_cases hp : (p.2, p.1) ∈ standardNormalSteinCrossingSet x scale <;>
        simp [Set.indicator_apply, hp]
  have heq := integral_comp_pair_eq_integral_integral_of_indepFun
    X Y hX hY hIndep.symm F hFint
  have hleft : Integrable (fun v => ∫ w, F (v, w) ∂nuY) nuX :=
    hFint.integral_prod_left
  have hscaled : Integrable
      (fun v : ℝ => (Real.sqrt (2 * Real.pi))⁻¹ * (scale * |v|)) nuX :=
    hXfirst.const_mul scale |>.const_mul (Real.sqrt (2 * Real.pi))⁻¹
  have hmajor : Integrable
      (fun v : ℝ => (Real.sqrt (2 * Real.pi))⁻¹ * (scale * |v|) + 2 * D) nuX :=
    hscaled.add (integrable_const (2 * D))
  change (∫ omega, F (X omega, Y omega) ∂mu) ≤ _
  rw [heq]
  calc
    (∫ v, ∫ w, F (v, w) ∂nuY ∂nuX) ≤
        ∫ v, ((Real.sqrt (2 * Real.pi))⁻¹ * (scale * |v|) + 2 * D) ∂nuX := by
      apply integral_mono hleft hmajor
      intro v
      exact (integral_standardNormalSteinCrossingSet_section_le_concentrationFunction
        nuY x scale v hscale).trans
          (concentrationFunction_le_standardNormal_length_add_kolmogorov
            nuY (mul_nonneg hscale (abs_nonneg v)))
    _ = (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ v, |v| ∂nuX) + 2 * D := by
      rw [integral_add hscaled (integrable_const _),
        integral_const_mul, integral_const_mul, integral_const,
        probReal_univ, one_smul]
      ring
    _ = _ := by rfl

/-- Weighted independent-pair crossing estimate.  The quadratic weight turns
the interval-length contribution into a third absolute moment and retains a
second-moment factor on the Kolmogorov term. -/
theorem integral_sq_mul_standardNormalSteinCrossingSet_comp_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (Y X : Omega → ℝ) (hY : AEMeasurable Y mu) (hX : AEMeasurable X mu)
    (hIndep : IndepFun Y X mu)
    (hXsecond : Integrable (fun v : ℝ => v ^ 2) (Measure.map X mu))
    (hXthird : Integrable (fun v : ℝ => |v| ^ 3) (Measure.map X mu))
    (x scale : ℝ) (hscale : 0 ≤ scale) :
    (∫ omega, (X omega) ^ 2 *
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (Y omega, X omega) ∂mu) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ v, |v| ^ 3 ∂Measure.map X mu) +
        2 * kolmogorovDistance (Measure.map Y mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
          (∫ v, v ^ 2 ∂Measure.map X mu) := by
  let nuY : Measure ℝ := Measure.map Y mu
  let nuX : Measure ℝ := Measure.map X mu
  let D : ℝ := kolmogorovDistance nuY
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let F : ℝ × ℝ → ℝ := fun p => p.1 ^ 2 *
    (standardNormalSteinCrossingSet x scale).indicator
      (fun _ : ℝ × ℝ => (1 : ℝ)) (p.2, p.1)
  letI : IsProbabilityMeasure nuY := Measure.isProbabilityMeasure_map hY
  letI : IsProbabilityMeasure nuX := Measure.isProbabilityMeasure_map hX
  have hFmeas : Measurable F := by
    dsimp only [F]
    exact (measurable_fst.pow_const 2).mul
      ((measurable_const.indicator
        (measurableSet_standardNormalSteinCrossingSet x scale)).comp measurable_swap)
  have hFint : Integrable F (nuX.prod nuY) := by
    apply (hXsecond.comp_fst nuY).mono' hFmeas.aestronglyMeasurable
    exact ae_of_all _ fun p => by
      dsimp only [F]
      by_cases hp : (p.2, p.1) ∈ standardNormalSteinCrossingSet x scale <;>
        simp [hp, abs_of_nonneg (sq_nonneg p.1), sq_nonneg]
  have heq := integral_comp_pair_eq_integral_integral_of_indepFun
    X Y hX hY hIndep.symm F hFint
  have hleft : Integrable (fun v => ∫ w, F (v, w) ∂nuY) nuX :=
    hFint.integral_prod_left
  have hcubic : Integrable
      (fun v : ℝ => ((Real.sqrt (2 * Real.pi))⁻¹ * scale) * |v| ^ 3) nuX :=
    hXthird.const_mul ((Real.sqrt (2 * Real.pi))⁻¹ * scale)
  have hquadratic : Integrable (fun v : ℝ => (2 * D) * v ^ 2) nuX :=
    hXsecond.const_mul (2 * D)
  have hmajor : Integrable (fun v : ℝ =>
      ((Real.sqrt (2 * Real.pi))⁻¹ * scale) * |v| ^ 3 +
        (2 * D) * v ^ 2) nuX :=
    hcubic.add hquadratic
  change (∫ omega, F (X omega, Y omega) ∂mu) ≤ _
  rw [heq]
  calc
    (∫ v, ∫ w, F (v, w) ∂nuY ∂nuX) ≤
        ∫ v, (((Real.sqrt (2 * Real.pi))⁻¹ * scale) * |v| ^ 3 +
          (2 * D) * v ^ 2) ∂nuX := by
      apply integral_mono hleft hmajor
      intro v
      have hsection :=
        (integral_standardNormalSteinCrossingSet_section_le_concentrationFunction
          nuY x scale v hscale).trans
          (concentrationFunction_le_standardNormal_length_add_kolmogorov
            nuY (mul_nonneg hscale (abs_nonneg v)))
      calc
        (∫ w, F (v, w) ∂nuY) =
            v ^ 2 * ∫ w,
              (standardNormalSteinCrossingSet x scale).indicator
                (fun _ : ℝ × ℝ => (1 : ℝ)) (w, v) ∂nuY := by
          dsimp only [F]
          rw [integral_const_mul]
        _ ≤ v ^ 2 *
            ((Real.sqrt (2 * Real.pi))⁻¹ * (scale * |v|) + 2 * D) :=
          mul_le_mul_of_nonneg_left hsection (sq_nonneg v)
        _ = ((Real.sqrt (2 * Real.pi))⁻¹ * scale) * |v| ^ 3 +
            (2 * D) * v ^ 2 := by
          rw [← sq_abs v]
          ring
    _ = (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ v, |v| ^ 3 ∂nuX) +
        2 * D * (∫ v, v ^ 2 ∂nuX) := by
      rw [integral_add hcubic hquadratic, integral_const_mul,
        integral_const_mul]
    _ = _ := by rfl

/-- Leave-one-out specialization of the weighted crossing estimate under the
third-moment hypothesis used by Berry--Esseen. -/
theorem integral_sq_mul_standardNormalSteinCrossingSet_leaveOneOut_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    (∫ omega, (X i.1 omega) ^ 2 *
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ))
          (Wi omega, X i.1 omega) ∂mu) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
        2 * kolmogorovDistance (Measure.map Wi mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
          (∫ omega, (X i.1 omega) ^ 2 ∂mu) := by
  dsimp only
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX2 N i
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hbase2 : Integrable (fun omega => (X i.1 omega) ^ 2) mu :=
    ((hX i.1).mono_exponent (by norm_num : (2 : ENNReal) ≤ 3)).integrable_sq
  have hbase3 : Integrable (fun omega => |X i.1 omega| ^ 3) mu := by
    simpa only [Real.norm_eq_abs] using
      (hX i.1).integrable_norm_pow (by norm_num)
  have hlaw2 : Integrable (fun v : ℝ => v ^ 2)
      (Measure.map (X i.1) mu) := by
    rw [integrable_map_measure (by fun_prop) (hX i.1).aemeasurable]
    simpa only [Function.comp_apply] using hbase2
  have hlaw3 : Integrable (fun v : ℝ => |v| ^ 3)
      (Measure.map (X i.1) mu) := by
    rw [integrable_map_measure (by fun_prop) (hX i.1).aemeasurable]
    simpa only [Function.comp_apply] using hbase3
  have h := integral_sq_mul_standardNormalSteinCrossingSet_comp_le
    (normalizedCenteredIidLeaveOneOut X N i) (X i.1)
    hLeave.aemeasurable (hX i.1).aemeasurable hind.symm hlaw2 hlaw3 x
    (Real.sqrt (N + 1 : ℝ))⁻¹ (inv_nonneg.mpr (Real.sqrt_nonneg _))
  rw [integral_map (hX i.1).aemeasurable
    (show AEStronglyMeasurable (fun v : ℝ => v ^ 2)
      (Measure.map (X i.1) mu) by fun_prop)] at h
  rw [integral_map (hX i.1).aemeasurable
    (show AEStronglyMeasurable (fun v : ℝ => |v| ^ 3)
      (Measure.map (X i.1) mu) by fun_prop)] at h
  simpa only [Function.comp_apply] using h

/-- Independence factors the continuous cubic part of the normalized
summand interval remainder. -/
theorem integral_leaveOneOut_cubicDriftMajorant_eq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) :
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    (∫ omega, (|Wi omega| + C) *
        (scale ^ 3 * |X i.1 omega| ^ 3 / 2) ∂mu) =
      (scale ^ 3 / 2) * (∫ omega, |Wi omega| + C ∂mu) *
        (∫ omega, |X i.1 omega| ^ 3 ∂mu) := by
  dsimp only
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX2 N i
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hfact := hind.integral_comp_mul_comp
    (hX i.1).aemeasurable hLeave.aemeasurable
    (show AEStronglyMeasurable (fun v : ℝ => |v| ^ 3)
      (Measure.map (X i.1) mu) by fun_prop)
    (show AEStronglyMeasurable (fun w : ℝ =>
        |w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ))
      (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu) by fun_prop)
  have hfact' :
      (∫ omega, |X i.1 omega| ^ 3 *
        (|normalizedCenteredIidLeaveOneOut X N i omega| +
          Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) ∂mu) =
        (∫ omega, |X i.1 omega| ^ 3 ∂mu) *
          ∫ omega, |normalizedCenteredIidLeaveOneOut X N i omega| +
            Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu := by
    simpa only [Function.comp_apply, Pi.mul_apply] using hfact
  calc
    (∫ omega, (|normalizedCenteredIidLeaveOneOut X N i omega| +
          Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
        ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 3 * |X i.1 omega| ^ 3 / 2) ∂mu) =
      ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 3 / 2) *
        (∫ omega, |X i.1 omega| ^ 3 *
          (|normalizedCenteredIidLeaveOneOut X N i omega| +
            Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) ∂mu) := by
      rw [← integral_const_mul]
      apply integral_congr_ae
      exact ae_of_all mu fun omega => by ring
    _ = ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 3 / 2) *
        ((∫ omega, |X i.1 omega| ^ 3 ∂mu) *
          ∫ omega, |normalizedCenteredIidLeaveOneOut X N i omega| +
            Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu) := by
      rw [hfact']
    _ = _ := by ring

/-- Leave-one-out specialization of the integrated crossing estimate. -/
theorem integral_standardNormalSteinCrossingSet_leaveOneOut_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    (∫ omega, (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega) ∂mu) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ omega, |X i.1 omega| ∂mu) +
        2 * kolmogorovDistance (Measure.map Wi mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  dsimp only
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hbase : Integrable (fun omega => |X i.1 omega|) mu := by
    simpa only [Real.norm_eq_abs] using
      ((hX i.1).integrable (by norm_num)).norm
  have hlaw : Integrable (fun v : ℝ => |v|) (Measure.map (X i.1) mu) := by
    rw [integrable_map_measure (by fun_prop) (hX i.1).aemeasurable]
    simpa only [Function.comp_apply] using hbase
  have h := integral_standardNormalSteinCrossingSet_comp_le
    (normalizedCenteredIidLeaveOneOut X N i) (X i.1)
    hLeave.aemeasurable (hX i.1).aemeasurable hind.symm hlaw x
    (Real.sqrt (N + 1 : ℝ))⁻¹ (inv_nonneg.mpr (Real.sqrt_nonneg _))
  rw [integral_map (hX i.1).aemeasurable
    (show AEStronglyMeasurable (fun v : ℝ => |v|)
      (Measure.map (X i.1) mu) by fun_prop)] at h
  simpa only [Function.comp_apply] using h

/-- Independence factors the continuous part of the pointwise leave-one-out
derivative majorant. -/
theorem integral_leaveOneOut_driftMajorant_eq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) :
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    (∫ omega, (|Wi omega| + C) * (scale * |X i.1 omega|) ∂mu) =
      scale * (∫ omega, |Wi omega| + C ∂mu) *
        (∫ omega, |X i.1 omega| ∂mu) := by
  dsimp only
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hfact := hind.integral_comp_mul_comp
    (hX i.1).aemeasurable hLeave.aemeasurable
    (show AEStronglyMeasurable (fun v : ℝ => |v|)
      (Measure.map (X i.1) mu) by fun_prop)
    (show AEStronglyMeasurable (fun w : ℝ =>
        |w| + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ))
      (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu) by fun_prop)
  have hfact' :
      (∫ omega, |X i.1 omega| *
        (|normalizedCenteredIidLeaveOneOut X N i omega| +
          Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) ∂mu) =
        (∫ omega, |X i.1 omega| ∂mu) *
          ∫ omega, |normalizedCenteredIidLeaveOneOut X N i omega| +
            Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu := by
    simpa only [Function.comp_apply, Pi.mul_apply] using hfact
  calc
    (∫ omega, (|normalizedCenteredIidLeaveOneOut X N i omega| +
          Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
        ((Real.sqrt (N + 1 : ℝ))⁻¹ * |X i.1 omega|) ∂mu) =
      (Real.sqrt (N + 1 : ℝ))⁻¹ *
        (∫ omega, |X i.1 omega| *
          (|normalizedCenteredIidLeaveOneOut X N i omega| +
            Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) ∂mu) := by
      rw [← integral_const_mul]
      apply integral_congr_ae
      exact ae_of_all mu fun omega => by ring
    _ = (Real.sqrt (N + 1 : ℝ))⁻¹ *
        ((∫ omega, |X i.1 omega| ∂mu) *
          ∫ omega, |normalizedCenteredIidLeaveOneOut X N i omega| +
            Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu) := by
      rw [hfact']
    _ = _ := by ring

/-- Integrated leave-one-out derivative increment.  The continuous term is
factored by independence and the atomic term is controlled by the
leave-one-out Kolmogorov distance. -/
theorem integral_abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    (∫ omega, |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ∂mu) ≤
      scale * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ∂mu) +
        (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ omega, |X i.1 omega| ∂mu) +
        2 * kolmogorovDistance (Measure.map Wi mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  dsimp only
  let Wi := normalizedCenteredIidLeaveOneOut X N i
  let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  let scale : ℝ := (Real.sqrt (N + 1 : ℝ))⁻¹
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hW :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hWiAbs : Integrable (fun omega => |Wi omega|) mu := by
    simpa only [Wi, Real.norm_eq_abs] using
      ((hLeave.integrable (by norm_num)).norm)
  have hXiAbs : Integrable (fun omega => |X i.1 omega|) mu := by
    simpa only [Real.norm_eq_abs] using
      (((hX i.1).integrable (by norm_num)).norm)
  have hA : Integrable (fun omega => |Wi omega| + C) mu :=
    hWiAbs.add (integrable_const C)
  have hindAbs0 := hind.symm.comp
    (show Measurable (fun w : ℝ => |w| + C) by fun_prop)
    (show Measurable (fun v : ℝ => |v|) by fun_prop)
  have hindAbs : IndepFun (fun omega => |Wi omega| + C)
      (fun omega => |X i.1 omega|) mu := by
    simpa only [Wi, Function.comp_apply] using hindAbs0
  have hprod : Integrable
      ((fun omega => |Wi omega| + C) * fun omega => |X i.1 omega|) mu :=
    hindAbs.integrable_mul hA hXiAbs
  have hDrift : Integrable (fun omega =>
      (|Wi omega| + C) * (scale * |X i.1 omega|)) mu := by
    exact (hprod.const_mul scale).congr (ae_of_all mu fun omega => by
      simp only [Pi.mul_apply]
      ring)
  have hCrossMeas : AEStronglyMeasurable (fun omega =>
      (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega)) mu :=
    ((measurable_const.indicator
      (measurableSet_standardNormalSteinCrossingSet x scale)).comp_aemeasurable
        (hLeave.aemeasurable.prodMk (hX i.1).aemeasurable)).aestronglyMeasurable
  have hCross : Integrable (fun omega =>
      (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega)) mu := by
    apply Integrable.of_bound hCrossMeas 1
    exact ae_of_all mu fun omega => by
      by_cases hp : (Wi omega, X i.1 omega) ∈
          standardNormalSteinCrossingSet x scale <;>
        simp [Set.indicator_apply, hp]
  have hLeft : Integrable (fun omega =>
      |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)|) mu := by
    simpa only [Real.norm_eq_abs] using
      ((integrable_standardNormalSteinDerivative_comp W hW.aemeasurable x).sub
        (integrable_standardNormalSteinDerivative_comp Wi hLeave.aemeasurable x)).norm
  have hPoint : ∀ omega,
      |standardNormalSteinDerivative x (W omega) -
          standardNormalSteinDerivative x (Wi omega)| ≤
        (|Wi omega| + C) * (scale * |X i.1 omega|) +
          (standardNormalSteinCrossingSet x scale).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega) := by
    intro omega
    simpa only [W, Wi, scale, C] using
      abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le_crossingSet
        X N i x omega
  have hMono := integral_mono hLeft (hDrift.add hCross) hPoint
  have hDriftEq := integral_leaveOneOut_driftMajorant_eq X hX hIndep N i
  have hCrossLe := integral_standardNormalSteinCrossingSet_leaveOneOut_le
    X hX hIndep N i x
  have hDriftEq' :
      (∫ omega, (|Wi omega| + C) * (scale * |X i.1 omega|) ∂mu) =
        scale * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ∂mu) := by
    simpa only [Wi, scale, C] using hDriftEq
  have hCrossLe' :
      (∫ omega, (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega) ∂mu) ≤
        (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ omega, |X i.1 omega| ∂mu) +
        2 * kolmogorovDistance (Measure.map Wi mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    simpa only [Wi, scale] using hCrossLe
  calc
    (∫ omega, |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ∂mu) ≤
      ∫ omega, ((|Wi omega| + C) * (scale * |X i.1 omega|) +
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega)) ∂mu := hMono
    _ = (∫ omega, (|Wi omega| + C) * (scale * |X i.1 omega|) ∂mu) +
        ∫ omega, (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega) ∂mu :=
      integral_add hDrift hCross
    _ ≤ scale * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ∂mu) +
        ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ omega, |X i.1 omega| ∂mu) +
        2 * kolmogorovDistance (Measure.map Wi mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
      rw [hDriftEq']
      exact add_le_add_right hCrossLe' _
    _ = _ := by ring

/-- Integrability of both terms in one normalized-summand interval remainder.
This is the linearity side condition needed to collect the
variance-cancellation bracket into a finite sum. -/
theorem integrable_normalizedSummand_steinIntervalRemainder_components
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    Integrable (fun omega => scale ^ 2 * (X i.1 omega) ^ 2 *
      standardNormalSteinDerivative x (Wi omega)) mu ∧
    Integrable (fun omega =>
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)) mu := by
  dsimp only
  let Wi := normalizedCenteredIidLeaveOneOut X N i
  let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  let scale : ℝ := (Real.sqrt (N + 1 : ℝ))⁻¹
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX2 N i
  have hW :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX2 N
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hXiSq : Integrable (fun omega => (X i.1 omega) ^ 2) mu :=
    (hX2 i.1).integrable_sq
  have hDWi : Integrable
      (fun omega => standardNormalSteinDerivative x (Wi omega)) mu := by
    simpa only [Wi] using
      integrable_standardNormalSteinDerivative_comp Wi hLeave.aemeasurable x
  have hindA0 := hind.comp
    (show Measurable (fun v : ℝ => v ^ 2) by fun_prop)
    (measurable_standardNormalSteinDerivative x)
  have hindA : IndepFun (fun omega => (X i.1 omega) ^ 2)
      (fun omega => standardNormalSteinDerivative x (Wi omega)) mu := by
    simpa only [Wi, Function.comp_apply] using hindA0
  have hA0 : Integrable (fun omega => (X i.1 omega) ^ 2 *
      standardNormalSteinDerivative x (Wi omega)) mu :=
    hindA.integrable_mul hXiSq hDWi
  have hA : Integrable (fun omega => scale ^ 2 * (X i.1 omega) ^ 2 *
      standardNormalSteinDerivative x (Wi omega)) mu := by
    exact (hA0.const_mul (scale ^ 2)).congr
      (ae_of_all mu fun omega => by ring)
  have hXi : Integrable (X i.1) mu := (hX i.1).integrable (by norm_num)
  have hfW : AEMeasurable
      (fun omega => standardNormalSteinSolution x (W omega)) mu :=
    (continuous_standardNormalSteinSolution x).measurable.comp_aemeasurable
      hW.aemeasurable
  have hfWi : AEMeasurable
      (fun omega => standardNormalSteinSolution x (Wi omega)) mu :=
    (continuous_standardNormalSteinSolution x).measurable.comp_aemeasurable
      hLeave.aemeasurable
  have hDiffMeas : AEStronglyMeasurable (fun omega =>
      standardNormalSteinSolution x (W omega) -
        standardNormalSteinSolution x (Wi omega)) mu :=
    (hfW.sub hfWi).aestronglyMeasurable
  have hDiffBound : ∀ᵐ omega ∂mu,
      ‖standardNormalSteinSolution x (W omega) -
        standardNormalSteinSolution x (Wi omega)‖ ≤ 2 * C :=
    ae_of_all mu fun omega => by
      rw [Real.norm_eq_abs]
      calc
        |standardNormalSteinSolution x (W omega) -
            standardNormalSteinSolution x (Wi omega)| ≤
          |standardNormalSteinSolution x (W omega)| +
            |standardNormalSteinSolution x (Wi omega)| := abs_sub _ _
        _ = standardNormalSteinSolution x (W omega) +
            standardNormalSteinSolution x (Wi omega) := by
          rw [abs_of_nonneg (standardNormalSteinSolution_nonneg x _),
            abs_of_nonneg (standardNormalSteinSolution_nonneg x _)]
        _ ≤ C + C := add_le_add
          (by simpa only [C] using standardNormalSteinSolution_le x (W omega))
          (by simpa only [C] using standardNormalSteinSolution_le x (Wi omega))
        _ = 2 * C := by ring
  have hB0 : Integrable (fun omega => X i.1 omega *
      (standardNormalSteinSolution x (W omega) -
        standardNormalSteinSolution x (Wi omega))) mu :=
    hXi.mul_bdd hDiffMeas hDiffBound
  have hB : Integrable (fun omega => scale * X i.1 omega *
      (∫ z in Wi omega..W omega,
        standardNormalSteinDerivative x z)) mu := by
    exact (hB0.const_mul scale).congr (ae_of_all mu fun omega => by
      have hInt := intervalIntegral_standardNormalSteinDerivative_eq_sub
        x (Wi omega) (W omega)
      calc
        scale * (X i.1 omega *
            (standardNormalSteinSolution x (W omega) -
              standardNormalSteinSolution x (Wi omega))) =
          scale * X i.1 omega *
            (standardNormalSteinSolution x (W omega) -
              standardNormalSteinSolution x (Wi omega)) := by ring
        _ = scale * X i.1 omega *
            (∫ z in Wi omega..W omega,
              standardNormalSteinDerivative x z) :=
          congrArg (fun q : ℝ => scale * X i.1 omega * q) hInt.symm)
  exact ⟨hA, hB⟩

/-- Integrability of one normalized-summand interval remainder. -/
theorem integrable_normalizedSummand_steinIntervalRemainder
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    Integrable (fun omega =>
      scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)) mu := by
  have hComponents :=
    integrable_normalizedSummand_steinIntervalRemainder_components
      X hX hIndep N i x
  exact hComponents.1.sub hComponents.2

/-- The integral of one normalized-summand remainder is exactly the
corresponding difference of the two terms in the variance-cancellation
bracket. -/
theorem integral_normalizedSummand_steinIntervalRemainder_eq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    (∫ omega, scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z) ∂mu) =
      scale ^ 2 * (∫ omega, (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) ∂mu) -
        scale * (∫ omega, X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z) ∂mu) := by
  dsimp only
  have hComponents :=
    integrable_normalizedSummand_steinIntervalRemainder_components
      X hX hIndep N i x
  rw [integral_sub hComponents.1 hComponents.2]
  congr 1
  · rw [← integral_const_mul]
    apply integral_congr_ae
    exact ae_of_all mu fun omega => by ring
  · rw [← integral_const_mul]
    apply integral_congr_ae
    exact ae_of_all mu fun omega => by ring

/-- The complete second variance-cancellation bracket is the finite sum of
the one-summand interval remainders. -/
theorem varianceCancellation_secondBracket_eq_sum_integral_remainders
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    scale ^ 2 * ∑ i : Fin (N + 1), ∫ omega,
        (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
          (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu -
      scale * ∑ i : Fin (N + 1), ∫ omega,
        X i.1 omega *
          (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..W omega,
            standardNormalSteinDerivative x z) ∂mu =
      ∑ i : Fin (N + 1), ∫ omega,
        scale ^ 2 * (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) -
          scale * X i.1 omega *
            (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..W omega,
              standardNormalSteinDerivative x z) ∂mu := by
  dsimp only
  rw [Finset.mul_sum, Finset.mul_sum, ← Finset.sum_sub_distrib]
  apply Finset.sum_congr rfl
  intro i _
  exact (integral_normalizedSummand_steinIntervalRemainder_eq
    X hX hIndep N i x).symm

/-- Integrated second-bracket remainder for one normalized summand.  The
continuous contribution factors into a leave-one-out first moment and the
third absolute summand moment; the atomic contribution is controlled by the
weighted crossing estimate. -/
theorem integral_abs_normalizedSummand_steinIntervalRemainder_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    (∫ omega, |scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)| ∂mu) ≤
      (scale ^ 3 / 2) * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
        scale ^ 2 *
          ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            2 * kolmogorovDistance (Measure.map Wi mu)
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
              (∫ omega, (X i.1 omega) ^ 2 ∂mu)) := by
  dsimp only
  let Wi := normalizedCenteredIidLeaveOneOut X N i
  let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  let scale : ℝ := (Real.sqrt (N + 1 : ℝ))⁻¹
  let C : ℝ := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hLeave := normalizedCenteredIidLeaveOneOut_memLp X hX2 N i
  have hW :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX2 N
  have hind := indepFun_normalizedCenteredIidLeaveOneOut
    X (fun j => (hX j).aemeasurable) hIndep N i
  have hWiAbs : Integrable (fun omega => |Wi omega|) mu := by
    simpa only [Wi, Real.norm_eq_abs] using
      ((hLeave.integrable (by norm_num)).norm)
  have hXiCube : Integrable (fun omega => |X i.1 omega| ^ 3) mu := by
    simpa only [Real.norm_eq_abs] using
      (hX i.1).integrable_norm_pow (by norm_num)
  have hXiSq : Integrable (fun omega => (X i.1 omega) ^ 2) mu :=
    (hX2 i.1).integrable_sq
  have hA : Integrable (fun omega => |Wi omega| + C) mu :=
    hWiAbs.add (integrable_const C)
  have hindCube0 := hind.symm.comp
    (show Measurable (fun w : ℝ => |w| + C) by fun_prop)
    (show Measurable (fun v : ℝ => |v| ^ 3) by fun_prop)
  have hindCube : IndepFun (fun omega => |Wi omega| + C)
      (fun omega => |X i.1 omega| ^ 3) mu := by
    simpa only [Wi, Function.comp_apply] using hindCube0
  have hprod : Integrable
      ((fun omega => |Wi omega| + C) *
        fun omega => |X i.1 omega| ^ 3) mu :=
    hindCube.integrable_mul hA hXiCube
  have hDrift : Integrable (fun omega =>
      (|Wi omega| + C) *
        (scale ^ 3 * |X i.1 omega| ^ 3 / 2)) mu := by
    exact (hprod.const_mul (scale ^ 3 / 2)).congr
      (ae_of_all mu fun omega => by
        simp only [Pi.mul_apply]
        ring)
  have hIndicatorMeas : AEStronglyMeasurable (fun omega =>
      (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega)) mu :=
    ((measurable_const.indicator
      (measurableSet_standardNormalSteinCrossingSet x scale)).comp_aemeasurable
        (hLeave.aemeasurable.prodMk (hX i.1).aemeasurable)).aestronglyMeasurable
  have hCross0 : Integrable (fun omega => (X i.1 omega) ^ 2 *
      (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega)) mu := by
    apply hXiSq.mono'
      (((hX i.1).aemeasurable.pow_const 2).aestronglyMeasurable.mul
        hIndicatorMeas)
    exact ae_of_all mu fun omega => by
      by_cases hp : (Wi omega, X i.1 omega) ∈
          standardNormalSteinCrossingSet x scale <;>
        simp [hp, abs_of_nonneg (sq_nonneg (X i.1 omega)), sq_nonneg]
  have hCross : Integrable (fun omega => scale ^ 2 * (X i.1 omega) ^ 2 *
      (standardNormalSteinCrossingSet x scale).indicator
        (fun _ : ℝ × ℝ => (1 : ℝ)) (Wi omega, X i.1 omega)) mu := by
    exact (hCross0.const_mul (scale ^ 2)).congr
      (ae_of_all mu fun omega => by ring)
  have hMajor := hDrift.add hCross
  have hPoint : ∀ omega,
      |scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)| ≤
        (|Wi omega| + C) *
            (scale ^ 3 * |X i.1 omega| ^ 3 / 2) +
          scale ^ 2 * (X i.1 omega) ^ 2 *
            (standardNormalSteinCrossingSet x scale).indicator
              (fun _ : ℝ × ℝ => (1 : ℝ))
              (Wi omega, X i.1 omega) := by
    intro omega
    simpa only [W, Wi, scale, C] using
      abs_normalizedSummand_steinIntervalRemainder_le X N i x omega
  have hLeftMeas : AEStronglyMeasurable (fun omega =>
      |scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)|) mu := by
    simp_rw [intervalIntegral_standardNormalSteinDerivative_eq_sub]
    have hXiMeas : AEMeasurable (X i.1) mu := (hX i.1).aemeasurable
    have hDWi : AEMeasurable
        (fun omega => standardNormalSteinDerivative x (Wi omega)) mu :=
      (measurable_standardNormalSteinDerivative x).comp_aemeasurable
        hLeave.aemeasurable
    have hfW : AEMeasurable
        (fun omega => standardNormalSteinSolution x (W omega)) mu :=
      (continuous_standardNormalSteinSolution x).measurable.comp_aemeasurable
        hW.aemeasurable
    have hfWi : AEMeasurable
        (fun omega => standardNormalSteinSolution x (Wi omega)) mu :=
      (continuous_standardNormalSteinSolution x).measurable.comp_aemeasurable
        hLeave.aemeasurable
    have hInner : AEMeasurable (fun omega =>
        scale ^ 2 * (X i.1 omega) ^ 2 *
            standardNormalSteinDerivative x (Wi omega) -
          scale * X i.1 omega *
            (standardNormalSteinSolution x (W omega) -
              standardNormalSteinSolution x (Wi omega))) mu :=
      (((hXiMeas.pow_const 2).const_mul (scale ^ 2)).mul hDWi).sub
        ((hXiMeas.const_mul scale).mul (hfW.sub hfWi))
    exact (continuous_abs.measurable.comp_aemeasurable hInner).aestronglyMeasurable
  have hLeft : Integrable (fun omega =>
      |scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)|) mu := by
    apply hMajor.mono' hLeftMeas
    exact ae_of_all mu fun omega => by
      rw [Real.norm_eq_abs, abs_of_nonneg (abs_nonneg _)]
      exact hPoint omega
  have hMono := integral_mono hLeft hMajor hPoint
  have hDriftEq := integral_leaveOneOut_cubicDriftMajorant_eq
    X hX hIndep N i
  have hCrossLe :=
    integral_sq_mul_standardNormalSteinCrossingSet_leaveOneOut_le
      X hX hIndep N i x
  have hDriftEq' :
      (∫ omega, (|Wi omega| + C) *
        (scale ^ 3 * |X i.1 omega| ^ 3 / 2) ∂mu) =
        (scale ^ 3 / 2) * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ^ 3 ∂mu) := by
    simpa only [Wi, scale, C] using hDriftEq
  have hCrossLe' :
      (∫ omega, (X i.1 omega) ^ 2 *
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ))
          (Wi omega, X i.1 omega) ∂mu) ≤
        (Real.sqrt (2 * Real.pi))⁻¹ * scale *
            (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
          2 * kolmogorovDistance (Measure.map Wi mu)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
            (∫ omega, (X i.1 omega) ^ 2 ∂mu) := by
    simpa only [Wi, scale] using hCrossLe
  have hCrossScaledLe :
      (∫ omega, scale ^ 2 * (X i.1 omega) ^ 2 *
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ))
          (Wi omega, X i.1 omega) ∂mu) ≤
        scale ^ 2 *
          ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            2 * kolmogorovDistance (Measure.map Wi mu)
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
              (∫ omega, (X i.1 omega) ^ 2 ∂mu)) := by
    calc
      (∫ omega, scale ^ 2 * (X i.1 omega) ^ 2 *
        (standardNormalSteinCrossingSet x scale).indicator
          (fun _ : ℝ × ℝ => (1 : ℝ))
          (Wi omega, X i.1 omega) ∂mu) =
        scale ^ 2 * ∫ omega, (X i.1 omega) ^ 2 *
          (standardNormalSteinCrossingSet x scale).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ))
            (Wi omega, X i.1 omega) ∂mu := by
          rw [← integral_const_mul]
          apply integral_congr_ae
          exact ae_of_all mu fun omega => by ring
      _ ≤ _ := mul_le_mul_of_nonneg_left hCrossLe' (sq_nonneg scale)
  calc
    (∫ omega, |scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z)| ∂mu) ≤
      ∫ omega, ((|Wi omega| + C) *
          (scale ^ 3 * |X i.1 omega| ^ 3 / 2) +
        scale ^ 2 * (X i.1 omega) ^ 2 *
          (standardNormalSteinCrossingSet x scale).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ))
            (Wi omega, X i.1 omega)) ∂mu := hMono
    _ = (∫ omega, (|Wi omega| + C) *
          (scale ^ 3 * |X i.1 omega| ^ 3 / 2) ∂mu) +
        ∫ omega, scale ^ 2 * (X i.1 omega) ^ 2 *
          (standardNormalSteinCrossingSet x scale).indicator
            (fun _ : ℝ × ℝ => (1 : ℝ))
            (Wi omega, X i.1 omega) ∂mu :=
      integral_add hDrift hCross
    _ ≤ (scale ^ 3 / 2) * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
        scale ^ 2 *
          ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            2 * kolmogorovDistance (Measure.map Wi mu)
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
              (∫ omega, (X i.1 omega) ^ 2 ∂mu)) :=
      add_le_add hDriftEq'.le hCrossScaledLe

/-- Expectation form of the one-summand second-bracket estimate. -/
theorem abs_integral_normalizedSummand_steinIntervalRemainder_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    |∫ omega, scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z) ∂mu| ≤
      (scale ^ 3 / 2) * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
        scale ^ 2 *
          ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            2 * kolmogorovDistance (Measure.map Wi mu)
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw *
              (∫ omega, (X i.1 omega) ^ 2 ∂mu)) := by
  dsimp only
  exact abs_integral_le_integral_abs.trans
    (integral_abs_normalizedSummand_steinIntervalRemainder_le
      X hX hIndep N i x)

/-- Unit-variance specialization of the one-summand second-bracket estimate. -/
theorem abs_integral_normalizedSummand_steinIntervalRemainder_le_unitSecond
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    |∫ omega, scale ^ 2 * (X i.1 omega) ^ 2 *
          standardNormalSteinDerivative x (Wi omega) -
        scale * X i.1 omega *
          (∫ z in Wi omega..W omega,
            standardNormalSteinDerivative x z) ∂mu| ≤
      (scale ^ 3 / 2) * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
        scale ^ 2 *
          ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            2 * kolmogorovDistance (Measure.map Wi mu)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  have h := abs_integral_normalizedSummand_steinIntervalRemainder_le
    X hX hIndep N i x
  simpa only [hSecond i.1, mul_one] using h

/-- The entire unit-variance second variance-cancellation bracket is bounded
by the sum of the explicit one-summand remainder estimates. -/
theorem abs_varianceCancellation_secondBracket_le_sum_unitSecond
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    |scale ^ 2 * ∑ i : Fin (N + 1), ∫ omega,
        (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
          (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu -
      scale * ∑ i : Fin (N + 1), ∫ omega,
        X i.1 omega *
          (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..W omega,
            standardNormalSteinDerivative x z) ∂mu| ≤
      ∑ i : Fin (N + 1), (
        (scale ^ 3 / 2) *
            (∫ omega,
              |normalizedCenteredIidLeaveOneOut X N i omega| + C ∂mu) *
            (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
          scale ^ 2 *
            ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
                (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
              2 * kolmogorovDistance
                (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) := by
  dsimp only
  calc
    |(Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
          ∑ i : Fin (N + 1), ∫ omega,
            (X i.1 omega) ^ 2 * standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu -
        (Real.sqrt (N + 1 : ℝ))⁻¹ *
          ∑ i : Fin (N + 1), ∫ omega,
            X i.1 omega *
              (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..
                  NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                    X N omega,
                standardNormalSteinDerivative x z) ∂mu| =
      |∑ i : Fin (N + 1), ∫ omega,
        (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (X i.1 omega) ^ 2 *
            standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) -
          (Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega *
            (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..
                NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                  X N omega,
              standardNormalSteinDerivative x z) ∂mu| :=
        congrArg abs
          (varianceCancellation_secondBracket_eq_sum_integral_remainders
            X hX hIndep N x)
    _ ≤ ∑ i : Fin (N + 1),
        |∫ omega,
          (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (X i.1 omega) ^ 2 *
              standardNormalSteinDerivative x
                (normalizedCenteredIidLeaveOneOut X N i omega) -
            (Real.sqrt (N + 1 : ℝ))⁻¹ * X i.1 omega *
              (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..
                  NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                    X N omega,
                standardNormalSteinDerivative x z) ∂mu| :=
      Finset.abs_sum_le_sum_abs _ _
    _ ≤ _ := by
      apply Finset.sum_le_sum
      intro i _
      exact abs_integral_normalizedSummand_steinIntervalRemainder_le_unitSecond
        X hX hIndep hSecond N i x

/-- The first variance-cancellation bracket is the normalized average of the
leave-one-out derivative-expectation differences. -/
theorem varianceCancellation_firstBracket_eq_scaled_sum_differences
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega}
    (X : ℕ → Omega → ℝ) (N : ℕ) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    (∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        scale ^ 2 * ∑ i : Fin (N + 1), ∫ omega,
          standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu =
      scale ^ 2 * ∑ i : Fin (N + 1),
        ((∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
          ∫ omega, standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) := by
  dsimp only
  have hsqrtPos : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hscale : (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (N + 1 : ℝ) = 1 := by
    rw [inv_pow]
    field_simp [hsqrtPos.ne']
    exact (Real.sq_sqrt (by positivity : (0 : ℝ) ≤ N + 1)).symm
  symm
  calc
    (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        ∑ i : Fin (N + 1),
          ((∫ omega, standardNormalSteinDerivative x
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X N omega) ∂mu) -
            ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) =
      (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        ((N + 1 : ℝ) *
            (∫ omega, standardNormalSteinDerivative x
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X N omega) ∂mu) -
          ∑ i : Fin (N + 1), ∫ omega, standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) := by
        rw [Finset.sum_sub_distrib]
        simp
    _ = (∫ omega, standardNormalSteinDerivative x
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
              X N omega) ∂mu) -
          (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
            ∑ i : Fin (N + 1), ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu := by
      rw [mul_sub, ← mul_assoc, hscale, one_mul]

/-- Expectation form of the integrated leave-one-out derivative estimate. -/
theorem abs_integral_standardNormalSteinDerivative_sum_sub_leaveOneOut_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (i : Fin (N + 1)) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let Wi := normalizedCenteredIidLeaveOneOut X N i
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    |(∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        ∫ omega, standardNormalSteinDerivative x (Wi omega) ∂mu| ≤
      scale * (∫ omega, |Wi omega| + C ∂mu) *
          (∫ omega, |X i.1 omega| ∂mu) +
        (Real.sqrt (2 * Real.pi))⁻¹ * scale *
          (∫ omega, |X i.1 omega| ∂mu) +
        2 * kolmogorovDistance (Measure.map Wi mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  dsimp only
  let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  let Wi := normalizedCenteredIidLeaveOneOut X N i
  have hW :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  have hWi := normalizedCenteredIidLeaveOneOut_memLp X hX N i
  have hDW := integrable_standardNormalSteinDerivative_comp W hW.aemeasurable x
  have hDWi := integrable_standardNormalSteinDerivative_comp Wi hWi.aemeasurable x
  have hAbs := integral_abs_standardNormalSteinDerivative_sum_sub_leaveOneOut_le
    X hX hIndep N i x
  dsimp only at hAbs
  calc
    |(∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        ∫ omega, standardNormalSteinDerivative x (Wi omega) ∂mu| =
      |∫ omega, (standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)) ∂mu| := by
      rw [integral_sub hDW hDWi]
    _ ≤ ∫ omega, |standardNormalSteinDerivative x (W omega) -
        standardNormalSteinDerivative x (Wi omega)| ∂mu := by
      simpa only [Real.norm_eq_abs] using
        norm_integral_le_integral_norm (fun omega =>
          standardNormalSteinDerivative x (W omega) -
            standardNormalSteinDerivative x (Wi omega))
    _ ≤ _ := by simpa only [W, Wi] using hAbs

/-- The complete first variance-cancellation bracket is bounded by the
normalized sum of its explicit leave-one-out derivative estimates. -/
theorem abs_varianceCancellation_firstBracket_le_sum
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 2 mu)
    (hIndep : iIndepFun X mu) (N : ℕ) (x : ℝ) :
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    |(∫ omega, standardNormalSteinDerivative x (W omega) ∂mu) -
        scale ^ 2 * ∑ i : Fin (N + 1), ∫ omega,
          standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu| ≤
      scale ^ 2 * ∑ i : Fin (N + 1), (
        scale *
            (∫ omega,
              |normalizedCenteredIidLeaveOneOut X N i omega| + C ∂mu) *
            (∫ omega, |X i.1 omega| ∂mu) +
          (Real.sqrt (2 * Real.pi))⁻¹ * scale *
            (∫ omega, |X i.1 omega| ∂mu) +
          2 * kolmogorovDistance
            (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  dsimp only
  have hEq := varianceCancellation_firstBracket_eq_scaled_sum_differences
    (mu := mu) X N x
  dsimp only at hEq
  calc
    |(∫ omega, standardNormalSteinDerivative x
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X N omega) ∂mu) -
        (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1),
          ∫ omega, standardNormalSteinDerivative x
            (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu| =
      |(Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        ∑ i : Fin (N + 1),
          ((∫ omega, standardNormalSteinDerivative x
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X N omega) ∂mu) -
            ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu)| :=
        congrArg abs hEq
    _ = (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        |∑ i : Fin (N + 1),
          ((∫ omega, standardNormalSteinDerivative x
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X N omega) ∂mu) -
            ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu)| := by
      rw [abs_mul, abs_of_nonneg (sq_nonneg _)]
    _ ≤ (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        ∑ i : Fin (N + 1),
          |(∫ omega, standardNormalSteinDerivative x
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X N omega) ∂mu) -
            ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu| :=
      mul_le_mul_of_nonneg_left (Finset.abs_sum_le_sum_abs _ _)
        (sq_nonneg _)
    _ ≤ _ := by
      apply mul_le_mul_of_nonneg_left _ (sq_nonneg _)
      apply Finset.sum_le_sum
      intro i _
      exact abs_integral_standardNormalSteinDerivative_sum_sub_leaveOneOut_le
        X hX hIndep N i x

/-- Pointwise standard-normal CDF error obtained by combining both complete
variance-cancellation bracket estimates. -/
theorem abs_cdf_normalizedCenteredIidSum_sub_standardNormal_le_steinSums
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu)
    (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (x : ℝ) :
    let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
      (hX j).mono_exponent (by norm_num)
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    |cdf (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw W
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX2 N).aemeasurable) x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      scale ^ 2 * ∑ i : Fin (N + 1), (
        scale *
            (∫ omega,
              |normalizedCenteredIidLeaveOneOut X N i omega| + C ∂mu) *
            (∫ omega, |X i.1 omega| ∂mu) +
          (Real.sqrt (2 * Real.pi))⁻¹ * scale *
            (∫ omega, |X i.1 omega| ∂mu) +
          2 * kolmogorovDistance
            (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) +
        ∑ i : Fin (N + 1), (
          (scale ^ 3 / 2) *
              (∫ omega,
                |normalizedCenteredIidLeaveOneOut X N i omega| + C ∂mu) *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            scale ^ 2 *
              ((Real.sqrt (2 * Real.pi))⁻¹ * scale *
                  (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
                2 * kolmogorovDistance
                  (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) := by
  dsimp only
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hIdentity :=
    integral_normalizedCenteredIidSum_stein_varianceCancellation
      X hX2 hIndep hMean hSecond N x
  have hFirst := abs_varianceCancellation_firstBracket_le_sum
    X hX2 hIndep N x
  have hSecondBound :=
    abs_varianceCancellation_secondBracket_le_sum_unitSecond
      X hX hIndep hSecond N x
  dsimp only at hIdentity hFirst hSecondBound
  calc
    |cdf (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX2 N).aemeasurable) x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| =
      |((∫ omega, standardNormalSteinDerivative x
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
              X N omega) ∂mu) -
          (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1),
            ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu) +
        ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1),
            ∫ omega, (X i.1 omega) ^ 2 *
              standardNormalSteinDerivative x
                (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu -
          (Real.sqrt (N + 1 : ℝ))⁻¹ * ∑ i : Fin (N + 1),
            ∫ omega, X i.1 omega *
              (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..
                  NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                    X N omega,
                standardNormalSteinDerivative x z) ∂mu)| :=
        congrArg abs hIdentity.symm
    _ ≤
      |(∫ omega, standardNormalSteinDerivative x
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
              X N omega) ∂mu) -
          (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1),
            ∫ omega, standardNormalSteinDerivative x
              (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu| +
        |(Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1),
            ∫ omega, (X i.1 omega) ^ 2 *
              standardNormalSteinDerivative x
                (normalizedCenteredIidLeaveOneOut X N i omega) ∂mu -
          (Real.sqrt (N + 1 : ℝ))⁻¹ * ∑ i : Fin (N + 1),
            ∫ omega, X i.1 omega *
              (∫ z in normalizedCenteredIidLeaveOneOut X N i omega..
                  NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                    X N omega,
                standardNormalSteinDerivative x z) ∂mu| := abs_add_le _ _
    _ ≤ _ := add_le_add hFirst hSecondBound

/-- Under the iid unit-variance hypotheses, the two explicit Stein sums
collapse to one deterministic `1 / sqrt (N + 1)` term and a recursive sum of
leave-one-out Kolmogorov distances.  The coefficient `4` records the two
distance contributions from each variance-cancellation bracket. -/
theorem abs_cdf_normalizedCenteredIidSum_sub_standardNormal_le_collapsedStein
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu)
    (hIdent : ∀ j, IdentDistrib (X j) (X 0) mu mu)
    (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    (N : ℕ) (x : ℝ) :
    let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
      (hX j).mono_exponent (by norm_num)
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    let rho := ∫ omega, |X 0 omega| ^ 3 ∂mu
    |cdf (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw W
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX2 N).aemeasurable) x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      scale *
          ((1 + C) + (Real.sqrt (2 * Real.pi))⁻¹ +
            rho * ((1 + C) / 2 + (Real.sqrt (2 * Real.pi))⁻¹)) +
        4 * scale ^ 2 * ∑ i : Fin (N + 1),
          kolmogorovDistance
            (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  dsimp only
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hBase :=
    abs_cdf_normalizedCenteredIidSum_sub_standardNormal_le_steinSums
      X hX hIndep hMean hSecond N x
  dsimp only at hBase
  refine hBase.trans ?_
  have hscale : 0 ≤ (Real.sqrt (N + 1 : ℝ))⁻¹ := by positivity
  have hnormal : 0 ≤ (Real.sqrt (2 * Real.pi))⁻¹ := by positivity
  have hrho : 0 ≤ ∫ omega, |X 0 omega| ^ 3 ∂mu :=
    integral_nonneg fun _ => by positivity
  have hscaleSq :
      (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (N + 1 : ℝ) = 1 := by
    rw [inv_pow]
    field_simp [show Real.sqrt (N + 1 : ℝ) ≠ 0 by positivity]
    exact (Real.sq_sqrt (by positivity : (0 : ℝ) ≤ N + 1)).symm
  calc
    (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1), (
        (Real.sqrt (N + 1 : ℝ))⁻¹ *
            (∫ omega,
              |normalizedCenteredIidLeaveOneOut X N i omega| +
                Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu) *
            (∫ omega, |X i.1 omega| ∂mu) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
              (Real.sqrt (N + 1 : ℝ))⁻¹ *
            (∫ omega, |X i.1 omega| ∂mu) +
          2 * kolmogorovDistance
            (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) +
        ∑ i : Fin (N + 1), (
          ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 3 / 2) *
              (∫ omega,
                |normalizedCenteredIidLeaveOneOut X N i omega| +
                  Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu) *
              (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
            (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
              ((Real.sqrt (2 * Real.pi))⁻¹ *
                    (Real.sqrt (N + 1 : ℝ))⁻¹ *
                  (∫ omega, |X i.1 omega| ^ 3 ∂mu) +
                2 * kolmogorovDistance
                  (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤
      (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * ∑ i : Fin (N + 1), (
        (Real.sqrt (N + 1 : ℝ))⁻¹ *
            (1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            (Real.sqrt (N + 1 : ℝ))⁻¹ +
          2 * kolmogorovDistance
            (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) +
        ∑ i : Fin (N + 1), (
          ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 3 / 2) *
              (1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) +
            (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
              ((Real.sqrt (2 * Real.pi))⁻¹ *
                    (Real.sqrt (N + 1 : ℝ))⁻¹ *
                  (∫ omega, |X 0 omega| ^ 3 ∂mu) +
                2 * kolmogorovDistance
                  (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) := by
      apply add_le_add
      · apply mul_le_mul_of_nonneg_left _ (sq_nonneg _)
        apply Finset.sum_le_sum
        intro i _
        have hXi := integral_abs_le_one_of_secondMoment_eq_one
          (hX2 i.1) (hSecond i.1)
        have hWi :=
          integral_abs_normalizedCenteredIidLeaveOneOut_add_const_le
            X hX2 hIndep hMean hSecond N i
              (Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ))
        have hXiNonneg : 0 ≤ ∫ omega, |X i.1 omega| ∂mu :=
          integral_nonneg fun _ => abs_nonneg _
        have hCNonneg :
            0 ≤ 1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) := by
          positivity
        apply add_le_add
        · apply add_le_add
          · calc
              (Real.sqrt (N + 1 : ℝ))⁻¹ *
                    (∫ omega,
                      |normalizedCenteredIidLeaveOneOut X N i omega| +
                        Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) ∂mu) *
                    (∫ omega, |X i.1 omega| ∂mu) ≤
                  (Real.sqrt (N + 1 : ℝ))⁻¹ *
                    (1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) * 1 := by
                      rw [mul_one, mul_assoc]
                      apply mul_le_mul_of_nonneg_left _ hscale
                      simpa using mul_le_mul hWi hXi hXiNonneg hCNonneg
              _ = _ := by ring
          · simpa using
              mul_le_mul_of_nonneg_left hXi (mul_nonneg hnormal hscale)
        · rfl
      · apply Finset.sum_le_sum
        intro i _
        have hWi :=
          integral_abs_normalizedCenteredIidLeaveOneOut_add_const_le
            X hX2 hIndep hMean hSecond N i
              (Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ))
        have hXi3 := integral_abs_cube_eq_of_identDistrib (hIdent i.1)
        rw [hXi3]
        apply add_le_add
        · exact mul_le_mul_of_nonneg_right
            (mul_le_mul_of_nonneg_left hWi (by positivity)) hrho
        · rfl
    _ = (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (N + 1 : ℝ) *
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            ((1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) +
              (Real.sqrt (2 * Real.pi))⁻¹ +
              (∫ omega, |X 0 omega| ^ 3 ∂mu) *
                ((1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) / 2 +
                  (Real.sqrt (2 * Real.pi))⁻¹))) +
        4 * (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
          ∑ i : Fin (N + 1),
            kolmogorovDistance
              (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
      simp only [Finset.sum_add_distrib, Finset.sum_const, Finset.card_fin,
        nsmul_eq_mul, ← Finset.mul_sum, Nat.cast_add, Nat.cast_one, Nat.add_comm]
      ring
    _ = _ := by rw [hscaleSq, one_mul]

/-- Substituting the exact leave-one-out law transport into the collapsed
Stein estimate yields an explicit recurrence.  Its inherited-error
coefficient is exactly `4`. -/
theorem abs_cdf_normalizedCenteredIidSum_sub_standardNormal_le_steinRecurrence
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ j, MemLp (X j) 3 mu)
    (hIndep : iIndepFun X mu)
    (hIdent : ∀ j, IdentDistrib (X j) (X 0) mu mu)
    (hMean : ∀ j, ∫ omega, X j omega ∂mu = 0)
    (hSecond : ∀ j, ∫ omega, (X j omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 0 < N) (x : ℝ) :
    let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
      (hX j).mono_exponent (by norm_num)
    let W := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let scale := (Real.sqrt (N + 1 : ℝ))⁻¹
    let C := Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)
    let rho := ∫ omega, |X 0 omega| ^ 3 ∂mu
    let previousLaw := Measure.map
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
        X (N - 1)) mu
    let contractedNormal := Measure.map
      (fun y : ℝ => Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
    |cdf (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw W
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX2 N).aemeasurable) x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      scale *
          ((1 + C) + (Real.sqrt (2 * Real.pi))⁻¹ +
            rho * ((1 + C) / 2 + (Real.sqrt (2 * Real.pi))⁻¹)) +
        4 * (kolmogorovDistance previousLaw
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
            kolmogorovDistance contractedNormal
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  dsimp only
  let hX2 : ∀ j, MemLp (X j) 2 mu := fun j =>
    (hX j).mono_exponent (by norm_num)
  have hBase :=
    abs_cdf_normalizedCenteredIidSum_sub_standardNormal_le_collapsedStein
      X hX hIndep hIdent hMean hSecond N x
  dsimp only at hBase
  refine hBase.trans ?_
  refine add_le_add le_rfl ?_
  have hEach : ∀ i : Fin (N + 1),
      kolmogorovDistance
          (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
        kolmogorovDistance
            (Measure.map
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X (N - 1)) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
          kolmogorovDistance
            (Measure.map
              (fun y : ℝ =>
                Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    intro i
    exact
      kolmogorovDistance_normalizedCenteredIidLeaveOneOut_le_prefix_add_scalingDefect
        X (fun j => (hX j).aemeasurable) hIndep hIdent hN i
  have hSum := Finset.sum_le_sum
    (s := Finset.univ) (fun i _ => hEach i)
  have hscaleSq :
      (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 * (N + 1 : ℝ) = 1 := by
    rw [inv_pow]
    field_simp [show Real.sqrt (N + 1 : ℝ) ≠ 0 by positivity]
    exact (Real.sq_sqrt (by positivity : (0 : ℝ) ≤ N + 1)).symm
  calc
    4 * (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        ∑ i : Fin (N + 1),
          kolmogorovDistance
            (Measure.map (normalizedCenteredIidLeaveOneOut X N i) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      4 * (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
        ∑ _i : Fin (N + 1),
          (kolmogorovDistance
              (Measure.map
                (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                  X (N - 1)) mu)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
            kolmogorovDistance
              (Measure.map
                (fun y : ℝ =>
                  Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
      mul_le_mul_of_nonneg_left hSum (by positivity)
    _ = 4 *
        (kolmogorovDistance
            (Measure.map
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                X (N - 1)) mu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
          kolmogorovDistance
            (Measure.map
              (fun y : ℝ =>
                Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
      rw [Finset.sum_const, Finset.card_fin]
      simp only [nsmul_eq_mul, Nat.cast_add, Nat.cast_one]
      calc
        4 * (Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
            ((N + 1 : ℝ) *
              (kolmogorovDistance
                  (Measure.map
                    (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                      X (N - 1)) mu)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
                kolmogorovDistance
                  (Measure.map
                    (fun y : ℝ =>
                      Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) =
            4 * ((Real.sqrt (N + 1 : ℝ))⁻¹ ^ 2 *
              (N + 1 : ℝ)) *
              (kolmogorovDistance
                  (Measure.map
                    (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                      X (N - 1)) mu)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
                kolmogorovDistance
                  (Measure.map
                    (fun y : ℝ =>
                      Real.sqrt (N : ℝ) / Real.sqrt (N + 1 : ℝ) * y)
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by ring
        _ = _ := by rw [hscaleSq, mul_one]

/-- The exact inherited-error coefficient exposed by the completed Stein
recurrence is noncontractive. -/
theorem one_lt_steinRecurrenceInheritedCoefficient : (1 : ℝ) < 4 := by
  norm_num

/-- Even the coefficient multiplying the standardized third moment in the
deterministic part of the collapsed Stein majorant exceeds one. -/
theorem one_lt_collapsedSteinRhoCoefficient :
    (1 : ℝ) <
      (1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) / 2 +
        (Real.sqrt (2 * Real.pi))⁻¹ := by
  have harg : (1 : ℝ) < 2 * Real.pi := by
    nlinarith [Real.pi_gt_three]
  have hsqrt : (1 : ℝ) < Real.sqrt (2 * Real.pi) := by
    simpa only [Real.sqrt_one] using
      Real.sqrt_lt_sqrt (by norm_num : (0 : ℝ) ≤ 1) harg
  have hexp : (1 : ℝ) < Real.exp (1 / 2 : ℝ) :=
    Real.one_lt_exp_iff.mpr (by norm_num)
  have hC : (1 : ℝ) <
      Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ) :=
    one_lt_mul_of_lt_of_le hsqrt hexp.le
  have hinv : (0 : ℝ) < (Real.sqrt (2 * Real.pi))⁻¹ := by positivity
  nlinarith

/-- Consequently, for every standardized third moment `rho ≥ 1`, the
deterministic coefficient in the completed Stein estimate already lies
strictly above the source budget `rho`, before either recursive distance term
is added. -/
theorem rho_lt_collapsedSteinDeterministicMajorant
    {rho : ℝ} (hrho : 1 ≤ rho) :
    rho <
      (1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) +
        (Real.sqrt (2 * Real.pi))⁻¹ +
        rho * ((1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) / 2 +
          (Real.sqrt (2 * Real.pi))⁻¹) := by
  have hrhoPos : 0 < rho := zero_lt_one.trans_le hrho
  have hCoeff := one_lt_collapsedSteinRhoCoefficient
  have hmul : 0 < rho *
      (((1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) / 2 +
        (Real.sqrt (2 * Real.pi))⁻¹) - 1) :=
    mul_pos hrhoPos (sub_pos.mpr hCoeff)
  have hadd : 0 <
      (1 + Real.sqrt (2 * Real.pi) * Real.exp (1 / 2 : ℝ)) +
        (Real.sqrt (2 * Real.pi))⁻¹ := by positivity
  nlinarith

/-- A shifted CDF sandwich converts to an ordinary pointwise CDF bound when
the comparison law has a Lipschitz CDF.  This is the order-theoretic step in
Esseen smoothing: the later Fourier argument only has to produce the two
shifted inequalities. -/
theorem cdfBound_of_shift_bounds
    (μ ν : Measure ℝ) {L δ ε : ℝ} (hδ : 0 ≤ δ)
    (hν : ∀ a b,
      |cdf ν b - cdf ν a| ≤ L * |b - a|)
    (hlower : ∀ x, cdf ν (x - δ) - ε ≤ cdf μ x)
    (hupper : ∀ x, cdf μ x ≤ cdf ν (x + δ) + ε) :
    CDFBound μ ν (ε + L * δ) := by
  intro x
  rw [cdfGap, abs_le]
  have hleft := hν (x - δ) x
  have hright := hν x (x + δ)
  have hleft' : cdf ν x - cdf ν (x - δ) ≤ L * δ := by
    calc
      cdf ν x - cdf ν (x - δ) ≤
          |cdf ν x - cdf ν (x - δ)| := le_abs_self _
      _ ≤ L * |x - (x - δ)| := hleft
      _ = L * δ := by
        rw [show x - (x - δ) = δ by ring, abs_of_nonneg hδ]
  have hright' : cdf ν (x + δ) - cdf ν x ≤ L * δ := by
    calc
      cdf ν (x + δ) - cdf ν x ≤
          |cdf ν (x + δ) - cdf ν x| := le_abs_self _
      _ ≤ L * |(x + δ) - x| := hright
      _ = L * δ := by
        rw [show (x + δ) - x = δ by ring, abs_of_nonneg hδ]
  constructor
  · linarith [hlower x]
  · linarith [hupper x]

/-- The shifted-sandwich reduction specialized to the standard-normal target.
It isolates the remaining Fourier-analytic part of the Berry--Esseen proof
from the already certified Gaussian-density regularity. -/
theorem standardNormalCDFBound_of_shift_bounds
    (μ : Measure ℝ) {δ ε : ℝ} (hδ : 0 ≤ δ)
    (hlower : ∀ x,
      cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (x - δ) - ε ≤
        cdf μ x)
    (hupper : ∀ x,
      cdf μ x ≤
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (x + δ) + ε) :
    CDFBound μ NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      (ε + (Real.sqrt (2 * Real.pi))⁻¹ * δ) :=
  cdfBound_of_shift_bounds μ
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hδ
    standardNormalCDF_lipschitz hlower hupper

/-- Supremum-form consequence of `standardNormalCDFBound_of_shift_bounds`. -/
theorem kolmogorovDistance_standardNormal_le_of_shift_bounds
    (μ : Measure ℝ) {δ ε : ℝ} (hδ : 0 ≤ δ)
    (hlower : ∀ x,
      cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (x - δ) - ε ≤
        cdf μ x)
    (hupper : ∀ x,
      cdf μ x ≤
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (x + δ) + ε) :
    kolmogorovDistance μ
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ε + (Real.sqrt (2 * Real.pi))⁻¹ * δ :=
  (kolmogorovDistance_le_iff _ _ _).2 <|
    standardNormalCDFBound_of_shift_bounds μ hδ hlower hupper

/-- A function `H` is a `δ`-smoothing of the CDF of `μ` when its value at
`x` lies between the CDF values at the two endpoints of the `δ`-window about
`x`.  Compactly supported convolution kernels will supply this property. -/
def CDFSmoothingApproximation
    (μ : Measure ℝ) (H : ℝ → ℝ) (δ : ℝ) : Prop :=
  ∀ x, cdf μ (x - δ) ≤ H x ∧ H x ≤ cdf μ (x + δ)

/-- Average the translates of a CDF against a smoothing measure. -/
noncomputable def smoothedCDF
    (μ κ : Measure ℝ) (x : ℝ) : ℝ :=
  ∫ y, cdf μ (x - y) ∂κ

/-- A measure is supported in the closed smoothing window `[-δ, δ]`, stated
in the almost-everywhere form used directly by integration. -/
def IsSupportedInSmoothingWindow (κ : Measure ℝ) (δ : ℝ) : Prop :=
  ∀ᵐ y ∂κ, y ∈ Icc (-δ) δ

/-- The normalized Lebesgue probability measure on `[-δ, δ]`.  It is used
as a concrete compact smoothing kernel when `δ > 0`. -/
noncomputable def uniformSmoothingKernel (δ : ℝ) : Measure ℝ :=
  (ENNReal.ofReal (2 * δ))⁻¹ • volume.restrict (Icc (-δ) δ)

theorem uniformSmoothingKernel_isProbabilityMeasure
    {δ : ℝ} (hδ : 0 < δ) :
    IsProbabilityMeasure (uniformSmoothingKernel δ) := by
  constructor
  rw [uniformSmoothingKernel, Measure.smul_apply, Measure.restrict_apply
    MeasurableSet.univ, univ_inter, Real.volume_Icc]
  have htwo : 0 < 2 * δ := mul_pos (by norm_num) hδ
  rw [show δ - -δ = 2 * δ by ring, smul_eq_mul,
    ENNReal.inv_mul_cancel]
  · exact (ENNReal.ofReal_pos.mpr htwo).ne'
  · exact ENNReal.ofReal_ne_top

theorem uniformSmoothingKernel_supported
    (δ : ℝ) :
    IsSupportedInSmoothingWindow (uniformSmoothingKernel δ) δ := by
  unfold IsSupportedInSmoothingWindow uniformSmoothingKernel
  exact Measure.ae_smul_measure
    (ae_restrict_mem measurableSet_Icc) _

/-- The characteristic function of the normalized uniform kernel is `sinc`.
This is the explicit Fourier multiplier used by the smoothing construction. -/
theorem charFun_uniformSmoothingKernel
    {δ : ℝ} (hδ : 0 < δ) (t : ℝ) :
    MeasureTheory.charFun (uniformSmoothingKernel δ) t =
      Real.sinc (t * δ) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  by_cases ht : t = 0
  · subst t
    simp
  rw [MeasureTheory.charFun_apply_real, uniformSmoothingKernel,
    integral_smul_measure, ENNReal.toReal_inv]
  have htwo : 0 < 2 * δ := mul_pos (by norm_num) hδ
  rw [ENNReal.toReal_ofReal htwo.le]
  have hscale :
      (∫ x in -δ..δ,
          Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I)) =
        t⁻¹ •
          ∫ u in -(t * δ)..t * δ, Complex.exp (u * Complex.I) := by
    simpa only [Complex.ofReal_mul, mul_neg] using
      (intervalIntegral.integral_comp_mul_left
        (f := fun u : ℝ => Complex.exp ((u : ℂ) * Complex.I))
        (a := -δ) (b := δ) ht)
  have hset :
      (∫ x in Icc (-δ) δ,
          Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I)) =
        ∫ x in -δ..δ, Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I) := by
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (neg_le_self hδ.le)]
  rw [hset, hscale, integral_exp_mul_I_eq_sinc]
  simp only [Algebra.smul_def]
  change (((2 * δ)⁻¹ : ℝ) : ℂ) *
      (((t⁻¹ : ℝ) : ℂ) *
        (2 * (((t * δ : ℝ) : ℂ)) * ((Real.sinc (t * δ) : ℝ) : ℂ))) =
    ((Real.sinc (t * δ) : ℝ) : ℂ)
  norm_cast
  field_simp

theorem abs_sinc_le_inv_abs {x : ℝ} (hx : x ≠ 0) :
    |Real.sinc x| ≤ |x|⁻¹ := by
  rw [Real.sinc_of_ne_zero hx, abs_div]
  simpa only [one_div] using
    div_le_div_of_nonneg_right (Real.abs_sin_le_one x) (abs_nonneg x)

/-- A CDF composed with a measurable real function is integrable against
every finite measure, by the universal bound `0 ≤ cdf ≤ 1`. -/
theorem integrable_cdf_comp
    (mu kappa : Measure ℝ) [IsFiniteMeasure kappa]
    {f : ℝ → ℝ} (hf : Measurable f) :
    Integrable (fun y => cdf mu (f y)) kappa := by
  refine Integrable.of_bound
    (((monotone_cdf mu).measurable.comp hf).aestronglyMeasurable) 1 ?_
  exact ae_of_all kappa fun y => by
    rw [Real.norm_eq_abs, abs_of_nonneg (cdf_nonneg mu (f y))]
    exact cdf_le_one mu (f y)

theorem integrable_cdf_translate
    (μ κ : Measure ℝ) [IsFiniteMeasure κ] (x : ℝ) :
    Integrable (fun y => cdf μ (x - y)) κ := by
  refine Integrable.of_bound
    (((monotone_cdf μ).measurable.comp
      (measurable_const.sub measurable_id)).aestronglyMeasurable) 1 ?_
  exact ae_of_all κ fun y => by
    rw [Real.norm_eq_abs, abs_of_nonneg (cdf_nonneg μ (x - y))]
    exact cdf_le_one μ (x - y)

/-- Averaging an upper signed CDF error against a probability law costs no
constant: the integral is bounded by the same one-sided supremum. -/
theorem integral_cdfUpperError_comp_le
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    {f : ℝ → ℝ} (hf : Measurable f) :
    (∫ y, cdfUpperError mu nu (f y) ∂kappa) ≤
      upperCDFDistance mu nu := by
  have hmu := integrable_cdf_comp mu kappa hf
  have hnu := integrable_cdf_comp nu kappa hf
  calc
    (∫ y, cdfUpperError mu nu (f y) ∂kappa) ≤
        ∫ _y, upperCDFDistance mu nu ∂kappa := by
      apply integral_mono (hmu.sub hnu) (integrable_const _)
      intro y
      exact cdfUpperError_le_upperCDFDistance mu nu (f y)
    _ = upperCDFDistance mu nu := by simp

/-- Lower signed-error companion to `integral_cdfUpperError_comp_le`. -/
theorem integral_cdfLowerError_comp_le
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    {f : ℝ → ℝ} (hf : Measurable f) :
    (∫ y, cdfLowerError mu nu (f y) ∂kappa) ≤
      lowerCDFDistance mu nu := by
  have hmu := integrable_cdf_comp mu kappa hf
  have hnu := integrable_cdf_comp nu kappa hf
  calc
    (∫ y, cdfLowerError mu nu (f y) ∂kappa) ≤
        ∫ _y, lowerCDFDistance mu nu ∂kappa := by
      apply integral_mono (hnu.sub hmu) (integrable_const _)
      intro y
      exact cdfLowerError_le_lowerCDFDistance mu nu (f y)
    _ = lowerCDFDistance mu nu := by simp

/-- Averaging against a probability measure supported in `[-δ, δ]` gives
the promised CDF smoothing approximation.  This supplies the compact-support
half of the smoothing method independently of any Fourier representation. -/
theorem smoothedCDF_isSmoothingApproximation
    (μ κ : Measure ℝ) [IsProbabilityMeasure κ] {δ : ℝ}
    (hκ : IsSupportedInSmoothingWindow κ δ) :
    CDFSmoothingApproximation μ (smoothedCDF μ κ) δ := by
  intro x
  have hInt : Integrable (fun y => cdf μ (x - y)) κ :=
    integrable_cdf_translate μ κ x
  constructor
  · rw [smoothedCDF]
    calc
      cdf μ (x - δ) = ∫ _ : ℝ, cdf μ (x - δ) ∂κ := by simp
      _ ≤ ∫ y, cdf μ (x - y) ∂κ :=
        integral_mono_ae (integrable_const _) hInt <|
          hκ.mono fun y hy =>
            monotone_cdf μ (by linarith [hy.2])
  · rw [smoothedCDF]
    calc
      (∫ y, cdf μ (x - y) ∂κ) ≤
          ∫ _ : ℝ, cdf μ (x + δ) ∂κ :=
        integral_mono_ae hInt (integrable_const _) <|
          hκ.mono fun y hy =>
            monotone_cdf μ (by linarith [hy.1])
      _ = cdf μ (x + δ) := by simp

/-- The concrete normalized-uniform kernel realizes the abstract smoothing
interface at every positive window radius. -/
theorem uniformSmoothedCDF_isSmoothingApproximation
    (μ : Measure ℝ) {δ : ℝ} (hδ : 0 < δ) :
    CDFSmoothingApproximation μ
      (smoothedCDF μ (uniformSmoothingKernel δ)) δ := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  exact smoothedCDF_isSmoothingApproximation μ
    (uniformSmoothingKernel δ) (uniformSmoothingKernel_supported δ)

/-- Averaging CDF translates is exactly the CDF of convolution by the
smoothing measure.  This bridge exposes `MeasureTheory.charFun_conv` to the
remaining Fourier comparison. -/
theorem smoothedCDF_eq_cdf_conv
    (μ κ : Measure ℝ) [IsProbabilityMeasure μ] [IsProbabilityMeasure κ]
    (x : ℝ) :
    smoothedCDF μ κ x = cdf (κ ∗ μ) x := by
  have hInt : Integrable
      ((Iic x).indicator (fun _ : ℝ => (1 : ℝ))) (κ ∗ μ) :=
    (integrable_const (1 : ℝ)).indicator measurableSet_Iic
  rw [smoothedCDF]
  calc
    (∫ y, cdf μ (x - y) ∂κ) =
        ∫ y, ∫ z, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) (y + z) ∂μ ∂κ := by
      apply integral_congr_ae
      exact ae_of_all κ fun y => by
        change cdf μ (x - y) = _
        rw [cdf_eq_real]
        calc
          μ.real (Iic (x - y)) =
              ∫ z, (Iic (x - y)).indicator (fun _ : ℝ => (1 : ℝ)) z ∂μ :=
            (integral_indicator_one (s := Iic (x - y)) measurableSet_Iic).symm
          _ = ∫ z, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) (y + z) ∂μ := by
            apply integral_congr_ae
            exact ae_of_all μ fun z => by
              simp only [Set.indicator_apply, mem_Iic]
              by_cases hz : z ≤ x - y
              · have hyz : y + z ≤ x := by linarith
                simp [hz, hyz]
              · have hyz : ¬ y + z ≤ x := by intro h; apply hz; linarith
                simp [hz, hyz]
    _ = ∫ z, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) z ∂(κ ∗ μ) :=
      (integral_conv hInt).symm
    _ = cdf (κ ∗ μ) x := by
      calc
        (∫ z, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) z ∂(κ ∗ μ)) =
            (κ ∗ μ).real (Iic x) :=
          integral_indicator_one (s := Iic x) measurableSet_Iic
        _ = cdf (κ ∗ μ) x := (cdf_eq_real (κ ∗ μ) x).symm

/-- Convolution by an arbitrary probability law preserves the global
Lipschitz constant of the standard-normal CDF. -/
theorem standardNormalCDF_lipschitz_conv
    (kappa : Measure ℝ) [IsProbabilityMeasure kappa] (u v : ℝ) :
    |cdf (kappa ∗
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) u -
        cdf (kappa ∗
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) v| ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * |u - v| := by
  rw [← smoothedCDF_eq_cdf_conv
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa u,
    ← smoothedCDF_eq_cdf_conv
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa v]
  unfold smoothedCDF
  rw [← integral_sub
    (integrable_cdf_translate
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa u)
    (integrable_cdf_translate
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa v)]
  calc
    |∫ y, (cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (u - y) -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (v - y)) ∂kappa| ≤
        ∫ y, |cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (u - y) -
          cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (v - y)| ∂kappa :=
      abs_integral_le_integral_abs
    _ ≤ ∫ _y : ℝ, (Real.sqrt (2 * Real.pi))⁻¹ * |u - v| ∂kappa := by
      apply integral_mono
      · exact (integrable_cdf_translate
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa u).sub
          (integrable_cdf_translate
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa v) |>.abs
      · exact integrable_const _
      · intro y
        simpa only [sub_sub_sub_cancel_right] using
          standardNormalCDF_lipschitz (v - y) (u - y)
    _ = (Real.sqrt (2 * Real.pi))⁻¹ * |u - v| := by simp

/-- Convolution by an arbitrary probability law preserves the uniform
quadratic remainder of a positive Gaussian-smoothed CDF section.  The three
polynomial coefficients are obtained by averaging their pointwise Gaussian
counterparts. -/
theorem exists_quadratic_remainder_cdf_conv_scaledStandardNormal
    (kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    {σ : ℝ} (hσ : 0 < σ) (a : ℝ) :
    ∃ a0 a1 a2 : ℝ, ∀ y : ℝ,
      |cdf (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a + y) -
          (a0 + a1 * y + a2 * y ^ 2)| ≤
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let c0 : ℝ → ℝ := fun z => cdf gamma (a - z)
  let c1 : ℝ → ℝ := fun z =>
    ProbabilityTheory.gaussianPDFReal 0 1 ((a - z) / σ) / σ
  let c2 : ℝ → ℝ := fun z =>
    (-((a - z) / σ) *
      ProbabilityTheory.gaussianPDFReal 0 1 ((a - z) / σ)) / (2 * σ ^ 2)
  have hc0 : Integrable c0 kappa := by
    simpa only [c0] using integrable_cdf_translate gamma kappa a
  have hc1 : Integrable c1 kappa := by
    have harg : Measurable (fun z : ℝ => (a - z) / σ) := by fun_prop
    have hmeas := (ProbabilityTheory.measurable_gaussianPDFReal 0 1).comp harg
    refine Integrable.of_bound (by
      simpa only [c1] using (hmeas.div_const σ).aestronglyMeasurable)
      ((Real.sqrt (2 * Real.pi))⁻¹ / σ) ?_
    exact ae_of_all kappa fun z => by
      rw [Real.norm_eq_abs, abs_div, abs_of_pos hσ,
        abs_of_nonneg (ProbabilityTheory.gaussianPDFReal_nonneg 0 1 _)]
      exact div_le_div_of_nonneg_right (standardNormalPDF_le _) hσ.le
  have hc2 : Integrable c2 kappa := by
    have harg : Measurable (fun z : ℝ => (a - z) / σ) := by fun_prop
    have hpdf := (ProbabilityTheory.measurable_gaussianPDFReal 0 1).comp harg
    refine Integrable.of_bound (by
      simpa only [c2] using
        ((harg.neg.mul hpdf).div_const (2 * σ ^ 2)).aestronglyMeasurable)
      ((2 * (Real.sqrt (2 * Real.pi))⁻¹) / |2 * σ ^ 2|) ?_
    exact ae_of_all kappa fun z => by
      rw [Real.norm_eq_abs, abs_div]
      exact div_le_div_of_nonneg_right
        (abs_standardNormalPDF_deriv_le _) (abs_nonneg _)
  refine ⟨∫ z, c0 z ∂kappa, ∫ z, c1 z ∂kappa, ∫ z, c2 z ∂kappa, ?_⟩
  intro y
  have hf : Integrable (fun z => cdf gamma (a + y - z)) kappa :=
    integrable_cdf_translate gamma kappa (a + y)
  have hc1y : Integrable (fun z => c1 z * y) kappa := by
    simpa only [mul_comm] using hc1.const_mul y
  have hc2y : Integrable (fun z => c2 z * y ^ 2) kappa := by
    simpa only [mul_comm] using hc2.const_mul (y ^ 2)
  have hp : Integrable (fun z => c0 z + c1 z * y + c2 z * y ^ 2) kappa :=
    (hc0.add hc1y).add hc2y
  have hpoint : ∀ z,
      |cdf gamma (a + y - z) - (c0 z + c1 z * y + c2 z * y ^ 2)| ≤
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 := by
    intro z
    simpa only [gamma, c0, c1, c2] using
      (show |cdf (Measure.map (fun z : ℝ => σ * z)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a + y - z) -
          (cdf (Measure.map (fun z : ℝ => σ * z)
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a - z) +
            (ProbabilityTheory.gaussianPDFReal 0 1 ((a - z) / σ) / σ) * y +
            ((-((a - z) / σ) *
                ProbabilityTheory.gaussianPDFReal 0 1 ((a - z) / σ)) /
              (2 * σ ^ 2)) * y ^ 2)| ≤
            (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 by
        convert scaledStandardNormalCDF_quadratic_remainder_canonical hσ (a - z) y using 1 <;>
          ring)
  rw [← smoothedCDF_eq_cdf_conv gamma kappa (a + y)]
  unfold smoothedCDF
  have hpoly :
      (∫ z, c0 z + c1 z * y + c2 z * y ^ 2 ∂kappa) =
        (∫ z, c0 z ∂kappa) + (∫ z, c1 z ∂kappa) * y +
          (∫ z, c2 z ∂kappa) * y ^ 2 := by
    calc
      (∫ z, c0 z + c1 z * y + c2 z * y ^ 2 ∂kappa) =
          (∫ z, c0 z + c1 z * y ∂kappa) +
            ∫ z, c2 z * y ^ 2 ∂kappa :=
        integral_add (hc0.add hc1y) hc2y
      _ = ((∫ z, c0 z ∂kappa) + ∫ z, c1 z * y ∂kappa) +
          ∫ z, c2 z * y ^ 2 ∂kappa := by
        rw [integral_add hc0 hc1y]
      _ = _ := by rw [integral_mul_const, integral_mul_const]
  rw [← hpoly, ← integral_sub hf hp]
  calc
    |∫ z, cdf gamma (a + y - z) -
        (c0 z + c1 z * y + c2 z * y ^ 2) ∂kappa| ≤
        ∫ z, |cdf gamma (a + y - z) -
          (c0 z + c1 z * y + c2 z * y ^ 2)| ∂kappa :=
      abs_integral_le_integral_abs
    _ ≤ ∫ _z : ℝ,
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 ∂kappa := by
      apply integral_mono (hf.sub hp).abs (integrable_const _)
      exact hpoint
    _ = (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 := by
      simp

/-- Gaussian smoothing makes the one-summand replacement section smooth
enough for exact first/second-moment cancellation. -/
theorem abs_integral_cdf_conv_scaledStandardNormal_sub_standardNormal_le
    (kappa mu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    {σ : ℝ} (hσ : 0 < σ) (a : ℝ)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    |(∫ y, cdf (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a + y) ∂mu) -
        ∫ y, cdf (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a + y)
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) *
        ((∫ y, |y| ^ 3 ∂mu) +
          ∫ y, |y| ^ 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let f : ℝ → ℝ := fun y => cdf (kappa ∗ gamma) (a + y)
  have hfmu : Integrable f mu := by
    exact integrable_cdf_comp (kappa ∗ gamma) mu
      (f := fun y : ℝ => a + y) (by fun_prop)
  have hfGaussian : Integrable f
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    exact integrable_cdf_comp (kappa ∗ gamma)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      (f := fun y : ℝ => a + y) (by fun_prop)
  obtain ⟨a0, a1, a2, hrem⟩ :=
    exists_quadratic_remainder_cdf_conv_scaledStandardNormal kappa hσ a
  simpa only [gamma, f] using
    abs_integral_sub_standardNormal_le_of_quadratic_remainder
      mu f hfmu hfGaussian hmu1 hmu2 hmean hsecond hmu3
      a0 a1 a2 ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) hrem

/-- Subtraction-oriented form of the convolved Gaussian Taylor remainder,
matching the `x - y` convention in convolution CDF formulas. -/
theorem exists_quadratic_remainder_cdf_conv_scaledStandardNormal_sub
    (kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    {σ : ℝ} (hσ : 0 < σ) (a : ℝ) :
    ∃ a0 a1 a2 : ℝ, ∀ y : ℝ,
      |cdf (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a - y) -
          (a0 + a1 * y + a2 * y ^ 2)| ≤
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) * |y| ^ 3 := by
  obtain ⟨a0, a1, a2, hrem⟩ :=
    exists_quadratic_remainder_cdf_conv_scaledStandardNormal kappa hσ a
  refine ⟨a0, -a1, a2, fun y => ?_⟩
  convert hrem (-y) using 1 <;> simp only [abs_neg, neg_sq] <;> ring

theorem abs_integral_cdf_conv_scaledStandardNormal_sub_standardNormal_le_sub
    (kappa mu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    {σ : ℝ} (hσ : 0 < σ) (a : ℝ)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    |(∫ y, cdf (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a - y) ∂mu) -
        ∫ y, cdf (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) (a - y)
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) *
        ((∫ y, |y| ^ 3 ∂mu) +
          ∫ y, |y| ^ 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let f : ℝ → ℝ := fun y => cdf (kappa ∗ gamma) (a - y)
  have hfmu : Integrable f mu := by
    exact integrable_cdf_comp (kappa ∗ gamma) mu
      (f := fun y : ℝ => a - y) (by fun_prop)
  have hfGaussian : Integrable f
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    exact integrable_cdf_comp (kappa ∗ gamma)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      (f := fun y : ℝ => a - y) (by fun_prop)
  obtain ⟨a0, a1, a2, hrem⟩ :=
    exists_quadratic_remainder_cdf_conv_scaledStandardNormal_sub kappa hσ a
  simpa only [gamma, f] using
    abs_integral_sub_standardNormal_le_of_quadratic_remainder
      mu f hfmu hfGaussian hmu1 hmu2 hmean hsecond hmu3
      a0 a1 a2 ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) hrem

/-- Pointwise signed one-summand replacement after a common arbitrary-law
and positive-Gaussian smoothing factor. -/
theorem abs_cdfUpperError_gaussianSmoothedReplacement_le
    (kappa mu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    {σ : ℝ} (hσ : 0 < σ) (x : ℝ)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    |cdfUpperError
        (mu ∗ (kappa ∗ Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
        (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗
          (kappa ∗ Measure.map (fun z : ℝ => σ * z)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6) *
        ((∫ y, |y| ^ 3 ∂mu) +
          ∫ y, |y| ^ 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  have h := abs_integral_cdf_conv_scaledStandardNormal_sub_standardNormal_le_sub
    kappa mu hσ x hmu1 hmu2 hmean hsecond hmu3
  rw [cdfUpperError, ← smoothedCDF_eq_cdf_conv (kappa ∗ gamma) mu x,
    ← smoothedCDF_eq_cdf_conv (kappa ∗ gamma)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x]
  simpa only [smoothedCDF, gamma] using h

/-- A common convolution factor averages the signed upper CDF error exactly.
This identity retains cancellations that are lost after taking absolute
values or a concentration supremum. -/
theorem cdfUpperError_conv_left_eq_integral
    (mu nu kappa : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    [IsProbabilityMeasure kappa] (x : ℝ) :
    cdfUpperError (kappa ∗ mu) (kappa ∗ nu) x =
      ∫ y, cdfUpperError mu nu (x - y) ∂kappa := by
  rw [cdfUpperError, ← smoothedCDF_eq_cdf_conv mu kappa x,
    ← smoothedCDF_eq_cdf_conv nu kappa x]
  unfold smoothedCDF cdfUpperError
  rw [integral_sub (integrable_cdf_translate mu kappa x)
    (integrable_cdf_translate nu kappa x)]

/-- The corresponding exact averaging identity for the signed lower CDF
error. -/
theorem cdfLowerError_conv_left_eq_integral
    (mu nu kappa : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    [IsProbabilityMeasure kappa] (x : ℝ) :
    cdfLowerError (kappa ∗ mu) (kappa ∗ nu) x =
      ∫ y, cdfLowerError mu nu (x - y) ∂kappa := by
  simpa only [cdfLowerError_eq_upper_swap] using
    cdfUpperError_conv_left_eq_integral nu mu kappa x

/-- Two spatial cancellations turn convolution by a positively scaled
standard normal into a second-order weighted pointwise bound for the upper
signed CDF error. -/
theorem abs_cdfUpperError_map_mul_standardNormal_conv_le_of_cancellations
    (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {σ : ℝ} (hσ : 0 < σ)
    (he : Integrable (cdfUpperError mu nu))
    (he1 : Integrable (fun z => z * cdfUpperError mu nu z))
    (he2 : Integrable (fun z => |z| ^ 2 * |cdfUpperError mu nu z|))
    (hzero : (∫ z, cdfUpperError mu nu z) = 0)
    (hone : (∫ z, z * cdfUpperError mu nu z) = 0)
    (x : ℝ) :
    |cdfUpperError
      (Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ mu)
      (Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ nu) x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3) *
        ∫ z, |z| ^ 2 * |cdfUpperError mu nu z| := by
  let kappa := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure kappa :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  have hmeas : Measurable (cdfUpperError mu nu) :=
    (monotone_cdf mu).measurable.sub (monotone_cdf nu).measurable
  rw [cdfUpperError_conv_left_eq_integral]
  rw [integral_comp_sub_map_mul_standardNormalLaw_eq_integral_scaledPDF
    (cdfUpperError mu nu) hmeas hσ x]
  exact abs_integral_scaledStandardNormalPDF_mul_le_of_cancellations
    (cdfUpperError mu nu) he he1 he2 hzero hone hσ x

/-- Lower signed-error companion to the scaled-normal cancellation bound. -/
theorem abs_cdfLowerError_map_mul_standardNormal_conv_le_of_cancellations
    (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {σ : ℝ} (hσ : 0 < σ)
    (he : Integrable (cdfLowerError mu nu))
    (he1 : Integrable (fun z => z * cdfLowerError mu nu z))
    (he2 : Integrable (fun z => |z| ^ 2 * |cdfLowerError mu nu z|))
    (hzero : (∫ z, cdfLowerError mu nu z) = 0)
    (hone : (∫ z, z * cdfLowerError mu nu z) = 0)
    (x : ℝ) :
    |cdfLowerError
      (Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ mu)
      (Measure.map (fun z : ℝ => σ * z)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ nu) x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / σ ^ 3) *
        ∫ z, |z| ^ 2 * |cdfLowerError mu nu z| := by
  simpa only [cdfLowerError_eq_upper_swap] using
    abs_cdfUpperError_map_mul_standardNormal_conv_le_of_cancellations
      nu mu hσ
      (by simpa only [cdfLowerError_eq_upper_swap] using he)
      (by simpa only [cdfLowerError_eq_upper_swap] using he1)
      (by simpa only [cdfLowerError_eq_upper_swap] using he2)
      (by simpa only [cdfLowerError_eq_upper_swap] using hzero)
      (by simpa only [cdfLowerError_eq_upper_swap] using hone) x

/-- A centered Gaussian density is bounded by its value at the mean. -/
theorem gaussianPDFReal_zero_le_peak
    {v : ℝ≥0} (hv : v ≠ 0) (y : ℝ) :
    ProbabilityTheory.gaussianPDFReal 0 v y ≤
      (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ := by
  rw [ProbabilityTheory.gaussianPDFReal]
  calc
    (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ *
        Real.exp (-(y - 0) ^ 2 / (2 * (v : ℝ))) ≤
        (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ * 1 := by
      apply mul_le_mul_of_nonneg_left _
        (inv_nonneg.mpr (Real.sqrt_nonneg _))
      rw [Real.exp_le_one_iff]
      exact div_nonpos_of_nonpos_of_nonneg
        (neg_nonpos.mpr (sq_nonneg (y - 0))) (by positivity)
    _ = (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ := mul_one _

/-- Convolution by a centered nondegenerate Gaussian maps the spatial `L¹`
norm of an upper signed CDF error to a pointwise bound using the exact peak
of the Gaussian density. -/
theorem abs_cdfUpperError_gaussianReal_conv_le_integral
    (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {v : ℝ≥0} (hv : v ≠ 0)
    (he : Integrable (cdfUpperError mu nu)) (x : ℝ) :
    |cdfUpperError
        (ProbabilityTheory.gaussianReal 0 v ∗ mu)
        (ProbabilityTheory.gaussianReal 0 v ∗ nu) x| ≤
      (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ *
        ∫ z, |cdfUpperError mu nu z| := by
  let kappa := ProbabilityTheory.gaussianReal 0 v
  letI : IsProbabilityMeasure kappa := inferInstance
  let C : ℝ := (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹
  let e : ℝ → ℝ := cdfUpperError mu nu
  have heTrans : Integrable (fun y => |e (x - y)|) := by
    have h := (he.norm.comp_add_right x).comp_neg
    simpa only [e, sub_eq_add_neg, add_comm, Real.norm_eq_abs] using h
  have htrans : (∫ y, |e (x - y)|) = ∫ z, |e z| := by
    calc
      (∫ y, |e (x - y)|) = ∫ y, |e (-y + x)| := by
        congr 1
        funext y
        congr 2
        ring
      _ = ∫ y, |e (y + x)| :=
        MeasureTheory.integral_neg_eq_self (fun y => |e (y + x)|) volume
      _ = ∫ z, |e z| :=
        MeasureTheory.integral_add_right_eq_self (fun z => |e z|) x
  have hpdfProd : Integrable (fun y =>
      ProbabilityTheory.gaussianPDFReal 0 v y * |e (x - y)|) := by
    apply heTrans.bdd_mul
    · exact (ProbabilityTheory.measurable_gaussianPDFReal 0 v).aestronglyMeasurable
    · exact ae_of_all _ fun y => by
        rw [Real.norm_eq_abs, abs_of_nonneg
          (ProbabilityTheory.gaussianPDFReal_nonneg 0 v y)]
        exact gaussianPDFReal_zero_le_peak hv y
  have hCTrans : Integrable (fun y => C * |e (x - y)|) :=
    heTrans.const_mul C
  rw [cdfUpperError_conv_left_eq_integral]
  calc
    |∫ y, e (x - y) ∂kappa| ≤ ∫ y, |e (x - y)| ∂kappa := by
      simpa only [Real.norm_eq_abs] using
        (norm_integral_le_integral_norm (fun y => e (x - y)) (μ := kappa))
    _ = ∫ y, ProbabilityTheory.gaussianPDFReal 0 v y *
        |e (x - y)| := by
      rw [ProbabilityTheory.integral_gaussianReal_eq_integral_smul hv]
      simp only [smul_eq_mul]
    _ ≤ ∫ y, C * |e (x - y)| := by
      apply integral_mono hpdfProd hCTrans
      intro y
      exact mul_le_mul_of_nonneg_right
        (gaussianPDFReal_zero_le_peak hv y) (abs_nonneg _)
    _ = C * ∫ z, |e z| := by rw [integral_const_mul, htrans]

/-- Lower signed-error companion to the Gaussian `L¹ -> L∞` estimate. -/
theorem abs_cdfLowerError_gaussianReal_conv_le_integral
    (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {v : ℝ≥0} (hv : v ≠ 0)
    (he : Integrable (cdfLowerError mu nu)) (x : ℝ) :
    |cdfLowerError
        (ProbabilityTheory.gaussianReal 0 v ∗ mu)
        (ProbabilityTheory.gaussianReal 0 v ∗ nu) x| ≤
      (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ *
        ∫ z, |cdfLowerError mu nu z| := by
  simpa only [cdfLowerError_eq_upper_swap] using
    abs_cdfUpperError_gaussianReal_conv_le_integral nu mu hv
      (by simpa only [cdfLowerError_eq_upper_swap] using he) x

/-- Coefficient of the preceding normalized sum in one Gaussian replacement
step. -/
noncomputable def normalizedGaussianKeepCoefficient (N : ℕ) : ℝ :=
  Real.sqrt (N + 1 : ℝ) * (Real.sqrt (N + 2 : ℝ))⁻¹

/-- Coefficient of the fresh standard Gaussian in one normalized replacement
step. -/
noncomputable def normalizedGaussianInnovationCoefficient (N : ℕ) : ℝ :=
  (Real.sqrt (N + 2 : ℝ))⁻¹

/-- One Gaussian-stationary keep/innovation step applied to a real
probability law. -/
noncomputable def normalizedGaussianStepLaw (N : ℕ) (nu : Measure ℝ) : Measure ℝ :=
  Measure.map
      (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗
    Measure.map (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu

theorem normalizedGaussianKeepCoefficient_pos (N : ℕ) :
    0 < normalizedGaussianKeepCoefficient N := by
  unfold normalizedGaussianKeepCoefficient
  positivity

theorem normalizedGaussianInnovationCoefficient_pos (N : ℕ) :
    0 < normalizedGaussianInnovationCoefficient N := by
  unfold normalizedGaussianInnovationCoefficient
  positivity

/-- The Gaussian density peak and keep-coordinate Jacobian simplify to the
closed coefficient used by the normalized smoothing estimate. -/
theorem normalizedGaussianDensityPeak_mul_keep (N : ℕ) :
    (Real.sqrt (2 * Real.pi *
      normalizedGaussianInnovationCoefficient N ^ 2))⁻¹ *
        normalizedGaussianKeepCoefficient N =
      Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [show 2 * Real.pi * normalizedGaussianInnovationCoefficient N ^ 2 =
      (2 * Real.pi) * normalizedGaussianInnovationCoefficient N ^ 2 by ring,
    Real.sqrt_mul (by positivity : (0 : ℝ) ≤ 2 * Real.pi),
    Real.sqrt_sq_eq_abs,
    abs_of_pos (normalizedGaussianInnovationCoefficient_pos N)]
  unfold normalizedGaussianKeepCoefficient normalizedGaussianInnovationCoefficient
  field_simp

/-- Exact simplification of the second-order cancellation coefficient: the
cubic keep Jacobian cancels the cubic innovation denominator, leaving the
three-halves power of the preceding sample count. -/
theorem normalizedGaussianCancellationCoefficient (N : ℕ) :
    (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) /
        normalizedGaussianInnovationCoefficient N ^ 3) *
      normalizedGaussianKeepCoefficient N ^ 3 =
    ((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
      (Real.sqrt (N + 1 : ℝ)) ^ 3 := by
  unfold normalizedGaussianKeepCoefficient normalizedGaussianInnovationCoefficient
  field_simp

theorem normalizedGaussianCoefficients_sq_add (N : ℕ) :
    normalizedGaussianKeepCoefficient N ^ 2 +
        normalizedGaussianInnovationCoefficient N ^ 2 = 1 := by
  unfold normalizedGaussianKeepCoefficient normalizedGaussianInnovationCoefficient
  rw [mul_pow, inv_pow,
    Real.sq_sqrt (by positivity : (0 : ℝ) ≤ N + 1),
    Real.sq_sqrt (by positivity : (0 : ℝ) ≤ N + 2)]
  field_simp
  ring

/-- Integrability of the identity is preserved by scalar pushforward. -/
theorem integrable_id_map_mul_real (mu : Measure ℝ) (c : ℝ)
    (hmu1 : Integrable (fun y : ℝ => y) mu) :
    Integrable (fun y : ℝ => y) (Measure.map (fun z : ℝ => c * z) mu) := by
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  simpa only [Function.comp_apply] using hmu1.const_mul c

/-- The first moment of a scalar pushforward. -/
theorem integral_id_map_mul_real (mu : Measure ℝ) (c : ℝ) :
    (∫ y : ℝ, y ∂Measure.map (fun z : ℝ => c * z) mu) = c * ∫ y, y ∂mu := by
  rw [integral_map (by fun_prop) (by fun_prop), integral_const_mul]

/-- Integrability of the square is preserved by scalar pushforward. -/
theorem integrable_sq_map_mul_real (mu : Measure ℝ) (c : ℝ)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu) :
    Integrable (fun y : ℝ => y ^ 2) (Measure.map (fun z : ℝ => c * z) mu) := by
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  change Integrable (fun z : ℝ => (c * z) ^ 2) mu
  simpa only [mul_pow] using hmu2.const_mul (c ^ 2)

/-- The second moment of a scalar pushforward. -/
theorem integral_sq_map_mul_real (mu : Measure ℝ) (c : ℝ) :
    (∫ y : ℝ, y ^ 2 ∂Measure.map (fun z : ℝ => c * z) mu) =
      c ^ 2 * ∫ y, y ^ 2 ∂mu := by
  rw [integral_map (by fun_prop) (by fun_prop)]
  simp only [mul_pow]
  rw [integral_const_mul]

/-- Integrability of the third absolute moment is preserved by scalar
pushforward. -/
theorem integrable_abs_cube_map_mul_real (mu : Measure ℝ) (c : ℝ)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    Integrable (fun y : ℝ => |y| ^ 3) (Measure.map (fun z : ℝ => c * z) mu) := by
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  change Integrable (fun z : ℝ => |c * z| ^ 3) mu
  simpa only [abs_mul, mul_pow] using hmu3.const_mul (|c| ^ 3)

/-- Convolution preserves finite first moments. -/
theorem integrable_id_conv (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hnu1 : Integrable (fun y : ℝ => y) nu) :
    Integrable (fun y : ℝ => y) (mu ∗ nu) := by
  unfold Measure.conv
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  exact (hmu1.comp_fst nu).add (hnu1.comp_snd mu)

/-- First moments add under convolution of probability laws. -/
theorem integral_id_conv (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hnu1 : Integrable (fun y : ℝ => y) nu) :
    (∫ y : ℝ, y ∂(mu ∗ nu)) = (∫ y, y ∂mu) + ∫ y, y ∂nu := by
  unfold Measure.conv
  rw [integral_map (by fun_prop) (by fun_prop)]
  rw [integral_add (hmu1.comp_fst nu) (hnu1.comp_snd mu)]
  rw [integral_fun_fst (μ := mu) (ν := nu) (fun x : ℝ => x),
    integral_fun_snd (μ := mu) (ν := nu) (fun x : ℝ => x)]
  simp

/-- Convolution preserves finite second moments when both first moments are
finite. -/
theorem integrable_sq_conv (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu) :
    Integrable (fun y : ℝ => y ^ 2) (mu ∗ nu) := by
  unfold Measure.conv
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  have hmu2p := hmu2.comp_fst nu
  have hnu2p := hnu2.comp_snd mu
  have hcross := hmu1.mul_prod hnu1
  apply Integrable.congr ((hmu2p.add (hcross.const_mul 2)).add hnu2p)
  filter_upwards [] with z
  simp only [Pi.add_apply, Function.comp_apply]
  ring

/-- The second moment of a convolution, including its mean cross term. -/
theorem integral_sq_conv (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu) :
    (∫ y : ℝ, y ^ 2 ∂(mu ∗ nu)) =
      (∫ y, y ^ 2 ∂mu) + 2 * (∫ y, y ∂mu) * (∫ y, y ∂nu) +
        ∫ y, y ^ 2 ∂nu := by
  unfold Measure.conv
  rw [integral_map (by fun_prop) (by fun_prop)]
  have hmu2p := hmu2.comp_fst nu
  have hnu2p := hnu2.comp_snd mu
  have hcross := hmu1.mul_prod hnu1
  have hmiddle : Integrable (fun z : ℝ × ℝ => 2 * (z.1 * z.2)) (mu.prod nu) :=
    hcross.const_mul 2
  calc
    (∫ z : ℝ × ℝ, (z.1 + z.2) ^ 2 ∂mu.prod nu) =
        ∫ z : ℝ × ℝ, z.1 ^ 2 + 2 * (z.1 * z.2) + z.2 ^ 2 ∂mu.prod nu := by
      apply integral_congr_ae
      filter_upwards [] with z
      ring
    _ = (∫ z : ℝ × ℝ, z.1 ^ 2 + 2 * (z.1 * z.2) ∂mu.prod nu) +
        ∫ z : ℝ × ℝ, z.2 ^ 2 ∂mu.prod nu := by
      convert integral_add (hmu2p.add hmiddle) hnu2p using 1
    _ = ((∫ z : ℝ × ℝ, z.1 ^ 2 ∂mu.prod nu) +
        ∫ z : ℝ × ℝ, 2 * (z.1 * z.2) ∂mu.prod nu) +
        ∫ z : ℝ × ℝ, z.2 ^ 2 ∂mu.prod nu := by
      rw [integral_add hmu2p hmiddle]
    _ = _ := by
      rw [integral_fun_fst (μ := mu) (ν := nu) (fun x : ℝ => x ^ 2),
        integral_fun_snd (μ := mu) (ν := nu) (fun x : ℝ => x ^ 2),
        integral_const_mul,
        integral_prod_mul (μ := mu) (ν := nu) (fun x : ℝ => x) (fun x : ℝ => x)]
      simp only [probReal_univ, one_smul]
      ring

/-- Convolution preserves finite third absolute moments. -/
theorem integrable_abs_cube_conv (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu) :
    Integrable (fun y : ℝ => |y| ^ 3) (mu ∗ nu) := by
  have hmuLp : MemLp (fun y : ℝ => y) 3 mu := by
    rw [← integrable_norm_rpow_iff (p := (3 : ℝ≥0∞)) (by fun_prop)
      (by norm_num) (by norm_num)]
    norm_num
    simpa only [Real.norm_eq_abs] using hmu3
  have hnuLp : MemLp (fun y : ℝ => y) 3 nu := by
    rw [← integrable_norm_rpow_iff (p := (3 : ℝ≥0∞)) (by fun_prop)
      (by norm_num) (by norm_num)]
    norm_num
    simpa only [Real.norm_eq_abs] using hnu3
  have hconv : MemLp (fun y : ℝ => y) 3 (mu ∗ nu) := by
    unfold Measure.conv
    rw [memLp_map_measure_iff (by fun_prop) (by fun_prop)]
    exact (hmuLp.comp_fst nu).add (hnuLp.comp_snd mu)
  simpa only [Real.norm_eq_abs] using hconv.integrable_norm_pow (by norm_num)

instance normalizedGaussianStepLaw_isProbabilityMeasure
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu] :
    IsProbabilityMeasure (normalizedGaussianStepLaw N nu) := by
  unfold normalizedGaussianStepLaw
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  infer_instance

/-- The Gaussian keep/innovation step preserves finite first moments. -/
theorem integrable_normalizedGaussianStepLaw_id
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu1 : Integrable (fun y : ℝ => y) nu) :
    Integrable (fun y : ℝ => y) (normalizedGaussianStepLaw N nu) := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let gammaScaled := Measure.map
    (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma
  let nuScaled := Measure.map
    (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu
  letI : IsProbabilityMeasure gammaScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure nuScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  change Integrable (fun y : ℝ => y) (gammaScaled ∗ nuScaled)
  apply integrable_id_conv gammaScaled nuScaled
  · exact integrable_id_map_mul_real gamma _ integrable_standardNormal_id
  · exact integrable_id_map_mul_real nu _ hnu1

/-- The Gaussian keep/innovation step preserves centering. -/
theorem integral_normalizedGaussianStepLaw_id
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hmean : (∫ y : ℝ, y ∂nu) = 0) :
    (∫ y : ℝ, y ∂normalizedGaussianStepLaw N nu) = 0 := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let gammaScaled := Measure.map
    (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma
  let nuScaled := Measure.map
    (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu
  letI : IsProbabilityMeasure gammaScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure nuScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  change (∫ y : ℝ, y ∂(gammaScaled ∗ nuScaled)) = 0
  rw [integral_id_conv gammaScaled nuScaled
      (integrable_id_map_mul_real gamma _ integrable_standardNormal_id)
      (integrable_id_map_mul_real nu _ hnu1),
    integral_id_map_mul_real, integral_id_map_mul_real,
    integral_standardNormal_id, hmean]
  ring

/-- The Gaussian keep/innovation step preserves finite second moments. -/
theorem integrable_normalizedGaussianStepLaw_sq
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu) :
    Integrable (fun y : ℝ => y ^ 2) (normalizedGaussianStepLaw N nu) := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let gammaScaled := Measure.map
    (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma
  let nuScaled := Measure.map
    (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu
  letI : IsProbabilityMeasure gammaScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure nuScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  change Integrable (fun y : ℝ => y ^ 2) (gammaScaled ∗ nuScaled)
  exact integrable_sq_conv gammaScaled nuScaled
    (integrable_id_map_mul_real gamma _ integrable_standardNormal_id)
    (integrable_id_map_mul_real nu _ hnu1)
    (integrable_sq_map_mul_real gamma _ integrable_standardNormal_sq)
    (integrable_sq_map_mul_real nu _ hnu2)

/-- The Gaussian keep/innovation step preserves unit second moment on
centered input laws. -/
theorem integral_normalizedGaussianStepLaw_sq
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu1 : Integrable (fun y : ℝ => y) nu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu)
    (hmean : (∫ y : ℝ, y ∂nu) = 0)
    (hsecond : (∫ y : ℝ, y ^ 2 ∂nu) = 1) :
    (∫ y : ℝ, y ^ 2 ∂normalizedGaussianStepLaw N nu) = 1 := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let gammaScaled := Measure.map
    (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma
  let nuScaled := Measure.map
    (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu
  letI : IsProbabilityMeasure gammaScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure nuScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  change (∫ y : ℝ, y ^ 2 ∂(gammaScaled ∗ nuScaled)) = 1
  rw [integral_sq_conv gammaScaled nuScaled
      (integrable_id_map_mul_real gamma _ integrable_standardNormal_id)
      (integrable_id_map_mul_real nu _ hnu1)
      (integrable_sq_map_mul_real gamma _ integrable_standardNormal_sq)
      (integrable_sq_map_mul_real nu _ hnu2),
    integral_sq_map_mul_real, integral_sq_map_mul_real,
    integral_id_map_mul_real, integral_id_map_mul_real,
    integral_standardNormal_id, integral_standardNormal_sq, hmean, hsecond]
  rw [show normalizedGaussianInnovationCoefficient N ^ 2 * 1 +
      2 * (normalizedGaussianInnovationCoefficient N * 0) *
        (normalizedGaussianKeepCoefficient N * 0) +
      normalizedGaussianKeepCoefficient N ^ 2 * 1 =
      normalizedGaussianKeepCoefficient N ^ 2 +
        normalizedGaussianInnovationCoefficient N ^ 2 by ring]
  exact normalizedGaussianCoefficients_sq_add N

/-- The Gaussian keep/innovation step preserves finiteness of the third
absolute moment. -/
theorem integrable_normalizedGaussianStepLaw_abs_cube
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu) :
    Integrable (fun y : ℝ => |y| ^ 3) (normalizedGaussianStepLaw N nu) := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let gammaScaled := Measure.map
    (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma
  let nuScaled := Measure.map
    (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu
  letI : IsProbabilityMeasure gammaScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure nuScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  change Integrable (fun y : ℝ => |y| ^ 3) (gammaScaled ∗ nuScaled)
  exact integrable_abs_cube_conv gammaScaled nuScaled
    (integrable_abs_cube_map_mul_real gamma _ integrable_standardNormal_abs_cube)
    (integrable_abs_cube_map_mul_real nu _ hnu3)

/-- For a real law, the `L³` condition on the identity is exactly finiteness
of the third absolute moment. -/
theorem memLp_id_three_iff_integrable_abs_cube (mu : Measure ℝ) :
    MemLp (fun y : ℝ => y) 3 mu ↔ Integrable (fun y : ℝ => |y| ^ 3) mu := by
  rw [← integrable_norm_rpow_iff (p := (3 : ℝ≥0∞)) (by fun_prop)
    (by norm_num) (by norm_num)]
  norm_num [Real.norm_eq_abs]

/-- Minkowski's inequality for the identity under convolution of real
probability laws. -/
theorem eLpNorm_id_conv_le (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu] :
    eLpNorm (fun y : ℝ => y) 3 (mu ∗ nu) ≤
      eLpNorm (fun y : ℝ => y) 3 mu + eLpNorm (fun y : ℝ => y) 3 nu := by
  unfold Measure.conv
  rw [eLpNorm_map_measure (by fun_prop) (by fun_prop)]
  calc
    eLpNorm ((fun z : ℝ × ℝ => z.1) + fun z => z.2) 3 (mu.prod nu) ≤
        eLpNorm (fun z : ℝ × ℝ => z.1) 3 (mu.prod nu) +
          eLpNorm (fun z : ℝ × ℝ => z.2) 3 (mu.prod nu) := by
      exact eLpNorm_add_le (by fun_prop) (by fun_prop) (by norm_num)
    _ = _ := by
      rw [show (fun z : ℝ × ℝ => z.1) = (fun y : ℝ => y) ∘ Prod.fst by rfl,
        show (fun z : ℝ × ℝ => z.2) = (fun y : ℝ => y) ∘ Prod.snd by rfl,
        eLpNorm_comp_measurePreserving (by fun_prop)
          (measurePreserving_fst (μ := mu) (ν := nu)),
        eLpNorm_comp_measurePreserving (by fun_prop)
          (measurePreserving_snd (μ := mu) (ν := nu))]

/-- Real-valued `L³` Minkowski inequality for convolution. -/
theorem lpNorm_id_conv_le (mu nu : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu3 : MemLp (fun y : ℝ => y) 3 mu)
    (hnu3 : MemLp (fun y : ℝ => y) 3 nu) :
    lpNorm (fun y : ℝ => y) 3 (mu ∗ nu) ≤
      lpNorm (fun y : ℝ => y) 3 mu + lpNorm (fun y : ℝ => y) 3 nu := by
  have hconv : MemLp (fun y : ℝ => y) 3 (mu ∗ nu) := by
    unfold Measure.conv
    rw [memLp_map_measure_iff (by fun_prop) (by fun_prop)]
    exact (hmu3.comp_fst nu).add (hnu3.comp_snd mu)
  rw [← toReal_eLpNorm hconv.aestronglyMeasurable,
    ← toReal_eLpNorm hmu3.aestronglyMeasurable,
    ← toReal_eLpNorm hnu3.aestronglyMeasurable,
    ← ENNReal.toReal_add hmu3.eLpNorm_ne_top hnu3.eLpNorm_ne_top]
  exact ENNReal.toReal_mono (ENNReal.add_ne_top.2
    ⟨hmu3.eLpNorm_ne_top, hnu3.eLpNorm_ne_top⟩)
    (eLpNorm_id_conv_le mu nu)

/-- The real `L³` norm of the identity scales by the absolute value of a
scalar pushforward. -/
theorem lpNorm_id_map_mul_real (mu : Measure ℝ) (c : ℝ) :
    lpNorm (fun y : ℝ => y) 3 (Measure.map (fun z : ℝ => c * z) mu) =
      |c| * lpNorm (fun y : ℝ => y) 3 mu := by
  rw [← toReal_eLpNorm (p := (3 : ℝ≥0∞)) (by fun_prop),
    eLpNorm_map_measure (by fun_prop) (by fun_prop)]
  change (eLpNorm (c • (fun y : ℝ => y)) 3 mu).toReal = _
  rw [eLpNorm_const_smul, ENNReal.toReal_mul, toReal_eLpNorm (by fun_prop)]
  simp [Real.norm_eq_abs]

/-- Quantitative third-moment-root envelope for one Gaussian
keep/innovation step. -/
theorem lpNorm_normalizedGaussianStepLaw_le
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu) :
    lpNorm (fun y : ℝ => y) 3 (normalizedGaussianStepLaw N nu) ≤
      normalizedGaussianInnovationCoefficient N *
          lpNorm (fun y : ℝ => y) 3
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        normalizedGaussianKeepCoefficient N * lpNorm (fun y : ℝ => y) 3 nu := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let gammaScaled := Measure.map
    (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma
  let nuScaled := Measure.map
    (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu
  letI : IsProbabilityMeasure gammaScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure nuScaled := Measure.isProbabilityMeasure_map (by fun_prop)
  have hgammaScaled3 : MemLp (fun y : ℝ => y) 3 gammaScaled :=
    (memLp_id_three_iff_integrable_abs_cube gammaScaled).2
      (integrable_abs_cube_map_mul_real gamma _ integrable_standardNormal_abs_cube)
  have hnuScaled3 : MemLp (fun y : ℝ => y) 3 nuScaled :=
    (memLp_id_three_iff_integrable_abs_cube nuScaled).2
      (integrable_abs_cube_map_mul_real nu _ hnu3)
  change lpNorm (fun y : ℝ => y) 3 (gammaScaled ∗ nuScaled) ≤ _
  calc
    lpNorm (fun y : ℝ => y) 3 (gammaScaled ∗ nuScaled) ≤
        lpNorm (fun y : ℝ => y) 3 gammaScaled +
          lpNorm (fun y : ℝ => y) 3 nuScaled :=
      lpNorm_id_conv_le gammaScaled nuScaled hgammaScaled3 hnuScaled3
    _ = |normalizedGaussianInnovationCoefficient N| *
          lpNorm (fun y : ℝ => y) 3 gamma +
        |normalizedGaussianKeepCoefficient N| * lpNorm (fun y : ℝ => y) 3 nu := by
      rw [lpNorm_id_map_mul_real, lpNorm_id_map_mul_real]
    _ = _ := by
      rw [abs_of_pos (normalizedGaussianInnovationCoefficient_pos N),
        abs_of_pos (normalizedGaussianKeepCoefficient_pos N)]

/-- A real probability law with zero mean, unit second moment, finite third
absolute moment, and a specified `L³` bound.  This is the admissible domain
for moment-sensitive estimates on the Gaussian keep/innovation operator. -/
structure IsStandardizedThirdMomentLaw (nu : Measure ℝ) (B : ℝ) : Prop where
  probability : IsProbabilityMeasure nu
  integrable_id : Integrable (fun y : ℝ => y) nu
  centered : (∫ y : ℝ, y ∂nu) = 0
  integrable_sq : Integrable (fun y : ℝ => y ^ 2) nu
  unit_second_moment : (∫ y : ℝ, y ^ 2 ∂nu) = 1
  integrable_abs_cube : Integrable (fun y : ℝ => |y| ^ 3) nu
  lpNorm_three_le : lpNorm (fun y : ℝ => y) 3 nu ≤ B

/-- The standard-normal law belongs to the bounded standardized-law domain
at its own `L³` norm. -/
theorem standardNormal_isStandardizedThirdMomentLaw :
    IsStandardizedThirdMomentLaw
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      (lpNorm (fun y : ℝ => y) 3
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  exact ⟨inferInstance, integrable_standardNormal_id, integral_standardNormal_id,
    integrable_standardNormal_sq, integral_standardNormal_sq,
    integrable_standardNormal_abs_cube, le_rfl⟩

/-- The exact bounded standardized-law domain transported by one Gaussian
keep/innovation step. -/
theorem IsStandardizedThirdMomentLaw.normalizedGaussianStepLaw
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) (N : ℕ) :
    IsStandardizedThirdMomentLaw (normalizedGaussianStepLaw N nu)
      (normalizedGaussianInnovationCoefficient N *
          lpNorm (fun y : ℝ => y) 3
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        normalizedGaussianKeepCoefficient N * B) := by
  letI : IsProbabilityMeasure nu := h.probability
  refine ⟨inferInstance,
    integrable_normalizedGaussianStepLaw_id N nu h.integrable_id,
    integral_normalizedGaussianStepLaw_id N nu h.integrable_id h.centered,
    integrable_normalizedGaussianStepLaw_sq N nu h.integrable_id h.integrable_sq,
    integral_normalizedGaussianStepLaw_sq N nu h.integrable_id h.integrable_sq
      h.centered h.unit_second_moment,
    integrable_normalizedGaussianStepLaw_abs_cube N nu h.integrable_abs_cube, ?_⟩
  refine (lpNorm_normalizedGaussianStepLaw_le N nu h.integrable_abs_cube).trans ?_
  exact add_le_add_right
    (mul_le_mul_of_nonneg_left h.lpNorm_three_le
      (normalizedGaussianKeepCoefficient_pos N).le) _

/-- The standard normal law is stationary under the precise Gaussian
keep/innovation split in the normalized leave-one-out recurrence. -/
theorem standardNormalLaw_normalizedGaussian_step (N : ℕ) :
    normalizedGaussianStepLaw N
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw =
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  unfold normalizedGaussianStepLaw
  rw [Measure.conv_comm,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
    ProbabilityTheory.gaussianReal_map_const_mul,
    ProbabilityTheory.gaussianReal_map_const_mul,
    ProbabilityTheory.gaussianReal_conv_gaussianReal]
  congr 2
  · ring
  · rw [← NNReal.coe_inj]
    simp only [NNReal.coe_add, NNReal.coe_mk, NNReal.coe_one, mul_one]
    exact normalizedGaussianCoefficients_sq_add N

/-- The affine Gaussian average in the normalized signed recurrence is
exactly one step of the Gaussian-stationary keep/innovation operator. -/
theorem integral_cdfUpperError_normalizedGaussian_affine_eq
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (N : ℕ) (x : ℝ) :
    (∫ y, cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      ((Real.sqrt (N + 1 : ℝ))⁻¹ *
        (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y))
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
      cdfUpperError
        (normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := inferInstance
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) gamma) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  calc
    (∫ y, cdfUpperError nu gamma
        ((Real.sqrt (N + 1 : ℝ))⁻¹ *
          (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y)) ∂gamma) =
        ∫ y, cdfUpperError nu gamma
          ((x - normalizedGaussianInnovationCoefficient N * y) /
            normalizedGaussianKeepCoefficient N) ∂gamma := by
      apply integral_congr_ae
      filter_upwards [] with y
      congr 1
      unfold normalizedGaussianKeepCoefficient
        normalizedGaussianInnovationCoefficient
      have h1 : Real.sqrt (N + 1 : ℝ) ≠ 0 := by positivity
      have h2 : Real.sqrt (N + 2 : ℝ) ≠ 0 := by positivity
      field_simp
    _ = ∫ y, cdfUpperError
        (Measure.map
          (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu)
        (Measure.map
          (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) gamma)
        (x - normalizedGaussianInnovationCoefficient N * y) ∂gamma := by
      apply integral_congr_ae
      filter_upwards [] with y
      rw [cdfUpperError_map_mul nu gamma
        (normalizedGaussianKeepCoefficient_pos N)]
    _ = ∫ w, cdfUpperError
        (Measure.map
          (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu)
        (Measure.map
          (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) gamma)
        (x - w)
        ∂(Measure.map
          (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma) := by
      have hmeas : Measurable (fun w : ℝ => cdfUpperError
          (Measure.map
            (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu)
          (Measure.map
            (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) gamma)
          (x - w)) := by
        exact ((monotone_cdf _).measurable.comp
          (measurable_const.sub measurable_id)).sub
          ((monotone_cdf _).measurable.comp
            (measurable_const.sub measurable_id))
      rw [integral_map (by fun_prop) hmeas.aestronglyMeasurable]
    _ = cdfUpperError
        (Measure.map
            (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma ∗
          Measure.map
            (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu)
        (Measure.map
            (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma ∗
          Measure.map
            (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) gamma) x := by
      rw [cdfUpperError_conv_left_eq_integral]
    _ = cdfUpperError
        (Measure.map
            (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma ∗
          Measure.map
            (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) nu)
        gamma x := by
      have hstationary :
          Measure.map
              (fun z : ℝ => normalizedGaussianInnovationCoefficient N * z) gamma ∗
            Measure.map
              (fun z : ℝ => normalizedGaussianKeepCoefficient N * z) gamma =
            gamma := by
        simpa only [gamma, normalizedGaussianStepLaw] using
          standardNormalLaw_normalizedGaussian_step N
      rw [hstationary]

/-- Lower signed-error companion to the exact Gaussian keep/innovation
identity. -/
theorem integral_cdfLowerError_normalizedGaussian_affine_eq
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (N : ℕ) (x : ℝ) :
    (∫ y, cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      ((Real.sqrt (N + 1 : ℝ))⁻¹ *
        (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y))
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
      cdfLowerError
        (normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
  simp only [cdfLowerError_eq_neg_upper, integral_neg]
  rw [integral_cdfUpperError_normalizedGaussian_affine_eq]

/-- A common convolution factor cannot increase the one-sided upper CDF
distance. -/
theorem upperCDFDistance_conv_left_le
    (mu nu kappa : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    [IsProbabilityMeasure kappa] :
    upperCDFDistance (kappa ∗ mu) (kappa ∗ nu) ≤
      upperCDFDistance mu nu := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  rw [cdfUpperError_conv_left_eq_integral]
  apply integral_cdfUpperError_comp_le
  fun_prop

/-- Lower one-sided companion to `upperCDFDistance_conv_left_le`. -/
theorem lowerCDFDistance_conv_left_le
    (mu nu kappa : Measure ℝ)
    [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    [IsProbabilityMeasure kappa] :
    lowerCDFDistance (kappa ∗ mu) (kappa ∗ nu) ≤
      lowerCDFDistance mu nu := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  rw [cdfLowerError_conv_left_eq_integral]
  apply integral_cdfLowerError_comp_le
  fun_prop

/-- The exact normalized Gaussian keep/innovation step is nonexpansive for
the upper one-sided Kolmogorov distance from its stationary law. -/
theorem upperCDFDistance_normalizedGaussianStepLaw_le
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (N : ℕ) :
    upperCDFDistance (normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      upperCDFDistance nu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  rw [← integral_cdfUpperError_normalizedGaussian_affine_eq]
  apply integral_cdfUpperError_comp_le
  fun_prop

/-- Lower one-sided companion to
`upperCDFDistance_normalizedGaussianStepLaw_le`. -/
theorem lowerCDFDistance_normalizedGaussianStepLaw_le
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (N : ℕ) :
    lowerCDFDistance (normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      lowerCDFDistance nu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  rw [← integral_cdfLowerError_normalizedGaussian_affine_eq]
  apply integral_cdfLowerError_comp_le
  fun_prop

/-- Kolmogorov nonexpansiveness of one exact normalized Gaussian
keep/innovation step. -/
theorem kolmogorovDistance_normalizedGaussianStepLaw_le
    (nu : Measure ℝ) [IsProbabilityMeasure nu] (N : ℕ) :
    kolmogorovDistance (normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      kolmogorovDistance nu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  calc
    kolmogorovDistance (normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw =
        max
          (upperCDFDistance (normalizedGaussianStepLaw N nu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          (lowerCDFDistance (normalizedGaussianStepLaw N nu)
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
      kolmogorovDistance_eq_max_signed _ _
    _ ≤ max
          (upperCDFDistance nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          (lowerCDFDistance nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
      max_le_max
        (upperCDFDistance_normalizedGaussianStepLaw_le nu N)
        (lowerCDFDistance_normalizedGaussianStepLaw_le nu N)
    _ = kolmogorovDistance nu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw :=
      (kolmogorovDistance_eq_max_signed _ _).symm

/-- Exact signed upper-error leave-one-out recurrence for a raw partial sum.
The first term replaces the newest summand by a standard Gaussian; the
integral retains the preceding signed error instead of taking its absolute
value before averaging. -/
theorem cdfUpperError_sum_range_succ_gaussian_telescope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) (x : ℝ) :
    cdfUpperError
        (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu)
        (ProbabilityTheory.gaussianReal 0 (N + 2 : ℝ≥0)) x =
      cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            Measure.map (X (N + 1)) mu)
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x +
        ∫ y, cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0)) (x - y)
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let Sprev : Omega → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure (Measure.map Sprev mu) :=
    Measure.isProbabilityMeasure_map hSprev
  letI : IsProbabilityMeasure (Measure.map (X (N + 1)) mu) :=
    Measure.isProbabilityMeasure_map (hX (N + 1))
  rw [map_sum_range_succ_eq_conv X hX hIndep (N + 1),
    ← gaussianReal_nat_succ_conv_standardNormalLaw N]
  calc
    cdfUpperError
        (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
          Measure.map (X (N + 1)) mu)
        (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0) ∗
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x =
      cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            Measure.map (X (N + 1)) mu)
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x +
        cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0) ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x :=
      cdfUpperError_conv_telescope _ _ _ _ x
    _ = _ := by
      congr 1
      rw [Measure.conv_comm
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
        Measure.conv_comm
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0))
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
        cdfUpperError_conv_left_eq_integral]

/-- Exact signed lower-error companion to
`cdfUpperError_sum_range_succ_gaussian_telescope`. -/
theorem cdfLowerError_sum_range_succ_gaussian_telescope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) (x : ℝ) :
    cdfLowerError
        (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu)
        (ProbabilityTheory.gaussianReal 0 (N + 2 : ℝ≥0)) x =
      cdfLowerError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            Measure.map (X (N + 1)) mu)
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x +
        ∫ y, cdfLowerError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0)) (x - y)
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let Sprev : Omega → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure (Measure.map Sprev mu) :=
    Measure.isProbabilityMeasure_map hSprev
  letI : IsProbabilityMeasure (Measure.map (X (N + 1)) mu) :=
    Measure.isProbabilityMeasure_map (hX (N + 1))
  rw [map_sum_range_succ_eq_conv X hX hIndep (N + 1),
    ← gaussianReal_nat_succ_conv_standardNormalLaw N]
  calc
    cdfLowerError
        (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
          Measure.map (X (N + 1)) mu)
        (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0) ∗
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x =
      cdfLowerError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            Measure.map (X (N + 1)) mu)
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x +
        cdfLowerError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0) ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x :=
      cdfLowerError_conv_telescope _ _ _ _ x
    _ = _ := by
      congr 1
      rw [Measure.conv_comm
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
        Measure.conv_comm
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0))
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
        cdfLowerError_conv_left_eq_integral]

/-- Normalized signed upper-error leave-one-out recurrence.  The first term
is the newest-summand replacement error after applying the current
normalization; the second term averages the preceding normalized signed error
against a standard Gaussian. -/
theorem cdfUpperError_normalizedCenteredIidSum_succ_gaussian_telescope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) (x : ℝ) :
    cdfUpperError
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
      cdfUpperError
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) x +
        ∫ y, cdfUpperError
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y))
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  have hc : 0 < (Real.sqrt (N + 2 : ℝ))⁻¹ := by positivity
  have hd : 0 < (Real.sqrt (N + 1 : ℝ))⁻¹ := by positivity
  let Sprev : Omega → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure (Measure.map Sprev mu) :=
    Measure.isProbabilityMeasure_map hSprev
  letI : IsProbabilityMeasure (Measure.map (X (N + 1)) mu) :=
    Measure.isProbabilityMeasure_map (hX (N + 1))
  have hSfull : AEMeasurable
      (∑ i ∈ Finset.range (N + 2), X i) mu := by
    fun_prop
  letI : IsProbabilityMeasure
      (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu) :=
    Measure.isProbabilityMeasure_map hSfull
  have hnorm :
      Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu =
        Measure.map (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
          (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu) := by
    rw [map_normalizedCenteredIidSum_eq_map_mul_sum_range X hX (N + 1)]
    norm_num [Nat.cast_add, add_assoc]
  have hgauss :
      Measure.map (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
          (ProbabilityTheory.gaussianReal 0 (N + 2 : ℝ≥0)) =
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    convert map_gaussianReal_nat_succ_eq_standardNormalLaw (N + 1) using 1 <;>
      norm_num [Nat.cast_add, add_assoc]
  calc
    cdfUpperError
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
      cdfUpperError
          (Measure.map (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu))
          (Measure.map (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (ProbabilityTheory.gaussianReal 0 (N + 2 : ℝ≥0))) x := by
      rw [hnorm, hgauss]
    _ = cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu)
          (ProbabilityTheory.gaussianReal 0 (N + 2 : ℝ≥0))
          (x / (Real.sqrt (N + 2 : ℝ))⁻¹) :=
      cdfUpperError_map_mul _ _ hc x
    _ = cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            Measure.map (X (N + 1)) mu)
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          (x / (Real.sqrt (N + 2 : ℝ))⁻¹) +
        ∫ y, cdfUpperError
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
          (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0))
          (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y)
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw :=
      cdfUpperError_sum_range_succ_gaussian_telescope
        X hX hIndep N (x / (Real.sqrt (N + 2 : ℝ))⁻¹)
    _ = _ := by
      congr 1
      · exact (cdfUpperError_map_mul _ _ hc x).symm
      · apply integral_congr_ae
        exact ae_of_all _ fun y => by
          calc
            cdfUpperError
                (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
                (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0))
                (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y) =
              cdfUpperError
                (Measure.map
                  (fun z : ℝ => (Real.sqrt (N + 1 : ℝ))⁻¹ * z)
                  (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu))
                (Measure.map
                  (fun z : ℝ => (Real.sqrt (N + 1 : ℝ))⁻¹ * z)
                  (ProbabilityTheory.gaussianReal 0 (N + 1 : ℝ≥0)))
                ((Real.sqrt (N + 1 : ℝ))⁻¹ *
                  (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y)) := by
              rw [cdfUpperError_map_mul _ _ hd]
              congr 1
              field_simp
            _ = _ := by
              rw [map_normalizedCenteredIidSum_eq_map_mul_sum_range X hX N,
                map_gaussianReal_nat_succ_eq_standardNormalLaw N]

/-- Normalized signed lower-error companion to
`cdfUpperError_normalizedCenteredIidSum_succ_gaussian_telescope`. -/
theorem cdfLowerError_normalizedCenteredIidSum_succ_gaussian_telescope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) (x : ℝ) :
    cdfLowerError
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
      cdfLowerError
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) x +
        ∫ y, cdfLowerError
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            (x / (Real.sqrt (N + 2 : ℝ))⁻¹ - y))
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  have h :=
    cdfUpperError_normalizedCenteredIidSum_succ_gaussian_telescope
      X hX hIndep N x
  simp only [cdfLowerError_eq_neg_upper, integral_neg]
  rw [h]
  ring

/-- Exact normalized upper-error recurrence with the inherited Gaussian
average exposed as a keep/innovation step on the preceding law. -/
theorem cdfUpperError_normalizedCenteredIidSum_succ_gaussian_step_telescope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) (x : ℝ) :
    cdfUpperError
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
      cdfUpperError
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) x +
        cdfUpperError
          (normalizedGaussianStepLaw N
            (Measure.map
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
              mu))
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
  letI : IsProbabilityMeasure
      (Measure.map
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        mu) := Measure.isProbabilityMeasure_map (by
          change AEMeasurable (fun omega =>
            (Real.sqrt (N + 1 : ℝ))⁻¹ *
              ∑ i : Fin (N + 1), X i.1 omega) mu
          fun_prop)
  rw [cdfUpperError_normalizedCenteredIidSum_succ_gaussian_telescope
    X hX hIndep N x,
    integral_cdfUpperError_normalizedGaussian_affine_eq]

/-- Exact normalized lower-error companion with the inherited term exposed
as the same Gaussian keep/innovation step. -/
theorem cdfLowerError_normalizedCenteredIidSum_succ_gaussian_step_telescope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) (x : ℝ) :
    cdfLowerError
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x =
      cdfLowerError
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) x +
        cdfLowerError
          (normalizedGaussianStepLaw N
            (Measure.map
              (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
              mu))
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x := by
  letI : IsProbabilityMeasure
      (Measure.map
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        mu) := Measure.isProbabilityMeasure_map (by
          change AEMeasurable (fun omega =>
            (Real.sqrt (N + 1 : ℝ))⁻¹ *
              ∑ i : Fin (N + 1), X i.1 omega) mu
          fun_prop)
  rw [cdfLowerError_normalizedCenteredIidSum_succ_gaussian_telescope
    X hX hIndep N x,
    integral_cdfLowerError_normalizedGaussian_affine_eq]

/-- One-sided upper-distance recurrence obtained from the exact normalized
telescope.  The inherited error appears with coefficient exactly one. -/
theorem upperCDFDistance_normalizedCenteredIidSum_succ_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) :
    upperCDFDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      upperCDFDistance
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        upperCDFDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  rw [cdfUpperError_normalizedCenteredIidSum_succ_gaussian_telescope
    X hX hIndep N x]
  apply add_le_add
  · apply cdfUpperError_le_upperCDFDistance
  · apply integral_cdfUpperError_comp_le
    fun_prop

/-- One-sided lower-distance companion to
`upperCDFDistance_normalizedCenteredIidSum_succ_le`. -/
theorem lowerCDFDistance_normalizedCenteredIidSum_succ_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) :
    lowerCDFDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      lowerCDFDistance
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        lowerCDFDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  rw [cdfLowerError_normalizedCenteredIidSum_succ_gaussian_telescope
    X hX hIndep N x]
  apply add_le_add
  · apply cdfLowerError_le_lowerCDFDistance
  · apply integral_cdfLowerError_comp_le
    fun_prop

/-- Kolmogorov recurrence through the normalized one-summand replacement.
Unlike the concentration recurrence, this bound carries the preceding
Kolmogorov error with coefficient one. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_succ_le_replacement
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) :
    kolmogorovDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      kolmogorovDistance
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  rw [kolmogorovDistance_eq_max_signed]
  apply max_le
  · exact (upperCDFDistance_normalizedCenteredIidSum_succ_le
      X hX hIndep N).trans (add_le_add
        (upperCDFDistance_le_kolmogorovDistance _ _)
        (upperCDFDistance_le_kolmogorovDistance _ _))
  · exact (lowerCDFDistance_normalizedCenteredIidSum_succ_le
      X hX hIndep N).trans (add_le_add
        (lowerCDFDistance_le_kolmogorovDistance _ _)
        (lowerCDFDistance_le_kolmogorovDistance _ _))

/-- The normalized coefficient-one recurrence with its one-summand
replacement term rewritten as an unscaled common-convolution distance. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_succ_le_unscaledReplacement
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ) :
    kolmogorovDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      kolmogorovDistance
          (Measure.map (X (N + 1)) mu ∗
            Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
          (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗
            Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu) +
        kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let Sprev : Omega → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure (Measure.map Sprev mu) :=
    Measure.isProbabilityMeasure_map hSprev
  letI : IsProbabilityMeasure (Measure.map (X (N + 1)) mu) :=
    Measure.isProbabilityMeasure_map (hX (N + 1))
  have hc : 0 < (Real.sqrt (N + 2 : ℝ))⁻¹ := by positivity
  calc
    kolmogorovDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      kolmogorovDistance
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              Measure.map (X (N + 1)) mu))
          (Measure.map
            (fun y : ℝ => (Real.sqrt (N + 2 : ℝ))⁻¹ * y)
            (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu ∗
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw :=
      kolmogorovDistance_normalizedCenteredIidSum_succ_le_replacement
        X hX hIndep N
    _ = _ := by
      rw [kolmogorovDistance_map_mul_conv_eq
        (Measure.map Sprev mu) (Measure.map (X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hc]

/-- Adding an independent convolution factor cannot increase interval mass
beyond the concentration function of the right factor. -/
theorem measureReal_Ioc_conv_le_concentrationFunction_right
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {h : ℝ} (hh : 0 ≤ h) (x : ℝ) :
    (mu ∗ nu).real (Ioc x (x + h)) ≤ concentrationFunction nu h := by
  have hUpper := integrable_cdf_translate nu mu (x + h)
  have hLower := integrable_cdf_translate nu mu x
  rw [measureReal_Ioc_eq_cdf_sub (mu ∗ nu) (by linarith)]
  rw [← smoothedCDF_eq_cdf_conv nu mu (x + h),
    ← smoothedCDF_eq_cdf_conv nu mu x]
  unfold smoothedCDF
  rw [← integral_sub hUpper hLower]
  calc
    (∫ y, cdf nu (x + h - y) - cdf nu (x - y) ∂mu) ≤
        ∫ _y, concentrationFunction nu h ∂mu := by
      apply integral_mono (hUpper.sub hLower) (integrable_const _)
      intro y
      calc
        cdf nu (x + h - y) - cdf nu (x - y) =
            nu.real (Ioc (x - y) ((x - y) + h)) := by
          rw [measureReal_Ioc_eq_cdf_sub nu (by linarith)]
          congr 2
          ring
        _ ≤ concentrationFunction nu h :=
          measureReal_Ioc_le_concentrationFunction nu h (x - y)
    _ = concentrationFunction nu h := by simp

/-- Convolution cannot increase the concentration function beyond that of
its right factor. -/
theorem concentrationFunction_conv_le_right
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction (mu ∗ nu) h ≤ concentrationFunction nu h := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  exact measureReal_Ioc_conv_le_concentrationFunction_right mu nu hh x

/-- Convolution cannot increase the concentration function beyond that of
its left factor. -/
theorem concentrationFunction_conv_le_left
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction (mu ∗ nu) h ≤ concentrationFunction mu h := by
  rw [Measure.conv_comm]
  exact concentrationFunction_conv_le_right nu mu hh

/-- Leave-one-out concentration recurrence for normalized independent sums.
The new summand disappears through convolution domination; the interval
length is rescaled to the preceding normalization. -/
theorem concentrationFunction_normalizedCenteredIidSum_succ_le
    {Ω : Type*} [MeasurableSpace Ω]
    {mu : Measure Ω} [IsProbabilityMeasure mu]
    (X : ℕ → Ω → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (N : ℕ) {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu)
        ((Real.sqrt (N + 1 : ℝ))⁻¹ *
          (h * Real.sqrt (N + 2 : ℝ))) := by
  let Sfull : Ω → ℝ := ∑ i ∈ Finset.range (N + 2), X i
  let Sprev : Ω → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  have hSfull : AEMeasurable Sfull mu := by
    dsimp only [Sfull]
    fun_prop
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure (Measure.map Sfull mu) :=
    Measure.isProbabilityMeasure_map hSfull
  letI : IsProbabilityMeasure (Measure.map Sprev mu) :=
    Measure.isProbabilityMeasure_map hSprev
  letI : IsProbabilityMeasure (Measure.map (X (N + 1)) mu) :=
    Measure.isProbabilityMeasure_map (hX (N + 1))
  have hcfull : 0 < (Real.sqrt (N + 2 : ℝ))⁻¹ := by positivity
  have hcprev : 0 < (Real.sqrt (N + 1 : ℝ))⁻¹ := by positivity
  rw [map_normalizedCenteredIidSum_eq_map_mul_sum_range X hX (N + 1)]
  norm_num [Nat.cast_add, Nat.add_assoc]
  rw [show (N : ℝ) + 1 + 1 = (N : ℝ) + 2 by ring]
  rw [concentrationFunction_map_mul_eq
      (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu) hcfull]
  calc
    concentrationFunction
        (Measure.map (∑ i ∈ Finset.range (N + 2), X i) mu)
        (h / (Real.sqrt (N + 2 : ℝ))⁻¹) ≤
      concentrationFunction
        (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu)
        (h / (Real.sqrt (N + 2 : ℝ))⁻¹) := by
      rw [map_sum_range_succ_eq_conv X hX hIndep (N + 1)]
      exact concentrationFunction_conv_le_left _ _
        (div_nonneg hh hcfull.le)
    _ = concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu)
        ((Real.sqrt (N + 1 : ℝ))⁻¹ *
          (h * Real.sqrt (N + 2 : ℝ))) := by
      rw [map_normalizedCenteredIidSum_eq_map_mul_sum_range X hX N,
        concentrationFunction_map_mul_eq
          (Measure.map (∑ i ∈ Finset.range (N + 1), X i) mu) hcprev]
      field_simp

/-- The leave-one-out recurrence combined with the standard-normal interval
bound for the preceding normalized sum. -/
theorem concentrationFunction_normalizedCenteredIidSum_succ_le_gaussian_previous
    {Ω : Type*} [MeasurableSpace Ω]
    {mu : Measure Ω} [IsProbabilityMeasure mu]
    (X : ℕ → Ω → ℝ) (hX : ∀ i, AEMeasurable (X i) mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (N : ℕ) {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      (Real.sqrt (2 * Real.pi))⁻¹ *
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            (h * Real.sqrt (N + 2 : ℝ))) +
        2 * kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  have hnorm : AEMeasurable
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu := by
    unfold NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
    fun_prop
  letI : IsProbabilityMeasure
      (Measure.map
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu) :=
    Measure.isProbabilityMeasure_map hnorm
  calc
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu)
        ((Real.sqrt (N + 1 : ℝ))⁻¹ *
          (h * Real.sqrt (N + 2 : ℝ))) :=
      concentrationFunction_normalizedCenteredIidSum_succ_le
        X hX hIndep N hh
    _ ≤ _ :=
      concentrationFunction_le_standardNormal_length_add_kolmogorov _
        (mul_nonneg (by positivity) (mul_nonneg hh (Real.sqrt_nonneg _)))

/-- A probability CDF is continuous at every point carrying no atom. -/
theorem continuousAt_cdf_of_measure_singleton_eq_zero
    (μ : Measure ℝ) [IsProbabilityMeasure μ] {x : ℝ}
    (hx : μ {x} = 0) : ContinuousAt (cdf μ) x := by
  rw [(ProbabilityTheory.monotone_cdf μ).continuousAt_iff_leftLim_eq_rightLim,
    (cdf μ).rightLim_eq]
  have hm : (cdf μ).measure {x} = 0 := by
    rw [ProbabilityTheory.measure_cdf μ]
    exact hx
  rw [StieltjesFunction.measure_singleton] at hm
  have hle : cdf μ x - Function.leftLim (cdf μ) x ≤ 0 :=
    ENNReal.ofReal_eq_zero.mp hm
  have hge : 0 ≤ cdf μ x - Function.leftLim (cdf μ) x :=
    sub_nonneg.mpr ((cdf μ).mono.leftLim_le le_rfl)
  linarith

/-- Absolute continuity with respect to Lebesgue measure removes every CDF
jump. -/
theorem continuous_cdf_of_absolutelyContinuous
    (μ : Measure ℝ) [IsProbabilityMeasure μ] (hμ : μ ≪ volume) :
    Continuous (cdf μ) := by
  rw [continuous_iff_continuousAt]
  intro x
  exact continuousAt_cdf_of_measure_singleton_eq_zero μ (hμ (by simp))

/-- The normalized uniform smoothing kernel has a Lebesgue density. -/
theorem uniformSmoothingKernel_absolutelyContinuous (δ : ℝ) :
    uniformSmoothingKernel δ ≪ volume := by
  unfold uniformSmoothingKernel
  exact Measure.smul_absolutelyContinuous.trans
    Measure.restrict_le_self.absolutelyContinuous

/-- Convolution with the uniform kernel has a Lebesgue density, even when
the other probability measure is singular. -/
theorem uniformSmoothingKernel_conv_absolutelyContinuous
    (μ : Measure ℝ) [IsProbabilityMeasure μ] {δ : ℝ} (hδ : 0 < δ) :
    uniformSmoothingKernel δ ∗ μ ≪ volume := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  rw [Measure.conv_comm]
  exact Measure.conv_absolutelyContinuous
    (uniformSmoothingKernel_absolutelyContinuous δ)

/-- Uniform smoothing makes every probability CDF continuous. -/
theorem continuous_cdf_uniformSmoothingKernel_conv
    (μ : Measure ℝ) [IsProbabilityMeasure μ] {δ : ℝ} (hδ : 0 < δ) :
    Continuous (cdf (uniformSmoothingKernel δ ∗ μ)) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  exact continuous_cdf_of_absolutelyContinuous _
    (uniformSmoothingKernel_conv_absolutelyContinuous μ hδ)

/-- A finite first absolute moment makes the radial real-valued tail
integrable on the positive half-line.  This is the layer-cake input for the
integrability of a difference of two CDFs. -/
theorem integrableOn_measureReal_abs_ge
    (μ : Measure ℝ) [IsProbabilityMeasure μ]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ) :
    IntegrableOn (fun t : ℝ => μ.real {x : ℝ | t ≤ |x|}) (Ioi 0) := by
  have hanti : Antitone (fun t : ℝ => μ.real {x : ℝ | t ≤ |x|}) := by
    intro s t hst
    exact measureReal_mono fun x hx => hst.trans hx
  have htail :
      ∫⁻ t in Ioi (0 : ℝ), μ {x : ℝ | t ≤ |x|} < ∞ := by
    rw [← lintegral_eq_lintegral_meas_le μ
      (f := fun x : ℝ => |x|)
      (ae_of_all μ fun x => abs_nonneg x) hμ1.aemeasurable]
    exact hμ1.lintegral_lt_top
  refine ⟨hanti.measurable.aestronglyMeasurable, ?_⟩
  rw [hasFiniteIntegral_iff_ofReal
    (ae_of_all (volume.restrict (Ioi (0 : ℝ))) fun t => measureReal_nonneg)]
  rw [show (∫⁻ t in Ioi (0 : ℝ),
      ENNReal.ofReal (μ.real {x : ℝ | t ≤ |x|})) =
      ∫⁻ t in Ioi (0 : ℝ), μ {x : ℝ | t ≤ |x|} by
    apply lintegral_congr
    intro t
    exact ofReal_measureReal]
  exact htail

/-- The difference of two probability CDFs is integrable when both measures
have a finite first absolute moment. -/
theorem integrable_cdf_sub_cdf_of_firstMoments
    (μ ν : Measure ℝ) [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ)
    (hν1 : Integrable (fun x : ℝ => |x|) ν) :
    Integrable (fun x : ℝ => cdf μ x - cdf ν x) := by
  let qμ : ℝ → ℝ := fun t => μ.real {x : ℝ | t ≤ |x|}
  let qν : ℝ → ℝ := fun t => ν.real {x : ℝ | t ≤ |x|}
  have hqμ : IntegrableOn qμ (Ioi 0) := by
    simpa only [qμ] using integrableOn_measureReal_abs_ge μ hμ1
  have hqν : IntegrableOn qν (Ioi 0) := by
    simpa only [qν] using integrableOn_measureReal_abs_ge ν hν1
  have hmeas : Measurable (fun x : ℝ => cdf μ x - cdf ν x) :=
    (ProbabilityTheory.monotone_cdf μ).measurable.sub
      (ProbabilityTheory.monotone_cdf ν).measurable
  have hpos : IntegrableOn (fun x : ℝ => cdf μ x - cdf ν x) (Ioi 0) := by
    apply (hqμ.add hqν).mono' hmeas.aestronglyMeasurable
    refine ae_restrict_of_forall_mem measurableSet_Ioi fun x hx => ?_
    rw [Real.norm_eq_abs]
    have htailμ : 1 - cdf μ x = μ.real (Ioi x) := by
      rw [cdf_eq_real, ← probReal_compl_eq_one_sub measurableSet_Iic, compl_Iic]
    have htailν : 1 - cdf ν x = ν.real (Ioi x) := by
      rw [cdf_eq_real, ← probReal_compl_eq_one_sub measurableSet_Iic, compl_Iic]
    have hμ_le : μ.real (Ioi x) ≤ qμ x := by
      exact measureReal_mono fun a ha =>
        (le_of_lt ha).trans (le_abs_self a)
    have hν_le : ν.real (Ioi x) ≤ qν x := by
      exact measureReal_mono fun a ha =>
        (le_of_lt ha).trans (le_abs_self a)
    calc
      |cdf μ x - cdf ν x| =
          |(1 - cdf ν x) - (1 - cdf μ x)| := by congr 1 <;> ring
      _ ≤ |1 - cdf ν x| + |1 - cdf μ x| := abs_sub _ _
      _ = (1 - cdf ν x) + (1 - cdf μ x) := by
        rw [abs_of_nonneg (sub_nonneg.mpr (cdf_le_one ν x)),
          abs_of_nonneg (sub_nonneg.mpr (cdf_le_one μ x))]
      _ = μ.real (Ioi x) + ν.real (Ioi x) := by rw [htailμ, htailν]; ring
      _ ≤ qμ x + qν x := add_le_add hμ_le hν_le
  have hqμ_neg : IntegrableOn (fun x : ℝ => qμ (-x)) (Iic 0) := by
    rw [← Measure.map_neg_eq_self (volume : Measure ℝ)]
    let m : MeasurableEmbedding fun x : ℝ => -x :=
      (Homeomorph.neg ℝ).measurableEmbedding
    rw [m.integrableOn_map_iff]
    simp_rw [Function.comp_def, neg_preimage, neg_Iic, neg_zero]
    apply Iff.mpr integrableOn_Ici_iff_integrableOn_Ioi
    simpa only [neg_neg] using hqμ
  have hqν_neg : IntegrableOn (fun x : ℝ => qν (-x)) (Iic 0) := by
    rw [← Measure.map_neg_eq_self (volume : Measure ℝ)]
    let m : MeasurableEmbedding fun x : ℝ => -x :=
      (Homeomorph.neg ℝ).measurableEmbedding
    rw [m.integrableOn_map_iff]
    simp_rw [Function.comp_def, neg_preimage, neg_Iic, neg_zero]
    apply Iff.mpr integrableOn_Ici_iff_integrableOn_Ioi
    simpa only [neg_neg] using hqν
  have hneg : IntegrableOn (fun x : ℝ => cdf μ x - cdf ν x) (Iic 0) := by
    apply (hqμ_neg.add hqν_neg).mono' hmeas.aestronglyMeasurable
    refine ae_restrict_of_forall_mem measurableSet_Iic fun x hx => ?_
    rw [Real.norm_eq_abs]
    have hμ_le : cdf μ x ≤ qμ (-x) := by
      rw [cdf_eq_real]
      exact measureReal_mono fun a ha => by
        have ha0 : a ≤ 0 := ha.trans hx
        change -x ≤ |a|
        rw [abs_of_nonpos ha0]
        exact neg_le_neg ha
    have hν_le : cdf ν x ≤ qν (-x) := by
      rw [cdf_eq_real]
      exact measureReal_mono fun a ha => by
        have ha0 : a ≤ 0 := ha.trans hx
        change -x ≤ |a|
        rw [abs_of_nonpos ha0]
        exact neg_le_neg ha
    calc
      |cdf μ x - cdf ν x| ≤ |cdf μ x| + |cdf ν x| := abs_sub _ _
      _ = cdf μ x + cdf ν x := by
        rw [abs_of_nonneg (cdf_nonneg μ x), abs_of_nonneg (cdf_nonneg ν x)]
      _ ≤ qμ (-x) + qν (-x) := add_le_add hμ_le hν_le
  rw [← integrableOn_univ, ← Iic_union_Ioi]
  exact hneg.union hpos

/-- Pointwise `L¹ -> L∞` control of the upper signed error after one exact
normalized Gaussian keep/innovation step.  The two displayed factors are the
exact Gaussian density peak and the positive keep-coordinate Jacobian. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfUpperError_normalizedGaussianStepLaw_le_integral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfUpperError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (Real.sqrt (2 * Real.pi *
        normalizedGaussianInnovationCoefficient N ^ 2))⁻¹ *
        (normalizedGaussianKeepCoefficient N *
          ∫ z, |cdfUpperError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z|) := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let a := normalizedGaussianKeepCoefficient N
  let b := normalizedGaussianInnovationCoefficient N
  let v : ℝ≥0 := ⟨b ^ 2, sq_nonneg b⟩
  letI : IsProbabilityMeasure nu := h.probability
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => a * z) nu) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure
      (Measure.map (fun z : ℝ => a * z) gamma) :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  have ha : 0 < a := normalizedGaussianKeepCoefficient_pos N
  have hb : 0 < b := normalizedGaussianInnovationCoefficient_pos N
  have hv : v ≠ 0 := by
    rw [Ne, ← NNReal.coe_inj]
    simp only [v, NNReal.coe_mk, NNReal.coe_zero]
    exact (sq_pos_of_pos hb).ne'
  have hkappa : Measure.map (fun z : ℝ => b * z) gamma =
      ProbabilityTheory.gaussianReal 0 v := by
    dsimp only [gamma]
    rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
      ProbabilityTheory.gaussianReal_map_const_mul]
    simp only [mul_zero, mul_one, v]
  have hnuMapId : Integrable (fun z : ℝ => z)
      (Measure.map (fun z : ℝ => a * z) nu) :=
    integrable_id_map_mul_real nu a h.integrable_id
  have hgammaMapId : Integrable (fun z : ℝ => z)
      (Measure.map (fun z : ℝ => a * z) gamma) :=
    integrable_id_map_mul_real gamma a integrable_standardNormal_id
  have hscaled : Integrable (cdfUpperError
      (Measure.map (fun z : ℝ => a * z) nu)
      (Measure.map (fun z : ℝ => a * z) gamma)) := by
    have hnuAbs : Integrable (fun z : ℝ => |z|)
        (Measure.map (fun z : ℝ => a * z) nu) := by
      simpa only [Real.norm_eq_abs] using hnuMapId.norm
    have hgammaAbs : Integrable (fun z : ℝ => |z|)
        (Measure.map (fun z : ℝ => a * z) gamma) := by
      simpa only [Real.norm_eq_abs] using hgammaMapId.norm
    simpa only [cdfUpperError] using
      integrable_cdf_sub_cdf_of_firstMoments _ _ hnuAbs hgammaAbs
  have hgauss := abs_cdfUpperError_gaussianReal_conv_le_integral
    (Measure.map (fun z : ℝ => a * z) nu)
    (Measure.map (fun z : ℝ => a * z) gamma) hv hscaled x
  nth_rewrite 1 [← standardNormalLaw_normalizedGaussian_step N]
  change |cdfUpperError
      (Measure.map (fun z : ℝ => b * z) gamma ∗
        Measure.map (fun z : ℝ => a * z) nu)
      (Measure.map (fun z : ℝ => b * z) gamma ∗
        Measure.map (fun z : ℝ => a * z) gamma) x| ≤ _
  rw [hkappa]
  calc
    |cdfUpperError
        (ProbabilityTheory.gaussianReal 0 v ∗
          Measure.map (fun z : ℝ => a * z) nu)
        (ProbabilityTheory.gaussianReal 0 v ∗
          Measure.map (fun z : ℝ => a * z) gamma) x| ≤
        (Real.sqrt (2 * Real.pi * (v : ℝ)))⁻¹ *
          ∫ z, |cdfUpperError
            (Measure.map (fun w : ℝ => a * w) nu)
            (Measure.map (fun w : ℝ => a * w) gamma) z| := hgauss
    _ = (Real.sqrt (2 * Real.pi * b ^ 2))⁻¹ *
        (a * ∫ z, |cdfUpperError nu gamma z|) := by
      rw [integral_abs_cdfUpperError_map_mul_eq nu gamma ha]
      rfl

/-- Lower signed-error companion to the normalized Gaussian `L¹ -> L∞`
estimate. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfLowerError_normalizedGaussianStepLaw_le_integral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfLowerError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (Real.sqrt (2 * Real.pi *
        normalizedGaussianInnovationCoefficient N ^ 2))⁻¹ *
        (normalizedGaussianKeepCoefficient N *
          ∫ z, |cdfLowerError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z|) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.abs_cdfUpperError_normalizedGaussianStepLaw_le_integral N x

/-- Closed-form upper pointwise smoothing estimate. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfUpperError_normalizedGaussianStepLaw_le_integral_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfUpperError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        ∫ z, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  calc
    |cdfUpperError
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
        (Real.sqrt (2 * Real.pi *
          normalizedGaussianInnovationCoefficient N ^ 2))⁻¹ *
          (normalizedGaussianKeepCoefficient N *
            ∫ z, |cdfUpperError nu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z|) :=
      h.abs_cdfUpperError_normalizedGaussianStepLaw_le_integral N x
    _ = (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        ∫ z, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
      rw [← mul_assoc, normalizedGaussianDensityPeak_mul_keep]

/-- Closed-form lower pointwise smoothing estimate. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfLowerError_normalizedGaussianStepLaw_le_integral_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfLowerError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        ∫ z, |cdfLowerError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.abs_cdfUpperError_normalizedGaussianStepLaw_le_integral_explicit N x

/-- The closed-form `L¹ -> L∞` estimate lifted to the upper one-sided CDF
distance. -/
theorem IsStandardizedThirdMomentLaw.upperCDFDistance_normalizedGaussianStepLaw_le_integral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) :
    upperCDFDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        ∫ z, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  calc
    cdfUpperError
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
        |cdfUpperError
          (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| :=
      le_abs_self _
    _ ≤ _ := h.abs_cdfUpperError_normalizedGaussianStepLaw_le_integral_explicit N x

/-- Lower one-sided distance companion to the closed-form smoothing bound. -/
theorem IsStandardizedThirdMomentLaw.lowerCDFDistance_normalizedGaussianStepLaw_le_integral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) :
    lowerCDFDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        ∫ z, |cdfLowerError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  apply csSup_le (Set.range_nonempty _)
  rintro _ ⟨x, rfl⟩
  calc
    cdfLowerError
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
        |cdfLowerError
          (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| :=
      le_abs_self _
    _ ≤ _ := h.abs_cdfLowerError_normalizedGaussianStepLaw_le_integral_explicit N x

/-- The closed-form smoothing estimate for the full Kolmogorov distance. -/
theorem IsStandardizedThirdMomentLaw.kolmogorovDistance_normalizedGaussianStepLaw_le_integral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) :
    kolmogorovDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        ∫ z, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  rw [kolmogorovDistance_eq_max_signed]
  apply max_le
  · exact h.upperCDFDistance_normalizedGaussianStepLaw_le_integral N
  · simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
      h.lowerCDFDistance_normalizedGaussianStepLaw_le_integral N

/-- The compactly supported uniform smoothing kernel has a finite first
absolute moment. -/
theorem integrable_abs_uniformSmoothingKernel {δ : ℝ} (hδ : 0 < δ) :
    Integrable (fun x : ℝ => |x|) (uniformSmoothingKernel δ) := by
  unfold uniformSmoothingKernel
  exact continuous_abs.integrableOn_Icc.integrable.smul_measure <| by
    exact ENNReal.inv_ne_top.mpr (ENNReal.ofReal_ne_zero_iff.mpr (by positivity))

/-- Convolution preserves finiteness of the first absolute moment. -/
theorem integrable_abs_conv_of_firstMoments
    (κ μ : Measure ℝ) [IsProbabilityMeasure κ] [IsProbabilityMeasure μ]
    (hκ1 : Integrable (fun x : ℝ => |x|) κ)
    (hμ1 : Integrable (fun x : ℝ => |x|) μ) :
    Integrable (fun x : ℝ => |x|) (κ ∗ μ) := by
  rw [Measure.conv]
  apply (integrable_map_measure (by fun_prop) (by fun_prop)).mpr
  change Integrable (fun p : ℝ × ℝ => |p.1 + p.2|) (κ.prod μ)
  have hmajor : Integrable
      (fun p : ℝ × ℝ => |p.1| + |p.2|) (κ.prod μ) :=
    (hκ1.comp_fst μ).add (hμ1.comp_snd κ)
  apply hmajor.mono' (by fun_prop)
  exact ae_of_all (κ.prod μ) fun p => by
    rw [Real.norm_eq_abs, abs_abs]
    exact abs_add_le p.1 p.2

/-- Both uniformly smoothed laws have finite first moments, so their CDF
difference is integrable. -/
theorem integrable_cdf_uniformSmoothingKernel_conv_sub
    (μ ν : Measure ℝ) [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ)
    (hν1 : Integrable (fun x : ℝ => |x|) ν)
    {δ : ℝ} (hδ : 0 < δ) :
    Integrable (fun x : ℝ =>
      cdf (uniformSmoothingKernel δ ∗ μ) x -
        cdf (uniformSmoothingKernel δ ∗ ν) x) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  exact integrable_cdf_sub_cdf_of_firstMoments _ _
    (integrable_abs_conv_of_firstMoments _ _
      (integrable_abs_uniformSmoothingKernel hδ) hμ1)
    (integrable_abs_conv_of_firstMoments _ _
      (integrable_abs_uniformSmoothingKernel hδ) hν1)

/-- A CDF centered against the atom at zero.  Unlike a single probability
CDF, this function decays at both ends and is integrable under a first-moment
hypothesis. -/
noncomputable def centeredCDF (μ : Measure ℝ) (x : ℝ) : ℝ :=
  cdf μ x - cdf (Measure.dirac 0) x

/-- Pointwise kernel whose expectation is `centeredCDF`. -/
def centeredCDFKernel (y x : ℝ) : ℝ :=
  (Ici y).indicator (fun _ : ℝ => (1 : ℝ)) x -
    (Ici (0 : ℝ)).indicator (fun _ : ℝ => (1 : ℝ)) x

@[simp] theorem cdf_dirac_zero (x : ℝ) :
    cdf (Measure.dirac (0 : ℝ)) x =
      (Ici (0 : ℝ)).indicator (fun _ : ℝ => (1 : ℝ)) x := by
  rw [cdf_eq_real, Measure.real_def]
  by_cases hx : 0 ≤ x <;> simp [hx]

/-- The centered CDF is the expectation of the centered half-line kernel. -/
theorem centeredCDF_eq_integral_kernel
    (μ : Measure ℝ) [IsProbabilityMeasure μ] (x : ℝ) :
    centeredCDF μ x = ∫ y, centeredCDFKernel y x ∂μ := by
  change cdf μ x - cdf (Measure.dirac 0) x =
    ∫ y, ((Ici y).indicator (fun _ : ℝ => (1 : ℝ)) x -
      (Ici (0 : ℝ)).indicator (fun _ : ℝ => (1 : ℝ)) x) ∂μ
  have hfirst :
      (fun y : ℝ => (Ici y).indicator (fun _ : ℝ => (1 : ℝ)) x) =
        (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) := by
    funext y
    simp [Set.indicator_apply]
  have hIntFirst : Integrable
      (fun y : ℝ => (Ici y).indicator (fun _ : ℝ => (1 : ℝ)) x) μ := by
    rw [hfirst]
    exact (integrable_const (1 : ℝ)).indicator measurableSet_Iic
  have hIntSecond : Integrable
      (fun _y : ℝ =>
        (Ici (0 : ℝ)).indicator (fun _ : ℝ => (1 : ℝ)) x) μ :=
    integrable_const _
  rw [integral_sub hIntFirst hIntSecond, hfirst]
  congr 1
  · rw [cdf_eq_real]
    exact (integral_indicator_one measurableSet_Iic).symm
  · rw [cdf_dirac_zero, integral_const, probReal_univ, one_smul]

/-- A centered probability CDF is integrable under a finite first absolute
moment. -/
theorem integrable_centeredCDF
    (μ : Measure ℝ) [IsProbabilityMeasure μ]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ) :
    Integrable (centeredCDF μ) := by
  have hdirac : Integrable (fun x : ℝ => |x|) (Measure.dirac 0) :=
    integrable_dirac (by simp)
  exact integrable_cdf_sub_cdf_of_firstMoments μ (Measure.dirac 0) hμ1 hdirac

theorem centeredCDFKernel_eq_indicator_of_nonpos
    {y : ℝ} (hy : y ≤ 0) :
    centeredCDFKernel y = (Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) := by
  funext x
  simp only [centeredCDFKernel, Set.indicator_apply, mem_Ici, mem_Ico]
  split_ifs <;> simp_all <;> linarith

theorem centeredCDFKernel_eq_neg_indicator_of_pos
    {y : ℝ} (hy : 0 < y) :
    centeredCDFKernel y = -(Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ)) := by
  funext x
  simp only [centeredCDFKernel, Set.indicator_apply, mem_Ici, mem_Ico,
    Pi.neg_apply]
  split_ifs <;> simp_all <;> linarith

/-- For fixed `y`, the centered half-line kernel is integrable in `x`. -/
theorem integrable_centeredCDFKernel (y : ℝ) :
    Integrable (centeredCDFKernel y) := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    exact (integrableOn_const (s := Ico y 0) (C := (1 : ℝ)) (by simp)).integrable_indicator
      measurableSet_Ico
  · rw [centeredCDFKernel_eq_neg_indicator_of_pos (lt_of_not_ge hy)]
    exact ((integrableOn_const (s := Ico 0 y) (C := (1 : ℝ)) (by simp)).integrable_indicator
      measurableSet_Ico).neg

/-- The exact `L¹` norm of the centered half-line kernel is the displacement
from its centering point. -/
theorem integral_norm_centeredCDFKernel (y : ℝ) :
    ∫ x, ‖centeredCDFKernel y x‖ = |y| := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    calc
      (∫ x : ℝ, ‖(Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x‖) =
          ∫ x : ℝ, (Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x := by
        apply integral_congr_ae
        exact ae_of_all volume fun x => by
          by_cases hx : x ∈ Ico y 0 <;> simp [Set.indicator_apply, hx]
      _ = volume.real (Ico y 0) := integral_indicator_one measurableSet_Ico
      _ = |y| := by rw [Real.volume_real_Ico_of_le hy, abs_of_nonpos hy]; ring
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    calc
      (∫ x : ℝ, ‖(-(Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ))) x‖) =
          ∫ x : ℝ, (Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ)) x := by
        apply integral_congr_ae
        exact ae_of_all volume fun x => by
          by_cases hx : x ∈ Ico 0 y <;> simp [Set.indicator_apply, hx]
      _ = volume.real (Ico 0 y) := integral_indicator_one measurableSet_Ico
      _ = |y| := by
        rw [Real.volume_real_Ico_of_le hy'.le, abs_of_pos hy']
        ring

/-- The signed spatial mass of one centered half-line kernel is its negative
displacement. -/
theorem integral_centeredCDFKernel_eq_neg (y : ℝ) :
    (∫ x, centeredCDFKernel y x) = -y := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    calc
      (∫ x : ℝ, (Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x) =
          volume.real (Ico y 0) := integral_indicator_one measurableSet_Ico
      _ = -y := by rw [Real.volume_real_Ico_of_le hy]; ring
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    change (∫ x : ℝ,
      -((Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ)) x)) = -y
    calc
      (∫ x : ℝ, -((Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ)) x)) =
          -(∫ x : ℝ, (Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ)) x) :=
        integral_neg _
      _ = -(volume.real (Ico 0 y)) := by
        exact congrArg Neg.neg (integral_indicator_one measurableSet_Ico)
      _ = -y := by rw [Real.volume_real_Ico_of_le hy'.le]; ring

/-- The first spatial moment of one centered half-line kernel is negative
one half of the squared displacement. -/
theorem integral_id_mul_centeredCDFKernel (y : ℝ) :
    (∫ x, x * centeredCDFKernel y x) = -(y ^ 2) / 2 := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    have hind :
        (fun x : ℝ => x * (Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x) =
          (Ico y 0).indicator (fun x : ℝ => x) := by
      funext x
      by_cases hx : x ∈ Ico y 0 <;> simp [hx]
    calc
      (∫ x : ℝ, x * (Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x) =
          ∫ x in Ico y 0, x := by
        rw [hind, integral_indicator measurableSet_Ico]
      _ = ∫ x in Ioc y 0, x := by rw [restrict_Ico_eq_restrict_Ioc]
      _ = ∫ x in y..0, x := (intervalIntegral.integral_of_le hy).symm
      _ = -(y ^ 2) / 2 := by rw [integral_id]; ring
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    have hind :
        (fun x : ℝ => x * (-(Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ))) x) =
          (fun x : ℝ => -((Ico 0 y).indicator (fun x : ℝ => x) x)) := by
      funext x
      by_cases hx : x ∈ Ico 0 y <;> simp [hx]
    calc
      (∫ x : ℝ, x * (-(Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ))) x) =
          -(∫ x in Ico 0 y, x) := by
        rw [hind, integral_neg, integral_indicator measurableSet_Ico]
      _ = -(∫ x in Ioc 0 y, x) := by rw [restrict_Ico_eq_restrict_Ioc]
      _ = -(∫ x in 0..y, x) := by rw [intervalIntegral.integral_of_le hy'.le]
      _ = -(y ^ 2) / 2 := by rw [integral_id]; ring

/-- The product of the spatial coordinate with a centered half-line kernel
is always nonpositive. -/
theorem id_mul_centeredCDFKernel_nonpos (y x : ℝ) :
    x * centeredCDFKernel y x ≤ 0 := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    by_cases hx : x ∈ Ico y 0
    · simp only [Set.indicator_of_mem hx, mul_one]
      exact hx.2.le
    · simp [hx]
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    by_cases hx : x ∈ Ico 0 y
    · simpa [hx] using (neg_nonpos.mpr hx.1)
    · simp [hx]

/-- Exact absolute first spatial moment of one centered half-line kernel. -/
theorem integral_norm_id_mul_centeredCDFKernel (y : ℝ) :
    (∫ x, ‖x * centeredCDFKernel y x‖) = y ^ 2 / 2 := by
  have hpoint (x : ℝ) : ‖x * centeredCDFKernel y x‖ =
      -(x * centeredCDFKernel y x) := by
    rw [Real.norm_eq_abs, abs_of_nonpos (id_mul_centeredCDFKernel_nonpos y x)]
  simp_rw [hpoint, integral_neg, integral_id_mul_centeredCDFKernel]
  ring

/-- Multiplying a centered half-line kernel by the spatial coordinate
preserves integrability. -/
theorem integrable_id_mul_centeredCDFKernel (y : ℝ) :
    Integrable (fun x => x * centeredCDFKernel y x) := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    have hIco : IntegrableOn (fun x : ℝ => x) (Ico y 0) :=
      (integrableOn_Icc_iff_integrableOn_Ico).mp continuous_id.integrableOn_Icc
    have hInd := hIco.integrable_indicator measurableSet_Ico
    convert hInd using 1
    funext x
    by_cases hx : x ∈ Ico y 0 <;> simp [Set.indicator_apply, hx]
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    have hIco : IntegrableOn (fun x : ℝ => x) (Ico 0 y) :=
      (integrableOn_Icc_iff_integrableOn_Ico).mp continuous_id.integrableOn_Icc
    have hInd := (hIco.integrable_indicator measurableSet_Ico).neg
    convert hInd using 1
    funext x
    by_cases hx : x ∈ Ico 0 y <;> simp [Set.indicator_apply, hx]

/-- The exact squared-spatial-weight mass of one centered half-line kernel
is one third of the cubed displacement. -/
theorem integral_abs_sq_mul_norm_centeredCDFKernel (y : ℝ) :
    (∫ x, |x| ^ 2 * ‖centeredCDFKernel y x‖) = |y| ^ 3 / 3 := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    have hind :
        (fun x : ℝ => |x| ^ 2 *
          ‖(Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x‖) =
          (Ico y 0).indicator (fun x : ℝ => x ^ 2) := by
      funext x
      by_cases hx : x ∈ Ico y 0 <;> simp [hx, sq_abs]
    rw [hind, integral_indicator measurableSet_Ico,
      restrict_Ico_eq_restrict_Ioc,
      ← intervalIntegral.integral_of_le hy, integral_pow]
    rw [abs_of_nonpos hy]
    ring
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    have hind :
        (fun x : ℝ => |x| ^ 2 *
          ‖(-(Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ))) x‖) =
          (Ico 0 y).indicator (fun x : ℝ => x ^ 2) := by
      funext x
      by_cases hx : x ∈ Ico 0 y <;> simp [hx, sq_abs]
    rw [hind, integral_indicator measurableSet_Ico,
      restrict_Ico_eq_restrict_Ioc,
      ← intervalIntegral.integral_of_le hy'.le, integral_pow,
      abs_of_pos hy']
    ring

/-- The squared-spatial-weight norm of a centered half-line kernel is
integrable. -/
theorem integrable_abs_sq_mul_norm_centeredCDFKernel (y : ℝ) :
    Integrable (fun x => |x| ^ 2 * ‖centeredCDFKernel y x‖) := by
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    have hIco : IntegrableOn (fun x : ℝ => x ^ 2) (Ico y 0) :=
      (integrableOn_Icc_iff_integrableOn_Ico).mp
        ((continuous_id.pow 2).integrableOn_Icc)
    have hInd := hIco.integrable_indicator measurableSet_Ico
    convert hInd using 1
    funext x
    by_cases hx : x ∈ Ico y 0 <;> simp [hx, sq_abs]
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    have hIco : IntegrableOn (fun x : ℝ => x ^ 2) (Ico 0 y) :=
      (integrableOn_Icc_iff_integrableOn_Ico).mp
        ((continuous_id.pow 2).integrableOn_Icc)
    have hInd := hIco.integrable_indicator measurableSet_Ico
    convert hInd using 1
    funext x
    by_cases hx : x ∈ Ico 0 y <;> simp [hx, sq_abs]

/-- The oscillatory factor in Mathlib's normalized real Fourier transform. -/
noncomputable def normalizedFourierPhase (w x : ℝ) : ℂ :=
  Complex.exp (((-2 * Real.pi * x * w : ℝ) : ℂ) * Complex.I)

theorem continuous_normalizedFourierPhase (w : ℝ) :
    Continuous (normalizedFourierPhase w) := by
  apply Complex.continuous_exp.comp
  change Continuous (fun x : ℝ => (((-2 * Real.pi * x * w : ℝ) : ℂ) * Complex.I))
  fun_prop

/-- The Fourier integral of one centered half-line kernel, away from frequency
zero. This is the elementary interval calculation behind the CDF/characteristic-
function transform identity. -/
theorem integral_normalizedFourierPhase_mul_centeredCDFKernel
    {w : ℝ} (hw : w ≠ 0) (y : ℝ) :
    (∫ x, normalizedFourierPhase w x * (centeredCDFKernel y x : ℂ)) =
      (normalizedFourierPhase w y - 1) /
        (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) := by
  let c : ℂ := (((-2 * Real.pi * w : ℝ) : ℂ) * Complex.I)
  have hc : c ≠ 0 := by
    apply mul_ne_zero
    · exact Complex.ofReal_ne_zero.mpr (mul_ne_zero (mul_ne_zero (by norm_num) Real.pi_ne_zero) hw)
    · exact Complex.I_ne_zero
  have hphase (x : ℝ) : normalizedFourierPhase w x = Complex.exp (c * x) := by
    simp only [normalizedFourierPhase, c]
    congr 1
    push_cast
    ring
  by_cases hy : y ≤ 0
  · rw [centeredCDFKernel_eq_indicator_of_nonpos hy]
    have hind :
        (fun x : ℝ => normalizedFourierPhase w x *
          (((Ico y 0).indicator (fun _ : ℝ => (1 : ℝ)) x : ℝ) : ℂ)) =
          (Ico y 0).indicator (fun x : ℝ => normalizedFourierPhase w x) := by
      funext x
      by_cases hx : x ∈ Ico y 0 <;> simp [hx]
    rw [hind, integral_indicator measurableSet_Ico]
    rw [restrict_Ico_eq_restrict_Ioc,
      ← intervalIntegral.integral_of_le hy]
    simp_rw [hphase]
    rw [integral_exp_mul_complex hc]
    have hd : (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) = -c := by
      dsimp [c]
      push_cast
      ring
    rw [show Complex.exp (c * (0 : ℝ)) = 1 by simp, ← hphase y, hd]
    field_simp
    ring
  · have hy' : 0 < y := lt_of_not_ge hy
    rw [centeredCDFKernel_eq_neg_indicator_of_pos hy']
    have hind :
        (fun x : ℝ => normalizedFourierPhase w x *
          (((-(Ico 0 y).indicator (fun _ : ℝ => (1 : ℝ))) x : ℝ) : ℂ)) =
          (fun x : ℝ => -((Ico 0 y).indicator
            (fun x : ℝ => normalizedFourierPhase w x) x)) := by
      funext x
      by_cases hx : x ∈ Ico 0 y <;> simp [hx]
    rw [hind, integral_neg, integral_indicator measurableSet_Ico]
    rw [restrict_Ico_eq_restrict_Ioc,
      ← intervalIntegral.integral_of_le hy'.le]
    simp_rw [hphase]
    rw [integral_exp_mul_complex hc]
    have hd : (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) = -c := by
      dsimp [c]
      push_cast
      ring
    rw [show Complex.exp (c * (0 : ℝ)) = 1 by simp, ← hphase y, hd]
    field_simp

/-- Joint measurability of the centered half-line kernel in displacement and
evaluation point. -/
theorem measurable_centeredCDFKernel_uncurry :
    Measurable (fun p : ℝ × ℝ => centeredCDFKernel p.1 p.2) := by
  simp only [centeredCDFKernel, Set.indicator_apply, mem_Ici]
  apply Measurable.sub
  · exact Measurable.ite (measurableSet_le measurable_fst measurable_snd)
      measurable_const measurable_const
  · exact Measurable.ite (measurableSet_le measurable_const measurable_snd)
      measurable_const measurable_const

/-- Multiplication by the normalized Fourier phase preserves integrability of
each centered half-line kernel section. -/
theorem integrable_normalizedFourierPhase_mul_centeredCDFKernel
    (w y : ℝ) :
    Integrable (fun x => normalizedFourierPhase w x *
      (centeredCDFKernel y x : ℂ)) := by
  apply (integrable_centeredCDFKernel y).ofReal.bdd_mul (c := 1)
  · exact (continuous_normalizedFourierPhase w).aestronglyMeasurable
  · exact ae_of_all volume fun x => by
      change ‖Complex.exp (((-2 * Real.pi * x * w : ℝ) : ℂ) * Complex.I)‖ ≤ 1
      rw [Complex.norm_exp_ofReal_mul_I]

/-- The normalized Fourier phase has unit norm, so the oscillatory kernel has
the same exact `L¹` norm as the underlying centered half-line kernel. -/
theorem integral_norm_normalizedFourierPhase_mul_centeredCDFKernel
    (w y : ℝ) :
    (∫ x, ‖normalizedFourierPhase w x * (centeredCDFKernel y x : ℂ)‖) = |y| := by
  simp_rw [norm_mul, normalizedFourierPhase,
    Complex.norm_exp_ofReal_mul_I, one_mul, Complex.norm_real]
  exact integral_norm_centeredCDFKernel y

/-- Exact first-moment domination makes the centered-CDF Fourier kernel
integrable on the product space, which licenses Fubini. -/
theorem integrable_prod_normalizedFourierPhase_mul_centeredCDFKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu) (w : ℝ) :
    Integrable (fun p : ℝ × ℝ => normalizedFourierPhase w p.2 *
      (centeredCDFKernel p.1 p.2 : ℂ)) (mu.prod volume) := by
  have hmeas : Measurable (fun p : ℝ × ℝ => normalizedFourierPhase w p.2 *
      (centeredCDFKernel p.1 p.2 : ℂ)) := by
    exact ((continuous_normalizedFourierPhase w).measurable.comp measurable_snd).mul
      (Complex.continuous_ofReal.measurable.comp measurable_centeredCDFKernel_uncurry)
  rw [integrable_prod_iff hmeas.aestronglyMeasurable]
  constructor
  · exact ae_of_all mu fun y =>
      integrable_normalizedFourierPhase_mul_centeredCDFKernel w y
  · convert hmu1 using 1
    funext y
    exact integral_norm_normalizedFourierPhase_mul_centeredCDFKernel w y

/-- Exact first-moment domination makes the real centered-CDF kernel
integrable on the product space. -/
theorem integrable_prod_centeredCDFKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu) :
    Integrable (fun p : ℝ × ℝ => centeredCDFKernel p.1 p.2)
      (mu.prod volume) := by
  have h :=
    (integrable_prod_normalizedFourierPhase_mul_centeredCDFKernel mu hmu1 0).re
  simpa [normalizedFourierPhase] using h

/-- A finite second moment makes the coordinate-weighted centered-CDF
kernel integrable on the product space. -/
theorem integrable_prod_id_mul_centeredCDFKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu) :
    Integrable (fun p : ℝ × ℝ =>
      p.2 * centeredCDFKernel p.1 p.2) (mu.prod volume) := by
  have hmeas : Measurable (fun p : ℝ × ℝ =>
      p.2 * centeredCDFKernel p.1 p.2) :=
    measurable_snd.mul measurable_centeredCDFKernel_uncurry
  rw [integrable_prod_iff hmeas.aestronglyMeasurable]
  constructor
  · exact ae_of_all mu fun y => integrable_id_mul_centeredCDFKernel y
  · have hhalf : Integrable (fun y : ℝ => y ^ 2 / 2) mu := by
      convert hmu2.const_mul (1 / 2 : ℝ) using 1
      funext y
      ring
    convert hhalf using 1
    funext y
    exact integral_norm_id_mul_centeredCDFKernel y

/-- A finite third absolute moment makes the squared-spatial-weight kernel
norm integrable on the product space. -/
theorem integrable_prod_abs_sq_mul_norm_centeredCDFKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    Integrable (fun p : ℝ × ℝ =>
      |p.2| ^ 2 * ‖centeredCDFKernel p.1 p.2‖) (mu.prod volume) := by
  have hmeas : Measurable (fun p : ℝ × ℝ =>
      |p.2| ^ 2 * ‖centeredCDFKernel p.1 p.2‖) :=
    ((continuous_abs.measurable.comp measurable_snd).pow_const 2).mul
      measurable_centeredCDFKernel_uncurry.norm
  rw [integrable_prod_iff hmeas.aestronglyMeasurable]
  constructor
  · exact ae_of_all mu fun y =>
      integrable_abs_sq_mul_norm_centeredCDFKernel y
  · have hthird : Integrable (fun y : ℝ => |y| ^ 3 / 3) mu := by
      convert hmu3.const_mul (1 / 3 : ℝ) using 1
      funext y
      ring
    convert hthird using 1
    funext y
    simpa only [Prod.snd, Prod.fst, Real.norm_eq_abs, abs_mul, abs_pow,
      abs_abs] using integral_abs_sq_mul_norm_centeredCDFKernel y

/-- Signed squared-spatial-weight kernel integrability, obtained from its
exact norm majorant. -/
theorem integrable_prod_sq_mul_centeredCDFKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    Integrable (fun p : ℝ × ℝ =>
      p.2 ^ 2 * centeredCDFKernel p.1 p.2) (mu.prod volume) := by
  apply (integrable_prod_abs_sq_mul_norm_centeredCDFKernel mu hmu3).mono'
  · exact ((measurable_snd.pow_const 2).mul
      measurable_centeredCDFKernel_uncurry).aestronglyMeasurable
  · exact ae_of_all _ fun p => by
      rw [Real.norm_eq_abs, abs_mul, abs_pow, Real.norm_eq_abs]

/-- The spatial integral of a centered probability CDF is the negative
first moment of the law. -/
theorem integral_centeredCDF_eq_neg_integral_id
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu) :
    (∫ x, centeredCDF mu x) = -(∫ y, y ∂mu) := by
  simp_rw [centeredCDF_eq_integral_kernel]
  have hprod := integrable_prod_centeredCDFKernel mu hmu1
  have hswap : Integrable
      (Function.uncurry (fun x y : ℝ => centeredCDFKernel y x))
        (volume.prod mu) := by
    simpa only [Function.uncurry_apply_pair, Function.comp_apply] using hprod.swap
  rw [integral_integral_swap hswap]
  simpa only [integral_centeredCDFKernel_eq_neg] using
    (integral_neg (μ := mu) (f := fun y : ℝ => y))

/-- A centered probability CDF has an integrable coordinate-weighted form
under a finite second moment. -/
theorem integrable_id_mul_centeredCDF
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu) :
    Integrable (fun x => x * centeredCDF mu x) := by
  have hprod := integrable_prod_id_mul_centeredCDFKernel mu hmu2
  have hout := hprod.swap.integral_prod_left
  convert hout using 1
  funext x
  change x * centeredCDF mu x =
    ∫ y, x * centeredCDFKernel y x ∂mu
  rw [centeredCDF_eq_integral_kernel, integral_const_mul]

/-- The first spatial moment of a centered probability CDF is negative one
half of the law's second moment. -/
theorem integral_id_mul_centeredCDF_eq_neg_half_secondMoment
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu) :
    (∫ x, x * centeredCDF mu x) =
      -(∫ y, y ^ 2 ∂mu) / 2 := by
  rw [show (fun x => x * centeredCDF mu x) =
      (fun x => ∫ y, x * centeredCDFKernel y x ∂mu) by
    funext x
    rw [centeredCDF_eq_integral_kernel, integral_const_mul]]
  have hprod := integrable_prod_id_mul_centeredCDFKernel mu hmu2
  have hswap : Integrable
      (Function.uncurry (fun x y : ℝ =>
        x * centeredCDFKernel y x)) (volume.prod mu) := by
    simpa only [Function.uncurry_apply_pair, Function.comp_apply] using hprod.swap
  rw [integral_integral_swap hswap]
  simp_rw [integral_id_mul_centeredCDFKernel, integral_div, integral_neg]

/-- A centered probability CDF becomes integrable after multiplication by
the squared spatial coordinate under a finite third absolute moment. -/
theorem integrable_sq_mul_centeredCDF
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    Integrable (fun x => x ^ 2 * centeredCDF mu x) := by
  have hprod := integrable_prod_sq_mul_centeredCDFKernel mu hmu3
  have hout := hprod.swap.integral_prod_left
  convert hout using 1
  funext x
  change x ^ 2 * centeredCDF mu x =
    ∫ y, x ^ 2 * centeredCDFKernel y x ∂mu
  rw [centeredCDF_eq_integral_kernel, integral_const_mul]

/-- The squared-spatial-weight `L¹` norm of a centered CDF is bounded by one
third of the law's third absolute moment. -/
theorem integral_abs_sq_mul_abs_centeredCDF_le
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    (∫ x, |x| ^ 2 * |centeredCDF mu x|) ≤
      ∫ y, |y| ^ 3 / 3 ∂mu := by
  have hsigned := integrable_sq_mul_centeredCDF mu hmu3
  have hleft : Integrable (fun x => |x| ^ 2 * |centeredCDF mu x|) := by
    simpa only [Real.norm_eq_abs, abs_mul, abs_pow] using hsigned.norm
  have habsprod :=
    integrable_prod_abs_sq_mul_norm_centeredCDFKernel mu hmu3
  have hswap : Integrable
      (Function.uncurry (fun x y : ℝ =>
        |x| ^ 2 * ‖centeredCDFKernel y x‖)) (volume.prod mu) := by
    simpa only [Function.uncurry_apply_pair, Function.comp_apply] using
      habsprod.swap
  have hright : Integrable (fun x =>
      ∫ y, |x| ^ 2 * ‖centeredCDFKernel y x‖ ∂mu) :=
    hswap.integral_prod_left
  calc
    (∫ x, |x| ^ 2 * |centeredCDF mu x|) ≤
        ∫ x, (∫ y, |x| ^ 2 * ‖centeredCDFKernel y x‖ ∂mu) := by
      apply integral_mono hleft hright
      intro x
      change |x| ^ 2 * |centeredCDF mu x| ≤
        ∫ y, |x| ^ 2 * ‖centeredCDFKernel y x‖ ∂mu
      rw [centeredCDF_eq_integral_kernel]
      have hnorm : |∫ y, centeredCDFKernel y x ∂mu| ≤
          ∫ y, ‖centeredCDFKernel y x‖ ∂mu := by
        simpa only [Real.norm_eq_abs] using
          (norm_integral_le_integral_norm
            (fun y => centeredCDFKernel y x) (μ := mu))
      calc
        |x| ^ 2 * |∫ y, centeredCDFKernel y x ∂mu| ≤
            |x| ^ 2 * ∫ y, ‖centeredCDFKernel y x‖ ∂mu :=
          mul_le_mul_of_nonneg_left hnorm (sq_nonneg |x|)
        _ = ∫ y, |x| ^ 2 * ‖centeredCDFKernel y x‖ ∂mu := by
          rw [integral_const_mul]
    _ = ∫ y, (∫ x, |x| ^ 2 * ‖centeredCDFKernel y x‖) ∂mu :=
      integral_integral_swap hswap
    _ = ∫ y, |y| ^ 3 / 3 ∂mu := by
      apply integral_congr_ae
      exact ae_of_all _ fun y =>
        integral_abs_sq_mul_norm_centeredCDFKernel y

/-- The spatial integral of a difference of probability CDFs is the reverse
difference of their first moments. -/
theorem integral_cdf_sub_cdf_eq_integral_id_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu) :
    (∫ x, cdf mu x - cdf nu x) =
      (∫ y, y ∂nu) - ∫ y, y ∂mu := by
  have hmuC := integrable_centeredCDF mu hmu1
  have hnuC := integrable_centeredCDF nu hnu1
  calc
    (∫ x, cdf mu x - cdf nu x) =
        ∫ x, centeredCDF mu x - centeredCDF nu x := by
      apply integral_congr_ae
      exact ae_of_all _ fun x => by unfold centeredCDF; ring
    _ = (∫ x, centeredCDF mu x) - ∫ x, centeredCDF nu x :=
      integral_sub hmuC hnuC
    _ = (∫ y, y ∂nu) - ∫ y, y ∂mu := by
      rw [integral_centeredCDF_eq_neg_integral_id mu hmu1,
        integral_centeredCDF_eq_neg_integral_id nu hnu1]
      ring

/-- The first spatial moment of a probability-CDF difference is one half
of the reverse difference of the laws' second moments. -/
theorem integral_id_mul_cdf_sub_cdf_eq_secondMoment_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hnu2 : Integrable (fun y : ℝ => y ^ 2) nu) :
    (∫ x, x * (cdf mu x - cdf nu x)) =
      ((∫ y, y ^ 2 ∂nu) - ∫ y, y ^ 2 ∂mu) / 2 := by
  have hmuC := integrable_id_mul_centeredCDF mu hmu2
  have hnuC := integrable_id_mul_centeredCDF nu hnu2
  calc
    (∫ x, x * (cdf mu x - cdf nu x)) =
        ∫ x, x * centeredCDF mu x - x * centeredCDF nu x := by
      apply integral_congr_ae
      exact ae_of_all _ fun x => by unfold centeredCDF; ring
    _ = (∫ x, x * centeredCDF mu x) -
        ∫ x, x * centeredCDF nu x := integral_sub hmuC hnuC
    _ = ((∫ y, y ^ 2 ∂nu) - ∫ y, y ^ 2 ∂mu) / 2 := by
      rw [integral_id_mul_centeredCDF_eq_neg_half_secondMoment mu hmu2,
        integral_id_mul_centeredCDF_eq_neg_half_secondMoment nu hnu2]
      ring

/-- A third absolute moment controls the squared-spatial-weight `L¹` norm
of the difference of two probability CDFs. -/
theorem integral_abs_sq_mul_abs_cdf_sub_cdf_le
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu) :
    (∫ x, |x| ^ 2 * |cdf mu x - cdf nu x|) ≤
      (∫ y, |y| ^ 3 / 3 ∂mu) + ∫ y, |y| ^ 3 / 3 ∂nu := by
  have hmuSigned := integrable_sq_mul_centeredCDF mu hmu3
  have hnuSigned := integrable_sq_mul_centeredCDF nu hnu3
  have hmuW : Integrable (fun x => |x| ^ 2 * |centeredCDF mu x|) := by
    simpa only [Real.norm_eq_abs, abs_mul, abs_pow] using hmuSigned.norm
  have hnuW : Integrable (fun x => |x| ^ 2 * |centeredCDF nu x|) := by
    simpa only [Real.norm_eq_abs, abs_mul, abs_pow] using hnuSigned.norm
  have hpoint (x : ℝ) :
      |x| ^ 2 * |cdf mu x - cdf nu x| ≤
        |x| ^ 2 * |centeredCDF mu x| +
          |x| ^ 2 * |centeredCDF nu x| := by
    have heq : cdf mu x - cdf nu x =
        centeredCDF mu x - centeredCDF nu x := by
      unfold centeredCDF
      ring
    rw [heq, ← mul_add]
    exact mul_le_mul_of_nonneg_left (abs_sub _ _)
      (sq_nonneg |x|)
  have hgapMeas : AEStronglyMeasurable
      (fun x => |x| ^ 2 * |cdf mu x - cdf nu x|) :=
    (((measurable_id.abs.pow_const 2).mul
      (((monotone_cdf mu).measurable.sub
        (monotone_cdf nu).measurable).abs))).aestronglyMeasurable
  have hgap : Integrable
      (fun x => |x| ^ 2 * |cdf mu x - cdf nu x|) := by
    apply (hmuW.add hnuW).mono' hgapMeas
    exact ae_of_all _ fun x => by
      rw [Real.norm_eq_abs, abs_of_nonneg
        (mul_nonneg (sq_nonneg |x|) (abs_nonneg _))]
      exact hpoint x
  calc
    (∫ x, |x| ^ 2 * |cdf mu x - cdf nu x|) ≤
        ∫ x, (|x| ^ 2 * |centeredCDF mu x| +
          |x| ^ 2 * |centeredCDF nu x|) := by
      apply integral_mono hgap (hmuW.add hnuW)
      exact hpoint
    _ = (∫ x, |x| ^ 2 * |centeredCDF mu x|) +
        ∫ x, |x| ^ 2 * |centeredCDF nu x| :=
      integral_add hmuW hnuW
    _ ≤ (∫ y, |y| ^ 3 / 3 ∂mu) + ∫ y, |y| ^ 3 / 3 ∂nu :=
      add_le_add
        (integral_abs_sq_mul_abs_centeredCDF_le mu hmu3)
        (integral_abs_sq_mul_abs_centeredCDF_le nu hnu3)

/-- The weighted CDF difference appearing in the third-moment estimate is
itself integrable. -/
theorem integrable_abs_sq_mul_abs_cdf_sub_cdf
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu) :
    Integrable (fun x => |x| ^ 2 * |cdf mu x - cdf nu x|) := by
  have hmuSigned := integrable_sq_mul_centeredCDF mu hmu3
  have hnuSigned := integrable_sq_mul_centeredCDF nu hnu3
  have hmuW : Integrable (fun x => |x| ^ 2 * |centeredCDF mu x|) := by
    simpa only [Real.norm_eq_abs, abs_mul, abs_pow] using hmuSigned.norm
  have hnuW : Integrable (fun x => |x| ^ 2 * |centeredCDF nu x|) := by
    simpa only [Real.norm_eq_abs, abs_mul, abs_pow] using hnuSigned.norm
  apply (hmuW.add hnuW).mono'
  · exact (((measurable_id.abs.pow_const 2).mul
      (((monotone_cdf mu).measurable.sub
        (monotone_cdf nu).measurable).abs))).aestronglyMeasurable
  · exact ae_of_all _ fun x => by
      rw [Real.norm_eq_abs, abs_of_nonneg
        (mul_nonneg (sq_nonneg |x|) (abs_nonneg _))]
      have heq : cdf mu x - cdf nu x =
          centeredCDF mu x - centeredCDF nu x := by
        unfold centeredCDF
        ring
      change |x| ^ 2 * |cdf mu x - cdf nu x| ≤
        |x| ^ 2 * |centeredCDF mu x| +
          |x| ^ 2 * |centeredCDF nu x|
      rw [heq, ← mul_add]
      exact mul_le_mul_of_nonneg_left (abs_sub _ _)
        (sq_nonneg |x|)

/-- Weighted Markov estimate for the spatial tail of an integrable real
function. -/
theorem integral_abs_restrict_abs_ge_le_inv_sq_mul
    (f : ℝ → ℝ) (hf : Measurable f)
    (hweighted : Integrable (fun x => |x| ^ 2 * |f x|))
    {R : ℝ} (hR : 0 < R) :
    (∫ x in {x : ℝ | R ≤ |x|}, |f x|) ≤
      (R ^ 2)⁻¹ * ∫ x, |x| ^ 2 * |f x| := by
  let s : Set ℝ := {x : ℝ | R ≤ |x|}
  have hs : MeasurableSet s :=
    measurableSet_le measurable_const continuous_abs.measurable
  have hInv : 0 ≤ (R ^ 2)⁻¹ := inv_nonneg.mpr (sq_nonneg R)
  have hpoint {x : ℝ} (hx : x ∈ s) :
      |f x| ≤ (R ^ 2)⁻¹ * (|x| ^ 2 * |f x|) := by
    have hxR : R ≤ |x| := hx
    have hsq : R ^ 2 ≤ |x| ^ 2 := by nlinarith [abs_nonneg x]
    calc
      |f x| = (R ^ 2)⁻¹ * (R ^ 2 * |f x|) := by
        field_simp
      _ ≤ (R ^ 2)⁻¹ * (|x| ^ 2 * |f x|) := by
        exact mul_le_mul_of_nonneg_left
          (mul_le_mul_of_nonneg_right hsq (abs_nonneg (f x))) hInv
  have hmajor : Integrable
      (fun x => (R ^ 2)⁻¹ * (|x| ^ 2 * |f x|)) :=
    hweighted.const_mul _
  have hleft : IntegrableOn (fun x => |f x|) s := by
    apply hmajor.integrableOn.mono' hf.abs.aestronglyMeasurable
    refine ae_restrict_of_forall_mem hs fun x hx => ?_
    simpa only [Real.norm_eq_abs, abs_abs] using hpoint hx
  calc
    (∫ x in {x : ℝ | R ≤ |x|}, |f x|) = ∫ x in s, |f x| := rfl
    _ ≤ ∫ x in s, (R ^ 2)⁻¹ * (|x| ^ 2 * |f x|) := by
      apply integral_mono_ae hleft hmajor.integrableOn
      exact ae_restrict_of_forall_mem hs fun x hx => hpoint hx
    _ = (R ^ 2)⁻¹ * ∫ x in s, |x| ^ 2 * |f x| := by
      rw [integral_const_mul]
    _ ≤ (R ^ 2)⁻¹ * ∫ x, |x| ^ 2 * |f x| := by
      apply mul_le_mul_of_nonneg_left _ hInv
      exact setIntegral_le_integral hweighted
        (ae_of_all _ fun x =>
          mul_nonneg (sq_nonneg |x|) (abs_nonneg (f x)))

/-- On a central interval, the absolute upper signed CDF error integrates to
at most the interval length times Kolmogorov distance. -/
theorem integral_abs_cdfUpperError_restrict_abs_lt_le
    (mu nu : Measure ℝ) {R : ℝ} (hR : 0 ≤ R) :
    (∫ x in {x : ℝ | |x| < R}, |cdfUpperError mu nu x|) ≤
      2 * R * kolmogorovDistance mu nu := by
  have hset : {x : ℝ | |x| < R} = Ioo (-R) R := by
    ext x
    simp only [mem_setOf_eq, mem_Ioo]
    exact abs_lt
  have hleft : IntegrableOn (fun x => |cdfUpperError mu nu x|)
      (Ioo (-R) R) := by
    have hmu : Integrable (fun x => cdf mu x)
        (volume.restrict (Ioo (-R) R)) :=
      integrable_cdf_comp mu _ measurable_id
    have hnu : Integrable (fun x => cdf nu x)
        (volume.restrict (Ioo (-R) R)) :=
      integrable_cdf_comp nu _ measurable_id
    simpa only [cdfUpperError, Real.norm_eq_abs] using (hmu.sub hnu).norm
  have hright : IntegrableOn
      (fun _x : ℝ => kolmogorovDistance mu nu) (Ioo (-R) R) :=
    integrableOn_const (by simp)
  rw [hset]
  calc
    (∫ x in Ioo (-R) R, |cdfUpperError mu nu x|) ≤
        ∫ _x in Ioo (-R) R, kolmogorovDistance mu nu := by
      apply integral_mono hleft hright
      intro x
      simpa only [cdfGap, cdfUpperError] using
        cdfGap_le_kolmogorovDistance mu nu x
    _ = 2 * R * kolmogorovDistance mu nu := by
      rw [setIntegral_const,
        Real.volume_real_Ioo_of_le (by linarith)]
      simp only [smul_eq_mul]
      ring

/-- Central-interval companion for the lower signed CDF error. -/
theorem integral_abs_cdfLowerError_restrict_abs_lt_le
    (mu nu : Measure ℝ) {R : ℝ} (hR : 0 ≤ R) :
    (∫ x in {x : ℝ | |x| < R}, |cdfLowerError mu nu x|) ≤
      2 * R * kolmogorovDistance mu nu := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    integral_abs_cdfUpperError_restrict_abs_lt_le mu nu hR

/-- The signed upper CDF error of every standardized law has zero spatial
mass against the standard normal. -/
theorem IsStandardizedThirdMomentLaw.integral_cdfUpperError_standardNormal
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) = 0 := by
  letI : IsProbabilityMeasure nu := h.probability
  have hnuAbs : Integrable (fun y : ℝ => |y|) nu := by
    simpa only [Real.norm_eq_abs] using h.integrable_id.norm
  have hgaussAbs : Integrable (fun y : ℝ => |y|)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    simpa only [Real.norm_eq_abs] using integrable_standardNormal_id.norm
  unfold cdfUpperError
  rw [integral_cdf_sub_cdf_eq_integral_id_sub _ _ hnuAbs hgaussAbs,
    h.centered, integral_standardNormal_id]
  ring

/-- The signed upper CDF error of every standardized law also has zero first
spatial moment against the standard normal. -/
theorem IsStandardizedThirdMomentLaw.integral_id_mul_cdfUpperError_standardNormal
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, x * cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) = 0 := by
  letI : IsProbabilityMeasure nu := h.probability
  unfold cdfUpperError
  rw [integral_id_mul_cdf_sub_cdf_eq_secondMoment_sub _ _
      h.integrable_sq integrable_standardNormal_sq,
    h.unit_second_moment, integral_standardNormal_sq]
  ring

/-- Lower signed-error companion to
`IsStandardizedThirdMomentLaw.integral_cdfUpperError_standardNormal`. -/
theorem IsStandardizedThirdMomentLaw.integral_cdfLowerError_standardNormal
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) = 0 := by
  simp only [cdfLowerError_eq_neg_upper, integral_neg,
    h.integral_cdfUpperError_standardNormal, neg_zero]

/-- First-spatial-moment companion for the lower signed CDF error. -/
theorem IsStandardizedThirdMomentLaw.integral_id_mul_cdfLowerError_standardNormal
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, x * cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) = 0 := by
  simp only [cdfLowerError_eq_neg_upper, mul_neg, integral_neg,
    h.integral_id_mul_cdfUpperError_standardNormal, neg_zero]

/-- Third-moment weighted `L¹` control of the standardized upper signed CDF
error against the standard normal. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_sq_mul_abs_cdfUpperError_le
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, |x| ^ 2 * |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      ((∫ y, |y| ^ 3 ∂nu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3 := by
  letI : IsProbabilityMeasure nu := h.probability
  unfold cdfUpperError
  calc
    (∫ x, |x| ^ 2 * |cdf nu x - cdf
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
        (∫ y, |y| ^ 3 / 3 ∂nu) +
          ∫ y, |y| ^ 3 / 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw :=
      integral_abs_sq_mul_abs_cdf_sub_cdf_le _ _
        h.integrable_abs_cube integrable_standardNormal_abs_cube
    _ = ((∫ y, |y| ^ 3 ∂nu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3 := by
      rw [integral_div, integral_div]
      ring

/-- The same weighted control for the lower signed CDF error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_sq_mul_abs_cdfLowerError_le
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, |x| ^ 2 * |cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      ((∫ y, |y| ^ 3 ∂nu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3 := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.integral_abs_sq_mul_abs_cdfUpperError_le

/-- Outside-radius `L¹` tail bound for the upper standardized signed CDF
error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfUpperError_restrict_abs_ge_le
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x in {x : ℝ | R ≤ |x|}, |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3) := by
  letI : IsProbabilityMeasure nu := h.probability
  have hmeas : Measurable (cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    (monotone_cdf nu).measurable.sub
      (monotone_cdf
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).measurable
  have hweighted : Integrable (fun x => |x| ^ 2 * |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) := by
    unfold cdfUpperError
    exact integrable_abs_sq_mul_abs_cdf_sub_cdf _ _
      h.integrable_abs_cube integrable_standardNormal_abs_cube
  calc
    (∫ x in {x : ℝ | R ≤ |x|}, |cdfUpperError nu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
        (R ^ 2)⁻¹ * ∫ x, |x| ^ 2 * |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| :=
      integral_abs_restrict_abs_ge_le_inv_sq_mul _ hmeas hweighted hR
    _ ≤ (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3) :=
      mul_le_mul_of_nonneg_left h.integral_abs_sq_mul_abs_cdfUpperError_le
        (inv_nonneg.mpr (sq_nonneg R))

/-- Outside-radius companion for the lower standardized signed CDF error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfLowerError_restrict_abs_ge_le
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x in {x : ℝ | R ≤ |x|}, |cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
        ∫ y, |y| ^ 3
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.integral_abs_cdfUpperError_restrict_abs_ge_le hR

/-- Combined central/tail `L¹` estimate for the upper standardized signed
CDF error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfUpperError_le_central_tail
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x, |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      2 * R * kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
          ∫ y, |y| ^ 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3) := by
  letI : IsProbabilityMeasure nu := h.probability
  let s : Set ℝ := {x : ℝ | |x| < R}
  have hs : MeasurableSet s :=
    measurableSet_lt continuous_abs.measurable measurable_const
  have hcompl : sᶜ = {x : ℝ | R ≤ |x|} := by
    ext x
    simp only [s, mem_compl_iff, mem_setOf_eq]
    exact not_lt
  have hnuAbs : Integrable (fun y : ℝ => |y|) nu := by
    simpa only [Real.norm_eq_abs] using h.integrable_id.norm
  have hgaussAbs : Integrable (fun y : ℝ => |y|)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
    simpa only [Real.norm_eq_abs] using integrable_standardNormal_id.norm
  have herr : Integrable (fun x => |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) := by
    unfold cdfUpperError
    simpa only [Real.norm_eq_abs] using
      (integrable_cdf_sub_cdf_of_firstMoments _ _ hnuAbs hgaussAbs).norm
  calc
    (∫ x, |cdfUpperError nu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) =
        (∫ x in s, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) +
        ∫ x in sᶜ, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| :=
      (integral_add_compl hs herr).symm
    _ = (∫ x in {x : ℝ | |x| < R}, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) +
        ∫ x in {x : ℝ | R ≤ |x|}, |cdfUpperError nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| := by
      rw [hcompl]
    _ ≤ 2 * R * kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
          ∫ y, |y| ^ 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3) :=
      add_le_add
        (integral_abs_cdfUpperError_restrict_abs_lt_le _ _ hR.le)
        (h.integral_abs_cdfUpperError_restrict_abs_ge_le hR)

/-- Combined central/tail companion for the lower standardized signed CDF
error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfLowerError_le_central_tail
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x, |cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      2 * R * kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
          ∫ y, |y| ^ 3
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) / 3) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.integral_abs_cdfUpperError_le_central_tail hR

/-- Away from frequency zero, the normalized Fourier transform of a centered
probability CDF is its characteristic-function increment divided by `2πwi`. -/
theorem fourier_centeredCDF
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {w : ℝ} (hw : w ≠ 0) :
    𝓕 (fun x : ℝ => (centeredCDF mu x : ℂ)) w =
      (MeasureTheory.charFun mu (-2 * Real.pi * w) - 1) /
        (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) := by
  rw [Real.fourier_real_eq_integral_exp_smul]
  change (∫ x, normalizedFourierPhase w x * (centeredCDF mu x : ℂ)) = _
  have hcenter (x : ℝ) :
      (centeredCDF mu x : ℂ) = ∫ y, (centeredCDFKernel y x : ℂ) ∂mu := by
    rw [centeredCDF_eq_integral_kernel]
    exact (integral_complex_ofReal (μ := mu)
      (f := fun y => centeredCDFKernel y x)).symm
  have hmul (x : ℝ) :
      normalizedFourierPhase w x *
          (∫ y, (centeredCDFKernel y x : ℂ) ∂mu) =
        ∫ y, normalizedFourierPhase w x * (centeredCDFKernel y x : ℂ) ∂mu := by
    exact (integral_const_mul (μ := mu) (normalizedFourierPhase w x)
      (fun y => (centeredCDFKernel y x : ℂ))).symm
  simp_rw [hcenter, hmul]
  have hprod :=
    integrable_prod_normalizedFourierPhase_mul_centeredCDFKernel mu hmu1 w
  have hswap : Integrable
      (Function.uncurry (fun x y : ℝ => normalizedFourierPhase w x *
        (centeredCDFKernel y x : ℂ))) (volume.prod mu) := by
    simpa only [Function.uncurry_apply_pair, Function.comp_apply] using hprod.swap
  rw [integral_integral_swap hswap]
  simp_rw [integral_normalizedFourierPhase_mul_centeredCDFKernel hw]
  have hphaseInt : Integrable (normalizedFourierPhase w) mu := by
    have hone : Integrable (fun _ : ℝ => (1 : ℂ)) mu := integrable_const (1 : ℂ)
    simpa only [mul_one] using hone.bdd_mul (c := 1)
      (continuous_normalizedFourierPhase w).aestronglyMeasurable
      (ae_of_all mu fun x => by
        change ‖Complex.exp (((-2 * Real.pi * x * w : ℝ) : ℂ) * Complex.I)‖ ≤ 1
        rw [Complex.norm_exp_ofReal_mul_I])
  have hchar :
      (∫ y, normalizedFourierPhase w y ∂mu) =
        MeasureTheory.charFun mu (-2 * Real.pi * w) := by
    rw [MeasureTheory.charFun_apply_real]
    apply integral_congr_ae
    exact ae_of_all mu fun y => by
      simp only [normalizedFourierPhase]
      congr 1
      push_cast
      ring
  let d : ℂ := (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I)
  calc
    (∫ y, (normalizedFourierPhase w y - 1) / d ∂mu) =
        (∫ y, normalizedFourierPhase w y - 1 ∂mu) / d :=
      integral_div d (fun y => normalizedFourierPhase w y - 1)
    _ = ((∫ y, normalizedFourierPhase w y ∂mu) -
          ∫ _y : ℝ, (1 : ℂ) ∂mu) / d := by
      rw [integral_sub hphaseInt (integrable_const (1 : ℂ))]
    _ = (MeasureTheory.charFun mu (-2 * Real.pi * w) - 1) / d := by
      rw [hchar, integral_const, probReal_univ, one_smul]

/-- The normalized Fourier phase preserves integrability of a centered CDF. -/
theorem integrable_normalizedFourierPhase_mul_centeredCDF
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu) (w : ℝ) :
    Integrable (fun x => normalizedFourierPhase w x *
      (centeredCDF mu x : ℂ)) := by
  apply (integrable_centeredCDF mu hmu1).ofReal.bdd_mul (c := 1)
  · exact (continuous_normalizedFourierPhase w).aestronglyMeasurable
  · exact ae_of_all volume fun x => by
      change ‖Complex.exp (((-2 * Real.pi * x * w : ℝ) : ℂ) * Complex.I)‖ ≤ 1
      rw [Complex.norm_exp_ofReal_mul_I]

/-- Subtracting two centered-CDF identities cancels the common Dirac term and
gives the Fourier transform of the ordinary CDF difference. -/
theorem fourier_cdf_sub_cdf
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {w : ℝ} (hw : w ≠ 0) :
    𝓕 (fun x : ℝ => ((cdf mu x - cdf nu x : ℝ) : ℂ)) w =
      (MeasureTheory.charFun mu (-2 * Real.pi * w) -
        MeasureTheory.charFun nu (-2 * Real.pi * w)) /
          (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) := by
  rw [Real.fourier_real_eq_integral_exp_smul]
  change (∫ x, normalizedFourierPhase w x *
    ((cdf mu x - cdf nu x : ℝ) : ℂ)) = _
  have hfun :
      (fun x : ℝ => normalizedFourierPhase w x *
        ((cdf mu x - cdf nu x : ℝ) : ℂ)) =
        (fun x : ℝ => normalizedFourierPhase w x *
          (centeredCDF mu x : ℂ) - normalizedFourierPhase w x *
            (centeredCDF nu x : ℂ)) := by
    funext x
    simp only [centeredCDF]
    push_cast
    ring
  rw [hfun]
  rw [integral_sub
    (integrable_normalizedFourierPhase_mul_centeredCDF mu hmu1 w)
    (integrable_normalizedFourierPhase_mul_centeredCDF nu hnu1 w)]
  have hmuFormula :
      (∫ x, normalizedFourierPhase w x * (centeredCDF mu x : ℂ)) =
        (MeasureTheory.charFun mu (-2 * Real.pi * w) - 1) /
          (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) := by
    simpa only [Real.fourier_real_eq_integral_exp_smul, smul_eq_mul,
      normalizedFourierPhase] using fourier_centeredCDF mu hmu1 hw
  have hnuFormula :
      (∫ x, normalizedFourierPhase w x * (centeredCDF nu x : ℂ)) =
        (MeasureTheory.charFun nu (-2 * Real.pi * w) - 1) /
          (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) := by
    simpa only [Real.fourier_real_eq_integral_exp_smul, smul_eq_mul,
      normalizedFourierPhase] using fourier_centeredCDF nu hnu1 hw
  rw [hmuFormula, hnuFormula]
  have hd : (((2 * Real.pi * w : ℝ) : ℂ) * Complex.I) ≠ 0 := by
    apply mul_ne_zero
    · exact Complex.ofReal_ne_zero.mpr
        (mul_ne_zero (mul_ne_zero (by norm_num) Real.pi_ne_zero) hw)
    · exact Complex.I_ne_zero
  field_simp [hd]
  ring

/-- The norm of the transformed CDF difference is exactly the characteristic-
function discrepancy quotient at the normalized frequency. -/
theorem norm_fourier_cdf_sub_cdf_eq_charFunDiscrepancyQuotient
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {w : ℝ} (hw : w ≠ 0) :
    ‖𝓕 (fun x : ℝ => ((cdf mu x - cdf nu x : ℝ) : ℂ)) w‖ =
      charFunDiscrepancyQuotient mu nu (-2 * Real.pi * w) := by
  have ht : -2 * Real.pi * w ≠ 0 :=
    mul_ne_zero (mul_ne_zero (by norm_num) Real.pi_ne_zero) hw
  rw [fourier_cdf_sub_cdf mu nu hmu1 hnu1 hw,
    charFunDiscrepancyQuotient, if_neg ht, norm_div, norm_mul,
    Complex.norm_real, Complex.norm_I, mul_one, Real.norm_eq_abs]
  congr 1
  rw [show -2 * Real.pi * w = -(2 * Real.pi * w) by ring, abs_neg]

/-- Any globally integrable characteristic-function discrepancy quotient makes
the Fourier transform of the corresponding CDF difference integrable. -/
theorem integrable_fourier_cdf_sub_cdf_of_integrable_discrepancy
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    (hquot : Integrable (charFunDiscrepancyQuotient mu nu)) :
    Integrable (𝓕 (fun x : ℝ => ((cdf mu x - cdf nu x : ℝ) : ℂ))) := by
  let f : ℝ → ℂ := fun x => ((cdf mu x - cdf nu x : ℝ) : ℂ)
  have hf : Integrable f :=
    (integrable_cdf_sub_cdf_of_firstMoments mu nu hmu1 hnu1).ofReal
  have hFourierCont : Continuous (𝓕 f) := by
    exact VectorFourier.fourierIntegral_continuous
      Real.continuous_fourierChar continuous_inner hf
  have hscale : Integrable (fun w : ℝ =>
      charFunDiscrepancyQuotient mu nu ((-2 * Real.pi) * w)) :=
    hquot.comp_mul_left' (mul_ne_zero (by norm_num) Real.pi_ne_zero)
  have hnorm : Integrable (fun w : ℝ => ‖𝓕 f w‖) := by
    apply hscale.congr
    filter_upwards [volume.ae_ne (0 : ℝ)] with w hw
    dsimp only [f]
    convert (norm_fourier_cdf_sub_cdf_eq_charFunDiscrepancyQuotient
      mu nu hmu1 hnu1 hw).symm using 1 <;> ring
  exact (integrable_norm_iff hFourierCont.aestronglyMeasurable).mp hnorm

/-- Convolution by a common probability smoothing measure cannot increase
the pointwise characteristic-function discrepancy. -/
theorem norm_charFun_conv_sub_charFun_conv_le
    (μ ν κ : Measure ℝ) [IsProbabilityMeasure μ]
    [IsProbabilityMeasure ν] [IsProbabilityMeasure κ] (t : ℝ) :
    ‖MeasureTheory.charFun (κ ∗ μ) t -
        MeasureTheory.charFun (κ ∗ ν) t‖ ≤
      ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ := by
  rw [MeasureTheory.charFun_conv, MeasureTheory.charFun_conv, ← mul_sub,
    norm_mul]
  calc
    ‖MeasureTheory.charFun κ t‖ *
        ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ ≤
        1 * ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ :=
      mul_le_mul_of_nonneg_right (MeasureTheory.norm_charFun_le_one t)
        (norm_nonneg _)
    _ = _ := one_mul _

/-- Exact characteristic-function damping supplied by the normalized uniform
kernel. -/
theorem norm_charFun_uniformSmoothingKernel_conv_sub
    (μ ν : Measure ℝ) [IsProbabilityMeasure μ]
    [IsProbabilityMeasure ν] {δ : ℝ} (hδ : 0 < δ) (t : ℝ) :
    ‖MeasureTheory.charFun (uniformSmoothingKernel δ ∗ μ) t -
        MeasureTheory.charFun (uniformSmoothingKernel δ ∗ ν) t‖ =
      |Real.sinc (t * δ)| *
        ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  rw [MeasureTheory.charFun_conv, MeasureTheory.charFun_conv,
    charFun_uniformSmoothingKernel hδ, ← mul_sub, norm_mul]
  simp only [Complex.norm_real, Real.norm_eq_abs]

/-- Away from the origin, uniform smoothing upgrades the discrepancy quotient
to inverse-square decay. -/
theorem charFunDiscrepancyQuotient_uniformSmoothingKernel_conv_le
    (μ ν : Measure ℝ) [IsProbabilityMeasure μ]
    [IsProbabilityMeasure ν] {δ : ℝ} (hδ : 0 < δ)
    {t : ℝ} (ht : t ≠ 0) :
    charFunDiscrepancyQuotient
        (uniformSmoothingKernel δ ∗ μ) (uniformSmoothingKernel δ ∗ ν) t ≤
      2 * δ⁻¹ * (|t| ^ 2)⁻¹ := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  rw [charFunDiscrepancyQuotient, if_neg ht,
    norm_charFun_uniformSmoothingKernel_conv_sub μ ν hδ]
  have htd : t * δ ≠ 0 := mul_ne_zero ht hδ.ne'
  have hsinc := abs_sinc_le_inv_abs htd
  have hdiff :
      ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ ≤ 2 := by
    calc
      ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ ≤
          ‖MeasureTheory.charFun μ t‖ + ‖MeasureTheory.charFun ν t‖ :=
        norm_sub_le _ _
      _ ≤ 1 + 1 := add_le_add
        (MeasureTheory.norm_charFun_le_one t)
        (MeasureTheory.norm_charFun_le_one t)
      _ = 2 := by norm_num
  calc
    |Real.sinc (t * δ)| *
          ‖MeasureTheory.charFun μ t - MeasureTheory.charFun ν t‖ / |t| ≤
        |t * δ|⁻¹ * 2 / |t| := by
      exact div_le_div_of_nonneg_right
        (mul_le_mul hsinc hdiff (norm_nonneg _)
          (inv_nonneg.mpr (abs_nonneg _))) (abs_nonneg t)
    _ = 2 * δ⁻¹ * (|t| ^ 2)⁻¹ := by
      rw [abs_mul, abs_of_pos hδ]
      field_simp [abs_ne_zero.mpr ht, hδ.ne']

theorem charFunDiscrepancyQuotient_conv_le
    (μ ν κ : Measure ℝ) [IsProbabilityMeasure μ]
    [IsProbabilityMeasure ν] [IsProbabilityMeasure κ] (t : ℝ) :
    charFunDiscrepancyQuotient (κ ∗ μ) (κ ∗ ν) t ≤
      charFunDiscrepancyQuotient μ ν t := by
  unfold charFunDiscrepancyQuotient
  split_ifs with ht
  · exact le_rfl
  · exact div_le_div_of_nonneg_right
      (norm_charFun_conv_sub_charFun_conv_le μ ν κ t) (abs_nonneg t)

/-- Uniform convolution makes the characteristic-function discrepancy
quotient globally integrable: first moments control the removable singularity
and the sinc multiplier supplies inverse-square decay at infinity. -/
theorem integrable_charFunDiscrepancyQuotient_uniformSmoothingKernel_conv
    {μ ν : Measure ℝ} [IsProbabilityMeasure μ] [IsProbabilityMeasure ν]
    (hμ1 : Integrable (fun x : ℝ => |x|) μ)
    (hν1 : Integrable (fun x : ℝ => |x|) ν)
    {δ : ℝ} (hδ : 0 < δ) :
    Integrable (charFunDiscrepancyQuotient
      (uniformSmoothingKernel δ ∗ μ) (uniformSmoothingKernel δ ∗ ν)) volume := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel δ) :=
    uniformSmoothingKernel_isProbabilityMeasure hδ
  let M : ℝ := (∫ x, |x| ∂μ) + ∫ x, |x| ∂ν
  let C : ℝ := 2 * M + 4 * δ⁻¹
  have hM : 0 ≤ M := by
    dsimp only [M]
    exact add_nonneg (integral_nonneg fun _ => abs_nonneg _)
      (integral_nonneg fun _ => abs_nonneg _)
  have hδinv : 0 ≤ δ⁻¹ := inv_nonneg.mpr hδ.le
  have hC : 0 ≤ C := by
    dsimp only [C]
    positivity
  have hMajorant : Integrable (fun t : ℝ => C * (1 + t ^ 2)⁻¹) volume :=
    integrable_inv_one_add_sq.const_mul C
  refine hMajorant.mono'
    (stronglyMeasurable_charFunDiscrepancyQuotient
      (uniformSmoothingKernel δ ∗ μ) (uniformSmoothingKernel δ ∗ ν)).aestronglyMeasurable
    (ae_of_all volume fun t => ?_)
  rw [Real.norm_eq_abs, abs_of_nonneg
    (charFunDiscrepancyQuotient_nonneg _ _ t)]
  have hlocal :
      charFunDiscrepancyQuotient
          (uniformSmoothingKernel δ ∗ μ) (uniformSmoothingKernel δ ∗ ν) t ≤ M :=
    (charFunDiscrepancyQuotient_conv_le μ ν (uniformSmoothingKernel δ) t).trans <| by
      simpa only [M] using charFunDiscrepancyQuotient_le_firstMoments hμ1 hν1 t
  have hden : 0 < 1 + t ^ 2 := by positivity
  by_cases htSmall : |t| ≤ 1
  · have htSq : t ^ 2 ≤ 1 := by
      have hsq : |t| ^ 2 ≤ 1 := by
        nlinarith [mul_nonneg (abs_nonneg t) (sub_nonneg.mpr htSmall)]
      simpa only [sq_abs] using hsq
    have hMmajor : M ≤ (2 * M) * (1 + t ^ 2)⁻¹ := by
      rw [← div_eq_mul_inv, le_div_iff₀ hden]
      nlinarith [mul_nonneg hM (sub_nonneg.mpr htSq)]
    calc
      charFunDiscrepancyQuotient
          (uniformSmoothingKernel δ ∗ μ) (uniformSmoothingKernel δ ∗ ν) t ≤ M := hlocal
      _ ≤ (2 * M) * (1 + t ^ 2)⁻¹ := hMmajor
      _ ≤ C * (1 + t ^ 2)⁻¹ := by
        apply mul_le_mul_of_nonneg_right _ (inv_nonneg.mpr hden.le)
        dsimp only [C]
        nlinarith
  · have htLarge : 1 < |t| := lt_of_not_ge htSmall
    have ht : t ≠ 0 := by
      intro ht0
      subst t
      norm_num at htLarge
    have habsSq : 0 < |t| ^ 2 := sq_pos_of_pos (abs_pos.mpr ht)
    have hdenle : 1 + t ^ 2 ≤ 2 * (|t| ^ 2) := by
      have hone : 1 ≤ |t| := htLarge.le
      have hsquare : 1 ≤ |t| ^ 2 := by
        nlinarith [mul_nonneg (abs_nonneg t) (sub_nonneg.mpr hone)]
      rw [sq_abs] at hsquare ⊢
      nlinarith
    have htail :=
      charFunDiscrepancyQuotient_uniformSmoothingKernel_conv_le μ ν hδ ht
    have htailMajor :
        2 * δ⁻¹ * (|t| ^ 2)⁻¹ ≤
          (4 * δ⁻¹) * (1 + t ^ 2)⁻¹ := by
      change (2 * δ⁻¹) / (|t| ^ 2) ≤
        (4 * δ⁻¹) / (1 + t ^ 2)
      rw [div_le_div_iff₀ habsSq hden]
      calc
        (2 * δ⁻¹) * (1 + t ^ 2) ≤
            (2 * δ⁻¹) * (2 * |t| ^ 2) :=
          mul_le_mul_of_nonneg_left hdenle
            (mul_nonneg (by norm_num) hδinv)
        _ = (4 * δ⁻¹) * |t| ^ 2 := by ring
    calc
      charFunDiscrepancyQuotient
          (uniformSmoothingKernel δ ∗ μ) (uniformSmoothingKernel δ ∗ ν) t ≤
          2 * δ⁻¹ * (|t| ^ 2)⁻¹ := htail
      _ ≤ (4 * δ⁻¹) * (1 + t ^ 2)⁻¹ := htailMajor
      _ ≤ C * (1 + t ^ 2)⁻¹ := by
        apply mul_le_mul_of_nonneg_right _ (inv_nonneg.mpr hden.le)
        dsimp only [C]
        nlinarith

/-- A pointwise Fourier-inversion estimate in Mathlib's normalized Fourier
convention.  It packages the analytic conclusion needed after identifying the
Fourier transform of a smoothed CDF difference. -/
theorem norm_le_integral_norm_fourier
    (f : ℝ → ℂ) (hf : Integrable f) (hFourier : Integrable (𝓕 f))
    {x : ℝ} (hcont : ContinuousAt f x) :
    ‖f x‖ ≤ ∫ t : ℝ, ‖𝓕 f t‖ := by
  rw [← hf.fourierInv_fourier_eq hFourier hcont, Real.fourierInv_eq]
  exact (norm_integral_le_integral_norm
    (μ := volume) (fun t : ℝ => 𝐞 (inner ℝ t x) • (𝓕 f) t)).trans_eq <| by
      apply integral_congr_ae
      exact ae_of_all volume fun t => Circle.norm_smul _ _

/-- Fourier inversion bounds the pointwise CDF gap between two uniformly
smoothed laws by the globally integrable, normalized characteristic-function
discrepancy quotient. -/
theorem cdfGap_uniformSmoothingKernel_conv_le_integral_discrepancy
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    {delta : ℝ} (hdelta : 0 < delta) (x : ℝ) :
    cdfGap (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) x ≤
      ∫ w : ℝ, charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) ((-2 * Real.pi) * w) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel delta) :=
    uniformSmoothingKernel_isProbabilityMeasure hdelta
  have hkappa1 := integrable_abs_uniformSmoothingKernel hdelta
  have hmuConv1 := integrable_abs_conv_of_firstMoments
    (uniformSmoothingKernel delta) mu hkappa1 hmu1
  have hnuConv1 := integrable_abs_conv_of_firstMoments
    (uniformSmoothingKernel delta) nu hkappa1 hnu1
  let f : ℝ → ℂ := fun y =>
    ((cdf (uniformSmoothingKernel delta ∗ mu) y -
      cdf (uniformSmoothingKernel delta ∗ nu) y : ℝ) : ℂ)
  have hf : Integrable f :=
    (integrable_cdf_uniformSmoothingKernel_conv_sub mu nu hmu1 hnu1 hdelta).ofReal
  have hquot :=
    integrable_charFunDiscrepancyQuotient_uniformSmoothingKernel_conv
      hmu1 hnu1 hdelta
  have hFourier : Integrable (𝓕 f) := by
    exact integrable_fourier_cdf_sub_cdf_of_integrable_discrepancy
      (uniformSmoothingKernel delta ∗ mu)
      (uniformSmoothingKernel delta ∗ nu) hmuConv1 hnuConv1 hquot
  have hcont : Continuous f := by
    apply Complex.continuous_ofReal.comp
    exact (continuous_cdf_uniformSmoothingKernel_conv mu hdelta).sub
      (continuous_cdf_uniformSmoothingKernel_conv nu hdelta)
  have hinv : ‖f x‖ ≤ ∫ w : ℝ, ‖𝓕 f w‖ :=
    norm_le_integral_norm_fourier f hf hFourier (x := x) hcont.continuousAt
  calc
    cdfGap (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) x = ‖f x‖ := by
      simp only [cdfGap, f, Complex.norm_real, Real.norm_eq_abs]
    _ ≤ ∫ w : ℝ, ‖𝓕 f w‖ := hinv
    _ = ∫ w : ℝ, charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) ((-2 * Real.pi) * w) := by
      apply integral_congr_ae
      filter_upwards [volume.ae_ne (0 : ℝ)] with w hw
      dsimp only [f]
      convert norm_fourier_cdf_sub_cdf_eq_charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) hmuConv1 hnuConv1 hw using 1 <;> ring

/-- The same inversion estimate after the exact `-2π` change of variables. -/
theorem cdfGap_uniformSmoothingKernel_conv_le_integral_discrepancy_scaled
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    {delta : ℝ} (hdelta : 0 < delta) (x : ℝ) :
    cdfGap (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) x ≤
      (2 * Real.pi)⁻¹ * ∫ t : ℝ, charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) t := by
  calc
    cdfGap (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) x ≤
      ∫ w : ℝ, charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) ((-2 * Real.pi) * w) :=
      cdfGap_uniformSmoothingKernel_conv_le_integral_discrepancy
        mu nu hmu1 hnu1 hdelta x
    _ = (2 * Real.pi)⁻¹ * ∫ t : ℝ, charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗ nu) t := by
      rw [Measure.integral_comp_mul_left]
      have hpi : 0 < 2 * Real.pi := mul_pos (by norm_num) Real.pi_pos
      rw [show |(-2 * Real.pi)⁻¹| = (2 * Real.pi)⁻¹ by
        rw [abs_inv, show -2 * Real.pi = -(2 * Real.pi) by ring,
          abs_neg, abs_of_pos hpi]]
      rfl

/-- Uniform comparison of two smoothed CDFs gives a shifted sandwich for the
underlying CDFs.  This is the deterministic interface between a compact
smoothing kernel and the Fourier estimate of the smoothed functions. -/
theorem cdf_shift_bounds_of_smoothing_approximation
    (μ ν : Measure ℝ) {Hμ Hν : ℝ → ℝ} {δ ε : ℝ}
    (hμ : CDFSmoothingApproximation μ Hμ δ)
    (hν : CDFSmoothingApproximation ν Hν δ)
    (hdiff : ∀ x, |Hμ x - Hν x| ≤ ε) :
    (∀ x, cdf ν (x - 2 * δ) - ε ≤ cdf μ x) ∧
      (∀ x, cdf μ x ≤ cdf ν (x + 2 * δ) + ε) := by
  constructor
  · intro x
    have hνLeft := (hν (x - δ)).1
    have hμRight := (hμ (x - δ)).2
    have hcompare := (abs_le.mp (hdiff (x - δ))).1
    have hνLeft' : cdf ν (x - 2 * δ) ≤ Hν (x - δ) := by
      convert hνLeft using 1 <;> ring
    have hμRight' : Hμ (x - δ) ≤ cdf μ x := by
      convert hμRight using 1 <;> ring
    linarith
  · intro x
    have hμLeft := (hμ (x + δ)).1
    have hνRight := (hν (x + δ)).2
    have hcompare := (abs_le.mp (hdiff (x + δ))).2
    have hμLeft' : cdf μ x ≤ Hμ (x + δ) := by
      convert hμLeft using 1 <;> ring
    have hνRight' : Hν (x + δ) ≤ cdf ν (x + 2 * δ) := by
      convert hνRight using 1 <;> ring
    linarith

/-- Standard-normal Kolmogorov consequence of comparing two smoothed CDFs.
The remaining Berry--Esseen Fourier step can therefore target only the
uniform error `ε` between the smoothed functions. -/
theorem kolmogorovDistance_standardNormal_le_of_smoothing_approximation
    (μ : Measure ℝ) {Hμ Hγ : ℝ → ℝ} {δ ε : ℝ} (hδ : 0 ≤ δ)
    (hμ : CDFSmoothingApproximation μ Hμ δ)
    (hγ : CDFSmoothingApproximation
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw Hγ δ)
    (hdiff : ∀ x, |Hμ x - Hγ x| ≤ ε) :
    kolmogorovDistance μ
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ε + (Real.sqrt (2 * Real.pi))⁻¹ * (2 * δ) := by
  rcases cdf_shift_bounds_of_smoothing_approximation
      μ NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hμ hγ hdiff with ⟨hlower, hupper⟩
  exact kolmogorovDistance_standardNormal_le_of_shift_bounds μ
    (mul_nonneg (by norm_num) hδ) hlower hupper

/-- The Chapter 1 standard-normal law has a finite first absolute moment. -/
theorem integrable_abs_standardNormalLaw :
    Integrable (fun x : ℝ => |x|)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  have hid : Integrable (fun x : ℝ => x)
      (ProbabilityTheory.gaussianReal 0 1) := by
    apply ProbabilityTheory.integrable_of_mem_interior_integrableExpSet
    simp
  simpa only [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
    Real.norm_eq_abs] using hid.norm

/-- Exact first absolute moment of the standard normal law. -/
theorem integral_standardNormal_abs :
    (∫ x : ℝ, |x|
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
      2 * (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
    integral_gaussianReal_eq_integral_smul (by norm_num : (1 : ℝ≥0) ≠ 0)]
  simp only [smul_eq_mul,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
  rw [show (fun x : ℝ =>
      (Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(x ^ 2) / 2) * |x|) =
      fun x => (Real.sqrt (2 * Real.pi))⁻¹ *
        (|x| * Real.exp (-(x ^ 2) / 2)) by
      funext x; ring,
    integral_const_mul]
  have hEven :
      (∫ x : ℝ, |x| * Real.exp (-(x ^ 2) / 2)) =
        2 * ∫ x in Ioi (0 : ℝ), x * Real.exp (-(x ^ 2) / 2) := by
    calc
      (∫ x : ℝ, |x| * Real.exp (-(x ^ 2) / 2)) =
          ∫ x : ℝ, (fun z : ℝ => z * Real.exp (-(z ^ 2) / 2)) |x| := by
        apply integral_congr_ae
        filter_upwards [] with x
        rw [sq_abs]
      _ = 2 * ∫ x in Ioi (0 : ℝ), x * Real.exp (-(x ^ 2) / 2) :=
        integral_comp_abs (f := fun z : ℝ => z * Real.exp (-(z ^ 2) / 2))
  rw [hEven,
    NumStability.HDP.Scalar.GaussianTails.integral_Ioi_mul_exp_neg_sq_div_two]
  simp
  ring

/-- Exact third absolute moment of the standard normal law. -/
theorem integral_standardNormal_abs_cube :
    (∫ x : ℝ, |x| ^ 3
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
      4 * (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
    integral_gaussianReal_eq_integral_smul (by norm_num : (1 : ℝ≥0) ≠ 0)]
  simp only [smul_eq_mul,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
  rw [show (fun x : ℝ =>
      (Real.sqrt (2 * Real.pi))⁻¹ * Real.exp (-(x ^ 2) / 2) * |x| ^ 3) =
      fun x => (Real.sqrt (2 * Real.pi))⁻¹ *
        (|x| ^ 3 * Real.exp (-(x ^ 2) / 2)) by
      funext x
      ring,
    integral_const_mul]
  have hHalf :
      (∫ x in Ioi (0 : ℝ), x ^ 3 * Real.exp (-(x ^ 2) / 2)) = 2 := by
    convert (integral_rpow_mul_exp_neg_mul_rpow
      (p := (2 : ℝ)) (q := (3 : ℝ)) (b := (1 / 2 : ℝ))
      (by norm_num) (by norm_num) (by norm_num)) using 1
    · apply setIntegral_congr_fun measurableSet_Ioi
      intro x hx
      simp only [Real.rpow_ofNat]
      congr 1
      ring
    · rw [show ((3 : ℝ) + 1) / 2 = 1 + 1 by norm_num,
        Real.Gamma_add_one one_ne_zero, Real.Gamma_one]
      norm_num
  have hEven :
      (∫ x : ℝ, |x| ^ 3 * Real.exp (-(x ^ 2) / 2)) =
        2 * ∫ x in Ioi (0 : ℝ), x ^ 3 * Real.exp (-(x ^ 2) / 2) := by
    calc
      (∫ x : ℝ, |x| ^ 3 * Real.exp (-(x ^ 2) / 2)) =
          ∫ x : ℝ, (fun z : ℝ => z ^ 3 * Real.exp (-(z ^ 2) / 2)) |x| := by
        apply integral_congr_ae
        filter_upwards [] with x
        rw [sq_abs]
      _ = 2 * ∫ x in Ioi (0 : ℝ), x ^ 3 * Real.exp (-(x ^ 2) / 2) :=
        integral_comp_abs (f := fun z : ℝ => z ^ 3 * Real.exp (-(z ^ 2) / 2))
  rw [hEven, hHalf]
  ring

/-- The real Fourier transform of the unnormalized standard Gaussian. -/
theorem integral_rexp_neg_sq_div_two_mul_cos (x : ℝ) :
    (∫ t : ℝ, Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) =
      Real.sqrt (2 * Real.pi) * Real.exp (-(x ^ 2) / 2) := by
  let h : ℝ → ℂ := fun t =>
    (ProbabilityTheory.gaussianPDFReal 0 1 t : ℂ) *
      Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I)
  have hh : Integrable h := by
    have hpdfCont : Continuous
        (ProbabilityTheory.gaussianPDFReal 0 1) := by
      rw [ProbabilityTheory.gaussianPDFReal_def]
      fun_prop
    have hcont : Continuous h :=
      (Complex.continuous_ofReal.comp hpdfCont).mul (by fun_prop)
    rw [← integrable_norm_iff hcont.aestronglyMeasurable]
    convert ProbabilityTheory.integrable_gaussianPDFReal 0 1 using 1
    funext t
    dsimp only [h]
    rw [norm_mul, Complex.norm_real, Real.norm_eq_abs,
      abs_of_nonneg (ProbabilityTheory.gaussianPDFReal_nonneg 0 1 t),
      Complex.norm_exp_ofReal_mul_I, mul_one]
  have hcf :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun x
  rw [MeasureTheory.charFun_apply_real,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
    ProbabilityTheory.integral_gaussianReal_eq_integral_smul
      (by norm_num : (1 : ℝ≥0) ≠ 0)] at hcf
  have hcf' : (∫ t : ℝ, h t) =
      Complex.exp (-(x : ℂ) ^ 2 / 2) := by
    simpa only [h, Complex.real_smul, Complex.ofReal_mul] using hcf
  have hreal := congrArg Complex.re hcf'
  have hreal' : (∫ t : ℝ, (h t).re) =
      (Complex.exp (-(x : ℂ) ^ 2 / 2)).re :=
    (integral_re hh).trans hreal
  have hrhs : (Complex.exp (-(x : ℂ) ^ 2 / 2)).re =
      Real.exp (-(x ^ 2) / 2) := by
    rw [show -(x : ℂ) ^ 2 / 2 = ((-(x ^ 2) / 2 : ℝ) : ℂ) by
      push_cast
      ring,
      Complex.exp_ofReal_re]
  have hnormalized :
      (∫ t : ℝ,
          (Real.sqrt (2 * Real.pi))⁻¹ *
            (Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) =
        Real.exp (-(x ^ 2) / 2) := by
    rw [hrhs] at hreal'
    calc
      (∫ t : ℝ, (Real.sqrt (2 * Real.pi))⁻¹ *
          (Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) =
          ∫ t : ℝ, ((Real.sqrt (2 * Real.pi))⁻¹ *
            Real.exp (-(t ^ 2) / 2)) * Real.cos (x * t) := by
        apply integral_congr_ae
        exact ae_of_all _ fun t => by ring
      _ = Real.exp (-(x ^ 2) / 2) := by
        simpa only [h, Complex.real_smul,
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf,
          Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im,
          Complex.exp_ofReal_mul_I_re, zero_mul, sub_zero] using hreal'
  rw [integral_const_mul] at hnormalized
  have hR : 0 < Real.sqrt (2 * Real.pi) := by positivity
  calc
    (∫ t : ℝ, Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) =
        Real.sqrt (2 * Real.pi) *
          ((Real.sqrt (2 * Real.pi))⁻¹ *
            ∫ t : ℝ, Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) := by
      field_simp [hR.ne']
      apply integral_congr_ae
      exact ae_of_all _ fun t => by
        dsimp only
        rw [mul_comm t x]
    _ = Real.sqrt (2 * Real.pi) * Real.exp (-(x ^ 2) / 2) := by
      rw [hnormalized]

/-- The exact positive-frequency half of the Gaussian cosine transform. -/
theorem integral_Ioi_rexp_neg_sq_div_two_mul_cos (x : ℝ) :
    (∫ t in Ioi (0 : ℝ),
        Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) =
      (Real.sqrt (2 * Real.pi) / 2) * Real.exp (-(x ^ 2) / 2) := by
  let f : ℝ → ℝ := fun t =>
    Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hEven : (∫ t : ℝ, f t) =
      2 * ∫ t in Ioi (0 : ℝ), f t := by
    calc
      (∫ t : ℝ, f t) = ∫ t : ℝ, f |t| := by
        apply integral_congr_ae
        exact ae_of_all _ fun t => by
          dsimp only [f]
          by_cases ht : 0 ≤ t
          · rw [abs_of_nonneg ht]
          · have ht' : t ≤ 0 := le_of_not_ge ht
            rw [abs_of_nonpos ht']
            rw [show (-t) ^ 2 = t ^ 2 by ring,
              show x * -t = -(x * t) by ring, Real.cos_neg]
      _ = 2 * ∫ t in Ioi (0 : ℝ), f t := integral_comp_abs
  have hfull := integral_rexp_neg_sq_div_two_mul_cos x
  change (∫ t in Ioi (0 : ℝ), f t) = _
  change (∫ t : ℝ, f t) = _ at hfull
  rw [hEven] at hfull
  linarith

/-- Exact first Gaussian moment on a finite positive frequency window. -/
theorem integral_Icc_mul_rexp_neg_sq_div_two
    {T : ℝ} (hT : 0 < T) :
    (∫ t in Icc (0 : ℝ) T, t * Real.exp (-(t ^ 2) / 2)) =
      1 - Real.exp (-(T ^ 2) / 2) := by
  let f : ℝ → ℝ := fun t => t * Real.exp (-(t ^ 2) / 2)
  have hf : Integrable f := by
    have h := integrable_mul_exp_neg_mul_sq
      (by norm_num : (0 : ℝ) < 1 / 2)
    apply h.congr
    exact ae_of_all _ fun t => by
      dsimp only [f]
      congr 1
      ring
  have hsubset : Ici T ⊆ Ioi (0 : ℝ) := by
    intro t ht
    exact lt_of_lt_of_le hT ht
  have hdiff := integral_diff measurableSet_Ici hf.integrableOn hsubset
  have hset : Ioi (0 : ℝ) \ Ici T = Ioo (0 : ℝ) T := by
    ext t
    simp only [mem_diff, mem_Ioi, mem_Ici, mem_Ioo]
    constructor
    · intro ht
      exact ⟨ht.1, lt_of_not_ge ht.2⟩
    · intro ht
      exact ⟨ht.1, not_le.mpr ht.2⟩
  rw [hset, integral_Ici_eq_integral_Ioi] at hdiff
  rw [integral_Icc_eq_integral_Ioo, hdiff]
  rw [show (∫ t in Ioi (0 : ℝ), f t) = 1 by
    have h0 :=
      NumStability.HDP.Scalar.GaussianTails.integral_Ioi_mul_exp_neg_sq_div_two 0
    have hzeroArg : -((0 : ℝ) ^ 2) / 2 = 0 := by norm_num
    rw [hzeroArg, Real.exp_zero] at h0
    simpa only [f] using h0,
    show (∫ t in Ioi T, f t) = Real.exp (-(T ^ 2) / 2) by
      simpa only [f] using
        NumStability.HDP.Scalar.GaussianTails.integral_Ioi_mul_exp_neg_sq_div_two T]

/-- Explicit standard-normal specialization of the weighted upper signed-CDF
tail control. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_sq_mul_abs_cdfUpperError_le_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, |x| ^ 2 * |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      ((∫ y, |y| ^ 3 ∂nu) +
        4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3 := by
  simpa only [integral_standardNormal_abs_cube] using
    h.integral_abs_sq_mul_abs_cdfUpperError_le

/-- Explicit standard-normal specialization for the lower signed CDF error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_sq_mul_abs_cdfLowerError_le_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) :
    (∫ x, |x| ^ 2 * |cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      ((∫ y, |y| ^ 3 ∂nu) +
        4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3 := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.integral_abs_sq_mul_abs_cdfUpperError_le_explicit

/-- Explicit outside-radius upper signed-error bound with the standard-normal
third moment evaluated. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfUpperError_restrict_abs_ge_le_explicit
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x in {x : ℝ | R ≤ |x|}, |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
        4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  simpa only [integral_standardNormal_abs_cube] using
    h.integral_abs_cdfUpperError_restrict_abs_ge_le hR

/-- Explicit outside-radius lower signed-error bound. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfLowerError_restrict_abs_ge_le_explicit
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x in {x : ℝ | R ≤ |x|}, |cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
        4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.integral_abs_cdfUpperError_restrict_abs_ge_le_explicit hR

/-- Explicit combined central/tail `L¹` estimate for the upper signed error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfUpperError_le_central_tail_explicit
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x, |cdfUpperError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      2 * R * kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
          4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  simpa only [integral_standardNormal_abs_cube] using
    h.integral_abs_cdfUpperError_le_central_tail hR

/-- Explicit combined central/tail estimate for the lower signed error. -/
theorem IsStandardizedThirdMomentLaw.integral_abs_cdfLowerError_le_central_tail_explicit
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (hR : 0 < R) :
    (∫ x, |cdfLowerError nu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x|) ≤
      2 * R * kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
          4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.integral_abs_cdfUpperError_le_central_tail_explicit hR

/-- The two standardized spatial cancellations specialized to the exact
Gaussian keep/innovation operator.  The innovation density contributes its
inverse-cubic scale and the keep pushforward contributes its exact cubic
weighted Jacobian. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfUpperError_normalizedGaussianStepLaw_le_weightedIntegral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfUpperError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) /
          normalizedGaussianInnovationCoefficient N ^ 3) *
        (normalizedGaussianKeepCoefficient N ^ 3 *
          ∫ z, |z| ^ 2 * |cdfUpperError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z|) := by
  let gamma := NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let a := normalizedGaussianKeepCoefficient N
  let b := normalizedGaussianInnovationCoefficient N
  let nuScaled := Measure.map (fun z : ℝ => a * z) nu
  let gammaScaled := Measure.map (fun z : ℝ => a * z) gamma
  letI : IsProbabilityMeasure nu := h.probability
  letI : IsProbabilityMeasure nuScaled :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  letI : IsProbabilityMeasure gammaScaled :=
    Measure.isProbabilityMeasure_map (by fun_prop)
  have ha : 0 < a := normalizedGaussianKeepCoefficient_pos N
  have hb : 0 < b := normalizedGaussianInnovationCoefficient_pos N
  have hnu1 : Integrable (fun z : ℝ => z) nuScaled :=
    integrable_id_map_mul_real nu a h.integrable_id
  have hgamma1 : Integrable (fun z : ℝ => z) gammaScaled :=
    integrable_id_map_mul_real gamma a integrable_standardNormal_id
  have hnuAbs : Integrable (fun z : ℝ => |z|) nuScaled := by
    simpa only [Real.norm_eq_abs] using hnu1.norm
  have hgammaAbs : Integrable (fun z : ℝ => |z|) gammaScaled := by
    simpa only [Real.norm_eq_abs] using hgamma1.norm
  have hnu2 : Integrable (fun z : ℝ => z ^ 2) nuScaled :=
    integrable_sq_map_mul_real nu a h.integrable_sq
  have hgamma2 : Integrable (fun z : ℝ => z ^ 2) gammaScaled :=
    integrable_sq_map_mul_real gamma a integrable_standardNormal_sq
  have hnu3 : Integrable (fun z : ℝ => |z| ^ 3) nuScaled :=
    integrable_abs_cube_map_mul_real nu a h.integrable_abs_cube
  have hgamma3 : Integrable (fun z : ℝ => |z| ^ 3) gammaScaled :=
    integrable_abs_cube_map_mul_real gamma a integrable_standardNormal_abs_cube
  have he : Integrable (cdfUpperError nuScaled gammaScaled) := by
    simpa only [cdfUpperError] using
      integrable_cdf_sub_cdf_of_firstMoments _ _ hnuAbs hgammaAbs
  have he1 : Integrable (fun z => z * cdfUpperError nuScaled gammaScaled z) := by
    have hnuC := integrable_id_mul_centeredCDF nuScaled hnu2
    have hgammaC := integrable_id_mul_centeredCDF gammaScaled hgamma2
    apply (hnuC.sub hgammaC).congr
    exact ae_of_all _ fun z => by
      simp only [Pi.sub_apply, cdfUpperError, centeredCDF]
      ring
  have he2 : Integrable
      (fun z => |z| ^ 2 * |cdfUpperError nuScaled gammaScaled z|) := by
    unfold cdfUpperError
    exact integrable_abs_sq_mul_abs_cdf_sub_cdf _ _ hnu3 hgamma3
  have hzero : (∫ z, cdfUpperError nuScaled gammaScaled z) = 0 := by
    unfold cdfUpperError
    rw [integral_cdf_sub_cdf_eq_integral_id_sub _ _ hnuAbs hgammaAbs,
      integral_id_map_mul_real, integral_id_map_mul_real, h.centered,
      integral_standardNormal_id]
    ring
  have hone : (∫ z, z * cdfUpperError nuScaled gammaScaled z) = 0 := by
    unfold cdfUpperError
    rw [integral_id_mul_cdf_sub_cdf_eq_secondMoment_sub _ _ hnu2 hgamma2,
      integral_sq_map_mul_real, integral_sq_map_mul_real,
      h.unit_second_moment, integral_standardNormal_sq]
    ring
  have hsmooth :=
    abs_cdfUpperError_map_mul_standardNormal_conv_le_of_cancellations
      nuScaled gammaScaled hb he he1 he2 hzero hone x
  change |cdfUpperError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N gamma) x| ≤
    (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / b ^ 3) *
      ∫ z, |z| ^ 2 * |cdfUpperError nuScaled gammaScaled z| at hsmooth
  rw [standardNormalLaw_normalizedGaussian_step N] at hsmooth
  calc
    |cdfUpperError
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / b ^ 3) *
          ∫ z, |z| ^ 2 * |cdfUpperError nuScaled gammaScaled z| := hsmooth
    _ = (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) / b ^ 3) *
        (a ^ 3 * ∫ z, |z| ^ 2 * |cdfUpperError nu gamma z|) := by
      rw [integral_abs_sq_mul_abs_cdfUpperError_map_mul_eq nu gamma ha]
    _ = _ := by rfl

/-- Lower signed-error companion to the exact weighted cancellation branch. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfLowerError_normalizedGaussianStepLaw_le_weightedIntegral
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfLowerError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) /
          normalizedGaussianInnovationCoefficient N ^ 3) *
        (normalizedGaussianKeepCoefficient N ^ 3 *
          ∫ z, |z| ^ 2 * |cdfLowerError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z|) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.abs_cdfUpperError_normalizedGaussianStepLaw_le_weightedIntegral N x

/-- Closed-form version of the upper second-order cancellation branch. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfUpperError_normalizedGaussianStepLaw_le_weightedIntegral_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfUpperError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          ∫ z, |z| ^ 2 * |cdfUpperError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  calc
    |cdfUpperError
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) /
            normalizedGaussianInnovationCoefficient N ^ 3) *
          (normalizedGaussianKeepCoefficient N ^ 3 *
            ∫ z, |z| ^ 2 * |cdfUpperError nu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z|) :=
      h.abs_cdfUpperError_normalizedGaussianStepLaw_le_weightedIntegral N x
    _ = (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          ∫ z, |z| ^ 2 * |cdfUpperError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
      rw [← mul_assoc, normalizedGaussianCancellationCoefficient]

/-- Closed-form lower second-order cancellation branch. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfLowerError_normalizedGaussianStepLaw_le_weightedIntegral_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfLowerError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          ∫ z, |z| ^ 2 * |cdfLowerError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.abs_cdfUpperError_normalizedGaussianStepLaw_le_weightedIntegral_explicit N x

/-- The upper cancellation branch with the weighted norm replaced by the
certified third absolute moments. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfUpperError_normalizedGaussianStepLaw_le_thirdMoment
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfUpperError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  calc
    |cdfUpperError
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
        (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
          (Real.sqrt (N + 1 : ℝ)) ^ 3) *
            ∫ z, |z| ^ 2 * |cdfUpperError nu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| :=
      h.abs_cdfUpperError_normalizedGaussianStepLaw_le_weightedIntegral_explicit N x
    _ ≤ (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) :=
      mul_le_mul_of_nonneg_left
        h.integral_abs_sq_mul_abs_cdfUpperError_le_explicit
        (mul_nonneg
          (div_nonneg
            (mul_nonneg (by norm_num) (inv_nonneg.mpr (Real.sqrt_nonneg _)))
            (by norm_num))
          (pow_nonneg (Real.sqrt_nonneg _) 3))

/-- Lower signed-error companion to the explicit third-moment branch. -/
theorem IsStandardizedThirdMomentLaw.abs_cdfLowerError_normalizedGaussianStepLaw_le_thirdMoment
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (x : ℝ) :
    |cdfLowerError
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x| ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  simpa only [cdfLowerError_eq_neg_upper, abs_neg] using
    h.abs_cdfUpperError_normalizedGaussianStepLaw_le_thirdMoment N x

/-- Full Kolmogorov version of the explicit two-cancellation branch. -/
theorem IsStandardizedThirdMomentLaw.kolmogorovDistance_normalizedGaussianStepLaw_le_cancellation
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) :
    kolmogorovDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
        (Real.sqrt (N + 1 : ℝ)) ^ 3) *
          (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3) := by
  rw [kolmogorovDistance_eq_max_signed]
  apply max_le
  · apply csSup_le (Set.range_nonempty _)
    rintro _ ⟨x, rfl⟩
    exact (le_abs_self _).trans
      (h.abs_cdfUpperError_normalizedGaussianStepLaw_le_thirdMoment N x)
  · apply csSup_le (Set.range_nonempty _)
    rintro _ ⟨x, rfl⟩
    exact (le_abs_self _).trans
      (h.abs_cdfLowerError_normalizedGaussianStepLaw_le_thirdMoment N x)

/-- Combining exact Gaussian `L¹ -> L∞` smoothing with the explicit
central/tail decomposition gives a second certified branch for one normalized
keep/innovation step. -/
theorem IsStandardizedThirdMomentLaw.kolmogorovDistance_normalizedGaussianStepLaw_le_central_tail
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (hR : 0 < R) :
    kolmogorovDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        (2 * R * kolmogorovDistance nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
          (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3)) := by
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
        (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
          ∫ z, |cdfUpperError nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw z| :=
      h.kolmogorovDistance_normalizedGaussianStepLaw_le_integral N
    _ ≤ (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
        (2 * R * kolmogorovDistance nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
          (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3)) :=
      mul_le_mul_of_nonneg_left
        (h.integral_abs_cdfUpperError_le_central_tail_explicit hR)
        (mul_nonneg (Real.sqrt_nonneg _)
          (inv_nonneg.mpr (Real.sqrt_nonneg _)))

/-- Expanded central/tail branch.  In particular, its exact multiplicative
coefficient on the incoming Kolmogorov distance is
`2 R sqrt(N+1) / sqrt(2π)`; the remaining term is the certified tail cost. -/
theorem IsStandardizedThirdMomentLaw.kolmogorovDistance_normalizedGaussianStepLaw_le_central_tail_expanded
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (hR : 0 < R) :
    kolmogorovDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (2 * R * (Real.sqrt (N + 1 : ℝ) *
        (Real.sqrt (2 * Real.pi))⁻¹)) *
          kolmogorovDistance nu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
        (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
          ((R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
            4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3)) := by
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
        (Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
          (2 * R * kolmogorovDistance nu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
            (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
              4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3)) :=
      h.kolmogorovDistance_normalizedGaussianStepLaw_le_central_tail N hR
    _ = _ := by ring

/-- The usable one-step estimate is the minimum of exact norm-one
nonexpansiveness and the quantitative central/tail smoothing branch. -/
theorem IsStandardizedThirdMomentLaw.kolmogorovDistance_normalizedGaussianStepLaw_le_min_central_tail
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (hR : 0 < R) :
    kolmogorovDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      min
        (kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        ((Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
          (2 * R * kolmogorovDistance nu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
            (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
              4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3))) := by
  letI : IsProbabilityMeasure nu := h.probability
  apply le_min
  · exact kolmogorovDistance_normalizedGaussianStepLaw_le nu N
  · exact h.kolmogorovDistance_normalizedGaussianStepLaw_le_central_tail N hR

/-- Mechanical comparison of all three certified Gaussian-step branches:
norm-one transport, density-peak plus central/tail splitting, and the
second-order two-cancellation remainder. -/
theorem IsStandardizedThirdMomentLaw.kolmogorovDistance_normalizedGaussianStepLaw_le_min_three
    {nu : Measure ℝ} {B R : ℝ} (h : IsStandardizedThirdMomentLaw nu B)
    (N : ℕ) (hR : 0 < R) :
    kolmogorovDistance
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      min
        (kolmogorovDistance nu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        (min
          ((Real.sqrt (N + 1 : ℝ) * (Real.sqrt (2 * Real.pi))⁻¹) *
            (2 * R * kolmogorovDistance nu
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw +
              (R ^ 2)⁻¹ * (((∫ y, |y| ^ 3 ∂nu) +
                4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3)))
          ((((3 * (Real.sqrt (2 * Real.pi))⁻¹) / 2) *
              (Real.sqrt (N + 1 : ℝ)) ^ 3) *
            (((∫ y, |y| ^ 3 ∂nu) +
              4 * (Real.sqrt (2 * Real.pi))⁻¹) / 3))) := by
  letI : IsProbabilityMeasure nu := h.probability
  apply le_min
  · exact kolmogorovDistance_normalizedGaussianStepLaw_le nu N
  · apply le_min
    · exact h.kolmogorovDistance_normalizedGaussianStepLaw_le_central_tail N hR
    · exact h.kolmogorovDistance_normalizedGaussianStepLaw_le_cancellation N

/-- The exact `L³` norm of the identity under the standard-normal law. -/
theorem lpNorm_id_standardNormal_three :
    lpNorm (fun y : ℝ => y) (3 : ℝ≥0∞)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw =
      (4 * (Real.sqrt (2 * Real.pi))⁻¹) ^ ((3 : ℝ)⁻¹) := by
  rw [lpNorm_eq_integral_norm_rpow_toReal (p := (3 : ℝ≥0∞))
    (by norm_num) (by norm_num) (by fun_prop)]
  calc
    (∫ x : ℝ, ‖x‖ ^ ENNReal.toReal 3
        ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) ^
        (ENNReal.toReal 3)⁻¹ =
      (∫ x : ℝ, |x| ^ (3 : ℕ)
        ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) ^
        ((3 : ℝ)⁻¹) := by
          congr 1
          apply integral_congr_ae
          filter_upwards [] with x
          rw [Real.norm_eq_abs]
          norm_num
    _ = _ := congrArg (fun r : ℝ => r ^ ((3 : ℝ)⁻¹))
      integral_standardNormal_abs_cube

/-- Explicit quantitative third-moment-root envelope for one Gaussian
keep/innovation step. -/
theorem lpNorm_normalizedGaussianStepLaw_le_explicit
    (N : ℕ) (nu : Measure ℝ) [IsProbabilityMeasure nu]
    (hnu3 : Integrable (fun y : ℝ => |y| ^ 3) nu) :
    lpNorm (fun y : ℝ => y) 3 (normalizedGaussianStepLaw N nu) ≤
      normalizedGaussianInnovationCoefficient N *
          (4 * (Real.sqrt (2 * Real.pi))⁻¹) ^ ((3 : ℝ)⁻¹) +
        normalizedGaussianKeepCoefficient N * lpNorm (fun y : ℝ => y) 3 nu := by
  simpa only [lpNorm_id_standardNormal_three] using
    lpNorm_normalizedGaussianStepLaw_le N nu hnu3

/-- Explicit bounded standardized-law transport using the exact
standard-normal third-moment root. -/
theorem IsStandardizedThirdMomentLaw.normalizedGaussianStepLaw_explicit
    {nu : Measure ℝ} {B : ℝ} (h : IsStandardizedThirdMomentLaw nu B) (N : ℕ) :
    IsStandardizedThirdMomentLaw
      (NumStability.HDP.Scalar.BerryEsseen.normalizedGaussianStepLaw N nu)
      (normalizedGaussianInnovationCoefficient N *
          (4 * (Real.sqrt (2 * Real.pi))⁻¹) ^ ((3 : ℝ)⁻¹) +
        normalizedGaussianKeepCoefficient N * B) := by
  simpa only [lpNorm_id_standardNormal_three] using
    IsStandardizedThirdMomentLaw.normalizedGaussianStepLaw h N

/-- Complete uniform-smoothing reduction for comparison with the standard
normal law. The only remaining quantitative input is the integral of the
smoothed characteristic-function discrepancy quotient. -/
theorem kolmogorovDistance_standardNormal_le_uniformSmoothing
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    {delta : ℝ} (hdelta : 0 < delta) :
    kolmogorovDistance mu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (2 * Real.pi)⁻¹ * (∫ t : ℝ, charFunDiscrepancyQuotient
        (uniformSmoothingKernel delta ∗ mu)
        (uniformSmoothingKernel delta ∗
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) t) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * delta) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel delta) :=
    uniformSmoothingKernel_isProbabilityMeasure hdelta
  apply kolmogorovDistance_standardNormal_le_of_smoothing_approximation
    mu hdelta.le
    (uniformSmoothedCDF_isSmoothingApproximation mu hdelta)
    (uniformSmoothedCDF_isSmoothingApproximation
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hdelta)
  intro x
  rw [smoothedCDF_eq_cdf_conv, smoothedCDF_eq_cdf_conv]
  exact cdfGap_uniformSmoothingKernel_conv_le_integral_discrepancy_scaled
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
    hmu1 integrable_abs_standardNormalLaw hdelta x

/-- Sharp global cubic remainder for the imaginary-axis exponential. Two
applications of the fundamental theorem of calculus preserve the optimal
Taylor coefficient because `x ↦ exp (i x)` has unit speed. -/
theorem norm_cexp_mul_I_sub_quadratic_le_sharp (y : ℝ) :
    ‖Complex.exp ((y : ℂ) * Complex.I) - 1 - (y : ℂ) * Complex.I +
        (((y ^ 2 : ℝ) : ℂ) / 2)‖ ≤ |y| ^ 3 / 6 := by
  let f : ℝ → ℂ := fun x => Complex.exp ((x : ℂ) * Complex.I)
  let f2 : ℝ → ℂ := fun x => -f x + 1
  let f1 : ℝ → ℂ := fun x => f x * Complex.I - Complex.I + x
  let R : ℝ → ℂ := fun x =>
    f x - 1 - x * Complex.I + (((x ^ 2 : ℝ) : ℂ) / 2)
  have hf (x : ℝ) : HasDerivAt f (f x * Complex.I) x := by
    convert
      ((Complex.ofRealCLM.hasDerivAt (x := x)).mul_const Complex.I).cexp
        using 1
    change Complex.exp ((x : ℂ) * Complex.I) * Complex.I =
      Complex.exp ((x : ℂ) * Complex.I) * ((1 : ℂ) * Complex.I)
    norm_num
  have hf2 (x : ℝ) : HasDerivAt f1 (f2 x) x := by
    have hfi : HasDerivAt (fun z => f z * Complex.I) (-f x) x := by
      convert (hf x).mul_const Complex.I using 1
      rw [mul_assoc, Complex.I_mul_I]
      ring
    have hone : HasDerivAt (fun z : ℝ => (z : ℂ)) 1 x := by
      convert Complex.ofRealCLM.hasDerivAt (x := x) using 1
    convert (hfi.sub_const Complex.I).add hone using 1
  have hR (x : ℝ) : HasDerivAt R (f1 x) x := by
    have hxi : HasDerivAt (fun z : ℝ => (z : ℂ) * Complex.I) Complex.I x := by
      convert (Complex.ofRealCLM.hasDerivAt (x := x)).mul_const Complex.I using 1
      change Complex.I = Complex.ofRealCLM 1 * Complex.I
      rw [Complex.ofRealCLM_apply]
      norm_num
    have hx2 : HasDerivAt
        (fun z : ℝ => (((z ^ 2 : ℝ) : ℂ) / 2)) (x : ℂ) x := by
      convert (((hasDerivAt_id x).pow 2).ofReal_comp).div_const 2 using 1 <;>
        norm_num
    convert (((hf x).sub_const 1).sub hxi).add hx2 using 1
  have hf2cont : Continuous f2 := by
    dsimp only [f2, f]
    fun_prop
  have hf1cont : Continuous f1 := by
    dsimp only [f1, f]
    fun_prop
  have hpos : ∀ x : ℝ, 0 ≤ x → ‖R x‖ ≤ x ^ 3 / 6 := by
    intro x hx
    have hnorm2 : ∀ z ∈ Icc (0 : ℝ) x, ‖f2 z‖ ≤ z := by
      intro z hz
      dsimp only [f2, f]
      calc
        ‖-Complex.exp ((z : ℂ) * Complex.I) + 1‖ =
            ‖Complex.exp ((z : ℂ) * Complex.I) - 1‖ := by
          rw [← norm_neg]
          congr 1
          ring
        _ ≤ z := by
          simpa only [mul_comm, Real.norm_eq_abs, abs_of_nonneg hz.1] using
            (Real.norm_exp_I_mul_ofReal_sub_one_le (x := z))
    have hnorm1 : ∀ z ∈ Icc (0 : ℝ) x, ‖f1 z‖ ≤ z ^ 2 / 2 := by
      intro z hz
      have hFTCz : (∫ w in (0 : ℝ)..z, f2 w) = f1 z := by
        have h := intervalIntegral.integral_eq_sub_of_hasDerivAt
          (a := (0 : ℝ)) (b := z) (f := f1) (f' := f2)
          (fun w _ => hf2 w) (hf2cont.intervalIntegrable (a := 0) (b := z))
        simpa [f1, f] using h
      rw [← hFTCz]
      calc
        ‖∫ w in (0 : ℝ)..z, f2 w‖ ≤ ∫ w in (0 : ℝ)..z, w := by
          apply intervalIntegral.norm_integral_le_of_norm_le hz.1
          · exact ae_of_all volume fun w hw =>
              hnorm2 w ⟨hw.1.le, hw.2.trans hz.2⟩
          · exact continuous_id.intervalIntegrable (a := 0) (b := z)
        _ = z ^ 2 / 2 := by norm_num
    have hFTCR : (∫ z in (0 : ℝ)..x, f1 z) = R x := by
      have h := intervalIntegral.integral_eq_sub_of_hasDerivAt
        (a := (0 : ℝ)) (b := x) (f := R) (f' := f1)
        (fun z _ => hR z) (hf1cont.intervalIntegrable (a := 0) (b := x))
      simpa [R, f] using h
    rw [← hFTCR]
    calc
      ‖∫ z in (0 : ℝ)..x, f1 z‖ ≤ ∫ z in (0 : ℝ)..x, z ^ 2 / 2 := by
        apply intervalIntegral.norm_integral_le_of_norm_le hx
        · exact ae_of_all volume fun z hz => hnorm1 z ⟨hz.1.le, hz.2⟩
        · exact (by fun_prop : Continuous fun z : ℝ => z ^ 2 / 2) |>.intervalIntegrable
            (a := 0) (b := x)
      _ = x ^ 3 / 6 := by norm_num; ring
  change ‖R y‖ ≤ |y| ^ 3 / 6
  by_cases hy : 0 ≤ y
  · simpa only [abs_of_nonneg hy] using hpos y hy
  · have hneg : 0 ≤ -y := le_of_lt (neg_pos.mpr (lt_of_not_ge hy))
    have hsymmetric : ‖R y‖ = ‖R (-y)‖ := by
      rw [← Complex.norm_conj]
      congr 1
      dsimp only [R, f]
      simp only [map_add, map_sub, map_mul, map_div₀,
        Complex.conj_ofReal, Complex.conj_I, map_ofNat]
      rw [← Complex.exp_conj]
      simp [map_mul, Complex.conj_ofReal, Complex.conj_I]
    rw [hsymmetric]
    simpa only [abs_of_neg (lt_of_not_ge hy)] using hpos (-y) hneg

/-- A global cubic remainder bound for the imaginary-axis exponential.

The constant is deliberately nonoptimal. Its global form is the useful point:
after substituting `y = u * X`, a finite third absolute moment supplies an
integrable majorant without any boundedness assumption on `X`. -/
theorem norm_cexp_mul_I_sub_quadratic_le (y : ℝ) :
    ‖Complex.exp ((y : ℂ) * Complex.I) - 1 - (y : ℂ) * Complex.I +
        (((y ^ 2 : ℝ) : ℂ) / 2)‖ ≤
      4 * |y| ^ 3 := by
  by_cases hy : |y| ≤ 1
  · have hz : ‖(y : ℂ) * Complex.I‖ ≤ 1 := by
      simpa [Complex.norm_mul, Real.norm_eq_abs] using hy
    calc
      ‖Complex.exp ((y : ℂ) * Complex.I) - 1 - (y : ℂ) * Complex.I +
          (((y ^ 2 : ℝ) : ℂ) / 2)‖ =
          ‖Complex.exp ((y : ℂ) * Complex.I) -
            ∑ m ∈ Finset.range 3,
              (((y : ℂ) * Complex.I) ^ m / m.factorial)‖ := by
            congr 1
            simp [Finset.sum_range_succ, Nat.factorial]
            ring_nf
            rw [Complex.I_sq]
            ring
      _ ≤ ‖(y : ℂ) * Complex.I‖ ^ 3 *
          ((Nat.succ 3 : ℝ) * ((Nat.factorial 3) * (3 : ℕ) : ℝ)⁻¹) :=
        Complex.exp_bound hz (by decide)
      _ ≤ 4 * |y| ^ 3 := by
        simp [Real.norm_eq_abs, Nat.factorial]
        nlinarith [pow_nonneg (abs_nonneg y) 3]
  · have hy1 : 1 < |y| := lt_of_not_ge hy
    have hy1' : 1 ≤ |y| := hy1.le
    have hy_sq : |y| ≤ |y| ^ 2 := by
      nlinarith [mul_nonneg (abs_nonneg y) (sub_nonneg.mpr hy1')]
    have hy_cube : |y| ^ 2 ≤ |y| ^ 3 := by
      nlinarith [mul_nonneg (sq_nonneg |y|) (sub_nonneg.mpr hy1')]
    have hone_cube : 1 ≤ |y| ^ 3 :=
      hy1'.trans (hy_sq.trans hy_cube)
    calc
      ‖Complex.exp ((y : ℂ) * Complex.I) - 1 - (y : ℂ) * Complex.I +
          (((y ^ 2 : ℝ) : ℂ) / 2)‖ ≤
          ‖Complex.exp ((y : ℂ) * Complex.I)‖ + ‖(1 : ℂ)‖ +
            ‖(y : ℂ) * Complex.I‖ + ‖(((y ^ 2 : ℝ) : ℂ) / 2)‖ := by
              calc
                _ ≤ ‖Complex.exp ((y : ℂ) * Complex.I) - 1 -
                    (y : ℂ) * Complex.I‖ + ‖(((y ^ 2 : ℝ) : ℂ) / 2)‖ :=
                  norm_add_le _ _
                _ ≤ (‖Complex.exp ((y : ℂ) * Complex.I) - 1‖ +
                    ‖(y : ℂ) * Complex.I‖) +
                    ‖(((y ^ 2 : ℝ) : ℂ) / 2)‖ := by
                  gcongr
                  exact norm_sub_le _ _
                _ ≤ (‖Complex.exp ((y : ℂ) * Complex.I)‖ + ‖(1 : ℂ)‖ +
                    ‖(y : ℂ) * Complex.I‖) +
                    ‖(((y ^ 2 : ℝ) : ℂ) / 2)‖ := by
                  gcongr
                  exact norm_sub_le _ _
                _ = _ := by ring
      _ = 2 + |y| + |y| ^ 2 / 2 := by
        norm_num [Complex.norm_exp_ofReal_mul_I, Complex.norm_mul,
          Complex.norm_div, Real.norm_eq_abs, sq_abs]
      _ ≤ 4 * |y| ^ 3 := by
        nlinarith [hy_sq.trans hy_cube, hone_cube]

/-- The cubic exponential remainder passes through expectation under a finite
third absolute moment. This is the quantitative Taylor input used in the
characteristic-function half of Berry--Esseen. -/
theorem norm_integral_cexp_mul_I_sub_quadratic_le_sharp
    {Omega : Type*} [MeasurableSpace Omega] {mu : Measure Omega}
    (X : Omega → ℝ) (hX3 : Integrable (fun omega => |X omega| ^ 3) mu) (u : ℝ) :
    ‖∫ omega,
        (Complex.exp (((u * X omega : ℝ) : ℂ) * Complex.I) - 1 -
          ((u * X omega : ℝ) : ℂ) * Complex.I +
          ((((u * X omega) ^ 2 : ℝ) : ℂ) / 2)) ∂mu‖ ≤
      (|u| ^ 3 / 6) * ∫ omega, |X omega| ^ 3 ∂mu := by
  have hg : Integrable (fun omega => (|u| ^ 3 / 6) * |X omega| ^ 3) mu :=
    hX3.const_mul (|u| ^ 3 / 6)
  calc
    ‖∫ omega,
        (Complex.exp (((u * X omega : ℝ) : ℂ) * Complex.I) - 1 -
          ((u * X omega : ℝ) : ℂ) * Complex.I +
          ((((u * X omega) ^ 2 : ℝ) : ℂ) / 2)) ∂mu‖ ≤
        ∫ omega, (|u| ^ 3 / 6) * |X omega| ^ 3 ∂mu :=
      norm_integral_le_of_norm_le hg <| ae_of_all mu fun omega => by
        convert norm_cexp_mul_I_sub_quadratic_le_sharp (u * X omega) using 1 <;>
          simp only [abs_mul, mul_pow] <;> ring
    _ = (|u| ^ 3 / 6) * ∫ omega, |X omega| ^ 3 ∂mu := by
      rw [integral_const_mul]

/-- Coarser predecessor retained for compatibility with the already-certified
Berry--Esseen chain while the sharp coefficient is propagated incrementally. -/
theorem norm_integral_cexp_mul_I_sub_quadratic_le
    {Ω : Type*} [MeasurableSpace Ω] {μ : Measure Ω}
    (X : Ω → ℝ) (hX3 : Integrable (fun ω => |X ω| ^ 3) μ) (u : ℝ) :
    ‖∫ ω,
        (Complex.exp (((u * X ω : ℝ) : ℂ) * Complex.I) - 1 -
          ((u * X ω : ℝ) : ℂ) * Complex.I +
          ((((u * X ω) ^ 2 : ℝ) : ℂ) / 2)) ∂μ‖ ≤
      4 * |u| ^ 3 * ∫ ω, |X ω| ^ 3 ∂μ := by
  have hg : Integrable (fun ω => 4 * |u| ^ 3 * |X ω| ^ 3) μ :=
    hX3.const_mul (4 * |u| ^ 3)
  calc
    ‖∫ ω,
        (Complex.exp (((u * X ω : ℝ) : ℂ) * Complex.I) - 1 -
          ((u * X ω : ℝ) : ℂ) * Complex.I +
          ((((u * X ω) ^ 2 : ℝ) : ℂ) / 2)) ∂μ‖ ≤
        ∫ ω, 4 * |u| ^ 3 * |X ω| ^ 3 ∂μ :=
      norm_integral_le_of_norm_le hg <| ae_of_all μ fun ω => by
        simpa only [abs_mul, mul_pow, mul_assoc] using
          norm_cexp_mul_I_sub_quadratic_le (u * X ω)
    _ = 4 * |u| ^ 3 * ∫ ω, |X ω| ^ 3 ∂μ := by
      rw [integral_const_mul]

/-- A centered unit-second-moment characteristic function differs from its
quadratic Taylor polynomial by at most one sixth of the cubic third-moment
term. This is the sharp Taylor input for the Berry--Esseen chain. -/
theorem norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t -
        (1 - (t : ℂ) ^ 2 / 2)‖ ≤
      (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ := by
  have hXint : Integrable X μ := hX.integrable (by norm_num)
  have hX2 : Integrable (fun ω => (X ω) ^ 2) μ :=
    (hX.mono_exponent (by norm_num : (2 : ENNReal) ≤ 3)).integrable_sq
  have hX3 : Integrable (fun ω => |X ω| ^ 3) μ := by
    simpa only [Real.norm_eq_abs] using hX.integrable_norm_pow (by norm_num)
  have hExp : Integrable
      (fun ω => Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I)) μ := by
    refine Integrable.of_bound (by fun_prop) 1 ?_
    exact ae_of_all μ fun ω => by
      simp only [Complex.norm_exp_ofReal_mul_I]
      norm_num
  have hOne : Integrable (fun _ : Ω => (1 : ℂ)) μ := integrable_const 1
  have hLin : Integrable
      (fun ω => ((t * X ω : ℝ) : ℂ) * Complex.I) μ :=
    ((hXint.const_mul t).ofReal).mul_const Complex.I
  have hQuadReal : Integrable (fun ω => (t * X ω) ^ 2) μ := by
    simpa only [mul_pow] using hX2.const_mul (t ^ 2)
  have hQuad : Integrable
      (fun ω => ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2)) μ :=
    hQuadReal.ofReal.div_const 2
  have hLinZero :
      (∫ ω, ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ) = 0 := by
    calc
      (∫ ω, ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ) =
          (∫ ω, ((t * X ω : ℝ) : ℂ) ∂μ) * Complex.I :=
        integral_mul_const _ _
      _ = ((∫ ω, t * X ω ∂μ : ℝ) : ℂ) * Complex.I := by
        have hc : (∫ ω, ((t * X ω : ℝ) : ℂ) ∂μ) =
            ((∫ ω, t * X ω ∂μ : ℝ) : ℂ) := integral_complex_ofReal
        rw [hc]
      _ = ((t * ∫ ω, X ω ∂μ : ℝ) : ℂ) * Complex.I := by
        rw [integral_const_mul]
      _ = 0 := by rw [hMean]; simp
  have hQuadIntegral :
      (∫ ω, ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2) ∂μ) =
        (t : ℂ) ^ 2 / 2 := by
    calc
      (∫ ω, ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2) ∂μ) =
          (∫ ω, (((t * X ω) ^ 2 : ℝ) : ℂ) ∂μ) / 2 :=
        integral_div _ _
      _ = ((∫ ω, (t * X ω) ^ 2 ∂μ : ℝ) : ℂ) / 2 := by
        have hc : (∫ ω, (((t * X ω) ^ 2 : ℝ) : ℂ) ∂μ) =
            ((∫ ω, (t * X ω) ^ 2 ∂μ : ℝ) : ℂ) := integral_complex_ofReal
        rw [hc]
      _ = ((t ^ 2 * ∫ ω, (X ω) ^ 2 ∂μ : ℝ) : ℂ) / 2 := by
        congr 2
        simpa only [mul_pow] using
          (integral_const_mul (μ := μ) (t ^ 2) (fun ω => (X ω) ^ 2))
      _ = (t : ℂ) ^ 2 / 2 := by rw [hSecond]; norm_num
  have hsubExp :
      (∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 ∂μ) =
        (∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) ∂μ) -
          (∫ _ : Ω, (1 : ℂ) ∂μ) :=
    integral_sub hExp hOne
  have hsubLin :
      (∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 -
        ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ) =
        (∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 ∂μ) -
          (∫ ω, ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ) :=
    integral_sub (hExp.sub hOne) hLin
  have hRemainderIntegral :
      (∫ ω,
        (Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 -
          ((t * X ω : ℝ) : ℂ) * Complex.I +
          ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2)) ∂μ) =
        (∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) ∂μ) - 1 +
          (t : ℂ) ^ 2 / 2 := by
    calc
      _ = (∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 -
            ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ) +
          (∫ ω, ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2) ∂μ) := by
        exact integral_add (hExp.sub hOne |>.sub hLin) hQuad
      _ = ((∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 ∂μ) -
            (∫ ω, ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ)) +
          (∫ ω, ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2) ∂μ) := by
        rw [hsubLin]
      _ = (((∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) ∂μ) -
            (∫ _ : Ω, (1 : ℂ) ∂μ)) -
            (∫ ω, ((t * X ω : ℝ) : ℂ) * Complex.I ∂μ)) +
          (∫ ω, ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2) ∂μ) := by
        rw [hsubExp]
      _ = _ := by rw [hLinZero, hQuadIntegral]; simp [integral_const]
  rw [NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw]
  simp_rw [← Complex.ofReal_mul]
  calc
    ‖(∫ ω, Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) ∂μ) -
        (1 - (t : ℂ) ^ 2 / 2)‖ =
        ‖∫ ω,
          (Complex.exp (((t * X ω : ℝ) : ℂ) * Complex.I) - 1 -
            ((t * X ω : ℝ) : ℂ) * Complex.I +
            ((((t * X ω) ^ 2 : ℝ) : ℂ) / 2)) ∂μ‖ := by
      rw [hRemainderIntegral]
      congr 1
      ring
    _ ≤ (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ :=
      norm_integral_cexp_mul_I_sub_quadratic_le_sharp X hX3 t

/-- Sharp real-coordinate consequence of the cubic characteristic-function
Taylor remainder. -/
theorem abs_charFun_probabilityLaw_re_sub_quadratic_le_thirdMoment_sharp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) :
    |(MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t).re - (1 - t ^ 2 / 2)| ≤
      (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ := by
  let q : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        X hX.aemeasurable : Measure ℝ) t -
      ((1 - t ^ 2 / 2 : ℝ) : ℂ)
  have hproxy : ((1 - t ^ 2 / 2 : ℝ) : ℂ) =
      1 - (t : ℂ) ^ 2 / 2 := by
    push_cast
    ring
  have hq : ‖q‖ ≤ (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ := by
    dsimp only [q]
    rw [hproxy]
    exact norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
      X hX hMean hSecond t
  have hre : q.re =
      (MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) t).re - (1 - t ^ 2 / 2) := by
    simp only [q, Complex.sub_re, Complex.ofReal_re]
  rw [← hre]
  exact (Complex.abs_re_le_norm q).trans hq

/-- Sharp imaginary-coordinate consequence of the cubic characteristic-function
Taylor remainder; centering removes its linear term. -/
theorem abs_charFun_probabilityLaw_im_le_thirdMoment_sharp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) :
    |(MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t).im| ≤
      (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ := by
  let q : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        X hX.aemeasurable : Measure ℝ) t -
      ((1 - t ^ 2 / 2 : ℝ) : ℂ)
  have hproxy : ((1 - t ^ 2 / 2 : ℝ) : ℂ) =
      1 - (t : ℂ) ^ 2 / 2 := by
    push_cast
    ring
  have hq : ‖q‖ ≤ (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ := by
    dsimp only [q]
    rw [hproxy]
    exact norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
      X hX hMean hSecond t
  have him : q.im =
      (MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) t).im := by
    simp only [q, Complex.sub_im, Complex.ofReal_im, sub_zero]
  rw [← him]
  exact (Complex.abs_im_le_norm q).trans hq

/-- Signed real-coordinate form of the sharp cubic characteristic-function
Taylor estimate. -/
theorem charFun_probabilityLaw_re_sub_quadratic_mem_Icc_thirdMoment_sharp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) :
    (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t).re - (1 - t ^ 2 / 2) ∈
      Icc (-((|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ))
        ((|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ) := by
  rw [mem_Icc, ← abs_le]
  exact abs_charFun_probabilityLaw_re_sub_quadratic_le_thirdMoment_sharp
    X hX hMean hSecond t

/-- Signed imaginary-coordinate form of the sharp cubic characteristic-function
Taylor estimate. -/
theorem charFun_probabilityLaw_im_mem_Icc_thirdMoment_sharp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) :
    (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t).im ∈
      Icc (-((|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ))
        ((|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ) := by
  rw [mem_Icc, ← abs_le]
  exact abs_charFun_probabilityLaw_im_le_thirdMoment_sharp
    X hX hMean hSecond t

/-- Exact recurrence for the real coordinate of a complex power relative to a
real power baseline. -/
theorem complex_pow_succ_re_sub_real_pow
    (z : ℂ) (a : ℝ) (n : ℕ) :
    (z ^ (n + 1)).re - a ^ (n + 1) =
      a * ((z ^ n).re - a ^ n) +
        (z.re - a) * (z ^ n).re - z.im * (z ^ n).im := by
  simp only [pow_succ, Complex.mul_re]
  ring

/-- Exact recurrence for the imaginary coordinate of a complex power, split
around an arbitrary real baseline. -/
theorem complex_pow_succ_im_of_real_baseline
    (z : ℂ) (a : ℝ) (n : ℕ) :
    (z ^ (n + 1)).im =
      a * (z ^ n).im + (z.re - a) * (z ^ n).im +
        z.im * (z ^ n).re := by
  simp only [pow_succ, Complex.mul_im]
  ring

/-- Real-coordinate recurrence for powers of a characteristic function around
its quadratic Taylor baseline. -/
theorem charFun_probabilityLaw_pow_succ_re_sub_quadratic_pow
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : AEMeasurable X μ) (t : ℝ) (n : ℕ) :
    ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t) ^ (n + 1)).re -
        (1 - t ^ 2 / 2) ^ (n + 1) =
      (1 - t ^ 2 / 2) *
          (((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t) ^ n).re -
            (1 - t ^ 2 / 2) ^ n) +
        ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t).re - (1 - t ^ 2 / 2)) *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t) ^ n).re -
        (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t).im *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t) ^ n).im := by
  exact complex_pow_succ_re_sub_real_pow _ _ _

/-- Imaginary-coordinate recurrence for powers of a characteristic function
around its real quadratic Taylor baseline. -/
theorem charFun_probabilityLaw_pow_succ_im_of_quadratic_baseline
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : AEMeasurable X μ) (t : ℝ) (n : ℕ) :
    ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t) ^ (n + 1)).im =
      (1 - t ^ 2 / 2) *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t) ^ n).im +
        ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t).re - (1 - t ^ 2 / 2)) *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t) ^ n).im +
        (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t).im *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX : Measure ℝ) t) ^ n).re := by
  exact complex_pow_succ_im_of_real_baseline _ _ _

/-- The geometric coefficient in the exact factorization of `z^n-a^n` when
the comparison baseline `a` is real. -/
noncomputable def complexPowerRealBaselineGeomSum
    (z : ℂ) (a : ℝ) (n : ℕ) : ℂ :=
  ∑ i ∈ Finset.range n, z ^ i * (a : ℂ) ^ (n - 1 - i)

/-- Exact real-coordinate telescoping formula for a complex power relative to
a real power. -/
theorem complex_pow_sub_real_pow_re_eq_geomSum
    (z : ℂ) (a : ℝ) (n : ℕ) :
    (z ^ n).re - a ^ n =
      (z.re - a) * (complexPowerRealBaselineGeomSum z a n).re -
        z.im * (complexPowerRealBaselineGeomSum z a n).im := by
  have h := (Commute.all z (a : ℂ)).mul_geom_sum₂ n
  rw [← Complex.ofReal_pow a n] at h
  have hre := congrArg Complex.re h
  simpa only [Complex.mul_re, Complex.sub_re, Complex.sub_im, Complex.ofReal_re,
    Complex.ofReal_im, zero_mul, sub_zero, complexPowerRealBaselineGeomSum]
      using hre.symm

/-- Exact imaginary-coordinate telescoping formula for a complex power
relative to a real baseline. -/
theorem complex_pow_sub_real_pow_im_eq_geomSum
    (z : ℂ) (a : ℝ) (n : ℕ) :
    (z ^ n).im =
      (z.re - a) * (complexPowerRealBaselineGeomSum z a n).im +
        z.im * (complexPowerRealBaselineGeomSum z a n).re := by
  have h := (Commute.all z (a : ℂ)).mul_geom_sum₂ n
  rw [← Complex.ofReal_pow a n] at h
  have him := congrArg Complex.im h
  simpa only [Complex.mul_im, Complex.sub_re, Complex.sub_im,
    Complex.ofReal_re, Complex.ofReal_im, zero_mul, sub_zero,
    complexPowerRealBaselineGeomSum] using him.symm

/-- Exact factorization of a complex power discrepancy into its one-step
residual against a real baseline, the common geometric coefficient, and a
remaining real target correction. -/
theorem complex_pow_sub_real_eq_taylorGeomSum
    (z : ℂ) (a g : ℝ) (n : ℕ) :
    z ^ n - (g : ℂ) =
      (z - (a : ℂ)) * complexPowerRealBaselineGeomSum z a n +
        ((a ^ n - g : ℝ) : ℂ) := by
  have h := (Commute.all z (a : ℂ)).mul_geom_sum₂ n
  rw [← Complex.ofReal_pow a n] at h
  change (z - (a : ℂ)) * complexPowerRealBaselineGeomSum z a n =
    z ^ n - ((a ^ n : ℝ) : ℂ) at h
  rw [h]
  push_cast
  ring

/-- Real coordinate of the exact Taylor/geometric factorization, recombined
as the real part of the powered discrepancy. -/
theorem taylorGeomSum_realCoordinate_eq_pow_sub_real_re
    (z : ℂ) (a g : ℝ) (n : ℕ) :
    (z.re - a) * (complexPowerRealBaselineGeomSum z a n).re -
        z.im * (complexPowerRealBaselineGeomSum z a n).im + (a ^ n - g) =
      (z ^ n - (g : ℂ)).re := by
  have hre := congrArg Complex.re
    (complex_pow_sub_real_eq_taylorGeomSum z a g n)
  simpa only [Complex.add_re, Complex.mul_re, Complex.ofReal_re,
    Complex.sub_re, Complex.sub_im, Complex.ofReal_im, sub_zero] using hre.symm

/-- Imaginary coordinate of the exact Taylor/geometric factorization,
recombined as the imaginary part of the complex power. -/
theorem taylorGeomSum_imagCoordinate_eq_pow_sub_real_im
    (z : ℂ) (a : ℝ) (n : ℕ) :
    (z.re - a) * (complexPowerRealBaselineGeomSum z a n).im +
        z.im * (complexPowerRealBaselineGeomSum z a n).re = (z ^ n).im :=
  (complex_pow_sub_real_pow_im_eq_geomSum z a n).symm

/-- Real coordinate after multiplying a complex discrepancy by the negative
spatial Fourier phase. -/
theorem negativePhase_mul_re (theta : ℝ) (d : ℂ) :
    (Complex.exp (((-theta : ℝ) : ℂ) * Complex.I) * d).re =
      Real.cos theta * d.re + Real.sin theta * d.im := by
  rw [Complex.mul_re, Complex.exp_ofReal_mul_I_re,
    Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg]
  ring

/-- Imaginary coordinate after multiplying a complex discrepancy by the
negative spatial Fourier phase. -/
theorem negativePhase_mul_im (theta : ℝ) (d : ℂ) :
    (Complex.exp (((-theta : ℝ) : ℂ) * Complex.I) * d).im =
      Real.cos theta * d.im - Real.sin theta * d.re := by
  rw [Complex.mul_im, Complex.exp_ofReal_mul_I_re,
    Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg]
  ring

/-- Recombination of the Fejer-weighted sine/cosine coordinates into a
single real spatial phase. -/
theorem phase_fejer_coordinate (w g theta : ℝ) (d : ℂ) :
    w * (Real.sin theta * d.im + Real.cos theta * d.re + g) =
      w * ((Complex.exp (((-theta : ℝ) : ℂ) * Complex.I) * d).re + g) := by
  rw [negativePhase_mul_re]
  ring

/-- Recombination of the cotangent-weighted sine/cosine coordinates into a
single negative imaginary spatial phase. -/
theorem phase_cotangent_coordinate (c theta : ℝ) (d : ℂ) :
    c * (Real.sin theta * d.re - Real.cos theta * d.im) =
      -c * (Complex.exp (((-theta : ℝ) : ℂ) * Complex.I) * d).im := by
  rw [negativePhase_mul_im]
  ring

/-- Exact real-coordinate telescoping formula for a characteristic-function
power around its real quadratic Taylor baseline. -/
theorem charFun_probabilityLaw_pow_sub_quadratic_pow_re_eq_geomSum
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : AEMeasurable X μ) (t : ℝ) (n : ℕ) :
    ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t) ^ n).re - (1 - t ^ 2 / 2) ^ n =
      ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t).re - (1 - t ^ 2 / 2)) *
          (complexPowerRealBaselineGeomSum
            (MeasureTheory.charFun
              (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                X hX : Measure ℝ) t)
            (1 - t ^ 2 / 2) n).re -
        (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t).im *
          (complexPowerRealBaselineGeomSum
            (MeasureTheory.charFun
              (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                X hX : Measure ℝ) t)
            (1 - t ^ 2 / 2) n).im := by
  exact complex_pow_sub_real_pow_re_eq_geomSum _ _ _

/-- Exact imaginary-coordinate telescoping formula for a characteristic-function
power around its real quadratic Taylor baseline. -/
theorem charFun_probabilityLaw_pow_im_eq_geomSum
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : AEMeasurable X μ) (t : ℝ) (n : ℕ) :
    ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t) ^ n).im =
      ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t).re - (1 - t ^ 2 / 2)) *
          (complexPowerRealBaselineGeomSum
            (MeasureTheory.charFun
              (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                X hX : Measure ℝ) t)
            (1 - t ^ 2 / 2) n).im +
        (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX : Measure ℝ) t).im *
          (complexPowerRealBaselineGeomSum
            (MeasureTheory.charFun
              (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                X hX : Measure ℝ) t)
            (1 - t ^ 2 / 2) n).re := by
  exact complex_pow_sub_real_pow_im_eq_geomSum _ _ _

/-- Maximum-radius bound for the geometric coefficient in the exact complex
power factorization. -/
theorem norm_complexPowerRealBaselineGeomSum_le
    (z : ℂ) (a : ℝ) (n : ℕ) :
    ‖complexPowerRealBaselineGeomSum z a n‖ ≤
      n * max ‖z‖ |a| ^ (n - 1) := by
  by_cases hn : n = 0
  · subst n
    simp [complexPowerRealBaselineGeomSum]
  let r : ℝ := max ‖z‖ |a|
  have hrz : ‖z‖ ≤ r := le_max_left _ _
  have hra : |a| ≤ r := le_max_right _ _
  have hr : 0 ≤ r := (norm_nonneg z).trans hrz
  have hnpos : 0 < n := Nat.pos_of_ne_zero hn
  change ‖complexPowerRealBaselineGeomSum z a n‖ ≤
    n * r ^ (n - 1)
  rw [complexPowerRealBaselineGeomSum]
  calc
    ‖∑ i ∈ Finset.range n, z ^ i * (a : ℂ) ^ (n - 1 - i)‖ ≤
        ∑ i ∈ Finset.range n,
          ‖z ^ i * (a : ℂ) ^ (n - 1 - i)‖ := norm_sum_le _ _
    _ ≤ ∑ _i ∈ Finset.range n, r ^ (n - 1) := by
      apply Finset.sum_le_sum
      intro i hi
      have hi_lt : i < n := Finset.mem_range.mp hi
      rw [norm_mul, norm_pow, norm_pow, Complex.norm_real, Real.norm_eq_abs]
      calc
        ‖z‖ ^ i * |a| ^ (n - 1 - i) ≤
            r ^ i * r ^ (n - 1 - i) := by
          gcongr
        _ = r ^ (n - 1) := by
          rw [← pow_add]
          congr 1
          omega
    _ = n * r ^ (n - 1) := by simp

/-- Real-coordinate consequence of the maximum-radius geometric-coefficient
bound. -/
theorem abs_complexPowerRealBaselineGeomSum_re_le
    (z : ℂ) (a : ℝ) (n : ℕ) :
    |(complexPowerRealBaselineGeomSum z a n).re| ≤
      n * max ‖z‖ |a| ^ (n - 1) :=
  (Complex.abs_re_le_norm _).trans
    (norm_complexPowerRealBaselineGeomSum_le z a n)

/-- Imaginary-coordinate consequence of the maximum-radius
geometric-coefficient bound. -/
theorem abs_complexPowerRealBaselineGeomSum_im_le
    (z : ℂ) (a : ℝ) (n : ℕ) :
    |(complexPowerRealBaselineGeomSum z a n).im| ≤
      n * max ‖z‖ |a| ^ (n - 1) :=
  (Complex.abs_im_le_norm _).trans
    (norm_complexPowerRealBaselineGeomSum_le z a n)

/-- Coarser predecessor retained as a compatibility wrapper while the sharp
coefficient is propagated through the existing Berry--Esseen chain. -/
theorem norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t -
        (1 - (t : ℂ) ^ 2 / 2)‖ ≤
      4 * |t| ^ 3 * ∫ ω, |X ω| ^ 3 ∂μ := by
  have hMomentNonneg : 0 ≤ ∫ ω, |X ω| ^ 3 ∂μ :=
    integral_nonneg fun ω => pow_nonneg (abs_nonneg (X ω)) 3
  have htNonneg : 0 ≤ |t| ^ 3 := pow_nonneg (abs_nonneg t) 3
  calc
    _ ≤ (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ :=
      norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
        X hX hMean hSecond t
    _ ≤ 4 * |t| ^ 3 * ∫ ω, |X ω| ^ 3 ∂μ :=
      mul_le_mul_of_nonneg_right (by nlinarith) hMomentNonneg

/-- Powers of two points in the closed complex unit ball separate at most
linearly in the exponent. This elementary telescoping estimate is the bridge
from a one-summand characteristic-function estimate to an i.i.d. sum. -/
theorem norm_pow_sub_pow_le_of_norm_le_one
    (a b : ℂ) (ha : ‖a‖ ≤ 1) (hb : ‖b‖ ≤ 1) (n : ℕ) :
    ‖a ^ n - b ^ n‖ ≤ n * ‖a - b‖ := by
  induction n with
  | zero => simp
  | succ n ih =>
      calc
        ‖a ^ (n + 1) - b ^ (n + 1)‖ =
            ‖a ^ n * (a - b) + (a ^ n - b ^ n) * b‖ := by
          congr 1
          ring
        _ ≤ ‖a ^ n * (a - b)‖ + ‖(a ^ n - b ^ n) * b‖ :=
          norm_add_le _ _
        _ = ‖a ^ n‖ * ‖a - b‖ + ‖a ^ n - b ^ n‖ * ‖b‖ := by
          rw [norm_mul, norm_mul]
        _ ≤ 1 * ‖a - b‖ + (n * ‖a - b‖) * 1 := by
          apply add_le_add
          · exact mul_le_mul_of_nonneg_right
              (by simpa only [norm_pow] using
                pow_le_one₀ (norm_nonneg a) ha)
              (norm_nonneg _)
          · exact mul_le_mul ih hb (norm_nonneg _) (by positivity)
        _ = ((n + 1 : ℕ) : ℝ) * ‖a - b‖ := by
          rw [Nat.cast_add, Nat.cast_one]
          ring

/-- The real quadratic Taylor proxy for a characteristic function remains in
the complex unit ball on the interval `|t| ≤ 2`. -/
theorem norm_one_sub_half_sq_le_one {t : ℝ} (ht : |t| ≤ 2) :
    ‖1 - (t : ℂ) ^ 2 / 2‖ ≤ 1 := by
  have htSq : t ^ 2 ≤ 4 := by
    rw [← sq_abs]
    nlinarith [abs_nonneg t]
  rw [show 1 - (t : ℂ) ^ 2 / 2 = ((1 - t ^ 2 / 2 : ℝ) : ℂ) by
    push_cast
    ring]
  rw [Complex.norm_real, Real.norm_eq_abs, abs_le]
  constructor <;> nlinarith [sq_nonneg t]

/-- Cubic control of a centered unit-variance characteristic function passes
to arbitrary powers on the Taylor interval. -/
theorem norm_charFun_pow_sub_quadratic_pow_le_thirdMoment
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    (n : ℕ) {t : ℝ} (ht : |t| ≤ 2) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t ^ n -
        (1 - (t : ℂ) ^ 2 / 2) ^ n‖ ≤
      n * (4 * |t| ^ 3 * ∫ omega, |X omega| ^ 3 ∂mu) := by
  calc
    _ ≤ n * ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t -
        (1 - (t : ℂ) ^ 2 / 2)‖ :=
      norm_pow_sub_pow_le_of_norm_le_one _ _
        (MeasureTheory.norm_charFun_le_one t)
        (norm_one_sub_half_sq_le_one ht) n
    _ ≤ n * (4 * |t| ^ 3 * ∫ omega, |X omega| ^ 3 ∂mu) := by
      exact mul_le_mul_of_nonneg_left
        (norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment
          X hX hMean hSecond t)
        (Nat.cast_nonneg n)

/-- Quantitative Taylor control for the characteristic function of the
normalized centered i.i.d. sum. The right side is the one-variable cubic
remainder multiplied by the number of summands. -/
theorem norm_charFun_normalizedCenteredIidSum_sub_quadratic_pow_le_thirdMoment
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 2) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
              Measure ℝ) t -
        (1 - ((t / Real.sqrt (N + 1 : ℝ) : ℝ) : ℂ) ^ 2 / 2) ^
          (N + 1)‖ ≤
      ((N + 1 : ℕ) : ℝ) *
        (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
          ∫ omega, |X 0 omega| ^ 3 ∂mu) := by
  let hX2 : ∀ i, MemLp (X i) 2 mu := fun i =>
    (hX i).mono_exponent (by norm_num)
  rw [NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw_normalizedCenteredIidSum
    X hX2 hIndep hIdent N t]
  have hcf :
      (∫ omega, Complex.exp
        ((((t / Real.sqrt (N + 1 : ℝ)) * X 0 omega : ℝ) : ℂ) * Complex.I) ∂mu) =
        MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) := by
    rw [NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw]
    apply integral_congr_ae
    filter_upwards with omega
    congr 1
    push_cast
    ring
  rw [hcf]
  exact norm_charFun_pow_sub_quadratic_pow_le_thirdMoment
    (X 0) (hX 0) hMean hSecond (N + 1) ht

/-- Sharp second-order remainder for the real exponential on the negative
half-line. -/
theorem abs_rexp_neg_sub_one_add_le_half_sq {x : ℝ} (hx : 0 ≤ x) :
    |Real.exp (-x) - 1 + x| ≤ x ^ 2 / 2 := by
  let R : ℝ → ℝ := fun z => Real.exp (-z) - 1 + z
  let d : ℝ → ℝ := fun z => 1 - Real.exp (-z)
  have hR (z : ℝ) : HasDerivAt R (d z) z := by
    have he := (Real.hasDerivAt_exp (-z)).comp z (hasDerivAt_neg z)
    convert (he.sub_const 1).add (hasDerivAt_id z) using 1 <;>
      dsimp only [R, d] <;> ring
  have hdcont : Continuous d := by
    dsimp only [d]
    fun_prop
  have hFTC : (∫ z in (0 : ℝ)..x, d z) = R x := by
    have h := intervalIntegral.integral_eq_sub_of_hasDerivAt
      (a := (0 : ℝ)) (b := x) (f := R) (f' := d)
      (fun z _ => hR z) (hdcont.intervalIntegrable (a := 0) (b := x))
    simpa [R] using h
  have hmono : (∫ z in (0 : ℝ)..x, d z) ≤ ∫ z in (0 : ℝ)..x, z := by
    apply intervalIntegral.integral_mono_on hx
      (hdcont.intervalIntegrable (a := 0) (b := x))
      (continuous_id.intervalIntegrable (a := 0) (b := x))
    intro z hz
    have hexp := Real.one_sub_le_exp_neg z
    dsimp only [d]
    change 1 - Real.exp (-z) ≤ z
    linarith
  have hRNonneg : 0 ≤ R x := by
    have hexp := Real.one_sub_le_exp_neg x
    dsimp only [R]
    linarith
  rw [abs_of_nonneg]
  · change R x ≤ x ^ 2 / 2
    rw [← hFTC]
    calc
      (∫ z in (0 : ℝ)..x, d z) ≤ ∫ z in (0 : ℝ)..x, z := hmono
      _ = x ^ 2 / 2 := by norm_num
  · simpa only [R] using hRNonneg

/-- On the unit Taylor window, the quadratic characteristic-function proxy
differs from the matching Gaussian exponential by the sharp quartic
remainder. -/
theorem norm_one_sub_half_sq_sub_cexp_neg_half_sq_le_sharp
    (u : ℝ) :
    ‖(1 - (u : ℂ) ^ 2 / 2) - Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤
      |u| ^ 4 / 8 := by
  let x : ℝ := u ^ 2 / 2
  have hx : 0 ≤ x := by dsimp only [x]; positivity
  have hreal :
      (1 - (u : ℂ) ^ 2 / 2) - Complex.exp (-(u : ℂ) ^ 2 / 2) =
        ((1 - x - Real.exp (-x) : ℝ) : ℂ) := by
    rw [show Complex.exp (-(u : ℂ) ^ 2 / 2) =
        ((Real.exp (-x) : ℝ) : ℂ) by
      rw [Complex.ofReal_exp]
      congr 1
      dsimp only [x]
      push_cast
      ring]
    dsimp only [x]
    push_cast
    ring
  rw [hreal, Complex.norm_real, Real.norm_eq_abs]
  have h := abs_rexp_neg_sub_one_add_le_half_sq hx
  rw [show |1 - x - Real.exp (-x)| = |Real.exp (-x) - 1 + x| by
    rw [← abs_neg]
    congr 1
    ring]
  calc
    |Real.exp (-x) - 1 + x| ≤ x ^ 2 / 2 := h
    _ = |u| ^ 4 / 8 := by
      dsimp only [x]
      rw [← abs_pow, abs_of_nonneg (by positivity : 0 ≤ u ^ 4)]
      ring

/-- Coarser predecessor retained for compatibility with the original
characteristic-function chain. -/
theorem norm_one_sub_half_sq_sub_cexp_neg_half_sq_le
    {u : ℝ} (hu : |u| ≤ 1) :
    ‖(1 - (u : ℂ) ^ 2 / 2) - Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤
      |u| ^ 4 / 4 := by
  let z : ℂ := -(u : ℂ) ^ 2 / 2
  have hz : ‖z‖ ≤ 1 := by
    dsimp [z]
    rw [norm_div, norm_neg, norm_pow, Complex.norm_real, Real.norm_eq_abs]
    norm_num
    nlinarith [sq_abs u,
      mul_nonneg (abs_nonneg u) (sub_nonneg.mpr hu)]
  calc
    ‖(1 - (u : ℂ) ^ 2 / 2) - Complex.exp (-(u : ℂ) ^ 2 / 2)‖ =
        ‖-(Complex.exp z - 1 - z)‖ := by
      congr 1
      dsimp [z]
      ring
    _ = ‖Complex.exp z - 1 - z‖ := norm_neg _
    _ ≤ ‖z‖ ^ 2 := Complex.norm_exp_sub_one_sub_id_le hz
    _ = |u| ^ 4 / 4 := by
      dsimp [z]
      rw [norm_div, norm_neg, norm_pow, Complex.norm_real, Real.norm_eq_abs]
      norm_num
      rw [← abs_pow, abs_of_nonneg (by positivity : 0 ≤ u ^ 4)]
      ring

/-- Raising the quadratic proxy and its matching Gaussian exponential to the
same power costs at most the exponent times the quartic one-step remainder. -/
theorem norm_one_sub_half_sq_pow_sub_cexp_neg_half_sq_pow_le
    {u : ℝ} (hu : |u| ≤ 1) (n : ℕ) :
    ‖(1 - (u : ℂ) ^ 2 / 2) ^ n -
        Complex.exp (-(u : ℂ) ^ 2 / 2) ^ n‖ ≤
      n * (|u| ^ 4 / 4) := by
  have hquad : ‖1 - (u : ℂ) ^ 2 / 2‖ ≤ 1 :=
    norm_one_sub_half_sq_le_one (hu.trans (by norm_num))
  have hexp : ‖Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤ 1 := by
    rw [Complex.norm_exp]
    apply Real.exp_le_one_iff.mpr
    norm_num [pow_two, Complex.mul_re]
    nlinarith [mul_self_nonneg u]
  calc
    _ ≤ n * ‖(1 - (u : ℂ) ^ 2 / 2) -
        Complex.exp (-(u : ℂ) ^ 2 / 2)‖ :=
      norm_pow_sub_pow_le_of_norm_le_one _ _ hquad hexp n
    _ ≤ n * (|u| ^ 4 / 4) := by
      exact mul_le_mul_of_nonneg_left
        (norm_one_sub_half_sq_sub_cexp_neg_half_sq_le hu)
        (Nat.cast_nonneg n)

/-- The Gaussian exponential at the normalized frequency, raised to the
number of summands, is exactly the standard-normal characteristic function. -/
theorem cexp_neg_half_sq_div_sqrt_pow_succ
    (N : ℕ) (t : ℝ) :
    Complex.exp
        (-((t / Real.sqrt (N + 1 : ℝ) : ℝ) : ℂ) ^ 2 / 2) ^ (N + 1) =
      Complex.exp (-(t : ℂ) ^ 2 / 2) := by
  have hsqrt : Real.sqrt (N + 1 : ℝ) ^ 2 = (N + 1 : ℝ) :=
    Real.sq_sqrt (by positivity)
  have hsqrtC : (Real.sqrt (N + 1 : ℝ) : ℂ) ^ 2 = (N + 1 : ℂ) := by
    exact_mod_cast hsqrt
  have hnC : (N + 1 : ℂ) ≠ 0 := by
    exact_mod_cast (Nat.succ_ne_zero N)
  rw [← Complex.exp_nat_mul]
  congr 1
  push_cast
  rw [div_pow, hsqrtC]
  field_simp [hnC]

/-- Local characteristic-function comparison between a normalized centered
i.i.d. sum and the standard normal. It cleanly separates the cubic
third-moment error from the quartic Gaussian-proxy error. -/
theorem norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
              Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ ≤
      ((N + 1 : ℕ) : ℝ) *
          (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
            ∫ omega, |X 0 omega| ^ 3 ∂mu) +
        ((N + 1 : ℕ) : ℝ) *
          (|t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4) := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun,
    ← cexp_neg_half_sq_div_sqrt_pow_succ N t]
  let A := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
        Measure ℝ) t
  let B := (1 - ((t / Real.sqrt (N + 1 : ℝ) : ℝ) : ℂ) ^ 2 / 2) ^
    (N + 1)
  let C := Complex.exp
    (-((t / Real.sqrt (N + 1 : ℝ) : ℝ) : ℂ) ^ 2 / 2) ^ (N + 1)
  change ‖A - C‖ ≤ _
  calc
    ‖A - C‖ = ‖(A - B) + (B - C)‖ := by
      congr 1
      ring
    _ ≤ ‖A - B‖ + ‖B - C‖ := norm_add_le _ _
    _ ≤ _ := add_le_add
      (norm_charFun_normalizedCenteredIidSum_sub_quadratic_pow_le_thirdMoment
        X hX hIndep hIdent hMean hSecond N (ht.trans (by norm_num)))
      (norm_one_sub_half_sq_pow_sub_cexp_neg_half_sq_pow_le ht (N + 1))

/-- The local normalized-sum estimate in the discrepancy-quotient form used
by the Fourier smoothing reduction. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      (((N + 1 : ℕ) : ℝ) *
            (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
              ∫ omega, |X 0 omega| ^ 3 ∂mu) +
          ((N + 1 : ℕ) : ℝ) *
            (|t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t| := by
  by_cases ht0 : t = 0
  · subst t
    simp [charFunDiscrepancyQuotient]
  · rw [charFunDiscrepancyQuotient, if_neg ht0]
    exact div_le_div_of_nonneg_right
      (norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le
        X hX hIndep hIdent hMean hSecond N ht)
      (abs_nonneg t)

/-- Algebraic normalization of the cubic and quartic quotient errors. -/
theorem normalized_cubic_quartic_quotient_eq
    (N : ℕ) (t rho : ℝ) :
    ((((N + 1 : ℕ) : ℝ) *
            (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 * rho) +
          ((N + 1 : ℕ) : ℝ) *
            (|t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t|) =
      4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ)) := by
  have hspos : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hs2 : Real.sqrt (N + 1 : ℝ) ^ 2 = (N + 1 : ℝ) :=
    Real.sq_sqrt (by positivity)
  have hn0 : (N + 1 : ℝ) ≠ 0 := by positivity
  generalize hsdef : Real.sqrt (N + 1 : ℝ) = s at *
  rw [abs_div, abs_of_pos hspos]
  norm_num [Nat.cast_add, Nat.cast_one]
  rw [← hs2]
  field_simp [hspos.ne', hn0]
  rw [sq_abs]
  rw [show |t| ^ 3 = t ^ 2 * |t| by
    calc
      |t| ^ 3 = |t| ^ 2 * |t| := by ring
      _ = t ^ 2 * |t| := by rw [sq_abs]]
  ring

/-- Simplified local polynomial bound for the normalized-sum discrepancy
quotient. This is the form intended for direct integration on a finite
frequency window. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_polynomial
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
          Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ)) := by
  calc
    _ ≤ ((((N + 1 : ℕ) : ℝ) *
            (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
              ∫ omega, |X 0 omega| ^ 3 ∂mu) +
          ((N + 1 : ℕ) : ℝ) *
            (|t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t|) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le
        X hX hIndep hIdent hMean hSecond N ht
    _ = _ := normalized_cubic_quartic_quotient_eq N t
      (∫ omega, |X 0 omega| ^ 3 ∂mu)

/-- A damped telescoping estimate for complex powers. Unlike the unit-ball
bound above, this retains the geometric factor needed to integrate the
Berry--Esseen characteristic-function error on a growing window. -/
theorem norm_pow_sub_pow_le_max
    (a b : ℂ) (n : ℕ) :
    ‖a ^ n - b ^ n‖ ≤
      n * max ‖a‖ ‖b‖ ^ (n - 1) * ‖a - b‖ := by
  let r : ℝ := max ‖a‖ ‖b‖
  have hra : ‖a‖ ≤ r := le_max_left _ _
  have hrb : ‖b‖ ≤ r := le_max_right _ _
  have hr : 0 ≤ r := (norm_nonneg a).trans hra
  change ‖a ^ n - b ^ n‖ ≤ n * r ^ (n - 1) * ‖a - b‖
  induction n with
  | zero => simp
  | succ n ih =>
      by_cases hn : n = 0
      · subst n
        simp
      · have hpowA : ‖a ^ n‖ ≤ r ^ n := by
          rw [norm_pow]
          exact pow_le_pow_left₀ (norm_nonneg a) hra n
        calc
          ‖a ^ (n + 1) - b ^ (n + 1)‖ =
              ‖a ^ n * (a - b) + (a ^ n - b ^ n) * b‖ := by
            congr 1
            ring
          _ ≤ ‖a ^ n * (a - b)‖ + ‖(a ^ n - b ^ n) * b‖ :=
            norm_add_le _ _
          _ = ‖a ^ n‖ * ‖a - b‖ + ‖a ^ n - b ^ n‖ * ‖b‖ := by
            rw [norm_mul, norm_mul]
          _ ≤ r ^ n * ‖a - b‖ +
              (n * r ^ (n - 1) * ‖a - b‖) * r := by
            exact add_le_add
              (mul_le_mul_of_nonneg_right hpowA (norm_nonneg _))
              (mul_le_mul ih hrb (norm_nonneg _) (by positivity))
          _ = ((n + 1 : ℕ) : ℝ) * r ^ n * ‖a - b‖ := by
            have hn1 : 1 ≤ n := Nat.one_le_iff_ne_zero.mpr hn
            have hp : r ^ (n - 1) * r = r ^ n := by
              rw [← pow_succ, Nat.sub_add_cancel hn1]
            have hterm : (n : ℝ) * r ^ (n - 1) * ‖a - b‖ * r =
                (n : ℝ) * r ^ n * ‖a - b‖ := by
              calc
                _ = (n : ℝ) * (r ^ (n - 1) * r) * ‖a - b‖ := by ring
                _ = _ := by rw [hp]
            rw [hterm, Nat.cast_add, Nat.cast_one]
            ring

/-- A centered unit-variance characteristic function contracts quadratically
on the window where its cubic remainder is dominated by the variance term. -/
theorem norm_charFun_probabilityLaw_le_one_sub_quarter_sq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 16 * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
      1 - u ^ 2 / 4 := by
  let rho : ℝ := ∫ omega, |X omega| ^ 3 ∂mu
  have hproxyNonneg : 0 ≤ 1 - u ^ 2 / 2 := by
    nlinarith [sq_abs u,
      mul_nonneg (abs_nonneg u) (sub_nonneg.mpr hu)]
  have hproxy : ‖1 - (u : ℂ) ^ 2 / 2‖ = 1 - u ^ 2 / 2 := by
    rw [show 1 - (u : ℂ) ^ 2 / 2 = ((1 - u ^ 2 / 2 : ℝ) : ℂ) by
      push_cast
      ring]
    rw [Complex.norm_real, Real.norm_eq_abs, abs_of_nonneg hproxyNonneg]
  have hscaleNonneg : 0 ≤ |u| ^ 2 / 4 := by positivity
  have hscaled := mul_le_mul_of_nonneg_right hsmall hscaleNonneg
  have hrem : 4 * |u| ^ 3 * rho ≤ u ^ 2 / 4 := by
    dsimp only [rho] at hscaled ⊢
    rw [sq_abs] at hscaled
    calc
      4 * |u| ^ 3 * (∫ omega, |X omega| ^ 3 ∂mu) =
          (16 * (∫ omega, |X omega| ^ 3 ∂mu) * |u|) *
            (u ^ 2 / 4) := by
        rw [show |u| ^ 3 = |u| * u ^ 2 by
          calc
            |u| ^ 3 = |u| * |u| ^ 2 := by ring
            _ = |u| * u ^ 2 := by rw [sq_abs]]
        ring
      _ ≤ 1 * (u ^ 2 / 4) := hscaled
      _ = u ^ 2 / 4 := one_mul _
  calc
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
        ‖MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u -
            (1 - (u : ℂ) ^ 2 / 2)‖ + ‖1 - (u : ℂ) ^ 2 / 2‖ := by
      calc
        _ = ‖(MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u -
              (1 - (u : ℂ) ^ 2 / 2)) + (1 - (u : ℂ) ^ 2 / 2)‖ := by
            congr 1
            ring
        _ ≤ _ := norm_add_le _ _
    _ ≤ 4 * |u| ^ 3 * rho + (1 - u ^ 2 / 2) := by
      exact add_le_add
        (norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment
          X hX hMean hSecond u)
        hproxy.le
    _ ≤ 1 - u ^ 2 / 4 := by
      linarith

/-- Exponential version of the local quadratic contraction bound. -/
theorem norm_charFun_probabilityLaw_le_exp_neg_quarter_sq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 16 * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
      Real.exp (-u ^ 2 / 4) := by
  exact (norm_charFun_probabilityLaw_le_one_sub_quarter_sq
    X hX hMean hSecond hu hsmall).trans (by
      simpa only [neg_div] using Real.one_sub_le_exp_neg (u ^ 2 / 4))

/-- Sharp local quadratic contraction obtained from the exact cubic Taylor
coefficient.  On the enlarged window `2 ρ |u| ≤ 1`, the cubic error consumes
at most one sixth of the quadratic variance term. -/
theorem norm_charFun_probabilityLaw_le_one_sub_five_twelfths_sq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
      1 - 5 * u ^ 2 / 12 := by
  let rho : ℝ := ∫ omega, |X omega| ^ 3 ∂mu
  have hproxyNonneg : 0 ≤ 1 - u ^ 2 / 2 := by
    nlinarith [sq_abs u,
      mul_nonneg (abs_nonneg u) (sub_nonneg.mpr hu)]
  have hproxy : ‖1 - (u : ℂ) ^ 2 / 2‖ = 1 - u ^ 2 / 2 := by
    rw [show 1 - (u : ℂ) ^ 2 / 2 = ((1 - u ^ 2 / 2 : ℝ) : ℂ) by
      push_cast
      ring]
    rw [Complex.norm_real, Real.norm_eq_abs, abs_of_nonneg hproxyNonneg]
  have hscaleNonneg : 0 ≤ u ^ 2 / 12 := by positivity
  have hscaled := mul_le_mul_of_nonneg_right hsmall hscaleNonneg
  have hrem : (|u| ^ 3 / 6) * rho ≤ u ^ 2 / 12 := by
    dsimp only [rho] at hscaled ⊢
    calc
      (|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) =
          (2 * (∫ omega, |X omega| ^ 3 ∂mu) * |u|) *
            (u ^ 2 / 12) := by
        rw [show |u| ^ 3 = |u| * u ^ 2 by
          calc
            |u| ^ 3 = |u| * |u| ^ 2 := by ring
            _ = |u| * u ^ 2 := by rw [sq_abs]]
        ring
      _ ≤ 1 * (u ^ 2 / 12) := hscaled
      _ = u ^ 2 / 12 := one_mul _
  calc
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
        ‖MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u -
            (1 - (u : ℂ) ^ 2 / 2)‖ + ‖1 - (u : ℂ) ^ 2 / 2‖ := by
      calc
        _ = ‖(MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u -
              (1 - (u : ℂ) ^ 2 / 2)) + (1 - (u : ℂ) ^ 2 / 2)‖ := by
            congr 1
            ring
        _ ≤ _ := norm_add_le _ _
    _ ≤ (|u| ^ 3 / 6) * rho + (1 - u ^ 2 / 2) := by
      exact add_le_add
        (norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
          X hX hMean hSecond u)
        hproxy.le
    _ ≤ 1 - 5 * u ^ 2 / 12 := by
      nlinarith [hrem]

/-- Exponential form of the sharp local contraction. -/
theorem norm_charFun_probabilityLaw_le_exp_neg_five_twelfths_sq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
      Real.exp (-(5 * u ^ 2 / 12)) := by
  exact (norm_charFun_probabilityLaw_le_one_sub_five_twelfths_sq
    X hX hMean hSecond hu hsmall).trans (by
      simpa only using Real.one_sub_le_exp_neg (5 * u ^ 2 / 12))

/-- Exponentially damped bound for the real-baseline geometric coefficient of
a centered unit-variance characteristic function. -/
theorem norm_complexPowerRealBaselineGeomSum_charFun_le_exp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ ω, |X ω| ^ 3 ∂μ) * |u| ≤ 1)
    (n : ℕ) :
    ‖complexPowerRealBaselineGeomSum
        (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u)
        (1 - u ^ 2 / 2) n‖ ≤
      n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1) := by
  have ha0 : 0 ≤ 1 - u ^ 2 / 2 := by
    nlinarith [sq_abs u,
      mul_nonneg (abs_nonneg u) (sub_nonneg.mpr hu)]
  have hbase : |1 - u ^ 2 / 2| ≤ Real.exp (-(5 * u ^ 2 / 12)) := by
    rw [abs_of_nonneg ha0]
    calc
      1 - u ^ 2 / 2 ≤ Real.exp (-(u ^ 2 / 2)) :=
        Real.one_sub_le_exp_neg (u ^ 2 / 2)
      _ ≤ Real.exp (-(5 * u ^ 2 / 12)) := by
        apply Real.exp_le_exp.mpr
        nlinarith [sq_nonneg u]
  have hcf := norm_charFun_probabilityLaw_le_exp_neg_five_twelfths_sq
    X hX hMean hSecond hu hsmall
  calc
    ‖complexPowerRealBaselineGeomSum
        (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u)
        (1 - u ^ 2 / 2) n‖ ≤
        n * max
          ‖MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u‖
          |1 - u ^ 2 / 2| ^ (n - 1) :=
      norm_complexPowerRealBaselineGeomSum_le _ _ _
    _ ≤ n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1) := by
      gcongr
      exact max_le hcf hbase

/-- Real-coordinate form of the exponentially damped geometric-coefficient
bound. -/
theorem abs_complexPowerRealBaselineGeomSum_charFun_re_le_exp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ ω, |X ω| ^ 3 ∂μ) * |u| ≤ 1)
    (n : ℕ) :
    |(complexPowerRealBaselineGeomSum
        (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u)
        (1 - u ^ 2 / 2) n).re| ≤
      n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1) :=
  (Complex.abs_re_le_norm _).trans
    (norm_complexPowerRealBaselineGeomSum_charFun_le_exp
      X hX hMean hSecond hu hsmall n)

/-- Imaginary-coordinate form of the exponentially damped
geometric-coefficient bound. -/
theorem abs_complexPowerRealBaselineGeomSum_charFun_im_le_exp
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ ω, |X ω| ^ 3 ∂μ) * |u| ≤ 1)
    (n : ℕ) :
    |(complexPowerRealBaselineGeomSum
        (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u)
        (1 - u ^ 2 / 2) n).im| ≤
      n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1) :=
  (Complex.abs_im_le_norm _).trans
    (norm_complexPowerRealBaselineGeomSum_charFun_le_exp
      X hX hMean hSecond hu hsmall n)

/-- Powered real-coordinate Taylor estimate retaining the separate real and
imaginary geometric-coefficient magnitudes. -/
theorem abs_charFun_probabilityLaw_pow_sub_quadratic_pow_re_le_geomSum
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) (n : ℕ) :
    |((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t) ^ n).re -
        (1 - t ^ 2 / 2) ^ n| ≤
      ((|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ) *
        (|(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) t)
          (1 - t ^ 2 / 2) n).re| +
        |(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) t)
          (1 - t ^ 2 / 2) n).im|) := by
  let z := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) t
  let S := complexPowerRealBaselineGeomSum z (1 - t ^ 2 / 2) n
  let eps := (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ
  have hre := abs_charFun_probabilityLaw_re_sub_quadratic_le_thirdMoment_sharp
    X hX hMean hSecond t
  have him := abs_charFun_probabilityLaw_im_le_thirdMoment_sharp
    X hX hMean hSecond t
  rw [charFun_probabilityLaw_pow_sub_quadratic_pow_re_eq_geomSum]
  change |(z.re - (1 - t ^ 2 / 2)) * S.re - z.im * S.im| ≤
    eps * (|S.re| + |S.im|)
  calc
    |(z.re - (1 - t ^ 2 / 2)) * S.re - z.im * S.im| ≤
        |(z.re - (1 - t ^ 2 / 2)) * S.re| + |z.im * S.im| :=
      abs_sub _ _
    _ = |z.re - (1 - t ^ 2 / 2)| * |S.re| + |z.im| * |S.im| := by
      rw [abs_mul, abs_mul]
    _ ≤ eps * |S.re| + eps * |S.im| := by
      exact add_le_add
        (mul_le_mul_of_nonneg_right hre (abs_nonneg _))
        (mul_le_mul_of_nonneg_right him (abs_nonneg _))
    _ = eps * (|S.re| + |S.im|) := by ring

/-- Powered imaginary-coordinate Taylor estimate retaining the separate real
and imaginary geometric-coefficient magnitudes. -/
theorem abs_charFun_probabilityLaw_pow_im_le_geomSum
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    (t : ℝ) (n : ℕ) :
    |((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) t) ^ n).im| ≤
      ((|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ) *
        (|(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) t)
          (1 - t ^ 2 / 2) n).re| +
        |(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) t)
          (1 - t ^ 2 / 2) n).im|) := by
  let z := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) t
  let S := complexPowerRealBaselineGeomSum z (1 - t ^ 2 / 2) n
  let eps := (|t| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ
  have hre := abs_charFun_probabilityLaw_re_sub_quadratic_le_thirdMoment_sharp
    X hX hMean hSecond t
  have him := abs_charFun_probabilityLaw_im_le_thirdMoment_sharp
    X hX hMean hSecond t
  rw [charFun_probabilityLaw_pow_im_eq_geomSum]
  change |(z.re - (1 - t ^ 2 / 2)) * S.im + z.im * S.re| ≤
    eps * (|S.re| + |S.im|)
  calc
    |(z.re - (1 - t ^ 2 / 2)) * S.im + z.im * S.re| ≤
        |(z.re - (1 - t ^ 2 / 2)) * S.im| + |z.im * S.re| :=
      abs_add_le _ _
    _ = |z.re - (1 - t ^ 2 / 2)| * |S.im| + |z.im| * |S.re| := by
      rw [abs_mul, abs_mul]
    _ ≤ eps * |S.im| + eps * |S.re| := by
      exact add_le_add
        (mul_le_mul_of_nonneg_right hre (abs_nonneg _))
        (mul_le_mul_of_nonneg_right him (abs_nonneg _))
    _ = eps * (|S.re| + |S.im|) := by ring

/-- Fully damped real-coordinate power bound obtained by bounding the two
geometric-coefficient coordinates independently. The explicit factor `2`
records that coordinate-separation loss. -/
theorem abs_charFun_probabilityLaw_pow_sub_quadratic_pow_re_le_exp_two
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ ω, |X ω| ^ 3 ∂μ) * |u| ≤ 1)
    (n : ℕ) :
    |((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u) ^ n).re -
        (1 - u ^ 2 / 2) ^ n| ≤
      2 * ((|u| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ) *
        (n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1)) := by
  let eps := (|u| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ
  let C := n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1)
  have heps : 0 ≤ eps := by
    dsimp only [eps]
    positivity
  have hbase :=
    abs_charFun_probabilityLaw_pow_sub_quadratic_pow_re_le_geomSum
      X hX hMean hSecond u n
  have hre := abs_complexPowerRealBaselineGeomSum_charFun_re_le_exp
    X hX hMean hSecond hu hsmall n
  have him := abs_complexPowerRealBaselineGeomSum_charFun_im_le_exp
    X hX hMean hSecond hu hsmall n
  calc
    _ ≤ eps *
        (|(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u)
          (1 - u ^ 2 / 2) n).re| +
        |(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u)
          (1 - u ^ 2 / 2) n).im|) := hbase
    _ ≤ eps * (C + C) := by
      gcongr
    _ = 2 * eps * C := by ring

/-- Fully damped imaginary-coordinate power bound obtained by bounding the two
geometric-coefficient coordinates independently. The explicit factor `2`
records that coordinate-separation loss. -/
theorem abs_charFun_probabilityLaw_pow_im_le_exp_two
    {Ω : Type*} [MeasurableSpace Ω]
    {μ : Measure Ω} [IsProbabilityMeasure μ]
    (X : Ω → ℝ) (hX : MemLp X 3 μ)
    (hMean : ∫ ω, X ω ∂μ = 0)
    (hSecond : ∫ ω, (X ω) ^ 2 ∂μ = 1)
    {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ ω, |X ω| ^ 3 ∂μ) * |u| ≤ 1)
    (n : ℕ) :
    |((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u) ^ n).im| ≤
      2 * ((|u| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ) *
        (n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1)) := by
  let eps := (|u| ^ 3 / 6) * ∫ ω, |X ω| ^ 3 ∂μ
  let C := n * (Real.exp (-(5 * u ^ 2 / 12))) ^ (n - 1)
  have heps : 0 ≤ eps := by
    dsimp only [eps]
    positivity
  have hbase := abs_charFun_probabilityLaw_pow_im_le_geomSum
    X hX hMean hSecond u n
  have hre := abs_complexPowerRealBaselineGeomSum_charFun_re_le_exp
    X hX hMean hSecond hu hsmall n
  have him := abs_complexPowerRealBaselineGeomSum_charFun_im_le_exp
    X hX hMean hSecond hu hsmall n
  calc
    _ ≤ eps *
        (|(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u)
          (1 - u ^ 2 / 2) n).re| +
        |(complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u)
          (1 - u ^ 2 / 2) n).im|) := hbase
    _ ≤ eps * (C + C) := by
      gcongr
    _ = 2 * eps * C := by ring

/-- The matching one-step Gaussian characteristic function contracts at
least as fast as `exp (-u²/4)`. -/
theorem norm_cexp_neg_half_sq_le_exp_neg_quarter_sq (u : ℝ) :
    ‖Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤ Real.exp (-u ^ 2 / 4) := by
  rw [Complex.norm_exp]
  apply Real.exp_le_exp.mpr
  norm_num [pow_two, Complex.mul_re]
  nlinarith [sq_nonneg u]

/-- The Gaussian one-step factor also lies below the sharper common damping
envelope used for the characteristic function. -/
theorem norm_cexp_neg_half_sq_le_exp_neg_five_twelfths_sq (u : ℝ) :
    ‖Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤
      Real.exp (-(5 * u ^ 2 / 12)) := by
  rw [Complex.norm_exp]
  apply Real.exp_le_exp.mpr
  norm_num [pow_two, Complex.mul_re]
  nlinarith [sq_nonneg u]

/-- Tunable quadratic damping coefficient obtained when the cubic Taylor
remainder is controlled on the window `c * rho * |u| ≤ 1`. -/
noncomputable def tunableCharFunDampingCoefficient (c : ℝ) : ℝ :=
  1 / 2 - 1 / (6 * c)

theorem tunableCharFunDampingCoefficient_nonneg
    {c : ℝ} (hc : 1 / 3 ≤ c) :
    0 ≤ tunableCharFunDampingCoefficient c := by
  unfold tunableCharFunDampingCoefficient
  have hc0 : 0 < c := lt_of_lt_of_le (by norm_num) hc
  rw [sub_nonneg, div_le_iff₀ (by positivity : 0 < 6 * c)]
  nlinarith

/-- Parameterized local contraction.  Increasing `c` shrinks the Taylor
window while improving the quadratic contraction continuously toward the
Gaussian coefficient `1/2`. -/
theorem norm_charFun_probabilityLaw_le_one_sub_tunable_sq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {c u : ℝ} (hc : 0 < c) (hu : |u| ≤ 1)
    (hsmall : c * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
      1 - tunableCharFunDampingCoefficient c * u ^ 2 := by
  let rho : ℝ := ∫ omega, |X omega| ^ 3 ∂mu
  have hproxyNonneg : 0 ≤ 1 - u ^ 2 / 2 := by
    nlinarith [sq_abs u,
      mul_nonneg (abs_nonneg u) (sub_nonneg.mpr hu)]
  have hproxy : ‖1 - (u : ℂ) ^ 2 / 2‖ = 1 - u ^ 2 / 2 := by
    rw [show 1 - (u : ℂ) ^ 2 / 2 = ((1 - u ^ 2 / 2 : ℝ) : ℂ) by
      push_cast
      ring]
    rw [Complex.norm_real, Real.norm_eq_abs, abs_of_nonneg hproxyNonneg]
  have hscaleNonneg : 0 ≤ u ^ 2 / (6 * c) := by positivity
  have hscaled := mul_le_mul_of_nonneg_right hsmall hscaleNonneg
  have hrem : (|u| ^ 3 / 6) * rho ≤ u ^ 2 / (6 * c) := by
    dsimp only [rho] at hscaled ⊢
    calc
      (|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) =
          (c * (∫ omega, |X omega| ^ 3 ∂mu) * |u|) *
            (u ^ 2 / (6 * c)) := by
        rw [show |u| ^ 3 = |u| * u ^ 2 by
          calc
            |u| ^ 3 = |u| * |u| ^ 2 := by ring
            _ = |u| * u ^ 2 := by rw [sq_abs]]
        field_simp [hc.ne']
      _ ≤ 1 * (u ^ 2 / (6 * c)) := hscaled
      _ = u ^ 2 / (6 * c) := one_mul _
  calc
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
        ‖MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u -
            (1 - (u : ℂ) ^ 2 / 2)‖ + ‖1 - (u : ℂ) ^ 2 / 2‖ := by
      calc
        _ = ‖(MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              X hX.aemeasurable : Measure ℝ) u -
              (1 - (u : ℂ) ^ 2 / 2)) + (1 - (u : ℂ) ^ 2 / 2)‖ := by
            congr 1
            ring
        _ ≤ _ := norm_add_le _ _
    _ ≤ (|u| ^ 3 / 6) * rho + (1 - u ^ 2 / 2) := by
      exact add_le_add
        (norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
          X hX hMean hSecond u)
        hproxy.le
    _ ≤ 1 - tunableCharFunDampingCoefficient c * u ^ 2 := by
      unfold tunableCharFunDampingCoefficient
      calc
        (|u| ^ 3 / 6) * rho + (1 - u ^ 2 / 2) ≤
            u ^ 2 / (6 * c) + (1 - u ^ 2 / 2) :=
          by simpa only [add_comm] using
            add_le_add_right hrem (1 - u ^ 2 / 2)
        _ = 1 - (1 / 2 - 1 / (6 * c)) * u ^ 2 := by ring

/-- Exponential form of the tunable characteristic-function contraction. -/
theorem norm_charFun_probabilityLaw_le_exp_neg_tunable_sq
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {c u : ℝ} (hc : 0 < c) (hu : |u| ≤ 1)
    (hsmall : c * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          X hX.aemeasurable : Measure ℝ) u‖ ≤
      Real.exp (-(tunableCharFunDampingCoefficient c * u ^ 2)) := by
  exact (norm_charFun_probabilityLaw_le_one_sub_tunable_sq
    X hX hMean hSecond hc hu hsmall).trans (by
      simpa only using
        Real.one_sub_le_exp_neg (tunableCharFunDampingCoefficient c * u ^ 2))

/-- The one-step Gaussian factor is dominated by every positive-window
tunable damping envelope. -/
theorem norm_cexp_neg_half_sq_le_exp_neg_tunable_sq
    {c : ℝ} (hc : 0 < c) (u : ℝ) :
    ‖Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤
      Real.exp (-(tunableCharFunDampingCoefficient c * u ^ 2)) := by
  rw [Complex.norm_exp]
  apply Real.exp_le_exp.mpr
  norm_num [pow_two, Complex.mul_re]
  unfold tunableCharFunDampingCoefficient
  have hinv : 0 ≤ 1 / (6 * c) := by positivity
  nlinarith [sq_nonneg u, mul_nonneg hinv (sq_nonneg u)]

/-- One-step characteristic-function comparison with the Gaussian
exponential, combining the cubic moment remainder and quartic proxy error. -/
theorem norm_charFun_probabilityLaw_sub_cexp_neg_half_sq_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {u : ℝ} (hu : |u| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u -
        Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤
      4 * |u| ^ 3 * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 4 := by
  let A := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) u
  let B := 1 - (u : ℂ) ^ 2 / 2
  let C := Complex.exp (-(u : ℂ) ^ 2 / 2)
  change ‖A - C‖ ≤ _
  calc
    ‖A - C‖ = ‖(A - B) + (B - C)‖ := by
      congr 1
      ring
    _ ≤ ‖A - B‖ + ‖B - C‖ := norm_add_le _ _
    _ ≤ _ := add_le_add
      (norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment
        X hX hMean hSecond u)
      (norm_one_sub_half_sq_sub_cexp_neg_half_sq_le hu)

/-- Sharp one-step comparison with the Gaussian exponential. -/
theorem norm_charFun_probabilityLaw_sub_cexp_neg_half_sq_le_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    {u : ℝ} (hu : |u| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u -
        Complex.exp (-(u : ℂ) ^ 2 / 2)‖ ≤
      (|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 8 := by
  let A := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) u
  let B := 1 - (u : ℂ) ^ 2 / 2
  let C := Complex.exp (-(u : ℂ) ^ 2 / 2)
  change ‖A - C‖ ≤ _
  calc
    ‖A - C‖ = ‖(A - B) + (B - C)‖ := by
      congr 1
      ring
    _ ≤ ‖A - B‖ + ‖B - C‖ := norm_add_le _ _
    _ ≤ _ := add_le_add
      (norm_charFun_probabilityLaw_sub_quadratic_le_thirdMoment_sharp
        X hX hMean hSecond u)
      (norm_one_sub_half_sq_sub_cexp_neg_half_sq_le_sharp u)

/-- Powered characteristic-function comparison retaining the tunable local
contraction parameter. -/
theorem norm_charFun_pow_sub_cexp_neg_half_sq_pow_le_damped_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    (n : ℕ) {c u : ℝ} (hc : 0 < c) (hu : |u| ≤ 1)
    (hsmall : c * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u ^ n -
        Complex.exp (-(u : ℂ) ^ 2 / 2) ^ n‖ ≤
      n * Real.exp (-(tunableCharFunDampingCoefficient c * u ^ 2)) ^ (n - 1) *
        ((|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 8) := by
  let A := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) u
  let B := Complex.exp (-(u : ℂ) ^ 2 / 2)
  let r := Real.exp (-(tunableCharFunDampingCoefficient c * u ^ 2))
  have hmax : max ‖A‖ ‖B‖ ≤ r := max_le
    (norm_charFun_probabilityLaw_le_exp_neg_tunable_sq
      X hX hMean hSecond hc hu hsmall)
    (norm_cexp_neg_half_sq_le_exp_neg_tunable_sq hc u)
  have hpow : max ‖A‖ ‖B‖ ^ (n - 1) ≤ r ^ (n - 1) :=
    pow_le_pow_left₀ (by positivity) hmax (n - 1)
  calc
    ‖A ^ n - B ^ n‖ ≤
        n * max ‖A‖ ‖B‖ ^ (n - 1) * ‖A - B‖ :=
      norm_pow_sub_pow_le_max A B n
    _ ≤ n * r ^ (n - 1) *
        ((|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 8) := by
      gcongr
      exact norm_charFun_probabilityLaw_sub_cexp_neg_half_sq_le_sharp
        X hX hMean hSecond hu

/-- Damped power comparison for a centered unit-variance characteristic
function. The geometric exponential factor is the key quantitative input
missing from the undamped polynomial estimate. -/
theorem norm_charFun_pow_sub_cexp_neg_half_sq_pow_le_damped
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    (n : ℕ) {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 16 * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u ^ n -
        Complex.exp (-(u : ℂ) ^ 2 / 2) ^ n‖ ≤
      n * Real.exp (-u ^ 2 / 4) ^ (n - 1) *
        (4 * |u| ^ 3 * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 4) := by
  let A := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) u
  let B := Complex.exp (-(u : ℂ) ^ 2 / 2)
  let r := Real.exp (-u ^ 2 / 4)
  have hmax : max ‖A‖ ‖B‖ ≤ r := max_le
    (norm_charFun_probabilityLaw_le_exp_neg_quarter_sq
      X hX hMean hSecond hu hsmall)
    (norm_cexp_neg_half_sq_le_exp_neg_quarter_sq u)
  have hpow : max ‖A‖ ‖B‖ ^ (n - 1) ≤ r ^ (n - 1) :=
    pow_le_pow_left₀ (by positivity) hmax (n - 1)
  calc
    ‖A ^ n - B ^ n‖ ≤
        n * max ‖A‖ ‖B‖ ^ (n - 1) * ‖A - B‖ :=
      norm_pow_sub_pow_le_max A B n
    _ ≤ n * r ^ (n - 1) *
        (4 * |u| ^ 3 * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 4) := by
      gcongr
      exact norm_charFun_probabilityLaw_sub_cexp_neg_half_sq_le
        X hX hMean hSecond hu

/-- Sharp damped power comparison, retaining both the exact cubic Taylor
coefficient and the stronger local contraction. -/
theorem norm_charFun_pow_sub_cexp_neg_half_sq_pow_le_damped_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : Omega → ℝ) (hX : MemLp X 3 mu)
    (hMean : ∫ omega, X omega ∂mu = 0)
    (hSecond : ∫ omega, (X omega) ^ 2 ∂mu = 1)
    (n : ℕ) {u : ℝ} (hu : |u| ≤ 1)
    (hsmall : 2 * (∫ omega, |X omega| ^ 3 ∂mu) * |u| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            X hX.aemeasurable : Measure ℝ) u ^ n -
        Complex.exp (-(u : ℂ) ^ 2 / 2) ^ n‖ ≤
      n * Real.exp (-(5 * u ^ 2 / 12)) ^ (n - 1) *
        ((|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 8) := by
  let A := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      X hX.aemeasurable : Measure ℝ) u
  let B := Complex.exp (-(u : ℂ) ^ 2 / 2)
  let r := Real.exp (-(5 * u ^ 2 / 12))
  have hmax : max ‖A‖ ‖B‖ ≤ r := max_le
    (norm_charFun_probabilityLaw_le_exp_neg_five_twelfths_sq
      X hX hMean hSecond hu hsmall)
    (norm_cexp_neg_half_sq_le_exp_neg_five_twelfths_sq u)
  have hpow : max ‖A‖ ‖B‖ ^ (n - 1) ≤ r ^ (n - 1) :=
    pow_le_pow_left₀ (by positivity) hmax (n - 1)
  calc
    ‖A ^ n - B ^ n‖ ≤
        n * max ‖A‖ ‖B‖ ^ (n - 1) * ‖A - B‖ :=
      norm_pow_sub_pow_le_max A B n
    _ ≤ n * r ^ (n - 1) *
        ((|u| ^ 3 / 6) * (∫ omega, |X omega| ^ 3 ∂mu) + |u| ^ 4 / 8) := by
      gcongr
      exact norm_charFun_probabilityLaw_sub_cexp_neg_half_sq_le_sharp
        X hX hMean hSecond hu

/-- Damped characteristic-function comparison for the normalized centered
i.i.d. sum and the standard normal. -/
theorem norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le_damped
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 16 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
              Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ ≤
      ((N + 1 : ℕ) : ℝ) *
        Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N *
        (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4) := by
  let hX2 : ∀ i, MemLp (X i) 2 mu := fun i =>
    (hX i).mono_exponent (by norm_num)
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun,
    ← cexp_neg_half_sq_div_sqrt_pow_succ N t,
    NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw_normalizedCenteredIidSum
      X hX2 hIndep hIdent N t]
  have hcf :
      (∫ omega, Complex.exp
        ((((t / Real.sqrt (N + 1 : ℝ)) * X 0 omega : ℝ) : ℂ) * Complex.I) ∂mu) =
        MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) := by
    rw [NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw]
    apply integral_congr_ae
    filter_upwards with omega
    congr 1
    push_cast
    ring
  rw [hcf]
  simpa only [Nat.add_sub_cancel] using
    (norm_charFun_pow_sub_cexp_neg_half_sq_pow_le_damped
      (X 0) (hX 0) hMean hSecond (N + 1) ht hsmall)

/-- Sharp damped characteristic-function comparison for the normalized
centered i.i.d. sum and the standard normal. -/
theorem norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le_damped_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 2 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
              Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ ≤
      ((N + 1 : ℕ) : ℝ) *
        Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8) := by
  let hX2 : ∀ i, MemLp (X i) 2 mu := fun i =>
    (hX i).mono_exponent (by norm_num)
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun,
    ← cexp_neg_half_sq_div_sqrt_pow_succ N t,
    NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw_normalizedCenteredIidSum
      X hX2 hIndep hIdent N t]
  have hcf :
      (∫ omega, Complex.exp
        ((((t / Real.sqrt (N + 1 : ℝ)) * X 0 omega : ℝ) : ℂ) * Complex.I) ∂mu) =
        MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) := by
    rw [NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw]
    apply integral_congr_ae
    filter_upwards with omega
    congr 1
    push_cast
    ring
  rw [hcf]
  simpa only [Nat.add_sub_cancel] using
    (norm_charFun_pow_sub_cexp_neg_half_sq_pow_le_damped_sharp
      (X 0) (hX 0) hMean hSecond (N + 1) ht hsmall)

/-- Damped normalized-sum estimate in discrepancy-quotient form. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 16 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      (((N + 1 : ℕ) : ℝ) *
        Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N *
        (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t| := by
  by_cases ht0 : t = 0
  · subst t
    simp [charFunDiscrepancyQuotient]
  · rw [charFunDiscrepancyQuotient, if_neg ht0]
    exact div_le_div_of_nonneg_right
      (norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le_damped
        X hX hIndep hIdent hMean hSecond N ht hsmall)
      (abs_nonneg t)

/-- Algebraic simplification of the damped normalized quotient. -/
theorem normalized_damped_cubic_quartic_quotient_eq
    (N : ℕ) (t rho damping : ℝ) :
    ((((N + 1 : ℕ) : ℝ) * damping *
        (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 * rho +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t|) =
      damping * (4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ))) := by
  calc
    _ = damping *
        (((((N + 1 : ℕ) : ℝ) *
              (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 * rho) +
            ((N + 1 : ℕ) : ℝ) *
              (|t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4))) / |t|) := by
      ring
    _ = _ := by rw [normalized_cubic_quartic_quotient_eq N t rho]

/-- Simplified damped polynomial bound for the normalized discrepancy
quotient. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_polynomial
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 16 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N *
        (4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            Real.sqrt (N + 1 : ℝ) +
          |t| ^ 3 / (4 * (N + 1 : ℝ))) := by
  calc
    _ ≤ ((((N + 1 : ℕ) : ℝ) *
        Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N *
        (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t|) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped
        X hX hIndep hIdent hMean hSecond N ht hsmall
    _ = _ := normalized_damped_cubic_quartic_quotient_eq N t
      (∫ omega, |X 0 omega| ^ 3 ∂mu)
      (Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N)

/-- Sharp damped normalized-sum estimate in discrepancy-quotient form. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 2 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      (((N + 1 : ℕ) : ℝ) *
        Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8)) / |t| := by
  by_cases ht0 : t = 0
  · subst t
    simp [charFunDiscrepancyQuotient]
  · rw [charFunDiscrepancyQuotient, if_neg ht0]
    exact div_le_div_of_nonneg_right
      (norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le_damped_sharp
        X hX hIndep hIdent hMean hSecond N ht hsmall)
      (abs_nonneg t)

/-- Algebraic normalization of the sharp damped cubic/quartic quotient. -/
theorem normalized_damped_cubic_quartic_quotient_eq_sharp
    (N : ℕ) (t rho damping : ℝ) :
    ((((N + 1 : ℕ) : ℝ) * damping *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) * rho +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8)) / |t|) =
      damping * (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ))) := by
  calc
    _ = ((((N + 1 : ℕ) : ℝ) * (damping / 2) *
        (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 * (rho / 12) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4)) / |t|) := by ring
    _ = (damping / 2) * (4 * (rho / 12) * |t| ^ 2 /
          Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ))) :=
      normalized_damped_cubic_quartic_quotient_eq N t (rho / 12) (damping / 2)
    _ = _ := by
      have hn : (N + 1 : ℝ) ≠ 0 := by positivity
      have hs : Real.sqrt (N + 1 : ℝ) ≠ 0 := by positivity
      field_simp [hn, hs]
      <;> ring

/-- Sharp simplified damped polynomial bound for the normalized discrepancy
quotient. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_polynomial_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 2 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N *
        ((∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ))) := by
  calc
    _ ≤ ((((N + 1 : ℕ) : ℝ) *
        Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8)) / |t|) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_sharp
        X hX hIndep hIdent hMean hSecond N ht hsmall
    _ = _ := normalized_damped_cubic_quartic_quotient_eq_sharp N t
      (∫ omega, |X 0 omega| ^ 3 ∂mu)
      (Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N)

/-- Tunable damped characteristic-function comparison for the normalized
centered i.i.d. sum and the standard normal.  The window parameter is retained
so that the eventual smoothing cutoff and Gaussian envelope can be optimized
jointly. -/
theorem norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le_damped_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {c t : ℝ} (hc : 0 < c)
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    ‖MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
              Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ ≤
      ((N + 1 : ℕ) : ℝ) *
        Real.exp (-(tunableCharFunDampingCoefficient c *
          (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8) := by
  let hX2 : ∀ i, MemLp (X i) 2 mu := fun i =>
    (hX i).mono_exponent (by norm_num)
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun,
    ← cexp_neg_half_sq_div_sqrt_pow_succ N t,
    NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw_normalizedCenteredIidSum
      X hX2 hIndep hIdent N t]
  have hcf :
      (∫ omega, Complex.exp
        ((((t / Real.sqrt (N + 1 : ℝ)) * X 0 omega : ℝ) : ℂ) * Complex.I) ∂mu) =
        MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) := by
    rw [NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw]
    apply integral_congr_ae
    filter_upwards with omega
    congr 1
    push_cast
    ring
  rw [hcf]
  simpa only [Nat.add_sub_cancel] using
    (norm_charFun_pow_sub_cexp_neg_half_sq_pow_le_damped_tunable
      (X 0) (hX 0) hMean hSecond (N + 1) hc ht hsmall)

/-- Tunable damped normalized-sum estimate in discrepancy-quotient form. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {c t : ℝ} (hc : 0 < c)
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      ((((N + 1 : ℕ) : ℝ) *
        Real.exp (-(tunableCharFunDampingCoefficient c *
          (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8)) / |t|) := by
  by_cases ht0 : t = 0
  · subst t
    simp [charFunDiscrepancyQuotient]
  · rw [charFunDiscrepancyQuotient, if_neg ht0]
    exact div_le_div_of_nonneg_right
      (norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le_damped_tunable
        X hX hIndep hIdent hMean hSecond N hc ht hsmall)
      (abs_nonneg t)

/-- Simplified tunable damped polynomial bound for the normalized discrepancy
quotient. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_polynomial_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {c t : ℝ} (hc : 0 < c)
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-(tunableCharFunDampingCoefficient c *
          (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N *
        ((∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ))) := by
  calc
    _ ≤ ((((N + 1 : ℕ) : ℝ) *
        Real.exp (-(tunableCharFunDampingCoefficient c *
          (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N *
        ((|t / Real.sqrt (N + 1 : ℝ)| ^ 3 / 6) *
            (∫ omega, |X 0 omega| ^ 3 ∂mu) +
          |t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 8)) / |t|) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_tunable
        X hX hIndep hIdent hMean hSecond N hc ht hsmall
    _ = _ := normalized_damped_cubic_quartic_quotient_eq_sharp N t
      (∫ omega, |X 0 omega| ^ 3 ∂mu)
      (Real.exp (-(tunableCharFunDampingCoefficient c *
        (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N)

theorem tunableCharFunDampingCoefficient_pos
    {c : ℝ} (hc : 1 / 3 < c) :
    0 < tunableCharFunDampingCoefficient c := by
  unfold tunableCharFunDampingCoefficient
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  rw [sub_pos, div_lt_iff₀ (by positivity : 0 < 6 * c)]
  nlinarith

/-- Exact conversion of the normalized powered damping into a Gaussian in
the original Fourier frequency. -/
theorem rexp_neg_tunable_div_sqrt_sq_pow_eq
    (c : ℝ) (N : ℕ) (t : ℝ) :
    Real.exp (-(tunableCharFunDampingCoefficient c *
        (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N =
      Real.exp (-((N : ℝ) / (N + 1 : ℝ) *
        tunableCharFunDampingCoefficient c * t ^ 2)) := by
  rw [← Real.exp_nat_mul]
  congr 1
  have hspos : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hs2 : Real.sqrt (N + 1 : ℝ) ^ 2 = (N + 1 : ℝ) :=
    Real.sq_sqrt (by positivity)
  generalize hsdef : Real.sqrt (N + 1 : ℝ) = s at *
  norm_num [Nat.cast_add, Nat.cast_one]
  rw [← hs2]
  field_simp [hspos.ne']

/-- For at least two summands, the exact tunable exponent admits an
`N`-independent Gaussian envelope while retaining the window parameter. -/
theorem rexp_neg_tunable_div_sqrt_sq_pow_le
    {c : ℝ} (hc : 1 / 3 ≤ c) {N : ℕ} (hN : 1 ≤ N) (t : ℝ) :
    Real.exp (-(tunableCharFunDampingCoefficient c *
        (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N ≤
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) := by
  rw [rexp_neg_tunable_div_sqrt_sq_pow_eq]
  apply Real.exp_le_exp.mpr
  have hratio : (1 / 2 : ℝ) ≤ (N : ℝ) / (N + 1 : ℝ) := by
    rw [le_div_iff₀ (by positivity : (0 : ℝ) < N + 1)]
    have hNc : (1 : ℝ) ≤ N := by exact_mod_cast hN
    nlinarith
  have hcoeff : 0 ≤ tunableCharFunDampingCoefficient c :=
    tunableCharFunDampingCoefficient_nonneg hc
  have hmul := mul_le_mul_of_nonneg_right hratio
    (mul_nonneg hcoeff (sq_nonneg t))
  nlinarith

/-- Endpoint of the local cubic-remainder frequency window. -/
noncomputable def tunableThirdMomentCutoff
    (c rho : ℝ) (N : ℕ) : ℝ :=
  Real.sqrt (N + 1 : ℝ) / (c * rho)

/-- The tunable local Taylor condition is exactly the absolute-frequency
cutoff, provided its two scale parameters are positive. -/
theorem tunableThirdMomentCondition_iff_abs_le_cutoff
    {c rho : ℝ} (hc : 0 < c) (hrho : 0 < rho) (N : ℕ) (t : ℝ) :
    c * rho * |t / Real.sqrt (N + 1 : ℝ)| ≤ 1 ↔
      |t| ≤ tunableThirdMomentCutoff c rho N := by
  have hspos : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  unfold tunableThirdMomentCutoff
  rw [abs_div, abs_of_nonneg (Real.sqrt_nonneg _)]
  rw [show c * rho * (|t| / Real.sqrt (N + 1 : ℝ)) =
      (c * rho * |t|) / Real.sqrt (N + 1 : ℝ) by ring,
    div_le_iff₀ hspos, le_div_iff₀ (mul_pos hc hrho)]
  simp only [mul_assoc, mul_left_comm, mul_comm, one_mul]

/-- Polynomial moments are integrable against every positive tunable
Gaussian envelope. -/
theorem integrable_abs_pow_mul_rexp_neg_tunable_sq
    {c : ℝ} (hc : 1 / 3 < c) (k : ℕ) :
    Integrable (fun t : ℝ => |t| ^ k *
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))) := by
  have hcoeff : 0 < tunableCharFunDampingCoefficient c :=
    tunableCharFunDampingCoefficient_pos hc
  have h := integrable_rpow_mul_exp_neg_mul_sq
    (b := tunableCharFunDampingCoefficient c / 2) (by positivity)
    (s := (k : ℝ)) (by
      have hk : (0 : ℝ) ≤ k := Nat.cast_nonneg k
      linarith)
  have hn := h.norm
  convert hn using 1
  funext t
  simp only [Real.rpow_natCast, Real.norm_eq_abs, abs_mul, abs_pow,
    abs_of_pos (Real.exp_pos _)]
  congr 2
  ring

/-- `c`-dependent Gaussian moments governing the integrated tunable
characteristic-function estimate. -/
noncomputable def tunableGaussianDampedMoment (c : ℝ) (k : ℕ) : ℝ :=
  ∫ t : ℝ, |t| ^ k *
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))

theorem tunableGaussianDampedMoment_nonneg (c : ℝ) (k : ℕ) :
    0 ≤ tunableGaussianDampedMoment c k := by
  exact integral_nonneg fun _ => by positivity

/-- The positive-frequency half of a tunable Gaussian moment is exactly half
of its full-line absolute moment.  This is the symmetry bridge needed to
integrate Prawitz bounds without losing a factor of two. -/
theorem integral_Ioi_abs_pow_mul_rexp_neg_tunable_sq
    (c : ℝ) (k : ℕ) :
    (∫ t in Ioi (0 : ℝ), |t| ^ k *
        Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))) =
      tunableGaussianDampedMoment c k / 2 := by
  let f : ℝ → ℝ := fun z => z ^ k *
    Real.exp (-(tunableCharFunDampingCoefficient c * z ^ 2 / 2))
  have hEven : tunableGaussianDampedMoment c k =
      2 * ∫ t in Ioi (0 : ℝ), f t := by
    unfold tunableGaussianDampedMoment
    calc
      (∫ t : ℝ, |t| ^ k *
          Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))) =
          ∫ t : ℝ, f |t| := by
        apply integral_congr_ae
        exact ae_of_all _ fun t => by
          dsimp only [f]
          rw [sq_abs]
      _ = 2 * ∫ t in Ioi (0 : ℝ), f t := integral_comp_abs
  have hPositive :
      (∫ t in Ioi (0 : ℝ), |t| ^ k *
          Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))) =
        ∫ t in Ioi (0 : ℝ), f t := by
    apply setIntegral_congr_fun measurableSet_Ioi
    intro t ht
    dsimp only [f]
    rw [abs_of_pos ht]
  rw [hPositive, hEven]
  ring

/-- Closed form of the quadratic tunable Gaussian moment. -/
theorem tunableGaussianDampedMoment_two
    {c : ℝ} (hc : 1 / 3 < c) :
    tunableGaussianDampedMoment c 2 =
      (tunableCharFunDampingCoefficient c / 2) ^ (-(3 / 2 : ℝ)) *
        (Real.sqrt Real.pi / 2) := by
  let a : ℝ := tunableCharFunDampingCoefficient c / 2
  have ha : 0 < a := by
    dsimp only [a]
    exact div_pos (tunableCharFunDampingCoefficient_pos hc) (by norm_num)
  have hHalf :
      (∫ x in Ioi (0 : ℝ), x ^ 2 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2))) =
        a ^ (-(3 / 2 : ℝ)) * (1 / 2) * Real.Gamma (3 / 2) := by
    convert (integral_rpow_mul_exp_neg_mul_rpow
        (p := (2 : ℝ)) (q := (2 : ℝ)) (b := a)
        (by norm_num) (by norm_num) ha) using 1
    · apply setIntegral_congr_fun measurableSet_Ioi
      intro x hx
      simp only [Real.rpow_ofNat]
      congr 1
      dsimp only [a]
      ring
    · ring
  have hEven :
      (∫ x : ℝ, |x| ^ 2 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2))) =
        2 * ∫ x in Ioi (0 : ℝ), x ^ 2 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2)) := by
    calc
      (∫ x : ℝ, |x| ^ 2 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2))) =
          ∫ x : ℝ, (fun z : ℝ => z ^ 2 *
            Real.exp (-(tunableCharFunDampingCoefficient c * z ^ 2 / 2)))
              |x| := by
        apply integral_congr_ae
        exact ae_of_all _ fun x => by
          change |x| ^ 2 *
              Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2)) =
            |x| ^ 2 * Real.exp
              (-(tunableCharFunDampingCoefficient c * |x| ^ 2 / 2))
          rw [sq_abs]
      _ = _ := integral_comp_abs
        (f := fun z : ℝ => z ^ 2 *
          Real.exp (-(tunableCharFunDampingCoefficient c * z ^ 2 / 2)))
  have hGamma : Real.Gamma (3 / 2) = Real.sqrt Real.pi / 2 := by
    rw [show (3 / 2 : ℝ) = 1 / 2 + 1 by ring,
      Real.Gamma_add_one (by norm_num), Real.Gamma_one_half_eq]
    ring
  unfold tunableGaussianDampedMoment
  rw [hEven, hHalf, hGamma]
  dsimp only [a]
  ring

/-- Closed form of the cubic tunable Gaussian moment. -/
theorem tunableGaussianDampedMoment_three
    {c : ℝ} (hc : 1 / 3 < c) :
    tunableGaussianDampedMoment c 3 =
      4 / tunableCharFunDampingCoefficient c ^ 2 := by
  let a : ℝ := tunableCharFunDampingCoefficient c / 2
  have ha : 0 < a := by
    dsimp only [a]
    exact div_pos (tunableCharFunDampingCoefficient_pos hc) (by norm_num)
  have hHalf :
      (∫ x in Ioi (0 : ℝ), x ^ 3 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2))) =
        1 / (2 * a ^ 2) := by
    convert (integral_rpow_mul_exp_neg_mul_rpow
        (p := (2 : ℝ)) (q := (3 : ℝ)) (b := a)
        (by norm_num) (by norm_num) ha) using 1
    · apply setIntegral_congr_fun measurableSet_Ioi
      intro x hx
      simp only [Real.rpow_ofNat]
      congr 1
      dsimp only [a]
      ring
    · rw [show ((3 : ℝ) + 1) / 2 = 1 + 1 by norm_num,
        Real.Gamma_add_one one_ne_zero, Real.Gamma_one]
      rw [show (-(3 + 1) / 2 : ℝ) = -2 by norm_num,
        Real.rpow_neg ha.le, Real.rpow_two]
      field_simp [ha.ne']
  have hEven :
      (∫ x : ℝ, |x| ^ 3 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2))) =
        2 * ∫ x in Ioi (0 : ℝ), x ^ 3 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2)) := by
    calc
      (∫ x : ℝ, |x| ^ 3 *
          Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2))) =
          ∫ x : ℝ, (fun z : ℝ => z ^ 3 *
            Real.exp (-(tunableCharFunDampingCoefficient c * z ^ 2 / 2)))
              |x| := by
        apply integral_congr_ae
        exact ae_of_all _ fun x => by
          change |x| ^ 3 *
              Real.exp (-(tunableCharFunDampingCoefficient c * x ^ 2 / 2)) =
            |x| ^ 3 * Real.exp
              (-(tunableCharFunDampingCoefficient c * |x| ^ 2 / 2))
          rw [sq_abs]
      _ = _ := integral_comp_abs
        (f := fun z : ℝ => z ^ 3 *
          Real.exp (-(tunableCharFunDampingCoefficient c * z ^ 2 / 2)))
  unfold tunableGaussianDampedMoment
  rw [hEven, hHalf]
  dsimp only [a]
  have hcoeff : tunableCharFunDampingCoefficient c ≠ 0 :=
    (tunableCharFunDampingCoefficient_pos hc).ne'
  field_simp [hcoeff]
  ring

/-- Kernel-independent positive-frequency cost of the tunable sharp Taylor
majorant at Fourier prefactor `1 / π`. -/
noncomputable def idealTunableBerryEsseenFrequencyConstant (c : ℝ) : ℝ :=
  (1 / Real.pi) *
    ((1 / 6) * tunableGaussianDampedMoment c 2 +
      (1 / 8) * tunableGaussianDampedMoment c 3)

/-- Even with an ideal unit filter and no spatial smoothing remainder, the
absolute cubic/quartic tunable Taylor route remains above coefficient one. -/
theorem one_lt_idealTunableBerryEsseenFrequencyConstant
    {c : ℝ} (hc : 1 / 3 < c) :
    1 < idealTunableBerryEsseenFrequencyConstant c := by
  let a : ℝ := tunableCharFunDampingCoefficient c
  have ha : 0 < a := tunableCharFunDampingCoefficient_pos hc
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have haHalf : a < 1 / 2 := by
    dsimp only [a, tunableCharFunDampingCoefficient]
    have hinv : 0 < 1 / (6 * c) := by positivity
    linarith
  have hb : a / 2 ≤ 1 / 4 := by linarith
  have hquarter : (1 / 4 : ℝ) ^ (-(3 / 2 : ℝ)) = 8 := by
    norm_num [Real.rpow_neg]
  have hrpow : 8 ≤ (a / 2) ^ (-(3 / 2 : ℝ)) := by
    rw [← hquarter]
    exact Real.rpow_le_rpow_of_nonpos (by positivity) hb (by norm_num)
  have hsqrt : 7 / 4 < Real.sqrt Real.pi := by
    rw [Real.lt_sqrt (by norm_num : (0 : ℝ) ≤ 7 / 4)]
    nlinarith [Real.pi_gt_d2]
  have hM2 : 4 * Real.sqrt Real.pi ≤
      tunableGaussianDampedMoment c 2 := by
    rw [tunableGaussianDampedMoment_two hc]
    dsimp only [a] at hrpow ⊢
    nlinarith [mul_le_mul_of_nonneg_right hrpow
      (by positivity : 0 ≤ Real.sqrt Real.pi / 2)]
  have hM3 : 16 < tunableGaussianDampedMoment c 3 := by
    rw [tunableGaussianDampedMoment_three hc]
    change 16 < 4 / a ^ 2
    rw [lt_div_iff₀ (sq_pos_of_pos ha)]
    nlinarith [sq_nonneg (a - 1 / 2)]
  have hsum : Real.pi <
      (1 / 6) * tunableGaussianDampedMoment c 2 +
        (1 / 8) * tunableGaussianDampedMoment c 3 := by
    nlinarith [Real.pi_lt_d2]
  have heq : idealTunableBerryEsseenFrequencyConstant c =
      ((1 / 6) * tunableGaussianDampedMoment c 2 +
        (1 / 8) * tunableGaussianDampedMoment c 3) / Real.pi := by
    unfold idealTunableBerryEsseenFrequencyConstant
    field_simp [Real.pi_ne_zero]
  rw [heq]
  exact (one_lt_div Real.pi_pos).2 hsum

/-- Ideal positive-frequency coefficient after absorbing the quartic proxy
term into the cubic term on the tunable window. -/
noncomputable def absorbedIdealTunableBerryEsseenFrequencyConstant
    (c : ℝ) : ℝ :=
  (1 / Real.pi) * (1 / 6 + 1 / (8 * c)) *
    tunableGaussianDampedMoment c 2

/-- At the concrete admissible choice `c = 2`, the absorbed Fourier cost is
strictly below one.  Thus the remaining coefficient-one obstruction belongs
to spatial smoothing, not to the local characteristic-function estimate. -/
theorem absorbedIdealTunableBerryEsseenFrequencyConstant_two_lt_one :
    absorbedIdealTunableBerryEsseenFrequencyConstant 2 < 1 := by
  have hc : (1 / 3 : ℝ) < 2 := by norm_num
  have hb : (0 : ℝ) < 5 / 24 := by norm_num
  have hrpow : (5 / 24 : ℝ) ^ (-(3 / 2 : ℝ)) =
      1 / ((5 / 24 : ℝ) * Real.sqrt (5 / 24 : ℝ)) := by
    rw [Real.rpow_neg hb.le]
    congr 1
    rw [show (3 / 2 : ℝ) = 1 + 1 / 2 by ring,
      Real.rpow_add hb, Real.rpow_one, ← Real.sqrt_eq_rpow]
    rw [one_div]
  have hM2eq : tunableGaussianDampedMoment 2 2 =
      Real.sqrt Real.pi /
        (2 * (5 / 24 : ℝ) * Real.sqrt (5 / 24 : ℝ)) := by
    rw [tunableGaussianDampedMoment_two hc]
    have hcoeff : tunableCharFunDampingCoefficient 2 / 2 = (5 / 24 : ℝ) := by
      norm_num [tunableCharFunDampingCoefficient]
    rw [hcoeff]
    rw [hrpow]
    ring
  have hsqrtPi : Real.sqrt Real.pi < 2 := by
    rw [Real.sqrt_lt' (by norm_num : (0 : ℝ) < 2)]
    nlinarith [Real.pi_lt_four]
  have hsqrtB : (9 / 20 : ℝ) < Real.sqrt (5 / 24 : ℝ) := by
    rw [Real.lt_sqrt (by norm_num : (0 : ℝ) ≤ 9 / 20)]
    norm_num
  have hden : (3 / 16 : ℝ) <
      2 * (5 / 24 : ℝ) * Real.sqrt (5 / 24 : ℝ) := by
    nlinarith
  have hden0 : 0 <
      2 * (5 / 24 : ℝ) * Real.sqrt (5 / 24 : ℝ) := by positivity
  have hM2 : tunableGaussianDampedMoment 2 2 < 32 / 3 := by
    rw [hM2eq, div_lt_iff₀ hden0]
    nlinarith
  unfold absorbedIdealTunableBerryEsseenFrequencyConstant
  norm_num
  rw [show Real.pi⁻¹ * (11 / 48) * tunableGaussianDampedMoment 2 2 =
      ((11 / 48) * tunableGaussianDampedMoment 2 2) / Real.pi by ring,
    div_lt_one Real.pi_pos]
  nlinarith [Real.pi_gt_d2]

/-- Tunable Gaussian-majorized discrepancy quotient on its local Taylor
window, valid once the normalized sum contains at least two variables. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 ≤ c) {N : ℕ} (hN : 1 ≤ N) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
        ((∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ))) := by
  have hc0 : 0 < c := lt_of_lt_of_le (by norm_num) hc
  have hpoly : 0 ≤
      (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
          (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)) := by
    have hrho : 0 ≤ ∫ omega, |X 0 omega| ^ 3 ∂mu :=
      integral_nonneg fun _ => by positivity
    positivity
  calc
    _ ≤ Real.exp (-(tunableCharFunDampingCoefficient c *
          (t / Real.sqrt (N + 1 : ℝ)) ^ 2)) ^ N *
        ((∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ))) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_polynomial_tunable
        X hX hIndep hIdent hMean hSecond N hc0 ht hsmall
    _ ≤ _ := mul_le_mul_of_nonneg_right
      (rexp_neg_tunable_div_sqrt_sq_pow_le hc hN t) hpoly

theorem integrable_tunableGaussianDiscrepancyMajorant
    {c : ℝ} (hc : 1 / 3 < c) (rho : ℝ) (N : ℕ) :
    Integrable (fun t : ℝ =>
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
        (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ)))) := by
  have h2 := integrable_abs_pow_mul_rexp_neg_tunable_sq hc 2
  have h3 := integrable_abs_pow_mul_rexp_neg_tunable_sq hc 3
  have hsum :=
    (h2.const_mul (rho / (6 * Real.sqrt (N + 1 : ℝ)))).add
      (h3.const_mul (1 / (8 * (N + 1 : ℝ))))
  convert hsum using 1
  funext t
  simp only [Pi.add_apply]
  ring

/-- Exact quadratic/cubic moment decomposition of the integrated tunable
Gaussian discrepancy majorant. -/
theorem integral_tunableGaussianDiscrepancyMajorant
    {c : ℝ} (hc : 1 / 3 < c) (rho : ℝ) (N : ℕ) :
    (∫ t : ℝ,
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
        (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ)))) =
      (rho / (6 * Real.sqrt (N + 1 : ℝ))) *
          tunableGaussianDampedMoment c 2 +
        (1 / (8 * (N + 1 : ℝ))) *
          tunableGaussianDampedMoment c 3 := by
  have h2 := integrable_abs_pow_mul_rexp_neg_tunable_sq hc 2
  have h3 := integrable_abs_pow_mul_rexp_neg_tunable_sq hc 3
  rw [show (fun t : ℝ =>
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
        (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ)))) =
      fun t => (rho / (6 * Real.sqrt (N + 1 : ℝ))) *
          (|t| ^ 2 * Real.exp
            (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))) +
        (1 / (8 * (N + 1 : ℝ))) *
          (|t| ^ 3 * Real.exp
            (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))) by
    funext t
    ring]
  rw [integral_add (h2.const_mul _) (h3.const_mul _),
    integral_const_mul, integral_const_mul]
  rfl

/-- Full frequency window on which both the unit-frequency Taylor hypothesis
and the tunable cubic-remainder condition hold. -/
def tunableNormalizedThirdMomentWindow
    (c rho : ℝ) (N : ℕ) : Set ℝ :=
  {t | |t / Real.sqrt (N + 1 : ℝ)| ≤ 1 ∧
    c * rho * |t / Real.sqrt (N + 1 : ℝ)| ≤ 1}

theorem measurableSet_tunableNormalizedThirdMomentWindow
    (c rho : ℝ) (N : ℕ) :
    MeasurableSet (tunableNormalizedThirdMomentWindow c rho N) := by
  simp only [tunableNormalizedThirdMomentWindow, Set.setOf_and]
  exact (measurableSet_le (by fun_prop) (by fun_prop)).inter
    (measurableSet_le (by fun_prop) (by fun_prop))

/-- Maximal symmetric cutoff satisfying both constraints in the full tunable
window. -/
noncomputable def admissibleTunableThirdMomentCutoff
    (c rho : ℝ) (N : ℕ) : ℝ :=
  min (Real.sqrt (N + 1 : ℝ)) (tunableThirdMomentCutoff c rho N)

theorem admissibleTunableThirdMomentCutoff_pos
    {c rho : ℝ} (hc : 0 < c) (hrho : 0 < rho) (N : ℕ) :
    0 < admissibleTunableThirdMomentCutoff c rho N := by
  unfold admissibleTunableThirdMomentCutoff tunableThirdMomentCutoff
  exact lt_min (by positivity) (div_pos (by positivity) (mul_pos hc hrho))

theorem admissibleTunableThirdMomentCutoff_eq_sqrt_div_max
    {c rho : ℝ} (hc : 0 < c) (hrho : 0 < rho) (N : ℕ) :
    admissibleTunableThirdMomentCutoff c rho N =
      Real.sqrt (N + 1 : ℝ) / max 1 (c * rho) := by
  have hs : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hcr : 0 < c * rho := mul_pos hc hrho
  unfold admissibleTunableThirdMomentCutoff tunableThirdMomentCutoff
  by_cases h : 1 ≤ c * rho
  · rw [max_eq_right h, min_eq_right]
    rw [div_le_iff₀ hcr]
    nlinarith
  · have hle : c * rho ≤ 1 := le_of_not_ge h
    rw [max_eq_left hle, div_one, min_eq_left]
    rw [le_div_iff₀ hcr]
    nlinarith

/-- Increasing the damping-window parameter can only decrease the admissible
Taylor cutoff. -/
theorem admissibleTunableThirdMomentCutoff_antitone
    {rho cOuter cInner : ℝ} (hrho : 0 < rho)
    (hcOuter : 0 < cOuter) (hcOuterInner : cOuter ≤ cInner) (N : ℕ) :
    admissibleTunableThirdMomentCutoff cInner rho N ≤
      admissibleTunableThirdMomentCutoff cOuter rho N := by
  have hcInner : 0 < cInner := hcOuter.trans_le hcOuterInner
  rw [admissibleTunableThirdMomentCutoff_eq_sqrt_div_max hcInner hrho N,
    admissibleTunableThirdMomentCutoff_eq_sqrt_div_max hcOuter hrho N]
  have hprod : cOuter * rho ≤ cInner * rho :=
    mul_le_mul_of_nonneg_right hcOuterInner hrho.le
  have hmax : max 1 (cOuter * rho) ≤ max 1 (cInner * rho) :=
    max_le_max le_rfl hprod
  exact div_le_div_of_nonneg_left (Real.sqrt_nonneg _)
    (lt_of_lt_of_le zero_lt_one (le_max_left _ _)) hmax

theorem Icc_admissibleTunableThirdMomentCutoff_subset_window
    {c rho : ℝ} (hc : 0 < c) (hrho : 0 < rho) (N : ℕ) :
    Icc (-(admissibleTunableThirdMomentCutoff c rho N))
        (admissibleTunableThirdMomentCutoff c rho N) ⊆
      tunableNormalizedThirdMomentWindow c rho N := by
  intro t ht
  have hs : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have habs : |t| ≤ admissibleTunableThirdMomentCutoff c rho N :=
    abs_le.mpr ht
  constructor
  · rw [abs_div, abs_of_pos hs, div_le_one hs]
    exact habs.trans (min_le_left _ _)
  · exact (tunableThirdMomentCondition_iff_abs_le_cutoff hc hrho N t).2
      (habs.trans (min_le_right _ _))

/-- Integrated tunable characteristic-function discrepancy on its full
admissible window. -/
theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in tunableNormalizedThirdMomentWindow c
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N,
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      ((∫ omega, |X 0 omega| ^ 3 ∂mu) /
          (6 * Real.sqrt (N + 1 : ℝ))) *
          tunableGaussianDampedMoment c 2 +
        (1 / (8 * (N + 1 : ℝ))) *
          tunableGaussianDampedMoment c 3 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let g : ℝ → ℝ := fun t =>
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
      (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)))
  let S : Set ℝ := tunableNormalizedThirdMomentWindow c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hgNonneg : ∀ t, 0 ≤ g t := fun t => by
    dsimp only [g]
    have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
    positivity
  have hg : Integrable g := by
    simpa only [g] using
      integrable_tunableGaussianDiscrepancyMajorant hc rho N
  have hqStrong : StronglyMeasurable q :=
    stronglyMeasurable_charFunDiscrepancyQuotient law target
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_tunableNormalizedThirdMomentWindow c rho N
  have hqS : Integrable q (volume.restrict S) := by
    apply Integrable.mono' hg.integrableOn hqStrong.aestronglyMeasurable
    filter_upwards [ae_restrict_mem hS] with t ht
    rw [Real.norm_eq_abs, abs_of_nonneg
      (charFunDiscrepancyQuotient_nonneg law target t)]
    exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable
      X hX hIndep hIdent hMean hSecond hc.le hN ht.1 ht.2
  change (∫ t in S, q t) ≤ _
  calc
    (∫ t in S, q t) ≤ ∫ t in S, g t := by
      apply integral_mono_ae hqS hg.integrableOn
      filter_upwards [ae_restrict_mem hS] with t ht
      exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable
        X hX hIndep hIdent hMean hSecond hc.le hN ht.1 ht.2
    _ ≤ ∫ t, g t :=
      integral_mono_measure Measure.restrict_le_self
        (ae_of_all volume hgNonneg) hg
    _ = _ := by
      simpa only [g, rho] using
        integral_tunableGaussianDiscrepancyMajorant hc rho N

theorem integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) :
    IntegrableOn
      (charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (tunableNormalizedThirdMomentWindow c
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let g : ℝ → ℝ := fun t =>
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
      (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)))
  let S : Set ℝ := tunableNormalizedThirdMomentWindow c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hg : Integrable g := by
    simpa only [g] using
      integrable_tunableGaussianDiscrepancyMajorant hc rho N
  have hqStrong : StronglyMeasurable q :=
    stronglyMeasurable_charFunDiscrepancyQuotient law target
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_tunableNormalizedThirdMomentWindow c rho N
  change Integrable q (volume.restrict S)
  apply Integrable.mono' hg.integrableOn hqStrong.aestronglyMeasurable
  filter_upwards [ae_restrict_mem hS] with t ht
  rw [Real.norm_eq_abs, abs_of_nonneg
    (charFunDiscrepancyQuotient_nonneg law target t)]
  exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable
    X hX hIndep hIdent hMean hSecond hc.le hN ht.1 ht.2

/-- For at least two summands, the normalized geometric damping is bounded
by a fixed Gaussian in the original Fourier frequency. -/
theorem rexp_neg_div_sqrt_sq_pow_le_rexp_neg_sq_eighth
    {N : ℕ} (hN : 1 ≤ N) (t : ℝ) :
    Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N ≤
      Real.exp (-t ^ 2 / 8) := by
  rw [← Real.exp_nat_mul]
  apply Real.exp_le_exp.mpr
  have hspos : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hs2 : Real.sqrt (N + 1 : ℝ) ^ 2 = (N + 1 : ℝ) :=
    Real.sq_sqrt (by positivity)
  generalize hsdef : Real.sqrt (N + 1 : ℝ) = s at *
  have hratio : (1 / 2 : ℝ) ≤ (N : ℝ) / (N + 1 : ℝ) := by
    rw [le_div_iff₀ (by positivity : (0 : ℝ) < N + 1)]
    have hNc : (1 : ℝ) ≤ N := by exact_mod_cast hN
    nlinarith
  have hexact :
      (N : ℝ) * (-(t / s) ^ 2 / 4) =
        -((N : ℝ) / (N + 1 : ℝ)) * t ^ 2 / 4 := by
    norm_num [Nat.cast_add, Nat.cast_one]
    rw [← hs2]
    field_simp [hspos.ne']
  rw [hexact]
  have hmul := mul_le_mul_of_nonneg_right hratio
    (div_nonneg (sq_nonneg t) (by norm_num : (0 : ℝ) ≤ 4))
  nlinarith

/-- Fixed Gaussian envelope for the sharper powered contraction. -/
theorem rexp_neg_five_twelfths_div_sqrt_sq_pow_le_rexp_neg_five_twentyfourths_sq
    {N : ℕ} (hN : 1 ≤ N) (t : ℝ) :
    Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N ≤
      Real.exp (-(5 * t ^ 2 / 24)) := by
  rw [← Real.exp_nat_mul]
  apply Real.exp_le_exp.mpr
  have hspos : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hs2 : Real.sqrt (N + 1 : ℝ) ^ 2 = (N + 1 : ℝ) :=
    Real.sq_sqrt (by positivity)
  generalize hsdef : Real.sqrt (N + 1 : ℝ) = s at *
  have hratio : (1 / 2 : ℝ) ≤ (N : ℝ) / (N + 1 : ℝ) := by
    rw [le_div_iff₀ (by positivity : (0 : ℝ) < N + 1)]
    have hNc : (1 : ℝ) ≤ N := by exact_mod_cast hN
    nlinarith
  have hexact :
      (N : ℝ) * (-(5 * (t / s) ^ 2 / 12)) =
        -((N : ℝ) / (N + 1 : ℝ)) * (5 * t ^ 2 / 12) := by
    norm_num [Nat.cast_add, Nat.cast_one]
    rw [← hs2]
    field_simp [hspos.ne']
  rw [hexact]
  have hmul := mul_le_mul_of_nonneg_right hratio
    (by positivity : 0 ≤ 5 * t ^ 2 / 12)
  nlinarith

/-- Gaussian-majorized discrepancy quotient on the full third-moment Taylor
window, valid once the normalized sum contains at least two variables. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 16 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-t ^ 2 / 8) *
        (4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            Real.sqrt (N + 1 : ℝ) +
          |t| ^ 3 / (4 * (N + 1 : ℝ))) := by
  have hrho : 0 ≤ ∫ omega, |X 0 omega| ^ 3 ∂mu :=
    integral_nonneg fun _ => by positivity
  have hpoly : 0 ≤
      4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
          Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ)) := by positivity
  calc
    _ ≤ Real.exp (-(t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 4) ^ N *
        (4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            Real.sqrt (N + 1 : ℝ) +
          |t| ^ 3 / (4 * (N + 1 : ℝ))) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_polynomial
        X hX hIndep hIdent hMean hSecond N ht hsmall
    _ ≤ _ := mul_le_mul_of_nonneg_right
      (rexp_neg_div_sqrt_sq_pow_le_rexp_neg_sq_eighth hN t) hpoly

/-- Sharp Gaussian-majorized discrepancy quotient on the enlarged
third-moment Taylor window. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : 2 * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-(5 * t ^ 2 / 24)) *
        ((∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ))) := by
  have hrho : 0 ≤ ∫ omega, |X 0 omega| ^ 3 ∂mu :=
    integral_nonneg fun _ => by positivity
  have hpoly : 0 ≤
      (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
          (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)) := by positivity
  calc
    _ ≤ Real.exp (-(5 * (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 12)) ^ N *
        ((∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            (6 * Real.sqrt (N + 1 : ℝ)) +
          |t| ^ 3 / (8 * (N + 1 : ℝ))) :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_damped_polynomial_sharp
        X hX hIndep hIdent hMean hSecond N ht hsmall
    _ ≤ _ := mul_le_mul_of_nonneg_right
      (rexp_neg_five_twelfths_div_sqrt_sq_pow_le_rexp_neg_five_twentyfourths_sq
        hN t)
      hpoly

/-- Every absolute polynomial moment is integrable against the fixed
Gaussian damping used above. -/
theorem integrable_abs_pow_mul_rexp_neg_sq_eighth (k : ℕ) :
    Integrable (fun t : ℝ => |t| ^ k * Real.exp (-t ^ 2 / 8)) := by
  have h := integrable_rpow_mul_exp_neg_mul_sq
    (b := (1 / 8 : ℝ)) (by norm_num) (s := (k : ℝ)) (by
      have hk : (0 : ℝ) ≤ k := Nat.cast_nonneg k
      linarith)
  have hn := h.norm
  convert hn using 1
  funext t
  simp only [Real.rpow_natCast, Real.norm_eq_abs, abs_mul, abs_pow,
    abs_of_pos (Real.exp_pos _)]
  congr 2
  ring

/-- Fixed Gaussian moments appearing in the integrated discrepancy bound. -/
noncomputable def gaussianDampedMoment (k : ℕ) : ℝ :=
  ∫ t : ℝ, |t| ^ k * Real.exp (-t ^ 2 / 8)

theorem gaussianDampedMoment_nonneg (k : ℕ) :
    0 ≤ gaussianDampedMoment k := by
  exact integral_nonneg fun _ => by positivity

/-- The Gaussian-majorized normalized discrepancy is globally integrable. -/
theorem integrable_gaussianDiscrepancyMajorant (rho : ℝ) (N : ℕ) :
    Integrable (fun t : ℝ => Real.exp (-t ^ 2 / 8) *
      (4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ)))) := by
  have h2 := integrable_abs_pow_mul_rexp_neg_sq_eighth 2
  have h3 := integrable_abs_pow_mul_rexp_neg_sq_eighth 3
  have hsum :=
    (h2.const_mul (4 * rho / Real.sqrt (N + 1 : ℝ))).add
      (h3.const_mul (1 / (4 * (N + 1 : ℝ))))
  convert hsum using 1
  funext t
  simp only [Pi.add_apply]
  ring

/-- Exact decomposition of the integrated Gaussian discrepancy majorant into
two fixed universal moments. -/
theorem integral_gaussianDiscrepancyMajorant (rho : ℝ) (N : ℕ) :
    (∫ t : ℝ, Real.exp (-t ^ 2 / 8) *
      (4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ)))) =
      (4 * rho / Real.sqrt (N + 1 : ℝ)) * gaussianDampedMoment 2 +
        (1 / (4 * (N + 1 : ℝ))) * gaussianDampedMoment 3 := by
  have h2 := integrable_abs_pow_mul_rexp_neg_sq_eighth 2
  have h3 := integrable_abs_pow_mul_rexp_neg_sq_eighth 3
  rw [show (fun t : ℝ => Real.exp (-t ^ 2 / 8) *
      (4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
        |t| ^ 3 / (4 * (N + 1 : ℝ)))) =
      fun t => (4 * rho / Real.sqrt (N + 1 : ℝ)) *
          (|t| ^ 2 * Real.exp (-t ^ 2 / 8)) +
        (1 / (4 * (N + 1 : ℝ))) *
          (|t| ^ 3 * Real.exp (-t ^ 2 / 8)) by
    funext t
    ring]
  rw [integral_add (h2.const_mul _) (h3.const_mul _),
    integral_const_mul, integral_const_mul]
  rfl

/-- Every absolute polynomial moment is integrable against the sharper fixed
Gaussian damping. -/
theorem integrable_abs_pow_mul_rexp_neg_five_twentyfourths_sq (k : ℕ) :
    Integrable (fun t : ℝ => |t| ^ k * Real.exp (-(5 * t ^ 2 / 24))) := by
  have h := integrable_rpow_mul_exp_neg_mul_sq
    (b := (5 / 24 : ℝ)) (by norm_num) (s := (k : ℝ)) (by
      have hk : (0 : ℝ) ≤ k := Nat.cast_nonneg k
      linarith)
  have hn := h.norm
  convert hn using 1
  funext t
  simp only [Real.rpow_natCast, Real.norm_eq_abs, abs_mul, abs_pow,
    abs_of_pos (Real.exp_pos _)]
  congr 2
  ring

/-- Fixed moments for the sharp Gaussian discrepancy majorant. -/
noncomputable def sharpGaussianDampedMoment (k : ℕ) : ℝ :=
  ∫ t : ℝ, |t| ^ k * Real.exp (-(5 * t ^ 2 / 24))

theorem sharpGaussianDampedMoment_nonneg (k : ℕ) :
    0 ≤ sharpGaussianDampedMoment k := by
  exact integral_nonneg fun _ => by positivity

/-- Exact cubic moment of the fixed sharp Gaussian damping profile. -/
theorem sharpGaussianDampedMoment_three :
    sharpGaussianDampedMoment 3 = 576 / 25 := by
  unfold sharpGaussianDampedMoment
  have hHalf :
      (∫ x in Ioi (0 : ℝ), x ^ 3 * Real.exp (-(5 * x ^ 2 / 24))) =
        288 / 25 := by
    convert (integral_rpow_mul_exp_neg_mul_rpow
        (p := (2 : ℝ)) (q := (3 : ℝ)) (b := (5 / 24 : ℝ))
        (by norm_num) (by norm_num) (by norm_num)) using 1
    · apply setIntegral_congr_fun measurableSet_Ioi
      intro x hx
      simp only [Real.rpow_ofNat]
      congr 1
      ring
    · rw [show ((3 : ℝ) + 1) / 2 = 1 + 1 by norm_num,
        Real.Gamma_add_one one_ne_zero, Real.Gamma_one]
      norm_num [Real.rpow_neg (by norm_num : (0 : ℝ) ≤ 5 / 24)]
  have hEven :
      (∫ x : ℝ, |x| ^ 3 * Real.exp (-(5 * x ^ 2 / 24))) =
        2 * ∫ x in Ioi (0 : ℝ),
          x ^ 3 * Real.exp (-(5 * x ^ 2 / 24)) := by
    calc
      (∫ x : ℝ, |x| ^ 3 * Real.exp (-(5 * x ^ 2 / 24))) =
          ∫ x : ℝ,
            (fun z : ℝ => z ^ 3 * Real.exp (-(5 * z ^ 2 / 24))) |x| := by
        apply integral_congr_ae
        exact ae_of_all _ fun x => by
          change |x| ^ 3 * Real.exp (-(5 * x ^ 2 / 24)) =
            |x| ^ 3 * Real.exp (-(5 * |x| ^ 2 / 24))
          rw [sq_abs]
      _ = _ := integral_comp_abs
        (f := fun z : ℝ => z ^ 3 * Real.exp (-(5 * z ^ 2 / 24)))
  rw [hEven, hHalf]
  ring

/-- A certified rational lower bound for the quadratic sharp Gaussian-
damped moment.  The elementary inequality `1-u ≤ exp(-u)` on `[-2,2]`
already suffices for the frequency-route audit. -/
theorem eight_div_three_le_sharpGaussianDampedMoment_two :
    8 / 3 ≤ sharpGaussianDampedMoment 2 := by
  let f : ℝ → ℝ := fun x => x ^ 2 * (1 - 5 * x ^ 2 / 24)
  let g : ℝ → ℝ := fun x => |x| ^ 2 * Real.exp (-(5 * x ^ 2 / 24))
  have hf : IntegrableOn f (Icc (-2 : ℝ) 2) := by
    apply ContinuousOn.integrableOn_Icc
    fun_prop
  have hg : Integrable g := by
    simpa only [g] using
      integrable_abs_pow_mul_rexp_neg_five_twentyfourths_sq 2
  have hgOn : IntegrableOn g (Icc (-2 : ℝ) 2) := hg.integrableOn
  have hpoint : ∀ x, f x ≤ g x := by
    intro x
    dsimp only [f, g]
    rw [show |x| ^ 2 = x ^ 2 by exact sq_abs x]
    have hexp : 1 - 5 * x ^ 2 / 24 ≤
        Real.exp (-(5 * x ^ 2 / 24)) := by
      convert Real.add_one_le_exp (-(5 * x ^ 2 / 24)) using 1 <;> ring
    exact mul_le_mul_of_nonneg_left hexp (sq_nonneg x)
  have hpoly : (∫ x in Icc (-2 : ℝ) 2, f x) = 8 / 3 := by
    have h2 : IntervalIntegrable (fun x : ℝ => x ^ 2) volume (-2) 2 :=
      (continuous_id.pow 2).intervalIntegrable _ _
    have h4 : IntervalIntegrable (fun x : ℝ => x ^ 4) volume (-2) 2 :=
      (continuous_id.pow 4).intervalIntegrable _ _
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (by norm_num : (-2 : ℝ) ≤ 2)]
    dsimp only [f]
    rw [show (fun x : ℝ => x ^ 2 * (1 - 5 * x ^ 2 / 24)) =
        fun x => x ^ 2 - (5 / 24) * x ^ 4 by funext x; ring,
      intervalIntegral.integral_sub h2 (h4.const_mul (5 / 24)),
      intervalIntegral.integral_const_mul, integral_pow, integral_pow]
    norm_num
  calc
    8 / 3 = ∫ x in Icc (-2 : ℝ) 2, f x := hpoly.symm
    _ ≤ ∫ x in Icc (-2 : ℝ) 2, g x := setIntegral_mono hf hgOn hpoint
    _ ≤ ∫ x : ℝ, g x := setIntegral_le_integral hg
      (ae_of_all _ fun x => by dsimp only [g]; positivity)
    _ = sharpGaussianDampedMoment 2 := rfl

/-- The sharp Gaussian-majorized discrepancy is globally integrable. -/
theorem integrable_sharpGaussianDiscrepancyMajorant (rho : ℝ) (N : ℕ) :
    Integrable (fun t : ℝ => Real.exp (-(5 * t ^ 2 / 24)) *
      (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)))) := by
  have h2 := integrable_abs_pow_mul_rexp_neg_five_twentyfourths_sq 2
  have h3 := integrable_abs_pow_mul_rexp_neg_five_twentyfourths_sq 3
  have hsum :=
    (h2.const_mul (rho / (6 * Real.sqrt (N + 1 : ℝ)))).add
      (h3.const_mul (1 / (8 * (N + 1 : ℝ))))
  convert hsum using 1
  funext t
  simp only [Pi.add_apply]
  ring

/-- Exact decomposition of the integrated sharp Gaussian discrepancy
majorant into two fixed universal moments. -/
theorem integral_sharpGaussianDiscrepancyMajorant (rho : ℝ) (N : ℕ) :
    (∫ t : ℝ, Real.exp (-(5 * t ^ 2 / 24)) *
      (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)))) =
      (rho / (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
        (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3 := by
  have h2 := integrable_abs_pow_mul_rexp_neg_five_twentyfourths_sq 2
  have h3 := integrable_abs_pow_mul_rexp_neg_five_twentyfourths_sq 3
  rw [show (fun t : ℝ => Real.exp (-(5 * t ^ 2 / 24)) *
      (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
        |t| ^ 3 / (8 * (N + 1 : ℝ)))) =
      fun t => (rho / (6 * Real.sqrt (N + 1 : ℝ))) *
          (|t| ^ 2 * Real.exp (-(5 * t ^ 2 / 24))) +
        (1 / (8 * (N + 1 : ℝ))) *
          (|t| ^ 3 * Real.exp (-(5 * t ^ 2 / 24))) by
    funext t
    ring]
  rw [integral_add (h2.const_mul _) (h3.const_mul _),
    integral_const_mul, integral_const_mul]
  rfl

/-- Frequency window on which the cubic characteristic-function remainder is
dominated by the quadratic variance term. -/
def normalizedThirdMomentWindow (rho : ℝ) (N : ℕ) : Set ℝ :=
  {t | |t / Real.sqrt (N + 1 : ℝ)| ≤ 1 ∧
    16 * rho * |t / Real.sqrt (N + 1 : ℝ)| ≤ 1}

theorem measurableSet_normalizedThirdMomentWindow (rho : ℝ) (N : ℕ) :
    MeasurableSet (normalizedThirdMomentWindow rho N) := by
  simp only [normalizedThirdMomentWindow, Set.setOf_and]
  exact (measurableSet_le (by fun_prop) (by fun_prop)).inter
    (measurableSet_le (by fun_prop) (by fun_prop))

/-- The integrated characteristic-function discrepancy on the third-moment
window has the required `rho / sqrt(n)` leading scale, plus a lower-order
`1 / n` Gaussian-proxy term. -/
theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_window_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in normalizedThirdMomentWindow
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N,
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ)) * gaussianDampedMoment 2 +
        (1 / (4 * (N + 1 : ℝ))) * gaussianDampedMoment 3 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let g : ℝ → ℝ := fun t => Real.exp (-t ^ 2 / 8) *
    (4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
      |t| ^ 3 / (4 * (N + 1 : ℝ)))
  let S : Set ℝ := normalizedThirdMomentWindow rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hgNonneg : ∀ t, 0 ≤ g t := fun t => by
    dsimp only [g]
    positivity
  have hg : Integrable g := by
    simpa only [g] using integrable_gaussianDiscrepancyMajorant rho N
  have hqStrong : StronglyMeasurable q := by
    exact stronglyMeasurable_charFunDiscrepancyQuotient law target
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_normalizedThirdMomentWindow rho N
  have hqS : Integrable q (volume.restrict S) := by
    apply Integrable.mono' hg.integrableOn hqStrong.aestronglyMeasurable
    filter_upwards [ae_restrict_mem hS] with t ht
    rw [Real.norm_eq_abs, abs_of_nonneg
      (charFunDiscrepancyQuotient_nonneg law target t)]
    exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian
      X hX hIndep hIdent hMean hSecond hN ht.1 ht.2
  change (∫ t in S, q t) ≤ _
  calc
    (∫ t in S, q t) ≤ ∫ t in S, g t := by
      apply integral_mono_ae hqS hg.integrableOn
      filter_upwards [ae_restrict_mem hS] with t ht
      exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian
        X hX hIndep hIdent hMean hSecond hN ht.1 ht.2
    _ ≤ ∫ t, g t :=
      integral_mono_measure Measure.restrict_le_self
        (ae_of_all volume hgNonneg) hg
    _ = _ := by
      simpa only [g, rho] using integral_gaussianDiscrepancyMajorant rho N

/-- Enlarged frequency window certified by the sharp cubic remainder. -/
def sharpNormalizedThirdMomentWindow (rho : ℝ) (N : ℕ) : Set ℝ :=
  {t | |t / Real.sqrt (N + 1 : ℝ)| ≤ 1 ∧
    2 * rho * |t / Real.sqrt (N + 1 : ℝ)| ≤ 1}

theorem measurableSet_sharpNormalizedThirdMomentWindow (rho : ℝ) (N : ℕ) :
    MeasurableSet (sharpNormalizedThirdMomentWindow rho N) := by
  simp only [sharpNormalizedThirdMomentWindow, Set.setOf_and]
  exact (measurableSet_le (by fun_prop) (by fun_prop)).inter
    (measurableSet_le (by fun_prop) (by fun_prop))

/-- Integrated sharp characteristic-function discrepancy on the enlarged
third-moment window. -/
theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in sharpNormalizedThirdMomentWindow
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N,
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      ((∫ omega, |X 0 omega| ^ 3 ∂mu) /
          (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
        (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let g : ℝ → ℝ := fun t => Real.exp (-(5 * t ^ 2 / 24)) *
    (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
      |t| ^ 3 / (8 * (N + 1 : ℝ)))
  let S : Set ℝ := sharpNormalizedThirdMomentWindow rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hgNonneg : ∀ t, 0 ≤ g t := fun t => by
    dsimp only [g]
    positivity
  have hg : Integrable g := by
    simpa only [g] using integrable_sharpGaussianDiscrepancyMajorant rho N
  have hqStrong : StronglyMeasurable q := by
    exact stronglyMeasurable_charFunDiscrepancyQuotient law target
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_sharpNormalizedThirdMomentWindow rho N
  have hqS : Integrable q (volume.restrict S) := by
    apply Integrable.mono' hg.integrableOn hqStrong.aestronglyMeasurable
    filter_upwards [ae_restrict_mem hS] with t ht
    rw [Real.norm_eq_abs, abs_of_nonneg
      (charFunDiscrepancyQuotient_nonneg law target t)]
    exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_sharp
      X hX hIndep hIdent hMean hSecond hN ht.1 ht.2
  change (∫ t in S, q t) ≤ _
  calc
    (∫ t in S, q t) ≤ ∫ t in S, g t := by
      apply integral_mono_ae hqS hg.integrableOn
      filter_upwards [ae_restrict_mem hS] with t ht
      exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_sharp
        X hX hIndep hIdent hMean hSecond hN ht.1 ht.2
    _ ≤ ∫ t, g t :=
      integral_mono_measure Measure.restrict_le_self
        (ae_of_all volume hgNonneg) hg
    _ = _ := by
      simpa only [g, rho] using integral_sharpGaussianDiscrepancyMajorant rho N

/-- One-sided absorption estimate for a smoothing measure with a controlled
right tail.  Unlike compact-spatial-support smoothing, the tail contribution
is proportional to the unknown Kolmogorov distance; this is the mechanism
used by compact-frequency Esseen kernels. -/
theorem cdf_sub_cdf_le_of_smoothedCDF_with_right_tail
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    (hkappa1 : Integrable (fun y : ℝ => y) kappa)
    {a q m epsilon L : ℝ} (hL : 0 ≤ L)
    (htail : kappa.real (Ioi a) ≤ q)
    (hmoment : (∫ y, |a - y| ∂kappa) ≤ m)
    (hnuLip : ∀ u v, |cdf nu u - cdf nu v| ≤ L * |u - v|)
    (hsmooth : ∀ x,
      |smoothedCDF mu kappa x - smoothedCDF nu kappa x| ≤ epsilon)
    (x : ℝ) :
    cdf mu x - cdf nu x ≤
      epsilon + L * m + 2 * q * kolmogorovDistance mu nu := by
  let d : ℝ := kolmogorovDistance mu nu
  let z : ℝ → ℝ := fun y => x + a - y
  let tail : ℝ → ℝ := (Ioi a).indicator (fun _ => (1 : ℝ))
  have hd : 0 ≤ d := kolmogorovDistance_nonneg mu nu
  have hmomentInt : Integrable (fun y : ℝ => |a - y|) kappa :=
    ((integrable_const a).sub hkappa1).norm
  have htailInt : Integrable tail kappa := by
    exact (integrable_const (1 : ℝ)).indicator measurableSet_Ioi
  have hdiffInt : Integrable
      (fun y => cdf mu (z y) - cdf nu (z y)) kappa := by
    exact (integrable_cdf_translate mu kappa (x + a)).sub
      (integrable_cdf_translate nu kappa (x + a))
  have hmajorInt : Integrable
      (fun y => cdf mu (z y) - cdf nu (z y) +
        L * |a - y| + 2 * d * tail y) kappa :=
    (hdiffInt.add (hmomentInt.const_mul L)).add
      (htailInt.const_mul (2 * d))
  have hpoint : ∀ y,
      cdf mu x - cdf nu x ≤
        cdf mu (z y) - cdf nu (z y) +
          L * |a - y| + 2 * d * tail y := by
    intro y
    by_cases hy : y ≤ a
    · have hmuMono : cdf mu x ≤ cdf mu (z y) := by
        apply monotone_cdf mu
        dsimp only [z]
        linarith
      have hnu := (abs_le.mp (hnuLip (z y) x)).2
      have hz : z y - x = a - y := by
        dsimp only [z]
        ring
      rw [hz] at hnu
      have htailZero : tail y = 0 := by
        simp only [tail, Set.indicator_apply]
        split_ifs with hya
        · exact (not_lt_of_ge hy hya).elim
        · rfl
      rw [htailZero, mul_zero, add_zero]
      linarith
    · have hyTail : y ∈ Ioi a := lt_of_not_ge hy
      have hxGap : cdf mu x - cdf nu x ≤ d := by
        exact (le_abs_self _).trans
          (cdfGap_le_kolmogorovDistance mu nu x)
      have hzGap : -d ≤ cdf mu (z y) - cdf nu (z y) := by
        exact (neg_le_neg
          (cdfGap_le_kolmogorovDistance mu nu (z y))).trans <| by
            simpa only [cdfGap] using neg_abs_le
              (cdf mu (z y) - cdf nu (z y))
      have htailOne : tail y = 1 := by
        simp only [tail, Set.indicator_apply, hyTail, if_true]
      rw [htailOne, mul_one]
      have hmomentNonneg : 0 ≤ L * |a - y| :=
        mul_nonneg hL (abs_nonneg _)
      linarith
  have hintegral :
      cdf mu x - cdf nu x ≤
        ∫ y, (cdf mu (z y) - cdf nu (z y) +
          L * |a - y| + 2 * d * tail y) ∂kappa := by
    calc
      cdf mu x - cdf nu x =
          ∫ _ : ℝ, (cdf mu x - cdf nu x) ∂kappa := by simp
      _ ≤ _ := integral_mono (integrable_const _) hmajorInt hpoint
  calc
    cdf mu x - cdf nu x ≤
        ∫ y, (cdf mu (z y) - cdf nu (z y) +
          L * |a - y| + 2 * d * tail y) ∂kappa := hintegral
    _ = (smoothedCDF mu kappa (x + a) -
          smoothedCDF nu kappa (x + a)) +
        L * (∫ y, |a - y| ∂kappa) +
        2 * d * kappa.real (Ioi a) := by
      calc
        _ = (∫ y, (cdf mu (z y) - cdf nu (z y) +
              L * |a - y|) ∂kappa) +
            ∫ y, 2 * d * tail y ∂kappa :=
          integral_add (hdiffInt.add (hmomentInt.const_mul L))
            (htailInt.const_mul (2 * d))
        _ = ((∫ y, cdf mu (z y) - cdf nu (z y) ∂kappa) +
              ∫ y, L * |a - y| ∂kappa) +
            ∫ y, 2 * d * tail y ∂kappa := by
          rw [integral_add hdiffInt (hmomentInt.const_mul L)]
        _ = (smoothedCDF mu kappa (x + a) -
              smoothedCDF nu kappa (x + a)) +
            L * (∫ y, |a - y| ∂kappa) +
            2 * d * (∫ y, tail y ∂kappa) := by
          rw [integral_sub (integrable_cdf_translate mu kappa (x + a))
              (integrable_cdf_translate nu kappa (x + a)),
            integral_const_mul, integral_const_mul]
          rfl
        _ = _ := by
          rw [show (∫ y, tail y ∂kappa) = kappa.real (Ioi a) by
            exact integral_indicator_one measurableSet_Ioi]
    _ ≤ epsilon + L * m + 2 * q * d := by
      have hs := (le_abs_self _).trans (hsmooth (x + a))
      have hm := mul_le_mul_of_nonneg_left hmoment hL
      have ht : (2 * d) * kappa.real (Ioi a) ≤ (2 * d) * q :=
        mul_le_mul_of_nonneg_left htail
          (mul_nonneg (show (0 : ℝ) ≤ 2 by norm_num) hd)
      nlinarith
    _ = epsilon + L * m + 2 * q * kolmogorovDistance mu nu := by
      rfl

/-- Mirrored one-sided absorption estimate for the left tail of a smoothing
measure. -/
theorem cdf_sub_cdf_le_of_smoothedCDF_with_left_tail
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    (hkappa1 : Integrable (fun y : ℝ => y) kappa)
    {a q m epsilon L : ℝ} (hL : 0 ≤ L)
    (htail : kappa.real (Iio (-a)) ≤ q)
    (hmoment : (∫ y, |-a - y| ∂kappa) ≤ m)
    (hnuLip : ∀ u v, |cdf nu u - cdf nu v| ≤ L * |u - v|)
    (hsmooth : ∀ x,
      |smoothedCDF mu kappa x - smoothedCDF nu kappa x| ≤ epsilon)
    (x : ℝ) :
    cdf nu x - cdf mu x ≤
      epsilon + L * m + 2 * q * kolmogorovDistance mu nu := by
  let d : ℝ := kolmogorovDistance mu nu
  let z : ℝ → ℝ := fun y => x - a - y
  let tail : ℝ → ℝ := (Iio (-a)).indicator (fun _ => (1 : ℝ))
  have hd : 0 ≤ d := kolmogorovDistance_nonneg mu nu
  have hmomentInt : Integrable (fun y : ℝ => |-a - y|) kappa :=
    ((integrable_const (-a)).sub hkappa1).norm
  have htailInt : Integrable tail kappa := by
    exact (integrable_const (1 : ℝ)).indicator measurableSet_Iio
  have hdiffInt : Integrable
      (fun y => cdf nu (z y) - cdf mu (z y)) kappa := by
    exact (integrable_cdf_translate nu kappa (x - a)).sub
      (integrable_cdf_translate mu kappa (x - a))
  have hmajorInt : Integrable
      (fun y => cdf nu (z y) - cdf mu (z y) +
        L * |-a - y| + 2 * d * tail y) kappa :=
    (hdiffInt.add (hmomentInt.const_mul L)).add
      (htailInt.const_mul (2 * d))
  have hpoint : ∀ y,
      cdf nu x - cdf mu x ≤
        cdf nu (z y) - cdf mu (z y) +
          L * |-a - y| + 2 * d * tail y := by
    intro y
    by_cases hy : -a ≤ y
    · have hmuMono : cdf mu (z y) ≤ cdf mu x := by
        apply monotone_cdf mu
        dsimp only [z]
        linarith
      have hnu := (abs_le.mp (hnuLip x (z y))).2
      have hz : x - z y = a + y := by
        dsimp only [z]
        ring
      rw [hz, show |a + y| = |-a - y| by rw [← abs_neg]; congr 1; ring] at hnu
      have htailZero : tail y = 0 := by
        simp only [tail, Set.indicator_apply]
        split_ifs with hya
        · exact (not_lt_of_ge hy hya).elim
        · rfl
      rw [htailZero, mul_zero, add_zero]
      linarith
    · have hyTail : y ∈ Iio (-a) := lt_of_not_ge hy
      have hxGap : cdf nu x - cdf mu x ≤ d := by
        exact (le_abs_self _).trans <| by
          simpa only [cdfGap, abs_sub_comm] using
            cdfGap_le_kolmogorovDistance mu nu x
      have hzGap : -d ≤ cdf nu (z y) - cdf mu (z y) := by
        have hgap := cdfGap_le_kolmogorovDistance mu nu (z y)
        rw [cdfGap, abs_sub_comm] at hgap
        exact (neg_le_neg hgap).trans
          (neg_abs_le (cdf nu (z y) - cdf mu (z y)))
      have htailOne : tail y = 1 := by
        simp only [tail, Set.indicator_apply, hyTail, if_true]
      rw [htailOne, mul_one]
      have hmomentNonneg : 0 ≤ L * |-a - y| :=
        mul_nonneg hL (abs_nonneg _)
      linarith
  have hintegral :
      cdf nu x - cdf mu x ≤
        ∫ y, (cdf nu (z y) - cdf mu (z y) +
          L * |-a - y| + 2 * d * tail y) ∂kappa := by
    calc
      cdf nu x - cdf mu x =
          ∫ _ : ℝ, (cdf nu x - cdf mu x) ∂kappa := by simp
      _ ≤ _ := integral_mono (integrable_const _) hmajorInt hpoint
  calc
    cdf nu x - cdf mu x ≤
        ∫ y, (cdf nu (z y) - cdf mu (z y) +
          L * |-a - y| + 2 * d * tail y) ∂kappa := hintegral
    _ = (smoothedCDF nu kappa (x - a) -
          smoothedCDF mu kappa (x - a)) +
        L * (∫ y, |-a - y| ∂kappa) +
        2 * d * kappa.real (Iio (-a)) := by
      calc
        _ = (∫ y, (cdf nu (z y) - cdf mu (z y) +
              L * |-a - y|) ∂kappa) +
            ∫ y, 2 * d * tail y ∂kappa :=
          integral_add (hdiffInt.add (hmomentInt.const_mul L))
            (htailInt.const_mul (2 * d))
        _ = ((∫ y, cdf nu (z y) - cdf mu (z y) ∂kappa) +
              ∫ y, L * |-a - y| ∂kappa) +
            ∫ y, 2 * d * tail y ∂kappa := by
          rw [integral_add hdiffInt (hmomentInt.const_mul L)]
        _ = (smoothedCDF nu kappa (x - a) -
              smoothedCDF mu kappa (x - a)) +
            L * (∫ y, |-a - y| ∂kappa) +
            2 * d * (∫ y, tail y ∂kappa) := by
          rw [integral_sub (integrable_cdf_translate nu kappa (x - a))
              (integrable_cdf_translate mu kappa (x - a)),
            integral_const_mul, integral_const_mul]
          rfl
        _ = _ := by
          rw [show (∫ y, tail y ∂kappa) = kappa.real (Iio (-a)) by
            exact integral_indicator_one measurableSet_Iio]
    _ ≤ epsilon + L * m + 2 * q * d := by
      have hs : smoothedCDF nu kappa (x - a) -
          smoothedCDF mu kappa (x - a) ≤ epsilon := by
        calc
          smoothedCDF nu kappa (x - a) -
              smoothedCDF mu kappa (x - a) ≤
              |smoothedCDF nu kappa (x - a) -
                smoothedCDF mu kappa (x - a)| := le_abs_self _
          _ = |smoothedCDF mu kappa (x - a) -
                smoothedCDF nu kappa (x - a)| := abs_sub_comm _ _
          _ ≤ epsilon := hsmooth (x - a)
      have hm := mul_le_mul_of_nonneg_left hmoment hL
      have ht : (2 * d) * kappa.real (Iio (-a)) ≤ (2 * d) * q :=
        mul_le_mul_of_nonneg_left htail
          (mul_nonneg (show (0 : ℝ) ≤ 2 by norm_num) hd)
      nlinarith
    _ = epsilon + L * m + 2 * q * kolmogorovDistance mu nu := by
      rfl

/-- Esseen's deterministic absorption principle.  If a common smoothing
measure has both tails bounded by `q < 1/2`, its smoothed CDFs are uniformly
`epsilon`-close, and the target CDF is `L`-Lipschitz, then the unsmoothed
Kolmogorov distance is controlled with the tail loss absorbed into the factor
`(1 - 2q)⁻¹`. -/
theorem kolmogorovDistance_le_of_smoothedCDF_with_tails
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure kappa]
    (hkappa1 : Integrable (fun y : ℝ => y) kappa)
    {a q m epsilon L : ℝ} (hL : 0 ≤ L) (hq : 2 * q < 1)
    (hRightTail : kappa.real (Ioi a) ≤ q)
    (hLeftTail : kappa.real (Iio (-a)) ≤ q)
    (hRightMoment : (∫ y, |a - y| ∂kappa) ≤ m)
    (hLeftMoment : (∫ y, |-a - y| ∂kappa) ≤ m)
    (hnuLip : ∀ u v, |cdf nu u - cdf nu v| ≤ L * |u - v|)
    (hsmooth : ∀ x,
      |smoothedCDF mu kappa x - smoothedCDF nu kappa x| ≤ epsilon) :
    kolmogorovDistance mu nu ≤
      (epsilon + L * m) / (1 - 2 * q) := by
  let d : ℝ := kolmogorovDistance mu nu
  let B : ℝ := epsilon + L * m
  have hpoint : ∀ x, cdfGap mu nu x ≤ B + 2 * q * d := by
    intro x
    rw [cdfGap, abs_le]
    constructor
    · have hleft := cdf_sub_cdf_le_of_smoothedCDF_with_left_tail
          mu nu kappa hkappa1 hL hLeftTail hLeftMoment hnuLip hsmooth x
      dsimp only [B, d]
      linarith
    · exact cdf_sub_cdf_le_of_smoothedCDF_with_right_tail
        mu nu kappa hkappa1 hL hRightTail hRightMoment hnuLip hsmooth x
  have hdBound : d ≤ B + 2 * q * d := by
    dsimp only [d]
    exact (kolmogorovDistance_le_iff mu nu (B + 2 * q * d)).2 hpoint
  have hden : 0 < 1 - 2 * q := by linarith
  rw [le_div_iff₀ hden]
  dsimp only [B, d] at hdBound ⊢
  nlinarith

/-- The deterministic tail-absorption inequality specialized to replacing
one centered variance-one summand by a standard Gaussian after an arbitrary
common convolution and positive scaled-Gaussian smoothing.  The tail and
shifted-moment estimates for the scaled Gaussian remain explicit inputs. -/
theorem kolmogorovDistance_conv_le_gaussianSmoothedReplacement
    (kappa mu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    {σ a q m : ℝ} (hσ : 0 < σ)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu)
    (hgamma1 : Integrable (fun y : ℝ => y)
      (Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
    (hq : 2 * q < 1)
    (hRightTail :
      (Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).real (Ioi a) ≤ q)
    (hLeftTail :
      (Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).real (Iio (-a)) ≤ q)
    (hRightMoment :
      (∫ y, |a - y| ∂(Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤ m)
    (hLeftMoment :
      (∫ y, |-a - y| ∂(Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤ m) :
    kolmogorovDistance (mu ∗ kappa)
        (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ kappa) ≤
      ((((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6 *
          ((∫ y, |y| ^ 3 ∂mu) +
            ∫ y, |y| ^ 3
              ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        (Real.sqrt (2 * Real.pi))⁻¹ * m) / (1 - 2 * q) := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  apply kolmogorovDistance_le_of_smoothedCDF_with_tails
    (mu ∗ kappa)
    (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ kappa)
    gamma (by simpa only [gamma] using hgamma1) (by positivity) hq
    (by simpa only [gamma] using hRightTail)
    (by simpa only [gamma] using hLeftTail)
    (by simpa only [gamma] using hRightMoment)
    (by simpa only [gamma] using hLeftMoment)
  · intro u v
    rw [Measure.conv_comm
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa]
    exact standardNormalCDF_lipschitz_conv kappa u v
  · intro x
    rw [smoothedCDF_eq_cdf_conv, smoothedCDF_eq_cdf_conv]
    have h := abs_cdfUpperError_gaussianSmoothedReplacement_le
      kappa mu hσ x hmu1 hmu2 hmean hsecond hmu3
    rw [cdfUpperError] at h
    have hmuConv : gamma ∗ (mu ∗ kappa) = mu ∗ (kappa ∗ gamma) := by
      rw [← Measure.conv_assoc, Measure.conv_comm gamma mu,
        Measure.conv_assoc, Measure.conv_comm gamma kappa]
    have hnormalConv :
        gamma ∗
            (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ kappa) =
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗
            (kappa ∗ gamma) := by
      rw [← Measure.conv_assoc,
        Measure.conv_comm gamma
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw,
        Measure.conv_assoc, Measure.conv_comm gamma kappa]
    rw [hmuConv, hnormalConv]
    simpa only [gamma] using h

theorem integrable_scaledStandardNormal_id (σ : ℝ) :
    Integrable (fun y : ℝ => y)
      (Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  simpa only [Function.comp_apply] using integrable_standardNormal_id.const_mul σ

theorem integrable_abs_scaledStandardNormal (σ : ℝ) :
    Integrable (fun y : ℝ => |y|)
      (Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) := by
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  exact (integrable_abs_standardNormalLaw.const_mul |σ|).congr <|
    ae_of_all _ fun z => by simp only [Function.comp_apply, abs_mul]

/-- First absolute moment of the standard Gaussian scaled by `σ`. -/
noncomputable def scaledStandardNormalFirstMoment (σ : ℝ) : ℝ :=
  ∫ y, |y| ∂(Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)

theorem scaledStandardNormalFirstMoment_eq (σ : ℝ) :
    scaledStandardNormalFirstMoment σ =
      |σ| * (2 * (Real.sqrt (2 * Real.pi))⁻¹) := by
  unfold scaledStandardNormalFirstMoment
  rw [integral_map (by fun_prop) (by fun_prop)]
  simp only [abs_mul]
  rw [integral_const_mul, integral_standardNormal_abs]

theorem scaledStandardNormalFirstMoment_nonneg (σ : ℝ) :
    0 ≤ scaledStandardNormalFirstMoment σ := by
  unfold scaledStandardNormalFirstMoment
  exact integral_nonneg fun _ => abs_nonneg _

/-- Markov radius at which both tails of a scaled standard Gaussian have
mass strictly below one quarter. -/
noncomputable def scaledStandardNormalTailRadius (σ : ℝ) : ℝ :=
  4 * scaledStandardNormalFirstMoment σ + 1

theorem scaledStandardNormalTailRadius_pos (σ : ℝ) :
    0 < scaledStandardNormalTailRadius σ := by
  unfold scaledStandardNormalTailRadius
  exact add_pos_of_nonneg_of_pos
    (mul_nonneg (by norm_num) (scaledStandardNormalFirstMoment_nonneg σ))
    zero_lt_one

theorem scaledStandardNormal_rightTail_lt_quarter (σ : ℝ) :
    (Measure.map (fun z : ℝ => σ * z)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).real
        (Ioi (scaledStandardNormalTailRadius σ)) < 1 / 4 := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let M : ℝ := scaledStandardNormalFirstMoment σ
  let a : ℝ := scaledStandardNormalTailRadius σ
  have hM : 0 ≤ M := scaledStandardNormalFirstMoment_nonneg σ
  have ha : 0 < a := scaledStandardNormalTailRadius_pos σ
  have hMarkov : a * gamma.real {x : ℝ | a ≤ |x|} ≤ M := by
    simpa only [M, a, gamma, scaledStandardNormalFirstMoment] using
      mul_meas_ge_le_integral_of_nonneg
        (ae_of_all gamma fun x => abs_nonneg x)
        (by simpa only [gamma] using integrable_abs_scaledStandardNormal σ) a
  have hratio : M / a < 1 / 4 := by
    rw [div_lt_iff₀ ha]
    dsimp only [a, scaledStandardNormalTailRadius, M]
    nlinarith
  have hradial : gamma.real {x : ℝ | a ≤ |x|} ≤ M / a := by
    rw [le_div_iff₀ ha]
    simpa only [mul_comm] using hMarkov
  have hsubset : Ioi a ⊆ {x : ℝ | a ≤ |x|} := by
    intro x hx
    exact hx.le.trans (le_abs_self x)
  simpa only [gamma, a] using
    (measureReal_mono hsubset).trans_lt (hradial.trans_lt hratio)

theorem scaledStandardNormal_leftTail_lt_quarter (σ : ℝ) :
    (Measure.map (fun z : ℝ => σ * z)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).real
        (Iio (-scaledStandardNormalTailRadius σ)) < 1 / 4 := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let M : ℝ := scaledStandardNormalFirstMoment σ
  let a : ℝ := scaledStandardNormalTailRadius σ
  have hM : 0 ≤ M := scaledStandardNormalFirstMoment_nonneg σ
  have ha : 0 < a := scaledStandardNormalTailRadius_pos σ
  have hMarkov : a * gamma.real {x : ℝ | a ≤ |x|} ≤ M := by
    simpa only [M, a, gamma, scaledStandardNormalFirstMoment] using
      mul_meas_ge_le_integral_of_nonneg
        (ae_of_all gamma fun x => abs_nonneg x)
        (by simpa only [gamma] using integrable_abs_scaledStandardNormal σ) a
  have hratio : M / a < 1 / 4 := by
    rw [div_lt_iff₀ ha]
    dsimp only [a, scaledStandardNormalTailRadius, M]
    nlinarith
  have hradial : gamma.real {x : ℝ | a ≤ |x|} ≤ M / a := by
    rw [le_div_iff₀ ha]
    simpa only [mul_comm] using hMarkov
  have hsubset : Iio (-a) ⊆ {x : ℝ | a ≤ |x|} := by
    intro x hx
    have hx' : x < -a := hx
    have hxneg : x < 0 := hx.trans_le (neg_nonpos.mpr ha.le)
    change a ≤ |x|
    rw [abs_of_neg hxneg]
    linarith [hx']
  simpa only [gamma, a] using
    (measureReal_mono hsubset).trans_lt (hradial.trans_lt hratio)

/-- Common bound for both shifted first moments at the scaled-Gaussian
Markov radius. -/
noncomputable def scaledStandardNormalShiftedMomentBound (σ : ℝ) : ℝ :=
  scaledStandardNormalTailRadius σ + scaledStandardNormalFirstMoment σ

theorem scaledStandardNormal_rightShiftedMoment_le (σ : ℝ) :
    (∫ y, |scaledStandardNormalTailRadius σ - y|
      ∂(Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤
      scaledStandardNormalShiftedMomentBound σ := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  have habs : Integrable (fun y : ℝ => |y|) gamma := by
    simpa only [gamma] using integrable_abs_scaledStandardNormal σ
  have hid : Integrable (fun y : ℝ => y) gamma := by
    simpa only [gamma] using integrable_scaledStandardNormal_id σ
  have hLeftInt : Integrable (fun y : ℝ =>
      |scaledStandardNormalTailRadius σ - y|) gamma := by
    simpa only [Pi.sub_apply, Real.norm_eq_abs] using
      ((integrable_const (scaledStandardNormalTailRadius σ)).sub hid).norm
  have hInt : Integrable (fun y : ℝ =>
      scaledStandardNormalTailRadius σ + |y|) gamma :=
    (integrable_const _).add habs
  calc
    (∫ y, |scaledStandardNormalTailRadius σ - y| ∂gamma) ≤
        ∫ y, (scaledStandardNormalTailRadius σ + |y|) ∂gamma := by
      apply integral_mono hLeftInt hInt
      intro y
      exact (abs_sub _ _).trans_eq <| by
        rw [abs_of_pos (scaledStandardNormalTailRadius_pos σ)]
    _ = scaledStandardNormalShiftedMomentBound σ := by
      simp [integral_add (integrable_const _) habs,
        scaledStandardNormalShiftedMomentBound,
        scaledStandardNormalFirstMoment, gamma]

theorem scaledStandardNormal_leftShiftedMoment_le (σ : ℝ) :
    (∫ y, |-scaledStandardNormalTailRadius σ - y|
      ∂(Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤
      scaledStandardNormalShiftedMomentBound σ := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  have habs : Integrable (fun y : ℝ => |y|) gamma := by
    simpa only [gamma] using integrable_abs_scaledStandardNormal σ
  have hid : Integrable (fun y : ℝ => y) gamma := by
    simpa only [gamma] using integrable_scaledStandardNormal_id σ
  have hLeftInt : Integrable (fun y : ℝ =>
      |-scaledStandardNormalTailRadius σ - y|) gamma := by
    simpa only [Pi.sub_apply, Real.norm_eq_abs] using
      ((integrable_const (-scaledStandardNormalTailRadius σ)).sub hid).norm
  have hRightInt : Integrable (fun y : ℝ =>
      scaledStandardNormalTailRadius σ + |y|) gamma :=
    (integrable_const _).add habs
  calc
    (∫ y, |-scaledStandardNormalTailRadius σ - y| ∂gamma) ≤
        ∫ y, (scaledStandardNormalTailRadius σ + |y|) ∂gamma := by
      apply integral_mono hLeftInt hRightInt
      intro y
      calc
        |-scaledStandardNormalTailRadius σ - y| ≤
            |-scaledStandardNormalTailRadius σ| + |y| := abs_sub _ _
        _ = scaledStandardNormalTailRadius σ + |y| := by
          rw [abs_neg, abs_of_pos (scaledStandardNormalTailRadius_pos σ)]
    _ = scaledStandardNormalShiftedMomentBound σ := by
      simp [integral_add (integrable_const _) habs,
        scaledStandardNormalShiftedMomentBound,
        scaledStandardNormalFirstMoment, gamma]

/-- Fully concrete Markov-tail specialization of the Gaussian-smoothed
one-summand replacement inequality. -/
theorem kolmogorovDistance_conv_le_gaussianSmoothedReplacement_markov
    (kappa mu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    {σ : ℝ} (hσ : 0 < σ)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    kolmogorovDistance (mu ∗ kappa)
        (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ kappa) ≤
      ((((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6 *
          ((∫ y, |y| ^ 3 ∂mu) +
            ∫ y, |y| ^ 3
              ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          scaledStandardNormalShiftedMomentBound σ) /
        (1 - 2 * (1 / 4)) := by
  exact kolmogorovDistance_conv_le_gaussianSmoothedReplacement
    kappa mu hσ hmu1 hmu2 hmean hsecond hmu3
    (integrable_scaledStandardNormal_id σ) (by norm_num)
    (le_of_lt (scaledStandardNormal_rightTail_lt_quarter σ))
    (le_of_lt (scaledStandardNormal_leftTail_lt_quarter σ))
    (scaledStandardNormal_rightShiftedMoment_le σ)
    (scaledStandardNormal_leftShiftedMoment_le σ)

/-- Homogeneous Markov radius for a positively scaled standard Gaussian. -/
noncomputable def positiveScaledStandardNormalTailRadius (σ : ℝ) : ℝ :=
  5 * scaledStandardNormalFirstMoment σ

/-- Common shifted-moment bound at the homogeneous Markov radius. -/
noncomputable def positiveScaledStandardNormalShiftedMomentBound (σ : ℝ) : ℝ :=
  6 * scaledStandardNormalFirstMoment σ

theorem positiveScaledStandardNormalShiftedMomentBound_eq
    {σ : ℝ} (hσ : 0 < σ) :
    positiveScaledStandardNormalShiftedMomentBound σ =
      12 * σ * (Real.sqrt (2 * Real.pi))⁻¹ := by
  rw [positiveScaledStandardNormalShiftedMomentBound,
    scaledStandardNormalFirstMoment_eq, abs_of_pos hσ]
  ring

theorem scaledStandardNormalFirstMoment_pos {σ : ℝ} (hσ : 0 < σ) :
    0 < scaledStandardNormalFirstMoment σ := by
  rw [scaledStandardNormalFirstMoment_eq, abs_of_pos hσ]
  positivity

theorem positiveScaledStandardNormalTailRadius_pos
    {σ : ℝ} (hσ : 0 < σ) :
    0 < positiveScaledStandardNormalTailRadius σ := by
  unfold positiveScaledStandardNormalTailRadius
  exact mul_pos (by norm_num) (scaledStandardNormalFirstMoment_pos hσ)

theorem positiveScaledStandardNormal_rightTail_le_one_fifth
    {σ : ℝ} (hσ : 0 < σ) :
    (Measure.map (fun z : ℝ => σ * z)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).real
        (Ioi (positiveScaledStandardNormalTailRadius σ)) ≤ 1 / 5 := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let M : ℝ := scaledStandardNormalFirstMoment σ
  let a : ℝ := positiveScaledStandardNormalTailRadius σ
  have hM : 0 < M := scaledStandardNormalFirstMoment_pos hσ
  have ha : 0 < a := positiveScaledStandardNormalTailRadius_pos hσ
  have hMarkov : a * gamma.real {x : ℝ | a ≤ |x|} ≤ M := by
    simpa only [M, a, gamma, scaledStandardNormalFirstMoment] using
      mul_meas_ge_le_integral_of_nonneg
        (ae_of_all gamma fun x => abs_nonneg x)
        (by simpa only [gamma] using integrable_abs_scaledStandardNormal σ) a
  have hradial : gamma.real {x : ℝ | a ≤ |x|} ≤ 1 / 5 := by
    calc
      gamma.real {x : ℝ | a ≤ |x|} ≤ M / a := by
        rw [le_div_iff₀ ha]
        simpa only [mul_comm] using hMarkov
      _ = 1 / 5 := by
        dsimp only [a, positiveScaledStandardNormalTailRadius, M]
        field_simp [(scaledStandardNormalFirstMoment_pos hσ).ne']
  have hsubset : Ioi a ⊆ {x : ℝ | a ≤ |x|} := by
    intro x hx
    exact hx.le.trans (le_abs_self x)
  simpa only [gamma, a] using (measureReal_mono hsubset).trans hradial

theorem positiveScaledStandardNormal_leftTail_le_one_fifth
    {σ : ℝ} (hσ : 0 < σ) :
    (Measure.map (fun z : ℝ => σ * z)
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw).real
        (Iio (-positiveScaledStandardNormalTailRadius σ)) ≤ 1 / 5 := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  let M : ℝ := scaledStandardNormalFirstMoment σ
  let a : ℝ := positiveScaledStandardNormalTailRadius σ
  have hM : 0 < M := scaledStandardNormalFirstMoment_pos hσ
  have ha : 0 < a := positiveScaledStandardNormalTailRadius_pos hσ
  have hMarkov : a * gamma.real {x : ℝ | a ≤ |x|} ≤ M := by
    simpa only [M, a, gamma, scaledStandardNormalFirstMoment] using
      mul_meas_ge_le_integral_of_nonneg
        (ae_of_all gamma fun x => abs_nonneg x)
        (by simpa only [gamma] using integrable_abs_scaledStandardNormal σ) a
  have hradial : gamma.real {x : ℝ | a ≤ |x|} ≤ 1 / 5 := by
    calc
      gamma.real {x : ℝ | a ≤ |x|} ≤ M / a := by
        rw [le_div_iff₀ ha]
        simpa only [mul_comm] using hMarkov
      _ = 1 / 5 := by
        dsimp only [a, positiveScaledStandardNormalTailRadius, M]
        field_simp [(scaledStandardNormalFirstMoment_pos hσ).ne']
  have hsubset : Iio (-a) ⊆ {x : ℝ | a ≤ |x|} := by
    intro x hx
    have hx' : x < -a := hx
    have hxneg : x < 0 := hx.trans_le (neg_nonpos.mpr ha.le)
    change a ≤ |x|
    rw [abs_of_neg hxneg]
    linarith [hx']
  simpa only [gamma, a] using (measureReal_mono hsubset).trans hradial

theorem positiveScaledStandardNormal_rightShiftedMoment_le
    {σ : ℝ} (hσ : 0 < σ) :
    (∫ y, |positiveScaledStandardNormalTailRadius σ - y|
      ∂(Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤
      positiveScaledStandardNormalShiftedMomentBound σ := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  have habs : Integrable (fun y : ℝ => |y|) gamma := by
    simpa only [gamma] using integrable_abs_scaledStandardNormal σ
  have hid : Integrable (fun y : ℝ => y) gamma := by
    simpa only [gamma] using integrable_scaledStandardNormal_id σ
  have hLeftInt : Integrable (fun y : ℝ =>
      |positiveScaledStandardNormalTailRadius σ - y|) gamma := by
    simpa only [Pi.sub_apply, Real.norm_eq_abs] using
      ((integrable_const (positiveScaledStandardNormalTailRadius σ)).sub hid).norm
  have hRightInt : Integrable (fun y : ℝ =>
      positiveScaledStandardNormalTailRadius σ + |y|) gamma :=
    (integrable_const _).add habs
  calc
    (∫ y, |positiveScaledStandardNormalTailRadius σ - y| ∂gamma) ≤
        ∫ y, (positiveScaledStandardNormalTailRadius σ + |y|) ∂gamma := by
      apply integral_mono hLeftInt hRightInt
      intro y
      exact (abs_sub _ _).trans_eq <| by
        rw [abs_of_pos (positiveScaledStandardNormalTailRadius_pos hσ)]
    _ = positiveScaledStandardNormalShiftedMomentBound σ := by
      simp [integral_add (integrable_const _) habs,
        positiveScaledStandardNormalTailRadius,
        positiveScaledStandardNormalShiftedMomentBound,
        scaledStandardNormalFirstMoment, gamma]
      ring

theorem positiveScaledStandardNormal_leftShiftedMoment_le
    {σ : ℝ} (hσ : 0 < σ) :
    (∫ y, |-positiveScaledStandardNormalTailRadius σ - y|
      ∂(Measure.map (fun z : ℝ => σ * z)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) ≤
      positiveScaledStandardNormalShiftedMomentBound σ := by
  let gamma : Measure ℝ := Measure.map (fun z : ℝ => σ * z)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  letI : IsProbabilityMeasure gamma := Measure.isProbabilityMeasure_map (by fun_prop)
  have habs : Integrable (fun y : ℝ => |y|) gamma := by
    simpa only [gamma] using integrable_abs_scaledStandardNormal σ
  have hid : Integrable (fun y : ℝ => y) gamma := by
    simpa only [gamma] using integrable_scaledStandardNormal_id σ
  have hLeftInt : Integrable (fun y : ℝ =>
      |-positiveScaledStandardNormalTailRadius σ - y|) gamma := by
    simpa only [Pi.sub_apply, Real.norm_eq_abs] using
      ((integrable_const (-positiveScaledStandardNormalTailRadius σ)).sub hid).norm
  have hRightInt : Integrable (fun y : ℝ =>
      positiveScaledStandardNormalTailRadius σ + |y|) gamma :=
    (integrable_const _).add habs
  calc
    (∫ y, |-positiveScaledStandardNormalTailRadius σ - y| ∂gamma) ≤
        ∫ y, (positiveScaledStandardNormalTailRadius σ + |y|) ∂gamma := by
      apply integral_mono hLeftInt hRightInt
      intro y
      calc
        |-positiveScaledStandardNormalTailRadius σ - y| ≤
            |-positiveScaledStandardNormalTailRadius σ| + |y| := abs_sub _ _
        _ = positiveScaledStandardNormalTailRadius σ + |y| := by
          rw [abs_neg, abs_of_pos
            (positiveScaledStandardNormalTailRadius_pos hσ)]
    _ = positiveScaledStandardNormalShiftedMomentBound σ := by
      simp [integral_add (integrable_const _) habs,
        positiveScaledStandardNormalTailRadius,
        positiveScaledStandardNormalShiftedMomentBound,
        scaledStandardNormalFirstMoment, gamma]
      ring

/-- Homogeneous refinement of the concrete Markov replacement inequality,
using radius `5M`, shifted bound `6M`, and tail parameter `1/5`. -/
theorem kolmogorovDistance_conv_le_gaussianSmoothedReplacement_markov_homogeneous
    (kappa mu : Measure ℝ)
    [IsProbabilityMeasure kappa] [IsProbabilityMeasure mu]
    {σ : ℝ} (hσ : 0 < σ)
    (hmu1 : Integrable (fun y : ℝ => y) mu)
    (hmu2 : Integrable (fun y : ℝ => y ^ 2) mu)
    (hmean : (∫ y, y ∂mu) = 0)
    (hsecond : (∫ y, y ^ 2 ∂mu) = 1)
    (hmu3 : Integrable (fun y : ℝ => |y| ^ 3) mu) :
    kolmogorovDistance (mu ∗ kappa)
        (NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ∗ kappa) ≤
      ((((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6 *
          ((∫ y, |y| ^ 3 ∂mu) +
            ∫ y, |y| ^ 3
              ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          positiveScaledStandardNormalShiftedMomentBound σ) /
        (1 - 2 * (1 / 5)) := by
  exact kolmogorovDistance_conv_le_gaussianSmoothedReplacement
    kappa mu hσ hmu1 hmu2 hmean hsecond hmu3
    (integrable_scaledStandardNormal_id σ) (by norm_num)
    (positiveScaledStandardNormal_rightTail_le_one_fifth hσ)
    (positiveScaledStandardNormal_leftTail_le_one_fifth hσ)
    (positiveScaledStandardNormal_rightShiftedMoment_le hσ)
    (positiveScaledStandardNormal_leftShiftedMoment_le hσ)

/-- The normalized coefficient-one recurrence with its replacement term
fully bounded by the concrete scaled-Gaussian Markov specialization. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_succ_le_replacement_markov
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ)
    (hMean : (∫ omega, X (N + 1) omega ∂mu) = 0)
    (hSecond : (∫ omega, (X (N + 1) omega) ^ 2 ∂mu) = 1)
    {σ : ℝ} (hσ : 0 < σ) :
    kolmogorovDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6 *
          ((∫ omega, |X (N + 1) omega| ^ 3 ∂mu) +
            ∫ y, |y| ^ 3
              ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          scaledStandardNormalShiftedMomentBound σ) /
        (1 - 2 * (1 / 4)) +
        kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let Sprev : Omega → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  let lastLaw : Measure ℝ := Measure.map (X (N + 1)) mu
  let previousLaw : Measure ℝ := Measure.map Sprev mu
  have hXAEM : ∀ i, AEMeasurable (X i) mu := fun i => (hX i).aemeasurable
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure lastLaw :=
    Measure.isProbabilityMeasure_map (hX (N + 1)).aemeasurable
  letI : IsProbabilityMeasure previousLaw :=
    Measure.isProbabilityMeasure_map hSprev
  have hlast1 : Integrable (fun y : ℝ => y) lastLaw := by
    dsimp only [lastLaw]
    rw [integrable_map_measure (by fun_prop) (hX (N + 1)).aemeasurable]
    exact (hX (N + 1)).integrable (by norm_num)
  have hlast2 : Integrable (fun y : ℝ => y ^ 2) lastLaw := by
    dsimp only [lastLaw]
    rw [integrable_map_measure (by fun_prop) (hX (N + 1)).aemeasurable]
    exact ((hX (N + 1)).mono_exponent
      (by norm_num : (2 : ENNReal) ≤ 3)).integrable_sq
  have hlast3 : Integrable (fun y : ℝ => |y| ^ 3) lastLaw := by
    dsimp only [lastLaw]
    rw [integrable_map_measure (by fun_prop) (hX (N + 1)).aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hX (N + 1)).integrable_norm_pow (by norm_num)
  have hlastMean : (∫ y, y ∂lastLaw) = 0 := by
    dsimp only [lastLaw]
    rw [integral_map (hX (N + 1)).aemeasurable (by fun_prop)]
    exact hMean
  have hlastSecond : (∫ y, y ^ 2 ∂lastLaw) = 1 := by
    dsimp only [lastLaw]
    rw [integral_map (hX (N + 1)).aemeasurable (by fun_prop)]
    exact hSecond
  have hlastThird :
      (∫ y, |y| ^ 3 ∂lastLaw) =
        ∫ omega, |X (N + 1) omega| ^ 3 ∂mu := by
    dsimp only [lastLaw]
    rw [integral_map (hX (N + 1)).aemeasurable (by fun_prop)]
  have hrec :=
    kolmogorovDistance_normalizedCenteredIidSum_succ_le_unscaledReplacement
      X hXAEM hIndep N
  have hreplace :=
    kolmogorovDistance_conv_le_gaussianSmoothedReplacement_markov
      previousLaw lastLaw hσ hlast1 hlast2 hlastMean hlastSecond hlast3
  rw [hlastThird] at hreplace
  exact hrec.trans (add_le_add hreplace le_rfl)

/-- The normalized coefficient-one recurrence with the homogeneous
scaled-Gaussian Markov replacement term. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_succ_le_replacement_markov_homogeneous
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu) (N : ℕ)
    (hMean : (∫ omega, X (N + 1) omega ∂mu) = 0)
    (hSecond : (∫ omega, (X (N + 1) omega) ^ 2 ∂mu) = 1)
    {σ : ℝ} (hσ : 0 < σ) :
    kolmogorovDistance
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
            X (N + 1)) mu)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((((3 * (Real.sqrt (2 * Real.pi))⁻¹) / σ ^ 3) / 6 *
          ((∫ omega, |X (N + 1) omega| ^ 3 ∂mu) +
            ∫ y, |y| ^ 3
              ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          positiveScaledStandardNormalShiftedMomentBound σ) /
        (1 - 2 * (1 / 5)) +
        kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let Sprev : Omega → ℝ := ∑ i ∈ Finset.range (N + 1), X i
  let lastLaw : Measure ℝ := Measure.map (X (N + 1)) mu
  let previousLaw : Measure ℝ := Measure.map Sprev mu
  have hXAEM : ∀ i, AEMeasurable (X i) mu := fun i => (hX i).aemeasurable
  have hSprev : AEMeasurable Sprev mu := by
    dsimp only [Sprev]
    fun_prop
  letI : IsProbabilityMeasure lastLaw :=
    Measure.isProbabilityMeasure_map (hX (N + 1)).aemeasurable
  letI : IsProbabilityMeasure previousLaw :=
    Measure.isProbabilityMeasure_map hSprev
  have hlast1 : Integrable (fun y : ℝ => y) lastLaw := by
    dsimp only [lastLaw]
    rw [integrable_map_measure (by fun_prop) (hX (N + 1)).aemeasurable]
    exact (hX (N + 1)).integrable (by norm_num)
  have hlast2 : Integrable (fun y : ℝ => y ^ 2) lastLaw := by
    dsimp only [lastLaw]
    rw [integrable_map_measure (by fun_prop) (hX (N + 1)).aemeasurable]
    exact ((hX (N + 1)).mono_exponent
      (by norm_num : (2 : ENNReal) ≤ 3)).integrable_sq
  have hlast3 : Integrable (fun y : ℝ => |y| ^ 3) lastLaw := by
    dsimp only [lastLaw]
    rw [integrable_map_measure (by fun_prop) (hX (N + 1)).aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hX (N + 1)).integrable_norm_pow (by norm_num)
  have hlastMean : (∫ y, y ∂lastLaw) = 0 := by
    dsimp only [lastLaw]
    rw [integral_map (hX (N + 1)).aemeasurable (by fun_prop)]
    exact hMean
  have hlastSecond : (∫ y, y ^ 2 ∂lastLaw) = 1 := by
    dsimp only [lastLaw]
    rw [integral_map (hX (N + 1)).aemeasurable (by fun_prop)]
    exact hSecond
  have hlastThird :
      (∫ y, |y| ^ 3 ∂lastLaw) =
        ∫ omega, |X (N + 1) omega| ^ 3 ∂mu := by
    dsimp only [lastLaw]
    rw [integral_map (hX (N + 1)).aemeasurable (by fun_prop)]
  have hrec :=
    kolmogorovDistance_normalizedCenteredIidSum_succ_le_unscaledReplacement
      X hXAEM hIndep N
  have hreplace :=
    kolmogorovDistance_conv_le_gaussianSmoothedReplacement_markov_homogeneous
      previousLaw lastLaw hσ hlast1 hlast2 hlastMean hlastSecond hlast3
  rw [hlastThird] at hreplace
  exact hrec.trans (add_le_add hreplace le_rfl)

/-- A probability smoothing measure is band-limited at frequency `T` when
its characteristic function vanishes strictly outside `[-T, T]`. -/
def IsCharFunSupportedInFrequencyWindow (kappa : Measure ℝ) (T : ℝ) : Prop :=
  ∀ t, T < |t| → MeasureTheory.charFun kappa t = 0

/-- A common band-limited convolution turns the locally integrable
characteristic-function discrepancy quotient into a globally integrable one.
Outside the frequency window the common multiplier is exactly zero. -/
theorem integrable_charFunDiscrepancyQuotient_conv_of_bandLimited
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure nu] [IsProbabilityMeasure kappa]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    {T : ℝ} (hT : 0 ≤ T)
    (hkappa : IsCharFunSupportedInFrequencyWindow kappa T) :
    Integrable (charFunDiscrepancyQuotient
      (kappa ∗ mu) (kappa ∗ nu)) volume := by
  let S : Set ℝ := Icc (-T) T
  let q : ℝ → ℝ := charFunDiscrepancyQuotient mu nu
  have hTorder : -T ≤ T := by linarith
  have hqOn : IntegrableOn q S := by
    dsimp only [q, S]
    exact (intervalIntegrable_iff_integrableOn_Icc_of_le hTorder).mp
      (intervalIntegrable_charFunDiscrepancyQuotient hmu1 hnu1 (-T) T)
  have hmajor : Integrable (S.indicator q) volume :=
    hqOn.integrable_indicator measurableSet_Icc
  refine hmajor.mono'
    (stronglyMeasurable_charFunDiscrepancyQuotient
      (kappa ∗ mu) (kappa ∗ nu)).aestronglyMeasurable
    (ae_of_all volume fun t => ?_)
  rw [Real.norm_eq_abs, abs_of_nonneg
    (charFunDiscrepancyQuotient_nonneg (kappa ∗ mu) (kappa ∗ nu) t)]
  by_cases ht : t ∈ S
  · rw [Set.indicator_of_mem ht]
    exact charFunDiscrepancyQuotient_conv_le mu nu kappa t
  · simp only [Set.indicator_apply, ht, if_false]
    have htOutside : T < |t| := by
      by_contra hnot
      have habs : |t| ≤ T := le_of_not_gt hnot
      have hmem : t ∈ Icc (-T) T := (abs_le.mp habs)
      exact ht (by simpa only [S] using hmem)
    have hchar : MeasureTheory.charFun kappa t = 0 := hkappa t htOutside
    unfold charFunDiscrepancyQuotient
    split_ifs
    · rfl
    · rw [MeasureTheory.charFun_conv, MeasureTheory.charFun_conv, hchar]
      simp

/-- Convolution by an absolutely continuous probability kernel gives every
smoothed probability law an absolutely continuous, hence continuous, CDF. -/
theorem continuous_cdf_smoothingKernel_conv
    (mu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure kappa] (hkappa : kappa ≪ volume) :
    Continuous (cdf (kappa ∗ mu)) := by
  apply continuous_cdf_of_absolutelyContinuous
  rw [Measure.conv_comm]
  exact Measure.conv_absolutelyContinuous hkappa

/-- Fourier inversion for a common absolutely continuous smoothing kernel,
before the exact `-2π` frequency rescaling. -/
theorem cdfGap_smoothingKernel_conv_le_integral_discrepancy
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure nu] [IsProbabilityMeasure kappa]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    (hkappa1 : Integrable (fun x : ℝ => |x|) kappa)
    (hkappaAC : kappa ≪ volume)
    (hquot : Integrable (charFunDiscrepancyQuotient
      (kappa ∗ mu) (kappa ∗ nu)) volume)
    (x : ℝ) :
    cdfGap (kappa ∗ mu) (kappa ∗ nu) x ≤
      ∫ w : ℝ, charFunDiscrepancyQuotient
        (kappa ∗ mu) (kappa ∗ nu) ((-2 * Real.pi) * w) := by
  have hmuConv1 := integrable_abs_conv_of_firstMoments
    kappa mu hkappa1 hmu1
  have hnuConv1 := integrable_abs_conv_of_firstMoments
    kappa nu hkappa1 hnu1
  let f : ℝ → ℂ := fun y =>
    ((cdf (kappa ∗ mu) y - cdf (kappa ∗ nu) y : ℝ) : ℂ)
  have hf : Integrable f :=
    (integrable_cdf_sub_cdf_of_firstMoments
      (kappa ∗ mu) (kappa ∗ nu) hmuConv1 hnuConv1).ofReal
  have hFourier : Integrable (𝓕 f) := by
    exact integrable_fourier_cdf_sub_cdf_of_integrable_discrepancy
      (kappa ∗ mu) (kappa ∗ nu) hmuConv1 hnuConv1 hquot
  have hcont : Continuous f := by
    apply Complex.continuous_ofReal.comp
    exact (continuous_cdf_smoothingKernel_conv mu kappa hkappaAC).sub
      (continuous_cdf_smoothingKernel_conv nu kappa hkappaAC)
  have hinv : ‖f x‖ ≤ ∫ w : ℝ, ‖𝓕 f w‖ :=
    norm_le_integral_norm_fourier f hf hFourier (x := x) hcont.continuousAt
  calc
    cdfGap (kappa ∗ mu) (kappa ∗ nu) x = ‖f x‖ := by
      simp only [cdfGap, f, Complex.norm_real, Real.norm_eq_abs]
    _ ≤ ∫ w : ℝ, ‖𝓕 f w‖ := hinv
    _ = ∫ w : ℝ, charFunDiscrepancyQuotient
        (kappa ∗ mu) (kappa ∗ nu) ((-2 * Real.pi) * w) := by
      apply integral_congr_ae
      filter_upwards [volume.ae_ne (0 : ℝ)] with w hw
      dsimp only [f]
      convert norm_fourier_cdf_sub_cdf_eq_charFunDiscrepancyQuotient
        (kappa ∗ mu) (kappa ∗ nu) hmuConv1 hnuConv1 hw using 1 <;> ring

/-- The generic common-kernel inversion estimate after the exact `-2π`
change of variables. -/
theorem cdfGap_smoothingKernel_conv_le_integral_discrepancy_scaled
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure nu] [IsProbabilityMeasure kappa]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    (hkappa1 : Integrable (fun x : ℝ => |x|) kappa)
    (hkappaAC : kappa ≪ volume)
    (hquot : Integrable (charFunDiscrepancyQuotient
      (kappa ∗ mu) (kappa ∗ nu)) volume)
    (x : ℝ) :
    cdfGap (kappa ∗ mu) (kappa ∗ nu) x ≤
      (2 * Real.pi)⁻¹ * ∫ t : ℝ, charFunDiscrepancyQuotient
        (kappa ∗ mu) (kappa ∗ nu) t := by
  calc
    cdfGap (kappa ∗ mu) (kappa ∗ nu) x ≤
      ∫ w : ℝ, charFunDiscrepancyQuotient
        (kappa ∗ mu) (kappa ∗ nu) ((-2 * Real.pi) * w) :=
      cdfGap_smoothingKernel_conv_le_integral_discrepancy
        mu nu kappa hmu1 hnu1 hkappa1 hkappaAC hquot x
    _ = (2 * Real.pi)⁻¹ * ∫ t : ℝ, charFunDiscrepancyQuotient
        (kappa ∗ mu) (kappa ∗ nu) t := by
      rw [Measure.integral_comp_mul_left]
      have hpi : 0 < 2 * Real.pi := mul_pos (by norm_num) Real.pi_pos
      rw [show |(-2 * Real.pi)⁻¹| = (2 * Real.pi)⁻¹ by
        rw [abs_inv, show -2 * Real.pi = -(2 * Real.pi) by ring,
          abs_neg, abs_of_pos hpi]]
      rfl

/-- For a band-limited smoothing kernel, Fourier inversion only sees the
unsmoothed discrepancy quotient on the certified compact frequency window. -/
theorem cdfGap_bandLimitedSmoothingKernel_conv_le_local_discrepancy
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure nu] [IsProbabilityMeasure kappa]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    (hkappa1 : Integrable (fun x : ℝ => |x|) kappa)
    (hkappaAC : kappa ≪ volume)
    {T : ℝ} (hT : 0 ≤ T)
    (hkappa : IsCharFunSupportedInFrequencyWindow kappa T)
    (x : ℝ) :
    cdfGap (kappa ∗ mu) (kappa ∗ nu) x ≤
      (2 * Real.pi)⁻¹ *
        ∫ t in Icc (-T) T, charFunDiscrepancyQuotient mu nu t := by
  let S : Set ℝ := Icc (-T) T
  let q : ℝ → ℝ := charFunDiscrepancyQuotient mu nu
  let qk : ℝ → ℝ := charFunDiscrepancyQuotient
    (kappa ∗ mu) (kappa ∗ nu)
  have hqk : Integrable qk := by
    simpa only [qk] using
      integrable_charFunDiscrepancyQuotient_conv_of_bandLimited
        mu nu kappa hmu1 hnu1 hT hkappa
  have hTorder : -T ≤ T := by linarith
  have hqOn : IntegrableOn q S := by
    dsimp only [q, S]
    exact (intervalIntegrable_iff_integrableOn_Icc_of_le hTorder).mp
      (intervalIntegrable_charFunDiscrepancyQuotient hmu1 hnu1 (-T) T)
  have hmajor : Integrable (S.indicator q) :=
    hqOn.integrable_indicator measurableSet_Icc
  have hpoint : ∀ t, qk t ≤ S.indicator q t := by
    intro t
    by_cases ht : t ∈ S
    · rw [Set.indicator_of_mem ht]
      exact charFunDiscrepancyQuotient_conv_le mu nu kappa t
    · simp only [Set.indicator_apply, ht, if_false]
      have htOutside : T < |t| := by
        by_contra hnot
        have habs : |t| ≤ T := le_of_not_gt hnot
        have hmem : t ∈ Icc (-T) T := abs_le.mp habs
        exact ht (by simpa only [S] using hmem)
      have hchar : MeasureTheory.charFun kappa t = 0 := hkappa t htOutside
      dsimp only [qk]
      unfold charFunDiscrepancyQuotient
      split_ifs
      · rfl
      · rw [MeasureTheory.charFun_conv, MeasureTheory.charFun_conv, hchar]
        simp
  have hintegral :
      (∫ t, qk t) ≤ ∫ t in S, q t := by
    calc
      (∫ t, qk t) ≤ ∫ t, S.indicator q t :=
        integral_mono hqk hmajor hpoint
      _ = ∫ t in S, q t := integral_indicator measurableSet_Icc
  calc
    cdfGap (kappa ∗ mu) (kappa ∗ nu) x ≤
        (2 * Real.pi)⁻¹ * ∫ t, qk t := by
      exact cdfGap_smoothingKernel_conv_le_integral_discrepancy_scaled
        mu nu kappa hmu1 hnu1 hkappa1 hkappaAC hqk x
    _ ≤ (2 * Real.pi)⁻¹ * ∫ t in S, q t := by
      exact mul_le_mul_of_nonneg_left hintegral (by positivity)
    _ = _ := by rfl

/-- Band-limited Fourier comparison stated directly for the averaged CDFs
used by the deterministic tail-absorption theorem. -/
theorem smoothedCDF_gap_bandLimited_le_local_discrepancy
    (mu nu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure nu] [IsProbabilityMeasure kappa]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hnu1 : Integrable (fun x : ℝ => |x|) nu)
    (hkappa1 : Integrable (fun x : ℝ => |x|) kappa)
    (hkappaAC : kappa ≪ volume)
    {T : ℝ} (hT : 0 ≤ T)
    (hkappa : IsCharFunSupportedInFrequencyWindow kappa T)
    (x : ℝ) :
    |smoothedCDF mu kappa x - smoothedCDF nu kappa x| ≤
      (2 * Real.pi)⁻¹ *
        ∫ t in Icc (-T) T, charFunDiscrepancyQuotient mu nu t := by
  rw [smoothedCDF_eq_cdf_conv, smoothedCDF_eq_cdf_conv]
  exact cdfGap_bandLimitedSmoothingKernel_conv_le_local_discrepancy
    mu nu kappa hmu1 hnu1 hkappa1 hkappaAC hT hkappa x

/-- Complete abstract Esseen smoothing inequality for comparison with the
standard normal law.  All Fourier work is confined to `[-T,T]`; the remaining
kernel obligations are an absolutely continuous band-limited probability law
with finite first moment and two fixed spatial-tail bounds. -/
theorem kolmogorovDistance_standardNormal_le_bandLimitedSmoothingKernel
    (mu kappa : Measure ℝ) [IsProbabilityMeasure mu]
    [IsProbabilityMeasure kappa]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    (hkappa1 : Integrable (fun x : ℝ => |x|) kappa)
    (hkappaAC : kappa ≪ volume)
    {T a q m : ℝ} (hT : 0 ≤ T) (hq : 2 * q < 1)
    (hkappaFreq : IsCharFunSupportedInFrequencyWindow kappa T)
    (hRightTail : kappa.real (Ioi a) ≤ q)
    (hLeftTail : kappa.real (Iio (-a)) ≤ q)
    (hRightMoment : (∫ y, |a - y| ∂kappa) ≤ m)
    (hLeftMoment : (∫ y, |-a - y| ∂kappa) ≤ m) :
    kolmogorovDistance mu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          (∫ t in Icc (-T) T, charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
        (Real.sqrt (2 * Real.pi))⁻¹ * m) / (1 - 2 * q) := by
  have hkappaId : Integrable (fun y : ℝ => y) kappa := by
    apply hkappa1.mono' (by fun_prop)
    exact ae_of_all kappa fun y => by
      rw [Real.norm_eq_abs]
  apply kolmogorovDistance_le_of_smoothedCDF_with_tails
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa
    hkappaId (by positivity) hq hRightTail hLeftTail
    hRightMoment hLeftMoment (fun u v => by
      simpa only using standardNormalCDF_lipschitz v u)
  intro x
  exact smoothedCDF_gap_bandLimited_le_local_discrepancy
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw kappa
    hmu1 integrable_abs_standardNormalLaw hkappa1 hkappaAC hT hkappaFreq x

/-- The removable cotangent factor in Prawitz's signed smoothing filter. -/
noncomputable def prawitzCotFactor (t : ℝ) : ℝ :=
  if t = 0 then 1 else Real.pi * t * Real.cot (Real.pi * t)

theorem prawitzCotFactor_zero : prawitzCotFactor 0 = 1 := by
  simp [prawitzCotFactor]

theorem prawitzCotFactor_neg (t : ℝ) :
    prawitzCotFactor (-t) = prawitzCotFactor t := by
  by_cases ht : t = 0
  · subst t
    simp [prawitzCotFactor]
  · rw [prawitzCotFactor, prawitzCotFactor, if_neg ht,
      if_neg (neg_ne_zero.mpr ht), Real.cot_eq_cos_div_sin,
      Real.cot_eq_cos_div_sin]
    have harg : Real.pi * -t = -(Real.pi * t) := by ring
    rw [harg, Real.cos_neg, Real.sin_neg]
    ring

/-- Real specialization of Mathlib's Mittag--Leffler expansion for cotangent
on the open unit interval. -/
theorem real_cot_series_rep
    {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    Real.pi * Real.cot (Real.pi * u) - 1 / u =
      ∑' n : ℕ,
        (1 / (u - ((n : ℝ) + 1)) + 1 / (u + ((n : ℝ) + 1))) := by
  have huC : (u : ℂ) ∈ Complex.integerComplement := by
    rw [Complex.mem_integerComplement_iff]
    rintro ⟨n, hn⟩
    apply_fun Complex.re at hn
    have hn' : (n : ℝ) = u := by simpa using hn
    have hn0 : (0 : ℤ) < n := by exact_mod_cast (hn' ▸ hu0)
    have hn1 : n < (1 : ℤ) := by exact_mod_cast (hn' ▸ hu1)
    omega
  have hc := cot_series_rep' huC
  have hs := summable_cotTerm huC
  have hre := congrArg Complex.re hc
  have hmap := (RCLike.reCLM : ℂ →L[ℝ] ℝ).map_tsum hs
  change
      (∑' n : ℕ,
        (1 / ((u : ℂ) - ((n : ℂ) + 1)) +
          1 / ((u : ℂ) + ((n : ℂ) + 1)))).re =
        ∑' n : ℕ,
          (1 / ((u : ℂ) - ((n : ℂ) + 1)) +
            1 / ((u : ℂ) + ((n : ℂ) + 1))).re at hmap
  rw [hmap] at hre
  have hcot :
      (Complex.cot ((Real.pi : ℂ) * (u : ℂ))).re =
        Real.cot (Real.pi * u) := by
    rw [← Complex.ofReal_mul]
    apply Complex.ofReal_injective
    rw [Complex.ofReal_cot_ofReal_re, Complex.ofReal_cot]
  simpa [Complex.mul_re, Complex.sub_re, hcot, Complex.div_re,
    Complex.normSq_apply] using hre

theorem summable_real_cot_series
    {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    Summable (fun n : ℕ =>
      (1 / (u - ((n : ℝ) + 1)) + 1 / (u + ((n : ℝ) + 1)))) := by
  have huC : (u : ℂ) ∈ Complex.integerComplement := by
    rw [Complex.mem_integerComplement_iff]
    rintro ⟨n, hn⟩
    apply_fun Complex.re at hn
    have hn' : (n : ℝ) = u := by simpa using hn
    have hn0 : (0 : ℤ) < n := by exact_mod_cast (hn' ▸ hu0)
    have hn1 : n < (1 : ℤ) := by exact_mod_cast (hn' ▸ hu1)
    omega
  have hs := summable_cotTerm huC
  have hmap := hs.map (RCLike.reCLM : ℂ →L[ℝ] ℝ)
    (RCLike.reCLM : ℂ →L[ℝ] ℝ).continuous
  simpa [Function.comp_def, Complex.div_re, Complex.normSq_apply] using hmap

theorem prawitzCotFactor_eq_one_add_mul_tsum
    {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzCotFactor u = 1 + u *
      ∑' n : ℕ,
        (1 / (u - ((n : ℝ) + 1)) + 1 / (u + ((n : ℝ) + 1))) := by
  have h := real_cot_series_rep hu0 hu1
  rw [prawitzCotFactor, if_neg hu0.ne']
  rw [show Real.pi * u * Real.cot (Real.pi * u) =
    u * (Real.pi * Real.cot (Real.pi * u)) by ring]
  rw [eq_add_of_sub_eq h]
  field_simp [hu0.ne']
  ring

/-- The positive inverse-square tail occurring in the spatial
Prawitz--Vaaler majorant remainder. -/
noncomputable def prawitzInverseSquareTail (x : ℝ) : ℝ :=
  ∑' n : ℕ, 1 / (x + (n : ℝ) + 1) ^ 2

theorem summable_prawitzInverseSquareTail (x : ℝ) :
    Summable (fun n : ℕ => 1 / (x + (n : ℝ) + 1) ^ 2) := by
  have h := (Real.summable_one_div_nat_add_rpow (x + 1) 2).2 (by norm_num)
  apply h.congr
  intro n
  rw [show (n : ℝ) + (x + 1) = x + (n : ℝ) + 1 by ring]
  simp [sq_abs]

/-- The inverse-cube tail is the derivative coordinate naturally paired
with `prawitzInverseSquareTail` on the positive half-line. -/
noncomputable def prawitzInverseCubeTail (x : ℝ) : ℝ :=
  ∑' n : ℕ, 1 / (x + (n : ℝ) + 1) ^ 3

/-- A summable, spatially uniform inverse-cube majorant for termwise
differentiation on the positive half-line. -/
theorem summable_two_div_nat_add_one_cube :
    Summable (fun n : ℕ => 2 / ((n : ℝ) + 1) ^ 3) := by
  have h := (Real.summable_one_div_nat_add_rpow 1 3).2 (by norm_num)
  have hcubic : Summable (fun n : ℕ => 1 / ((n : ℝ) + 1) ^ 3) := by
    apply h.congr
    intro n
    have hn : 0 ≤ (n : ℝ) + 1 := by positivity
    rw [abs_of_nonneg hn]
    exact congrArg (fun z : ℝ => 1 / z)
      (Real.rpow_natCast ((n : ℝ) + 1) 3)
  simpa [div_eq_mul_inv] using hcubic.mul_left 2

set_option maxHeartbeats 800000 in
/-- On `x > 0`, the inverse-square tail can be differentiated termwise;
its derivative is minus twice the positive inverse-cube tail. -/
theorem hasDerivAt_prawitzInverseSquareTail
    {x : ℝ} (hx : 0 < x) :
    HasDerivAt prawitzInverseSquareTail (-2 * prawitzInverseCubeTail x) x := by
  unfold prawitzInverseSquareTail prawitzInverseCubeTail
  have hg : ∀ n : ℕ, ∀ y : ℝ, y ∈ Ioi (0 : ℝ) →
      HasDerivAt (fun z : ℝ => 1 / (z + (n : ℝ) + 1) ^ 2)
        (-2 / (y + (n : ℝ) + 1) ^ 3) y := by
    intro n y hy
    change 0 < y at hy
    have hlin : HasDerivAt (fun z : ℝ => z + (n : ℝ) + 1) 1 y := by
      simpa using (((hasDerivAt_id y).add_const (n : ℝ)).add_const 1)
    have hden : y + (n : ℝ) + 1 ≠ 0 := by positivity
    have hpow := (hlin.pow 2).inv (pow_ne_zero 2 hden)
    simp only [one_div, Pi.pow_apply] at hpow ⊢
    convert hpow using 1
    field_simp [hden]
    ring
  have hg' : ∀ n : ℕ, ∀ y : ℝ, y ∈ Ioi (0 : ℝ) →
      ‖-2 / (y + (n : ℝ) + 1) ^ 3‖ ≤
        2 / ((n : ℝ) + 1) ^ 3 := by
    intro n y hy
    change 0 < y at hy
    rw [Real.norm_eq_abs, abs_div, abs_neg, abs_of_nonneg (by norm_num),
      abs_pow, abs_of_pos (by positivity)]
    have hbase : (n : ℝ) + 1 ≤ y + (n : ℝ) + 1 := by linarith
    exact div_le_div_of_nonneg_left (by norm_num) (by positivity)
      (pow_le_pow_left₀ (by positivity) hbase 3)
  have hseries : HasDerivAt
      (fun z : ℝ => ∑' n : ℕ, 1 / (z + (n : ℝ) + 1) ^ 2)
      (∑' n : ℕ, -2 / (x + (n : ℝ) + 1) ^ 3) x :=
    hasDerivAt_tsum_of_isPreconnected
      (u := fun n : ℕ => 2 / ((n : ℝ) + 1) ^ 3)
      (g := fun (n : ℕ) (y : ℝ) => 1 / (y + (n : ℝ) + 1) ^ 2)
      (g' := fun (n : ℕ) (y : ℝ) => -2 / (y + (n : ℝ) + 1) ^ 3)
      (t := Ioi (0 : ℝ)) (y₀ := (1 : ℝ))
      summable_two_div_nat_add_one_cube isOpen_Ioi isPreconnected_Ioi
      hg hg' (by norm_num) (summable_prawitzInverseSquareTail 1) hx
  convert hseries using 1
  rw [← tsum_mul_left]
  apply tsum_congr
  intro n
  ring

/-- Integral-test bound for the positive inverse-square tail, proved by a
telescoping majorant. -/
theorem prawitzInverseSquareTail_le_inv
    {x : ℝ} (hx : 0 < x) : prawitzInverseSquareTail x ≤ 1 / x := by
  have hterm (n : ℕ) :
      1 / (x + (n : ℝ) + 1) ^ 2 ≤
        1 / (x + (n : ℝ)) - 1 / (x + (n : ℝ) + 1) := by
    have hxn : 0 < x + (n : ℝ) := by positivity
    have hxns : 0 < x + (n : ℝ) + 1 := by positivity
    field_simp [hxn.ne', hxns.ne']
    nlinarith
  have hsum (N : ℕ) :
      (∑ n ∈ Finset.range N, 1 / (x + (n : ℝ) + 1) ^ 2) ≤ 1 / x := by
    calc
      _ ≤ ∑ n ∈ Finset.range N,
          (1 / (x + (n : ℝ)) - 1 / (x + (n : ℝ) + 1)) := by
        exact Finset.sum_le_sum fun n _ => hterm n
      _ = 1 / x - 1 / (x + (N : ℝ)) := by
        induction N with
        | zero => simp
        | succ N ih =>
            rw [Finset.sum_range_succ, ih]
            norm_num [Nat.cast_add, Nat.cast_one]
            ring
      _ ≤ 1 / x := sub_le_self _ (by positivity)
  unfold prawitzInverseSquareTail
  exact Real.tsum_le_of_sum_range_le
    (fun n => by positivity) (by simpa only [Finset.sum_range] using hsum)

/-- The initial inverse-square term together with the positive tail also
dominates `1 / x`. This is the complementary telescoping estimate needed on
the reflected spatial half-line. -/
theorem inv_le_inv_sq_add_prawitzInverseSquareTail
    {x : ℝ} (hx : 0 < x) :
    1 / x ≤ 1 / x ^ 2 + prawitzInverseSquareTail x := by
  let f : ℕ → ℝ := fun n => 1 / (x + (n : ℝ)) ^ 2
  have hsum : Summable f := by
    have h := summable_prawitzInverseSquareTail (x - 1)
    apply h.congr
    intro n
    dsimp only [f]
    congr 2
    ring
  have hterm (n : ℕ) :
      1 / (x + (n : ℝ)) - 1 / (x + (n : ℝ) + 1) ≤ f n := by
    have hxn : 0 < x + (n : ℝ) := by positivity
    have hxns : 0 < x + (n : ℝ) + 1 := by positivity
    dsimp only [f]
    field_simp [hxn.ne', hxns.ne']
    nlinarith
  have hfinite (N : ℕ) :
      1 / x - 1 / (x + (N : ℝ)) ≤
        ∑ n ∈ Finset.range N, f n := by
    calc
      _ = ∑ n ∈ Finset.range N,
          (1 / (x + (n : ℝ)) - 1 / (x + (n : ℝ) + 1)) := by
        induction N with
        | zero => simp
        | succ N ih =>
            rw [Finset.sum_range_succ, ← ih]
            norm_num [Nat.cast_add, Nat.cast_one]
            ring
      _ ≤ ∑ n ∈ Finset.range N, f n := by
        exact Finset.sum_le_sum fun n _ => hterm n
  have hle (N : ℕ) :
      1 / x - 1 / (x + (N : ℝ)) ≤ ∑' n, f n := by
    exact (hfinite N).trans
      (hsum.sum_le_tsum (Finset.range N)
        (fun n _ => by dsimp only [f]; positivity))
  have hden : Tendsto (fun N : ℕ => x + (N : ℝ)) atTop atTop := by
    exact tendsto_atTop.2 fun b =>
      (tendsto_natCast_atTop_atTop.eventually
        (eventually_ge_atTop (b - x))).mono (by
          intro N hN
          linarith)
  have hlim : Tendsto (fun N : ℕ => 1 / x - 1 / (x + (N : ℝ)))
      atTop (nhds (1 / x)) := by
    have hinv : Tendsto (fun N : ℕ => (x + (N : ℝ))⁻¹) atTop (nhds 0) :=
      tendsto_inv_atTop_zero.comp hden
    simpa only [one_div, sub_zero] using
      (tendsto_const_nhds.sub hinv :
        Tendsto (fun N : ℕ => x⁻¹ - (x + (N : ℝ))⁻¹) atTop
          (nhds (x⁻¹ - 0)))
  have hfull : 1 / x ≤ ∑' n, f n :=
    le_of_tendsto hlim (Filter.Eventually.of_forall hle)
  have hsplit := hsum.sum_add_tsum_nat_add 1
  have htail : (∑' n : ℕ, f (n + 1)) = prawitzInverseSquareTail x := by
    unfold prawitzInverseSquareTail
    apply tsum_congr
    intro n
    dsimp only [f]
    congr 2
    push_cast
    ring
  calc
    1 / x ≤ ∑' n, f n := hfull
    _ = f 0 + ∑' n : ℕ, f (n + 1) := by simpa using hsplit.symm
    _ = 1 / x ^ 2 + prawitzInverseSquareTail x := by rw [htail]; simp [f]

/-- Manifestly nonnegative spatial remainder in the Prawitz--Vaaler
majorant on the positive half-line. -/
noncomputable def prawitzVaalerPositiveRemainder (x : ℝ) : ℝ :=
  2 * (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
    (1 / x - prawitzInverseSquareTail x)

theorem prawitzVaalerPositiveRemainder_nonneg
    {x : ℝ} (hx : 0 < x) :
    0 ≤ prawitzVaalerPositiveRemainder x := by
  unfold prawitzVaalerPositiveRemainder
  exact mul_nonneg (mul_nonneg (by norm_num) (sq_nonneg _))
    (sub_nonneg.mpr (prawitzInverseSquareTail_le_inv hx))

/-- Manifestly nonnegative spatial remainder in the reflected
Prawitz--Vaaler majorant on the negative half-line. -/
noncomputable def prawitzVaalerNegativeRemainder (x : ℝ) : ℝ :=
  2 * (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
    (1 / x ^ 2 + prawitzInverseSquareTail x - 1 / x)

theorem prawitzVaalerNegativeRemainder_nonneg
    {x : ℝ} (hx : 0 < x) :
    0 ≤ prawitzVaalerNegativeRemainder x := by
  unfold prawitzVaalerNegativeRemainder
  exact mul_nonneg (mul_nonneg (by norm_num) (sq_nonneg _))
    (sub_nonneg.mpr (inv_le_inv_sq_add_prawitzInverseSquareTail hx))

/-- The two spatial remainders add to the Fejér kernel at scale `π`. -/
theorem prawitzVaalerPositiveRemainder_add_negative
    {x : ℝ} (hx : x ≠ 0) :
    prawitzVaalerPositiveRemainder x +
        prawitzVaalerNegativeRemainder x =
      2 * (Real.sin (Real.pi * x) / (Real.pi * x)) ^ 2 := by
  unfold prawitzVaalerPositiveRemainder prawitzVaalerNegativeRemainder
  field_simp [Real.pi_ne_zero, hx]
  ring

/-- The right-continuous centered half-line step `2 * 1_[0,∞) - 1`. -/
noncomputable def centeredHalfLineStep (x : ℝ) : ℝ :=
  if x < 0 then -1 else 1

theorem centeredHalfLineStep_eq_indicator (x : ℝ) :
    centeredHalfLineStep x =
      2 * (Ici (0 : ℝ)).indicator (fun _ : ℝ => (1 : ℝ)) x - 1 := by
  by_cases hx : x < 0
  · simp [centeredHalfLineStep, hx, not_le.mpr hx]
  · simp [centeredHalfLineStep, hx, le_of_not_gt hx]
    norm_num

/-- Piecewise spatial form of the Beurling--Selberg majorant associated to
Prawitz's signed smoothing filter. -/
noncomputable def prawitzVaalerMajorant (x : ℝ) : ℝ :=
  if 0 < x then 1 + prawitzVaalerPositiveRemainder x
  else if x < 0 then -1 + prawitzVaalerNegativeRemainder (-x)
  else 1

/-- The explicit spatial Prawitz--Vaaler function majorizes the centered
right-continuous half-line step. -/
theorem centeredHalfLineStep_le_prawitzVaalerMajorant (x : ℝ) :
    centeredHalfLineStep x ≤ prawitzVaalerMajorant x := by
  by_cases hxpos : 0 < x
  · rw [centeredHalfLineStep, prawitzVaalerMajorant,
      if_pos hxpos, if_neg (not_lt_of_ge hxpos.le)]
    linarith [prawitzVaalerPositiveRemainder_nonneg hxpos]
  · by_cases hxneg : x < 0
    · rw [centeredHalfLineStep, prawitzVaalerMajorant,
        if_neg hxpos, if_pos hxneg, if_pos hxneg]
      linarith [prawitzVaalerNegativeRemainder_nonneg (neg_pos.mpr hxneg)]
    · have hx : x = 0 := by linarith
      subst x
      simp [centeredHalfLineStep, prawitzVaalerMajorant]

theorem measurable_prawitzCotFactor : Measurable prawitzCotFactor := by
  unfold prawitzCotFactor
  apply Measurable.ite (measurableSet_singleton 0) measurable_const
  simp_rw [Real.cot_eq_cos_div_sin]
  fun_prop

/-- The endpoint weight cancels the cotangent singularity uniformly on the
positive half of Prawitz's unit frequency window. -/
theorem abs_one_sub_mul_prawitzCotFactor_le_two
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    |(1 - u) * prawitzCotFactor u| ≤ 2 := by
  rcases hu0.eq_or_lt with rfl | hu0
  · simp [prawitzCotFactor]
  have hu0' : u ≠ 0 := hu0.ne'
  have hOne : 0 ≤ 1 - u := sub_nonneg.mpr hu1.le
  have hArg0 : 0 < Real.pi * u := mul_pos Real.pi_pos hu0
  have hArgPi : Real.pi * u < Real.pi := by
    nlinarith [mul_pos Real.pi_pos (sub_pos.mpr hu1)]
  have hSin : 0 < Real.sin (Real.pi * u) :=
    Real.sin_pos_of_pos_of_lt_pi hArg0 hArgPi
  rw [prawitzCotFactor, if_neg hu0', Real.cot_eq_cos_div_sin]
  have hFormula :
      |(1 - u) * (Real.pi * u *
          (Real.cos (Real.pi * u) / Real.sin (Real.pi * u)))| =
        (1 - u) * Real.pi * u * |Real.cos (Real.pi * u)| /
          Real.sin (Real.pi * u) := by
    rw [abs_mul, abs_mul, abs_div, abs_of_nonneg hOne,
      abs_of_pos hArg0, abs_of_pos hSin]
    ring
  rw [hFormula]
  apply (div_le_iff₀ hSin).2
  have hCos : |Real.cos (Real.pi * u)| ≤ 1 := Real.abs_cos_le_one _
  have hWeighted :
      (1 - u) * Real.pi * u * |Real.cos (Real.pi * u)| ≤
        (1 - u) * Real.pi * u := by
    simpa only [mul_one] using
      mul_le_mul_of_nonneg_left hCos
        (mul_nonneg (mul_nonneg hOne Real.pi_pos.le) hu0.le)
  apply hWeighted.trans
  by_cases huHalf : u ≤ 1 / 2
  · have hArgHalf : Real.pi * u ≤ Real.pi / 2 := by
      nlinarith [Real.pi_pos]
    have hJordan : 2 * u ≤ Real.sin (Real.pi * u) := by
      have := Real.mul_le_sin hArg0.le hArgHalf
      convert this using 1 <;> field_simp [Real.pi_ne_zero]
    have hNum : (1 - u) * Real.pi * u ≤ 4 * u := by
      calc
        (1 - u) * Real.pi * u = (1 - u) * (Real.pi * u) := by ring
        _ ≤ 1 * (Real.pi * u) :=
          mul_le_mul_of_nonneg_right (by linarith) hArg0.le
        _ = Real.pi * u := by ring
        _ ≤ 4 * u := mul_le_mul_of_nonneg_right Real.pi_le_four hu0.le
    nlinarith
  · have huHalf' : 1 / 2 ≤ u := le_of_not_ge huHalf
    have hOneHalf : 1 - u ≤ 1 / 2 := by linarith
    have hArgHalf : Real.pi * (1 - u) ≤ Real.pi / 2 := by
      nlinarith [Real.pi_pos]
    have hJordan' : 2 * (1 - u) ≤ Real.sin (Real.pi * u) := by
      have hj := Real.mul_le_sin
        (mul_nonneg Real.pi_pos.le hOne) hArgHalf
      have hSinEq : Real.sin (Real.pi * (1 - u)) =
          Real.sin (Real.pi * u) := by
        rw [show Real.pi * (1 - u) = Real.pi - Real.pi * u by ring,
          Real.sin_pi_sub]
      rw [hSinEq] at hj
      convert hj using 1 <;> field_simp [Real.pi_ne_zero]
    have hPiU : Real.pi * u ≤ 4 := by
      calc
        Real.pi * u ≤ Real.pi * 1 :=
          mul_le_mul_of_nonneg_left hu1.le Real.pi_pos.le
        _ ≤ 4 := by simpa using Real.pi_le_four
    have hNum : (1 - u) * Real.pi * u ≤ 4 * (1 - u) := by
      nlinarith
    nlinarith

/-- Prawitz's compactly supported signed smoothing filter. Its real part is
even, its imaginary part is odd, and the value at zero is supplied by the
removable limit of the cotangent factor. -/
noncomputable def prawitzSmoothingFilter (t : ℝ) : ℂ :=
  if |t| < 1 then
    (((1 - |t|) * prawitzCotFactor t + |t| : ℝ) : ℂ) -
      (((1 - |t|) * Real.pi * t : ℝ) : ℂ) * Complex.I
  else 0

theorem prawitzSmoothingFilter_zero : prawitzSmoothingFilter 0 = 1 := by
  simp [prawitzSmoothingFilter, prawitzCotFactor]

theorem prawitzSmoothingFilter_eq_zero_of_one_le_abs
    {t : ℝ} (ht : 1 ≤ |t|) :
    prawitzSmoothingFilter t = 0 := by
  simp [prawitzSmoothingFilter, not_lt.mpr ht]

theorem prawitzSmoothingFilter_neg (t : ℝ) :
    prawitzSmoothingFilter (-t) =
      (starRingEnd ℂ) (prawitzSmoothingFilter t) := by
  by_cases ht : |t| < 1
  · rw [prawitzSmoothingFilter, prawitzSmoothingFilter,
      if_pos ht, if_pos (by simpa only [abs_neg] using ht),
      abs_neg, prawitzCotFactor_neg]
    apply Complex.ext <;> simp <;> ring
  · have ht' : ¬ |-t| < 1 := by simpa only [abs_neg] using ht
    rw [prawitzSmoothingFilter, prawitzSmoothingFilter,
      if_neg ht, if_neg ht']
    simp

theorem measurable_prawitzSmoothingFilter :
    Measurable prawitzSmoothingFilter := by
  have habs : Measurable fun t : ℝ => |t| := by fun_prop
  have hre : Measurable fun t : ℝ =>
      (1 - |t|) * prawitzCotFactor t + |t| :=
    ((measurable_const.sub habs).mul measurable_prawitzCotFactor).add habs
  have him : Measurable fun t : ℝ =>
      (1 - |t|) * Real.pi * t :=
    ((measurable_const.sub habs).mul measurable_const).mul measurable_id
  unfold prawitzSmoothingFilter
  apply Measurable.ite (measurableSet_lt habs measurable_const)
  · exact (Complex.continuous_ofReal.measurable.comp hre).sub
      ((Complex.continuous_ofReal.measurable.comp him).mul measurable_const)
  · exact measurable_const

/-- Exact real part of the Prawitz filter on the positive unit window. -/
theorem prawitzSmoothingFilter_re_of_mem_Ico
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    (prawitzSmoothingFilter u).re =
      (1 - u) * prawitzCotFactor u + u := by
  rw [prawitzSmoothingFilter, if_pos (by simpa [abs_of_nonneg hu0] using hu1),
    abs_of_nonneg hu0]
  simp

/-- Exact imaginary part of the Prawitz filter on the positive unit window. -/
theorem prawitzSmoothingFilter_im_of_mem_Ico
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    (prawitzSmoothingFilter u).im =
      -((1 - u) * Real.pi * u) := by
  rw [prawitzSmoothingFilter, if_pos (by simpa [abs_of_nonneg hu0] using hu1),
    abs_of_nonneg hu0]
  simp

/-- Exact squared norm of the Prawitz filter on the positive unit window. -/
theorem sq_norm_prawitzSmoothingFilter_of_mem_Ico
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ ^ 2 =
      ((1 - u) * prawitzCotFactor u + u) ^ 2 +
        ((1 - u) * Real.pi * u) ^ 2 := by
  rw [Complex.sq_norm, Complex.normSq_apply,
    prawitzSmoothingFilter_re_of_mem_Ico hu0 hu1,
    prawitzSmoothingFilter_im_of_mem_Ico hu0 hu1]
  ring

/-- Exact norm of the Prawitz filter on the positive unit window. -/
theorem norm_prawitzSmoothingFilter_of_mem_Ico
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ =
      Real.sqrt (((1 - u) * prawitzCotFactor u + u) ^ 2 +
        ((1 - u) * Real.pi * u) ^ 2) := by
  rw [Complex.norm_def, Complex.normSq_apply,
    prawitzSmoothingFilter_re_of_mem_Ico hu0 hu1,
    prawitzSmoothingFilter_im_of_mem_Ico hu0 hu1]
  congr 1
  ring

/-- The cotangent factor is nonnegative on the first half of the positive
unit window. -/
theorem prawitzCotFactor_nonneg_of_mem_Ioc_half
    {u : ℝ} (hu0 : 0 < u) (huHalf : u ≤ 1 / 2) :
    0 ≤ prawitzCotFactor u := by
  have hx0 : 0 < Real.pi * u := mul_pos Real.pi_pos hu0
  have hxHalf : Real.pi * u ≤ Real.pi / 2 := by
    nlinarith [Real.pi_pos]
  have hsin : 0 < Real.sin (Real.pi * u) := by
    apply Real.sin_pos_of_pos_of_lt_pi hx0
    nlinarith [Real.pi_pos]
  have hcos : 0 ≤ Real.cos (Real.pi * u) :=
    Real.cos_nonneg_of_neg_pi_div_two_le_of_le (by linarith [Real.pi_pos]) hxHalf
  rw [prawitzCotFactor, if_neg hu0.ne', Real.cot_eq_cos_div_sin]
  positivity

/-- The positive first-half cotangent factor is at most its removable value
at zero. -/
theorem prawitzCotFactor_le_one_of_mem_Ioo_half
    {u : ℝ} (hu0 : 0 < u) (huHalf : u < 1 / 2) :
    prawitzCotFactor u ≤ 1 := by
  have hx0 : 0 < Real.pi * u := mul_pos Real.pi_pos hu0
  have hxHalf : Real.pi * u < Real.pi / 2 := by
    nlinarith [Real.pi_pos]
  have htanPos := Real.tan_pos_of_pos_of_lt_pi_div_two hx0 hxHalf
  have htan := Real.le_tan hx0.le hxHalf
  rw [prawitzCotFactor, if_neg hu0.ne', ← Real.tan_inv_eq_cot,
    ← div_eq_mul_inv, div_le_one htanPos]
  exact htan

/-- The positive first-half cotangent factor is at most one, including the
midpoint endpoint. -/
theorem prawitzCotFactor_le_one_of_mem_Ioc_half
    {u : ℝ} (hu0 : 0 < u) (huHalf : u ≤ 1 / 2) :
    prawitzCotFactor u ≤ 1 := by
  rcases huHalf.eq_or_lt with rfl | huHalf
  · rw [prawitzCotFactor, if_neg (by norm_num : (1 / 2 : ℝ) ≠ 0)]
    rw [show Real.pi * (1 / 2 : ℝ) = Real.pi / 2 by ring,
      Real.cot_eq_cos_div_sin]
    simp
  · exact prawitzCotFactor_le_one_of_mem_Ioo_half hu0 huHalf

/-- On the positive first half-window the cotangent factor dominates the
corresponding cosine. -/
theorem cos_pi_mul_le_prawitzCotFactor_of_mem_Ioc_half
    {u : ℝ} (hu0 : 0 < u) (huHalf : u ≤ 1 / 2) :
    Real.cos (Real.pi * u) ≤ prawitzCotFactor u := by
  have hx0 : 0 < Real.pi * u := mul_pos Real.pi_pos hu0
  have hxHalf : Real.pi * u ≤ Real.pi / 2 := by
    nlinarith [Real.pi_pos]
  have hsin : 0 < Real.sin (Real.pi * u) := by
    apply Real.sin_pos_of_pos_of_lt_pi hx0
    nlinarith [Real.pi_pos]
  have hcos : 0 ≤ Real.cos (Real.pi * u) :=
    Real.cos_nonneg_of_neg_pi_div_two_le_of_le
      (by linarith [Real.pi_pos]) hxHalf
  have hsinx : Real.sin (Real.pi * u) ≤ Real.pi * u :=
    Real.sin_le hx0.le
  rw [prawitzCotFactor, if_neg hu0.ne', Real.cot_eq_cos_div_sin]
  rw [show Real.pi * u *
      (Real.cos (Real.pi * u) / Real.sin (Real.pi * u)) =
      (Real.pi * u * Real.cos (Real.pi * u)) /
        Real.sin (Real.pi * u) by ring]
  exact (le_div_iff₀ hsin).2
    (by simpa only [mul_comm] using
      mul_le_mul_of_nonneg_right hsinx hcos)

/-- The cotangent factor is nonpositive on the second half of the positive
unit window. -/
theorem prawitzCotFactor_nonpos_of_half_le
    {u : ℝ} (huHalf : 1 / 2 ≤ u) (hu1 : u < 1) :
    prawitzCotFactor u ≤ 0 := by
  have hu0 : 0 < u := by linarith
  have hx0 : 0 < Real.pi * u := mul_pos Real.pi_pos hu0
  have hxPi : Real.pi * u < Real.pi := by
    nlinarith [Real.pi_pos]
  have hxHalf : Real.pi / 2 ≤ Real.pi * u := by
    nlinarith [Real.pi_pos]
  have hsin : 0 < Real.sin (Real.pi * u) :=
    Real.sin_pos_of_pos_of_lt_pi hx0 hxPi
  have hcos : Real.cos (Real.pi * u) ≤ 0 :=
    Real.cos_nonpos_of_pi_div_two_le_of_le hxHalf (by linarith [Real.pi_pos])
  rw [prawitzCotFactor, if_neg hu0.ne', Real.cot_eq_cos_div_sin]
  exact mul_nonpos_of_nonneg_of_nonpos hx0.le
    (div_nonpos_of_nonpos_of_nonneg hcos hsin.le)

/-- Reflection through the midpoint exchanges the weighted cotangent terms. -/
theorem one_sub_mul_prawitzCotFactor_eq
    {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    (1 - u) * prawitzCotFactor u =
      -u * prawitzCotFactor (1 - u) := by
  have hOne : 1 - u ≠ 0 := sub_ne_zero.mpr hu1.ne'
  rw [prawitzCotFactor, if_neg hu0.ne', prawitzCotFactor, if_neg hOne]
  have hcot : Real.cot (Real.pi * (1 - u)) =
      -Real.cot (Real.pi * u) := by
    rw [show Real.pi * (1 - u) = Real.pi - Real.pi * u by ring,
      Real.cot_eq_cos_div_sin, Real.cot_eq_cos_div_sin,
      Real.cos_pi_sub, Real.sin_pi_sub]
    ring
  rw [hcot]
  ring

/-- The real part of the positive-window Prawitz filter is nonnegative. -/
theorem prawitzSmoothingFilter_re_nonneg_of_mem_Ico
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    0 ≤ (prawitzSmoothingFilter u).re := by
  rw [prawitzSmoothingFilter_re_of_mem_Ico hu0 hu1]
  rcases hu0.eq_or_lt with rfl | hu0
  · simp [prawitzCotFactor]
  by_cases huHalf : u ≤ 1 / 2
  · have hcot := prawitzCotFactor_nonneg_of_mem_Ioc_half hu0 huHalf
    have hOne : 0 ≤ 1 - u := sub_nonneg.mpr hu1.le
    nlinarith [mul_nonneg hOne hcot]
  · have hOne0 : 0 < 1 - u := sub_pos.mpr hu1
    have hOneHalf : 1 - u < 1 / 2 := by linarith [lt_of_not_ge huHalf]
    have hcot := prawitzCotFactor_le_one_of_mem_Ioo_half hOne0 hOneHalf
    rw [one_sub_mul_prawitzCotFactor_eq hu0 hu1]
    nlinarith

/-- The real part of the positive-window Prawitz filter is at most one. -/
theorem prawitzSmoothingFilter_re_le_one_of_mem_Ico
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    (prawitzSmoothingFilter u).re ≤ 1 := by
  rw [prawitzSmoothingFilter_re_of_mem_Ico hu0 hu1]
  rcases hu0.eq_or_lt with rfl | hu0
  · simp [prawitzCotFactor]
  by_cases huHalf : u < 1 / 2
  · have hcot := prawitzCotFactor_le_one_of_mem_Ioo_half hu0 huHalf
    have hOne : 0 ≤ 1 - u := sub_nonneg.mpr hu1.le
    nlinarith
  · have hcot := prawitzCotFactor_nonpos_of_half_le (le_of_not_gt huHalf) hu1
    have hOne : 0 ≤ 1 - u := sub_nonneg.mpr hu1.le
    nlinarith

/-- The exact imaginary part and the unit bound on the real part give a
square-root norm majorant. -/
theorem norm_prawitzSmoothingFilter_le_sqrt_one_add_sq
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ ≤
      Real.sqrt (1 + ((1 - u) * Real.pi * u) ^ 2) := by
  rw [norm_prawitzSmoothingFilter_of_mem_Ico hu0 hu1]
  apply Real.sqrt_le_sqrt
  have hre0 := prawitzSmoothingFilter_re_nonneg_of_mem_Ico hu0 hu1
  have hre1 := prawitzSmoothingFilter_re_le_one_of_mem_Ico hu0 hu1
  rw [prawitzSmoothingFilter_re_of_mem_Ico hu0 hu1] at hre0 hre1
  nlinarith

/-- Elementary polynomial majorant for `sqrt (1 + x^2)`. -/
theorem sqrt_one_add_sq_le_one_add_half_sq (x : ℝ) :
    Real.sqrt (1 + x ^ 2) ≤ 1 + x ^ 2 / 2 := by
  rw [Real.sqrt_le_iff]
  constructor
  · positivity
  · nlinarith [sq_nonneg (x ^ 2)]

/-- Polynomial positive-window majorant for the Prawitz filter norm. -/
theorem norm_prawitzSmoothingFilter_le_one_add_half_im_sq
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ ≤
      1 + ((1 - u) * Real.pi * u) ^ 2 / 2 :=
  (norm_prawitzSmoothingFilter_le_sqrt_one_add_sq hu0 hu1).trans
    (sqrt_one_add_sq_le_one_add_half_sq _)

/-- Uniform consequence of the polynomial norm majorant on the positive unit
window. -/
theorem norm_prawitzSmoothingFilter_le_one_add_pi_sq_div_thirtytwo
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ ≤ 1 + Real.pi ^ 2 / 32 := by
  have hprod0 : 0 ≤ (1 - u) * u :=
    mul_nonneg (sub_nonneg.mpr hu1.le) hu0
  have hprod : (1 - u) * u ≤ 1 / 4 := by
    nlinarith [sq_nonneg (u - 1 / 2)]
  have hb0 : 0 ≤ (1 - u) * Real.pi * u := by
    rw [show (1 - u) * Real.pi * u = Real.pi * ((1 - u) * u) by ring]
    exact mul_nonneg Real.pi_pos.le hprod0
  have hb : (1 - u) * Real.pi * u ≤ Real.pi / 4 := by
    calc
      (1 - u) * Real.pi * u = Real.pi * ((1 - u) * u) := by ring
      _ ≤ Real.pi * (1 / 4) :=
        mul_le_mul_of_nonneg_left hprod Real.pi_pos.le
      _ = Real.pi / 4 := by ring
  have hbSq := mul_self_le_mul_self hb0 hb
  calc
    ‖prawitzSmoothingFilter u‖ ≤
        1 + ((1 - u) * Real.pi * u) ^ 2 / 2 :=
      norm_prawitzSmoothingFilter_le_one_add_half_im_sq hu0 hu1
    _ ≤ 1 + Real.pi ^ 2 / 32 := by
      rw [pow_two, pow_two]
      nlinarith [hbSq]

/-- The Prawitz filter vanishes linearly at the upper endpoint of its positive
unit window. -/
theorem norm_prawitzSmoothingFilter_le_endpoint_linear
    {u : ℝ} (huHalf : 1 / 2 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ ≤
      (Real.pi ^ 2 / 2 + Real.pi) * (1 - u) := by
  let v : ℝ := 1 - u
  have hu0 : 0 < u := by linarith
  have huOne : u ≤ 1 := hu1.le
  have hv0 : 0 < v := by dsimp only [v]; linarith
  have hvHalf : v ≤ 1 / 2 := by dsimp only [v]; linarith
  have hvOne : v ≤ 1 := hvHalf.trans (by norm_num)
  have hcot1 : prawitzCotFactor v ≤ 1 :=
    prawitzCotFactor_le_one_of_mem_Ioc_half hv0 hvHalf
  have hcosCot : Real.cos (Real.pi * v) ≤ prawitzCotFactor v :=
    cos_pi_mul_le_prawitzCotFactor_of_mem_Ioc_half hv0 hvHalf
  have hcosTaylor := Real.one_sub_sq_div_two_le_cos (x := Real.pi * v)
  have hfactor0 : 0 ≤ 1 - prawitzCotFactor v := by linarith
  have hfactor : 1 - prawitzCotFactor v ≤ (Real.pi * v) ^ 2 / 2 := by
    linarith
  have hvSq : v ^ 2 ≤ v := by
    nlinarith [mul_nonneg hv0.le (sub_nonneg.mpr hvOne)]
  have hreEq : (prawitzSmoothingFilter u).re =
      u * (1 - prawitzCotFactor v) := by
    rw [prawitzSmoothingFilter_re_of_mem_Ico hu0.le hu1,
      one_sub_mul_prawitzCotFactor_eq hu0 hu1]
    dsimp only [v]
    ring
  have hre0 : 0 ≤ (prawitzSmoothingFilter u).re := by
    rw [hreEq]
    positivity
  have hre : (prawitzSmoothingFilter u).re ≤
      Real.pi ^ 2 / 2 * v := by
    rw [hreEq]
    calc
      u * (1 - prawitzCotFactor v) ≤ 1 * ((Real.pi * v) ^ 2 / 2) :=
        mul_le_mul huOne hfactor hfactor0 (by positivity)
      _ ≤ Real.pi ^ 2 / 2 * v := by
        nlinarith [sq_nonneg Real.pi, hvSq]
  have himEq : (prawitzSmoothingFilter u).im = -(v * Real.pi * u) := by
    rw [prawitzSmoothingFilter_im_of_mem_Ico hu0.le hu1]
  have him : |(prawitzSmoothingFilter u).im| ≤ Real.pi * v := by
    rw [himEq, abs_neg, abs_of_nonneg (by positivity : 0 ≤ v * Real.pi * u)]
    nlinarith [mul_nonneg Real.pi_pos.le hv0.le]
  calc
    ‖prawitzSmoothingFilter u‖ ≤
        |(prawitzSmoothingFilter u).re| +
          |(prawitzSmoothingFilter u).im| :=
      Complex.norm_le_abs_re_add_abs_im _
    _ = (prawitzSmoothingFilter u).re +
          |(prawitzSmoothingFilter u).im| := by rw [abs_of_nonneg hre0]
    _ ≤ Real.pi ^ 2 / 2 * v + Real.pi * v := add_le_add hre him
    _ = (Real.pi ^ 2 / 2 + Real.pi) * (1 - u) := by
      dsimp only [v]
      ring

/-- A coarse uniform norm bound for the Prawitz filter on the positive unit
window. The constant is chosen for a robust integrability majorant. -/
theorem norm_prawitzSmoothingFilter_le_seven
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    ‖prawitzSmoothingFilter u‖ ≤ 7 := by
  have habs : |u| = u := abs_of_nonneg hu0
  rw [prawitzSmoothingFilter, if_pos (by simpa [habs] using hu1), habs]
  let a : ℝ := (1 - u) * prawitzCotFactor u + u
  let b : ℝ := (1 - u) * Real.pi * u
  have ha : |a| ≤ 3 := by
    calc
      |a| ≤ |(1 - u) * prawitzCotFactor u| + |u| := by
        dsimp only [a]
        exact abs_add_le _ _
      _ ≤ 2 + u := by
        gcongr
        · exact abs_one_sub_mul_prawitzCotFactor_le_two hu0 hu1
        · exact habs.le
      _ ≤ 3 := by linarith
  have hb : |b| ≤ 4 := by
    have hOne : 0 ≤ 1 - u := sub_nonneg.mpr hu1.le
    have hb0 : 0 ≤ b := by dsimp only [b]; positivity
    rw [abs_of_nonneg hb0]
    calc
      b ≤ Real.pi * u := by
        dsimp only [b]
        nlinarith [mul_nonneg Real.pi_pos.le hu0]
      _ ≤ 4 * u := mul_le_mul_of_nonneg_right Real.pi_le_four hu0
      _ ≤ 4 := by linarith
  change ‖(a : ℂ) - (b : ℂ) * Complex.I‖ ≤ 7
  calc
    ‖(a : ℂ) - (b : ℂ) * Complex.I‖ ≤
        ‖(a : ℂ)‖ + ‖(b : ℂ) * Complex.I‖ := norm_sub_le _ _
    _ = |a| + |b| := by
      rw [norm_mul, Complex.norm_real, Complex.norm_real,
        Complex.norm_I, mul_one, Real.norm_eq_abs, Real.norm_eq_abs]
    _ ≤ 7 := by linarith

/-- The odd imaginary part of the Prawitz filter vanishes linearly at zero. -/
theorem abs_im_prawitzSmoothingFilter_le
    {u : ℝ} (hu0 : 0 ≤ u) (hu1 : u < 1) :
    |(prawitzSmoothingFilter u).im| ≤ Real.pi * u := by
  have habs : |u| = u := abs_of_nonneg hu0
  rw [prawitzSmoothingFilter, if_pos (by simpa [habs] using hu1), habs]
  simp only [Complex.sub_im, Complex.ofReal_im, Complex.mul_im,
    Complex.ofReal_re, Complex.I_im, Complex.I_re, zero_mul, mul_one,
    zero_sub, abs_neg, add_zero]
  have hOne : 0 ≤ 1 - u := sub_nonneg.mpr hu1.le
  rw [abs_of_nonneg (mul_nonneg (mul_nonneg hOne Real.pi_pos.le) hu0)]
  nlinarith [mul_nonneg Real.pi_pos.le hu0]

/-- Frequency dilation of the Prawitz filter to the symmetric window
`[-T,T]`. -/
noncomputable def scaledPrawitzSmoothingFilter (T t : ℝ) : ℂ :=
  prawitzSmoothingFilter (t / T)

theorem scaledPrawitzSmoothingFilter_re_of_mem_Ico
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t < T) :
    (scaledPrawitzSmoothingFilter T t).re =
      (1 - t / T) * prawitzCotFactor (t / T) + t / T := by
  unfold scaledPrawitzSmoothingFilter
  exact prawitzSmoothingFilter_re_of_mem_Ico
    (div_nonneg ht0 hT.le) ((div_lt_one hT).2 htT)

theorem scaledPrawitzSmoothingFilter_im_of_mem_Ico
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t < T) :
    (scaledPrawitzSmoothingFilter T t).im =
      -((1 - t / T) * Real.pi * (t / T)) := by
  unfold scaledPrawitzSmoothingFilter
  exact prawitzSmoothingFilter_im_of_mem_Ico
    (div_nonneg ht0 hT.le) ((div_lt_one hT).2 htT)

/-- Exact norm of the scaled Prawitz filter inside a positive frequency
window. -/
theorem norm_scaledPrawitzSmoothingFilter_of_mem_Ico
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t < T) :
    ‖scaledPrawitzSmoothingFilter T t‖ =
      Real.sqrt
        (((1 - t / T) * prawitzCotFactor (t / T) + t / T) ^ 2 +
          ((1 - t / T) * Real.pi * (t / T)) ^ 2) := by
  rw [scaledPrawitzSmoothingFilter,
    norm_prawitzSmoothingFilter_of_mem_Ico
      (div_nonneg ht0 hT.le) ((div_lt_one hT).2 htT)]

/-- Square-root majorant for the scaled Prawitz filter norm inside a positive
frequency window. -/
theorem norm_scaledPrawitzSmoothingFilter_le_sqrt_one_add_sq
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t < T) :
    ‖scaledPrawitzSmoothingFilter T t‖ ≤
      Real.sqrt (1 + ((1 - t / T) * Real.pi * (t / T)) ^ 2) := by
  unfold scaledPrawitzSmoothingFilter
  exact norm_prawitzSmoothingFilter_le_sqrt_one_add_sq
    (div_nonneg ht0 hT.le) ((div_lt_one hT).2 htT)

/-- Polynomial majorant for the scaled Prawitz filter norm inside a positive
frequency window. -/
theorem norm_scaledPrawitzSmoothingFilter_le_one_add_half_im_sq
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t < T) :
    ‖scaledPrawitzSmoothingFilter T t‖ ≤
      1 + ((1 - t / T) * Real.pi * (t / T)) ^ 2 / 2 := by
  unfold scaledPrawitzSmoothingFilter
  exact norm_prawitzSmoothingFilter_le_one_add_half_im_sq
    (div_nonneg ht0 hT.le) ((div_lt_one hT).2 htT)

/-- Uniform scaled-window form of the positive Prawitz filter norm bound. -/
theorem norm_scaledPrawitzSmoothingFilter_le_one_add_pi_sq_div_thirtytwo
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t < T) :
    ‖scaledPrawitzSmoothingFilter T t‖ ≤ 1 + Real.pi ^ 2 / 32 := by
  unfold scaledPrawitzSmoothingFilter
  exact norm_prawitzSmoothingFilter_le_one_add_pi_sq_div_thirtytwo
    (div_nonneg ht0 hT.le) ((div_lt_one hT).2 htT)

theorem scaledPrawitzSmoothingFilter_zero (T : ℝ) :
    scaledPrawitzSmoothingFilter T 0 = 1 := by
  simp [scaledPrawitzSmoothingFilter, prawitzSmoothingFilter_zero]

theorem scaledPrawitzSmoothingFilter_eq_zero_of_abs_ge
    {T t : ℝ} (hT : 0 < T) (ht : T ≤ |t|) :
    scaledPrawitzSmoothingFilter T t = 0 := by
  apply prawitzSmoothingFilter_eq_zero_of_one_le_abs
  rw [abs_div, abs_of_pos hT]
  exact (le_div_iff₀ hT).2 (by simpa only [one_mul] using ht)

theorem scaledPrawitzSmoothingFilter_neg (T t : ℝ) :
    scaledPrawitzSmoothingFilter T (-t) =
      (starRingEnd ℂ) (scaledPrawitzSmoothingFilter T t) := by
  rw [scaledPrawitzSmoothingFilter, scaledPrawitzSmoothingFilter,
    neg_div, prawitzSmoothingFilter_neg]

theorem measurable_scaledPrawitzSmoothingFilter (T : ℝ) :
    Measurable (scaledPrawitzSmoothingFilter T) := by
  exact measurable_prawitzSmoothingFilter.comp (by fun_prop)

/-- Multiplying a characteristic function by a spatial Fourier phase retains
the first-moment linear modulus at frequency zero. -/
theorem norm_phase_mul_charFun_sub_one_le_firstMoment
    {mu : Measure ℝ} [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu) (x t : ℝ) :
    ‖Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t - 1‖ ≤
      |t| * (|x| + ∫ y, |y| ∂mu) := by
  let e : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I)
  have he : ‖e‖ = 1 := by
    dsimp only [e]
    rw [Complex.norm_exp_ofReal_mul_I]
  calc
    ‖e * MeasureTheory.charFun mu t - 1‖ =
        ‖e * (MeasureTheory.charFun mu t - 1) + (e - 1)‖ := by
      congr 1
      ring
    _ ≤ ‖e * (MeasureTheory.charFun mu t - 1)‖ + ‖e - 1‖ :=
      norm_add_le _ _
    _ = ‖MeasureTheory.charFun mu t - 1‖ + ‖e - 1‖ := by
      rw [norm_mul, he, one_mul]
    _ ≤ |t| * (∫ y, |y| ∂mu) + |t * x| := by
      apply add_le_add (norm_charFun_sub_one_le_firstMoment hmu1 t)
      dsimp only [e]
      simpa only [mul_comm Complex.I, Real.norm_eq_abs, abs_neg] using
        (Real.norm_exp_I_mul_ofReal_sub_one_le (x := -(t * x)))
    _ = |t| * (|x| + ∫ y, |y| ∂mu) := by
      rw [abs_mul]
      ring

/-- Conjugation symmetry required of a Fourier--Stieltjes transform of a real
measure. -/
def IsHermitianFrequencyFunction (f : ℝ → ℂ) : Prop :=
  ∀ t, f (-t) = (starRingEnd ℂ) (f t)

theorem isHermitianFrequencyFunction_charFun (mu : Measure ℝ) :
    IsHermitianFrequencyFunction (MeasureTheory.charFun mu) := by
  intro t
  exact MeasureTheory.charFun_neg t

/-- The nonsingular numerator of the Prawitz principal-value integrand. -/
noncomputable def prawitzPVNumerator
    (T : ℝ) (f : ℝ → ℂ) (x t : ℝ) : ℂ :=
  Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
    scaledPrawitzSmoothingFilter T t * f t

/-- The singular integrand in the Prawitz principal-value inversion formula. -/
noncomputable def prawitzPVIntegrand
    (T : ℝ) (f : ℝ → ℂ) (x t : ℝ) : ℂ :=
  prawitzPVNumerator T f x t / (t : ℂ)

theorem prawitzPVNumerator_sub (T : ℝ) (f g : ℝ → ℂ) (x t : ℝ) :
    prawitzPVNumerator T f x t - prawitzPVNumerator T g x t =
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        scaledPrawitzSmoothingFilter T t * (f t - g t) := by
  unfold prawitzPVNumerator
  ring

theorem measurable_prawitzPVNumerator
    {T : ℝ} {f : ℝ → ℂ} (hf : Measurable f) (x : ℝ) :
    Measurable (prawitzPVNumerator T f x) := by
  unfold prawitzPVNumerator
  have hphase : Measurable (fun t : ℝ =>
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I)) := by
    fun_prop
  exact (hphase.mul (measurable_scaledPrawitzSmoothingFilter T)).mul hf

/-- A finite first absolute moment makes the imaginary part of the Prawitz
numerator vanish linearly at zero, uniformly over its full positive window. -/
theorem abs_im_prawitzPVNumerator_charFun_le
    {mu : Measure ℝ} [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) {t : ℝ} (ht : t ∈ Ioc 0 T) :
    |(prawitzPVNumerator T (MeasureTheory.charFun mu) x t).im| ≤
      (7 * (|x| + ∫ y, |y| ∂mu) + Real.pi / T) * t := by
  have hMoment : 0 ≤ ∫ y, |y| ∂mu := integral_nonneg fun _ => abs_nonneg _
  have hD : 0 ≤ |x| + ∫ y, |y| ∂mu := add_nonneg (abs_nonneg x) hMoment
  rcases ht.2.eq_or_lt with htEq | htT
  · subst t
    have hFilter : scaledPrawitzSmoothingFilter T T = 0 := by
      rw [scaledPrawitzSmoothingFilter, div_self hT.ne',
        prawitzSmoothingFilter]
      norm_num
    rw [prawitzPVNumerator, hFilter, mul_zero, zero_mul,
      Complex.zero_im, abs_zero]
    exact mul_nonneg
      (add_nonneg (mul_nonneg (by norm_num) hD)
        (div_nonneg Real.pi_pos.le hT.le)) hT.le
  let u : ℝ := t / T
  let M : ℂ := prawitzSmoothingFilter u
  let q : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
    MeasureTheory.charFun mu t
  have hu0 : 0 ≤ u := div_nonneg ht.1.le hT.le
  have hu1 : u < 1 := (div_lt_one hT).2 htT
  have hMNorm : ‖M‖ ≤ 7 :=
    norm_prawitzSmoothingFilter_le_seven hu0 hu1
  have hMIm : |M.im| ≤ Real.pi * u :=
    abs_im_prawitzSmoothingFilter_le hu0 hu1
  have hq : ‖q - 1‖ ≤ t * (|x| + ∫ y, |y| ∂mu) := by
    simpa only [q, abs_of_pos ht.1] using
      norm_phase_mul_charFun_sub_one_le_firstMoment hmu1 x t
  have hNumerator :
      prawitzPVNumerator T (MeasureTheory.charFun mu) x t = M * q := by
    dsimp only [prawitzPVNumerator, M, q, scaledPrawitzSmoothingFilter, u]
    ring
  rw [hNumerator]
  have hDecomp : M * q = M * (q - 1) + M := by ring
  rw [hDecomp]
  calc
    |(M * (q - 1) + M).im| ≤
        |(M * (q - 1)).im| + |M.im| := by
      simpa only [Complex.add_im] using
        abs_add_le (M * (q - 1)).im M.im
    _ ≤ ‖M * (q - 1)‖ + |M.im| :=
      add_le_add (Complex.abs_im_le_norm _) le_rfl
    _ = ‖M‖ * ‖q - 1‖ + |M.im| := by rw [norm_mul]
    _ ≤ 7 * (t * (|x| + ∫ y, |y| ∂mu)) + Real.pi * u := by
      apply add_le_add
      · exact mul_le_mul hMNorm hq (norm_nonneg _) (by norm_num)
      · exact hMIm
    _ = (7 * (|x| + ∫ y, |y| ∂mu) + Real.pi / T) * t := by
      dsimp only [u]
      ring

theorem measurable_prawitzPVIntegrand
    {T : ℝ} {f : ℝ → ℂ} (hf : Measurable f) (x : ℝ) :
    Measurable (prawitzPVIntegrand T f x) := by
  unfold prawitzPVIntegrand
  exact (measurable_prawitzPVNumerator hf x).div
    (Complex.continuous_ofReal.measurable.comp measurable_id)

theorem prawitzPVIntegrand_neg
    {T : ℝ} {f : ℝ → ℂ} (hf : IsHermitianFrequencyFunction f)
    (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    prawitzPVIntegrand T f x (-t) =
      -(starRingEnd ℂ) (prawitzPVIntegrand T f x t) := by
  have hexp :
      Complex.exp (((-((-t) * x) : ℝ) : ℂ) * Complex.I) =
        (starRingEnd ℂ)
          (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I)) := by
    have harg :
        (((-((-t) * x) : ℝ) : ℂ) * Complex.I) =
          (starRingEnd ℂ) (((-(t * x) : ℝ) : ℂ) * Complex.I) := by
      apply Complex.ext <;> simp <;> ring
    rw [harg, Complex.exp_conj]
  rw [prawitzPVIntegrand, prawitzPVIntegrand,
    prawitzPVNumerator, prawitzPVNumerator, hexp,
    scaledPrawitzSmoothingFilter_neg, hf]
  simp only [map_div₀, map_mul, Complex.conj_ofReal, Complex.ofReal_neg]
  rw [div_neg]

/-- Pairing positive and negative frequencies removes the principal-value
singularity algebraically. -/
noncomputable def symmetricPrawitzPVIntegrand
    (T : ℝ) (f : ℝ → ℂ) (x t : ℝ) : ℂ :=
  prawitzPVIntegrand T f x t + prawitzPVIntegrand T f x (-t)

theorem measurable_symmetricPrawitzPVIntegrand
    {T : ℝ} {f : ℝ → ℂ} (hf : Measurable f) (x : ℝ) :
    Measurable (symmetricPrawitzPVIntegrand T f x) := by
  exact (measurable_prawitzPVIntegrand hf x).add
    ((measurable_prawitzPVIntegrand hf x).comp measurable_neg)

/-- Pairing converts the norm of the principal-value integrand to the
imaginary part of its nonsingular positive-frequency numerator. -/
theorem norm_symmetricPrawitzPVIntegrand
    {T : ℝ} {f : ℝ → ℂ} (hf : IsHermitianFrequencyFunction f)
    (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    ‖symmetricPrawitzPVIntegrand T f x t‖ =
      2 * |(prawitzPVNumerator T f x t).im| / |t| := by
  rw [symmetricPrawitzPVIntegrand, prawitzPVIntegrand_neg hf x ht,
    ← sub_eq_add_neg, Complex.sub_conj, norm_mul, Complex.norm_real,
    Complex.norm_I, mul_one, Real.norm_eq_abs, abs_mul,
    abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
  rw [prawitzPVIntegrand, Complex.div_ofReal_im, abs_div]
  ring

/-- Exact signed form of the paired Prawitz integrand at nonzero frequency. -/
theorem symmetricPrawitzPVIntegrand_eq_two_mul_im
    {T : ℝ} {f : ℝ → ℂ} (hf : IsHermitianFrequencyFunction f)
    (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    symmetricPrawitzPVIntegrand T f x t =
      ((2 * (prawitzPVNumerator T f x t).im / t : ℝ) : ℂ) * Complex.I := by
  rw [symmetricPrawitzPVIntegrand, prawitzPVIntegrand_neg hf x ht,
    ← sub_eq_add_neg, Complex.sub_conj, prawitzPVIntegrand,
    Complex.div_ofReal_im]
  push_cast
  ring

/-- Real part of a signed difference of paired Prawitz integrands, expressed
without complex norms through the positive-frequency numerators. -/
theorem re_prawitzCoefficient_mul_symmetric_sub
    {T : ℝ} {f g : ℝ → ℂ}
    (hf : IsHermitianFrequencyFunction f)
    (hg : IsHermitianFrequencyFunction g)
    (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        (symmetricPrawitzPVIntegrand T f x t -
          symmetricPrawitzPVIntegrand T g x t)).re =
      -((prawitzPVNumerator T f x t).im -
          (prawitzPVNumerator T g x t).im) /
        (Real.pi * t) := by
  have hcoef :
      Complex.I / (2 * Real.pi : ℝ) =
        (((2 * Real.pi)⁻¹ : ℝ) : ℂ) * Complex.I := by
    rw [div_eq_mul_inv, ← Complex.ofReal_inv]
    ring
  rw [symmetricPrawitzPVIntegrand_eq_two_mul_im hf x ht,
    symmetricPrawitzPVIntegrand_eq_two_mul_im hg x ht, hcoef]
  change (((((2 * Real.pi)⁻¹ : ℝ) : ℂ) * Complex.I) *
      ((((2 * (prawitzPVNumerator T f x t).im / t : ℝ) : ℂ) * Complex.I) -
        (((2 * (prawitzPVNumerator T g x t).im / t : ℝ) : ℂ) *
          Complex.I))).re = _
  simp only [Complex.I_re, Complex.I_im, Complex.ofReal_re,
    Complex.ofReal_im, Complex.mul_re, Complex.mul_im, Complex.sub_re,
    Complex.sub_im, zero_mul, mul_one, sub_zero, zero_sub, add_zero,
    zero_add]
  field_simp [Real.pi_ne_zero, ht]
  ring

/-- A linear bound on the imaginary part of the paired numerator removes the
Prawitz singularity and supplies absolute integrability on a finite window. -/
theorem integrableOn_symmetricPrawitzPVIntegrand_of_im_le
    {T C : ℝ} {f : ℝ → ℂ} (hT : 0 ≤ T) (hC : 0 ≤ C)
    (hfHermitian : IsHermitianFrequencyFunction f) (hfMeasurable : Measurable f)
    (hIm : ∀ t ∈ Ioc 0 T,
      |(prawitzPVNumerator T f x t).im| ≤ C * t) :
    IntegrableOn (symmetricPrawitzPVIntegrand T f x) (Icc 0 T) := by
  apply Measure.integrableOn_of_bounded
      (μ := volume) (s := Icc 0 T) (M := 2 * C)
  · simp [Real.volume_Icc]
  · exact Measurable.aestronglyMeasurable
      (measurable_symmetricPrawitzPVIntegrand hfMeasurable x)
  · filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
    rcases eq_or_lt_of_le ht.1 with rfl | ht0
    · simpa [symmetricPrawitzPVIntegrand, prawitzPVIntegrand] using hC
    rw [norm_symmetricPrawitzPVIntegrand hfHermitian x ht0.ne']
    rw [abs_of_pos ht0]
    calc
      2 * |(prawitzPVNumerator T f x t).im| / t ≤
          2 * (C * t) / t :=
        div_le_div_of_nonneg_right
          (mul_le_mul_of_nonneg_left (hIm t ⟨ht0, ht.2⟩) (by norm_num)) ht0.le
      _ = 2 * C := by field_simp

/-- The paired Prawitz principal-value integrand of any probability law with
a finite first absolute moment is absolutely integrable on every positive
frequency window. -/
theorem integrableOn_symmetricPrawitzPVIntegrand_charFun
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    IntegrableOn
      (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x)
      (Icc 0 T) := by
  let C : ℝ := 7 * (|x| + ∫ y, |y| ∂mu) + Real.pi / T
  have hMoment : 0 ≤ ∫ y, |y| ∂mu := integral_nonneg fun _ => abs_nonneg _
  have hC : 0 ≤ C := by
    dsimp only [C]
    positivity
  apply integrableOn_symmetricPrawitzPVIntegrand_of_im_le
    hT.le hC (isHermitianFrequencyFunction_charFun mu)
    MeasureTheory.measurable_charFun
  intro t ht
  exact abs_im_prawitzPVNumerator_charFun_le hmu1 hT x ht

theorem star_symmetricPrawitzPVIntegrand
    {T : ℝ} {f : ℝ → ℂ} (hf : IsHermitianFrequencyFunction f)
    (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t) =
      -symmetricPrawitzPVIntegrand T f x t := by
  rw [symmetricPrawitzPVIntegrand, map_add,
    prawitzPVIntegrand_neg hf x ht]
  simp

/-- Symmetrically truncated Prawitz inversion functional. The eventual
principal value is obtained by sending `epsilon` to zero. -/
noncomputable def truncatedPrawitzInversion
    (T epsilon : ℝ) (f : ℝ → ℂ) (x : ℝ) : ℂ :=
  (Complex.I / (2 * Real.pi : ℝ)) *
    ∫ t in Icc epsilon T, symmetricPrawitzPVIntegrand T f x t

theorem star_truncatedPrawitzInversion
    {T epsilon : ℝ} {f : ℝ → ℂ}
    (hf : IsHermitianFrequencyFunction f) (hepsilon : 0 < epsilon)
    (x : ℝ) :
    (starRingEnd ℂ) (truncatedPrawitzInversion T epsilon f x) =
      truncatedPrawitzInversion T epsilon f x := by
  have hstar : ∀ t ∈ Icc epsilon T,
      (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t) =
        -symmetricPrawitzPVIntegrand T f x t := by
    intro t ht
    exact star_symmetricPrawitzPVIntegrand hf x (ne_of_gt (hepsilon.trans_le ht.1))
  rw [truncatedPrawitzInversion, map_mul, map_div₀,
    Complex.conj_I, Complex.conj_ofReal]
  have hInt :
      (∫ t in Icc epsilon T,
          (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t)) =
        -(∫ t in Icc epsilon T,
          symmetricPrawitzPVIntegrand T f x t) := by
    rw [← integral_neg]
    apply integral_congr_ae
    filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
    exact hstar t ht
  have hConj :
      (starRingEnd ℂ) (∫ t in Icc epsilon T,
          symmetricPrawitzPVIntegrand T f x t) =
        ∫ t in Icc epsilon T,
          (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t) := by
    exact (integral_conj
      (μ := volume.restrict (Icc epsilon T))
      (f := fun t => symmetricPrawitzPVIntegrand T f x t)).symm
  rw [hConj, hInt]
  ring

theorem truncatedPrawitzInversion_im_eq_zero
    {T epsilon : ℝ} {f : ℝ → ℂ}
    (hf : IsHermitianFrequencyFunction f) (hepsilon : 0 < epsilon)
    (x : ℝ) :
    (truncatedPrawitzInversion T epsilon f x).im = 0 := by
  have hstar := star_truncatedPrawitzInversion (T := T) hf hepsilon x
  apply_fun Complex.im at hstar
  simp only [map_neg, Complex.conj_im] at hstar
  linarith

/-- The full paired Prawitz inversion functional, defined when the removable
singularity at zero is integrable. -/
noncomputable def prawitzInversion
    (T : ℝ) (f : ℝ → ℂ) (x : ℝ) : ℂ :=
  (Complex.I / (2 * Real.pi : ℝ)) *
    ∫ t in Icc 0 T, symmetricPrawitzPVIntegrand T f x t

/-- Hermitian frequency symmetry makes the full paired Prawitz inversion
functional real-valued. -/
theorem star_prawitzInversion
    {T : ℝ} {f : ℝ → ℂ} (hf : IsHermitianFrequencyFunction f) (x : ℝ) :
    (starRingEnd ℂ) (prawitzInversion T f x) = prawitzInversion T f x := by
  have hstar : ∀ t ∈ Icc 0 T,
      (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t) =
        -symmetricPrawitzPVIntegrand T f x t := by
    intro t ht
    by_cases ht0 : t = 0
    · subst t
      simp [symmetricPrawitzPVIntegrand, prawitzPVIntegrand]
    · exact star_symmetricPrawitzPVIntegrand hf x ht0
  rw [prawitzInversion, map_mul, map_div₀,
    Complex.conj_I, Complex.conj_ofReal]
  have hInt :
      (∫ t in Icc 0 T,
          (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t)) =
        -(∫ t in Icc 0 T,
          symmetricPrawitzPVIntegrand T f x t) := by
    rw [← integral_neg]
    apply integral_congr_ae
    filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
    exact hstar t ht
  have hConj :
      (starRingEnd ℂ) (∫ t in Icc 0 T,
          symmetricPrawitzPVIntegrand T f x t) =
        ∫ t in Icc 0 T,
          (starRingEnd ℂ) (symmetricPrawitzPVIntegrand T f x t) := by
    exact (integral_conj
      (μ := volume.restrict (Icc 0 T))
      (f := fun t => symmetricPrawitzPVIntegrand T f x t)).symm
  rw [hConj, hInt]
  ring

theorem prawitzInversion_im_eq_zero
    {T : ℝ} {f : ℝ → ℂ} (hf : IsHermitianFrequencyFunction f) (x : ℝ) :
    (prawitzInversion T f x).im = 0 := by
  have hstar := star_prawitzInversion (T := T) hf x
  apply_fun Complex.im at hstar
  simp only [Complex.conj_im] at hstar
  linarith

/-- Translating a point mass and its evaluation point by the same amount does
not change the Prawitz principal-value integrand. -/
theorem prawitzPVIntegrand_charFun_dirac_translate
    (T x y t : ℝ) :
    prawitzPVIntegrand T (MeasureTheory.charFun (Measure.dirac y)) x t =
      prawitzPVIntegrand T
        (MeasureTheory.charFun (Measure.dirac (y - x))) 0 t := by
  unfold prawitzPVIntegrand prawitzPVNumerator
  rw [MeasureTheory.charFun_dirac, MeasureTheory.charFun_dirac]
  rw [show inner ℝ y t = y * t by
        change t * y = y * t
        ring,
      show inner ℝ (y - x) t = (y - x) * t by
        change t * (y - x) = (y - x) * t
        ring]
  simp only [mul_zero, neg_zero, map_zero, zero_mul, Complex.exp_zero, one_mul]
  have hExp :
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          Complex.exp ((((y * t : ℝ) : ℂ)) * Complex.I) =
        Complex.exp (((((y - x) * t : ℝ) : ℂ)) * Complex.I) := by
    rw [← Complex.exp_add]
    congr 1
    push_cast
    ring
  rw [show Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          scaledPrawitzSmoothingFilter T t *
            Complex.exp ((((y * t : ℝ) : ℂ)) * Complex.I) =
        scaledPrawitzSmoothingFilter T t *
          (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
            Complex.exp ((((y * t : ℝ) : ℂ)) * Complex.I)) by ring,
    hExp]
  simp

theorem symmetricPrawitzPVIntegrand_charFun_dirac_translate
    (T x y t : ℝ) :
    symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (Measure.dirac y)) x t =
      symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (Measure.dirac (y - x))) 0 t := by
  unfold symmetricPrawitzPVIntegrand
  exact congrArg₂ (· + ·)
    (prawitzPVIntegrand_charFun_dirac_translate T x y t)
    (prawitzPVIntegrand_charFun_dirac_translate T x y (-t))

theorem prawitzInversion_charFun_dirac_translate (T x y : ℝ) :
    prawitzInversion T (MeasureTheory.charFun (Measure.dirac y)) x =
      prawitzInversion T
        (MeasureTheory.charFun (Measure.dirac (y - x))) 0 := by
  unfold prawitzInversion
  congr 1
  apply integral_congr_ae
  exact ae_of_all _ fun t =>
    symmetricPrawitzPVIntegrand_charFun_dirac_translate T x y t

/-- The deterministic Prawitz point kernel, normalized so its argument is the
displacement from the CDF evaluation point. -/
noncomputable def prawitzPointKernel (T z : ℝ) : ℝ :=
  (prawitzInversion T
    (MeasureTheory.charFun (Measure.dirac z)) 0).re

theorem prawitzInversion_charFun_dirac_eq_pointKernel (T x y : ℝ) :
    prawitzInversion T (MeasureTheory.charFun (Measure.dirac y)) x =
      (prawitzPointKernel T (y - x) : ℂ) := by
  rw [prawitzInversion_charFun_dirac_translate]
  apply Complex.ext
  · rfl
  · simpa only [Complex.ofReal_im] using
      prawitzInversion_im_eq_zero
        (T := T) (isHermitianFrequencyFunction_charFun (Measure.dirac (y - x))) 0

/-- Positive dilation of the frequency window reduces a point-mass Prawitz
integrand to the unit window with the reciprocal Jacobian factor. -/
theorem prawitzPVIntegrand_charFun_dirac_scale
    {T : ℝ} (hT : 0 < T) (z u : ℝ) :
    prawitzPVIntegrand T (MeasureTheory.charFun (Measure.dirac z)) 0 (T * u) =
      ((T : ℂ)⁻¹) * prawitzPVIntegrand 1
        (MeasureTheory.charFun (Measure.dirac (T * z))) 0 u := by
  by_cases hu : u = 0
  · subst u
    simp [prawitzPVIntegrand]
  unfold prawitzPVIntegrand prawitzPVNumerator
  rw [MeasureTheory.charFun_dirac, MeasureTheory.charFun_dirac]
  rw [show inner ℝ z (T * u) = z * (T * u) by
        change (T * u) * z = z * (T * u)
        ring,
      show inner ℝ (T * z) u = (T * z) * u by
        change u * (T * z) = (T * z) * u
        ring]
  simp only [mul_zero, neg_zero, map_zero, zero_mul, Complex.exp_zero, one_mul,
    scaledPrawitzSmoothingFilter]
  rw [mul_div_cancel_left₀ u hT.ne', div_one]
  have hphase : z * (T * u) = (T * z) * u := by ring
  rw [hphase]
  push_cast
  field_simp

theorem symmetricPrawitzPVIntegrand_charFun_dirac_scale
    {T : ℝ} (hT : 0 < T) (z u : ℝ) :
    symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (Measure.dirac z)) 0 (T * u) =
      ((T : ℂ)⁻¹) * symmetricPrawitzPVIntegrand 1
        (MeasureTheory.charFun (Measure.dirac (T * z))) 0 u := by
  unfold symmetricPrawitzPVIntegrand
  rw [prawitzPVIntegrand_charFun_dirac_scale hT z u,
    show -(T * u) = T * (-u) by ring,
    prawitzPVIntegrand_charFun_dirac_scale hT z (-u)]
  ring

theorem prawitzInversion_charFun_dirac_scale
    {T : ℝ} (hT : 0 < T) (z : ℝ) :
    prawitzInversion T (MeasureTheory.charFun (Measure.dirac z)) 0 =
      prawitzInversion 1
        (MeasureTheory.charFun (Measure.dirac (T * z))) 0 := by
  let gT : ℝ → ℂ := fun t => symmetricPrawitzPVIntegrand T
    (MeasureTheory.charFun (Measure.dirac z)) 0 t
  let g1 : ℝ → ℂ := fun u => symmetricPrawitzPVIntegrand 1
    (MeasureTheory.charFun (Measure.dirac (T * z))) 0 u
  have hsetT : (∫ t in Icc 0 T, gT t) = ∫ t in 0..T, gT t := by
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le hT.le]
  have hset1 : (∫ u in Icc (0 : ℝ) 1, g1 u) = ∫ u in 0..1, g1 u := by
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)]
  have hchange :
      (∫ t in 0..T, gT t) = T • ∫ u in 0..1, gT (T * u) := by
    symm
    simpa only [mul_zero, mul_one] using
      (intervalIntegral.smul_integral_comp_mul_left
        (f := gT) (a := 0) (b := 1) T)
  have hpoint : ∀ u, gT (T * u) = ((T : ℂ)⁻¹) * g1 u := by
    intro u
    exact symmetricPrawitzPVIntegrand_charFun_dirac_scale hT z u
  have hintegral : (∫ t in Icc 0 T, gT t) = ∫ u in Icc (0 : ℝ) 1, g1 u := by
    rw [hsetT, hset1, hchange]
    have hcongr :
        (∫ u in 0..1, gT (T * u)) =
          ∫ u in 0..1, ((T : ℂ)⁻¹) * g1 u := by
      apply intervalIntegral.integral_congr
      intro u hu
      exact hpoint u
    have hconst :
        (∫ u in 0..1, ((T : ℂ)⁻¹) * g1 u) =
          ((T : ℂ)⁻¹) * ∫ u in 0..1, g1 u :=
      intervalIntegral.integral_const_mul (T : ℂ)⁻¹ g1
    rw [hcongr, hconst, Complex.real_smul]
    change (T : ℂ) * ((T : ℂ)⁻¹ * ∫ u in 0..1, g1 u) =
      ∫ u in 0..1, g1 u
    have hTC : (T : ℂ) ≠ 0 := by exact_mod_cast hT.ne'
    field_simp [hTC]
  unfold prawitzInversion
  change (Complex.I / (2 * Real.pi : ℝ)) * (∫ t in Icc 0 T, gT t) =
    (Complex.I / (2 * Real.pi : ℝ)) * (∫ u in Icc 0 1, g1 u)
  rw [hintegral]

theorem prawitzPointKernel_scale
    {T : ℝ} (hT : 0 < T) (z : ℝ) :
    prawitzPointKernel T z = prawitzPointKernel 1 (T * z) := by
  unfold prawitzPointKernel
  rw [prawitzInversion_charFun_dirac_scale hT z]

/-- The real unit-window integrand whose integral is the deterministic
Prawitz point kernel. -/
noncomputable def prawitzUnitPointIntegrand (z u : ℝ) : ℝ :=
  ((Complex.I / (2 * Real.pi : ℝ)) *
    symmetricPrawitzPVIntegrand 1
      (MeasureTheory.charFun (Measure.dirac z)) 0 u).re

/-- Away from the removable endpoint, the real point integrand is the
negative imaginary part of the nonsingular numerator divided by `πu`. -/
theorem prawitzUnitPointIntegrand_eq_neg_im_div
    (z : ℝ) {u : ℝ} (hu : u ≠ 0) :
    prawitzUnitPointIntegrand z u =
      -(prawitzPVNumerator 1
          (MeasureTheory.charFun (Measure.dirac z)) 0 u).im /
        (Real.pi * u) := by
  have hcomplex (a : ℝ) :
      (Complex.I / (2 * Real.pi : ℝ)) *
          (((2 * a : ℝ) : ℂ) * Complex.I) =
        ((-a / Real.pi : ℝ) : ℂ) := by
    apply Complex.ext <;>
      simp [Complex.div_re, Complex.div_im, Complex.normSq_apply]
    field_simp [Real.pi_ne_zero]
  rw [prawitzUnitPointIntegrand, symmetricPrawitzPVIntegrand,
    prawitzPVIntegrand_neg
      (isHermitianFrequencyFunction_charFun (Measure.dirac z)) 0 hu,
    ← sub_eq_add_neg, Complex.sub_conj, prawitzPVIntegrand,
    Complex.div_ofReal_im, hcomplex]
  simp only [Complex.ofReal_re]
  field_simp [Real.pi_ne_zero, hu]

/-- Explicit real trigonometric form of the unit-window Prawitz point
integrand on the open positive frequency window. -/
theorem prawitzUnitPointIntegrand_eq_explicit
    (z : ℝ) {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzUnitPointIntegrand z u =
      (1 - u) * Real.cos (z * u) -
        (((1 - u) * prawitzCotFactor u + u) * Real.sin (z * u)) /
          (Real.pi * u) := by
  rw [prawitzUnitPointIntegrand_eq_neg_im_div z hu0.ne']
  unfold prawitzPVNumerator scaledPrawitzSmoothingFilter
  simp only [div_one]
  rw [MeasureTheory.charFun_dirac]
  rw [show inner ℝ z u = z * u by
    change u * z = z * u
    ring]
  simp only [mul_zero, neg_zero, Complex.ofReal_zero, zero_mul,
    Complex.exp_zero, one_mul]
  rw [prawitzSmoothingFilter,
    if_pos (by simpa [abs_of_pos hu0] using hu1), abs_of_pos hu0,
    Complex.exp_ofReal_mul_I]
  simp only [Complex.mul_im, Complex.sub_re, Complex.ofReal_re,
    Complex.mul_re, Complex.I_re, mul_zero, Complex.I_im, mul_one,
    add_zero, Complex.sub_im, Complex.ofReal_im, zero_sub,
    Complex.add_re, Complex.add_im]
  field_simp [Real.pi_ne_zero, hu0.ne']
  ring

/-- The only non-elementary spatial term left by the explicit Prawitz point
integrand on the positive frequency window. -/
noncomputable def prawitzCotangentSineIntegrand (x u : ℝ) : ℝ :=
  (1 - u) * Real.cot (Real.pi * u) * Real.sin (2 * Real.pi * x * u)

/-- At spatial argument `-2πx`, the Prawitz point integrand splits into an
elementary cosine term, an elementary sine term, and the cotangent term. -/
theorem prawitzUnitPointIntegrand_neg_two_pi_eq
    (x : ℝ) {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzUnitPointIntegrand (-2 * Real.pi * x) u =
      (1 - u) * Real.cos (2 * Real.pi * x * u) +
        Real.sin (2 * Real.pi * x * u) / Real.pi +
          prawitzCotangentSineIntegrand x u := by
  rw [prawitzUnitPointIntegrand_eq_explicit (-2 * Real.pi * x) hu0 hu1]
  rw [show -2 * Real.pi * x * u = -(2 * Real.pi * x * u) by ring,
    Real.cos_neg, Real.sin_neg]
  rw [prawitzCotFactor, if_neg hu0.ne']
  unfold prawitzCotangentSineIntegrand
  field_simp [Real.pi_ne_zero, hu0.ne']
  ring

/-- Endpoint-regularized terms in the real cotangent expansion. The zeroth
term is algebraically canceled at `u = 1`; later terms retain the symmetric
partial-fraction form. -/
noncomputable def prawitzCotSeriesTerm (x : ℝ) : ℕ → ℝ → ℝ
  | 0, u => (-2 * u / (Real.pi * (u + 1))) *
      Real.sin (2 * Real.pi * x * u)
  | n + 1, u => ((1 - u) / Real.pi) *
      (1 / (u - ((n : ℝ) + 2)) + 1 / (u + ((n : ℝ) + 2))) *
        Real.sin (2 * Real.pi * x * u)

theorem prawitzCotSeriesTerm_eq
    (x : ℝ) (n : ℕ) {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzCotSeriesTerm x n u =
      ((1 - u) / Real.pi) *
        (1 / (u - ((n : ℝ) + 1)) + 1 / (u + ((n : ℝ) + 1))) *
          Real.sin (2 * Real.pi * x * u) := by
  cases n with
  | zero =>
      simp only [prawitzCotSeriesTerm, Nat.cast_zero, zero_add]
      have hu1ne : u - 1 ≠ 0 := sub_ne_zero.mpr hu1.ne
      have hupne : u + 1 ≠ 0 := by positivity
      field_simp [Real.pi_ne_zero, hu0.ne', hu1ne, hupne]
      ring
  | succ n =>
      simp only [prawitzCotSeriesTerm, Nat.cast_add, Nat.cast_one]
      congr 3 <;> ring

theorem summable_prawitzCotSeriesTerm
    (x : ℝ) {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    Summable (fun n => prawitzCotSeriesTerm x n u) := by
  have hs := (summable_real_cot_series hu0 hu1).mul_left
    (((1 - u) / Real.pi) * Real.sin (2 * Real.pi * x * u))
  apply hs.congr
  intro n
  rw [prawitzCotSeriesTerm_eq x n hu0 hu1]
  ring

/-- The removable `1/u` term separated from the real cotangent series. -/
noncomputable def prawitzCotBaseIntegrand (x u : ℝ) : ℝ :=
  (1 - u) * Real.sin (2 * Real.pi * x * u) / (Real.pi * u)

/-- Pointwise Mittag--Leffler expansion of the cotangent--sine integrand on
the open unit interval, with the endpoint-regularized series terms. -/
theorem prawitzCotangentSineIntegrand_eq_base_add_tsum
    (x : ℝ) {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzCotangentSineIntegrand x u =
      prawitzCotBaseIntegrand x u + ∑' n, prawitzCotSeriesTerm x n u := by
  have hcot := real_cot_series_rep hu0 hu1
  have htsum :
      (∑' n, prawitzCotSeriesTerm x n u) =
        (((1 - u) / Real.pi) * Real.sin (2 * Real.pi * x * u)) *
          ∑' n : ℕ,
            (1 / (u - ((n : ℝ) + 1)) +
              1 / (u + ((n : ℝ) + 1))) := by
    rw [← tsum_mul_left]
    apply tsum_congr
    intro n
    rw [prawitzCotSeriesTerm_eq x n hu0 hu1]
    ring
  rw [htsum]
  unfold prawitzCotangentSineIntegrand prawitzCotBaseIntegrand
  rw [← hcot]
  field_simp [Real.pi_ne_zero, hu0.ne']
  ring

/-- Uniform inverse-square bound for the endpoint-regularized cotangent
series on the closed unit interval. -/
theorem abs_prawitzCotSeriesTerm_le
    (x : ℝ) (n : ℕ) {u : ℝ} (hu : u ∈ Icc (0 : ℝ) 1) :
    |prawitzCotSeriesTerm x n u| ≤ 4 / ((n : ℝ) + 1) ^ 2 := by
  cases n with
  | zero =>
      simp only [prawitzCotSeriesTerm, Nat.cast_zero, zero_add, one_pow,
        div_one]
      have hup : 0 < u + 1 := by linarith [hu.1]
      have hden : 0 < Real.pi * (u + 1) := mul_pos Real.pi_pos hup
      have hcoef : 2 * u / (Real.pi * (u + 1)) ≤ 4 := by
        apply (div_le_iff₀ hden).2
        nlinarith [Real.two_le_pi]
      calc
        |(-2 * u / (Real.pi * (u + 1))) *
            Real.sin (2 * Real.pi * x * u)| =
            (2 * u / (Real.pi * (u + 1))) *
              |Real.sin (2 * Real.pi * x * u)| := by
          rw [abs_mul, abs_div,
            abs_of_nonpos (by linarith [hu.1] : -2 * u ≤ 0),
            abs_of_pos hden]
          ring
        _ ≤ (2 * u / (Real.pi * (u + 1))) * 1 := by
          exact mul_le_mul_of_nonneg_left (Real.abs_sin_le_one _)
            (div_nonneg (mul_nonneg (by norm_num) hu.1) hden.le)
        _ ≤ 4 := by simpa using hcoef
  | succ n =>
      let m : ℝ := (n : ℝ) + 2
      have hm : 2 ≤ m := by
        dsimp [m]
        exact le_add_of_nonneg_left (Nat.cast_nonneg n)
      have hum : u - m < 0 := by linarith [hu.2]
      have hup : 0 < u + m := by linarith [hu.1]
      have hden : 0 < m ^ 2 - u ^ 2 := by
        nlinarith [sq_nonneg (m - u)]
      have hqeq :
          1 / (u - m) + 1 / (u + m) = -2 * u / (m ^ 2 - u ^ 2) := by
        field_simp [hum.ne, hup.ne']
        ring
      have hqabs :
          |1 / (u - m) + 1 / (u + m)| =
            2 * u / (m ^ 2 - u ^ 2) := by
        rw [hqeq, abs_div,
          abs_of_nonpos (by linarith [hu.1] : -2 * u ≤ 0),
          abs_of_pos hden]
        ring
      have hq :
          |1 / (u - m) + 1 / (u + m)| ≤ 4 / m ^ 2 := by
        rw [hqabs]
        have hmpos : 0 < m ^ 2 := sq_pos_of_pos (by linarith : 0 < m)
        apply (div_le_div_iff₀ hden hmpos).2
        have hu_mul : 0 ≤ u * (1 - u) :=
          mul_nonneg hu.1 (sub_nonneg.mpr hu.2)
        have hu_sq : u ^ 2 ≤ 1 := by nlinarith
        have hm_sq : 4 ≤ m ^ 2 := by nlinarith [sq_nonneg (m - 2)]
        have hleft : 2 * u * m ^ 2 ≤ 2 * m ^ 2 := by
          exact mul_le_mul_of_nonneg_right
            (by linarith [hu.2] : 2 * u ≤ 2) (sq_nonneg m)
        have hright : 2 * m ^ 2 ≤ 4 * (m ^ 2 - u ^ 2) := by
          nlinarith
        exact hleft.trans hright
      have hfac : |(1 - u) / Real.pi| ≤ 1 := by
        rw [abs_div, abs_of_nonneg (by linarith [hu.2] : 0 ≤ 1 - u),
          abs_of_pos Real.pi_pos]
        apply (div_le_one Real.pi_pos).2
        linarith [Real.two_le_pi, hu.1]
      simp only [prawitzCotSeriesTerm, Nat.cast_add, Nat.cast_one]
      rw [show (n : ℝ) + 1 + 1 = (n : ℝ) + 2 by ring]
      change |((1 - u) / Real.pi) *
          (1 / (u - m) + 1 / (u + m)) *
            Real.sin (2 * Real.pi * x * u)| ≤ 4 / m ^ 2
      rw [abs_mul, abs_mul]
      calc
        |(1 - u) / Real.pi| *
              |1 / (u - m) + 1 / (u + m)| *
            |Real.sin (2 * Real.pi * x * u)| ≤
            |(1 - u) / Real.pi| *
              |1 / (u - m) + 1 / (u + m)| * 1 := by
          exact mul_le_mul_of_nonneg_left (Real.abs_sin_le_one _) (by positivity)
        _ ≤ |1 / (u - m) + 1 / (u + m)| := by
          simpa only [mul_one] using
            (mul_le_of_le_one_left (abs_nonneg _) hfac)
        _ ≤ 4 / m ^ 2 := hq

theorem integrableOn_prawitzCotSeriesTerm (x : ℝ) (n : ℕ) :
    IntegrableOn (prawitzCotSeriesTerm x n) (Icc 0 1) := by
  have hmeas : AEStronglyMeasurable (prawitzCotSeriesTerm x n)
      (volume.restrict (Icc (0 : ℝ) 1)) := by
    apply Measurable.aestronglyMeasurable
    cases n with
    | zero =>
        change Measurable (fun u : ℝ =>
          (-2 * u / (Real.pi * (u + 1))) *
            Real.sin (2 * Real.pi * x * u))
        fun_prop
    | succ n =>
        change Measurable (fun u : ℝ => ((1 - u) / Real.pi) *
          (1 / (u - ((n : ℝ) + 2)) + 1 / (u + ((n : ℝ) + 2))) *
            Real.sin (2 * Real.pi * x * u))
        fun_prop
  apply (integrable_const (4 / ((n : ℝ) + 1) ^ 2 : ℝ)).mono' hmeas
  filter_upwards [ae_restrict_mem measurableSet_Icc] with u hu
  rw [Real.norm_eq_abs]
  exact abs_prawitzCotSeriesTerm_le x n hu

theorem integral_norm_prawitzCotSeriesTerm_le (x : ℝ) (n : ℕ) :
    (∫ u in Icc (0 : ℝ) 1, ‖prawitzCotSeriesTerm x n u‖) ≤
      4 / ((n : ℝ) + 1) ^ 2 := by
  calc
    _ ≤ ∫ _u in Icc (0 : ℝ) 1, (4 / ((n : ℝ) + 1) ^ 2 : ℝ) := by
      exact MeasureTheory.integral_mono_ae
        (integrableOn_prawitzCotSeriesTerm x n).norm
        (integrable_const _) (by
          filter_upwards [ae_restrict_mem measurableSet_Icc] with u hu
          rw [Real.norm_eq_abs]
          exact abs_prawitzCotSeriesTerm_le x n hu)
    _ = 4 / ((n : ℝ) + 1) ^ 2 := by simp

theorem summable_integral_norm_prawitzCotSeriesTerm (x : ℝ) :
    Summable (fun n : ℕ =>
      ∫ u in Icc (0 : ℝ) 1, ‖prawitzCotSeriesTerm x n u‖) := by
  have hs : Summable (fun n : ℕ => 4 / ((n : ℝ) + 1) ^ 2) := by
    have h := (summable_prawitzInverseSquareTail (0 : ℝ)).mul_left 4
    simpa using h
  exact Summable.of_nonneg_of_le
    (fun n => integral_nonneg fun _ => norm_nonneg _)
    (fun n => integral_norm_prawitzCotSeriesTerm_le x n) hs

/-- The endpoint-regularized cotangent series may be integrated term by
term on the unit interval. -/
theorem integral_tsum_prawitzCotSeriesTerm (x : ℝ) :
    (∫ u : ℝ in 0..1, ∑' n, prawitzCotSeriesTerm x n u) =
      ∑' n, ∫ u : ℝ in 0..1, prawitzCotSeriesTerm x n u := by
  have hset := (MeasureTheory.integral_tsum_of_summable_integral_norm
    (fun n => integrableOn_prawitzCotSeriesTerm x n)
    (summable_integral_norm_prawitzCotSeriesTerm x)).symm
  change (∫ u in Icc (0 : ℝ) 1, ∑' n, prawitzCotSeriesTerm x n u) =
    ∑' n, ∫ u in Icc (0 : ℝ) 1, prawitzCotSeriesTerm x n u at hset
  simpa only [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)] using hset

theorem abs_prawitzCotBaseIntegrand_le
    (x : ℝ) {u : ℝ} (hu : u ∈ Icc (0 : ℝ) 1) :
    |prawitzCotBaseIntegrand x u| ≤ 2 * |x| := by
  by_cases hu0 : u = 0
  · subst u
    simp [prawitzCotBaseIntegrand]
  · have habsu : 0 < |u| := abs_pos.mpr hu0
    have hden : 0 < Real.pi * |u| := mul_pos Real.pi_pos habsu
    have hfac : |1 - u| ≤ 1 := by
      rw [abs_of_nonneg (by linarith [hu.2] : 0 ≤ 1 - u)]
      linarith [hu.1]
    unfold prawitzCotBaseIntegrand
    rw [abs_div, abs_mul, abs_mul, abs_of_pos Real.pi_pos]
    calc
      |1 - u| * |Real.sin (2 * Real.pi * x * u)| /
          (Real.pi * |u|) ≤
          |1 - u| * |2 * Real.pi * x * u| /
            (Real.pi * |u|) := by
        apply (div_le_div_iff_of_pos_right hden).2
        exact mul_le_mul_of_nonneg_left
          (Real.abs_sin_le_abs :
            |Real.sin (2 * Real.pi * x * u)| ≤ |2 * Real.pi * x * u|)
          (abs_nonneg _)
      _ = 2 * |1 - u| * |x| := by
        rw [abs_mul, abs_mul, abs_mul,
          abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2),
          abs_of_pos Real.pi_pos]
        field_simp [abs_ne_zero.mpr hu0, Real.pi_ne_zero]
      _ ≤ 2 * |x| := by
        simpa only [mul_one] using
          (mul_le_mul_of_nonneg_right
            (mul_le_mul_of_nonneg_left hfac (by norm_num : (0 : ℝ) ≤ 2))
            (abs_nonneg x))

theorem integrableOn_prawitzCotBaseIntegrand (x : ℝ) :
    IntegrableOn (prawitzCotBaseIntegrand x) (Icc 0 1) := by
  have hmeas : AEStronglyMeasurable (prawitzCotBaseIntegrand x)
      (volume.restrict (Icc (0 : ℝ) 1)) := by
    apply Measurable.aestronglyMeasurable
    unfold prawitzCotBaseIntegrand
    fun_prop
  apply (integrable_const (2 * |x| : ℝ)).mono' hmeas
  filter_upwards [ae_restrict_mem measurableSet_Icc] with u hu
  rw [Real.norm_eq_abs]
  exact abs_prawitzCotBaseIntegrand_le x hu

/-- The unit Prawitz point kernel is exactly the interval integral of its
real trigonometric integrand. -/
theorem prawitzPointKernel_eq_integral_unitPointIntegrand (z : ℝ) :
    prawitzPointKernel 1 z =
      ∫ u in Icc (0 : ℝ) 1, prawitzUnitPointIntegrand z u := by
  have hdirac : Integrable (fun y : ℝ => |y|) (Measure.dirac z) :=
    integrable_dirac (by simp)
  have hInt : IntegrableOn
      (symmetricPrawitzPVIntegrand 1
        (MeasureTheory.charFun (Measure.dirac z)) 0) (Icc 0 1) :=
    integrableOn_symmetricPrawitzPVIntegrand_charFun
      (Measure.dirac z) hdirac (by norm_num) 0
  unfold prawitzPointKernel prawitzInversion prawitzUnitPointIntegrand
  symm
  calc
    (∫ u in Icc (0 : ℝ) 1,
        ((Complex.I / (2 * Real.pi : ℝ)) *
          symmetricPrawitzPVIntegrand 1
            (MeasureTheory.charFun (Measure.dirac z)) 0 u).re) =
      (∫ u in Icc (0 : ℝ) 1,
        (Complex.I / (2 * Real.pi : ℝ)) *
          symmetricPrawitzPVIntegrand 1
            (MeasureTheory.charFun (Measure.dirac z)) 0 u).re :=
        integral_re (hInt.const_mul _)
    _ = ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ u in Icc (0 : ℝ) 1,
          symmetricPrawitzPVIntegrand 1
            (MeasureTheory.charFun (Measure.dirac z)) 0 u).re := by
      exact congrArg Complex.re
        (MeasureTheory.integral_const_mul
          (μ := volume.restrict (Icc (0 : ℝ) 1))
          (Complex.I / (2 * Real.pi : ℝ))
          (symmetricPrawitzPVIntegrand 1
            (MeasureTheory.charFun (Measure.dirac z)) 0))

/-- At zero displacement the paired unit-window point integrand is the
elementary triangular imaginary kernel. -/
theorem symmetricPrawitzPVIntegrand_charFun_dirac_zero
    {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    symmetricPrawitzPVIntegrand 1
        (MeasureTheory.charFun (Measure.dirac 0)) 0 u =
      (((-2 * Real.pi * (1 - u) : ℝ) : ℂ)) * Complex.I := by
  have hnum :
      prawitzPVNumerator 1
          (MeasureTheory.charFun (Measure.dirac 0)) 0 u =
        prawitzSmoothingFilter u := by
    unfold prawitzPVNumerator scaledPrawitzSmoothingFilter
    rw [MeasureTheory.charFun_dirac]
    simp
  have him : (prawitzSmoothingFilter u).im =
      -(1 - u) * Real.pi * u := by
    rw [prawitzSmoothingFilter, if_pos (by simpa [abs_of_pos hu0] using hu1)]
    simp only [abs_of_pos hu0, Complex.sub_im, Complex.ofReal_im,
      Complex.mul_im, Complex.ofReal_re, Complex.I_im, Complex.I_re,
      zero_mul, mul_one, add_zero, zero_sub]
    ring
  rw [symmetricPrawitzPVIntegrand,
    prawitzPVIntegrand_neg
      (isHermitianFrequencyFunction_charFun (Measure.dirac 0)) 0 hu0.ne',
    ← sub_eq_add_neg, Complex.sub_conj, prawitzPVIntegrand,
    Complex.div_ofReal_im, hnum, him]
  push_cast
  field_simp [hu0.ne']

theorem integral_symmetricPrawitzPVIntegrand_charFun_dirac_zero :
    (∫ u in Icc (0 : ℝ) 1,
      symmetricPrawitzPVIntegrand 1
        (MeasureTheory.charFun (Measure.dirac 0)) 0 u) =
      -Real.pi * Complex.I := by
  have hcongr :
      (∫ u in Icc (0 : ℝ) 1,
        symmetricPrawitzPVIntegrand 1
          (MeasureTheory.charFun (Measure.dirac 0)) 0 u) =
        ∫ u in Icc (0 : ℝ) 1,
          (((-2 * Real.pi * (1 - u) : ℝ) : ℂ)) * Complex.I := by
    apply integral_congr_ae
    filter_upwards [ae_restrict_mem measurableSet_Icc,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
    exact symmetricPrawitzPVIntegrand_charFun_dirac_zero
      (lt_of_le_of_ne hu.1 (Ne.symm hu0))
      (lt_of_le_of_ne hu.2 hu1)
  rw [hcongr, integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)]
  have hpoly :
      (∫ u : ℝ in 0..1, (((1 - u : ℝ) : ℂ))) = (1 / 2 : ℂ) := by
    rw [intervalIntegral.integral_ofReal]
    have hreal :
        (∫ u : ℝ in 0..1, (1 - u : ℝ)) = (1 / 2 : ℝ) := by
      calc
        _ = (∫ _u : ℝ in 0..1, (1 : ℝ)) -
            (∫ u : ℝ in 0..1, u) :=
          intervalIntegral.integral_sub
            intervalIntegral.intervalIntegrable_const
            intervalIntegral.intervalIntegrable_id
        _ = (1 : ℝ) - 1 / 2 := by
          rw [integral_one, integral_id]
          field_simp
          norm_num
        _ = 1 / 2 := by norm_num
    rw [hreal]
    norm_num
  have hinner :
      (∫ u : ℝ in 0..1, (((1 - u : ℝ) : ℂ)) * Complex.I) =
        (1 / 2 : ℂ) * Complex.I := by
    calc
      _ = (∫ u : ℝ in 0..1, (((1 - u : ℝ) : ℂ))) * Complex.I :=
        intervalIntegral.integral_mul_const Complex.I
          (fun u : ℝ => (((1 - u : ℝ) : ℂ)))
      _ = (1 / 2 : ℂ) * Complex.I := by rw [hpoly]
  have houter :
      (∫ u : ℝ in 0..1,
        (((-2 * Real.pi * (1 - u) : ℝ) : ℂ)) * Complex.I) =
        ((-2 * Real.pi : ℝ) : ℂ) * ((1 / 2 : ℂ) * Complex.I) := by
    rw [show (fun u : ℝ => (((-2 * Real.pi * (1 - u) : ℝ) : ℂ)) *
        Complex.I) = fun u => (((-2 * Real.pi : ℝ) : ℂ) *
          ((((1 - u : ℝ) : ℂ)) * Complex.I)) by
        funext u
        push_cast
        ring]
    calc
      _ = ((-2 * Real.pi : ℝ) : ℂ) *
          (∫ u : ℝ in 0..1, (((1 - u : ℝ) : ℂ)) * Complex.I) :=
        intervalIntegral.integral_const_mul ((-2 * Real.pi : ℝ) : ℂ)
          (fun u : ℝ => (((1 - u : ℝ) : ℂ)) * Complex.I)
      _ = ((-2 * Real.pi : ℝ) : ℂ) *
          ((1 / 2 : ℂ) * Complex.I) := by rw [hinner]
  rw [houter]
  push_cast
  ring

theorem prawitzPointKernel_zero : prawitzPointKernel 1 0 = 1 / 2 := by
  unfold prawitzPointKernel prawitzInversion
  rw [integral_symmetricPrawitzPVIntegrand_charFun_dirac_zero]
  have hcomplex :
      (Complex.I / (2 * Real.pi : ℝ)) * (-Real.pi * Complex.I) =
        (1 / 2 : ℂ) := by
    field_simp [Real.pi_ne_zero]
    rw [pow_two, Complex.I_mul_I]
    push_cast
    ring
  rw [hcomplex]
  norm_num

theorem integrableOn_prawitzUnitPointIntegrand (z : ℝ) :
    IntegrableOn (prawitzUnitPointIntegrand z) (Icc 0 1) := by
  have hdirac : Integrable (fun y : ℝ => |y|) (Measure.dirac z) :=
    integrable_dirac (by simp)
  have hInt : IntegrableOn
      (symmetricPrawitzPVIntegrand 1
        (MeasureTheory.charFun (Measure.dirac z)) 0) (Icc 0 1) :=
    integrableOn_symmetricPrawitzPVIntegrand_charFun
      (Measure.dirac z) hdirac (by norm_num) 0
  unfold prawitzUnitPointIntegrand
  exact (hInt.const_mul _).re

/-- The cotangent--sine term has removable endpoint singularities and is
integrable on the closed unit window. -/
theorem integrableOn_prawitzCotangentSineIntegrand (x : ℝ) :
    IntegrableOn (prawitzCotangentSineIntegrand x) (Icc 0 1) := by
  let e : ℝ → ℝ := fun u =>
    (1 - u) * Real.cos (2 * Real.pi * x * u) +
      Real.sin (2 * Real.pi * x * u) / Real.pi
  have he : IntegrableOn e (Icc 0 1) :=
    (by fun_prop : Continuous e).integrableOn_Icc
  have hp := integrableOn_prawitzUnitPointIntegrand (-2 * Real.pi * x)
  apply (hp.sub he).congr
  filter_upwards [ae_restrict_mem measurableSet_Icc,
      (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
      (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
  have hpoint :
      prawitzUnitPointIntegrand (-2 * Real.pi * x) u =
        e u + prawitzCotangentSineIntegrand x u := by
    dsimp only [e]
    exact prawitzUnitPointIntegrand_neg_two_pi_eq x
      (lt_of_le_of_ne hu.1 (Ne.symm hu0))
      (lt_of_le_of_ne hu.2 hu1)
  change prawitzUnitPointIntegrand (-2 * Real.pi * x) u - e u = _
  rw [hpoint]
  ring

theorem integrableOn_tsum_prawitzCotSeriesTerm (x : ℝ) :
    IntegrableOn (fun u => ∑' n, prawitzCotSeriesTerm x n u) (Icc 0 1) := by
  have hcot := integrableOn_prawitzCotangentSineIntegrand x
  have hbase := integrableOn_prawitzCotBaseIntegrand x
  apply (hcot.sub hbase).congr
  filter_upwards [ae_restrict_mem measurableSet_Icc,
      (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
      (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
  have hpoint := prawitzCotangentSineIntegrand_eq_base_add_tsum x
    (lt_of_le_of_ne hu.1 (Ne.symm hu0))
    (lt_of_le_of_ne hu.2 hu1)
  change prawitzCotangentSineIntegrand x u -
    prawitzCotBaseIntegrand x u = _
  linarith

/-- Integral form of the Mittag--Leffler expansion: the cotangent integral
is a removable base integral plus the sum of the regularized term integrals. -/
theorem integral_prawitzCotangentSineIntegrand_eq_base_add_tsum (x : ℝ) :
    (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) =
      (∫ u : ℝ in 0..1, prawitzCotBaseIntegrand x u) +
        ∑' n, ∫ u : ℝ in 0..1, prawitzCotSeriesTerm x n u := by
  have hcongr :
      (∫ u in Icc (0 : ℝ) 1, prawitzCotangentSineIntegrand x u) =
        ∫ u in Icc (0 : ℝ) 1,
          (prawitzCotBaseIntegrand x u +
            ∑' n, prawitzCotSeriesTerm x n u) := by
    apply integral_congr_ae
    filter_upwards [ae_restrict_mem measurableSet_Icc,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
    exact prawitzCotangentSineIntegrand_eq_base_add_tsum x
      (lt_of_le_of_ne hu.1 (Ne.symm hu0))
      (lt_of_le_of_ne hu.2 hu1)
  rw [MeasureTheory.integral_add (integrableOn_prawitzCotBaseIntegrand x)
    (integrableOn_tsum_prawitzCotSeriesTerm x)] at hcongr
  simp_rw [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)] at hcongr
  rw [hcongr, integral_tsum_prawitzCotSeriesTerm]

/-- Real Riemann--Lebesgue lemma in the sine-transform normalization used by
the Prawitz formulas. -/
theorem tendsto_integral_sin_mul_atTop_of_integrable
    (f : ℝ → ℝ) (hf : Integrable f) :
    Tendsto (fun w : ℝ => ∫ v : ℝ, Real.sin (2 * Real.pi * v * w) * f v)
      atTop (𝓝 0) := by
  let fc : ℝ → ℂ := fun v => (f v : ℂ)
  have hfc : Integrable fc := hf.ofReal
  have hcomplex :=
    (Real.tendsto_integral_exp_smul_cocompact fc).mono_left
      (_root_.atTop_le_cocompact : atTop ≤ cocompact ℝ)
  have him := Complex.continuous_im.continuousAt.tendsto.comp hcomplex
  have hneg := him.neg
  convert hneg using 1
  · funext w
    simp only [Function.comp_apply]
    have hInt : Integrable (fun v : ℝ => 𝐞 (-(v * w)) • fc v) := by
      simpa only [real_inner_comm] using
        ((Real.fourierIntegral_convergent_iff w).2 hfc)
    change (∫ v : ℝ, Real.sin (2 * Real.pi * v * w) * f v) =
      -RCLike.im (∫ v : ℝ, 𝐞 (-(v * w)) • fc v)
    calc
      _ = -∫ v : ℝ, RCLike.im (𝐞 (-(v * w)) • fc v) := by
        rw [← MeasureTheory.integral_neg]
        apply MeasureTheory.integral_congr_ae
        filter_upwards with v
        have hexp :
            (Complex.exp (↑(2 * Real.pi * (-(v * w))) * Complex.I)).im =
              -Real.sin (2 * Real.pi * v * w) := by
          rw [show (↑(2 * Real.pi * (-(v * w))) : ℂ) * Complex.I =
              ((-(2 * Real.pi * v * w) : ℝ) : ℂ) * Complex.I by
                push_cast
                ring,
            Complex.exp_ofReal_mul_I_im, Real.sin_neg]
        rw [Circle.smul_def, Real.fourierChar_apply]
        change Real.sin (2 * Real.pi * v * w) * f v =
          -(Complex.exp (↑(2 * Real.pi * (-(v * w))) * Complex.I) *
            (f v : ℂ)).im
        rw [Complex.mul_im, hexp]
        simp
      _ = _ := congrArg Neg.neg (integral_im hInt)
  · simp

/-- The frequency-independent coefficient of a regularized cotangent-series
term. -/
noncomputable def prawitzCotSeriesWeight : ℕ → ℝ → ℝ
  | 0, u => -2 * u / (Real.pi * (u + 1))
  | n + 1, u => ((1 - u) / Real.pi) *
      (1 / (u - ((n : ℝ) + 2)) + 1 / (u + ((n : ℝ) + 2)))

theorem prawitzCotSeriesTerm_eq_weight_mul (x : ℝ) (n : ℕ) (u : ℝ) :
    prawitzCotSeriesTerm x n u =
      prawitzCotSeriesWeight n u * Real.sin (2 * Real.pi * x * u) := by
  cases n <;> simp [prawitzCotSeriesTerm, prawitzCotSeriesWeight]

theorem abs_prawitzCotSeriesWeight_le
    (n : ℕ) {u : ℝ} (hu : u ∈ Icc (0 : ℝ) 1) :
    |prawitzCotSeriesWeight n u| ≤ 4 / ((n : ℝ) + 1) ^ 2 := by
  cases n with
  | zero =>
      simp only [prawitzCotSeriesWeight, Nat.cast_zero, zero_add,
        one_pow, div_one]
      have hup : 0 < u + 1 := by linarith [hu.1]
      have hden : 0 < Real.pi * (u + 1) := mul_pos Real.pi_pos hup
      rw [abs_div, abs_of_nonpos (by linarith [hu.1] : -2 * u ≤ 0),
        abs_of_pos hden]
      apply (div_le_iff₀ hden).2
      nlinarith [Real.two_le_pi]
  | succ n =>
      let m : ℝ := (n : ℝ) + 2
      have hm : 2 ≤ m := by
        dsimp [m]
        exact le_add_of_nonneg_left (Nat.cast_nonneg n)
      have hum : u - m < 0 := by linarith [hu.2]
      have hup : 0 < u + m := by linarith [hu.1]
      have hden : 0 < m ^ 2 - u ^ 2 := by
        nlinarith [sq_nonneg (m - u)]
      have hqeq :
          1 / (u - m) + 1 / (u + m) = -2 * u / (m ^ 2 - u ^ 2) := by
        field_simp [hum.ne, hup.ne']
        ring
      have hqabs :
          |1 / (u - m) + 1 / (u + m)| =
            2 * u / (m ^ 2 - u ^ 2) := by
        rw [hqeq, abs_div,
          abs_of_nonpos (by linarith [hu.1] : -2 * u ≤ 0),
          abs_of_pos hden]
        ring
      have hq :
          |1 / (u - m) + 1 / (u + m)| ≤ 4 / m ^ 2 := by
        rw [hqabs]
        have hmpos : 0 < m ^ 2 := sq_pos_of_pos (by linarith : 0 < m)
        apply (div_le_div_iff₀ hden hmpos).2
        have hu_sq : u ^ 2 ≤ 1 := by
          nlinarith [sq_nonneg (1 - u), hu.1, hu.2]
        have hm_sq : 4 ≤ m ^ 2 := by nlinarith [sq_nonneg (m - 2)]
        have hleft : 2 * u * m ^ 2 ≤ 2 * m ^ 2 := by
          exact mul_le_mul_of_nonneg_right
            (by linarith [hu.2] : 2 * u ≤ 2) (sq_nonneg m)
        have hright : 2 * m ^ 2 ≤ 4 * (m ^ 2 - u ^ 2) := by
          nlinarith
        exact hleft.trans hright
      have hfac : |(1 - u) / Real.pi| ≤ 1 := by
        rw [abs_div, abs_of_nonneg (by linarith [hu.2] : 0 ≤ 1 - u),
          abs_of_pos Real.pi_pos]
        apply (div_le_one Real.pi_pos).2
        linarith [Real.two_le_pi, hu.1]
      simp only [prawitzCotSeriesWeight, Nat.cast_add, Nat.cast_one]
      rw [show (n : ℝ) + 1 + 1 = (n : ℝ) + 2 by ring]
      change |((1 - u) / Real.pi) *
          (1 / (u - m) + 1 / (u + m))| ≤ 4 / m ^ 2
      rw [abs_mul]
      exact (mul_le_of_le_one_left (abs_nonneg _) hfac).trans hq

theorem integrableOn_prawitzCotSeriesWeight (n : ℕ) :
    IntegrableOn (prawitzCotSeriesWeight n) (Icc 0 1) := by
  have hmeas : AEStronglyMeasurable (prawitzCotSeriesWeight n)
      (volume.restrict (Icc (0 : ℝ) 1)) := by
    apply Measurable.aestronglyMeasurable
    cases n with
    | zero =>
        change Measurable (fun u : ℝ => -2 * u / (Real.pi * (u + 1)))
        fun_prop
    | succ n =>
        change Measurable (fun u : ℝ => ((1 - u) / Real.pi) *
          (1 / (u - ((n : ℝ) + 2)) + 1 / (u + ((n : ℝ) + 2))))
        fun_prop
  apply (integrable_const (4 / ((n : ℝ) + 1) ^ 2 : ℝ)).mono' hmeas
  filter_upwards [ae_restrict_mem measurableSet_Icc] with u hu
  rw [Real.norm_eq_abs]
  exact abs_prawitzCotSeriesWeight_le n hu

theorem tendsto_integral_prawitzCotSeriesTerm_atTop (n : ℕ) :
    Tendsto (fun x : ℝ =>
      ∫ u : ℝ in 0..1, prawitzCotSeriesTerm x n u) atTop (𝓝 0) := by
  let f : ℝ → ℝ := (Icc (0 : ℝ) 1).indicator
    (prawitzCotSeriesWeight n)
  have hf : Integrable f :=
    (integrableOn_prawitzCotSeriesWeight n).integrable_indicator measurableSet_Icc
  have hrl := tendsto_integral_sin_mul_atTop_of_integrable f hf
  apply hrl.congr'
  filter_upwards with x
  have hset :
      (∫ u in Icc (0 : ℝ) 1, prawitzCotSeriesTerm x n u) =
        ∫ u : ℝ, Real.sin (2 * Real.pi * u * x) * f u := by
    dsimp only [f]
    rw [show (fun u : ℝ => Real.sin (2 * Real.pi * u * x) *
        (Icc (0 : ℝ) 1).indicator (prawitzCotSeriesWeight n) u) =
      fun u => (Icc (0 : ℝ) 1).indicator (prawitzCotSeriesWeight n) u *
        Real.sin (2 * Real.pi * u * x) by funext u; ring_nf]
    rw [show (fun u : ℝ =>
        (Icc (0 : ℝ) 1).indicator (prawitzCotSeriesWeight n) u *
          Real.sin (2 * Real.pi * u * x)) =
      (Icc (0 : ℝ) 1).indicator (fun u =>
        prawitzCotSeriesWeight n u * Real.sin (2 * Real.pi * u * x)) by
          funext u
          by_cases hu : u ∈ Icc (0 : ℝ) 1 <;> simp [hu]]
    rw [integral_indicator measurableSet_Icc]
    apply MeasureTheory.integral_congr_ae
    filter_upwards with u
    rw [prawitzCotSeriesTerm_eq_weight_mul]
    ring_nf
  simpa only [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)] using hset.symm

theorem norm_integral_prawitzCotSeriesTerm_le (x : ℝ) (n : ℕ) :
    ‖∫ u : ℝ in 0..1, prawitzCotSeriesTerm x n u‖ ≤
      4 / ((n : ℝ) + 1) ^ 2 := by
  have hEq :
      (∫ u in Icc (0 : ℝ) 1, prawitzCotSeriesTerm x n u) =
        ∫ u : ℝ in 0..1, prawitzCotSeriesTerm x n u := by
    simp only [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)]
  rw [← hEq]
  exact (norm_integral_le_integral_norm _).trans
    (integral_norm_prawitzCotSeriesTerm_le x n)

/-- The sum of all regularized cotangent-series integrals vanishes at high
spatial frequency. -/
theorem tendsto_tsum_integral_prawitzCotSeriesTerm_atTop :
    Tendsto (fun x : ℝ =>
      ∑' n, ∫ u : ℝ in 0..1, prawitzCotSeriesTerm x n u)
      atTop (𝓝 0) := by
  have hs : Summable (fun n : ℕ => 4 / ((n : ℝ) + 1) ^ 2) := by
    have h := (summable_prawitzInverseSquareTail (0 : ℝ)).mul_left 4
    simpa using h
  have h := tendsto_tsum_of_dominated_convergence hs
    tendsto_integral_prawitzCotSeriesTerm_atTop
    (Eventually.of_forall fun x n => norm_integral_prawitzCotSeriesTerm_le x n)
  simpa using h

/-- The regularized cotangent-series contribution vanishes at high spatial
frequency; only the removable `1/u` base integral survives. -/
theorem tendsto_prawitzCotangentIntegral_sub_base_atTop :
    Tendsto (fun x : ℝ =>
      (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) -
        ∫ u : ℝ in 0..1, prawitzCotBaseIntegrand x u)
      atTop (𝓝 0) := by
  convert tendsto_tsum_integral_prawitzCotSeriesTerm_atTop using 1
  funext x
  rw [integral_prawitzCotangentSineIntegrand_eq_base_add_tsum]
  ring

/-- Reflection cancels the odd cotangent/sine part of the real point
integrand, leaving an elementary Fejér-type cosine kernel. -/
theorem prawitzUnitPointIntegrand_add_neg
    (z : ℝ) {u : ℝ} (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzUnitPointIntegrand z u +
        prawitzUnitPointIntegrand (-z) u =
      2 * (1 - u) * Real.cos (z * u) := by
  rw [prawitzUnitPointIntegrand_eq_explicit z hu0 hu1,
    prawitzUnitPointIntegrand_eq_explicit (-z) hu0 hu1]
  rw [show -z * u = -(z * u) by ring, Real.cos_neg, Real.sin_neg]
  ring

theorem integral_two_mul_one_sub_mul_cos
    {z : ℝ} (hz : z ≠ 0) :
    (∫ u : ℝ in 0..1, 2 * (1 - u) * Real.cos (z * u)) =
      2 * (1 - Real.cos z) / z ^ 2 := by
  let F : ℝ → ℝ := fun u => 2 *
    ((1 - u) * Real.sin (z * u) / z - Real.cos (z * u) / z ^ 2)
  have hF (u : ℝ) :
      HasDerivAt F (2 * (1 - u) * Real.cos (z * u)) u := by
    have hlin : HasDerivAt (fun v : ℝ => 1 - v) (-1) u := by
      convert (hasDerivAt_const u 1).sub (hasDerivAt_id u) using 1 <;> ring
    have harg : HasDerivAt (fun v : ℝ => z * v) z u :=
      by convert (hasDerivAt_id u).const_mul z using 1 <;> simp
    have hsin : HasDerivAt (fun v : ℝ => Real.sin (z * v))
        (Real.cos (z * u) * z) u :=
      (Real.hasDerivAt_sin (z * u)).comp u harg
    have hcos : HasDerivAt (fun v : ℝ => Real.cos (z * v))
        (-Real.sin (z * u) * z) u :=
      (Real.hasDerivAt_cos (z * u)).comp u harg
    have h := ((hlin.mul hsin).div_const z).sub (hcos.div_const (z ^ 2))
    convert h.const_mul 2 using 1 <;>
      dsimp only [F] <;> field_simp [hz] <;> ring
  have hInt : IntervalIntegrable
      (fun u : ℝ => 2 * (1 - u) * Real.cos (z * u)) volume 0 1 :=
    (by fun_prop : Continuous fun u : ℝ =>
      2 * (1 - u) * Real.cos (z * u)).intervalIntegrable 0 1
  have hFTC := intervalIntegral.integral_eq_sub_of_hasDerivAt
    (a := (0 : ℝ)) (b := 1) (f := F)
    (f' := fun u => 2 * (1 - u) * Real.cos (z * u))
    (fun u _ => hF u) hInt
  rw [hFTC]
  dsimp only [F]
  simp only [sub_self, zero_mul, Real.sin_zero, mul_zero, zero_div,
    Real.cos_zero]
  field_simp [hz]
  ring

theorem integral_one_sub_mul_cos
    {a : ℝ} (ha : a ≠ 0) :
    (∫ u : ℝ in 0..1, (1 - u) * Real.cos (a * u)) =
      (1 - Real.cos a) / a ^ 2 := by
  have h := integral_two_mul_one_sub_mul_cos (z := a) ha
  rw [show (fun u : ℝ => 2 * (1 - u) * Real.cos (a * u)) =
      fun u => 2 * ((1 - u) * Real.cos (a * u)) by funext u; ring,
    intervalIntegral.integral_const_mul] at h
  calc
    _ = (2 * (∫ u : ℝ in 0..1, (1 - u) * Real.cos (a * u))) / 2 := by
      ring
    _ = (2 * (1 - Real.cos a) / a ^ 2) / 2 := by rw [h]
    _ = (1 - Real.cos a) / a ^ 2 := by ring

theorem integral_sin_mul_div_pi
    {a : ℝ} (ha : a ≠ 0) :
    (∫ u : ℝ in 0..1, Real.sin (a * u) / Real.pi) =
      (1 - Real.cos a) / (Real.pi * a) := by
  have hchange := intervalIntegral.integral_comp_mul_left
    (f := Real.sin) (a := (0 : ℝ)) (b := 1) ha
  simp only [mul_zero, mul_one] at hchange
  rw [integral_sin] at hchange
  simp only [Real.cos_zero, smul_eq_mul] at hchange
  rw [show (fun u : ℝ => Real.sin (a * u) / Real.pi) =
      fun u => (1 / Real.pi) * Real.sin (a * u) by funext u; ring,
    intervalIntegral.integral_const_mul, hchange]
  field_simp [Real.pi_ne_zero, ha]

/-- Exact reduction of the negative-spatial Prawitz point kernel to two
elementary integrals and one cotangent--sine integral. -/
theorem prawitzPointKernel_neg_two_pi_eq
    {x : ℝ} (hx : x ≠ 0) :
    prawitzPointKernel 1 (-2 * Real.pi * x) =
      (1 - Real.cos (2 * Real.pi * x)) / (2 * Real.pi * x) ^ 2 +
        (1 - Real.cos (2 * Real.pi * x)) /
          (Real.pi * (2 * Real.pi * x)) +
        ∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u := by
  rw [prawitzPointKernel_eq_integral_unitPointIntegrand]
  have hcongr :
      (∫ u in Icc (0 : ℝ) 1,
          prawitzUnitPointIntegrand (-2 * Real.pi * x) u) =
        ∫ u in Icc (0 : ℝ) 1,
          ((1 - u) * Real.cos (2 * Real.pi * x * u) +
            Real.sin (2 * Real.pi * x * u) / Real.pi +
              prawitzCotangentSineIntegrand x u) := by
    apply integral_congr_ae
    filter_upwards [ae_restrict_mem measurableSet_Icc,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
    exact prawitzUnitPointIntegrand_neg_two_pi_eq x
      (lt_of_le_of_ne hu.1 (Ne.symm hu0))
      (lt_of_le_of_ne hu.2 hu1)
  rw [hcongr]
  have hcos : IntegrableOn
      (fun u : ℝ => (1 - u) * Real.cos (2 * Real.pi * x * u))
      (Icc 0 1) := (by fun_prop : Continuous fun u : ℝ =>
        (1 - u) * Real.cos (2 * Real.pi * x * u)).integrableOn_Icc
  have hsin : IntegrableOn
      (fun u : ℝ => Real.sin (2 * Real.pi * x * u) / Real.pi)
      (Icc 0 1) := (by fun_prop : Continuous fun u : ℝ =>
        Real.sin (2 * Real.pi * x * u) / Real.pi).integrableOn_Icc
  have hsplit1 :
      (∫ u in Icc (0 : ℝ) 1,
        ((1 - u) * Real.cos (2 * Real.pi * x * u) +
          Real.sin (2 * Real.pi * x * u) / Real.pi) +
            prawitzCotangentSineIntegrand x u) =
        (∫ u in Icc (0 : ℝ) 1,
          ((1 - u) * Real.cos (2 * Real.pi * x * u) +
            Real.sin (2 * Real.pi * x * u) / Real.pi)) +
          ∫ u in Icc (0 : ℝ) 1, prawitzCotangentSineIntegrand x u :=
    MeasureTheory.integral_add (hcos.add hsin)
      (integrableOn_prawitzCotangentSineIntegrand x)
  have hsplit2 :
      (∫ u in Icc (0 : ℝ) 1,
        ((1 - u) * Real.cos (2 * Real.pi * x * u) +
          Real.sin (2 * Real.pi * x * u) / Real.pi)) =
        (∫ u in Icc (0 : ℝ) 1,
          (1 - u) * Real.cos (2 * Real.pi * x * u)) +
          ∫ u in Icc (0 : ℝ) 1,
            Real.sin (2 * Real.pi * x * u) / Real.pi :=
    MeasureTheory.integral_add hcos hsin
  rw [hsplit1, hsplit2]
  simp_rw [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)]
  rw [integral_one_sub_mul_cos (mul_ne_zero (mul_ne_zero (by norm_num)
      Real.pi_ne_zero) hx),
    integral_sin_mul_div_pi (mul_ne_zero (mul_ne_zero (by norm_num)
      Real.pi_ne_zero) hx)]

/-- The sole remaining analytic content of the positive-half-line Fourier
identification is an exact cotangent--sine integral. -/
theorem prawitzPointKernel_positive_identity_iff_cotangentIntegral
    {x : ℝ} (hx : 0 < x) :
    (2 * prawitzPointKernel 1 (-2 * Real.pi * x) - 1 =
        prawitzVaalerPositiveRemainder x) ↔
      (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) =
        1 / 2 - (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
          (prawitzInverseSquareTail x + 1 / (2 * x ^ 2)) := by
  rw [prawitzPointKernel_neg_two_pi_eq hx.ne']
  unfold prawitzVaalerPositiveRemainder
  rw [show Real.cos (2 * Real.pi * x) = Real.cos (2 * (Real.pi * x)) by
    congr 1
    ring]
  rw [Real.cos_two_mul]
  rw [show 1 - (2 * Real.cos (Real.pi * x) ^ 2 - 1) =
      2 * Real.sin (Real.pi * x) ^ 2 by rw [Real.sin_sq]; ring]
  field_simp [Real.pi_ne_zero, hx.ne']
  constructor <;> intro h <;> linarith

/-- Shifting the spatial argument by one removes the cotangent singularity
from the difference of the two Prawitz integrands. -/
theorem prawitzCotangentSineIntegrand_add_one_sub (x : ℝ) {u : ℝ}
    (hu0 : 0 < u) (hu1 : u < 1) :
    prawitzCotangentSineIntegrand (x + 1) u -
        prawitzCotangentSineIntegrand x u =
      (1 - u) * (Real.cos (2 * Real.pi * x * u) +
        Real.cos (2 * Real.pi * (x + 1) * u)) := by
  unfold prawitzCotangentSineIntegrand
  rw [Real.cot_eq_cos_div_sin]
  have hsin : Real.sin (Real.pi * u) ≠ 0 := by
    rw [Real.sin_ne_zero_iff]
    intro n hn
    have : u = n := by
      apply (mul_left_cancel₀ Real.pi_ne_zero)
      simpa [mul_comm] using hn.symm
    have hn0 : (0 : ℤ) < n := by exact_mod_cast (this ▸ hu0)
    have hn1 : n < (1 : ℤ) := by exact_mod_cast (this ▸ hu1)
    omega
  have htrig :
      Real.sin (2 * Real.pi * (x + 1) * u) -
          Real.sin (2 * Real.pi * x * u) =
        2 * Real.sin (Real.pi * u) *
          Real.cos (2 * Real.pi * x * u + Real.pi * u) := by
    rw [show 2 * Real.pi * (x + 1) * u =
        (2 * Real.pi * x * u + Real.pi * u) + Real.pi * u by ring,
      show 2 * Real.pi * x * u =
        (2 * Real.pi * x * u + Real.pi * u) - Real.pi * u by ring,
      Real.sin_add, Real.sin_sub]
    norm_num
    ring
  have hcos :
      Real.cos (2 * Real.pi * x * u) +
          Real.cos (2 * Real.pi * (x + 1) * u) =
        2 * Real.cos (Real.pi * u) *
          Real.cos (2 * Real.pi * x * u + Real.pi * u) := by
    rw [Real.two_mul_cos_mul_cos]
    rw [show Real.pi * u - (2 * Real.pi * x * u + Real.pi * u) =
        -(2 * Real.pi * x * u) by ring,
      Real.cos_neg]
    congr 1
    ring_nf
  rw [hcos, ← mul_sub, htrig]
  simp only [div_eq_mul_inv]
  ring_nf
  have hsin' : Real.sin (u * Real.pi) ≠ 0 := by
    simpa [mul_comm] using hsin
  field_simp [hsin']

/-- The interval integral of the shifted cotangent--sine integrand differs by
two elementary cosine integrals. -/
theorem integral_prawitzCotangentSineIntegrand_add_one_sub (x : ℝ) :
    (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand (x + 1) u) -
        (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) =
      ∫ u : ℝ in 0..1, (1 - u) *
        (Real.cos (2 * Real.pi * x * u) +
          Real.cos (2 * Real.pi * (x + 1) * u)) := by
  have hsub :
      (∫ u in Icc (0 : ℝ) 1, prawitzCotangentSineIntegrand (x + 1) u) -
          (∫ u in Icc (0 : ℝ) 1, prawitzCotangentSineIntegrand x u) =
        ∫ u in Icc (0 : ℝ) 1,
          (prawitzCotangentSineIntegrand (x + 1) u -
            prawitzCotangentSineIntegrand x u) := by
    rw [MeasureTheory.integral_sub
      (integrableOn_prawitzCotangentSineIntegrand (x + 1))
      (integrableOn_prawitzCotangentSineIntegrand x)]
  have hcongr :
      (∫ u in Icc (0 : ℝ) 1,
        (prawitzCotangentSineIntegrand (x + 1) u -
          prawitzCotangentSineIntegrand x u)) =
        ∫ u in Icc (0 : ℝ) 1, (1 - u) *
          (Real.cos (2 * Real.pi * x * u) +
            Real.cos (2 * Real.pi * (x + 1) * u)) := by
    apply integral_congr_ae
    filter_upwards [ae_restrict_mem measurableSet_Icc,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
        (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
    exact prawitzCotangentSineIntegrand_add_one_sub x
      (lt_of_le_of_ne hu.1 (Ne.symm hu0)) (lt_of_le_of_ne hu.2 hu1)
  simp_rw [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)] at hsub hcongr
  rw [hsub, hcongr]

/-- Closed form of the unit-shift recurrence for the cotangent--sine
integral, away from the two removable spatial endpoints. -/
theorem integral_prawitzCotangentSineIntegrand_add_one_sub_closed {x : ℝ}
    (hx : x ≠ 0) (hx1 : x + 1 ≠ 0) :
    (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand (x + 1) u) -
        (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) =
      (1 - Real.cos (2 * Real.pi * x)) / (2 * Real.pi * x) ^ 2 +
        (1 - Real.cos (2 * Real.pi * (x + 1))) /
          (2 * Real.pi * (x + 1)) ^ 2 := by
  rw [integral_prawitzCotangentSineIntegrand_add_one_sub]
  rw [show (fun u : ℝ => (1 - u) *
      (Real.cos (2 * Real.pi * x * u) +
        Real.cos (2 * Real.pi * (x + 1) * u))) =
      fun u => (1 - u) * Real.cos (2 * Real.pi * x * u) +
        (1 - u) * Real.cos (2 * Real.pi * (x + 1) * u) by
      funext u
      ring]
  rw [intervalIntegral.integral_add
    ((by fun_prop : Continuous
      (fun u : ℝ => (1 - u) * Real.cos (2 * Real.pi * x * u))).intervalIntegrable 0 1)
    ((by fun_prop : Continuous
      (fun u : ℝ => (1 - u) * Real.cos
        (2 * Real.pi * (x + 1) * u))).intervalIntegrable 0 1)]
  rw [integral_one_sub_mul_cos
      (mul_ne_zero (mul_ne_zero (by norm_num) Real.pi_ne_zero) hx),
    integral_one_sub_mul_cos
      (mul_ne_zero (mul_ne_zero (by norm_num) Real.pi_ne_zero) hx1)]

/-- The cotangent--sine integral equals one half at the first positive integer. -/
theorem integral_prawitzCotangentSineIntegrand_one :
    (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand 1 u) = 1 / 2 := by
  have h := integral_prawitzCotangentSineIntegrand_add_one_sub (0 : ℝ)
  simp only [zero_add] at h
  have hzero :
      (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand 0 u) = 0 := by
    simp [prawitzCotangentSineIntegrand]
  rw [hzero, sub_zero] at h
  rw [show (fun u : ℝ => (1 - u) *
      (Real.cos (2 * Real.pi * 0 * u) +
        Real.cos (2 * Real.pi * 1 * u))) =
      fun u => (1 - u) + (1 - u) * Real.cos (2 * Real.pi * u) by
      funext u
      simp
      ring] at h
  rw [intervalIntegral.integral_add
    ((by fun_prop : Continuous (fun u : ℝ => 1 - u)).intervalIntegrable 0 1)
    ((by fun_prop : Continuous
      (fun u : ℝ => (1 - u) * Real.cos (2 * Real.pi * u))).intervalIntegrable 0 1)] at h
  have hone : (∫ u : ℝ in 0..1, (1 - u)) = 1 / 2 := by
    calc
      _ = (∫ _u : ℝ in 0..1, (1 : ℝ)) - ∫ u : ℝ in 0..1, u := by
        rw [intervalIntegral.integral_sub
          intervalIntegral.intervalIntegrable_const
          intervalIntegral.intervalIntegrable_id]
      _ = 1 / 2 := by rw [integral_one, integral_id]; norm_num
  rw [hone, integral_one_sub_mul_cos
    (mul_ne_zero (by norm_num) Real.pi_ne_zero), Real.cos_two_pi] at h
  simpa only [sub_self, zero_div, add_zero] using h

/-- At every positive integer, the cotangent--sine integral is exactly one
half.  The shift recurrence makes the induction increments vanish. -/
theorem integral_prawitzCotangentSineIntegrand_nat_succ (n : ℕ) :
    (∫ u : ℝ in 0..1,
      prawitzCotangentSineIntegrand ((n + 1 : ℕ) : ℝ) u) = 1 / 2 := by
  induction n with
  | zero => simpa using integral_prawitzCotangentSineIntegrand_one
  | succ n ih =>
      have hx : ((n + 1 : ℕ) : ℝ) ≠ 0 := by positivity
      have hx1 : ((n + 1 : ℕ) : ℝ) + 1 ≠ 0 := by positivity
      have h := integral_prawitzCotangentSineIntegrand_add_one_sub_closed hx hx1
      have hc0 :
          Real.cos (2 * Real.pi * ((n + 1 : ℕ) : ℝ)) = 1 := by
        rw [show 2 * Real.pi * ((n + 1 : ℕ) : ℝ) =
            ((n + 1 : ℕ) : ℝ) * (2 * Real.pi) by ring,
          Real.cos_nat_mul_two_pi]
      have hc1 :
          Real.cos (2 * Real.pi * (((n + 1 : ℕ) : ℝ) + 1)) = 1 := by
        rw [show 2 * Real.pi * (((n + 1 : ℕ) : ℝ) + 1) =
            ((n + 2 : ℕ) : ℝ) * (2 * Real.pi) by push_cast; ring,
          Real.cos_nat_mul_two_pi]
      rw [hc0, hc1] at h
      simp only [sub_self, zero_div, zero_add] at h
      rw [show ((Nat.succ n + 1 : ℕ) : ℝ) =
          ((n + 1 : ℕ) : ℝ) + 1 by push_cast; ring]
      linarith

/-- The positive-half-line Fourier identity holds on the positive integer
lattice. -/
theorem prawitzPointKernel_positive_identity_nat_succ (n : ℕ) :
    2 * prawitzPointKernel 1
        (-2 * Real.pi * ((n + 1 : ℕ) : ℝ)) - 1 =
      prawitzVaalerPositiveRemainder ((n + 1 : ℕ) : ℝ) := by
  have hx : 0 < ((n + 1 : ℕ) : ℝ) := by positivity
  apply (prawitzPointKernel_positive_identity_iff_cotangentIntegral hx).2
  rw [integral_prawitzCotangentSineIntegrand_nat_succ]
  have hsin :
      Real.sin (Real.pi * ((n + 1 : ℕ) : ℝ)) = 0 := by
    rw [mul_comm, Real.sin_nat_mul_pi]
  rw [hsin]
  ring

/-- Real Riemann--Lebesgue lemma for cosine transforms in the normalization
used by the Prawitz formulas. -/
theorem tendsto_integral_cos_mul_atTop_of_integrable
    (f : ℝ → ℝ) (hf : Integrable f) :
    Tendsto (fun w : ℝ => ∫ v : ℝ, Real.cos (2 * Real.pi * v * w) * f v)
      atTop (𝓝 0) := by
  let fc : ℝ → ℂ := fun v => (f v : ℂ)
  have hfc : Integrable fc := hf.ofReal
  have hcomplex :=
    (Real.tendsto_integral_exp_smul_cocompact fc).mono_left
      (_root_.atTop_le_cocompact : atTop ≤ cocompact ℝ)
  have hre := Complex.continuous_re.continuousAt.tendsto.comp hcomplex
  convert hre using 1
  funext w
  simp only [Function.comp_apply]
  have hInt : Integrable (fun v : ℝ => 𝐞 (-(v * w)) • fc v) := by
    simpa only [real_inner_comm] using
      ((Real.fourierIntegral_convergent_iff w).2 hfc)
  change (∫ v : ℝ, Real.cos (2 * Real.pi * v * w) * f v) =
    RCLike.re (∫ v : ℝ, 𝐞 (-(v * w)) • fc v)
  calc
    _ = ∫ v : ℝ, RCLike.re (𝐞 (-(v * w)) • fc v) := by
      apply MeasureTheory.integral_congr_ae
      filter_upwards with v
      have hexp :
          (Complex.exp (↑(2 * Real.pi * (-(v * w))) * Complex.I)).re =
            Real.cos (2 * Real.pi * v * w) := by
        rw [show (↑(2 * Real.pi * (-(v * w))) : ℂ) * Complex.I =
            ((-(2 * Real.pi * v * w) : ℝ) : ℂ) * Complex.I by
              push_cast
              ring,
          Complex.exp_ofReal_mul_I_re, Real.cos_neg]
      rw [Circle.smul_def, Real.fourierChar_apply]
      change Real.cos (2 * Real.pi * v * w) * f v =
        (Complex.exp (↑(2 * Real.pi * (-(v * w))) * Complex.I) *
          (f v : ℂ)).re
      rw [Complex.mul_re, hexp]
      simp
    _ = _ := integral_re hInt

/-- Continuity of a complex Fourier integral in the unnormalized phase
`exp (-i t x)`.  The explicit normalization bridge keeps later Prawitz
arguments independent of the library's `2 * pi` Fourier convention. -/
theorem continuous_integral_cexp_neg_mul_I_mul
    (f : ℝ → ℂ) (hf : Integrable f) :
    Continuous (fun x : ℝ => ∫ t : ℝ,
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * f t) := by
  have hFourier : Continuous
      (VectorFourier.fourierIntegral 𝐞 volume
        (LinearMap.mul ℝ ℝ) f) :=
    VectorFourier.fourierIntegral_continuous
      Real.continuous_fourierChar (by fun_prop) hf
  have h := hFourier.comp
    (by fun_prop : Continuous (fun x : ℝ => (2 * Real.pi)⁻¹ * x))
  convert h using 1
  funext x
  apply integral_congr_ae
  filter_upwards with t
  rw [Circle.smul_def, Real.fourierChar_apply]
  simp only [LinearMap.mul_apply']
  congr 1
  push_cast
  field_simp [Real.pi_ne_zero]

/-- Complex Riemann--Lebesgue decay in the positive spatial tail, written
with the unnormalized phase used by the signed Prawitz formulas. -/
theorem tendsto_integral_cexp_neg_mul_I_mul_atTop (f : ℝ → ℂ) :
    Tendsto (fun x : ℝ => ∫ t : ℝ,
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * f t)
      atTop (𝓝 0) := by
  have hscale :
      Tendsto (fun x : ℝ => (2 * Real.pi)⁻¹ * x) atTop atTop :=
    tendsto_id.const_mul_atTop
      (inv_pos.mpr (mul_pos (by norm_num) Real.pi_pos))
  have hrl :=
    (Real.tendsto_integral_exp_smul_cocompact f).mono_left
      (_root_.atTop_le_cocompact : atTop ≤ cocompact ℝ)
  have h := hrl.comp hscale
  convert h using 1
  funext x
  apply integral_congr_ae
  filter_upwards with t
  rw [Circle.smul_def, Real.fourierChar_apply]
  congr 1
  push_cast
  field_simp [Real.pi_ne_zero]

/-- Complex Riemann--Lebesgue decay in the negative spatial tail, written
with the unnormalized phase used by the signed Prawitz formulas. -/
theorem tendsto_integral_cexp_neg_mul_I_mul_atBot (f : ℝ → ℂ) :
    Tendsto (fun x : ℝ => ∫ t : ℝ,
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * f t)
      atBot (𝓝 0) := by
  have hscale :
      Tendsto (fun x : ℝ => (2 * Real.pi)⁻¹ * x) atBot atBot :=
    tendsto_id.const_mul_atBot
      (inv_pos.mpr (mul_pos (by norm_num) Real.pi_pos))
  have hrl :=
    (Real.tendsto_integral_exp_smul_cocompact f).mono_left
      (_root_.atBot_le_cocompact : atBot ≤ cocompact ℝ)
  have h := hrl.comp hscale
  convert h using 1
  funext x
  apply integral_congr_ae
  filter_upwards with t
  rw [Circle.smul_def, Real.fourierChar_apply]
  congr 1
  push_cast
  field_simp [Real.pi_ne_zero]

/-- The removable coefficient in the difference of two shifted base
integrands. -/
noncomputable def prawitzCotBaseShiftWeight (x u : ℝ) : ℝ :=
  2 * prawitzCotBaseIntegrand (x / 2) u

theorem abs_prawitzCotBaseShiftWeight_le
    (x : ℝ) {u : ℝ} (hu : u ∈ Icc (0 : ℝ) 1) :
    |prawitzCotBaseShiftWeight x u| ≤ 2 * |x| := by
  unfold prawitzCotBaseShiftWeight
  rw [abs_mul, abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
  calc
    2 * |prawitzCotBaseIntegrand (x / 2) u| ≤
        2 * (2 * |x / 2|) :=
      mul_le_mul_of_nonneg_left (abs_prawitzCotBaseIntegrand_le (x / 2) hu)
        (by norm_num)
    _ = 2 * |x| := by rw [abs_div]; norm_num; ring

theorem integrableOn_prawitzCotBaseShiftWeight (x : ℝ) :
    IntegrableOn (prawitzCotBaseShiftWeight x) (Icc 0 1) := by
  unfold prawitzCotBaseShiftWeight
  exact (integrableOn_prawitzCotBaseIntegrand (x / 2)).const_mul 2

theorem prawitzCotBaseIntegrand_add_sub (w x u : ℝ) :
    prawitzCotBaseIntegrand (w + x) u - prawitzCotBaseIntegrand w u =
      Real.cos (2 * Real.pi * u * (w + x / 2)) *
        prawitzCotBaseShiftWeight x u := by
  have htrig :
      Real.sin (2 * Real.pi * (w + x) * u) -
          Real.sin (2 * Real.pi * w * u) =
        2 * Real.sin (Real.pi * x * u) *
          Real.cos (2 * Real.pi * u * (w + x / 2)) := by
    rw [show 2 * Real.pi * (w + x) * u =
        (2 * Real.pi * u * (w + x / 2)) + Real.pi * x * u by ring,
      show 2 * Real.pi * w * u =
        (2 * Real.pi * u * (w + x / 2)) - Real.pi * x * u by ring,
      Real.sin_add, Real.sin_sub]
    ring
  simp only [prawitzCotBaseIntegrand, prawitzCotBaseShiftWeight]
  rw [show 2 * Real.pi * (x / 2) * u = Real.pi * x * u by ring,
    ← sub_div, ← mul_sub, htrig]
  ring

theorem integral_prawitzCotBaseIntegrand_add_sub (w x : ℝ) :
    (∫ u : ℝ in 0..1, prawitzCotBaseIntegrand (w + x) u) -
        (∫ u : ℝ in 0..1, prawitzCotBaseIntegrand w u) =
      ∫ u : ℝ in 0..1,
        Real.cos (2 * Real.pi * u * (w + x / 2)) *
          prawitzCotBaseShiftWeight x u := by
  have hsub :
      (∫ u in Icc (0 : ℝ) 1, prawitzCotBaseIntegrand (w + x) u) -
          (∫ u in Icc (0 : ℝ) 1, prawitzCotBaseIntegrand w u) =
        ∫ u in Icc (0 : ℝ) 1,
          (prawitzCotBaseIntegrand (w + x) u -
            prawitzCotBaseIntegrand w u) := by
    rw [MeasureTheory.integral_sub
      (integrableOn_prawitzCotBaseIntegrand (w + x))
      (integrableOn_prawitzCotBaseIntegrand w)]
  have hcongr :
      (∫ u in Icc (0 : ℝ) 1,
        (prawitzCotBaseIntegrand (w + x) u -
          prawitzCotBaseIntegrand w u)) =
        ∫ u in Icc (0 : ℝ) 1,
          Real.cos (2 * Real.pi * u * (w + x / 2)) *
            prawitzCotBaseShiftWeight x u := by
    apply MeasureTheory.integral_congr_ae
    filter_upwards with u
    exact prawitzCotBaseIntegrand_add_sub w x u
  simp_rw [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)] at hsub hcongr
  rw [hsub, hcongr]

theorem tendsto_integral_prawitzCotBaseIntegrand_add_sub_atTop (x : ℝ) :
    Tendsto (fun w : ℝ =>
      (∫ u : ℝ in 0..1, prawitzCotBaseIntegrand (w + x) u) -
        ∫ u : ℝ in 0..1, prawitzCotBaseIntegrand w u)
      atTop (𝓝 0) := by
  let f : ℝ → ℝ := (Icc (0 : ℝ) 1).indicator (prawitzCotBaseShiftWeight x)
  have hf : Integrable f :=
    (integrableOn_prawitzCotBaseShiftWeight x).integrable_indicator measurableSet_Icc
  have hrl := (tendsto_integral_cos_mul_atTop_of_integrable f hf).comp
    (tendsto_atTop_add_const_right atTop (x / 2) tendsto_id)
  apply hrl.congr'
  filter_upwards with w
  rw [integral_prawitzCotBaseIntegrand_add_sub]
  have hset :
      (∫ u in Icc (0 : ℝ) 1,
        Real.cos (2 * Real.pi * u * (w + x / 2)) *
          prawitzCotBaseShiftWeight x u) =
        ∫ u : ℝ, Real.cos (2 * Real.pi * u * (w + x / 2)) * f u := by
    dsimp only [f]
    rw [show (fun u : ℝ => Real.cos (2 * Real.pi * u * (w + x / 2)) *
        (Icc (0 : ℝ) 1).indicator (prawitzCotBaseShiftWeight x) u) =
      (Icc (0 : ℝ) 1).indicator (fun u =>
        Real.cos (2 * Real.pi * u * (w + x / 2)) *
          prawitzCotBaseShiftWeight x u) by
          funext u
          by_cases hu : u ∈ Icc (0 : ℝ) 1 <;> simp [hu]]
    rw [integral_indicator measurableSet_Icc]
  simpa only [integral_Icc_eq_integral_Ioc,
    ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)] using hset.symm

/-- The removable base integral converges to one half along the positive
integer lattice, anchored by the exact lattice cotangent identity. -/
theorem tendsto_integral_prawitzCotBaseIntegrand_nat_succ :
    Tendsto (fun n : ℕ =>
      ∫ u : ℝ in 0..1,
        prawitzCotBaseIntegrand ((n + 1 : ℕ) : ℝ) u)
      atTop (𝓝 (1 / 2)) := by
  have hcast : Tendsto (fun n : ℕ => ((n + 1 : ℕ) : ℝ)) atTop atTop :=
    tendsto_natCast_atTop_atTop.comp (tendsto_add_atTop_nat 1)
  have hrem := tendsto_prawitzCotangentIntegral_sub_base_atTop.comp hcast
  have hconst : Tendsto (fun _n : ℕ => (1 / 2 : ℝ)) atTop (𝓝 (1 / 2)) :=
    tendsto_const_nhds
  have h := hconst.sub hrem
  convert h using 1
  · funext n
    simp only [Function.comp_apply]
    rw [integral_prawitzCotangentSineIntegrand_nat_succ]
    linarith
  · ring_nf

theorem tendsto_integral_prawitzCotBaseIntegrand_nat_succ_add (x : ℝ) :
    Tendsto (fun n : ℕ =>
      ∫ u : ℝ in 0..1,
        prawitzCotBaseIntegrand (((n + 1 : ℕ) : ℝ) + x) u)
      atTop (𝓝 (1 / 2)) := by
  have hcast : Tendsto (fun n : ℕ => ((n + 1 : ℕ) : ℝ)) atTop atTop :=
    tendsto_natCast_atTop_atTop.comp (tendsto_add_atTop_nat 1)
  have hdiff :=
    (tendsto_integral_prawitzCotBaseIntegrand_add_sub_atTop x).comp hcast
  have h := hdiff.add tendsto_integral_prawitzCotBaseIntegrand_nat_succ
  convert h using 1
  · funext n
    simp only [Function.comp_apply]
    ring_nf
  · ring_nf

/-- Every fixed translate of the cotangent--sine integral tends to one half
along the positive integer lattice. -/
theorem tendsto_integral_prawitzCotangentSineIntegrand_nat_succ_add (x : ℝ) :
    Tendsto (fun n : ℕ =>
      ∫ u : ℝ in 0..1,
        prawitzCotangentSineIntegrand (((n + 1 : ℕ) : ℝ) + x) u)
      atTop (𝓝 (1 / 2)) := by
  have hcast : Tendsto (fun n : ℕ => ((n + 1 : ℕ) : ℝ)) atTop atTop :=
    tendsto_natCast_atTop_atTop.comp (tendsto_add_atTop_nat 1)
  have hshift :
      Tendsto (fun n : ℕ => ((n + 1 : ℕ) : ℝ) + x) atTop atTop :=
    tendsto_atTop_add_const_right atTop x hcast
  have hrem := tendsto_prawitzCotangentIntegral_sub_base_atTop.comp hshift
  have h := hrem.add (tendsto_integral_prawitzCotBaseIntegrand_nat_succ_add x)
  convert h using 1
  · funext n
    simp only [Function.comp_apply]
    ring_nf
  · ring_nf

theorem prawitzInverseSquareTail_nonneg (x : ℝ) :
    0 ≤ prawitzInverseSquareTail x := by
  unfold prawitzInverseSquareTail
  exact tsum_nonneg fun _n => by positivity

/-- Removing the first inverse-square summand shifts the spatial tail by one. -/
theorem prawitzInverseSquareTail_eq_inv_add (x : ℝ) :
    prawitzInverseSquareTail x =
      1 / (x + 1) ^ 2 + prawitzInverseSquareTail (x + 1) := by
  let f : ℕ → ℝ := fun n => 1 / (x + (n : ℝ) + 1) ^ 2
  have hs : Summable f := summable_prawitzInverseSquareTail x
  unfold prawitzInverseSquareTail
  change (∑' n, f n) = _
  rw [hs.tsum_eq_zero_add]
  congr 1
  · simp [f]
  · apply tsum_congr
    intro n
    dsimp only [f]
    congr 2
    push_cast
    ring

/-- The apparent singularity of the inverse-square tail at zero is absent:
shifting its first summand expresses a neighborhood of zero through the
smooth positive-half-line series at one. -/
theorem continuousAt_prawitzInverseSquareTail_zero :
    ContinuousAt prawitzInverseSquareTail 0 := by
  have htailOne : ContinuousAt prawitzInverseSquareTail 1 :=
    (hasDerivAt_prawitzInverseSquareTail (by norm_num)).continuousAt
  have hshift : ContinuousAt
      (fun x : ℝ => prawitzInverseSquareTail (x + 1)) 0 := by
    have hadd : ContinuousAt (fun x : ℝ => x + 1) 0 :=
      continuousAt_id.add continuousAt_const
    have htailOne' :
        ContinuousAt prawitzInverseSquareTail ((0 : ℝ) + 1) := by
      simpa using htailOne
    exact ContinuousAt.comp' (f := fun x : ℝ => x + 1) htailOne' hadd
  have hinv : ContinuousAt (fun x : ℝ => 1 / (x + 1) ^ 2) 0 := by
    exact continuousAt_const.div
      ((continuousAt_id.add continuousAt_const).pow 2) (by norm_num)
  rw [show prawitzInverseSquareTail = fun x : ℝ =>
      1 / (x + 1) ^ 2 + prawitzInverseSquareTail (x + 1) by
    funext x
    exact prawitzInverseSquareTail_eq_inv_add x]
  exact hinv.add hshift

/-- Closed form predicted for the positive cotangent--sine integral. -/
noncomputable def prawitzCotangentIntegralTarget (x : ℝ) : ℝ :=
  1 / 2 - (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
    (prawitzInverseSquareTail x + 1 / (2 * x ^ 2))

theorem prawitzCotangentIntegralTarget_add_one_sub
    {x : ℝ} (hx : x ≠ 0) (hx1 : x + 1 ≠ 0) :
    prawitzCotangentIntegralTarget (x + 1) -
        prawitzCotangentIntegralTarget x =
      (1 - Real.cos (2 * Real.pi * x)) / (2 * Real.pi * x) ^ 2 +
        (1 - Real.cos (2 * Real.pi * (x + 1))) /
          (2 * Real.pi * (x + 1)) ^ 2 := by
  have hsin : Real.sin (Real.pi * (x + 1)) =
      -Real.sin (Real.pi * x) := by
    rw [show Real.pi * (x + 1) = Real.pi * x + Real.pi by ring,
      Real.sin_add, Real.sin_pi, Real.cos_pi]
    ring
  have hcos0 : 1 - Real.cos (2 * Real.pi * x) =
      2 * Real.sin (Real.pi * x) ^ 2 := by
    rw [show 2 * Real.pi * x = 2 * (Real.pi * x) by ring,
      Real.cos_two_mul, Real.sin_sq]
    ring
  have hcos1 : 1 - Real.cos (2 * Real.pi * (x + 1)) =
      2 * Real.sin (Real.pi * (x + 1)) ^ 2 := by
    rw [show 2 * Real.pi * (x + 1) =
        2 * (Real.pi * (x + 1)) by ring,
      Real.cos_two_mul, Real.sin_sq]
    ring
  unfold prawitzCotangentIntegralTarget
  rw [prawitzInverseSquareTail_eq_inv_add x, hcos0, hcos1, hsin]
  field_simp [Real.pi_ne_zero, hx, hx1]
  ring

noncomputable def prawitzCotangentIntegralError (x : ℝ) : ℝ :=
  (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) -
    prawitzCotangentIntegralTarget x

theorem prawitzCotangentIntegralError_add_one
    {x : ℝ} (hx : x ≠ 0) (hx1 : x + 1 ≠ 0) :
    prawitzCotangentIntegralError (x + 1) =
      prawitzCotangentIntegralError x := by
  have hJ := integral_prawitzCotangentSineIntegrand_add_one_sub_closed hx hx1
  have hT := prawitzCotangentIntegralTarget_add_one_sub hx hx1
  unfold prawitzCotangentIntegralError
  linarith

theorem prawitzCotangentIntegralTarget_tendsto_nat_succ_add
    {x : ℝ} (hx : 0 < x) :
    Tendsto (fun n : ℕ =>
      prawitzCotangentIntegralTarget (((n + 1 : ℕ) : ℝ) + x))
      atTop (𝓝 (1 / 2)) := by
  let y : ℕ → ℝ := fun n => ((n + 1 : ℕ) : ℝ) + x
  have hcast : Tendsto (fun n : ℕ => ((n + 1 : ℕ) : ℝ)) atTop atTop :=
    tendsto_natCast_atTop_atTop.comp (tendsto_add_atTop_nat 1)
  have hy : Tendsto y atTop atTop :=
    tendsto_atTop_add_const_right atTop x hcast
  have hbound : Tendsto (fun n => 2 / y n) atTop (𝓝 0) :=
    tendsto_const_nhds.div_atTop hy
  let a : ℕ → ℝ := fun n =>
    prawitzInverseSquareTail (y n) + 1 / (2 * (y n) ^ 2)
  have ha_nonneg (n : ℕ) : 0 ≤ a n := by
    dsimp only [a]
    exact add_nonneg (prawitzInverseSquareTail_nonneg _) (by positivity)
  have ha_le (n : ℕ) : a n ≤ 2 / y n := by
    have hyn : 0 < y n := by
      dsimp only [y]
      positivity
    have hy1 : 1 ≤ y n := by
      dsimp only [y]
      have hn : (1 : ℝ) ≤ ((n + 1 : ℕ) : ℝ) := by
        exact_mod_cast Nat.succ_le_succ (Nat.zero_le n)
      linarith
    have hhalf : 1 / (2 * (y n) ^ 2) ≤ 1 / y n := by
      apply (div_le_div_iff₀ (by positivity : 0 < 2 * (y n) ^ 2) hyn).2
      nlinarith [sq_nonneg (y n - 1)]
    dsimp only [a]
    calc
      _ ≤ 1 / y n + 1 / y n :=
        add_le_add (prawitzInverseSquareTail_le_inv hyn) hhalf
      _ = 2 / y n := by ring
  have ha : Tendsto a atTop (𝓝 0) :=
    squeeze_zero ha_nonneg ha_le hbound
  let q : ℕ → ℝ := fun n =>
    (Real.sin (Real.pi * y n) / Real.pi) ^ 2
  have hq_nonneg (n : ℕ) : 0 ≤ q n := by
    dsimp only [q]
    positivity
  have hq_le (n : ℕ) : q n ≤ 1 := by
    have hratio : |Real.sin (Real.pi * y n) / Real.pi| ≤ 1 := by
      rw [abs_div, abs_of_pos Real.pi_pos]
      exact (div_le_one Real.pi_pos).2
        ((Real.abs_sin_le_one _).trans (by linarith [Real.two_le_pi]))
    dsimp only [q]
    rw [← sq_abs]
    nlinarith [abs_nonneg (Real.sin (Real.pi * y n) / Real.pi)]
  have hqa : Tendsto (fun n => q n * a n) atTop (𝓝 0) :=
    squeeze_zero
      (fun n => mul_nonneg (hq_nonneg n) (ha_nonneg n))
      (fun n => mul_le_of_le_one_left (ha_nonneg n) (hq_le n)) ha
  have h := (tendsto_const_nhds :
    Tendsto (fun _n : ℕ => (1 / 2 : ℝ)) atTop (𝓝 (1 / 2))).sub hqa
  simpa only [prawitzCotangentIntegralTarget, q, a, y, sub_zero] using h

theorem prawitzCotangentIntegralError_nat_add
    {x : ℝ} (hx : 0 < x) (n : ℕ) :
    prawitzCotangentIntegralError (x + n) =
      prawitzCotangentIntegralError x := by
  induction n with
  | zero => simp
  | succ n ih =>
      rw [show x + ((Nat.succ n : ℕ) : ℝ) =
        (x + (n : ℝ)) + 1 by push_cast; ring]
      rw [prawitzCotangentIntegralError_add_one (by positivity) (by positivity), ih]

theorem prawitzCotangentIntegralError_eq_zero
    {x : ℝ} (hx : 0 < x) : prawitzCotangentIntegralError x = 0 := by
  have hJ := tendsto_integral_prawitzCotangentSineIntegrand_nat_succ_add x
  have hT := prawitzCotangentIntegralTarget_tendsto_nat_succ_add hx
  have hzero : Tendsto (fun n : ℕ =>
      prawitzCotangentIntegralError (((n + 1 : ℕ) : ℝ) + x))
      atTop (𝓝 0) := by
    have h := hJ.sub hT
    simpa only [prawitzCotangentIntegralError, sub_self] using h
  have heq (n : ℕ) :
      prawitzCotangentIntegralError (((n + 1 : ℕ) : ℝ) + x) =
        prawitzCotangentIntegralError x := by
    rw [add_comm]
    exact prawitzCotangentIntegralError_nat_add hx (n + 1)
  have hconst : Tendsto (fun n : ℕ =>
      prawitzCotangentIntegralError (((n + 1 : ℕ) : ℝ) + x))
      atTop (𝓝 (prawitzCotangentIntegralError x)) := by
    convert (tendsto_const_nhds : Tendsto
      (fun _n : ℕ => prawitzCotangentIntegralError x) atTop
        (𝓝 (prawitzCotangentIntegralError x))) using 1
    funext n
    exact heq n
  exact tendsto_nhds_unique hconst hzero

/-- Exact evaluation of the remaining cotangent--sine integral on the
positive half-line. -/
theorem integral_prawitzCotangentSineIntegrand_eq_target
    {x : ℝ} (hx : 0 < x) :
    (∫ u : ℝ in 0..1, prawitzCotangentSineIntegrand x u) =
      prawitzCotangentIntegralTarget x := by
  have h := prawitzCotangentIntegralError_eq_zero hx
  unfold prawitzCotangentIntegralError at h
  linarith

/-- Exact positive-half-line Fourier identification for the Prawitz point
kernel and the nonnegative Vaaler remainder. -/
theorem prawitzPointKernel_positive_identity {x : ℝ} (hx : 0 < x) :
    2 * prawitzPointKernel 1 (-2 * Real.pi * x) - 1 =
      prawitzVaalerPositiveRemainder x := by
  apply (prawitzPointKernel_positive_identity_iff_cotangentIntegral hx).2
  rw [integral_prawitzCotangentSineIntegrand_eq_target hx]
  rfl

/-- The reflected Prawitz point kernels add to the nonnegative Fejér
kernel, with the removable value `1` at the origin. -/
theorem prawitzPointKernel_add_neg (z : ℝ) :
    prawitzPointKernel 1 z + prawitzPointKernel 1 (-z) =
      if z = 0 then 1 else 2 * (1 - Real.cos z) / z ^ 2 := by
  by_cases hz : z = 0
  · subst z
    rw [if_pos rfl, neg_zero, prawitzPointKernel_zero]
    field_simp
    norm_num
  · rw [if_neg hz, prawitzPointKernel_eq_integral_unitPointIntegrand,
      prawitzPointKernel_eq_integral_unitPointIntegrand]
    rw [← MeasureTheory.integral_add
      (integrableOn_prawitzUnitPointIntegrand z)
      (integrableOn_prawitzUnitPointIntegrand (-z))]
    have hcongr :
        (∫ u in Icc (0 : ℝ) 1,
          (prawitzUnitPointIntegrand z u +
            prawitzUnitPointIntegrand (-z) u)) =
          ∫ u in Icc (0 : ℝ) 1,
            2 * (1 - u) * Real.cos (z * u) := by
      apply integral_congr_ae
      filter_upwards [ae_restrict_mem measurableSet_Icc,
          (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 0,
          (volume.restrict (Icc (0 : ℝ) 1)).ae_ne 1] with u hu hu0 hu1
      exact prawitzUnitPointIntegrand_add_neg z
        (lt_of_le_of_ne hu.1 (Ne.symm hu0))
        (lt_of_le_of_ne hu.2 hu1)
    rw [hcongr, integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1),
      integral_two_mul_one_sub_mul_cos hz]

theorem prawitzPointKernel_add_neg_nonneg (z : ℝ) :
    0 ≤ prawitzPointKernel 1 z + prawitzPointKernel 1 (-z) := by
  rw [prawitzPointKernel_add_neg]
  split_ifs with hz
  · norm_num
  · exact div_nonneg (mul_nonneg (by norm_num)
      (sub_nonneg.mpr (Real.cos_le_one z))) (sq_nonneg z)

/-- The sum of the positive and reflected Prawitz--Vaaler remainders is
exactly twice the reflected Prawitz point-kernel sum. -/
theorem prawitzVaalerPositiveRemainder_add_negative_eq_pointKernel
    {x : ℝ} (hx : 0 < x) :
    prawitzVaalerPositiveRemainder x +
        prawitzVaalerNegativeRemainder x =
      2 * (prawitzPointKernel 1 (-2 * Real.pi * x) +
        prawitzPointKernel 1 (2 * Real.pi * x)) := by
  have hkernel := prawitzPointKernel_add_neg (-2 * Real.pi * x)
  rw [if_neg (by positivity : -2 * Real.pi * x ≠ 0)] at hkernel
  rw [show -(-2 * Real.pi * x) = 2 * Real.pi * x by ring] at hkernel
  rw [hkernel]
  rw [show Real.cos (-2 * Real.pi * x) = Real.cos (2 * (Real.pi * x)) by
    rw [show -2 * Real.pi * x = -(2 * (Real.pi * x)) by ring,
      Real.cos_neg]]
  rw [Real.cos_two_mul]
  rw [prawitzVaalerPositiveRemainder, prawitzVaalerNegativeRemainder]
  field_simp [Real.pi_ne_zero, hx.ne']
  rw [Real.sin_sq]
  ring

/-- The positive-half-line Fourier identification implies its reflected
negative-half-line companion through the exact Fejér reflection identity. -/
theorem prawitzVaalerNegativeRemainder_eq_pointKernel_of_positive
    {x : ℝ} (hx : 0 < x)
    (hpos : 2 * prawitzPointKernel 1 (-2 * Real.pi * x) - 1 =
      prawitzVaalerPositiveRemainder x) :
    2 * prawitzPointKernel 1 (2 * Real.pi * x) + 1 =
      prawitzVaalerNegativeRemainder x := by
  linarith [prawitzVaalerPositiveRemainder_add_negative_eq_pointKernel hx]

/-- Once the positive-half-line Fourier identity is supplied, the entire
piecewise spatial majorant is exactly the scaled Prawitz point kernel. -/
theorem prawitzVaalerMajorant_eq_pointKernel_of_positive
    (hpos : ∀ x : ℝ, 0 < x →
      2 * prawitzPointKernel 1 (-2 * Real.pi * x) - 1 =
        prawitzVaalerPositiveRemainder x) (x : ℝ) :
    prawitzVaalerMajorant x =
      2 * prawitzPointKernel 1 (-2 * Real.pi * x) := by
  by_cases hxpos : 0 < x
  · rw [prawitzVaalerMajorant, if_pos hxpos]
    linarith [hpos x hxpos]
  · by_cases hxneg : x < 0
    · rw [prawitzVaalerMajorant, if_neg hxpos, if_pos hxneg]
      have hy : 0 < -x := neg_pos.mpr hxneg
      have hneg := prawitzVaalerNegativeRemainder_eq_pointKernel_of_positive
        hy (hpos (-x) hy)
      rw [show -2 * Real.pi * x = 2 * Real.pi * (-x) by ring]
      linarith
    · have hx : x = 0 := by linarith
      subst x
      rw [prawitzVaalerMajorant]
      simp [prawitzPointKernel_zero]

/-- The explicit Vaaler majorant is exactly the spatial Prawitz point kernel
on the whole real line. -/
theorem prawitzVaalerMajorant_eq_pointKernel (x : ℝ) :
    prawitzVaalerMajorant x =
      2 * prawitzPointKernel 1 (-2 * Real.pi * x) :=
  prawitzVaalerMajorant_eq_pointKernel_of_positive
    (fun _x hx => prawitzPointKernel_positive_identity hx) x

/-- The unit-window Prawitz point kernel takes the exact upper-step value at
the first negative Fourier lattice point. -/
theorem prawitzPointKernel_neg_two_pi :
    prawitzPointKernel 1 (-2 * Real.pi) = 1 / 2 := by
  have h := prawitzPointKernel_positive_identity
    (x := (1 : ℝ)) (by norm_num)
  simp [prawitzVaalerPositiveRemainder] at h
  rw [show -2 * Real.pi = -(2 * Real.pi) by ring]
  linarith

/-- The reflected unit-window point kernel takes the exact lower-step value
at the first positive Fourier lattice point. -/
theorem prawitzPointKernel_two_pi :
    prawitzPointKernel 1 (2 * Real.pi) = -(1 / 2) := by
  have hpos := prawitzPointKernel_positive_identity
    (x := (1 : ℝ)) (by norm_num)
  have hneg := prawitzVaalerNegativeRemainder_eq_pointKernel_of_positive
    (x := (1 : ℝ)) (by norm_num) hpos
  simp [prawitzVaalerNegativeRemainder] at hneg
  linarith

/-- Positive scaling converts the centered CDF kernel exactly to the spatial
argument used by the Prawitz--Vaaler majorant. -/
theorem centeredHalfLineStep_scaled_eq_cdfKernel
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    centeredHalfLineStep (-T * (y - x) / (2 * Real.pi)) =
      2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 := by
  by_cases hy : y ≤ x
  · have harg : ¬ -T * (y - x) / (2 * Real.pi) < 0 := by
      have hnum : 0 ≤ -T * (y - x) :=
        mul_nonneg_of_nonpos_of_nonpos (by linarith) (sub_nonpos.mpr hy)
      exact not_lt.mpr (div_nonneg hnum (by positivity))
    rw [centeredHalfLineStep, if_neg harg]
    simp [hy]
    norm_num
  · have hy' : x < y := lt_of_not_ge hy
    have harg : -T * (y - x) / (2 * Real.pi) < 0 := by
      exact div_neg_of_neg_of_pos
        (mul_neg_of_neg_of_pos (by linarith) (sub_pos.mpr hy')) (by positivity)
    rw [centeredHalfLineStep, if_pos harg]
    simp [hy]

/-- The exact Prawitz point kernel majorizes the centered lower-ray
indicator at every positive frequency scale. -/
theorem centeredCDFKernel_le_two_mul_prawitzPointKernel
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ≤
      2 * prawitzPointKernel T (y - x) := by
  rw [← centeredHalfLineStep_scaled_eq_cdfKernel hT x y]
  calc
    centeredHalfLineStep (-T * (y - x) / (2 * Real.pi)) ≤
        prawitzVaalerMajorant (-T * (y - x) / (2 * Real.pi)) :=
      centeredHalfLineStep_le_prawitzVaalerMajorant _
    _ = 2 * prawitzPointKernel 1
          (-2 * Real.pi * (-T * (y - x) / (2 * Real.pi))) :=
      prawitzVaalerMajorant_eq_pointKernel _
    _ = 2 * prawitzPointKernel T (y - x) := by
      rw [prawitzPointKernel_scale hT]
      congr 2
      field_simp [Real.pi_ne_zero]

/-- The upper Prawitz point-kernel majorization is sharp on the atom-sensitive
diagonal selected by the right-continuous CDF convention. -/
theorem centeredCDFKernel_eq_two_mul_prawitzPointKernel_diagonal
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) x - 1 =
      2 * prawitzPointKernel T (x - x) := by
  rw [prawitzPointKernel_scale hT]
  simp only [mem_Iic, le_refl, Set.indicator_of_mem, mul_one, sub_self,
    mul_zero, prawitzPointKernel_zero]
  norm_num

/-- No positive uniform constant can be subtracted from the upper Prawitz
point-kernel majorant while retaining all spatial points. -/
theorem not_centeredCDFKernel_le_prawitzPointKernel_sub_pos
    {T : ℝ} (hT : 0 < T) (x : ℝ) {delta : ℝ} (hdelta : 0 < delta) :
    ¬ ∀ y : ℝ,
      2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ≤
        2 * prawitzPointKernel T (y - x) - delta := by
  intro h
  have hdiag := h x
  rw [centeredCDFKernel_eq_two_mul_prawitzPointKernel_diagonal
    hT x] at hdiag
  linarith

/-- Reflecting the same majorant gives the corresponding lower envelope,
with the right-continuous endpoint convention valid even at an atom. -/
theorem neg_two_mul_prawitzPointKernel_le_centeredCDFKernel
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    -2 * prawitzPointKernel T (x - y) ≤
      2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 := by
  have hscale :
      prawitzVaalerMajorant (T * (y - x) / (2 * Real.pi)) =
        2 * prawitzPointKernel T (x - y) := by
    rw [prawitzVaalerMajorant_eq_pointKernel, prawitzPointKernel_scale hT]
    congr 2
    field_simp [Real.pi_ne_zero]
    ring
  rw [show -2 * prawitzPointKernel T (x - y) =
      -(2 * prawitzPointKernel T (x - y)) by ring, ← hscale]
  calc
    -prawitzVaalerMajorant (T * (y - x) / (2 * Real.pi)) ≤
        -centeredHalfLineStep (T * (y - x) / (2 * Real.pi)) :=
      neg_le_neg (centeredHalfLineStep_le_prawitzVaalerMajorant _)
    _ ≤ 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 := by
      by_cases hylt : y < x
      · have harg : T * (y - x) / (2 * Real.pi) < 0 := by
          exact div_neg_of_neg_of_pos
            (mul_neg_of_pos_of_neg hT (sub_neg.mpr hylt)) (by positivity)
        simp [centeredHalfLineStep, hylt.le, harg]
        norm_num
      · by_cases hy : y = x
        · subst y
          simp [centeredHalfLineStep]
        · have hygt : x < y := lt_of_le_of_ne (le_of_not_gt hylt) (Ne.symm hy)
          have harg : 0 < T * (y - x) / (2 * Real.pi) :=
            div_pos (mul_pos hT (sub_pos.mpr hygt)) (by positivity)
          simp [centeredHalfLineStep,
            not_le.mpr hygt, not_lt.mpr harg.le]

/-- Integrating the centered lower-ray indicator recovers twice the CDF
minus one. -/
theorem integral_centeredCDFKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (x : ℝ) :
    (∫ y, 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ∂mu) =
      2 * cdf mu x - 1 := by
  have hInd : Integrable
      ((Iic x).indicator (fun _ : ℝ => (1 : ℝ))) mu :=
    (integrable_const (1 : ℝ)).indicator measurableSet_Iic
  have hIntInd :
      (∫ y, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y ∂mu) =
        mu.real (Iic x) := integral_indicator_one measurableSet_Iic
  rw [integral_sub (hInd.const_mul 2) (integrable_const 1),
    integral_const_mul, hIntInd, integral_const, probReal_univ, one_smul,
    cdf_eq_real]

/-- The Prawitz point-kernel expectation is an upper envelope for the
centered CDF whenever that expectation is integrable. -/
theorem cdf_sub_half_le_integral_prawitzPointKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hInt : Integrable (fun y => prawitzPointKernel T (y - x)) mu) :
    cdf mu x - 1 / 2 ≤ ∫ y, prawitzPointKernel T (y - x) ∂mu := by
  have hInd : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  have hle :
      (∫ y, 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ∂mu) ≤
        ∫ y, 2 * prawitzPointKernel T (y - x) ∂mu := by
    apply integral_mono hInd (hInt.const_mul 2)
    exact centeredCDFKernel_le_two_mul_prawitzPointKernel hT x
  rw [integral_centeredCDFKernel, integral_const_mul] at hle
  linarith

/-- Pointwise surplus in the upper Prawitz majorant.  Its expectation records
exactly how much the integrated envelope exceeds the centered CDF. -/
noncomputable def prawitzUpperSlack (T x y : ℝ) : ℝ :=
  2 * prawitzPointKernel T (y - x) -
    (2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1)

theorem prawitzUpperSlack_nonneg
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    0 ≤ prawitzUpperSlack T x y := by
  exact sub_nonneg.mpr
    (centeredCDFKernel_le_two_mul_prawitzPointKernel hT x y)

theorem integrable_prawitzUpperSlack
    (mu : Measure ℝ) [IsFiniteMeasure mu] {T : ℝ} (x : ℝ)
    (hInt : Integrable (fun y => prawitzPointKernel T (y - x)) mu) :
    Integrable (prawitzUpperSlack T x) mu := by
  have hInd : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  exact (hInt.const_mul 2).sub hInd

/-- Half the expected upper slack is the exact gap between the Prawitz
expectation and the centered CDF. -/
theorem half_integral_prawitzUpperSlack_eq_envelope_gap
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (x : ℝ)
    (hInt : Integrable (fun y => prawitzPointKernel T (y - x)) mu) :
    (1 / 2 : ℝ) * ∫ y, prawitzUpperSlack T x y ∂mu =
      (∫ y, prawitzPointKernel T (y - x) ∂mu) - (cdf mu x - 1 / 2) := by
  have hInd : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  unfold prawitzUpperSlack
  rw [integral_sub (hInt.const_mul 2) hInd,
    integral_const_mul, integral_centeredCDFKernel]
  ring

/-- Pointwise surplus in the reflected lower Prawitz majorant. -/
noncomputable def prawitzLowerSlack (T x y : ℝ) : ℝ :=
  (2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) +
    2 * prawitzPointKernel T (x - y)

theorem prawitzLowerSlack_nonneg
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    0 ≤ prawitzLowerSlack T x y := by
  have h := neg_two_mul_prawitzPointKernel_le_centeredCDFKernel hT x y
  unfold prawitzLowerSlack
  linarith

theorem integrable_prawitzLowerSlack
    (mu : Measure ℝ) [IsFiniteMeasure mu] {T : ℝ} (x : ℝ)
    (hInt : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    Integrable (prawitzLowerSlack T x) mu := by
  have hInd : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  exact hInd.add (hInt.const_mul 2)

/-- Half the expected lower slack is the exact gap from the reflected Prawitz
envelope to the centered CDF. -/
theorem half_integral_prawitzLowerSlack_eq_envelope_gap
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (x : ℝ)
    (hInt : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (1 / 2 : ℝ) * ∫ y, prawitzLowerSlack T x y ∂mu =
      (cdf mu x - 1 / 2) -
        (-(∫ y, prawitzPointKernel T (x - y) ∂mu)) := by
  have hInd : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  unfold prawitzLowerSlack
  rw [integral_add hInd (hInt.const_mul 2), integral_const_mul,
    integral_centeredCDFKernel]
  ring

/-- Exact two-law forward recentering: the CDF difference is the point-kernel
expectation difference corrected by half the upper-slack difference. -/
theorem cdf_sub_cdf_eq_prawitzUpperRecentered
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (x : ℝ)
    (hmu : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hnu : Integrable (fun y => prawitzPointKernel T (y - x)) nu) :
    cdf mu x - cdf nu x =
      ((∫ y, prawitzPointKernel T (y - x) ∂mu) -
          ∫ y, prawitzPointKernel T (y - x) ∂nu) -
        (1 / 2 : ℝ) *
          ((∫ y, prawitzUpperSlack T x y ∂mu) -
            ∫ y, prawitzUpperSlack T x y ∂nu) := by
  have hmuGap := half_integral_prawitzUpperSlack_eq_envelope_gap mu x hmu
  have hnuGap := half_integral_prawitzUpperSlack_eq_envelope_gap nu x hnu
  linarith

/-- Exact two-law reflected recentering using the lower-slack correction. -/
theorem cdf_sub_cdf_eq_prawitzLowerRecentered
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (x : ℝ)
    (hmu : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnu : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    cdf mu x - cdf nu x =
      -((∫ y, prawitzPointKernel T (x - y) ∂mu) -
          ∫ y, prawitzPointKernel T (x - y) ∂nu) +
        (1 / 2 : ℝ) *
          ((∫ y, prawitzLowerSlack T x y ∂mu) -
            ∫ y, prawitzLowerSlack T x y ∂nu) := by
  have hmuGap := half_integral_prawitzLowerSlack_eq_envelope_gap mu x hmu
  have hnuGap := half_integral_prawitzLowerSlack_eq_envelope_gap nu x hnu
  linarith

/-- The one-sided Prawitz expectation bound is attained by a Dirac law at
its atom under the right-continuous CDF convention. -/
theorem cdf_dirac_sub_half_eq_integral_prawitzPointKernel
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf (Measure.dirac x) x - 1 / 2 =
      ∫ y, prawitzPointKernel T (y - x) ∂Measure.dirac x := by
  rw [integral_dirac, prawitzPointKernel_scale hT]
  simp only [sub_self, mul_zero, prawitzPointKernel_zero]
  rw [cdf_eq_real, Measure.real_def]
  simp
  norm_num

/-- No positive universal additive improvement of the integrated upper
Prawitz envelope can hold for arbitrary probability laws. -/
theorem not_cdf_dirac_sub_half_le_integral_prawitzPointKernel_sub_pos
    {T : ℝ} (hT : 0 < T) (x : ℝ) {delta : ℝ} (hdelta : 0 < delta) :
    ¬ cdf (Measure.dirac x) x - 1 / 2 ≤
      (∫ y, prawitzPointKernel T (y - x) ∂Measure.dirac x) - delta := by
  rw [cdf_dirac_sub_half_eq_integral_prawitzPointKernel hT x]
  linarith

/-- The reflected Prawitz point-kernel expectation is a lower envelope for
the centered CDF, again without an atomlessness assumption. -/
theorem neg_integral_prawitzPointKernel_le_cdf_sub_half
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hInt : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    -(∫ y, prawitzPointKernel T (x - y) ∂mu) ≤ cdf mu x - 1 / 2 := by
  have hInd : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  have hle :
      (∫ y, -2 * prawitzPointKernel T (x - y) ∂mu) ≤
        ∫ y, 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ∂mu := by
    apply integral_mono (hInt.const_mul (-2)) hInd
    exact neg_two_mul_prawitzPointKernel_le_centeredCDFKernel hT x
  rw [integral_const_mul, integral_centeredCDFKernel] at hle
  linarith

/-- Joint point-mass paired integrand used to exchange spatial expectation
with the finite-frequency Prawitz integral. -/
noncomputable def prawitzDiracPairedIntegrand
    (T x : ℝ) (p : ℝ × ℝ) : ℂ :=
  symmetricPrawitzPVIntegrand T
    (MeasureTheory.charFun (Measure.dirac p.1)) x p.2

theorem measurable_prawitzDiracPairedIntegrand (T x : ℝ) :
    Measurable (prawitzDiracPairedIntegrand T x) := by
  have hphase : Measurable (fun p : ℝ × ℝ =>
      Complex.exp (((-(p.2 * x) : ℝ) : ℂ) * Complex.I)) := by fun_prop
  have hphaseNeg : Measurable (fun p : ℝ × ℝ =>
      Complex.exp (((-((-p.2) * x) : ℝ) : ℂ) * Complex.I)) := by fun_prop
  have hfilter : Measurable (fun p : ℝ × ℝ =>
      scaledPrawitzSmoothingFilter T p.2) :=
    (measurable_scaledPrawitzSmoothingFilter T).comp measurable_snd
  have hfilterNeg : Measurable (fun p : ℝ × ℝ =>
      scaledPrawitzSmoothingFilter T (-p.2)) :=
    (measurable_scaledPrawitzSmoothingFilter T).comp
      (measurable_neg.comp measurable_snd)
  have hchar : Measurable (fun p : ℝ × ℝ =>
      MeasureTheory.charFun (Measure.dirac p.1) p.2) := by
    simp_rw [MeasureTheory.charFun_dirac]
    fun_prop
  have hcharNeg : Measurable (fun p : ℝ × ℝ =>
      MeasureTheory.charFun (Measure.dirac p.1) (-p.2)) := by
    simp_rw [MeasureTheory.charFun_dirac]
    fun_prop
  unfold prawitzDiracPairedIntegrand symmetricPrawitzPVIntegrand
    prawitzPVIntegrand prawitzPVNumerator
  exact (((hphase.mul hfilter).mul hchar).div
      (Complex.continuous_ofReal.measurable.comp measurable_snd)).add
    (((hphaseNeg.mul hfilterNeg).mul hcharNeg).div
      (Complex.continuous_ofReal.measurable.comp
        (measurable_neg.comp measurable_snd)))

/-- The joint point-mass paired integrand has a first-moment majorant uniform
over the closed positive frequency window. -/
theorem norm_prawitzDiracPairedIntegrand_le
    {T : ℝ} (hT : 0 < T) (x y : ℝ) {t : ℝ} (ht : t ∈ Icc 0 T) :
    ‖prawitzDiracPairedIntegrand T x (y, t)‖ ≤
      2 * (7 * (|x| + |y|) + Real.pi / T) := by
  rcases ht.1.eq_or_lt with rfl | ht0
  · simp [prawitzDiracPairedIntegrand, symmetricPrawitzPVIntegrand,
      prawitzPVIntegrand]
    positivity
  have hDirac : Integrable (fun z : ℝ => |z|) (Measure.dirac y) := by
    exact integrable_dirac (by simp)
  have hIm := abs_im_prawitzPVNumerator_charFun_le
    hDirac hT x (show t ∈ Ioc 0 T from ⟨ht0, ht.2⟩)
  simp only [integral_dirac] at hIm
  rw [prawitzDiracPairedIntegrand,
    norm_symmetricPrawitzPVIntegrand
      (isHermitianFrequencyFunction_charFun (Measure.dirac y)) x ht0.ne',
    abs_of_pos ht0]
  calc
    2 * |(prawitzPVNumerator T
        (MeasureTheory.charFun (Measure.dirac y)) x t).im| / t ≤
        2 * ((7 * (|x| + |y|) + Real.pi / T) * t) / t :=
      div_le_div_of_nonneg_right
        (mul_le_mul_of_nonneg_left hIm (by norm_num)) ht0.le
    _ = 2 * (7 * (|x| + |y|) + Real.pi / T) := by field_simp

/-- A finite first absolute moment makes the point-mass Prawitz integrand
integrable on the spatial-frequency product space, licensing Fubini. -/
theorem integrable_prod_prawitzDiracPairedIntegrand
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (prawitzDiracPairedIntegrand T x)
      (mu.prod (volume.restrict (Icc 0 T))) := by
  have hmeas : AEStronglyMeasurable (prawitzDiracPairedIntegrand T x)
      (mu.prod (volume.restrict (Icc 0 T))) :=
    (measurable_prawitzDiracPairedIntegrand T x).aestronglyMeasurable
  rw [integrable_prod_iff hmeas]
  constructor
  · exact ae_of_all mu fun y => by
      have hDirac : Integrable (fun z : ℝ => |z|) (Measure.dirac y) :=
        integrable_dirac (by simp)
      simpa only [prawitzDiracPairedIntegrand] using
        integrableOn_symmetricPrawitzPVIntegrand_charFun
          (Measure.dirac y) hDirac hT x
  · let B : ℝ → ℝ := fun y =>
      T * (2 * (7 * (|x| + |y|) + Real.pi / T))
    have hB : Integrable B mu := by
      have hAbs : Integrable (fun y : ℝ => 14 * T * |y|) mu :=
        hmu1.const_mul (14 * T)
      have hConst : Integrable
          (fun _y : ℝ => 14 * T * |x| + 2 * Real.pi) mu :=
        integrable_const _
      apply (hAbs.add hConst).congr
      filter_upwards with y
      dsimp only [B]
      simp only [Pi.add_apply]
      field_simp [hT.ne']
      ring
    apply hB.mono' hmeas.norm.integral_prod_right'
    filter_upwards with y
    have hSection : Integrable
        (fun t => prawitzDiracPairedIntegrand T x (y, t))
        (volume.restrict (Icc 0 T)) := by
      have hDirac : Integrable (fun z : ℝ => |z|) (Measure.dirac y) :=
        integrable_dirac (by simp)
      simpa only [prawitzDiracPairedIntegrand] using
        integrableOn_symmetricPrawitzPVIntegrand_charFun
          (Measure.dirac y) hDirac hT x
    have hConst : Integrable
        (fun _t : ℝ => 2 * (7 * (|x| + |y|) + Real.pi / T))
        (volume.restrict (Icc 0 T)) := integrable_const _
    have hle :
        (∫ t, ‖prawitzDiracPairedIntegrand T x (y, t)‖
            ∂(volume.restrict (Icc 0 T))) ≤
          ∫ _t, 2 * (7 * (|x| + |y|) + Real.pi / T)
            ∂(volume.restrict (Icc 0 T)) := by
      apply integral_mono_ae hSection.norm hConst
      filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
      exact norm_prawitzDiracPairedIntegrand_le hT x y ht
    rw [Real.norm_eq_abs, abs_of_nonneg (integral_nonneg fun _ => norm_nonneg _)]
    calc
      (∫ t, ‖prawitzDiracPairedIntegrand T x (y, t)‖
          ∂(volume.restrict (Icc 0 T))) ≤
          ∫ _t, 2 * (7 * (|x| + |y|) + Real.pi / T)
            ∂(volume.restrict (Icc 0 T)) := hle
      _ = B y := by simp [B, hT.le]

/-- The translated forward Prawitz point kernel is integrable under every
probability law with a finite first absolute moment. -/
theorem integrable_prawitzPointKernel_sub
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (fun y => prawitzPointKernel T (y - x)) mu := by
  have hProd := integrable_prod_prawitzDiracPairedIntegrand mu hmu1 hT x
  have hInner : Integrable (fun y =>
      ∫ t, prawitzDiracPairedIntegrand T x (y, t)
        ∂(volume.restrict (Icc 0 T))) mu :=
    hProd.integral_prod_left
  have hScaled : Integrable (fun y =>
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t, prawitzDiracPairedIntegrand T x (y, t)
          ∂(volume.restrict (Icc 0 T))).re) mu :=
    (hInner.const_mul _).re
  apply hScaled.congr
  filter_upwards with y
  have hDirac := prawitzInversion_charFun_dirac_eq_pointKernel T x y
  apply_fun Complex.re at hDirac
  simpa only [prawitzInversion, prawitzDiracPairedIntegrand,
    Complex.ofReal_re] using hDirac

/-- The reflected translated Prawitz point kernel has the same first-moment
integrability property. -/
theorem integrable_prawitzPointKernel_reflected_sub
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (fun y => prawitzPointKernel T (x - y)) mu := by
  let muNeg : Measure ℝ := Measure.map (fun y : ℝ => -y) mu
  letI : IsProbabilityMeasure muNeg :=
    Measure.isProbabilityMeasure_map measurable_neg.aemeasurable
  have hNeg1 : Integrable (fun y : ℝ => |y|) muNeg := by
    dsimp only [muNeg]
    rw [integrable_map_measure (by fun_prop) measurable_neg.aemeasurable]
    change Integrable (fun y : ℝ => |-y|) mu
    simpa only [abs_neg] using hmu1
  have hForward : Integrable
      (fun y => prawitzPointKernel T (y - (-x))) muNeg :=
    integrable_prawitzPointKernel_sub muNeg hNeg1 hT (-x)
  have hComp := (integrable_map_measure hForward.aestronglyMeasurable
    measurable_neg.aemeasurable).mp hForward
  apply hComp.congr
  filter_upwards with y
  dsimp only [Function.comp_apply]
  exact congrArg (prawitzPointKernel T) (by ring)

/-- For fixed frequency, the point-mass Prawitz integrand is integrable in
the spatial point under every finite measure. -/
theorem integrable_prawitzPVIntegrand_charFun_dirac
    (mu : Measure ℝ) [IsFiniteMeasure mu] (T x t : ℝ) :
    Integrable (fun y => prawitzPVIntegrand T
      (MeasureTheory.charFun (Measure.dirac y)) x t) mu := by
  let c : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
    scaledPrawitzSmoothingFilter T t / (t : ℂ)
  have hExp : Integrable (fun y : ℝ =>
      Complex.exp (((inner ℝ y t : ℝ) : ℂ) * Complex.I)) mu := by
    apply Integrable.of_bound (by fun_prop) 1
    exact ae_of_all mu fun y => by
      rw [Complex.norm_exp_ofReal_mul_I]
  have hEq : (fun y => prawitzPVIntegrand T
      (MeasureTheory.charFun (Measure.dirac y)) x t) =
      fun y => c * Complex.exp (((inner ℝ y t : ℝ) : ℂ) * Complex.I) := by
    funext y
    rw [prawitzPVIntegrand, prawitzPVNumerator,
      MeasureTheory.charFun_dirac]
    dsimp only [c]
    ring
  rw [hEq]
  exact hExp.const_mul c

/-- Averaging the fixed-frequency point-mass Prawitz integrand recovers the
Prawitz integrand of the averaged law. -/
theorem integral_prawitzPVIntegrand_charFun_dirac
    (mu : Measure ℝ) [IsFiniteMeasure mu] (T x t : ℝ) :
    (∫ y, prawitzPVIntegrand T
        (MeasureTheory.charFun (Measure.dirac y)) x t ∂mu) =
      prawitzPVIntegrand T (MeasureTheory.charFun mu) x t := by
  let c : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
    scaledPrawitzSmoothingFilter T t / (t : ℂ)
  have hEq : (fun y => prawitzPVIntegrand T
      (MeasureTheory.charFun (Measure.dirac y)) x t) =
      fun y => c * Complex.exp (((inner ℝ y t : ℝ) : ℂ) * Complex.I) := by
    funext y
    rw [prawitzPVIntegrand, prawitzPVNumerator,
      MeasureTheory.charFun_dirac]
    dsimp only [c]
    ring
  rw [hEq]
  calc
    (∫ y, c * Complex.exp (((inner ℝ y t : ℝ) : ℂ) * Complex.I) ∂mu) =
        c * ∫ y, Complex.exp (((inner ℝ y t : ℝ) : ℂ) * Complex.I) ∂mu :=
      integral_const_mul c _
    _ = prawitzPVIntegrand T (MeasureTheory.charFun mu) x t := by
      rw [prawitzPVIntegrand, prawitzPVNumerator,
        MeasureTheory.charFun_apply]
      dsimp only [c]
      ring

/-- The same averaging identity holds for the paired Prawitz integrand. -/
theorem integral_prawitzDiracPairedIntegrand
    (mu : Measure ℝ) [IsFiniteMeasure mu] (T x t : ℝ) :
    (∫ y, prawitzDiracPairedIntegrand T x (y, t) ∂mu) =
      symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t := by
  change (∫ y,
      (prawitzPVIntegrand T (MeasureTheory.charFun (Measure.dirac y)) x t +
        prawitzPVIntegrand T (MeasureTheory.charFun (Measure.dirac y)) x (-t)) ∂mu) =
    prawitzPVIntegrand T (MeasureTheory.charFun mu) x t +
      prawitzPVIntegrand T (MeasureTheory.charFun mu) x (-t)
  rw [integral_add
      (integrable_prawitzPVIntegrand_charFun_dirac mu T x t)
      (integrable_prawitzPVIntegrand_charFun_dirac mu T x (-t)),
    integral_prawitzPVIntegrand_charFun_dirac,
    integral_prawitzPVIntegrand_charFun_dirac]

/-- Fubini identifies the average point-mass Prawitz inversion with the
Prawitz inversion of the averaged probability law. -/
theorem integral_prawitzInversion_charFun_dirac
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzInversion T
        (MeasureTheory.charFun (Measure.dirac y)) x ∂mu) =
      prawitzInversion T (MeasureTheory.charFun mu) x := by
  have hProd := integrable_prod_prawitzDiracPairedIntegrand mu hmu1 hT x
  have hSwap :
      (∫ y, ∫ t, prawitzDiracPairedIntegrand T x (y, t)
          ∂(volume.restrict (Icc 0 T)) ∂mu) =
        ∫ t, ∫ y, prawitzDiracPairedIntegrand T x (y, t) ∂mu
          ∂(volume.restrict (Icc 0 T)) :=
    integral_integral_swap hProd
  unfold prawitzInversion
  change (∫ y, (Complex.I / (2 * Real.pi : ℝ)) *
      ∫ t, prawitzDiracPairedIntegrand T x (y, t)
        ∂(volume.restrict (Icc 0 T)) ∂mu) =
    (Complex.I / (2 * Real.pi : ℝ)) *
      ∫ t, symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun mu) x t ∂(volume.restrict (Icc 0 T))
  calc
    (∫ y, (Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t, prawitzDiracPairedIntegrand T x (y, t)
          ∂(volume.restrict (Icc 0 T)) ∂mu) =
        (Complex.I / (2 * Real.pi : ℝ)) *
          ∫ y, ∫ t, prawitzDiracPairedIntegrand T x (y, t)
            ∂(volume.restrict (Icc 0 T)) ∂mu :=
      integral_const_mul _ _
    _ = (Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t, ∫ y, prawitzDiracPairedIntegrand T x (y, t) ∂mu
          ∂(volume.restrict (Icc 0 T)) := by rw [hSwap]
    _ = (Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t, symmetricPrawitzPVIntegrand T
          (MeasureTheory.charFun mu) x t ∂(volume.restrict (Icc 0 T)) := by
      congr 1
      apply integral_congr_ae
      exact ae_of_all _ fun t => integral_prawitzDiracPairedIntegrand mu T x t

/-- The forward Prawitz point-kernel expectation is the real part of the
Prawitz inversion of the law. -/
theorem integral_prawitzPointKernel_sub_eq_re_prawitzInversion
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzPointKernel T (y - x) ∂mu) =
      (prawitzInversion T (MeasureTheory.charFun mu) x).re := by
  have hProd := integrable_prod_prawitzDiracPairedIntegrand mu hmu1 hT x
  have hInner : Integrable (fun y =>
      ∫ t, prawitzDiracPairedIntegrand T x (y, t)
        ∂(volume.restrict (Icc 0 T))) mu :=
    hProd.integral_prod_left
  have hInv : Integrable (fun y => prawitzInversion T
      (MeasureTheory.charFun (Measure.dirac y)) x) mu := by
    unfold prawitzInversion
    simpa only [prawitzDiracPairedIntegrand] using hInner.const_mul
      (Complex.I / (2 * Real.pi : ℝ))
  calc
    (∫ y, prawitzPointKernel T (y - x) ∂mu) =
        ∫ y, (prawitzInversion T
          (MeasureTheory.charFun (Measure.dirac y)) x).re ∂mu := by
      apply integral_congr_ae
      exact ae_of_all _ fun y => by
        have hDirac := prawitzInversion_charFun_dirac_eq_pointKernel T x y
        apply_fun Complex.re at hDirac
        simpa only [Complex.ofReal_re] using hDirac.symm
    _ = (∫ y, prawitzInversion T
        (MeasureTheory.charFun (Measure.dirac y)) x ∂mu).re :=
      integral_re hInv
    _ = (prawitzInversion T (MeasureTheory.charFun mu) x).re := by
      rw [integral_prawitzInversion_charFun_dirac mu hmu1 hT x]

/-- The exact signed Prawitz inversion is an upper envelope for the centered
CDF. -/
theorem cdf_sub_half_le_re_prawitzInversion
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x - 1 / 2 ≤
      (prawitzInversion T (MeasureTheory.charFun mu) x).re := by
  have h := cdf_sub_half_le_integral_prawitzPointKernel
    mu hT x (integrable_prawitzPointKernel_sub mu hmu1 hT x)
  rwa [integral_prawitzPointKernel_sub_eq_re_prawitzInversion
    mu hmu1 hT x] at h

/-- Exact one-sided signed smoothing inequality, before taking the norm of the
Prawitz inversion difference.  This retains cancellation with the reflected
Fejer remainder. -/
theorem cdf_sub_cdf_le_re_prawitzInversion_sub_add_reflection
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x - cdf nu x ≤
      (prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x).re +
      ∫ y, (prawitzPointKernel T (y - x) +
        prawitzPointKernel T (x - y)) ∂nu := by
  have hMu := cdf_sub_half_le_re_prawitzInversion mu hmu1 hT x
  have hNu := neg_integral_prawitzPointKernel_le_cdf_sub_half
    nu hT x (integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x)
  have hNuForward := integrable_prawitzPointKernel_sub nu hnu1 hT x
  have hNuReflected :=
    integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x
  have hForwardEq :=
    integral_prawitzPointKernel_sub_eq_re_prawitzInversion nu hnu1 hT x
  have hSum :
      (∫ y, (prawitzPointKernel T (y - x) +
          prawitzPointKernel T (x - y)) ∂nu) =
        (∫ y, prawitzPointKernel T (y - x) ∂nu) +
          ∫ y, prawitzPointKernel T (x - y) ∂nu :=
    integral_add hNuForward hNuReflected
  rw [Complex.sub_re, hSum, hForwardEq]
  linarith

/-- One-sided signed smoothing inequality: the CDF discrepancy is bounded by
the finite-frequency Prawitz discrepancy plus the reflected Fejér remainder
of the comparison law. -/
theorem cdf_sub_cdf_le_norm_prawitzInversion_sub_add_reflection
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x - cdf nu x ≤
      ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ +
      ∫ y, (prawitzPointKernel T (y - x) +
        prawitzPointKernel T (x - y)) ∂nu := by
  have hMu := cdf_sub_half_le_re_prawitzInversion mu hmu1 hT x
  have hNu := neg_integral_prawitzPointKernel_le_cdf_sub_half
    nu hT x (integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x)
  have hNuForward := integrable_prawitzPointKernel_sub nu hnu1 hT x
  have hNuReflected :=
    integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x
  have hForwardEq :=
    integral_prawitzPointKernel_sub_eq_re_prawitzInversion nu hnu1 hT x
  have hSum :
      (∫ y, (prawitzPointKernel T (y - x) +
          prawitzPointKernel T (x - y)) ∂nu) =
        (∫ y, prawitzPointKernel T (y - x) ∂nu) +
          ∫ y, prawitzPointKernel T (x - y) ∂nu :=
    integral_add hNuForward hNuReflected
  have hRe :
      (prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x).re ≤
      ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ :=
    Complex.re_le_norm _
  rw [Complex.sub_re] at hRe
  rw [hSum, hForwardEq]
  linarith

/-- Absolute integrability of the paired integrand is exactly the analytic
input needed to remove the symmetric truncation in Prawitz inversion. -/
theorem tendsto_truncatedPrawitzInversion_of_integrableOn
    {T : ℝ} {f : ℝ → ℂ} {x : ℝ} (hT : 0 < T)
    (hInt : IntegrableOn (symmetricPrawitzPVIntegrand T f x) (Icc 0 T)) :
    Tendsto (fun epsilon => truncatedPrawitzInversion T epsilon f x)
      (nhdsWithin (0 : ℝ) (Ioi 0)) (nhds (prawitzInversion T f x)) := by
  let g : ℝ → ℂ := symmetricPrawitzPVIntegrand T f x
  have hMeasure :
      Tendsto (fun epsilon : ℝ => volume (Ico 0 epsilon))
        (nhdsWithin (0 : ℝ) (Ioi 0)) (nhds 0) := by
    simp_rw [Real.volume_Ico, sub_zero]
    have hId : Tendsto (fun epsilon : ℝ => epsilon)
        (nhdsWithin (0 : ℝ) (Ioi 0)) (nhds 0) :=
      tendsto_id.mono_left nhdsWithin_le_nhds
    simpa only [ENNReal.ofReal_zero] using ENNReal.tendsto_ofReal hId
  have hSmall :
      Tendsto (fun epsilon : ℝ =>
          ∫ t in Ico 0 epsilon, (Icc 0 T).indicator g t)
        (nhdsWithin (0 : ℝ) (Ioi 0)) (nhds 0) :=
    Integrable.tendsto_setIntegral_nhds_zero
      (hInt.integrable_indicator measurableSet_Icc) hMeasure
  have hIntegral :
      Tendsto (fun epsilon : ℝ => ∫ t in Icc epsilon T, g t)
        (nhdsWithin (0 : ℝ) (Ioi 0)) (nhds (∫ t in Icc 0 T, g t)) := by
    have hSub : Tendsto (fun epsilon : ℝ =>
          (∫ t in Icc 0 T, g t) -
            ∫ t in Ico 0 epsilon, (Icc 0 T).indicator g t)
        (nhdsWithin (0 : ℝ) (Ioi 0)) (nhds (∫ t in Icc 0 T, g t)) := by
      simpa only [sub_zero] using tendsto_const_nhds.sub hSmall
    refine hSub.congr' ?_
    filter_upwards [self_mem_nhdsWithin,
        (eventually_le_nhds hT).filter_mono nhdsWithin_le_nhds] with
      epsilon hepsilon hepsilonT
    have hsub : Ico 0 epsilon ⊆ Icc 0 T := by
      intro t ht
      exact ⟨ht.1, ht.2.le.trans hepsilonT⟩
    have hdiff : Icc 0 T \ Ico 0 epsilon = Icc epsilon T := by
      ext t
      simp only [mem_diff, mem_Icc, mem_Ico]
      constructor
      · rintro ⟨⟨ht0, htT⟩, ht⟩
        exact ⟨le_of_not_gt fun hte => ht ⟨ht0, hte⟩, htT⟩
      · rintro ⟨hte, htT⟩
        exact ⟨⟨hepsilon.le.trans hte, htT⟩,
          fun ht => (ht.2.trans_le hte).false⟩
    rw [← hdiff, integral_diff measurableSet_Ico hInt hsub]
    rw [setIntegral_indicator measurableSet_Icc,
      inter_eq_left.mpr hsub]
  simpa only [truncatedPrawitzInversion, prawitzInversion, g] using
    hIntegral.const_mul (Complex.I / (2 * Real.pi : ℝ))

/-- For every probability law with a finite first absolute moment, the
symmetrically truncated Prawitz functional converges to its full paired
inversion integral as the truncation is removed. -/
theorem tendsto_truncatedPrawitzInversion_charFun
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Tendsto (fun epsilon =>
        truncatedPrawitzInversion T epsilon (MeasureTheory.charFun mu) x)
      (nhdsWithin (0 : ℝ) (Ioi 0))
      (nhds (prawitzInversion T (MeasureTheory.charFun mu) x)) :=
  tendsto_truncatedPrawitzInversion_of_integrableOn hT
    (integrableOn_symmetricPrawitzPVIntegrand_charFun mu hmu1 hT x)

/-- At a positive frequency, the difference of two paired Prawitz integrands
is controlled by twice the corresponding positive-frequency difference. -/
theorem norm_symmetricPrawitzPVIntegrand_charFun_sub_le
    (mu nu : Measure ℝ) {T t : ℝ} (ht : 0 < t) (x : ℝ) :
    ‖symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t‖ ≤
      2 * ‖prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t‖ := by
  let z : ℂ := prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
    prawitzPVIntegrand T (MeasureTheory.charFun nu) x t
  have hmuNeg := prawitzPVIntegrand_neg (T := T)
    (isHermitianFrequencyFunction_charFun mu) x ht.ne'
  have hnuNeg := prawitzPVIntegrand_neg (T := T)
    (isHermitianFrequencyFunction_charFun nu) x ht.ne'
  have hEq :
      symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
          symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t =
        z - (starRingEnd ℂ) z := by
    rw [symmetricPrawitzPVIntegrand, symmetricPrawitzPVIntegrand,
      hmuNeg, hnuNeg]
    dsimp only [z]
    simp only [map_sub]
    ring
  rw [hEq]
  calc
    ‖z - (starRingEnd ℂ) z‖ ≤ ‖z‖ + ‖(starRingEnd ℂ) z‖ := norm_sub_le _ _
    _ = 2 * ‖z‖ := by rw [Complex.norm_conj]; ring

/-- The positive-frequency Prawitz integrand difference is exactly the filter
norm times the characteristic-function discrepancy quotient. -/
theorem norm_prawitzPVIntegrand_charFun_sub_eq
    (mu nu : Measure ℝ) {T t : ℝ} (ht : t ≠ 0) (x : ℝ) :
    ‖prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t‖ =
      ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t := by
  have hEq :
      prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
          prawitzPVIntegrand T (MeasureTheory.charFun nu) x t =
        Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
            scaledPrawitzSmoothingFilter T t *
              (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t) /
                (t : ℂ) := by
    unfold prawitzPVIntegrand prawitzPVNumerator
    ring
  rw [hEq, norm_div, norm_mul, norm_mul,
    Complex.norm_exp_ofReal_mul_I, one_mul, Complex.norm_real,
    Real.norm_eq_abs, charFunDiscrepancyQuotient, if_neg ht]
  ring

/-- The scaled Prawitz filter retains the uniform unit-window bound throughout
its closed positive frequency window. -/
theorem norm_scaledPrawitzSmoothingFilter_le_seven
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Icc 0 T) :
    ‖scaledPrawitzSmoothingFilter T t‖ ≤ 7 := by
  rcases ht.2.eq_or_lt with htEq | htT
  · subst t
    simp [scaledPrawitzSmoothingFilter, hT.ne', prawitzSmoothingFilter]
  · apply norm_prawitzSmoothingFilter_le_seven
    · exact div_nonneg ht.1 hT.le
    · exact (div_lt_one hT).2 htT

/-- The filter-weighted characteristic-function discrepancy is integrable on
every positive Prawitz frequency window. -/
theorem integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) :
    IntegrableOn (fun t => ‖scaledPrawitzSmoothingFilter T t‖ *
      charFunDiscrepancyQuotient mu nu t) (Icc 0 T) := by
  have hq : IntegrableOn (charFunDiscrepancyQuotient mu nu) (Icc 0 T) :=
    (intervalIntegrable_iff_integrableOn_Icc_of_le hT.le).mp
      (intervalIntegrable_charFunDiscrepancyQuotient hmu1 hnu1 0 T)
  apply hq.bdd_mul (c := 7)
  · exact (measurable_scaledPrawitzSmoothingFilter T).norm.aestronglyMeasurable
  · filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
    simpa only [Real.norm_eq_abs, abs_of_nonneg (norm_nonneg _)] using
      norm_scaledPrawitzSmoothingFilter_le_seven hT ht

/-- After subtracting two characteristic functions, the positive-frequency
Prawitz integrand has a genuinely removable singularity and is integrable on
the whole closed smoothing window.  This phase-sensitive form is the input
needed to apply Riemann--Lebesgue before taking norms. -/
theorem integrableOn_prawitzPVIntegrand_charFun_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    IntegrableOn (fun t =>
      prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t) (Icc 0 T) := by
  let d : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
      prawitzPVIntegrand T (MeasureTheory.charFun nu) x t
  let q : ℝ → ℝ := fun t =>
    ‖scaledPrawitzSmoothingFilter T t‖ *
      charFunDiscrepancyQuotient mu nu t
  have hq : IntegrableOn q (Icc 0 T) := by
    simpa only [q] using
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        mu nu hmu1 hnu1 hT
  have hd : AEStronglyMeasurable d (volume.restrict (Icc 0 T)) := by
    dsimp only [d]
    exact ((measurable_prawitzPVIntegrand
      MeasureTheory.measurable_charFun x).sub
        (measurable_prawitzPVIntegrand
          MeasureTheory.measurable_charFun x)).aestronglyMeasurable
  apply hq.mono' hd
  filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
  rcases ht.1.eq_or_lt with rfl | ht0
  · simp [d, q, prawitzPVIntegrand, prawitzPVNumerator,
      charFunDiscrepancyQuotient]
  · simpa only [d, q] using
      (norm_prawitzPVIntegrand_charFun_sub_eq
        (T := T) mu nu ht0.ne' x).le

/-- The entire spatial dependence of a positive-frequency Prawitz
characteristic-function difference is its unit complex exponential phase. -/
theorem prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero
    (mu nu : Measure ℝ) (T x t : ℝ) :
    prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t =
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        (prawitzPVIntegrand T (MeasureTheory.charFun mu) 0 t -
          prawitzPVIntegrand T (MeasureTheory.charFun nu) 0 t) := by
  unfold prawitzPVIntegrand prawitzPVNumerator
  simp only [mul_zero, neg_zero, Complex.ofReal_zero, zero_mul,
    Complex.exp_zero, one_mul]
  ring

/-- The phase-preserving positive-frequency Prawitz difference is continuous
in the spatial parameter. -/
theorem continuous_setIntegral_prawitzPVIntegrand_charFun_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) :
    Continuous (fun x : ℝ => ∫ t in Icc (0 : ℝ) T,
      (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t)) := by
  let a : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T (MeasureTheory.charFun mu) 0 t -
      prawitzPVIntegrand T (MeasureTheory.charFun nu) 0 t
  let f : ℝ → ℂ := (Icc (0 : ℝ) T).indicator a
  have ha : IntegrableOn a (Icc (0 : ℝ) T) := by
    simpa only [a] using
      integrableOn_prawitzPVIntegrand_charFun_sub
        mu nu hmu1 hnu1 hT 0
  have hf : Integrable f := by
    simpa only [f] using ha.integrable_indicator measurableSet_Icc
  have hFourier := continuous_integral_cexp_neg_mul_I_mul f hf
  convert hFourier using 1
  funext x
  rw [← integral_indicator measurableSet_Icc]
  apply integral_congr_ae
  filter_upwards with t
  by_cases ht : t ∈ Icc (0 : ℝ) T
  · simp only [Set.indicator_of_mem ht, f, a]
    exact prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero
      mu nu T x t
  · simp only [Set.indicator_of_notMem ht, f, mul_zero]

/-- The phase-preserving positive-frequency Prawitz difference vanishes in
the positive spatial tail before taking any complex norm. -/
theorem tendsto_setIntegral_prawitzPVIntegrand_charFun_sub_atTop
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) :
    Tendsto (fun x : ℝ => ∫ t in Icc (0 : ℝ) T,
      (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t))
      atTop (𝓝 0) := by
  let a : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T (MeasureTheory.charFun mu) 0 t -
      prawitzPVIntegrand T (MeasureTheory.charFun nu) 0 t
  let f : ℝ → ℂ := (Icc (0 : ℝ) T).indicator a
  have ha : IntegrableOn a (Icc (0 : ℝ) T) := by
    simpa only [a] using
      integrableOn_prawitzPVIntegrand_charFun_sub
        mu nu hmu1 hnu1 hT 0
  have hf : Integrable f := by
    simpa only [f] using ha.integrable_indicator measurableSet_Icc
  have hrl := tendsto_integral_cexp_neg_mul_I_mul_atTop f
  convert hrl using 1
  funext x
  rw [← integral_indicator measurableSet_Icc]
  apply integral_congr_ae
  filter_upwards with t
  by_cases ht : t ∈ Icc (0 : ℝ) T
  · simp only [Set.indicator_of_mem ht, f, a]
    exact prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero
      mu nu T x t
  · simp only [Set.indicator_of_notMem ht, f, mul_zero]

/-- The phase-preserving positive-frequency Prawitz difference also vanishes
in the negative spatial tail before taking any complex norm. -/
theorem tendsto_setIntegral_prawitzPVIntegrand_charFun_sub_atBot
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) :
    Tendsto (fun x : ℝ => ∫ t in Icc (0 : ℝ) T,
      (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T (MeasureTheory.charFun nu) x t))
      atBot (𝓝 0) := by
  let a : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T (MeasureTheory.charFun mu) 0 t -
      prawitzPVIntegrand T (MeasureTheory.charFun nu) 0 t
  let f : ℝ → ℂ := (Icc (0 : ℝ) T).indicator a
  have ha : IntegrableOn a (Icc (0 : ℝ) T) := by
    simpa only [a] using
      integrableOn_prawitzPVIntegrand_charFun_sub
        mu nu hmu1 hnu1 hT 0
  have hf : Integrable f := by
    simpa only [f] using ha.integrable_indicator measurableSet_Icc
  have hrl := tendsto_integral_cexp_neg_mul_I_mul_atBot f
  convert hrl using 1
  funext x
  rw [← integral_indicator measurableSet_Icc]
  apply integral_congr_ae
  filter_upwards with t
  by_cases ht : t ∈ Icc (0 : ℝ) T
  · simp only [Set.indicator_of_mem ht, f, a]
    exact prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero
      mu nu T x t
  · simp only [Set.indicator_of_notMem ht, f, mul_zero]

/-- The structural uniform filter bound reduces the weighted positive-window
discrepancy integral to its unweighted counterpart. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_le_const
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) :
    (∫ t in Icc 0 T, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t) ≤
      (1 + Real.pi ^ 2 / 32) *
        ∫ t in Icc 0 T, charFunDiscrepancyQuotient mu nu t := by
  let q : ℝ → ℝ := charFunDiscrepancyQuotient mu nu
  let C : ℝ := 1 + Real.pi ^ 2 / 32
  have hq : IntegrableOn q (Icc 0 T) :=
    (intervalIntegrable_iff_integrableOn_Icc_of_le hT.le).mp
      (intervalIntegrable_charFunDiscrepancyQuotient hmu1 hnu1 0 T)
  have hweighted : IntegrableOn
      (fun t => ‖scaledPrawitzSmoothingFilter T t‖ * q t) (Icc 0 T) := by
    simpa only [q] using
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        mu nu hmu1 hnu1 hT
  have hC : 0 ≤ C := by dsimp only [C]; positivity
  change (∫ t in Icc 0 T,
      ‖scaledPrawitzSmoothingFilter T t‖ * q t) ≤
    C * ∫ t in Icc 0 T, q t
  rw [← integral_const_mul]
  apply setIntegral_mono_on hweighted (hq.const_mul C) measurableSet_Icc
  intro s hs
  have hq0 : 0 ≤ q s := charFunDiscrepancyQuotient_nonneg mu nu s
  rcases hs.2.eq_or_lt with hsT | hsT
  · subst s
    rw [scaledPrawitzSmoothingFilter_eq_zero_of_abs_ge hT
        (by rw [abs_of_pos hT])]
    simp only [norm_zero, zero_mul]
    exact mul_nonneg hC hq0
  · exact mul_le_mul_of_nonneg_right
      (norm_scaledPrawitzSmoothingFilter_le_one_add_pi_sq_div_thirtytwo
        hT hs.1 hsT) hq0

/-- Uniform filter reduction on an initial subwindow of a possibly larger
Prawitz smoothing band. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_local_le_const
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T₀ T : ℝ} (hT₀ : 0 ≤ T₀) (hT : 0 < T) (hT₀T : T₀ ≤ T) :
    (∫ t in Icc 0 T₀, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t) ≤
      (1 + Real.pi ^ 2 / 32) *
        ∫ t in Icc 0 T₀, charFunDiscrepancyQuotient mu nu t := by
  let q : ℝ → ℝ := charFunDiscrepancyQuotient mu nu
  let C : ℝ := 1 + Real.pi ^ 2 / 32
  have hq : IntegrableOn q (Icc 0 T₀) :=
    (intervalIntegrable_iff_integrableOn_Icc_of_le hT₀).mp
      (intervalIntegrable_charFunDiscrepancyQuotient hmu1 hnu1 0 T₀)
  have hweighted : IntegrableOn
      (fun t => ‖scaledPrawitzSmoothingFilter T t‖ * q t) (Icc 0 T₀) := by
    have hfull :=
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        mu nu hmu1 hnu1 hT
    exact hfull.mono_set (Icc_subset_Icc_right hT₀T)
  have hC : 0 ≤ C := by dsimp only [C]; positivity
  change (∫ t in Icc 0 T₀,
      ‖scaledPrawitzSmoothingFilter T t‖ * q t) ≤
    C * ∫ t in Icc 0 T₀, q t
  rw [← integral_const_mul]
  apply setIntegral_mono_on hweighted (hq.const_mul C) measurableSet_Icc
  intro t ht
  have hq0 : 0 ≤ q t := charFunDiscrepancyQuotient_nonneg mu nu t
  by_cases htEq : t = T
  · subst t
    rw [scaledPrawitzSmoothingFilter_eq_zero_of_abs_ge hT
        (by rw [abs_of_pos hT])]
    simp only [norm_zero, zero_mul]
    exact mul_nonneg hC hq0
  · exact mul_le_mul_of_nonneg_right
      (norm_scaledPrawitzSmoothingFilter_le_one_add_pi_sq_div_thirtytwo
        hT ht.1 (lt_of_le_of_ne (ht.2.trans hT₀T) htEq)) hq0

/-- Exact decomposition of a filter-weighted discrepancy integral at an
arbitrary positive inner cutoff. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_split
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T₀ T : ℝ} (hT₀ : 0 < T₀) (hT₀T : T₀ ≤ T) :
    (∫ t in Icc 0 T, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t) =
      (∫ t in Icc 0 T₀, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t) +
      ∫ t in Ioc T₀ T, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t := by
  have hT : 0 < T := hT₀.trans_le hT₀T
  let f : ℝ → ℝ := fun t => ‖scaledPrawitzSmoothingFilter T t‖ *
    charFunDiscrepancyQuotient mu nu t
  have hf : IntegrableOn f (Icc 0 T) := by
    simpa only [f] using
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        mu nu hmu1 hnu1 hT
  have hf₀ : IntegrableOn f (Icc 0 T₀) :=
    hf.mono_set (Icc_subset_Icc_right hT₀T)
  have hftail : IntegrableOn f (Ioc T₀ T) := by
    apply hf.mono_set
    intro t ht
    exact ⟨hT₀.le.trans ht.1.le, ht.2⟩
  have hunion : Icc (0 : ℝ) T₀ ∪ Ioc T₀ T = Icc 0 T := by
    ext t
    simp only [mem_union, mem_Icc, mem_Ioc]
    constructor
    · rintro (ht | ht)
      · exact ⟨ht.1, ht.2.trans hT₀T⟩
      · exact ⟨hT₀.le.trans ht.1.le, ht.2⟩
    · intro ht
      by_cases h : t ≤ T₀
      · exact Or.inl ⟨ht.1, h⟩
      · exact Or.inr ⟨lt_of_not_ge h, ht.2⟩
  have hdisjoint : Disjoint (Icc (0 : ℝ) T₀) (Ioc T₀ T) := by
    rw [Set.disjoint_left]
    intro t ht₀ htTail
    exact (not_lt_of_ge ht₀.2) htTail.1
  change (∫ t in Icc 0 T, f t) =
    (∫ t in Icc 0 T₀, f t) + ∫ t in Ioc T₀ T, f t
  rw [← hunion, setIntegral_union hdisjoint measurableSet_Ioc hf₀ hftail]

/-- Two-cutoff Prawitz decomposition.  Frequencies up to `T₀` retain the
exact discrepancy, while the larger smoothing band uses only the universal
`2 / |t|` characteristic-function bound. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_le_twoCutoff
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T₀ T : ℝ} (hT₀ : 0 < T₀) (hT₀T : T₀ ≤ T) :
    (∫ t in Icc 0 T, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t) ≤
      (∫ t in Icc 0 T₀, ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient mu nu t) +
      ∫ t in Ioc T₀ T, ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|) := by
  have hT : 0 < T := hT₀.trans_le hT₀T
  let f : ℝ → ℝ := fun t => ‖scaledPrawitzSmoothingFilter T t‖ *
    charFunDiscrepancyQuotient mu nu t
  let g : ℝ → ℝ := fun t => ‖scaledPrawitzSmoothingFilter T t‖ *
    (2 / |t|)
  have hf : IntegrableOn f (Icc 0 T) := by
    simpa only [f] using
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        mu nu hmu1 hnu1 hT
  have hf₀ : IntegrableOn f (Icc 0 T₀) :=
    hf.mono_set (Icc_subset_Icc_right hT₀T)
  have hftail : IntegrableOn f (Ioc T₀ T) := by
    apply hf.mono_set
    intro t ht
    exact ⟨hT₀.le.trans ht.1.le, ht.2⟩
  have hgStrong : StronglyMeasurable g := by
    dsimp only [g]
    exact ((measurable_scaledPrawitzSmoothingFilter T).norm.mul
      (measurable_const.div measurable_abs)).stronglyMeasurable
  have hgTail : IntegrableOn g (Ioc T₀ T) := by
    have hconst : IntegrableOn (fun _ : ℝ => 14 / T₀) (Ioc T₀ T) :=
      integrableOn_const measure_Ioc_lt_top.ne
    apply Integrable.mono' hconst hgStrong.aestronglyMeasurable
    filter_upwards [ae_restrict_mem measurableSet_Ioc] with t ht
    rw [Real.norm_eq_abs, abs_of_nonneg (by
      dsimp only [g]
      positivity : 0 ≤ g t)]
    have ht0 : 0 < t := hT₀.trans ht.1
    have hfilter : ‖scaledPrawitzSmoothingFilter T t‖ ≤ 7 :=
      norm_scaledPrawitzSmoothingFilter_le_seven hT
        ⟨ht0.le, ht.2⟩
    have hdiv : 2 / |t| ≤ 2 / T₀ := by
      rw [abs_of_pos ht0]
      exact div_le_div_of_nonneg_left (by norm_num) hT₀ ht.1.le
    dsimp only [g]
    have hnonneg : 0 ≤ 2 / |t| := by positivity
    calc
      ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|) ≤
          7 * (2 / |t|) := mul_le_mul_of_nonneg_right hfilter hnonneg
      _ ≤ 7 * (2 / T₀) := mul_le_mul_of_nonneg_left hdiv (by norm_num)
      _ = 14 / T₀ := by ring
  have hunion : Icc (0 : ℝ) T₀ ∪ Ioc T₀ T = Icc 0 T := by
    ext t
    simp only [mem_union, mem_Icc, mem_Ioc]
    constructor
    · rintro (ht | ht)
      · exact ⟨ht.1, ht.2.trans hT₀T⟩
      · exact ⟨hT₀.le.trans ht.1.le, ht.2⟩
    · intro ht
      by_cases h : t ≤ T₀
      · exact Or.inl ⟨ht.1, h⟩
      · exact Or.inr ⟨lt_of_not_ge h, ht.2⟩
  have hdisjoint : Disjoint (Icc (0 : ℝ) T₀) (Ioc T₀ T) := by
    rw [Set.disjoint_left]
    intro t ht₀ htTail
    exact (not_lt_of_ge ht₀.2) htTail.1
  have hsplit : (∫ t in Icc 0 T, f t) =
      (∫ t in Icc 0 T₀, f t) + ∫ t in Ioc T₀ T, f t := by
    rw [← hunion, setIntegral_union hdisjoint measurableSet_Ioc hf₀ hftail]
  have htail : (∫ t in Ioc T₀ T, f t) ≤ ∫ t in Ioc T₀ T, g t := by
    apply integral_mono hftail hgTail
    intro t
    exact mul_le_mul_of_nonneg_left
      (charFunDiscrepancyQuotient_le_two_div_abs mu nu t) (norm_nonneg _)
  change (∫ t in Icc 0 T, f t) ≤
    (∫ t in Icc 0 T₀, f t) + ∫ t in Ioc T₀ T, g t
  rw [hsplit]
  simpa only [add_comm] using
    add_le_add_left htail (∫ t in Icc 0 T₀, f t)

/-- Scaling the outer Prawitz band to the unit interval removes the smoothing
bandwidth from its universal discrepancy majorant. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_eq_unit
    {T₀ T : ℝ} (hT₀ : 0 < T₀) (hT₀T : T₀ ≤ T) :
    (∫ t in Ioc T₀ T,
      ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|)) =
      ∫ u in Ioc (T₀ / T) 1,
        ‖prawitzSmoothingFilter u‖ * (2 / |u|) := by
  have hT : 0 < T := hT₀.trans_le hT₀T
  have hratio : T₀ / T ≤ 1 := (div_le_one hT).2 hT₀T
  let f : ℝ → ℝ := fun t =>
    ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|)
  rw [← intervalIntegral.integral_of_le hT₀T,
    ← intervalIntegral.integral_of_le hratio]
  have hchange := intervalIntegral.smul_integral_comp_mul_left
    (f := f) (a := T₀ / T) (b := 1) T
  calc
    (∫ t in T₀..T, f t) = T * ∫ u in T₀ / T..1, f (T * u) := by
      simpa only [smul_eq_mul, mul_one, mul_div_cancel₀ T₀ hT.ne'] using
        hchange.symm
    _ = ∫ u in T₀ / T..1,
        ‖prawitzSmoothingFilter u‖ * (2 / |u|) := by
      rw [← intervalIntegral.integral_const_mul]
      apply intervalIntegral.integral_congr
      intro u _
      by_cases hu : u = 0
      · subst u
        simp [f, scaledPrawitzSmoothingFilter]
      · dsimp only [f, scaledPrawitzSmoothingFilter]
        rw [mul_div_cancel_left₀ u hT.ne', abs_mul, abs_of_pos hT]
        field_simp

/-- Optimization-ready form of the scaled outer-band identity, with its
universal characteristic-function factor pulled outside the unit integral. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_eq_two_mul
    {T₀ T : ℝ} (hT₀ : 0 < T₀) (hT₀T : T₀ ≤ T) :
    (∫ t in Ioc T₀ T,
      ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|)) =
      2 * ∫ u in Ioc (T₀ / T) 1,
        ‖prawitzSmoothingFilter u‖ / u := by
  rw [integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_eq_unit
    hT₀ hT₀T, ← integral_const_mul]
  apply integral_congr_ae
  filter_upwards [ae_restrict_mem measurableSet_Ioc] with u hu
  have hT : 0 < T := hT₀.trans_le hT₀T
  have hu0 : 0 < u := (div_pos hT₀ hT).trans hu.1
  rw [abs_of_pos hu0]
  ring

/-- A closed logarithmic upper bound for the dimensionless Prawitz tail. -/
theorem integral_norm_prawitzSmoothingFilter_div_le_log
    {a : ℝ} (ha : 0 < a) (ha1 : a ≤ 1) :
    (∫ u in Ioc a 1, ‖prawitzSmoothingFilter u‖ / u) ≤
      (1 + Real.pi ^ 2 / 32) * Real.log (1 / a) := by
  let g : ℝ → ℝ := fun u => ‖prawitzSmoothingFilter u‖ / u
  let C : ℝ := 1 + Real.pi ^ 2 / 32
  let G : ℝ → ℝ := fun u => C * (1 / u)
  have hgStrong : StronglyMeasurable g := by
    dsimp only [g]
    exact (measurable_prawitzSmoothingFilter.norm.div
      measurable_id).stronglyMeasurable
  have hg : IntegrableOn g (Ioc a 1) := by
    have hconst : IntegrableOn (fun _ : ℝ => 7 / a) (Ioc a 1) :=
      integrableOn_const measure_Ioc_lt_top.ne
    apply Integrable.mono' hconst hgStrong.aestronglyMeasurable
    filter_upwards [ae_restrict_mem measurableSet_Ioc] with u hu
    have hu0 : 0 < u := ha.trans hu.1
    rw [Real.norm_eq_abs, abs_of_nonneg (by
      dsimp only [g]
      positivity : 0 ≤ g u)]
    have hnorm : ‖prawitzSmoothingFilter u‖ ≤ 7 := by
      rcases hu.2.eq_or_lt with rfl | hu1
      · simp [prawitzSmoothingFilter]
      · exact norm_prawitzSmoothingFilter_le_seven hu0.le hu1
    have hdiv : 1 / u ≤ 1 / a := by
      exact one_div_le_one_div_of_le ha hu.1.le
    dsimp only [g]
    calc
      ‖prawitzSmoothingFilter u‖ / u ≤ 7 / u :=
        (div_le_div_iff_of_pos_right hu0).2 hnorm
      _ ≤ 7 / a := by
        exact (div_le_div_iff₀ hu0 ha).2 (by nlinarith [hu.1])
  have hG : IntegrableOn G (Ioc a 1) := by
    have hinv : IntervalIntegrable (fun u : ℝ => 1 / u) volume a 1 :=
      intervalIntegral.intervalIntegrable_one_div
        (f := fun u : ℝ => u)
        (fun u hu => by
          rw [uIcc_of_le ha1] at hu
          exact ne_of_gt (ha.trans_le hu.1)) continuousOn_id
    have hIcc := (intervalIntegrable_iff_integrableOn_Icc_of_le ha1).mp
      (hinv.const_mul C)
    simpa only [G] using hIcc.mono_set Ioc_subset_Icc_self
  have hpoint : ∀ u ∈ Ioc a 1, g u ≤ G u := by
    intro u hu
    have hu0 : 0 < u := ha.trans hu.1
    have hC : 0 ≤ C := by dsimp only [C]; positivity
    have hnorm : ‖prawitzSmoothingFilter u‖ ≤ C := by
      rcases hu.2.eq_or_lt with rfl | hu1
      · dsimp only [C]
        simp [prawitzSmoothingFilter]
        positivity
      · dsimp only [C]
        exact norm_prawitzSmoothingFilter_le_one_add_pi_sq_div_thirtytwo
          hu0.le hu1
    dsimp only [g, G]
    calc
      ‖prawitzSmoothingFilter u‖ / u ≤ C / u :=
        (div_le_div_iff_of_pos_right hu0).2 hnorm
      _ = C * (1 / u) := by ring
  change (∫ u in Ioc a 1, g u) ≤ _
  calc
    (∫ u in Ioc a 1, g u) ≤ ∫ u in Ioc a 1, G u := by
      exact setIntegral_mono_on hg hG measurableSet_Ioc hpoint
    _ = C * Real.log (1 / a) := by
      rw [← intervalIntegral.integral_of_le ha1]
      dsimp only [G]
      rw [intervalIntegral.integral_const_mul,
        integral_one_div_of_pos ha zero_lt_one]
    _ = _ := rfl

/-- Endpoint-sensitive unit-tail bound.  Unlike the uniform logarithmic
estimate, this majorant is quadratic as `a` tends to one. -/
theorem integral_norm_prawitzSmoothingFilter_div_le_endpoint
    {a : ℝ} (haHalf : 1 / 2 ≤ a) (ha1 : a ≤ 1) :
    (∫ u in Ioc a 1, ‖prawitzSmoothingFilter u‖ / u) ≤
      (Real.pi ^ 2 / 2 + Real.pi) *
        (Real.log (1 / a) - (1 - a)) := by
  have ha : 0 < a := lt_of_lt_of_le (by norm_num) haHalf
  let g : ℝ → ℝ := fun u => ‖prawitzSmoothingFilter u‖ / u
  let K : ℝ := Real.pi ^ 2 / 2 + Real.pi
  let G : ℝ → ℝ := fun u => K * ((1 - u) / u)
  have hgStrong : StronglyMeasurable g := by
    dsimp only [g]
    exact (measurable_prawitzSmoothingFilter.norm.div
      measurable_id).stronglyMeasurable
  have hg : IntegrableOn g (Ioc a 1) := by
    have hconst : IntegrableOn (fun _ : ℝ => 7 / a) (Ioc a 1) :=
      integrableOn_const measure_Ioc_lt_top.ne
    apply Integrable.mono' hconst hgStrong.aestronglyMeasurable
    filter_upwards [ae_restrict_mem measurableSet_Ioc] with u hu
    have hu0 : 0 < u := ha.trans hu.1
    rw [Real.norm_eq_abs, abs_of_nonneg (by
      dsimp only [g]
      positivity : 0 ≤ g u)]
    have hnorm : ‖prawitzSmoothingFilter u‖ ≤ 7 := by
      rcases hu.2.eq_or_lt with rfl | hu1
      · simp [prawitzSmoothingFilter]
      · exact norm_prawitzSmoothingFilter_le_seven hu0.le hu1
    dsimp only [g]
    calc
      ‖prawitzSmoothingFilter u‖ / u ≤ 7 / u :=
        (div_le_div_iff_of_pos_right hu0).2 hnorm
      _ ≤ 7 / a := (div_le_div_iff₀ hu0 ha).2 (by nlinarith [hu.1])
  have hG : IntegrableOn G (Ioc a 1) := by
    have hden : ∀ u ∈ Icc a 1, u ≠ 0 := fun u hu =>
      ne_of_gt (ha.trans_le hu.1)
    have hratio : ContinuousOn (fun u : ℝ => (1 - u) / u) (Icc a 1) :=
      (continuousOn_const.sub continuousOn_id).div continuousOn_id hden
    have hcont : ContinuousOn G (Icc a 1) := by
      dsimp only [G]
      exact continuousOn_const.mul hratio
    exact hcont.integrableOn_Icc.mono_set Ioc_subset_Icc_self
  have hpoint : ∀ u ∈ Ioc a 1, g u ≤ G u := by
    intro u hu
    have hu0 : 0 < u := ha.trans hu.1
    rcases hu.2.eq_or_lt with rfl | hu1
    · simp [g, G, prawitzSmoothingFilter]
    · have hnorm := norm_prawitzSmoothingFilter_le_endpoint_linear
        (haHalf.trans hu.1.le) hu1
      dsimp only [g, G, K]
      calc
        ‖prawitzSmoothingFilter u‖ / u ≤
            ((Real.pi ^ 2 / 2 + Real.pi) * (1 - u)) / u :=
          (div_le_div_iff_of_pos_right hu0).2 hnorm
        _ = (Real.pi ^ 2 / 2 + Real.pi) * ((1 - u) / u) := by ring
  change (∫ u in Ioc a 1, g u) ≤ _
  calc
    (∫ u in Ioc a 1, g u) ≤ ∫ u in Ioc a 1, G u := by
      exact setIntegral_mono_on hg hG measurableSet_Ioc hpoint
    _ = K * (Real.log (1 / a) - (1 - a)) := by
      rw [← intervalIntegral.integral_of_le ha1]
      have hinv : IntervalIntegrable (fun u : ℝ => 1 / u) volume a 1 :=
        intervalIntegral.intervalIntegrable_one_div
          (f := fun u : ℝ => u)
          (fun u hu => by
            rw [uIcc_of_le ha1] at hu
            exact ne_of_gt (ha.trans_le hu.1)) continuousOn_id
      dsimp only [G]
      have hcongr : (∫ u in a..1, K * ((1 - u) / u)) =
          ∫ u in a..1, K * (1 / u - 1) := by
        apply intervalIntegral.integral_congr
        intro u hu
        rw [uIcc_of_le ha1] at hu
        have hu0 : u ≠ 0 := ne_of_gt (ha.trans_le hu.1)
        field_simp
      rw [hcongr, intervalIntegral.integral_const_mul,
        intervalIntegral.integral_sub hinv intervalIntegral.intervalIntegrable_const,
        integral_one_div_of_pos ha zero_lt_one,
        integral_one]
    _ = _ := rfl

/-- Closed logarithmic bound for the universal outer band in the original
frequency scale. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_le_log
    {T₀ T : ℝ} (hT₀ : 0 < T₀) (hT₀T : T₀ ≤ T) :
    (∫ t in Ioc T₀ T,
      ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|)) ≤
      2 * (1 + Real.pi ^ 2 / 32) * Real.log (T / T₀) := by
  have hT : 0 < T := hT₀.trans_le hT₀T
  have ha : 0 < T₀ / T := div_pos hT₀ hT
  have ha1 : T₀ / T ≤ 1 := (div_le_one hT).2 hT₀T
  rw [integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_eq_two_mul
    hT₀ hT₀T]
  calc
    2 * (∫ u in Ioc (T₀ / T) 1,
        ‖prawitzSmoothingFilter u‖ / u) ≤
        2 * ((1 + Real.pi ^ 2 / 32) * Real.log (1 / (T₀ / T))) :=
      mul_le_mul_of_nonneg_left
        (integral_norm_prawitzSmoothingFilter_div_le_log ha ha1) (by norm_num)
    _ = 2 * (1 + Real.pi ^ 2 / 32) * Real.log (T / T₀) := by
      have harg : 1 / (T₀ / T) = T / T₀ := by
        field_simp
      rw [harg]
      ring

/-- Endpoint-sensitive scaled outer-band estimate, valid while the smoothing
bandwidth is at most twice the Taylor cutoff. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_le_endpoint
    {T₀ T : ℝ} (hT₀ : 0 < T₀) (hT₀T : T₀ ≤ T) (hTT₀ : T ≤ 2 * T₀) :
    (∫ t in Ioc T₀ T,
      ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|)) ≤
      2 * (Real.pi ^ 2 / 2 + Real.pi) *
        (Real.log (T / T₀) - (1 - T₀ / T)) := by
  have hT : 0 < T := hT₀.trans_le hT₀T
  have haHalf : 1 / 2 ≤ T₀ / T := by
    rw [le_div_iff₀ hT]
    nlinarith
  have ha1 : T₀ / T ≤ 1 := (div_le_one hT).2 hT₀T
  rw [integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_eq_two_mul
    hT₀ hT₀T]
  calc
    2 * (∫ u in Ioc (T₀ / T) 1,
        ‖prawitzSmoothingFilter u‖ / u) ≤
        2 * ((Real.pi ^ 2 / 2 + Real.pi) *
          (Real.log (1 / (T₀ / T)) - (1 - T₀ / T))) :=
      mul_le_mul_of_nonneg_left
        (integral_norm_prawitzSmoothingFilter_div_le_endpoint haHalf ha1)
        (by norm_num)
    _ = 2 * (Real.pi ^ 2 / 2 + Real.pi) *
        (Real.log (T / T₀) - (1 - T₀ / T)) := by
      have harg : 1 / (T₀ / T) = T / T₀ := by field_simp
      rw [harg]
      ring

/-- Exact signed integral representation of the real part of a Prawitz
inversion difference. -/
theorem re_prawitzInversion_charFun_sub_eq_signedIntegral
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x).re =
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t)).re := by
  have hmu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    mu hmu1 hT x
  have hnu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    nu hnu1 hT x
  rw [prawitzInversion, prawitzInversion, ← mul_sub,
    ← integral_sub hmu hnu]

/-- The difference of two full Prawitz inversion functionals is bounded by
the integral norm of their paired-frequency discrepancy. -/
theorem norm_prawitzInversion_charFun_sub_le_symmetricIntegral
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ ≤
      ‖Complex.I / (2 * Real.pi : ℝ)‖ *
        ∫ t in Icc 0 T,
          ‖symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t‖ := by
  have hmu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    mu hmu1 hT x
  have hnu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    nu hnu1 hT x
  rw [prawitzInversion, prawitzInversion, ← mul_sub,
    ← integral_sub hmu hnu, norm_mul]
  exact mul_le_mul_of_nonneg_left
    (norm_integral_le_integral_norm _) (norm_nonneg _)

/-- Direct comparison of two Prawitz inversion functionals by their weighted
positive-frequency characteristic-function discrepancy. -/
theorem norm_prawitzInversion_charFun_sub_le_integral_discrepancy
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ ≤
      (1 / Real.pi) * ∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu nu t := by
  let d : ℝ → ℂ := fun t =>
    symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
      symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t
  let q : ℝ → ℝ := fun t => ‖scaledPrawitzSmoothingFilter T t‖ *
    charFunDiscrepancyQuotient mu nu t
  have hmu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    mu hmu1 hT x
  have hnu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    nu hnu1 hT x
  have hd : IntegrableOn d (Icc 0 T) := by
    simpa only [d] using hmu.sub hnu
  have hq : IntegrableOn q (Icc 0 T) := by
    simpa only [q] using
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        mu nu hmu1 hnu1 hT
  have hpoint : ∀ t ∈ Icc 0 T, ‖d t‖ ≤ 2 * q t := by
    intro t ht
    rcases ht.1.eq_or_lt with rfl | ht0
    · simp [d, q, symmetricPrawitzPVIntegrand, prawitzPVIntegrand,
        charFunDiscrepancyQuotient]
    · calc
        ‖d t‖ ≤ 2 *
            ‖prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
              prawitzPVIntegrand T (MeasureTheory.charFun nu) x t‖ := by
          simpa only [d] using
            norm_symmetricPrawitzPVIntegrand_charFun_sub_le mu nu ht0 x
        _ = 2 * q t := by
          rw [norm_prawitzPVIntegrand_charFun_sub_eq mu nu ht0.ne' x]
  have hIntegral :
      (∫ t in Icc 0 T, ‖d t‖) ≤ 2 * ∫ t in Icc 0 T, q t := by
    calc
      (∫ t in Icc 0 T, ‖d t‖) ≤
          ∫ t in Icc 0 T, 2 * q t := by
        apply integral_mono_ae hd.norm (hq.const_mul 2)
        filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
        exact hpoint t ht
      _ = 2 * ∫ t in Icc 0 T, q t := integral_const_mul 2 q
  calc
    ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ ≤
        ‖Complex.I / (2 * Real.pi : ℝ)‖ * ∫ t in Icc 0 T, ‖d t‖ := by
      simpa only [d] using
        norm_prawitzInversion_charFun_sub_le_symmetricIntegral
          mu nu hmu1 hnu1 hT x
    _ ≤ ‖Complex.I / (2 * Real.pi : ℝ)‖ *
          (2 * ∫ t in Icc 0 T, q t) :=
      mul_le_mul_of_nonneg_left hIntegral (norm_nonneg _)
    _ = (1 / Real.pi) * ∫ t in Icc 0 T, q t := by
      rw [norm_div, Complex.norm_I, Complex.norm_real, Real.norm_eq_abs,
        abs_of_pos (mul_pos (by norm_num) Real.pi_pos)]
      field_simp [Real.pi_ne_zero]
    _ = (1 / Real.pi) * ∫ t in Icc 0 T,
          ‖scaledPrawitzSmoothingFilter T t‖ *
            charFunDiscrepancyQuotient mu nu t := rfl

/-- The unnormalized spatial density of the concrete Esseen smoothing
kernel.  The fourth power gives a finite first moment while retaining compact
Fourier support. -/
noncomputable def sincFourthWeight (x : ℝ) : ℝ := Real.sinc x ^ 4

theorem sincFourthWeight_nonneg (x : ℝ) : 0 ≤ sincFourthWeight x := by
  unfold sincFourthWeight
  positivity

theorem continuous_sincFourthWeight : Continuous sincFourthWeight := by
  exact Real.continuous_sinc.pow 4

/-- The sinc-fourth kernel density is integrable. -/
theorem integrable_sincFourthWeight : Integrable sincFourthWeight := by
  have hmajor : Integrable (fun x : ℝ => 2 * (1 + x ^ 2)⁻¹) :=
    integrable_inv_one_add_sq.const_mul 2
  apply hmajor.mono' continuous_sincFourthWeight.aestronglyMeasurable
  exact ae_of_all volume fun x => by
    rw [Real.norm_eq_abs, abs_of_nonneg (sincFourthWeight_nonneg x)]
    by_cases hx : |x| ≤ 1
    · have hs : |Real.sinc x| ^ 4 ≤ 1 := by
        exact pow_le_one₀ (abs_nonneg _) (Real.abs_sinc_le_one x)
      have hxSq : x ^ 2 ≤ 1 := by
        rw [← sq_abs]
        nlinarith [abs_nonneg x]
      have hden : 0 < 1 + x ^ 2 := by positivity
      unfold sincFourthWeight
      rw [show Real.sinc x ^ 4 = |Real.sinc x| ^ 4 by
        rw [← abs_pow, abs_of_nonneg (by positivity)]]
      calc
        |Real.sinc x| ^ 4 ≤ 1 := hs
        _ ≤ 2 * (1 + x ^ 2)⁻¹ := by
          rw [← div_eq_mul_inv, le_div_iff₀ hden]
          linarith
    · have hxLarge : 1 < |x| := lt_of_not_ge hx
      have hx0 : x ≠ 0 := by
        intro h
        subst x
        norm_num at hxLarge
      have hsinc := abs_sinc_le_inv_abs hx0
      have hsPow : |Real.sinc x| ^ 4 ≤ |x|⁻¹ ^ 4 :=
        pow_le_pow_left₀ (abs_nonneg _) hsinc 4
      have hxPow : 0 < |x| ^ 4 := pow_pos (abs_pos.mpr hx0) 4
      have hden : 0 < 1 + x ^ 2 := by positivity
      have hfrac : (|x| ^ 4)⁻¹ ≤ 2 * (1 + x ^ 2)⁻¹ := by
        have hxSq : 1 ≤ x ^ 2 := by
          rw [← sq_abs]
          nlinarith
        have hmul : 0 ≤ x ^ 2 * (x ^ 2 - 1) :=
          mul_nonneg (sq_nonneg x) (sub_nonneg.mpr hxSq)
        calc
          (|x| ^ 4)⁻¹ = 1 / (|x| ^ 4) := by rw [one_div]
          _ ≤ 2 / (1 + x ^ 2) := by
            rw [div_le_div_iff₀ hxPow hden]
            rw [show |x| ^ 4 = (x ^ 2) ^ 2 by
              calc
                |x| ^ 4 = (|x| ^ 2) ^ 2 := by ring
                _ = (x ^ 2) ^ 2 := by rw [sq_abs]]
            nlinarith
          _ = 2 * (1 + x ^ 2)⁻¹ := by rw [div_eq_mul_inv]
      unfold sincFourthWeight
      rw [show Real.sinc x ^ 4 = |Real.sinc x| ^ 4 by
        rw [← abs_pow, abs_of_nonneg (by positivity)]]
      calc
        |Real.sinc x| ^ 4 ≤ |x|⁻¹ ^ 4 := hsPow
        _ = (|x| ^ 4)⁻¹ := by rw [inv_pow]
        _ ≤ 2 * (1 + x ^ 2)⁻¹ := hfrac

/-- The sinc-fourth kernel has a finite first absolute moment. -/
theorem integrable_abs_mul_sincFourthWeight :
    Integrable (fun x : ℝ => |x| * sincFourthWeight x) := by
  have hmajor : Integrable (fun x : ℝ => 2 * (1 + x ^ 2)⁻¹) :=
    integrable_inv_one_add_sq.const_mul 2
  apply hmajor.mono' (continuous_abs.mul continuous_sincFourthWeight).aestronglyMeasurable
  exact ae_of_all volume fun x => by
    change ‖|x| * sincFourthWeight x‖ ≤ 2 * (1 + x ^ 2)⁻¹
    rw [Real.norm_eq_abs, abs_of_nonneg
      (mul_nonneg (abs_nonneg x) (sincFourthWeight_nonneg x))]
    by_cases hx : |x| ≤ 1
    · have hs : sincFourthWeight x ≤ 1 := by
        unfold sincFourthWeight
        rw [show Real.sinc x ^ 4 = |Real.sinc x| ^ 4 by
          rw [← abs_pow, abs_of_nonneg (by positivity)]]
        exact pow_le_one₀ (abs_nonneg _) (Real.abs_sinc_le_one x)
      have hprod : |x| * sincFourthWeight x ≤ 1 := by
        nlinarith [sincFourthWeight_nonneg x, abs_nonneg x]
      have hxSq : x ^ 2 ≤ 1 := by
        rw [← sq_abs]
        nlinarith [abs_nonneg x]
      have hden : 0 < 1 + x ^ 2 := by positivity
      calc
        |x| * sincFourthWeight x ≤ 1 := hprod
        _ ≤ 2 * (1 + x ^ 2)⁻¹ := by
          rw [← div_eq_mul_inv, le_div_iff₀ hden]
          linarith
    · have hxLarge : 1 < |x| := lt_of_not_ge hx
      have hx0 : x ≠ 0 := by
        intro h
        subst x
        norm_num at hxLarge
      have hsinc := abs_sinc_le_inv_abs hx0
      have hsPow : |Real.sinc x| ^ 4 ≤ |x|⁻¹ ^ 4 :=
        pow_le_pow_left₀ (abs_nonneg _) hsinc 4
      have hxPow : 0 < |x| ^ 3 := pow_pos (abs_pos.mpr hx0) 3
      have hden : 0 < 1 + x ^ 2 := by positivity
      have hfrac : (|x| ^ 3)⁻¹ ≤ 2 * (1 + x ^ 2)⁻¹ := by
        have hxSq : 1 ≤ |x| ^ 2 := by nlinarith [abs_nonneg x]
        have hmul : 0 ≤ |x| ^ 2 * (|x| - 1) :=
          mul_nonneg (sq_nonneg |x|) (sub_nonneg.mpr hxLarge.le)
        have habsSq : |x| ^ 2 = x ^ 2 := sq_abs x
        calc
          (|x| ^ 3)⁻¹ = 1 / (|x| ^ 3) := by rw [one_div]
          _ ≤ 2 / (1 + x ^ 2) := by
            rw [div_le_div_iff₀ hxPow hden]
            nlinarith [hmul]
          _ = 2 * (1 + x ^ 2)⁻¹ := by rw [div_eq_mul_inv]
      unfold sincFourthWeight
      rw [show Real.sinc x ^ 4 = |Real.sinc x| ^ 4 by
        rw [← abs_pow, abs_of_nonneg (by positivity)]]
      calc
        |x| * |Real.sinc x| ^ 4 ≤ |x| * |x|⁻¹ ^ 4 :=
          mul_le_mul_of_nonneg_left hsPow (abs_nonneg x)
        _ = (|x| ^ 3)⁻¹ := by field_simp [abs_ne_zero.mpr hx0]
        _ ≤ 2 * (1 + x ^ 2)⁻¹ := hfrac

/-- Total mass of the unnormalized sinc-fourth density. -/
noncomputable def sincFourthMass : ℝ := ∫ x : ℝ, sincFourthWeight x

theorem sincFourthMass_pos : 0 < sincFourthMass := by
  unfold sincFourthMass
  exact integral_pos_of_integrable_nonneg_nonzero
    continuous_sincFourthWeight integrable_sincFourthWeight
    sincFourthWeight_nonneg (x := 0) (by norm_num [sincFourthWeight])

/-- The finite measure with density `sincFourthWeight` before normalization. -/
noncomputable def sincFourthBaseMeasure : Measure ℝ :=
  volume.withDensity (fun x => ENNReal.ofReal (sincFourthWeight x))

theorem sincFourthBaseMeasure_univ :
    sincFourthBaseMeasure Set.univ = ENNReal.ofReal sincFourthMass := by
  rw [sincFourthBaseMeasure, withDensity_apply _ MeasurableSet.univ,
    Measure.restrict_univ, ← ofReal_integral_eq_lintegral_ofReal
      integrable_sincFourthWeight
      (ae_of_all volume sincFourthWeight_nonneg)]
  rfl

/-- The normalized sinc-fourth probability kernel at its base spatial scale. -/
noncomputable def sincFourthKernel : Measure ℝ :=
  (sincFourthBaseMeasure Set.univ)⁻¹ • sincFourthBaseMeasure

theorem sincFourthKernel_isProbabilityMeasure :
    IsProbabilityMeasure sincFourthKernel := by
  constructor
  rw [sincFourthKernel, Measure.smul_apply, sincFourthBaseMeasure_univ,
    smul_eq_mul, ENNReal.inv_mul_cancel]
  · exact (ENNReal.ofReal_pos.mpr sincFourthMass_pos).ne'
  · exact ENNReal.ofReal_ne_top

theorem sincFourthBaseMeasure_absolutelyContinuous :
    sincFourthBaseMeasure ≪ volume := by
  exact withDensity_absolutelyContinuous volume _

theorem sincFourthKernel_absolutelyContinuous :
    sincFourthKernel ≪ volume := by
  unfold sincFourthKernel
  exact Measure.smul_absolutelyContinuous.trans
    sincFourthBaseMeasure_absolutelyContinuous

theorem integrable_abs_sincFourthBaseMeasure :
    Integrable (fun x : ℝ => |x|) sincFourthBaseMeasure := by
  rw [sincFourthBaseMeasure,
    integrable_withDensity_iff
      (continuous_sincFourthWeight.measurable.ennreal_ofReal)
      (ae_of_all volume fun _ => ENNReal.ofReal_lt_top)]
  simpa only [ENNReal.toReal_ofReal (sincFourthWeight_nonneg _),
    smul_eq_mul] using integrable_abs_mul_sincFourthWeight

theorem integrable_abs_sincFourthKernel :
    Integrable (fun x : ℝ => |x|) sincFourthKernel := by
  unfold sincFourthKernel
  apply integrable_abs_sincFourthBaseMeasure.smul_measure
  rw [ENNReal.inv_ne_top]
  simpa only [sincFourthBaseMeasure_univ] using
    (ENNReal.ofReal_pos.mpr sincFourthMass_pos).ne'

/-- First absolute moment of the normalized sinc-fourth kernel. -/
noncomputable def sincFourthFirstMoment : ℝ :=
  ∫ x : ℝ, |x| ∂sincFourthKernel

theorem sincFourthFirstMoment_nonneg : 0 ≤ sincFourthFirstMoment := by
  unfold sincFourthFirstMoment
  exact integral_nonneg fun _ => abs_nonneg _

/-- A fixed spatial radius for which each sinc-fourth tail has mass strictly
less than one quarter. -/
noncomputable def sincFourthTailRadius : ℝ :=
  4 * sincFourthFirstMoment + 1

theorem sincFourthTailRadius_pos : 0 < sincFourthTailRadius := by
  unfold sincFourthTailRadius
  exact add_pos_of_nonneg_of_pos
    (mul_nonneg (by norm_num) sincFourthFirstMoment_nonneg) zero_lt_one

theorem sincFourthKernel_rightTail_lt_quarter :
    sincFourthKernel.real (Ioi sincFourthTailRadius) < 1 / 4 := by
  letI : IsProbabilityMeasure sincFourthKernel :=
    sincFourthKernel_isProbabilityMeasure
  let M : ℝ := sincFourthFirstMoment
  let a : ℝ := sincFourthTailRadius
  have hM : 0 ≤ M := sincFourthFirstMoment_nonneg
  have ha : 0 < a := sincFourthTailRadius_pos
  have hMarkov :
      a * sincFourthKernel.real {x : ℝ | a ≤ |x|} ≤ M := by
    simpa only [M, a, sincFourthFirstMoment] using
      mul_meas_ge_le_integral_of_nonneg
        (ae_of_all sincFourthKernel fun x => abs_nonneg x)
        integrable_abs_sincFourthKernel a
  have hratio : M / a < 1 / 4 := by
    rw [div_lt_iff₀ ha]
    dsimp only [a, sincFourthTailRadius, M]
    nlinarith
  have hradial : sincFourthKernel.real {x : ℝ | a ≤ |x|} ≤ M / a := by
    rw [le_div_iff₀ ha]
    simpa only [mul_comm] using hMarkov
  have hsubset : Ioi a ⊆ {x : ℝ | a ≤ |x|} := by
    intro x hx
    exact hx.le.trans (le_abs_self x)
  exact (measureReal_mono hsubset).trans_lt (hradial.trans_lt hratio)

theorem sincFourthKernel_leftTail_lt_quarter :
    sincFourthKernel.real (Iio (-sincFourthTailRadius)) < 1 / 4 := by
  letI : IsProbabilityMeasure sincFourthKernel :=
    sincFourthKernel_isProbabilityMeasure
  let M : ℝ := sincFourthFirstMoment
  let a : ℝ := sincFourthTailRadius
  have hM : 0 ≤ M := sincFourthFirstMoment_nonneg
  have ha : 0 < a := sincFourthTailRadius_pos
  have hMarkov :
      a * sincFourthKernel.real {x : ℝ | a ≤ |x|} ≤ M := by
    simpa only [M, a, sincFourthFirstMoment] using
      mul_meas_ge_le_integral_of_nonneg
        (ae_of_all sincFourthKernel fun x => abs_nonneg x)
        integrable_abs_sincFourthKernel a
  have hratio : M / a < 1 / 4 := by
    rw [div_lt_iff₀ ha]
    dsimp only [a, sincFourthTailRadius, M]
    nlinarith
  have hradial : sincFourthKernel.real {x : ℝ | a ≤ |x|} ≤ M / a := by
    rw [le_div_iff₀ ha]
    simpa only [mul_comm] using hMarkov
  have hsubset : Iio (-a) ⊆ {x : ℝ | a ≤ |x|} := by
    intro x hx
    have hx' : x < -a := hx
    have hxneg : x < 0 := hx.trans_le (neg_nonpos.mpr ha.le)
    change a ≤ |x|
    rw [abs_of_neg hxneg]
    linarith [hx']
  exact (measureReal_mono hsubset).trans_lt (hradial.trans_lt hratio)

/-- Common upper bound for the two shifted first moments used in tail
absorption. -/
noncomputable def sincFourthShiftedMomentBound : ℝ :=
  sincFourthTailRadius + sincFourthFirstMoment

theorem sincFourthKernel_rightShiftedMoment_le :
    (∫ y, |sincFourthTailRadius - y| ∂sincFourthKernel) ≤
      sincFourthShiftedMomentBound := by
  letI : IsProbabilityMeasure sincFourthKernel :=
    sincFourthKernel_isProbabilityMeasure
  have hInt : Integrable (fun y : ℝ =>
      sincFourthTailRadius + |y|) sincFourthKernel :=
    (integrable_const sincFourthTailRadius).add integrable_abs_sincFourthKernel
  calc
    (∫ y, |sincFourthTailRadius - y| ∂sincFourthKernel) ≤
        ∫ y, (sincFourthTailRadius + |y|) ∂sincFourthKernel := by
      apply integral_mono
        ((integrable_const sincFourthTailRadius).sub
          (integrable_abs_sincFourthKernel.mono' (by fun_prop)
            (ae_of_all sincFourthKernel fun y => by
              rw [Real.norm_eq_abs]))).norm hInt
      intro y
      exact (abs_sub _ _).trans_eq <| by
        rw [abs_of_pos sincFourthTailRadius_pos]
    _ = sincFourthShiftedMomentBound := by
      simp [integral_add (integrable_const _) integrable_abs_sincFourthKernel,
        sincFourthShiftedMomentBound, sincFourthFirstMoment]

theorem sincFourthKernel_leftShiftedMoment_le :
    (∫ y, |-sincFourthTailRadius - y| ∂sincFourthKernel) ≤
      sincFourthShiftedMomentBound := by
  letI : IsProbabilityMeasure sincFourthKernel :=
    sincFourthKernel_isProbabilityMeasure
  have hLeftInt : Integrable (fun y : ℝ =>
      |-sincFourthTailRadius - y|) sincFourthKernel :=
    ((integrable_const (-sincFourthTailRadius)).sub
      (integrable_abs_sincFourthKernel.mono' (by fun_prop)
        (ae_of_all sincFourthKernel fun y => by
          rw [Real.norm_eq_abs]))).norm
  have hRightInt : Integrable (fun y : ℝ =>
      sincFourthTailRadius + |y|) sincFourthKernel :=
    (integrable_const sincFourthTailRadius).add integrable_abs_sincFourthKernel
  calc
    (∫ y, |-sincFourthTailRadius - y| ∂sincFourthKernel) ≤
        ∫ y, (sincFourthTailRadius + |y|) ∂sincFourthKernel := by
      apply integral_mono hLeftInt hRightInt
      intro y
      calc
        |-sincFourthTailRadius - y| ≤
            |-sincFourthTailRadius| + |y| := abs_sub _ _
        _ = sincFourthTailRadius + |y| := by
          rw [abs_neg, abs_of_pos sincFourthTailRadius_pos]
    _ = sincFourthShiftedMomentBound := by
      simp [integral_add (integrable_const _) integrable_abs_sincFourthKernel,
        sincFourthShiftedMomentBound, sincFourthFirstMoment]

/-- Convolution adds deterministic spatial support radii. -/
theorem isSupportedInSmoothingWindow_conv
    {κ η : Measure ℝ} {δ ε : ℝ} [SFinite η]
    (hκ : IsSupportedInSmoothingWindow κ δ)
    (hη : IsSupportedInSmoothingWindow η ε) :
    IsSupportedInSmoothingWindow (κ ∗ η) (δ + ε) := by
  unfold IsSupportedInSmoothingWindow at hκ hη ⊢
  unfold Measure.conv
  apply (ae_map_iff (by fun_prop) measurableSet_Icc).2
  refine (Measure.ae_prod_iff_ae_ae
    (measurableSet_Icc.preimage (by fun_prop :
      Measurable fun z : ℝ × ℝ => z.1 + z.2))).2 ?_
  filter_upwards [hκ] with x hx
  filter_upwards [hη] with y hy
  constructor <;> linarith [hx.1, hx.2, hy.1, hy.2]

/-- The sum of four independent uniform laws on `[-1,1]`. Its characteristic
function is the sinc-fourth spatial weight, while its spatial support supplies
the dual compact-frequency window for the Esseen kernel. -/
noncomputable def fourfoldUniformKernel : Measure ℝ :=
  let υ := uniformSmoothingKernel 1
  ((υ ∗ υ) ∗ υ) ∗ υ

theorem fourfoldUniformKernel_isProbabilityMeasure :
    IsProbabilityMeasure fourfoldUniformKernel := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  unfold fourfoldUniformKernel
  infer_instance

theorem fourfoldUniformKernel_supported :
    IsSupportedInSmoothingWindow fourfoldUniformKernel 4 := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  let hυ := uniformSmoothingKernel_supported 1
  unfold fourfoldUniformKernel
  convert isSupportedInSmoothingWindow_conv
    (isSupportedInSmoothingWindow_conv
      (isSupportedInSmoothingWindow_conv hυ hυ) hυ) hυ using 1 <;>
    norm_num

theorem charFun_fourfoldUniformKernel (t : ℝ) :
    MeasureTheory.charFun fourfoldUniformKernel t =
      (sincFourthWeight t : ℂ) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  have hchar :
      MeasureTheory.charFun (uniformSmoothingKernel 1) t =
        (Real.sinc t : ℂ) := by
    simpa using charFun_uniformSmoothingKernel (δ := 1) (by norm_num) t
  unfold fourfoldUniformKernel
  rw [MeasureTheory.charFun_conv, MeasureTheory.charFun_conv,
    MeasureTheory.charFun_conv, hchar]
  simp only [sincFourthWeight, Complex.ofReal_pow]
  ring

/-- Lebesgue density of the uniform probability law on `[-1,1]`. -/
noncomputable def uniformUnitDensity (x : ℝ) : ℝ≥0∞ :=
  (Icc (-1 : ℝ) 1).indicator (fun _ => (2 : ℝ≥0∞)⁻¹) x

/-- The triangular density of the sum of two independent uniform laws on
`[-1,1]`, kept in `ℝ≥0∞` form for measure convolution. -/
noncomputable def twofoldUniformDensity (x : ℝ) : ℝ≥0∞ :=
  (2 : ℝ≥0∞)⁻¹ * (2 : ℝ≥0∞)⁻¹ *
    ENNReal.ofReal (max (2 - |x|) 0)

theorem uniformUnitDensity_lconvolution_self (x : ℝ) :
    (uniformUnitDensity ⋆ₗ[volume] uniformUnitDensity) x =
      twofoldUniformDensity x := by
  have hmem (y : ℝ) :
      -y + x ∈ Icc (-1 : ℝ) 1 ↔
        y ∈ Icc (x - 1) (x + 1) := by
    constructor <;> intro h <;> constructor <;> linarith [h.1, h.2]
  have hfun :
      (fun y : ℝ => uniformUnitDensity y * uniformUnitDensity (-y + x)) =
        (Icc (-1 : ℝ) 1 ∩ Icc (x - 1) (x + 1)).indicator
          (fun _ => (2 : ℝ≥0∞)⁻¹ * (2 : ℝ≥0∞)⁻¹) := by
    funext y
    by_cases hy : y ∈ Icc (-1 : ℝ) 1 <;>
      by_cases hxy : y ∈ Icc (x - 1) (x + 1) <;>
      simp [uniformUnitDensity, hy, hxy, hmem y]
  rw [MeasureTheory.lconvolution_def, hfun,
    lintegral_indicator (measurableSet_Icc.inter measurableSet_Icc)]
  simp only [setLIntegral_const, Set.Icc_inter_Icc, Real.volume_Icc]
  unfold twofoldUniformDensity
  congr 1
  by_cases hx0 : x ≤ 0
  · by_cases hxm : x < -2
    · have : x + 1 < -1 := by linarith
      simp [max_eq_left (by linarith : (-1 : ℝ) ≥ x - 1),
        min_eq_right this.le, abs_of_nonpos hx0]
      rw [min_eq_right (by linarith : x + 1 ≤ (1 : ℝ))]
      simp [ENNReal.ofReal_eq_zero.mpr (by linarith : x + 1 + 1 ≤ 0),
        ENNReal.ofReal_eq_zero.mpr (by linarith : 2 + x ≤ 0)]
    · have hx2 : -2 ≤ x := le_of_not_gt hxm
      have hmax : max (-1 : ℝ) (x - 1) = -1 := max_eq_left (by linarith)
      have hmin : min (1 : ℝ) (x + 1) = x + 1 := min_eq_right (by linarith)
      rw [hmax, hmin, abs_of_nonpos hx0]
      congr 1
      rw [max_eq_left (by linarith)]
      ring
  · have hx0' : 0 ≤ x := le_of_not_ge hx0
    by_cases hx2 : 2 < x
    · have : 1 < x - 1 := by linarith
      simp [max_eq_right this.le,
        min_eq_left (by linarith : (1 : ℝ) ≤ x + 1), abs_of_nonneg hx0']
      rw [max_eq_right (by linarith : (-1 : ℝ) ≤ x - 1)]
      simp [ENNReal.ofReal_eq_zero.mpr (by linarith : 1 - (x - 1) ≤ 0),
        ENNReal.ofReal_eq_zero.mpr (by linarith : 2 - x ≤ 0)]
    · have hx2' : x ≤ 2 := le_of_not_gt hx2
      have hmax : max (-1 : ℝ) (x - 1) = x - 1 := max_eq_right (by linarith)
      have hmin : min (1 : ℝ) (x + 1) = 1 := min_eq_left (by linarith)
      rw [hmax, hmin, abs_of_nonneg hx0']
      congr 1
      rw [max_eq_left (by linarith)]
      ring

theorem measurable_uniformUnitDensity : Measurable uniformUnitDensity := by
  exact measurable_const.indicator measurableSet_Icc

theorem uniformSmoothingKernel_one_eq_withDensity :
    uniformSmoothingKernel 1 = volume.withDensity uniformUnitDensity := by
  unfold uniformSmoothingKernel
  calc
    (ENNReal.ofReal (2 * (1 : ℝ)))⁻¹ • volume.restrict (Icc (-1 : ℝ) 1) =
        (ENNReal.ofReal 2)⁻¹ •
          volume.withDensity ((Icc (-1 : ℝ) 1).indicator 1) := by
      rw [withDensity_indicator_one measurableSet_Icc]
      norm_num
    _ = volume.withDensity
        ((ENNReal.ofReal 2)⁻¹ • (Icc (-1 : ℝ) 1).indicator 1) := by
      rw [withDensity_smul]
      exact measurable_const.indicator measurableSet_Icc
    _ = volume.withDensity uniformUnitDensity := by
      congr 1
      funext x
      by_cases hx : x ∈ Icc (-1 : ℝ) 1 <;>
        simp [uniformUnitDensity, Pi.smul_apply, smul_eq_mul, hx]

noncomputable def twofoldUniformDensityReal (x : ℝ) : ℝ :=
  max (2 - |x|) 0 / 4

theorem twofoldUniformDensity_toReal (x : ℝ) :
    (twofoldUniformDensity x).toReal = twofoldUniformDensityReal x := by
  simp only [twofoldUniformDensity, ENNReal.toReal_mul, ENNReal.toReal_inv,
    ENNReal.toReal_ofNat,
    ENNReal.toReal_ofReal (le_max_right (2 - |x|) 0)]
  unfold twofoldUniformDensityReal
  norm_num
  ring

theorem measurable_twofoldUniformDensity :
    Measurable twofoldUniformDensity := by
  unfold twofoldUniformDensity
  fun_prop

theorem continuous_twofoldUniformDensityReal :
    Continuous twofoldUniformDensityReal := by
  unfold twofoldUniformDensityReal
  fun_prop

theorem twofoldUniformDensityReal_nonneg (x : ℝ) :
    0 ≤ twofoldUniformDensityReal x := by
  unfold twofoldUniformDensityReal
  positivity

theorem support_twofoldUniformDensityReal_subset :
    Function.support twofoldUniformDensityReal ⊆ Icc (-2 : ℝ) 2 := by
  intro x hx
  change twofoldUniformDensityReal x ≠ 0 at hx
  constructor
  · by_contra h
    have hlt : x < -2 := lt_of_not_ge h
    have hxneg : x < 0 := by linarith
    have hinner : 2 - |x| ≤ 0 := by rw [abs_of_neg hxneg]; linarith
    simp [twofoldUniformDensityReal, max_eq_right hinner] at hx
  · by_contra h
    have hlt : 2 < x := lt_of_not_ge h
    have hxpos : 0 ≤ x := by linarith
    have hinner : 2 - |x| ≤ 0 := by rw [abs_of_nonneg hxpos]; linarith
    simp [twofoldUniformDensityReal, max_eq_right hinner] at hx

theorem hasCompactSupport_twofoldUniformDensityReal :
    HasCompactSupport twofoldUniformDensityReal :=
  HasCompactSupport.of_support_subset_isCompact isCompact_Icc
    support_twofoldUniformDensityReal_subset

theorem integrable_twofoldUniformDensityReal :
    Integrable twofoldUniformDensityReal :=
  continuous_twofoldUniformDensityReal.integrable_of_hasCompactSupport
    hasCompactSupport_twofoldUniformDensityReal

/-- Continuous compactly supported density of the fourfold uniform sum. -/
noncomputable def fourfoldUniformDensity : ℝ → ℝ :=
  twofoldUniformDensityReal ⋆[ContinuousLinearMap.mul ℝ ℝ, volume] twofoldUniformDensityReal

theorem continuous_fourfoldUniformDensity :
    Continuous fourfoldUniformDensity := by
  unfold fourfoldUniformDensity
  exact hasCompactSupport_twofoldUniformDensityReal.continuous_convolution_right
    (ContinuousLinearMap.mul ℝ ℝ)
    continuous_twofoldUniformDensityReal.locallyIntegrable
    continuous_twofoldUniformDensityReal

theorem integrable_fourfoldUniformDensity :
    Integrable fourfoldUniformDensity := by
  unfold fourfoldUniformDensity
  exact integrable_twofoldUniformDensityReal.integrable_convolution
    (ContinuousLinearMap.mul ℝ ℝ) integrable_twofoldUniformDensityReal

theorem fourfoldUniformDensity_nonneg (x : ℝ) :
    0 ≤ fourfoldUniformDensity x := by
  rw [fourfoldUniformDensity, convolution_mul]
  exact integral_nonneg fun y => mul_nonneg
    (twofoldUniformDensityReal_nonneg y)
    (twofoldUniformDensityReal_nonneg (x - y))

theorem support_fourfoldUniformDensity_subset :
    Function.support fourfoldUniformDensity ⊆ Icc (-4 : ℝ) 4 := by
  refine (support_convolution_subset
    (ContinuousLinearMap.mul ℝ ℝ)).trans ?_
  rintro x ⟨a, ha, b, hb, rfl⟩
  have ha' := support_twofoldUniformDensityReal_subset ha
  have hb' := support_twofoldUniformDensityReal_subset hb
  constructor <;> linarith [ha'.1, ha'.2, hb'.1, hb'.2]

theorem hasCompactSupport_fourfoldUniformDensity :
    HasCompactSupport fourfoldUniformDensity :=
  HasCompactSupport.of_support_subset_isCompact isCompact_Icc
    support_fourfoldUniformDensity_subset

theorem twofoldUniformDensity_lconvolution_self (x : ℝ) :
    (twofoldUniformDensity ⋆ₗ[volume] twofoldUniformDensity) x =
      ENNReal.ofReal (fourfoldUniformDensity x) := by
  have hconv : Integrable (fun y : ℝ =>
      twofoldUniformDensityReal y * twofoldUniformDensityReal (x - y)) :=
    (hasCompactSupport_twofoldUniformDensityReal.convolutionExists_right
      (ContinuousLinearMap.mul ℝ ℝ)
      continuous_twofoldUniformDensityReal.locallyIntegrable
      continuous_twofoldUniformDensityReal x)
  rw [MeasureTheory.lconvolution_def, fourfoldUniformDensity,
    convolution_mul,
    ofReal_integral_eq_lintegral_ofReal hconv
      (ae_of_all volume fun y => mul_nonneg
        (twofoldUniformDensityReal_nonneg y)
        (twofoldUniformDensityReal_nonneg (x - y)))]
  apply lintegral_congr
  intro y
  have harg : -y + x = x - y := by ring
  rw [harg, ← twofoldUniformDensity_toReal,
    ← twofoldUniformDensity_toReal]
  rw [ENNReal.ofReal_mul ENNReal.toReal_nonneg]
  rw [ENNReal.ofReal_toReal (by unfold twofoldUniformDensity; finiteness)]
  rw [ENNReal.ofReal_toReal (by unfold twofoldUniformDensity; finiteness)]

theorem uniformSmoothingKernel_conv_self_eq_withDensity :
    uniformSmoothingKernel 1 ∗ uniformSmoothingKernel 1 =
      volume.withDensity twofoldUniformDensity := by
  rw [uniformSmoothingKernel_one_eq_withDensity,
    conv_withDensity_eq_lconvolution measurable_uniformUnitDensity
      measurable_uniformUnitDensity]
  congr 1
  funext x
  exact uniformUnitDensity_lconvolution_self x

theorem fourfoldUniformKernel_eq_withDensity :
    fourfoldUniformKernel =
      volume.withDensity (fun x => ENNReal.ofReal (fourfoldUniformDensity x)) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  unfold fourfoldUniformKernel
  rw [Measure.conv_assoc, uniformSmoothingKernel_conv_self_eq_withDensity,
    conv_withDensity_eq_lconvolution measurable_twofoldUniformDensity
      measurable_twofoldUniformDensity]
  congr 1
  funext x
  exact twofoldUniformDensity_lconvolution_self x

noncomputable def fourfoldUniformDensityComplex (x : ℝ) : ℂ :=
  fourfoldUniformDensity x

theorem continuous_fourfoldUniformDensityComplex :
    Continuous fourfoldUniformDensityComplex := by
  exact Complex.continuous_ofReal.comp continuous_fourfoldUniformDensity

theorem integrable_fourfoldUniformDensityComplex :
    Integrable fourfoldUniformDensityComplex :=
  integrable_fourfoldUniformDensity.ofReal

theorem sincFourthWeight_eq_fourierInv_fourfoldUniformDensityComplex (t : ℝ) :
    (sincFourthWeight t : ℂ) =
      𝓕⁻ fourfoldUniformDensityComplex (t / (2 * Real.pi)) := by
  letI : IsProbabilityMeasure fourfoldUniformKernel :=
    fourfoldUniformKernel_isProbabilityMeasure
  rw [← charFun_fourfoldUniformKernel t,
    fourfoldUniformKernel_eq_withDensity,
    MeasureTheory.charFun_apply_real,
    integral_withDensity_eq_integral_toReal_smul
      (continuous_fourfoldUniformDensity.measurable.ennreal_ofReal)
      (ae_of_all volume fun _ => ENNReal.ofReal_lt_top)
      (fun x : ℝ => Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I)),
    Real.fourierInv_eq']
  apply integral_congr_ae
  exact ae_of_all volume fun x => by
    dsimp only
    rw [ENNReal.toReal_ofReal (fourfoldUniformDensity_nonneg x)]
    change (fourfoldUniformDensity x : ℂ) *
        Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I) =
      Complex.exp (((2 * Real.pi * inner ℝ x (t / (2 * Real.pi)) : ℝ) : ℂ) *
        Complex.I) * (fourfoldUniformDensity x : ℂ)
    rw [mul_comm]
    congr 2
    congr 1
    norm_cast
    rw [real_inner_eq_re_inner (𝕜 := ℝ)]
    simp only [RCLike.inner_apply, conj_trivial]
    have hre (z : ℝ) : RCLike.re z = z := rfl
    rw [hre]
    field_simp [Real.pi_ne_zero]

theorem fourierInv_fourfoldUniformDensityComplex_eq_sincFourthWeight (w : ℝ) :
    𝓕⁻ fourfoldUniformDensityComplex w =
      (sincFourthWeight ((2 * Real.pi) * w) : ℂ) := by
  rw [sincFourthWeight_eq_fourierInv_fourfoldUniformDensityComplex]
  congr 1
  field_simp [Real.pi_ne_zero]

theorem integrable_fourierInv_fourfoldUniformDensityComplex :
    Integrable (𝓕⁻ fourfoldUniformDensityComplex) := by
  have htwoPi : (2 * Real.pi : ℝ) ≠ 0 :=
    mul_ne_zero (by norm_num) Real.pi_ne_zero
  have hbase : Integrable (fun w : ℝ => (sincFourthWeight w : ℂ)) :=
    integrable_sincFourthWeight.ofReal
  have hscaled : Integrable (fun w : ℝ =>
      (sincFourthWeight ((2 * Real.pi) * w) : ℂ)) :=
    hbase.comp_mul_left' htwoPi
  exact hscaled.congr (ae_of_all volume fun w =>
    (fourierInv_fourfoldUniformDensityComplex_eq_sincFourthWeight w).symm)

theorem fourier_fourfoldUniformDensityComplex_eq_sincFourthWeight (w : ℝ) :
    𝓕 fourfoldUniformDensityComplex w =
      (sincFourthWeight ((-2 * Real.pi) * w) : ℂ) := by
  calc
    𝓕 fourfoldUniformDensityComplex w =
        𝓕⁻ fourfoldUniformDensityComplex (-w) := by
      symm
      simpa using Real.fourierInv_eq_fourier_neg
        fourfoldUniformDensityComplex (-w)
    _ = (sincFourthWeight ((2 * Real.pi) * (-w)) : ℂ) :=
      fourierInv_fourfoldUniformDensityComplex_eq_sincFourthWeight (-w)
    _ = (sincFourthWeight ((-2 * Real.pi) * w) : ℂ) := by
      congr 2
      ring

theorem integrable_fourier_fourfoldUniformDensityComplex :
    Integrable (𝓕 fourfoldUniformDensityComplex) := by
  have hnegTwoPi : (-2 * Real.pi : ℝ) ≠ 0 :=
    mul_ne_zero (by norm_num : (-2 : ℝ) ≠ 0) Real.pi_ne_zero
  have hbase : Integrable (fun w : ℝ => (sincFourthWeight w : ℂ)) :=
    integrable_sincFourthWeight.ofReal
  have hscaled : Integrable (fun w : ℝ =>
      (sincFourthWeight ((-2 * Real.pi) * w) : ℂ)) :=
    hbase.comp_mul_left' hnegTwoPi
  exact hscaled.congr (ae_of_all volume fun w =>
    (fourier_fourfoldUniformDensityComplex_eq_sincFourthWeight w).symm)

theorem fourier_fourierInv_fourfoldUniformDensityComplex :
    𝓕 (𝓕⁻ fourfoldUniformDensityComplex) = fourfoldUniformDensityComplex :=
  continuous_fourfoldUniformDensityComplex.fourier_fourierInv_eq
    integrable_fourfoldUniformDensityComplex
    integrable_fourier_fourfoldUniformDensityComplex

theorem integral_eq_mul_integral_comp_mul_left
    (g : ℝ → ℂ) {a : ℝ} (ha : 0 < a) :
    (∫ x : ℝ, g x) = a • ∫ x : ℝ, g (a * x) := by
  rw [Measure.integral_comp_mul_left g a,
    abs_of_pos (inv_pos.mpr ha)]
  rw [Complex.real_smul]
  have haC : (a : ℂ) ≠ 0 := by
    exact_mod_cast ha.ne'
  change (∫ x : ℝ, g x) =
    (a : ℂ) * ((a⁻¹ : ℝ) • ∫ x : ℝ, g x)
  rw [@Complex.real_smul (a⁻¹) (∫ x : ℝ, g x)]
  change (∫ x : ℝ, g x) =
    (a : ℂ) * ((a⁻¹ : ℝ) * ∫ x : ℝ, g x)
  rw [← mul_assoc]
  rw [← Complex.ofReal_mul, mul_inv_cancel₀ ha.ne']
  simp

theorem integral_cexp_mul_sincFourthWeight_eq_density (s : ℝ) :
    (∫ x : ℝ, Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
        (sincFourthWeight x : ℂ)) =
      (2 * Real.pi) • (fourfoldUniformDensityComplex (-s)) := by
  let g : ℝ → ℂ := fun x =>
    Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
      (sincFourthWeight x : ℂ)
  calc
    (∫ x : ℝ, Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
        (sincFourthWeight x : ℂ)) = ∫ x : ℝ, g x := rfl
    _ = (2 * Real.pi) • ∫ w : ℝ, g ((2 * Real.pi) * w) :=
      integral_eq_mul_integral_comp_mul_left g Real.two_pi_pos
    _ = (2 * Real.pi) • 𝓕 (𝓕⁻ fourfoldUniformDensityComplex) (-s) := by
      congr 1
      rw [Real.fourier_eq']
      apply integral_congr_ae
      exact ae_of_all volume fun w => by
        dsimp only [g]
        rw [fourierInv_fourfoldUniformDensityComplex_eq_sincFourthWeight]
        congr 1
        congr 1
        congr 1
        norm_cast
        rw [real_inner_eq_re_inner (𝕜 := ℝ)]
        simp only [RCLike.inner_apply, conj_trivial]
        have hre (z : ℝ) : RCLike.re z = z := rfl
        rw [hre]
        norm_num
        ring
    _ = (2 * Real.pi) • fourfoldUniformDensityComplex (-s) := by
      rw [fourier_fourierInv_fourfoldUniformDensityComplex]

theorem charFun_sincFourthKernel (s : ℝ) :
    MeasureTheory.charFun sincFourthKernel s =
      (sincFourthMass⁻¹ : ℝ) •
        ∫ x : ℝ, Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
          (sincFourthWeight x : ℂ) := by
  letI : IsProbabilityMeasure sincFourthKernel :=
    sincFourthKernel_isProbabilityMeasure
  rw [MeasureTheory.charFun_apply_real, sincFourthKernel,
    integral_smul_measure, ENNReal.toReal_inv,
    sincFourthBaseMeasure_univ, ENNReal.toReal_ofReal sincFourthMass_pos.le,
    sincFourthBaseMeasure,
    integral_withDensity_eq_integral_toReal_smul
      (continuous_sincFourthWeight.measurable.ennreal_ofReal)
      (ae_of_all volume fun _ => ENNReal.ofReal_lt_top)
      (fun x : ℝ => Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I))]
  congr 1
  apply integral_congr_ae
  exact ae_of_all volume fun x => by
    dsimp only
    rw [ENNReal.toReal_ofReal (sincFourthWeight_nonneg x)]
    change (sincFourthWeight x : ℂ) *
        Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) =
      Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
        (sincFourthWeight x : ℂ)
    rw [mul_comm]

theorem charFun_sincFourthKernel_eq_density (s : ℝ) :
    MeasureTheory.charFun sincFourthKernel s =
      (sincFourthMass⁻¹ * (2 * Real.pi)) •
        fourfoldUniformDensityComplex (-s) := by
  rw [charFun_sincFourthKernel,
    integral_cexp_mul_sincFourthWeight_eq_density]
  simp only [Complex.real_smul]
  push_cast
  ring

theorem sincFourthKernel_charFun_supported :
    IsCharFunSupportedInFrequencyWindow sincFourthKernel 4 := by
  intro s hs
  rw [charFun_sincFourthKernel_eq_density]
  have hnot : -s ∉ Icc (-4 : ℝ) 4 := by
    intro h
    have habs : |s| ≤ 4 := (abs_le).2 ⟨by linarith [h.2], by linarith [h.1]⟩
    linarith
  have hzero : fourfoldUniformDensityComplex (-s) = 0 := by
    unfold fourfoldUniformDensityComplex
    norm_cast
    by_contra hne
    exact hnot (support_fourfoldUniformDensity_subset hne)
  rw [hzero]
  simp

/-- The unit-scale reflected Prawitz point kernel is exactly the
sinc-squared Fejér profile. -/
theorem prawitzPointKernel_add_neg_eq_sinc_sq (z : ℝ) :
    prawitzPointKernel 1 z + prawitzPointKernel 1 (-z) =
      Real.sinc (z / 2) ^ 2 := by
  rw [prawitzPointKernel_add_neg]
  by_cases hz : z = 0
  · subst z
    simp [Real.sinc]
  · rw [if_neg hz, Real.sinc, if_neg (div_ne_zero hz (by norm_num))]
    have hrewrite : z = 2 * (z / 2) := by ring
    rw [hrewrite, Real.cos_two_mul]
    field_simp [hz]
    nlinarith [Real.sin_sq_add_cos_sq (z / 2)]

theorem prawitzPointKernel_add_neg_scale_eq_sinc_sq
    {T : ℝ} (hT : 0 < T) (z : ℝ) :
    prawitzPointKernel T z + prawitzPointKernel T (-z) =
      Real.sinc (T * z / 2) ^ 2 := by
  rw [prawitzPointKernel_scale hT, prawitzPointKernel_scale hT,
    show T * -z = -(T * z) by ring,
    prawitzPointKernel_add_neg_eq_sinc_sq]

theorem prawitzPointKernel_reflection_eq_sinc_sq
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    prawitzPointKernel T (y - x) + prawitzPointKernel T (x - y) =
      Real.sinc (T * (y - x) / 2) ^ 2 := by
  rw [show x - y = -(y - x) by ring,
    prawitzPointKernel_add_neg_scale_eq_sinc_sq hT]

/-- The total bidirectional pointwise slack is exactly twice the translated
sinc-squared Fejér profile. -/
theorem prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    prawitzUpperSlack T x y + prawitzLowerSlack T x y =
      2 * Real.sinc (T * (y - x) / 2) ^ 2 := by
  rw [← prawitzPointKernel_reflection_eq_sinc_sq hT x y]
  unfold prawitzUpperSlack prawitzLowerSlack
  ring

/-- The total expected bidirectional slack is exactly twice the law's
translated sinc-squared mass. -/
theorem integral_prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (∫ y, prawitzUpperSlack T x y ∂mu) +
        ∫ y, prawitzLowerSlack T x y ∂mu =
      2 * ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu := by
  have hUpper := integrable_prawitzUpperSlack mu x hForward
  have hLower := integrable_prawitzLowerSlack mu x hReflected
  calc
    _ = ∫ y, prawitzUpperSlack T x y + prawitzLowerSlack T x y ∂mu :=
      (integral_add hUpper hLower).symm
    _ = ∫ y, 2 * Real.sinc (T * (y - x) / 2) ^ 2 ∂mu := by
      apply integral_congr_ae
      exact ae_of_all _
        (prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq hT x)
    _ = _ := by rw [integral_const_mul]

/-- For two laws, the sum of the two one-sided slack differences is exactly
twice their translated sinc-squared mass difference. -/
theorem prawitzSlackDifference_add_eq_two_mul_sincMassDifference
    (mu nu : Measure ℝ) [IsFiniteMeasure mu] [IsFiniteMeasure nu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hmuForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hmuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnuForward : Integrable (fun y => prawitzPointKernel T (y - x)) nu)
    (hnuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    ((∫ y, prawitzUpperSlack T x y ∂mu) -
        ∫ y, prawitzUpperSlack T x y ∂nu) +
      ((∫ y, prawitzLowerSlack T x y ∂mu) -
        ∫ y, prawitzLowerSlack T x y ∂nu) =
      2 * ((∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
        ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu) := by
  have hmu :=
    integral_prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq
      mu hT x hmuForward hmuReflected
  have hnu :=
    integral_prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq
      nu hT x hnuForward hnuReflected
  linarith

/-- Signed half-difference controlling how the fixed sinc-squared slack mass
is divided between the upper and lower Prawitz bounds. -/
noncomputable def prawitzSlackImbalance (T x y : ℝ) : ℝ :=
  (prawitzUpperSlack T x y - prawitzLowerSlack T x y) / 2

/-- The signed imbalance is exactly the antisymmetric point-kernel component
minus the centered lower-ray step. -/
theorem prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep
    (T x y : ℝ) :
    prawitzSlackImbalance T x y =
      prawitzPointKernel T (y - x) - prawitzPointKernel T (x - y) -
        (2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) := by
  unfold prawitzSlackImbalance prawitzUpperSlack prawitzLowerSlack
  ring

/-- The slack imbalance depends on the spatial variables only through their
displacement. -/
theorem prawitzSlackImbalance_translate (T x y : ℝ) :
    prawitzSlackImbalance T x y =
      prawitzSlackImbalance T 0 (y - x) := by
  rw [prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep,
    prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep]
  by_cases hy : y ≤ x
  · have hz : y - x ≤ 0 := sub_nonpos.mpr hy
    simp [hy, hz]
  · have hz : ¬y - x ≤ 0 := by linarith
    simp [hy, hz]

/-- Positive frequency scaling reduces the translated imbalance to its
universal unit-window profile. -/
theorem prawitzSlackImbalance_scale
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    prawitzSlackImbalance T x y =
      prawitzSlackImbalance 1 0 (T * (y - x)) := by
  rw [prawitzSlackImbalance_translate T x y,
    prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep,
    prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep,
    prawitzPointKernel_scale hT]
  rw [show 0 - (y - x) = -(y - x) by ring,
    prawitzPointKernel_scale hT]
  rw [show T * (-(y - x)) = -(T * (y - x)) by ring]
  by_cases hz : y - x ≤ 0
  · have hTz : T * (y - x) ≤ 0 := mul_nonpos_of_nonneg_of_nonpos hT.le hz
    simp [hz, hTz]
  · have hzpos : 0 < y - x := lt_of_not_ge hz
    have hTzpos : 0 < T * (y - x) := mul_pos hT hzpos
    simp [not_le.mpr hzpos, not_le.mpr hTzpos]

/-- At the CDF jump itself, the right-continuous convention assigns the full
unit signed imbalance to the lower Prawitz slack. -/
theorem prawitzSlackImbalance_diagonal (T x : ℝ) :
    prawitzSlackImbalance T x x = -1 := by
  rw [prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep]
  simp
  norm_num

/-- To the right of the CDF evaluation point, the imbalance is the
antisymmetric point-kernel component plus one. -/
theorem prawitzSlackImbalance_of_lt
    (T : ℝ) {x y : ℝ} (hxy : x < y) :
    prawitzSlackImbalance T x y =
      prawitzPointKernel T (y - x) - prawitzPointKernel T (x - y) + 1 := by
  rw [prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep]
  simp [not_le.mpr hxy]

/-- On and to the left of the CDF evaluation point, the imbalance is the
antisymmetric point-kernel component minus one. -/
theorem prawitzSlackImbalance_of_le
    (T : ℝ) {x y : ℝ} (hyx : y ≤ x) :
    prawitzSlackImbalance T x y =
      prawitzPointKernel T (y - x) - prawitzPointKernel T (x - y) - 1 := by
  rw [prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep]
  simp [hyx]
  norm_num

/-- The universal imbalance profile vanishes at the first negative Fourier
lattice point. -/
theorem prawitzSlackImbalance_neg_two_pi :
    prawitzSlackImbalance 1 0 (-2 * Real.pi) = 0 := by
  have hle : -2 * Real.pi ≤ 0 := by nlinarith [Real.pi_pos]
  rw [prawitzSlackImbalance_of_le 1 hle, sub_zero, zero_sub]
  rw [show -(-2 * Real.pi) = 2 * Real.pi by ring,
    prawitzPointKernel_neg_two_pi, prawitzPointKernel_two_pi]
  ring

/-- The universal imbalance profile also vanishes at the reflected first
Fourier lattice point. -/
theorem prawitzSlackImbalance_two_pi :
    prawitzSlackImbalance 1 0 (2 * Real.pi) = 0 := by
  have hlt : 0 < 2 * Real.pi := by positivity
  rw [prawitzSlackImbalance_of_lt 1 hlt, sub_zero, zero_sub]
  rw [show -(2 * Real.pi) = -2 * Real.pi by ring,
    prawitzPointKernel_two_pi, prawitzPointKernel_neg_two_pi]
  ring

/-- The three certified values around the CDF jump already contribute two to
the variation of the universal imbalance profile. -/
theorem prawitzSlackImbalance_threePointVariation :
    |prawitzSlackImbalance 1 0 (-2 * Real.pi) -
        prawitzSlackImbalance 1 0 0| +
      |prawitzSlackImbalance 1 0 0 -
        prawitzSlackImbalance 1 0 (2 * Real.pi)| = 2 := by
  rw [prawitzSlackImbalance_neg_two_pi,
    prawitzSlackImbalance_diagonal,
    prawitzSlackImbalance_two_pi]
  norm_num

/-- Consequently, every proposed variation majorant for these three
increments has coefficient at least two. -/
theorem two_le_of_prawitzSlackImbalance_threePointVariation_le
    {C : ℝ}
    (hC : |prawitzSlackImbalance 1 0 (-2 * Real.pi) -
          prawitzSlackImbalance 1 0 0| +
        |prawitzSlackImbalance 1 0 0 -
          prawitzSlackImbalance 1 0 (2 * Real.pi)| ≤ C) :
    2 ≤ C := by
  rw [prawitzSlackImbalance_threePointVariation] at hC
  exact hC

/-- At every positive bandwidth, the imbalance vanishes one first-lattice
spacing to the left of the spatial center. -/
theorem prawitzSlackImbalance_left_latticePoint
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzSlackImbalance T x (x - 2 * Real.pi / T) = 0 := by
  rw [prawitzSlackImbalance_scale hT]
  convert prawitzSlackImbalance_neg_two_pi using 1
  field_simp [hT.ne']
  ring

/-- At every positive bandwidth, the imbalance vanishes one first-lattice
spacing to the right of the spatial center. -/
theorem prawitzSlackImbalance_right_latticePoint
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzSlackImbalance T x (x + 2 * Real.pi / T) = 0 := by
  rw [prawitzSlackImbalance_scale hT]
  convert prawitzSlackImbalance_two_pi using 1
  field_simp [hT.ne']
  ring

/-- The coefficient-two variation obstruction is invariant under positive
bandwidth scaling and spatial translation. -/
theorem prawitzSlackImbalance_threePointVariation_scaled
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |prawitzSlackImbalance T x (x - 2 * Real.pi / T) -
        prawitzSlackImbalance T x x| +
      |prawitzSlackImbalance T x x -
        prawitzSlackImbalance T x (x + 2 * Real.pi / T)| = 2 := by
  rw [prawitzSlackImbalance_left_latticePoint hT,
    prawitzSlackImbalance_diagonal,
    prawitzSlackImbalance_right_latticePoint hT]
  norm_num

/-- No bandwidth-dependent variation majorant for these three increments can
have coefficient strictly below two. -/
theorem two_le_of_prawitzSlackImbalance_threePointVariation_scaled_le
    {T : ℝ} (hT : 0 < T) (x C : ℝ)
    (hC : |prawitzSlackImbalance T x (x - 2 * Real.pi / T) -
          prawitzSlackImbalance T x x| +
        |prawitzSlackImbalance T x x -
          prawitzSlackImbalance T x (x + 2 * Real.pi / T)| ≤ C) :
    2 ≤ C := by
  rw [prawitzSlackImbalance_threePointVariation_scaled hT x] at hC
  exact hC

/-- The off-diagonal imbalance removes only the value at the CDF evaluation
point, leaving the rest of the universal profile unchanged. -/
noncomputable def prawitzSlackImbalanceOffDiagonal (T x y : ℝ) : ℝ :=
  if y = x then 0 else prawitzSlackImbalance T x y

/-- The off-diagonal profile, like the raw imbalance, depends only on spatial
displacement. -/
theorem prawitzSlackImbalanceOffDiagonal_translate (T x y : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x y =
      prawitzSlackImbalanceOffDiagonal T 0 (y - x) := by
  by_cases hy : y = x
  · subst y
    simp [prawitzSlackImbalanceOffDiagonal]
  · have hsub : y - x ≠ 0 := sub_ne_zero.mpr hy
    simp [prawitzSlackImbalanceOffDiagonal, hy, hsub,
      prawitzSlackImbalance_translate]

/-- At positive bandwidth, dilation reduces the off-diagonal profile to the
universal unit-window profile without reintroducing the diagonal atom. -/
theorem prawitzSlackImbalanceOffDiagonal_scale
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x y =
      prawitzSlackImbalanceOffDiagonal 1 0 (T * (y - x)) := by
  by_cases hy : y = x
  · subst y
    simp [prawitzSlackImbalanceOffDiagonal]
  · have hsub : y - x ≠ 0 := sub_ne_zero.mpr hy
    have hscaled : T * (y - x) ≠ 0 := mul_ne_zero hT.ne' hsub
    simp [prawitzSlackImbalanceOffDiagonal, hy, hscaled,
      prawitzSlackImbalance_scale hT]

/-- Removing the exceptional diagonal value restores exact odd symmetry to
the centered imbalance profile. -/
theorem prawitzSlackImbalanceOffDiagonal_neg (T z : ℝ) :
    prawitzSlackImbalanceOffDiagonal T 0 (-z) =
      -prawitzSlackImbalanceOffDiagonal T 0 z := by
  by_cases hz : z = 0
  · subst z
    simp [prawitzSlackImbalanceOffDiagonal]
  · have hneg_ne : -z ≠ 0 := neg_ne_zero.mpr hz
    by_cases hzpos : 0 < z
    · rw [show prawitzSlackImbalanceOffDiagonal T 0 (-z) =
          prawitzSlackImbalance T 0 (-z) by
            simp [prawitzSlackImbalanceOffDiagonal, hneg_ne],
        show prawitzSlackImbalanceOffDiagonal T 0 z =
          prawitzSlackImbalance T 0 z by
            simp [prawitzSlackImbalanceOffDiagonal, hz],
        prawitzSlackImbalance_of_le T (by linarith : -z ≤ 0),
        prawitzSlackImbalance_of_lt T hzpos]
      simp only [sub_zero, zero_sub, neg_neg]
      ring
    · have hzneg : z < 0 := lt_of_le_of_ne (not_lt.mp hzpos) hz
      rw [show prawitzSlackImbalanceOffDiagonal T 0 (-z) =
          prawitzSlackImbalance T 0 (-z) by
            simp [prawitzSlackImbalanceOffDiagonal, hneg_ne],
        show prawitzSlackImbalanceOffDiagonal T 0 z =
          prawitzSlackImbalance T 0 z by
            simp [prawitzSlackImbalanceOffDiagonal, hz],
        prawitzSlackImbalance_of_lt T (neg_pos.mpr hzneg),
        prawitzSlackImbalance_of_le T hzneg.le]
      simp only [sub_zero, zero_sub, neg_neg]
      ring

/-- Around an arbitrary center, the off-diagonal imbalance changes sign
under spatial reflection. -/
theorem prawitzSlackImbalanceOffDiagonal_reflect (T x y : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x (2 * x - y) =
      -prawitzSlackImbalanceOffDiagonal T x y := by
  calc
    prawitzSlackImbalanceOffDiagonal T x (2 * x - y) =
        prawitzSlackImbalanceOffDiagonal T 0 (2 * x - y - x) :=
      prawitzSlackImbalanceOffDiagonal_translate T x (2 * x - y)
    _ = prawitzSlackImbalanceOffDiagonal T 0 (-(y - x)) := by
      congr 1
      ring
    _ = -prawitzSlackImbalanceOffDiagonal T 0 (y - x) :=
      prawitzSlackImbalanceOffDiagonal_neg T (y - x)
    _ = -prawitzSlackImbalanceOffDiagonal T x y := by
      exact congrArg Neg.neg
        (prawitzSlackImbalanceOffDiagonal_translate T x y).symm

/-- On the positive universal half-line, the off-diagonal imbalance is
exactly half the difference of the two nonnegative Prawitz--Vaaler
remainders. -/
theorem prawitzSlackImbalanceOffDiagonal_two_pi_mul
    {a : ℝ} (ha : 0 < a) :
    prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * a) =
      (prawitzVaalerNegativeRemainder a -
        prawitzVaalerPositiveRemainder a) / 2 := by
  have hz : 2 * Real.pi * a ≠ 0 := by positivity
  rw [show prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * a) =
      prawitzSlackImbalance 1 0 (2 * Real.pi * a) by
        simp [prawitzSlackImbalanceOffDiagonal, hz],
    prawitzSlackImbalance_of_lt 1 (by positivity)]
  simp only [sub_zero, zero_sub]
  rw [show -(2 * Real.pi * a) = -2 * Real.pi * a by ring]
  have hpos := prawitzPointKernel_positive_identity ha
  have hneg :=
    prawitzVaalerNegativeRemainder_eq_pointKernel_of_positive ha hpos
  linarith

/-- The negative universal half-line has the reflected remainder difference,
as forced by odd symmetry. -/
theorem prawitzSlackImbalanceOffDiagonal_neg_two_pi_mul
    {a : ℝ} (ha : 0 < a) :
    prawitzSlackImbalanceOffDiagonal 1 0 (-2 * Real.pi * a) =
      (prawitzVaalerPositiveRemainder a -
        prawitzVaalerNegativeRemainder a) / 2 := by
  rw [show -2 * Real.pi * a = -(2 * Real.pi * a) by ring,
    prawitzSlackImbalanceOffDiagonal_neg,
    prawitzSlackImbalanceOffDiagonal_two_pi_mul ha]
  ring

/-- Explicit positive-half-line coordinate for the universal off-diagonal
imbalance, with the inverse-square series isolated for differentiation. -/
noncomputable def prawitzOffDiagonalHalfProfile (x : ℝ) : ℝ :=
  (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
    (1 / x ^ 2 + 2 * prawitzInverseSquareTail x - 2 / x)

/-- The universal off-diagonal imbalance is the explicit half-profile after
the natural `2 * π` spatial rescaling. -/
theorem prawitzSlackImbalanceOffDiagonal_two_pi_mul_eq_halfProfile
    {x : ℝ} (hx : 0 < x) :
    prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * x) =
      prawitzOffDiagonalHalfProfile x := by
  rw [prawitzSlackImbalanceOffDiagonal_two_pi_mul hx]
  unfold prawitzOffDiagonalHalfProfile prawitzVaalerNegativeRemainder
    prawitzVaalerPositiveRemainder
  ring

/-- Exact derivative of the universal positive half-profile. -/
theorem hasDerivAt_prawitzOffDiagonalHalfProfile
    {x : ℝ} (hx : 0 < x) :
    HasDerivAt prawitzOffDiagonalHalfProfile
      (2 * (Real.sin (Real.pi * x) / Real.pi) * Real.cos (Real.pi * x) *
          (1 / x ^ 2 + 2 * prawitzInverseSquareTail x - 2 / x) +
        (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
          (-2 / x ^ 3 - 4 * prawitzInverseCubeTail x + 2 / x ^ 2)) x := by
  have hsinArg : HasDerivAt (fun y : ℝ => Real.sin (Real.pi * y))
      (Real.pi * Real.cos (Real.pi * x)) x := by
    convert (Real.hasDerivAt_sin (Real.pi * x)).comp x
      ((hasDerivAt_const x Real.pi).mul (hasDerivAt_id x)) using 1 <;> ring
  have hsin : HasDerivAt (fun y : ℝ => Real.sin (Real.pi * y) / Real.pi)
      (Real.cos (Real.pi * x)) x := by
    convert hsinArg.div_const Real.pi using 1
    field_simp [Real.pi_ne_zero]
  have hInv : HasDerivAt (fun y : ℝ => 1 / y) (-1 / x ^ 2) x := by
    simpa only [one_div] using (hasDerivAt_id x).inv hx.ne'
  have hInvSq : HasDerivAt (fun y : ℝ => 1 / y ^ 2) (-2 / x ^ 3) x := by
    have h := hInv.pow 2
    convert h using 1
    · funext y
      rw [Pi.pow_apply]
      simp only [one_div]
      exact (inv_pow y 2).symm
    · simp only [Nat.cast_ofNat, Nat.reduceSub, pow_one]
      field_simp [hx.ne']
  have hBracket : HasDerivAt
      (fun y : ℝ => 1 / y ^ 2 + 2 * prawitzInverseSquareTail y - 2 / y)
      (-2 / x ^ 3 - 4 * prawitzInverseCubeTail x + 2 / x ^ 2) x := by
    have h := hInvSq.add
      ((hasDerivAt_prawitzInverseSquareTail hx).const_mul 2) |>.sub
        (hInv.const_mul 2)
    convert h using 1
    · funext y
      change 1 / y ^ 2 + 2 * prawitzInverseSquareTail y - 2 / y =
        (1 / y ^ 2 + 2 * prawitzInverseSquareTail y) - 2 * (1 / y)
      ring
    · ring
  unfold prawitzOffDiagonalHalfProfile
  have h := (hsin.pow 2).mul hBracket
  simp only [Pi.pow_apply] at h
  convert h using 1 <;> ring

/-- A sinc-based continuous extension of the positive half-profile through
the spatial center. -/
noncomputable def prawitzOffDiagonalHalfProfileExtension (x : ℝ) : ℝ :=
  Real.sinc (Real.pi * x) ^ 2 +
    2 * (Real.sin (Real.pi * x) / Real.pi) ^ 2 *
      prawitzInverseSquareTail x -
    2 * x * Real.sinc (Real.pi * x) ^ 2

/-- Away from zero, the sinc extension agrees with the explicit half-profile. -/
theorem prawitzOffDiagonalHalfProfile_eq_extension
    {x : ℝ} (hx : x ≠ 0) :
    prawitzOffDiagonalHalfProfile x =
      prawitzOffDiagonalHalfProfileExtension x := by
  unfold prawitzOffDiagonalHalfProfile
    prawitzOffDiagonalHalfProfileExtension
  rw [Real.sinc_of_ne_zero (mul_ne_zero Real.pi_ne_zero hx)]
  field_simp [Real.pi_ne_zero, hx]

/-- The sinc extension is continuous at the spatial center. -/
theorem continuousAt_prawitzOffDiagonalHalfProfileExtension :
    ContinuousAt prawitzOffDiagonalHalfProfileExtension 0 := by
  have hsinc : ContinuousAt (fun x : ℝ => Real.sinc (Real.pi * x)) 0 := by
    fun_prop
  have hsin : ContinuousAt (fun x : ℝ =>
      Real.sin (Real.pi * x) / Real.pi) 0 := by
    fun_prop
  unfold prawitzOffDiagonalHalfProfileExtension
  exact (hsinc.pow 2).add
    ((continuousAt_const.mul (hsin.pow 2)).mul
      continuousAt_prawitzInverseSquareTail_zero) |>.sub
        ((continuousAt_const.mul continuousAt_id).mul (hsinc.pow 2))

/-- The continuous extension takes the value one at the spatial center. -/
@[simp] theorem prawitzOffDiagonalHalfProfileExtension_zero :
    prawitzOffDiagonalHalfProfileExtension 0 = 1 := by
  simp [prawitzOffDiagonalHalfProfileExtension]

/-- The universal positive half-profile tends to one on approach to its
deleted spatial center. -/
theorem tendsto_prawitzOffDiagonalHalfProfile_right :
    Tendsto prawitzOffDiagonalHalfProfile (nhdsWithin 0 (Ioi 0)) (nhds 1) := by
  have hext : Tendsto prawitzOffDiagonalHalfProfileExtension
      (nhdsWithin 0 (Ioi 0)) (nhds 1) := by
    rw [show (1 : ℝ) = prawitzOffDiagonalHalfProfileExtension 0 by simp]
    exact continuousAt_prawitzOffDiagonalHalfProfileExtension.continuousWithinAt
  apply hext.congr'
  filter_upwards [self_mem_nhdsWithin] with x hx
  exact (prawitzOffDiagonalHalfProfile_eq_extension hx.ne').symm

/-- The off-diagonal imbalance has right-hand limit one at its deleted
center, after the natural `2 * π` rescaling. -/
theorem tendsto_prawitzSlackImbalanceOffDiagonal_positive :
    Tendsto (fun a : ℝ =>
      prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * a))
      (nhdsWithin 0 (Ioi 0)) (nhds 1) := by
  apply tendsto_prawitzOffDiagonalHalfProfile_right.congr'
  filter_upwards [self_mem_nhdsWithin] with a ha
  exact
    (prawitzSlackImbalanceOffDiagonal_two_pi_mul_eq_halfProfile ha).symm

/-- Odd symmetry gives the corresponding left-hand profile limit minus one. -/
theorem tendsto_prawitzSlackImbalanceOffDiagonal_negative :
    Tendsto (fun a : ℝ =>
      prawitzSlackImbalanceOffDiagonal 1 0 (-2 * Real.pi * a))
      (nhdsWithin 0 (Ioi 0)) (nhds (-1)) := by
  have hneg := tendsto_prawitzSlackImbalanceOffDiagonal_positive.neg
  apply hneg.congr'
  filter_upwards with a
  rw [show -2 * Real.pi * a = -(2 * Real.pi * a) by ring,
    prawitzSlackImbalanceOffDiagonal_neg]

/-- The two increments adjacent to the deleted center tend to total variation
two; removing the diagonal atom therefore does not remove the coefficient-two
variation obstruction. -/
theorem tendsto_prawitzSlackImbalanceOffDiagonal_twoJumpVariation :
    Tendsto (fun a : ℝ =>
      |prawitzSlackImbalanceOffDiagonal 1 0 (-2 * Real.pi * a) -
          prawitzSlackImbalanceOffDiagonal 1 0 0| +
        |prawitzSlackImbalanceOffDiagonal 1 0 0 -
          prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * a)|)
      (nhdsWithin 0 (Ioi 0)) (nhds 2) := by
  have hconst : Tendsto (fun _a : ℝ => (2 : ℝ))
      (nhdsWithin 0 (Ioi 0)) (nhds 2) := tendsto_const_nhds
  have hlim :=
    hconst.mul tendsto_prawitzSlackImbalanceOffDiagonal_positive.abs
  have htwo : Tendsto (fun a : ℝ =>
      2 * |prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * a)|)
      (nhdsWithin 0 (Ioi 0)) (nhds 2) := by
    simpa using hlim
  apply htwo.congr'
  filter_upwards with a
  rw [show -2 * Real.pi * a = -(2 * Real.pi * a) by ring,
    prawitzSlackImbalanceOffDiagonal_neg,
    show prawitzSlackImbalanceOffDiagonal 1 0 0 = 0 by
      simp [prawitzSlackImbalanceOffDiagonal]]
  simp only [sub_zero, zero_sub, abs_neg]
  ring

/-- Every uniform upper bound on the two off-diagonal increments adjacent to
the center has coefficient at least two. -/
theorem two_le_of_prawitzSlackImbalanceOffDiagonal_twoJumpVariation_le
    (C : ℝ)
    (hC : ∀ a : ℝ, 0 < a →
      |prawitzSlackImbalanceOffDiagonal 1 0 (-2 * Real.pi * a) -
          prawitzSlackImbalanceOffDiagonal 1 0 0| +
        |prawitzSlackImbalanceOffDiagonal 1 0 0 -
          prawitzSlackImbalanceOffDiagonal 1 0 (2 * Real.pi * a)| ≤ C) :
    2 ≤ C := by
  apply le_of_tendsto
    tendsto_prawitzSlackImbalanceOffDiagonal_twoJumpVariation
  filter_upwards [self_mem_nhdsWithin] with a ha
  exact hC a ha

/-- Removing the diagonal atom sets the spatial center to zero. -/
@[simp] theorem prawitzSlackImbalanceOffDiagonal_self (T x : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x x = 0 := by
  simp [prawitzSlackImbalanceOffDiagonal]

/-- The off-diagonal profile still vanishes at the first Fourier lattice
point to the left of the spatial center. -/
theorem prawitzSlackImbalanceOffDiagonal_left_latticePoint
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x (x - 2 * Real.pi / T) = 0 := by
  have hshift : 0 < 2 * Real.pi / T := div_pos (by positivity) hT
  have hne : x - 2 * Real.pi / T ≠ x := ne_of_lt (sub_lt_self x hshift)
  simp [prawitzSlackImbalanceOffDiagonal, hne,
    prawitzSlackImbalance_left_latticePoint hT]

/-- The reflected first Fourier lattice point also remains a zero of the
off-diagonal profile. -/
theorem prawitzSlackImbalanceOffDiagonal_right_latticePoint
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x (x + 2 * Real.pi / T) = 0 := by
  have hshift : 0 < 2 * Real.pi / T := div_pos (by positivity) hT
  have hne : x + 2 * Real.pi / T ≠ x := ne_of_gt (lt_add_of_pos_right x hshift)
  simp [prawitzSlackImbalanceOffDiagonal, hne,
    prawitzSlackImbalance_right_latticePoint hT]

/-- Once the diagonal atom is removed, the earlier three-point variation
obstruction vanishes exactly. -/
theorem prawitzSlackImbalanceOffDiagonal_threePointVariation
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |prawitzSlackImbalanceOffDiagonal T x (x - 2 * Real.pi / T) -
        prawitzSlackImbalanceOffDiagonal T x x| +
      |prawitzSlackImbalanceOffDiagonal T x x -
        prawitzSlackImbalanceOffDiagonal T x (x + 2 * Real.pi / T)| = 0 := by
  rw [prawitzSlackImbalanceOffDiagonal_left_latticePoint hT,
    prawitzSlackImbalanceOffDiagonal_self,
    prawitzSlackImbalanceOffDiagonal_right_latticePoint hT]
  simp only [sub_self, abs_zero, add_zero]

/-- The full imbalance is the off-diagonal profile minus the singleton
indicator.  This isolates the atom created by the right-continuous CDF
convention. -/
theorem prawitzSlackImbalance_eq_offDiagonal_sub_singletonIndicator
    (T x y : ℝ) :
    prawitzSlackImbalance T x y =
      prawitzSlackImbalanceOffDiagonal T x y -
        ({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ)) y := by
  by_cases hy : y = x
  · subst y
    simp [prawitzSlackImbalanceOffDiagonal,
      prawitzSlackImbalance_diagonal]
  · simp [prawitzSlackImbalanceOffDiagonal, hy]

/-- Equivalently, the off-diagonal profile restores the singleton indicator
to the full imbalance. -/
theorem prawitzSlackImbalanceOffDiagonal_eq_add_singletonIndicator
    (T x y : ℝ) :
    prawitzSlackImbalanceOffDiagonal T x y =
      prawitzSlackImbalance T x y +
        ({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ)) y := by
  have h :=
    prawitzSlackImbalance_eq_offDiagonal_sub_singletonIndicator T x y
  linarith

/-- Removing the diagonal preserves integrability under the same point-kernel
hypotheses. -/
theorem integrable_prawitzSlackImbalanceOffDiagonal
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    Integrable (prawitzSlackImbalanceOffDiagonal T x) mu := by
  have hUpper := integrable_prawitzUpperSlack mu x hForward
  have hLower := integrable_prawitzLowerSlack mu x hReflected
  have hImbalance : Integrable (prawitzSlackImbalance T x) mu := by
    unfold prawitzSlackImbalance
    exact (hUpper.sub hLower).div_const 2
  have hSingleton : Integrable
      (({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ))) mu :=
    (integrable_const (1 : ℝ)).indicator (measurableSet_singleton x)
  have heq : prawitzSlackImbalanceOffDiagonal T x =
      fun y => prawitzSlackImbalance T x y +
        ({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ)) y := by
    funext y
    exact prawitzSlackImbalanceOffDiagonal_eq_add_singletonIndicator T x y
  rw [heq]
  exact hImbalance.add hSingleton

/-- The expected imbalance splits into its off-diagonal expectation minus the
law's atom at the CDF evaluation point. -/
theorem integral_prawitzSlackImbalance_eq_offDiagonal_sub_atom
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (∫ y, prawitzSlackImbalance T x y ∂mu) =
      (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        mu.real {x} := by
  have hOff := integrable_prawitzSlackImbalanceOffDiagonal
    mu x hForward hReflected
  have hSingleton : Integrable
      (({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ))) mu :=
    (integrable_const (1 : ℝ)).indicator (measurableSet_singleton x)
  have hAtom :
      (∫ y, ({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ)) y ∂mu) =
        mu.real {x} := by
    exact integral_indicator_one (μ := mu) (measurableSet_singleton x)
  calc
    (∫ y, prawitzSlackImbalance T x y ∂mu) =
        ∫ y, (prawitzSlackImbalanceOffDiagonal T x y -
          ({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ)) y) ∂mu := by
      apply integral_congr_ae
      exact ae_of_all mu
        (prawitzSlackImbalance_eq_offDiagonal_sub_singletonIndicator T x)
    _ = (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        ∫ y, ({x} : Set ℝ).indicator (fun _ : ℝ => (1 : ℝ)) y ∂mu :=
      integral_sub hOff hSingleton
    _ = _ := by rw [hAtom]

/-- For two laws, the diagonal contribution is exactly their singleton-mass
difference and all remaining imbalance is off-diagonal. -/
theorem integral_prawitzSlackImbalance_sub_eq_offDiagonal_sub_atomDifference
    (mu nu : Measure ℝ) [IsFiniteMeasure mu] [IsFiniteMeasure nu]
    {T : ℝ} (x : ℝ)
    (hmuForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hmuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnuForward : Integrable (fun y => prawitzPointKernel T (y - x)) nu)
    (hnuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    (∫ y, prawitzSlackImbalance T x y ∂mu) -
        (∫ y, prawitzSlackImbalance T x y ∂nu) =
      ((∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        ∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu) -
      (mu.real {x} - nu.real {x}) := by
  rw [integral_prawitzSlackImbalance_eq_offDiagonal_sub_atom
      mu x hmuForward hmuReflected,
    integral_prawitzSlackImbalance_eq_offDiagonal_sub_atom
      nu x hnuForward hnuReflected]
  ring

theorem prawitzUpperSlack_eq_sinc_sq_add_imbalance
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    prawitzUpperSlack T x y =
      Real.sinc (T * (y - x) / 2) ^ 2 +
        prawitzSlackImbalance T x y := by
  have hsum := prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq hT x y
  unfold prawitzSlackImbalance
  linarith

theorem prawitzLowerSlack_eq_sinc_sq_sub_imbalance
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    prawitzLowerSlack T x y =
      Real.sinc (T * (y - x) / 2) ^ 2 -
        prawitzSlackImbalance T x y := by
  have hsum := prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq hT x y
  unfold prawitzSlackImbalance
  linarith

/-- Nonnegativity of both one-sided slacks bounds their signed imbalance by
the common sinc-squared mass. -/
theorem abs_prawitzSlackImbalance_le_sinc_sq
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    |prawitzSlackImbalance T x y| ≤
      Real.sinc (T * (y - x) / 2) ^ 2 := by
  rw [abs_le]
  constructor
  · have hUpper := prawitzUpperSlack_nonneg hT x y
    rw [prawitzUpperSlack_eq_sinc_sq_add_imbalance hT x y] at hUpper
    linarith
  · have hLower := prawitzLowerSlack_nonneg hT x y
    rw [prawitzLowerSlack_eq_sinc_sq_sub_imbalance hT x y] at hLower
    linarith

theorem integrable_prawitzSlackImbalance
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    Integrable (prawitzSlackImbalance T x) mu := by
  have hUpper := integrable_prawitzUpperSlack mu x hForward
  have hLower := integrable_prawitzLowerSlack mu x hReflected
  unfold prawitzSlackImbalance
  exact (hUpper.sub hLower).div_const 2

theorem integral_prawitzSlackImbalance_eq_half_sub
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (∫ y, prawitzSlackImbalance T x y ∂mu) =
      ((∫ y, prawitzUpperSlack T x y ∂mu) -
        ∫ y, prawitzLowerSlack T x y ∂mu) / 2 := by
  have hUpper := integrable_prawitzUpperSlack mu x hForward
  have hLower := integrable_prawitzLowerSlack mu x hReflected
  unfold prawitzSlackImbalance
  rw [integral_div, integral_sub hUpper hLower]

/-- Expectation of the imbalance in spatial point-kernel coordinates. -/
theorem integral_prawitzSlackImbalance_eq_pointKernelDifference_sub_centeredCDF
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (∫ y, prawitzSlackImbalance T x y ∂mu) =
      ((∫ y, prawitzPointKernel T (y - x) ∂mu) -
        ∫ y, prawitzPointKernel T (x - y) ∂mu) -
        (2 * cdf mu x - 1) := by
  have hStep : Integrable
      (fun y => 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) mu :=
    ((integrable_const (1 : ℝ)).indicator measurableSet_Iic).const_mul 2 |>.sub
      (integrable_const 1)
  calc
    (∫ y, prawitzSlackImbalance T x y ∂mu) =
        ∫ y, (prawitzPointKernel T (y - x) -
            prawitzPointKernel T (x - y)) -
          (2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1) ∂mu := by
      apply integral_congr_ae
      exact ae_of_all mu fun y =>
        prawitzSlackImbalance_eq_pointKernel_sub_reflected_sub_centeredStep
          T x y
    _ = (∫ y, prawitzPointKernel T (y - x) -
          prawitzPointKernel T (x - y) ∂mu) -
        ∫ y, 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ∂mu :=
      integral_sub (hForward.sub hReflected) hStep
    _ = ((∫ y, prawitzPointKernel T (y - x) ∂mu) -
          ∫ y, prawitzPointKernel T (x - y) ∂mu) -
        ∫ y, 2 * (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y - 1 ∂mu := by
      rw [integral_sub hForward hReflected]
    _ = ((∫ y, prawitzPointKernel T (y - x) ∂mu) -
          ∫ y, prawitzPointKernel T (x - y) ∂mu) -
        (2 * cdf mu x - 1) := by rw [integral_centeredCDFKernel]

theorem integral_prawitzUpperSlack_eq_sincMass_add_imbalance
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (∫ y, prawitzUpperSlack T x y ∂mu) =
      (∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) +
        ∫ y, prawitzSlackImbalance T x y ∂mu := by
  have hsum := integral_prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq
    mu hT x hForward hReflected
  have himb := integral_prawitzSlackImbalance_eq_half_sub
    mu x hForward hReflected
  linarith

theorem integral_prawitzLowerSlack_eq_sincMass_sub_imbalance
    (mu : Measure ℝ) [IsFiniteMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu) :
    (∫ y, prawitzLowerSlack T x y ∂mu) =
      (∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
        ∫ y, prawitzSlackImbalance T x y ∂mu := by
  have hsum := integral_prawitzUpperSlack_add_lowerSlack_eq_two_mul_sinc_sq
    mu hT x hForward hReflected
  have himb := integral_prawitzSlackImbalance_eq_half_sub
    mu x hForward hReflected
  linarith

/-- The difference of two upper-slack expectations splits exactly into the
difference of their common sinc-squared masses and the difference of their
signed imbalances. -/
theorem prawitzUpperSlackDifference_eq_sincMassDifference_add_imbalanceDifference
    (mu nu : Measure ℝ) [IsFiniteMeasure mu] [IsFiniteMeasure nu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hmuForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hmuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnuForward : Integrable (fun y => prawitzPointKernel T (y - x)) nu)
    (hnuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    ((∫ y, prawitzUpperSlack T x y ∂mu) -
        ∫ y, prawitzUpperSlack T x y ∂nu) =
      ((∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
          ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu) +
        ((∫ y, prawitzSlackImbalance T x y ∂mu) -
          ∫ y, prawitzSlackImbalance T x y ∂nu) := by
  have hmu := integral_prawitzUpperSlack_eq_sincMass_add_imbalance
    mu hT x hmuForward hmuReflected
  have hnu := integral_prawitzUpperSlack_eq_sincMass_add_imbalance
    nu hT x hnuForward hnuReflected
  linarith

/-- The corresponding lower-slack difference has the same common-mass
coordinate and the opposite signed-imbalance coordinate. -/
theorem prawitzLowerSlackDifference_eq_sincMassDifference_sub_imbalanceDifference
    (mu nu : Measure ℝ) [IsFiniteMeasure mu] [IsFiniteMeasure nu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hmuForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hmuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnuForward : Integrable (fun y => prawitzPointKernel T (y - x)) nu)
    (hnuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    ((∫ y, prawitzLowerSlack T x y ∂mu) -
        ∫ y, prawitzLowerSlack T x y ∂nu) =
      ((∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
          ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu) -
        ((∫ y, prawitzSlackImbalance T x y ∂mu) -
          ∫ y, prawitzSlackImbalance T x y ∂nu) := by
  have hmu := integral_prawitzLowerSlack_eq_sincMass_sub_imbalance
    mu hT x hmuForward hmuReflected
  have hnu := integral_prawitzLowerSlack_eq_sincMass_sub_imbalance
    nu hT x hnuForward hnuReflected
  linarith

/-- Forward Prawitz recentering in common-mass and signed-imbalance
coordinates. -/
theorem cdf_sub_cdf_eq_prawitzUpperMassImbalanceRecentered
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hmuForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hmuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnuForward : Integrable (fun y => prawitzPointKernel T (y - x)) nu)
    (hnuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    cdf mu x - cdf nu x =
      ((∫ y, prawitzPointKernel T (y - x) ∂mu) -
          ∫ y, prawitzPointKernel T (y - x) ∂nu) -
        (1 / 2 : ℝ) *
          (((∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
              ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu) +
            ((∫ y, prawitzSlackImbalance T x y ∂mu) -
              ∫ y, prawitzSlackImbalance T x y ∂nu)) := by
  rw [cdf_sub_cdf_eq_prawitzUpperRecentered mu nu x hmuForward hnuForward,
    prawitzUpperSlackDifference_eq_sincMassDifference_add_imbalanceDifference
      mu nu hT x hmuForward hmuReflected hnuForward hnuReflected]

/-- Reflected Prawitz recentering in the same coordinates, with the signed
imbalance entering with its opposite sign. -/
theorem cdf_sub_cdf_eq_prawitzLowerMassImbalanceRecentered
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (hT : 0 < T) (x : ℝ)
    (hmuForward : Integrable (fun y => prawitzPointKernel T (y - x)) mu)
    (hmuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) mu)
    (hnuForward : Integrable (fun y => prawitzPointKernel T (y - x)) nu)
    (hnuReflected : Integrable (fun y => prawitzPointKernel T (x - y)) nu) :
    cdf mu x - cdf nu x =
      -((∫ y, prawitzPointKernel T (x - y) ∂mu) -
          ∫ y, prawitzPointKernel T (x - y) ∂nu) +
        (1 / 2 : ℝ) *
          (((∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
              ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu) -
            ((∫ y, prawitzSlackImbalance T x y ∂mu) -
              ∫ y, prawitzSlackImbalance T x y ∂nu)) := by
  rw [cdf_sub_cdf_eq_prawitzLowerRecentered mu nu x hmuReflected hnuReflected,
    prawitzLowerSlackDifference_eq_sincMassDifference_sub_imbalanceDifference
      mu nu hT x hmuForward hmuReflected hnuForward hnuReflected]

/-- The integrable sinc-squared profile underlying the reflected Fejér
remainder. -/
noncomputable def sincSquareWeight (x : ℝ) : ℝ := Real.sinc x ^ 2

theorem sincSquareWeight_nonneg (x : ℝ) : 0 ≤ sincSquareWeight x := by
  unfold sincSquareWeight
  positivity

theorem continuous_sincSquareWeight : Continuous sincSquareWeight := by
  exact Real.continuous_sinc.pow 2

theorem integrable_sincSquareWeight : Integrable sincSquareWeight := by
  have hmajor : Integrable (fun x : ℝ => 2 * (1 + x ^ 2)⁻¹) :=
    integrable_inv_one_add_sq.const_mul 2
  apply hmajor.mono' continuous_sincSquareWeight.aestronglyMeasurable
  exact ae_of_all volume fun x => by
    rw [Real.norm_eq_abs, abs_of_nonneg (sincSquareWeight_nonneg x)]
    by_cases hx : |x| ≤ 1
    · have hs : |Real.sinc x| ^ 2 ≤ 1 :=
        pow_le_one₀ (abs_nonneg _) (Real.abs_sinc_le_one x)
      have hxSq : x ^ 2 ≤ 1 := by
        rw [← sq_abs]
        nlinarith [abs_nonneg x]
      have hden : 0 < 1 + x ^ 2 := by positivity
      unfold sincSquareWeight
      rw [show Real.sinc x ^ 2 = |Real.sinc x| ^ 2 by
        rw [← abs_pow, abs_of_nonneg (by positivity)]]
      calc
        |Real.sinc x| ^ 2 ≤ 1 := hs
        _ ≤ 2 * (1 + x ^ 2)⁻¹ := by
          rw [← div_eq_mul_inv, le_div_iff₀ hden]
          linarith
    · have hxLarge : 1 < |x| := lt_of_not_ge hx
      have hx0 : x ≠ 0 := by
        intro h
        subst x
        norm_num at hxLarge
      have hsinc := abs_sinc_le_inv_abs hx0
      have hsPow : |Real.sinc x| ^ 2 ≤ |x|⁻¹ ^ 2 :=
        pow_le_pow_left₀ (abs_nonneg _) hsinc 2
      have hxPow : 0 < |x| ^ 2 := pow_pos (abs_pos.mpr hx0) 2
      have hden : 0 < 1 + x ^ 2 := by positivity
      have hfrac : (|x| ^ 2)⁻¹ ≤ 2 * (1 + x ^ 2)⁻¹ := by
        have hxSq : 1 ≤ x ^ 2 := by
          rw [← sq_abs]
          nlinarith
        calc
          (|x| ^ 2)⁻¹ = 1 / (|x| ^ 2) := by rw [one_div]
          _ ≤ 2 / (1 + x ^ 2) := by
            rw [div_le_div_iff₀ hxPow hden]
            rw [sq_abs]
            nlinarith
          _ = 2 * (1 + x ^ 2)⁻¹ := by rw [div_eq_mul_inv]
      unfold sincSquareWeight
      rw [show Real.sinc x ^ 2 = |Real.sinc x| ^ 2 by
        rw [← abs_pow, abs_of_nonneg (by positivity)]]
      calc
        |Real.sinc x| ^ 2 ≤ |x|⁻¹ ^ 2 := hsPow
        _ = (|x| ^ 2)⁻¹ := by rw [inv_pow]
        _ ≤ 2 * (1 + x ^ 2)⁻¹ := hfrac

/-- Sum of two independent uniform laws on `[-1,1]`. -/
noncomputable def twofoldUniformKernel : Measure ℝ :=
  uniformSmoothingKernel 1 ∗ uniformSmoothingKernel 1

theorem twofoldUniformKernel_isProbabilityMeasure :
    IsProbabilityMeasure twofoldUniformKernel := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  unfold twofoldUniformKernel
  infer_instance

theorem twofoldUniformKernel_supported :
    IsSupportedInSmoothingWindow twofoldUniformKernel 2 := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  let hUniform := uniformSmoothingKernel_supported 1
  unfold twofoldUniformKernel
  convert isSupportedInSmoothingWindow_conv hUniform hUniform using 1 <;>
    norm_num

theorem charFun_twofoldUniformKernel (t : ℝ) :
    MeasureTheory.charFun twofoldUniformKernel t =
      (sincSquareWeight t : ℂ) := by
  letI : IsProbabilityMeasure (uniformSmoothingKernel 1) :=
    uniformSmoothingKernel_isProbabilityMeasure (by norm_num)
  have hchar :
      MeasureTheory.charFun (uniformSmoothingKernel 1) t =
        (Real.sinc t : ℂ) := by
    simpa using charFun_uniformSmoothingKernel (δ := 1) (by norm_num) t
  unfold twofoldUniformKernel
  rw [MeasureTheory.charFun_conv, hchar]
  simp only [sincSquareWeight, Complex.ofReal_pow]
  ring

theorem twofoldUniformKernel_eq_withDensity :
    twofoldUniformKernel = volume.withDensity twofoldUniformDensity := by
  exact uniformSmoothingKernel_conv_self_eq_withDensity

/-- Integration against the twofold uniform kernel in terms of its real
triangular Lebesgue density. -/
theorem integral_twofoldUniformKernel_eq_integral_density
    (f : ℝ → ℂ) :
    (∫ z, f z ∂twofoldUniformKernel) =
      ∫ z, (twofoldUniformDensityReal z : ℂ) * f z := by
  rw [twofoldUniformKernel_eq_withDensity,
    integral_withDensity_eq_integral_toReal_smul
      measurable_twofoldUniformDensity
      (ae_of_all volume fun x => by unfold twofoldUniformDensity; finiteness)
      f]
  apply integral_congr_ae
  exact ae_of_all volume fun z => by
    change (twofoldUniformDensity z).toReal • f z =
      (twofoldUniformDensityReal z : ℂ) * f z
    rw [twofoldUniformDensity_toReal]
    rw [Complex.real_smul]

noncomputable def twofoldUniformDensityComplex (x : ℝ) : ℂ :=
  twofoldUniformDensityReal x

theorem continuous_twofoldUniformDensityComplex :
    Continuous twofoldUniformDensityComplex := by
  exact Complex.continuous_ofReal.comp continuous_twofoldUniformDensityReal

theorem integrable_twofoldUniformDensityComplex :
    Integrable twofoldUniformDensityComplex :=
  integrable_twofoldUniformDensityReal.ofReal

theorem sincSquareWeight_eq_fourierInv_twofoldUniformDensityComplex (t : ℝ) :
    (sincSquareWeight t : ℂ) =
      𝓕⁻ twofoldUniformDensityComplex (t / (2 * Real.pi)) := by
  letI : IsProbabilityMeasure twofoldUniformKernel :=
    twofoldUniformKernel_isProbabilityMeasure
  rw [← charFun_twofoldUniformKernel t,
    twofoldUniformKernel_eq_withDensity,
    MeasureTheory.charFun_apply_real,
    integral_withDensity_eq_integral_toReal_smul
      measurable_twofoldUniformDensity
      (ae_of_all volume fun x => by unfold twofoldUniformDensity; finiteness)
      (fun x : ℝ => Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I)),
    Real.fourierInv_eq']
  apply integral_congr_ae
  exact ae_of_all volume fun x => by
    dsimp only
    rw [twofoldUniformDensity_toReal]
    change (twofoldUniformDensityReal x : ℂ) *
        Complex.exp ((t : ℂ) * (x : ℂ) * Complex.I) =
      Complex.exp (((2 * Real.pi * inner ℝ x (t / (2 * Real.pi)) : ℝ) : ℂ) *
        Complex.I) * (twofoldUniformDensityReal x : ℂ)
    rw [mul_comm]
    congr 2
    congr 1
    norm_cast
    rw [real_inner_eq_re_inner (𝕜 := ℝ)]
    simp only [RCLike.inner_apply, conj_trivial]
    have hre (z : ℝ) : RCLike.re z = z := rfl
    rw [hre]
    field_simp [Real.pi_ne_zero]

theorem fourierInv_twofoldUniformDensityComplex_eq_sincSquareWeight (w : ℝ) :
    𝓕⁻ twofoldUniformDensityComplex w =
      (sincSquareWeight ((2 * Real.pi) * w) : ℂ) := by
  rw [sincSquareWeight_eq_fourierInv_twofoldUniformDensityComplex]
  congr 1
  field_simp [Real.pi_ne_zero]

theorem integrable_fourierInv_twofoldUniformDensityComplex :
    Integrable (𝓕⁻ twofoldUniformDensityComplex) := by
  have htwoPi : (2 * Real.pi : ℝ) ≠ 0 :=
    mul_ne_zero (by norm_num) Real.pi_ne_zero
  have hbase : Integrable (fun w : ℝ => (sincSquareWeight w : ℂ)) :=
    integrable_sincSquareWeight.ofReal
  have hscaled : Integrable (fun w : ℝ =>
      (sincSquareWeight ((2 * Real.pi) * w) : ℂ)) :=
    hbase.comp_mul_left' htwoPi
  exact hscaled.congr (ae_of_all volume fun w =>
    (fourierInv_twofoldUniformDensityComplex_eq_sincSquareWeight w).symm)

theorem fourier_twofoldUniformDensityComplex_eq_sincSquareWeight (w : ℝ) :
    𝓕 twofoldUniformDensityComplex w =
      (sincSquareWeight ((-2 * Real.pi) * w) : ℂ) := by
  calc
    𝓕 twofoldUniformDensityComplex w =
        𝓕⁻ twofoldUniformDensityComplex (-w) := by
      symm
      simpa using Real.fourierInv_eq_fourier_neg
        twofoldUniformDensityComplex (-w)
    _ = (sincSquareWeight ((2 * Real.pi) * (-w)) : ℂ) :=
      fourierInv_twofoldUniformDensityComplex_eq_sincSquareWeight (-w)
    _ = (sincSquareWeight ((-2 * Real.pi) * w) : ℂ) := by
      congr 2
      ring

theorem integrable_fourier_twofoldUniformDensityComplex :
    Integrable (𝓕 twofoldUniformDensityComplex) := by
  have hnegTwoPi : (-2 * Real.pi : ℝ) ≠ 0 :=
    mul_ne_zero (by norm_num : (-2 : ℝ) ≠ 0) Real.pi_ne_zero
  have hbase : Integrable (fun w : ℝ => (sincSquareWeight w : ℂ)) :=
    integrable_sincSquareWeight.ofReal
  have hscaled : Integrable (fun w : ℝ =>
      (sincSquareWeight ((-2 * Real.pi) * w) : ℂ)) :=
    hbase.comp_mul_left' hnegTwoPi
  exact hscaled.congr (ae_of_all volume fun w =>
    (fourier_twofoldUniformDensityComplex_eq_sincSquareWeight w).symm)

theorem fourier_fourierInv_twofoldUniformDensityComplex :
    𝓕 (𝓕⁻ twofoldUniformDensityComplex) = twofoldUniformDensityComplex :=
  continuous_twofoldUniformDensityComplex.fourier_fourierInv_eq
    integrable_twofoldUniformDensityComplex
    integrable_fourier_twofoldUniformDensityComplex

theorem integral_cexp_mul_sincSquareWeight_eq_density (s : ℝ) :
    (∫ x : ℝ, Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
        (sincSquareWeight x : ℂ)) =
      (2 * Real.pi) • (twofoldUniformDensityComplex (-s)) := by
  let g : ℝ → ℂ := fun x =>
    Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
      (sincSquareWeight x : ℂ)
  calc
    (∫ x : ℝ, Complex.exp ((s : ℂ) * (x : ℂ) * Complex.I) *
        (sincSquareWeight x : ℂ)) = ∫ x : ℝ, g x := rfl
    _ = (2 * Real.pi) • ∫ w : ℝ, g ((2 * Real.pi) * w) :=
      integral_eq_mul_integral_comp_mul_left g Real.two_pi_pos
    _ = (2 * Real.pi) • 𝓕 (𝓕⁻ twofoldUniformDensityComplex) (-s) := by
      congr 1
      rw [Real.fourier_eq']
      apply integral_congr_ae
      exact ae_of_all volume fun w => by
        dsimp only [g]
        rw [fourierInv_twofoldUniformDensityComplex_eq_sincSquareWeight]
        congr 1
        congr 1
        congr 1
        norm_cast
        rw [real_inner_eq_re_inner (𝕜 := ℝ)]
        simp only [RCLike.inner_apply, conj_trivial]
        have hre (z : ℝ) : RCLike.re z = z := rfl
        rw [hre]
        norm_num
        ring
    _ = (2 * Real.pi) • twofoldUniformDensityComplex (-s) := by
      rw [fourier_fourierInv_twofoldUniformDensityComplex]

/-- The exact Lebesgue mass of the sinc-squared Fejér profile. -/
theorem integral_sincSquareWeight :
    (∫ x : ℝ, sincSquareWeight x) = Real.pi := by
  have h := integral_cexp_mul_sincSquareWeight_eq_density 0
  simp only [Complex.ofReal_zero, zero_mul, Complex.exp_zero, one_mul,
    neg_zero, twofoldUniformDensityComplex, twofoldUniformDensityReal,
    abs_zero, sub_zero, max_eq_left (by norm_num : (0 : ℝ) ≤ 2),
    Complex.real_smul] at h
  have hOfReal :
      (∫ x : ℝ, (sincSquareWeight x : ℂ)) =
        Complex.ofReal (∫ x : ℝ, sincSquareWeight x) :=
    integral_complex_ofReal
  rw [hOfReal] at h
  apply_fun Complex.re at h
  norm_num at h
  calc
    (∫ x : ℝ, sincSquareWeight x) = 2 * Real.pi * (1 / 2 : ℝ) := h
    _ = Real.pi := by ring

/-- The sinc-squared profile is exactly the characteristic-function integral
of the twofold uniform kernel. -/
theorem sincSquareWeight_eq_integral_twofoldUniformKernel (s : ℝ) :
    (sincSquareWeight s : ℂ) =
      ∫ z, Complex.exp (((s * z : ℝ) : ℂ) * Complex.I)
        ∂twofoldUniformKernel := by
  rw [← charFun_twofoldUniformKernel s,
    MeasureTheory.charFun_apply_real]
  apply integral_congr_ae
  exact ae_of_all twofoldUniformKernel fun z => by
    congr 1
    push_cast
    ring

/-- The bounded joint phase occurring when the Fejer profile is averaged
against a probability law is integrable on the product space. -/
theorem integrable_prod_prawitzReflectionFourierPhase
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (T x : ℝ) :
    Integrable (fun p : ℝ × ℝ =>
      Complex.exp ((((T * (p.1 - x) / 2) * p.2 : ℝ) : ℂ) * Complex.I))
      (mu.prod twofoldUniformKernel) := by
  letI : IsProbabilityMeasure twofoldUniformKernel :=
    twofoldUniformKernel_isProbabilityMeasure
  have hmeas : AEStronglyMeasurable (fun p : ℝ × ℝ =>
      Complex.exp ((((T * (p.1 - x) / 2) * p.2 : ℝ) : ℂ) * Complex.I))
      (mu.prod twofoldUniformKernel) := by
    fun_prop
  apply (integrable_const (1 : ℝ)).mono' hmeas
  exact ae_of_all (mu.prod twofoldUniformKernel) fun p => by
    rw [Complex.norm_exp_ofReal_mul_I]

/-- Averaging a translated and scaled Fejer profile is exactly a compact
frequency integral involving the characteristic function of the law. -/
theorem integral_sincSquareWeight_eq_re_frequencyIntegral
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (T x : ℝ) :
    (∫ y, sincSquareWeight (T * (y - x) / 2) ∂mu) =
      (∫ z,
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2)
        ∂twofoldUniformKernel).re := by
  letI : IsProbabilityMeasure twofoldUniformKernel :=
    twofoldUniformKernel_isProbabilityMeasure
  have hProd := integrable_prod_prawitzReflectionFourierPhase mu T x
  have hComplex :
      (∫ y, (sincSquareWeight (T * (y - x) / 2) : ℂ) ∂mu) =
        ∫ z,
          Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu (T * z / 2)
          ∂twofoldUniformKernel := by
    simp_rw [sincSquareWeight_eq_integral_twofoldUniformKernel]
    rw [integral_integral_swap hProd]
    apply integral_congr_ae
    exact ae_of_all twofoldUniformKernel fun z => by
      change (∫ y,
          Complex.exp ((((T * (y - x) / 2) * z : ℝ) : ℂ) * Complex.I) ∂mu) =
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2)
      rw [MeasureTheory.charFun_apply_real]
      calc
        (∫ y,
            Complex.exp ((((T * (y - x) / 2) * z : ℝ) : ℂ) * Complex.I) ∂mu) =
            ∫ y,
              Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
                Complex.exp (((T * z / 2 : ℝ) : ℂ) * (y : ℂ) * Complex.I)
              ∂mu := by
          apply integral_congr_ae
          exact ae_of_all mu fun y => by
            change Complex.exp
                ((((T * (y - x) / 2) * z : ℝ) : ℂ) * Complex.I) =
              Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
                Complex.exp (((T * z / 2 : ℝ) : ℂ) * (y : ℂ) * Complex.I)
            rw [← Complex.exp_add]
            congr 1
            push_cast
            ring
        _ = Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
              ∫ y, Complex.exp (((T * z / 2 : ℝ) : ℂ) * (y : ℂ) * Complex.I)
                ∂mu := integral_const_mul _ _
  have hReal := congrArg Complex.re hComplex
  have hOfReal :
      (∫ y, (sincSquareWeight (T * (y - x) / 2) : ℂ) ∂mu) =
        Complex.ofReal (∫ y, sincSquareWeight (T * (y - x) / 2) ∂mu) :=
    integral_complex_ofReal
  rw [hOfReal] at hReal
  simpa using hReal

/-- The difference of two translated sinc-squared masses is controlled by a
compact twofold-uniform average of the corresponding characteristic-function
discrepancy. -/
theorem abs_sincMassDifference_le_integral_charFunDiscrepancy
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (T x : ℝ) :
    |(∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
        ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu| ≤
      ∫ z, ‖MeasureTheory.charFun mu (T * z / 2) -
        MeasureTheory.charFun nu (T * z / 2)‖ ∂twofoldUniformKernel := by
  letI : IsProbabilityMeasure twofoldUniformKernel :=
    twofoldUniformKernel_isProbabilityMeasure
  have hmu : Integrable (fun z =>
      Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu (T * z / 2)) twofoldUniformKernel := by
    apply (integrable_const (1 : ℝ)).mono'
    · fun_prop
    · exact ae_of_all twofoldUniformKernel fun z => by
        rw [norm_mul, Complex.norm_exp_ofReal_mul_I, one_mul]
        exact MeasureTheory.norm_charFun_le_one _
  have hnu : Integrable (fun z =>
      Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun nu (T * z / 2)) twofoldUniformKernel := by
    apply (integrable_const (1 : ℝ)).mono'
    · fun_prop
    · exact ae_of_all twofoldUniformKernel fun z => by
        rw [norm_mul, Complex.norm_exp_ofReal_mul_I, one_mul]
        exact MeasureTheory.norm_charFun_le_one _
  rw [show (fun y => Real.sinc (T * (y - x) / 2) ^ 2) =
      fun y => sincSquareWeight (T * (y - x) / 2) by rfl,
    integral_sincSquareWeight_eq_re_frequencyIntegral mu T x,
    integral_sincSquareWeight_eq_re_frequencyIntegral nu T x,
    ← Complex.sub_re]
  calc
    |((∫ z,
          Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu (T * z / 2)
          ∂twofoldUniformKernel) -
        ∫ z,
          Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun nu (T * z / 2)
          ∂twofoldUniformKernel).re| ≤
        ‖(∫ z,
            Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu (T * z / 2)
            ∂twofoldUniformKernel) -
          ∫ z,
            Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun nu (T * z / 2)
            ∂twofoldUniformKernel‖ := Complex.abs_re_le_norm _
    _ = ‖∫ z,
          Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            (MeasureTheory.charFun mu (T * z / 2) -
              MeasureTheory.charFun nu (T * z / 2))
          ∂twofoldUniformKernel‖ := by
      rw [← integral_sub hmu hnu]
      congr 1
      apply integral_congr_ae
      exact ae_of_all twofoldUniformKernel fun z => by ring
    _ ≤ ∫ z, ‖Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          (MeasureTheory.charFun mu (T * z / 2) -
            MeasureTheory.charFun nu (T * z / 2))‖
        ∂twofoldUniformKernel := norm_integral_le_integral_norm _
    _ = ∫ z, ‖MeasureTheory.charFun mu (T * z / 2) -
          MeasureTheory.charFun nu (T * z / 2)‖
        ∂twofoldUniformKernel := by
      apply integral_congr_ae
      exact ae_of_all twofoldUniformKernel fun z => by
        change ‖Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            (MeasureTheory.charFun mu (T * z / 2) -
              MeasureTheory.charFun nu (T * z / 2))‖ =
          ‖MeasureTheory.charFun mu (T * z / 2) -
            MeasureTheory.charFun nu (T * z / 2)‖
        rw [norm_mul, Complex.norm_exp_ofReal_mul_I, one_mul]

/-- A uniform characteristic-function discrepancy bound on the support of the
twofold-uniform kernel gives the same uniform bound for every translated
sinc-squared mass difference. -/
theorem abs_sincMassDifference_le_of_charFunDiscrepancy_le
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (T x C : ℝ)
    (hC : ∀ z ∈ Icc (-2 : ℝ) 2,
      ‖MeasureTheory.charFun mu (T * z / 2) -
        MeasureTheory.charFun nu (T * z / 2)‖ ≤ C) :
    |(∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
        ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu| ≤ C := by
  letI : IsProbabilityMeasure twofoldUniformKernel :=
    twofoldUniformKernel_isProbabilityMeasure
  have hInt : Integrable (fun z =>
      ‖MeasureTheory.charFun mu (T * z / 2) -
        MeasureTheory.charFun nu (T * z / 2)‖) twofoldUniformKernel := by
    apply (integrable_const (2 : ℝ)).mono'
    · fun_prop
    · exact ae_of_all twofoldUniformKernel fun z => by
        rw [Real.norm_eq_abs, abs_of_nonneg (norm_nonneg _)]
        calc
          ‖MeasureTheory.charFun mu (T * z / 2) -
              MeasureTheory.charFun nu (T * z / 2)‖ ≤
              ‖MeasureTheory.charFun mu (T * z / 2)‖ +
                ‖MeasureTheory.charFun nu (T * z / 2)‖ := norm_sub_le _ _
          _ ≤ 2 := by
            linarith [MeasureTheory.norm_charFun_le_one
              (μ := mu) (T * z / 2),
              MeasureTheory.norm_charFun_le_one
                (μ := nu) (T * z / 2)]
  have hIntegral :
      (∫ z, ‖MeasureTheory.charFun mu (T * z / 2) -
          MeasureTheory.charFun nu (T * z / 2)‖ ∂twofoldUniformKernel) ≤ C := by
    calc
      (∫ z, ‖MeasureTheory.charFun mu (T * z / 2) -
          MeasureTheory.charFun nu (T * z / 2)‖ ∂twofoldUniformKernel) ≤
          ∫ _z : ℝ, C ∂twofoldUniformKernel := by
        apply integral_mono_ae hInt (integrable_const C)
        filter_upwards [twofoldUniformKernel_supported] with z hz
        exact hC z hz
      _ = C := by simp
  exact (abs_sincMassDifference_le_integral_charFunDiscrepancy
    mu nu T x).trans hIntegral

/-- Window form of the preceding estimate: only frequencies of magnitude at
most the nonnegative scale `T` are needed. -/
theorem abs_sincMassDifference_le_of_charFunDiscrepancy_le_on_window
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (hT : 0 ≤ T) (x C : ℝ)
    (hC : ∀ t, |t| ≤ T →
      ‖MeasureTheory.charFun mu t - MeasureTheory.charFun nu t‖ ≤ C) :
    |(∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂mu) -
        ∫ y, Real.sinc (T * (y - x) / 2) ^ 2 ∂nu| ≤ C := by
  apply abs_sincMassDifference_le_of_charFunDiscrepancy_le mu nu T x C
  intro z hz
  apply hC
  have hzAbs : |z| ≤ 2 := (abs_le).2 hz
  calc
    |T * z / 2| = T * |z| / 2 := by
      rw [abs_div, abs_mul, abs_of_nonneg hT]
      norm_num
    _ ≤ T * 2 / 2 := by gcongr
    _ = T := by ring

/-- Quantitative sinc-mass comparison between a normalized centered i.i.d.
sum and the standard normal on any window contained in the one-step Taylor
regime. -/
theorem abs_sincMassDifference_normalizedCenteredIidSum_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) {T : ℝ} (hT : 0 ≤ T)
    (hWindow : T / Real.sqrt (N + 1 : ℝ) ≤ 1) (x : ℝ) :
    |(∫ y, Real.sinc (T * (y - x) / 2) ^ 2
          ∂NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable) -
        ∫ y, Real.sinc (T * (y - x) / 2) ^ 2
          ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw| ≤
      ((N + 1 : ℕ) : ℝ) *
          (4 * (T / Real.sqrt (N + 1 : ℝ)) ^ 3 *
            ∫ omega, |X 0 omega| ^ 3 ∂mu) +
        ((N + 1 : ℕ) : ℝ) *
          ((T / Real.sqrt (N + 1 : ℝ)) ^ 4 / 4) := by
  apply abs_sincMassDifference_le_of_charFunDiscrepancy_le_on_window
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x _
  intro t ht
  have hsqrt : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have htScale : |t / Real.sqrt (N + 1 : ℝ)| ≤
      T / Real.sqrt (N + 1 : ℝ) := by
    rw [abs_div, abs_of_pos hsqrt]
    exact (div_le_div_iff_of_pos_right hsqrt).2 ht
  have hlocal : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1 :=
    htScale.trans hWindow
  have hbase := norm_charFun_normalizedCenteredIidSum_sub_standardNormalLaw_le
    X hX hIndep hIdent hMean hSecond N hlocal
  have hrho : 0 ≤ ∫ omega, |X 0 omega| ^ 3 ∂mu :=
    integral_nonneg fun omega => by positivity
  calc
    _ ≤ ((N + 1 : ℕ) : ℝ) *
          (4 * |t / Real.sqrt (N + 1 : ℝ)| ^ 3 *
            ∫ omega, |X 0 omega| ^ 3 ∂mu) +
        ((N + 1 : ℕ) : ℝ) *
          (|t / Real.sqrt (N + 1 : ℝ)| ^ 4 / 4) := hbase
    _ ≤ ((N + 1 : ℕ) : ℝ) *
          (4 * (T / Real.sqrt (N + 1 : ℝ)) ^ 3 *
            ∫ omega, |X 0 omega| ^ 3 ∂mu) +
        ((N + 1 : ℕ) : ℝ) *
          ((T / Real.sqrt (N + 1 : ℝ)) ^ 4 / 4) := by
      gcongr

/-- The compact twofold-uniform frequency integral in the Prawitz remainder,
rescaled to the same real frequency variable used by the inversion integral. -/
theorem integral_prawitzReflectionFrequency_eq_scaledDensity
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ z,
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2)
        ∂twofoldUniformKernel) =
      (2 / T : ℝ) •
        ∫ t,
          (twofoldUniformDensityReal (2 * t / T) : ℂ) *
            (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t) := by
  rw [integral_twofoldUniformKernel_eq_integral_density]
  let g : ℝ → ℂ := fun t =>
    (twofoldUniformDensityReal (2 * t / T) : ℂ) *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t)
  have hcomp := Measure.integral_comp_mul_left g (T / 2)
  rw [abs_of_pos (inv_pos.mpr (div_pos hT (by norm_num)))] at hcomp
  have hfun :
      (fun z : ℝ => (twofoldUniformDensityReal z : ℂ) *
        (Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2))) =
        fun z => g ((T / 2) * z) := by
    funext z
    dsimp only [g]
    congr 3 <;> try ring
    congr 1
    field_simp [hT.ne']
  rw [hfun, hcomp]
  congr 1
  field_simp [hT.ne']

/-- The triangular frequency weight obtained by rescaling the density of the
sum of two independent uniform variables. -/
noncomputable def prawitzFejerFrequencyWeight (T t : ℝ) : ℝ :=
  max (1 - |t| / T) 0 / T

theorem prawitzFejerFrequencyWeight_of_mem_Icc
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) (htT : t ≤ T) :
    prawitzFejerFrequencyWeight T t = (1 - t / T) / T := by
  have hratio : t / T ≤ 1 := (div_le_one hT).2 htT
  unfold prawitzFejerFrequencyWeight
  rw [abs_of_nonneg ht0, max_eq_left (sub_nonneg.mpr hratio)]

/-- Coefficient of the signed imaginary characteristic-function discrepancy
after expanding the real part of the Prawitz filter. -/
noncomputable def prawitzCotangentFrequencyCoefficient (T t : ℝ) : ℝ :=
  ((1 - t / T) * prawitzCotFactor (t / T) + t / T) /
    (Real.pi * t)

theorem prawitzFejerFrequencyWeight_nonneg
    {T : ℝ} (hT : 0 < T) (t : ℝ) :
    0 ≤ prawitzFejerFrequencyWeight T t := by
  unfold prawitzFejerFrequencyWeight
  exact div_nonneg (le_max_right _ _) hT.le

theorem prawitzFejerFrequencyWeight_le_inv
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 ≤ t) :
    prawitzFejerFrequencyWeight T t ≤ 1 / T := by
  have hratio0 : 0 ≤ |t| / T := div_nonneg (abs_nonneg t) hT.le
  unfold prawitzFejerFrequencyWeight
  rw [div_le_div_iff_of_pos_right hT]
  exact max_le (by linarith) (by norm_num)

theorem prawitzCotangentFrequencyCoefficient_nonneg
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) :
    0 ≤ prawitzCotangentFrequencyCoefficient T t := by
  unfold prawitzCotangentFrequencyCoefficient
  apply div_nonneg
  · rw [← scaledPrawitzSmoothingFilter_re_of_mem_Ico hT ht.1.le ht.2]
    unfold scaledPrawitzSmoothingFilter
    exact prawitzSmoothingFilter_re_nonneg_of_mem_Ico
      (div_nonneg ht.1.le hT.le) ((div_lt_one hT).2 ht.2)
  · exact mul_nonneg Real.pi_pos.le ht.1.le

theorem prawitzCotangentFrequencyCoefficient_le_inv
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) :
    prawitzCotangentFrequencyCoefficient T t ≤ 1 / (Real.pi * t) := by
  unfold prawitzCotangentFrequencyCoefficient
  have hden : 0 < Real.pi * t := mul_pos Real.pi_pos ht.1
  rw [div_le_div_iff_of_pos_right hden]
  rw [← scaledPrawitzSmoothingFilter_re_of_mem_Ico hT ht.1.le ht.2]
  unfold scaledPrawitzSmoothingFilter
  exact prawitzSmoothingFilter_re_le_one_of_mem_Ico
    (div_nonneg ht.1.le hT.le) ((div_lt_one hT).2 ht.2)

/-- Inside the positive Prawitz window, the surviving imaginary-filter
coefficient is exactly twice the triangular Fejer weight. -/
theorem neg_two_mul_scaledPrawitzSmoothingFilter_im_div_pi
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (q : ℝ) :
    (-2 * (scaledPrawitzSmoothingFilter T t).im * q / t) / Real.pi =
      2 * prawitzFejerFrequencyWeight T t * q := by
  rw [scaledPrawitzSmoothingFilter_im_of_mem_Ico hT ht.1.le ht.2,
    prawitzFejerFrequencyWeight_of_mem_Icc hT ht.1.le ht.2.le]
  field_simp [hT.ne', ht.1.ne', Real.pi_ne_zero]

/-- Inside the positive Prawitz window, the surviving real-filter coefficient
is exactly twice the cotangent frequency coefficient. -/
theorem two_mul_scaledPrawitzSmoothingFilter_re_div_pi
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (q : ℝ) :
    (2 * (scaledPrawitzSmoothingFilter T t).re * q / t) / Real.pi =
      2 * prawitzCotangentFrequencyCoefficient T t * q := by
  rw [scaledPrawitzSmoothingFilter_re_of_mem_Ico hT ht.1.le ht.2]
  unfold prawitzCotangentFrequencyCoefficient
  field_simp [ht.1.ne', Real.pi_ne_zero]

theorem two_div_mul_twofoldUniformDensityReal_scaled
    {T : ℝ} (hT : 0 < T) (t : ℝ) :
    (2 / T) * twofoldUniformDensityReal (2 * t / T) =
      prawitzFejerFrequencyWeight T t := by
  unfold twofoldUniformDensityReal prawitzFejerFrequencyWeight
  rw [abs_div, abs_of_pos hT, abs_mul, abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
  by_cases ht : |t| ≤ T
  · have hratio : |t| / T ≤ 1 := (div_le_one hT).2 ht
    have hleft : 0 ≤ 2 - 2 * |t| / T := by
      calc
        0 ≤ 2 * (1 - |t| / T) :=
          mul_nonneg (by norm_num) (sub_nonneg.mpr hratio)
        _ = 2 - 2 * |t| / T := by ring
    have hright : 0 ≤ 1 - |t| / T := by
      linarith
    rw [max_eq_left hleft, max_eq_left hright]
    field_simp [hT.ne']
    ring
  · have ht' : T < |t| := lt_of_not_ge ht
    have hratio : 1 < |t| / T := (one_lt_div hT).2 ht'
    have hleft : 2 - 2 * |t| / T ≤ 0 := by
      calc
        2 - 2 * |t| / T = 2 * (1 - |t| / T) := by ring
        _ ≤ 0 := mul_nonpos_of_nonneg_of_nonpos (by norm_num)
          (sub_nonpos.mpr hratio.le)
    have hright : 1 - |t| / T ≤ 0 := by
      linarith
    rw [max_eq_right hleft, max_eq_right hright]
    ring

theorem integrable_prawitzFejerFrequencyWeight
    {T : ℝ} (hT : 0 < T) :
    Integrable (prawitzFejerFrequencyWeight T) := by
  have hscale : (2 / T : ℝ) ≠ 0 := div_ne_zero (by norm_num) hT.ne'
  have hdensity : Integrable (fun t : ℝ =>
      twofoldUniformDensityReal ((2 / T) * t)) :=
    integrable_twofoldUniformDensityReal.comp_mul_left' hscale
  have hscaled := hdensity.const_mul (2 / T)
  apply hscaled.congr
  exact ae_of_all volume fun t => by
    dsimp only [Function.comp_apply]
    rw [show (2 / T) * t = 2 * t / T by ring,
      two_div_mul_twofoldUniformDensityReal_scaled hT]

theorem prawitzFejerFrequencyWeight_eq_zero_of_not_mem_Icc
    {T : ℝ} (hT : 0 < T) {t : ℝ} (ht : t ∉ Icc (-T) T) :
    prawitzFejerFrequencyWeight T t = 0 := by
  have habs : T < |t| := by
    by_contra h
    have hle : |t| ≤ T := le_of_not_gt h
    exact ht (abs_le.mp hle)
  have hratio : 1 < |t| / T := (one_lt_div hT).2 habs
  unfold prawitzFejerFrequencyWeight
  rw [max_eq_right (sub_nonpos.mpr hratio.le)]
  simp

theorem prawitzFejerFrequencyWeight_neg (T t : ℝ) :
    prawitzFejerFrequencyWeight T (-t) =
      prawitzFejerFrequencyWeight T t := by
  simp only [prawitzFejerFrequencyWeight, abs_neg]

/-- Pair an integrable function on a symmetric closed window into its positive
and reflected positive-frequency sections. -/
theorem integral_Icc_neg_eq_integral_Icc_add_reflection
    (f : ℝ → ℂ) (hf : Integrable f) {T : ℝ} (hT : 0 ≤ T) :
    (∫ t in Icc (-T) T, f t) =
      ∫ t in Icc 0 T, (f (-t) + f t) := by
  have hfNeg : Integrable (fun t => f (-t)) := hf.comp_neg
  have hFull :
      (∫ t in Icc (-T) T, f t) = ∫ t in (-T)..T, f t := by
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (neg_le_self hT)]
  have hPos :
      (∫ t in Icc 0 T, (f (-t) + f t)) =
        ∫ t in 0..T, (f (-t) + f t) := by
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le hT]
  calc
    (∫ t in Icc (-T) T, f t) = ∫ t in (-T)..T, f t := hFull
    _ = (∫ t in (-T)..0, f t) + ∫ t in 0..T, f t :=
      (intervalIntegral.integral_add_adjacent_intervals
        hf.intervalIntegrable hf.intervalIntegrable).symm
    _ = (∫ t in 0..T, f (-t)) + ∫ t in 0..T, f t := by
      rw [intervalIntegral.integral_comp_neg]
      simp
    _ = ∫ t in 0..T, (f (-t) + f t) :=
      (intervalIntegral.integral_add
        hfNeg.intervalIntegrable hf.intervalIntegrable).symm
    _ = ∫ t in Icc 0 T, (f (-t) + f t) := hPos.symm

theorem const_mul_setIntegral_add_setIntegral_eq_integral_add
    (f g : ℝ → ℂ) (s : Set ℝ)
    (hf : IntegrableOn f s) (hg : IntegrableOn g s) (c : ℂ) :
    c * (∫ t in s, f t) + (∫ t in s, g t) =
      ∫ t in s, (c * f t + g t) := by
  calc
    c * (∫ t in s, f t) + (∫ t in s, g t) =
        (∫ t in s, c * f t) + (∫ t in s, g t) := by
      congr 1
      exact (integral_const_mul
        (μ := volume.restrict s) c f).symm
    _ = ∫ t in s, (c * f t + g t) :=
      (integral_add (hf.const_mul c) hg).symm

theorem integrable_prawitzFejerFrequencyIntegrand
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (fun t =>
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu t)) := by
  have h : Integrable (fun t =>
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu t) *
        (prawitzFejerFrequencyWeight T t : ℂ)) := by
    apply (integrable_prawitzFejerFrequencyWeight hT).ofReal.bdd_mul (c := 1)
    · fun_prop
    · exact ae_of_all volume fun t => by
        rw [norm_mul, Complex.norm_exp_ofReal_mul_I, one_mul]
        exact MeasureTheory.norm_charFun_le_one t
  apply h.congr
  exact ae_of_all volume fun t => by ring

/-- The Prawitz reflection frequency integral with its exact triangular
Lebesgue weight on the whole real line. -/
theorem integral_prawitzReflectionFrequency_eq_triangularIntegral
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ z,
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2)
        ∂twofoldUniformKernel) =
      ∫ t,
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t) := by
  rw [integral_prawitzReflectionFrequency_eq_scaledDensity mu hT x,
    Complex.real_smul]
  calc
    ((2 / T : ℝ) : ℂ) *
        (∫ t,
          (twofoldUniformDensityReal (2 * t / T) : ℂ) *
            (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t)) =
        ∫ t, ((2 / T : ℝ) : ℂ) *
          ((twofoldUniformDensityReal (2 * t / T) : ℂ) *
            (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t)) :=
      (integral_const_mul _ _).symm
    _ = ∫ t,
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t) := by
      apply integral_congr_ae
      exact ae_of_all volume fun t => by
        change ((2 / T : ℝ) : ℂ) *
              ((twofoldUniformDensityReal (2 * t / T) : ℂ) *
                (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun mu t)) =
          (prawitzFejerFrequencyWeight T t : ℂ) *
            (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t)
        rw [← mul_assoc, ← Complex.ofReal_mul,
          two_div_mul_twofoldUniformDensityReal_scaled hT]

/-- The triangular weight vanishes off `[-T,T]`, so the reflection frequency
integral is exactly supported on the Prawitz window. -/
theorem integral_prawitzReflectionFrequency_eq_triangularIntegral_Icc
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ z,
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2)
        ∂twofoldUniformKernel) =
      ∫ t in Icc (-T) T,
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t) := by
  rw [integral_prawitzReflectionFrequency_eq_triangularIntegral mu hT x,
    ← integral_indicator measurableSet_Icc]
  apply integral_congr_ae
  exact ae_of_all volume fun t => by
    by_cases ht : t ∈ Icc (-T) T
    · simp only [Set.indicator_of_mem ht]
    · rw [Set.indicator_of_notMem ht]
      change (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu t) = 0
      rw [prawitzFejerFrequencyWeight_eq_zero_of_not_mem_Icc hT ht]
      simp only [Complex.ofReal_zero, zero_mul]

/-- Positive-frequency pairing of the exact Prawitz reflection remainder. -/
theorem integral_prawitzReflectionFrequency_eq_pairedTriangularIntegral
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ z,
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu (T * z / 2)
        ∂twofoldUniformKernel) =
      ∫ t in Icc 0 T,
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t) := by
  rw [integral_prawitzReflectionFrequency_eq_triangularIntegral_Icc mu hT x]
  let f : ℝ → ℂ := fun t =>
    (prawitzFejerFrequencyWeight T t : ℂ) *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t)
  have hf : Integrable f := by
    simpa only [f] using integrable_prawitzFejerFrequencyIntegrand mu hT x
  rw [integral_Icc_neg_eq_integral_Icc_add_reflection f hf hT.le]
  apply integral_congr_ae
  filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
  dsimp only [f]
  rw [prawitzFejerFrequencyWeight_neg]
  rw [show -(x * -t) = x * t by ring]
  ring

theorem integrable_pairedPrawitzReflectionFrequencyIntegrand
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (fun t =>
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t)) := by
  let r : ℝ → ℂ := fun t =>
    (prawitzFejerFrequencyWeight T t : ℂ) *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t)
  have hr : Integrable r := by
    simpa only [r] using
      integrable_prawitzFejerFrequencyIntegrand mu hT x
  apply (hr.comp_neg.add hr).congr
  exact ae_of_all volume fun t => by
    simp only [Pi.add_apply]
    dsimp only [r]
    rw [prawitzFejerFrequencyWeight_neg,
      show -(x * -t) = x * t by ring]
    ring

/-- For an arbitrary probability law, the real part of the paired triangular
reflection integrand is twice its positive-frequency phase coordinate. -/
theorem re_pairedPrawitzReflectionFrequencyIntegrand_charFun
    (mu : Measure ℝ) (T x t : ℝ) :
    ((prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t)).re =
      2 * prawitzFejerFrequencyWeight T t *
        (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu t).re := by
  rw [MeasureTheory.charFun_neg]
  simp only [Complex.mul_re, Complex.add_re, Complex.ofReal_re,
    Complex.ofReal_im, Complex.exp_ofReal_mul_I_re,
    Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg,
    Complex.conj_re, Complex.conj_im]
  ring

/-- The positive-frequency Fejér phase integral is exactly one half of a
translated sinc-squared average against the probability law. -/
theorem two_mul_integral_prawitzFejer_phase_re_eq_sincSquare
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    2 * (∫ t in Icc (0 : ℝ) T,
      prawitzFejerFrequencyWeight T t *
        (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu t).re) =
      ∫ y, sincSquareWeight (T * (y - x) / 2) ∂mu := by
  have hsinc := integral_sincSquareWeight_eq_re_frequencyIntegral mu T x
  rw [integral_prawitzReflectionFrequency_eq_pairedTriangularIntegral
    mu hT x] at hsinc
  have hpair : IntegrableOn (fun t =>
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t)) (Icc (0 : ℝ) T) :=
    (integrable_pairedPrawitzReflectionFrequencyIntegrand mu hT x).integrableOn
  calc
    2 * (∫ t in Icc (0 : ℝ) T,
        prawitzFejerFrequencyWeight T t *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t).re) =
      ∫ t in Icc (0 : ℝ) T,
        2 * (prawitzFejerFrequencyWeight T t *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t).re) := by
          rw [integral_const_mul]
    _ = ∫ t in Icc (0 : ℝ) T,
        ((prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t)).re := by
      apply integral_congr_ae
      filter_upwards with t
      symm
      simpa only [mul_assoc] using
        re_pairedPrawitzReflectionFrequencyIntegrand_charFun mu T x t
    _ = (∫ t in Icc (0 : ℝ) T,
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun mu t)).re := integral_re hpair
    _ = _ := hsinc.symm

/-- The real positive-frequency Fejér phase integral is nonnegative for
every probability law. -/
theorem integral_prawitzFejer_phase_re_nonneg
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    0 ≤ ∫ t in Icc (0 : ℝ) T,
      prawitzFejerFrequencyWeight T t *
        (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun mu t).re := by
  have hsinc : 0 ≤ ∫ y, sincSquareWeight (T * (y - x) / 2) ∂mu := by
    exact integral_nonneg fun y => sincSquareWeight_nonneg _
  have heq := two_mul_integral_prawitzFejer_phase_re_eq_sincSquare
    mu hT x
  nlinarith

/-- The standard-normal characteristic function after multiplication by the
negative spatial phase has the expected Gaussian cosine real part. -/
theorem standardNormal_negativePhase_charFun_re (x t : ℝ) :
    (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
      MeasureTheory.charFun
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t).re =
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun]
  have hpos :
      -((t : ℂ) ^ 2) / 2 = (((-(t ^ 2) / 2 : ℝ) : ℂ)) := by
    push_cast
    ring_nf
  rw [hpos, ← Complex.ofReal_exp]
  simp only [Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im,
    Complex.exp_ofReal_mul_I_re, Complex.exp_ofReal_mul_I_im,
    Real.cos_neg, Real.sin_neg]
  ring_nf

/-- Exact real part of the paired triangular reflection integrand for the
standard normal target. -/
theorem re_pairedPrawitzReflectionFrequencyIntegrand_standardNormal
    (T x t : ℝ) :
    ((prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re =
      2 * prawitzFejerFrequencyWeight T t *
        Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun,
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun]
  have hneg :
      -(((-t : ℝ) : ℂ) ^ 2) / 2 = (((-(t ^ 2) / 2 : ℝ) : ℂ)) := by
    push_cast
    ring
  have hpos :
      -((t : ℂ) ^ 2) / 2 = (((-(t ^ 2) / 2 : ℝ) : ℂ)) := by
    push_cast
    ring
  rw [hneg, hpos, ← Complex.ofReal_exp]
  simp only [Complex.mul_re, Complex.add_re, Complex.ofReal_re,
    Complex.ofReal_im, Complex.exp_ofReal_mul_I_re, zero_mul, sub_zero,
    add_zero, mul_zero]
  rw [Real.cos_neg]
  ring

/-- The exact Gaussian part of the paired positive-frequency Prawitz
integrand, before replacing its oscillatory cosine by the bound one. -/
noncomputable def prawitzGaussianCosineRemainder (T x : ℝ) : ℝ :=
  ∫ t in Icc (0 : ℝ) T,
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)

/-- The signed Fejér part of the exact bidirectional law-phase envelope. -/
noncomputable def prawitzLawPhaseFejerIntegral
    (mu : Measure ℝ) (T x : ℝ) : ℝ :=
  ∫ t in Icc (0 : ℝ) T,
    let d : ℂ := MeasureTheory.charFun mu t -
      MeasureTheory.charFun
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
    let q : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * d
    prawitzFejerFrequencyWeight T t *
      (q.re + 2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))

/-- The signed cotangent part of the exact bidirectional law-phase
envelope. -/
noncomputable def prawitzLawPhaseCotangentIntegral
    (mu : Measure ℝ) (T x : ℝ) : ℝ :=
  ∫ t in Icc (0 : ℝ) T,
    let d : ℂ := MeasureTheory.charFun mu t -
      MeasureTheory.charFun
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
    let q : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * d
    (-prawitzCotangentFrequencyCoefficient T t * q.im)

/-- The exact bidirectional law-phase envelope: its signed Fejér coordinate
plus the absolute value of its signed cotangent coordinate. -/
noncomputable def prawitzLawPhaseEnvelope
    (mu : Measure ℝ) (T x : ℝ) : ℝ :=
  prawitzLawPhaseFejerIntegral mu T x +
    |prawitzLawPhaseCotangentIntegral mu T x|

/-- Half of the exact Gaussian cosine remainder is a lower bound on the Fejér
part of the bidirectional law-phase envelope. -/
theorem half_prawitzGaussianCosineRemainder_le_lawPhaseFejerIntegral
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (1 / 2 : ℝ) * prawitzGaussianCosineRemainder T x ≤
      prawitzLawPhaseFejerIntegral mu T x := by
  unfold prawitzLawPhaseFejerIntegral
  let fmu : ℝ → ℝ := fun t =>
    prawitzFejerFrequencyWeight T t *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t).re
  let fnormal : ℝ → ℝ := fun t =>
    prawitzFejerFrequencyWeight T t *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t).re
  have hfmu : IntegrableOn fmu (Icc (0 : ℝ) T) := by
    have h : IntegrableOn (fun t =>
        ((prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t)).re) (Icc (0 : ℝ) T) :=
      (integrable_prawitzFejerFrequencyIntegrand mu hT x).re.integrableOn
    simpa only [fmu, Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im,
      zero_mul, sub_zero] using h
  have hfnormal : IntegrableOn fnormal (Icc (0 : ℝ) T) := by
    have h : IntegrableOn (fun t =>
        ((prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re)
        (Icc (0 : ℝ) T) :=
      (integrable_prawitzFejerFrequencyIntegrand
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).re.integrableOn
    simpa only [fnormal, Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im,
      zero_mul, sub_zero] using h
  have hfmu0 : 0 ≤ ∫ t in Icc (0 : ℝ) T, fmu t := by
    simpa only [fmu] using integral_prawitzFejer_phase_re_nonneg mu hT x
  have htarget :
      (∫ t in Icc (0 : ℝ) T,
        let d : ℂ := MeasureTheory.charFun mu t -
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
        let q : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * d
        prawitzFejerFrequencyWeight T t *
          (q.re + 2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) =
        (∫ t in Icc (0 : ℝ) T, fmu t) +
          ∫ t in Icc (0 : ℝ) T, fnormal t := by
    rw [← integral_add hfmu hfnormal]
    apply integral_congr_ae
    filter_upwards with t
    dsimp only [fmu, fnormal]
    rw [mul_sub, Complex.sub_re, mul_comm t x,
      standardNormal_negativePhase_charFun_re]
    ring
  have hgauss :
      (1 / 2 : ℝ) * prawitzGaussianCosineRemainder T x =
        ∫ t in Icc (0 : ℝ) T, fnormal t := by
    unfold prawitzGaussianCosineRemainder
    rw [show (fun t : ℝ =>
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) =
        fun t => 2 * fnormal t by
      funext t
      dsimp only [fnormal]
      rw [standardNormal_negativePhase_charFun_re]
      ring,
      integral_const_mul]
    ring
  rw [htarget, hgauss]
  linarith

/-- Twice the Fejér coordinate of the exact law-phase envelope is the sum of
the translated sinc-squared smoothing masses of the law and the standard
normal. -/
theorem two_mul_prawitzLawPhaseFejerIntegral_eq_sincSquare_add
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    2 * prawitzLawPhaseFejerIntegral mu T x =
      (∫ y, sincSquareWeight (T * (y - x) / 2) ∂mu) +
      ∫ y, sincSquareWeight (T * (y - x) / 2)
        ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  let fmu : ℝ → ℝ := fun t =>
    prawitzFejerFrequencyWeight T t *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t).re
  let fnormal : ℝ → ℝ := fun t =>
    prawitzFejerFrequencyWeight T t *
      (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t).re
  have hfmu : IntegrableOn fmu (Icc (0 : ℝ) T) := by
    have h : IntegrableOn (fun t =>
        ((prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun mu t)).re) (Icc (0 : ℝ) T) :=
      (integrable_prawitzFejerFrequencyIntegrand mu hT x).re.integrableOn
    simpa only [fmu, Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im,
      zero_mul, sub_zero] using h
  have hfnormal : IntegrableOn fnormal (Icc (0 : ℝ) T) := by
    have h : IntegrableOn (fun t =>
        ((prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re)
        (Icc (0 : ℝ) T) :=
      (integrable_prawitzFejerFrequencyIntegrand
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).re.integrableOn
    simpa only [fnormal, Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im,
      zero_mul, sub_zero] using h
  have hsplit : prawitzLawPhaseFejerIntegral mu T x =
      (∫ t in Icc (0 : ℝ) T, fmu t) +
        ∫ t in Icc (0 : ℝ) T, fnormal t := by
    unfold prawitzLawPhaseFejerIntegral
    rw [← integral_add hfmu hfnormal]
    apply integral_congr_ae
    filter_upwards with t
    dsimp only [fmu, fnormal]
    rw [mul_sub, Complex.sub_re, mul_comm t x,
      standardNormal_negativePhase_charFun_re]
    ring
  rw [hsplit]
  calc
    2 * ((∫ t in Icc (0 : ℝ) T, fmu t) +
        ∫ t in Icc (0 : ℝ) T, fnormal t) =
        2 * (∫ t in Icc (0 : ℝ) T, fmu t) +
          2 * (∫ t in Icc (0 : ℝ) T, fnormal t) := by ring
    _ = _ := by
      rw [show 2 * (∫ t in Icc (0 : ℝ) T, fmu t) =
          ∫ y, sincSquareWeight (T * (y - x) / 2) ∂mu by
        simpa only [fmu] using
          two_mul_integral_prawitzFejer_phase_re_eq_sincSquare mu hT x,
        show 2 * (∫ t in Icc (0 : ℝ) T, fnormal t) =
          ∫ y, sincSquareWeight (T * (y - x) / 2)
            ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw by
        simpa only [fnormal] using
          two_mul_integral_prawitzFejer_phase_re_eq_sincSquare
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x]

/-- For every fixed cutoff, the exact Gaussian cosine remainder is continuous
in the spatial parameter. -/
theorem continuous_prawitzGaussianCosineRemainder (T : ℝ) :
    Continuous (prawitzGaussianCosineRemainder T) := by
  unfold prawitzGaussianCosineRemainder
  apply continuous_parametric_integral_of_continuous
  · unfold prawitzFejerFrequencyWeight
    fun_prop
  · exact isCompact_Icc

/-- On every nonempty compact spatial interval, the exact Gaussian cosine
remainder attains its maximum. -/
theorem exists_prawitzGaussianCosineRemainder_isMaxOn_Icc
    (T : ℝ) {a b : ℝ} (hab : a ≤ b) :
    ∃ x ∈ Icc a b, ∀ y ∈ Icc a b,
      prawitzGaussianCosineRemainder T y ≤
        prawitzGaussianCosineRemainder T x := by
  simpa only [IsMaxOn] using
    isCompact_Icc.exists_isMaxOn (nonempty_Icc.mpr hab)
      (continuous_prawitzGaussianCosineRemainder T).continuousOn

/-- For a fixed Prawitz window, the exact Gaussian cosine remainder vanishes
in the positive spatial tail.  Thus the obstruction at `x = 0` is not a
uniform spatial floor; a sharper comparison may localize the remaining joint
frequency/spatial estimate to a bounded `x`-region. -/
theorem tendsto_prawitzGaussianCosineRemainder_atTop
    (T : ℝ) :
    Tendsto (prawitzGaussianCosineRemainder T) atTop (𝓝 0) := by
  let f : ℝ → ℝ := (Icc (0 : ℝ) T).indicator fun t =>
    2 * prawitzFejerFrequencyWeight T t * Real.exp (-(t ^ 2) / 2)
  have hf : Integrable f := by
    have hamp : IntegrableOn (fun t : ℝ =>
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2)) (Icc (0 : ℝ) T) := by
      apply Continuous.integrableOn_Icc
      unfold prawitzFejerFrequencyWeight
      fun_prop
    exact hamp.integrable_indicator measurableSet_Icc
  have hscale :
      Tendsto (fun x : ℝ => (2 * Real.pi)⁻¹ * x) atTop atTop :=
    tendsto_id.const_mul_atTop
      (inv_pos.mpr (mul_pos (by norm_num) Real.pi_pos))
  have hrl :=
    (tendsto_integral_cos_mul_atTop_of_integrable f hf).comp hscale
  convert hrl using 1
  funext x
  unfold prawitzGaussianCosineRemainder
  rw [← integral_indicator measurableSet_Icc]
  apply integral_congr_ae
  filter_upwards with t
  by_cases ht : t ∈ Icc (0 : ℝ) T
  · rw [Set.indicator_of_mem ht]
    simp only [f, Set.indicator_of_mem ht]
    have harg :
        2 * Real.pi * t * ((2 * Real.pi)⁻¹ * x) = x * t := by
      field_simp [Real.pi_ne_zero]
    rw [harg]
    ring
  · simp only [Set.indicator_of_notMem ht, f, mul_zero]

/-- The exact Gaussian cosine remainder is even in the spatial parameter. -/
theorem prawitzGaussianCosineRemainder_neg (T x : ℝ) :
    prawitzGaussianCosineRemainder T (-x) =
      prawitzGaussianCosineRemainder T x := by
  unfold prawitzGaussianCosineRemainder
  apply integral_congr_ae
  filter_upwards with t
  rw [neg_mul, Real.cos_neg]

/-- For a fixed Prawitz window, the exact Gaussian cosine remainder also
vanishes in the negative spatial tail. -/
theorem tendsto_prawitzGaussianCosineRemainder_atBot
    (T : ℝ) :
    Tendsto (prawitzGaussianCosineRemainder T) atBot (𝓝 0) := by
  have h := (tendsto_prawitzGaussianCosineRemainder_atTop T).comp
    tendsto_neg_atBot_atTop
  refine Tendsto.congr' ?_ h
  filter_upwards with x
  simpa only [Function.comp_apply] using
    prawitzGaussianCosineRemainder_neg T x

/-- The forward Prawitz error with its characteristic-function phase and the
exact Gaussian cosine term kept together. -/
noncomputable def prawitzPhasePreservingRemainder
    (mu : Measure ℝ) (T x : ℝ) : ℝ :=
  ((Complex.I / (Real.pi : ℝ)) *
      (∫ t in Icc (0 : ℝ) T,
        (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
          prawitzPVIntegrand T
            (MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
            x t))).re +
    prawitzGaussianCosineRemainder T x

/-- The zero-spatial positive-frequency amplitude of a Prawitz difference. -/
noncomputable def prawitzZeroPhaseDifference
    (mu nu : Measure ℝ) (T t : ℝ) : ℂ :=
  prawitzPVIntegrand T (MeasureTheory.charFun mu) 0 t -
    prawitzPVIntegrand T (MeasureTheory.charFun nu) 0 t

/-- Real coordinate of the zero-phase Prawitz amplitude, retaining the real
and imaginary filter/discrepancy interaction exactly. -/
theorem prawitzZeroPhaseDifference_re
    (mu nu : Measure ℝ) (T t : ℝ) :
    (prawitzZeroPhaseDifference mu nu T t).re =
      ((scaledPrawitzSmoothingFilter T t).re *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).re -
        (scaledPrawitzSmoothingFilter T t).im *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).im) / t := by
  unfold prawitzZeroPhaseDifference prawitzPVIntegrand prawitzPVNumerator
  simp only [mul_zero, neg_zero, Complex.ofReal_zero, zero_mul,
    Complex.exp_zero, one_mul]
  rw [← sub_div, ← mul_sub]
  rw [Complex.div_ofReal_re, Complex.mul_re]

/-- Imaginary coordinate of the zero-phase Prawitz amplitude, retaining the
real and imaginary filter/discrepancy interaction exactly. -/
theorem prawitzZeroPhaseDifference_im
    (mu nu : Measure ℝ) (T t : ℝ) :
    (prawitzZeroPhaseDifference mu nu T t).im =
      ((scaledPrawitzSmoothingFilter T t).re *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).im +
        (scaledPrawitzSmoothingFilter T t).im *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).re) / t := by
  unfold prawitzZeroPhaseDifference prawitzPVIntegrand prawitzPVNumerator
  simp only [mul_zero, neg_zero, Complex.ofReal_zero, zero_mul,
    Complex.exp_zero, one_mul]
  rw [← sub_div, ← mul_sub]
  rw [Complex.div_ofReal_im, Complex.mul_im]

/-- Interior-window real coordinate after substituting the exact cotangent
and triangular coordinates of the scaled Prawitz filter. -/
theorem prawitzZeroPhaseDifference_re_of_mem_Ioo
    (mu nu : Measure ℝ) {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) :
    (prawitzZeroPhaseDifference mu nu T t).re =
      (((1 - t / T) * prawitzCotFactor (t / T) + t / T) *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).re +
        ((1 - t / T) * Real.pi * (t / T)) *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).im) / t := by
  rw [prawitzZeroPhaseDifference_re mu nu T t,
    scaledPrawitzSmoothingFilter_re_of_mem_Ico hT ht.1.le ht.2,
    scaledPrawitzSmoothingFilter_im_of_mem_Ico hT ht.1.le ht.2]
  ring

/-- Interior-window imaginary coordinate after substituting the exact
cotangent and triangular coordinates of the scaled Prawitz filter. -/
theorem prawitzZeroPhaseDifference_im_of_mem_Ioo
    (mu nu : Measure ℝ) {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) :
    (prawitzZeroPhaseDifference mu nu T t).im =
      (((1 - t / T) * prawitzCotFactor (t / T) + t / T) *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).im -
        ((1 - t / T) * Real.pi * (t / T)) *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t).re) / t := by
  rw [prawitzZeroPhaseDifference_im mu nu T t,
    scaledPrawitzSmoothingFilter_re_of_mem_Ico hT ht.1.le ht.2,
    scaledPrawitzSmoothingFilter_im_of_mem_Ico hT ht.1.le ht.2]
  ring

/-- Exact normalized-iid characteristic-function discrepancy as the power of
the common summand characteristic function minus the real Gaussian factor. -/
theorem charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (t : ℝ) :
    MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X hX N).aemeasurable : Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t =
      (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1) -
        ((Real.exp (-(t ^ 2) / 2) : ℝ) : ℂ) := by
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_charFun,
    NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw_normalizedCenteredIidSum
      X hX hIndep hIdent N t]
  have hcf := NumStability.HDP.Scalar.LimitTheorems.charFun_probabilityLaw
    (X 0) (hX 0).aemeasurable (t / Real.sqrt (N + 1 : ℝ))
  rw [hcf]
  congr 2
  · apply integral_congr_ae
    filter_upwards with omega
    congr 1
    push_cast
    ring
  · rw [Complex.ofReal_exp]
    congr 1
    push_cast
    ring

/-- The zero-phase Prawitz difference is the scaled filter times the raw
characteristic-function discrepancy, divided by the real frequency. -/
theorem prawitzZeroPhaseDifference_eq_filter_mul_charFun_sub
    (mu nu : Measure ℝ) (T t : ℝ) :
    prawitzZeroPhaseDifference mu nu T t =
      scaledPrawitzSmoothingFilter T t *
        (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t) / t := by
  unfold prawitzZeroPhaseDifference prawitzPVIntegrand prawitzPVNumerator
  simp only [mul_zero, neg_zero, Complex.ofReal_zero, zero_mul,
    Complex.exp_zero, one_mul]
  rw [← sub_div, ← mul_sub]

/-- Exact normalized-iid discrepancy factorization into the one-step Taylor
residual, its full complex geometric coefficient, and the real Gaussian-proxy
correction. -/
theorem charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_taylorGeomSum
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (t : ℝ) :
    MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X hX N).aemeasurable : Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t =
      (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) -
        ((1 - (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 2 : ℝ) : ℂ)) *
        complexPowerRealBaselineGeomSum
          (MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ)
            (t / Real.sqrt (N + 1 : ℝ)))
          (1 - (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 2) (N + 1) +
        ((((1 - (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 2) ^ (N + 1) -
          Real.exp (-(t ^ 2) / 2) : ℝ)) : ℂ) := by
  rw [charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    X hX hIndep hIdent N t]
  exact complex_pow_sub_real_eq_taylorGeomSum _ _ _ _

/-- Exact Taylor-product factorization inside the zero-phase Prawitz amplitude
for a normalized centered iid sum. No absolute value or complex norm is taken. -/
theorem prawitzZeroPhaseDifference_normalizedCenteredIidSum_eq_taylorGeomSum
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (T t : ℝ) :
    prawitzZeroPhaseDifference
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t =
      scaledPrawitzSmoothingFilter T t *
        ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ)
            (t / Real.sqrt (N + 1 : ℝ)) -
          ((1 - (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 2 : ℝ) : ℂ)) *
          complexPowerRealBaselineGeomSum
            (MeasureTheory.charFun
              (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                (X 0) (hX 0).aemeasurable : Measure ℝ)
              (t / Real.sqrt (N + 1 : ℝ)))
            (1 - (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 2) (N + 1) +
          ((((1 - (t / Real.sqrt (N + 1 : ℝ)) ^ 2 / 2) ^ (N + 1) -
            Real.exp (-(t ^ 2) / 2) : ℝ)) : ℂ)) / t := by
  rw [prawitzZeroPhaseDifference_eq_filter_mul_charFun_sub,
    charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_taylorGeomSum
      X hX hIndep hIdent N t]

/-- Real coordinate of a filtered complex Taylor residual times its geometric
coefficient, with a separate real correction and real frequency divisor. -/
theorem complex_mul_taylorProduct_div_re
    (k r s : ℂ) (c t : ℝ) :
    (k * (r * s + (c : ℂ)) / t).re =
      (k.re * (r.re * s.re - r.im * s.im + c) -
        k.im * (r.re * s.im + r.im * s.re)) / t := by
  rw [Complex.div_ofReal_re]
  simp only [Complex.mul_re, Complex.mul_im, Complex.add_re, Complex.add_im,
    Complex.ofReal_re, Complex.ofReal_im]
  ring

/-- Imaginary coordinate of a filtered complex Taylor residual times its
geometric coefficient, with a separate real correction and real frequency
divisor. -/
theorem complex_mul_taylorProduct_div_im
    (k r s : ℂ) (c t : ℝ) :
    (k * (r * s + (c : ℂ)) / t).im =
      (k.re * (r.re * s.im + r.im * s.re) +
        k.im * (r.re * s.re - r.im * s.im + c)) / t := by
  rw [Complex.div_ofReal_im]
  simp only [Complex.mul_re, Complex.mul_im, Complex.add_re, Complex.add_im,
    Complex.ofReal_re, Complex.ofReal_im]
  ring

/-- Real coordinate of a filtered conjugated complex Taylor residual times
its geometric coefficient, with a separate real correction and divisor. -/
theorem complex_mul_conj_taylorProduct_div_re
    (k r s : ℂ) (c t : ℝ) :
    (k * (starRingEnd ℂ) (r * s + (c : ℂ)) / t).re =
      (k.re * (r.re * s.re - r.im * s.im + c) +
        k.im * (r.re * s.im + r.im * s.re)) / t := by
  rw [Complex.div_ofReal_re]
  simp only [Complex.mul_re, Complex.mul_im, map_add, map_mul,
    Complex.conj_re, Complex.conj_im, Complex.add_re, Complex.add_im,
    Complex.ofReal_re, Complex.ofReal_im]
  ring

/-- Imaginary coordinate of a filtered conjugated complex Taylor residual
times its geometric coefficient, with a separate real correction and divisor. -/
theorem complex_mul_conj_taylorProduct_div_im
    (k r s : ℂ) (c t : ℝ) :
    (k * (starRingEnd ℂ) (r * s + (c : ℂ)) / t).im =
      (-k.re * (r.re * s.im + r.im * s.re) +
        k.im * (r.re * s.re - r.im * s.im + c)) / t := by
  rw [Complex.div_ofReal_im]
  simp only [Complex.mul_im, map_add, map_mul,
    Complex.conj_re, Complex.conj_im, Complex.add_re, Complex.add_im,
    Complex.mul_re, Complex.ofReal_re, Complex.ofReal_im]
  ring

/-- Adding the forward and reflected Taylor phases cancels every real-filter
contribution, leaving only the imaginary filter coordinate. -/
theorem prawitzTaylorPhaseCoordinates_add
    (k r s : ℂ) (c t theta : ℝ) :
    (Real.sin theta *
          ((k.re * (r.re * s.re - r.im * s.im + c) -
              k.im * (r.re * s.im + r.im * s.re)) / t) -
        Real.cos theta *
          ((k.re * (r.re * s.im + r.im * s.re) +
              k.im * (r.re * s.re - r.im * s.im + c)) / t)) +
      (-Real.sin theta *
          ((k.re * (r.re * s.re - r.im * s.im + c) +
              k.im * (r.re * s.im + r.im * s.re)) / t) -
        Real.cos theta *
          ((-k.re * (r.re * s.im + r.im * s.re) +
              k.im * (r.re * s.re - r.im * s.im + c)) / t)) =
      -2 * k.im *
        (Real.sin theta * (r.re * s.im + r.im * s.re) +
          Real.cos theta * (r.re * s.re - r.im * s.im + c)) / t := by
  ring

/-- Subtracting the reflected Taylor phase from the forward phase cancels
every imaginary-filter contribution, leaving only the real filter coordinate. -/
theorem prawitzTaylorPhaseCoordinates_sub
    (k r s : ℂ) (c t theta : ℝ) :
    (Real.sin theta *
          ((k.re * (r.re * s.re - r.im * s.im + c) -
              k.im * (r.re * s.im + r.im * s.re)) / t) -
        Real.cos theta *
          ((k.re * (r.re * s.im + r.im * s.re) +
              k.im * (r.re * s.re - r.im * s.im + c)) / t)) -
      (-Real.sin theta *
          ((k.re * (r.re * s.re - r.im * s.im + c) +
              k.im * (r.re * s.im + r.im * s.re)) / t) -
        Real.cos theta *
          ((-k.re * (r.re * s.im + r.im * s.re) +
              k.im * (r.re * s.re - r.im * s.im + c)) / t)) =
      2 * k.re *
        (Real.sin theta * (r.re * s.re - r.im * s.im + c) -
          Real.cos theta * (r.re * s.im + r.im * s.re)) / t := by
  ring

/-- Exact real coordinate of the normalized-iid Prawitz Taylor-product
amplitude. The filter, residual, geometric coefficient, and real correction
remain separately visible. -/
theorem prawitzZeroPhaseDifference_normalizedCenteredIidSum_re_eq_taylorCoordinates
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (T t : ℝ) :
    let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
    let phi : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (X 0) (hX 0).aemeasurable : Measure ℝ) u
    let a : ℝ := 1 - u ^ 2 / 2
    let r : ℂ := phi - (a : ℂ)
    let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
    let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
    (prawitzZeroPhaseDifference
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).re =
      ((scaledPrawitzSmoothingFilter T t).re *
          (r.re * s.re - r.im * s.im + c) -
        (scaledPrawitzSmoothingFilter T t).im *
          (r.re * s.im + r.im * s.re)) / t := by
  dsimp only
  rw [prawitzZeroPhaseDifference_normalizedCenteredIidSum_eq_taylorGeomSum
    X hX hIndep hIdent N T t]
  exact complex_mul_taylorProduct_div_re _ _ _ _ _

/-- Exact imaginary coordinate of the normalized-iid Prawitz Taylor-product
amplitude. The filter, residual, geometric coefficient, and real correction
remain separately visible. -/
theorem prawitzZeroPhaseDifference_normalizedCenteredIidSum_im_eq_taylorCoordinates
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (T t : ℝ) :
    let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
    let phi : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (X 0) (hX 0).aemeasurable : Measure ℝ) u
    let a : ℝ := 1 - u ^ 2 / 2
    let r : ℂ := phi - (a : ℂ)
    let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
    let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
    (prawitzZeroPhaseDifference
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).im =
      ((scaledPrawitzSmoothingFilter T t).re *
          (r.re * s.im + r.im * s.re) +
        (scaledPrawitzSmoothingFilter T t).im *
          (r.re * s.re - r.im * s.im + c)) / t := by
  dsimp only
  rw [prawitzZeroPhaseDifference_normalizedCenteredIidSum_eq_taylorGeomSum
    X hX hIndep hIdent N T t]
  exact complex_mul_taylorProduct_div_im _ _ _ _ _

/-- Real coordinate of the exact normalized-iid characteristic-function
discrepancy. -/
theorem charFun_normalizedCenteredIidSum_sub_standardNormalLaw_re
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (t : ℝ) :
    (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X hX N).aemeasurable : Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t).re =
      ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1)).re -
        Real.exp (-(t ^ 2) / 2) := by
  rw [charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    X hX hIndep hIdent N t]
  simp only [Complex.sub_re, Complex.ofReal_re]

/-- Imaginary coordinate of the exact normalized-iid characteristic-function
discrepancy; the Gaussian factor has zero imaginary part. -/
theorem charFun_normalizedCenteredIidSum_sub_standardNormalLaw_im
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (t : ℝ) :
    (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X hX N).aemeasurable : Measure ℝ) t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t).im =
      ((MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1)).im := by
  rw [charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    X hX hIndep hIdent N t]
  simp only [Complex.sub_im, Complex.ofReal_im, sub_zero]

/-- Interior Prawitz real amplitude for a normalized iid sum, with the powered
summand characteristic function and Gaussian factor exposed exactly. -/
theorem prawitzZeroPhaseDifference_normalizedCenteredIidSum_re_of_mem_Ioo
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) :
    (prawitzZeroPhaseDifference
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).re =
      (((1 - t / T) * prawitzCotFactor (t / T) + t / T) *
          (((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ)
            (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1)).re -
              Real.exp (-(t ^ 2) / 2)) +
        ((1 - t / T) * Real.pi * (t / T)) *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ)
            (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1)).im) / t := by
  rw [prawitzZeroPhaseDifference_re_of_mem_Ioo _ _ hT ht,
    charFun_normalizedCenteredIidSum_sub_standardNormalLaw_re
      X hX hIndep hIdent N t,
    charFun_normalizedCenteredIidSum_sub_standardNormalLaw_im
      X hX hIndep hIdent N t]

/-- Interior Prawitz imaginary amplitude for a normalized iid sum, with the
powered summand characteristic function and Gaussian factor exposed exactly. -/
theorem prawitzZeroPhaseDifference_normalizedCenteredIidSum_im_of_mem_Ioo
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) :
    (prawitzZeroPhaseDifference
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X hX N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).im =
      (((1 - t / T) * prawitzCotFactor (t / T) + t / T) *
          ((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ)
            (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1)).im -
        ((1 - t / T) * Real.pi * (t / T)) *
          (((MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ)
            (t / Real.sqrt (N + 1 : ℝ))) ^ (N + 1)).re -
              Real.exp (-(t ^ 2) / 2))) / t := by
  rw [prawitzZeroPhaseDifference_im_of_mem_Ioo _ _ hT ht,
    charFun_normalizedCenteredIidSum_sub_standardNormalLaw_re
      X hX hIndep hIdent N t,
    charFun_normalizedCenteredIidSum_sub_standardNormalLaw_im
      X hX hIndep hIdent N t]

/-- Exact sine/cosine coordinate expansion of the forward phase-preserving
Prawitz remainder. -/
theorem prawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzPhasePreservingRemainder mu T x =
      ∫ t in Icc (0 : ℝ) T,
        (Real.sin (t * x) *
              (prawitzZeroPhaseDifference mu
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).re -
            Real.cos (t * x) *
              (prawitzZeroPhaseDifference mu
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).im) /
            Real.pi +
          2 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  let z : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
      prawitzPVIntegrand T
        (MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x t
  let P : ℝ → ℂ := fun t => (Complex.I / (Real.pi : ℝ)) * z t
  let G : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hz : IntegrableOn z (Icc (0 : ℝ) T) := by
    simpa only [z] using integrableOn_prawitzPVIntegrand_charFun_sub
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  have hP : IntegrableOn P (Icc (0 : ℝ) T) := by
    simpa only [P] using hz.const_mul (Complex.I / (Real.pi : ℝ))
  have hG : IntegrableOn G (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [G]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  unfold prawitzPhasePreservingRemainder prawitzGaussianCosineRemainder
  change ((Complex.I / (Real.pi : ℝ)) *
      (∫ t in Icc (0 : ℝ) T, z t)).re +
        ∫ t in Icc (0 : ℝ) T, G t = _
  calc
    ((Complex.I / (Real.pi : ℝ)) *
        (∫ t in Icc (0 : ℝ) T, z t)).re +
          ∫ t in Icc (0 : ℝ) T, G t =
        (∫ t in Icc (0 : ℝ) T, P t).re +
          ∫ t in Icc (0 : ℝ) T, G t := by
      congr 1
      exact congrArg Complex.re
        (integral_const_mul
          (μ := volume.restrict (Icc (0 : ℝ) T))
          (Complex.I / (Real.pi : ℝ)) z).symm
    _ = (∫ t in Icc (0 : ℝ) T, (P t).re) +
          ∫ t in Icc (0 : ℝ) T, G t := by
      exact congrArg (fun z : ℝ =>
        z + ∫ t in Icc (0 : ℝ) T, G t) (integral_re hP).symm
    _ = ∫ t in Icc (0 : ℝ) T, ((P t).re + G t) :=
      (integral_add hP.re hG).symm
    _ = _ := by
      apply integral_congr_ae
      filter_upwards with t
      dsimp only [P, z, G, prawitzZeroPhaseDifference]
      rw [prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero]
      rw [Complex.mul_re, Complex.div_ofReal_re,
        Complex.div_ofReal_im, Complex.mul_re, Complex.mul_im]
      simp only [Complex.I_re, Complex.I_im, zero_div, one_div,
        zero_mul, Complex.exp_ofReal_mul_I_re,
        Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg]
      field_simp [Real.pi_ne_zero]
      ring

/-- The exact forward sine/cosine phase-coordinate integrand is integrable on
the closed Prawitz window. This is the analytic bridge for later signed
coordinate cancellations. -/
theorem integrableOn_prawitzPhaseCoordinateIntegrand
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    IntegrableOn (fun t : ℝ =>
      (Real.sin (t * x) *
            (prawitzZeroPhaseDifference mu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).re -
          Real.cos (t * x) *
            (prawitzZeroPhaseDifference mu
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T t).im) /
          Real.pi +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) (Icc (0 : ℝ) T) := by
  let z : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
      prawitzPVIntegrand T
        (MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x t
  let P : ℝ → ℂ := fun t => (Complex.I / (Real.pi : ℝ)) * z t
  let G : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hz : IntegrableOn z (Icc (0 : ℝ) T) := by
    simpa only [z] using integrableOn_prawitzPVIntegrand_charFun_sub
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  have hP : IntegrableOn P (Icc (0 : ℝ) T) := by
    simpa only [P] using hz.const_mul (Complex.I / (Real.pi : ℝ))
  have hG : IntegrableOn G (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [G]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  apply (hP.re.add hG).congr
  filter_upwards with t
  simp only [Pi.add_apply]
  change Complex.re (P t) + G t = _
  dsimp only [P, z, G, prawitzZeroPhaseDifference]
  rw [prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero]
  rw [Complex.mul_re, Complex.div_ofReal_re,
    Complex.div_ofReal_im, Complex.mul_re, Complex.mul_im]
  simp only [Complex.I_re, Complex.I_im, zero_div, one_div,
    zero_mul, Complex.exp_ofReal_mul_I_re,
    Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg]
  field_simp [Real.pi_ne_zero]
  ring

/-- Exact Taylor-coordinate expansion of the forward phase-preserving Prawitz
remainder for a normalized centered iid sum. Every signed filter, residual,
geometric-coefficient, and real Gaussian-correction term remains visible. -/
theorem prawitzPhasePreservingRemainder_normalizedCenteredIidSum_eq_integral_taylorCoordinates
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    prawitzPhasePreservingRemainder law T x =
      ∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        (Real.sin (t * x) *
              (((scaledPrawitzSmoothingFilter T t).re *
                    (r.re * s.re - r.im * s.im + c) -
                  (scaledPrawitzSmoothingFilter T t).im *
                    (r.re * s.im + r.im * s.re)) / t) -
            Real.cos (t * x) *
              (((scaledPrawitzSmoothingFilter T t).re *
                    (r.re * s.im + r.im * s.re) +
                  (scaledPrawitzSmoothingFilter T t).im *
                    (r.re * s.re - r.im * s.im + c)) / t)) /
            Real.pi +
          2 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  dsimp only
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  rw [prawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
    law hLaw1 hT x]
  apply integral_congr_ae
  filter_upwards with t
  rw [prawitzZeroPhaseDifference_normalizedCenteredIidSum_re_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_normalizedCenteredIidSum_im_eq_taylorCoordinates
      X hX hIndep hIdent N T t]

/-- The phase-preserving forward Prawitz remainder is continuous in the
spatial parameter. -/
theorem continuous_prawitzPhasePreservingRemainder
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    Continuous (prawitzPhasePreservingRemainder mu T) := by
  unfold prawitzPhasePreservingRemainder
  exact (Complex.continuous_re.comp
    (continuous_const.mul
      (continuous_setIntegral_prawitzPVIntegrand_charFun_sub
        mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        hmu1 integrable_abs_standardNormalLaw hT))).add
    (continuous_prawitzGaussianCosineRemainder T)

/-- The phase-preserving forward Prawitz remainder vanishes in the positive
spatial tail. -/
theorem tendsto_prawitzPhasePreservingRemainder_atTop
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    Tendsto (prawitzPhasePreservingRemainder mu T) atTop (𝓝 0) := by
  have hphase : Tendsto (fun x : ℝ =>
      (Complex.I / (Real.pi : ℝ)) *
        (∫ t in Icc (0 : ℝ) T,
          (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            prawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t))) atTop (𝓝 0) := by
    simpa only [mul_zero] using
      (tendsto_const_nhds.mul
        (tendsto_setIntegral_prawitzPVIntegrand_charFun_sub_atTop
          mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
          hmu1 integrable_abs_standardNormalLaw hT) :
        Tendsto (fun x : ℝ =>
          (Complex.I / (Real.pi : ℝ)) *
            (∫ t in Icc (0 : ℝ) T,
              (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                prawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t))) atTop (𝓝 ((Complex.I / (Real.pi : ℝ)) * 0)))
  have hre := Complex.continuous_re.continuousAt.tendsto.comp hphase
  have hgauss := tendsto_prawitzGaussianCosineRemainder_atTop T
  simpa only [prawitzPhasePreservingRemainder, mul_zero,
    Complex.zero_re, zero_add] using hre.add hgauss

/-- The phase-preserving forward Prawitz remainder also vanishes in the
negative spatial tail. -/
theorem tendsto_prawitzPhasePreservingRemainder_atBot
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    Tendsto (prawitzPhasePreservingRemainder mu T) atBot (𝓝 0) := by
  have hphase : Tendsto (fun x : ℝ =>
      (Complex.I / (Real.pi : ℝ)) *
        (∫ t in Icc (0 : ℝ) T,
          (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            prawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t))) atBot (𝓝 0) := by
    simpa only [mul_zero] using
      (tendsto_const_nhds.mul
        (tendsto_setIntegral_prawitzPVIntegrand_charFun_sub_atBot
          mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
          hmu1 integrable_abs_standardNormalLaw hT) :
        Tendsto (fun x : ℝ =>
          (Complex.I / (Real.pi : ℝ)) *
            (∫ t in Icc (0 : ℝ) T,
              (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                prawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t))) atBot (𝓝 ((Complex.I / (Real.pi : ℝ)) * 0)))
  have hre := Complex.continuous_re.continuousAt.tendsto.comp hphase
  have hgauss := tendsto_prawitzGaussianCosineRemainder_atBot T
  simpa only [prawitzPhasePreservingRemainder, mul_zero,
    Complex.zero_re, zero_add] using hre.add hgauss

/-- A continuous real function vanishing in both tails has an
epsilon-accurate global upper bound given by a maximum on a symmetric compact
interval. -/
theorem exists_compact_max_le_global_max_epsilon_of_tendsto_zero
    (f : ℝ → ℝ) (hf : Continuous f)
    (htop : Tendsto f atTop (𝓝 0))
    (hbot : Tendsto f atBot (𝓝 0))
    {epsilon : ℝ} (hepsilon : 0 < epsilon) :
    ∃ R x : ℝ, 0 ≤ R ∧ x ∈ Icc (-R) R ∧
      (∀ y ∈ Icc (-R) R, f y ≤ f x) ∧
      ∀ y : ℝ, f y ≤ max epsilon (f x) := by
  have htop' : ∀ᶠ y : ℝ in atTop, f y < epsilon := by
    simpa only [sub_pos] using
      ((tendsto_order.1 htop).2 epsilon hepsilon)
  have hbot' : ∀ᶠ y : ℝ in atBot, f y < epsilon := by
    simpa only [sub_pos] using
      ((tendsto_order.1 hbot).2 epsilon hepsilon)
  obtain ⟨a, ha⟩ := eventually_atTop.1 htop'
  obtain ⟨b, hb⟩ := eventually_atBot.1 hbot'
  let R : ℝ := max |a| |b|
  have hR : 0 ≤ R := by
    dsimp only [R]
    exact (abs_nonneg a).trans (le_max_left |a| |b|)
  have hinterval : (-R : ℝ) ≤ R := by linarith
  obtain ⟨x, hx, hmax⟩ :=
    isCompact_Icc.exists_isMaxOn (nonempty_Icc.mpr hinterval)
      hf.continuousOn
  refine ⟨R, x, hR, hx, hmax, ?_⟩
  intro y
  by_cases hy : y ∈ Icc (-R) R
  · exact (hmax hy).trans (le_max_right epsilon (f x))
  by_cases hyleft : y < -R
  · have hnegR_le_b : -R ≤ b := by
      have habs : |b| ≤ R := by
        dsimp only [R]
        exact le_max_right |a| |b|
      exact (neg_le_neg habs).trans (neg_abs_le b)
    exact (hb y ((le_of_lt hyleft).trans hnegR_le_b)).le.trans
      (le_max_left epsilon (f x))
  · have hR_lt_y : R < y := by
      have hnegR_le_y : -R ≤ y := le_of_not_gt hyleft
      exact lt_of_not_ge fun hyR => hy ⟨hnegR_le_y, hyR⟩
    have ha_le_R : a ≤ R := by
      have habs : |a| ≤ R := by
        dsimp only [R]
        exact le_max_left |a| |b|
      exact (le_abs_self a).trans habs
    exact (ha y (ha_le_R.trans (le_of_lt hR_lt_y))).le.trans
      (le_max_left epsilon (f x))

/-- The phase-preserving Prawitz remainder is globally controlled, up to any
positive epsilon, by an attained maximum on a symmetric compact interval. -/
theorem exists_prawitzPhasePreservingRemainder_compact_max_le_global
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T)
    {epsilon : ℝ} (hepsilon : 0 < epsilon) :
    ∃ R x : ℝ, 0 ≤ R ∧ x ∈ Icc (-R) R ∧
      (∀ y ∈ Icc (-R) R,
        prawitzPhasePreservingRemainder mu T y ≤
          prawitzPhasePreservingRemainder mu T x) ∧
      ∀ y : ℝ, prawitzPhasePreservingRemainder mu T y ≤
        max epsilon (prawitzPhasePreservingRemainder mu T x) := by
  exact exists_compact_max_le_global_max_epsilon_of_tendsto_zero
    (prawitzPhasePreservingRemainder mu T)
    (continuous_prawitzPhasePreservingRemainder mu hmu1 hT)
    (tendsto_prawitzPhasePreservingRemainder_atTop mu hmu1 hT)
    (tendsto_prawitzPhasePreservingRemainder_atBot mu hmu1 hT)
    hepsilon

/-- Exact truncation decomposition of the Gaussian Prawitz cosine term.  The
full positive-half-line transform is evaluated, while both the Gaussian tail
and the triangular first-moment correction remain signed. -/
theorem prawitzGaussianCosineRemainder_eq_full_sub_tail_sub_correction
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzGaussianCosineRemainder T x =
      (Real.sqrt (2 * Real.pi) / T) * Real.exp (-(x ^ 2) / 2) -
        (2 / T) * (∫ t in Ioi T,
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) -
        (2 / T ^ 2) * (∫ t in Icc (0 : ℝ) T,
          t * (Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) := by
  let f : ℝ → ℝ := fun t =>
    Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hgauss : Integrable (fun t : ℝ => Real.exp (-(t ^ 2) / 2)) := by
    have h := integrable_exp_neg_mul_sq (by norm_num : (0 : ℝ) < 1 / 2)
    apply h.congr
    exact ae_of_all _ fun t => by
      congr 1
      ring
  have hf : Integrable f := by
    apply hgauss.mono' (by dsimp only [f]; fun_prop)
    exact ae_of_all _ fun t => by
      dsimp only [f]
      rw [Real.norm_eq_abs, abs_mul, abs_of_pos (Real.exp_pos _)]
      exact mul_le_of_le_one_right (Real.exp_pos _).le
        (Real.abs_cos_le_one _)
  have htf : IntegrableOn (fun t : ℝ => t * f t) (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [f]
    fun_prop
  have hsplit : (∫ t in Icc (0 : ℝ) T, f t) =
      (Real.sqrt (2 * Real.pi) / 2) * Real.exp (-(x ^ 2) / 2) -
        ∫ t in Ioi T, f t := by
    have hsubset : Ici T ⊆ Ioi (0 : ℝ) := by
      intro t ht
      exact lt_of_lt_of_le hT ht
    have hdiff := integral_diff measurableSet_Ici hf.integrableOn hsubset
    have hset : Ioi (0 : ℝ) \ Ici T = Ioo (0 : ℝ) T := by
      ext t
      simp only [mem_diff, mem_Ioi, mem_Ici, mem_Ioo]
      constructor
      · intro ht
        exact ⟨ht.1, lt_of_not_ge ht.2⟩
      · intro ht
        exact ⟨ht.1, not_le.mpr ht.2⟩
    rw [hset, integral_Ici_eq_integral_Ioi] at hdiff
    rw [integral_Icc_eq_integral_Ioo, hdiff]
    rw [show (∫ t in Ioi (0 : ℝ), f t) =
        (Real.sqrt (2 * Real.pi) / 2) * Real.exp (-(x ^ 2) / 2) by
      simpa only [f] using integral_Ioi_rexp_neg_sq_div_two_mul_cos x]
  have hexpand : prawitzGaussianCosineRemainder T x =
      (2 / T) * (∫ t in Icc (0 : ℝ) T, f t) -
        (2 / T ^ 2) * (∫ t in Icc (0 : ℝ) T, t * f t) := by
    unfold prawitzGaussianCosineRemainder
    rw [integral_Icc_eq_integral_Ioo]
    have hfirst : IntegrableOn (fun t => (2 / T) * f t) (Ioo (0 : ℝ) T) :=
      (hf.const_mul (2 / T)).integrableOn
    have hsecond : IntegrableOn (fun t => (2 / T ^ 2) * (t * f t))
        (Ioo (0 : ℝ) T) :=
      (htf.mono_set Ioo_subset_Icc_self).const_mul (2 / T ^ 2)
    rw [show (∫ t in Ioo (0 : ℝ) T,
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) =
        ∫ t in Ioo (0 : ℝ) T,
          (2 / T) * f t - (2 / T ^ 2) * (t * f t) by
      apply setIntegral_congr_fun measurableSet_Ioo
      intro t ht
      dsimp only
      rw [prawitzFejerFrequencyWeight_of_mem_Icc hT ht.1.le ht.2.le]
      dsimp only [f]
      field_simp [hT.ne']]
    rw [integral_sub hfirst hsecond, integral_const_mul,
      integral_const_mul]
    rw [← integral_Icc_eq_integral_Ioo]
    rw [show (∫ t in Ioo (0 : ℝ) T, t * f t) =
        ∫ t in Icc (0 : ℝ) T, t * f t by
      exact integral_Icc_eq_integral_Ioo.symm]
  rw [hexpand, hsplit]
  dsimp only [f]
  ring

/-- The finite Gaussian Prawitz cosine remainder is uniformly maximized at
spatial frequency zero. -/
theorem prawitzGaussianCosineRemainder_le_zero
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    prawitzGaussianCosineRemainder T x ≤
      prawitzGaussianCosineRemainder T 0 := by
  unfold prawitzGaussianCosineRemainder
  have hleft : IntegrableOn (fun t : ℝ =>
      2 * prawitzFejerFrequencyWeight T t *
        Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    unfold prawitzFejerFrequencyWeight
    fun_prop
  have hright : IntegrableOn (fun t : ℝ =>
      2 * prawitzFejerFrequencyWeight T t *
        Real.exp (-(t ^ 2) / 2) * Real.cos (0 * t)) (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    unfold prawitzFejerFrequencyWeight
    fun_prop
  apply integral_mono hleft hright
  intro t
  have hcoef : 0 ≤ 2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) := by
    exact mul_nonneg
      (mul_nonneg (by norm_num) (prawitzFejerFrequencyWeight_nonneg hT t))
      (Real.exp_pos _).le
  simpa only [zero_mul, Real.cos_zero, mul_one] using
    mul_le_mul_of_nonneg_left (Real.cos_le_one (x * t)) hcoef

/-- Exact value of the uniform Gaussian Prawitz remainder.  Compared with the
half-line extension, it retains both the missing Gaussian tail and the
strictly positive triangular correction. -/
theorem prawitzGaussianCosineRemainder_zero
    {T : ℝ} (hT : 0 < T) :
    prawitzGaussianCosineRemainder T 0 =
      (2 / T) * (∫ t in Icc (0 : ℝ) T,
        Real.exp (-(t ^ 2) / 2)) -
      (2 / T ^ 2) * (1 - Real.exp (-(T ^ 2) / 2)) := by
  have hdecomp :=
    prawitzGaussianCosineRemainder_eq_full_sub_tail_sub_correction hT 0
  have hgauss : Integrable (fun t : ℝ => Real.exp (-(t ^ 2) / 2)) := by
    have h := integrable_exp_neg_mul_sq (by norm_num : (0 : ℝ) < 1 / 2)
    apply h.congr
    exact ae_of_all _ fun t => by
      congr 1
      ring
  have hsubset : Ici T ⊆ Ioi (0 : ℝ) := by
    intro t ht
    exact lt_of_lt_of_le hT ht
  have hdiff := integral_diff measurableSet_Ici hgauss.integrableOn hsubset
  have hset : Ioi (0 : ℝ) \ Ici T = Ioo (0 : ℝ) T := by
    ext t
    simp only [mem_diff, mem_Ioi, mem_Ici, mem_Ioo]
    constructor
    · intro ht
      exact ⟨ht.1, lt_of_not_ge ht.2⟩
    · intro ht
      exact ⟨ht.1, not_le.mpr ht.2⟩
  rw [hset, integral_Ici_eq_integral_Ioi] at hdiff
  have hmass : (∫ t in Icc (0 : ℝ) T,
      Real.exp (-(t ^ 2) / 2)) =
      Real.sqrt (2 * Real.pi) / 2 -
        ∫ t in Ioi T, Real.exp (-(t ^ 2) / 2) := by
    rw [integral_Icc_eq_integral_Ioo, hdiff]
    rw [show (∫ t in Ioi (0 : ℝ), Real.exp (-(t ^ 2) / 2)) =
        Real.sqrt (2 * Real.pi) / 2 by
      simpa using integral_Ioi_rexp_neg_sq_div_two_mul_cos 0]
  simp only [zero_mul, Real.cos_zero, mul_one, zero_pow, neg_zero,
    Real.exp_zero] at hdecomp
  have hexpZero : Real.exp (-((0 : ℝ) ^ 2) / 2) = 1 := by
    rw [show -((0 : ℝ) ^ 2) / 2 = 0 by norm_num, Real.exp_zero]
  rw [hexpZero, mul_one] at hdecomp
  rw [integral_Icc_mul_rexp_neg_sq_div_two hT] at hdecomp
  rw [hmass]
  rw [hdecomp]
  ring

/-- For every sufficiently wide window, the exact finite Gaussian remainder
still exceeds the reciprocal cutoff.  This is the rational core of the
coefficient-one obstruction; no numerical Gaussian approximation is used. -/
theorem one_div_lt_prawitzGaussianCosineRemainder_zero
    {T : ℝ} (hT6 : 6 ≤ T) :
    1 / T < prawitzGaussianCosineRemainder T 0 := by
  have hT : 0 < T := lt_of_lt_of_le (by norm_num) hT6
  let f : ℝ → ℝ := fun t => Real.exp (-(t ^ 2) / 2)
  let p : ℝ → ℝ := fun t => 1 - t ^ 2 / 2
  have hp : IntegrableOn p (Icc (0 : ℝ) 1) := by
    apply Continuous.integrableOn_Icc
    dsimp only [p]
    fun_prop
  have hf : IntegrableOn f (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [f]
    fun_prop
  have hfOne : IntegrableOn f (Icc (0 : ℝ) 1) := by
    apply Continuous.integrableOn_Icc
    dsimp only [f]
    fun_prop
  have hpoint : ∀ t, p t ≤ f t := by
    intro t
    dsimp only [p, f]
    convert Real.add_one_le_exp (-(t ^ 2) / 2) using 1 <;> ring
  have hpoly : (∫ t in Icc (0 : ℝ) 1, p t) = 5 / 6 := by
    have h2 : IntervalIntegrable (fun t : ℝ => t ^ 2) volume 0 1 :=
      (continuous_id.pow 2).intervalIntegrable _ _
    rw [integral_Icc_eq_integral_Ioc,
      ← intervalIntegral.integral_of_le (by norm_num : (0 : ℝ) ≤ 1)]
    dsimp only [p]
    rw [intervalIntegral.integral_sub
      intervalIntegral.intervalIntegrable_const (h2.div_const 2),
      intervalIntegral.integral_div, integral_one, integral_pow]
    norm_num
  have hsmall : 5 / 6 ≤ ∫ t in Icc (0 : ℝ) 1, f t := by
    rw [← hpoly]
    exact setIntegral_mono hp hfOne hpoint
  have hsubset : Icc (0 : ℝ) 1 ⊆ Icc (0 : ℝ) T := by
    intro t ht
    exact ⟨ht.1, ht.2.trans (by linarith)⟩
  have hmass : 5 / 6 ≤ ∫ t in Icc (0 : ℝ) T, f t := by
    exact hsmall.trans (setIntegral_mono_set hf
      (ae_of_all (volume.restrict (Icc (0 : ℝ) T)) fun t => by
        dsimp only [f]
        positivity)
      hsubset.eventuallyLE)
  have hcorr : 1 - Real.exp (-(T ^ 2) / 2) ≤ 1 := by
    linarith [Real.exp_pos (-(T ^ 2) / 2)]
  rw [prawitzGaussianCosineRemainder_zero hT]
  calc
    1 / T < (2 / T) * (5 / 6) - (2 / T ^ 2) * 1 := by
      field_simp [hT.ne']
      nlinarith
    _ ≤ (2 / T) * (∫ t in Icc (0 : ℝ) T, f t) -
          (2 / T ^ 2) * (1 - Real.exp (-(T ^ 2) / 2)) := by
      exact sub_le_sub
        (mul_le_mul_of_nonneg_left hmass (by positivity))
        (mul_le_mul_of_nonneg_left hcorr (by positivity))
    _ = _ := rfl

/-- For every sufficiently wide window, the exact bidirectional law-phase
envelope has a strict reciprocal-cutoff floor at the spatial origin. -/
theorem one_div_two_mul_lt_prawitzLawPhaseEnvelope_zero
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T : ℝ} (hT6 : 6 ≤ T) :
    1 / (2 * T) < prawitzLawPhaseEnvelope mu T 0 := by
  have hT : 0 < T := lt_of_lt_of_le (by norm_num) hT6
  have hgauss : 1 / T < prawitzGaussianCosineRemainder T 0 :=
    one_div_lt_prawitzGaussianCosineRemainder_zero hT6
  have hhalf :
      (1 / 2 : ℝ) * prawitzGaussianCosineRemainder T 0 ≤
        prawitzLawPhaseFejerIntegral mu T 0 :=
    half_prawitzGaussianCosineRemainder_le_lawPhaseFejerIntegral mu hT 0
  have hstrict :
      (1 / 2 : ℝ) * (1 / T) <
        (1 / 2 : ℝ) * prawitzGaussianCosineRemainder T 0 :=
    mul_lt_mul_of_pos_left hgauss (by norm_num)
  have hscale : 1 / (2 * T) = (1 / 2 : ℝ) * (1 / T) := by
    field_simp
  rw [prawitzLawPhaseEnvelope, hscale]
  exact (hstrict.trans_le hhalf).trans_le
    (le_add_of_nonneg_right (abs_nonneg _))

/-- At unit standardized third moment, every admissible tunable parameter has
large-sample windows on which the exact Gaussian remainder alone exceeds the
coefficient-one Berry scale. -/
theorem inverse_sqrt_lt_prawitzGaussianCosineRemainder_admissible_unit
    {c : ℝ} (hc : 0 < c) {N : ℕ}
    (hcut : 6 ≤ admissibleTunableThirdMomentCutoff c 1 N) :
    1 / Real.sqrt (N + 1 : ℝ) <
      prawitzGaussianCosineRemainder
        (admissibleTunableThirdMomentCutoff c 1 N) 0 := by
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let m : ℝ := max 1 c
  let T : ℝ := admissibleTunableThirdMomentCutoff c 1 N
  have hs : 0 < s := by dsimp only [s]; positivity
  have hm : 1 ≤ m := by
    dsimp only [m]
    exact le_max_left 1 c
  have hm0 : 0 < m := lt_of_lt_of_le zero_lt_one hm
  have hT : T = s / m := by
    dsimp only [T, s, m]
    simpa only [mul_one] using
      admissibleTunableThirdMomentCutoff_eq_sqrt_div_max hc
        (by norm_num : (0 : ℝ) < 1) N
  have hinv : 1 / s ≤ 1 / T := by
    rw [hT, show 1 / (s / m) = m / s by field_simp [hs.ne', hm0.ne']]
    exact (div_le_div_iff_of_pos_right hs).2 hm
  simpa only [T, s] using
    hinv.trans_lt (one_div_lt_prawitzGaussianCosineRemainder_zero hcut)

/-- Every positive tunable parameter admits a sample size whose unit-moment
admissible cutoff is at least six. -/
theorem exists_nat_admissibleTunableThirdMomentCutoff_unit_ge_six
    {c : ℝ} (hc : 0 < c) :
    ∃ N : ℕ, 1 ≤ N ∧
      6 ≤ admissibleTunableThirdMomentCutoff c 1 N := by
  let m : ℝ := max 1 c
  have hm : 1 ≤ m := by
    dsimp only [m]
    exact le_max_left 1 c
  have hm0 : 0 < m := lt_of_lt_of_le zero_lt_one hm
  obtain ⟨N, hN⟩ := exists_nat_gt ((6 * m) ^ 2)
  have hNpos : 1 ≤ N := by
    rw [Nat.one_le_iff_ne_zero]
    intro hzero
    subst N
    norm_num at hN
    nlinarith [sq_nonneg (6 * m)]
  have hsquare : (6 * m) ^ 2 ≤ (N + 1 : ℕ) := by
    norm_num [Nat.cast_add, Nat.cast_one] at hN ⊢
    linarith
  refine ⟨N, hNpos, ?_⟩
  rw [admissibleTunableThirdMomentCutoff_eq_sqrt_div_max hc
    (by norm_num : (0 : ℝ) < 1), mul_one]
  rw [le_div_iff₀ hm0]
  exact (Real.le_sqrt (by positivity) (by positivity)).2 (by
    simpa only [Nat.cast_add, Nat.cast_one] using hsquare)

/-- Consequently, for every positive tunable parameter there is a valid
large-sample unit-third-moment regime in which the exact finite Gaussian
remainder alone exceeds the source's coefficient-one Berry scale. -/
theorem exists_inverse_sqrt_lt_prawitzGaussianCosineRemainder_admissible_unit
    {c : ℝ} (hc : 0 < c) :
    ∃ N : ℕ, 1 ≤ N ∧
      1 / Real.sqrt (N + 1 : ℝ) <
        prawitzGaussianCosineRemainder
          (admissibleTunableThirdMomentCutoff c 1 N) 0 := by
  obtain ⟨N, hN, hcut⟩ :=
    exists_nat_admissibleTunableThirdMomentCutoff_unit_ge_six hc
  exact ⟨N, hN,
    inverse_sqrt_lt_prawitzGaussianCosineRemainder_admissible_unit hc hcut⟩

/-- Pointwise real normal form of the forward cancellation-preserving
integrand at positive frequency. -/
theorem re_forwardPrawitzSingleIntegrand_eq
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (ht : t ≠ 0) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re =
      -(Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          scaledPrawitzSmoothingFilter T t *
            (MeasureTheory.charFun mu t -
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).im /
          (Real.pi * t) +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [Complex.add_re,
    re_prawitzCoefficient_mul_symmetric_sub
      (isHermitianFrequencyFunction_charFun mu)
      (isHermitianFrequencyFunction_charFun
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) x ht,
    re_pairedPrawitzReflectionFrequencyIntegrand_standardNormal]
  rw [← Complex.sub_im, prawitzPVNumerator_sub]

/-- The real forward integrand equals the real part of the positive-frequency
phase-preserving difference plus the exact Gaussian cosine contribution. -/
theorem re_forwardPrawitzSingleIntegrand_eq_phasePreserving
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (ht : t ≠ 0) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re =
      ((Complex.I / (Real.pi : ℝ)) *
        (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
          prawitzPVIntegrand T
            (MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
            x t)).re +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [re_forwardPrawitzSingleIntegrand_eq mu ht x]
  unfold prawitzPVIntegrand
  rw [← sub_div, ← prawitzPVNumerator_sub]
  congr 1
  rw [Complex.mul_re, Complex.div_ofReal_re, Complex.div_ofReal_im,
    Complex.div_ofReal_re, Complex.div_ofReal_im]
  simp only [Complex.I_re, Complex.I_im, zero_div, one_div, zero_mul]
  field_simp [ht, Real.pi_ne_zero]
  ring

/-- Interior-window expansion of the forward signed integrand. It isolates
the cotangent coefficient of the imaginary characteristic-function error and
the triangular coefficient of its real part, while retaining the exact
Gaussian term. -/
theorem re_forwardPrawitzSingleIntegrand_eq_expanded
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re =
      -prawitzCotangentFrequencyCoefficient T t *
          (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
            (MeasureTheory.charFun mu t -
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).im +
        prawitzFejerFrequencyWeight T t *
          (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
            (MeasureTheory.charFun mu t -
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [re_forwardPrawitzSingleIntegrand_eq mu ht.1.ne' x]
  let q : ℂ :=
    Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
      (MeasureTheory.charFun mu t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)
  have hprod :
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          scaledPrawitzSmoothingFilter T t *
            (MeasureTheory.charFun mu t -
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) =
        scaledPrawitzSmoothingFilter T t * q := by
    dsimp only [q]
    ring
  rw [hprod, Complex.mul_im,
    scaledPrawitzSmoothingFilter_re_of_mem_Ico hT ht.1.le ht.2,
    scaledPrawitzSmoothingFilter_im_of_mem_Ico hT ht.1.le ht.2,
    prawitzFejerFrequencyWeight_of_mem_Icc hT ht.1.le ht.2.le]
  change
    -(((1 - t / T) * prawitzCotFactor (t / T) + t / T) * q.im +
        -((1 - t / T) * Real.pi * (t / T)) * q.re) /
          (Real.pi * t) +
        2 * ((1 - t / T) / T) * Real.exp (-(t ^ 2) / 2) *
          Real.cos (x * t) = _
  unfold prawitzCotangentFrequencyCoefficient
  dsimp only [q]
  field_simp [hT.ne', ht.1.ne', Real.pi_ne_zero]
  ring

/-- Pointwise signed majorant which bounds only the characteristic-function
components and keeps the oscillatory Gaussian term exact. -/
theorem re_forwardPrawitzSingleIntegrand_le_with_exactGaussian
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
      (prawitzCotangentFrequencyCoefficient T t +
          prawitzFejerFrequencyWeight T t) *
        ‖MeasureTheory.charFun mu t -
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [re_forwardPrawitzSingleIntegrand_eq_expanded mu hT ht x]
  let q : ℂ :=
    Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
      (MeasureTheory.charFun mu t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)
  let c := prawitzCotangentFrequencyCoefficient T t
  let w := prawitzFejerFrequencyWeight T t
  have hc : 0 ≤ c := prawitzCotangentFrequencyCoefficient_nonneg hT ht
  have hw : 0 ≤ w := prawitzFejerFrequencyWeight_nonneg hT t
  have hqnorm :
      ‖q‖ = ‖MeasureTheory.charFun mu t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ := by
    dsimp only [q]
    rw [norm_mul, Complex.norm_exp_ofReal_mul_I, one_mul]
  have hIm : -c * q.im ≤ c * ‖q‖ := by
    calc
      -c * q.im = c * (-q.im) := by ring
      _ ≤ c * ‖q‖ := mul_le_mul_of_nonneg_left
        ((neg_le_abs q.im).trans (Complex.abs_im_le_norm q)) hc
  have hRe : w * q.re ≤ w * ‖q‖ :=
    mul_le_mul_of_nonneg_left (Complex.re_le_norm q) hw
  change -c * q.im + w * q.re +
      2 * w * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) ≤
    (c + w) *
        ‖MeasureTheory.charFun mu t -
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ +
      2 * w * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  rw [← hqnorm]
  linarith

/-- Pointwise quantitative majorant obtained from the signed normal form. The
two characteristic-function components retain their distinct exact weights;
only the final scalar components are bounded by the discrepancy norm. -/
theorem re_forwardPrawitzSingleIntegrand_le
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
      (prawitzCotangentFrequencyCoefficient T t +
          prawitzFejerFrequencyWeight T t) *
        ‖MeasureTheory.charFun mu t -
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) := by
  rw [re_forwardPrawitzSingleIntegrand_eq_expanded mu hT ht x]
  let q : ℂ :=
    Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
      (MeasureTheory.charFun mu t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)
  let c := prawitzCotangentFrequencyCoefficient T t
  let w := prawitzFejerFrequencyWeight T t
  have hc : 0 ≤ c := prawitzCotangentFrequencyCoefficient_nonneg hT ht
  have hw : 0 ≤ w := prawitzFejerFrequencyWeight_nonneg hT t
  have hqnorm :
      ‖q‖ = ‖MeasureTheory.charFun mu t -
        MeasureTheory.charFun
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ := by
    dsimp only [q]
    rw [norm_mul, Complex.norm_exp_ofReal_mul_I, one_mul]
  have hIm : -c * q.im ≤ c * ‖q‖ := by
    calc
      -c * q.im = c * (-q.im) := by ring
      _ ≤ c * ‖q‖ := mul_le_mul_of_nonneg_left
        ((neg_le_abs q.im).trans (Complex.abs_im_le_norm q)) hc
  have hRe : w * q.re ≤ w * ‖q‖ :=
    mul_le_mul_of_nonneg_left (Complex.re_le_norm q) hw
  have hGauss :
      2 * w * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) ≤
        2 * w * Real.exp (-(t ^ 2) / 2) := by
    have hcoef : 0 ≤ 2 * w * Real.exp (-(t ^ 2) / 2) := by positivity
    simpa only [mul_one] using
      mul_le_mul_of_nonneg_left (Real.cos_le_one (x * t)) hcoef
  change -c * q.im + w * q.re +
      2 * w * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) ≤
    (c + w) *
        ‖MeasureTheory.charFun mu t -
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t‖ +
      2 * w * Real.exp (-(t ^ 2) / 2)
  rw [← hqnorm]
  linarith

/-- Universal coefficient form of the signed pointwise majorant on the open
Prawitz window. -/
theorem re_forwardPrawitzSingleIntegrand_le_discrepancyQuotient
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
      (1 / Real.pi + t / T) *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t +
        (2 / T) * Real.exp (-(t ^ 2) / 2) := by
  have hbase := re_forwardPrawitzSingleIntegrand_le mu hT ht x
  have hc := prawitzCotangentFrequencyCoefficient_le_inv hT ht
  have hw := prawitzFejerFrequencyWeight_le_inv hT ht.1.le
  have hq := charFunDiscrepancyQuotient_nonneg mu
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
  have hnorm := norm_charFun_sub_eq_abs_mul_charFunDiscrepancyQuotient
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ht.1.ne'
  rw [abs_of_pos ht.1] at hnorm
  rw [hnorm] at hbase
  calc
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
        (prawitzCotangentFrequencyCoefficient T t +
            prawitzFejerFrequencyWeight T t) *
          (t * charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
          2 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) := hbase
    _ ≤ (1 / (Real.pi * t) + 1 / T) *
          (t * charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
        2 * (1 / T) * Real.exp (-(t ^ 2) / 2) := by
      apply add_le_add
      · exact mul_le_mul_of_nonneg_right (add_le_add hc hw)
          (mul_nonneg ht.1.le hq)
      · exact mul_le_mul_of_nonneg_right
          (mul_le_mul_of_nonneg_left hw (by norm_num))
          (Real.exp_pos _).le
    _ = (1 / Real.pi + t / T) *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t +
        (2 / T) * Real.exp (-(t ^ 2) / 2) := by
      field_simp [Real.pi_ne_zero, hT.ne', ht.1.ne']

/-- Universal coefficient form of the signed majorant with the finite-window
Gaussian cosine contribution left unchanged. -/
theorem re_forwardPrawitzSingleIntegrand_le_discrepancyQuotient_with_exactGaussian
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
      (1 / Real.pi + t / T) *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  have hbase := re_forwardPrawitzSingleIntegrand_le_with_exactGaussian
    mu hT ht x
  have hc := prawitzCotangentFrequencyCoefficient_le_inv hT ht
  have hw := prawitzFejerFrequencyWeight_le_inv hT ht.1.le
  have hq := charFunDiscrepancyQuotient_nonneg mu
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
  have hnorm := norm_charFun_sub_eq_abs_mul_charFunDiscrepancyQuotient
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ht.1.ne'
  rw [abs_of_pos ht.1] at hnorm
  rw [hnorm] at hbase
  calc
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
        (prawitzCotangentFrequencyCoefficient T t +
            prawitzFejerFrequencyWeight T t) *
          (t * charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
          2 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := hbase
    _ ≤ (1 / (Real.pi * t) + 1 / T) *
          (t * charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
      exact add_le_add
        (mul_le_mul_of_nonneg_right (add_le_add hc hw)
          (mul_nonneg ht.1.le hq)) le_rfl
    _ = (1 / Real.pi + t / T) *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
      field_simp [Real.pi_ne_zero, hT.ne', ht.1.ne']

/-- Exact frequency-space form of the standard-normal Fejer remainder in the
Prawitz comparison. -/
theorem integral_prawitzReflection_standardNormal_eq_re_frequencyIntegral
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, (prawitzPointKernel T (y - x) +
        prawitzPointKernel T (x - y))
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) =
      (∫ z,
        Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
            (T * z / 2)
        ∂twofoldUniformKernel).re := by
  simp_rw [prawitzPointKernel_reflection_eq_sinc_sq hT]
  change (∫ y, sincSquareWeight (T * (y - x) / 2)
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) = _
  exact integral_sincSquareWeight_eq_re_frequencyIntegral
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw T x

/-- Exact mass of every translated, positively scaled Fejér profile. -/
theorem integral_sinc_sq_half_scale {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y : ℝ, Real.sinc (T * (y - x) / 2) ^ 2) =
      2 * Real.pi / T := by
  have hcomp := Measure.integral_comp_mul_left
    (fun z : ℝ => sincSquareWeight z) (T / 2)
  rw [abs_of_pos (inv_pos.mpr (div_pos hT (by norm_num)))] at hcomp
  have htrans :
      (∫ y : ℝ, sincSquareWeight ((T / 2) * (y - x))) =
        ∫ y : ℝ, sincSquareWeight ((T / 2) * y) := by
    simpa only [sub_eq_add_neg, add_comm] using
      (MeasureTheory.integral_add_right_eq_self
        (μ := volume) (fun y : ℝ => sincSquareWeight ((T / 2) * y)) (-x))
  rw [show (fun y : ℝ => Real.sinc (T * (y - x) / 2) ^ 2) =
      fun y => sincSquareWeight ((T / 2) * (y - x)) by
        funext y
        unfold sincSquareWeight
        congr 2
        ring,
    htrans]
  rw [integral_sincSquareWeight] at hcomp
  calc
    (∫ y : ℝ, sincSquareWeight (T / 2 * y)) =
        (T / 2)⁻¹ • Real.pi := hcomp
    _ = 2 * Real.pi / T := by
      simp only [smul_eq_mul]
      field_simp [hT.ne']

/-- Integrability companion to the exact translated Fejér mass. -/
theorem integrable_sinc_sq_half_scale {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (fun y : ℝ => Real.sinc (T * (y - x) / 2) ^ 2) := by
  have hscale : (T / 2 : ℝ) ≠ 0 := div_ne_zero hT.ne' (by norm_num)
  have hscaled : Integrable
      (fun y : ℝ => sincSquareWeight ((T / 2) * y)) :=
    integrable_sincSquareWeight.comp_mul_left' hscale
  have htranslated :=
    (measurePreserving_add_right volume (-x)).integrable_comp_of_integrable hscaled
  apply htranslated.congr
  filter_upwards with y
  dsimp only [Function.comp_apply, sincSquareWeight]
  congr 2
  ring

/-- The reflected Prawitz remainder against the standard normal is bounded
by the sharp density supremum times the exact Fejér mass. -/
theorem integral_prawitzReflection_standardNormal_le
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, (prawitzPointKernel T (y - x) +
        prawitzPointKernel T (x - y))
      ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let k : ℝ → ℝ := fun y => Real.sinc (T * (y - x) / 2) ^ 2
  let c : ℝ := (Real.sqrt (2 * Real.pi))⁻¹
  have hk : Integrable k := by
    simpa only [k] using integrable_sinc_sq_half_scale hT x
  have hkNonneg : ∀ y, 0 ≤ k y := fun y => by dsimp only [k]; positivity
  have hpdf : Integrable (fun y =>
      ProbabilityTheory.gaussianPDFReal 0 1 y * k y) := by
    have hmajor : Integrable (fun y => c * k y) := hk.const_mul c
    apply hmajor.mono' (by fun_prop)
    exact ae_of_all volume fun y => by
      rw [Real.norm_eq_abs, abs_of_nonneg
        (mul_nonneg (ProbabilityTheory.gaussianPDFReal_nonneg 0 1 y)
          (hkNonneg y))]
      have hbound : ProbabilityTheory.gaussianPDFReal 0 1 y ≤ c := by
        rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
        have hexp : Real.exp (-(y ^ 2) / 2) ≤ 1 :=
          Real.exp_le_one_iff.mpr (by nlinarith [sq_nonneg y])
        simpa only [mul_one, c] using mul_le_mul_of_nonneg_left hexp
          (inv_nonneg.mpr (Real.sqrt_nonneg (2 * Real.pi)))
      exact mul_le_mul_of_nonneg_right hbound (hkNonneg y)
  have hle :
      (∫ y, ProbabilityTheory.gaussianPDFReal 0 1 y * k y) ≤
        ∫ y, c * k y := by
    apply integral_mono hpdf (hk.const_mul c)
    intro y
    have hbound : ProbabilityTheory.gaussianPDFReal 0 1 y ≤ c := by
      rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw_pdf]
      have hexp : Real.exp (-(y ^ 2) / 2) ≤ 1 :=
        Real.exp_le_one_iff.mpr (by nlinarith [sq_nonneg y])
      simpa only [mul_one, c] using mul_le_mul_of_nonneg_left hexp
        (inv_nonneg.mpr (Real.sqrt_nonneg (2 * Real.pi)))
    exact mul_le_mul_of_nonneg_right hbound (hkNonneg y)
  rw [integral_const_mul, show (∫ y, k y) = 2 * Real.pi / T by
    simpa only [k] using integral_sinc_sq_half_scale hT x] at hle
  simp_rw [prawitzPointKernel_reflection_eq_sinc_sq hT]
  change (∫ y, k y
    ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T)
  rw [NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw]
  have hone : (1 : NNReal) ≠ 0 := one_ne_zero
  have hgauss :
      (∫ y, k y ∂ProbabilityTheory.gaussianReal 0 1) =
        ∫ y, ProbabilityTheory.gaussianPDFReal 0 1 y • k y :=
    ProbabilityTheory.integral_gaussianReal_eq_integral_smul hone
  rw [hgauss]
  simpa only [smul_eq_mul, k, c] using hle

/-- One-sided Prawitz comparison against the standard normal with both the
signed frequency integral and reflected Fejer remainder kept exact. -/
theorem cdf_sub_standardNormalCDF_le_prawitz_signed
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t)).re +
      ∫ y, (prawitzPointKernel T (y - x) +
          prawitzPointKernel T (x - y))
        ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  have hsigned :=
    cdf_sub_cdf_le_re_prawitzInversion_sub_add_reflection
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  rw [re_prawitzInversion_charFun_sub_eq_signedIntegral
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
    hmu1 integrable_abs_standardNormalLaw hT x] at hsigned
  exact hsigned

/-- The forward standard-normal Prawitz comparison with the inversion and
Fejer terms combined inside one real part before any norm estimate. -/
theorem cdf_sub_standardNormalCDF_le_prawitz_signed_frequency
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (((Complex.I / (2 * Real.pi : ℝ)) *
          ∫ t in Icc 0 T,
            (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
              symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                x t)) +
        ∫ z,
          Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
              (T * z / 2)
          ∂twofoldUniformKernel).re := by
  have hsigned := cdf_sub_standardNormalCDF_le_prawitz_signed
    mu hmu1 hT x
  rw [integral_prawitzReflection_standardNormal_eq_re_frequencyIntegral
    hT x] at hsigned
  simpa only [Complex.add_re] using hsigned

/-- The forward comparison as one signed positive-frequency integral. This is
the cancellation-preserving form: no norm or pointwise absolute value has yet
separated the Prawitz and Fejer contributions. -/
theorem cdf_sub_standardNormalCDF_le_prawitz_singleSignedIntegral
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (∫ t in Icc 0 T,
        (Complex.I / (2 * Real.pi : ℝ)) *
            (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
              symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                x t) +
          (prawitzFejerFrequencyWeight T t : ℂ) *
            (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
              Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re := by
  have hsigned := cdf_sub_standardNormalCDF_le_prawitz_signed_frequency
    mu hmu1 hT x
  rw [integral_prawitzReflectionFrequency_eq_pairedTriangularIntegral
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x] at hsigned
  have hmu := integrableOn_symmetricPrawitzPVIntegrand_charFun
    mu hmu1 hT x
  have hnormal := integrableOn_symmetricPrawitzPVIntegrand_charFun
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
    integrable_abs_standardNormalLaw hT x
  have hP := (hmu.sub hnormal).const_mul
    (Complex.I / (2 * Real.pi : ℝ))
  have hR : IntegrableOn (fun t =>
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t))
      (Icc 0 T) :=
    (integrable_pairedPrawitzReflectionFrequencyIntegrand
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).integrableOn
  have hcombine :
      (Complex.I / (2 * Real.pi : ℝ)) *
          (∫ t in Icc 0 T,
            (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
              symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                x t)) +
        (∫ t in Icc 0 T,
          (prawitzFejerFrequencyWeight T t : ℂ) *
            (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
              Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)) =
        ∫ t in Icc 0 T,
          (Complex.I / (2 * Real.pi : ℝ)) *
              (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                symmetricPrawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t) +
            (prawitzFejerFrequencyWeight T t : ℂ) *
              (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
                Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) := by
    calc
      (Complex.I / (2 * Real.pi : ℝ)) *
            (∫ t in Icc 0 T,
              (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                symmetricPrawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t)) +
          (∫ t in Icc 0 T,
            (prawitzFejerFrequencyWeight T t : ℂ) *
              (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
                Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)) =
          (∫ t in Icc 0 T,
            (Complex.I / (2 * Real.pi : ℝ)) *
              (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                symmetricPrawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t)) +
          (∫ t in Icc 0 T,
            (prawitzFejerFrequencyWeight T t : ℂ) *
              (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
                Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)) := by
        congr 1
        exact (integral_const_mul
          (μ := volume.restrict (Icc 0 T))
          (Complex.I / (2 * Real.pi : ℝ))
          (fun t =>
            symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
              symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                x t)).symm
      _ = ∫ t in Icc 0 T,
          (Complex.I / (2 * Real.pi : ℝ)) *
              (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                symmetricPrawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t) +
            (prawitzFejerFrequencyWeight T t : ℂ) *
              (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
                Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                  MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) :=
        (integral_add hP hR).symm
  rw [hcombine] at hsigned
  exact hsigned

/-- The forward signed CDF error is bounded by a single remainder that keeps
the characteristic-function phase and exact Gaussian cosine term together.
This is the cancellation-preserving bridge used for the remaining compact
spatial analysis. -/
theorem cdf_sub_standardNormalCDF_le_prawitzPhasePreservingRemainder
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      prawitzPhasePreservingRemainder mu T x := by
  have hsigned := cdf_sub_standardNormalCDF_le_prawitz_singleSignedIntegral
    mu hmu1 hT x
  let F : ℝ → ℂ := fun t =>
    (Complex.I / (2 * Real.pi : ℝ)) *
        (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
          symmetricPrawitzPVIntegrand T
            (MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
            x t) +
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)
  let P : ℝ → ℂ := fun t =>
    (Complex.I / (Real.pi : ℝ)) *
      (prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        prawitzPVIntegrand T
          (MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
          x t)
  let G : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hF : IntegrableOn F (Icc (0 : ℝ) T) := by
    have hmu := integrableOn_symmetricPrawitzPVIntegrand_charFun
      mu hmu1 hT x
    have hnormal := integrableOn_symmetricPrawitzPVIntegrand_charFun
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      integrable_abs_standardNormalLaw hT x
    have hP := (hmu.sub hnormal).const_mul
      (Complex.I / (2 * Real.pi : ℝ))
    have hR : IntegrableOn (fun t =>
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t))
        (Icc (0 : ℝ) T) :=
      (integrable_pairedPrawitzReflectionFrequencyIntegrand
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).integrableOn
    simpa only [F] using hP.add hR
  have hP : IntegrableOn P (Icc (0 : ℝ) T) := by
    simpa only [P] using
      (integrableOn_prawitzPVIntegrand_charFun_sub
        mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        hmu1 integrable_abs_standardNormalLaw hT x).const_mul
          (Complex.I / (Real.pi : ℝ))
  have hG : IntegrableOn G (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [G]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  have heq : (∫ t in Icc (0 : ℝ) T, F t).re =
      prawitzPhasePreservingRemainder mu T x := by
    calc
      (∫ t in Icc (0 : ℝ) T, F t).re =
          ∫ t in Icc (0 : ℝ) T, (F t).re :=
        (integral_re hF).symm
      _ = ∫ t in Icc (0 : ℝ) T, ((P t).re + G t) := by
        apply integral_congr_ae
        filter_upwards [
          (volume.restrict (Icc (0 : ℝ) T)).ae_ne 0] with t ht
        dsimp only [F, P, G]
        exact re_forwardPrawitzSingleIntegrand_eq_phasePreserving mu ht x
      _ = (∫ t in Icc (0 : ℝ) T, P t).re +
          ∫ t in Icc (0 : ℝ) T, G t := by
        exact (integral_add hP.re hG).trans
          (congrArg (fun z : ℝ =>
            z + ∫ t in Icc (0 : ℝ) T, G t) (integral_re hP))
      _ = prawitzPhasePreservingRemainder mu T x := by
        dsimp only [P, G]
        unfold prawitzPhasePreservingRemainder
          prawitzGaussianCosineRemainder
        congr 1
        exact congrArg Complex.re
          (integral_const_mul
            (μ := volume.restrict (Icc (0 : ℝ) T))
            (Complex.I / (Real.pi : ℝ))
            (fun t =>
              prawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
                prawitzPVIntegrand T
                  (MeasureTheory.charFun
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                  x t))
  simpa only [F] using hsigned.trans_eq heq

/-- One-sided Prawitz smoothing against the standard normal, with exact
frequency coefficient `1/π` and exact Fejér spatial remainder. -/
theorem cdf_sub_standardNormalCDF_le_prawitz
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf mu x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (1 / Real.pi) * (∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  have hsigned :=
    cdf_sub_cdf_le_norm_prawitzInversion_sub_add_reflection
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  have hfreq := norm_prawitzInversion_charFun_sub_le_integral_discrepancy
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
    hmu1 integrable_abs_standardNormalLaw hT x
  have hreflect := integral_prawitzReflection_standardNormal_le hT x
  exact hsigned.trans (add_le_add hfreq hreflect)

/-- The law obtained by reflecting a real-valued law through the origin. -/
noncomputable def reflectedLaw (mu : Measure ℝ) : Measure ℝ :=
  mu.map ((-1 : ℝ) * ·)

/-- Reflection preserves total probability mass. -/
theorem reflectedLaw_isProbabilityMeasure
    (mu : Measure ℝ) [IsProbabilityMeasure mu] :
    IsProbabilityMeasure (reflectedLaw mu) := by
  unfold reflectedLaw
  exact Measure.isProbabilityMeasure_map (by fun_prop)

/-- Reflection preserves finiteness of the first absolute moment. -/
theorem integrable_abs_reflectedLaw
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu) :
    Integrable (fun y : ℝ => |y|) (reflectedLaw mu) := by
  unfold reflectedLaw
  rw [integrable_map_measure (by fun_prop) (by fun_prop)]
  convert hmu1 using 1
  ext y
  simp

/-- The characteristic function of a reflected law is evaluated at the
opposite frequency. -/
theorem charFun_reflectedLaw (mu : Measure ℝ) (t : ℝ) :
    MeasureTheory.charFun (reflectedLaw mu) t =
      MeasureTheory.charFun mu (-t) := by
  unfold reflectedLaw
  rw [MeasureTheory.charFun_map_mul]
  congr 1
  ring

/-- Simultaneous reflection conjugates the raw characteristic-function
discrepancy while leaving the complex Prawitz filter itself in place. -/
theorem prawitzZeroPhaseDifference_reflectedLaw_eq_filter_mul_conj_charFun_sub
    (mu nu : Measure ℝ) (T t : ℝ) :
    prawitzZeroPhaseDifference (reflectedLaw mu) (reflectedLaw nu) T t =
      scaledPrawitzSmoothingFilter T t *
        (starRingEnd ℂ)
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t) / t := by
  rw [prawitzZeroPhaseDifference_eq_filter_mul_charFun_sub,
    charFun_reflectedLaw, charFun_reflectedLaw,
    MeasureTheory.charFun_neg, MeasureTheory.charFun_neg]
  simp only [map_sub]

/-- Exact conjugated Taylor-product factorization of the reflected
normalized-iid zero-phase Prawitz amplitude. -/
theorem prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_eq_conj_taylorGeomSum
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (T t : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
    let phi : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (X 0) (hX 0).aemeasurable : Measure ℝ) u
    let a : ℝ := 1 - u ^ 2 / 2
    let r : ℂ := phi - (a : ℂ)
    let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
    let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
    prawitzZeroPhaseDifference
        (reflectedLaw law)
        (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        T t =
      scaledPrawitzSmoothingFilter T t *
        (starRingEnd ℂ) (r * s + (c : ℂ)) / t := by
  dsimp only
  rw [prawitzZeroPhaseDifference_reflectedLaw_eq_filter_mul_conj_charFun_sub,
    charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_taylorGeomSum
      X hX hIndep hIdent N t]

/-- Real coordinate of the exact reflected normalized-iid Prawitz Taylor
product. Reflection changes the discrepancy cross-term signs by conjugation. -/
theorem prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_re_eq_taylorCoordinates
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (T t : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
    let phi : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (X 0) (hX 0).aemeasurable : Measure ℝ) u
    let a : ℝ := 1 - u ^ 2 / 2
    let r : ℂ := phi - (a : ℂ)
    let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
    let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
    (prawitzZeroPhaseDifference
        (reflectedLaw law)
        (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        T t).re =
      ((scaledPrawitzSmoothingFilter T t).re *
          (r.re * s.re - r.im * s.im + c) +
        (scaledPrawitzSmoothingFilter T t).im *
          (r.re * s.im + r.im * s.re)) / t := by
  dsimp only
  rw [prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_eq_conj_taylorGeomSum
    X hX hIndep hIdent N T t]
  exact complex_mul_conj_taylorProduct_div_re _ _ _ _ _

/-- Imaginary coordinate of the exact reflected normalized-iid Prawitz Taylor
product. Reflection changes the discrepancy cross-term signs by conjugation. -/
theorem prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_im_eq_taylorCoordinates
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) (T t : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
    let phi : ℂ := MeasureTheory.charFun
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (X 0) (hX 0).aemeasurable : Measure ℝ) u
    let a : ℝ := 1 - u ^ 2 / 2
    let r : ℂ := phi - (a : ℂ)
    let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
    let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
    (prawitzZeroPhaseDifference
        (reflectedLaw law)
        (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        T t).im =
      (-(scaledPrawitzSmoothingFilter T t).re *
          (r.re * s.im + r.im * s.re) +
        (scaledPrawitzSmoothingFilter T t).im *
          (r.re * s.re - r.im * s.im + c)) / t := by
  dsimp only
  rw [prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_eq_conj_taylorGeomSum
    X hX hIndep hIdent N T t]
  exact complex_mul_conj_taylorProduct_div_im _ _ _ _ _

/-- Simultaneous reflection preserves the characteristic-function discrepancy
quotient. -/
theorem charFunDiscrepancyQuotient_reflectedLaw
    (mu nu : Measure ℝ) (t : ℝ) :
    charFunDiscrepancyQuotient (reflectedLaw mu) (reflectedLaw nu) t =
      charFunDiscrepancyQuotient mu nu t := by
  unfold charFunDiscrepancyQuotient
  by_cases ht : t = 0
  · simp [ht]
  · rw [if_neg ht, if_neg ht, charFun_reflectedLaw, charFun_reflectedLaw,
      MeasureTheory.charFun_neg, MeasureTheory.charFun_neg,
      ← map_sub, Complex.norm_conj]

/-- The reflected translated point-kernel integral is the real part of the
Prawitz inversion functional of the reflected law. -/
theorem integral_prawitzPointKernel_reflected_sub_eq_re_prawitzInversion
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzPointKernel T (x - y) ∂mu) =
      (prawitzInversion T
        (MeasureTheory.charFun (reflectedLaw mu)) (-x)).re := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  have hRef1 := integrable_abs_reflectedLaw mu hmu1
  have hForward := integrable_prawitzPointKernel_sub
    (reflectedLaw mu) hRef1 hT (-x)
  have hMap :
      (∫ z, prawitzPointKernel T (z - (-x)) ∂reflectedLaw mu) =
        ∫ y, prawitzPointKernel T (x - y) ∂mu := by
    unfold reflectedLaw
    rw [integral_map (by fun_prop) hForward.aestronglyMeasurable]
    apply integral_congr_ae
    exact ae_of_all mu fun y => congrArg (prawitzPointKernel T) (by ring)
  rw [← hMap]
  exact integral_prawitzPointKernel_sub_eq_re_prawitzInversion
    (reflectedLaw mu) hRef1 hT (-x)

/-- Frequency-space form of the expected signed imbalance.  The centered-CDF
term is retained explicitly, recording the part not supplied by the compact
Prawitz inversion. -/
theorem integral_prawitzSlackImbalance_eq_re_inversions_sub_centeredCDF
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzSlackImbalance T x y ∂mu) =
      (prawitzInversion T (MeasureTheory.charFun mu) x).re -
        (prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x)).re -
        (2 * cdf mu x - 1) := by
  rw [integral_prawitzSlackImbalance_eq_pointKernelDifference_sub_centeredCDF
      mu x
      (integrable_prawitzPointKernel_sub mu hmu1 hT x)
      (integrable_prawitzPointKernel_reflected_sub mu hmu1 hT x),
    integral_prawitzPointKernel_sub_eq_re_prawitzInversion mu hmu1 hT x,
    integral_prawitzPointKernel_reflected_sub_eq_re_prawitzInversion
      mu hmu1 hT x]

/-- Exact reverse one-sided CDF comparison, retaining the real part of the
reflected Prawitz inversion difference before taking its norm. -/
theorem cdf_sub_cdf_le_re_reflectedPrawitzInversion_sub_add_reflection
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf nu x - cdf mu x ≤
      (prawitzInversion T (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T (MeasureTheory.charFun (reflectedLaw nu)) (-x)).re +
      ∫ y, (prawitzPointKernel T (y - x) +
        prawitzPointKernel T (x - y)) ∂nu := by
  have hNu := cdf_sub_half_le_integral_prawitzPointKernel nu hT x
    (integrable_prawitzPointKernel_sub nu hnu1 hT x)
  have hMu := neg_integral_prawitzPointKernel_le_cdf_sub_half mu hT x
    (integrable_prawitzPointKernel_reflected_sub mu hmu1 hT x)
  have hNuForward := integrable_prawitzPointKernel_sub nu hnu1 hT x
  have hNuReflected :=
    integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x
  have hMuEq :=
    integral_prawitzPointKernel_reflected_sub_eq_re_prawitzInversion
      mu hmu1 hT x
  have hNuEq :=
    integral_prawitzPointKernel_reflected_sub_eq_re_prawitzInversion
      nu hnu1 hT x
  have hSum :
      (∫ y, (prawitzPointKernel T (y - x) +
          prawitzPointKernel T (x - y)) ∂nu) =
        (∫ y, prawitzPointKernel T (y - x) ∂nu) +
          ∫ y, prawitzPointKernel T (x - y) ∂nu :=
    integral_add hNuForward hNuReflected
  rw [Complex.sub_re, ← hMuEq, ← hNuEq, hSum]
  linarith

/-- The reverse one-sided CDF comparison, expressed through Prawitz inversion
of reflected laws and the same point-kernel reflection remainder. -/
theorem cdf_sub_cdf_le_norm_reflectedPrawitzInversion_sub_add_reflection
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf nu x - cdf mu x ≤
      ‖prawitzInversion T (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T (MeasureTheory.charFun (reflectedLaw nu)) (-x)‖ +
      ∫ y, (prawitzPointKernel T (y - x) +
        prawitzPointKernel T (x - y)) ∂nu := by
  have hNu := cdf_sub_half_le_integral_prawitzPointKernel nu hT x
    (integrable_prawitzPointKernel_sub nu hnu1 hT x)
  have hMu := neg_integral_prawitzPointKernel_le_cdf_sub_half mu hT x
    (integrable_prawitzPointKernel_reflected_sub mu hmu1 hT x)
  have hNuForward := integrable_prawitzPointKernel_sub nu hnu1 hT x
  have hNuReflected :=
    integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x
  have hMuEq :=
    integral_prawitzPointKernel_reflected_sub_eq_re_prawitzInversion
      mu hmu1 hT x
  have hNuEq :=
    integral_prawitzPointKernel_reflected_sub_eq_re_prawitzInversion
      nu hnu1 hT x
  have hSum :
      (∫ y, (prawitzPointKernel T (y - x) +
          prawitzPointKernel T (x - y)) ∂nu) =
        (∫ y, prawitzPointKernel T (y - x) ∂nu) +
          ∫ y, prawitzPointKernel T (x - y) ∂nu :=
    integral_add hNuForward hNuReflected
  have hRe :
      (prawitzInversion T (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T (MeasureTheory.charFun (reflectedLaw nu)) (-x)).re ≤
      ‖prawitzInversion T (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T (MeasureTheory.charFun (reflectedLaw nu)) (-x)‖ :=
    Complex.re_le_norm _
  rw [Complex.sub_re] at hRe
  rw [← hMuEq, ← hNuEq] at hRe
  rw [hSum]
  linarith

/-- Simultaneous reflection leaves the weighted positive-frequency Prawitz
discrepancy estimate unchanged. -/
theorem norm_prawitzInversion_reflectedLaw_sub_le_integral_discrepancy
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    ‖prawitzInversion T (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T (MeasureTheory.charFun (reflectedLaw nu)) (-x)‖ ≤
      (1 / Real.pi) * ∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu nu t := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure (reflectedLaw nu) :=
    reflectedLaw_isProbabilityMeasure nu
  have hmuRef := integrable_abs_reflectedLaw mu hmu1
  have hnuRef := integrable_abs_reflectedLaw nu hnu1
  have h := norm_prawitzInversion_charFun_sub_le_integral_discrepancy
    (reflectedLaw mu) (reflectedLaw nu) hmuRef hnuRef hT (-x)
  simpa only [charFunDiscrepancyQuotient_reflectedLaw] using h

/-- Subtracting the expected slack imbalances of two laws separates into the
two Prawitz inversion differences and twice their signed CDF difference. -/
theorem integral_prawitzSlackImbalance_sub_eq_re_inversionDifferences_sub_two_mul_cdfDifference
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzSlackImbalance T x y ∂mu) -
        (∫ y, prawitzSlackImbalance T x y ∂nu) =
      (prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x).re -
      (prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x)).re -
      2 * (cdf mu x - cdf nu x) := by
  rw [integral_prawitzSlackImbalance_eq_re_inversions_sub_centeredCDF
      mu hmu1 hT x,
    integral_prawitzSlackImbalance_eq_re_inversions_sub_centeredCDF
      nu hnu1 hT x]
  simp only [Complex.sub_re]
  ring

/-- The expected imbalance difference is controlled by the forward and
reflected inversion discrepancies together with twice the CDF gap. -/
theorem abs_integral_prawitzSlackImbalance_sub_le_inversions_add_two_mul_cdfGap
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |(∫ y, prawitzSlackImbalance T x y ∂mu) -
        (∫ y, prawitzSlackImbalance T x y ∂nu)| ≤
      ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ +
      ‖prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x)‖ +
      2 * cdfGap mu nu x := by
  rw [integral_prawitzSlackImbalance_sub_eq_re_inversionDifferences_sub_two_mul_cdfDifference
    mu nu hmu1 hnu1 hT x]
  let A : ℂ := prawitzInversion T (MeasureTheory.charFun mu) x -
    prawitzInversion T (MeasureTheory.charFun nu) x
  let B : ℂ := prawitzInversion T
      (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
    prawitzInversion T
      (MeasureTheory.charFun (reflectedLaw nu)) (-x)
  let d : ℝ := cdf mu x - cdf nu x
  change |A.re - B.re - 2 * d| ≤ ‖A‖ + ‖B‖ + 2 * |d|
  calc
    |A.re - B.re - 2 * d| ≤ |A.re - B.re| + |2 * d| := abs_sub _ _
    _ ≤ (|A.re| + |B.re|) + |2 * d| :=
      add_le_add (abs_sub A.re B.re) le_rfl
    _ = |A.re| + |B.re| + 2 * |d| := by
      rw [abs_mul, abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
    _ ≤ ‖A‖ + ‖B‖ + 2 * |d| := by
      gcongr
      · exact Complex.abs_re_le_norm A
      · exact Complex.abs_re_le_norm B

/-- Applying the common positive-frequency discrepancy estimate to both
inversion terms exposes the factor-two CDF-gap feedback in the imbalance
comparison. -/
theorem abs_integral_prawitzSlackImbalance_sub_le_two_mul_discrepancy_add_two_mul_cdfGap
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |(∫ y, prawitzSlackImbalance T x y ∂mu) -
        (∫ y, prawitzSlackImbalance T x y ∂nu)| ≤
      2 * ((1 / Real.pi) * ∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu nu t) +
      2 * cdfGap mu nu x := by
  have himbalance :=
    abs_integral_prawitzSlackImbalance_sub_le_inversions_add_two_mul_cdfGap
      mu nu hmu1 hnu1 hT x
  have hforward :=
    norm_prawitzInversion_charFun_sub_le_integral_discrepancy
      mu nu hmu1 hnu1 hT x
  have hreflected :=
    norm_prawitzInversion_reflectedLaw_sub_le_integral_discrepancy
      mu nu hmu1 hnu1 hT x
  linarith

/-- After removing the diagonal atom, the exact two-law imbalance retains one
closed-CDF difference and one strict-lower-tail difference, with their signs
kept explicit. -/
theorem integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_inversionDifferences_sub_cdf_sub_Iio
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu) =
      (prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x).re -
      (prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x)).re -
      (cdf mu x - cdf nu x) -
      (mu.real (Iio x) - nu.real (Iio x)) := by
  have himbalance :=
    integral_prawitzSlackImbalance_sub_eq_re_inversionDifferences_sub_two_mul_cdfDifference
      mu nu hmu1 hnu1 hT x
  have hsplit :=
    integral_prawitzSlackImbalance_sub_eq_offDiagonal_sub_atomDifference
      mu nu x
      (integrable_prawitzPointKernel_sub mu hmu1 hT x)
      (integrable_prawitzPointKernel_reflected_sub mu hmu1 hT x)
      (integrable_prawitzPointKernel_sub nu hnu1 hT x)
      (integrable_prawitzPointKernel_reflected_sub nu hnu1 hT x)
  have hatom := measureReal_singleton_sub_eq_cdf_sub_sub_Iio mu nu x
  linarith

/-- Midpoint-CDF form of the exact off-diagonal imbalance identity.  The two
CDF-side coordinates combine into twice their atom-symmetric midpoint. -/
theorem integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_inversionDifferences_sub_two_mul_midpointCDFDifference
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu) =
      (prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x).re -
      (prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x)).re -
      2 * midpointCDFDifference mu nu x := by
  rw [integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_inversionDifferences_sub_cdf_sub_Iio
    mu nu hmu1 hnu1 hT x, midpointCDFDifference_eq]
  ring

/-- Taking norms in the midpoint-CDF identity keeps the residual CDF term as
one canonical quantity with its exact coefficient two. -/
theorem abs_integral_prawitzSlackImbalanceOffDiagonal_sub_le_inversions_add_two_mul_midpointCDFDifference
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |(∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu)| ≤
      ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ +
      ‖prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x)‖ +
      2 * |midpointCDFDifference mu nu x| := by
  rw [integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_inversionDifferences_sub_two_mul_midpointCDFDifference
    mu nu hmu1 hnu1 hT x]
  let A : ℂ := prawitzInversion T (MeasureTheory.charFun mu) x -
    prawitzInversion T (MeasureTheory.charFun nu) x
  let B : ℂ := prawitzInversion T
      (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
    prawitzInversion T
      (MeasureTheory.charFun (reflectedLaw nu)) (-x)
  let d : ℝ := midpointCDFDifference mu nu x
  change |A.re - B.re - 2 * d| ≤ ‖A‖ + ‖B‖ + 2 * |d|
  calc
    |A.re - B.re - 2 * d| ≤ |A.re - B.re| + |2 * d| := abs_sub _ _
    _ ≤ (|A.re| + |B.re|) + |2 * d| :=
      add_le_add (abs_sub A.re B.re) le_rfl
    _ = |A.re| + |B.re| + 2 * |d| := by
      rw [abs_mul, abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
    _ ≤ ‖A‖ + ‖B‖ + 2 * |d| := by
      gcongr
      · exact Complex.abs_re_le_norm A
      · exact Complex.abs_re_le_norm B

/-- The off-diagonal imbalance difference is bounded by both inversion norms
plus its closed and strict-lower CDF coordinates. -/
theorem abs_integral_prawitzSlackImbalanceOffDiagonal_sub_le_inversions_add_cdf_coordinates
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |(∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu)| ≤
      ‖prawitzInversion T (MeasureTheory.charFun mu) x -
        prawitzInversion T (MeasureTheory.charFun nu) x‖ +
      ‖prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x)‖ +
      cdfGap mu nu x +
      |mu.real (Iio x) - nu.real (Iio x)| := by
  rw [integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_inversionDifferences_sub_cdf_sub_Iio
    mu nu hmu1 hnu1 hT x]
  let A : ℂ := prawitzInversion T (MeasureTheory.charFun mu) x -
    prawitzInversion T (MeasureTheory.charFun nu) x
  let B : ℂ := prawitzInversion T
      (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
    prawitzInversion T
      (MeasureTheory.charFun (reflectedLaw nu)) (-x)
  let d : ℝ := cdf mu x - cdf nu x
  let o : ℝ := mu.real (Iio x) - nu.real (Iio x)
  change |A.re - B.re - d - o| ≤ ‖A‖ + ‖B‖ + |d| + |o|
  calc
    |A.re - B.re - d - o| ≤ |A.re - B.re - d| + |o| := abs_sub _ _
    _ ≤ (|A.re - B.re| + |d|) + |o| :=
      add_le_add (abs_sub (A.re - B.re) d) le_rfl
    _ ≤ ((|A.re| + |B.re|) + |d|) + |o| := by
      gcongr
      exact abs_sub A.re B.re
    _ ≤ ‖A‖ + ‖B‖ + |d| + |o| := by
      gcongr
      · exact Complex.abs_re_le_norm A
      · exact Complex.abs_re_le_norm B

/-- Applying the common frequency discrepancy estimate to both inversion
coordinates leaves exactly the two CDF-side coordinates. -/
theorem abs_integral_prawitzSlackImbalanceOffDiagonal_sub_le_two_mul_discrepancy_add_cdf_coordinates
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |(∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu)| ≤
      2 * ((1 / Real.pi) * ∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu nu t) +
      cdfGap mu nu x +
      |mu.real (Iio x) - nu.real (Iio x)| := by
  have hoff :=
    abs_integral_prawitzSlackImbalanceOffDiagonal_sub_le_inversions_add_cdf_coordinates
      mu nu hmu1 hnu1 hT x
  have hforward :=
    norm_prawitzInversion_charFun_sub_le_integral_discrepancy
      mu nu hmu1 hnu1 hT x
  have hreflected :=
    norm_prawitzInversion_reflectedLaw_sub_le_integral_discrepancy
      mu nu hmu1 hnu1 hT x
  linarith

/-- Fully separating the two CDF coordinates recovers the same coefficient
two in Kolmogorov distance. -/
theorem abs_integral_prawitzSlackImbalanceOffDiagonal_sub_le_two_mul_discrepancy_add_two_mul_kolmogorovDistance
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    |(∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu)| ≤
      2 * ((1 / Real.pi) * ∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu nu t) +
      2 * kolmogorovDistance mu nu := by
  have hoff :=
    abs_integral_prawitzSlackImbalanceOffDiagonal_sub_le_two_mul_discrepancy_add_cdf_coordinates
      mu nu hmu1 hnu1 hT x
  have hClosed := cdfGap_le_kolmogorovDistance mu nu x
  have hOpen := measureReal_Iio_gap_le_kolmogorovDistance mu nu x
  linarith

/-- Exact signed-integral representation for the reflected Prawitz inversion
difference used in the reverse CDF comparison. -/
theorem re_reflectedPrawitzInversion_charFun_sub_eq_signedIntegral
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (prawitzInversion T (MeasureTheory.charFun (reflectedLaw mu)) (-x) -
        prawitzInversion T (MeasureTheory.charFun (reflectedLaw nu)) (-x)).re =
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          (symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun (reflectedLaw nu)) (-x) t)).re := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure (reflectedLaw nu) :=
    reflectedLaw_isProbabilityMeasure nu
  exact re_prawitzInversion_charFun_sub_eq_signedIntegral
    (reflectedLaw mu) (reflectedLaw nu)
    (integrable_abs_reflectedLaw mu hmu1)
    (integrable_abs_reflectedLaw nu hnu1) hT (-x)

/-- The joint positive-frequency integrand that retains the forward,
reflected, and midpoint-CDF coordinates before any triangle inequality. -/
noncomputable def midpointCorrectedPrawitzJointIntegrand
    (mu nu : Measure ℝ) (T x t : ℝ) : ℂ :=
  (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
      symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t) -
    (symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
      symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw nu)) (-x) t) +
    (((4 * Real.pi / T * midpointCDFDifference mu nu x : ℝ) : ℂ) *
      Complex.I)

/-- The joint forward/reflected/midpoint integrand is integrable on every
positive frequency window under finite first moments. -/
theorem integrableOn_midpointCorrectedPrawitzJointIntegrand
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    IntegrableOn (midpointCorrectedPrawitzJointIntegrand mu nu T x)
      (Icc 0 T) := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure (reflectedLaw nu) :=
    reflectedLaw_isProbabilityMeasure nu
  have hForward : IntegrableOn (fun t =>
      symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
        symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t)
      (Icc 0 T) :=
    (integrableOn_symmetricPrawitzPVIntegrand_charFun mu hmu1 hT x).sub
      (integrableOn_symmetricPrawitzPVIntegrand_charFun nu hnu1 hT x)
  have hReflected : IntegrableOn (fun t =>
      symmetricPrawitzPVIntegrand T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
        symmetricPrawitzPVIntegrand T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x) t)
      (Icc 0 T) :=
    (integrableOn_symmetricPrawitzPVIntegrand_charFun
        (reflectedLaw mu) (integrable_abs_reflectedLaw mu hmu1) hT (-x)).sub
      (integrableOn_symmetricPrawitzPVIntegrand_charFun
        (reflectedLaw nu) (integrable_abs_reflectedLaw nu hnu1) hT (-x))
  have hConstant : IntegrableOn (fun _t : ℝ =>
      (((4 * Real.pi / T * midpointCDFDifference mu nu x : ℝ) : ℂ) *
        Complex.I)) (Icc 0 T) :=
    integrableOn_const (by simp [Real.volume_Icc])
  simpa only [midpointCorrectedPrawitzJointIntegrand] using
    (hForward.sub hReflected).add hConstant

/-- Exact one-integral form of the off-diagonal Prawitz imbalance.  The
constant imaginary correction integrates to the midpoint-CDF term, so all
relative signs remain visible inside one positive-frequency integral. -/
theorem integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_jointSignedIntegral
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu) =
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          midpointCorrectedPrawitzJointIntegrand mu nu T x t).re := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure (reflectedLaw nu) :=
    reflectedLaw_isProbabilityMeasure nu
  let F : ℝ → ℂ := fun t =>
    symmetricPrawitzPVIntegrand T (MeasureTheory.charFun mu) x t -
      symmetricPrawitzPVIntegrand T (MeasureTheory.charFun nu) x t
  let R : ℝ → ℂ := fun t =>
    symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
      symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw nu)) (-x) t
  let C : ℂ :=
    ((4 * Real.pi / T * midpointCDFDifference mu nu x : ℝ) : ℂ) *
      Complex.I
  have hF : IntegrableOn F (Icc 0 T) :=
    (integrableOn_symmetricPrawitzPVIntegrand_charFun mu hmu1 hT x).sub
      (integrableOn_symmetricPrawitzPVIntegrand_charFun nu hnu1 hT x)
  have hR : IntegrableOn R (Icc 0 T) :=
    (integrableOn_symmetricPrawitzPVIntegrand_charFun
        (reflectedLaw mu) (integrable_abs_reflectedLaw mu hmu1) hT (-x)).sub
      (integrableOn_symmetricPrawitzPVIntegrand_charFun
        (reflectedLaw nu) (integrable_abs_reflectedLaw nu hnu1) hT (-x))
  have hJoint :
      (∫ t in Icc 0 T,
          midpointCorrectedPrawitzJointIntegrand mu nu T x t) =
        (∫ t in Icc 0 T, F t) - (∫ t in Icc 0 T, R t) + T • C := by
    change (∫ t in Icc 0 T, F t - R t + C) = _
    have hC : IntegrableOn (fun _t : ℝ => C) (Icc 0 T) :=
      integrableOn_const (by simp [Real.volume_Icc])
    have hAdd := integral_add (hF.sub hR) hC
    change (∫ t in Icc 0 T, F t - R t + C) =
      (∫ t in Icc 0 T, F t - R t) + (∫ _t in Icc 0 T, C) at hAdd
    rw [hAdd, integral_sub hF hR, setIntegral_const]
    congr 1
    rw [Measure.real, Real.volume_Icc,
      ENNReal.toReal_ofReal (sub_nonneg.mpr hT.le)]
    simp only [sub_zero]
    rfl
  rw [integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_inversionDifferences_sub_two_mul_midpointCDFDifference
      mu nu hmu1 hnu1 hT x,
    re_prawitzInversion_charFun_sub_eq_signedIntegral mu nu hmu1 hnu1 hT x,
    re_reflectedPrawitzInversion_charFun_sub_eq_signedIntegral
      mu nu hmu1 hnu1 hT x, hJoint]
  change
    ((Complex.I / (2 * Real.pi : ℝ)) * (∫ t in Icc 0 T, F t)).re -
          ((Complex.I / (2 * Real.pi : ℝ)) *
            (∫ t in Icc 0 T, R t)).re -
        2 * midpointCDFDifference mu nu x =
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ((∫ t in Icc 0 T, F t) - (∫ t in Icc 0 T, R t) + T • C)).re
  have hcoef :
      Complex.I / (2 * Real.pi : ℝ) =
        (((2 * Real.pi)⁻¹ : ℝ) : ℂ) * Complex.I := by
    rw [div_eq_mul_inv, ← Complex.ofReal_inv]
    ring
  have hTC :
      T • C =
        (((4 * Real.pi * midpointCDFDifference mu nu x : ℝ) : ℂ) *
          Complex.I) := by
    dsimp only [C]
    change ((T : ℂ) *
      (((4 * Real.pi / T * midpointCDFDifference mu nu x : ℝ) : ℂ) *
        Complex.I)) = _
    push_cast
    field_simp [hT.ne']
  have hCorrection :
      ((Complex.I / (2 * Real.pi : ℝ)) * (T • C)).re =
        -2 * midpointCDFDifference mu nu x := by
    rw [hcoef, hTC]
    simp only [Complex.mul_re, Complex.mul_im, Complex.I_re, Complex.I_im,
      Complex.ofReal_re, Complex.ofReal_im, mul_zero, sub_zero, add_zero,
      mul_one]
    field_simp [hT.ne', Real.pi_ne_zero]
    ring
  rw [mul_add, mul_sub]
  simp only [Complex.add_re, Complex.sub_re, hCorrection]
  ring

/-- Exact real coordinate of the joint signed integrand at nonzero frequency. -/
theorem re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand
    (mu nu : Measure ℝ) {T : ℝ} (hT : T ≠ 0) (x : ℝ)
    {t : ℝ} (ht : t ≠ 0) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
      -((prawitzPVNumerator T (MeasureTheory.charFun mu) x t).im -
          (prawitzPVNumerator T (MeasureTheory.charFun nu) x t).im) /
          (Real.pi * t) +
        ((prawitzPVNumerator T
            (MeasureTheory.charFun (reflectedLaw mu)) (-x) t).im -
          (prawitzPVNumerator T
            (MeasureTheory.charFun (reflectedLaw nu)) (-x) t).im) /
          (Real.pi * t) -
        2 * midpointCDFDifference mu nu x / T := by
  have hForward := re_prawitzCoefficient_mul_symmetric_sub (T := T)
    (isHermitianFrequencyFunction_charFun mu)
    (isHermitianFrequencyFunction_charFun nu) x ht
  have hReflected := re_prawitzCoefficient_mul_symmetric_sub (T := T)
    (isHermitianFrequencyFunction_charFun (reflectedLaw mu))
    (isHermitianFrequencyFunction_charFun (reflectedLaw nu)) (-x) ht
  have hCorrection :
      ((Complex.I / (2 * Real.pi : ℝ)) *
        (((4 * Real.pi / T * midpointCDFDifference mu nu x : ℝ) : ℂ) *
          Complex.I)).re =
        -2 * midpointCDFDifference mu nu x / T := by
    have hcoef :
        Complex.I / (2 * Real.pi : ℝ) =
          (((2 * Real.pi)⁻¹ : ℝ) : ℂ) * Complex.I := by
      rw [div_eq_mul_inv, ← Complex.ofReal_inv]
      ring
    rw [hcoef]
    simp only [Complex.mul_re, Complex.mul_im, Complex.I_re, Complex.I_im,
      Complex.ofReal_re, Complex.ofReal_im, mul_zero, sub_zero, add_zero,
      mul_one]
    field_simp [hT, Real.pi_ne_zero]
    ring
  rw [midpointCorrectedPrawitzJointIntegrand, mul_add, mul_sub]
  simp only [Complex.add_re, Complex.sub_re, hForward, hReflected,
    hCorrection]
  ring

/-- Simultaneous reflection cancels the imaginary filter coordinate from the
signed difference of forward and reflected Prawitz numerators. -/
theorem reflected_sub_im_sub_forward_eq_filter_re_mul_phase_im
    (mu nu : Measure ℝ) (T x t : ℝ) :
    -((prawitzPVNumerator T (MeasureTheory.charFun mu) x t).im -
        (prawitzPVNumerator T (MeasureTheory.charFun nu) x t).im) +
      ((prawitzPVNumerator T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) t).im -
        (prawitzPVNumerator T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x) t).im) =
      -2 * (scaledPrawitzSmoothingFilter T t).re *
        (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t)).im := by
  let M : ℂ := scaledPrawitzSmoothingFilter T t
  let z : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
    (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t)
  have hForward :
      prawitzPVNumerator T (MeasureTheory.charFun mu) x t -
          prawitzPVNumerator T (MeasureTheory.charFun nu) x t = M * z := by
    rw [prawitzPVNumerator_sub]
    dsimp only [M, z]
    ring
  have hPhase :
      Complex.exp (((-(t * (-x)) : ℝ) : ℂ) * Complex.I) *
          (MeasureTheory.charFun (reflectedLaw mu) t -
            MeasureTheory.charFun (reflectedLaw nu) t) =
        (starRingEnd ℂ) z := by
    dsimp only [z]
    rw [charFun_reflectedLaw, charFun_reflectedLaw,
      MeasureTheory.charFun_neg, MeasureTheory.charFun_neg]
    simp only [map_mul, map_sub]
    rw [← Complex.exp_conj]
    congr 2
    apply Complex.ext <;> simp <;> ring
  have hReflected :
      prawitzPVNumerator T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
        prawitzPVNumerator T
          (MeasureTheory.charFun (reflectedLaw nu)) (-x) t =
        M * (starRingEnd ℂ) z := by
    rw [prawitzPVNumerator_sub]
    calc
      Complex.exp (((-(t * -x) : ℝ) : ℂ) * Complex.I) *
            scaledPrawitzSmoothingFilter T t *
          (MeasureTheory.charFun (reflectedLaw mu) t -
            MeasureTheory.charFun (reflectedLaw nu) t) =
          M *
            (Complex.exp (((-(t * -x) : ℝ) : ℂ) * Complex.I) *
              (MeasureTheory.charFun (reflectedLaw mu) t -
                MeasureTheory.charFun (reflectedLaw nu) t)) := by
            dsimp only [M]
            ring
      _ = M * (starRingEnd ℂ) z := by rw [hPhase]
  rw [← Complex.sub_im, ← Complex.sub_im, hForward, hReflected]
  dsimp only [M, z]
  simp only [Complex.mul_im, Complex.conj_re, Complex.conj_im]
  ring

/-- The phase-shifted characteristic function has the expected sine
coordinate centered at the spatial point. -/
theorem phase_mul_charFun_im_eq_integral_sin_sub
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (x t : ℝ) :
    (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        MeasureTheory.charFun mu t).im =
      ∫ y, Real.sin (t * (y - x)) ∂mu := by
  rw [MeasureTheory.charFun_apply_real]
  have hMul :
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          (∫ y, Complex.exp ((t : ℂ) * (y : ℂ) * Complex.I) ∂mu) =
        ∫ y, Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
          Complex.exp ((t : ℂ) * (y : ℂ) * Complex.I) ∂mu :=
    (integral_const_mul _ _).symm
  rw [hMul]
  have hInt : Integrable (fun y : ℝ =>
      Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        Complex.exp ((t : ℂ) * (y : ℂ) * Complex.I)) mu := by
    refine Integrable.of_bound (by fun_prop) 1 ?_
    exact ae_of_all mu fun y => by
      simp [Complex.norm_exp, Complex.mul_re]
  have hIm :
      (∫ y, Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        Complex.exp ((t : ℂ) * (y : ℂ) * Complex.I) ∂mu).im =
      ∫ y, (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        Complex.exp ((t : ℂ) * (y : ℂ) * Complex.I)).im ∂mu := by
    simpa using (integral_im hInt).symm
  rw [hIm]
  apply integral_congr_ae
  exact ae_of_all mu fun y => by
    change (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
      Complex.exp ((t : ℂ) * (y : ℂ) * Complex.I)).im =
        Real.sin (t * (y - x))
    rw [← Complex.exp_add]
    have harg :
        (((-(t * x) : ℝ) : ℂ) * Complex.I) +
            ((t : ℂ) * (y : ℂ) * Complex.I) =
          (((t * (y - x) : ℝ) : ℂ) * Complex.I) := by
      push_cast
      ring
    rw [harg, Complex.exp_ofReal_mul_I_im]

/-- The phase-shifted characteristic-function discrepancy is the difference
of centered sine expectations. -/
theorem phase_mul_charFun_sub_im_eq_integral_sin_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (x t : ℝ) :
    (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
        (MeasureTheory.charFun mu t - MeasureTheory.charFun nu t)).im =
      (∫ y, Real.sin (t * (y - x)) ∂mu) -
        ∫ y, Real.sin (t * (y - x)) ∂nu := by
  rw [mul_sub, Complex.sub_im,
    phase_mul_charFun_im_eq_integral_sin_sub,
    phase_mul_charFun_im_eq_integral_sin_sub]

/-- Atom-symmetric lower-ray kernel: one below the cut, one half at the cut,
and zero above it. -/
noncomputable def midpointRayKernel (x y : ℝ) : ℝ :=
  (((Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y) +
    ((Iio x).indicator (fun _ : ℝ => (1 : ℝ)) y)) / 2

/-- Joint measurability of the atom-symmetric lower-ray kernel in its cut and
evaluation coordinates. -/
theorem measurable_midpointRayKernel_uncurry :
    Measurable (Function.uncurry midpointRayKernel) := by
  change Measurable (fun p : ℝ × ℝ =>
    ((if p.2 ≤ p.1 then (1 : ℝ) else 0) +
      (if p.2 < p.1 then (1 : ℝ) else 0)) / 2)
  apply Measurable.div_const
  apply Measurable.add
  · exact Measurable.ite (measurableSet_le measurable_snd measurable_fst)
      measurable_const measurable_const
  · exact Measurable.ite (measurableSet_lt measurable_snd measurable_fst)
      measurable_const measurable_const

theorem integral_midpointRayKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (x : ℝ) :
    ∫ y, midpointRayKernel x y ∂mu = midpointCDF mu x := by
  have hClosed : Integrable
      ((Iic x).indicator (fun _ : ℝ => (1 : ℝ))) mu :=
    (integrable_const (1 : ℝ)).indicator measurableSet_Iic
  have hOpen : Integrable
      ((Iio x).indicator (fun _ : ℝ => (1 : ℝ))) mu :=
    (integrable_const (1 : ℝ)).indicator measurableSet_Iio
  have hClosedInt :
      (∫ y, (Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y ∂mu) =
        mu.real (Iic x) := by
    simpa only [Pi.one_apply] using
      (integral_indicator_one (μ := mu) measurableSet_Iic)
  have hOpenInt :
      (∫ y, (Iio x).indicator (fun _ : ℝ => (1 : ℝ)) y ∂mu) =
        mu.real (Iio x) := by
    simpa only [Pi.one_apply] using
      (integral_indicator_one (μ := mu) measurableSet_Iio)
  change (∫ y, (((Iic x).indicator (fun _ : ℝ => (1 : ℝ)) y) +
    ((Iio x).indicator (fun _ : ℝ => (1 : ℝ)) y)) / 2 ∂mu) = _
  rw [integral_div, integral_add hClosed hOpen,
    hClosedInt, hOpenInt,
    midpointCDF, ProbabilityTheory.cdf_eq_real]

/-- One-law real kernel whose expectation combines the centered sine
coordinate with the atom-symmetric midpoint correction. -/
noncomputable def midpointCorrectedSineKernel
    (T x t y : ℝ) : ℝ :=
  (-2 * (scaledPrawitzSmoothingFilter T t).re /
      (Real.pi * t)) * Real.sin (t * (y - x)) -
    (2 / T) * midpointRayKernel x y

/-- Joint measurability of the midpoint-corrected sine kernel in frequency
and space. -/
theorem measurable_midpointCorrectedSineKernel_uncurry (T x : ℝ) :
    Measurable (fun p : ℝ × ℝ =>
      midpointCorrectedSineKernel T x p.1 p.2) := by
  unfold midpointCorrectedSineKernel
  have hfilter : Measurable (fun p : ℝ × ℝ =>
      scaledPrawitzSmoothingFilter T p.1) :=
    (measurable_scaledPrawitzSmoothingFilter T).comp measurable_fst
  have hray : Measurable (fun p : ℝ × ℝ => midpointRayKernel x p.2) := by
    unfold midpointRayKernel
    exact (((measurable_const.indicator measurableSet_Iic).comp measurable_snd).add
      ((measurable_const.indicator measurableSet_Iio).comp measurable_snd)).div_const 2
  fun_prop

theorem integral_midpointCorrectedSineKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu] (T x t : ℝ) :
    ∫ y, midpointCorrectedSineKernel T x t y ∂mu =
      (-2 * (scaledPrawitzSmoothingFilter T t).re *
          (∫ y, Real.sin (t * (y - x)) ∂mu)) /
        (Real.pi * t) -
      2 * midpointCDF mu x / T := by
  have hSin : Integrable (fun y : ℝ => Real.sin (t * (y - x))) mu := by
    refine Integrable.of_bound (by fun_prop) 1 ?_
    exact ae_of_all mu fun y => by
      simpa only [Real.norm_eq_abs] using Real.abs_sin_le_one (t * (y - x))
  have hMid : Integrable (midpointRayKernel x) mu := by
    unfold midpointRayKernel
    exact (((integrable_const (1 : ℝ)).indicator measurableSet_Iic).add
      ((integrable_const (1 : ℝ)).indicator measurableSet_Iio)).div_const 2
  change (∫ y,
      (-2 * (scaledPrawitzSmoothingFilter T t).re /
          (Real.pi * t)) * Real.sin (t * (y - x)) -
        (2 / T) * midpointRayKernel x y ∂mu) = _
  rw [integral_sub (hSin.const_mul _) (hMid.const_mul _),
    integral_const_mul, integral_const_mul,
    integral_midpointRayKernel]
  ring

theorem midpointRayKernel_of_lt {x y : ℝ} (hyx : y < x) :
    midpointRayKernel x y = 1 := by
  simp [midpointRayKernel, hyx, hyx.le]

@[simp] theorem midpointRayKernel_self (x : ℝ) :
    midpointRayKernel x x = 1 / 2 := by
  simp [midpointRayKernel]

theorem midpointRayKernel_of_lt' {x y : ℝ} (hxy : x < y) :
    midpointRayKernel x y = 0 := by
  simp [midpointRayKernel, hxy.le, not_le_of_gt hxy]

/-- The atom-symmetric lower-ray kernel always takes values in the unit
interval. -/
theorem abs_midpointRayKernel_le_one (x y : ℝ) :
    |midpointRayKernel x y| ≤ 1 := by
  rcases lt_trichotomy y x with hyx | hxy | hxy
  · rw [midpointRayKernel_of_lt hyx]
    norm_num
  · subst y
    rw [midpointRayKernel_self]
    norm_num
  · rw [midpointRayKernel_of_lt' hxy]
    norm_num

theorem midpointCorrectedSineKernel_of_lt
    (T t : ℝ) {x y : ℝ} (hyx : y < x) :
    midpointCorrectedSineKernel T x t y =
      (-2 * (scaledPrawitzSmoothingFilter T t).re /
        (Real.pi * t)) * Real.sin (t * (y - x)) - 2 / T := by
  rw [midpointCorrectedSineKernel, midpointRayKernel_of_lt hyx]
  ring

@[simp] theorem midpointCorrectedSineKernel_self
    (T x t : ℝ) :
    midpointCorrectedSineKernel T x t x = -1 / T := by
  rw [midpointCorrectedSineKernel, midpointRayKernel_self]
  simp only [sub_self, mul_zero, Real.sin_zero, mul_zero, zero_sub]
  ring

theorem midpointCorrectedSineKernel_of_lt'
    (T t : ℝ) {x y : ℝ} (hxy : x < y) :
    midpointCorrectedSineKernel T x t y =
      (-2 * (scaledPrawitzSmoothingFilter T t).re /
        (Real.pi * t)) * Real.sin (t * (y - x)) := by
  rw [midpointCorrectedSineKernel, midpointRayKernel_of_lt' hxy]
  ring

/-- A first-moment-compatible majorant for the midpoint-corrected sine
kernel on the positive Prawitz frequency window.  The apparent singularity
at `t = 0` is removed by `|sin (t z)| ≤ |t z|`. -/
theorem abs_midpointCorrectedSineKernel_le
    {T t : ℝ} (hT : 0 < T) (ht0 : 0 < t) (htT : t ≤ T)
    (x y : ℝ) :
    |midpointCorrectedSineKernel T x t y| ≤
      14 * |y - x| + 2 / T := by
  have hfilter : |(scaledPrawitzSmoothingFilter T t).re| ≤ 7 := by
    exact (Complex.abs_re_le_norm _).trans
      (norm_scaledPrawitzSmoothingFilter_le_seven hT ⟨ht0.le, htT⟩)
  have hsin : |Real.sin (t * (y - x))| ≤ t * |y - x| := by
    calc
      |Real.sin (t * (y - x))| ≤ |t * (y - x)| := Real.abs_sin_le_abs
      _ = t * |y - x| := by rw [abs_mul, abs_of_pos ht0]
  have hpi : 1 ≤ Real.pi := by
    nlinarith [Real.one_le_pi_div_two]
  have hfirst :
      |(-2 * (scaledPrawitzSmoothingFilter T t).re /
          (Real.pi * t)) * Real.sin (t * (y - x))| ≤
        14 * |y - x| := by
    rw [abs_mul, abs_div, abs_mul, abs_neg,
      abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2),
      abs_mul, abs_of_pos Real.pi_pos, abs_of_pos ht0]
    calc
      (2 * |(scaledPrawitzSmoothingFilter T t).re| / (Real.pi * t)) *
          |Real.sin (t * (y - x))| ≤
        (2 * 7 / (Real.pi * t)) * (t * |y - x|) := by
          gcongr
      _ = (14 / Real.pi) * |y - x| := by
        field_simp [ht0.ne', Real.pi_ne_zero]
        ring
      _ ≤ 14 * |y - x| := by
        gcongr
        exact (div_le_iff₀ Real.pi_pos).2 (by nlinarith)
  have hray : |(2 / T) * midpointRayKernel x y| ≤ 2 / T := by
    rw [abs_mul, abs_of_nonneg (div_nonneg (by norm_num) hT.le)]
    exact mul_le_of_le_one_right (div_nonneg (by norm_num) hT.le)
      (abs_midpointRayKernel_le_one x y)
  unfold midpointCorrectedSineKernel
  calc
    |_ - _| ≤
        |(-2 * (scaledPrawitzSmoothingFilter T t).re /
          (Real.pi * t)) * Real.sin (t * (y - x))| +
        |(2 / T) * midpointRayKernel x y| := abs_sub _ _
    _ ≤ 14 * |y - x| + 2 / T := add_le_add hfirst hray

/-- Closed-window form of the first-moment majorant, including the removable
frequency `t = 0`. -/
theorem abs_midpointCorrectedSineKernel_le_of_mem_Icc
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Icc (0 : ℝ) T)
    (x y : ℝ) :
    |midpointCorrectedSineKernel T x t y| ≤
      14 * |y - x| + 2 / T := by
  rcases ht with ⟨ht0, htT⟩
  rcases ht0.eq_or_lt with rfl | ht0
  · unfold midpointCorrectedSineKernel
    simp only [mul_zero, Real.sin_zero, zero_mul, div_zero,
      zero_sub, abs_neg]
    have hray : |(2 / T) * midpointRayKernel x y| ≤ 2 / T := by
      rw [abs_mul, abs_of_nonneg (div_nonneg (by norm_num) hT.le)]
      exact mul_le_of_le_one_right (div_nonneg (by norm_num) hT.le)
        (abs_midpointRayKernel_le_one x y)
    exact hray.trans
      (le_add_of_nonneg_left (mul_nonneg (by norm_num) (abs_nonneg _)))
  · exact abs_midpointCorrectedSineKernel_le hT ht0 htT x y

/-- Finite first moment makes the midpoint-corrected sine kernel integrable
on the product of its closed frequency window with the probability law. -/
theorem integrable_prod_midpointCorrectedSineKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (fun p : ℝ × ℝ =>
      midpointCorrectedSineKernel T x p.1 p.2)
      ((volume.restrict (Icc 0 T)).prod mu) := by
  have hid : Integrable (fun y : ℝ => y) mu := by
    refine (integrable_norm_iff
      (f := fun y : ℝ => y) measurable_id.aestronglyMeasurable).mp ?_
    simpa only [Real.norm_eq_abs] using hmu1
  have hshift : Integrable (fun y : ℝ => y - x) mu :=
    hid.sub (integrable_const x)
  have habs : Integrable (fun y : ℝ => |y - x|) mu := by
    simpa only [Real.norm_eq_abs] using hshift.norm
  have hmajorY : Integrable (fun y : ℝ => 14 * |y - x| + 2 / T) mu :=
    (habs.const_mul 14).add (integrable_const (2 / T))
  have hmajor : Integrable (fun p : ℝ × ℝ =>
      14 * |p.2 - x| + 2 / T) ((volume.restrict (Icc 0 T)).prod mu) :=
    hmajorY.comp_snd (volume.restrict (Icc 0 T))
  apply hmajor.mono'
  · exact (measurable_midpointCorrectedSineKernel_uncurry T x).aestronglyMeasurable
  · have hmem : ∀ᵐ p : ℝ × ℝ ∂((volume.restrict (Icc 0 T)).prod mu),
        p.1 ∈ Icc (0 : ℝ) T := by
      rw [MeasureTheory.Measure.ae_prod_iff_ae_ae
        (measurableSet_Icc.preimage measurable_fst)]
      filter_upwards [ae_restrict_mem measurableSet_Icc] with t ht
      exact ae_of_all mu fun _ => ht
    filter_upwards [hmem] with p hp
    simpa only [Real.norm_eq_abs] using
      (abs_midpointCorrectedSineKernel_le_of_mem_Icc hT hp x p.2)

/-- Spatial kernel obtained by retaining the signed midpoint-corrected
frequency average over the full Prawitz window. -/
noncomputable def midpointCorrectedSineAveragedKernel
    (T x y : ℝ) : ℝ :=
  ∫ t in Icc 0 T, midpointCorrectedSineKernel T x t y

/-- The averaged kernel retains the centered diagonal value exactly. -/
@[simp] theorem midpointCorrectedSineAveragedKernel_self
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    midpointCorrectedSineAveragedKernel T x x = -1 := by
  unfold midpointCorrectedSineAveragedKernel
  simp_rw [midpointCorrectedSineKernel_self]
  simp [hT.le]
  field_simp [hT.ne']

/-- The frequency-averaged spatial kernel is integrable under every
probability law with a finite first absolute moment. -/
theorem integrable_midpointCorrectedSineAveragedKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    Integrable (midpointCorrectedSineAveragedKernel T x) mu := by
  exact (integrable_prod_midpointCorrectedSineKernel mu hmu1 hT x).integral_prod_right

/-- Fubini identifies the frequency integral of kernel expectations with the
expectation of the signed frequency-averaged spatial kernel. -/
theorem integral_integral_midpointCorrectedSineKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ t in Icc 0 T,
        ∫ y, midpointCorrectedSineKernel T x t y ∂mu) =
      ∫ y, midpointCorrectedSineAveragedKernel T x y ∂mu := by
  exact integral_integral_swap
    (integrable_prod_midpointCorrectedSineKernel mu hmu1 hT x)

/-- One full sine period to the left of the center, the sine coordinate
vanishes and only the lower-ray correction remains. -/
theorem midpointCorrectedSineKernel_left_latticePoint
    {T t : ℝ} (ht : 0 < t) (x : ℝ) :
    midpointCorrectedSineKernel T x t (x - 2 * Real.pi / t) = -2 / T := by
  have hyx : x - 2 * Real.pi / t < x := by
    have hdiv : 0 < 2 * Real.pi / t := div_pos (by positivity) ht
    linarith
  rw [midpointCorrectedSineKernel_of_lt T t hyx]
  have hphase : t * ((x - 2 * Real.pi / t) - x) = -2 * Real.pi := by
    field_simp [ht.ne']
    ring
  rw [hphase, show -2 * Real.pi = -(2 * Real.pi) by ring,
    Real.sin_neg, Real.sin_two_pi]
  ring

/-- One full sine period to the right of the center, both terms in the
midpoint-corrected kernel vanish. -/
theorem midpointCorrectedSineKernel_right_latticePoint
    {T t : ℝ} (ht : 0 < t) (x : ℝ) :
    midpointCorrectedSineKernel T x t (x + 2 * Real.pi / t) = 0 := by
  have hxy : x < x + 2 * Real.pi / t := by
    have hdiv : 0 < 2 * Real.pi / t := div_pos (by positivity) ht
    linarith
  rw [midpointCorrectedSineKernel_of_lt' T t hxy]
  have hphase : t * ((x + 2 * Real.pi / t) - x) = 2 * Real.pi := by
    field_simp [ht.ne']
    ring
  rw [hphase, Real.sin_two_pi]
  ring

/-- Three exact values across the midpoint jump already contribute `2 / T`
to the variation of the pointwise-frequency kernel. -/
theorem midpointCorrectedSineKernel_threePointVariation
    {T t : ℝ} (hT : 0 < T) (ht : 0 < t) (x : ℝ) :
    |midpointCorrectedSineKernel T x t (x - 2 * Real.pi / t) -
        midpointCorrectedSineKernel T x t x| +
      |midpointCorrectedSineKernel T x t x -
        midpointCorrectedSineKernel T x t (x + 2 * Real.pi / t)| = 2 / T := by
  rw [midpointCorrectedSineKernel_left_latticePoint ht,
    midpointCorrectedSineKernel_self,
    midpointCorrectedSineKernel_right_latticePoint ht]
  have hneg1 : -2 / T - (-1 / T) ≤ 0 := by
    have hTinv : 0 ≤ 1 / T := div_nonneg (by norm_num) hT.le
    rw [show -2 / T - (-1 / T) = -(1 / T) by ring]
    exact neg_nonpos.mpr hTinv
  have hneg2 : -1 / T - 0 ≤ 0 := by
    have hTinv : 0 ≤ 1 / T := div_nonneg (by norm_num) hT.le
    rw [show -1 / T - 0 = -(1 / T) by ring]
    exact neg_nonpos.mpr hTinv
  rw [abs_of_nonpos hneg1, abs_of_nonpos hneg2]
  ring

/-- Any `C / T` majorant for these three increments has coefficient at least
two, so the joint signed kernel cannot yield coefficient-one Kolmogorov
control through a plain total-variation estimate. -/
theorem two_le_of_midpointCorrectedSineKernel_threePointVariation_le
    {T t : ℝ} (hT : 0 < T) (ht : 0 < t) (x C : ℝ)
    (hC :
      |midpointCorrectedSineKernel T x t (x - 2 * Real.pi / t) -
          midpointCorrectedSineKernel T x t x| +
        |midpointCorrectedSineKernel T x t x -
          midpointCorrectedSineKernel T x t (x + 2 * Real.pi / t)| ≤ C / T) :
    2 ≤ C := by
  rw [midpointCorrectedSineKernel_threePointVariation hT ht x] at hC
  exact (div_le_div_iff_of_pos_right hT).mp hC

/-- The pointwise joint coordinate depends only on the real Prawitz-filter
coordinate; simultaneous reflection cancels its imaginary coordinate. -/
theorem re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand_eq_filter_re
    (mu nu : Measure ℝ) {T : ℝ} (hT : T ≠ 0) (x : ℝ)
    {t : ℝ} (ht : t ≠ 0) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
      (-2 * (scaledPrawitzSmoothingFilter T t).re *
          (Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) *
            (MeasureTheory.charFun mu t -
              MeasureTheory.charFun nu t)).im) /
        (Real.pi * t) -
      2 * midpointCDFDifference mu nu x / T := by
  rw [re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand
    mu nu hT x ht]
  have hCancel :=
    reflected_sub_im_sub_forward_eq_filter_re_mul_phase_im mu nu T x t
  calc
    -((prawitzPVNumerator T (MeasureTheory.charFun mu) x t).im -
          (prawitzPVNumerator T (MeasureTheory.charFun nu) x t).im) /
          (Real.pi * t) +
        ((prawitzPVNumerator T
            (MeasureTheory.charFun (reflectedLaw mu)) (-x) t).im -
          (prawitzPVNumerator T
            (MeasureTheory.charFun (reflectedLaw nu)) (-x) t).im) /
          (Real.pi * t) -
        2 * midpointCDFDifference mu nu x / T =
      (-((prawitzPVNumerator T (MeasureTheory.charFun mu) x t).im -
          (prawitzPVNumerator T (MeasureTheory.charFun nu) x t).im) +
        ((prawitzPVNumerator T
            (MeasureTheory.charFun (reflectedLaw mu)) (-x) t).im -
          (prawitzPVNumerator T
            (MeasureTheory.charFun (reflectedLaw nu)) (-x) t).im)) /
          (Real.pi * t) -
        2 * midpointCDFDifference mu nu x / T := by ring
    _ = _ := by rw [hCancel]

/-- Real measure-coordinate form of the joint signed integrand.  The frequency
term is the difference of centered sine expectations, kept signed against the
midpoint-CDF correction. -/
theorem re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand_eq_sine
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (hT : T ≠ 0) (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
      (-2 * (scaledPrawitzSmoothingFilter T t).re *
          ((∫ y, Real.sin (t * (y - x)) ∂mu) -
            ∫ y, Real.sin (t * (y - x)) ∂nu)) /
        (Real.pi * t) -
      2 * midpointCDFDifference mu nu x / T := by
  rw [re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand_eq_filter_re
    mu nu hT x ht,
    phase_mul_charFun_sub_im_eq_integral_sin_sub]

/-- The joint signed Prawitz coordinate is one real-kernel expectation
difference, before any triangle or variation bound. -/
theorem re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand_eq_kernel
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    {T : ℝ} (hT : T ≠ 0) (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
      (∫ y, midpointCorrectedSineKernel T x t y ∂mu) -
        ∫ y, midpointCorrectedSineKernel T x t y ∂nu := by
  rw [re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand_eq_sine
    mu nu hT x ht,
    integral_midpointCorrectedSineKernel,
    integral_midpointCorrectedSineKernel]
  unfold midpointCDFDifference
  ring

/-- Retaining the signed frequency integral turns the joint Prawitz
coordinate into an integral of real kernel-expectation differences.  This is
the pre-Fubini form needed before constructing an averaged spatial kernel. -/
theorem re_jointSignedIntegral_eq_integral_midpointCorrectedSineKernel_expectation_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
      ∫ t in Icc 0 T,
        ((∫ y, midpointCorrectedSineKernel T x t y ∂mu) -
          ∫ y, midpointCorrectedSineKernel T x t y ∂nu) := by
  have hJoint : IntegrableOn
      (midpointCorrectedPrawitzJointIntegrand mu nu T x) (Icc 0 T) :=
    integrableOn_midpointCorrectedPrawitzJointIntegrand
      mu nu hmu1 hnu1 hT x
  let c : ℂ := Complex.I / (2 * Real.pi : ℝ)
  have hScaled : IntegrableOn (fun t => c *
      midpointCorrectedPrawitzJointIntegrand mu nu T x t) (Icc 0 T) :=
    hJoint.const_mul c
  calc
    (c * ∫ t in Icc 0 T,
        midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
        (∫ t in Icc 0 T,
          c * midpointCorrectedPrawitzJointIntegrand mu nu T x t).re := by
      congr 1
      exact (integral_const_mul
        (μ := volume.restrict (Icc (0 : ℝ) T)) c
        (midpointCorrectedPrawitzJointIntegrand mu nu T x)).symm
    _ = ∫ t in Icc 0 T,
        (c * midpointCorrectedPrawitzJointIntegrand mu nu T x t).re :=
      (integral_re hScaled).symm
    _ = ∫ t in Icc 0 T,
        ((∫ y, midpointCorrectedSineKernel T x t y ∂mu) -
          ∫ y, midpointCorrectedSineKernel T x t y ∂nu) := by
      apply integral_congr_ae
      filter_upwards [(volume.restrict (Icc (0 : ℝ) T)).ae_ne 0] with t ht
      dsimp only [c]
      exact re_prawitzCoefficient_mul_midpointCorrectedPrawitzJointIntegrand_eq_kernel
        mu nu hT.ne' x ht

/-- Fubini form of the exact joint signed coordinate: it is the difference
of expectations of one frequency-averaged spatial kernel. -/
theorem re_jointSignedIntegral_eq_integral_midpointCorrectedSineAveragedKernel_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          midpointCorrectedPrawitzJointIntegrand mu nu T x t).re =
      (∫ y, midpointCorrectedSineAveragedKernel T x y ∂mu) -
        ∫ y, midpointCorrectedSineAveragedKernel T x y ∂nu := by
  rw [re_jointSignedIntegral_eq_integral_midpointCorrectedSineKernel_expectation_sub
    mu nu hmu1 hnu1 hT x]
  have hmuProd := integrable_prod_midpointCorrectedSineKernel mu hmu1 hT x
  have hnuProd := integrable_prod_midpointCorrectedSineKernel nu hnu1 hT x
  rw [integral_sub hmuProd.integral_prod_left hnuProd.integral_prod_left,
    integral_integral_midpointCorrectedSineKernel mu hmu1 hT x,
    integral_integral_midpointCorrectedSineKernel nu hnu1 hT x]

/-- The off-diagonal imbalance difference and the averaged-kernel difference
are the same signed law coordinate. -/
theorem integral_prawitzSlackImbalanceOffDiagonal_sub_eq_integral_midpointCorrectedSineAveragedKernel_sub
    (mu nu : Measure ℝ) [IsProbabilityMeasure mu] [IsProbabilityMeasure nu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    (hnu1 : Integrable (fun y : ℝ => |y|) nu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂mu) -
        (∫ y, prawitzSlackImbalanceOffDiagonal T x y ∂nu) =
      (∫ y, midpointCorrectedSineAveragedKernel T x y ∂mu) -
        ∫ y, midpointCorrectedSineAveragedKernel T x y ∂nu := by
  rw [integral_prawitzSlackImbalanceOffDiagonal_sub_eq_re_jointSignedIntegral
    mu nu hmu1 hnu1 hT x,
    re_jointSignedIntegral_eq_integral_midpointCorrectedSineAveragedKernel_sub
      mu nu hmu1 hnu1 hT x]

/-- Pointwise identification of the frequency-averaged kernel: averaging
removes the diagonal atom from the imbalance profile and changes only a
law-independent constant. -/
theorem midpointCorrectedSineAveragedKernel_eq_offDiagonal_sub_one
    {T : ℝ} (hT : 0 < T) (x y : ℝ) :
    midpointCorrectedSineAveragedKernel T x y =
      prawitzSlackImbalanceOffDiagonal T x y - 1 := by
  have hy1 : Integrable (fun z : ℝ => |z|) (Measure.dirac y) :=
    integrable_dirac (by simp)
  have hx1 : Integrable (fun z : ℝ => |z|) (Measure.dirac x) :=
    integrable_dirac (by simp)
  have h :=
    integral_prawitzSlackImbalanceOffDiagonal_sub_eq_integral_midpointCorrectedSineAveragedKernel_sub
      (Measure.dirac y) (Measure.dirac x) hy1 hx1 hT x
  simp only [integral_dirac] at h
  have hOff : prawitzSlackImbalanceOffDiagonal T x x = 0 := by
    simp [prawitzSlackImbalanceOffDiagonal]
  rw [hOff, midpointCorrectedSineAveragedKernel_self hT x] at h
  linarith

/-- Reverse one-sided Prawitz comparison against the standard normal with the
signed reflected-frequency integral and Fejer remainder both kept exact. -/
theorem standardNormalCDF_sub_cdf_le_prawitz_signed
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x - cdf mu x ≤
      ((Complex.I / (2 * Real.pi : ℝ)) *
        ∫ t in Icc 0 T,
          (symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                (reflectedLaw
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
              (-x) t)).re +
      ∫ y, (prawitzPointKernel T (y - x) +
          prawitzPointKernel T (x - y))
        ∂NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := by
  have hsigned :=
    cdf_sub_cdf_le_re_reflectedPrawitzInversion_sub_add_reflection
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  rw [re_reflectedPrawitzInversion_charFun_sub_eq_signedIntegral
    mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
    hmu1 integrable_abs_standardNormalLaw hT x] at hsigned
  exact hsigned

/-- The reverse standard-normal Prawitz comparison with the reflected
inversion and Fejer terms combined inside one real part. -/
theorem standardNormalCDF_sub_cdf_le_prawitz_signed_frequency
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x - cdf mu x ≤
      (((Complex.I / (2 * Real.pi : ℝ)) *
          ∫ t in Icc 0 T,
            (symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
              symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun
                  (reflectedLaw
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
                (-x) t)) +
        ∫ z,
          Complex.exp (((-(T * x * z / 2) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
              (T * z / 2)
          ∂twofoldUniformKernel).re := by
  have hsigned := standardNormalCDF_sub_cdf_le_prawitz_signed
    mu hmu1 hT x
  rw [integral_prawitzReflection_standardNormal_eq_re_frequencyIntegral
    hT x] at hsigned
  simpa only [Complex.add_re] using hsigned

/-- The reflected reverse comparison as one signed positive-frequency
integral, preserving pointwise cancellation with the Gaussian Fejer term. -/
theorem standardNormalCDF_sub_cdf_le_prawitz_singleSignedIntegral
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x - cdf mu x ≤
      (∫ t in Icc 0 T,
        (Complex.I / (2 * Real.pi : ℝ)) *
            (symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
              symmetricPrawitzPVIntegrand T
                (MeasureTheory.charFun
                  (reflectedLaw
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
                (-x) t) +
          (prawitzFejerFrequencyWeight T t : ℂ) *
            (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
                MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
              Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
                MeasureTheory.charFun
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re := by
  have hsigned := standardNormalCDF_sub_cdf_le_prawitz_signed_frequency
    mu hmu1 hT x
  rw [integral_prawitzReflectionFrequency_eq_pairedTriangularIntegral
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x] at hsigned
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let P : ℝ → ℂ := fun t =>
    symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
      symmetricPrawitzPVIntegrand T
        (MeasureTheory.charFun
          (reflectedLaw
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
        (-x) t
  let R : ℝ → ℂ := fun t =>
    (prawitzFejerFrequencyWeight T t : ℂ) *
      (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
        Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)
  have hP : IntegrableOn P (Icc 0 T) := by
    exact (integrableOn_symmetricPrawitzPVIntegrand_charFun
      (reflectedLaw mu) (integrable_abs_reflectedLaw mu hmu1) hT (-x)).sub
      (integrableOn_symmetricPrawitzPVIntegrand_charFun
        (reflectedLaw
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        (integrable_abs_reflectedLaw
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
          integrable_abs_standardNormalLaw) hT (-x))
  have hR : IntegrableOn R (Icc 0 T) := by
    exact (integrable_pairedPrawitzReflectionFrequencyIntegrand
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).integrableOn
  have hcombine := const_mul_setIntegral_add_setIntegral_eq_integral_add
    P R (Icc 0 T) hP hR (Complex.I / (2 * Real.pi : ℝ))
  change cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x -
      cdf mu x ≤
    ((Complex.I / (2 * Real.pi : ℝ)) * (∫ t in Icc 0 T, P t) +
      ∫ t in Icc 0 T, R t).re at hsigned
  change cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x -
      cdf mu x ≤
    (∫ t in Icc 0 T,
      ((Complex.I / (2 * Real.pi : ℝ)) * P t + R t)).re
  rw [hcombine] at hsigned
  exact hsigned

/-- Pairing Hermitian positive and negative frequencies has the same real
coefficient as twice the positive-frequency principal-value difference. -/
theorem re_prawitzCoefficient_mul_symmetric_sub_eq_positiveFrequency
    {T : ℝ} {f g : ℝ → ℂ}
    (hf : IsHermitianFrequencyFunction f)
    (hg : IsHermitianFrequencyFunction g)
    (x : ℝ) {t : ℝ} (ht : t ≠ 0) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
        (symmetricPrawitzPVIntegrand T f x t -
          symmetricPrawitzPVIntegrand T g x t)).re =
      ((Complex.I / (Real.pi : ℝ)) *
        (prawitzPVIntegrand T f x t -
          prawitzPVIntegrand T g x t)).re := by
  rw [re_prawitzCoefficient_mul_symmetric_sub hf hg x ht]
  rw [← Complex.sub_im]
  unfold prawitzPVIntegrand
  rw [← sub_div]
  rw [Complex.mul_re, Complex.div_ofReal_re, Complex.div_ofReal_im,
    Complex.div_ofReal_re, Complex.div_ofReal_im]
  simp only [Complex.I_re, Complex.I_im, zero_div, one_div, zero_mul]
  field_simp [ht, Real.pi_ne_zero]
  ring

/-- The reverse Prawitz error keeps the reflected characteristic-function
phase and exact Gaussian cosine term together. -/
noncomputable def reversePrawitzPhasePreservingRemainder
    (mu : Measure ℝ) (T x : ℝ) : ℝ :=
  ((Complex.I / (Real.pi : ℝ)) *
      (∫ t in Icc (0 : ℝ) T,
        (prawitzPVIntegrand T
            (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
          prawitzPVIntegrand T
            (MeasureTheory.charFun
              (reflectedLaw
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
            (-x) t))).re +
    prawitzGaussianCosineRemainder T x

/-- Exact sine/cosine coordinate expansion of the reflected reverse
phase-preserving Prawitz remainder. -/
theorem reversePrawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    reversePrawitzPhasePreservingRemainder mu T x =
      ∫ t in Icc (0 : ℝ) T,
        (-Real.sin (t * x) *
              (prawitzZeroPhaseDifference (reflectedLaw mu)
                (reflectedLaw
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                T t).re -
            Real.cos (t * x) *
              (prawitzZeroPhaseDifference (reflectedLaw mu)
                (reflectedLaw
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
                T t).im) /
            Real.pi +
          2 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let z : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
      prawitzPVIntegrand T
        (MeasureTheory.charFun
          (reflectedLaw
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) (-x) t
  let P : ℝ → ℂ := fun t => (Complex.I / (Real.pi : ℝ)) * z t
  let G : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hz : IntegrableOn z (Icc (0 : ℝ) T) := by
    simpa only [z] using integrableOn_prawitzPVIntegrand_charFun_sub
      (reflectedLaw mu)
      (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (integrable_abs_reflectedLaw mu hmu1)
      (integrable_abs_reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        integrable_abs_standardNormalLaw) hT (-x)
  have hP : IntegrableOn P (Icc (0 : ℝ) T) := by
    simpa only [P] using hz.const_mul (Complex.I / (Real.pi : ℝ))
  have hG : IntegrableOn G (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [G]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  unfold reversePrawitzPhasePreservingRemainder
    prawitzGaussianCosineRemainder
  change ((Complex.I / (Real.pi : ℝ)) *
      (∫ t in Icc (0 : ℝ) T, z t)).re +
        ∫ t in Icc (0 : ℝ) T, G t = _
  calc
    ((Complex.I / (Real.pi : ℝ)) *
        (∫ t in Icc (0 : ℝ) T, z t)).re +
          ∫ t in Icc (0 : ℝ) T, G t =
        (∫ t in Icc (0 : ℝ) T, P t).re +
          ∫ t in Icc (0 : ℝ) T, G t := by
      congr 1
      exact congrArg Complex.re
        (integral_const_mul
          (μ := volume.restrict (Icc (0 : ℝ) T))
          (Complex.I / (Real.pi : ℝ)) z).symm
    _ = (∫ t in Icc (0 : ℝ) T, (P t).re) +
          ∫ t in Icc (0 : ℝ) T, G t := by
      exact congrArg (fun z : ℝ =>
        z + ∫ t in Icc (0 : ℝ) T, G t) (integral_re hP).symm
    _ = ∫ t in Icc (0 : ℝ) T, ((P t).re + G t) :=
      (integral_add hP.re hG).symm
    _ = _ := by
      apply integral_congr_ae
      filter_upwards with t
      dsimp only [P, z, G, prawitzZeroPhaseDifference]
      rw [prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero]
      rw [Complex.mul_re, Complex.div_ofReal_re,
        Complex.div_ofReal_im, Complex.mul_re, Complex.mul_im]
      simp only [Complex.I_re, Complex.I_im, zero_div, one_div,
        zero_mul, Complex.exp_ofReal_mul_I_re,
        Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg]
      rw [show t * -x = -(t * x) by ring, Real.cos_neg, Real.sin_neg]
      field_simp [Real.pi_ne_zero]
      ring

/-- The exact reflected reverse sine/cosine phase-coordinate integrand is
integrable on the closed Prawitz window. -/
theorem integrableOn_reversePrawitzPhaseCoordinateIntegrand
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    IntegrableOn (fun t : ℝ =>
      (-Real.sin (t * x) *
            (prawitzZeroPhaseDifference (reflectedLaw mu)
              (reflectedLaw
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              T t).re -
          Real.cos (t * x) *
            (prawitzZeroPhaseDifference (reflectedLaw mu)
              (reflectedLaw
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              T t).im) /
          Real.pi +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) (Icc (0 : ℝ) T) := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let z : ℝ → ℂ := fun t =>
    prawitzPVIntegrand T
        (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
      prawitzPVIntegrand T
        (MeasureTheory.charFun
          (reflectedLaw
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) (-x) t
  let P : ℝ → ℂ := fun t => (Complex.I / (Real.pi : ℝ)) * z t
  let G : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hz : IntegrableOn z (Icc (0 : ℝ) T) := by
    simpa only [z] using integrableOn_prawitzPVIntegrand_charFun_sub
      (reflectedLaw mu)
      (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (integrable_abs_reflectedLaw mu hmu1)
      (integrable_abs_reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        integrable_abs_standardNormalLaw) hT (-x)
  have hP : IntegrableOn P (Icc (0 : ℝ) T) := by
    simpa only [P] using hz.const_mul (Complex.I / (Real.pi : ℝ))
  have hG : IntegrableOn G (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [G]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  apply (hP.re.add hG).congr
  filter_upwards with t
  simp only [Pi.add_apply]
  change Complex.re (P t) + G t = _
  dsimp only [P, z, G, prawitzZeroPhaseDifference]
  rw [prawitzPVIntegrand_charFun_sub_eq_phase_mul_zero]
  rw [Complex.mul_re, Complex.div_ofReal_re,
    Complex.div_ofReal_im, Complex.mul_re, Complex.mul_im]
  simp only [Complex.I_re, Complex.I_im, zero_div, one_div,
    zero_mul, Complex.exp_ofReal_mul_I_re,
    Complex.exp_ofReal_mul_I_im, Real.cos_neg, Real.sin_neg]
  rw [show t * -x = -(t * x) by ring, Real.cos_neg, Real.sin_neg]
  field_simp [Real.pi_ne_zero]
  ring

/-- Adding the forward and reflected reverse phase-preserving remainders for a
normalized iid sum cancels the real filter coordinate exactly. -/
theorem prawitzPhasePreservingRemainders_normalizedCenteredIidSum_add_eq_integral
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    prawitzPhasePreservingRemainder law T x +
        reversePrawitzPhasePreservingRemainder law T x =
      ∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        (-2 * (scaledPrawitzSmoothingFilter T t).im *
              (Real.sin (t * x) * (r.re * s.im + r.im * s.re) +
                Real.cos (t * x) *
                  (r.re * s.re - r.im * s.im + c)) / t) /
            Real.pi +
          4 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  dsimp only
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  rw [prawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
      law hLaw1 hT x,
    reversePrawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
      law hLaw1 hT x]
  rw [← integral_add
    (integrableOn_prawitzPhaseCoordinateIntegrand law hLaw1 hT x)
    (integrableOn_reversePrawitzPhaseCoordinateIntegrand law hLaw1 hT x)]
  apply integral_congr_ae
  filter_upwards with t
  rw [prawitzZeroPhaseDifference_normalizedCenteredIidSum_re_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_normalizedCenteredIidSum_im_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_re_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_im_eq_taylorCoordinates
      X hX hIndep hIdent N T t]
  let u : ℝ := t / Real.sqrt ((N : ℝ) + 1)
  let phi : ℂ := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (X 0) (hX 0).aemeasurable : Measure ℝ) u
  let a : ℝ := 1 - u ^ 2 / 2
  let r : ℂ := phi - (a : ℂ)
  let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
  let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
  let k : ℂ := scaledPrawitzSmoothingFilter T t
  let g : ℝ := 2 * prawitzFejerFrequencyWeight T t *
    Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hcancel := prawitzTaylorPhaseCoordinates_add k r s c t (t * x)
  dsimp only [u, phi, a, r, s, c, k, g] at hcancel ⊢
  linear_combination (1 / Real.pi) * hcancel

/-- Subtracting the reflected reverse phase-preserving remainder from the
forward remainder cancels the imaginary filter coordinate and the Gaussian
term exactly. -/
theorem prawitzPhasePreservingRemainders_normalizedCenteredIidSum_sub_eq_integral
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    prawitzPhasePreservingRemainder law T x -
        reversePrawitzPhasePreservingRemainder law T x =
      ∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        (2 * (scaledPrawitzSmoothingFilter T t).re *
              (Real.sin (t * x) *
                  (r.re * s.re - r.im * s.im + c) -
                Real.cos (t * x) *
                  (r.re * s.im + r.im * s.re)) / t) /
            Real.pi := by
  dsimp only
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  rw [prawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
      law hLaw1 hT x,
    reversePrawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
      law hLaw1 hT x]
  rw [← integral_sub
    (integrableOn_prawitzPhaseCoordinateIntegrand law hLaw1 hT x)
    (integrableOn_reversePrawitzPhaseCoordinateIntegrand law hLaw1 hT x)]
  apply integral_congr_ae
  filter_upwards with t
  rw [prawitzZeroPhaseDifference_normalizedCenteredIidSum_re_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_normalizedCenteredIidSum_im_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_re_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_im_eq_taylorCoordinates
      X hX hIndep hIdent N T t]
  let u : ℝ := t / Real.sqrt ((N : ℝ) + 1)
  let phi : ℂ := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (X 0) (hX 0).aemeasurable : Measure ℝ) u
  let a : ℝ := 1 - u ^ 2 / 2
  let r : ℂ := phi - (a : ℂ)
  let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
  let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
  let k : ℂ := scaledPrawitzSmoothingFilter T t
  let g : ℝ := 2 * prawitzFejerFrequencyWeight T t *
    Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hcancel := prawitzTaylorPhaseCoordinates_sub k r s c t (t * x)
  dsimp only [u, phi, a, r, s, c, k, g] at hcancel ⊢
  linear_combination (1 / Real.pi) * hcancel

/-- Exact Taylor-coordinate expansion of the reflected reverse
phase-preserving Prawitz remainder for a normalized centered iid sum. -/
theorem reversePrawitzPhasePreservingRemainder_normalizedCenteredIidSum_eq_integral_taylorCoordinates
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    reversePrawitzPhasePreservingRemainder law T x =
      ∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        (-Real.sin (t * x) *
              (((scaledPrawitzSmoothingFilter T t).re *
                    (r.re * s.re - r.im * s.im + c) +
                  (scaledPrawitzSmoothingFilter T t).im *
                    (r.re * s.im + r.im * s.re)) / t) -
            Real.cos (t * x) *
              ((-(scaledPrawitzSmoothingFilter T t).re *
                    (r.re * s.im + r.im * s.re) +
                  (scaledPrawitzSmoothingFilter T t).im *
                    (r.re * s.re - r.im * s.im + c)) / t)) /
            Real.pi +
          2 * prawitzFejerFrequencyWeight T t *
            Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  dsimp only
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  rw [reversePrawitzPhasePreservingRemainder_eq_integral_phaseCoordinates
    law hLaw1 hT x]
  apply integral_congr_ae
  filter_upwards with t
  rw [prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_re_eq_taylorCoordinates
      X hX hIndep hIdent N T t,
    prawitzZeroPhaseDifference_reflected_normalizedCenteredIidSum_im_eq_taylorCoordinates
      X hX hIndep hIdent N T t]

/-- The reflected reverse phase-preserving remainder is continuous in the
spatial parameter. -/
theorem continuous_reversePrawitzPhasePreservingRemainder
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    Continuous (reversePrawitzPhasePreservingRemainder mu T) := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  have hphase :=
    (continuous_setIntegral_prawitzPVIntegrand_charFun_sub
      (reflectedLaw mu)
      (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (integrable_abs_reflectedLaw mu hmu1)
      (integrable_abs_reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        integrable_abs_standardNormalLaw) hT).comp continuous_neg
  unfold reversePrawitzPhasePreservingRemainder
  exact (Complex.continuous_re.comp
    (continuous_const.mul hphase)).add
      (continuous_prawitzGaussianCosineRemainder T)

/-- The reflected reverse phase-preserving remainder vanishes in the positive
spatial tail. -/
theorem tendsto_reversePrawitzPhasePreservingRemainder_atTop
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    Tendsto (reversePrawitzPhasePreservingRemainder mu T) atTop (𝓝 0) := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  have hraw :=
    (tendsto_setIntegral_prawitzPVIntegrand_charFun_sub_atBot
      (reflectedLaw mu)
      (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (integrable_abs_reflectedLaw mu hmu1)
      (integrable_abs_reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        integrable_abs_standardNormalLaw) hT).comp
      tendsto_neg_atTop_atBot
  have hphase : Tendsto (fun x : ℝ =>
      (Complex.I / (Real.pi : ℝ)) *
        (∫ t in Icc (0 : ℝ) T,
          (prawitzPVIntegrand T
              (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
            prawitzPVIntegrand T
              (MeasureTheory.charFun
                (reflectedLaw
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
              (-x) t))) atTop (𝓝 0) := by
    simpa only [Function.comp_apply, mul_zero] using
      (tendsto_const_nhds.mul hraw : Tendsto (fun x : ℝ =>
        (Complex.I / (Real.pi : ℝ)) *
          (∫ t in Icc (0 : ℝ) T,
            (prawitzPVIntegrand T
                (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
              prawitzPVIntegrand T
                (MeasureTheory.charFun
                  (reflectedLaw
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
                (-x) t))) atTop
          (𝓝 ((Complex.I / (Real.pi : ℝ)) * 0)))
  have hre := Complex.continuous_re.continuousAt.tendsto.comp hphase
  simpa only [reversePrawitzPhasePreservingRemainder, mul_zero,
    Complex.zero_re, zero_add] using
      hre.add (tendsto_prawitzGaussianCosineRemainder_atTop T)

/-- The reflected reverse phase-preserving remainder also vanishes in the
negative spatial tail. -/
theorem tendsto_reversePrawitzPhasePreservingRemainder_atBot
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    Tendsto (reversePrawitzPhasePreservingRemainder mu T) atBot (𝓝 0) := by
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  have hraw :=
    (tendsto_setIntegral_prawitzPVIntegrand_charFun_sub_atTop
      (reflectedLaw mu)
      (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (integrable_abs_reflectedLaw mu hmu1)
      (integrable_abs_reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        integrable_abs_standardNormalLaw) hT).comp
      tendsto_neg_atBot_atTop
  have hphase : Tendsto (fun x : ℝ =>
      (Complex.I / (Real.pi : ℝ)) *
        (∫ t in Icc (0 : ℝ) T,
          (prawitzPVIntegrand T
              (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
            prawitzPVIntegrand T
              (MeasureTheory.charFun
                (reflectedLaw
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
              (-x) t))) atBot (𝓝 0) := by
    simpa only [Function.comp_apply, mul_zero] using
      (tendsto_const_nhds.mul hraw : Tendsto (fun x : ℝ =>
        (Complex.I / (Real.pi : ℝ)) *
          (∫ t in Icc (0 : ℝ) T,
            (prawitzPVIntegrand T
                (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
              prawitzPVIntegrand T
                (MeasureTheory.charFun
                  (reflectedLaw
                    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
                (-x) t))) atBot
          (𝓝 ((Complex.I / (Real.pi : ℝ)) * 0)))
  have hre := Complex.continuous_re.continuousAt.tendsto.comp hphase
  simpa only [reversePrawitzPhasePreservingRemainder, mul_zero,
    Complex.zero_re, zero_add] using
      hre.add (tendsto_prawitzGaussianCosineRemainder_atBot T)

/-- The reflected reverse signed integrand has the positive-frequency phase
form plus the same exact Gaussian cosine contribution. -/
theorem re_reversePrawitzSingleIntegrand_eq_phasePreserving
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {T t : ℝ} (ht : t ≠ 0) (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                (reflectedLaw
                  NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
              (-x) t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re =
      ((Complex.I / (Real.pi : ℝ)) *
        (prawitzPVIntegrand T
            (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
          prawitzPVIntegrand T
            (MeasureTheory.charFun
              (reflectedLaw
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
            (-x) t)).re +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  rw [Complex.add_re,
    re_prawitzCoefficient_mul_symmetric_sub_eq_positiveFrequency
      (isHermitianFrequencyFunction_charFun (reflectedLaw mu))
      (isHermitianFrequencyFunction_charFun
        (reflectedLaw
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)) (-x) ht,
    re_pairedPrawitzReflectionFrequencyIntegrand_standardNormal]

/-- The reverse signed CDF error is bounded by the reflected
phase-preserving Prawitz remainder without an intervening complex norm. -/
theorem standardNormalCDF_sub_cdf_le_reversePrawitzPhasePreservingRemainder
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x - cdf mu x ≤
      reversePrawitzPhasePreservingRemainder mu T x := by
  have hsigned := standardNormalCDF_sub_cdf_le_prawitz_singleSignedIntegral
    mu hmu1 hT x
  letI : IsProbabilityMeasure (reflectedLaw mu) :=
    reflectedLaw_isProbabilityMeasure mu
  letI : IsProbabilityMeasure
      (reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw) :=
    reflectedLaw_isProbabilityMeasure
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let F : ℝ → ℂ := fun t =>
    (Complex.I / (2 * Real.pi : ℝ)) *
        (symmetricPrawitzPVIntegrand T
            (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
          symmetricPrawitzPVIntegrand T
            (MeasureTheory.charFun
              (reflectedLaw
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
            (-x) t) +
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)
  let P : ℝ → ℂ := fun t =>
    (Complex.I / (Real.pi : ℝ)) *
      (prawitzPVIntegrand T
          (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
        prawitzPVIntegrand T
          (MeasureTheory.charFun
            (reflectedLaw
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
          (-x) t)
  let G : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  have hF : IntegrableOn F (Icc (0 : ℝ) T) := by
    have hmu := integrableOn_symmetricPrawitzPVIntegrand_charFun
      (reflectedLaw mu) (integrable_abs_reflectedLaw mu hmu1) hT (-x)
    have hnormal := integrableOn_symmetricPrawitzPVIntegrand_charFun
      (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (integrable_abs_reflectedLaw
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
        integrable_abs_standardNormalLaw) hT (-x)
    have hP := (hmu.sub hnormal).const_mul
      (Complex.I / (2 * Real.pi : ℝ))
    have hR : IntegrableOn (fun t =>
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t))
        (Icc (0 : ℝ) T) :=
      (integrable_pairedPrawitzReflectionFrequencyIntegrand
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).integrableOn
    simpa only [F] using hP.add hR
  have hP : IntegrableOn P (Icc (0 : ℝ) T) := by
    simpa only [P] using
      (integrableOn_prawitzPVIntegrand_charFun_sub
        (reflectedLaw mu)
        (reflectedLaw NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
        (integrable_abs_reflectedLaw mu hmu1)
        (integrable_abs_reflectedLaw
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
          integrable_abs_standardNormalLaw) hT (-x)).const_mul
            (Complex.I / (Real.pi : ℝ))
  have hG : IntegrableOn G (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [G]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  have heq : (∫ t in Icc (0 : ℝ) T, F t).re =
      reversePrawitzPhasePreservingRemainder mu T x := by
    calc
      (∫ t in Icc (0 : ℝ) T, F t).re =
          ∫ t in Icc (0 : ℝ) T, (F t).re :=
        (integral_re hF).symm
      _ = ∫ t in Icc (0 : ℝ) T, ((P t).re + G t) := by
        apply integral_congr_ae
        filter_upwards [
          (volume.restrict (Icc (0 : ℝ) T)).ae_ne 0] with t ht
        dsimp only [F, P, G]
        exact re_reversePrawitzSingleIntegrand_eq_phasePreserving mu ht x
      _ = (∫ t in Icc (0 : ℝ) T, P t).re +
          ∫ t in Icc (0 : ℝ) T, G t := by
        exact (integral_add hP.re hG).trans
          (congrArg (fun z : ℝ =>
            z + ∫ t in Icc (0 : ℝ) T, G t) (integral_re hP))
      _ = reversePrawitzPhasePreservingRemainder mu T x := by
        dsimp only [P, G]
        unfold reversePrawitzPhasePreservingRemainder
          prawitzGaussianCosineRemainder
        congr 1
        exact congrArg Complex.re
          (integral_const_mul
            (μ := volume.restrict (Icc (0 : ℝ) T))
            (Complex.I / (Real.pi : ℝ))
            (fun t =>
              prawitzPVIntegrand T
                  (MeasureTheory.charFun (reflectedLaw mu)) (-x) t -
                prawitzPVIntegrand T
                  (MeasureTheory.charFun
                    (reflectedLaw
                      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw))
                  (-x) t))
  simpa only [F] using hsigned.trans_eq heq

/-- The two signed phase-preserving Prawitz inequalities combine into a
pointwise absolute CDF bound through the exact sum and difference of their
remainders. -/
theorem cdfGap_le_prawitzPhasePreservingRemainders_add_abs_sub
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdfGap mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (prawitzPhasePreservingRemainder mu T x +
          reversePrawitzPhasePreservingRemainder mu T x +
        |prawitzPhasePreservingRemainder mu T x -
          reversePrawitzPhasePreservingRemainder mu T x|) / 2 := by
  rw [cdfGap_eq_max_signed]
  calc
    max
        (cdfUpperError mu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x)
        (cdfLowerError mu
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x) ≤
      max (prawitzPhasePreservingRemainder mu T x)
        (reversePrawitzPhasePreservingRemainder mu T x) :=
      max_le_max
        (cdf_sub_standardNormalCDF_le_prawitzPhasePreservingRemainder
          mu hmu1 hT x)
        (standardNormalCDF_sub_cdf_le_reversePrawitzPhasePreservingRemainder
          mu hmu1 hT x)
    _ = _ := max_eq_add_add_abs_sub_div_two _ _

/-- For normalized centered iid sums, the bidirectional Prawitz CDF bound can
be written as one integral containing only the imaginary filter coordinate
plus the absolute value of one integral containing only the real coordinate.
The Gaussian correction remains only in the first integral. -/
theorem cdfGap_normalizedCenteredIidSum_le_taylorPhaseIntegrals
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    cdfGap law NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      ((∫ t in Icc (0 : ℝ) T,
          let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
          let phi : ℂ := MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ) u
          let a : ℝ := 1 - u ^ 2 / 2
          let r : ℂ := phi - (a : ℂ)
          let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
          let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
          (-2 * (scaledPrawitzSmoothingFilter T t).im *
                (Real.sin (t * x) * (r.re * s.im + r.im * s.re) +
                  Real.cos (t * x) *
                    (r.re * s.re - r.im * s.im + c)) / t) /
              Real.pi +
            4 * prawitzFejerFrequencyWeight T t *
              Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) +
        |∫ t in Icc (0 : ℝ) T,
          let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
          let phi : ℂ := MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ) u
          let a : ℝ := 1 - u ^ 2 / 2
          let r : ℂ := phi - (a : ℂ)
          let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
          let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
          (2 * (scaledPrawitzSmoothingFilter T t).re *
                (Real.sin (t * x) *
                    (r.re * s.re - r.im * s.im + c) -
                  Real.cos (t * x) *
                    (r.re * s.im + r.im * s.re)) / t) /
              Real.pi|) / 2 := by
  dsimp only
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hEnvelope :=
    cdfGap_le_prawitzPhasePreservingRemainders_add_abs_sub
      law hLaw1 hT x
  rw [prawitzPhasePreservingRemainders_normalizedCenteredIidSum_add_eq_integral
      X hX hIndep hIdent N hT x,
    prawitzPhasePreservingRemainders_normalizedCenteredIidSum_sub_eq_integral
      X hX hIndep hIdent N hT x] at hEnvelope
  simpa only [law, Y] using hEnvelope

/-- After exact interior-window coefficient substitution, the sum of the two
phase-preserving remainders is twice a Fejer-weighted Taylor-coordinate
integral. -/
theorem prawitzPhasePreservingRemainders_normalizedCenteredIidSum_add_eq_two_mul_integral
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    prawitzPhasePreservingRemainder law T x +
        reversePrawitzPhasePreservingRemainder law T x =
      2 * ∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        prawitzFejerFrequencyWeight T t *
          (Real.sin (t * x) * (r.re * s.im + r.im * s.re) +
            Real.cos (t * x) * (r.re * s.re - r.im * s.im + c) +
            2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)) := by
  dsimp only
  rw [prawitzPhasePreservingRemainders_normalizedCenteredIidSum_add_eq_integral
    X hX hIndep hIdent N hT x]
  rw [← integral_const_mul]
  apply integral_congr_ae
  filter_upwards [
    ae_restrict_mem measurableSet_Icc,
    (volume.restrict (Icc (0 : ℝ) T)).ae_ne 0,
    (volume.restrict (Icc (0 : ℝ) T)).ae_ne T] with t htmem ht0 htT
  have ht : t ∈ Ioo (0 : ℝ) T :=
    ⟨lt_of_le_of_ne htmem.1 (Ne.symm ht0),
      lt_of_le_of_ne htmem.2 htT⟩
  let u : ℝ := t / Real.sqrt ((N : ℝ) + 1)
  let phi : ℂ := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (X 0) (hX 0).aemeasurable : Measure ℝ) u
  let a : ℝ := 1 - u ^ 2 / 2
  let r : ℂ := phi - (a : ℂ)
  let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
  let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
  let q : ℝ :=
    Real.sin (t * x) * (r.re * s.im + r.im * s.re) +
      Real.cos (t * x) * (r.re * s.re - r.im * s.im + c)
  change
    (-2 * (scaledPrawitzSmoothingFilter T t).im * q / t) / Real.pi +
        4 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) =
      2 * (prawitzFejerFrequencyWeight T t *
        (q + 2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)))
  rw [neg_two_mul_scaledPrawitzSmoothingFilter_im_div_pi hT ht q]
  ring

/-- After exact interior-window coefficient substitution, the difference of
the two phase-preserving remainders is twice a cotangent-weighted
Taylor-coordinate integral. -/
theorem prawitzPhasePreservingRemainders_normalizedCenteredIidSum_sub_eq_two_mul_integral
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    prawitzPhasePreservingRemainder law T x -
        reversePrawitzPhasePreservingRemainder law T x =
      2 * ∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        prawitzCotangentFrequencyCoefficient T t *
          (Real.sin (t * x) * (r.re * s.re - r.im * s.im + c) -
            Real.cos (t * x) * (r.re * s.im + r.im * s.re)) := by
  dsimp only
  rw [prawitzPhasePreservingRemainders_normalizedCenteredIidSum_sub_eq_integral
    X hX hIndep hIdent N hT x]
  rw [← integral_const_mul]
  apply integral_congr_ae
  filter_upwards [
    ae_restrict_mem measurableSet_Icc,
    (volume.restrict (Icc (0 : ℝ) T)).ae_ne 0,
    (volume.restrict (Icc (0 : ℝ) T)).ae_ne T] with t htmem ht0 htT
  have ht : t ∈ Ioo (0 : ℝ) T :=
    ⟨lt_of_le_of_ne htmem.1 (Ne.symm ht0),
      lt_of_le_of_ne htmem.2 htT⟩
  let u : ℝ := t / Real.sqrt ((N : ℝ) + 1)
  let phi : ℂ := MeasureTheory.charFun
    (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (X 0) (hX 0).aemeasurable : Measure ℝ) u
  let a : ℝ := 1 - u ^ 2 / 2
  let r : ℂ := phi - (a : ℂ)
  let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
  let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
  let q : ℝ :=
    Real.sin (t * x) * (r.re * s.re - r.im * s.im + c) -
      Real.cos (t * x) * (r.re * s.im + r.im * s.re)
  change
    (2 * (scaledPrawitzSmoothingFilter T t).re * q / t) / Real.pi =
      2 * (prawitzCotangentFrequencyCoefficient T t * q)
  simpa only [mul_assoc] using
    two_mul_scaledPrawitzSmoothingFilter_re_div_pi hT ht q

/-- The exact bidirectional phase envelope after removing the common factor
two: one signed Fejer-weighted integral plus the absolute value of one
cotangent-weighted integral. -/
theorem cdfGap_normalizedCenteredIidSum_le_simplifiedTaylorPhaseIntegrals
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    cdfGap law NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let a : ℝ := 1 - u ^ 2 / 2
        let r : ℂ := phi - (a : ℂ)
        let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
        let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
        prawitzFejerFrequencyWeight T t *
          (Real.sin (t * x) * (r.re * s.im + r.im * s.re) +
            Real.cos (t * x) * (r.re * s.re - r.im * s.im + c) +
            2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) +
        |∫ t in Icc (0 : ℝ) T,
          let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
          let phi : ℂ := MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ) u
          let a : ℝ := 1 - u ^ 2 / 2
          let r : ℂ := phi - (a : ℂ)
          let s : ℂ := complexPowerRealBaselineGeomSum phi a (N + 1)
          let c : ℝ := a ^ (N + 1) - Real.exp (-(t ^ 2) / 2)
          prawitzCotangentFrequencyCoefficient T t *
            (Real.sin (t * x) * (r.re * s.re - r.im * s.im + c) -
              Real.cos (t * x) * (r.re * s.im + r.im * s.re))| := by
  dsimp only
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp X hX N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hEnvelope :=
    cdfGap_le_prawitzPhasePreservingRemainders_add_abs_sub
      law hLaw1 hT x
  rw [prawitzPhasePreservingRemainders_normalizedCenteredIidSum_add_eq_two_mul_integral
      X hX hIndep hIdent N hT x,
    prawitzPhasePreservingRemainders_normalizedCenteredIidSum_sub_eq_two_mul_integral
      X hX hIndep hIdent N hT x] at hEnvelope
  have htwo (a b : ℝ) : (2 * a + |2 * b|) / 2 = a + |b| := by
    rw [abs_mul, abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2)]
    ring
  simpa only [law, Y, htwo] using hEnvelope

/-- The simplified bidirectional phase envelope with its Taylor/geometric
coordinates recombined into the full powered characteristic-function
discrepancy.  The Fejer/Gaussian interaction remains signed. -/
theorem cdfGap_normalizedCenteredIidSum_le_poweredPhaseIntegrals
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    cdfGap law NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (∫ t in Icc (0 : ℝ) T,
        let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
        let phi : ℂ := MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ) u
        let d : ℂ := phi ^ (N + 1) -
          (Real.exp (-(t ^ 2) / 2) : ℂ)
        prawitzFejerFrequencyWeight T t *
          (Real.sin (t * x) * d.im + Real.cos (t * x) * d.re +
            2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) +
        |∫ t in Icc (0 : ℝ) T,
          let u : ℝ := t / Real.sqrt (N + 1 : ℝ)
          let phi : ℂ := MeasureTheory.charFun
            (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
              (X 0) (hX 0).aemeasurable : Measure ℝ) u
          let d : ℂ := phi ^ (N + 1) -
            (Real.exp (-(t ^ 2) / 2) : ℂ)
          prawitzCotangentFrequencyCoefficient T t *
            (Real.sin (t * x) * d.re - Real.cos (t * x) * d.im)| := by
  have h := cdfGap_normalizedCenteredIidSum_le_simplifiedTaylorPhaseIntegrals
    X hX hIndep hIdent N hT x
  simpa only [taylorGeomSum_imagCoordinate_eq_pow_sub_real_im,
    taylorGeomSum_realCoordinate_eq_pow_sub_real_re,
    Complex.sub_re, Complex.sub_im, Complex.ofReal_re, Complex.ofReal_im,
    sub_zero] using h

/-- The bidirectional normalized-iid Prawitz envelope in its compact law-level
phase form: a signed Fejer-weighted real phase plus the absolute value of a
cotangent-weighted imaginary phase. -/
theorem cdfGap_normalizedCenteredIidSum_le_lawPhaseIntegrals
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    cdfGap law NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      (∫ t in Icc (0 : ℝ) T,
        let d : ℂ := MeasureTheory.charFun law t -
          MeasureTheory.charFun
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
        let q : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * d
        prawitzFejerFrequencyWeight T t *
          (q.re + 2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))) +
        |∫ t in Icc (0 : ℝ) T,
          let d : ℂ := MeasureTheory.charFun law t -
            MeasureTheory.charFun
              NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t
          let q : ℂ := Complex.exp (((-(t * x) : ℝ) : ℂ) * Complex.I) * d
          (-prawitzCotangentFrequencyCoefficient T t * q.im)| := by
  dsimp only
  have h := cdfGap_normalizedCenteredIidSum_le_poweredPhaseIntegrals
    X hX hIndep hIdent N hT x
  simp_rw [← charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    X hX hIndep hIdent N] at h
  simpa only [phase_fejer_coordinate, phase_cotangent_coordinate] using h

/-- The average of the forward and reverse normalized-iid one-sided Prawitz
bounds is exactly the Fejér coordinate of the named law-phase envelope. -/
theorem prawitzPhasePreservingRemainders_normalizedCenteredIidSum_average_eq_lawPhaseFejer
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    (prawitzPhasePreservingRemainder law T x +
        reversePrawitzPhasePreservingRemainder law T x) / 2 =
      prawitzLawPhaseFejerIntegral law T x := by
  dsimp only
  have h :=
    prawitzPhasePreservingRemainders_normalizedCenteredIidSum_add_eq_two_mul_integral
      X hX hIndep hIdent N hT x
  simp only [taylorGeomSum_imagCoordinate_eq_pow_sub_real_im,
    taylorGeomSum_realCoordinate_eq_pow_sub_real_re,
    Complex.sub_re, Complex.sub_im, Complex.ofReal_re, Complex.ofReal_im,
    sub_zero] at h
  rw [h]
  have htwo (a : ℝ) : (2 * a) / 2 = a := by ring
  rw [htwo]
  unfold prawitzLawPhaseFejerIntegral
  apply integral_congr_ae
  filter_upwards with t
  rw [charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    X hX hIndep hIdent N]
  dsimp only
  simpa only [Complex.sub_re, Complex.sub_im, Complex.ofReal_re,
    Complex.ofReal_im, sub_zero] using
    (phase_fejer_coordinate
      (prawitzFejerFrequencyWeight T t)
      (2 * Real.exp (-(t ^ 2) / 2) * Real.cos (x * t))
      (t * x)
      (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) ^ (N + 1) -
        (Real.exp (-(t ^ 2) / 2) : ℂ)))

/-- The half-difference of the forward and reverse normalized-iid one-sided
Prawitz bounds is exactly the cotangent coordinate of the named law-phase
envelope. -/
theorem prawitzPhasePreservingRemainders_normalizedCenteredIidSum_halfDifference_eq_lawPhaseCotangent
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    (prawitzPhasePreservingRemainder law T x -
        reversePrawitzPhasePreservingRemainder law T x) / 2 =
      prawitzLawPhaseCotangentIntegral law T x := by
  dsimp only
  have h :=
    prawitzPhasePreservingRemainders_normalizedCenteredIidSum_sub_eq_two_mul_integral
      X hX hIndep hIdent N hT x
  simp only [taylorGeomSum_imagCoordinate_eq_pow_sub_real_im,
    taylorGeomSum_realCoordinate_eq_pow_sub_real_re,
    Complex.sub_re, Complex.sub_im, Complex.ofReal_re, Complex.ofReal_im,
    sub_zero] at h
  rw [h]
  have htwo (a : ℝ) : (2 * a) / 2 = a := by ring
  rw [htwo]
  unfold prawitzLawPhaseCotangentIntegral
  apply integral_congr_ae
  filter_upwards with t
  rw [charFun_normalizedCenteredIidSum_sub_standardNormalLaw_eq_pow
    X hX hIndep hIdent N]
  dsimp only
  simpa only [Complex.sub_re, Complex.sub_im, Complex.ofReal_re,
    Complex.ofReal_im, sub_zero] using
    (phase_cotangent_coordinate
      (prawitzCotangentFrequencyCoefficient T t) (t * x)
      (MeasureTheory.charFun
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (X 0) (hX 0).aemeasurable : Measure ℝ)
          (t / Real.sqrt (N + 1 : ℝ)) ^ (N + 1) -
        (Real.exp (-(t ^ 2) / 2) : ℂ)))

/-- API form of the exact bidirectional normalized-iid Prawitz bound using
the named law-phase envelope. -/
theorem cdfGap_normalizedCenteredIidSum_le_prawitzLawPhaseEnvelope
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 2 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (N : ℕ) {T : ℝ} (hT : 0 < T) (x : ℝ) :
    let law : Measure ℝ :=
      NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
          X hX N).aemeasurable
    cdfGap law NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      prawitzLawPhaseEnvelope law T x := by
  simpa only [prawitzLawPhaseEnvelope, prawitzLawPhaseFejerIntegral,
    prawitzLawPhaseCotangentIntegral] using
    cdfGap_normalizedCenteredIidSum_le_lawPhaseIntegrals
      X hX hIndep hIdent N hT x

/-- The reflected reverse phase-preserving remainder is globally controlled,
up to any positive epsilon, by an attained compact maximum. -/
theorem exists_reversePrawitzPhasePreservingRemainder_compact_max_le_global
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T)
    {epsilon : ℝ} (hepsilon : 0 < epsilon) :
    ∃ R x : ℝ, 0 ≤ R ∧ x ∈ Icc (-R) R ∧
      (∀ y ∈ Icc (-R) R,
        reversePrawitzPhasePreservingRemainder mu T y ≤
          reversePrawitzPhasePreservingRemainder mu T x) ∧
      ∀ y : ℝ, reversePrawitzPhasePreservingRemainder mu T y ≤
        max epsilon (reversePrawitzPhasePreservingRemainder mu T x) := by
  exact exists_compact_max_le_global_max_epsilon_of_tendsto_zero
    (reversePrawitzPhasePreservingRemainder mu T)
    (continuous_reversePrawitzPhasePreservingRemainder mu hmu1 hT)
    (tendsto_reversePrawitzPhasePreservingRemainder_atTop mu hmu1 hT)
    (tendsto_reversePrawitzPhasePreservingRemainder_atBot mu hmu1 hT)
    hepsilon

/-- At the admissible tunable cutoff, the absolute CDF gap of a normalized
centered iid sum is controlled, up to epsilon, by attained compact maxima of
the forward and reflected phase-preserving remainders. -/
theorem exists_normalizedCenteredIidSum_bidirectionalPrawitzPhase_compact_bound
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hrhoPos : 0 < ∫ omega, |X 0 omega| ^ 3 ∂mu)
    {c : ℝ} (hc : 0 < c) (N : ℕ)
    {epsilon : ℝ} (hepsilon : 0 < epsilon) :
    let Y := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
    let hY := NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
    let law := NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      Y hY.aemeasurable
    let rho := ∫ omega, |X 0 omega| ^ 3 ∂mu
    let T := admissibleTunableThirdMomentCutoff c rho N
    ∃ Rf xf Rr xr : ℝ,
      0 ≤ Rf ∧ xf ∈ Icc (-Rf) Rf ∧
      (∀ y ∈ Icc (-Rf) Rf,
        prawitzPhasePreservingRemainder law T y ≤
          prawitzPhasePreservingRemainder law T xf) ∧
      0 ≤ Rr ∧ xr ∈ Icc (-Rr) Rr ∧
      (∀ y ∈ Icc (-Rr) Rr,
        reversePrawitzPhasePreservingRemainder law T y ≤
          reversePrawitzPhasePreservingRemainder law T xr) ∧
      ∀ y : ℝ,
        cdfGap law
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw y ≤
          max
            (max epsilon (prawitzPhasePreservingRemainder law T xf))
            (max epsilon
              (reversePrawitzPhasePreservingRemainder law T xr)) := by
  dsimp only
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  letI : IsProbabilityMeasure law := inferInstance
  have hrho0 : 0 < rho := by simpa only [rho] using hrhoPos
  have hT : 0 < T := admissibleTunableThirdMomentCutoff_pos hc hrho0 N
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  obtain ⟨Rf, xf, hRf, hxf, hFmax, hFglobal⟩ :=
    exists_prawitzPhasePreservingRemainder_compact_max_le_global
      law hLaw1 hT hepsilon
  obtain ⟨Rr, xr, hRr, hxr, hRmax, hRglobal⟩ :=
    exists_reversePrawitzPhasePreservingRemainder_compact_max_le_global
      law hLaw1 hT hepsilon
  refine ⟨Rf, xf, Rr, xr, hRf, hxf, hFmax, hRr, hxr, hRmax, ?_⟩
  intro y
  rw [cdfGap_eq_max_signed]
  apply max_le_max
  · exact (cdf_sub_standardNormalCDF_le_prawitzPhasePreservingRemainder
      law hLaw1 hT y).trans (hFglobal y)
  · exact (standardNormalCDF_sub_cdf_le_reversePrawitzPhasePreservingRemainder
      law hLaw1 hT y).trans (hRglobal y)

/-- The opposite one-sided Prawitz smoothing estimate against the standard
normal, with the same frequency and exact Fejér terms. -/
theorem standardNormalCDF_sub_cdf_le_prawitz
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) (x : ℝ) :
    cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x - cdf mu x ≤
      (1 / Real.pi) * (∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  have hsigned :=
    cdf_sub_cdf_le_norm_reflectedPrawitzInversion_sub_add_reflection
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  have hfreq :=
    norm_prawitzInversion_reflectedLaw_sub_le_integral_discrepancy
      mu NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      hmu1 integrable_abs_standardNormalLaw hT x
  have hreflect := integral_prawitzReflection_standardNormal_le hT x
  exact hsigned.trans (add_le_add hfreq hreflect)

/-- The two one-sided Prawitz inequalities combine into a pointwise CDF
bound against the standard normal. -/
theorem cdfBound_standardNormal_le_prawitz
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    CDFBound mu
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
      ((1 / Real.pi) * (∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T)) := by
  intro x
  rw [cdfGap, abs_le]
  constructor
  · have h := standardNormalCDF_sub_cdf_le_prawitz mu hmu1 hT x
    linarith
  · exact cdf_sub_standardNormalCDF_le_prawitz mu hmu1 hT x

/-- Supremum-form bidirectional Prawitz smoothing inequality against the
standard normal. -/
theorem kolmogorovDistance_standardNormal_le_prawitz
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun y : ℝ => |y|) mu)
    {T : ℝ} (hT : 0 < T) :
    kolmogorovDistance mu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) * (∫ t in Icc 0 T,
        ‖scaledPrawitzSmoothingFilter T t‖ *
          charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) :=
  (kolmogorovDistance_le_iff _ _ _).2
    (cdfBound_standardNormal_le_prawitz mu hmu1 hT)

/-- The sinc-fourth kernel rescaled so its characteristic function is
supported in the target frequency window. -/
noncomputable def scaledSincFourthKernel (T : ℝ) : Measure ℝ :=
  sincFourthKernel.map ((4 / T) * ·)

theorem scaledSincFourthKernel_isProbabilityMeasure {T : ℝ} (hT : 0 < T) :
    IsProbabilityMeasure (scaledSincFourthKernel T) := by
  unfold scaledSincFourthKernel
  letI : IsProbabilityMeasure sincFourthKernel :=
    sincFourthKernel_isProbabilityMeasure
  exact Measure.isProbabilityMeasure_map (by fun_prop)

theorem scaledSincFourthKernel_charFun_supported {T : ℝ} (hT : 0 < T) :
    IsCharFunSupportedInFrequencyWindow (scaledSincFourthKernel T) T := by
  intro t ht
  rw [scaledSincFourthKernel, MeasureTheory.charFun_map_mul]
  apply sincFourthKernel_charFun_supported
  rw [abs_mul, abs_of_pos (div_pos (by norm_num) hT)]
  calc
    4 = (4 / T) * T := by field_simp
    _ < (4 / T) * |t| :=
      mul_lt_mul_of_pos_left ht (div_pos (by norm_num) hT)

theorem integrable_abs_scaledSincFourthKernel {T : ℝ} (hT : 0 < T) :
    Integrable (fun x : ℝ => |x|) (scaledSincFourthKernel T) := by
  unfold scaledSincFourthKernel
  have hr : 0 < 4 / T := div_pos (by norm_num) hT
  have hemb : MeasurableEmbedding (fun x : ℝ => (4 / T) * x) :=
    (Homeomorph.mulLeft₀ (4 / T) hr.ne').measurableEmbedding
  rw [hemb.integrable_map_iff]
  have h := integrable_abs_sincFourthKernel.const_mul (4 / T)
  exact h.congr (ae_of_all sincFourthKernel fun x => by
    simp only [Function.comp_apply, abs_mul, abs_of_pos hr])

theorem scaledSincFourthKernel_absolutelyContinuous {T : ℝ} (hT : 0 < T) :
    scaledSincFourthKernel T ≪ volume := by
  unfold scaledSincFourthKernel
  have hr : (4 / T : ℝ) ≠ 0 := (div_pos (by norm_num) hT).ne'
  have hemb : MeasurableEmbedding (fun x : ℝ => (4 / T) * x) :=
    (Homeomorph.mulLeft₀ (4 / T) hr).measurableEmbedding
  have hmap := hemb.absolutelyContinuous_map
    sincFourthKernel_absolutelyContinuous
  rw [Real.map_volume_mul_left hr] at hmap
  exact hmap.trans Measure.smul_absolutelyContinuous

theorem scaledSincFourthKernel_rightTail_lt_quarter {T : ℝ} (hT : 0 < T) :
    (scaledSincFourthKernel T).real
        (Ioi ((4 / T) * sincFourthTailRadius)) < 1 / 4 := by
  rw [scaledSincFourthKernel,
    map_measureReal_apply (by fun_prop) measurableSet_Ioi]
  have hpre : (fun x : ℝ => (4 / T) * x) ⁻¹'
      Ioi ((4 / T) * sincFourthTailRadius) = Ioi sincFourthTailRadius := by
    ext x
    simp only [mem_preimage, mem_Ioi]
    rw [mul_lt_mul_iff_of_pos_left (div_pos (by norm_num) hT)]
  rw [hpre]
  exact sincFourthKernel_rightTail_lt_quarter

theorem scaledSincFourthKernel_leftTail_lt_quarter {T : ℝ} (hT : 0 < T) :
    (scaledSincFourthKernel T).real
        (Iio (-((4 / T) * sincFourthTailRadius))) < 1 / 4 := by
  rw [scaledSincFourthKernel,
    map_measureReal_apply (by fun_prop) measurableSet_Iio]
  have hpre : (fun x : ℝ => (4 / T) * x) ⁻¹'
      Iio (-((4 / T) * sincFourthTailRadius)) = Iio (-sincFourthTailRadius) := by
    ext x
    simp only [mem_preimage, mem_Iio]
    rw [show -((4 / T) * sincFourthTailRadius) =
        (4 / T) * (-sincFourthTailRadius) by ring]
    rw [mul_lt_mul_iff_of_pos_left (div_pos (by norm_num) hT)]
  rw [hpre]
  exact sincFourthKernel_leftTail_lt_quarter

theorem scaledSincFourthKernel_rightShiftedMoment_le {T : ℝ} (hT : 0 < T) :
    (∫ y, |(4 / T) * sincFourthTailRadius - y|
      ∂scaledSincFourthKernel T) ≤
      (4 / T) * sincFourthShiftedMomentBound := by
  rw [scaledSincFourthKernel, integral_map (by fun_prop) (by fun_prop)]
  have hr : 0 < 4 / T := div_pos (by norm_num) hT
  have hfun : (fun y : ℝ =>
      |(4 / T) * sincFourthTailRadius - (4 / T) * y|) =
      fun y => (4 / T) * |sincFourthTailRadius - y| := by
    funext y
    rw [← mul_sub, abs_mul, abs_of_pos hr]
  rw [hfun, integral_const_mul]
  exact mul_le_mul_of_nonneg_left
    sincFourthKernel_rightShiftedMoment_le hr.le

theorem scaledSincFourthKernel_leftShiftedMoment_le {T : ℝ} (hT : 0 < T) :
    (∫ y, |-((4 / T) * sincFourthTailRadius) - y|
      ∂scaledSincFourthKernel T) ≤
      (4 / T) * sincFourthShiftedMomentBound := by
  rw [scaledSincFourthKernel, integral_map (by fun_prop) (by fun_prop)]
  have hr : 0 < 4 / T := div_pos (by norm_num) hT
  have hfun : (fun y : ℝ =>
      |-((4 / T) * sincFourthTailRadius) - (4 / T) * y|) =
      fun y => (4 / T) * |-sincFourthTailRadius - y| := by
    funext y
    rw [show -((4 / T) * sincFourthTailRadius) - (4 / T) * y =
      (4 / T) * (-sincFourthTailRadius - y) by ring,
      abs_mul, abs_of_pos hr]
  rw [hfun, integral_const_mul]
  exact mul_le_mul_of_nonneg_left
    sincFourthKernel_leftShiftedMoment_le hr.le

theorem kolmogorovDistance_standardNormal_le_scaledSincFourthKernel
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    (hmu1 : Integrable (fun x : ℝ => |x|) mu)
    {T : ℝ} (hT : 0 < T) :
    kolmogorovDistance mu
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          (∫ t in Icc (-T) T, charFunDiscrepancyQuotient mu
            NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / T) * sincFourthShiftedMomentBound)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
  letI : IsProbabilityMeasure (scaledSincFourthKernel T) :=
    scaledSincFourthKernel_isProbabilityMeasure hT
  exact kolmogorovDistance_standardNormal_le_bandLimitedSmoothingKernel
    mu (scaledSincFourthKernel T) hmu1
    (integrable_abs_scaledSincFourthKernel hT)
    (scaledSincFourthKernel_absolutelyContinuous hT)
    hT.le (by norm_num)
    (scaledSincFourthKernel_charFun_supported hT)
    (scaledSincFourthKernel_rightTail_lt_quarter hT).le
    (scaledSincFourthKernel_leftTail_lt_quarter hT).le
    (scaledSincFourthKernel_rightShiftedMoment_le hT)
    (scaledSincFourthKernel_leftShiftedMoment_le hT)

/-- A positive symmetric cutoff contained in the normalized third-moment
window for every nonnegative third moment. -/
noncomputable def berryEsseenFrequencyCutoff (rho : ℝ) (N : ℕ) : ℝ :=
  Real.sqrt (N + 1 : ℝ) / (1 + 16 * rho)

theorem berryEsseenFrequencyCutoff_pos {rho : ℝ} (hrho : 0 ≤ rho) (N : ℕ) :
    0 < berryEsseenFrequencyCutoff rho N := by
  unfold berryEsseenFrequencyCutoff
  exact div_pos (by positivity) (by positivity)

theorem Icc_berryEsseenFrequencyCutoff_subset_normalizedThirdMomentWindow
    {rho : ℝ} (hrho : 0 ≤ rho) (N : ℕ) :
    Icc (-(berryEsseenFrequencyCutoff rho N))
        (berryEsseenFrequencyCutoff rho N) ⊆
      normalizedThirdMomentWindow rho N := by
  intro t ht
  have hsqrt : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hden : 0 < 1 + 16 * rho := by positivity
  have habs : |t| ≤ berryEsseenFrequencyCutoff rho N := by
    exact abs_le.mpr ht
  constructor
  · rw [abs_div, abs_of_pos hsqrt, div_le_one hsqrt]
    calc
      |t| ≤ berryEsseenFrequencyCutoff rho N := habs
      _ ≤ Real.sqrt (N + 1 : ℝ) := by
        unfold berryEsseenFrequencyCutoff
        rw [div_le_iff₀ hden]
        nlinarith
  · rw [abs_div, abs_of_pos hsqrt]
    rw [← mul_div_assoc, div_le_one hsqrt]
    calc
      16 * rho * |t| ≤
          16 * rho * berryEsseenFrequencyCutoff rho N := by
        exact mul_le_mul_of_nonneg_left habs (mul_nonneg (by norm_num) hrho)
      _ ≤ Real.sqrt (N + 1 : ℝ) := by
        unfold berryEsseenFrequencyCutoff
        rw [← mul_div_assoc, div_le_iff₀ hden]
        nlinarith

/-- Enlarged positive cutoff certified by the sharp Taylor window. -/
noncomputable def sharpBerryEsseenFrequencyCutoff (rho : ℝ) (N : ℕ) : ℝ :=
  Real.sqrt (N + 1 : ℝ) / (1 + 2 * rho)

theorem sharpBerryEsseenFrequencyCutoff_pos
    {rho : ℝ} (hrho : 0 ≤ rho) (N : ℕ) :
    0 < sharpBerryEsseenFrequencyCutoff rho N := by
  unfold sharpBerryEsseenFrequencyCutoff
  exact div_pos (by positivity) (by positivity)

theorem Icc_sharpBerryEsseenFrequencyCutoff_subset_sharpNormalizedThirdMomentWindow
    {rho : ℝ} (hrho : 0 ≤ rho) (N : ℕ) :
    Icc (-(sharpBerryEsseenFrequencyCutoff rho N))
        (sharpBerryEsseenFrequencyCutoff rho N) ⊆
      sharpNormalizedThirdMomentWindow rho N := by
  intro t ht
  have hsqrt : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hden : 0 < 1 + 2 * rho := by positivity
  have habs : |t| ≤ sharpBerryEsseenFrequencyCutoff rho N := by
    exact abs_le.mpr ht
  constructor
  · rw [abs_div, abs_of_pos hsqrt, div_le_one hsqrt]
    calc
      |t| ≤ sharpBerryEsseenFrequencyCutoff rho N := habs
      _ ≤ Real.sqrt (N + 1 : ℝ) := by
        unfold sharpBerryEsseenFrequencyCutoff
        rw [div_le_iff₀ hden]
        nlinarith
  · rw [abs_div, abs_of_pos hsqrt]
    rw [← mul_div_assoc, div_le_one hsqrt]
    calc
      2 * rho * |t| ≤
          2 * rho * sharpBerryEsseenFrequencyCutoff rho N := by
        exact mul_le_mul_of_nonneg_left habs (mul_nonneg (by norm_num) hrho)
      _ ≤ Real.sqrt (N + 1 : ℝ) := by
        unfold sharpBerryEsseenFrequencyCutoff
        rw [← mul_div_assoc, div_le_iff₀ hden]
        nlinarith

/-- The maximal symmetric cutoff allowed by the sharp cubic window once the
standardized Lyapunov bound `rho ≥ 1` is known. -/
noncomputable def maximalSharpBerryEsseenFrequencyCutoff
    (rho : ℝ) (N : ℕ) : ℝ :=
  Real.sqrt (N + 1 : ℝ) / (2 * rho)

theorem maximalSharpBerryEsseenFrequencyCutoff_pos
    {rho : ℝ} (hrho : 1 ≤ rho) (N : ℕ) :
    0 < maximalSharpBerryEsseenFrequencyCutoff rho N := by
  unfold maximalSharpBerryEsseenFrequencyCutoff
  exact div_pos (by positivity) (mul_pos (by norm_num) (by positivity))

/-- The reciprocal half of the maximal sharp cutoff is exactly the
coefficient-one Berry--Esseen scale. -/
theorem one_div_two_mul_maximalSharpBerryEsseenFrequencyCutoff
    {rho : ℝ} (hrho : 0 < rho) (N : ℕ) :
    1 / (2 * maximalSharpBerryEsseenFrequencyCutoff rho N) =
      rho / Real.sqrt (N + 1 : ℝ) := by
  have hsqrt : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  unfold maximalSharpBerryEsseenFrequencyCutoff
  field_simp

/-- At every sufficiently wide maximal sharp cutoff, the exact bidirectional
law-phase envelope already strictly exceeds the coefficient-one
Berry--Esseen scale at the spatial origin. -/
theorem berryScale_lt_prawitzLawPhaseEnvelope_maximalSharp_zero
    (mu : Measure ℝ) [IsProbabilityMeasure mu]
    {rho : ℝ} (hrho : 0 < rho) (N : ℕ)
    (hcut6 : 6 ≤ maximalSharpBerryEsseenFrequencyCutoff rho N) :
    rho / Real.sqrt (N + 1 : ℝ) <
      prawitzLawPhaseEnvelope mu
        (maximalSharpBerryEsseenFrequencyCutoff rho N) 0 := by
  rw [← one_div_two_mul_maximalSharpBerryEsseenFrequencyCutoff hrho N]
  exact one_div_two_mul_lt_prawitzLawPhaseEnvelope_zero mu hcut6

theorem Icc_maximalSharpBerryEsseenFrequencyCutoff_subset_sharpNormalizedThirdMomentWindow
    {rho : ℝ} (hrho : 1 ≤ rho) (N : ℕ) :
    Icc (-(maximalSharpBerryEsseenFrequencyCutoff rho N))
        (maximalSharpBerryEsseenFrequencyCutoff rho N) ⊆
      sharpNormalizedThirdMomentWindow rho N := by
  intro t ht
  have hsqrt : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have hrhoPos : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hden : 0 < 2 * rho := mul_pos (by norm_num) hrhoPos
  have habs : |t| ≤ maximalSharpBerryEsseenFrequencyCutoff rho N := by
    exact abs_le.mpr ht
  constructor
  · rw [abs_div, abs_of_pos hsqrt, div_le_one hsqrt]
    calc
      |t| ≤ maximalSharpBerryEsseenFrequencyCutoff rho N := habs
      _ ≤ Real.sqrt (N + 1 : ℝ) := by
        unfold maximalSharpBerryEsseenFrequencyCutoff
        rw [div_le_iff₀ hden]
        nlinarith
  · rw [abs_div, abs_of_pos hsqrt]
    rw [← mul_div_assoc, div_le_one hsqrt]
    calc
      2 * rho * |t| ≤
          2 * rho * maximalSharpBerryEsseenFrequencyCutoff rho N := by
        exact mul_le_mul_of_nonneg_left habs hden.le
      _ = Real.sqrt (N + 1 : ℝ) := by
        unfold maximalSharpBerryEsseenFrequencyCutoff
        field_simp [hden.ne']

theorem integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_window
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    IntegrableOn
      (charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (normalizedThirdMomentWindow
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let g : ℝ → ℝ := fun t => Real.exp (-t ^ 2 / 8) *
    (4 * rho * |t| ^ 2 / Real.sqrt (N + 1 : ℝ) +
      |t| ^ 3 / (4 * (N + 1 : ℝ)))
  let S : Set ℝ := normalizedThirdMomentWindow rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hg : Integrable g := by
    simpa only [g] using integrable_gaussianDiscrepancyMajorant rho N
  have hqStrong : StronglyMeasurable q := by
    exact stronglyMeasurable_charFunDiscrepancyQuotient law target
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_normalizedThirdMomentWindow rho N
  change Integrable q (volume.restrict S)
  apply Integrable.mono' hg.integrableOn hqStrong.aestronglyMeasurable
  filter_upwards [ae_restrict_mem hS] with t ht
  rw [Real.norm_eq_abs, abs_of_nonneg
    (charFunDiscrepancyQuotient_nonneg law target t)]
  exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian
    X hX hIndep hIdent hMean hSecond hN ht.1 ht.2

theorem integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    IntegrableOn
      (charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
      (sharpNormalizedThirdMomentWindow
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let g : ℝ → ℝ := fun t => Real.exp (-(5 * t ^ 2 / 24)) *
    (rho * |t| ^ 2 / (6 * Real.sqrt (N + 1 : ℝ)) +
      |t| ^ 3 / (8 * (N + 1 : ℝ)))
  let S : Set ℝ := sharpNormalizedThirdMomentWindow rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hg : Integrable g := by
    simpa only [g] using integrable_sharpGaussianDiscrepancyMajorant rho N
  have hqStrong : StronglyMeasurable q := by
    exact stronglyMeasurable_charFunDiscrepancyQuotient law target
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_sharpNormalizedThirdMomentWindow rho N
  change Integrable q (volume.restrict S)
  apply Integrable.mono' hg.integrableOn hqStrong.aestronglyMeasurable
  filter_upwards [ae_restrict_mem hS] with t ht
  rw [Real.norm_eq_abs, abs_of_nonneg
    (charFunDiscrepancyQuotient_nonneg law target t)]
  exact charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_sharp
    X hX hIndep hIdent hMean hSecond hN ht.1 ht.2

theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_cutoff_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in Icc
        (-(berryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N))
        (berryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ)) * gaussianDampedMoment 2 +
        (1 / (4 * (N + 1 : ℝ))) * gaussianDampedMoment 3 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let S : Set ℝ := normalizedThirdMomentWindow rho N
  let J : Set ℝ := Icc (-(berryEsseenFrequencyCutoff rho N))
    (berryEsseenFrequencyCutoff rho N)
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hqS : IntegrableOn q S := by
    simpa only [q, S, law, target, rho] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_window
        X hX hIndep hIdent hMean hSecond hN
  have hJS : J ⊆ S := by
    simpa only [J, S] using
      Icc_berryEsseenFrequencyCutoff_subset_normalizedThirdMomentWindow
        hrho N
  have hlocal : (∫ t in J, q t) ≤ ∫ t in S, q t := by
    exact setIntegral_mono_set hqS
      (ae_of_all (volume.restrict S) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hJS.eventuallyLE
  calc
    (∫ t in Icc
        (-(berryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N))
        (berryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) =
        ∫ t in J, q t := rfl
    _ ≤ ∫ t in S, q t := hlocal
    _ ≤ _ := by
      simpa only [q, S, law, target, rho] using
        integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_window_le
          X hX hIndep hIdent hMean hSecond hN

theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpCutoff_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in Icc
        (-(sharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N))
        (sharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      ((∫ omega, |X 0 omega| ^ 3 ∂mu) /
          (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
        (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let S : Set ℝ := sharpNormalizedThirdMomentWindow rho N
  let J : Set ℝ := Icc (-(sharpBerryEsseenFrequencyCutoff rho N))
    (sharpBerryEsseenFrequencyCutoff rho N)
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hqS : IntegrableOn q S := by
    simpa only [q, S, law, target, rho] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow
        X hX hIndep hIdent hMean hSecond hN
  have hJS : J ⊆ S := by
    simpa only [J, S] using
      Icc_sharpBerryEsseenFrequencyCutoff_subset_sharpNormalizedThirdMomentWindow
        hrho N
  have hlocal : (∫ t in J, q t) ≤ ∫ t in S, q t := by
    exact setIntegral_mono_set hqS
      (ae_of_all (volume.restrict S) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hJS.eventuallyLE
  calc
    (∫ t in Icc
        (-(sharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N))
        (sharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) =
        ∫ t in J, q t := rfl
    _ ≤ ∫ t in S, q t := hlocal
    _ ≤ _ := by
      simpa only [q, S, law, target, rho] using
        integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow_le
          X hX hIndep hIdent hMean hSecond hN

theorem one_le_thirdAbsoluteMoment_of_secondMoment_eq_one
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    {X : Omega → ℝ} (hX : MemLp X 3 mu)
    (hSecond : ∫ omega, X omega ^ 2 ∂mu = 1) :
    1 ≤ ∫ omega, |X omega| ^ 3 ∂mu := by
  have hmonoE : eLpNorm X 2 mu ≤ eLpNorm X 3 mu :=
    eLpNorm_le_eLpNorm_of_exponent_le (by norm_num) hX.aestronglyMeasurable
  have hmono : lpNorm X 2 mu ≤ lpNorm X 3 mu := by
    rw [← toReal_eLpNorm hX.aestronglyMeasurable,
      ← toReal_eLpNorm hX.aestronglyMeasurable]
    exact ENNReal.toReal_mono hX.eLpNorm_ne_top hmonoE
  have hnorm2 : lpNorm X 2 mu = 1 := by
    rw [lpNorm_eq_integral_norm_rpow_toReal (by norm_num) (by norm_num)
      hX.aestronglyMeasurable]
    simp only [ENNReal.toReal_ofNat, Real.norm_eq_abs]
    rw [show (∫ omega, |X omega| ^ (2 : ℝ) ∂mu) = 1 by
      simpa only [Real.rpow_two, sq_abs] using hSecond]
    norm_num
  have hnorm3 : lpNorm X 3 mu =
      (∫ omega, |X omega| ^ 3 ∂mu) ^ ((3 : ℝ)⁻¹) := by
    rw [lpNorm_eq_integral_norm_rpow_toReal (by norm_num) (by norm_num)
      hX.aestronglyMeasurable]
    simp only [ENNReal.toReal_ofNat, Real.norm_eq_abs]
    congr 1
    apply integral_congr_ae
    exact ae_of_all mu fun omega => by
      dsimp only
      norm_num [Real.rpow_natCast]
  rw [hnorm2, hnorm3] at hmono
  have hrho : 0 ≤ ∫ omega, |X omega| ^ 3 ∂mu :=
    integral_nonneg fun _ => by positivity
  calc
    1 = (1 : ℝ) ^ 3 := by norm_num
    _ ≤ ((∫ omega, |X omega| ^ 3 ∂mu) ^ ((3 : ℝ)⁻¹)) ^ 3 :=
      pow_le_pow_left₀ (by norm_num) hmono 3
    _ = ∫ omega, |X omega| ^ 3 ∂mu := by
      exact Real.rpow_inv_natCast_pow hrho (by norm_num)

/-- On the full tunable window, the quartic Gaussian-proxy term is absorbed
into the standardized cubic term.  This removes the frequency obstruction
caused by converting a separate `1 / n` contribution to Berry--Esseen scale. -/
theorem charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable_absorbed
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) {t : ℝ}
    (ht : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1) :
    charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t ≤
      Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
        ((1 / 6 + 1 / (8 * c)) *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
            Real.sqrt (N + 1 : ℝ)) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hs : 0 < s := by dsimp only [s]; positivity
  have hsSq : s ^ 2 = (N + 1 : ℝ) := by
    dsimp only [s]
    rw [Real.sq_sqrt]
    positivity
  have hsmall' : c * rho * |t| ≤ s := by
    rw [abs_div, abs_of_pos hs] at hsmall
    rw [show c * rho * (|t| / s) = (c * rho * |t|) / s by ring,
      div_le_one hs] at hsmall
    exact hsmall
  have hcabs : c * |t| ≤ c * rho * |t| := by
    have h := mul_le_mul_of_nonneg_left hrho
      (mul_nonneg hc0.le (abs_nonneg t))
    nlinarith
  have hsrho : s ≤ rho * s := by
    have h := mul_le_mul_of_nonneg_right hrho hs.le
    nlinarith
  have hbase : c * |t| ≤ rho * s :=
    hcabs.trans (hsmall'.trans hsrho)
  have hscaled : c * |t| ^ 3 ≤ rho * s * |t| ^ 2 := by
    have h := mul_le_mul_of_nonneg_right hbase (sq_nonneg |t|)
    nlinarith
  have hquartic : |t| ^ 3 / (8 * (N + 1 : ℝ)) ≤
      (1 / (8 * c)) * rho * |t| ^ 2 / s := by
    rw [← hsSq]
    rw [show (1 / (8 * c)) * rho * |t| ^ 2 / s =
      (rho * |t| ^ 2) / (8 * c * s) by ring]
    rw [div_le_div_iff₀ (by positivity : 0 < 8 * s ^ 2)
      (by positivity : 0 < 8 * c * s)]
    have h := mul_le_mul_of_nonneg_right hscaled
      (mul_nonneg (by norm_num : (0 : ℝ) ≤ 8) hs.le)
    nlinarith
  have hpoly :
      rho * |t| ^ 2 / (6 * s) + |t| ^ 3 / (8 * (N + 1 : ℝ)) ≤
        (1 / 6 + 1 / (8 * c)) * rho * |t| ^ 2 / s := by
    calc
      rho * |t| ^ 2 / (6 * s) + |t| ^ 3 / (8 * (N + 1 : ℝ)) ≤
          rho * |t| ^ 2 / (6 * s) +
            (1 / (8 * c)) * rho * |t| ^ 2 / s :=
        by simpa only [add_comm] using
          add_le_add_left hquartic (rho * |t| ^ 2 / (6 * s))
      _ = (1 / 6 + 1 / (8 * c)) * rho * |t| ^ 2 / s := by ring
  have h :=
    charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable
      X hX hIndep hIdent hMean hSecond hc.le hN ht hsmall
  simpa only [rho, s] using h.trans
    (mul_le_mul_of_nonneg_left hpoly (Real.exp_pos _).le)

/-- Pointwise signed-Prawitz majorant for a normalized centered iid sum on a
tunable local window. -/
theorem re_forwardPrawitzSingleIntegrand_normalizedCenteredIidSum_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T)
    (htaylor : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                  (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                    X N)
                  (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
                    X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable))
              x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
      (1 / Real.pi + t / T) *
          (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
            ((1 / 6 + 1 / (8 * c)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
                Real.sqrt (N + 1 : ℝ))) +
        (2 / T) * Real.exp (-(t ^ 2) / 2) := by
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hbase := re_forwardPrawitzSingleIntegrand_le_discrepancyQuotient
    law hT ht x
  have hq :=
    charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable_absorbed
      X hX hIndep hIdent hMean hSecond hc hN htaylor hsmall
  have hcoef : 0 ≤ 1 / Real.pi + t / T :=
    add_nonneg (div_nonneg (by norm_num) Real.pi_pos.le)
      (div_nonneg ht.1.le hT.le)
  exact hbase.trans (add_le_add
    (mul_le_mul_of_nonneg_left hq hcoef) le_rfl)

/-- Normalized-iid signed majorant with the finite-window Gaussian cosine
term preserved exactly. -/
theorem re_forwardPrawitzSingleIntegrand_normalizedCenteredIidSum_le_with_exactGaussian
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T t : ℝ} (hT : 0 < T) (ht : t ∈ Ioo 0 T)
    (htaylor : |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (hsmall : c * (∫ omega, |X 0 omega| ^ 3 ∂mu) *
        |t / Real.sqrt (N + 1 : ℝ)| ≤ 1)
    (x : ℝ) :
    ((Complex.I / (2 * Real.pi : ℝ)) *
          (symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
                  (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum
                    X N)
                  (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
                    X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable))
              x t -
            symmetricPrawitzPVIntegrand T
              (MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw)
              x t) +
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun
                NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t)).re ≤
      (1 / Real.pi + t / T) *
          (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
            ((1 / 6 + 1 / (8 * c)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
                Real.sqrt (N + 1 : ℝ))) +
        2 * prawitzFejerFrequencyWeight T t *
          Real.exp (-(t ^ 2) / 2) * Real.cos (x * t) := by
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  letI : IsProbabilityMeasure law := inferInstance
  have hbase :=
    re_forwardPrawitzSingleIntegrand_le_discrepancyQuotient_with_exactGaussian
      law hT ht x
  have hq :=
    charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable_absorbed
      X hX hIndep hIdent hMean hSecond hc hN htaylor hsmall
  have hcoef : 0 ≤ 1 / Real.pi + t / T :=
    add_nonneg (div_nonneg (by norm_num) Real.pi_pos.le)
      (div_nonneg ht.1.le hT.le)
  exact hbase.trans (add_le_add
    (mul_le_mul_of_nonneg_left hq hcoef) le_rfl)

/-- The absorbed characteristic-function contribution integrates to its two
positive-half-line tunable moments.  No Gaussian remainder is included in
this estimate. -/
theorem integral_Icc_absorbedPrawitzFrequencyMajorant_le
    {c A T : ℝ} (hc : 1 / 3 < c) (hA : 0 ≤ A) (hT : 0 < T) :
    (∫ t in Icc (0 : ℝ) T,
        (1 / Real.pi + t / T) *
          (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
            (A * |t| ^ 2))) ≤
      A * ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
        (1 / T) * (tunableGaussianDampedMoment c 3 / 2)) := by
  let f2 : ℝ → ℝ := fun t => |t| ^ 2 *
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))
  let f3 : ℝ → ℝ := fun t => |t| ^ 3 *
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))
  let g : ℝ → ℝ := fun t =>
    (A / Real.pi) * f2 t + (A / T) * f3 t
  have hf2 : Integrable f2 := by
    simpa only [f2] using
      integrable_abs_pow_mul_rexp_neg_tunable_sq hc 2
  have hf3 : Integrable f3 := by
    simpa only [f3] using
      integrable_abs_pow_mul_rexp_neg_tunable_sq hc 3
  have hg : Integrable g := by
    dsimp only [g]
    exact (hf2.const_mul (A / Real.pi)).add (hf3.const_mul (A / T))
  have hgNonneg : ∀ t, 0 ≤ g t := fun t => by
    dsimp only [g, f2, f3]
    positivity
  have hrewrite :
      (∫ t in Ioo (0 : ℝ) T,
          (1 / Real.pi + t / T) *
            (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
              (A * |t| ^ 2))) =
        ∫ t in Ioo (0 : ℝ) T, g t := by
    apply setIntegral_congr_fun measurableSet_Ioo
    intro t ht
    dsimp only [g, f2, f3]
    rw [abs_of_pos ht.1]
    ring
  calc
    (∫ t in Icc (0 : ℝ) T,
        (1 / Real.pi + t / T) *
          (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
            (A * |t| ^ 2))) =
        ∫ t in Ioo (0 : ℝ) T,
          (1 / Real.pi + t / T) *
            (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
              (A * |t| ^ 2)) := integral_Icc_eq_integral_Ioo
    _ = ∫ t in Ioo (0 : ℝ) T, g t := hrewrite
    _ ≤ ∫ t in Ioi (0 : ℝ), g t := by
      exact integral_mono_measure
        (Measure.restrict_mono_set volume Ioo_subset_Ioi_self)
        (ae_of_all (volume.restrict (Ioi (0 : ℝ))) hgNonneg) hg.integrableOn
    _ = (A / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (A / T) * (tunableGaussianDampedMoment c 3 / 2) := by
      rw [show (∫ t in Ioi (0 : ℝ), g t) =
          (∫ t in Ioi (0 : ℝ), (A / Real.pi) * f2 t) +
            ∫ t in Ioi (0 : ℝ), (A / T) * f3 t by
        simpa only [g, Pi.add_apply] using
          integral_add
            (μ := volume.restrict (Ioi (0 : ℝ)))
            (hf2.const_mul (A / Real.pi)).integrableOn
            (hf3.const_mul (A / T)).integrableOn,
        integral_const_mul, integral_const_mul,
        show (∫ t in Ioi (0 : ℝ), f2 t) =
            tunableGaussianDampedMoment c 2 / 2 by
          simpa only [f2] using
            integral_Ioi_abs_pow_mul_rexp_neg_tunable_sq c 2,
        show (∫ t in Ioi (0 : ℝ), f3 t) =
            tunableGaussianDampedMoment c 3 / 2 by
          simpa only [f3] using
            integral_Ioi_abs_pow_mul_rexp_neg_tunable_sq c 3]
    _ = A * ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (1 / T) * (tunableGaussianDampedMoment c 3 / 2)) := by ring

/-- Integrating the absorbed positive-frequency Prawitz majorant costs exactly
one half of the full-line quadratic and cubic tunable moments.  The finite
window is retained on the left; extending only the nonnegative majorant to the
positive half-line yields the displayed universal upper bound. -/
theorem integral_Icc_absorbedPrawitzMajorant_le
    {c A T : ℝ} (hc : 1 / 3 < c) (hA : 0 ≤ A) (hT : 0 < T) :
    (∫ t in Icc (0 : ℝ) T,
        (1 / Real.pi + t / T) *
            (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
              (A * |t| ^ 2)) +
          (2 / T) * Real.exp (-(t ^ 2) / 2)) ≤
      A * ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
        (1 / T) * (tunableGaussianDampedMoment c 3 / 2)) +
        Real.sqrt (2 * Real.pi) / T := by
  let f2 : ℝ → ℝ := fun t => |t| ^ 2 *
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))
  let f3 : ℝ → ℝ := fun t => |t| ^ 3 *
    Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2))
  let f0 : ℝ → ℝ := fun t => Real.exp (-(t ^ 2) / 2)
  let g : ℝ → ℝ := fun t =>
    (A / Real.pi) * f2 t + (A / T) * f3 t + (2 / T) * f0 t
  have hf2 : Integrable f2 := by
    simpa only [f2] using
      integrable_abs_pow_mul_rexp_neg_tunable_sq hc 2
  have hf3 : Integrable f3 := by
    simpa only [f3] using
      integrable_abs_pow_mul_rexp_neg_tunable_sq hc 3
  have hf0 : Integrable f0 := by
    have h := integrable_exp_neg_mul_sq (by norm_num : (0 : ℝ) < 1 / 2)
    apply h.congr
    exact ae_of_all _ fun t => by
      dsimp only [f0]
      congr 1
      ring
  have hg : Integrable g := by
    dsimp only [g]
    exact ((hf2.const_mul (A / Real.pi)).add
      (hf3.const_mul (A / T))).add (hf0.const_mul (2 / T))
  have hgNonneg : ∀ t, 0 ≤ g t := fun t => by
    dsimp only [g, f2, f3, f0]
    positivity
  have hrewrite :
      (∫ t in Ioo (0 : ℝ) T,
          (1 / Real.pi + t / T) *
              (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
                (A * |t| ^ 2)) +
            (2 / T) * Real.exp (-(t ^ 2) / 2)) =
        ∫ t in Ioo (0 : ℝ) T, g t := by
    apply setIntegral_congr_fun measurableSet_Ioo
    intro t ht
    dsimp only [g, f2, f3, f0]
    rw [abs_of_pos ht.1]
    ring
  have hgauss : (∫ t in Ioi (0 : ℝ), f0 t) =
      Real.sqrt (2 * Real.pi) / 2 := by
    rw [show (fun t : ℝ => f0 t) =
        fun t => Real.exp (-(1 / 2 : ℝ) * t ^ 2) by
      funext t
      dsimp only [f0]
      congr 1
      ring,
      integral_gaussian_Ioi]
    congr 2
    ring
  calc
    (∫ t in Icc (0 : ℝ) T,
        (1 / Real.pi + t / T) *
            (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
              (A * |t| ^ 2)) +
          (2 / T) * Real.exp (-(t ^ 2) / 2)) =
        ∫ t in Ioo (0 : ℝ) T,
          (1 / Real.pi + t / T) *
              (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
                (A * |t| ^ 2)) +
            (2 / T) * Real.exp (-(t ^ 2) / 2) :=
      integral_Icc_eq_integral_Ioo
    _ = ∫ t in Ioo (0 : ℝ) T, g t := hrewrite
    _ ≤ ∫ t in Ioi (0 : ℝ), g t := by
      exact integral_mono_measure
        (Measure.restrict_mono_set volume Ioo_subset_Ioi_self)
        (ae_of_all (volume.restrict (Ioi (0 : ℝ))) hgNonneg) hg.integrableOn
    _ = (A / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (A / T) * (tunableGaussianDampedMoment c 3 / 2) +
          (2 / T) * (Real.sqrt (2 * Real.pi) / 2) := by
      have h12 :
          (∫ t in Ioi (0 : ℝ),
              (A / Real.pi) * f2 t + (A / T) * f3 t) =
            (∫ t in Ioi (0 : ℝ), (A / Real.pi) * f2 t) +
              ∫ t in Ioi (0 : ℝ), (A / T) * f3 t := by
        simpa only [Pi.add_apply] using
          integral_add
            (μ := volume.restrict (Ioi (0 : ℝ)))
            (hf2.const_mul (A / Real.pi)).integrableOn
            (hf3.const_mul (A / T)).integrableOn
      have h123 :
          (∫ t in Ioi (0 : ℝ),
              ((A / Real.pi) * f2 t + (A / T) * f3 t) +
                (2 / T) * f0 t) =
            (∫ t in Ioi (0 : ℝ),
              (A / Real.pi) * f2 t + (A / T) * f3 t) +
              ∫ t in Ioi (0 : ℝ), (2 / T) * f0 t := by
        simpa only [Pi.add_apply] using
          integral_add
            (μ := volume.restrict (Ioi (0 : ℝ)))
            ((hf2.const_mul (A / Real.pi)).add
              (hf3.const_mul (A / T))).integrableOn
            (hf0.const_mul (2 / T)).integrableOn
      change (∫ t in Ioi (0 : ℝ),
          (A / Real.pi) * f2 t + (A / T) * f3 t + (2 / T) * f0 t) = _
      rw [h123, h12]
      rw [integral_const_mul, integral_const_mul, integral_const_mul,
        show (∫ t in Ioi (0 : ℝ), f2 t) =
            tunableGaussianDampedMoment c 2 / 2 by
          simpa only [f2] using
            integral_Ioi_abs_pow_mul_rexp_neg_tunable_sq c 2,
        show (∫ t in Ioi (0 : ℝ), f3 t) =
            tunableGaussianDampedMoment c 3 / 2 by
          simpa only [f3] using
            integral_Ioi_abs_pow_mul_rexp_neg_tunable_sq c 3,
        hgauss]
    _ = A * ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (1 / T) * (tunableGaussianDampedMoment c 3 / 2)) +
          Real.sqrt (2 * Real.pi) / T := by ring

/-- Forward signed-CDF consequence of the absorbed Prawitz integrand bound at
the maximal cutoff allowed by the tunable Taylor window.  This keeps the
quadratic and cubic frequency costs separate from the exact Gaussian Fejér
remainder. -/
theorem cdf_sub_standardNormalCDF_normalizedCenteredIidSum_le_signedPrawitz_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) (x : ℝ) :
    cdf
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      ((1 / 6 + 1 / (8 * c)) *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) *
        ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (1 / admissibleTunableThirdMomentCutoff c
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) *
            (tunableGaussianDampedMoment c 3 / 2)) +
        Real.sqrt (2 * Real.pi) /
          admissibleTunableThirdMomentCutoff c
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  let A : ℝ := (1 / 6 + 1 / (8 * c)) * rho /
    Real.sqrt (N + 1 : ℝ)
  let F : ℝ → ℂ := fun t =>
    (Complex.I / (2 * Real.pi : ℝ)) *
        (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun law) x t -
          symmetricPrawitzPVIntegrand T
            (MeasureTheory.charFun target) x t) +
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun target (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun target t)
  let g : ℝ → ℝ := fun t =>
    (1 / Real.pi + t / T) *
        (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
          (A * |t| ^ 2)) +
      (2 / T) * Real.exp (-(t ^ 2) / 2)
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hT : 0 < T := admissibleTunableThirdMomentCutoff_pos hc0 hrho0 N
  have hA : 0 ≤ A := by
    dsimp only [A]
    positivity
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hF : IntegrableOn F (Icc (0 : ℝ) T) := by
    have hLaw := integrableOn_symmetricPrawitzPVIntegrand_charFun
      law hLaw1 hT x
    have hTarget := integrableOn_symmetricPrawitzPVIntegrand_charFun
      target integrable_abs_standardNormalLaw hT x
    have hP := (hLaw.sub hTarget).const_mul
      (Complex.I / (2 * Real.pi : ℝ))
    have hR : IntegrableOn (fun t =>
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun target (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun target t)) (Icc (0 : ℝ) T) := by
      simpa only [target] using
        (integrable_pairedPrawitzReflectionFrequencyIntegrand
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).integrableOn
    simpa only [F] using hP.add hR
  have hg : IntegrableOn g (Icc (0 : ℝ) T) := by
    exact (by fun_prop : Continuous g).integrableOn_Icc
  have hFre : IntegrableOn (fun t => (F t).re) (Icc (0 : ℝ) T) :=
    Integrable.re hF
  have hwindow : Icc (-T) T ⊆
      tunableNormalizedThirdMomentWindow c rho N := by
    simpa only [T] using
      Icc_admissibleTunableThirdMomentCutoff_subset_window hc0 hrho0 N
  have hIntegral : (∫ t in Icc (0 : ℝ) T, (F t).re) ≤
      ∫ t in Icc (0 : ℝ) T, g t := by
    rw [integral_Icc_eq_integral_Ioo, integral_Icc_eq_integral_Ioo]
    apply integral_mono_ae
      (hFre.mono_set Ioo_subset_Icc_self)
      (hg.mono_set Ioo_subset_Icc_self)
    filter_upwards [ae_restrict_mem measurableSet_Ioo] with t ht
    have htJ : t ∈ Icc (-T) T := ⟨by linarith [hT.le, ht.1], ht.2.le⟩
    have htWindow := hwindow htJ
    have hpoint :=
      re_forwardPrawitzSingleIntegrand_normalizedCenteredIidSum_le
        X hX hIndep hIdent hMean hSecond hc hN hT ht
          htWindow.1 htWindow.2 x
    dsimp only [F, g, law, target, T, A, Y, rho]
    convert hpoint using 1 <;> ring
  have hsigned := cdf_sub_standardNormalCDF_le_prawitz_singleSignedIntegral
    law hLaw1 hT x
  have hmajor := integral_Icc_absorbedPrawitzMajorant_le hc hA hT
  change cdf law x - cdf target x ≤ _
  calc
    cdf law x - cdf target x ≤ (∫ t in Icc (0 : ℝ) T, F t).re := by
      simpa only [F, target] using hsigned
    _ = ∫ t in Icc (0 : ℝ) T, (F t).re :=
      (integral_re hF).symm
    _ ≤ ∫ t in Icc (0 : ℝ) T, g t := hIntegral
    _ ≤ A * ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (1 / T) * (tunableGaussianDampedMoment c 3 / 2)) +
          Real.sqrt (2 * Real.pi) / T := by
      simpa only [g] using hmajor
    _ = _ := rfl

/-- Forward signed-CDF bound at the admissible tunable cutoff which bounds
only the characteristic-function contribution and retains the exact uniform
finite-window Gaussian remainder. -/
theorem cdf_sub_standardNormalCDF_normalizedCenteredIidSum_le_signedPrawitz_tunable_finiteGaussian
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) (x : ℝ) :
    cdf
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          x -
        cdf NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw x ≤
      ((1 / 6 + 1 / (8 * c)) *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) *
        ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (1 / admissibleTunableThirdMomentCutoff c
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) *
            (tunableGaussianDampedMoment c 3 / 2)) +
        prawitzGaussianCosineRemainder
          (admissibleTunableThirdMomentCutoff c
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N) 0 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  let A : ℝ := (1 / 6 + 1 / (8 * c)) * rho /
    Real.sqrt (N + 1 : ℝ)
  let F : ℝ → ℂ := fun t =>
    (Complex.I / (2 * Real.pi : ℝ)) *
        (symmetricPrawitzPVIntegrand T (MeasureTheory.charFun law) x t -
          symmetricPrawitzPVIntegrand T
            (MeasureTheory.charFun target) x t) +
      (prawitzFejerFrequencyWeight T t : ℂ) *
        (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun target (-t) +
          Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
            MeasureTheory.charFun target t)
  let g : ℝ → ℝ := fun t =>
    (1 / Real.pi + t / T) *
      (Real.exp (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)) *
        (A * |t| ^ 2))
  let r : ℝ → ℝ := fun t =>
    2 * prawitzFejerFrequencyWeight T t *
      Real.exp (-(t ^ 2) / 2) * Real.cos (x * t)
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hT : 0 < T := admissibleTunableThirdMomentCutoff_pos hc0 hrho0 N
  have hA : 0 ≤ A := by
    dsimp only [A]
    positivity
  have hLaw1 : Integrable (fun y : ℝ => |y|) law := by
    change Integrable (fun y : ℝ => |y|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hF : IntegrableOn F (Icc (0 : ℝ) T) := by
    have hLaw := integrableOn_symmetricPrawitzPVIntegrand_charFun
      law hLaw1 hT x
    have hTarget := integrableOn_symmetricPrawitzPVIntegrand_charFun
      target integrable_abs_standardNormalLaw hT x
    have hP := (hLaw.sub hTarget).const_mul
      (Complex.I / (2 * Real.pi : ℝ))
    have hR : IntegrableOn (fun t =>
        (prawitzFejerFrequencyWeight T t : ℂ) *
          (Complex.exp ((((x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun target (-t) +
            Complex.exp (((-(x * t) : ℝ) : ℂ) * Complex.I) *
              MeasureTheory.charFun target t)) (Icc (0 : ℝ) T) := by
      simpa only [target] using
        (integrable_pairedPrawitzReflectionFrequencyIntegrand
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw hT x).integrableOn
    simpa only [F] using hP.add hR
  have hg : IntegrableOn g (Icc (0 : ℝ) T) :=
    (by fun_prop : Continuous g).integrableOn_Icc
  have hr : IntegrableOn r (Icc (0 : ℝ) T) := by
    apply Continuous.integrableOn_Icc
    dsimp only [r]
    unfold prawitzFejerFrequencyWeight
    fun_prop
  have hgr : IntegrableOn (fun t => g t + r t) (Icc (0 : ℝ) T) := by
    simpa only [Pi.add_apply] using hg.add hr
  have hFre : IntegrableOn (fun t => (F t).re) (Icc (0 : ℝ) T) :=
    Integrable.re hF
  have hwindow : Icc (-T) T ⊆
      tunableNormalizedThirdMomentWindow c rho N := by
    simpa only [T] using
      Icc_admissibleTunableThirdMomentCutoff_subset_window hc0 hrho0 N
  have hIntegral : (∫ t in Icc (0 : ℝ) T, (F t).re) ≤
      ∫ t in Icc (0 : ℝ) T, (g t + r t) := by
    rw [integral_Icc_eq_integral_Ioo, integral_Icc_eq_integral_Ioo]
    apply integral_mono_ae
      (hFre.mono_set Ioo_subset_Icc_self)
      (hgr.mono_set Ioo_subset_Icc_self)
    filter_upwards [ae_restrict_mem measurableSet_Ioo] with t ht
    have htJ : t ∈ Icc (-T) T := ⟨by linarith [hT.le, ht.1], ht.2.le⟩
    have htWindow := hwindow htJ
    have hpoint :=
      re_forwardPrawitzSingleIntegrand_normalizedCenteredIidSum_le_with_exactGaussian
        X hX hIndep hIdent hMean hSecond hc hN hT ht
          htWindow.1 htWindow.2 x
    dsimp only [F, g, r, law, target, T, A, Y, rho]
    convert hpoint using 1 <;> ring
  have hsigned := cdf_sub_standardNormalCDF_le_prawitz_singleSignedIntegral
    law hLaw1 hT x
  have hmajor := integral_Icc_absorbedPrawitzFrequencyMajorant_le hc hA hT
  have hrBound : (∫ t in Icc (0 : ℝ) T, r t) ≤
      prawitzGaussianCosineRemainder T 0 := by
    simpa only [r, prawitzGaussianCosineRemainder] using
      prawitzGaussianCosineRemainder_le_zero hT x
  change cdf law x - cdf target x ≤ _
  calc
    cdf law x - cdf target x ≤ (∫ t in Icc (0 : ℝ) T, F t).re := by
      simpa only [F, target] using hsigned
    _ = ∫ t in Icc (0 : ℝ) T, (F t).re :=
      (integral_re hF).symm
    _ ≤ ∫ t in Icc (0 : ℝ) T, (g t + r t) := hIntegral
    _ = (∫ t in Icc (0 : ℝ) T, g t) +
          ∫ t in Icc (0 : ℝ) T, r t := by
      exact integral_add hg hr
    _ ≤ A * ((1 / Real.pi) * (tunableGaussianDampedMoment c 2 / 2) +
          (1 / T) * (tunableGaussianDampedMoment c 3 / 2)) +
        prawitzGaussianCosineRemainder T 0 := add_le_add hmajor hrBound
    _ = _ := rfl

/-- Integrated absorbed tunable discrepancy: only the quadratic damped
moment remains in the leading Berry--Esseen scale. -/
theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow_le_absorbed
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in tunableNormalizedThirdMomentWindow c
        (∫ omega, |X 0 omega| ^ 3 ∂mu) N,
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      ((1 / 6 + 1 / (8 * c)) *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let A : ℝ := (1 / 6 + 1 / (8 * c)) * rho / s
  let g : ℝ → ℝ := fun t => A *
    (|t| ^ 2 * Real.exp
      (-(tunableCharFunDampingCoefficient c * t ^ 2 / 2)))
  let S : Set ℝ := tunableNormalizedThirdMomentWindow c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hs : 0 < s := by dsimp only [s]; positivity
  have hA : 0 ≤ A := by dsimp only [A]; positivity
  have hgNonneg : ∀ t, 0 ≤ g t := fun t => by
    dsimp only [g]
    positivity
  have hg : Integrable g := by
    have h2 := integrable_abs_pow_mul_rexp_neg_tunable_sq hc 2
    simpa only [g] using h2.const_mul A
  have hqS : IntegrableOn q S := by
    simpa only [q, S, law, target, rho] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow
        X hX hIndep hIdent hMean hSecond hc hN
  have hS : MeasurableSet S := by
    simpa only [S] using measurableSet_tunableNormalizedThirdMomentWindow c rho N
  change (∫ t in S, q t) ≤ _
  calc
    (∫ t in S, q t) ≤ ∫ t in S, g t := by
      apply integral_mono_ae hqS hg.integrableOn
      filter_upwards [ae_restrict_mem hS] with t ht
      have hpoint :=
        charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable_absorbed
          X hX hIndep hIdent hMean hSecond hc hN ht.1 ht.2
      dsimp only [g, A, q, law, target, rho, s]
      convert hpoint using 1 <;> ring
    _ ≤ ∫ t, g t :=
      integral_mono_measure Measure.restrict_le_self
        (ae_of_all volume hgNonneg) hg
    _ = A * tunableGaussianDampedMoment c 2 := by
      rw [integral_const_mul]
      rfl
    _ = _ := rfl

theorem integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_maximalSharpCutoff_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in Icc
        (-(maximalSharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N))
        (maximalSharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      ((∫ omega, |X 0 omega| ^ 3 ∂mu) /
          (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
        (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3 := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
      (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
        X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let S : Set ℝ := sharpNormalizedThirdMomentWindow rho N
  let J : Set ℝ := Icc (-(maximalSharpBerryEsseenFrequencyCutoff rho N))
    (maximalSharpBerryEsseenFrequencyCutoff rho N)
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hqS : IntegrableOn q S := by
    simpa only [q, S, law, target, rho] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow
        X hX hIndep hIdent hMean hSecond hN
  have hJS : J ⊆ S := by
    simpa only [J, S] using
      Icc_maximalSharpBerryEsseenFrequencyCutoff_subset_sharpNormalizedThirdMomentWindow
        hrho N
  have hlocal : (∫ t in J, q t) ≤ ∫ t in S, q t := by
    exact setIntegral_mono_set hqS
      (ae_of_all (volume.restrict S) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hJS.eventuallyLE
  calc
    (∫ t in Icc
        (-(maximalSharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N))
        (maximalSharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      charFunDiscrepancyQuotient
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) =
        ∫ t in J, q t := rfl
    _ ≤ ∫ t in S, q t := hlocal
    _ ≤ _ := by
      simpa only [q, S, law, target, rho] using
        integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow_le
          X hX hIndep hIdent hMean hSecond hN

/-- The maximal sharp normalized-iid discrepancy window with the certified
uniform Prawitz-filter factor. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_maximalSharpCutoff_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in Icc 0
        (maximalSharpBerryEsseenFrequencyCutoff
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      ‖scaledPrawitzSmoothingFilter
          (maximalSharpBerryEsseenFrequencyCutoff
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N) t‖ *
        charFunDiscrepancyQuotient
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (1 + Real.pi ^ 2 / 32) *
        (((∫ omega, |X 0 omega| ^ 3 ∂mu) /
            (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
          (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let T : ℝ := maximalSharpBerryEsseenFrequencyCutoff rho N
  let J : Set ℝ := Icc (-T) T
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT : 0 < T := maximalSharpBerryEsseenFrequencyCutoff_pos hrho N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hweighted :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_le_const
      law target hLaw1 integrable_abs_standardNormalLaw hT
  have hqJ : IntegrableOn q J := by
    have hqS : IntegrableOn q (sharpNormalizedThirdMomentWindow rho N) := by
      simpa only [q, law, target, rho, Y] using
        integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpWindow
          X hX hIndep hIdent hMean hSecond hN
    have hJS : J ⊆ sharpNormalizedThirdMomentWindow rho N := by
      simpa only [J, T] using
        Icc_maximalSharpBerryEsseenFrequencyCutoff_subset_sharpNormalizedThirdMomentWindow
          hrho N
    exact hqS.mono_set hJS
  have hpositive : (∫ t in Icc 0 T, q t) ≤ ∫ t in J, q t := by
    have hsubset : Icc 0 T ⊆ J := by
      intro t ht
      exact ⟨by linarith [hT.le, ht.1], ht.2⟩
    exact setIntegral_mono_set hqJ
      (ae_of_all (volume.restrict J) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hsubset.eventuallyLE
  have hlocal :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_maximalSharpCutoff_le
      X hX hIndep hIdent hMean hSecond hN
  have hC : 0 ≤ 1 + Real.pi ^ 2 / 32 := by positivity
  change (∫ t in Icc 0 T,
      ‖scaledPrawitzSmoothingFilter T t‖ * q t) ≤ _
  calc
    _ ≤ (1 + Real.pi ^ 2 / 32) * ∫ t in Icc 0 T, q t := hweighted
    _ ≤ (1 + Real.pi ^ 2 / 32) * ∫ t in J, q t :=
      mul_le_mul_of_nonneg_left hpositive hC
    _ ≤ _ := by
      apply mul_le_mul_of_nonneg_left _ hC
      simpa only [J, T, q, rho, law, target, Y] using hlocal

/-- Explicit bidirectional Prawitz smoothing estimate for normalized centered
iid sums at the maximal certified sharp Taylor cutoff. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_maximalSharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) *
          ((1 + Real.pi ^ 2 / 32) *
            (((∫ omega, |X 0 omega| ^ 3 ∂mu) /
                (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
              (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3)) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          (2 * Real.pi /
            maximalSharpBerryEsseenFrequencyCutoff
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := maximalSharpBerryEsseenFrequencyCutoff rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT : 0 < T := maximalSharpBerryEsseenFrequencyCutoff_pos hrho N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth := kolmogorovDistance_standardNormal_le_prawitz law hLaw1 hT
  have hlocal :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_maximalSharpCutoff_le
      X hX hIndep hIdent hMean hSecond hN
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        (1 / Real.pi) * (∫ t in Icc 0 T,
          ‖scaledPrawitzSmoothingFilter T t‖ *
            charFunDiscrepancyQuotient law target t) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := hsmooth
    _ ≤ _ := by
      dsimp only [T, rho, law, target, Y]
      gcongr

/-- The full tunable normalized-iid discrepancy window with the certified
uniform Prawitz-filter factor. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableCutoff_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in Icc 0
        (admissibleTunableThirdMomentCutoff c
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      ‖scaledPrawitzSmoothingFilter
          (admissibleTunableThirdMomentCutoff c
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N) t‖ *
        charFunDiscrepancyQuotient
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (1 + Real.pi ^ 2 / 32) *
        (((∫ omega, |X 0 omega| ^ 3 ∂mu) /
            (6 * Real.sqrt (N + 1 : ℝ))) *
              tunableGaussianDampedMoment c 2 +
          (1 / (8 * (N + 1 : ℝ))) *
              tunableGaussianDampedMoment c 3) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  let J : Set ℝ := Icc (-T) T
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hT : 0 < T := admissibleTunableThirdMomentCutoff_pos hc0 hrho0 N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hweighted :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_le_const
      law target hLaw1 integrable_abs_standardNormalLaw hT
  have hqS : IntegrableOn q
      (tunableNormalizedThirdMomentWindow c rho N) := by
    simpa only [q, law, target, rho, Y] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow
        X hX hIndep hIdent hMean hSecond hc hN
  have hJS : J ⊆ tunableNormalizedThirdMomentWindow c rho N := by
    simpa only [J, T] using
      Icc_admissibleTunableThirdMomentCutoff_subset_window hc0 hrho0 N
  have hqJ : IntegrableOn q J := hqS.mono_set hJS
  have hpositive : (∫ t in Icc 0 T, q t) ≤ ∫ t in J, q t := by
    have hsubset : Icc 0 T ⊆ J := by
      intro t ht
      exact ⟨by linarith [hT.le, ht.1], ht.2⟩
    exact setIntegral_mono_set hqJ
      (ae_of_all (volume.restrict J) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hsubset.eventuallyLE
  have hlocal :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow_le
      X hX hIndep hIdent hMean hSecond hc hN
  have hJwindow : (∫ t in J, q t) ≤
      ∫ t in tunableNormalizedThirdMomentWindow c rho N, q t := by
    exact setIntegral_mono_set hqS
      (ae_of_all (volume.restrict
          (tunableNormalizedThirdMomentWindow c rho N)) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hJS.eventuallyLE
  have hC : 0 ≤ 1 + Real.pi ^ 2 / 32 := by positivity
  change (∫ t in Icc 0 T,
      ‖scaledPrawitzSmoothingFilter T t‖ * q t) ≤ _
  calc
    _ ≤ (1 + Real.pi ^ 2 / 32) * ∫ t in Icc 0 T, q t := hweighted
    _ ≤ (1 + Real.pi ^ 2 / 32) * ∫ t in J, q t :=
      mul_le_mul_of_nonneg_left hpositive hC
    _ ≤ _ := by
      apply mul_le_mul_of_nonneg_left _ hC
      exact hJwindow.trans (by
        simpa only [q, rho, law, target, Y] using hlocal)

/-- Bidirectional Prawitz smoothing for the normalized iid sum at the full
tunable cutoff, with the spatial term kept exact. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) *
          ((1 + Real.pi ^ 2 / 32) *
            (((∫ omega, |X 0 omega| ^ 3 ∂mu) /
                (6 * Real.sqrt (N + 1 : ℝ))) *
                  tunableGaussianDampedMoment c 2 +
              (1 / (8 * (N + 1 : ℝ))) *
                  tunableGaussianDampedMoment c 3)) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          (2 * Real.pi /
            admissibleTunableThirdMomentCutoff c
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT : 0 < T := admissibleTunableThirdMomentCutoff_pos
    (lt_trans (by norm_num) hc) (lt_of_lt_of_le zero_lt_one hrho) N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth := kolmogorovDistance_standardNormal_le_prawitz law hLaw1 hT
  have hlocal :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableCutoff_le
      X hX hIndep hIdent hMean hSecond hc hN
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        (1 / Real.pi) * (∫ t in Icc 0 T,
          ‖scaledPrawitzSmoothingFilter T t‖ *
            charFunDiscrepancyQuotient law target t) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := hsmooth
    _ ≤ _ := by
      dsimp only [T, rho, law, target, Y]
      gcongr

/-- The absorbed tunable discrepancy controls an initial Taylor band even
when the Prawitz filter uses a larger smoothing bandwidth. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableLocal_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T : ℝ}
    (hT₀T : admissibleTunableThirdMomentCutoff c
      (∫ omega, |X 0 omega| ^ 3 ∂mu) N ≤ T) :
    (∫ t in Icc 0
        (admissibleTunableThirdMomentCutoff c
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (1 + Real.pi ^ 2 / 32) *
        (((1 / 6 + 1 / (8 * c)) *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let T₀ : ℝ := admissibleTunableThirdMomentCutoff c rho N
  let J : Set ℝ := Icc (-T₀) T₀
  let S : Set ℝ := tunableNormalizedThirdMomentWindow c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hT₀ : 0 < T₀ :=
    admissibleTunableThirdMomentCutoff_pos hc0 hrho0 N
  have hT : 0 < T := hT₀.trans_le (by simpa only [T₀, rho] using hT₀T)
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hfilter :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_local_le_const
      law target hLaw1 integrable_abs_standardNormalLaw hT₀.le hT
        (by simpa only [T₀, rho] using hT₀T)
  have hqS : IntegrableOn q S := by
    simpa only [q, S, law, target, rho, Y] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow
        X hX hIndep hIdent hMean hSecond hc hN
  have hJS : J ⊆ S := by
    simpa only [J, S, T₀] using
      Icc_admissibleTunableThirdMomentCutoff_subset_window hc0 hrho0 N
  have hqJ : IntegrableOn q J := hqS.mono_set hJS
  have hpositive : (∫ t in Icc 0 T₀, q t) ≤ ∫ t in J, q t := by
    have hsubset : Icc 0 T₀ ⊆ J := by
      intro t ht
      exact ⟨by linarith [hT₀.le, ht.1], ht.2⟩
    exact setIntegral_mono_set hqJ
      (ae_of_all (volume.restrict J) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hsubset.eventuallyLE
  have hJwindow : (∫ t in J, q t) ≤ ∫ t in S, q t := by
    exact setIntegral_mono_set hqS
      (ae_of_all (volume.restrict S) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hJS.eventuallyLE
  have habsorbed :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow_le_absorbed
      X hX hIndep hIdent hMean hSecond hc hN
  have hlocal : (∫ t in Icc 0 T₀, q t) ≤
      ((1 / 6 + 1 / (8 * c)) * rho /
        Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2 := by
    exact hpositive.trans (hJwindow.trans (by
      simpa only [q, S, law, target, rho, Y] using habsorbed))
  have hC : 0 ≤ 1 + Real.pi ^ 2 / 32 := by positivity
  change (∫ t in Icc 0 T₀,
      ‖scaledPrawitzSmoothingFilter T t‖ * q t) ≤ _
  exact hfilter.trans (mul_le_mul_of_nonneg_left hlocal hC)

/-- The normalized-iid Prawitz integral with independent Taylor and smoothing
cutoffs.  The certified absorbed discrepancy controls the Taylor window, while
the exact scaled-filter tail is retained on the additional smoothing band. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableTwoCutoff_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T : ℝ}
    (hT₀T : admissibleTunableThirdMomentCutoff c
      (∫ omega, |X 0 omega| ^ 3 ∂mu) N ≤ T) :
    (∫ t in Icc 0 T,
      ‖scaledPrawitzSmoothingFilter T t‖ *
        charFunDiscrepancyQuotient
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (1 + Real.pi ^ 2 / 32) *
        (((1 / 6 + 1 / (8 * c)) *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2) +
      ∫ t in Ioc
          (admissibleTunableThirdMomentCutoff c
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N) T,
        ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let T₀ : ℝ := admissibleTunableThirdMomentCutoff c rho N
  let J : Set ℝ := Icc (-T₀) T₀
  let S : Set ℝ := tunableNormalizedThirdMomentWindow c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hT₀ : 0 < T₀ :=
    admissibleTunableThirdMomentCutoff_pos hc0 hrho0 N
  have hT : 0 < T := hT₀.trans_le (by simpa only [T₀, rho] using hT₀T)
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsplit :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_le_twoCutoff
      law target hLaw1 integrable_abs_standardNormalLaw hT₀
        (by simpa only [T₀, rho] using hT₀T)
  have hfilter :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_local_le_const
      law target hLaw1 integrable_abs_standardNormalLaw hT₀.le hT
        (by simpa only [T₀, rho] using hT₀T)
  have hqS : IntegrableOn q S := by
    simpa only [q, S, law, target, rho, Y] using
      integrableOn_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow
        X hX hIndep hIdent hMean hSecond hc hN
  have hJS : J ⊆ S := by
    simpa only [J, S, T₀] using
      Icc_admissibleTunableThirdMomentCutoff_subset_window hc0 hrho0 N
  have hqJ : IntegrableOn q J := hqS.mono_set hJS
  have hpositive : (∫ t in Icc 0 T₀, q t) ≤ ∫ t in J, q t := by
    have hsubset : Icc 0 T₀ ⊆ J := by
      intro t ht
      exact ⟨by linarith [hT₀.le, ht.1], ht.2⟩
    exact setIntegral_mono_set hqJ
      (ae_of_all (volume.restrict J) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hsubset.eventuallyLE
  have hJwindow : (∫ t in J, q t) ≤ ∫ t in S, q t := by
    exact setIntegral_mono_set hqS
      (ae_of_all (volume.restrict S) fun t =>
        charFunDiscrepancyQuotient_nonneg law target t)
      hJS.eventuallyLE
  have habsorbed :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_tunableWindow_le_absorbed
      X hX hIndep hIdent hMean hSecond hc hN
  have hlocal : (∫ t in Icc 0 T₀, q t) ≤
      ((1 / 6 + 1 / (8 * c)) * rho /
        Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2 := by
    exact hpositive.trans (hJwindow.trans (by
      simpa only [q, S, law, target, rho, Y] using habsorbed))
  have hC : 0 ≤ 1 + Real.pi ^ 2 / 32 := by positivity
  change (∫ t in Icc 0 T,
      ‖scaledPrawitzSmoothingFilter T t‖ * q t) ≤ _
  calc
    _ ≤ (∫ t in Icc 0 T₀,
          ‖scaledPrawitzSmoothingFilter T t‖ * q t) +
        ∫ t in Ioc T₀ T,
          ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|) := hsplit
    _ ≤ (1 + Real.pi ^ 2 / 32) * (∫ t in Icc 0 T₀, q t) +
        ∫ t in Ioc T₀ T,
          ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|) :=
      add_le_add hfilter le_rfl
    _ ≤ _ := by
      simpa only [T₀, rho] using
        add_le_add (mul_le_mul_of_nonneg_left hlocal hC) le_rfl

/-- Nested tunable windows keep Berry-scale Gaussian control on the outer
annulus instead of replacing it by the scale-free universal discrepancy
bound.  The exact smoothing filter remains inside the annular integral. -/
theorem integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableNested_le
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {cOuter cInner : ℝ} (hcOuter : 1 / 3 < cOuter)
    (hcOuterInner : cOuter ≤ cInner) {N : ℕ} (hN : 1 ≤ N) :
    (∫ t in Icc 0
        (admissibleTunableThirdMomentCutoff cOuter
          (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
      ‖scaledPrawitzSmoothingFilter
          (admissibleTunableThirdMomentCutoff cOuter
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N) t‖ *
        charFunDiscrepancyQuotient
          (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
              X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw t) ≤
      (1 + Real.pi ^ 2 / 32) *
        (((1 / 6 + 1 / (8 * cInner)) *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment cInner 2) +
      ∫ t in Ioc
          (admissibleTunableThirdMomentCutoff cInner
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N)
          (admissibleTunableThirdMomentCutoff cOuter
            (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
        ‖scaledPrawitzSmoothingFilter
            (admissibleTunableThirdMomentCutoff cOuter
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) t‖ *
          (Real.exp (-(tunableCharFunDampingCoefficient cOuter * t ^ 2 / 2)) *
            ((1 / 6 + 1 / (8 * cOuter)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
                Real.sqrt (N + 1 : ℝ))) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let q : ℝ → ℝ := charFunDiscrepancyQuotient law target
  let TInner : ℝ := admissibleTunableThirdMomentCutoff cInner rho N
  let TOuter : ℝ := admissibleTunableThirdMomentCutoff cOuter rho N
  let AOuter : ℝ := 1 / 6 + 1 / (8 * cOuter)
  let m : ℝ → ℝ := fun t =>
    Real.exp (-(tunableCharFunDampingCoefficient cOuter * t ^ 2 / 2)) *
      (AOuter * rho * |t| ^ 2 / s)
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hcOuter0 : 0 < cOuter := lt_trans (by norm_num) hcOuter
  have hcInner : 1 / 3 < cInner := hcOuter.trans_le hcOuterInner
  have hcInner0 : 0 < cInner := lt_trans (by norm_num) hcInner
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hs : 0 < s := by dsimp only [s]; positivity
  have hTInner : 0 < TInner :=
    admissibleTunableThirdMomentCutoff_pos hcInner0 hrho0 N
  have hTOuter : 0 < TOuter :=
    admissibleTunableThirdMomentCutoff_pos hcOuter0 hrho0 N
  have hcut : TInner ≤ TOuter := by
    dsimp only [TInner, TOuter]
    exact admissibleTunableThirdMomentCutoff_antitone hrho0 hcOuter0
      hcOuterInner N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsplit :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_split
      law target hLaw1 integrable_abs_standardNormalLaw hTInner hcut
  have hinner :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableLocal_le
      X hX hIndep hIdent hMean hSecond hcInner hN
        (by simpa only [TInner, TOuter, rho] using hcut)
  have hm : Integrable m := by
    have hbase := integrable_abs_pow_mul_rexp_neg_tunable_sq hcOuter 2
    have hscaled := hbase.const_mul (AOuter * rho / s)
    convert hscaled using 1
    funext t
    dsimp only [m]
    ring
  have hm0 : ∀ t, 0 ≤ m t := fun t => by
    dsimp only [m, AOuter]
    positivity
  have hmajor : IntegrableOn
      (fun t => ‖scaledPrawitzSmoothingFilter TOuter t‖ * m t)
      (Ioc TInner TOuter) := by
    apply hm.integrableOn.bdd_mul (c := 7)
    · exact (measurable_scaledPrawitzSmoothingFilter TOuter).norm
        |>.aestronglyMeasurable
    · filter_upwards [ae_restrict_mem measurableSet_Ioc] with t ht
      simpa only [Real.norm_eq_abs, abs_of_nonneg (norm_nonneg _)] using
        norm_scaledPrawitzSmoothingFilter_le_seven hTOuter
          ⟨hTInner.le.trans ht.1.le, ht.2⟩
  have hactual : IntegrableOn
      (fun t => ‖scaledPrawitzSmoothingFilter TOuter t‖ * q t)
      (Ioc TInner TOuter) := by
    have hfull :=
      integrableOn_norm_scaledPrawitzSmoothingFilter_mul_discrepancy
        law target hLaw1 integrable_abs_standardNormalLaw hTOuter
    apply hfull.mono_set
    intro t ht
    exact ⟨hTInner.le.trans ht.1.le, ht.2⟩
  have htail : (∫ t in Ioc TInner TOuter,
      ‖scaledPrawitzSmoothingFilter TOuter t‖ * q t) ≤
      ∫ t in Ioc TInner TOuter,
        ‖scaledPrawitzSmoothingFilter TOuter t‖ * m t := by
    apply setIntegral_mono_on hactual hmajor measurableSet_Ioc
    intro t ht
    have htWindow :=
      Icc_admissibleTunableThirdMomentCutoff_subset_window
        hcOuter0 hrho0 N
        (show t ∈ Icc (-TOuter) TOuter from
          ⟨by linarith [hTInner.le, ht.1], ht.2⟩)
    have hpoint :=
      charFunDiscrepancyQuotient_normalizedCenteredIidSum_le_gaussian_tunable_absorbed
        X hX hIndep hIdent hMean hSecond hcOuter hN
          htWindow.1 htWindow.2
    exact mul_le_mul_of_nonneg_left
      (by simpa only [q, m, AOuter, law, target, rho, s, Y] using hpoint)
      (norm_nonneg _)
  change (∫ t in Icc 0 TOuter,
      ‖scaledPrawitzSmoothingFilter TOuter t‖ * q t) ≤ _
  calc
    _ = (∫ t in Icc 0 TInner,
          ‖scaledPrawitzSmoothingFilter TOuter t‖ * q t) +
        ∫ t in Ioc TInner TOuter,
          ‖scaledPrawitzSmoothingFilter TOuter t‖ * q t := hsplit
    _ ≤ (1 + Real.pi ^ 2 / 32) *
          (((1 / 6 + 1 / (8 * cInner)) * rho / s) *
            tunableGaussianDampedMoment cInner 2) +
        ∫ t in Ioc TInner TOuter,
          ‖scaledPrawitzSmoothingFilter TOuter t‖ * q t := by
      simpa only [TInner, TOuter, rho, s, q, law, target, Y] using
        add_le_add hinner le_rfl
    _ ≤ _ := by
      simpa only [TInner, TOuter, rho, s, q, m, AOuter, law, target, Y] using
        add_le_add le_rfl htail

/-- Bidirectional Prawitz smoothing with two nested tunable characteristic-
function windows and the exact outer-annulus filter integral. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunableNested
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {cOuter cInner : ℝ} (hcOuter : 1 / 3 < cOuter)
    (hcOuterInner : cOuter ≤ cInner) {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) *
          ((1 + Real.pi ^ 2 / 32) *
            (((1 / 6 + 1 / (8 * cInner)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) /
                Real.sqrt (N + 1 : ℝ)) *
                  tunableGaussianDampedMoment cInner 2) +
            ∫ t in Ioc
                (admissibleTunableThirdMomentCutoff cInner
                  (∫ omega, |X 0 omega| ^ 3 ∂mu) N)
                (admissibleTunableThirdMomentCutoff cOuter
                  (∫ omega, |X 0 omega| ^ 3 ∂mu) N),
              ‖scaledPrawitzSmoothingFilter
                  (admissibleTunableThirdMomentCutoff cOuter
                    (∫ omega, |X 0 omega| ^ 3 ∂mu) N) t‖ *
                (Real.exp
                    (-(tunableCharFunDampingCoefficient cOuter * t ^ 2 / 2)) *
                  ((1 / 6 + 1 / (8 * cOuter)) *
                    (∫ omega, |X 0 omega| ^ 3 ∂mu) * |t| ^ 2 /
                      Real.sqrt (N + 1 : ℝ)))) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          (2 * Real.pi /
            admissibleTunableThirdMomentCutoff cOuter
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let TOuter : ℝ := admissibleTunableThirdMomentCutoff cOuter rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hTOuter : 0 < TOuter := admissibleTunableThirdMomentCutoff_pos
    (lt_trans (by norm_num) hcOuter) (lt_of_lt_of_le zero_lt_one hrho) N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth :=
    kolmogorovDistance_standardNormal_le_prawitz law hLaw1 hTOuter
  have hweighted :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableNested_le
      X hX hIndep hIdent hMean hSecond hcOuter hcOuterInner hN
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        (1 / Real.pi) * (∫ t in Icc 0 TOuter,
          ‖scaledPrawitzSmoothingFilter TOuter t‖ *
            charFunDiscrepancyQuotient law target t) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / TOuter) := hsmooth
    _ ≤ _ := by
      dsimp only [TOuter, rho, law, target, Y]
      gcongr

/-- Bidirectional Prawitz smoothing with the Taylor cutoff decoupled from the
smoothing bandwidth.  This is the exact two-cutoff endpoint from which the
remaining dimensionless filter tail can be optimized. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable_twoCutoff
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T : ℝ}
    (hT₀T : admissibleTunableThirdMomentCutoff c
      (∫ omega, |X 0 omega| ^ 3 ∂mu) N ≤ T) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) *
          ((1 + Real.pi ^ 2 / 32) *
            (((1 / 6 + 1 / (8 * c)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) /
                Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2) +
            ∫ t in Ioc
                (admissibleTunableThirdMomentCutoff c
                  (∫ omega, |X 0 omega| ^ 3 ∂mu) N) T,
              ‖scaledPrawitzSmoothingFilter T t‖ * (2 / |t|)) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T₀ : ℝ := admissibleTunableThirdMomentCutoff c rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT₀ : 0 < T₀ := admissibleTunableThirdMomentCutoff_pos
    (lt_trans (by norm_num) hc) (lt_of_lt_of_le zero_lt_one hrho) N
  have hT : 0 < T := hT₀.trans_le (by simpa only [T₀, rho] using hT₀T)
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth := kolmogorovDistance_standardNormal_le_prawitz law hLaw1 hT
  have hlocal :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_discrepancy_normalizedCenteredIidSum_tunableTwoCutoff_le
      X hX hIndep hIdent hMean hSecond hc hN hT₀T
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        (1 / Real.pi) * (∫ t in Icc 0 T,
          ‖scaledPrawitzSmoothingFilter T t‖ *
            charFunDiscrepancyQuotient law target t) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := hsmooth
    _ ≤ _ := by
      dsimp only [T₀, rho, law, target, Y]
      gcongr

/-- The two-cutoff normalized-iid Prawitz estimate with its outer filter band
replaced by a closed logarithmic function of the bandwidth ratio. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable_twoCutoff_log
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T : ℝ}
    (hT₀T : admissibleTunableThirdMomentCutoff c
      (∫ omega, |X 0 omega| ^ 3 ∂mu) N ≤ T) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) *
          ((1 + Real.pi ^ 2 / 32) *
            (((1 / 6 + 1 / (8 * c)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) /
                Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2) +
            2 * (1 + Real.pi ^ 2 / 32) *
              Real.log (T /
                admissibleTunableThirdMomentCutoff c
                  (∫ omega, |X 0 omega| ^ 3 ∂mu) N)) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let T₀ : ℝ := admissibleTunableThirdMomentCutoff c rho N
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT₀ : 0 < T₀ := admissibleTunableThirdMomentCutoff_pos
    (lt_trans (by norm_num) hc) (lt_of_lt_of_le zero_lt_one hrho) N
  have htail :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_le_log
      hT₀ (by simpa only [T₀, rho] using hT₀T)
  have hexact :=
    kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable_twoCutoff
      X hX hIndep hIdent hMean hSecond hc hN hT₀T
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤ _ := hexact
    _ ≤ _ := by
      dsimp only [T₀, rho] at htail
      gcongr

/-- The endpoint-sensitive two-cutoff Prawitz estimate.  Its explicit outer
cost is quadratic as the smoothing bandwidth approaches the Taylor cutoff. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable_twoCutoff_endpoint
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N)
    {T : ℝ}
    (hT₀T : admissibleTunableThirdMomentCutoff c
      (∫ omega, |X 0 omega| ^ 3 ∂mu) N ≤ T)
    (hTT₀ : T ≤ 2 * admissibleTunableThirdMomentCutoff c
      (∫ omega, |X 0 omega| ^ 3 ∂mu) N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      (1 / Real.pi) *
          ((1 + Real.pi ^ 2 / 32) *
            (((1 / 6 + 1 / (8 * c)) *
              (∫ omega, |X 0 omega| ^ 3 ∂mu) /
                Real.sqrt (N + 1 : ℝ)) * tunableGaussianDampedMoment c 2) +
            2 * (Real.pi ^ 2 / 2 + Real.pi) *
              (Real.log (T /
                  admissibleTunableThirdMomentCutoff c
                    (∫ omega, |X 0 omega| ^ 3 ∂mu) N) -
                (1 - admissibleTunableThirdMomentCutoff c
                    (∫ omega, |X 0 omega| ^ 3 ∂mu) N / T))) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let T₀ : ℝ := admissibleTunableThirdMomentCutoff c rho N
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT₀ : 0 < T₀ := admissibleTunableThirdMomentCutoff_pos
    (lt_trans (by norm_num) hc) (lt_of_lt_of_le zero_lt_one hrho) N
  have htail :=
    integral_norm_scaledPrawitzSmoothingFilter_mul_two_div_abs_tail_le_endpoint
      hT₀ (by simpa only [T₀, rho] using hT₀T)
        (by simpa only [T₀, rho] using hTT₀)
  have hexact :=
    kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable_twoCutoff
      X hX hIndep hIdent hMean hSecond hc hN hT₀T
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤ _ := hexact
    _ ≤ _ := by
      dsimp only [T₀, rho] at htail
      gcongr

/-- If the Taylor cutoff is `s / (2 rho)` and the smoothing bandwidth stays
within the endpoint-sensitive factor-two range, then the spatial Prawitz term
alone is strictly larger than the target Berry--Esseen scale `rho / s`. -/
theorem prawitzEndpointSpatialTerm_gt_berryScale
    {rho s T₀ T : ℝ} (hrho : 0 < rho) (hs : 0 < s)
    (hT₀Eq : T₀ = s / (2 * rho)) (hT₀T : T₀ ≤ T)
    (hTT₀ : T ≤ 2 * T₀) :
    rho / s < (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let R : ℝ := Real.sqrt (2 * Real.pi)
  have hR : 1 < R := by
    dsimp only [R]
    rw [Real.lt_sqrt (by norm_num : (0 : ℝ) ≤ 1)]
    nlinarith [Real.two_le_pi]
  have hRSq : R ^ 2 = 2 * Real.pi := by
    dsimp only [R]
    rw [Real.sq_sqrt]
    positivity
  have hT₀ : 0 < T₀ := by rw [hT₀Eq]; positivity
  have hT : 0 < T := hT₀.trans_le hT₀T
  have hUpper : T * rho ≤ s := by
    have hEq : 2 * T₀ = s / rho := by
      rw [hT₀Eq]
      field_simp
    rw [hEq] at hTT₀
    exact (le_div_iff₀ hrho).mp hTT₀
  have hInv : rho / s ≤ 1 / T := by
    rw [div_le_div_iff₀ hs hT]
    simpa only [mul_one, mul_comm] using hUpper
  have hStrict : 1 / T < R / T :=
    (div_lt_div_iff_of_pos_right hT).2 hR
  have hSpatial : R⁻¹ * (2 * Real.pi / T) = R / T := by
    field_simp [hT.ne', (lt_trans zero_lt_one hR).ne']
    nlinarith [hRSq]
  rw [show (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) =
      R⁻¹ * (2 * Real.pi / T) by rfl, hSpatial]
  exact hInv.trans_lt hStrict

/-- Concrete `c = 2` consequence: every bandwidth covered by the sharpened
endpoint tail estimate still has a spatial term exceeding coefficient one. -/
theorem prawitzTunableTwoCutoffEndpoint_spatial_gt_berryScale
    {rho : ℝ} (hrho : 1 ≤ rho) {N : ℕ} {T : ℝ}
    (hT₀T : admissibleTunableThirdMomentCutoff 2 rho N ≤ T)
    (hTT₀ : T ≤ 2 * admissibleTunableThirdMomentCutoff 2 rho N) :
    rho / Real.sqrt (N + 1 : ℝ) <
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let T₀ : ℝ := admissibleTunableThirdMomentCutoff 2 rho N
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hs : 0 < s := by dsimp only [s]; positivity
  have hmax : max 1 (2 * rho) = 2 * rho :=
    max_eq_right (by nlinarith)
  have hT₀Eq : T₀ = s / (2 * rho) := by
    dsimp only [T₀, s]
    rw [admissibleTunableThirdMomentCutoff_eq_sqrt_div_max
      (by norm_num) hrho0 N, hmax]
  exact prawitzEndpointSpatialTerm_gt_berryScale hrho0 hs hT₀Eq
    (by simpa only [T₀] using hT₀T)
    (by simpa only [T₀] using hTT₀)

/-- Every admissible tunable Taylor cutoff is capped by `sqrt (N+1)`.  Hence
the Prawitz spatial remainder alone exceeds the coefficient-one scale when
the standardized third moment takes its minimal value `rho = 1`, independently
of how the tunable frequency window is split. -/
theorem prawitzAdmissibleTunableCutoff_spatial_gt_minimalBerryScale
    {c rho : ℝ} (hc : 0 < c) (hrho : 0 < rho) (N : ℕ) :
    1 / Real.sqrt (N + 1 : ℝ) <
      (Real.sqrt (2 * Real.pi))⁻¹ *
        (2 * Real.pi / admissibleTunableThirdMomentCutoff c rho N) := by
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  let R : ℝ := Real.sqrt (2 * Real.pi)
  have hs : 0 < s := by dsimp only [s]; positivity
  have hT : 0 < T := admissibleTunableThirdMomentCutoff_pos hc hrho N
  have hTs : T ≤ s := by
    dsimp only [T, s, admissibleTunableThirdMomentCutoff]
    exact min_le_left _ _
  have hR : 1 < R := by
    dsimp only [R]
    rw [Real.lt_sqrt (by norm_num : (0 : ℝ) ≤ 1)]
    nlinarith [Real.two_le_pi]
  have hRSq : R ^ 2 = 2 * Real.pi := by
    dsimp only [R]
    rw [Real.sq_sqrt]
    positivity
  have hInv : 1 / s ≤ 1 / T := one_div_le_one_div_of_le hT hTs
  have hStrict : 1 / T < R / T :=
    (div_lt_div_iff_of_pos_right hT).2 hR
  have hSpatial : R⁻¹ * (2 * Real.pi / T) = R / T := by
    field_simp [hT.ne', (lt_trans zero_lt_one hR).ne']
    nlinarith [hRSq]
  change 1 / s < R⁻¹ * (2 * Real.pi / T)
  rw [hSpatial]
  exact hInv.trans_lt hStrict

/-- The Gaussian half-line term produced by the integrated signed majorant is
algebraically the same Prawitz spatial remainder that appears in the original
one-sided smoothing inequality. -/
theorem sqrt_two_pi_div_eq_prawitzSpatialRemainder
    {T : ℝ} (hT : 0 < T) :
    Real.sqrt (2 * Real.pi) / T =
      (Real.sqrt (2 * Real.pi))⁻¹ * (2 * Real.pi / T) := by
  let R : ℝ := Real.sqrt (2 * Real.pi)
  have hR : 0 < R := by dsimp only [R]; positivity
  have hRSq : R ^ 2 = 2 * Real.pi := by
    dsimp only [R]
    rw [Real.sq_sqrt]
    positivity
  change R / T = R⁻¹ * (2 * Real.pi / T)
  field_simp [hT.ne', hR.ne']
  nlinarith

/-- Consequently, the positive Gaussian remainder left by the componentwise
signed-integrand estimate already exceeds the minimal coefficient-one
Berry--Esseen scale at every admissible tunable cutoff. -/
theorem signedPrawitzTunableGaussianRemainder_gt_minimalBerryScale
    {c rho : ℝ} (hc : 0 < c) (hrho : 0 < rho) (N : ℕ) :
    1 / Real.sqrt (N + 1 : ℝ) <
      Real.sqrt (2 * Real.pi) /
        admissibleTunableThirdMomentCutoff c rho N := by
  rw [sqrt_two_pi_div_eq_prawitzSpatialRemainder
    (admissibleTunableThirdMomentCutoff_pos hc hrho N)]
  exact prawitzAdmissibleTunableCutoff_spatial_gt_minimalBerryScale
    hc hrho N

/-- Universal coefficient exposed by the tunable Prawitz route after the
exact admissible cutoff is converted to the Berry--Esseen scale. -/
noncomputable def prawitzTunableBerryEsseenConstant (c : ℝ) : ℝ :=
  (1 / Real.pi) * (1 + Real.pi ^ 2 / 32) *
      ((1 / 6) * tunableGaussianDampedMoment c 2 +
        (1 / 8) * tunableGaussianDampedMoment c 3) +
    Real.sqrt (2 * Real.pi) * max 1 c

theorem prawitzTunableBerryEsseenConstant_nonneg
    {c : ℝ} (hc : 0 ≤ c) :
    0 ≤ prawitzTunableBerryEsseenConstant c := by
  have hM2 := tunableGaussianDampedMoment_nonneg c 2
  have hM3 := tunableGaussianDampedMoment_nonneg c 3
  unfold prawitzTunableBerryEsseenConstant
  positivity

/-- The exact spatial term alone rules out coefficient one for every
admissible value of the tunable Prawitz parameter. -/
theorem one_lt_prawitzTunableBerryEsseenConstant
    {c : ℝ} (hc : 1 / 3 < c) :
    1 < prawitzTunableBerryEsseenConstant c := by
  have hM2 := tunableGaussianDampedMoment_nonneg c 2
  have hM3 := tunableGaussianDampedMoment_nonneg c 3
  have hR : 1 < Real.sqrt (2 * Real.pi) := by
    rw [Real.lt_sqrt (by norm_num : (0 : ℝ) ≤ 1)]
    nlinarith [Real.two_le_pi]
  have hmax : 1 ≤ max 1 c := le_max_left _ _
  have hfreq : 0 ≤
      (1 / Real.pi) * (1 + Real.pi ^ 2 / 32) *
        ((1 / 6) * tunableGaussianDampedMoment c 2 +
          (1 / 8) * tunableGaussianDampedMoment c 3) := by positivity
  unfold prawitzTunableBerryEsseenConstant
  nlinarith [mul_le_mul_of_nonneg_left hmax (by positivity :
    0 ≤ Real.sqrt (2 * Real.pi))]

/-- Consolidated normalized-iid consequence of the tunable Prawitz route. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitzTunableBerryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {c : ℝ} (hc : 1 / 3 < c) {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      prawitzTunableBerryEsseenConstant c *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let M2 : ℝ := tunableGaussianDampedMoment c 2
  let M3 : ℝ := tunableGaussianDampedMoment c 3
  let C : ℝ := 1 + Real.pi ^ 2 / 32
  let R : ℝ := Real.sqrt (2 * Real.pi)
  let T : ℝ := admissibleTunableThirdMomentCutoff c rho N
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hc0 : 0 < c := lt_trans (by norm_num) hc
  have hrho0 : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hs : 0 < s := by dsimp only [s]; positivity
  have hn : 0 < (N + 1 : ℝ) := by positivity
  have hsSq : s ^ 2 = (N + 1 : ℝ) := by
    dsimp only [s]
    rw [Real.sq_sqrt hn.le]
  have hM2 : 0 ≤ M2 := tunableGaussianDampedMoment_nonneg c 2
  have hM3 : 0 ≤ M3 := tunableGaussianDampedMoment_nonneg c 3
  have hC : 0 ≤ C := by dsimp only [C]; positivity
  have hR : 0 < R := by dsimp only [R]; positivity
  have hRSq : R ^ 2 = 2 * Real.pi := by
    dsimp only [R]
    rw [Real.sq_sqrt]
    positivity
  have hInv : 1 / (N + 1 : ℝ) ≤ rho / s := by
    rw [div_le_div_iff₀ hn hs]
    have hs_le_n : s ≤ (N + 1 : ℝ) := by
      rw [← hsSq]
      nlinarith
    have hn_le : (N + 1 : ℝ) ≤ rho * (N + 1 : ℝ) := by
      nlinarith
    simpa only [one_mul] using hs_le_n.trans hn_le
  have hFirst :
      (rho / (6 * s)) * M2 = ((1 / 6) * M2) * (rho / s) := by ring
  have hSecondTerm :
      (1 / (8 * (N + 1 : ℝ))) * M3 ≤
        ((1 / 8) * M3) * (rho / s) := by
    calc
      (1 / (8 * (N + 1 : ℝ))) * M3 =
          ((1 / 8) * M3) * (1 / (N + 1 : ℝ)) := by
        field_simp [hn.ne']
      _ ≤ ((1 / 8) * M3) * (rho / s) :=
        mul_le_mul_of_nonneg_left hInv (by positivity)
  have hFrequency :
      (1 / Real.pi) *
          (C * ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3)) ≤
        ((1 / Real.pi) * C *
          ((1 / 6) * M2 + (1 / 8) * M3)) * (rho / s) := by
    rw [hFirst]
    have hadd := add_le_add_left hSecondTerm (((1 / 6) * M2) * (rho / s))
    calc
      _ ≤ (1 / Real.pi) *
          (C * ((((1 / 6) * M2) * (rho / s)) +
            ((1 / 8) * M3) * (rho / s))) := by
        gcongr
      _ = _ := by ring
  have hmax : max 1 (c * rho) ≤ max 1 c * rho := by
    apply max_le
    · calc
        (1 : ℝ) = 1 * 1 := by ring
        _ ≤ max 1 c * rho := mul_le_mul
          (le_max_left _ _) hrho (by norm_num) (by positivity)
    · exact mul_le_mul_of_nonneg_right (le_max_right _ _) hrho0.le
  have hSpatialExact :
      R⁻¹ * (2 * Real.pi / T) = R * max 1 (c * rho) / s := by
    rw [show T = s / max 1 (c * rho) by
      dsimp only [T, s]
      exact admissibleTunableThirdMomentCutoff_eq_sqrt_div_max hc0 hrho0 N]
    have hm : 0 < max 1 (c * rho) := lt_of_lt_of_le zero_lt_one (le_max_left _ _)
    field_simp [hs.ne', hR.ne', hm.ne']
    nlinarith [hRSq]
  have hSpatial :
      R⁻¹ * (2 * Real.pi / T) ≤
        (R * max 1 c) * (rho / s) := by
    rw [hSpatialExact]
    calc
      R * max 1 (c * rho) / s ≤ R * (max 1 c * rho) / s := by
        gcongr
      _ = (R * max 1 c) * (rho / s) := by ring
  have h := kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_tunable
    X hX hIndep hIdent hMean hSecond hc hN
  calc
    _ ≤ (1 / Real.pi) *
          (C * ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3)) +
        R⁻¹ * (2 * Real.pi / T) := by
      simpa only [rho, s, M2, M3, C, R, T] using h
    _ ≤ ((1 / Real.pi) * C *
          ((1 / 6) * M2 + (1 / 8) * M3)) * (rho / s) +
        (R * max 1 c) * (rho / s) :=
      add_le_add hFrequency hSpatial
    _ = prawitzTunableBerryEsseenConstant c * rho / s := by
      dsimp only [prawitzTunableBerryEsseenConstant, C, M2, M3, R]
      ring
    _ = _ := rfl

/-- Universal constant exposed by the maximal-cutoff bidirectional Prawitz
route with the certified uniform filter factor. -/
noncomputable def prawitzMaximalSharpBerryEsseenConstant : ℝ :=
  (1 / Real.pi) * (1 + Real.pi ^ 2 / 32) *
      ((1 / 6) * sharpGaussianDampedMoment 2 +
        (1 / 8) * sharpGaussianDampedMoment 3) +
    2 * Real.sqrt (2 * Real.pi)

theorem prawitzMaximalSharpBerryEsseenConstant_nonneg :
    0 ≤ prawitzMaximalSharpBerryEsseenConstant := by
  have hM2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 := sharpGaussianDampedMoment_nonneg 3
  unfold prawitzMaximalSharpBerryEsseenConstant
  positivity

/-- The exact Fejer term at the maximal sharp Taylor cutoff already forces
this Prawitz-route constant above the source coefficient one. -/
theorem one_lt_prawitzMaximalSharpBerryEsseenConstant :
    1 < prawitzMaximalSharpBerryEsseenConstant := by
  have hM2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 := sharpGaussianDampedMoment_nonneg 3
  have hR : 1 < Real.sqrt (2 * Real.pi) := by
    rw [Real.lt_sqrt (by norm_num : (0 : ℝ) ≤ 1)]
    nlinarith [Real.two_le_pi]
  have hfreq : 0 ≤
      (1 / Real.pi) * (1 + Real.pi ^ 2 / 32) *
        ((1 / 6) * sharpGaussianDampedMoment 2 +
          (1 / 8) * sharpGaussianDampedMoment 3) := by
    positivity
  unfold prawitzMaximalSharpBerryEsseenConstant
  nlinarith

/-- Consolidated normalized-iid consequence of the maximal-cutoff Prawitz
route. -/
theorem kolmogorovDistance_normalizedCenteredIidSum_le_prawitzMaximalSharpBerryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      prawitzMaximalSharpBerryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let M2 : ℝ := sharpGaussianDampedMoment 2
  let M3 : ℝ := sharpGaussianDampedMoment 3
  let C : ℝ := 1 + Real.pi ^ 2 / 32
  let R : ℝ := Real.sqrt (2 * Real.pi)
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hrhoPos : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hs : 0 < s := by dsimp only [s]; positivity
  have hn : 0 < (N + 1 : ℝ) := by positivity
  have hsSq : s ^ 2 = (N + 1 : ℝ) := by
    dsimp only [s]
    rw [Real.sq_sqrt hn.le]
  have hM2 : 0 ≤ M2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 : 0 ≤ M3 := sharpGaussianDampedMoment_nonneg 3
  have hC : 0 ≤ C := by dsimp only [C]; positivity
  have hR : 0 < R := by dsimp only [R]; positivity
  have hRSq : R ^ 2 = 2 * Real.pi := by
    dsimp only [R]
    rw [Real.sq_sqrt]
    positivity
  have hInv : 1 / (N + 1 : ℝ) ≤ rho / s := by
    rw [div_le_div_iff₀ hn hs]
    have hs_le_n : s ≤ (N + 1 : ℝ) := by
      rw [← hsSq]
      nlinarith
    have hn_le : (N + 1 : ℝ) ≤ rho * (N + 1 : ℝ) := by
      nlinarith
    simpa only [one_mul] using hs_le_n.trans hn_le
  have hFirst :
      (rho / (6 * s)) * M2 = ((1 / 6) * M2) * (rho / s) := by ring
  have hSecondTerm :
      (1 / (8 * (N + 1 : ℝ))) * M3 ≤
        ((1 / 8) * M3) * (rho / s) := by
    calc
      (1 / (8 * (N + 1 : ℝ))) * M3 =
          ((1 / 8) * M3) * (1 / (N + 1 : ℝ)) := by
        field_simp [hn.ne']
      _ ≤ ((1 / 8) * M3) * (rho / s) :=
        mul_le_mul_of_nonneg_left hInv (by positivity)
  have hFrequency :
      (1 / Real.pi) *
          (C * ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3)) ≤
        ((1 / Real.pi) * C *
          ((1 / 6) * M2 + (1 / 8) * M3)) * (rho / s) := by
    rw [hFirst]
    have hadd := add_le_add_left hSecondTerm (((1 / 6) * M2) * (rho / s))
    calc
      _ ≤ (1 / Real.pi) *
          (C * ((((1 / 6) * M2) * (rho / s)) +
            ((1 / 8) * M3) * (rho / s))) := by
        gcongr
      _ = _ := by ring
  have hSpatial :
      R⁻¹ * (2 * Real.pi /
          maximalSharpBerryEsseenFrequencyCutoff rho N) =
        (2 * R) * (rho / s) := by
    rw [show maximalSharpBerryEsseenFrequencyCutoff rho N = s / (2 * rho) by rfl]
    field_simp [hs.ne', hrhoPos.ne', hR.ne']
    nlinarith [hRSq]
  have h := kolmogorovDistance_normalizedCenteredIidSum_le_prawitz_maximalSharp
    X hX hIndep hIdent hMean hSecond hN
  calc
    _ ≤ (1 / Real.pi) *
          (C * ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3)) +
        R⁻¹ * (2 * Real.pi /
          maximalSharpBerryEsseenFrequencyCutoff rho N) := by
      simpa only [rho, s, M2, M3, C, R] using h
    _ ≤ ((1 / Real.pi) * C *
          ((1 / 6) * M2 + (1 / 8) * M3)) * (rho / s) +
        (2 * R) * (rho / s) := by
      rw [hSpatial]
      exact add_le_add hFrequency le_rfl
    _ = prawitzMaximalSharpBerryEsseenConstant * rho / s := by
      dsimp only [prawitzMaximalSharpBerryEsseenConstant, C, M2, M3, R]
      ring
    _ = _ := rfl

theorem kolmogorovDistance_normalizedCenteredIidSum_le_explicit
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          ((4 * (∫ omega, |X 0 omega| ^ 3 ∂mu) /
              Real.sqrt (N + 1 : ℝ)) * gaussianDampedMoment 2 +
            (1 / (4 * (N + 1 : ℝ))) * gaussianDampedMoment 3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / berryEsseenFrequencyCutoff
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) *
            sincFourthShiftedMomentBound)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := berryEsseenFrequencyCutoff rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hT : 0 < T := berryEsseenFrequencyCutoff_pos hrho N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth := kolmogorovDistance_standardNormal_le_scaledSincFourthKernel
    law hLaw1 hT
  have hlocal :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_cutoff_le
      X hX hIndep hIdent hMean hSecond hN
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        ((2 * Real.pi)⁻¹ *
            (∫ t in Icc (-T) T,
              charFunDiscrepancyQuotient law target t) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((4 / T) * sincFourthShiftedMomentBound)) /
          (1 - 2 * (1 / 4 : ℝ)) := hsmooth
    _ ≤ _ := by
      dsimp only [T, rho, law, target, Y]
      gcongr

theorem kolmogorovDistance_normalizedCenteredIidSum_le_explicit_sharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          (((∫ omega, |X 0 omega| ^ 3 ∂mu) /
              (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
            (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / sharpBerryEsseenFrequencyCutoff
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) *
            sincFourthShiftedMomentBound)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := sharpBerryEsseenFrequencyCutoff rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 0 ≤ rho := integral_nonneg fun _ => by positivity
  have hT : 0 < T := sharpBerryEsseenFrequencyCutoff_pos hrho N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth := kolmogorovDistance_standardNormal_le_scaledSincFourthKernel
    law hLaw1 hT
  have hlocal :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_sharpCutoff_le
      X hX hIndep hIdent hMean hSecond hN
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        ((2 * Real.pi)⁻¹ *
            (∫ t in Icc (-T) T,
              charFunDiscrepancyQuotient law target t) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((4 / T) * sincFourthShiftedMomentBound)) /
          (1 - 2 * (1 / 4 : ℝ)) := hsmooth
    _ ≤ _ := by
      dsimp only [T, rho, law, target, Y]
      gcongr

theorem kolmogorovDistance_normalizedCenteredIidSum_le_explicit_maximalSharp
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          (((∫ omega, |X 0 omega| ^ 3 ∂mu) /
              (6 * Real.sqrt (N + 1 : ℝ))) * sharpGaussianDampedMoment 2 +
            (1 / (8 * (N + 1 : ℝ))) * sharpGaussianDampedMoment 3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / maximalSharpBerryEsseenFrequencyCutoff
              (∫ omega, |X 0 omega| ^ 3 ∂mu) N) *
            sincFourthShiftedMomentBound)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let Y : Omega → ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N
  have hY : MemLp Y 2 mu :=
    NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
      X (fun i => (hX i).mono_exponent (by norm_num)) N
  let law : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.probabilityLaw Y hY.aemeasurable
  let target : Measure ℝ :=
    NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw
  let T : ℝ := maximalSharpBerryEsseenFrequencyCutoff rho N
  letI : IsProbabilityMeasure law := inferInstance
  letI : IsProbabilityMeasure target := inferInstance
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hT : 0 < T := maximalSharpBerryEsseenFrequencyCutoff_pos hrho N
  have hLaw1 : Integrable (fun x : ℝ => |x|) law := by
    change Integrable (fun x : ℝ => |x|) (Measure.map Y mu)
    rw [integrable_map_measure (by fun_prop) hY.aemeasurable]
    simpa only [Function.comp_apply, Real.norm_eq_abs] using
      (hY.integrable (by norm_num)).norm
  have hsmooth := kolmogorovDistance_standardNormal_le_scaledSincFourthKernel
    law hLaw1 hT
  have hlocal :=
    integral_charFunDiscrepancyQuotient_normalizedCenteredIidSum_maximalSharpCutoff_le
      X hX hIndep hIdent hMean hSecond hN
  change kolmogorovDistance law target ≤ _
  calc
    kolmogorovDistance law target ≤
        ((2 * Real.pi)⁻¹ *
            (∫ t in Icc (-T) T,
              charFunDiscrepancyQuotient law target t) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((4 / T) * sincFourthShiftedMomentBound)) /
          (1 - 2 * (1 / 4 : ℝ)) := hsmooth
    _ ≤ _ := by
      dsimp only [T, rho, law, target, Y]
      gcongr

/-- A universal constant certified by the concrete sinc-fourth smoothing
kernel and the Gaussian-damped Fourier majorant. -/
noncomputable def berryEsseenConstant : ℝ :=
  2 * ((2 * Real.pi)⁻¹ *
      (4 * gaussianDampedMoment 2 + (1 / 4) * gaussianDampedMoment 3) +
    (Real.sqrt (2 * Real.pi))⁻¹ *
      (68 * sincFourthShiftedMomentBound))

theorem berryEsseenConstant_nonneg : 0 ≤ berryEsseenConstant := by
  have hM2 := gaussianDampedMoment_nonneg 2
  have hM3 := gaussianDampedMoment_nonneg 3
  have hK : 0 ≤ sincFourthShiftedMomentBound := by
    unfold sincFourthShiftedMomentBound
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  unfold berryEsseenConstant
  exact mul_nonneg (by norm_num) (add_nonneg
    (mul_nonneg (inv_nonneg.mpr (by positivity))
      (add_nonneg (mul_nonneg (by norm_num) hM2)
        (mul_nonneg (by norm_num) hM3)))
    (mul_nonneg (inv_nonneg.mpr (by positivity))
      (mul_nonneg (by norm_num) hK)))

/-- Improved universal constant obtained from the sharp Taylor route while
retaining the existing sinc-fourth smoothing kernel. -/
noncomputable def sharpBerryEsseenConstant : ℝ :=
  2 * ((2 * Real.pi)⁻¹ *
      ((1 / 6) * sharpGaussianDampedMoment 2 +
        (1 / 8) * sharpGaussianDampedMoment 3) +
    (Real.sqrt (2 * Real.pi))⁻¹ *
      (12 * sincFourthShiftedMomentBound))

theorem sharpBerryEsseenConstant_nonneg : 0 ≤ sharpBerryEsseenConstant := by
  have hM2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 := sharpGaussianDampedMoment_nonneg 3
  have hK : 0 ≤ sincFourthShiftedMomentBound := by
    unfold sincFourthShiftedMomentBound
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  unfold sharpBerryEsseenConstant
  exact mul_nonneg (by norm_num) (add_nonneg
    (mul_nonneg (inv_nonneg.mpr (by positivity))
      (add_nonneg (mul_nonneg (by norm_num) hM2)
        (mul_nonneg (by norm_num) hM3)))
    (mul_nonneg (inv_nonneg.mpr (by positivity))
      (mul_nonneg (by norm_num) hK)))

/-- Frequency-only part of the maximal-cutoff sharp sinc-fourth route. -/
noncomputable def maximalSharpBerryEsseenFrequencyConstant : ℝ :=
  2 * (2 * Real.pi)⁻¹ *
    ((1 / 6) * sharpGaussianDampedMoment 2 +
      (1 / 8) * sharpGaussianDampedMoment 3)

/-- The sharp sinc-fourth route already exceeds coefficient one in its
frequency term, before adding its nonnegative spatial smoothing remainder. -/
theorem one_lt_maximalSharpBerryEsseenFrequencyConstant :
    1 < maximalSharpBerryEsseenFrequencyConstant := by
  have hsum : Real.pi <
      (1 / 6) * sharpGaussianDampedMoment 2 +
        (1 / 8) * sharpGaussianDampedMoment 3 := by
    rw [sharpGaussianDampedMoment_three]
    nlinarith [eight_div_three_le_sharpGaussianDampedMoment_two,
      Real.pi_lt_d2]
  have heq : maximalSharpBerryEsseenFrequencyConstant =
      ((1 / 6) * sharpGaussianDampedMoment 2 +
        (1 / 8) * sharpGaussianDampedMoment 3) / Real.pi := by
    unfold maximalSharpBerryEsseenFrequencyConstant
    field_simp [Real.pi_ne_zero]
  rw [heq]
  exact (one_lt_div Real.pi_pos).2 hsum

/-- Universal constant obtained from the maximal cutoff allowed by the sharp
Taylor window and the existing sinc-fourth smoothing kernel. -/
noncomputable def maximalSharpBerryEsseenConstant : ℝ :=
  2 * ((2 * Real.pi)⁻¹ *
      ((1 / 6) * sharpGaussianDampedMoment 2 +
        (1 / 8) * sharpGaussianDampedMoment 3) +
    (Real.sqrt (2 * Real.pi))⁻¹ *
      (8 * sincFourthShiftedMomentBound))

theorem maximalSharpBerryEsseenConstant_nonneg :
    0 ≤ maximalSharpBerryEsseenConstant := by
  have hM2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 := sharpGaussianDampedMoment_nonneg 3
  have hK : 0 ≤ sincFourthShiftedMomentBound := by
    unfold sincFourthShiftedMomentBound
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  unfold maximalSharpBerryEsseenConstant
  exact mul_nonneg (by norm_num) (add_nonneg
    (mul_nonneg (inv_nonneg.mpr (by positivity))
      (add_nonneg (mul_nonneg (by norm_num) hM2)
        (mul_nonneg (by norm_num) hM3)))
    (mul_nonneg (inv_nonneg.mpr (by positivity))
      (mul_nonneg (by norm_num) hK)))

/-- Consequently the complete sharp sinc-fourth constant also exceeds one. -/
theorem one_lt_maximalSharpBerryEsseenConstant :
    1 < maximalSharpBerryEsseenConstant := by
  have hK : 0 ≤ sincFourthShiftedMomentBound := by
    unfold sincFourthShiftedMomentBound
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  calc
    1 < maximalSharpBerryEsseenFrequencyConstant :=
      one_lt_maximalSharpBerryEsseenFrequencyConstant
    _ ≤ maximalSharpBerryEsseenConstant := by
      unfold maximalSharpBerryEsseenFrequencyConstant
        maximalSharpBerryEsseenConstant
      have hspatial : 0 ≤ (Real.sqrt (2 * Real.pi))⁻¹ *
          (8 * sincFourthShiftedMomentBound) := by positivity
      nlinarith

theorem kolmogorovDistance_normalizedCenteredIidSum_le_sharpBerryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      sharpBerryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let M2 : ℝ := sharpGaussianDampedMoment 2
  let M3 : ℝ := sharpGaussianDampedMoment 3
  let K : ℝ := sincFourthShiftedMomentBound
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hs : 0 < s := by
    dsimp only [s]
    positivity
  have hn : 0 < (N + 1 : ℝ) := by positivity
  have hsSq : s ^ 2 = (N + 1 : ℝ) := by
    dsimp only [s]
    rw [Real.sq_sqrt hn.le]
  have hsOne : 1 ≤ s := by
    rw [← Real.sqrt_one]
    dsimp only [s]
    exact Real.sqrt_le_sqrt (by exact_mod_cast Nat.le_add_left 1 N)
  have hM2 : 0 ≤ M2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 : 0 ≤ M3 := sharpGaussianDampedMoment_nonneg 3
  have hK : 0 ≤ K := by
    dsimp only [K, sincFourthShiftedMomentBound]
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  have hInv : 1 / (N + 1 : ℝ) ≤ rho / s := by
    rw [div_le_div_iff₀ hn hs]
    have hs_le_n : s ≤ (N + 1 : ℝ) := by
      rw [← hsSq]
      nlinarith
    have hn_le : (N + 1 : ℝ) ≤ rho * (N + 1 : ℝ) := by
      nlinarith
    simpa only [one_mul] using hs_le_n.trans hn_le
  have hFirst :
      (rho / (6 * s)) * M2 = ((1 / 6) * M2) * (rho / s) := by ring
  have hSecondTerm :
      (1 / (8 * (N + 1 : ℝ))) * M3 ≤
        ((1 / 8) * M3) * (rho / s) := by
    calc
      (1 / (8 * (N + 1 : ℝ))) * M3 =
          ((1 / 8) * M3) * (1 / (N + 1 : ℝ)) := by
        field_simp [hn.ne']
        <;> ring
      _ ≤ ((1 / 8) * M3) * (rho / s) :=
        mul_le_mul_of_nonneg_left hInv (by positivity)
  have hden : 0 < 1 + 2 * rho := by positivity
  have hdenBound : 1 + 2 * rho ≤ 3 * rho := by nlinarith
  have hSpatial :
      (4 / sharpBerryEsseenFrequencyCutoff rho N) * K ≤
        (12 * K) * (rho / s) := by
    have hcut : sharpBerryEsseenFrequencyCutoff rho N = s / (1 + 2 * rho) := rfl
    rw [hcut]
    calc
      (4 / (s / (1 + 2 * rho))) * K =
          (4 * K) * ((1 + 2 * rho) / s) := by
        field_simp [hs.ne', hden.ne']
        <;> ring
      _ ≤ (4 * K) * ((3 * rho) / s) := by
        exact mul_le_mul_of_nonneg_left
          ((div_le_div_iff_of_pos_right hs).2 hdenBound) (by positivity)
      _ = (12 * K) * (rho / s) := by ring
  have hFourier :
      (2 * Real.pi)⁻¹ *
          ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / sharpBerryEsseenFrequencyCutoff rho N) * K) ≤
      ((2 * Real.pi)⁻¹ *
          ((1 / 6) * M2 + (1 / 8) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (12 * K)) *
        (rho / s) := by
    have hLocalPart :
        (rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3 ≤
          ((1 / 6) * M2) * (rho / s) +
            ((1 / 8) * M3) * (rho / s) := by
      calc
        (rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3 =
          ((1 / 6) * M2) * (rho / s) +
            (1 / (8 * (N + 1 : ℝ))) * M3 := by rw [hFirst]
        _ ≤ ((1 / 6) * M2) * (rho / s) +
            ((1 / 8) * M3) * (rho / s) :=
          add_le_add le_rfl hSecondTerm
    calc
      (2 * Real.pi)⁻¹ *
            ((rho / (6 * s)) * M2 +
              (1 / (8 * (N + 1 : ℝ))) * M3) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((4 / sharpBerryEsseenFrequencyCutoff rho N) * K) ≤
        (2 * Real.pi)⁻¹ *
            (((1 / 6) * M2) * (rho / s) +
              ((1 / 8) * M3) * (rho / s)) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((12 * K) * (rho / s)) := by
          exact add_le_add
            (mul_le_mul_of_nonneg_left hLocalPart
              (inv_nonneg.mpr (by positivity)))
            (mul_le_mul_of_nonneg_left hSpatial
              (inv_nonneg.mpr (by positivity)))
      _ = ((2 * Real.pi)⁻¹ *
            ((1 / 6) * M2 + (1 / 8) * M3) +
          (Real.sqrt (2 * Real.pi))⁻¹ * (12 * K)) *
          (rho / s) := by ring
  have hexplicit :=
    kolmogorovDistance_normalizedCenteredIidSum_le_explicit_sharp
      X hX hIndep hIdent hMean hSecond hN
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / sharpBerryEsseenFrequencyCutoff rho N) * K)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
      simpa only [rho, s, M2, M3, K] using hexplicit
    _ = 2 * ((2 * Real.pi)⁻¹ *
          ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / sharpBerryEsseenFrequencyCutoff rho N) * K)) := by ring
    _ ≤ 2 * (((2 * Real.pi)⁻¹ *
          ((1 / 6) * M2 + (1 / 8) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (12 * K)) *
          (rho / s)) := mul_le_mul_of_nonneg_left hFourier (by norm_num)
    _ = sharpBerryEsseenConstant * rho / s := by
      unfold sharpBerryEsseenConstant
      dsimp only [M2, M3, K]
      ring
    _ = sharpBerryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := rfl

theorem kolmogorovDistance_normalizedCenteredIidSum_le_maximalSharpBerryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      maximalSharpBerryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let M2 : ℝ := sharpGaussianDampedMoment 2
  let M3 : ℝ := sharpGaussianDampedMoment 3
  let K : ℝ := sincFourthShiftedMomentBound
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hrhoPos : 0 < rho := lt_of_lt_of_le zero_lt_one hrho
  have hs : 0 < s := by
    dsimp only [s]
    positivity
  have hn : 0 < (N + 1 : ℝ) := by positivity
  have hsSq : s ^ 2 = (N + 1 : ℝ) := by
    dsimp only [s]
    rw [Real.sq_sqrt hn.le]
  have hM2 : 0 ≤ M2 := sharpGaussianDampedMoment_nonneg 2
  have hM3 : 0 ≤ M3 := sharpGaussianDampedMoment_nonneg 3
  have hK : 0 ≤ K := by
    dsimp only [K, sincFourthShiftedMomentBound]
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  have hInv : 1 / (N + 1 : ℝ) ≤ rho / s := by
    rw [div_le_div_iff₀ hn hs]
    have hs_le_n : s ≤ (N + 1 : ℝ) := by
      rw [← hsSq]
      nlinarith
    have hn_le : (N + 1 : ℝ) ≤ rho * (N + 1 : ℝ) := by
      nlinarith
    simpa only [one_mul] using hs_le_n.trans hn_le
  have hFirst :
      (rho / (6 * s)) * M2 = ((1 / 6) * M2) * (rho / s) := by ring
  have hSecondTerm :
      (1 / (8 * (N + 1 : ℝ))) * M3 ≤
        ((1 / 8) * M3) * (rho / s) := by
    calc
      (1 / (8 * (N + 1 : ℝ))) * M3 =
          ((1 / 8) * M3) * (1 / (N + 1 : ℝ)) := by
        field_simp [hn.ne']
        <;> ring
      _ ≤ ((1 / 8) * M3) * (rho / s) :=
        mul_le_mul_of_nonneg_left hInv (by positivity)
  have hSpatial :
      (4 / maximalSharpBerryEsseenFrequencyCutoff rho N) * K =
        (8 * K) * (rho / s) := by
    have hcut : maximalSharpBerryEsseenFrequencyCutoff rho N = s / (2 * rho) := rfl
    rw [hcut]
    field_simp [hs.ne', hrhoPos.ne']
    <;> ring
  have hFourier :
      (2 * Real.pi)⁻¹ *
          ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / maximalSharpBerryEsseenFrequencyCutoff rho N) * K) ≤
      ((2 * Real.pi)⁻¹ *
          ((1 / 6) * M2 + (1 / 8) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (8 * K)) *
        (rho / s) := by
    have hLocalPart :
        (rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3 ≤
          ((1 / 6) * M2) * (rho / s) +
            ((1 / 8) * M3) * (rho / s) := by
      calc
        (rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3 =
          ((1 / 6) * M2) * (rho / s) +
            (1 / (8 * (N + 1 : ℝ))) * M3 := by rw [hFirst]
        _ ≤ ((1 / 6) * M2) * (rho / s) +
            ((1 / 8) * M3) * (rho / s) :=
          add_le_add le_rfl hSecondTerm
    calc
      (2 * Real.pi)⁻¹ *
            ((rho / (6 * s)) * M2 +
              (1 / (8 * (N + 1 : ℝ))) * M3) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((4 / maximalSharpBerryEsseenFrequencyCutoff rho N) * K) ≤
        (2 * Real.pi)⁻¹ *
            (((1 / 6) * M2) * (rho / s) +
              ((1 / 8) * M3) * (rho / s)) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((8 * K) * (rho / s)) := by
          rw [hSpatial]
          exact add_le_add
            (mul_le_mul_of_nonneg_left hLocalPart
              (inv_nonneg.mpr (by positivity)))
            le_rfl
      _ = ((2 * Real.pi)⁻¹ *
            ((1 / 6) * M2 + (1 / 8) * M3) +
          (Real.sqrt (2 * Real.pi))⁻¹ * (8 * K)) *
          (rho / s) := by ring
  have hexplicit :=
    kolmogorovDistance_normalizedCenteredIidSum_le_explicit_maximalSharp
      X hX hIndep hIdent hMean hSecond hN
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / maximalSharpBerryEsseenFrequencyCutoff rho N) * K)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
      simpa only [rho, s, M2, M3, K] using hexplicit
    _ = 2 * ((2 * Real.pi)⁻¹ *
          ((rho / (6 * s)) * M2 +
            (1 / (8 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / maximalSharpBerryEsseenFrequencyCutoff rho N) * K)) := by ring
    _ ≤ 2 * (((2 * Real.pi)⁻¹ *
          ((1 / 6) * M2 + (1 / 8) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (8 * K)) *
          (rho / s)) := mul_le_mul_of_nonneg_left hFourier (by norm_num)
    _ = maximalSharpBerryEsseenConstant * rho / s := by
      unfold maximalSharpBerryEsseenConstant
      dsimp only [M2, M3, K]
      ring
    _ = maximalSharpBerryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := rfl

theorem kolmogorovDistance_normalizedCenteredIidSum_le_berryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) :
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      berryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := by
  let rho : ℝ := ∫ omega, |X 0 omega| ^ 3 ∂mu
  let s : ℝ := Real.sqrt (N + 1 : ℝ)
  let M2 : ℝ := gaussianDampedMoment 2
  let M3 : ℝ := gaussianDampedMoment 3
  let K : ℝ := sincFourthShiftedMomentBound
  have hrho : 1 ≤ rho := by
    simpa only [rho] using
      one_le_thirdAbsoluteMoment_of_secondMoment_eq_one (hX 0) hSecond
  have hs : 0 < s := by
    dsimp only [s]
    positivity
  have hn : 0 < (N + 1 : ℝ) := by positivity
  have hsSq : s ^ 2 = (N + 1 : ℝ) := by
    dsimp only [s]
    rw [Real.sq_sqrt hn.le]
  have hsOne : 1 ≤ s := by
    rw [← Real.sqrt_one]
    dsimp only [s]
    exact Real.sqrt_le_sqrt (by exact_mod_cast Nat.le_add_left 1 N)
  have hM2 : 0 ≤ M2 := gaussianDampedMoment_nonneg 2
  have hM3 : 0 ≤ M3 := gaussianDampedMoment_nonneg 3
  have hK : 0 ≤ K := by
    dsimp only [K, sincFourthShiftedMomentBound]
    exact add_nonneg sincFourthTailRadius_pos.le sincFourthFirstMoment_nonneg
  have hInv : 1 / (N + 1 : ℝ) ≤ rho / s := by
    rw [div_le_div_iff₀ hn hs]
    have hs_le_n : s ≤ (N + 1 : ℝ) := by
      rw [← hsSq]
      nlinarith
    have hn_le : (N + 1 : ℝ) ≤ rho * (N + 1 : ℝ) := by
      nlinarith
    simpa only [one_mul] using hs_le_n.trans hn_le
  have hFirst :
      (4 * rho / s) * M2 = (4 * M2) * (rho / s) := by ring
  have hSecondTerm :
      (1 / (4 * (N + 1 : ℝ))) * M3 ≤
        ((1 / 4) * M3) * (rho / s) := by
    calc
      (1 / (4 * (N + 1 : ℝ))) * M3 =
          ((1 / 4) * M3) * (1 / (N + 1 : ℝ)) := by
        field_simp [hn.ne']
        <;> ring
      _ ≤ ((1 / 4) * M3) * (rho / s) :=
        mul_le_mul_of_nonneg_left hInv (by positivity)
  have hden : 0 < 1 + 16 * rho := by positivity
  have hdenBound : 1 + 16 * rho ≤ 17 * rho := by nlinarith
  have hSpatial :
      (4 / berryEsseenFrequencyCutoff rho N) * K ≤
        (68 * K) * (rho / s) := by
    have hcut : berryEsseenFrequencyCutoff rho N = s / (1 + 16 * rho) := rfl
    rw [hcut]
    calc
      (4 / (s / (1 + 16 * rho))) * K =
          (4 * K) * ((1 + 16 * rho) / s) := by
        field_simp [hs.ne', hden.ne']
        <;> ring
      _ ≤ (4 * K) * ((17 * rho) / s) := by
        exact mul_le_mul_of_nonneg_left
          ((div_le_div_iff_of_pos_right hs).2 hdenBound) (by positivity)
      _ = (68 * K) * (rho / s) := by ring
  have hFourier :
      (2 * Real.pi)⁻¹ *
          ((4 * rho / s) * M2 +
            (1 / (4 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / berryEsseenFrequencyCutoff rho N) * K) ≤
      ((2 * Real.pi)⁻¹ *
          (4 * M2 + (1 / 4) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (68 * K)) *
        (rho / s) := by
    have hLocalPart :
        (4 * rho / s) * M2 +
            (1 / (4 * (N + 1 : ℝ))) * M3 ≤
          (4 * M2) * (rho / s) +
            ((1 / 4) * M3) * (rho / s) := by
      calc
        (4 * rho / s) * M2 +
            (1 / (4 * (N + 1 : ℝ))) * M3 =
          (4 * M2) * (rho / s) +
            (1 / (4 * (N + 1 : ℝ))) * M3 := by rw [hFirst]
        _ ≤ (4 * M2) * (rho / s) +
            ((1 / 4) * M3) * (rho / s) :=
          add_le_add le_rfl hSecondTerm
    calc
      (2 * Real.pi)⁻¹ *
            ((4 * rho / s) * M2 +
              (1 / (4 * (N + 1 : ℝ))) * M3) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((4 / berryEsseenFrequencyCutoff rho N) * K) ≤
        (2 * Real.pi)⁻¹ *
            ((4 * M2) * (rho / s) +
              ((1 / 4) * M3) * (rho / s)) +
          (Real.sqrt (2 * Real.pi))⁻¹ *
            ((68 * K) * (rho / s)) := by
          exact add_le_add
            (mul_le_mul_of_nonneg_left hLocalPart
              (inv_nonneg.mpr (by positivity)))
            (mul_le_mul_of_nonneg_left hSpatial
              (inv_nonneg.mpr (by positivity)))
      _ = ((2 * Real.pi)⁻¹ *
            (4 * M2 + (1 / 4) * M3) +
          (Real.sqrt (2 * Real.pi))⁻¹ * (68 * K)) *
          (rho / s) := by ring
  have hexplicit :=
    kolmogorovDistance_normalizedCenteredIidSum_le_explicit
      X hX hIndep hIdent hMean hSecond hN
  calc
    kolmogorovDistance
        (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable)
        NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw ≤
      ((2 * Real.pi)⁻¹ *
          ((4 * rho / s) * M2 +
            (1 / (4 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / berryEsseenFrequencyCutoff rho N) * K)) /
        (1 - 2 * (1 / 4 : ℝ)) := by
      simpa only [rho, s, M2, M3, K] using hexplicit
    _ = 2 * ((2 * Real.pi)⁻¹ *
          ((4 * rho / s) * M2 +
            (1 / (4 * (N + 1 : ℝ))) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ *
          ((4 / berryEsseenFrequencyCutoff rho N) * K)) := by ring
    _ ≤ 2 * (((2 * Real.pi)⁻¹ *
          (4 * M2 + (1 / 4) * M3) +
        (Real.sqrt (2 * Real.pi))⁻¹ * (68 * K)) *
          (rho / s)) := mul_le_mul_of_nonneg_left hFourier (by norm_num)
    _ = berryEsseenConstant * rho / s := by
      unfold berryEsseenConstant
      dsimp only [M2, M3, K]
      ring
    _ = berryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := rfl

/-- Explicit leave-one-out concentration estimate obtained by inserting the
certified Berry--Esseen bound for the preceding normalized partial sum. -/
theorem concentrationFunction_normalizedCenteredIidSum_succ_le_explicit
    {Ω : Type*} [MeasurableSpace Ω]
    {mu : Measure Ω} [IsProbabilityMeasure mu]
    (X : ℕ → Ω → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) (hN : 1 ≤ N) {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      (Real.sqrt (2 * Real.pi))⁻¹ *
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            (h * Real.sqrt (N + 2 : ℝ))) +
        2 * (berryEsseenConstant *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) := by
  have hprev :=
    kolmogorovDistance_normalizedCenteredIidSum_le_berryEsseenConstant
      X hX hIndep hIdent hMean hSecond hN
  have hconc :=
    concentrationFunction_normalizedCenteredIidSum_succ_le_gaussian_previous
      X (fun i => (hX i).aemeasurable) hIndep N hh
  calc
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      (Real.sqrt (2 * Real.pi))⁻¹ *
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            (h * Real.sqrt (N + 2 : ℝ))) +
        2 * kolmogorovDistance
          (Measure.map
            (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N) mu)
          NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw := hconc
    _ ≤ _ := by
      exact add_le_add le_rfl <|
        mul_le_mul_of_nonneg_left
          (by
            simpa [NumStability.HDP.Scalar.LimitTheorems.probabilityLaw] using hprev)
          (by norm_num)

/-- The exact interval rescaling in the normalized leave-one-out recurrence is
uniformly bounded by `sqrt 2`. -/
theorem normalizedCenteredIidSum_succ_intervalScale_le_sqrt_two
    (N : ℕ) {h : ℝ} (hh : 0 ≤ h) :
    (Real.sqrt (N + 1 : ℝ))⁻¹ * (h * Real.sqrt (N + 2 : ℝ)) ≤
      Real.sqrt 2 * h := by
  have hb : 0 < Real.sqrt (N + 1 : ℝ) := by positivity
  have ha_le : Real.sqrt (N + 2 : ℝ) ≤
      Real.sqrt 2 * Real.sqrt (N + 1 : ℝ) := by
    apply (sq_le_sq₀ (Real.sqrt_nonneg _) (mul_nonneg (Real.sqrt_nonneg _) hb.le)).mp
    rw [Real.sq_sqrt (by positivity), mul_pow,
      Real.sq_sqrt (by positivity), Real.sq_sqrt (by positivity)]
    linarith
  rw [inv_mul_eq_div, div_le_iff₀ hb]
  calc
    h * Real.sqrt (N + 2 : ℝ) ≤
        h * (Real.sqrt 2 * Real.sqrt (N + 1 : ℝ)) :=
      mul_le_mul_of_nonneg_left ha_le hh
    _ = (Real.sqrt 2 * h) * Real.sqrt (N + 1 : ℝ) := by ring

/-- A simplified explicit leave-one-out concentration estimate with a
uniform `sqrt 2` normalization factor. -/
theorem concentrationFunction_normalizedCenteredIidSum_succ_le_explicit_sqrt_two
    {Ω : Type*} [MeasurableSpace Ω]
    {mu : Measure Ω} [IsProbabilityMeasure mu]
    (X : ℕ → Ω → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    (N : ℕ) (hN : 1 ≤ N) {h : ℝ} (hh : 0 ≤ h) :
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      (Real.sqrt (2 * Real.pi))⁻¹ * (Real.sqrt 2 * h) +
        2 * (berryEsseenConstant *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) := by
  have hbase := concentrationFunction_normalizedCenteredIidSum_succ_le_explicit
    X hX hIndep hIdent hMean hSecond N hN hh
  calc
    concentrationFunction
        (Measure.map
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X (N + 1)) mu) h ≤
      (Real.sqrt (2 * Real.pi))⁻¹ *
          ((Real.sqrt (N + 1 : ℝ))⁻¹ *
            (h * Real.sqrt (N + 2 : ℝ))) +
        2 * (berryEsseenConstant *
          (∫ omega, |X 0 omega| ^ 3 ∂mu) /
            Real.sqrt (N + 1 : ℝ)) := hbase
    _ ≤ _ := add_le_add
      (mul_le_mul_of_nonneg_left
        (normalizedCenteredIidSum_succ_intervalScale_le_sqrt_two N hh)
        (inv_nonneg.mpr (Real.sqrt_nonneg _))) le_rfl

/-- Closed-upper-tail form of the certified Berry--Esseen estimate for a
centered unit-variance iid family. Unlike a direct CDF rewrite, this remains
valid when the normalized sum has atoms. -/
theorem measureReal_Ici_normalizedCenteredIidSum_gap_le_berryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = 0)
    (hSecond : ∫ omega, (X 0 omega) ^ 2 ∂mu = 1)
    {N : ℕ} (hN : 1 ≤ N) (t : ℝ) :
    |(NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum X N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            X (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
        Measure ℝ).real (Ici t) -
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw.real (Ici t)| ≤
      berryEsseenConstant *
        (∫ omega, |X 0 omega| ^ 3 ∂mu) /
          Real.sqrt (N + 1 : ℝ) := by
  exact (measureReal_Ici_gap_le_kolmogorovDistance _ _ t).trans
    (kolmogorovDistance_normalizedCenteredIidSum_le_berryEsseenConstant
      X hX hIndep hIdent hMean hSecond hN)

/-- Mean/variance-standardized closed-upper-tail Berry--Esseen estimate. This
is the reusable form corresponding to the normalization in Chapter 1. -/
theorem measureReal_Ici_normalizedIidSum_gap_le_berryEsseenConstant
    {Omega : Type*} [MeasurableSpace Omega]
    {mu : Measure Omega} [IsProbabilityMeasure mu]
    (X : ℕ → Omega → ℝ) (m sigma : ℝ) (hsigma : 0 < sigma)
    (hX : ∀ i, MemLp (X i) 3 mu)
    (hIndep : ProbabilityTheory.iIndepFun X mu)
    (hIdent : ∀ i, ProbabilityTheory.IdentDistrib (X i) (X 0) mu mu)
    (hMean : ∫ omega, X 0 omega ∂mu = m)
    (hVariance : ProbabilityTheory.variance (X 0) mu = sigma ^ 2)
    {N : ℕ} (hN : 1 ≤ N) (t : ℝ) :
    |(NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum X m sigma N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum_memLp
            X m sigma (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
        Measure ℝ).real (Ici t) -
      NumStability.HDP.Scalar.LimitTheorems.standardNormalLaw.real (Ici t)| ≤
      berryEsseenConstant *
        ((∫ omega, |X 0 omega - m| ^ 3 ∂mu) / sigma ^ 3) /
          Real.sqrt (N + 1 : ℝ) := by
  let Y : ℕ → Omega → ℝ := fun i omega => sigma⁻¹ * (X i omega - m)
  have hY : ∀ i, MemLp (Y i) 3 mu := by
    intro i
    exact ((hX i).sub (memLp_const m)).const_mul sigma⁻¹
  have hIndepY : ProbabilityTheory.iIndepFun Y mu := by
    have h := hIndep.comp
      (fun _ (x : ℝ) => sigma⁻¹ * (x - m)) (fun _ => by fun_prop)
    simpa [Y, Function.comp_def] using h
  have hIdentY : ∀ i, ProbabilityTheory.IdentDistrib (Y i) (Y 0) mu mu := by
    intro i
    have h := (hIdent i).comp
      (by fun_prop : Measurable fun x : ℝ => sigma⁻¹ * (x - m))
    simpa [Y, Function.comp_def] using h
  have hMeanY : ∫ omega, Y 0 omega ∂mu = 0 := by
    dsimp [Y]
    rw [integral_const_mul,
      integral_sub ((hX 0).integrable (by norm_num)) (integrable_const m), hMean]
    simp
  have hSecondY : ∫ omega, (Y 0 omega) ^ 2 ∂mu = 1 := by
    have hX2 : MemLp (X 0) 2 mu := (hX 0).mono_exponent (by norm_num)
    have hc : (∫ omega, (X 0 omega - m) ^ 2 ∂mu) = sigma ^ 2 := by
      calc
        (∫ omega, (X 0 omega - m) ^ 2 ∂mu) =
            ProbabilityTheory.variance (X 0) mu := by
          rw [ProbabilityTheory.variance_eq_integral hX2.aemeasurable, hMean]
        _ = sigma ^ 2 := hVariance
    dsimp [Y]
    calc
      (∫ omega, (sigma⁻¹ * (X 0 omega - m)) ^ 2 ∂mu) =
          ∫ omega, sigma⁻¹ ^ 2 * (X 0 omega - m) ^ 2 ∂mu := by
        congr 1
        funext omega
        ring
      _ = sigma⁻¹ ^ 2 * ∫ omega, (X 0 omega - m) ^ 2 ∂mu :=
        integral_const_mul _ _
      _ = 1 := by
        rw [hc]
        field_simp [hsigma.ne']
  have hEq :
      NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum Y N =
        NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum X m sigma N := by
    funext omega
    simp only [NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum,
      NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum, Y]
    rw [← Finset.mul_sum Finset.univ
      (fun i : Fin (N + 1) => X i.1 omega - m) sigma⁻¹]
    rw [← mul_assoc]
    apply congrArg
      (fun c : ℝ => c * ∑ i : Fin (N + 1), (X i.1 omega - m))
    rw [mul_inv_rev]
  have hThirdY :
      (∫ omega, |Y 0 omega| ^ 3 ∂mu) =
        (∫ omega, |X 0 omega - m| ^ 3 ∂mu) / sigma ^ 3 := by
    have hfun : (fun omega => |Y 0 omega| ^ 3) =
        fun omega => sigma⁻¹ ^ 3 * |X 0 omega - m| ^ 3 := by
      funext omega
      dsimp [Y]
      rw [abs_mul, abs_of_pos (inv_pos.mpr hsigma)]
      ring
    rw [hfun, integral_const_mul]
    field_simp [hsigma.ne']
  have htail :=
    measureReal_Ici_normalizedCenteredIidSum_gap_le_berryEsseenConstant
      Y hY hIndepY hIdentY hMeanY hSecondY hN t
  have hLaw :
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum Y N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum_memLp
            Y (fun i => (hY i).mono_exponent (by norm_num)) N).aemeasurable :
        Measure ℝ) =
      (NumStability.HDP.Scalar.LimitTheorems.probabilityLaw
          (NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum X m sigma N)
          (NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum_memLp
            X m sigma (fun i => (hX i).mono_exponent (by norm_num)) N).aemeasurable :
        Measure ℝ) := by
    change Measure.map
        (NumStability.HDP.Scalar.LimitTheorems.normalizedCenteredIidSum Y N) mu =
      Measure.map
        (NumStability.HDP.Scalar.LimitTheorems.normalizedIidSum X m sigma N) mu
    rw [hEq]
  rw [hLaw, hThirdY] at htail
  exact htail

end NumStability.HDP.Scalar.BerryEsseen
