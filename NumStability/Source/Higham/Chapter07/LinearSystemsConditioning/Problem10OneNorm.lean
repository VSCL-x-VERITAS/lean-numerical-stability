import Mathlib.Algebra.Order.GroupWithZero.Finset
import Mathlib.LinearAlgebra.Basis.VectorSpace
import Mathlib.LinearAlgebra.Charpoly.ToMatrix
import Mathlib.LinearAlgebra.Eigenspace.Charpoly
import Mathlib.LinearAlgebra.Finsupp.Pi
import Mathlib.LinearAlgebra.Matrix.FiniteDimensional
import Mathlib.LinearAlgebra.Matrix.Irreducible.Defs
import NumStability.Algorithms.NormEstimation.OneNorm.PowerMethod
import NumStability.Analysis.Asymptotics.Bounds
import NumStability.Analysis.Conditioning.DistanceToSingularity
import NumStability.Analysis.Conditioning.LinearSystems.PerronFrobenius
import NumStability.Analysis.MatrixAlgebra
import NumStability.Analysis.MatrixNorms.Basic
import NumStability.Analysis.MatrixNorms.Comparisons
import NumStability.Analysis.MatrixNorms.Lp
import NumStability.Analysis.MatrixNorms.SpectralRadius
import NumStability.Analysis.PerturbationTheory
import NumStability.Analysis.SingularValues.Basic
import NumStability.Analysis.SingularValues.Realification
import NumStability.Analysis.Summation.Signs
import NumStability.Analysis.VectorNorms.Basic
import NumStability.Source.Higham.Chapter06.Problem05
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Problem09Linearized
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Problem10Bauer.Part01
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Problem10Bauer.Part02
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Problem10Bauer.Part03

-- Analysis/HighamChapter7.lean
--
-- Source-facing wrappers for Higham Chapter 7.
--
-- The heavy perturbation arguments live in `PerturbationTheory.lean`; this file
-- records Chapter 7 statements whose exact source shape is a relative
-- infinity-norm or practical-error corollary of those componentwise results.

















namespace NumStability

open scoped BigOperators

-- ============================================================
-- Chapter 7 forward-error kernels
-- ============================================================














































-- ============================================================
-- Problem 7.1: local Neumann contraction infrastructure
-- ============================================================

































































































































































































































































































































































































































































































-- ============================================================
-- Theorem 7.1: arbitrary subordinate-norm Rigal-Gaches wrappers
-- ============================================================




























































































































































































































































































-- ============================================================
-- Equations (7.11), (7.13), and (7.14): condition numbers
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.5: SVD-projected perturbation bound
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.6(a): row-wise relative perturbations
-- ============================================================





















































































































































































































-- ============================================================
-- Problem 7.6(b): columnwise relative perturbations
-- ============================================================












































































































































































































































-- ============================================================
-- Chapter 7 finite sign helpers
-- ============================================================
















-- ============================================================
-- Problem 7.9: linearized scalar-output componentwise condition
-- ============================================================






































































































































































































































































































































































































































































-- ------------------------------------------------------------
-- Problem 7.9: finite normwise scalar-output formula
-- ------------------------------------------------------------














































































































































































































































































































































































-- ------------------------------------------------------------
-- Problem 7.9: first-order source-radius condition values
-- ------------------------------------------------------------






















































































































































































































































































































-- ============================================================
-- Problem 7.11: linearized inverse componentwise perturbations
-- ============================================================















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.9: exact componentwise scalar-output bridge
-- ============================================================



























































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.9: exact normwise scalar-output bridge
-- ============================================================













































































































































































































































































































































































































































































































































-- ============================================================
-- Equation (7.26): distance to singularity
-- ============================================================





















-- ============================================================
-- Equation (7.15) / Problem 7.3 row-equilibration infrastructure
-- ============================================================










































































































































































































































































































































































-- ============================================================
-- Equation (7.17): Kahan's symbolic 3 × 3 example
-- ============================================================












































































































































































































































































































































































































-- ============================================================
-- Problem 7.4: unit-diagonal SPD scaling core bound
-- ============================================================






















































































































































-- ============================================================
-- Theorem 7.5 dependency: Euclidean column-equilibration bounds
-- ============================================================











































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Corollary 7.6 dependency: SPD diagonal scaling via Cholesky factors
-- ============================================================


























































































































-- ============================================================
-- Theorem 7.7: Stewart-Sun Frobenius right scaling
-- ============================================================
























































































































































































































































































-- ============================================================
-- Theorem 7.8 / Problem 7.10: Bauer two-sided scaling algebraic core
-- ============================================================























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































section Ch7MatrixOperatorNorm

open scoped Matrix.Norms.Operator





























































































































































































































































end Ch7MatrixOperatorNorm











































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.10(e): one-norm Bauer transpose reductions
-- ============================================================










/-- Problem 7.10(e)'s source reduction `‖M‖₁ = ‖Mᵀ‖∞` for the two-sided
Bauer product. -/
lemma ch7TwoSidedScaledOneCond_eq_transpose_infCond {n : ℕ}
    (B C : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ) :
    ch7TwoSidedScaledOneCond B C d₁ d₁Inv d₂ d₂Inv =
      ch7TwoSidedScaledInfCond (matTranspose B) (matTranspose C)
        d₂ d₂Inv d₁ d₁Inv := by
  unfold ch7TwoSidedScaledOneCond ch7TwoSidedScaledInfCond oneNorm
  rw [show (fun i j => ch7TwoSidedScale d₁ B d₂ j i) =
      ch7TwoSidedScale d₂ (matTranspose B) d₁ by
        change matTranspose (ch7TwoSidedScale d₁ B d₂) =
          ch7TwoSidedScale d₂ (matTranspose B) d₁
        rw [ch7_matTranspose_twoSidedScale],
    show (fun i j => ch7TwoSidedScale d₂Inv C d₁Inv j i) =
      ch7TwoSidedScale d₁Inv (matTranspose C) d₂Inv by
        change matTranspose (ch7TwoSidedScale d₂Inv C d₁Inv) =
          ch7TwoSidedScale d₁Inv (matTranspose C) d₂Inv
        rw [ch7_matTranspose_twoSidedScale]]

/-- Problem 7.10(e), one-norm lower-bound half under an explicit positive
Perron-vector certificate for `BᵀCᵀ`.  This is the source transpose reduction
from the proved infinity-norm Bauer core. -/
theorem problem7_10e_scaled_oneCond_ge_perron_of_transpose_positive_eigenvector
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hC : ∀ i j : Fin n, 0 ≤ C i j)
    (hy : ∀ i : Fin n, 0 < y i)
    (heigT : ∀ i : Fin n,
      matMulVec n (matMul n (matTranspose B) (matTranspose C)) y i = ρ * y i)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ)
    (hd₁pos : ∀ i : Fin n, 0 < d₁ i)
    (hd₁Invpos : ∀ i : Fin n, 0 < d₁Inv i)
    (hd₂pos : ∀ i : Fin n, 0 < d₂ i)
    (hd₂Invpos : ∀ i : Fin n, 0 < d₂Inv i)
    (hd₁ : ∀ i : Fin n, d₁ i * d₁Inv i = 1)
    (hd₂ : ∀ i : Fin n, d₂ i * d₂Inv i = 1) :
    ρ ≤ ch7TwoSidedScaledOneCond B C d₁ d₁Inv d₂ d₂Inv := by
  rw [ch7TwoSidedScaledOneCond_eq_transpose_infCond B C d₁ d₁Inv d₂ d₂Inv]
  exact problem7_10a_scaled_infCond_ge_perron_of_positive_eigenvector
    hn (matTranspose B) (matTranspose C) ρ y
    (by intro i j; exact hB j i)
    (by intro i j; exact hC j i)
    hy heigT d₂ d₂Inv d₁ d₁Inv
    hd₂pos hd₂Invpos hd₁pos hd₁Invpos hd₂ hd₁

/-- Problem 7.10(e), one-norm lower-bound half under a positive
subeigenvector for the transpose product `BᵀCᵀ`. -/
theorem problem7_10e_scaled_oneCond_ge_perron_of_transpose_positive_subeigenvector
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hρ : 0 ≤ ρ)
    (hy : ∀ i : Fin n, 0 < y i)
    (hsubT : ∀ i : Fin n,
      ρ * y i ≤
        matMulVec n (matMul n (matTranspose B) (matTranspose C)) y i)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ)
    (hd₁pos : ∀ i : Fin n, 0 < d₁ i)
    (hd₁Invpos : ∀ i : Fin n, 0 < d₁Inv i)
    (hd₂pos : ∀ i : Fin n, 0 < d₂ i)
    (hd₂Invpos : ∀ i : Fin n, 0 < d₂Inv i)
    (hd₁ : ∀ i : Fin n, d₁ i * d₁Inv i = 1)
    (hd₂ : ∀ i : Fin n, d₂ i * d₂Inv i = 1) :
    ρ ≤ ch7TwoSidedScaledOneCond B C d₁ d₁Inv d₂ d₂Inv := by
  rw [ch7TwoSidedScaledOneCond_eq_transpose_infCond B C d₁ d₁Inv d₂ d₂Inv]
  exact problem7_10a_scaled_infCond_ge_perron_of_positive_subeigenvector
    hn (matTranspose B) (matTranspose C) ρ y hρ hy hsubT
    d₂ d₂Inv d₁ d₁Inv
    hd₂pos hd₂Invpos hd₁pos hd₁Invpos hd₂ hd₁

/-- Problem 7.10(e), canonical one-norm Bauer scaling under an explicit
positive Perron-vector certificate for `BᵀCᵀ`.  The diagonal choice is the
transpose-side version of the scaling in Problem 7.10(a). -/
theorem problem7_10e_canonical_scaled_oneCond_eq_perron_of_transpose_positive_eigenvector
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hC : ∀ i j : Fin n, 0 ≤ C i j)
    (hy : ∀ i : Fin n, 0 < y i)
    (hCt_y : ∀ i : Fin n, 0 < matMulVec n (matTranspose C) y i)
    (heigT : ∀ i : Fin n,
      matMulVec n (matMul n (matTranspose B) (matTranspose C)) y i = ρ * y i) :
    ch7TwoSidedScaledOneCond B C
        (fun i => matMulVec n (matTranspose C) y i)
        (fun i => (matMulVec n (matTranspose C) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ := by
  rw [ch7TwoSidedScaledOneCond_eq_transpose_infCond]
  exact problem7_10a_canonical_scaled_infCond_eq_perron_of_positive_eigenvector
    hn (matTranspose B) (matTranspose C) ρ y
    (by intro i j; exact hB j i)
    (by intro i j; exact hC j i)
    hy hCt_y heigT

/-- Problem 7.10(e), positive-entry wrapper for the canonical one-norm Bauer
scaling under a transpose-side positive Perron-vector certificate. -/
theorem problem7_10e_positive_entries_canonical_scaled_oneCond_eq_perron_of_transpose
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBpos : ∀ i j : Fin n, 0 < B i j)
    (hCpos : ∀ i j : Fin n, 0 < C i j)
    (hy : ∀ i : Fin n, 0 < y i)
    (heigT : ∀ i : Fin n,
      matMulVec n (matMul n (matTranspose B) (matTranspose C)) y i = ρ * y i) :
    ch7TwoSidedScaledOneCond B C
        (fun i => matMulVec n (matTranspose C) y i)
        (fun i => (matMulVec n (matTranspose C) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ :=
  problem7_10e_canonical_scaled_oneCond_eq_perron_of_transpose_positive_eigenvector
    hn B C ρ y
    (fun i j => le_of_lt (hBpos i j))
    (fun i j => le_of_lt (hCpos i j))
    hy
    (ch7_bauer_Cx_pos_of_pos hn (matTranspose C) y
      (fun i j => hCpos j i) hy)
    heigT

/-- The one-norm condition product for the two-sidedly scaled matrix
`D₁ A D₂`, with the inverse-scaled partner `D₂⁻¹ A_inv D₁⁻¹` supplied
entrywise. -/
noncomputable def ch7TwoSidedScaledOneKappa {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ) : ℝ :=
  oneNorm (ch7TwoSidedScale d₁ A d₂) *
    oneNorm (ch7TwoSidedScale d₂Inv A_inv d₁Inv)

/-- Problem 7.10(e)'s one-norm condition-product values over positive
reciprocal two-sided diagonal scalings, represented with an explicit
inverse-side matrix `A_inv`. -/
noncomputable def ch7TwoSidedScaledOneKappaSet {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ) : Set ℝ :=
  {κ : ℝ | ∃ d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ,
    (∀ i : Fin n, 0 < d₁ i) ∧
    (∀ i : Fin n, 0 < d₁Inv i) ∧
    (∀ i : Fin n, 0 < d₂ i) ∧
    (∀ i : Fin n, 0 < d₂Inv i) ∧
    (∀ i : Fin n, d₁ i * d₁Inv i = 1) ∧
    (∀ i : Fin n, d₂ i * d₂Inv i = 1) ∧
    κ = ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv}

/-- Membership helper for the positive two-sided one-norm condition-product
value set. -/
theorem ch7TwoSidedScaledOneKappa_mem_set {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ)
    (hd₁pos : ∀ i : Fin n, 0 < d₁ i)
    (hd₁Invpos : ∀ i : Fin n, 0 < d₁Inv i)
    (hd₂pos : ∀ i : Fin n, 0 < d₂ i)
    (hd₂Invpos : ∀ i : Fin n, 0 < d₂Inv i)
    (hd₁ : ∀ i : Fin n, d₁ i * d₁Inv i = 1)
    (hd₂ : ∀ i : Fin n, d₂ i * d₂Inv i = 1) :
    ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv ∈
      ch7TwoSidedScaledOneKappaSet A A_inv := by
  refine ⟨d₁, d₁Inv, d₂, d₂Inv,
    hd₁pos, hd₁Invpos, hd₂pos, hd₂Invpos, hd₁, hd₂, ?_⟩
  rfl

/-- The positive one-norm condition-product scaling value set is nonempty by
the identity scaling. -/
theorem ch7TwoSidedScaledOneKappaSet_nonempty {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ) :
    (ch7TwoSidedScaledOneKappaSet A A_inv).Nonempty := by
  refine ⟨oneNorm A * oneNorm A_inv, ?_⟩
  refine ⟨fun _ => 1, fun _ => 1, fun _ => 1, fun _ => 1,
    (by intro i; norm_num), (by intro i; norm_num),
    (by intro i; norm_num), (by intro i; norm_num),
    (by intro i; norm_num), (by intro i; norm_num), ?_⟩
  have hA :
      ch7TwoSidedScale (fun _ : Fin n => (1 : ℝ)) A
        (fun _ : Fin n => (1 : ℝ)) = A := by
    ext i j
    simp [ch7TwoSidedScale]
  have hAinv :
      ch7TwoSidedScale (fun _ : Fin n => (1 : ℝ)) A_inv
        (fun _ : Fin n => (1 : ℝ)) = A_inv := by
    ext i j
    simp [ch7TwoSidedScale]
  simp [ch7TwoSidedScaledOneKappa, hA, hAinv]

/-- The positive one-norm condition-product scaling value set is bounded below
by zero. -/
theorem ch7TwoSidedScaledOneKappaSet_bddBelow {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ) :
    BddBelow (ch7TwoSidedScaledOneKappaSet A A_inv) := by
  refine ⟨0, ?_⟩
  intro κ hκ
  rcases hκ with
    ⟨d₁, d₁Inv, d₂, d₂Inv, _hd₁pos, _hd₁Invpos,
      _hd₂pos, _hd₂Invpos, _hd₁, _hd₂, rfl⟩
  exact mul_nonneg (oneNorm_nonneg _) (oneNorm_nonneg _)

/-- The infimum of the positive one-norm condition-product scaling value set
is nonnegative. -/
theorem ch7TwoSidedScaledOneKappaSet_sInf_nonneg {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ) :
    0 ≤ sInf (ch7TwoSidedScaledOneKappaSet A A_inv) := by
  let S : Set ℝ := ch7TwoSidedScaledOneKappaSet A A_inv
  have hS_nonempty : S.Nonempty := by
    simpa [S] using ch7TwoSidedScaledOneKappaSet_nonempty A A_inv
  have hlower : ∀ κ : ℝ, κ ∈ S → 0 ≤ κ := by
    intro κ hκ
    rcases hκ with
      ⟨d₁, d₁Inv, d₂, d₂Inv, _hd₁pos, _hd₁Invpos,
        _hd₂pos, _hd₂Invpos, _hd₁, _hd₂, rfl⟩
    exact mul_nonneg (oneNorm_nonneg _) (oneNorm_nonneg _)
  exact le_csInf hS_nonempty hlower

/-- Problem 7.10(e)'s absolute-value reduction for the one-norm condition
product. -/
lemma ch7TwoSidedScaledOneKappa_eq_abs_scaledOneCond {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ)
    (hd₁ : ∀ i : Fin n, 0 ≤ d₁ i)
    (hd₁Inv : ∀ i : Fin n, 0 ≤ d₁Inv i)
    (hd₂ : ∀ i : Fin n, 0 ≤ d₂ i)
    (hd₂Inv : ∀ i : Fin n, 0 ≤ d₂Inv i) :
    ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv =
      ch7TwoSidedScaledOneCond (absMatrix n A) (absMatrix n A_inv)
        d₁ d₁Inv d₂ d₂Inv := by
  unfold ch7TwoSidedScaledOneKappa ch7TwoSidedScaledOneCond oneNorm
  rw [show (fun i j => ch7TwoSidedScale d₁ A d₂ j i) =
      ch7TwoSidedScale d₂ (matTranspose A) d₁ by
        change matTranspose (ch7TwoSidedScale d₁ A d₂) =
          ch7TwoSidedScale d₂ (matTranspose A) d₁
        rw [ch7_matTranspose_twoSidedScale],
    show (fun i j => ch7TwoSidedScale d₁ (absMatrix n A) d₂ j i) =
      ch7TwoSidedScale d₂ (matTranspose (absMatrix n A)) d₁ by
        change matTranspose (ch7TwoSidedScale d₁ (absMatrix n A) d₂) =
          ch7TwoSidedScale d₂ (matTranspose (absMatrix n A)) d₁
        rw [ch7_matTranspose_twoSidedScale],
    show (fun i j => ch7TwoSidedScale d₂Inv A_inv d₁Inv j i) =
      ch7TwoSidedScale d₁Inv (matTranspose A_inv) d₂Inv by
        change matTranspose (ch7TwoSidedScale d₂Inv A_inv d₁Inv) =
          ch7TwoSidedScale d₁Inv (matTranspose A_inv) d₂Inv
        rw [ch7_matTranspose_twoSidedScale],
    show (fun i j => ch7TwoSidedScale d₂Inv (absMatrix n A_inv) d₁Inv j i) =
      ch7TwoSidedScale d₁Inv (matTranspose (absMatrix n A_inv)) d₂Inv by
        change matTranspose (ch7TwoSidedScale d₂Inv (absMatrix n A_inv) d₁Inv) =
          ch7TwoSidedScale d₁Inv (matTranspose (absMatrix n A_inv)) d₂Inv
        rw [ch7_matTranspose_twoSidedScale]]
  change infNorm (ch7TwoSidedScale d₂ (matTranspose A) d₁) *
      infNorm (ch7TwoSidedScale d₁Inv (matTranspose A_inv) d₂Inv) =
    infNorm (ch7TwoSidedScale d₂ (absMatrix n (matTranspose A)) d₁) *
      infNorm (ch7TwoSidedScale d₁Inv (absMatrix n (matTranspose A_inv)) d₂Inv)
  rw [ch7_infNorm_twoSidedScale_absMatrix_eq hn d₂ (matTranspose A) d₁ hd₂ hd₁,
    ch7_infNorm_twoSidedScale_absMatrix_eq hn
      d₁Inv (matTranspose A_inv) d₂Inv hd₁Inv hd₂Inv]

/-- Problem 7.10(e), one-norm condition-number lower-bound half under an
explicit positive Perron-vector certificate for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_scaled_oneKappa_ge_perron_of_abs_inverse_transpose_positive_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hy : ∀ i : Fin n, 0 < y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ)
    (hd₁pos : ∀ i : Fin n, 0 < d₁ i)
    (hd₁Invpos : ∀ i : Fin n, 0 < d₁Inv i)
    (hd₂pos : ∀ i : Fin n, 0 < d₂ i)
    (hd₂Invpos : ∀ i : Fin n, 0 < d₂Inv i)
    (hd₁ : ∀ i : Fin n, d₁ i * d₁Inv i = 1)
    (hd₂ : ∀ i : Fin n, d₂ i * d₂Inv i = 1) :
    ρ ≤ ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv := by
  rw [ch7TwoSidedScaledOneKappa_eq_abs_scaledOneCond hn
    A A_inv d₁ d₁Inv d₂ d₂Inv
    (fun i => le_of_lt (hd₁pos i)) (fun i => le_of_lt (hd₁Invpos i))
    (fun i => le_of_lt (hd₂pos i)) (fun i => le_of_lt (hd₂Invpos i))]
  exact problem7_10e_scaled_oneCond_ge_perron_of_transpose_positive_eigenvector
    hn (absMatrix n A) (absMatrix n A_inv) ρ y
    (by intro i j; exact abs_nonneg _)
    (by intro i j; exact abs_nonneg _)
    hy heigT d₁ d₁Inv d₂ d₂Inv
    hd₁pos hd₁Invpos hd₂pos hd₂Invpos hd₁ hd₂

/-- Problem 7.10(e), one-norm condition-number lower-bound half under a
positive subeigenvector for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_scaled_oneKappa_ge_perron_of_abs_inverse_transpose_positive_subeigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hρ : 0 ≤ ρ)
    (hy : ∀ i : Fin n, 0 < y i)
    (hsubT : ∀ i : Fin n,
      ρ * y i ≤
        matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ)
    (hd₁pos : ∀ i : Fin n, 0 < d₁ i)
    (hd₁Invpos : ∀ i : Fin n, 0 < d₁Inv i)
    (hd₂pos : ∀ i : Fin n, 0 < d₂ i)
    (hd₂Invpos : ∀ i : Fin n, 0 < d₂Inv i)
    (hd₁ : ∀ i : Fin n, d₁ i * d₁Inv i = 1)
    (hd₂ : ∀ i : Fin n, d₂ i * d₂Inv i = 1) :
    ρ ≤ ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv := by
  rw [ch7TwoSidedScaledOneKappa_eq_abs_scaledOneCond hn
    A A_inv d₁ d₁Inv d₂ d₂Inv
    (fun i => le_of_lt (hd₁pos i)) (fun i => le_of_lt (hd₁Invpos i))
    (fun i => le_of_lt (hd₂pos i)) (fun i => le_of_lt (hd₂Invpos i))]
  exact problem7_10e_scaled_oneCond_ge_perron_of_transpose_positive_subeigenvector
    hn (absMatrix n A) (absMatrix n A_inv) ρ y hρ hy hsubT
    d₁ d₁Inv d₂ d₂Inv
    hd₁pos hd₁Invpos hd₂pos hd₂Invpos hd₁ hd₂

/-- Problem 7.10(e), one-norm `κ₁` value-set lower bound from a positive
subeigenvector for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_scaledOneKappaSet_sInf_ge_perron_of_abs_inverse_transpose_positive_subeigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hρ : 0 ≤ ρ)
    (hy : ∀ i : Fin n, 0 < y i)
    (hsubT : ∀ i : Fin n,
      ρ * y i ≤
        matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i) :
    ρ ≤ sInf (ch7TwoSidedScaledOneKappaSet A A_inv) := by
  let S : Set ℝ := ch7TwoSidedScaledOneKappaSet A A_inv
  have hS_nonempty : S.Nonempty := by
    simpa [S] using ch7TwoSidedScaledOneKappaSet_nonempty A A_inv
  have hlower : ∀ κ : ℝ, κ ∈ S → ρ ≤ κ := by
    intro κ hκ
    rcases hκ with
      ⟨d₁, d₁Inv, d₂, d₂Inv, hd₁pos, hd₁Invpos,
        hd₂pos, hd₂Invpos, hd₁, hd₂, rfl⟩
    exact
      problem7_10e_scaled_oneKappa_ge_perron_of_abs_inverse_transpose_positive_subeigenvector
        hn A A_inv ρ y hρ hy hsubT d₁ d₁Inv d₂ d₂Inv
        hd₁pos hd₁Invpos hd₂pos hd₂Invpos hd₁ hd₂
  exact le_csInf hS_nonempty hlower

/-- Problem 7.10(e), canonical one-norm two-sided scaling for
`B = |A|`, `C = |A⁻¹|`, under an explicit positive Perron-vector certificate
for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_canonical_scaled_oneKappa_eq_perron_of_abs_inverse_transpose_positive_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hy : ∀ i : Fin n, 0 < y i)
    (hCt_y : ∀ i : Fin n, 0 < matMulVec n (matTranspose (absMatrix n A_inv)) y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    ch7TwoSidedScaledOneKappa A A_inv
        (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
        (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ := by
  rw [ch7TwoSidedScaledOneKappa_eq_abs_scaledOneCond hn A A_inv
    (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
    (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
    (fun i => (y i)⁻¹) y
    (fun i => le_of_lt (hCt_y i))
    (fun i => inv_nonneg.mpr (le_of_lt (hCt_y i)))
    (fun i => inv_nonneg.mpr (le_of_lt (hy i)))
    (fun i => le_of_lt (hy i))]
  exact problem7_10e_canonical_scaled_oneCond_eq_perron_of_transpose_positive_eigenvector
    hn (absMatrix n A) (absMatrix n A_inv) ρ y
    (by intro i j; exact abs_nonneg _)
    (by intro i j; exact abs_nonneg _)
    hy hCt_y heigT

/-- Problem 7.10(e), positive-entry wrapper for the one-norm canonical
`B = |A|`, `C = |A⁻¹|` scaling.  Perron-Frobenius existence and
spectral-radius identification remain separate source-facing targets. -/
theorem problem7_10e_positive_abs_entries_canonical_scaled_oneKappa_eq_perron_of_transpose
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hApos : ∀ i j : Fin n, 0 < absMatrix n A i j)
    (hAinvpos : ∀ i j : Fin n, 0 < absMatrix n A_inv i j)
    (hy : ∀ i : Fin n, 0 < y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    ch7TwoSidedScaledOneKappa A A_inv
        (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
        (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ := by
  rw [ch7TwoSidedScaledOneKappa_eq_abs_scaledOneCond hn A A_inv
    (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
    (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
    (fun i => (y i)⁻¹) y
    (fun i => le_of_lt (ch7_bauer_Cx_pos_of_pos hn
      (matTranspose (absMatrix n A_inv)) y (fun i j => hAinvpos j i) hy i))
    (fun i => inv_nonneg.mpr (le_of_lt (ch7_bauer_Cx_pos_of_pos hn
      (matTranspose (absMatrix n A_inv)) y (fun i j => hAinvpos j i) hy i)))
    (fun i => inv_nonneg.mpr (le_of_lt (hy i)))
    (fun i => le_of_lt (hy i))]
  exact problem7_10e_positive_entries_canonical_scaled_oneCond_eq_perron_of_transpose
    hn (absMatrix n A) (absMatrix n A_inv) ρ y hApos hAinvpos hy heigT

/-- Problem 7.10(e), source-shaped one-norm condition-product infimum under an
explicit positive Perron-vector certificate for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_scaledOneKappaSet_sInf_eq_perron_of_abs_inverse_transpose_positive_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hy : ∀ i : Fin n, 0 < y i)
    (hCt_y : ∀ i : Fin n, 0 < matMulVec n (matTranspose (absMatrix n A_inv)) y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    sInf (ch7TwoSidedScaledOneKappaSet A A_inv) = ρ := by
  let S : Set ℝ := ch7TwoSidedScaledOneKappaSet A A_inv
  have hS_nonempty : S.Nonempty := by
    simpa [S] using ch7TwoSidedScaledOneKappaSet_nonempty A A_inv
  have hS_bdd : BddBelow S := by
    simpa [S] using ch7TwoSidedScaledOneKappaSet_bddBelow A A_inv
  have hlower : ∀ κ : ℝ, κ ∈ S → ρ ≤ κ := by
    intro κ hκ
    rcases hκ with
      ⟨d₁, d₁Inv, d₂, d₂Inv, hd₁pos, hd₁Invpos,
        hd₂pos, hd₂Invpos, hd₁, hd₂, rfl⟩
    exact
      problem7_10e_scaled_oneKappa_ge_perron_of_abs_inverse_transpose_positive_eigenvector
        hn A A_inv ρ y hy heigT d₁ d₁Inv d₂ d₂Inv
        hd₁pos hd₁Invpos hd₂pos hd₂Invpos hd₁ hd₂
  have hle_sInf : ρ ≤ sInf S := le_csInf hS_nonempty hlower
  let d₁ : Fin n → ℝ :=
    fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i
  let d₁Inv : Fin n → ℝ :=
    fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹
  let d₂ : Fin n → ℝ := fun i => (y i)⁻¹
  let d₂Inv : Fin n → ℝ := y
  have hcan :
      ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv = ρ := by
    dsimp [d₁, d₁Inv, d₂, d₂Inv]
    exact
      problem7_10e_canonical_scaled_oneKappa_eq_perron_of_abs_inverse_transpose_positive_eigenvector
        hn A A_inv ρ y hy hCt_y heigT
  have hmem :
      ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv ∈ S := by
    dsimp [S, d₁, d₁Inv, d₂, d₂Inv]
    exact ch7TwoSidedScaledOneKappa_mem_set A A_inv
      (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
      (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
      (fun i => (y i)⁻¹) y
      hCt_y
      (fun i => inv_pos.mpr (hCt_y i))
      (fun i => inv_pos.mpr (hy i))
      hy
      (fun i => mul_inv_cancel₀ (ne_of_gt (hCt_y i)))
      (fun i => inv_mul_cancel₀ (ne_of_gt (hy i)))
  have hρmem : ρ ∈ S := by
    simpa [hcan] using hmem
  have hsInf_le : sInf S ≤ ρ := csInf_le hS_bdd hρmem
  exact le_antisymm hsInf_le hle_sInf

/-- Problem 7.10(e), genuine minimum-attainment form for the positive
two-sided `κ₁` value set, under the same explicit transpose-side
Perron-vector certificate as the `sInf` wrapper. -/
theorem problem7_10e_scaledOneKappaSet_isLeast_perron_of_abs_inverse_transpose_positive_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hy : ∀ i : Fin n, 0 < y i)
    (hCt_y : ∀ i : Fin n, 0 < matMulVec n (matTranspose (absMatrix n A_inv)) y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    IsLeast (ch7TwoSidedScaledOneKappaSet A A_inv) ρ := by
  constructor
  · let d₁ : Fin n → ℝ :=
      fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i
    let d₁Inv : Fin n → ℝ :=
      fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹
    let d₂ : Fin n → ℝ := fun i => (y i)⁻¹
    let d₂Inv : Fin n → ℝ := y
    have hcan :
        ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv = ρ := by
      dsimp [d₁, d₁Inv, d₂, d₂Inv]
      exact
        problem7_10e_canonical_scaled_oneKappa_eq_perron_of_abs_inverse_transpose_positive_eigenvector
          hn A A_inv ρ y hy hCt_y heigT
    have hmem :
        ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv ∈
          ch7TwoSidedScaledOneKappaSet A A_inv := by
      dsimp [d₁, d₁Inv, d₂, d₂Inv]
      exact ch7TwoSidedScaledOneKappa_mem_set A A_inv
        (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
        (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
        (fun i => (y i)⁻¹) y
        hCt_y
        (fun i => inv_pos.mpr (hCt_y i))
        (fun i => inv_pos.mpr (hy i))
        hy
        (fun i => mul_inv_cancel₀ (ne_of_gt (hCt_y i)))
        (fun i => inv_mul_cancel₀ (ne_of_gt (hy i)))
    simpa [hcan] using hmem
  · intro κ hκ
    rcases hκ with
      ⟨d₁, d₁Inv, d₂, d₂Inv, hd₁pos, hd₁Invpos,
        hd₂pos, hd₂Invpos, hd₁, hd₂, rfl⟩
    exact
      problem7_10e_scaled_oneKappa_ge_perron_of_abs_inverse_transpose_positive_eigenvector
        hn A A_inv ρ y hy heigT d₁ d₁Inv d₂ d₂Inv
        hd₁pos hd₁Invpos hd₂pos hd₂Invpos hd₁ hd₂

/-- Problem 7.10(e), positive-entry one-norm `sInf` wrapper at the strength of
an explicit transpose-side positive Perron-vector certificate. -/
theorem problem7_10e_positive_abs_entries_scaledOneKappaSet_sInf_eq_perron_of_transpose
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (_hApos : ∀ i j : Fin n, 0 < absMatrix n A i j)
    (hAinvpos : ∀ i j : Fin n, 0 < absMatrix n A_inv i j)
    (hy : ∀ i : Fin n, 0 < y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    sInf (ch7TwoSidedScaledOneKappaSet A A_inv) = ρ :=
  problem7_10e_scaledOneKappaSet_sInf_eq_perron_of_abs_inverse_transpose_positive_eigenvector
    hn A A_inv ρ y hy
    (ch7_bauer_Cx_pos_of_pos hn
      (matTranspose (absMatrix n A_inv)) y (fun i j => hAinvpos j i) hy)
    heigT

/-- Problem 7.10(e), positive-entry one-norm minimum-attainment wrapper at the
strength of an explicit transpose-side positive Perron-vector certificate. -/
theorem problem7_10e_positive_abs_entries_scaledOneKappaSet_isLeast_perron_of_transpose
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (_hApos : ∀ i j : Fin n, 0 < absMatrix n A i j)
    (hAinvpos : ∀ i j : Fin n, 0 < absMatrix n A_inv i j)
    (hy : ∀ i : Fin n, 0 < y i)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    IsLeast (ch7TwoSidedScaledOneKappaSet A A_inv) ρ :=
  problem7_10e_scaledOneKappaSet_isLeast_perron_of_abs_inverse_transpose_positive_eigenvector
    hn A A_inv ρ y hy
    (ch7_bauer_Cx_pos_of_pos hn
      (matTranspose (absMatrix n A_inv)) y (fun i j => hAinvpos j i) hy)
    heigT

/-- Problem 7.10(e), canonical one-norm Bauer scaling from source-shaped
irreducible transpose products and a nonzero nonnegative right-eigenvector
certificate for `BᵀCᵀ`.  Strict positivity of the transpose-side Perron vector
and of `Cᵀy` is proved from irreducibility rather than assumed. -/
theorem problem7_10e_irreducible_transpose_products_canonical_scaled_oneCond_eq_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hC : ∀ i j : Fin n, 0 ≤ C i j)
    (hBTCT : Matrix.IsIrreducible
      (Matrix.of (matMul n (matTranspose B) (matTranspose C)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCTBT : Matrix.IsIrreducible
      (Matrix.of (matMul n (matTranspose C) (matTranspose B)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n (matMul n (matTranspose B) (matTranspose C)) y i = ρ * y i) :
    ch7TwoSidedScaledOneCond B C
        (fun i => matMulVec n (matTranspose C) y i)
        (fun i => (matMulVec n (matTranspose C) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ := by
  have hy : ∀ i : Fin n, 0 < y i :=
    ch7_nonzero_nonneg_irreducible_right_eigenvector_pos
      (matMul n (matTranspose B) (matTranspose C)) ρ y
      hBTCT hy_nonneg hy_ne heigT
  have hCt_y : ∀ i : Fin n, 0 < matMulVec n (matTranspose C) y i :=
    problem7_10d_Cx_pos_of_irreducible_BC_CB hn
      (matTranspose B) (matTranspose C) ρ y
      (by intro i j; exact hB j i)
      (by intro i j; exact hC j i)
      hBTCT hCTBT hy heigT
  exact problem7_10e_canonical_scaled_oneCond_eq_perron_of_transpose_positive_eigenvector
    hn B C ρ y hB hC hy hCt_y heigT

/-- Problem 7.10(e), canonical one-norm scaled-`κ` equality for
`B = |A|`, `C = |A⁻¹|` from irreducible transpose products and a nonzero
nonnegative right-eigenvector certificate for `|A|ᵀ|A⁻¹|ᵀ`.  This removes the
explicit strict-positivity hypotheses from the positive-certificate wrapper;
Perron-Frobenius existence and spectral-radius identification remain separate
source-facing targets. -/
theorem problem7_10e_irreducible_transpose_products_canonical_scaled_oneKappa_eq_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBTCT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv))) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCTBT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A_inv))
          (matTranspose (absMatrix n A))) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    ch7TwoSidedScaledOneKappa A A_inv
        (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
        (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ := by
  have hy : ∀ i : Fin n, 0 < y i :=
    ch7_nonzero_nonneg_irreducible_right_eigenvector_pos
      (matMul n (matTranspose (absMatrix n A))
        (matTranspose (absMatrix n A_inv))) ρ y
      hBTCT hy_nonneg hy_ne heigT
  have hCt_y :
      ∀ i : Fin n,
        0 < matMulVec n (matTranspose (absMatrix n A_inv)) y i :=
    problem7_10d_Cx_pos_of_irreducible_BC_CB hn
      (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv)) ρ y
      (by intro i j; exact abs_nonneg _)
      (by intro i j; exact abs_nonneg _)
      hBTCT hCTBT hy heigT
  exact
    problem7_10e_canonical_scaled_oneKappa_eq_perron_of_abs_inverse_transpose_positive_eigenvector
      hn A A_inv ρ y hy hCt_y heigT

/-- Problem 7.10(e), one-norm scaled-`κ` value-set equality from irreducible
transpose products and a nonzero nonnegative right-eigenvector certificate for
`|A|ᵀ|A⁻¹|ᵀ`.  This is the one-norm analogue of the irreducible infinity-norm
Bauer wrapper; the remaining full Theorem 7.8 work is Perron-Frobenius
existence, spectral-radius identification, and compatible common scaling for
the operator-2 interpolation step. -/
theorem problem7_10e_irreducible_transpose_products_scaledOneKappaSet_sInf_eq_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBTCT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv))) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCTBT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A_inv))
          (matTranspose (absMatrix n A))) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    sInf (ch7TwoSidedScaledOneKappaSet A A_inv) = ρ := by
  have hy : ∀ i : Fin n, 0 < y i :=
    ch7_nonzero_nonneg_irreducible_right_eigenvector_pos
      (matMul n (matTranspose (absMatrix n A))
        (matTranspose (absMatrix n A_inv))) ρ y
      hBTCT hy_nonneg hy_ne heigT
  have hCt_y :
      ∀ i : Fin n,
        0 < matMulVec n (matTranspose (absMatrix n A_inv)) y i :=
    problem7_10d_Cx_pos_of_irreducible_BC_CB hn
      (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv)) ρ y
      (by intro i j; exact abs_nonneg _)
      (by intro i j; exact abs_nonneg _)
      hBTCT hCTBT hy heigT
  exact
    problem7_10e_scaledOneKappaSet_sInf_eq_perron_of_abs_inverse_transpose_positive_eigenvector
      hn A A_inv ρ y hy hCt_y heigT

/-- Problem 7.10(e), one-norm scaled-`κ` minimum-attainment wrapper from
irreducible transpose products and a nonzero nonnegative right-eigenvector
certificate for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_irreducible_transpose_products_scaledOneKappaSet_isLeast_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBTCT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv))) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCTBT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A_inv))
          (matTranspose (absMatrix n A))) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    IsLeast (ch7TwoSidedScaledOneKappaSet A A_inv) ρ := by
  have hy : ∀ i : Fin n, 0 < y i :=
    ch7_nonzero_nonneg_irreducible_right_eigenvector_pos
      (matMul n (matTranspose (absMatrix n A))
        (matTranspose (absMatrix n A_inv))) ρ y
      hBTCT hy_nonneg hy_ne heigT
  have hCt_y :
      ∀ i : Fin n,
        0 < matMulVec n (matTranspose (absMatrix n A_inv)) y i :=
    problem7_10d_Cx_pos_of_irreducible_BC_CB hn
      (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv)) ρ y
      (by intro i j; exact abs_nonneg _)
      (by intro i j; exact abs_nonneg _)
      hBTCT hCTBT hy heigT
  exact
    problem7_10e_scaledOneKappaSet_isLeast_perron_of_abs_inverse_transpose_positive_eigenvector
      hn A A_inv ρ y hy hCt_y heigT

/-- The repository function-level transpose agrees with Mathlib matrix
transpose after coercing through `Matrix.of`. -/
lemma ch7_matrix_of_matTranspose {n : ℕ} (M : Fin n → Fin n → ℝ) :
    (Matrix.of (matTranspose M) : Matrix (Fin n) (Fin n) ℝ) =
      (Matrix.of M : Matrix (Fin n) (Fin n) ℝ).transpose := by
  ext i j
  rfl

/-- Irreducibility is preserved by the repository function-level transpose. -/
lemma ch7_irreducible_matTranspose {n : ℕ} (M : Fin n → Fin n → ℝ)
    (hM : Matrix.IsIrreducible
      (Matrix.of M : Matrix (Fin n) (Fin n) ℝ)) :
    Matrix.IsIrreducible
      (Matrix.of (matTranspose M) : Matrix (Fin n) (Fin n) ℝ) := by
  rw [ch7_matrix_of_matTranspose M]
  exact Matrix.IsIrreducible.transpose hM

/-- Source-product irreducibility for `BC` supplies transpose-product
irreducibility for `CᵀBᵀ`. -/
lemma ch7_irreducible_transpose_product_of_irreducible_product {n : ℕ}
    (B C : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n B C) : Matrix (Fin n) (Fin n) ℝ)) :
    Matrix.IsIrreducible
      (Matrix.of (matMul n (matTranspose C) (matTranspose B)) :
        Matrix (Fin n) (Fin n) ℝ) := by
  have hT := ch7_irreducible_matTranspose (matMul n B C) hBC
  simpa [matTranspose_matMul] using hT

/-- Problem 7.10(e), source spectral-radius fact
`ρ((|A⁻¹||A|)ᵀ) = ρ(|A⁻¹||A|)` for the transpose product used by the
one-norm branch. -/
theorem problem7_10e_abs_transpose_product_toLin_spectralRadius_eq_reverse_abs_product
    {n : ℕ} (A A_inv : Fin n → Fin n → ℝ) :
    spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from
            realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv))))) =
      spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from
            realRectToCMatrix
              (matMul n (absMatrix n A_inv) (absMatrix n A)))) := by
  have hprod :
      matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv)) =
        matTranspose (matMul n (absMatrix n A_inv) (absMatrix n A)) := by
    rw [matTranspose_matMul]
  rw [hprod]
  exact ch7_toLin_spectralRadius_realRectToCMatrix_matTranspose_eq
    (matMul n (absMatrix n A_inv) (absMatrix n A))

/-- Problem 7.10(e), source spectral-radius fact
`ρ(|A||A⁻¹|) = ρ(|A⁻¹||A|)`.  This closes the algebraic product-order
spectral-radius dependency used to put the one-norm and infinity-norm Bauer
branches at the same scalar value; Perron-Frobenius eigenpair existence remains
separate. -/
theorem problem7_10e_abs_products_toLin_spectralRadius_mul_comm_eq {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ) :
    spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from
            realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv)))) =
      spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from
            realRectToCMatrix
              (matMul n (absMatrix n A_inv) (absMatrix n A)))) := by
  let B : Matrix (Fin n) (Fin n) ℂ :=
    (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix (absMatrix n A))
  let C : Matrix (Fin n) (Fin n) ℂ :=
    (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix (absMatrix n A_inv))
  have hBC :
      (show Matrix (Fin n) (Fin n) ℂ from
        realRectToCMatrix (matMul n (absMatrix n A) (absMatrix n A_inv))) =
        B * C := by
    ext i j
    simp [B, C, realRectToCMatrix, matMul, Matrix.mul_apply]
  have hCB :
      (show Matrix (Fin n) (Fin n) ℂ from
        realRectToCMatrix (matMul n (absMatrix n A_inv) (absMatrix n A))) =
        C * B := by
    ext i j
    simp [B, C, realRectToCMatrix, matMul, Matrix.mul_apply]
  rw [hBC, hCB]
  exact ch7_toLin_spectralRadius_mul_comm_eq B C

/-- Problem 7.10(e), combined source spectral-radius fact for the one-norm
transpose product: `ρ(|A|ᵀ|A⁻¹|ᵀ) = ρ(|A||A⁻¹|)`.  This packages
`ρ(Aᵀ)=ρ(A)` and `ρ(AB)=ρ(BA)` in the exact absolute-product orientation used
by Bauer's infinity-norm branch. -/
theorem problem7_10e_abs_transpose_product_toLin_spectralRadius_eq_abs_product
    {n : ℕ} (A A_inv : Fin n → Fin n → ℝ) :
    spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from
            realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv))))) =
      spectralRadius ℂ
        (Matrix.toLin'
          (show Matrix (Fin n) (Fin n) ℂ from
            realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv)))) := by
  rw [problem7_10e_abs_transpose_product_toLin_spectralRadius_eq_reverse_abs_product
    A A_inv]
  exact (problem7_10e_abs_products_toLin_spectralRadius_mul_comm_eq
    A A_inv).symm

/-- Problem 7.10(a), finite complex spectral-radius eigenpair for the product
`BC`.  This supplies the algebraic complex eigenpair at `ρ(BC)`; Bauer's
Perron-Frobenius row still needs the source positive real Perron vector under
irreducibility. -/
theorem problem7_10a_product_toLin_exists_spectralRadius_attaining_eigenpair
    {n : ℕ} (hn : 0 < n) (B C : Fin n → Fin n → ℝ) :
    ∃ lam : ℂ, ∃ z : CVec n,
      z ≠ 0 ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix (matMul n B C)) z i =
          lam * z i) ∧
      (nnnorm lam : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix (matMul n B C))) := by
  let M : Matrix (Fin n) (Fin n) ℂ :=
    (show Matrix (Fin n) (Fin n) ℂ from realRectToCMatrix (matMul n B C))
  obtain ⟨lam, z, hz_ne, hz_lin, hrad⟩ :=
    ch7_toLin_exists_spectralRadius_attaining_eigenpair hn M
  refine ⟨lam, z, hz_ne, ?_, by simpa [M] using hrad⟩
  intro i
  have hi := congrFun hz_lin i
  simpa [M, complexMatrixVecMul, Matrix.toLin'_apply, Matrix.mulVec,
    dotProduct, Pi.smul_apply, smul_eq_mul] using hi

/-- Problem 7.10(b), finite complex spectral-radius eigenpair for
`|A||A⁻¹|`.  This closes the algebraic complex eigenpair existence dependency
for the infinity-norm Bauer product, without asserting Perron positivity. -/
theorem problem7_10b_abs_product_toLin_exists_spectralRadius_attaining_eigenpair
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ) :
    ∃ lam : ℂ, ∃ z : CVec n,
      z ≠ 0 ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          lam * z i) ∧
      (nnnorm lam : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) := by
  exact problem7_10a_product_toLin_exists_spectralRadius_attaining_eigenpair
    hn (absMatrix n A) (absMatrix n A_inv)

/-- Problem 7.10(e), finite complex spectral-radius eigenpair for the
transpose one-norm product `|A|ᵀ|A⁻¹|ᵀ`.  Together with the preceding
product/transpose equalities, this isolates the remaining Perron-Frobenius
positivity/common-scaling obligation. -/
theorem problem7_10e_abs_transpose_product_toLin_exists_spectralRadius_attaining_eigenpair
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ) :
    ∃ lam : ℂ, ∃ z : CVec n,
      z ≠ 0 ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv)))) z i =
          lam * z i) ∧
      (nnnorm lam : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (matTranspose (absMatrix n A))
                  (matTranspose (absMatrix n A_inv))))) := by
  exact problem7_10a_product_toLin_exists_spectralRadius_attaining_eigenpair
    hn (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv))

/-- Problem 7.10(a), product form of the absolute-value Perron-Frobenius
intermediate: the nonnegative product `BC` has a spectral-radius-attaining
complex eigenpair whose componentwise modulus vector is a nonzero nonnegative
subeigenvector. -/
theorem problem7_10a_product_exists_spectralRadius_attaining_nonneg_subeigenvector
    {n : ℕ} (hn : 0 < n) (B C : Fin n → Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hC : ∀ i j : Fin n, 0 ≤ C i j) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix (matMul n B C)) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix (matMul n B C))) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n (matMul n B C) x i) := by
  exact ch7_exists_spectralRadius_attaining_nonneg_subeigenvector hn
    (matMul n B C)
    (by
      intro i j
      unfold matMul
      exact Finset.sum_nonneg fun k _ => mul_nonneg (hB i k) (hC k j))

/-- Problem 7.10(b), infinity-norm Bauer product form of the absolute-value
Perron-Frobenius intermediate for `|A||A⁻¹|`. -/
theorem problem7_10b_abs_product_exists_spectralRadius_attaining_nonneg_subeigenvector
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) x i) := by
  exact problem7_10a_product_exists_spectralRadius_attaining_nonneg_subeigenvector
    hn (absMatrix n A) (absMatrix n A_inv)
    (by intro i j; exact abs_nonneg _)
    (by intro i j; exact abs_nonneg _)

/-- Problem 7.10(e), one-norm transpose-product form of the absolute-value
Perron-Frobenius intermediate for `|A|ᵀ|A⁻¹|ᵀ`. -/
theorem problem7_10e_abs_transpose_product_exists_spectralRadius_attaining_nonneg_subeigenvector
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv)))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (matTranspose (absMatrix n A))
                  (matTranspose (absMatrix n A_inv))))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) x i) := by
  exact problem7_10a_product_exists_spectralRadius_attaining_nonneg_subeigenvector
    hn (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv))
    (by intro i j; exact abs_nonneg _)
    (by intro i j; exact abs_nonneg _)

/-- Problem 7.10(a), product form of the positive subeigenvector
Perron-Frobenius intermediate: if `BC` is irreducible, the
spectral-radius-attaining nonnegative subeigenvector can be upgraded to a
strictly positive subeigenvector.  This still does not assert equality
`BC y = ρ y`. -/
theorem problem7_10a_product_exists_spectralRadius_attaining_positive_subeigenvector
    {n : ℕ} (hn : 0 < n) (B C : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n B C) : Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix (matMul n B C)) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix (matMul n B C))) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n (matMul n B C) x i) ∧
      (∀ i : Fin n, ‖mu‖ * y i ≤ matMulVec n (matMul n B C) y i) := by
  exact ch7_exists_spectralRadius_attaining_positive_subeigenvector
    hn (matMul n B C) hBC

/-- Problem 7.10(a), product form of the Perron-Frobenius closure: if `BC`
is irreducible, the spectral-radius-attaining complex eigenpair yields a
strictly positive real right eigenvector at `|μ|`. -/
theorem problem7_10a_product_exists_spectralRadius_attaining_positive_eigenvector
    {n : ℕ} (hn : 0 < n) (B C : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n B C) : Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix (matMul n B C)) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix (matMul n B C))) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n (matMul n B C) x i) ∧
      (∀ i : Fin n, matMulVec n (matMul n B C) y i = ‖mu‖ * y i) := by
  exact ch7_exists_spectralRadius_attaining_positive_eigenvector
    hn (matMul n B C) hBC

/-- Problem 7.10(a), full source-shaped infinity-norm Bauer equality from
irreducible products, now producing the Perron vector from irreducibility
rather than assuming it as a certificate. -/
theorem problem7_10a_irreducible_products_scaledInfCondSet_sInf_eq_spectralRadius
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hC : ∀ i j : Fin n, 0 ≤ C i j)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n B C) : Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n C B) : Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix (matMul n B C)) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix (matMul n B C))) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n (matMul n B C) x i) ∧
      (∀ i : Fin n, matMulVec n (matMul n B C) y i = ‖mu‖ * y i) ∧
      sInf (ch7TwoSidedScaledInfCondSet B C) = ‖mu‖ := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real⟩ :=
    problem7_10a_product_exists_spectralRadius_attaining_positive_eigenvector
      hn B C hBC
  have hsInf :
      sInf (ch7TwoSidedScaledInfCondSet B C) = ‖mu‖ :=
    problem7_10a_irreducible_products_scaledInfCondSet_sInf_eq_perron_of_nonzero_nonneg_eigenvector
      hn B C ‖mu‖ y hB hC hBC hCB
      (fun i => le_of_lt (hy_pos i))
      (by
        intro hy_zero
        let i0 : Fin n := ⟨0, hn⟩
        have : y i0 = 0 := by simp [hy_zero]
        linarith [hy_pos i0])
      heig_real
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real, hsInf⟩

/-- Problem 7.10(b), positive Perron vector for `|A||A⁻¹|` from
irreducibility of the absolute product. -/
theorem problem7_10b_abs_product_exists_spectralRadius_attaining_positive_eigenvector
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) x i) ∧
      (∀ i : Fin n,
        matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) y i =
          ‖mu‖ * y i) := by
  exact problem7_10a_product_exists_spectralRadius_attaining_positive_eigenvector
    hn (absMatrix n A) (absMatrix n A_inv) hBC

/-- Problem 7.10(b) / Theorem 7.8, infinity-norm two-sided condition-product
infimum equals the spectral-radius witness supplied by irreducibility. -/
theorem problem7_10b_irreducible_products_scaledInfKappaSet_sInf_eq_spectralRadius
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) x i) ∧
      (∀ i : Fin n,
        matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) y i =
          ‖mu‖ * y i) ∧
      sInf (ch7TwoSidedScaledInfKappaSet A A_inv) = ‖mu‖ := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real⟩ :=
    problem7_10b_abs_product_exists_spectralRadius_attaining_positive_eigenvector
      hn A A_inv hBC
  have hsInf :
      sInf (ch7TwoSidedScaledInfKappaSet A A_inv) = ‖mu‖ :=
    problem7_10b_irreducible_products_scaledInfKappaSet_sInf_eq_perron
      hn A A_inv ‖mu‖ y hBC hCB hy_pos heig_real
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real, hsInf⟩

/-- Problem 7.10(b) / **Theorem 7.8 (Bauer)**, genuine attained-minimum form.
Under the source irreducibility hypotheses, Perron--Frobenius supplies a
positive right eigenvector at the spectral radius.  The canonical diagonal
scalings built from that vector therefore attain the least value of the
two-sided scaled infinity-norm condition numbers. -/
theorem problem7_10b_irreducible_products_scaledInfKappaSet_isLeast_spectralRadius
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) x i) ∧
      (∀ i : Fin n,
        matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) y i =
          ‖mu‖ * y i) ∧
      IsLeast (ch7TwoSidedScaledInfKappaSet A A_inv) ‖mu‖ := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real⟩ :=
    problem7_10b_abs_product_exists_spectralRadius_attaining_positive_eigenvector
      hn A A_inv hBC
  have hleast :
      IsLeast (ch7TwoSidedScaledInfKappaSet A A_inv) ‖mu‖ :=
    problem7_10b_irreducible_products_scaledInfKappaSet_isLeast_perron
      hn A A_inv ‖mu‖ y hBC hCB hy_pos heig_real
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real, hleast⟩

/-- Problem 7.10(e), transpose-side positive Perron vector for
`|A|ᵀ|A⁻¹|ᵀ` from irreducibility of `|A⁻¹||A|`. -/
theorem problem7_10e_abs_transpose_product_exists_spectralRadius_attaining_positive_eigenvector
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ)
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv)))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (matTranspose (absMatrix n A))
                  (matTranspose (absMatrix n A_inv))))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) x i) ∧
      (∀ i : Fin n,
        matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) y i =
          ‖mu‖ * y i) := by
  have hT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv))) :
          Matrix (Fin n) (Fin n) ℝ) :=
    ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A_inv) (absMatrix n A) hCB
  exact problem7_10a_product_exists_spectralRadius_attaining_positive_eigenvector
    hn (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv)) hT

/-- Problem 7.10(e), one-norm two-sided condition-product infimum equals the
transpose-side spectral-radius witness supplied by irreducibility. -/
theorem problem7_10e_irreducible_products_scaledOneKappaSet_sInf_eq_spectralRadius
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv)))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (matTranspose (absMatrix n A))
                  (matTranspose (absMatrix n A_inv))))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) x i) ∧
      (∀ i : Fin n,
        matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) y i =
          ‖mu‖ * y i) ∧
      sInf (ch7TwoSidedScaledOneKappaSet A A_inv) = ‖mu‖ := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real⟩ :=
    problem7_10e_abs_transpose_product_exists_spectralRadius_attaining_positive_eigenvector
      hn A A_inv hCB
  have hy_nonneg : ∀ i : Fin n, 0 ≤ y i := fun i => le_of_lt (hy_pos i)
  have hy_ne : y ≠ 0 := by
    intro hy_zero
    let i0 : Fin n := ⟨0, hn⟩
    have : y i0 = 0 := by simp [hy_zero]
    linarith [hy_pos i0]
  have hsInf :
      sInf (ch7TwoSidedScaledOneKappaSet A A_inv) = ‖mu‖ :=
    problem7_10e_irreducible_transpose_products_scaledOneKappaSet_sInf_eq_perron_of_nonzero_nonneg_eigenvector
      hn A A_inv ‖mu‖ y
      (ch7_irreducible_transpose_product_of_irreducible_product
        (absMatrix n A_inv) (absMatrix n A) hCB)
      (ch7_irreducible_transpose_product_of_irreducible_product
        (absMatrix n A) (absMatrix n A_inv) hBC)
      hy_nonneg hy_ne heig_real
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, heig_real, hsInf⟩

/-- Problem 7.10(a), source lower-bound half obtained from irreducibility:
there is a spectral-radius-attaining complex eigenvalue whose induced positive
real subeigenvector proves `ρ(BC) ≤ inf_D₁,D₂ ||D₁BD₂||∞ ||D₂⁻¹CD₁⁻¹||∞`.
This is not the full Bauer equality, since the canonical attaining scaling
still needs a genuine real Perron eigenvector equation. -/
theorem problem7_10a_irreducible_product_scaledInfCondSet_sInf_ge_spectralRadius
    {n : ℕ} (hn : 0 < n) (B C : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n B C) : Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul (realRectToCMatrix (matMul n B C)) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix (matMul n B C))) ∧
      (∀ i : Fin n, ‖mu‖ * x i ≤ matMulVec n (matMul n B C) x i) ∧
      (∀ i : Fin n, ‖mu‖ * y i ≤ matMulVec n (matMul n B C) y i) ∧
      ‖mu‖ ≤ sInf (ch7TwoSidedScaledInfCondSet B C) := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby⟩ :=
    problem7_10a_product_exists_spectralRadius_attaining_positive_subeigenvector
      hn B C hBC
  have hlower :
      ‖mu‖ ≤ sInf (ch7TwoSidedScaledInfCondSet B C) :=
    problem7_10a_scaledInfCondSet_sInf_ge_perron_of_positive_subeigenvector
      hn B C ‖mu‖ y (norm_nonneg mu) hy_pos hsuby
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby, hlower⟩

/-- Problem 7.10(b), infinity-norm Bauer product form of the positive
subeigenvector Perron-Frobenius intermediate for `|A||A⁻¹|`. -/
theorem problem7_10b_abs_product_exists_spectralRadius_attaining_positive_subeigenvector
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) x i) ∧
      (∀ i : Fin n,
        ‖mu‖ * y i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) y i) := by
  exact problem7_10a_product_exists_spectralRadius_attaining_positive_subeigenvector
    hn (absMatrix n A) (absMatrix n A_inv) hBC

/-- Problem 7.10(b) / Theorem 7.8, infinity-norm lower-bound half obtained
from irreducibility of `|A||A⁻¹|`: a spectral-radius-attaining complex
eigenvalue supplies a positive subeigenvector proving
`ρ(|A||A⁻¹|) ≤ inf_D₁,D₂ κ∞(D₁AD₂)`.  Full equality/minimum attainment still
requires the real Perron eigenvector equation. -/
theorem problem7_10b_irreducible_abs_product_scaledInfKappaSet_sInf_ge_spectralRadius
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (absMatrix n A) (absMatrix n A_inv))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (absMatrix n A) (absMatrix n A_inv)))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) x i) ∧
      (∀ i : Fin n,
        ‖mu‖ * y i ≤
          matMulVec n (matMul n (absMatrix n A) (absMatrix n A_inv)) y i) ∧
      ‖mu‖ ≤ sInf (ch7TwoSidedScaledInfKappaSet A A_inv) := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby⟩ :=
    problem7_10b_abs_product_exists_spectralRadius_attaining_positive_subeigenvector
      hn A A_inv hBC
  have hlower :
      ‖mu‖ ≤ sInf (ch7TwoSidedScaledInfKappaSet A A_inv) :=
    problem7_10b_scaledInfKappaSet_sInf_ge_perron_of_abs_inverse_positive_subeigenvector
      hn A A_inv ‖mu‖ y (norm_nonneg mu) hy_pos hsuby
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby, hlower⟩

/-- Problem 7.10(e), one-norm transpose-product form of the positive
subeigenvector Perron-Frobenius intermediate for `|A|ᵀ|A⁻¹|ᵀ`, using the
source irreducibility of `|A⁻¹||A|` transported by transpose. -/
theorem problem7_10e_abs_transpose_product_exists_spectralRadius_attaining_positive_subeigenvector
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ)
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv)))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (matTranspose (absMatrix n A))
                  (matTranspose (absMatrix n A_inv))))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) x i) ∧
      (∀ i : Fin n,
        ‖mu‖ * y i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) y i) := by
  have hT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv))) :
          Matrix (Fin n) (Fin n) ℝ) :=
    ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A_inv) (absMatrix n A) hCB
  exact problem7_10a_product_exists_spectralRadius_attaining_positive_subeigenvector
    hn (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv)) hT

/-- Problem 7.10(e), one-norm lower-bound half obtained from irreducibility
of `|A⁻¹||A|`: a spectral-radius-attaining complex eigenvalue for
`|A|ᵀ|A⁻¹|ᵀ` supplies a positive subeigenvector proving the lower bound for
the one-norm two-sided condition-product infimum.  The common attaining
scaling needed for the full two-norm Bauer theorem remains the Perron
eigenvector/equality gate. -/
theorem problem7_10e_irreducible_abs_transpose_product_scaledOneKappaSet_sInf_ge_spectralRadius
    {n : ℕ} (hn : 0 < n) (A A_inv : Fin n → Fin n → ℝ)
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ)) :
    ∃ mu : ℂ, ∃ z : CVec n, ∃ x : Fin n → ℝ, ∃ y : Fin n → ℝ,
      z ≠ 0 ∧ x ≠ 0 ∧ (∀ i : Fin n, 0 ≤ x i) ∧
      (∀ i : Fin n, 0 < y i) ∧
      (∀ i : Fin n,
        complexMatrixVecMul
            (realRectToCMatrix
              (matMul n (matTranspose (absMatrix n A))
                (matTranspose (absMatrix n A_inv)))) z i =
          mu * z i) ∧
      (nnnorm mu : ENNReal) =
        spectralRadius ℂ
          (Matrix.toLin'
            (show Matrix (Fin n) (Fin n) ℂ from
              realRectToCMatrix
                (matMul n (matTranspose (absMatrix n A))
                  (matTranspose (absMatrix n A_inv))))) ∧
      (∀ i : Fin n,
        ‖mu‖ * x i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) x i) ∧
      (∀ i : Fin n,
        ‖mu‖ * y i ≤
          matMulVec n
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))) y i) ∧
      ‖mu‖ ≤ sInf (ch7TwoSidedScaledOneKappaSet A A_inv) := by
  obtain ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby⟩ :=
    problem7_10e_abs_transpose_product_exists_spectralRadius_attaining_positive_subeigenvector
      hn A A_inv hCB
  have hlower :
      ‖mu‖ ≤ sInf (ch7TwoSidedScaledOneKappaSet A A_inv) :=
    problem7_10e_scaledOneKappaSet_sInf_ge_perron_of_abs_inverse_transpose_positive_subeigenvector
      hn A A_inv ‖mu‖ y (norm_nonneg mu) hy_pos hsuby
  exact ⟨mu, z, x, y, hz_ne, hx_ne, hx_nonneg, hy_pos, heig, hrad,
    hsubx, hsuby, hlower⟩

/-- Problem 7.10(e), one-norm transpose-product Mathlib `spectralRadius`
form for `|A|ᵀ|A⁻¹|ᵀ`.  Irreducibility is taken in the source orientation
`|A⁻¹||A|` and transported through transpose; the theorem still assumes the
nonzero nonnegative transpose-side eigenpair rather than proving its
Perron-Frobenius existence. -/
theorem problem7_10e_abs_transpose_product_toLin_spectralRadius_eq_of_irreducible_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    spectralRadius ℂ
      (Matrix.toLin'
        (show Matrix (Fin n) (Fin n) ℂ from
          realRectToCMatrix
            (matMul n (matTranspose (absMatrix n A))
              (matTranspose (absMatrix n A_inv))))) =
      ENNReal.ofReal ρ := by
  have hT : Matrix.IsIrreducible
      (Matrix.of
        (matMul n (matTranspose (absMatrix n A))
          (matTranspose (absMatrix n A_inv))) :
          Matrix (Fin n) (Fin n) ℝ) :=
    ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A_inv) (absMatrix n A) hCB
  have hy : ∀ i : Fin n, 0 < y i :=
    ch7_nonzero_nonneg_irreducible_right_eigenvector_pos
      (matMul n (matTranspose (absMatrix n A))
        (matTranspose (absMatrix n A_inv))) ρ y
      hT hy_nonneg hy_ne heigT
  exact problem7_10a_product_toLin_spectralRadius_eq_of_positive_eigenvector
    hn (matTranspose (absMatrix n A)) (matTranspose (absMatrix n A_inv))
    ρ y
    (by intro i j; exact abs_nonneg _)
    (by intro i j; exact abs_nonneg _)
    hy heigT

/-- Problem 7.10(e), canonical one-norm Bauer scaling from the source-shaped
irreducible original products.  The transpose-product irreducibility needed by
the one-norm reduction is obtained by transposing the two source products. -/
theorem problem7_10e_irreducible_products_canonical_scaled_oneCond_eq_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (B C : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hC : ∀ i j : Fin n, 0 ≤ C i j)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n B C) : Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n C B) : Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n (matMul n (matTranspose B) (matTranspose C)) y i = ρ * y i) :
    ch7TwoSidedScaledOneCond B C
        (fun i => matMulVec n (matTranspose C) y i)
        (fun i => (matMulVec n (matTranspose C) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ :=
  problem7_10e_irreducible_transpose_products_canonical_scaled_oneCond_eq_perron_of_nonzero_nonneg_eigenvector
    hn B C ρ y hB hC
    (ch7_irreducible_transpose_product_of_irreducible_product C B hCB)
    (ch7_irreducible_transpose_product_of_irreducible_product B C hBC)
    hy_nonneg hy_ne heigT

/-- Problem 7.10(e), canonical one-norm scaled-`κ` equality for
`B = |A|`, `C = |A⁻¹|` from the source-shaped irreducible original products.
Perron-vector existence and spectral-radius identification remain the separate
Perron-Frobenius gate for the full Bauer theorem. -/
theorem problem7_10e_irreducible_products_canonical_scaled_oneKappa_eq_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    ch7TwoSidedScaledOneKappa A A_inv
        (fun i => matMulVec n (matTranspose (absMatrix n A_inv)) y i)
        (fun i => (matMulVec n (matTranspose (absMatrix n A_inv)) y i)⁻¹)
        (fun i => (y i)⁻¹) y = ρ :=
  problem7_10e_irreducible_transpose_products_canonical_scaled_oneKappa_eq_perron_of_nonzero_nonneg_eigenvector
    hn A A_inv ρ y
    (ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A_inv) (absMatrix n A) hCB)
    (ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A) (absMatrix n A_inv) hBC)
    hy_nonneg hy_ne heigT

/-- Problem 7.10(e), one-norm scaled-`κ` value-set equality from the
source-shaped irreducible original products and a transpose-side nonzero
nonnegative Perron-vector certificate. -/
theorem problem7_10e_irreducible_products_scaledOneKappaSet_sInf_eq_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    sInf (ch7TwoSidedScaledOneKappaSet A A_inv) = ρ :=
  problem7_10e_irreducible_transpose_products_scaledOneKappaSet_sInf_eq_perron_of_nonzero_nonneg_eigenvector
    hn A A_inv ρ y
    (ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A_inv) (absMatrix n A) hCB)
    (ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A) (absMatrix n A_inv) hBC)
    hy_nonneg hy_ne heigT

/-- Problem 7.10(e), one-norm scaled-`κ` minimum-attainment wrapper from the
source-shaped irreducible original products and a transpose-side nonzero
nonnegative Perron-vector certificate. -/
theorem problem7_10e_irreducible_products_scaledOneKappaSet_isLeast_perron_of_nonzero_nonneg_eigenvector
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ) (ρ : ℝ) (y : Fin n → ℝ)
    (hBC : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A) (absMatrix n A_inv)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hCB : Matrix.IsIrreducible
      (Matrix.of (matMul n (absMatrix n A_inv) (absMatrix n A)) :
        Matrix (Fin n) (Fin n) ℝ))
    (hy_nonneg : ∀ i : Fin n, 0 ≤ y i)
    (hy_ne : y ≠ 0)
    (heigT : ∀ i : Fin n,
      matMulVec n
          (matMul n (matTranspose (absMatrix n A))
            (matTranspose (absMatrix n A_inv))) y i = ρ * y i) :
    IsLeast (ch7TwoSidedScaledOneKappaSet A A_inv) ρ :=
  problem7_10e_irreducible_transpose_products_scaledOneKappaSet_isLeast_perron_of_nonzero_nonneg_eigenvector
    hn A A_inv ρ y
    (ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A_inv) (absMatrix n A) hCB)
    (ch7_irreducible_transpose_product_of_irreducible_product
      (absMatrix n A) (absMatrix n A_inv) hBC)
    hy_nonneg hy_ne heigT

/-- The complexified real matrix one-norm is bounded by the repository real
one-norm.  This bridges the Chapter 6 complex `p`-norm interpolation theorem
to Chapter 7's real condition-number notation. -/
lemma ch7_complexMatrixOneNorm_realRectToCMatrix_le_oneNorm {n : ℕ}
    (A : Fin n → Fin n → ℝ) :
    complexMatrixOneNorm (realRectToCMatrix A) ≤ oneNorm A := by
  apply complexMatrixOneNorm_le_of_col_sum_le (oneNorm_nonneg A)
  intro j
  have hsum :
      (∑ i : Fin n, ‖realRectToCMatrix A i j‖) =
        ∑ i : Fin n, |A i j| := by
    apply Finset.sum_congr rfl
    intro i _hi
    simp [realRectToCMatrix]
  simpa [hsum] using col_sum_le_oneNorm A j

/-- The complexified real matrix infinity-norm is bounded by the repository
real infinity-norm. -/
lemma ch7_complexMatrixInfNorm_realRectToCMatrix_le_infNorm {n : ℕ}
    (A : Fin n → Fin n → ℝ) :
    complexMatrixInfNorm (realRectToCMatrix A) ≤ infNorm A := by
  apply complexMatrixInfNorm_le_of_row_sum_le (infNorm_nonneg A)
  intro i
  have hsum :
      (∑ j : Fin n, ‖realRectToCMatrix A i j‖) =
        ∑ j : Fin n, |A i j| := by
    apply Finset.sum_congr rfl
    intro j _hj
    simp [realRectToCMatrix]
  simpa [hsum] using row_sum_le_infNorm A i

/-- Higham Problem 7.10(e)'s norm-interpolation ingredient:
`||A||₂ <= sqrt(||A||₁ ||A||∞)` for the repository's exact Euclidean
operator norm on a complexified real square matrix. -/
theorem problem7_10e_complexMatrixOp2_realRectToCMatrix_le_sqrt_one_mul_inf
    {n : ℕ} (hn : 0 < n) (A : Fin n → Fin n → ℝ) :
    complexMatrixOp2 (realRectToCMatrix A) ≤
      Real.sqrt (oneNorm A * infNorm A) := by
  let C : CMatrix n n := realRectToCMatrix A
  have hinterp :=
    complexMatrixLpNormOfReal_rieszThorin_one_top
      (m := n) (n := n) hn (p := (2 : ℝ)) (by norm_num) C
  have htwo :
      complexMatrixOp2 C ≤
        complexMatrixOneNorm C ^ (2 : ℝ)⁻¹ *
          complexMatrixInfNorm C ^ (1 - (2 : ℝ)⁻¹) := by
    simpa [complexMatrixLpNormOfReal_two_eq_complexMatrixOp2 hn C] using hinterp
  have hhalf : 1 - (2 : ℝ)⁻¹ = (2 : ℝ)⁻¹ := by norm_num
  rw [hhalf] at htwo
  have hone :
      complexMatrixOneNorm C ^ (2 : ℝ)⁻¹ ≤ oneNorm A ^ (2 : ℝ)⁻¹ := by
    exact Real.rpow_le_rpow (complexMatrixOneNorm_nonneg C)
      (by simpa [C] using ch7_complexMatrixOneNorm_realRectToCMatrix_le_oneNorm A)
      (by norm_num)
  have hinf :
      complexMatrixInfNorm C ^ (2 : ℝ)⁻¹ ≤ infNorm A ^ (2 : ℝ)⁻¹ := by
    exact Real.rpow_le_rpow (complexMatrixInfNorm_nonneg C)
      (by simpa [C] using ch7_complexMatrixInfNorm_realRectToCMatrix_le_infNorm A)
      (by norm_num)
  have hprod :
      complexMatrixOneNorm C ^ (2 : ℝ)⁻¹ *
          complexMatrixInfNorm C ^ (2 : ℝ)⁻¹ ≤
        oneNorm A ^ (2 : ℝ)⁻¹ * infNorm A ^ (2 : ℝ)⁻¹ :=
    mul_le_mul hone hinf
      (Real.rpow_nonneg (complexMatrixInfNorm_nonneg C) _)
      (Real.rpow_nonneg (oneNorm_nonneg A) _)
  have hsqrt :
      oneNorm A ^ (2 : ℝ)⁻¹ * infNorm A ^ (2 : ℝ)⁻¹ =
        Real.sqrt (oneNorm A * infNorm A) := by
    rw [Real.sqrt_eq_rpow, Real.mul_rpow (oneNorm_nonneg A) (infNorm_nonneg A)]
    congr 1 <;> norm_num
  exact htwo.trans (hprod.trans (le_of_eq hsqrt))

/-- Row-wise weighted Cauchy--Schwarz in the zero-safe form needed for
Bauer's operator-2 Schur test. -/
lemma ch7_weighted_row_cauchy_sum_sq {n : ℕ}
    (M : Fin n → Fin n → ℝ) (c x : Fin n → ℝ)
    (hM : ∀ i j : Fin n, 0 ≤ M i j)
    (hc : ∀ j : Fin n, 0 < c j) (i : Fin n) :
    (∑ j : Fin n, M i j * |x j|) ^ 2 ≤
      (∑ j : Fin n, M i j * x j ^ 2 / c j) *
        (∑ j : Fin n, M i j * c j) := by
  have hf : ∀ j ∈ (Finset.univ : Finset (Fin n)),
      0 ≤ M i j * x j ^ 2 / c j := by
    intro j _
    exact div_nonneg (mul_nonneg (hM i j) (sq_nonneg (x j))) (le_of_lt (hc j))
  have hg : ∀ j ∈ (Finset.univ : Finset (Fin n)),
      0 ≤ M i j * c j := by
    intro j _
    exact mul_nonneg (hM i j) (le_of_lt (hc j))
  have ht : ∀ j ∈ (Finset.univ : Finset (Fin n)),
      (M i j * |x j|) ^ 2 = (M i j * x j ^ 2 / c j) * (M i j * c j) := by
    intro j _
    have hcne : c j ≠ 0 := ne_of_gt (hc j)
    field_simp [hcne]
    exact by rw [sq_abs]
  simpa using
    (Finset.sum_sq_le_sum_mul_sum_of_sq_eq_mul
      (s := (Finset.univ : Finset (Fin n))) (r := fun j => M i j * |x j|)
      (f := fun j => M i j * x j ^ 2 / c j)
      (g := fun j => M i j * c j) hf hg ht)

/-- For positive `u,v`, the normalization `u / sqrt(u*v)` sends `v` to
`sqrt(u*v)`. -/
lemma ch7_div_sqrt_mul_mul_eq_sqrt_mul {u v : ℝ} (hu : 0 < u) (hv : 0 < v) :
    u / Real.sqrt (u * v) * v = Real.sqrt (u * v) := by
  have hspos : 0 < Real.sqrt (u * v) := Real.sqrt_pos.2 (mul_pos hu hv)
  have hsqr : Real.sqrt (u * v) ^ 2 = u * v :=
    Real.sq_sqrt (le_of_lt (mul_pos hu hv))
  field_simp [ne_of_gt hspos]
  rw [hsqr]

/-- Symmetric form of `ch7_div_sqrt_mul_mul_eq_sqrt_mul`. -/
lemma ch7_div_sqrt_mul_mul_eq_sqrt_mul_comm {u v : ℝ}
    (hu : 0 < u) (hv : 0 < v) :
    v / Real.sqrt (u * v) * u = Real.sqrt (u * v) := by
  rw [mul_comm u v]
  simpa [mul_comm] using ch7_div_sqrt_mul_mul_eq_sqrt_mul hv hu

/-- Reciprocal product for the positive normalization by `sqrt(u*v)`. -/
lemma ch7_div_sqrt_mul_mul_div_sqrt_eq_one {u v : ℝ}
    (hu : 0 < u) (hv : 0 < v) :
    (u / Real.sqrt (u * v)) * (v / Real.sqrt (u * v)) = 1 := by
  have hspos : 0 < Real.sqrt (u * v) := Real.sqrt_pos.2 (mul_pos hu hv)
  have hsqr : Real.sqrt (u * v) ^ 2 = u * v :=
    Real.sq_sqrt (le_of_lt (mul_pos hu hv))
  field_simp [ne_of_gt hspos]
  rw [hsqr]

/-- Squared Schur-test estimate for nonnegative matrices with positive
left/right weights. -/
lemma ch7_vecNorm2Sq_matMulVec_le_of_schur_bounds {n : ℕ}
    (M : Fin n → Fin n → ℝ) (r c : Fin n → ℝ) {α β : ℝ}
    (hM : ∀ i j : Fin n, 0 ≤ M i j)
    (_hr : ∀ i : Fin n, 0 ≤ r i) (hc : ∀ j : Fin n, 0 < c j)
    (hα : 0 ≤ α) (_hβ : 0 ≤ β)
    (hrow : ∀ i : Fin n, ∑ j : Fin n, M i j * c j ≤ α * r i)
    (hcol : ∀ j : Fin n, ∑ i : Fin n, r i * M i j ≤ β * c j)
    (x : Fin n → ℝ) :
    vecNorm2Sq (matMulVec n M x) ≤ α * β * vecNorm2Sq x := by
  have hrowabs_nonneg : ∀ i : Fin n, 0 ≤ ∑ j : Fin n, M i j * |x j| := by
    intro i
    exact Finset.sum_nonneg fun j _ => mul_nonneg (hM i j) (abs_nonneg (x j))
  have h_abs_sq :
      vecNorm2Sq (matMulVec n M x) ≤
        ∑ i : Fin n, (∑ j : Fin n, M i j * |x j|) ^ 2 := by
    unfold vecNorm2Sq
    apply Finset.sum_le_sum
    intro i _
    have habs : |matMulVec n M x i| ≤ ∑ j : Fin n, M i j * |x j| := by
      calc
        |matMulVec n M x i| ≤ ∑ j : Fin n, |M i j| * |x j| :=
          abs_matMulVec_le n M x i
        _ = ∑ j : Fin n, M i j * |x j| := by
          apply Finset.sum_congr rfl
          intro j _
          rw [abs_of_nonneg (hM i j)]
    have hsq := (sq_le_sq₀ (abs_nonneg _) (hrowabs_nonneg i)).mpr habs
    simpa [sq_abs] using hsq
  have h_rows :
      (∑ i : Fin n, (∑ j : Fin n, M i j * |x j|) ^ 2) ≤
        ∑ i : Fin n, (∑ j : Fin n, M i j * x j ^ 2 / c j) * (α * r i) := by
    apply Finset.sum_le_sum
    intro i _
    have hcauchy := ch7_weighted_row_cauchy_sum_sq M c x hM hc i
    have hf_nonneg : 0 ≤ ∑ j : Fin n, M i j * x j ^ 2 / c j := by
      exact Finset.sum_nonneg fun j _ =>
        div_nonneg (mul_nonneg (hM i j) (sq_nonneg (x j))) (le_of_lt (hc j))
    exact hcauchy.trans (mul_le_mul_of_nonneg_left (hrow i) hf_nonneg)
  have h_rearrange :
      (∑ i : Fin n, (∑ j : Fin n, M i j * x j ^ 2 / c j) * (α * r i)) =
        α * ∑ j : Fin n, (x j ^ 2 / c j) * (∑ i : Fin n, r i * M i j) := by
    calc
      (∑ i : Fin n, (∑ j : Fin n, M i j * x j ^ 2 / c j) * (α * r i))
          = ∑ i : Fin n, α * (r i * (∑ j : Fin n, M i j * x j ^ 2 / c j)) := by
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = α * ∑ i : Fin n, r i * (∑ j : Fin n, M i j * x j ^ 2 / c j) := by
            rw [Finset.mul_sum]
      _ = α * ∑ i : Fin n, ∑ j : Fin n, (x j ^ 2 / c j) * (r i * M i j) := by
            congr 1
            apply Finset.sum_congr rfl
            intro i _
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro j _
            ring
      _ = α * ∑ j : Fin n, ∑ i : Fin n, (x j ^ 2 / c j) * (r i * M i j) := by
            rw [Finset.sum_comm]
      _ = α * ∑ j : Fin n, (x j ^ 2 / c j) * (∑ i : Fin n, r i * M i j) := by
            congr 1
            apply Finset.sum_congr rfl
            intro j _
            rw [Finset.mul_sum]
  have h_col_bound :
      α * (∑ j : Fin n, (x j ^ 2 / c j) * (∑ i : Fin n, r i * M i j)) ≤
        α * (∑ j : Fin n, (x j ^ 2 / c j) * (β * c j)) := by
    apply mul_le_mul_of_nonneg_left
    · apply Finset.sum_le_sum
      intro j _
      have hxnonneg : 0 ≤ x j ^ 2 / c j :=
        div_nonneg (sq_nonneg _) (le_of_lt (hc j))
      exact mul_le_mul_of_nonneg_left (hcol j) hxnonneg
    · exact hα
  have h_simplify :
      α * (∑ j : Fin n, (x j ^ 2 / c j) * (β * c j)) =
        α * β * vecNorm2Sq x := by
    unfold vecNorm2Sq
    calc
      α * (∑ j : Fin n, (x j ^ 2 / c j) * (β * c j))
          = α * (∑ j : Fin n, β * x j ^ 2) := by
            congr 1
            apply Finset.sum_congr rfl
            intro j _
            have hcne : c j ≠ 0 := ne_of_gt (hc j)
            field_simp [hcne]
      _ = α * (β * ∑ j : Fin n, x j ^ 2) := by rw [← Finset.mul_sum]
      _ = α * β * ∑ j : Fin n, x j ^ 2 := by ring
  calc
    vecNorm2Sq (matMulVec n M x) ≤
        ∑ i : Fin n, (∑ j : Fin n, M i j * |x j|) ^ 2 := h_abs_sq
    _ ≤ ∑ i : Fin n, (∑ j : Fin n, M i j * x j ^ 2 / c j) * (α * r i) :=
        h_rows
    _ = α * ∑ j : Fin n, (x j ^ 2 / c j) * (∑ i : Fin n, r i * M i j) :=
        h_rearrange
    _ ≤ α * (∑ j : Fin n, (x j ^ 2 / c j) * (β * c j)) :=
        h_col_bound
    _ = α * β * vecNorm2Sq x := h_simplify

/-- Finite Schur-test certificate for the repository's real operator-2 bound. -/
lemma ch7_opNorm2Le_of_schur_bounds {n : ℕ}
    (M : Fin n → Fin n → ℝ) (r c : Fin n → ℝ) {α β : ℝ}
    (hM : ∀ i j : Fin n, 0 ≤ M i j)
    (hr : ∀ i : Fin n, 0 ≤ r i) (hc : ∀ j : Fin n, 0 < c j)
    (hα : 0 ≤ α) (hβ : 0 ≤ β)
    (hrow : ∀ i : Fin n, ∑ j : Fin n, M i j * c j ≤ α * r i)
    (hcol : ∀ j : Fin n, ∑ i : Fin n, r i * M i j ≤ β * c j) :
    opNorm2Le M (Real.sqrt (α * β)) := by
  intro x
  have hsq :=
    ch7_vecNorm2Sq_matMulVec_le_of_schur_bounds M r c hM hr hc hα hβ
      hrow hcol x
  unfold vecNorm2
  have hαβ : 0 ≤ α * β := mul_nonneg hα hβ
  calc
    Real.sqrt (vecNorm2Sq (matMulVec n M x)) ≤
        Real.sqrt ((α * β) * vecNorm2Sq x) := Real.sqrt_le_sqrt hsq
    _ = Real.sqrt (α * β) * Real.sqrt (vecNorm2Sq x) := by
        rw [Real.sqrt_mul hαβ]

/-- Bauer op-2 Schur certificate for the first scaled nonnegative factor.
The weights are `r_i = sqrt(a_i*x_i)` and `s_j = sqrt(y_j*b_j)`, giving
`||D₁ B D₂||₂ <= rho` from the right and left Perron-vector equations. -/
lemma ch7_bauer_left_scaled_opNorm2Le_of_left_right_vectors {n : ℕ}
    (B : Fin n → Fin n → ℝ) (ρ : ℝ)
    (x y a b : Fin n → ℝ)
    (hB : ∀ i j : Fin n, 0 ≤ B i j)
    (hρ : 0 ≤ ρ)
    (hx : ∀ i : Fin n, 0 < x i)
    (hy : ∀ i : Fin n, 0 < y i)
    (ha : ∀ i : Fin n, 0 < a i)
    (hb : ∀ i : Fin n, 0 < b i)
    (hBy : ∀ i : Fin n, matMulVec n B y i = ρ * x i)
    (hBta : ∀ j : Fin n, matMulVec n (matTranspose B) a j = ρ * b j) :
    opNorm2Le
      (ch7TwoSidedScale
        (fun i : Fin n => a i / Real.sqrt (a i * x i)) B
        (fun j : Fin n => y j / Real.sqrt (y j * b j))) ρ := by
  let r : Fin n → ℝ := fun i => Real.sqrt (a i * x i)
  let s : Fin n → ℝ := fun j => Real.sqrt (y j * b j)
  let d₁ : Fin n → ℝ := fun i => a i / r i
  let d₂ : Fin n → ℝ := fun j => y j / s j
  have hr_nonneg : ∀ i : Fin n, 0 ≤ r i := fun i => Real.sqrt_nonneg _
  have hs_pos : ∀ j : Fin n, 0 < s j := by
    intro j
    exact Real.sqrt_pos.2 (mul_pos (hy j) (hb j))
  have hd₁_pos : ∀ i : Fin n, 0 < d₁ i := by
    intro i
    exact div_pos (ha i) (Real.sqrt_pos.2 (mul_pos (ha i) (hx i)))
  have hd₂_pos : ∀ j : Fin n, 0 < d₂ j := by
    intro j
    exact div_pos (hy j) (hs_pos j)
  have hM_nonneg :
      ∀ i j : Fin n, 0 ≤ ch7TwoSidedScale d₁ B d₂ i j := by
    intro i j
    unfold ch7TwoSidedScale
    exact mul_nonneg (mul_nonneg (le_of_lt (hd₁_pos i)) (hB i j))
      (le_of_lt (hd₂_pos j))
  have hrow :
      ∀ i : Fin n, ∑ j : Fin n, ch7TwoSidedScale d₁ B d₂ i j * s j ≤
        ρ * r i := by
    intro i
    exact le_of_eq <| by
      have hsqr : r i ^ 2 = a i * x i := by
        dsimp [r]
        exact Real.sq_sqrt (le_of_lt (mul_pos (ha i) (hx i)))
      have hrpos : 0 < r i := by
        dsimp [r]
        exact Real.sqrt_pos.2 (mul_pos (ha i) (hx i))
      have hBy_sum : ∑ j : Fin n, B i j * y j = ρ * x i := by
        simpa [matMulVec] using hBy i
      calc
        ∑ j : Fin n, ch7TwoSidedScale d₁ B d₂ i j * s j
            = (a i / r i) * (∑ j : Fin n, B i j * y j) := by
              rw [Finset.mul_sum]
              apply Finset.sum_congr rfl
              intro j _
              have hsne : s j ≠ 0 := ne_of_gt (hs_pos j)
              unfold ch7TwoSidedScale d₁ d₂
              field_simp [hsne]
        _ = (a i / r i) * (ρ * x i) := by rw [hBy_sum]
        _ = ρ * r i := by
              rw [show (a i / r i) * (ρ * x i) =
                    ρ * ((a i / r i) * x i) by ring]
              dsimp [r] at hrpos hsqr ⊢
              rw [ch7_div_sqrt_mul_mul_eq_sqrt_mul (ha i) (hx i)]
  have hcol :
      ∀ j : Fin n, ∑ i : Fin n, r i * ch7TwoSidedScale d₁ B d₂ i j ≤
        ρ * s j := by
    intro j
    exact le_of_eq <| by
      have hspos : 0 < s j := hs_pos j
      have hBta_sum : ∑ i : Fin n, a i * B i j = ρ * b j := by
        calc
          ∑ i : Fin n, a i * B i j = ∑ i : Fin n, B i j * a i := by
            apply Finset.sum_congr rfl
            intro i _
            ring
          _ = ρ * b j := by
            simpa [matMulVec, matTranspose] using hBta j
      calc
        ∑ i : Fin n, r i * ch7TwoSidedScale d₁ B d₂ i j
            = (y j / s j) * (∑ i : Fin n, a i * B i j) := by
              rw [Finset.mul_sum]
              apply Finset.sum_congr rfl
              intro i _
              have hrne : r i ≠ 0 := by
                dsimp [r]
                exact ne_of_gt (Real.sqrt_pos.2 (mul_pos (ha i) (hx i)))
              unfold ch7TwoSidedScale d₁ d₂
              field_simp [hrne]
        _ = (y j / s j) * (ρ * b j) := by rw [hBta_sum]
        _ = ρ * s j := by
              rw [show (y j / s j) * (ρ * b j) =
                    ρ * ((y j / s j) * b j) by ring]
              dsimp [s] at hspos ⊢
              rw [ch7_div_sqrt_mul_mul_eq_sqrt_mul (hy j) (hb j)]
  have hschur :
      opNorm2Le (ch7TwoSidedScale d₁ B d₂) (Real.sqrt (ρ * ρ)) :=
    ch7_opNorm2Le_of_schur_bounds
      (ch7TwoSidedScale d₁ B d₂) r s hM_nonneg hr_nonneg hs_pos hρ hρ hrow hcol
  have hsqrtrho : Real.sqrt (ρ * ρ) = ρ := by
    rw [show ρ * ρ = ρ ^ 2 by ring, Real.sqrt_sq_eq_abs, abs_of_nonneg hρ]
  exact opNorm2Le_mono hschur (le_of_eq hsqrtrho)

/-- Bauer op-2 Schur certificate for the inverse-side scaled nonnegative
factor.  With the same weights as the left factor, the compatible inverse
scaling gives `||D₂⁻¹ C D₁⁻¹||₂ <= 1`. -/
lemma ch7_bauer_right_scaled_opNorm2Le_of_left_right_vectors {n : ℕ}
    (C : Fin n → Fin n → ℝ) (x y a b : Fin n → ℝ)
    (hC : ∀ i j : Fin n, 0 ≤ C i j)
    (hx : ∀ i : Fin n, 0 < x i)
    (hy : ∀ i : Fin n, 0 < y i)
    (ha : ∀ i : Fin n, 0 < a i)
    (hb : ∀ i : Fin n, 0 < b i)
    (hCx : ∀ i : Fin n, matMulVec n C x i = y i)
    (hCtb : ∀ i : Fin n, matMulVec n (matTranspose C) b i = a i) :
    opNorm2Le
      (ch7TwoSidedScale
        (fun j : Fin n => b j / Real.sqrt (y j * b j)) C
        (fun i : Fin n => x i / Real.sqrt (a i * x i))) 1 := by
  let r : Fin n → ℝ := fun i => Real.sqrt (a i * x i)
  let s : Fin n → ℝ := fun j => Real.sqrt (y j * b j)
  let d₁Inv : Fin n → ℝ := fun i => x i / r i
  let d₂Inv : Fin n → ℝ := fun j => b j / s j
  have hr_pos : ∀ i : Fin n, 0 < r i := by
    intro i
    exact Real.sqrt_pos.2 (mul_pos (ha i) (hx i))
  have hr_nonneg : ∀ i : Fin n, 0 ≤ r i := fun i => le_of_lt (hr_pos i)
  have hs_pos : ∀ j : Fin n, 0 < s j := by
    intro j
    exact Real.sqrt_pos.2 (mul_pos (hy j) (hb j))
  have hd₁Inv_pos : ∀ i : Fin n, 0 < d₁Inv i := by
    intro i
    exact div_pos (hx i) (hr_pos i)
  have hd₂Inv_pos : ∀ j : Fin n, 0 < d₂Inv j := by
    intro j
    exact div_pos (hb j) (hs_pos j)
  have hM_nonneg :
      ∀ j i : Fin n, 0 ≤ ch7TwoSidedScale d₂Inv C d₁Inv j i := by
    intro j i
    unfold ch7TwoSidedScale
    exact mul_nonneg (mul_nonneg (le_of_lt (hd₂Inv_pos j)) (hC j i))
      (le_of_lt (hd₁Inv_pos i))
  have hrow :
      ∀ j : Fin n, ∑ i : Fin n, ch7TwoSidedScale d₂Inv C d₁Inv j i * r i ≤
        1 * s j := by
    intro j
    exact le_of_eq <| by
      have hCx_sum : ∑ i : Fin n, C j i * x i = y j := by
        simpa [matMulVec] using hCx j
      calc
        ∑ i : Fin n, ch7TwoSidedScale d₂Inv C d₁Inv j i * r i
            = (b j / s j) * (∑ i : Fin n, C j i * x i) := by
              rw [Finset.mul_sum]
              apply Finset.sum_congr rfl
              intro i _
              have hrne : r i ≠ 0 := ne_of_gt (hr_pos i)
              unfold ch7TwoSidedScale d₁Inv d₂Inv
              field_simp [hrne]
        _ = (b j / s j) * y j := by rw [hCx_sum]
        _ = s j := by
              dsimp [s]
              rw [ch7_div_sqrt_mul_mul_eq_sqrt_mul_comm (hy j) (hb j)]
        _ = 1 * s j := by ring
  have hcol :
      ∀ i : Fin n, ∑ j : Fin n, s j * ch7TwoSidedScale d₂Inv C d₁Inv j i ≤
        1 * r i := by
    intro i
    exact le_of_eq <| by
      have hCtb_sum : ∑ j : Fin n, b j * C j i = a i := by
        calc
          ∑ j : Fin n, b j * C j i = ∑ j : Fin n, C j i * b j := by
            apply Finset.sum_congr rfl
            intro j _
            ring
          _ = a i := by
            simpa [matMulVec, matTranspose] using hCtb i
      calc
        ∑ j : Fin n, s j * ch7TwoSidedScale d₂Inv C d₁Inv j i
            = (x i / r i) * (∑ j : Fin n, b j * C j i) := by
              rw [Finset.mul_sum]
              apply Finset.sum_congr rfl
              intro j _
              have hsne : s j ≠ 0 := ne_of_gt (hs_pos j)
              unfold ch7TwoSidedScale d₁Inv d₂Inv
              field_simp [hsne]
        _ = (x i / r i) * a i := by rw [hCtb_sum]
        _ = r i := by
              dsimp [r]
              rw [ch7_div_sqrt_mul_mul_eq_sqrt_mul_comm (ha i) (hx i)]
        _ = 1 * r i := by ring
  simpa using
    ch7_opNorm2Le_of_schur_bounds
      (ch7TwoSidedScale d₂Inv C d₁Inv) s r hM_nonneg
      (fun j => le_of_lt (hs_pos j)) hr_pos (by norm_num) (by norm_num)
      hrow hcol

/-- The exact Euclidean operator-2 condition product for a fixed reciprocal
two-sided scaling. -/
noncomputable def ch7TwoSidedScaledOp2Kappa {n : ℕ}
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ) : ℝ :=
  complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₁ A d₂)) *
    complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₂Inv A_inv d₁Inv))

/-- Problem 7.10(e), fixed-scaling op-2 Bauer upper bound from compatible
left and right Perron-vector data.  The scaling is
`d₁ᵢ = aᵢ / sqrt(aᵢ*xᵢ)` and `d₂ⱼ = yⱼ / sqrt(yⱼ*bⱼ)`, with inverse-side
partners `xᵢ / sqrt(aᵢ*xᵢ)` and `bⱼ / sqrt(yⱼ*bⱼ)`. -/
theorem problem7_10e_scaled_op2Kappa_le_perron_of_abs_left_right_vectors
    {n : ℕ} (A A_inv : Fin n → Fin n → ℝ) {ρ : ℝ}
    (x y a b : Fin n → ℝ)
    (hρ : 0 ≤ ρ)
    (hx : ∀ i : Fin n, 0 < x i)
    (hy : ∀ i : Fin n, 0 < y i)
    (ha : ∀ i : Fin n, 0 < a i)
    (hb : ∀ i : Fin n, 0 < b i)
    (hBy : ∀ i : Fin n,
      matMulVec n (absMatrix n A) y i = ρ * x i)
    (hCx : ∀ i : Fin n,
      matMulVec n (absMatrix n A_inv) x i = y i)
    (hCtb : ∀ i : Fin n,
      matMulVec n (matTranspose (absMatrix n A_inv)) b i = a i)
    (hBta : ∀ j : Fin n,
      matMulVec n (matTranspose (absMatrix n A)) a j = ρ * b j) :
    ch7TwoSidedScaledOp2Kappa A A_inv
        (fun i : Fin n => a i / Real.sqrt (a i * x i))
        (fun i : Fin n => x i / Real.sqrt (a i * x i))
        (fun j : Fin n => y j / Real.sqrt (y j * b j))
        (fun j : Fin n => b j / Real.sqrt (y j * b j)) ≤ ρ := by
  let B : Fin n → Fin n → ℝ := absMatrix n A
  let C : Fin n → Fin n → ℝ := absMatrix n A_inv
  let d₁ : Fin n → ℝ := fun i => a i / Real.sqrt (a i * x i)
  let d₁Inv : Fin n → ℝ := fun i => x i / Real.sqrt (a i * x i)
  let d₂ : Fin n → ℝ := fun j => y j / Real.sqrt (y j * b j)
  let d₂Inv : Fin n → ℝ := fun j => b j / Real.sqrt (y j * b j)
  have hB_nonneg : ∀ i j : Fin n, 0 ≤ B i j := by
    intro i j
    dsimp [B, absMatrix]
    exact abs_nonneg _
  have hC_nonneg : ∀ i j : Fin n, 0 ≤ C i j := by
    intro i j
    dsimp [C, absMatrix]
    exact abs_nonneg _
  have hd₁_nonneg : ∀ i : Fin n, 0 ≤ d₁ i := by
    intro i
    exact le_of_lt (div_pos (ha i) (Real.sqrt_pos.2 (mul_pos (ha i) (hx i))))
  have hd₂_nonneg : ∀ j : Fin n, 0 ≤ d₂ j := by
    intro j
    exact le_of_lt (div_pos (hy j) (Real.sqrt_pos.2 (mul_pos (hy j) (hb j))))
  have hd₁Inv_nonneg : ∀ i : Fin n, 0 ≤ d₁Inv i := by
    intro i
    exact le_of_lt (div_pos (hx i) (Real.sqrt_pos.2 (mul_pos (ha i) (hx i))))
  have hd₂Inv_nonneg : ∀ j : Fin n, 0 ≤ d₂Inv j := by
    intro j
    exact le_of_lt (div_pos (hb j) (Real.sqrt_pos.2 (mul_pos (hy j) (hb j))))
  have hBop_abs :
      opNorm2Le (ch7TwoSidedScale d₁ B d₂) ρ := by
    simpa [B, d₁, d₂] using
      ch7_bauer_left_scaled_opNorm2Le_of_left_right_vectors
        (absMatrix n A) ρ x y a b
        (fun i j => by simp [absMatrix])
        hρ hx hy ha hb hBy hBta
  have hCop_abs :
      opNorm2Le (ch7TwoSidedScale d₂Inv C d₁Inv) 1 := by
    simpa [C, d₁Inv, d₂Inv] using
      ch7_bauer_right_scaled_opNorm2Le_of_left_right_vectors
        (absMatrix n A_inv) x y a b
        (fun i j => by simp [absMatrix])
        hx hy ha hb hCx hCtb
  have hBrect :
      rectOpNorm2Le (ch7TwoSidedScale d₁ A d₂) ρ := by
    apply rectOpNorm2Le_of_abs_entry_le
      (B := ch7TwoSidedScale d₁ B d₂)
    · intro i j
      unfold ch7TwoSidedScale B absMatrix
      rw [abs_mul, abs_mul, abs_of_nonneg (hd₁_nonneg i),
        abs_of_nonneg (hd₂_nonneg j)]
    · exact opNorm2Le_to_rectOpNorm2Le hBop_abs
  have hCrect :
      rectOpNorm2Le (ch7TwoSidedScale d₂Inv A_inv d₁Inv) 1 := by
    apply rectOpNorm2Le_of_abs_entry_le
      (B := ch7TwoSidedScale d₂Inv C d₁Inv)
    · intro i j
      unfold ch7TwoSidedScale C absMatrix
      rw [abs_mul, abs_mul, abs_of_nonneg (hd₂Inv_nonneg i),
        abs_of_nonneg (hd₁Inv_nonneg j)]
    · exact opNorm2Le_to_rectOpNorm2Le hCop_abs
  have hBop :
      complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₁ A d₂)) ≤ ρ :=
    complexMatrixOp2_realRectToCMatrix_le_of_rectOpNorm2Le
      (ch7TwoSidedScale d₁ A d₂) hρ hBrect
  have hCop :
      complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₂Inv A_inv d₁Inv)) ≤ 1 :=
    complexMatrixOp2_realRectToCMatrix_le_of_rectOpNorm2Le
      (ch7TwoSidedScale d₂Inv A_inv d₁Inv) (by norm_num) hCrect
  change
    complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₁ A d₂)) *
        complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₂Inv A_inv d₁Inv)) ≤ ρ
  calc
    complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₁ A d₂)) *
        complexMatrixOp2 (realRectToCMatrix (ch7TwoSidedScale d₂Inv A_inv d₁Inv))
        ≤ ρ * 1 := by
          exact mul_le_mul hBop hCop (complexMatrixOp2_nonneg _) hρ
    _ = ρ := by ring

/-- Problem 7.10(e), pointwise two-norm interpolation for a fixed two-sided
scaling: the `κ₂` value is bounded by the geometric mean of the corresponding
`κ₁` and `κ∞` products. -/
theorem problem7_10e_scaled_op2Kappa_le_sqrt_oneKappa_mul_infKappa
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ) :
    ch7TwoSidedScaledOp2Kappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤
      Real.sqrt
        (ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv *
          ch7TwoSidedScaledInfKappa A A_inv d₁ d₁Inv d₂ d₂Inv) := by
  let M : Fin n → Fin n → ℝ := ch7TwoSidedScale d₁ A d₂
  let N : Fin n → Fin n → ℝ := ch7TwoSidedScale d₂Inv A_inv d₁Inv
  have hM :=
    problem7_10e_complexMatrixOp2_realRectToCMatrix_le_sqrt_one_mul_inf hn M
  have hN :=
    problem7_10e_complexMatrixOp2_realRectToCMatrix_le_sqrt_one_mul_inf hn N
  have hprod :
      complexMatrixOp2 (realRectToCMatrix M) *
          complexMatrixOp2 (realRectToCMatrix N) ≤
        Real.sqrt (oneNorm M * infNorm M) *
          Real.sqrt (oneNorm N * infNorm N) :=
    mul_le_mul hM hN (complexMatrixOp2_nonneg _) (Real.sqrt_nonneg _)
  have hMrad : 0 ≤ oneNorm M * infNorm M :=
    mul_nonneg (oneNorm_nonneg M) (infNorm_nonneg M)
  have hsqrts :
      Real.sqrt (oneNorm M * infNorm M) *
          Real.sqrt (oneNorm N * infNorm N) =
        Real.sqrt ((oneNorm M * oneNorm N) * (infNorm M * infNorm N)) := by
    rw [← Real.sqrt_mul hMrad]
    congr 1
    ring
  exact hprod.trans (by
    rw [hsqrts]
    rfl)

/-- Problem 7.10(e), fixed-scaling consequence of the source interpolation
step: if the same two-sided scaling has both its `κ₁` and `κ∞` products
bounded by `ρ`, then its exact Euclidean `κ₂` product is also bounded by `ρ`.
The remaining global Bauer/Perron work is to supply such a scaling at
`ρ = ρ(|A||A⁻¹|)`. -/
theorem problem7_10e_scaled_op2Kappa_le_of_one_inf_bounds
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ) {ρ : ℝ}
    (hρ : 0 ≤ ρ)
    (hone :
      ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤ ρ)
    (hinf :
      ch7TwoSidedScaledInfKappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤ ρ) :
    ch7TwoSidedScaledOp2Kappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤ ρ := by
  have hinterp :=
    problem7_10e_scaled_op2Kappa_le_sqrt_oneKappa_mul_infKappa
      hn A A_inv d₁ d₁Inv d₂ d₂Inv
  have hone_nonneg :
      0 ≤ ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv := by
    unfold ch7TwoSidedScaledOneKappa
    exact mul_nonneg (oneNorm_nonneg _) (oneNorm_nonneg _)
  have hinf_nonneg :
      0 ≤ ch7TwoSidedScaledInfKappa A A_inv d₁ d₁Inv d₂ d₂Inv := by
    unfold ch7TwoSidedScaledInfKappa
    exact mul_nonneg (infNorm_nonneg _) (infNorm_nonneg _)
  have hrad_le :
      ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv *
          ch7TwoSidedScaledInfKappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤ ρ * ρ :=
    mul_le_mul hone hinf hinf_nonneg hρ
  have hsqrt_le :=
    Real.sqrt_le_sqrt hrad_le
  have hsqrtρ : Real.sqrt (ρ * ρ) = ρ := by
    rw [show ρ * ρ = ρ ^ 2 by ring, Real.sqrt_sq_eq_abs, abs_of_nonneg hρ]
  exact hinterp.trans (hsqrt_le.trans (le_of_eq hsqrtρ))

/-- Problem 7.10(e), product-bound form of the fixed-scaling interpolation
step.  It is enough for one admissible scaling to satisfy
`κ₁(D₁AD₂) * κ∞(D₁AD₂) <= ρ^2`; the stronger API requiring each factor to be
separately bounded by `ρ` is a convenient specialization of this statement. -/
theorem problem7_10e_scaled_op2Kappa_le_of_one_inf_product_bound
    {n : ℕ} (hn : 0 < n)
    (A A_inv : Fin n → Fin n → ℝ)
    (d₁ d₁Inv d₂ d₂Inv : Fin n → ℝ) {ρ : ℝ}
    (hρ : 0 ≤ ρ)
    (hprod :
      ch7TwoSidedScaledOneKappa A A_inv d₁ d₁Inv d₂ d₂Inv *
          ch7TwoSidedScaledInfKappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤
        ρ * ρ) :
    ch7TwoSidedScaledOp2Kappa A A_inv d₁ d₁Inv d₂ d₂Inv ≤ ρ := by
  have hinterp :=
    problem7_10e_scaled_op2Kappa_le_sqrt_oneKappa_mul_infKappa
      hn A A_inv d₁ d₁Inv d₂ d₂Inv
  have hsqrt_le := Real.sqrt_le_sqrt hprod
  have hsqrtρ : Real.sqrt (ρ * ρ) = ρ := by
    rw [show ρ * ρ = ρ ^ 2 by ring, Real.sqrt_sq_eq_abs, abs_of_nonneg hρ]
  exact hinterp.trans (hsqrt_le.trans (le_of_eq hsqrtρ))

-- ============================================================
-- Problem 7.15: Hadamard product algebra under two-sided scaling
-- ============================================================




























































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Theorem 7.4, relative infinity-norm form of (7.10)
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.2: residual/error sandwich in the infinity norm
-- ============================================================

































































































































































































-- ============================================================
-- Theorem 7.2: arbitrary subordinate-norm form of (7.4)
-- ============================================================






























































































































-- ============================================================
-- Equation (7.5): generic normwise condition number
-- ============================================================

























































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.7: dropping the right-hand-side perturbation budget
-- ============================================================
















































































































































































































































































































































































-- ============================================================
-- Problem 7.8: rectangular Frobenius backward error
-- ============================================================


































































































































































































































































































































































-- ============================================================
-- Lemma 7.9: practical error bounds
-- ============================================================






























































































-- ============================================================
-- Equations (7.30) and (7.31): computed residual bounds
-- ============================================================




































































































-- ============================================================
-- Problem 7.13: sparse computed residual bounds
-- ============================================================





































































































































































































































































































































-- ============================================================
-- Equation (7.32): perturbation theory by calculus
-- ============================================================


















































































































































-- ============================================================
-- Equation (7.33): stochastic matrices
-- ============================================================














/-! ## Theorem 7.8 (Bauer) — source-labeled aliases

Higham 2nd ed., Chapter 7, §7.3, Theorem 7.8 (p. 127, eq. (7.24)): for
nonsingular `A` with `|A||A⁻¹|` and `|A⁻¹||A|` irreducible,
`min_{D₁,D₂ ∈ 𝒟ₙ} κ_∞(D₁AD₂) = ρ(|A||A⁻¹|)`. The book's proof is delegated
to Problem 7.10, so the full formalization lives in the `problem7_10*`
family (with `B = |A|`, `C = |A⁻¹|`); these thin aliases give the primary
source label its own traceable surface. -/




















end NumStability
