import Mathlib.Algebra.Order.GroupWithZero.Finset
import Mathlib.LinearAlgebra.Basis.VectorSpace
import Mathlib.LinearAlgebra.Charpoly.ToMatrix
import Mathlib.LinearAlgebra.Eigenspace.Charpoly
import Mathlib.LinearAlgebra.Finsupp.Pi
import Mathlib.LinearAlgebra.Matrix.FiniteDimensional
import Mathlib.LinearAlgebra.Matrix.Irreducible.Defs
import NumStability.Algorithms.NormEstimation.OneNorm.PowerMethod
import NumStability.Analysis.Asymptotics.Bounds
import NumStability.Analysis.Conditioning.DistanceToSingularity
import NumStability.Analysis.MatrixAlgebra
import NumStability.Analysis.MatrixNorms.SpectralRadius
import NumStability.Analysis.PerturbationTheory
import NumStability.Analysis.Summation.Signs
import NumStability.Source.Higham.Chapter06.Problem05
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Theorem05.Part01
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Theorem05.Part02
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Theorem05.Part03
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Theorem05.Part04
import NumStability.Source.Higham.Chapter07.LinearSystemsConditioning.Theorem05.Part05

-- Analysis/HighamChapter7.lean
--
-- Source-facing wrappers for Higham Chapter 7.
--
-- The heavy perturbation arguments live in `PerturbationTheory.lean`; this file
-- records Chapter 7 statements whose exact source shape is a relative
-- infinity-norm or practical-error corollary of those componentwise results.

















namespace NumStability

open scoped BigOperators

-- ============================================================
-- Chapter 7 forward-error kernels
-- ============================================================














































-- ============================================================
-- Problem 7.1: local Neumann contraction infrastructure
-- ============================================================

































































































































































































































































































































































































































































































-- ============================================================
-- Theorem 7.1: arbitrary subordinate-norm Rigal-Gaches wrappers
-- ============================================================




























































































































































































































































































-- ============================================================
-- Equations (7.11), (7.13), and (7.14): condition numbers
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.5: SVD-projected perturbation bound
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.6(a): row-wise relative perturbations
-- ============================================================





















































































































































































































-- ============================================================
-- Problem 7.6(b): columnwise relative perturbations
-- ============================================================












































































































































































































































-- ============================================================
-- Chapter 7 finite sign helpers
-- ============================================================
















-- ============================================================
-- Problem 7.9: linearized scalar-output componentwise condition
-- ============================================================






































































































































































































































































































































































































































































-- ------------------------------------------------------------
-- Problem 7.9: finite normwise scalar-output formula
-- ------------------------------------------------------------














































































































































































































































































































































































-- ------------------------------------------------------------
-- Problem 7.9: first-order source-radius condition values
-- ------------------------------------------------------------






















































































































































































































































































































-- ============================================================
-- Problem 7.11: linearized inverse componentwise perturbations
-- ============================================================















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.9: exact componentwise scalar-output bridge
-- ============================================================



























































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.9: exact normwise scalar-output bridge
-- ============================================================













































































































































































































































































































































































































































































































































-- ============================================================
-- Equation (7.26): distance to singularity
-- ============================================================





















-- ============================================================
-- Equation (7.15) / Problem 7.3 row-equilibration infrastructure
-- ============================================================










































































































































































































































































































































































-- ============================================================
-- Equation (7.17): Kahan's symbolic 3 × 3 example
-- ============================================================












































































































































































































































































































































































































-- ============================================================
-- Problem 7.4: unit-diagonal SPD scaling core bound
-- ============================================================






















































































































































-- ============================================================
-- Theorem 7.5 dependency: Euclidean column-equilibration bounds
-- ============================================================













































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































/-- Theorem 7.5 Moore-Penrose bridge, full-column-rank side: the first
Penrose equation `A A⁺ A = A`, together with injectivity of `x ↦ A*x`, forces
the explicit rectangular left-inverse identity `A⁺ A = I` used by the existing
projection wrappers. -/
theorem theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAinj : Function.Injective (rectMatMulVec A)) :
    ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0 := by
  intro i j
  have hfix :
      rectMatMulVec (rectMatMul Aplus A) (basisVec (n := n) j) =
        basisVec (n := n) j := by
    apply hAinj
    calc
      rectMatMulVec A (rectMatMulVec (rectMatMul Aplus A) (basisVec (n := n) j))
          = rectMatMulVec (rectMatMul A (rectMatMul Aplus A))
              (basisVec (n := n) j) := by
            rw [← rectMatMulVec_rectMatMul]
      _ = rectMatMulVec (rectMatMul (rectMatMul A Aplus) A)
              (basisVec (n := n) j) := by
            rw [← rectMatMul_assoc A Aplus A]
      _ = rectMatMulVec A (basisVec (n := n) j) := by
            rw [hPenrose1]
  have hi := congrFun hfix i
  simpa [rectMatMulVec, rectMatMul, basisVec] using hi

/-- Theorem 7.5 Moore-Penrose bridge, full-row-rank side: the first Penrose
equation `A A⁺ A = A`, together with surjectivity of `x ↦ A*x`, forces the
explicit rectangular right-inverse identity `A A⁺ = I` used by the existing
projection wrappers. -/
theorem theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAsurj : Function.Surjective (rectMatMulVec A)) :
    ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0 := by
  intro i j
  obtain ⟨x, hx⟩ := hAsurj (basisVec (n := m) j)
  have hfix :
      rectMatMulVec (rectMatMul A Aplus) (basisVec (n := m) j) =
        basisVec (n := m) j := by
    calc
      rectMatMulVec (rectMatMul A Aplus) (basisVec (n := m) j)
          = rectMatMulVec (rectMatMul A Aplus) (rectMatMulVec A x) := by
            rw [hx]
      _ = rectMatMulVec (rectMatMul (rectMatMul A Aplus) A) x := by
            rw [← rectMatMulVec_rectMatMul]
      _ = rectMatMulVec A x := by
            rw [hPenrose1]
      _ = basisVec (n := m) j := hx
  have hi := congrFun hfix i
  simpa [rectMatMulVec, rectMatMul, basisVec] using hi

/-- Matrix-rank source form of the full-column-rank Penrose bridge. -/
theorem theorem7_5_rect_left_inverse_of_penrose1_matrix_rank_eq_width
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n) :
    ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0 :=
  theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective A Aplus
    hPenrose1 (ch7_rectMatMulVec_injective_of_matrix_rank_eq_width A hrank)

/-- Matrix-rank source form of the full-row-rank Penrose bridge. -/
theorem theorem7_5_rect_right_inverse_of_penrose1_matrix_rank_eq_height
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m) :
    ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0 :=
  theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective A Aplus
    hPenrose1 (ch7_rectMatMulVec_surjective_of_matrix_rank_eq_height A hrank)

/-- Theorem 7.5, `p = 1` column minimum-attainment form with the printed
rank hypothesis and first Penrose equation supplying the explicit left inverse
needed by the endpoint least-element theorem.  This is still conditional on
the displayed `Aplus`; it does not construct the Moore-Penrose inverse. -/
theorem theorem7_5_p1_column_equilibration_isLeast_right_scalings_of_penrose1_matrix_rank_eq_width
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n) :
    IsLeast (ch7OneNormRightScaledCondSet A Aplus)
      (ch7OneNormRightScaledCond A Aplus
        (ch7ColumnEquilibratingScale1 A)
        (fun j : Fin n => ch7RectColumnNorm1 A j)) :=
  theorem7_5_p1_column_equilibration_isLeast_right_scalings_of_rect_left_inverse
    A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_matrix_rank_eq_width
      A Aplus hPenrose1 hrank)

/-- Theorem 7.5, `p = 2` column infimum form with the printed rank
hypothesis and first Penrose equation supplying the explicit left inverse. -/
theorem theorem7_5_p2_column_equilibration_le_sqrt_card_sInf_right_scalings_of_penrose1_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n) :
    ch7Op2RightScaledCond A Aplus
        (ch7ColumnEquilibratingScale2 A)
        (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
      Real.sqrt (n : ℝ) * sInf (ch7Op2RightScaledCondSet A Aplus) :=
  theorem7_5_p2_column_equilibration_le_sqrt_card_sInf_right_scalings_of_rect_left_inverse
    hn A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_matrix_rank_eq_width
      A Aplus hPenrose1 hrank)

/-- Theorem 7.5, `p = 2` column source-`min` wrapper with the printed rank
hypothesis and first Penrose equation supplying the explicit left inverse. -/
theorem theorem7_5_p2_column_equilibration_le_sqrt_card_min_right_scalings_of_penrose1_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n)
    {μ : ℝ} (hmin : IsLeast (ch7Op2RightScaledCondSet A Aplus) μ) :
    ch7Op2RightScaledCond A Aplus
        (ch7ColumnEquilibratingScale2 A)
        (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
      Real.sqrt (n : ℝ) * μ :=
  theorem7_5_p2_column_equilibration_le_sqrt_card_min_right_scalings_of_rect_left_inverse
    hn A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_matrix_rank_eq_width
      A Aplus hPenrose1 hrank)
    hmin

/-- Theorem 7.5, finite-real `p` column infimum form with the printed rank
hypothesis and first Penrose equation supplying the explicit left inverse. -/
theorem theorem7_5_lp_column_equilibration_le_card_rpow_sInf_right_scalings_of_penrose1_matrix_rank_eq_width
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p : ℝ} (hp : 1 ≤ p)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n) :
    ch7LpRightScaledCondOfReal hm hn p hp A Aplus
        (ch7ColumnEquilibratingScaleLpOfReal p A)
        (fun j : Fin n => ch7RectColumnLpNormOfReal p A j) ≤
      (n : ℝ) ^ (1 - p⁻¹) *
        sInf (ch7LpRightScaledCondSetOfReal hm hn p hp A Aplus) :=
  theorem7_5_lp_column_equilibration_le_card_rpow_sInf_right_scalings_of_rect_left_inverse
    hm hn hp A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_matrix_rank_eq_width
      A Aplus hPenrose1 hrank)

/-- Theorem 7.5, finite-real `p` column source-`min` wrapper with the
printed rank hypothesis and first Penrose equation supplying the explicit left
inverse.  The `IsLeast` argument is the existing non-endpoint minimizer
certificate, not a hidden proof obligation. -/
theorem theorem7_5_lp_column_equilibration_le_card_rpow_min_right_scalings_of_penrose1_matrix_rank_eq_width
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p : ℝ} (hp : 1 ≤ p)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n)
    {μ : ℝ}
    (hmin : IsLeast (ch7LpRightScaledCondSetOfReal hm hn p hp A Aplus) μ) :
    ch7LpRightScaledCondOfReal hm hn p hp A Aplus
        (ch7ColumnEquilibratingScaleLpOfReal p A)
        (fun j : Fin n => ch7RectColumnLpNormOfReal p A j) ≤
      (n : ℝ) ^ (1 - p⁻¹) * μ :=
  theorem7_5_lp_column_equilibration_le_card_rpow_min_right_scalings_of_rect_left_inverse
    hm hn hp A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_matrix_rank_eq_width
      A Aplus hPenrose1 hrank)
    hmin

/-- Theorem 7.5, finite-real conjugate-row infimum form with the printed row
rank hypothesis and first Penrose equation supplying the explicit right
inverse. -/
theorem theorem7_5_lp_dual_row_equilibration_le_card_rpow_sInf_left_scalings_of_penrose1_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m) :
    ch7LpLeftScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
        (ch7RowDualEquilibratingScaleLpOfReal q A)
        (fun i : Fin m => ch7RectRowDualLpNormOfReal q A i) ≤
      (m : ℝ) ^ p⁻¹ *
        sInf (ch7LpLeftScaledCondSetOfReal hm hn p (le_of_lt hpq.lt) A Aplus) :=
  theorem7_5_lp_dual_row_equilibration_le_card_rpow_sInf_left_scalings_of_rect_right_inverse
    hm hn hpq A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_matrix_rank_eq_height
      A Aplus hPenrose1 hrank)

/-- Theorem 7.5, finite-real conjugate-row source-`min` wrapper with the
printed row rank hypothesis and first Penrose equation supplying the explicit
right inverse. -/
theorem theorem7_5_lp_dual_row_equilibration_le_card_rpow_min_left_scalings_of_penrose1_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m)
    {μ : ℝ}
    (hmin : IsLeast
      (ch7LpLeftScaledCondSetOfReal hm hn p (le_of_lt hpq.lt) A Aplus) μ) :
    ch7LpLeftScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
        (ch7RowDualEquilibratingScaleLpOfReal q A)
        (fun i : Fin m => ch7RectRowDualLpNormOfReal q A i) ≤
      (m : ℝ) ^ p⁻¹ * μ :=
  theorem7_5_lp_dual_row_equilibration_le_card_rpow_min_left_scalings_of_rect_right_inverse
    hm hn hpq A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_matrix_rank_eq_height
      A Aplus hPenrose1 hrank)
    hmin

/-- Theorem 7.5, `p = 2` row infimum form with the printed row rank
hypothesis and first Penrose equation supplying the explicit right inverse. -/
theorem theorem7_5_p2_row_equilibration_le_sqrt_card_sInf_left_scalings_of_penrose1_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m) :
    ch7Op2LeftScaledCond A Aplus
        (ch7RowEquilibratingScale2Rect A)
        (fun i : Fin m => ch7RectRowNorm2 A i) ≤
      Real.sqrt (m : ℝ) * sInf (ch7Op2LeftScaledCondSet A Aplus) :=
  theorem7_5_p2_row_equilibration_le_sqrt_card_sInf_left_scalings_of_rect_right_inverse
    hm A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_matrix_rank_eq_height
      A Aplus hPenrose1 hrank)

/-- Theorem 7.5, `p = 2` row source-`min` wrapper with the printed row rank
hypothesis and first Penrose equation supplying the explicit right inverse. -/
theorem theorem7_5_p2_row_equilibration_le_sqrt_card_min_left_scalings_of_penrose1_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m)
    {μ : ℝ} (hmin : IsLeast (ch7Op2LeftScaledCondSet A Aplus) μ) :
    ch7Op2LeftScaledCond A Aplus
        (ch7RowEquilibratingScale2Rect A)
        (fun i : Fin m => ch7RectRowNorm2 A i) ≤
      Real.sqrt (m : ℝ) * μ :=
  theorem7_5_p2_row_equilibration_le_sqrt_card_min_left_scalings_of_rect_right_inverse
    hm A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_matrix_rank_eq_height
      A Aplus hPenrose1 hrank)
    hmin

/-- Theorem 7.5, `p = ∞` row minimum-attainment form with the printed row
rank hypothesis and first Penrose equation supplying the explicit right
inverse needed by the endpoint least-element theorem. -/
theorem theorem7_5_pinf_row_equilibration_isLeast_left_scalings_of_penrose1_matrix_rank_eq_height
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m) :
    IsLeast (ch7InfNormLeftScaledCondSet A Aplus)
      (ch7InfNormLeftScaledCond A Aplus
        (ch7RowEquilibratingScale1Rect A)
        (fun i : Fin m => ch7RectRowNorm1 A i)) :=
  theorem7_5_pinf_row_equilibration_isLeast_left_scalings_of_rect_right_inverse
    A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_matrix_rank_eq_height
      A Aplus hPenrose1 hrank)

/-- The identity matrix is symmetric in the repository's finite-matrix
symmetry predicate. -/
lemma ch7_idMatrix_symmetric (n : ℕ) :
    IsSymmetricFiniteMatrix (idMatrix n) := by
  intro i j
  by_cases hij : i = j
  · subst j
    rfl
  · have hji : j ≠ i := fun h => hij h.symm
    simp [idMatrix, hij, hji]

/-- Theorem 7.5 Moore-Penrose dependency: a rectangular left inverse
`A⁺ A = I`, together with symmetry of `A A⁺`, supplies all four Penrose
equations/symmetry conditions.  This is the full-column-rank algebraic
package; it does not construct the Moore-Penrose inverse. -/
theorem theorem7_5_rect_left_inverse_symmetric_penrose_equations
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSymRange : IsSymmetricFiniteMatrix (rectMatMul A Aplus)) :
    rectMatMul (rectMatMul A Aplus) A = A ∧
      rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
      IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
      IsSymmetricFiniteMatrix (rectMatMul Aplus A) := by
  have hleftEq : rectMatMul Aplus A = idMatrix n := by
    ext i j
    simpa [rectMatMul, idMatrix] using hleft i j
  constructor
  · calc
      rectMatMul (rectMatMul A Aplus) A
          = rectMatMul A (rectMatMul Aplus A) :=
            rectMatMul_assoc A Aplus A
      _ = rectMatMul A (idMatrix n) := by rw [hleftEq]
      _ = A := rectMatMul_id_right A
  constructor
  · calc
      rectMatMul (rectMatMul Aplus A) Aplus
          = rectMatMul (idMatrix n) Aplus := by rw [hleftEq]
      _ = Aplus := rectMatMul_id_left Aplus
  constructor
  · exact hSymRange
  · simpa [hleftEq] using ch7_idMatrix_symmetric n

/-- Theorem 7.5 Moore-Penrose dependency: a rectangular right inverse
`A A⁺ = I`, together with symmetry of `A⁺ A`, supplies all four Penrose
equations/symmetry conditions.  This is the full-row-rank algebraic package; it
does not construct the Moore-Penrose inverse. -/
theorem theorem7_5_rect_right_inverse_symmetric_penrose_equations
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSymDomain : IsSymmetricFiniteMatrix (rectMatMul Aplus A)) :
    rectMatMul (rectMatMul A Aplus) A = A ∧
      rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
      IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
      IsSymmetricFiniteMatrix (rectMatMul Aplus A) := by
  have hrightEq : rectMatMul A Aplus = idMatrix m := by
    ext i j
    simpa [rectMatMul, idMatrix] using hright i j
  constructor
  · calc
      rectMatMul (rectMatMul A Aplus) A
          = rectMatMul (idMatrix m) A := by rw [hrightEq]
      _ = A := rectMatMul_id_left A
  constructor
  · calc
      rectMatMul (rectMatMul Aplus A) Aplus
          = rectMatMul Aplus (rectMatMul A Aplus) :=
            rectMatMul_assoc Aplus A Aplus
      _ = rectMatMul Aplus (idMatrix m) := by rw [hrightEq]
      _ = Aplus := rectMatMul_id_right Aplus
  constructor
  · simpa [hrightEq] using ch7_idMatrix_symmetric m
  · exact hSymDomain

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation,
full-column-rank injectivity, and symmetry of `A A⁺`, all four Penrose
equations/symmetry conditions hold. -/
theorem theorem7_5_rect_penrose_injective_symmetric_range_penrose_equations
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAinj : Function.Injective (rectMatMulVec A))
    (hSymRange : IsSymmetricFiniteMatrix (rectMatMul A Aplus)) :
    rectMatMul (rectMatMul A Aplus) A = A ∧
      rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
      IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
      IsSymmetricFiniteMatrix (rectMatMul Aplus A) :=
  theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective
      A Aplus hPenrose1 hAinj)
    hSymRange

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation,
full-row-rank surjectivity, and symmetry of `A⁺ A`, all four Penrose
equations/symmetry conditions hold. -/
theorem theorem7_5_rect_penrose_surjective_symmetric_domain_penrose_equations
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAsurj : Function.Surjective (rectMatMulVec A))
    (hSymDomain : IsSymmetricFiniteMatrix (rectMatMul Aplus A)) :
    rectMatMul (rectMatMul A Aplus) A = A ∧
      rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
      IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
      IsSymmetricFiniteMatrix (rectMatMul Aplus A) :=
  theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective
      A Aplus hPenrose1 hAsurj)
    hSymDomain

/-- Printed-rank form of the full-column-rank Penrose-equation package. -/
theorem theorem7_5_rect_penrose_matrix_rank_eq_width_symmetric_range_penrose_equations
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n)
    (hSymRange : IsSymmetricFiniteMatrix (rectMatMul A Aplus)) :
    rectMatMul (rectMatMul A Aplus) A = A ∧
      rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
      IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
      IsSymmetricFiniteMatrix (rectMatMul Aplus A) :=
  theorem7_5_rect_penrose_injective_symmetric_range_penrose_equations
    A Aplus hPenrose1
    (ch7_rectMatMulVec_injective_of_matrix_rank_eq_width A hrank)
    hSymRange

/-- Printed-rank form of the full-row-rank Penrose-equation package. -/
theorem theorem7_5_rect_penrose_matrix_rank_eq_height_symmetric_domain_penrose_equations
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m)
    (hSymDomain : IsSymmetricFiniteMatrix (rectMatMul Aplus A)) :
    rectMatMul (rectMatMul A Aplus) A = A ∧
      rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
      IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
      IsSymmetricFiniteMatrix (rectMatMul Aplus A) :=
  theorem7_5_rect_penrose_surjective_symmetric_domain_penrose_equations
    A Aplus hPenrose1
    (ch7_rectMatMulVec_surjective_of_matrix_rank_eq_height A hrank)
    hSymDomain

/-- Theorem 7.5 Moore-Penrose construction, full-column-rank side: injectivity
of `x ↦ A*x` constructs a rectangular inverse satisfying all four Penrose
equations/symmetry conditions. -/
theorem theorem7_5_exists_rect_penrose_equations_of_rectMatMulVec_injective
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (hA : Function.Injective (rectMatMulVec A)) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_rectMatMulVec_injective A hA
  exact ⟨Aplus, theorem7_5_rect_left_inverse_symmetric_penrose_equations
    A Aplus hleft hSym⟩

/-- Matrix-rank source form of the full-column-rank Moore-Penrose
construction and Penrose-equation package. -/
theorem theorem7_5_exists_rect_penrose_equations_of_matrix_rank_eq_width
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A) :=
  theorem7_5_exists_rect_penrose_equations_of_rectMatMulVec_injective A
    (ch7_rectMatMulVec_injective_of_matrix_rank_eq_width A hrank)

/-- Matrix-rank source form of the full-row-rank Moore-Penrose construction
and Penrose-equation package. -/
theorem theorem7_5_exists_rect_penrose_equations_of_matrix_rank_eq_height
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  exact ⟨Aplus, theorem7_5_rect_right_inverse_symmetric_penrose_equations
    A Aplus hright hSym⟩

/-- Theorem 7.5, `p = 1` column branch with the printed full-column-rank
side condition: construct the Penrose inverse candidate and prove the endpoint
least-element scaling statement for the same `A⁺`. -/
theorem theorem7_5_p1_column_equilibration_exists_penrose_of_matrix_rank_eq_width
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      IsLeast (ch7OneNormRightScaledCondSet A Aplus)
        (ch7OneNormRightScaledCond A Aplus
          (ch7ColumnEquilibratingScale1 A)
          (fun j : Fin n => ch7RectColumnNorm1 A j)) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_p1_column_equilibration_isLeast_right_scalings_of_rect_left_inverse
      A Aplus hleft

/-- Theorem 7.5, `p = 2` column `sInf` branch with the printed
full-column-rank side condition: construct the Penrose inverse candidate and
prove the scaling inequality for the same `A⁺`. -/
theorem theorem7_5_p2_column_equilibration_exists_penrose_of_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ch7Op2RightScaledCond A Aplus
          (ch7ColumnEquilibratingScale2 A)
          (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
        Real.sqrt (n : ℝ) * sInf (ch7Op2RightScaledCondSet A Aplus) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_p2_column_equilibration_le_sqrt_card_sInf_right_scalings_of_rect_left_inverse
      hn A Aplus hleft

/-- Theorem 7.5, finite-real `p` column `sInf` branch with the printed
full-column-rank side condition: construct the Penrose inverse candidate and
prove the scaling inequality for the same `A⁺`. -/
theorem theorem7_5_lp_column_equilibration_exists_penrose_of_matrix_rank_eq_width
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p : ℝ} (hp : 1 ≤ p)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ch7LpRightScaledCondOfReal hm hn p hp A Aplus
          (ch7ColumnEquilibratingScaleLpOfReal p A)
          (fun j : Fin n => ch7RectColumnLpNormOfReal p A j) ≤
        (n : ℝ) ^ (1 - p⁻¹) *
          sInf (ch7LpRightScaledCondSetOfReal hm hn p hp A Aplus) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_lp_column_equilibration_le_card_rpow_sInf_right_scalings_of_rect_left_inverse
      hm hn hp A Aplus hleft

/-- Theorem 7.5, `p = 2` column source-`min` branch with the printed
full-column-rank side condition: construct the Penrose inverse candidate and,
for the same `A⁺`, apply the source-minimum inequality whenever a minimum
certificate is supplied.  This does not assert non-endpoint minimizer
existence. -/
theorem theorem7_5_p2_column_equilibration_exists_penrose_min_imp_of_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      (∀ {μ : ℝ}, IsLeast (ch7Op2RightScaledCondSet A Aplus) μ →
        ch7Op2RightScaledCond A Aplus
            (ch7ColumnEquilibratingScale2 A)
            (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
          Real.sqrt (n : ℝ) * μ) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · intro μ hmin
    exact theorem7_5_p2_column_equilibration_le_sqrt_card_min_right_scalings_of_rect_left_inverse
      hn A Aplus hleft hmin

/-- Theorem 7.5, `p = 2` column normalized-slice source-`min` branch with the
printed full-column-rank side condition.  The constructed Penrose inverse is
the same `A⁺` used by the normalized value-set certificate. -/
theorem theorem7_5_p2_column_equilibration_exists_penrose_min_normalized_imp_of_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      (∀ {μ : ℝ}, IsLeast (ch7Op2RightScaledCondNormalizedSet A Aplus) μ →
        ch7Op2RightScaledCond A Aplus
            (ch7ColumnEquilibratingScale2 A)
            (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
          Real.sqrt (n : ℝ) * μ) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · intro μ hmin
    exact theorem7_5_p2_column_equilibration_le_sqrt_card_min_right_scalings_of_rect_left_inverse_of_normalized
      hn A Aplus hleft hmin

/-- Theorem 7.5, `p = 2` column source-`min` branch with the printed
full-column-rank side condition: construct the Penrose inverse candidate and
extract the minimum over reciprocal right scalings for the same `A⁺`. -/
theorem theorem7_5_p2_column_equilibration_exists_penrose_min_of_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast (ch7Op2RightScaledCondSet A Aplus) μ ∧
        ch7Op2RightScaledCond A Aplus
            (ch7ColumnEquilibratingScale2 A)
            (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
          Real.sqrt (n : ℝ) * μ := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_p2_column_equilibration_exists_min_right_scalings_of_rect_left_inverse
      hn A Aplus hleft

/-- Normalized-slice version of the compactness-backed Theorem 7.5 `p = 2`
column Penrose/rank source-`min` wrapper. -/
theorem theorem7_5_p2_column_equilibration_exists_penrose_min_normalized_of_matrix_rank_eq_width
    {m n : ℕ} (hn : 0 < n)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast (ch7Op2RightScaledCondNormalizedSet A Aplus) μ ∧
        ch7Op2RightScaledCond A Aplus
            (ch7ColumnEquilibratingScale2 A)
            (fun j : Fin n => ch7RectColumnNorm2 A j) ≤
          Real.sqrt (n : ℝ) * μ := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_p2_column_equilibration_exists_min_right_scalings_of_rect_left_inverse_of_normalized
      hn A Aplus hleft

/-- Theorem 7.5, finite-real `p` column source-`min` branch with the printed
full-column-rank side condition: construct the Penrose inverse candidate and,
for the same `A⁺`, apply the source-minimum inequality whenever a minimum
certificate is supplied.  This does not assert non-endpoint minimizer
existence. -/
theorem theorem7_5_lp_column_equilibration_exists_penrose_min_imp_of_matrix_rank_eq_width
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p : ℝ} (hp : 1 ≤ p)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      (∀ {μ : ℝ},
        IsLeast (ch7LpRightScaledCondSetOfReal hm hn p hp A Aplus) μ →
          ch7LpRightScaledCondOfReal hm hn p hp A Aplus
              (ch7ColumnEquilibratingScaleLpOfReal p A)
              (fun j : Fin n => ch7RectColumnLpNormOfReal p A j) ≤
            (n : ℝ) ^ (1 - p⁻¹) * μ) := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · intro μ hmin
    exact theorem7_5_lp_column_equilibration_le_card_rpow_min_right_scalings_of_rect_left_inverse
      hm hn hp A Aplus hleft hmin

/-- Theorem 7.5, finite-real `p` column source-`min` branch with the printed
full-column-rank side condition: construct the Penrose inverse candidate and
extract the minimum over reciprocal right scalings for the same `A⁺`. -/
theorem theorem7_5_lp_column_equilibration_exists_penrose_min_of_matrix_rank_eq_width
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast
          (ch7LpRightScaledCondSetOfReal hm hn p (le_of_lt hpq.lt) A Aplus)
          μ ∧
        ch7LpRightScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
            (ch7ColumnEquilibratingScaleLpOfReal p A)
            (fun j : Fin n => ch7RectColumnLpNormOfReal p A j) ≤
          (n : ℝ) ^ (1 - p⁻¹) * μ := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_lp_column_equilibration_exists_min_right_scalings_of_rect_left_inverse
      hm hn hpq A Aplus hleft

/-- Normalized-slice version of the compactness-backed Theorem 7.5
finite-real `p` column Penrose/rank source-`min` wrapper. -/
theorem theorem7_5_lp_column_equilibration_exists_penrose_min_normalized_of_matrix_rank_eq_width
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = n) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast
          (ch7LpRightScaledCondNormalizedSetOfReal
            hm hn p (le_of_lt hpq.lt) A Aplus)
          μ ∧
        ch7LpRightScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
            (ch7ColumnEquilibratingScaleLpOfReal p A)
            (fun j : Fin n => ch7RectColumnLpNormOfReal p A j) ≤
          (n : ℝ) ^ (1 - p⁻¹) * μ := by
  obtain ⟨Aplus, hleft, hSym⟩ :=
    theorem7_5_exists_rect_left_inverse_symmetric_range_of_matrix_rank_eq_width A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_left_inverse_symmetric_penrose_equations A Aplus hleft hSym
  · exact theorem7_5_lp_column_equilibration_exists_min_right_scalings_of_rect_left_inverse_of_normalized
      hm hn hpq A Aplus hleft

/-- Theorem 7.5, finite-real conjugate-row `sInf` branch with the printed
full-row-rank side condition: construct the Penrose inverse candidate and
prove the row scaling inequality for the same `A⁺`. -/
theorem theorem7_5_lp_dual_row_equilibration_exists_penrose_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ch7LpLeftScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
          (ch7RowDualEquilibratingScaleLpOfReal q A)
          (fun i : Fin m => ch7RectRowDualLpNormOfReal q A i) ≤
        (m : ℝ) ^ p⁻¹ *
          sInf (ch7LpLeftScaledCondSetOfReal hm hn p (le_of_lt hpq.lt) A Aplus) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_lp_dual_row_equilibration_le_card_rpow_sInf_left_scalings_of_rect_right_inverse
      hm hn hpq A Aplus hright

/-- Theorem 7.5, finite-real conjugate-row source-`min` branch with the
printed full-row-rank side condition: construct the Penrose inverse candidate
and, for the same `A⁺`, apply the source-minimum inequality whenever a minimum
certificate is supplied.  This follows the conjugate-row proof route used by
the source. -/
theorem theorem7_5_lp_dual_row_equilibration_exists_penrose_min_imp_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      (∀ {μ : ℝ},
        IsLeast (ch7LpLeftScaledCondSetOfReal hm hn p (le_of_lt hpq.lt) A Aplus) μ →
          ch7LpLeftScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
              (ch7RowDualEquilibratingScaleLpOfReal q A)
              (fun i : Fin m => ch7RectRowDualLpNormOfReal q A i) ≤
            (m : ℝ) ^ p⁻¹ * μ) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · intro μ hmin
    exact theorem7_5_lp_dual_row_equilibration_le_card_rpow_min_left_scalings_of_rect_right_inverse
      hm hn hpq A Aplus hright hmin

/-- Theorem 7.5, finite-real conjugate-row source-`min` branch with the
printed full-row-rank side condition: construct the Penrose inverse candidate
and extract the minimum over reciprocal left scalings for the same `A⁺`. -/
theorem theorem7_5_lp_dual_row_equilibration_exists_penrose_min_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast
          (ch7LpLeftScaledCondSetOfReal hm hn p (le_of_lt hpq.lt) A Aplus)
          μ ∧
        ch7LpLeftScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
            (ch7RowDualEquilibratingScaleLpOfReal q A)
            (fun i : Fin m => ch7RectRowDualLpNormOfReal q A i) ≤
          (m : ℝ) ^ p⁻¹ * μ := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_lp_dual_row_equilibration_exists_min_left_scalings_of_rect_right_inverse
      hm hn hpq A Aplus hright

/-- Normalized-slice version of the compactness-backed Theorem 7.5
finite-real conjugate-row Penrose/rank source-`min` wrapper. -/
theorem theorem7_5_lp_dual_row_equilibration_exists_penrose_min_normalized_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m) (hn : 0 < n) {p q : ℝ}
    (hpq : p.HolderConjugate q)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast
          (ch7LpLeftScaledCondNormalizedSetOfReal
            hm hn p (le_of_lt hpq.lt) A Aplus)
          μ ∧
        ch7LpLeftScaledCondOfReal hm hn p (le_of_lt hpq.lt) A Aplus
            (ch7RowDualEquilibratingScaleLpOfReal q A)
            (fun i : Fin m => ch7RectRowDualLpNormOfReal q A i) ≤
          (m : ℝ) ^ p⁻¹ * μ := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_lp_dual_row_equilibration_exists_min_left_scalings_of_rect_right_inverse_of_normalized
      hm hn hpq A Aplus hright

/-- Theorem 7.5, `p = 2` row `sInf` branch with the printed full-row-rank
side condition: construct the Penrose inverse candidate and prove the scaling
inequality for the same `A⁺`. -/
theorem theorem7_5_p2_row_equilibration_exists_penrose_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ch7Op2LeftScaledCond A Aplus
          (ch7RowEquilibratingScale2Rect A)
          (fun i : Fin m => ch7RectRowNorm2 A i) ≤
        Real.sqrt (m : ℝ) * sInf (ch7Op2LeftScaledCondSet A Aplus) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_p2_row_equilibration_le_sqrt_card_sInf_left_scalings_of_rect_right_inverse
      hm A Aplus hright

/-- Theorem 7.5, `p = 2` row source-`min` branch with the printed
full-row-rank side condition: construct the Penrose inverse candidate and, for
the same `A⁺`, apply the source-minimum inequality whenever a minimum
certificate is supplied.  This does not assert non-endpoint minimizer
existence. -/
theorem theorem7_5_p2_row_equilibration_exists_penrose_min_imp_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      (∀ {μ : ℝ}, IsLeast (ch7Op2LeftScaledCondSet A Aplus) μ →
        ch7Op2LeftScaledCond A Aplus
            (ch7RowEquilibratingScale2Rect A)
            (fun i : Fin m => ch7RectRowNorm2 A i) ≤
          Real.sqrt (m : ℝ) * μ) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · intro μ hmin
    exact theorem7_5_p2_row_equilibration_le_sqrt_card_min_left_scalings_of_rect_right_inverse
      hm A Aplus hright hmin

/-- Theorem 7.5, `p = 2` row normalized-slice source-`min` branch with the
printed full-row-rank side condition.  The constructed Penrose inverse is the
same `A⁺` used by the normalized value-set certificate. -/
theorem theorem7_5_p2_row_equilibration_exists_penrose_min_normalized_imp_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      (∀ {μ : ℝ}, IsLeast (ch7Op2LeftScaledCondNormalizedSet A Aplus) μ →
        ch7Op2LeftScaledCond A Aplus
            (ch7RowEquilibratingScale2Rect A)
            (fun i : Fin m => ch7RectRowNorm2 A i) ≤
          Real.sqrt (m : ℝ) * μ) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · intro μ hmin
    exact theorem7_5_p2_row_equilibration_le_sqrt_card_min_left_scalings_of_rect_right_inverse_of_normalized
      hm A Aplus hright hmin

/-- Theorem 7.5, `p = 2` row source-`min` branch with the printed
full-row-rank side condition: construct the Penrose inverse candidate and
extract the minimum over reciprocal left scalings for the same `A⁺`. -/
theorem theorem7_5_p2_row_equilibration_exists_penrose_min_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast (ch7Op2LeftScaledCondSet A Aplus) μ ∧
        ch7Op2LeftScaledCond A Aplus
            (ch7RowEquilibratingScale2Rect A)
            (fun i : Fin m => ch7RectRowNorm2 A i) ≤
          Real.sqrt (m : ℝ) * μ := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_p2_row_equilibration_exists_min_left_scalings_of_rect_right_inverse
      hm A Aplus hright

/-- Normalized-slice version of the compactness-backed Theorem 7.5 `p = 2`
row Penrose/rank source-`min` wrapper. -/
theorem theorem7_5_p2_row_equilibration_exists_penrose_min_normalized_of_matrix_rank_eq_height
    {m n : ℕ} (hm : 0 < m)
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      ∃ μ : ℝ,
        IsLeast (ch7Op2LeftScaledCondNormalizedSet A Aplus) μ ∧
        ch7Op2LeftScaledCond A Aplus
            (ch7RowEquilibratingScale2Rect A)
            (fun i : Fin m => ch7RectRowNorm2 A i) ≤
          Real.sqrt (m : ℝ) * μ := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_p2_row_equilibration_exists_min_left_scalings_of_rect_right_inverse_of_normalized
      hm A Aplus hright

/-- Theorem 7.5, `p = ∞` row branch with the printed full-row-rank side
condition: construct the Penrose inverse candidate and prove the endpoint
least-element scaling statement for the same `A⁺`. -/
theorem theorem7_5_pinf_row_equilibration_exists_penrose_of_matrix_rank_eq_height
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (hrank : (Matrix.of A).rank = m) :
    ∃ Aplus : Fin n → Fin m → ℝ,
      (rectMatMul (rectMatMul A Aplus) A = A ∧
        rectMatMul (rectMatMul Aplus A) Aplus = Aplus ∧
        IsSymmetricFiniteMatrix (rectMatMul A Aplus) ∧
        IsSymmetricFiniteMatrix (rectMatMul Aplus A)) ∧
      IsLeast (ch7InfNormLeftScaledCondSet A Aplus)
        (ch7InfNormLeftScaledCond A Aplus
          (ch7RowEquilibratingScale1Rect A)
          (fun i : Fin m => ch7RectRowNorm1 A i)) := by
  obtain ⟨Aplus, hright, hSym⟩ :=
    theorem7_5_exists_rect_right_inverse_symmetric_domain_of_matrix_rank_eq_height A hrank
  refine ⟨Aplus, ?_, ?_⟩
  · exact theorem7_5_rect_right_inverse_symmetric_penrose_equations A Aplus hright hSym
  · exact theorem7_5_pinf_row_equilibration_isLeast_left_scalings_of_rect_right_inverse
      A Aplus hright

/-- Theorem 7.5 Moore-Penrose dependency, algebraic projection half:
an explicit rectangular left inverse `A⁺ A = I` makes the range-side product
`A A⁺` idempotent.  This is only the algebraic projection fact; it does not
assert that `A⁺` is the Moore-Penrose inverse or that the projection is
orthogonal. -/
theorem theorem7_5_rect_left_inverse_range_projection_idempotent
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0) :
    rectMatMul (rectMatMul A Aplus) (rectMatMul A Aplus) =
      rectMatMul A Aplus := by
  apply rectMatMul_rangeProjection_idempotent_of_left_inverse A Aplus
  ext i j
  simpa [rectMatMul, idMatrix] using hleft i j

/-- Theorem 7.5 Moore-Penrose dependency, algebraic projection half:
an explicit rectangular right inverse `A A⁺ = I` makes the domain-side product
`A⁺ A` idempotent.  This is only the algebraic projection fact; it does not
assert that `A⁺` is the Moore-Penrose inverse or that the projection is
orthogonal. -/
theorem theorem7_5_rect_right_inverse_domain_projection_idempotent
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0) :
    rectMatMul (rectMatMul Aplus A) (rectMatMul Aplus A) =
      rectMatMul Aplus A := by
  apply rectMatMul_domainProjection_idempotent_of_right_inverse A Aplus
  ext i j
  simpa [rectMatMul, idMatrix] using hright i j

/-- Theorem 7.5 Moore-Penrose dependency, orthogonal-projection contraction
half: a rectangular left inverse whose range-side product `A A⁺` is symmetric
has operator-2 norm at most one.  The symmetry condition is the explicit local
Penrose-style side condition; this theorem does not construct `A⁺`. -/
theorem theorem7_5_rect_left_inverse_symmetric_range_projection_op2Le_one
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus)) :
    rectOpNorm2Le (rectMatMul A Aplus) 1 := by
  apply rectOpNorm2Le_rangeProjection_of_symmetric_left_inverse A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hleft i j
  · exact hSym

/-- Theorem 7.5 Moore-Penrose dependency, orthogonal-projection contraction
half: a rectangular right inverse whose domain-side product `A⁺ A` is
symmetric has operator-2 norm at most one.  The symmetry condition is the
explicit local Penrose-style side condition; this theorem does not construct
`A⁺`. -/
theorem theorem7_5_rect_right_inverse_symmetric_domain_projection_op2Le_one
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A)) :
    rectOpNorm2Le (rectMatMul Aplus A) 1 := by
  apply rectOpNorm2Le_domainProjection_of_symmetric_right_inverse A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hright i j
  · exact hSym

/-- Theorem 7.5 Moore-Penrose dependency, range-fixing half: an explicit
rectangular left inverse `A⁺ A = I` makes `A A⁺` act as identity on the range
of `A`.  This is a projection dependency only; it does not construct the
Moore-Penrose inverse. -/
theorem theorem7_5_rect_left_inverse_range_projection_fixes_range
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (x : Fin n → ℝ) :
    rectMatMulVec (rectMatMul A Aplus) (rectMatMulVec A x) =
      rectMatMulVec A x := by
  apply rectMatMulVec_rangeProjection_apply_range_of_left_inverse A Aplus
  ext i j
  simpa [rectMatMul, idMatrix] using hleft i j

/-- Theorem 7.5 Moore-Penrose dependency, domain-fixing half: an explicit
rectangular right inverse `A A⁺ = I` makes `A⁺ A` act as identity on the range
of `A⁺`.  This is a projection dependency only; it does not construct the
Moore-Penrose inverse. -/
theorem theorem7_5_rect_right_inverse_domain_projection_fixes_range
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (y : Fin m → ℝ) :
    rectMatMulVec (rectMatMul Aplus A) (rectMatMulVec Aplus y) =
      rectMatMulVec Aplus y := by
  apply rectMatMulVec_domainProjection_apply_range_of_right_inverse A Aplus
  ext i j
  simpa [rectMatMul, idMatrix] using hright i j

/-- Theorem 7.5 Moore-Penrose dependency, orthogonality half: a rectangular
left inverse whose range-side product `A A⁺` is symmetric has residual
`y - A A⁺ y` orthogonal to every vector in the range of `A`.  The symmetry
condition is the explicit local Penrose-style side condition; this theorem
does not construct `A⁺`. -/
theorem theorem7_5_rect_left_inverse_symmetric_range_projection_residual_orthogonal_range
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) (x : Fin n → ℝ) :
    (∑ i : Fin m,
      (y i - rectMatMulVec (rectMatMul A Aplus) y i) *
        rectMatMulVec A x i) = 0 := by
  apply
    rectMatMulVec_rangeProjection_residual_orthogonal_range_of_symmetric_left_inverse
      A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hleft i j
  · exact hSym

/-- Theorem 7.5 Moore-Penrose dependency, orthogonality half: a rectangular
right inverse whose domain-side product `A⁺ A` is symmetric has residual
`x - A⁺ A x` orthogonal to every vector in the range of `A⁺`.  The symmetry
condition is the explicit local Penrose-style side condition; this theorem
does not construct `A⁺`. -/
theorem theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_orthogonal_range
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) (y : Fin m → ℝ) :
    (∑ j : Fin n,
      (x j - rectMatMulVec (rectMatMul Aplus A) x j) *
        rectMatMulVec Aplus y j) = 0 := by
  apply
    rectMatMulVec_domainProjection_residual_orthogonal_range_of_symmetric_right_inverse
      A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hright i j
  · exact hSym

/-- Theorem 7.5 Moore-Penrose dependency, least-squares projection half: a
rectangular left inverse whose range-side product `A A⁺` is symmetric gives
the squared Euclidean best-approximation inequality against all vectors in the
range of `A`.  This theorem is still conditional on the explicit local
Penrose-style symmetry witness and does not construct `A⁺`. -/
theorem theorem7_5_rect_left_inverse_symmetric_range_projection_residual_normSq_le_range_residual
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) (x : Fin n → ℝ) :
    vecNorm2Sq (fun i : Fin m =>
      y i - rectMatMulVec (rectMatMul A Aplus) y i) ≤
      vecNorm2Sq (fun i : Fin m => y i - rectMatMulVec A x i) := by
  apply
    rectMatMulVec_rangeProjection_residual_normSq_le_range_residual_of_symmetric_left_inverse
      A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hleft i j
  · exact hSym

/-- Theorem 7.5 Moore-Penrose dependency, least-squares projection half: a
rectangular right inverse whose domain-side product `A⁺ A` is symmetric gives
the squared Euclidean best-approximation inequality against all vectors in the
range of `A⁺`.  This theorem is still conditional on the explicit local
Penrose-style symmetry witness and does not construct `A⁺`. -/
theorem theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_normSq_le_range_residual
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) (y : Fin m → ℝ) :
    vecNorm2Sq (fun j : Fin n =>
      x j - rectMatMulVec (rectMatMul Aplus A) x j) ≤
      vecNorm2Sq (fun j : Fin n => x j - rectMatMulVec Aplus y j) := by
  apply
    rectMatMulVec_domainProjection_residual_normSq_le_range_residual_of_symmetric_right_inverse
      A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hright i j
  · exact hSym

/-- Norm form of the Theorem 7.5 Moore-Penrose dependency least-squares
projection inequality for `A A⁺`. -/
theorem theorem7_5_rect_left_inverse_symmetric_range_projection_residual_norm_le_range_residual
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) (x : Fin n → ℝ) :
    vecNorm2 (fun i : Fin m =>
      y i - rectMatMulVec (rectMatMul A Aplus) y i) ≤
      vecNorm2 (fun i : Fin m => y i - rectMatMulVec A x i) := by
  apply
    rectMatMulVec_rangeProjection_residual_norm_le_range_residual_of_symmetric_left_inverse
      A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hleft i j
  · exact hSym

/-- Norm form of the Theorem 7.5 Moore-Penrose dependency least-squares
projection inequality for `A⁺ A`. -/
theorem theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_norm_le_range_residual
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) (y : Fin m → ℝ) :
    vecNorm2 (fun j : Fin n =>
      x j - rectMatMulVec (rectMatMul Aplus A) x j) ≤
      vecNorm2 (fun j : Fin n => x j - rectMatMulVec Aplus y j) := by
  apply
    rectMatMulVec_domainProjection_residual_norm_le_range_residual_of_symmetric_right_inverse
      A Aplus
  · ext i j
    simpa [rectMatMul, idMatrix] using hright i j
  · exact hSym

/-- Theorem 7.5 Moore-Penrose dependency, set-level least-squares projection
half: the range-side projection residual has least squared Euclidean residual
among all vectors in the range of `A`. -/
theorem theorem7_5_rect_left_inverse_symmetric_range_projection_residual_normSq_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) :
    IsLeast
      {r : ℝ | ∃ x : Fin n → ℝ,
        r = vecNorm2Sq (fun i : Fin m => y i - rectMatMulVec A x i)}
      (vecNorm2Sq (fun i : Fin m =>
        y i - rectMatMulVec (rectMatMul A Aplus) y i)) := by
  constructor
  · refine ⟨rectMatMulVec Aplus y, ?_⟩
    rw [rectMatMulVec_rectMatMul]
  · intro r hr
    rcases hr with ⟨x, rfl⟩
    exact
      theorem7_5_rect_left_inverse_symmetric_range_projection_residual_normSq_le_range_residual
        A Aplus hleft hSym y x

/-- Theorem 7.5 Moore-Penrose dependency, set-level least-squares projection
half: the domain-side projection residual has least squared Euclidean residual
among all vectors in the range of `A⁺`. -/
theorem theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_normSq_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) :
    IsLeast
      {r : ℝ | ∃ y : Fin m → ℝ,
        r = vecNorm2Sq (fun j : Fin n => x j - rectMatMulVec Aplus y j)}
      (vecNorm2Sq (fun j : Fin n =>
        x j - rectMatMulVec (rectMatMul Aplus A) x j)) := by
  constructor
  · refine ⟨rectMatMulVec A x, ?_⟩
    rw [rectMatMulVec_rectMatMul]
  · intro r hr
    rcases hr with ⟨y, rfl⟩
    exact
      theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_normSq_le_range_residual
        A Aplus hright hSym x y

/-- Theorem 7.5 Moore-Penrose dependency, set-level least-squares projection
half: the range-side projection residual has least Euclidean residual among all
vectors in the range of `A`. -/
theorem theorem7_5_rect_left_inverse_symmetric_range_projection_residual_norm_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hleft : ∀ i j : Fin n,
      ∑ k : Fin m, Aplus i k * A k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) :
    IsLeast
      {r : ℝ | ∃ x : Fin n → ℝ,
        r = vecNorm2 (fun i : Fin m => y i - rectMatMulVec A x i)}
      (vecNorm2 (fun i : Fin m =>
        y i - rectMatMulVec (rectMatMul A Aplus) y i)) := by
  constructor
  · refine ⟨rectMatMulVec Aplus y, ?_⟩
    rw [rectMatMulVec_rectMatMul]
  · intro r hr
    rcases hr with ⟨x, rfl⟩
    exact
      theorem7_5_rect_left_inverse_symmetric_range_projection_residual_norm_le_range_residual
        A Aplus hleft hSym y x

/-- Theorem 7.5 Moore-Penrose dependency, set-level least-squares projection
half: the domain-side projection residual has least Euclidean residual among
all vectors in the range of `A⁺`. -/
theorem theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_norm_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hright : ∀ i j : Fin m,
      ∑ k : Fin n, A i k * Aplus k j = if i = j then 1 else 0)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) :
    IsLeast
      {r : ℝ | ∃ y : Fin m → ℝ,
        r = vecNorm2 (fun j : Fin n => x j - rectMatMulVec Aplus y j)}
      (vecNorm2 (fun j : Fin n =>
        x j - rectMatMulVec (rectMatMul Aplus A) x j)) := by
  constructor
  · refine ⟨rectMatMulVec A x, ?_⟩
    rw [rectMatMulVec_rectMatMul]
  · intro r hr
    rcases hr with ⟨y, rfl⟩
    exact
      theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_norm_le_range_residual
        A Aplus hright hSym x y

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation and
full-column-rank injectivity, symmetry of `A A⁺` makes the range projection a
Euclidean contraction. -/
theorem theorem7_5_rect_penrose_injective_symmetric_range_projection_op2Le_one
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAinj : Function.Injective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus)) :
    rectOpNorm2Le (rectMatMul A Aplus) 1 :=
  theorem7_5_rect_left_inverse_symmetric_range_projection_op2Le_one A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective
      A Aplus hPenrose1 hAinj)
    hSym

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation and
full-row-rank surjectivity, symmetry of `A⁺ A` makes the domain projection a
Euclidean contraction. -/
theorem theorem7_5_rect_penrose_surjective_symmetric_domain_projection_op2Le_one
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAsurj : Function.Surjective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A)) :
    rectOpNorm2Le (rectMatMul Aplus A) 1 :=
  theorem7_5_rect_right_inverse_symmetric_domain_projection_op2Le_one A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective
      A Aplus hPenrose1 hAsurj)
    hSym

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation and
full-column-rank injectivity, the range-projection residual is orthogonal to
every vector in the range of `A`. -/
theorem theorem7_5_rect_penrose_injective_symmetric_range_projection_residual_orthogonal_range
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAinj : Function.Injective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) (x : Fin n → ℝ) :
    (∑ i : Fin m,
      (y i - rectMatMulVec (rectMatMul A Aplus) y i) *
        rectMatMulVec A x i) = 0 :=
  theorem7_5_rect_left_inverse_symmetric_range_projection_residual_orthogonal_range
    A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective
      A Aplus hPenrose1 hAinj)
    hSym y x

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation and
full-row-rank surjectivity, the domain-projection residual is orthogonal to
every vector in the range of `A⁺`. -/
theorem theorem7_5_rect_penrose_surjective_symmetric_domain_projection_residual_orthogonal_range
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAsurj : Function.Surjective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) (y : Fin m → ℝ) :
    (∑ j : Fin n,
      (x j - rectMatMulVec (rectMatMul Aplus A) x j) *
        rectMatMulVec Aplus y j) = 0 :=
  theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_orthogonal_range
    A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective
      A Aplus hPenrose1 hAsurj)
    hSym x y

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation and
full-column-rank injectivity, the range-side Moore-Penrose projection has
least squared Euclidean residual among all vectors in the range of `A`. -/
theorem theorem7_5_rect_penrose_injective_symmetric_range_projection_residual_normSq_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAinj : Function.Injective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) :
    IsLeast
      {r : ℝ | ∃ x : Fin n → ℝ,
        r = vecNorm2Sq (fun i : Fin m => y i - rectMatMulVec A x i)}
      (vecNorm2Sq (fun i : Fin m =>
        y i - rectMatMulVec (rectMatMul A Aplus) y i)) :=
  theorem7_5_rect_left_inverse_symmetric_range_projection_residual_normSq_isLeast
    A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective
      A Aplus hPenrose1 hAinj)
    hSym y

/-- Theorem 7.5 Moore-Penrose bridge: under the first Penrose equation and
full-row-rank surjectivity, the domain-side Moore-Penrose projection has least
squared Euclidean residual among all vectors in the range of `A⁺`. -/
theorem theorem7_5_rect_penrose_surjective_symmetric_domain_projection_residual_normSq_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAsurj : Function.Surjective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) :
    IsLeast
      {r : ℝ | ∃ y : Fin m → ℝ,
        r = vecNorm2Sq (fun j : Fin n => x j - rectMatMulVec Aplus y j)}
      (vecNorm2Sq (fun j : Fin n =>
        x j - rectMatMulVec (rectMatMul Aplus A) x j)) :=
  theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_normSq_isLeast
    A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective
      A Aplus hPenrose1 hAsurj)
    hSym x

/-- Norm version of the full-column-rank Moore-Penrose least-residual bridge. -/
theorem theorem7_5_rect_penrose_injective_symmetric_range_projection_residual_norm_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAinj : Function.Injective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) :
    IsLeast
      {r : ℝ | ∃ x : Fin n → ℝ,
        r = vecNorm2 (fun i : Fin m => y i - rectMatMulVec A x i)}
      (vecNorm2 (fun i : Fin m =>
        y i - rectMatMulVec (rectMatMul A Aplus) y i)) :=
  theorem7_5_rect_left_inverse_symmetric_range_projection_residual_norm_isLeast
    A Aplus
    (theorem7_5_rect_left_inverse_of_penrose1_rectMatMulVec_injective
      A Aplus hPenrose1 hAinj)
    hSym y

/-- Norm version of the full-row-rank Moore-Penrose least-residual bridge. -/
theorem theorem7_5_rect_penrose_surjective_symmetric_domain_projection_residual_norm_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hAsurj : Function.Surjective (rectMatMulVec A))
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) :
    IsLeast
      {r : ℝ | ∃ y : Fin m → ℝ,
        r = vecNorm2 (fun j : Fin n => x j - rectMatMulVec Aplus y j)}
      (vecNorm2 (fun j : Fin n =>
        x j - rectMatMulVec (rectMatMul Aplus A) x j)) :=
  theorem7_5_rect_right_inverse_symmetric_domain_projection_residual_norm_isLeast
    A Aplus
    (theorem7_5_rect_right_inverse_of_penrose1_rectMatMulVec_surjective
      A Aplus hPenrose1 hAsurj)
    hSym x

/-- Printed-rank form of the full-column-rank Moore-Penrose squared
least-residual bridge. -/
theorem theorem7_5_rect_penrose_matrix_rank_eq_width_symmetric_range_projection_residual_normSq_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = n)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (y : Fin m → ℝ) :
    IsLeast
      {r : ℝ | ∃ x : Fin n → ℝ,
        r = vecNorm2Sq (fun i : Fin m => y i - rectMatMulVec A x i)}
      (vecNorm2Sq (fun i : Fin m =>
        y i - rectMatMulVec (rectMatMul A Aplus) y i)) :=
  theorem7_5_rect_penrose_injective_symmetric_range_projection_residual_normSq_isLeast
    A Aplus hPenrose1
    (ch7_rectMatMulVec_injective_of_matrix_rank_eq_width A hrank)
    hSym y

/-- Printed-rank form of the full-row-rank Moore-Penrose squared
least-residual bridge. -/
theorem theorem7_5_rect_penrose_matrix_rank_eq_height_symmetric_domain_projection_residual_normSq_isLeast
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (hPenrose1 : rectMatMul (rectMatMul A Aplus) A = A)
    (hrank : (Matrix.of A).rank = m)
    (hSym : IsSymmetricFiniteMatrix (rectMatMul Aplus A))
    (x : Fin n → ℝ) :
    IsLeast
      {r : ℝ | ∃ y : Fin m → ℝ,
        r = vecNorm2Sq (fun j : Fin n => x j - rectMatMulVec Aplus y j)}
      (vecNorm2Sq (fun j : Fin n =>
        x j - rectMatMulVec (rectMatMul Aplus A) x j)) :=
  theorem7_5_rect_penrose_surjective_symmetric_domain_projection_residual_normSq_isLeast
    A Aplus hPenrose1
    (ch7_rectMatMulVec_surjective_of_matrix_rank_eq_height A hrank)
    hSym x

-- ============================================================
-- Corollary 7.6 dependency: SPD diagonal scaling via Cholesky factors
-- ============================================================


























































































































-- ============================================================
-- Theorem 7.7: Stewart-Sun Frobenius right scaling
-- ============================================================
























































































































































































































































































-- ============================================================
-- Theorem 7.8 / Problem 7.10: Bauer two-sided scaling algebraic core
-- ============================================================























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































section Ch7MatrixOperatorNorm

open scoped Matrix.Norms.Operator





























































































































































































































































end Ch7MatrixOperatorNorm











































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.10(e): one-norm Bauer transpose reductions
-- ============================================================























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.15: Hadamard product algebra under two-sided scaling
-- ============================================================




























































































































































































































































































































































































































































































































































































































































































































































































































-- ============================================================
-- Theorem 7.4, relative infinity-norm form of (7.10)
-- ============================================================












































































































































































































































































































































































-- ============================================================
-- Problem 7.2: residual/error sandwich in the infinity norm
-- ============================================================

































































































































































































-- ============================================================
-- Theorem 7.2: arbitrary subordinate-norm form of (7.4)
-- ============================================================






























































































































-- ============================================================
-- Equation (7.5): generic normwise condition number
-- ============================================================

























































































































































































































































































































































































































































































-- ============================================================
-- Problem 7.7: dropping the right-hand-side perturbation budget
-- ============================================================
















































































































































































































































































































































































-- ============================================================
-- Problem 7.8: rectangular Frobenius backward error
-- ============================================================


































































































































































































































































































































































-- ============================================================
-- Lemma 7.9: practical error bounds
-- ============================================================






























































































-- ============================================================
-- Equations (7.30) and (7.31): computed residual bounds
-- ============================================================




































































































-- ============================================================
-- Problem 7.13: sparse computed residual bounds
-- ============================================================





































































































































































































































































































































-- ============================================================
-- Equation (7.32): perturbation theory by calculus
-- ============================================================


















































































































































-- ============================================================
-- Equation (7.33): stochastic matrices
-- ============================================================














/-! ## Theorem 7.8 (Bauer) — source-labeled aliases

Higham 2nd ed., Chapter 7, §7.3, Theorem 7.8 (p. 127, eq. (7.24)): for
nonsingular `A` with `|A||A⁻¹|` and `|A⁻¹||A|` irreducible,
`min_{D₁,D₂ ∈ 𝒟ₙ} κ_∞(D₁AD₂) = ρ(|A||A⁻¹|)`. The book's proof is delegated
to Problem 7.10, so the full formalization lives in the `problem7_10*`
family (with `B = |A|`, `C = |A⁻¹|`); these thin aliases give the primary
source label its own traceable surface. -/




















end NumStability
