import NumStability.Analysis.DoubleRounding
import NumStability.Source.Higham.Chapter02.Problem10.DivisionRoundTrip.Basic

/-!
# IEEE double audit for Higham Problem 2.10

The complete division-round-trip certificate, including its large private
proof closure, lives here atomically so every retained public theorem remains
available from a canonical Higham source endpoint.
-/

noncomputable section

namespace NumStability

namespace FloatingPointFormat

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 3 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (3 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (3 : ℝ) ≤ (2 : ℝ) ^ (2 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 3) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_ninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 9 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (9 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (9 : ℝ) ≤ (2 : ℝ) ^ (4 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 9) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_seventeenth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 17 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (17 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (17 : ℝ) ≤ (2 : ℝ) ^ (5 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 17) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 33 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (33 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (33 : ℝ) ≤ (2 : ℝ) ^ (6 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 33) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 65 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (65 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (65 : ℝ) ≤ (2 : ℝ) ^ (7 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 65) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredtwentyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 129 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (129 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (129 : ℝ) ≤ (2 : ℝ) ^ (8 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 129) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredfiftyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 257 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (257 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (257 : ℝ) ≤ (2 : ℝ) ^ (9 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 257) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredthirteenth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 513 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (513 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (513 : ℝ) ≤ (2 : ℝ) ^ (10 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 513) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onethousandtwentyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1025 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1025 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1025 : ℝ) ≤ (2 : ℝ) ^ (11 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1025) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twothousandfortyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2049 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2049 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2049 : ℝ) ≤ (2 : ℝ) ^ (12 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2049) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fourthousandninetyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4097 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4097 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4097 : ℝ) ≤ (2 : ℝ) ^ (13 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4097) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_eightthousandonehundredninetythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 8193 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (8193 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (8193 : ℝ) ≤ (2 : ℝ) ^ (14 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 8193) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixteenthousandthreehundredeightyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 16385 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (16385 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (16385 : ℝ) ≤ (2 : ℝ) ^ (15 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 16385) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtytwothousandsevenhundredsixtyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 32769 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (32769 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (32769 : ℝ) ≤ (2 : ℝ) ^ (16 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 32769) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtyfivethousandfivehundredthirtyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 65537 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (65537 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (65537 : ℝ) ≤ (2 : ℝ) ^ (17 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 65537) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredthirtyonethousandseventythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 131073 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (131073 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (131073 : ℝ) ≤ (2 : ℝ) ^ (18 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 131073) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredsixtytwothousandonehundredfortyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 262145 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (262145 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (262145 : ℝ) ≤ (2 : ℝ) ^ (19 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 262145) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredtwentyfourthousandtwohundredeightyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 524289 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (524289 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (524289 : ℝ) ≤ (2 : ℝ) ^ (20 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 524289) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onemillionfortyeightthousandfivehundredseventyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1048577 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1048577 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1048577 : ℝ) ≤ (2 : ℝ) ^ (21 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1048577) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twomillionninetyseventhousandonehundredfiftythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2097153 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2097153 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2097153 : ℝ) ≤ (2 : ℝ) ^ (22 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2097153) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fourmilliononehundredninetyfourthousandthreehundredfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4194305 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4194305 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4194305 : ℝ) ≤ (2 : ℝ) ^ (23 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4194305) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_eightmillionthreehundredeightyeightthousandsixhundredninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 8388609 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (8388609 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (8388609 : ℝ) ≤ (2 : ℝ) ^ (24 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 8388609) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 16777217 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (16777217 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (16777217 : ℝ) ≤ (2 : ℝ) ^ (25 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 16777217) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 33554433 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (33554433 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (33554433 : ℝ) ≤ (2 : ℝ) ^ (26 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 33554433) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 67108865 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (67108865 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (67108865 : ℝ) ≤ (2 : ℝ) ^ (27 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 67108865) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 134217729 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (134217729 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (134217729 : ℝ) ≤ (2 : ℝ) ^ (28 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 134217729) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 268435457 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (268435457 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (268435457 : ℝ) ≤ (2 : ℝ) ^ (29 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 268435457) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 536870913 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (536870913 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (536870913 : ℝ) ≤ (2 : ℝ) ^ (30 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 536870913) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1073741825 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1073741825 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1073741825 : ℝ) ≤ (2 : ℝ) ^ (31 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1073741825) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2147483649 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2147483649 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2147483649 : ℝ) ≤ (2 : ℝ) ^ (32 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2147483649) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4294967297 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4294967297 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4294967297 : ℝ) ≤ (2 : ℝ) ^ (33 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4294967297) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 8589934593 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (8589934593 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (8589934593 : ℝ) ≤ (2 : ℝ) ^ (34 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 8589934593) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 17179869185 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (17179869185 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (17179869185 : ℝ) ≤ (2 : ℝ) ^ (35 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 17179869185) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 34359738369 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (34359738369 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (34359738369 : ℝ) ≤ (2 : ℝ) ^ (36 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 34359738369) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseventh :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 68719476737 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (68719476737 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (68719476737 : ℝ) ≤ (2 : ℝ) ^ (37 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 68719476737) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythird :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 137438953473 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (137438953473 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (137438953473 : ℝ) ≤ (2 : ℝ) ^ (38 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 137438953473) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 274877906945 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (274877906945 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (274877906945 : ℝ) ≤ (2 : ℝ) ^ (39 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 274877906945) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 549755813889 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (549755813889 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (549755813889 : ℝ) ≤ (2 : ℝ) ^ (40 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 549755813889) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1099511627777 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1099511627777 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1099511627777 : ℝ) ≤ (2 : ℝ) ^ (41 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1099511627777) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_one_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2199023255553 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2199023255553 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2199023255553 : ℝ) ≤ (2 : ℝ) ^ (42 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2199023255553) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_two_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4398046511105 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4398046511105 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4398046511105 : ℝ) ≤ (2 : ℝ) ^ (43 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4398046511105) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_three_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 8796093022209 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (8796093022209 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (8796093022209 : ℝ) ≤ (2 : ℝ) ^ (44 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 8796093022209) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_four_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 17592186044417 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (17592186044417 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (17592186044417 : ℝ) ≤ (2 : ℝ) ^ (45 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 17592186044417) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_five_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 35184372088833 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (35184372088833 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (35184372088833 : ℝ) ≤ (2 : ℝ) ^ (46 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 35184372088833) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_six_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 70368744177665 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (70368744177665 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (70368744177665 : ℝ) ≤ (2 : ℝ) ^ (47 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 70368744177665) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_seven_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 140737488355329 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (140737488355329 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (140737488355329 : ℝ) ≤ (2 : ℝ) ^ (48 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 140737488355329) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_eight_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 281474976710657 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (281474976710657 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (281474976710657 : ℝ) ≤ (2 : ℝ) ^ (49 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 281474976710657) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_nine_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 562949953421313 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (562949953421313 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (562949953421313 : ℝ) ≤ (2 : ℝ) ^ (50 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 562949953421313) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1125899906842625 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1125899906842625 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1125899906842625 : ℝ) ≤ (2 : ℝ) ^ (51 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1125899906842625) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_one_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2251799813685249 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2251799813685249 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2251799813685249 : ℝ) ≤ (2 : ℝ) ^ (52 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2251799813685249) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_two_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4503599627370497 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4503599627370497 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4503599627370497 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4503599627370497) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_three_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 9007199254740993 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (9007199254740993 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (9007199254740993 : ℝ) ≤ (2 : ℝ) ^ (54 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 9007199254740993) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_four_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 18014398509481985 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (18014398509481985 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (18014398509481985 : ℝ) ≤ (2 : ℝ) ^ (55 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 18014398509481985) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_five_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 36028797018963969 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (36028797018963969 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (36028797018963969 : ℝ) ≤ (2 : ℝ) ^ (56 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 36028797018963969) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_six_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 72057594037927937 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (72057594037927937 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (72057594037927937 : ℝ) ≤ (2 : ℝ) ^ (57 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 72057594037927937) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_seven_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 144115188075855873 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (144115188075855873 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (144115188075855873 : ℝ) ≤ (2 : ℝ) ^ (58 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 144115188075855873) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_eight_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 288230376151711745 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (288230376151711745 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (288230376151711745 : ℝ) ≤ (2 : ℝ) ^ (59 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 288230376151711745) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_nine_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 576460752303423489 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (576460752303423489 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (576460752303423489 : ℝ) ≤ (2 : ℝ) ^ (60 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 576460752303423489) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1152921504606846977 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1152921504606846977 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1152921504606846977 : ℝ) ≤ (2 : ℝ) ^ (61 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1152921504606846977) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_one_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2305843009213693953 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2305843009213693953 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2305843009213693953 : ℝ) ≤ (2 : ℝ) ^ (62 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2305843009213693953) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_two_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4611686018427387905 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4611686018427387905 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4611686018427387905 : ℝ) ≤ (2 : ℝ) ^ (63 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4611686018427387905) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_three_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 9223372036854775809 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (9223372036854775809 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (9223372036854775809 : ℝ) ≤ (2 : ℝ) ^ (64 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 9223372036854775809) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_four_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 18446744073709551617 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (18446744073709551617 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (18446744073709551617 : ℝ) ≤ (2 : ℝ) ^ (65 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 18446744073709551617) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_five_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 36893488147419103233 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (36893488147419103233 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (36893488147419103233 : ℝ) ≤ (2 : ℝ) ^ (66 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 36893488147419103233) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_six_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 73786976294838206465 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (73786976294838206465 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (73786976294838206465 : ℝ) ≤ (2 : ℝ) ^ (67 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 73786976294838206465) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_seven_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 147573952589676412929 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (147573952589676412929 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (147573952589676412929 : ℝ) ≤ (2 : ℝ) ^ (68 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 147573952589676412929) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_eight_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 295147905179352825857 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (295147905179352825857 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (295147905179352825857 : ℝ) ≤ (2 : ℝ) ^ (69 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 295147905179352825857) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_nine_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 590295810358705651713 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (590295810358705651713 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (590295810358705651713 : ℝ) ≤ (2 : ℝ) ^ (70 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 590295810358705651713) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 1180591620717411303425 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (1180591620717411303425 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (1180591620717411303425 : ℝ) ≤ (2 : ℝ) ^ (71 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 1180591620717411303425) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_one_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 2361183241434822606849 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (2361183241434822606849 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (2361183241434822606849 : ℝ) ≤ (2 : ℝ) ^ (72 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2361183241434822606849) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_two_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 4722366482869645213697 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (4722366482869645213697 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (4722366482869645213697 : ℝ) ≤ (2 : ℝ) ^ (73 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 4722366482869645213697) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_three_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 9444732965739290427393 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (9444732965739290427393 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (9444732965739290427393 : ℝ) ≤ (2 : ℝ) ^ (74 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 9444732965739290427393) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_four_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 18889465931478580854785 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (18889465931478580854785 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (18889465931478580854785 : ℝ) ≤ (2 : ℝ) ^ (75 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 18889465931478580854785) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_five_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 37778931862957161709569 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (37778931862957161709569 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (37778931862957161709569 : ℝ) ≤ (2 : ℝ) ^ (76 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 37778931862957161709569) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_six_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 75557863725914323419137 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (75557863725914323419137 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (75557863725914323419137 : ℝ) ≤ (2 : ℝ) ^ (77 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 75557863725914323419137) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_seven_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 151115727451828646838273 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (151115727451828646838273 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (151115727451828646838273 : ℝ) ≤ (2 : ℝ) ^ (78 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 151115727451828646838273) hden

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_eight_plus_one :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 302231454903657293676545 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (302231454903657293676545 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (302231454903657293676545 : ℝ) ≤ (2 : ℝ) ^ (79 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 302231454903657293676545) hden

theorem problem2_10_ieeeDouble_two_pow_div_three_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 3) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 3 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤ ((2 : ℝ) ^ k) / 3)
  · have hdiv_le : ((2 : ℝ) ^ k) / 3 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`3` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/3` to the lower adjacent endpoint, whenever the source value is
within the finite normal range. -/
theorem problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 3) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 (((k : ℤ) - 1))
  let b : ℝ :=
    fmt.normalizedValue false (6004799503160661 + 1) (((k : ℤ) - 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 3
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (((k : ℤ) - 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 54)) * (2 : ℝ) ^ (54 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 54)) (54 : ℤ)
    have hexp : ((k : ℤ) - 54) + 54 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = ((2 : ℝ) ^ (((k : ℤ) - 54))) / 3 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2 * ((2 : ℝ) ^ (((k : ℤ) - 54)))) / 3 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 54)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < ((2 : ℝ) ^ (((k : ℤ) - 54))) / 3 :=
      div_pos hzpos (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2 * ((2 : ℝ) ^ (((k : ℤ) - 54)))) / 3 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_three_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    have hpos : 0 < ((2 : ℝ) ^ (((k : ℤ) - 54))) / 3 :=
      div_pos hzpos (by norm_num)
    linarith
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the generic denominator-`3` power-of-two first
operation in Problem 2.10. -/
theorem problem2_10_ieeeDouble_div_two_pow_three
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))
  exact problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower hk

/-- Result-by-result generic audit for the second Problem 2.10 operation in
the denominator-`3`, power-of-two numerator subfamily: after the rounded
division has stored the lower IEEE-double approximation to `(2^k)/3`, the
exact multiplication by `3` is the midpoint immediately below `2^k`. -/
theorem problem2_10_ieeeDouble_div_two_pow_three_exact_mul_three_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 3) * 3 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_three hk]
  exact problem2_10_ieeeDouble_rounded_two_pow_thirds_mul_three_midpoint

private theorem problem2_10_ieeeDouble_two_pow_boundary_left_value
    {k : ℕ} :
    ieeeDoubleFormat.normalizedValue false ieeeDoubleFormat.maxNormalMantissa
        (k : ℤ) =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
  simp [ieeeDoubleFormat, normalizedValue, signValue, betaR, maxNormalMantissa]
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 53)) * (2 : ℝ) ^ (53 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 53)) (53 : ℤ)
    have hexp : ((k : ℤ) - 53) + 53 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  rw [hpow]
  norm_num [zpow_natCast]
  ring

private theorem problem2_10_ieeeDouble_two_pow_boundary_right_value
    {k : ℕ} :
    ieeeDoubleFormat.normalizedValue false ieeeDoubleFormat.minNormalMantissa
        (((k : ℤ) + 1)) =
      (2 : ℝ) ^ k := by
  simp [ieeeDoubleFormat, normalizedValue, signValue, betaR, minNormalMantissa]
  have hexp2 : (k : ℤ) + 1 - 53 = (k : ℤ) - 52 := by ring
  rw [hexp2]
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ ((k : ℤ) - 52) * (2 : ℝ) ^ (52 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) ((k : ℤ) - 52) (52 : ℤ)
    have hexp : (k : ℤ) - 52 + 52 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  rw [hpow]
  norm_num [zpow_natCast]
  ring

private theorem problem2_10_two_pow_midpoint_below_boundary_left_gap
    {k : ℕ} :
    ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54))) -
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53))) =
      (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  have hpow53 :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        2 * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 54)) (1 : ℤ)
    have hexp : ((k : ℤ) - 54) + 1 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    simpa [zpow_one, mul_comm] using h
  rw [hpow53]
  ring

private theorem problem2_10_two_pow_midpoint_lower_bound
    {k : ℕ} :
    (1 / 3 : ℝ) ≤
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  let z : ℝ := (2 : ℝ) ^ (((k : ℤ) - 54))
  have hpow54 : (2 : ℝ) ^ k = z * (2 : ℝ) ^ (54 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 54)) (54 : ℤ)
    have hexp : ((k : ℤ) - 54) + 54 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [z, zpow_natCast] using h
  have hx_value :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) =
        z * ((2 : ℝ) ^ (54 : ℤ) - 1) := by
    simp [z, hpow54]
    ring
  have hz_ge_min : (2 : ℝ) ^ (-54 : ℤ) ≤ z := by
    simpa [z] using
      (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by omega : (-54 : ℤ) ≤ (k : ℤ) - 54))
  have hfactor_nonneg : 0 ≤ (2 : ℝ) ^ (54 : ℤ) - 1 := by
    norm_num [zpow_natCast]
  have hprod_le :
      (2 : ℝ) ^ (-54 : ℤ) * ((2 : ℝ) ^ (54 : ℤ) - 1) ≤
        z * ((2 : ℝ) ^ (54 : ℤ) - 1) :=
    mul_le_mul_of_nonneg_right hz_ge_min hfactor_nonneg
  have hminprod :
      (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ (-54 : ℤ) * ((2 : ℝ) ^ (54 : ℤ) - 1) := by
    norm_num [zpow_neg, zpow_natCast]
  calc
    (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ (-54 : ℤ) * ((2 : ℝ) ^ (54 : ℤ) - 1) := hminprod
    _ ≤ z * ((2 : ℝ) ^ (54 : ℤ) - 1) := hprod_le
    _ = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := hx_value.symm

private theorem problem2_10_ieeeDouble_two_pow_midpoint_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  rw [finiteNormalRange]
  have hthird := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      hthird
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 54)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`3`, power-of-two final rounding fact for Problem
2.10: the midpoint immediately below `2^k` ties to the even power-of-two
endpoint in IEEE double. -/
theorem problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    simpa [x, ha_value] using
      problem2_10_two_pow_midpoint_below_boundary_left_gap (k := k)
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    simp [x, hb_value]
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 54)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by linarith
  have hright_pos : 0 < b - x := by linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_midpoint_finiteNormalRange hk)
  have hleft : a = fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ) :=
    rfl
  have htie : |x - a| = |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
  have hodd : ¬ evenMantissa fmt.maxNormalMantissa := by
    norm_num [fmt, ieeeDoubleFormat, maxNormalMantissa, evenMantissa]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_tie_odd
      hpolicy hadj hstrict hm hleft htie hodd
  simpa [x, fmt, hb_value] using hround

private theorem problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 56)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 56) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 56)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`17`, power-of-two final rounding fact for Problem
2.10: the exact product after the first rounded division is one sixteenth of
an ulp below `2^k`, hence rounds back to `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 56)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        8 * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 56)) (3 : ℤ)
    have hexp : ((k : ℤ) - 56) + 3 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 56)) * (2 : ℝ) ^ (3 : ℤ) := h
      _ = 8 * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 7 * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

private theorem problem2_10_ieeeDouble_two_pow_minus_one_sixtyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 58)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 58) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 58)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`536870913`, power-of-two final rounding fact for
Problem 2.10: the exact product after the first rounded division is one
sixty-fourth of an ulp below `2^k`, hence rounds back to `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_sixtyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 58)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        32 * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 58)) (5 : ℤ)
    have hexp : ((k : ℤ) - 58) + 5 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 58)) * (2 : ℝ) ^ (5 : ℤ) := h
      _ = 32 * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 31 * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_sixtyfourth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`536870913`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_sixtyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Positive power-of-two numerator subfamily of Kahan's Problem 2.10 theorem
for denominator `3` in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_thirds_times_three
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 3) 3 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_three hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))) * 3) =
    (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_two_pow_thirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow hk

/-- Signed companion of the generic power-of-two first operation: finite
IEEE-double round-to-even rounds `-(2^k)/3` by oddness. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_three
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)))
  have harg : (-((2 : ℝ) ^ k)) / 3 = -(((2 : ℝ) ^ k) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second Problem 2.10
operation in the denominator-`3`, power-of-two numerator subfamily. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_three_exact_mul_three_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 3) *
        3 =
      -((2 : ℝ) ^ k) + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_three hk]
  exact problem2_10_ieeeDouble_negative_rounded_two_pow_thirds_mul_three_midpoint

/-- Generic signed final rounding fact for Problem 2.10: the midpoint
immediately above `-2^k` ties to the even endpoint `-2^k`. -/
theorem problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k) + (2 : ℝ) ^ (((k : ℤ) - 54))) =
      -((2 : ℝ) ^ k) := by
  have harg :
      -((2 : ℝ) ^ k) + (2 : ℝ) ^ (((k : ℤ) - 54)) =
        -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54))) := by
    ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Negative power-of-two numerator subfamily of Kahan's Problem 2.10 theorem
for denominator `3` in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_thirds_times_three
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 3)
        3 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_three hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)))) * 3) =
    -((2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_thirds_mul_three_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow hk

/-- Signed denominator-`3`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_thirds_times_three
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 3)
          3 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 3)
          3 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_thirds_times_three hk,
    problem2_10_ieeeDouble_neg_two_pow_thirds_times_three hk⟩

private theorem problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude :
    (1 : ℝ) ≤ ieeeDoubleFormat.maxFiniteMagnitude := by
  rw [maxFiniteMagnitude, ieeeDoubleFormat, betaR]
  change (1 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) *
    (1 - (2 : ℝ) ^ (-53 : ℤ))
  have hfactor : (1 / 2 : ℝ) ≤ 1 - (2 : ℝ) ^ (-53 : ℤ) := by
    rw [zpow_neg]
    have hden : (2 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by
      exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by norm_num : (53 : ℕ) ≠ 0)
    have hinv : 1 / ((2 : ℝ) ^ (53 : ℕ)) ≤ 1 / (2 : ℝ) :=
      one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2) hden
    norm_num at hinv ⊢
  have hpow_nat : (2 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℕ) := by
    exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by norm_num : (1024 : ℕ) ≠ 0)
  have hpow : (2 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) := by
    simpa [zpow_natCast] using hpow_nat
  have hmul := mul_le_mul hpow hfactor
    (by norm_num : (0 : ℝ) ≤ (1 / 2 : ℝ))
    (by positivity : (0 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ))
  simpa using hmul

private theorem problem2_10_two_le_ieeeDoubleFormat_maxFiniteMagnitude :
    (2 : ℝ) ≤ ieeeDoubleFormat.maxFiniteMagnitude := by
  rw [maxFiniteMagnitude, ieeeDoubleFormat, betaR]
  change (2 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) *
    (1 - (2 : ℝ) ^ (-53 : ℤ))
  have hfactor : (1 / 2 : ℝ) ≤ 1 - (2 : ℝ) ^ (-53 : ℤ) := by
    rw [zpow_neg]
    have hden : (2 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by
      exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by norm_num : (53 : ℕ) ≠ 0)
    have hinv : 1 / ((2 : ℝ) ^ (53 : ℕ)) ≤ 1 / (2 : ℝ) :=
      one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2) hden
    norm_num at hinv ⊢
  have hpow_nat : (4 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℕ) := by
    calc
      (4 : ℝ) = (2 : ℝ) ^ (2 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1024 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  have hpow : (4 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) := by
    simpa [zpow_natCast] using hpow_nat
  have hmul := mul_le_mul hpow hfactor
    (by norm_num : (0 : ℝ) ≤ (1 / 2 : ℝ))
    (by positivity : (0 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ))
  simpa [show (4 : ℝ) * (2 : ℝ)⁻¹ = 2 by norm_num] using hmul

private theorem problem2_10_four_le_ieeeDoubleFormat_maxFiniteMagnitude :
    (4 : ℝ) ≤ ieeeDoubleFormat.maxFiniteMagnitude := by
  rw [maxFiniteMagnitude, ieeeDoubleFormat, betaR]
  change (4 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) *
    (1 - (2 : ℝ) ^ (-53 : ℤ))
  have hfactor : (1 / 2 : ℝ) ≤ 1 - (2 : ℝ) ^ (-53 : ℤ) := by
    rw [zpow_neg]
    have hden : (2 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by
      exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by norm_num : (53 : ℕ) ≠ 0)
    have hinv : 1 / ((2 : ℝ) ^ (53 : ℕ)) ≤ 1 / (2 : ℝ) :=
      one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2) hden
    norm_num at hinv ⊢
  have hpow_nat : (8 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℕ) := by
    calc
      (8 : ℝ) = (2 : ℝ) ^ (3 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1024 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  have hpow : (8 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) := by
    simpa [zpow_natCast] using hpow_nat
  have hmul := mul_le_mul hpow hfactor
    (by norm_num : (0 : ℝ) ≤ (1 / 2 : ℝ))
    (by positivity : (0 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ))
  simpa [show (8 : ℝ) * (2 : ℝ)⁻¹ = 4 by norm_num] using hmul

private theorem problem2_10_eight_le_ieeeDoubleFormat_maxFiniteMagnitude :
    (8 : ℝ) ≤ ieeeDoubleFormat.maxFiniteMagnitude := by
  rw [maxFiniteMagnitude, ieeeDoubleFormat, betaR]
  change (8 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) *
    (1 - (2 : ℝ) ^ (-53 : ℤ))
  have hfactor : (1 / 2 : ℝ) ≤ 1 - (2 : ℝ) ^ (-53 : ℤ) := by
    rw [zpow_neg]
    have hden : (2 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by
      exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by norm_num : (53 : ℕ) ≠ 0)
    have hinv : 1 / ((2 : ℝ) ^ (53 : ℕ)) ≤ 1 / (2 : ℝ) :=
      one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2) hden
    norm_num at hinv ⊢
  have hpow_nat : (16 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℕ) := by
    calc
      (16 : ℝ) = (2 : ℝ) ^ (4 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1024 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  have hpow : (16 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) := by
    simpa [zpow_natCast] using hpow_nat
  have hmul := mul_le_mul hpow hfactor
    (by norm_num : (0 : ℝ) ≤ (1 / 2 : ℝ))
    (by positivity : (0 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ))
  simpa [show (16 : ℝ) * (2 : ℝ)⁻¹ = 8 by norm_num] using hmul

private theorem problem2_10_sixteen_le_ieeeDoubleFormat_maxFiniteMagnitude :
    (16 : ℝ) ≤ ieeeDoubleFormat.maxFiniteMagnitude := by
  rw [maxFiniteMagnitude, ieeeDoubleFormat, betaR]
  change (16 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) *
    (1 - (2 : ℝ) ^ (-53 : ℤ))
  have hfactor : (1 / 2 : ℝ) ≤ 1 - (2 : ℝ) ^ (-53 : ℤ) := by
    rw [zpow_neg]
    have hden : (2 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by
      exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by norm_num : (53 : ℕ) ≠ 0)
    have hinv : 1 / ((2 : ℝ) ^ (53 : ℕ)) ≤ 1 / (2 : ℝ) :=
      one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2) hden
    norm_num at hinv ⊢
  have hpow_nat : (32 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℕ) := by
    calc
      (32 : ℝ) = (2 : ℝ) ^ (5 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1024 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  have hpow : (32 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) := by
    simpa [zpow_natCast] using hpow_nat
  have hmul := mul_le_mul hpow hfactor
    (by norm_num : (0 : ℝ) ≤ (1 / 2 : ℝ))
    (by positivity : (0 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ))
  simpa [show (32 : ℝ) * (2 : ℝ)⁻¹ = 16 by norm_num] using hmul

private theorem problem2_10_thirtytwo_le_ieeeDoubleFormat_maxFiniteMagnitude :
    (32 : ℝ) ≤ ieeeDoubleFormat.maxFiniteMagnitude := by
  rw [maxFiniteMagnitude, ieeeDoubleFormat, betaR]
  change (32 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) *
    (1 - (2 : ℝ) ^ (-53 : ℤ))
  have hfactor : (1 / 2 : ℝ) ≤ 1 - (2 : ℝ) ^ (-53 : ℤ) := by
    rw [zpow_neg]
    have hden : (2 : ℝ) ≤ (2 : ℝ) ^ (53 : ℕ) := by
      exact le_self_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by norm_num : (53 : ℕ) ≠ 0)
    have hinv : 1 / ((2 : ℝ) ^ (53 : ℕ)) ≤ 1 / (2 : ℝ) :=
      one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 2) hden
    norm_num at hinv ⊢
  have hpow_nat : (64 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℕ) := by
    calc
      (64 : ℝ) = (2 : ℝ) ^ (6 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1024 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  have hpow : (64 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ) := by
    simpa [zpow_natCast] using hpow_nat
  have hmul := mul_le_mul hpow hfactor
    (by norm_num : (0 : ℝ) ≤ (1 / 2 : ℝ))
    (by positivity : (0 : ℝ) ≤ (2 : ℝ) ^ (1024 : ℤ))
  simpa [show (64 : ℝ) * (2 : ℝ)⁻¹ = 32 by norm_num] using hmul

private theorem problem2_10_ieeeDouble_oneThird_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (1 / 3 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 1 / 3)]
  constructor
  · exact problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
  · exact le_trans (by norm_num : (1 / 3 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

private theorem problem2_10_ieeeDouble_twoThirds_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (2 / 3 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2 / 3)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by norm_num : (1 / 3 : ℝ) ≤ 2 / 3)
  · exact le_trans (by norm_num : (2 / 3 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

private theorem problem2_10_ieeeDouble_fourThirds_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (4 / 3 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 4 / 3)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by norm_num : (1 / 3 : ℝ) ≤ 4 / 3)
  · exact le_trans (by norm_num : (4 / 3 : ℝ) ≤ 2)
      problem2_10_two_le_ieeeDoubleFormat_maxFiniteMagnitude

private theorem problem2_10_ieeeDouble_eightThirds_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (8 / 3 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 8 / 3)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by norm_num : (1 / 3 : ℝ) ≤ 8 / 3)
  · exact le_trans (by norm_num : (8 / 3 : ℝ) ≤ 4)
      problem2_10_four_le_ieeeDoubleFormat_maxFiniteMagnitude

private theorem problem2_10_ieeeDouble_sixteenThirds_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (16 / 3 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 16 / 3)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by norm_num : (1 / 3 : ℝ) ≤ 16 / 3)
  · exact le_trans (by norm_num : (16 / 3 : ℝ) ≤ 8)
      problem2_10_eight_le_ieeeDoubleFormat_maxFiniteMagnitude

private theorem problem2_10_ieeeDouble_thirtytwoThirds_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (32 / 3 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 32 / 3)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by norm_num : (1 / 3 : ℝ) ≤ 32 / 3)
  · exact le_trans (by norm_num : (32 / 3 : ℝ) ≤ 16)
      problem2_10_sixteen_le_ieeeDoubleFormat_maxFiniteMagnitude

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 5 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (5 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (5 : ℝ) ≤ (2 : ℝ) ^ (3 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 5) hden

private theorem problem2_10_ieeeDouble_oneFifth_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (1 / 5 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 1 / 5)]
  constructor
  · exact problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
  · exact le_trans (by norm_num : (1 / 5 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

/-- First rounded operation for the next displayed-prefix denominator beyond
`3`: finite IEEE-double round-to-even rounds `1/5` upward to the nearest
double. -/
theorem problem2_10_ieeeDouble_oneFifth_rounds_to_upper :
    ieeeDoubleFormat.finiteRoundToEven (1 / 5 : ℝ) =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 7205759403792793 (-2)
  let b : ℝ := fmt.normalizedValue false 7205759403792794 (-2)
  let x : ℝ := (1 / 5 : ℝ)
  have hm : fmt.normalizedMantissa 7205759403792793 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (7205759403792793 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 7205759403792793, (-2 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (7205759403792793 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_oneFifth_finiteNormalRange
  have hrightCloser : |x - b| < |x - a| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level version of the first `m = 1`, `n = 5` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_one_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 5 =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((1 : ℝ) / 5) =
    (7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ)
  exact problem2_10_ieeeDouble_oneFifth_rounds_to_upper

/-- Result-by-result audit for the `m = 1`, `n = 5` second operation: after
the rounded division has stored the IEEE-double approximation to `1/5`, the
exact product with `5` is `1 + 2^-54`. -/
theorem problem2_10_ieeeDouble_div_one_five_exact_mul_five_above_one :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 5) * 5 =
      (1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_one_five]
  exact problem2_10_ieeeDouble_rounded_oneFifth_mul_five_above_one

/-- Final rounding fact for the `m = 1`, `n = 5` trace: `1 + 2^-54` is closer
to `1` than to the next IEEE-double value above `1`. -/
theorem problem2_10_ieeeDouble_one_plus_two_pow_neg54_rounds_to_one :
    ieeeDoubleFormat.finiteRoundToEven
        ((1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ)) = (1 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 1
  let b : ℝ := fmt.normalizedValue false (fmt.minNormalMantissa + 1) 1
  let x : ℝ := (1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ)
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (1 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (1 : ℝ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hb_value : b = (1 : ℝ) + (2 : ℝ) ^ (-52 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
        (by norm_num [x, zpow_neg] : (1 / 5 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (2 : ℝ))
        problem2_10_two_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Problem 2.10 denominator-`5`, numerator-`1` finite-selector instance:
`fl((1/5) * 5) = 1` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_one_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 5) 5 =
      (1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_one_five]
  change ieeeDoubleFormat.finiteRoundToEven
      (((7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ)) * 5) = (1 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_oneFifth_mul_five_above_one]
  exact problem2_10_ieeeDouble_one_plus_two_pow_neg54_rounds_to_one

/-- Signed companion of the `m = 1`, `n = 5` first operation: finite
IEEE-double round-to-even rounds `-1/5` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_one_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 5 =
      -((7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-1 : ℝ) / 5) =
    -((7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ))
  have harg : ((-1 : ℝ) / 5) = -((1 : ℝ) / 5) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_oneFifth_rounds_to_upper]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -1`, `n = 5` second operation. -/
theorem problem2_10_ieeeDouble_div_neg_one_five_exact_mul_five_below_neg_one :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 5) * 5 =
      -((1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ)) := by
  rw [problem2_10_ieeeDouble_div_neg_one_five]
  exact problem2_10_ieeeDouble_negative_rounded_oneFifth_mul_five_below_neg_one

/-- Final signed rounding fact for the `m = -1`, `n = 5` trace. -/
theorem problem2_10_ieeeDouble_negative_one_plus_two_pow_neg54_rounds_to_neg_one :
    ieeeDoubleFormat.finiteRoundToEven
        (-((1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ))) = (-1 : ℝ) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_one_plus_two_pow_neg54_rounds_to_one]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Problem 2.10 denominator-`5`, numerator-`-1` finite-selector instance:
`fl((-1/5) * 5) = -1` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_one_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 5) 5 =
      (-1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_one_five]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((7205759403792794 : ℝ) * (2 : ℝ) ^ (-55 : ℤ))) * 5) = (-1 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_oneFifth_mul_five_below_neg_one]
  exact problem2_10_ieeeDouble_negative_one_plus_two_pow_neg54_rounds_to_neg_one

/-- Signed denominator-`5`, numerator-`1` Problem 2.10 trace in the finite
IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_signed_one_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 5) 5 =
        (1 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 5) 5 =
        (-1 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_one_fifth_times_five,
    problem2_10_ieeeDouble_neg_one_fifth_times_five⟩

private theorem problem2_10_ieeeDouble_twoFifths_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (2 / 5 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 2 / 5)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
      (by norm_num : (1 / 5 : ℝ) ≤ 2 / 5)
  · exact le_trans (by norm_num : (2 / 5 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

/-- Denominator-`5`, numerator-`2` first operation: finite IEEE-double
round-to-even rounds `2/5` upward to the nearest double. -/
theorem problem2_10_ieeeDouble_twoFifths_rounds_to_upper :
    ieeeDoubleFormat.finiteRoundToEven (2 / 5 : ℝ) =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 7205759403792793 (-1)
  let b : ℝ := fmt.normalizedValue false 7205759403792794 (-1)
  let x : ℝ := (2 / 5 : ℝ)
  have hm : fmt.normalizedMantissa 7205759403792793 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (7205759403792793 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 7205759403792793, (-1 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (7205759403792793 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_twoFifths_finiteNormalRange
  have hrightCloser : |x - b| < |x - a| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level version of the first `m = 2`, `n = 5` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 5 =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((2 : ℝ) / 5) =
    (7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ)
  exact problem2_10_ieeeDouble_twoFifths_rounds_to_upper

/-- Result-by-result audit for the `m = 2`, `n = 5` second operation: after
the rounded division has stored the IEEE-double approximation to `2/5`, the
exact product with `5` is `2 + 2^-53`. -/
theorem problem2_10_ieeeDouble_div_two_five_exact_mul_five_above_two :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 5) * 5 =
      (2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_two_five]
  exact problem2_10_ieeeDouble_rounded_twoFifths_mul_five_above_two

/-- Final rounding fact for the `m = 2`, `n = 5` trace: `2 + 2^-53` is
closer to `2` than to the next IEEE-double value above `2`. -/
theorem problem2_10_ieeeDouble_two_plus_two_pow_neg53_rounds_to_two :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ)) = (2 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 2
  let b : ℝ := fmt.normalizedValue false (fmt.minNormalMantissa + 1) 2
  let x : ℝ := (2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ)
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (2 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hb_value : b = (2 : ℝ) + (2 : ℝ) ^ (-51 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
        (by norm_num [x, zpow_neg] : (1 / 5 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (4 : ℝ))
        problem2_10_four_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Problem 2.10 denominator-`5`, numerator-`2` finite-selector instance:
`fl((2/5) * 5) = 2` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_two_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 5) 5 =
      (2 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_two_five]
  change ieeeDoubleFormat.finiteRoundToEven
      (((7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ)) * 5) = (2 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_twoFifths_mul_five_above_two]
  exact problem2_10_ieeeDouble_two_plus_two_pow_neg53_rounds_to_two

/-- Signed companion of the `m = 2`, `n = 5` first operation: finite
IEEE-double round-to-even rounds `-2/5` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_two_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 5 =
      -((7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-2 : ℝ) / 5) =
    -((7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ))
  have harg : ((-2 : ℝ) / 5) = -((2 : ℝ) / 5) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_twoFifths_rounds_to_upper]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -2`, `n = 5` second operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_five_exact_mul_five_below_neg_two :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 5) * 5 =
      -((2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_five]
  exact problem2_10_ieeeDouble_negative_rounded_twoFifths_mul_five_below_neg_two

/-- Final signed rounding fact for the `m = -2`, `n = 5` trace. -/
theorem problem2_10_ieeeDouble_negative_two_plus_two_pow_neg53_rounds_to_neg_two :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ))) = (-2 : ℝ) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_plus_two_pow_neg53_rounds_to_two]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Problem 2.10 denominator-`5`, numerator-`-2` finite-selector instance:
`fl((-2/5) * 5) = -2` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_two_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 5) 5 =
      (-2 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_two_five]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((7205759403792794 : ℝ) * (2 : ℝ) ^ (-54 : ℤ))) * 5) = (-2 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_twoFifths_mul_five_below_neg_two]
  exact problem2_10_ieeeDouble_negative_two_plus_two_pow_neg53_rounds_to_neg_two

/-- Signed denominator-`5`, numerator-`2` Problem 2.10 trace in the finite
IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 5) 5 =
        (2 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 5) 5 =
        (-2 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_two_fifth_times_five,
    problem2_10_ieeeDouble_neg_two_fifth_times_five⟩

private theorem problem2_10_ieeeDouble_threeFifths_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (3 / 5 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 3 / 5)]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
      (by norm_num : (1 / 5 : ℝ) ≤ 3 / 5)
  · exact le_trans (by norm_num : (3 / 5 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

/-- Denominator-`5`, numerator-`3` first operation: finite IEEE-double
round-to-even rounds `3/5` downward to the nearest double.  This is the first
denominator-`5` trace here whose numerator is not a power of two. -/
theorem problem2_10_ieeeDouble_threeFifths_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (3 / 5 : ℝ) =
      (5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 5404319552844595 0
  let b : ℝ := fmt.normalizedValue false (5404319552844595 + 1) 0
  let x : ℝ := (3 / 5 : ℝ)
  have hm : fmt.normalizedMantissa 5404319552844595 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (5404319552844595 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 5404319552844595, (0 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (5404319552844596 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_threeFifths_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 3`, `n = 5` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_three_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (3 : ℝ) 5 =
      (5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((3 : ℝ) / 5) =
    (5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ)
  exact problem2_10_ieeeDouble_threeFifths_rounds_to_lower

/-- Result-by-result audit for the `m = 3`, `n = 5` second operation: after
the rounded division has stored the IEEE-double approximation to `3/5`, the
exact product with `5` is `3 - 2^-53`. -/
theorem problem2_10_ieeeDouble_div_three_five_exact_mul_five_below_three :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (3 : ℝ) 5) * 5 =
      (3 : ℝ) - (2 : ℝ) ^ (-53 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_three_five]
  exact problem2_10_ieeeDouble_rounded_threeFifths_mul_five_below_three

/-- Final rounding fact for the `m = 3`, `n = 5` trace: `3 - 2^-53` is
closer to `3` than to the previous IEEE-double value. -/
theorem problem2_10_ieeeDouble_three_minus_two_pow_neg53_rounds_to_three :
    ieeeDoubleFormat.finiteRoundToEven
        ((3 : ℝ) - (2 : ℝ) ^ (-53 : ℤ)) = (3 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6755399441055743 2
  let b : ℝ := fmt.normalizedValue false 6755399441055744 2
  let x : ℝ := (3 : ℝ) - (2 : ℝ) ^ (-53 : ℤ)
  have hm : fmt.normalizedMantissa 6755399441055743 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6755399441055743 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6755399441055743, (2 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (3 : ℝ) - (2 : ℝ) ^ (-51 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value : b = (3 : ℝ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
        (by norm_num [x, zpow_neg] : (1 / 5 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (4 : ℝ))
        problem2_10_four_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hrightCloser : |x - b| < |x - a| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Problem 2.10 denominator-`5`, numerator-`3` finite-selector instance:
`fl((3/5) * 5) = 3` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_three_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (3 : ℝ) 5) 5 =
      (3 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_three_five]
  change ieeeDoubleFormat.finiteRoundToEven
      (((5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ)) * 5) = (3 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_threeFifths_mul_five_below_three]
  exact problem2_10_ieeeDouble_three_minus_two_pow_neg53_rounds_to_three

/-- Signed companion of the `m = 3`, `n = 5` first operation: finite
IEEE-double round-to-even rounds `-3/5` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_three_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-3 : ℝ) 5 =
      -((5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-3 : ℝ) / 5) =
    -((5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ))
  have harg : ((-3 : ℝ) / 5) = -((3 : ℝ) / 5) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_threeFifths_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -3`, `n = 5` second operation. -/
theorem problem2_10_ieeeDouble_div_neg_three_five_exact_mul_five_above_neg_three :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-3 : ℝ) 5) * 5 =
      -((3 : ℝ) - (2 : ℝ) ^ (-53 : ℤ)) := by
  rw [problem2_10_ieeeDouble_div_neg_three_five]
  exact problem2_10_ieeeDouble_negative_rounded_threeFifths_mul_five_above_neg_three

/-- Final signed rounding fact for the `m = -3`, `n = 5` trace. -/
theorem problem2_10_ieeeDouble_negative_three_minus_two_pow_neg53_rounds_to_neg_three :
    ieeeDoubleFormat.finiteRoundToEven
        (-((3 : ℝ) - (2 : ℝ) ^ (-53 : ℤ))) = (-3 : ℝ) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_three_minus_two_pow_neg53_rounds_to_three]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Problem 2.10 denominator-`5`, numerator-`-3` finite-selector instance:
`fl((-3/5) * 5) = -3` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_three_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-3 : ℝ) 5) 5 =
      (-3 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_three_five]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((5404319552844595 : ℝ) * (2 : ℝ) ^ (-53 : ℤ))) * 5) = (-3 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_threeFifths_mul_five_above_neg_three]
  exact problem2_10_ieeeDouble_negative_three_minus_two_pow_neg53_rounds_to_neg_three

/-- Signed denominator-`5`, numerator-`3` Problem 2.10 trace in the finite
IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_signed_three_fifth_times_five :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (3 : ℝ) 5) 5 =
        (3 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-3 : ℝ) 5) 5 =
        (-3 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_three_fifth_times_five,
    problem2_10_ieeeDouble_neg_three_fifth_times_five⟩

theorem problem2_10_ieeeDouble_three_mul_two_pow_div_five_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((3 : ℝ) * (2 : ℝ) ^ k) / 5) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((3 : ℝ) * (2 : ℝ) ^ k) / 5 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
      (by nlinarith : (1 / 5 : ℝ) ≤ ((3 : ℝ) * (2 : ℝ) ^ k) / 5)
  · have hdiv_le : ((3 : ℝ) * (2 : ℝ) ^ k) / 5 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      nlinarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`5` first-operation fact for the scaled
non-power-of-two numerator family: finite IEEE-double round-to-even rounds
`(3*2^k)/5` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_three_mul_two_pow_fifths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((3 : ℝ) * (2 : ℝ) ^ k) / 5) =
      (5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 5404319552844595 (k : ℤ)
  let b : ℝ := fmt.normalizedValue false (5404319552844595 + 1) (k : ℤ)
  let x : ℝ := ((3 : ℝ) * (2 : ℝ) ^ k) / 5
  have hm : fmt.normalizedMantissa 5404319552844595 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (5404319552844595 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 5404319552844595, (k : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
  have hb_value :
      b = (5404319552844596 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 53)) * (2 : ℝ) ^ (53 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 53)) (53 : ℤ)
    have hexp : ((k : ℤ) - 53) + 53 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = ((2 : ℝ) ^ (((k : ℤ) - 53))) / 5 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (4 * ((2 : ℝ) ^ (((k : ℤ) - 53)))) / 5 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 53)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < ((2 : ℝ) ^ (((k : ℤ) - 53))) / 5 :=
      div_pos hzpos (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (4 * ((2 : ℝ) ^ (((k : ℤ) - 53)))) / 5 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_three_mul_two_pow_div_five_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`5`,
`3*2^k` numerator step. -/
theorem problem2_10_ieeeDouble_div_three_mul_two_pow_five
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
        ((3 : ℝ) * (2 : ℝ) ^ k) 5 =
      (5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((3 : ℝ) * (2 : ℝ) ^ k) / 5) =
    (5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53))
  exact problem2_10_ieeeDouble_three_mul_two_pow_fifths_rounds_to_lower hk

theorem problem2_10_ieeeDouble_div_three_mul_two_pow_five_exact_mul_five_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
        ((3 : ℝ) * (2 : ℝ) ^ k) 5) * 5 =
      (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
  rw [problem2_10_ieeeDouble_div_three_mul_two_pow_five hk]
  exact problem2_10_ieeeDouble_rounded_three_mul_two_pow_fifths_mul_five_below

private theorem problem2_10_three_mul_two_pow_fifth_product_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteNormalRange
      ((3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53))) := by
  rw [finiteNormalRange]
  have hpow_pos : 0 < (2 : ℝ) ^ k := pow_pos (by norm_num) k
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 53)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have htail_le : (2 : ℝ) ^ (((k : ℤ) - 53)) ≤ (2 : ℝ) ^ k := by
    simpa [zpow_natCast] using
      (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by omega : ((k : ℤ) - 53) ≤ (k : ℤ)))
  have hxnonneg :
      0 ≤ (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    nlinarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
      (by nlinarith : (1 / 5 : ℝ) ≤
        (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)))
  · have hx_le_pow :
        (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) ≤
          (2 : ℝ) ^ (k + 2) := by
      have hpow_add : (2 : ℝ) ^ (k + 2) = 4 * (2 : ℝ) ^ k := by
        rw [pow_add]
        norm_num
        ring
      rw [hpow_add]
      nlinarith
    exact le_trans hx_le_pow
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k + 2 ≤ 1023))

private theorem problem2_10_ieeeDouble_three_mul_two_pow_value
    {k : ℕ} :
    ieeeDoubleFormat.normalizedValue false 6755399441055744 (((k : ℤ) + 2)) =
      (3 : ℝ) * (2 : ℝ) ^ k := by
  simp [ieeeDoubleFormat, normalizedValue, signValue, betaR]
  have hexp2 : (k : ℤ) + 2 - 53 = (k : ℤ) - 51 := by ring
  rw [hexp2]
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 51)) * (2 : ℝ) ^ (51 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 51)) (51 : ℤ)
    have hexp : ((k : ℤ) - 51) + 51 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  rw [hpow]
  norm_num [zpow_natCast]
  ring

private theorem problem2_10_ieeeDouble_three_mul_two_pow_predecessor_value
    {k : ℕ} :
    ieeeDoubleFormat.normalizedValue false 6755399441055743 (((k : ℤ) + 2)) =
      (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 51)) := by
  simp [ieeeDoubleFormat, normalizedValue, signValue, betaR]
  have hexp2 : (k : ℤ) + 2 - 53 = (k : ℤ) - 51 := by ring
  rw [hexp2]
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 51)) * (2 : ℝ) ^ (51 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 51)) (51 : ℤ)
    have hexp : ((k : ℤ) - 51) + 51 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  rw [hpow]
  norm_num [zpow_natCast]
  ring

/-- Final rounding fact for the denominator-`5`, scaled non-power-of-two
family: the exact product after rounded division is a quarter ulp below
`3*2^k`, so finite IEEE-double round-to-even returns `3*2^k`. -/
theorem problem2_10_ieeeDouble_three_mul_two_pow_minus_quarter_ulp_rounds
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEven
        ((3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53))) =
      (3 : ℝ) * (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6755399441055743 (((k : ℤ) + 2))
  let b : ℝ := fmt.normalizedValue false 6755399441055744 (((k : ℤ) + 2))
  let x : ℝ := (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53))
  have hm : fmt.normalizedMantissa 6755399441055743 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6755399441055743 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6755399441055743, (((k : ℤ) + 2)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 51)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_three_mul_two_pow_predecessor_value (k := k)
  have hb_value :
      b = (3 : ℝ) * (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_three_mul_two_pow_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 53)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 51)) =
        4 * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 53)) (2 : ℤ)
    have hexp : ((k : ℤ) - 53) + 2 = (k : ℤ) - 51 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 51)) =
          (2 : ℝ) ^ (((k : ℤ) - 53)) * (2 : ℝ) ^ (2 : ℤ) := h
      _ = 4 * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
        norm_num [zpow_natCast]
        ring
  have hright_pos : 0 < b - x := by
    simp [x, hb_value]
    exact hzpos
  have hleft_dist : x - a = 3 * (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simp [x, ha_value, htail_to_ulp]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_three_mul_two_pow_fifth_product_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Positive `3*2^k` numerator subfamily of Kahan's Problem 2.10 theorem for
denominator `5` in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_three_mul_two_pow_fifths_times_five
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          ((3 : ℝ) * (2 : ℝ) ^ k) 5) 5 =
      (3 : ℝ) * (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_three_mul_two_pow_five (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53))) * 5) =
    (3 : ℝ) * (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_three_mul_two_pow_fifths_mul_five_below]
  exact problem2_10_ieeeDouble_three_mul_two_pow_minus_quarter_ulp_rounds hk

/-- Signed companion of the generic denominator-`5`, `3*2^k` first operation:
finite IEEE-double round-to-even rounds `-(3*2^k)/5` by oddness. -/
theorem problem2_10_ieeeDouble_div_neg_three_mul_two_pow_five
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
        (-((3 : ℝ) * (2 : ℝ) ^ k)) 5 =
      -((5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53))) := by
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((3 : ℝ) * (2 : ℝ) ^ k)) / 5) =
    -((5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53)))
  have harg :
      (-((3 : ℝ) * (2 : ℝ) ^ k)) / 5 =
        -(((3 : ℝ) * (2 : ℝ) ^ k) / 5) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_three_mul_two_pow_fifths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_three_mul_two_pow_five_exact_mul_five_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
        (-((3 : ℝ) * (2 : ℝ) ^ k)) 5) * 5 =
      -((3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53))) := by
  rw [problem2_10_ieeeDouble_div_neg_three_mul_two_pow_five hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_three_mul_two_pow_fifths_mul_five_above

/-- Signed final rounding fact for the denominator-`5`, `3*2^k` subfamily. -/
theorem problem2_10_ieeeDouble_negative_three_mul_two_pow_minus_quarter_ulp_rounds
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((3 : ℝ) * (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)))) =
      -((3 : ℝ) * (2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_three_mul_two_pow_minus_quarter_ulp_rounds hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Negative `3*2^k` numerator subfamily of Kahan's Problem 2.10 theorem for
denominator `5` in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_neg_three_mul_two_pow_fifths_times_five
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((3 : ℝ) * (2 : ℝ) ^ k)) 5) 5 =
      -((3 : ℝ) * (2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_three_mul_two_pow_five
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((5404319552844595 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 53)))) * 5) =
    -((3 : ℝ) * (2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_three_mul_two_pow_fifths_mul_five_above]
  exact
    problem2_10_ieeeDouble_negative_three_mul_two_pow_minus_quarter_ulp_rounds hk

/-- Signed denominator-`5`, scaled non-power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even operation
wrapper. -/
theorem problem2_10_ieeeDouble_signed_three_mul_two_pow_fifths_times_five
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            ((3 : ℝ) * (2 : ℝ) ^ k) 5) 5 =
        (3 : ℝ) * (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            (-((3 : ℝ) * (2 : ℝ) ^ k)) 5) 5 =
        -((3 : ℝ) * (2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_three_mul_two_pow_fifths_times_five hk,
    problem2_10_ieeeDouble_neg_three_mul_two_pow_fifths_times_five hk⟩

theorem problem2_10_ieeeDouble_two_pow_div_five_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 5) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 5 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
      (by linarith : (1 / 5 : ℝ) ≤ ((2 : ℝ) ^ k) / 5)
  · have hdiv_le : ((2 : ℝ) ^ k) / 5 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`5` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/5` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 5) =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 7205759403792793 (((k : ℤ) - 2))
  let b : ℝ :=
    fmt.normalizedValue false (7205759403792793 + 1) (((k : ℤ) - 2))
  let x : ℝ := ((2 : ℝ) ^ k) / 5
  have hm : fmt.normalizedMantissa 7205759403792793 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (7205759403792793 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 7205759403792793, (((k : ℤ) - 2)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (7205759403792793 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 55)) * (2 : ℝ) ^ (55 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 55)) (55 : ℤ)
    have hexp : ((k : ℤ) - 55) + 55 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (3 * ((2 : ℝ) ^ (((k : ℤ) - 55)))) / 5 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2 * ((2 : ℝ) ^ (((k : ℤ) - 55)))) / 5 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 55)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (3 * ((2 : ℝ) ^ (((k : ℤ) - 55)))) / 5 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2 * ((2 : ℝ) ^ (((k : ℤ) - 55)))) / 5 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_five_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level version of the first denominator-`5`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_five
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 5 =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 5) =
    (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))
  exact problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper hk

theorem problem2_10_ieeeDouble_div_two_pow_five_exact_mul_five_above_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 5) * 5 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_five hk]
  exact problem2_10_ieeeDouble_rounded_two_pow_fifths_mul_five_above_two_pow

private theorem problem2_10_two_pow_fifth_product_upper_bound
    {k : ℕ} (_hk : k ≤ 1022) :
    (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
      (2 : ℝ) ^ (k + 1) := by
  have hsmall :
      (2 : ℝ) ^ (((k : ℤ) - 54)) ≤ (2 : ℝ) ^ k := by
    simpa [zpow_natCast] using
      (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
        (by omega : ((k : ℤ) - 54) ≤ (k : ℤ)))
  have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
    rw [pow_succ]
    ring
  calc
    (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
    _ = (2 : ℝ) ^ (k + 1) := hdouble

private theorem problem2_10_ieeeDouble_two_pow_fifth_product_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 54)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fifth
      (by linarith : (1 / 5 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)))
  · calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
          (2 : ℝ) ^ (k + 1) :=
        problem2_10_two_pow_fifth_product_upper_bound hk
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

private theorem problem2_10_ieeeDouble_two_pow_successor_value
    {k : ℕ} :
    ieeeDoubleFormat.normalizedValue false (ieeeDoubleFormat.minNormalMantissa + 1)
        (((k : ℤ) + 1)) =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
  simp [ieeeDoubleFormat, normalizedValue, signValue, betaR, minNormalMantissa]
  have hexp2 : (k : ℤ) + 1 - 53 = (k : ℤ) - 52 := by ring
  rw [hexp2]
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ ((k : ℤ) - 52) * (2 : ℝ) ^ (52 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) ((k : ℤ) - 52) (52 : ℤ)
    have hexp : (k : ℤ) - 52 + 52 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  rw [hpow]
  norm_num [zpow_natCast]
  ring

/-- Generic denominator-`5`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-54)`, rounds back
to `2^k` because it is only a quarter ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 54)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        4 * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 54)) (2 : ℤ)
    have hexp : ((k : ℤ) - 54) + 2 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 54)) * (2 : ℝ) ^ (2 : ℤ) := h
      _ = 4 * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 3 * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_fifth_product_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Finite-normal range side condition for the denominator-`33` final product:
`2^k + 2^(k-55)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 55)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 55)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 55) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`33`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-55)`, rounds back
to `2^k` because it is one eighth of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 55)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        8 * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 55)) (3 : ℤ)
    have hexp : ((k : ℤ) - 55) + 3 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 55)) * (2 : ℝ) ^ (3 : ℤ) := h
      _ = 8 * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 55)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 7 * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Finite-normal range side condition for the denominator-`257` final product:
`2^k + 2^(k-56)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_sixteenth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 56)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 56)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 56) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`257`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-56)`, rounds back
to `2^k` because it is one sixteenth of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_sixteenth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 56)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        16 * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 56)) (4 : ℤ)
    have hexp : ((k : ℤ) - 56) + 4 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 56)) * (2 : ℝ) ^ (4 : ℤ) := h
      _ = 16 * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 15 * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_sixteenth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Finite-normal range side condition for the denominator-`524289` final
product: `2^k + 2^(k-57)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_thirtysecond_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 57)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 57)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 57) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`524289`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-57)`, rounds back
to `2^k` because it is one thirty-second of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_thirtysecond_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 57)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        32 * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 57)) (5 : ℤ)
    have hexp : ((k : ℤ) - 57) + 5 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 57)) * (2 : ℝ) ^ (5 : ℤ) := h
      _ = 32 * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 57)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 31 * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_thirtysecond_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Generic signed final rounding fact for the denominator-`524289`
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtysecond_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_thirtysecond_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`288230376151711745`
final product: `2^k + 2^(k-58)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_sixtyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 58)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 58)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 58) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`288230376151711745`, power-of-two final rounding fact
for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-58)`, rounds back to `2^k` because it is one sixty-fourth of an
ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_sixtyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 58)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        64 * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 58)) (6 : ℤ)
    have hexp : ((k : ℤ) - 58) + 6 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 58)) * (2 : ℝ) ^ (6 : ℤ) := h
      _ = 64 * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 63 * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_sixtyfourth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Generic signed final rounding fact for the denominator-`288230376151711745`
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_sixtyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`576460752303423489`
final product: `2^k + 2^(k-59)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_onehundredtwentyeighth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 59)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 59)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 59) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`576460752303423489`, power-of-two final rounding fact
for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-59)`, rounds back to `2^k` because it is one 128th of an ulp above
`2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_onehundredtwentyeighth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 59)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        128 * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 59)) (7 : ℤ)
    have hexp : ((k : ℤ) - 59) + 7 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 59)) * (2 : ℝ) ^ (7 : ℤ) := h
      _ = 128 * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 59)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 127 * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_onehundredtwentyeighth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Generic signed final rounding fact for the denominator-`576460752303423489`
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_onehundredtwentyeighth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_onehundredtwentyeighth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Positive power-of-two numerator subfamily of Kahan's Problem 2.10 theorem
for denominator `5` in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fifths_times_five
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 5) 5 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_five (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))) * 5) =
    (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_two_pow_fifths_mul_five_above_two_pow]
  exact problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow hk

/-- Signed companion of the generic denominator-`5` power-of-two first
operation: finite IEEE-double round-to-even rounds `-(2^k)/5` by oddness. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_five
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 5 =
      -((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 5) =
    -((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)))
  have harg : (-((2 : ℝ) ^ k)) / 5 = -(((2 : ℝ) ^ k) / 5) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_five_exact_mul_five_below_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 5) *
        5 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_five hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fifths_mul_five_below_two_pow

/-- Generic signed final rounding fact for the denominator-`5` power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Negative power-of-two numerator subfamily of Kahan's Problem 2.10 theorem
for denominator `5` in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fifths_times_five
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 5)
        5 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_five (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)))) * 5) =
    -((2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_fifths_mul_five_below_two_pow]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`5`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fifths_times_five
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 5)
          5 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 5)
          5 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fifths_times_five hk,
    problem2_10_ieeeDouble_neg_two_pow_fifths_times_five hk⟩

/-- Finite-normal range side condition for the denominator-`9`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_nine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 9) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 9 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_ninth
      (by linarith : (1 / 9 : ℝ) ≤ ((2 : ℝ) ^ k) / 9)
  · have hdiv_le : ((2 : ℝ) ^ k) / 9 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`9` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/9` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_ninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9) =
      (8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8006399337547548 (((k : ℤ) - 3))
  let b : ℝ :=
    fmt.normalizedValue false (8006399337547548 + 1) (((k : ℤ) - 3))
  let x : ℝ := ((2 : ℝ) ^ k) / 9
  have hm : fmt.normalizedMantissa 8006399337547548 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8006399337547548 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8006399337547548, (((k : ℤ) - 3)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8006399337547549 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 56)) * (2 : ℝ) ^ (56 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 56)) (56 : ℤ)
    have hexp : ((k : ℤ) - 56) + 56 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4 * ((2 : ℝ) ^ (((k : ℤ) - 56)))) / 9 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (5 * ((2 : ℝ) ^ (((k : ℤ) - 56)))) / 9 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 56)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4 * ((2 : ℝ) ^ (((k : ℤ) - 56)))) / 9 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (5 * ((2 : ℝ) ^ (((k : ℤ) - 56)))) / 9 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_nine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`9`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_nine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9 =
      (8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9) =
    (8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56))
  exact problem2_10_ieeeDouble_two_pow_ninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`9`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_nine_exact_mul_nine_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9) *
        9 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_nine hk]
  exact problem2_10_ieeeDouble_rounded_two_pow_ninths_mul_nine_midpoint

/-- Positive denominator-`9`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_ninths_times_nine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9)
        9 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_nine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56))) * 9) =
    (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_two_pow_ninths_mul_nine_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow hk

/-- Signed companion of the denominator-`9` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_nine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9 =
      -((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 9) =
    -((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)))
  have harg : (-((2 : ℝ) ^ k)) / 9 = -(((2 : ℝ) ^ k) / 9) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_ninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_nine_exact_mul_nine_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9) *
        9 =
      -((2 : ℝ) ^ k) + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_nine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_ninths_mul_nine_midpoint

/-- Negative denominator-`9`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_ninths_times_nine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9)
        9 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_nine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)))) * 9) =
    -((2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_ninths_mul_nine_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow hk

/-- Signed denominator-`9`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_ninths_times_nine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9)
          9 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9)
          9 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_ninths_times_nine hk,
    problem2_10_ieeeDouble_neg_two_pow_ninths_times_nine hk⟩

/-- Finite-normal range side condition for the denominator-`17`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_seventeen_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 17) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 17 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_seventeenth
      (by linarith : (1 / 17 : ℝ) ≤ ((2 : ℝ) ^ k) / 17)
  · have hdiv_le : ((2 : ℝ) ^ k) / 17 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`17` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/17` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_seventeenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 17) =
      (8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8477364004462110 (((k : ℤ) - 4))
  let b : ℝ :=
    fmt.normalizedValue false (8477364004462110 + 1) (((k : ℤ) - 4))
  let x : ℝ := ((2 : ℝ) ^ k) / 17
  have hm : fmt.normalizedMantissa 8477364004462110 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8477364004462110 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8477364004462110, (((k : ℤ) - 4)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8477364004462111 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 57)) * (2 : ℝ) ^ (57 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 57)) (57 : ℤ)
    have hexp : ((k : ℤ) - 57) + 57 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (2 * ((2 : ℝ) ^ (((k : ℤ) - 57)))) / 17 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (15 * ((2 : ℝ) ^ (((k : ℤ) - 57)))) / 17 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 57)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (2 * ((2 : ℝ) ^ (((k : ℤ) - 57)))) / 17 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (15 * ((2 : ℝ) ^ (((k : ℤ) - 57)))) / 17 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_seventeen_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`17`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_seventeen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17 =
      (8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 17) =
    (8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57))
  exact problem2_10_ieeeDouble_two_pow_seventeenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`17`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_seventeen_exact_mul_seventeen_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17) *
        17 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_seventeen hk]
  exact problem2_10_ieeeDouble_rounded_two_pow_seventeenths_mul_seventeen_below

/-- Positive denominator-`17`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_seventeenths_times_seventeen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17)
        17 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_seventeen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57))) * 17) =
    (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_two_pow_seventeenths_mul_seventeen_below]
  exact problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`17` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_seventeen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17 =
      -((8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 17) =
    -((8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57)))
  have harg : (-((2 : ℝ) ^ k)) / 17 = -(((2 : ℝ) ^ k) / 17) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_seventeenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_seventeen_exact_mul_seventeen_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17) *
        17 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_seventeen hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_seventeenths_mul_seventeen_below

/-- Signed final rounding fact for the denominator-`17`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Negative denominator-`17`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_seventeenths_times_seventeen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17)
        17 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_seventeen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8477364004462110 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 57)))) * 17) =
    -((2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_seventeenths_mul_seventeen_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`17`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_seventeenths_times_seventeen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17)
          17 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17)
          17 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_seventeenths_times_seventeen hk,
    problem2_10_ieeeDouble_neg_two_pow_seventeenths_times_seventeen hk⟩

/-- Finite-normal range side condition for the denominator-`33`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_thirtythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 33) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 33 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtythird
      (by linarith : (1 / 33 : ℝ) ≤ ((2 : ℝ) ^ k) / 33)
  · have hdiv_le : ((2 : ℝ) ^ k) / 33 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`33` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/33` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_thirtythirds_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 33) =
      (8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8734253822779143 (((k : ℤ) - 5))
  let b : ℝ :=
    fmt.normalizedValue false (8734253822779143 + 1) (((k : ℤ) - 5))
  let x : ℝ := ((2 : ℝ) ^ k) / 33
  have hm : fmt.normalizedMantissa 8734253822779143 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8734253822779143 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8734253822779143, (((k : ℤ) - 5)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8734253822779143 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 58)) * (2 : ℝ) ^ (58 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 58)) (58 : ℤ)
    have hexp : ((k : ℤ) - 58) + 58 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (25 * ((2 : ℝ) ^ (((k : ℤ) - 58)))) / 33 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (8 * ((2 : ℝ) ^ (((k : ℤ) - 58)))) / 33 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 58)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (25 * ((2 : ℝ) ^ (((k : ℤ) - 58)))) / 33 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (8 * ((2 : ℝ) ^ (((k : ℤ) - 58)))) / 33 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_thirtythree_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`33`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33 =
      (8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 33) =
    (8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58))
  exact problem2_10_ieeeDouble_two_pow_thirtythirds_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`33`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtythree_exact_mul_thirtythree_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33) *
        33 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_thirtythirds_mul_thirtythree_above

/-- Positive denominator-`33`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_thirtythirds_times_thirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33)
        33 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtythree
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58))) * 33) =
    (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_two_pow_thirtythirds_mul_thirtythree_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`33` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33 =
      -((8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 33) =
    -((8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58)))
  have harg : (-((2 : ℝ) ^ k)) / 33 = -(((2 : ℝ) ^ k) / 33) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_thirtythirds_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtythree_exact_mul_thirtythree_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33) *
        33 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtythirds_mul_thirtythree_above

/-- Generic signed final rounding fact for the denominator-`33` power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_eighth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Negative denominator-`33`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_thirtythirds_times_thirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33)
        33 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtythree
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8734253822779144 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 58)))) * 33) =
    -((2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_thirtythirds_mul_thirtythree_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_eighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`33`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_thirtythirds_times_thirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33)
          33 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33)
          33 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_thirtythirds_times_thirtythree hk,
    problem2_10_ieeeDouble_neg_two_pow_thirtythirds_times_thirtythree hk⟩

/-- Finite-normal range side condition for the denominator-`65`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_sixtyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 65) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 65 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtyfifth
      (by linarith : (1 / 65 : ℝ) ≤ ((2 : ℝ) ^ k) / 65)
  · have hdiv_le : ((2 : ℝ) ^ k) / 65 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`65` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/65` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_sixtyfifths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 65) =
      (8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8868626958514207 (((k : ℤ) - 6))
  let b : ℝ :=
    fmt.normalizedValue false (8868626958514207 + 1) (((k : ℤ) - 6))
  let x : ℝ := ((2 : ℝ) ^ k) / 65
  have hm : fmt.normalizedMantissa 8868626958514207 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8868626958514207 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8868626958514207, (((k : ℤ) - 6)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8868626958514207 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 59)) * (2 : ℝ) ^ (59 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 59)) (59 : ℤ)
    have hexp : ((k : ℤ) - 59) + 59 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (33 * ((2 : ℝ) ^ (((k : ℤ) - 59)))) / 65 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (32 * ((2 : ℝ) ^ (((k : ℤ) - 59)))) / 65 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 59)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (33 * ((2 : ℝ) ^ (((k : ℤ) - 59)))) / 65 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (32 * ((2 : ℝ) ^ (((k : ℤ) - 59)))) / 65 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_sixtyfive_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`65`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65 =
      (8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 65) =
    (8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59))
  exact problem2_10_ieeeDouble_two_pow_sixtyfifths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`65`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtyfive_exact_mul_sixtyfive_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65) *
        65 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_sixtyfifths_mul_sixtyfive_above

/-- Positive denominator-`65`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_sixtyfifths_times_sixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65)
        65 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtyfive
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59))) * 65) =
    (2 : ℝ) ^ k
  rw [problem2_10_ieeeDouble_rounded_two_pow_sixtyfifths_mul_sixtyfive_above]
  exact problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`65` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65 =
      -((8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 65) =
    -((8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59)))
  have harg : (-((2 : ℝ) ^ k)) / 65 = -(((2 : ℝ) ^ k) / 65) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_sixtyfifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtyfive_exact_mul_sixtyfive_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65) *
        65 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtyfifths_mul_sixtyfive_above

/-- Negative denominator-`65`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_sixtyfifths_times_sixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65)
        65 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtyfive
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8868626958514208 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 59)))) * 65) =
    -((2 : ℝ) ^ k)
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_sixtyfifths_mul_sixtyfive_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`65`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_sixtyfifths_times_sixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65)
          65 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65)
          65 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_sixtyfifths_times_sixtyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_sixtyfifths_times_sixtyfive hk⟩

/-- Finite-normal range side condition for the denominator-`129`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onehundredtwentynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 129) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 129 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredtwentyninth
      (by linarith : (1 / 129 : ℝ) ≤ ((2 : ℝ) ^ k) / 129)
  · have hdiv_le : ((2 : ℝ) ^ k) / 129 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`129` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/129` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredtwentyninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 129) =
      (8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8937376004704240 (((k : ℤ) - 7))
  let b : ℝ :=
    fmt.normalizedValue false (8937376004704240 + 1) (((k : ℤ) - 7))
  let x : ℝ := ((2 : ℝ) ^ k) / 129
  have hm : fmt.normalizedMantissa 8937376004704240 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8937376004704240 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8937376004704240, (((k : ℤ) - 7)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8937376004704241 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 60)) * (2 : ℝ) ^ (60 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 60)) (60 : ℤ)
    have hexp : ((k : ℤ) - 60) + 60 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (16 * ((2 : ℝ) ^ (((k : ℤ) - 60)))) / 129 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (113 * ((2 : ℝ) ^ (((k : ℤ) - 60)))) / 129 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 60)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (16 * ((2 : ℝ) ^ (((k : ℤ) - 60)))) / 129 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (113 * ((2 : ℝ) ^ (((k : ℤ) - 60)))) / 129 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onehundredtwentynine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`129`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredtwentynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 129 =
      (8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 129) =
    (8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60))
  exact problem2_10_ieeeDouble_two_pow_onehundredtwentyninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`129`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredtwentynine_exact_mul_onehundredtwentynine_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 129) *
        129 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredtwentynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onehundredtwentyninths_mul_onehundredtwentynine_below

/-- Positive denominator-`129`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredtwentyninths_times_onehundredtwentynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 129)
        129 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredtwentynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60))) * 129) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onehundredtwentyninths_mul_onehundredtwentynine_below]
  exact problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`129` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredtwentynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 129 =
      -((8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 129) =
    -((8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60)))
  have harg : (-((2 : ℝ) ^ k)) / 129 = -(((2 : ℝ) ^ k) / 129) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_onehundredtwentyninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredtwentynine_exact_mul_onehundredtwentynine_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 129) *
        129 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredtwentynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredtwentyninths_mul_onehundredtwentynine_below

/-- Negative denominator-`129`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onehundredtwentyninths_times_onehundredtwentynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 129)
        129 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredtwentynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8937376004704240 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 60)))) * 129) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredtwentyninths_mul_onehundredtwentynine_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`129`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onehundredtwentyninths_times_onehundredtwentynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 129)
          129 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 129)
          129 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onehundredtwentyninths_times_onehundredtwentynine hk,
    problem2_10_ieeeDouble_neg_two_pow_onehundredtwentyninths_times_onehundredtwentynine hk⟩

/-- Finite-normal range side condition for the denominator-`257`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twohundredfiftyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 257) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 257 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredfiftyseventh
      (by linarith : (1 / 257 : ℝ) ≤ ((2 : ℝ) ^ k) / 257)
  · have hdiv_le : ((2 : ℝ) ^ k) / 257 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`257` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/257` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredfiftysevenths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 257) =
      (8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8972151786823711 (((k : ℤ) - 8))
  let b : ℝ :=
    fmt.normalizedValue false (8972151786823711 + 1) (((k : ℤ) - 8))
  let x : ℝ := ((2 : ℝ) ^ k) / 257
  have hm : fmt.normalizedMantissa 8972151786823711 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8972151786823711 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8972151786823711, (((k : ℤ) - 8)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8972151786823711 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 61)) * (2 : ℝ) ^ (61 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 61)) (61 : ℤ)
    have hexp : ((k : ℤ) - 61) + 61 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (225 * ((2 : ℝ) ^ (((k : ℤ) - 61)))) / 257 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (32 * ((2 : ℝ) ^ (((k : ℤ) - 61)))) / 257 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 61)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (225 * ((2 : ℝ) ^ (((k : ℤ) - 61)))) / 257 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (32 * ((2 : ℝ) ^ (((k : ℤ) - 61)))) / 257 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twohundredfiftyseven_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`257`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredfiftyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 257 =
      (8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 257) =
    (8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61))
  exact problem2_10_ieeeDouble_two_pow_twohundredfiftysevenths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`257`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredfiftyseven_exact_mul_twohundredfiftyseven_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 257) *
        257 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredfiftyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twohundredfiftysevenths_mul_twohundredfiftyseven_above

/-- Positive denominator-`257`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredfiftysevenths_times_twohundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 257)
        257 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredfiftyseven
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61))) * 257) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twohundredfiftysevenths_mul_twohundredfiftyseven_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_sixteenth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`257` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredfiftyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 257 =
      -((8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 257) =
    -((8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61)))
  have harg : (-((2 : ℝ) ^ k)) / 257 = -(((2 : ℝ) ^ k) / 257) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_twohundredfiftysevenths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredfiftyseven_exact_mul_twohundredfiftyseven_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 257) *
        257 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredfiftyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredfiftysevenths_mul_twohundredfiftyseven_above

/-- Generic signed final rounding fact for the denominator-`257` power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_sixteenth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Negative denominator-`257`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twohundredfiftysevenths_times_twohundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 257)
        257 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredfiftyseven
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8972151786823712 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 61)))) * 257) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredfiftysevenths_mul_twohundredfiftyseven_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`257`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twohundredfiftysevenths_times_twohundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 257)
          257 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 257)
          257 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twohundredfiftysevenths_times_twohundredfiftyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_twohundredfiftysevenths_times_twohundredfiftyseven hk⟩

/-- Finite-normal range side condition for the denominator-`513`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fivehundredthirteen_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 513) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 513 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredthirteenth
      (by linarith : (1 / 513 : ℝ) ≤ ((2 : ℝ) ^ k) / 513)
  · have hdiv_le : ((2 : ℝ) ^ k) / 513 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`513` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/513` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredthirteenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 513) =
      (8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8989641361456896 (((k : ℤ) - 9))
  let b : ℝ :=
    fmt.normalizedValue false (8989641361456896 + 1) (((k : ℤ) - 9))
  let x : ℝ := ((2 : ℝ) ^ k) / 513
  have hm : fmt.normalizedMantissa 8989641361456896 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8989641361456896 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8989641361456896, (((k : ℤ) - 9)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8989641361456897 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 62)) * (2 : ℝ) ^ (62 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 62)) (62 : ℤ)
    have hexp : ((k : ℤ) - 62) + 62 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (256 * ((2 : ℝ) ^ (((k : ℤ) - 62)))) / 513 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (257 * ((2 : ℝ) ^ (((k : ℤ) - 62)))) / 513 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 62)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (256 * ((2 : ℝ) ^ (((k : ℤ) - 62)))) / 513 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (257 * ((2 : ℝ) ^ (((k : ℤ) - 62)))) / 513 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fivehundredthirteen_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`513`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredthirteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 513 =
      (8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 513) =
    (8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62))
  exact problem2_10_ieeeDouble_two_pow_fivehundredthirteenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`513`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredthirteen_exact_mul_fivehundredthirteen_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 513) *
        513 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredthirteen hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredthirteenths_mul_fivehundredthirteen_midpoint

/-- Positive denominator-`513`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredthirteenths_times_fivehundredthirteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 513)
        513 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredthirteen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62))) * 513) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredthirteenths_mul_fivehundredthirteen_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow hk

/-- Signed companion of the denominator-`513` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 513 =
      -((8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 513) =
    -((8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62)))
  have harg : (-((2 : ℝ) ^ k)) / 513 = -(((2 : ℝ) ^ k) / 513) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_fivehundredthirteenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirteen_exact_mul_fivehundredthirteen_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 513) *
        513 =
      -((2 : ℝ) ^ k) + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirteen hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredthirteenths_mul_fivehundredthirteen_midpoint

/-- Negative denominator-`513`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fivehundredthirteenths_times_fivehundredthirteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 513)
        513 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirteen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8989641361456896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 62)))) * 513) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredthirteenths_mul_fivehundredthirteen_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow hk

/-- Signed denominator-`513`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fivehundredthirteenths_times_fivehundredthirteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 513)
          513 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 513)
          513 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fivehundredthirteenths_times_fivehundredthirteen hk,
    problem2_10_ieeeDouble_neg_two_pow_fivehundredthirteenths_times_fivehundredthirteen hk⟩

/-- Finite-normal range side condition for the denominator-`1025` final
product: `2^k - 2^(k-60)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 60)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 60) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 60)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`1025`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k - 2^(k-60)`, rounds back
to `2^k` because it is strictly closer to the power-of-two endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 60)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        128 * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 60)) (7 : ℤ)
    have hexp : ((k : ℤ) - 60) + 7 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 60)) * (2 : ℝ) ^ (7 : ℤ) := h
      _ = 128 * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 127 * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`1025`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`2147483649` final
product: `2^k - 2^(k-62)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_onethousandtwentyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 62)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 62) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 62)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`2147483649`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-62)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_onethousandtwentyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 62)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        512 * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 62)) (9 : ℤ)
    have hexp : ((k : ℤ) - 62) + 9 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 62)) * (2 : ℝ) ^ (9 : ℤ) := h
      _ = 512 * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 511 * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_onethousandtwentyfourth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`2147483649`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_onethousandtwentyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_onethousandtwentyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`1025`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onethousandtwentyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1025) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1025 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onethousandtwentyfifth
      (by linarith : (1 / 1025 : ℝ) ≤ ((2 : ℝ) ^ k) / 1025)
  · have hdiv_le : ((2 : ℝ) ^ k) / 1025 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`1025` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/1025` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onethousandtwentyfifths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1025) =
      (8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 8998411743272952 (((k : ℤ) - 10))
  let b : ℝ :=
    fmt.normalizedValue false (8998411743272952 + 1) (((k : ℤ) - 10))
  let x : ℝ := ((2 : ℝ) ^ k) / 1025
  have hm : fmt.normalizedMantissa 8998411743272952 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (8998411743272952 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 8998411743272952, (((k : ℤ) - 10)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (8998411743272953 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 63)) * (2 : ℝ) ^ (63 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 63)) (63 : ℤ)
    have hexp : ((k : ℤ) - 63) + 63 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (8 * ((2 : ℝ) ^ (((k : ℤ) - 63)))) / 1025 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (1017 * ((2 : ℝ) ^ (((k : ℤ) - 63)))) / 1025 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 63)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (8 * ((2 : ℝ) ^ (((k : ℤ) - 63)))) / 1025 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (1017 * ((2 : ℝ) ^ (((k : ℤ) - 63)))) / 1025 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onethousandtwentyfive_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`1025`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onethousandtwentyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1025 =
      (8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1025) =
    (8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63))
  exact problem2_10_ieeeDouble_two_pow_onethousandtwentyfifths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`1025`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onethousandtwentyfive_exact_mul_onethousandtwentyfive_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1025) *
        1025 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onethousandtwentyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onethousandtwentyfifths_mul_onethousandtwentyfive_below

/-- Positive denominator-`1025`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onethousandtwentyfifths_times_onethousandtwentyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1025)
        1025 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onethousandtwentyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63))) * 1025) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onethousandtwentyfifths_mul_onethousandtwentyfive_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`1025` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onethousandtwentyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1025 =
      -((8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1025) =
    -((8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63)))
  have harg : (-((2 : ℝ) ^ k)) / 1025 = -(((2 : ℝ) ^ k) / 1025) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_onethousandtwentyfifths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_onethousandtwentyfive_exact_mul_onethousandtwentyfive_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1025) *
        1025 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onethousandtwentyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onethousandtwentyfifths_mul_onethousandtwentyfive_below

/-- Negative denominator-`1025`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onethousandtwentyfifths_times_onethousandtwentyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1025)
        1025 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onethousandtwentyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8998411743272952 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 63)))) * 1025) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onethousandtwentyfifths_mul_onethousandtwentyfive_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`1025`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onethousandtwentyfifths_times_onethousandtwentyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1025)
          1025 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1025)
          1025 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onethousandtwentyfifths_times_onethousandtwentyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_onethousandtwentyfifths_times_onethousandtwentyfive hk⟩

/-- Finite-normal range side condition for the denominator-`2049`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twothousandfortynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2049) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2049 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twothousandfortyninth
      (by linarith : (1 / 2049 : ℝ) ≤ ((2 : ℝ) ^ k) / 2049)
  · have hdiv_le : ((2 : ℝ) ^ k) / 2049 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`2049` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/2049` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twothousandfortyninths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2049) =
      (9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9002803354665471 (((k : ℤ) - 11))
  let b : ℝ :=
    fmt.normalizedValue false (9002803354665471 + 1) (((k : ℤ) - 11))
  let x : ℝ := ((2 : ℝ) ^ k) / 2049
  have hm : fmt.normalizedMantissa 9002803354665471 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9002803354665471 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9002803354665471, (((k : ℤ) - 11)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9002803354665471 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 64)) * (2 : ℝ) ^ (64 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 64)) (64 : ℤ)
    have hexp : ((k : ℤ) - 64) + 64 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (1537 * ((2 : ℝ) ^ (((k : ℤ) - 64)))) / 2049 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (512 * ((2 : ℝ) ^ (((k : ℤ) - 64)))) / 2049 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 64)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (1537 * ((2 : ℝ) ^ (((k : ℤ) - 64)))) / 2049 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (512 * ((2 : ℝ) ^ (((k : ℤ) - 64)))) / 2049 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twothousandfortynine_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`2049`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twothousandfortynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2049 =
      (9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2049) =
    (9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64))
  exact problem2_10_ieeeDouble_two_pow_twothousandfortyninths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`2049`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twothousandfortynine_exact_mul_twothousandfortynine_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2049) *
        2049 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twothousandfortynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twothousandfortyninths_mul_twothousandfortynine_above

/-- Positive denominator-`2049`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twothousandfortyninths_times_twothousandfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2049)
        2049 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twothousandfortynine
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64))) * 2049) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twothousandfortyninths_mul_twothousandfortynine_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`2049` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twothousandfortynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2049 =
      -((9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2049) =
    -((9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64)))
  have harg : (-((2 : ℝ) ^ k)) / 2049 = -(((2 : ℝ) ^ k) / 2049) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_twothousandfortyninths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_twothousandfortynine_exact_mul_twothousandfortynine_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2049) *
        2049 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twothousandfortynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twothousandfortyninths_mul_twothousandfortynine_above

/-- Negative denominator-`2049`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twothousandfortyninths_times_twothousandfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2049)
        2049 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twothousandfortynine
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9002803354665472 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 64)))) * 2049) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twothousandfortyninths_mul_twothousandfortynine_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_eighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`2049`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twothousandfortyninths_times_twothousandfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2049)
          2049 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2049)
          2049 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twothousandfortyninths_times_twothousandfortynine hk,
    problem2_10_ieeeDouble_neg_two_pow_twothousandfortyninths_times_twothousandfortynine hk⟩

/-- Finite-normal range side condition for the denominator-`4097` final
product: `2^k + 2^(k-60)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 60)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 60)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 60) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`4097`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-60)`, rounds back
to `2^k` because it is one 256th of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 60)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        256 * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 60)) (8 : ℤ)
    have hexp : ((k : ℤ) - 60) + 8 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 60)) * (2 : ℝ) ^ (8 : ℤ) := h
      _ = 256 * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 255 * (2 : ℝ) ^ (((k : ℤ) - 60)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`4097`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`2305843009213693953`
final product: `2^k + 2^(k-61)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwelfth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 61)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 61)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 61) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`2305843009213693953`, power-of-two final rounding fact
for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-61)`, rounds back to `2^k` because it is one 512th of an ulp above
`2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwelfth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 61)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        512 * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 61)) (9 : ℤ)
    have hexp : ((k : ℤ) - 61) + 9 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 61)) * (2 : ℝ) ^ (9 : ℤ) := h
      _ = 512 * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 61)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 511 * (2 : ℝ) ^ (((k : ℤ) - 61)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwelfth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`2305843009213693953`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_fivehundredtwelfth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwelfth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`4611686018427387905`
final product: `2^k + 2^(k-62)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_onethousandtwentyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 62)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 62)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 62) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`4611686018427387905`, power-of-two final rounding fact
for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-62)`, rounds back to `2^k` because it is one 1024th of an ulp above
`2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_onethousandtwentyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 62)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        1024 * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 62)) (10 : ℤ)
    have hexp : ((k : ℤ) - 62) + 10 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 62)) * (2 : ℝ) ^ (10 : ℤ) := h
      _ = 1024 * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 1023 * (2 : ℝ) ^ (((k : ℤ) - 62)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_onethousandtwentyfourth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`4611686018427387905`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_onethousandtwentyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_onethousandtwentyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`2097153` final
product: `2^k + 2^(k-63)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_twothousandfortyeighth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 63)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 63)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 63) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`2097153`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-63)`, rounds back
to `2^k` because it is one 2048th of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 63)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        2048 * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 63)) (11 : ℤ)
    have hexp : ((k : ℤ) - 63) + 11 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 63)) * (2 : ℝ) ^ (11 : ℤ) := h
      _ = 2048 * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 63)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 2047 * (2 : ℝ) ^ (((k : ℤ) - 63)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_twothousandfortyeighth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`2097153`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`18446744073709551617`
final product: `2^k + 2^(k-64)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_fourthousandninetysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 64)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 64)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 64) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`18446744073709551617`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-64)`, rounds back to `2^k` because it is one 4096th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_fourthousandninetysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 64)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        4096 * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 64)) (12 : ℤ)
    have hexp : ((k : ℤ) - 64) + 12 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 64)) * (2 : ℝ) ^ (12 : ℤ) := h
      _ = 4096 * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 4095 * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_fourthousandninetysixth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`18446744073709551617`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_fourthousandninetysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_fourthousandninetysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`4194305` final
product: `2^k + 2^(k-66)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 66)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 66)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 66) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`4194305`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-66)`, rounds back
to `2^k` because it is one 16384th of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 66)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        16384 * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 66)) (14 : ℤ)
    have hexp : ((k : ℤ) - 66) + 14 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 66)) * (2 : ℝ) ^ (14 : ℤ) := h
      _ = 16384 * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 16383 * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`4194305`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`147573952589676412929`
final product: `2^k + 2^(k-67)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 67)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 67)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 67) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`147573952589676412929`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-67)`, rounds back to `2^k` because it is one 32768th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 67)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        32768 * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 67)) (15 : ℤ)
    have hexp : ((k : ℤ) - 67) + 15 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 67)) * (2 : ℝ) ^ (15 : ℤ) := h
      _ = 32768 * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 67)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 32767 * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`147573952589676412929`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`295147905179352825857`
final product: `2^k + 2^(k-68)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 68)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 68)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 68) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`295147905179352825857`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-68)`, rounds back to `2^k` because it is one 65536th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 68)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        65536 * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 68)) (16 : ℤ)
    have hexp : ((k : ℤ) - 68) + 16 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 68)) * (2 : ℝ) ^ (16 : ℤ) := h
      _ = 65536 * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 65535 * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`295147905179352825857`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`8589934593` final
product: `2^k - 2^(k-66)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 66)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 66) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 66)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`8589934593`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-66)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 66)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        8192 * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 66)) (13 : ℤ)
    have hexp : ((k : ℤ) - 66) + 13 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 66)) * (2 : ℝ) ^ (13 : ℤ) := h
      _ = 8192 * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 8191 * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`8589934593`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`8388609` final
product: `2^k + 2^(k-69)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 69)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 69)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 69) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`8388609`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-69)`, rounds back
to `2^k` because it is one 131072nd of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 69)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        131072 * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 69)) (17 : ℤ)
    have hexp : ((k : ℤ) - 69) + 17 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 69)) * (2 : ℝ) ^ (17 : ℤ) := h
      _ = 131072 * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 69)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 131071 * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`8388609`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`1180591620717411303425`
final product: `2^k + 2^(k-70)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 70)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 70)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 70) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`1180591620717411303425`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-70)`, rounds back to `2^k` because it is one 262144th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 70)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        262144 * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 70)) (18 : ℤ)
    have hexp : ((k : ℤ) - 70) + 18 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 70)) * (2 : ℝ) ^ (18 : ℤ) := h
      _ = 262144 * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 262143 * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`1180591620717411303425`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`2361183241434822606849`
final product: `2^k + 2^(k-71)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 71)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 71)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 71) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`2361183241434822606849`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-71)`, rounds back to `2^k` because it is one 524288th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 71)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        524288 * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 71)) (19 : ℤ)
    have hexp : ((k : ℤ) - 71) + 19 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 71)) * (2 : ℝ) ^ (19 : ℤ) := h
      _ = 524288 * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 71)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 524287 * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`2361183241434822606849`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`16777217` final
product: `2^k + 2^(k-72)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 72)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 72)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 72) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`16777217`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k + 2^(k-72)`,
rounds back to `2^k` because it is one 1048576th of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 72)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        1048576 * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 72)) (20 : ℤ)
    have hexp : ((k : ℤ) - 72) + 20 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 72)) * (2 : ℝ) ^ (20 : ℤ) := h
      _ = 1048576 * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 1048575 * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`16777217`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`9444732965739290427393`
final product: `2^k + 2^(k-73)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 73)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 73)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 73) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`9444732965739290427393`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-73)`, rounds back to `2^k` because it is one 2097152nd of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 73)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        2097152 * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 73)) (21 : ℤ)
    have hexp : ((k : ℤ) - 73) + 21 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 73)) * (2 : ℝ) ^ (21 : ℤ) := h
      _ = 2097152 * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 73)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 2097151 * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`9444732965739290427393`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`18889465931478580854785`
final product: `2^k + 2^(k-74)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 74)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 74)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 74) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`18889465931478580854785`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-74)`, rounds back to `2^k` because it is one 4194304th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 74)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        4194304 * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 74)) (22 : ℤ)
    have hexp : ((k : ℤ) - 74) + 22 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 74)) * (2 : ℝ) ^ (22 : ℤ) := h
      _ = 4194304 * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 4194303 * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`18889465931478580854785`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`33554433` final
product: `2^k + 2^(k-75)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 75)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 75)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 75) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`33554433`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k + 2^(k-75)`,
rounds back to `2^k` because it is one 8388608th of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 75)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        8388608 * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 75)) (23 : ℤ)
    have hexp : ((k : ℤ) - 75) + 23 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 75)) * (2 : ℝ) ^ (23 : ℤ) := h
      _ = 8388608 * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 75)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 8388607 * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`33554433`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`75557863725914323419137`
final product: `2^k + 2^(k-76)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 76)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 76)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 76) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`75557863725914323419137`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-76)`, rounds back to `2^k` because it is one 16777216th of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 76)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        16777216 * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 76)) (24 : ℤ)
    have hexp : ((k : ℤ) - 76) + 24 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 76)) * (2 : ℝ) ^ (24 : ℤ) := h
      _ = 16777216 * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 16777215 * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`75557863725914323419137`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`151115727451828646838273`
final product: `2^k + 2^(k-77)` stays in the finite normal range for
`k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 77)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 77)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 77) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`151115727451828646838273`, power-of-two final rounding
fact for Problem 2.10: the exact product after rounded division,
`2^k + 2^(k-77)`, rounds back to `2^k` because it is one 33554432nd of an ulp
above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 77)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        33554432 * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 77)) (25 : ℤ)
    have hexp : ((k : ℤ) - 77) + 25 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 77)) * (2 : ℝ) ^ (25 : ℤ) := h
      _ = 33554432 * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 77)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 33554431 * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`151115727451828646838273`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`67108865` final
product: `2^k + 2^(k-78)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 78)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 78)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 78) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`67108865`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k + 2^(k-78)`,
rounds back to `2^k` because it is one 67108864th of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 78)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        67108864 * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 78)) (26 : ℤ)
    have hexp : ((k : ℤ) - 78) + 26 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 78)) * (2 : ℝ) ^ (26 : ℤ) := h
      _ = 67108864 * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 67108863 * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`67108865`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`4097`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fourthousandninetyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4097) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4097 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fourthousandninetyseventh
      (by linarith : (1 / 4097 : ℝ) ≤ ((2 : ℝ) ^ k) / 4097)
  · have hdiv_le : ((2 : ℝ) ^ k) / 4097 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`4097` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/4097` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fourthousandninetysevenths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4097) =
      (9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9005000768225311 (((k : ℤ) - 12))
  let b : ℝ :=
    fmt.normalizedValue false (9005000768225311 + 1) (((k : ℤ) - 12))
  let x : ℝ := ((2 : ℝ) ^ k) / 4097
  have hm : fmt.normalizedMantissa 9005000768225311 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9005000768225311 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9005000768225311, (((k : ℤ) - 12)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9005000768225311 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 65)) * (2 : ℝ) ^ (65 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 65)) (65 : ℤ)
    have hexp : ((k : ℤ) - 65) + 65 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4065 * ((2 : ℝ) ^ (((k : ℤ) - 65)))) / 4097 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (32 * ((2 : ℝ) ^ (((k : ℤ) - 65)))) / 4097 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 65)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4065 * ((2 : ℝ) ^ (((k : ℤ) - 65)))) / 4097 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (32 * ((2 : ℝ) ^ (((k : ℤ) - 65)))) / 4097 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fourthousandninetyseven_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`4097`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fourthousandninetyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4097 =
      (9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4097) =
    (9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65))
  exact problem2_10_ieeeDouble_two_pow_fourthousandninetysevenths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`4097`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fourthousandninetyseven_exact_mul_fourthousandninetyseven_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4097) *
        4097 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fourthousandninetyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fourthousandninetysevenths_mul_fourthousandninetyseven_above

/-- Positive denominator-`4097`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fourthousandninetysevenths_times_fourthousandninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4097)
        4097 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fourthousandninetyseven
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65))) * 4097) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fourthousandninetysevenths_mul_fourthousandninetyseven_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`4097` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fourthousandninetyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4097 =
      -((9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4097) =
    -((9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65)))
  have harg : (-((2 : ℝ) ^ k)) / 4097 = -(((2 : ℝ) ^ k) / 4097) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_fourthousandninetysevenths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_fourthousandninetyseven_exact_mul_fourthousandninetyseven_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4097) *
        4097 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fourthousandninetyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fourthousandninetysevenths_mul_fourthousandninetyseven_above

/-- Negative denominator-`4097`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fourthousandninetysevenths_times_fourthousandninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4097)
        4097 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fourthousandninetyseven
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9005000768225312 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 65)))) * 4097) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fourthousandninetysevenths_mul_fourthousandninetyseven_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`4097`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fourthousandninetysevenths_times_fourthousandninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4097)
          4097 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4097)
          4097 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fourthousandninetysevenths_times_fourthousandninetyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_fourthousandninetysevenths_times_fourthousandninetyseven hk⟩

/-- Finite-normal range side condition for the denominator-`8193` final
product: `2^k + 2^(k-65)` stays in the finite normal range for `k <= 1022`. -/
private theorem problem2_10_ieeeDouble_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65))) := by
  rw [finiteNormalRange]
  have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
    one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
  have htail_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 65)) :=
    le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (by linarith : (1 / 3 : ℝ) ≤
        (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65)))
  · have hsmall :
        (2 : ℝ) ^ (((k : ℤ) - 65)) ≤ (2 : ℝ) ^ k := by
      simpa [zpow_natCast] using
        (zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
          (by omega : ((k : ℤ) - 65) ≤ (k : ℤ)))
    have hdouble : (2 : ℝ) ^ k + (2 : ℝ) ^ k = (2 : ℝ) ^ (k + 1) := by
      rw [pow_succ]
      ring
    calc
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65)) ≤
          (2 : ℝ) ^ k + (2 : ℝ) ^ k := add_le_add (le_refl _) hsmall
      _ = (2 : ℝ) ^ (k + 1) := hdouble
      _ ≤ ieeeDoubleFormat.maxFiniteMagnitude :=
        problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
          (by omega : k + 1 ≤ 1023)

/-- Generic denominator-`8193`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k + 2^(k-65)`, rounds back
to `2^k` because it is one 8192nd of an ulp above `2^k`. -/
theorem problem2_10_ieeeDouble_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let b : ℝ :=
    fmt.normalizedValue false (fmt.minNormalMantissa + 1) (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65))
  have hm : fmt.normalizedMantissa fmt.minNormalMantissa :=
    fmt.minNormalMantissa_normalized
  have hmnext : fmt.normalizedMantissa (fmt.minNormalMantissa + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, fmt.minNormalMantissa, (((k : ℤ) + 1)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) ^ k := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 52)) := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_successor_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 65)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have htail_to_ulp :
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
        8192 * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 65)) (13 : ℤ)
    have hexp : ((k : ℤ) - 65) + 13 = (k : ℤ) - 52 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 52)) =
          (2 : ℝ) ^ (((k : ℤ) - 65)) * (2 : ℝ) ^ (13 : ℤ) := h
      _ = 8192 * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_pos : 0 < x - a := by
    simpa [x, ha_value] using hzpos
  have hleft_dist : x - a = (2 : ℝ) ^ (((k : ℤ) - 65)) := by
    simp [x, ha_value]
  have hright_dist : b - x = 8191 * (2 : ℝ) ^ (((k : ℤ) - 65)) := by
    simp [x, hb_value, htail_to_ulp]
    ring
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    nlinarith [hzpos]
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hleft_pos),
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Signed final rounding fact for the denominator-`8193`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`8193`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_eightthousandonehundredninetythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 8193) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 8193 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_eightthousandonehundredninetythird
      (by linarith : (1 / 8193 : ℝ) ≤ ((2 : ℝ) ^ k) / 8193)
  · have hdiv_le : ((2 : ℝ) ^ k) / 8193 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`8193` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/8193` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_eightthousandonehundredninetythirds_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8193) =
      (9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9006099877314561 (((k : ℤ) - 13))
  let b : ℝ :=
    fmt.normalizedValue false (9006099877314561 + 1) (((k : ℤ) - 13))
  let x : ℝ := ((2 : ℝ) ^ k) / 8193
  have hm : fmt.normalizedMantissa 9006099877314561 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9006099877314561 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9006099877314561, (((k : ℤ) - 13)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9006099877314561 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 66)) * (2 : ℝ) ^ (66 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 66)) (66 : ℤ)
    have hexp : ((k : ℤ) - 66) + 66 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (8191 * ((2 : ℝ) ^ (((k : ℤ) - 66)))) / 8193 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2 * ((2 : ℝ) ^ (((k : ℤ) - 66)))) / 8193 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 66)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (8191 * ((2 : ℝ) ^ (((k : ℤ) - 66)))) / 8193 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2 * ((2 : ℝ) ^ (((k : ℤ) - 66)))) / 8193 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_eightthousandonehundredninetythree_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`8193`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_eightthousandonehundredninetythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8193 =
      (9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8193) =
    (9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66))
  exact problem2_10_ieeeDouble_two_pow_eightthousandonehundredninetythirds_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`8193`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_eightthousandonehundredninetythree_exact_mul_eightthousandonehundredninetythree_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8193) *
        8193 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_eightthousandonehundredninetythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_eightthousandonehundredninetythirds_mul_eightthousandonehundredninetythree_above

/-- Positive denominator-`8193`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_eightthousandonehundredninetythirds_times_eightthousandonehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8193)
        8193 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_eightthousandonehundredninetythree
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66))) * 8193) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_eightthousandonehundredninetythirds_mul_eightthousandonehundredninetythree_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`8193` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_eightthousandonehundredninetythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8193 =
      -((9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 8193) =
    -((9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66)))
  have harg : (-((2 : ℝ) ^ k)) / 8193 = -(((2 : ℝ) ^ k) / 8193) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_eightthousandonehundredninetythirds_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_eightthousandonehundredninetythree_exact_mul_eightthousandonehundredninetythree_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8193) *
        8193 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_eightthousandonehundredninetythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_eightthousandonehundredninetythirds_mul_eightthousandonehundredninetythree_above

/-- Negative denominator-`8193`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_eightthousandonehundredninetythirds_times_eightthousandonehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8193)
        8193 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_eightthousandonehundredninetythree
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9006099877314562 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 66)))) * 8193) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_eightthousandonehundredninetythirds_mul_eightthousandonehundredninetythree_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`8193`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_eightthousandonehundredninetythirds_times_eightthousandonehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8193)
          8193 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8193)
          8193 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_eightthousandonehundredninetythirds_times_eightthousandonehundredninetythree hk,
    problem2_10_ieeeDouble_neg_two_pow_eightthousandonehundredninetythirds_times_eightthousandonehundredninetythree hk⟩

/-- Finite-normal range side condition for the denominator-`16385`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_sixteenthousandthreehundredeightyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 16385) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 16385 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixteenthousandthreehundredeightyfifth
      (by linarith : (1 / 16385 : ℝ) ≤ ((2 : ℝ) ^ k) / 16385)
  · have hdiv_le : ((2 : ℝ) ^ k) / 16385 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`16385` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/16385` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_sixteenthousandthreehundredeightyfifths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 16385) =
      (9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9006649532479488 (((k : ℤ) - 14))
  let b : ℝ :=
    fmt.normalizedValue false (9006649532479488 + 1) (((k : ℤ) - 14))
  let x : ℝ := ((2 : ℝ) ^ k) / 16385
  have hm : fmt.normalizedMantissa 9006649532479488 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9006649532479488 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9006649532479488, (((k : ℤ) - 14)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9006649532479489 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 67)) * (2 : ℝ) ^ (67 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 67)) (67 : ℤ)
    have hexp : ((k : ℤ) - 67) + 67 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (2048 * ((2 : ℝ) ^ (((k : ℤ) - 67)))) / 16385 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (14337 * ((2 : ℝ) ^ (((k : ℤ) - 67)))) / 16385 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 67)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (2048 * ((2 : ℝ) ^ (((k : ℤ) - 67)))) / 16385 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (14337 * ((2 : ℝ) ^ (((k : ℤ) - 67)))) / 16385 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_sixteenthousandthreehundredeightyfive_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`16385`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixteenthousandthreehundredeightyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16385 =
      (9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 16385) =
    (9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67))
  exact problem2_10_ieeeDouble_two_pow_sixteenthousandthreehundredeightyfifths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`16385`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixteenthousandthreehundredeightyfive_exact_mul_sixteenthousandthreehundredeightyfive_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16385) *
        16385 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixteenthousandthreehundredeightyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_sixteenthousandthreehundredeightyfifths_mul_sixteenthousandthreehundredeightyfive_below

/-- Positive denominator-`16385`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_sixteenthousandthreehundredeightyfifths_times_sixteenthousandthreehundredeightyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16385)
        16385 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixteenthousandthreehundredeightyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67))) * 16385) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_sixteenthousandthreehundredeightyfifths_mul_sixteenthousandthreehundredeightyfive_below]
  exact problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`16385` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixteenthousandthreehundredeightyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16385 =
      -((9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 16385) =
    -((9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67)))
  have harg : (-((2 : ℝ) ^ k)) / 16385 = -(((2 : ℝ) ^ k) / 16385) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_sixteenthousandthreehundredeightyfifths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_sixteenthousandthreehundredeightyfive_exact_mul_sixteenthousandthreehundredeightyfive_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16385) *
        16385 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixteenthousandthreehundredeightyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixteenthousandthreehundredeightyfifths_mul_sixteenthousandthreehundredeightyfive_below

/-- Negative denominator-`16385`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_sixteenthousandthreehundredeightyfifths_times_sixteenthousandthreehundredeightyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16385)
        16385 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixteenthousandthreehundredeightyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9006649532479488 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 67)))) * 16385) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixteenthousandthreehundredeightyfifths_mul_sixteenthousandthreehundredeightyfive_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`16385`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_sixteenthousandthreehundredeightyfifths_times_sixteenthousandthreehundredeightyfive
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16385)
          16385 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16385)
          16385 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_sixteenthousandthreehundredeightyfifths_times_sixteenthousandthreehundredeightyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_sixteenthousandthreehundredeightyfifths_times_sixteenthousandthreehundredeightyfive hk⟩

/-- Finite-normal range side condition for the denominator-`32769`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_thirtytwothousandsevenhundredsixtynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 32769) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 32769 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtytwothousandsevenhundredsixtyninth
      (by linarith : (1 / 32769 : ℝ) ≤ ((2 : ℝ) ^ k) / 32769)
  · have hdiv_le : ((2 : ℝ) ^ k) / 32769 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`32769` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/32769` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_thirtytwothousandsevenhundredsixtyninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 32769) =
      (9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9006924385222400 (((k : ℤ) - 15))
  let b : ℝ :=
    fmt.normalizedValue false (9006924385222400 + 1) (((k : ℤ) - 15))
  let x : ℝ := ((2 : ℝ) ^ k) / 32769
  have hm : fmt.normalizedMantissa 9006924385222400 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9006924385222400 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9006924385222400, (((k : ℤ) - 15)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9006924385222401 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 68)) * (2 : ℝ) ^ (68 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 68)) (68 : ℤ)
    have hexp : ((k : ℤ) - 68) + 68 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (256 * ((2 : ℝ) ^ (((k : ℤ) - 68)))) / 32769 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (32513 * ((2 : ℝ) ^ (((k : ℤ) - 68)))) / 32769 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 68)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (256 * ((2 : ℝ) ^ (((k : ℤ) - 68)))) / 32769 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (32513 * ((2 : ℝ) ^ (((k : ℤ) - 68)))) / 32769 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_thirtytwothousandsevenhundredsixtynine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`32769`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtytwothousandsevenhundredsixtynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 32769 =
      (9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 32769) =
    (9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68))
  exact problem2_10_ieeeDouble_two_pow_thirtytwothousandsevenhundredsixtyninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`32769`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtytwothousandsevenhundredsixtynine_exact_mul_thirtytwothousandsevenhundredsixtynine_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 32769) *
        32769 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtytwothousandsevenhundredsixtynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_thirtytwothousandsevenhundredsixtyninths_mul_thirtytwothousandsevenhundredsixtynine_below

/-- Positive denominator-`32769`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_thirtytwothousandsevenhundredsixtyninths_times_thirtytwothousandsevenhundredsixtynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 32769)
        32769 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtytwothousandsevenhundredsixtynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68))) * 32769) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_thirtytwothousandsevenhundredsixtyninths_mul_thirtytwothousandsevenhundredsixtynine_below]
  exact problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`32769` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtytwothousandsevenhundredsixtynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 32769 =
      -((9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 32769) =
    -((9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68)))
  have harg : (-((2 : ℝ) ^ k)) / 32769 = -(((2 : ℝ) ^ k) / 32769) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_thirtytwothousandsevenhundredsixtyninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtytwothousandsevenhundredsixtynine_exact_mul_thirtytwothousandsevenhundredsixtynine_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 32769) *
        32769 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtytwothousandsevenhundredsixtynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtytwothousandsevenhundredsixtyninths_mul_thirtytwothousandsevenhundredsixtynine_below

/-- Negative denominator-`32769`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_thirtytwothousandsevenhundredsixtyninths_times_thirtytwothousandsevenhundredsixtynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 32769)
        32769 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtytwothousandsevenhundredsixtynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9006924385222400 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 68)))) * 32769) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtytwothousandsevenhundredsixtyninths_mul_thirtytwothousandsevenhundredsixtynine_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`32769`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_thirtytwothousandsevenhundredsixtyninths_times_thirtytwothousandsevenhundredsixtynine
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 32769)
          32769 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 32769)
          32769 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_thirtytwothousandsevenhundredsixtyninths_times_thirtytwothousandsevenhundredsixtynine hk,
    problem2_10_ieeeDouble_neg_two_pow_thirtytwothousandsevenhundredsixtyninths_times_thirtytwothousandsevenhundredsixtynine hk⟩

/-- Finite-normal range side condition for the denominator-`65537` final
product: `2^k - 2^(k-64)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_fourthousandninetysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 64)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 64) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 64)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`65537`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k - 2^(k-64)`, rounds back
to `2^k` because it is strictly closer to the power-of-two endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 64)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        2048 * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 64)) (11 : ℤ)
    have hexp : ((k : ℤ) - 64) + 11 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 64)) * (2 : ℝ) ^ (11 : ℤ) := h
      _ = 2048 * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 2047 * (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 64)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_fourthousandninetysixth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`65537`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`65537`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_sixtyfivethousandfivehundredthirtyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 65537) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 65537 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtyfivethousandfivehundredthirtyseventh
      (by linarith : (1 / 65537 : ℝ) ≤ ((2 : ℝ) ^ k) / 65537)
  · have hdiv_le : ((2 : ℝ) ^ k) / 65537 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`65537` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/65537` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_sixtyfivethousandfivehundredthirtysevenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 65537) =
      (9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007061817884640 (((k : ℤ) - 16))
  let b : ℝ :=
    fmt.normalizedValue false (9007061817884640 + 1) (((k : ℤ) - 16))
  let x : ℝ := ((2 : ℝ) ^ k) / 65537
  have hm : fmt.normalizedMantissa 9007061817884640 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007061817884640 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007061817884640, (((k : ℤ) - 16)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007061817884641 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 69)) * (2 : ℝ) ^ (69 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 69)) (69 : ℤ)
    have hexp : ((k : ℤ) - 69) + 69 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (32 * ((2 : ℝ) ^ (((k : ℤ) - 69)))) / 65537 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (65505 * ((2 : ℝ) ^ (((k : ℤ) - 69)))) / 65537 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 69)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (32 * ((2 : ℝ) ^ (((k : ℤ) - 69)))) / 65537 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (65505 * ((2 : ℝ) ^ (((k : ℤ) - 69)))) / 65537 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_sixtyfivethousandfivehundredthirtyseven_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`65537`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtyfivethousandfivehundredthirtyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65537 =
      (9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 65537) =
    (9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69))
  exact problem2_10_ieeeDouble_two_pow_sixtyfivethousandfivehundredthirtysevenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`65537`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtyfivethousandfivehundredthirtyseven_exact_mul_sixtyfivethousandfivehundredthirtyseven_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65537) *
        65537 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtyfivethousandfivehundredthirtyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_sixtyfivethousandfivehundredthirtysevenths_mul_sixtyfivethousandfivehundredthirtyseven_below

/-- Positive denominator-`65537`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_sixtyfivethousandfivehundredthirtysevenths_times_sixtyfivethousandfivehundredthirtyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65537)
        65537 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtyfivethousandfivehundredthirtyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69))) * 65537) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_sixtyfivethousandfivehundredthirtysevenths_mul_sixtyfivethousandfivehundredthirtyseven_below]
  exact problem2_10_ieeeDouble_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`65537` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtyfivethousandfivehundredthirtyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65537 =
      -((9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 65537) =
    -((9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69)))
  have harg : (-((2 : ℝ) ^ k)) / 65537 = -(((2 : ℝ) ^ k) / 65537) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_sixtyfivethousandfivehundredthirtysevenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtyfivethousandfivehundredthirtyseven_exact_mul_sixtyfivethousandfivehundredthirtyseven_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65537) *
        65537 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtyfivethousandfivehundredthirtyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtyfivethousandfivehundredthirtysevenths_mul_sixtyfivethousandfivehundredthirtyseven_below

/-- Negative denominator-`65537`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_sixtyfivethousandfivehundredthirtysevenths_times_sixtyfivethousandfivehundredthirtyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65537)
        65537 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtyfivethousandfivehundredthirtyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007061817884640 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 69)))) * 65537) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtyfivethousandfivehundredthirtysevenths_mul_sixtyfivethousandfivehundredthirtyseven_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`65537`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_sixtyfivethousandfivehundredthirtysevenths_times_sixtyfivethousandfivehundredthirtyseven
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 65537)
          65537 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 65537)
          65537 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_sixtyfivethousandfivehundredthirtysevenths_times_sixtyfivethousandfivehundredthirtyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_sixtyfivethousandfivehundredthirtysevenths_times_sixtyfivethousandfivehundredthirtyseven hk⟩

/-- Finite-normal range side condition for the denominator-`131073` final
product: `2^k - 2^(k-68)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 68)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 68) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 68)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`131073`, power-of-two final rounding fact for Problem
2.10: the exact product after rounded division, `2^k - 2^(k-68)`, rounds back
to `2^k` because it is strictly closer to the power-of-two endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 68)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        32768 * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 68)) (15 : ℤ)
    have hexp : ((k : ℤ) - 68) + 15 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 68)) * (2 : ℝ) ^ (15 : ℤ) := h
      _ = 32768 * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 32767 * (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 68)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`131073`, power-of-two
subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`34359738369` final
product: `2^k - 2^(k-70)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 70)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 70) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 70)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`34359738369`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-70)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 70)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        131072 * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 70)) (17 : ℤ)
    have hexp : ((k : ℤ) - 70) + 17 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 70)) * (2 : ℝ) ^ (17 : ℤ) := h
      _ = 131072 * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 131071 * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`34359738369`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`68719476737`
final product: `2^k - 2^(k-72)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 72)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 72) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 72)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`68719476737`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-72)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 72)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        524288 * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 72)) (19 : ℤ)
    have hexp : ((k : ℤ) - 72) + 19 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 72)) * (2 : ℝ) ^ (19 : ℤ) := h
      _ = 524288 * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 524287 * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`68719476737`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`137438953473`
final product: `2^k - 2^(k-74)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 74)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 74) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 74)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`137438953473`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-74)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 74)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        2097152 * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 74)) (21 : ℤ)
    have hexp : ((k : ℤ) - 74) + 21 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 74)) * (2 : ℝ) ^ (21 : ℤ) := h
      _ = 2097152 * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 2097151 * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`137438953473`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`274877906945`
final product: `2^k - 2^(k-76)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 76)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 76) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 76)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`274877906945`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-76)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 76)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        8388608 * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 76)) (23 : ℤ)
    have hexp : ((k : ℤ) - 76) + 23 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 76)) * (2 : ℝ) ^ (23 : ℤ) := h
      _ = 8388608 * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 8388607 * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`274877906945`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`549755813889`
final product: `2^k - 2^(k-78)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 78)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 78) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 78)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`549755813889`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-78)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 78)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        33554432 * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 78)) (25 : ℤ)
    have hexp : ((k : ℤ) - 78) + 25 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 78)) * (2 : ℝ) ^ (25 : ℤ) := h
      _ = 33554432 * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 33554431 * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`549755813889`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`1099511627777`
final product: `2^k - 2^(k-80)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 80)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 80) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 80)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`1099511627777`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-80)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 80)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        134217728 * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 80)) (27 : ℤ)
    have hexp : ((k : ℤ) - 80) + 27 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 80)) * (2 : ℝ) ^ (27 : ℤ) := h
      _ = 134217728 * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 134217727 * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`1099511627777`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`2199023255553`
final product: `2^k - 2^(k-82)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 82)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 82) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 82)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`2199023255553`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-82)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 82)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        536870912 * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 82)) (29 : ℤ)
    have hexp : ((k : ℤ) - 82) + 29 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 82)) * (2 : ℝ) ^ (29 : ℤ) := h
      _ = 536870912 * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 536870911 * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`2199023255553`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`4398046511105`
final product: `2^k - 2^(k-84)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_two_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 84)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 84) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 84)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`4398046511105`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-84)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_two_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 84)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        2147483648 * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 84)) (31 : ℤ)
    have hexp : ((k : ℤ) - 84) + 31 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 84)) * (2 : ℝ) ^ (31 : ℤ) := h
      _ = 2147483648 * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 2147483647 * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_two_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`4398046511105`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_two_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_two_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`8796093022209`
final product: `2^k - 2^(k-86)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_four_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 86)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 86) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 86)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`8796093022209`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-86)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_four_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 86)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        8589934592 * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 86)) (33 : ℤ)
    have hexp : ((k : ℤ) - 86) + 33 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 86)) * (2 : ℝ) ^ (33 : ℤ) := h
      _ = 8589934592 * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 8589934591 * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_four_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`8796093022209`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_four_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_four_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`17592186044417`
final product: `2^k - 2^(k-88)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_six_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 88)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 88) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 88)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`17592186044417`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-88)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_six_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 88)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        34359738368 * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 88)) (35 : ℤ)
    have hexp : ((k : ℤ) - 88) + 35 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 88)) * (2 : ℝ) ^ (35 : ℤ) := h
      _ = 34359738368 * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 34359738367 * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_six_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`17592186044417`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_six_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_six_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`35184372088833`
final product: `2^k - 2^(k-90)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_eight_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 90)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 90) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 90)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`35184372088833`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-90)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_eight_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 90)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        137438953472 * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 90)) (37 : ℤ)
    have hexp : ((k : ℤ) - 90) + 37 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 90)) * (2 : ℝ) ^ (37 : ℤ) := h
      _ = 137438953472 * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 137438953471 * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_eight_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`35184372088833`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_eight_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_eight_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`70368744177665`
final product: `2^k - 2^(k-92)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 92)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 92) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 92)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`70368744177665`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-92)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 92)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        549755813888 * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 92)) (39 : ℤ)
    have hexp : ((k : ℤ) - 92) + 39 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 92)) * (2 : ℝ) ^ (39 : ℤ) := h
      _ = 549755813888 * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 549755813887 * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`70368744177665`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`140737488355329`
final product: `2^k - 2^(k-94)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_two_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 94)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 94) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 94)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`140737488355329`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-94)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_two_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 94)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        2199023255552 * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 94)) (41 : ℤ)
    have hexp : ((k : ℤ) - 94) + 41 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 94)) * (2 : ℝ) ^ (41 : ℤ) := h
      _ = 2199023255552 * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 2199023255551 * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_two_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`140737488355329`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_two_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_two_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`281474976710657`
final product: `2^k - 2^(k-96)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_four_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 96)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 96) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 96)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`281474976710657`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-96)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_four_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 96)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        8796093022208 * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 96)) (43 : ℤ)
    have hexp : ((k : ℤ) - 96) + 43 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 96)) * (2 : ℝ) ^ (43 : ℤ) := h
      _ = 8796093022208 * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 8796093022207 * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_four_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`281474976710657`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_four_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_four_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`562949953421313`
final product: `2^k - 2^(k-98)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_six_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 98)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 98) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 98)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`562949953421313`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-98)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_six_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 98)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        35184372088832 * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 98)) (45 : ℤ)
    have hexp : ((k : ℤ) - 98) + 45 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 98)) * (2 : ℝ) ^ (45 : ℤ) := h
      _ = 35184372088832 * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 35184372088831 * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_six_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`562949953421313`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_six_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_six_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`1125899906842625`
final product: `2^k - 2^(k-100)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_eight_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 100)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 100) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 100)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`1125899906842625`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-100)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_eight_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 100)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        140737488355328 * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 100)) (47 : ℤ)
    have hexp : ((k : ℤ) - 100) + 47 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 100)) * (2 : ℝ) ^ (47 : ℤ) := h
      _ = 140737488355328 * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 140737488355327 * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_eight_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`1125899906842625`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_eight_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_eight_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`2251799813685249`
final product: `2^k - 2^(k-102)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 102)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 102) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 102)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`2251799813685249`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-102)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 102)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        562949953421312 * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 102)) (49 : ℤ)
    have hexp : ((k : ℤ) - 102) + 49 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 102)) * (2 : ℝ) ^ (49 : ℤ) := h
      _ = 562949953421312 * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 562949953421311 * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`2251799813685249`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_fifty_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`4503599627370497`
final product: `2^k - 2^(k-104)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_two_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 104)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 104) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 104)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`4503599627370497`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-104)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_two_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 104)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        2251799813685248 * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 104)) (51 : ℤ)
    have hexp : ((k : ℤ) - 104) + 51 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 104)) * (2 : ℝ) ^ (51 : ℤ) := h
      _ = 2251799813685248 * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 2251799813685247 * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_two_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`4503599627370497`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_fifty_two_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_two_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`9007199254740993`
final product: `2^k - 2^(k-106)` stays in the finite normal range. -/
private theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_four_ulp_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange
      ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106))) := by
  rw [finiteNormalRange]
  have hmid := problem2_10_two_pow_midpoint_lower_bound (k := k)
  have htail_le_mid :
      (2 : ℝ) ^ (((k : ℤ) - 106)) ≤
        (2 : ℝ) ^ (((k : ℤ) - 54)) := by
    exact zpow_le_zpow_right₀ (by norm_num : (1 : ℝ) ≤ 2)
      (by omega : ((k : ℤ) - 106) ≤ (k : ℤ) - 54)
  have hx_ge_mid :
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) ≤
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    linarith
  have hxnonneg :
      0 ≤ (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    linarith
  rw [abs_of_nonneg hxnonneg]
  constructor
  · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
      (le_trans hmid hx_ge_mid)
  · have hsub_nonneg : 0 ≤ (2 : ℝ) ^ (((k : ℤ) - 106)) :=
      le_of_lt (zpow_pos (by norm_num : (0 : ℝ) < 2) _)
    have hxle :
        (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106)) ≤ (2 : ℝ) ^ k := by
      linarith
    exact le_trans hxle
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`9007199254740993`, power-of-two final rounding fact for
Problem 2.10: the exact product after rounded division, `2^k - 2^(k-106)`,
rounds back to `2^k` because it is strictly closer to the power-of-two
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_four_ulp_rounds_to_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106))) =
      (2 : ℝ) ^ k := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa (k : ℤ)
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa (((k : ℤ) + 1))
  let x : ℝ := (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106))
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (k : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 53)) := by
    simpa [a, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_left_value (k := k)
  have hb_value : b = (2 : ℝ) ^ k := by
    simpa [b, fmt] using
      problem2_10_ieeeDouble_two_pow_boundary_right_value (k := k)
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 106)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hpow53_to_tail :
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
        9007199254740992 * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 106)) (53 : ℤ)
    have hexp : ((k : ℤ) - 106) + 53 = (k : ℤ) - 53 := by ring
    rw [hexp] at h
    calc
      (2 : ℝ) ^ (((k : ℤ) - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 106)) * (2 : ℝ) ^ (53 : ℤ) := h
      _ = 9007199254740992 * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
        norm_num [zpow_natCast]
        ring
  have hleft_dist : x - a = 9007199254740991 * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    simp [x, ha_value, hpow53_to_tail]
    ring
  have hright_dist : b - x = (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    simp [x, hb_value]
  have hleft_pos : 0 < x - a := by
    rw [hleft_dist]
    nlinarith [hzpos]
  have hright_pos : 0 < b - x := by
    rw [hright_dist]
    exact hzpos
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_four_ulp_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Signed final rounding fact for the denominator-`9007199254740993`,
power-of-two subfamily. -/
theorem problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_fifty_four_ulp_rounds_to_neg_two_pow
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven
        (-((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106)))) =
      -((2 : ℝ) ^ k) := by
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_four_ulp_rounds_to_two_pow hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Finite-normal range side condition for the denominator-`131073`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onehundredthirtyonethousandseventythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 131073) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 131073 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredthirtyonethousandseventythird
      (by linarith : (1 / 131073 : ℝ) ≤ ((2 : ℝ) ^ k) / 131073)
  · have hdiv_le : ((2 : ℝ) ^ k) / 131073 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude hk)

/-- Generic denominator-`131073` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/131073` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredthirtyonethousandseventythirds_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 131073) =
      (9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007130535788540 (((k : ℤ) - 17))
  let b : ℝ :=
    fmt.normalizedValue false (9007130535788540 + 1) (((k : ℤ) - 17))
  let x : ℝ := ((2 : ℝ) ^ k) / 131073
  have hm : fmt.normalizedMantissa 9007130535788540 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007130535788540 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007130535788540, (((k : ℤ) - 17)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007130535788541 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 70)) * (2 : ℝ) ^ (70 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 70)) (70 : ℤ)
    have hexp : ((k : ℤ) - 70) + 70 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4 * ((2 : ℝ) ^ (((k : ℤ) - 70)))) / 131073 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (131069 * ((2 : ℝ) ^ (((k : ℤ) - 70)))) / 131073 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 70)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4 * ((2 : ℝ) ^ (((k : ℤ) - 70)))) / 131073 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (131069 * ((2 : ℝ) ^ (((k : ℤ) - 70)))) / 131073 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onehundredthirtyonethousandseventythree_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`131073`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredthirtyonethousandseventythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 131073 =
      (9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 131073) =
    (9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70))
  exact problem2_10_ieeeDouble_two_pow_onehundredthirtyonethousandseventythirds_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`131073`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredthirtyonethousandseventythree_exact_mul_onehundredthirtyonethousandseventythree_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 131073) *
        131073 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredthirtyonethousandseventythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onehundredthirtyonethousandseventythirds_mul_onehundredthirtyonethousandseventythree_below

/-- Positive denominator-`131073`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredthirtyonethousandseventythirds_times_onehundredthirtyonethousandseventythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 131073)
        131073 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredthirtyonethousandseventythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70))) * 131073) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onehundredthirtyonethousandseventythirds_mul_onehundredthirtyonethousandseventythree_below]
  exact problem2_10_ieeeDouble_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`131073` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyonethousandseventythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 131073 =
      -((9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 131073) =
    -((9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70)))
  have harg : (-((2 : ℝ) ^ k)) / 131073 = -(((2 : ℝ) ^ k) / 131073) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_onehundredthirtyonethousandseventythirds_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyonethousandseventythree_exact_mul_onehundredthirtyonethousandseventythree_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 131073) *
        131073 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyonethousandseventythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredthirtyonethousandseventythirds_mul_onehundredthirtyonethousandseventythree_below

/-- Negative denominator-`131073`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onehundredthirtyonethousandseventythirds_times_onehundredthirtyonethousandseventythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 131073)
        131073 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyonethousandseventythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007130535788540 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 70)))) * 131073) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredthirtyonethousandseventythirds_mul_onehundredthirtyonethousandseventythree_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`131073`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onehundredthirtyonethousandseventythirds_times_onehundredthirtyonethousandseventythree
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 131073)
          131073 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 131073)
          131073 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onehundredthirtyonethousandseventythirds_times_onehundredthirtyonethousandseventythree hk,
    problem2_10_ieeeDouble_neg_two_pow_onehundredthirtyonethousandseventythirds_times_onehundredthirtyonethousandseventythree hk⟩

/-- Finite-normal range side condition for the denominator-`262145`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twohundredsixtytwothousandonehundredfortyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 262145) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 262145 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredsixtytwothousandonehundredfortyfifth
      (by linarith : (1 / 262145 : ℝ) ≤ ((2 : ℝ) ^ k) / 262145)
  · have hdiv_le : ((2 : ℝ) ^ k) / 262145 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`262145` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/262145` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredsixtytwothousandonehundredfortyfifths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 262145) =
      (9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007164895133695 (((k : ℤ) - 18))
  let b : ℝ :=
    fmt.normalizedValue false (9007164895133695 + 1) (((k : ℤ) - 18))
  let x : ℝ := ((2 : ℝ) ^ k) / 262145
  have hm : fmt.normalizedMantissa 9007164895133695 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007164895133695 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007164895133695, (((k : ℤ) - 18)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007164895133695 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 71)) * (2 : ℝ) ^ (71 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 71)) (71 : ℤ)
    have hexp : ((k : ℤ) - 71) + 71 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (131073 * ((2 : ℝ) ^ (((k : ℤ) - 71)))) / 262145 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (131072 * ((2 : ℝ) ^ (((k : ℤ) - 71)))) / 262145 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 71)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (131073 * ((2 : ℝ) ^ (((k : ℤ) - 71)))) / 262145 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (131072 * ((2 : ℝ) ^ (((k : ℤ) - 71)))) / 262145 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twohundredsixtytwothousandonehundredfortyfive_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`262145`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredsixtytwothousandonehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 262145 =
      (9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 262145) =
    (9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71))
  exact problem2_10_ieeeDouble_two_pow_twohundredsixtytwothousandonehundredfortyfifths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`262145`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredsixtytwothousandonehundredfortyfive_exact_mul_twohundredsixtytwothousandonehundredfortyfive_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 262145) *
        262145 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredsixtytwothousandonehundredfortyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twohundredsixtytwothousandonehundredfortyfifths_mul_twohundredsixtytwothousandonehundredfortyfive_above

/-- Positive denominator-`262145`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredsixtytwothousandonehundredfortyfifths_times_twohundredsixtytwothousandonehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 262145)
        262145 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredsixtytwothousandonehundredfortyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71))) * 262145) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twohundredsixtytwothousandonehundredfortyfifths_mul_twohundredsixtytwothousandonehundredfortyfive_above]
  exact problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`262145` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtytwothousandonehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 262145 =
      -((9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 262145) =
    -((9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71)))
  have harg : (-((2 : ℝ) ^ k)) / 262145 = -(((2 : ℝ) ^ k) / 262145) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_twohundredsixtytwothousandonehundredfortyfifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtytwothousandonehundredfortyfive_exact_mul_twohundredsixtytwothousandonehundredfortyfive_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 262145) *
        262145 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtytwothousandonehundredfortyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredsixtytwothousandonehundredfortyfifths_mul_twohundredsixtytwothousandonehundredfortyfive_above

/-- Negative denominator-`262145`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twohundredsixtytwothousandonehundredfortyfifths_times_twohundredsixtytwothousandonehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 262145)
        262145 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtytwothousandonehundredfortyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007164895133696 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 71)))) * 262145) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredsixtytwothousandonehundredfortyfifths_mul_twohundredsixtytwothousandonehundredfortyfive_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`262145`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twohundredsixtytwothousandonehundredfortyfifths_times_twohundredsixtytwothousandonehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 262145)
          262145 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 262145)
          262145 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twohundredsixtytwothousandonehundredfortyfifths_times_twohundredsixtytwothousandonehundredfortyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_twohundredsixtytwothousandonehundredfortyfifths_times_twohundredsixtytwothousandonehundredfortyfive hk⟩

/-- Finite-normal range side condition for the denominator-`524289`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fivehundredtwentyfourthousandtwohundredeightynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 524289) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 524289 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredtwentyfourthousandtwohundredeightyninth
      (by linarith : (1 / 524289 : ℝ) ≤ ((2 : ℝ) ^ k) / 524289)
  · have hdiv_le : ((2 : ℝ) ^ k) / 524289 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`524289` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/524289` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 524289) =
      (9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007182074904575 (((k : ℤ) - 19))
  let b : ℝ :=
    fmt.normalizedValue false (9007182074904575 + 1) (((k : ℤ) - 19))
  let x : ℝ := ((2 : ℝ) ^ k) / 524289
  have hm : fmt.normalizedMantissa 9007182074904575 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007182074904575 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007182074904575, (((k : ℤ) - 19)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007182074904575 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 72)) * (2 : ℝ) ^ (72 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 72)) (72 : ℤ)
    have hexp : ((k : ℤ) - 72) + 72 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (491521 * ((2 : ℝ) ^ (((k : ℤ) - 72)))) / 524289 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (32768 * ((2 : ℝ) ^ (((k : ℤ) - 72)))) / 524289 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 72)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (491521 * ((2 : ℝ) ^ (((k : ℤ) - 72)))) / 524289 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (32768 * ((2 : ℝ) ^ (((k : ℤ) - 72)))) / 524289 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fivehundredtwentyfourthousandtwohundredeightynine_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`524289`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredtwentyfourthousandtwohundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 524289 =
      (9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 524289) =
    (9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72))
  exact problem2_10_ieeeDouble_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`524289`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredtwentyfourthousandtwohundredeightynine_exact_mul_fivehundredtwentyfourthousandtwohundredeightynine_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 524289) *
        524289 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredtwentyfourthousandtwohundredeightynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_mul_fivehundredtwentyfourthousandtwohundredeightynine_above

/-- Positive denominator-`524289`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_times_fivehundredtwentyfourthousandtwohundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 524289)
        524289 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredtwentyfourthousandtwohundredeightynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72))) * 524289) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_mul_fivehundredtwentyfourthousandtwohundredeightynine_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_thirtysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`524289` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredtwentyfourthousandtwohundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 524289 =
      -((9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 524289) =
    -((9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72)))
  have harg : (-((2 : ℝ) ^ k)) / 524289 = -(((2 : ℝ) ^ k) / 524289) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredtwentyfourthousandtwohundredeightynine_exact_mul_fivehundredtwentyfourthousandtwohundredeightynine_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 524289) *
        524289 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredtwentyfourthousandtwohundredeightynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_mul_fivehundredtwentyfourthousandtwohundredeightynine_above

/-- Negative denominator-`524289`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_times_fivehundredtwentyfourthousandtwohundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 524289)
        524289 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredtwentyfourthousandtwohundredeightynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007182074904576 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 72)))) * 524289) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_mul_fivehundredtwentyfourthousandtwohundredeightynine_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`524289`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_times_fivehundredtwentyfourthousandtwohundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 524289)
          524289 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 524289)
          524289 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_times_fivehundredtwentyfourthousandtwohundredeightynine hk,
    problem2_10_ieeeDouble_neg_two_pow_fivehundredtwentyfourthousandtwohundredeightyninths_times_fivehundredtwentyfourthousandtwohundredeightynine hk⟩

/-- Finite-normal range side condition for the denominator-`1048577`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onemillionfortyeightthousandfivehundredseventyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1048577) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1048577 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onemillionfortyeightthousandfivehundredseventyseventh
      (by linarith : (1 / 1048577 : ℝ) ≤ ((2 : ℝ) ^ k) / 1048577)
  · have hdiv_le : ((2 : ℝ) ^ k) / 1048577 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`1048577` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/1048577` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1048577) =
      (9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007190664814591 (((k : ℤ) - 20))
  let b : ℝ :=
    fmt.normalizedValue false (9007190664814591 + 1) (((k : ℤ) - 20))
  let x : ℝ := ((2 : ℝ) ^ k) / 1048577
  have hm : fmt.normalizedMantissa 9007190664814591 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007190664814591 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007190664814591, (((k : ℤ) - 20)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007190664814591 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 73)) * (2 : ℝ) ^ (73 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 73)) (73 : ℤ)
    have hexp : ((k : ℤ) - 73) + 73 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (1040385 * ((2 : ℝ) ^ (((k : ℤ) - 73)))) / 1048577 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (8192 * ((2 : ℝ) ^ (((k : ℤ) - 73)))) / 1048577 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 73)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (1040385 * ((2 : ℝ) ^ (((k : ℤ) - 73)))) / 1048577 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (8192 * ((2 : ℝ) ^ (((k : ℤ) - 73)))) / 1048577 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onemillionfortyeightthousandfivehundredseventyseven_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`1048577`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onemillionfortyeightthousandfivehundredseventyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1048577 =
      (9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1048577) =
    (9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73))
  exact problem2_10_ieeeDouble_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`1048577`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onemillionfortyeightthousandfivehundredseventyseven_exact_mul_onemillionfortyeightthousandfivehundredseventyseven_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1048577) *
        1048577 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onemillionfortyeightthousandfivehundredseventyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_mul_onemillionfortyeightthousandfivehundredseventyseven_above

/-- Positive denominator-`1048577`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_times_onemillionfortyeightthousandfivehundredseventyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1048577)
        1048577 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onemillionfortyeightthousandfivehundredseventyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73))) * 1048577) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_mul_onemillionfortyeightthousandfivehundredseventyseven_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`1048577` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onemillionfortyeightthousandfivehundredseventyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1048577 =
      -((9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1048577) =
    -((9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73)))
  have harg : (-((2 : ℝ) ^ k)) / 1048577 = -(((2 : ℝ) ^ k) / 1048577) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_onemillionfortyeightthousandfivehundredseventyseven_exact_mul_onemillionfortyeightthousandfivehundredseventyseven_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1048577) *
        1048577 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onemillionfortyeightthousandfivehundredseventyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_mul_onemillionfortyeightthousandfivehundredseventyseven_above

/-- Negative denominator-`1048577`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_times_onemillionfortyeightthousandfivehundredseventyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1048577)
        1048577 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onemillionfortyeightthousandfivehundredseventyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007190664814592 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 73)))) * 1048577) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_mul_onemillionfortyeightthousandfivehundredseventyseven_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`1048577`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_times_onemillionfortyeightthousandfivehundredseventyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1048577)
          1048577 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1048577)
          1048577 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_times_onemillionfortyeightthousandfivehundredseventyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_onemillionfortyeightthousandfivehundredseventysevenths_times_onemillionfortyeightthousandfivehundredseventyseven hk⟩

/-- Finite-normal range side condition for the denominator-`2097153`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twomillionninetyseventhousandonehundredfiftythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2097153) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2097153 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twomillionninetyseventhousandonehundredfiftythird
      (by linarith : (1 / 2097153 : ℝ) ≤ ((2 : ℝ) ^ k) / 2097153)
  · have hdiv_le : ((2 : ℝ) ^ k) / 2097153 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`2097153` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/2097153` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twomillionninetyseventhousandonehundredfiftythirds_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2097153) =
      (9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007194959775743 (((k : ℤ) - 21))
  let b : ℝ :=
    fmt.normalizedValue false (9007194959775743 + 1) (((k : ℤ) - 21))
  let x : ℝ := ((2 : ℝ) ^ k) / 2097153
  have hm : fmt.normalizedMantissa 9007194959775743 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007194959775743 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007194959775743, (((k : ℤ) - 21)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007194959775743 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 74)) * (2 : ℝ) ^ (74 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 74)) (74 : ℤ)
    have hexp : ((k : ℤ) - 74) + 74 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (2095105 * ((2 : ℝ) ^ (((k : ℤ) - 74)))) / 2097153 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2048 * ((2 : ℝ) ^ (((k : ℤ) - 74)))) / 2097153 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 74)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (2095105 * ((2 : ℝ) ^ (((k : ℤ) - 74)))) / 2097153 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2048 * ((2 : ℝ) ^ (((k : ℤ) - 74)))) / 2097153 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twomillionninetyseventhousandonehundredfiftythree_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`2097153`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twomillionninetyseventhousandonehundredfiftythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2097153 =
      (9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2097153) =
    (9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74))
  exact problem2_10_ieeeDouble_two_pow_twomillionninetyseventhousandonehundredfiftythirds_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`2097153`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twomillionninetyseventhousandonehundredfiftythree_exact_mul_twomillionninetyseventhousandonehundredfiftythree_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2097153) *
        2097153 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twomillionninetyseventhousandonehundredfiftythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twomillionninetyseventhousandonehundredfiftythirds_mul_twomillionninetyseventhousandonehundredfiftythree_above

/-- Positive denominator-`2097153`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twomillionninetyseventhousandonehundredfiftythirds_times_twomillionninetyseventhousandonehundredfiftythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2097153)
        2097153 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twomillionninetyseventhousandonehundredfiftythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74))) * 2097153) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twomillionninetyseventhousandonehundredfiftythirds_mul_twomillionninetyseventhousandonehundredfiftythree_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`2097153` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twomillionninetyseventhousandonehundredfiftythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2097153 =
      -((9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2097153) =
    -((9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74)))
  have harg : (-((2 : ℝ) ^ k)) / 2097153 = -(((2 : ℝ) ^ k) / 2097153) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_twomillionninetyseventhousandonehundredfiftythirds_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_twomillionninetyseventhousandonehundredfiftythree_exact_mul_twomillionninetyseventhousandonehundredfiftythree_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2097153) *
        2097153 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twomillionninetyseventhousandonehundredfiftythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twomillionninetyseventhousandonehundredfiftythirds_mul_twomillionninetyseventhousandonehundredfiftythree_above

/-- Negative denominator-`2097153`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twomillionninetyseventhousandonehundredfiftythirds_times_twomillionninetyseventhousandonehundredfiftythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2097153)
        2097153 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twomillionninetyseventhousandonehundredfiftythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007194959775744 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 74)))) * 2097153) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twomillionninetyseventhousandonehundredfiftythirds_mul_twomillionninetyseventhousandonehundredfiftythree_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`2097153`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twomillionninetyseventhousandonehundredfiftythirds_times_twomillionninetyseventhousandonehundredfiftythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2097153)
          2097153 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2097153)
          2097153 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twomillionninetyseventhousandonehundredfiftythirds_times_twomillionninetyseventhousandonehundredfiftythree hk,
    problem2_10_ieeeDouble_neg_two_pow_twomillionninetyseventhousandonehundredfiftythirds_times_twomillionninetyseventhousandonehundredfiftythree hk⟩

/-- Finite-normal range side condition for the denominator-`4194305`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fourmilliononehundredninetyfourthousandthreehundredfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4194305) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4194305 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fourmilliononehundredninetyfourthousandthreehundredfifth
      (by linarith : (1 / 4194305 : ℝ) ≤ ((2 : ℝ) ^ k) / 4194305)
  · have hdiv_le : ((2 : ℝ) ^ k) / 4194305 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`4194305` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/4194305` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4194305) =
      (9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007197107257855 (((k : ℤ) - 22))
  let b : ℝ :=
    fmt.normalizedValue false (9007197107257855 + 1) (((k : ℤ) - 22))
  let x : ℝ := ((2 : ℝ) ^ k) / 4194305
  have hm : fmt.normalizedMantissa 9007197107257855 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007197107257855 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007197107257855, (((k : ℤ) - 22)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007197107257855 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 75)) * (2 : ℝ) ^ (75 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 75)) (75 : ℤ)
    have hexp : ((k : ℤ) - 75) + 75 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4193793 * ((2 : ℝ) ^ (((k : ℤ) - 75)))) / 4194305 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (512 * ((2 : ℝ) ^ (((k : ℤ) - 75)))) / 4194305 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 75)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4193793 * ((2 : ℝ) ^ (((k : ℤ) - 75)))) / 4194305 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (512 * ((2 : ℝ) ^ (((k : ℤ) - 75)))) / 4194305 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fourmilliononehundredninetyfourthousandthreehundredfive_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`4194305`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4194305 =
      (9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4194305) =
    (9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75))
  exact
    problem2_10_ieeeDouble_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`4194305`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive_exact_mul_fourmilliononehundredninetyfourthousandthreehundredfive_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4194305) *
        4194305 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_mul_fourmilliononehundredninetyfourthousandthreehundredfive_above

/-- Positive denominator-`4194305`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_times_fourmilliononehundredninetyfourthousandthreehundredfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4194305)
        4194305 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75))) * 4194305) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_mul_fourmilliononehundredninetyfourthousandthreehundredfive_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`4194305` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4194305 =
      -((9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4194305) =
    -((9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75)))
  have harg : (-((2 : ℝ) ^ k)) / 4194305 = -(((2 : ℝ) ^ k) / 4194305) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive_exact_mul_fourmilliononehundredninetyfourthousandthreehundredfive_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4194305) *
        4194305 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_mul_fourmilliononehundredninetyfourthousandthreehundredfive_above

/-- Negative denominator-`4194305`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_times_fourmilliononehundredninetyfourthousandthreehundredfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4194305)
        4194305 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fourmilliononehundredninetyfourthousandthreehundredfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007197107257856 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 75)))) * 4194305) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_mul_fourmilliononehundredninetyfourthousandthreehundredfive_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`4194305`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_times_fourmilliononehundredninetyfourthousandthreehundredfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4194305)
          4194305 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4194305)
          4194305 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_times_fourmilliononehundredninetyfourthousandthreehundredfive hk,
    problem2_10_ieeeDouble_neg_two_pow_fourmilliononehundredninetyfourthousandthreehundredfifths_times_fourmilliononehundredninetyfourthousandthreehundredfive hk⟩

/-- Finite-normal range side condition for the denominator-`8388609`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_eightmillionthreehundredeightyeightthousandsixhundrednine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 8388609) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 8388609 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_eightmillionthreehundredeightyeightthousandsixhundredninth
      (by linarith : (1 / 8388609 : ℝ) ≤ ((2 : ℝ) ^ k) / 8388609)
  · have hdiv_le : ((2 : ℝ) ^ k) / 8388609 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`8388609` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/8388609` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8388609) =
      (9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007198180999295 (((k : ℤ) - 23))
  let b : ℝ :=
    fmt.normalizedValue false (9007198180999295 + 1) (((k : ℤ) - 23))
  let x : ℝ := ((2 : ℝ) ^ k) / 8388609
  have hm : fmt.normalizedMantissa 9007198180999295 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007198180999295 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007198180999295, (((k : ℤ) - 23)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007198180999295 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 76)) * (2 : ℝ) ^ (76 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 76)) (76 : ℤ)
    have hexp : ((k : ℤ) - 76) + 76 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (8388481 * ((2 : ℝ) ^ (((k : ℤ) - 76)))) / 8388609 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (128 * ((2 : ℝ) ^ (((k : ℤ) - 76)))) / 8388609 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 76)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (8388481 * ((2 : ℝ) ^ (((k : ℤ) - 76)))) / 8388609 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (128 * ((2 : ℝ) ^ (((k : ℤ) - 76)))) / 8388609 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_eightmillionthreehundredeightyeightthousandsixhundrednine_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`8388609`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8388609 =
      (9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8388609) =
    (9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76))
  exact
    problem2_10_ieeeDouble_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`8388609`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine_exact_mul_eightmillionthreehundredeightyeightthousandsixhundrednine_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8388609) *
        8388609 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_mul_eightmillionthreehundredeightyeightthousandsixhundrednine_above

/-- Positive denominator-`8388609`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_times_eightmillionthreehundredeightyeightthousandsixhundrednine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8388609)
        8388609 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76))) * 8388609) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_mul_eightmillionthreehundredeightyeightthousandsixhundrednine_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`8388609` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8388609 =
      -((9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 8388609) =
    -((9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76)))
  have harg : (-((2 : ℝ) ^ k)) / 8388609 = -(((2 : ℝ) ^ k) / 8388609) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine_exact_mul_eightmillionthreehundredeightyeightthousandsixhundrednine_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8388609) *
        8388609 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_mul_eightmillionthreehundredeightyeightthousandsixhundrednine_above

/-- Negative denominator-`8388609`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_times_eightmillionthreehundredeightyeightthousandsixhundrednine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8388609)
        8388609 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_eightmillionthreehundredeightyeightthousandsixhundrednine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007198180999296 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 76)))) * 8388609) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_mul_eightmillionthreehundredeightyeightthousandsixhundrednine_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`8388609`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_times_eightmillionthreehundredeightyeightthousandsixhundrednine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8388609)
          8388609 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8388609)
          8388609 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_times_eightmillionthreehundredeightyeightthousandsixhundrednine hk,
    problem2_10_ieeeDouble_neg_two_pow_eightmillionthreehundredeightyeightthousandsixhundredninths_times_eightmillionthreehundredeightyeightthousandsixhundrednine hk⟩

/-- Finite-normal range side condition for the denominator-`16777217`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 16777217) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 16777217 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenth
      (by linarith : (1 / 16777217 : ℝ) ≤ ((2 : ℝ) ^ k) / 16777217)
  · have hdiv_le : ((2 : ℝ) ^ k) / 16777217 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`16777217` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/16777217` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 16777217) =
      (9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007198717870111 (((k : ℤ) - 24))
  let b : ℝ :=
    fmt.normalizedValue false (9007198717870111 + 1) (((k : ℤ) - 24))
  let x : ℝ := ((2 : ℝ) ^ k) / 16777217
  have hm : fmt.normalizedMantissa 9007198717870111 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007198717870111 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007198717870111, (((k : ℤ) - 24)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007198717870111 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 77)) * (2 : ℝ) ^ (77 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 77)) (77 : ℤ)
    have hexp : ((k : ℤ) - 77) + 77 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (16777185 * ((2 : ℝ) ^ (((k : ℤ) - 77)))) / 16777217 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (32 * ((2 : ℝ) ^ (((k : ℤ) - 77)))) / 16777217 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 77)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (16777185 * ((2 : ℝ) ^ (((k : ℤ) - 77)))) / 16777217 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (32 * ((2 : ℝ) ^ (((k : ℤ) - 77)))) / 16777217 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`16777217`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16777217 =
      (9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 16777217) =
    (9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77))
  exact
    problem2_10_ieeeDouble_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`16777217`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_exact_mul_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16777217) *
        16777217 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_mul_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_above

/-- Positive denominator-`16777217`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_times_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16777217)
        16777217 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77))) * 16777217) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_mul_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`16777217` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16777217 =
      -((9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 16777217) =
    -((9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77)))
  have harg : (-((2 : ℝ) ^ k)) / 16777217 = -(((2 : ℝ) ^ k) / 16777217) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_exact_mul_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16777217) *
        16777217 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_mul_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_above

/-- Negative denominator-`16777217`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_times_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16777217)
        16777217 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007198717870112 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 77)))) * 16777217) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_mul_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`16777217`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_times_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 16777217)
          16777217 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 16777217)
          16777217 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_times_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen hk,
    problem2_10_ieeeDouble_neg_two_pow_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeenths_times_sixteenmillionsevenhundredseventyseventhousandtwohundredseventeen hk⟩

/-- Finite-normal range side condition for the denominator-`33554433`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 33554433) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 33554433 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythird
      (by linarith : (1 / 33554433 : ℝ) ≤ ((2 : ℝ) ^ k) / 33554433)
  · have hdiv_le : ((2 : ℝ) ^ k) / 33554433 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`33554433` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/33554433` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 33554433) =
      (9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007198986305543 (((k : ℤ) - 25))
  let b : ℝ :=
    fmt.normalizedValue false (9007198986305543 + 1) (((k : ℤ) - 25))
  let x : ℝ := ((2 : ℝ) ^ k) / 33554433
  have hm : fmt.normalizedMantissa 9007198986305543 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007198986305543 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007198986305543, (((k : ℤ) - 25)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007198986305543 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 78)) * (2 : ℝ) ^ (78 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 78)) (78 : ℤ)
    have hexp : ((k : ℤ) - 78) + 78 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (33554425 * ((2 : ℝ) ^ (((k : ℤ) - 78)))) / 33554433 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (8 * ((2 : ℝ) ^ (((k : ℤ) - 78)))) / 33554433 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 78)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (33554425 * ((2 : ℝ) ^ (((k : ℤ) - 78)))) / 33554433 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (8 * ((2 : ℝ) ^ (((k : ℤ) - 78)))) / 33554433 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`33554433`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33554433 =
      (9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 33554433) =
    (9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78))
  exact
    problem2_10_ieeeDouble_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`33554433`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_exact_mul_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33554433) *
        33554433 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_mul_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_above

/-- Positive denominator-`33554433`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_times_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33554433)
        33554433 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78))) * 33554433) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_mul_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`33554433` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33554433 =
      -((9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 33554433) =
    -((9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78)))
  have harg : (-((2 : ℝ) ^ k)) / 33554433 = -(((2 : ℝ) ^ k) / 33554433) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_exact_mul_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33554433) *
        33554433 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_mul_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_above

/-- Negative denominator-`33554433`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_times_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33554433)
        33554433 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007198986305544 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 78)))) * 33554433) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_mul_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`33554433`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_times_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 33554433)
          33554433 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 33554433)
          33554433 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_times_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree hk,
    problem2_10_ieeeDouble_neg_two_pow_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythirds_times_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtythree hk⟩

/-- Finite-normal range side condition for the denominator-`67108865`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 67108865) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 67108865 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifth
      (by linarith : (1 / 67108865 : ℝ) ≤ ((2 : ℝ) ^ k) / 67108865)
  · have hdiv_le : ((2 : ℝ) ^ k) / 67108865 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`67108865` first-operation fact for the power-of-two
subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double round-to-even
rounds `(2^k)/67108865` to the upper adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 67108865) =
      (9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 9007199120523265 (((k : ℤ) - 26))
  let b : ℝ :=
    fmt.normalizedValue false (9007199120523265 + 1) (((k : ℤ) - 26))
  let x : ℝ := ((2 : ℝ) ^ k) / 67108865
  have hm : fmt.normalizedMantissa 9007199120523265 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199120523265 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199120523265, (((k : ℤ) - 26)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199120523265 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 79)) * (2 : ℝ) ^ (79 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 79)) (79 : ℤ)
    have hexp : ((k : ℤ) - 79) + 79 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (67108863 * ((2 : ℝ) ^ (((k : ℤ) - 79)))) / 67108865 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2 * ((2 : ℝ) ^ (((k : ℤ) - 79)))) / 67108865 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 79)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (67108863 * ((2 : ℝ) ^ (((k : ℤ) - 79)))) / 67108865 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2 * ((2 : ℝ) ^ (((k : ℤ) - 79)))) / 67108865 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`67108865`, power-of-two
Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 67108865 =
      (9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 67108865) =
    (9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79))
  exact
    problem2_10_ieeeDouble_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`67108865`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_exact_mul_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 67108865) *
        67108865 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_mul_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_above

/-- Positive denominator-`67108865`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_times_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 67108865)
        67108865 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79))) * 67108865) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_mul_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`67108865` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 67108865 =
      -((9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 67108865) =
    -((9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79)))
  have harg : (-((2 : ℝ) ^ k)) / 67108865 = -(((2 : ℝ) ^ k) / 67108865) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_exact_mul_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 67108865) *
        67108865 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_mul_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_above

/-- Negative denominator-`67108865`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_times_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 67108865)
        67108865 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199120523266 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 79)))) * 67108865) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_mul_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`67108865`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_times_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 67108865)
          67108865 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 67108865)
          67108865 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_times_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_sixtysevenmilliononehundredeightthousandeighthundredsixtyfifths_times_sixtysevenmilliononehundredeightthousandeighthundredsixtyfive hk⟩

/-- Finite-normal range side condition for the denominator-`134217729`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 134217729) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 134217729 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninth
      (by linarith : (1 / 134217729 : ℝ) ≤ ((2 : ℝ) ^ k) / 134217729)
  · have hdiv_le : ((2 : ℝ) ^ k) / 134217729 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`134217729` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/134217729` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 134217729) =
      (9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199187632128 (((k : ℤ) - 27))
  let b : ℝ :=
    fmt.normalizedValue false (9007199187632128 + 1) (((k : ℤ) - 27))
  let x : ℝ := ((2 : ℝ) ^ k) / 134217729
  have hm : fmt.normalizedMantissa 9007199187632128 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199187632128 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199187632128, (((k : ℤ) - 27)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199187632129 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 80)) * (2 : ℝ) ^ (80 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 80)) (80 : ℤ)
    have hexp : ((k : ℤ) - 80) + 80 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (67108864 * ((2 : ℝ) ^ (((k : ℤ) - 80)))) / 134217729 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (67108865 * ((2 : ℝ) ^ (((k : ℤ) - 80)))) / 134217729 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 80)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (67108864 * ((2 : ℝ) ^ (((k : ℤ) - 80)))) / 134217729 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (67108865 * ((2 : ℝ) ^ (((k : ℤ) - 80)))) / 134217729 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`134217729`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 134217729 =
      (9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 134217729) =
    (9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80))
  exact
    problem2_10_ieeeDouble_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`134217729`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_exact_mul_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_midpoint
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 134217729) *
        134217729 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_mul_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_midpoint

/-- Positive denominator-`134217729`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_times_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 134217729)
        134217729 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80))) * 134217729) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_mul_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_midpoint]
  exact
    problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`134217729` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 134217729 =
      -((9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 134217729) =
    -((9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80)))
  have harg : (-((2 : ℝ) ^ k)) / 134217729 =
      -(((2 : ℝ) ^ k) / 134217729) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second denominator-`134217729`
operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_exact_mul_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_midpoint
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 134217729) *
        134217729 =
      -((2 : ℝ) ^ k) + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_mul_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_midpoint

/-- Negative denominator-`134217729`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_times_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 134217729)
        134217729 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199187632128 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 80)))) * 134217729) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_mul_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`134217729`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_times_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 134217729)
          134217729 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 134217729)
          134217729 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_times_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine hk,
    problem2_10_ieeeDouble_neg_two_pow_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentyninths_times_onehundredthirtyfourmilliontwohundredseventeenthousandsevenhundredtwentynine hk⟩

/-- Finite-normal range side condition for the denominator-`268435457`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 268435457) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 268435457 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseventh
      (by linarith : (1 / 268435457 : ℝ) ≤ ((2 : ℝ) ^ k) / 268435457)
  · have hdiv_le : ((2 : ℝ) ^ k) / 268435457 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`268435457` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/268435457` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 268435457) =
      (9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199221186560 (((k : ℤ) - 28))
  let b : ℝ :=
    fmt.normalizedValue false (9007199221186560 + 1) (((k : ℤ) - 28))
  let x : ℝ := ((2 : ℝ) ^ k) / 268435457
  have hm : fmt.normalizedMantissa 9007199221186560 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199221186560 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199221186560, (((k : ℤ) - 28)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199221186561 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 81)) * (2 : ℝ) ^ (81 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 81)) (81 : ℤ)
    have hexp : ((k : ℤ) - 81) + 81 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (33554432 * ((2 : ℝ) ^ (((k : ℤ) - 81)))) / 268435457 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (234881025 * ((2 : ℝ) ^ (((k : ℤ) - 81)))) / 268435457 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 81)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (33554432 * ((2 : ℝ) ^ (((k : ℤ) - 81)))) / 268435457 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (234881025 * ((2 : ℝ) ^ (((k : ℤ) - 81)))) / 268435457 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`268435457`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 268435457 =
      (9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 268435457) =
    (9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81))
  exact
    problem2_10_ieeeDouble_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`268435457`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_exact_mul_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 268435457) *
        268435457 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_mul_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_below

/-- Positive denominator-`268435457`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_times_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 268435457)
        268435457 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81))) * 268435457) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_mul_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_sixteenth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`268435457` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 268435457 =
      -((9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 268435457) =
    -((9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81)))
  have harg : (-((2 : ℝ) ^ k)) / 268435457 =
      -(((2 : ℝ) ^ k) / 268435457) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second denominator-`268435457`
operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_exact_mul_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 268435457) *
        268435457 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_mul_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_below

/-- Negative denominator-`268435457`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_times_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 268435457)
        268435457 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199221186560 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 81)))) * 268435457) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_mul_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`268435457`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_times_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 268435457)
          268435457 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 268435457)
          268435457 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_times_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysevenths_times_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftyseven hk⟩

/-- Finite-normal range side condition for the denominator-`536870913`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 536870913) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 536870913 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenth
      (by linarith : (1 / 536870913 : ℝ) ≤ ((2 : ℝ) ^ k) / 536870913)
  · have hdiv_le : ((2 : ℝ) ^ k) / 536870913 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`536870913` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/536870913` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 536870913) =
      (9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199237963776 (((k : ℤ) - 29))
  let b : ℝ :=
    fmt.normalizedValue false (9007199237963776 + 1) (((k : ℤ) - 29))
  let x : ℝ := ((2 : ℝ) ^ k) / 536870913
  have hm : fmt.normalizedMantissa 9007199237963776 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199237963776 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199237963776, (((k : ℤ) - 29)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199237963777 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 82)) * (2 : ℝ) ^ (82 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 82)) (82 : ℤ)
    have hexp : ((k : ℤ) - 82) + 82 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (16777216 * ((2 : ℝ) ^ (((k : ℤ) - 82)))) / 536870913 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (520093697 * ((2 : ℝ) ^ (((k : ℤ) - 82)))) / 536870913 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 82)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (16777216 * ((2 : ℝ) ^ (((k : ℤ) - 82)))) / 536870913 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (520093697 * ((2 : ℝ) ^ (((k : ℤ) - 82)))) / 536870913 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`536870913`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 536870913 =
      (9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 536870913) =
    (9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82))
  exact
    problem2_10_ieeeDouble_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`536870913`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_exact_mul_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 536870913) *
        536870913 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_mul_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_below

/-- Positive denominator-`536870913`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_times_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 536870913)
        536870913 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82))) * 536870913) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_mul_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_sixtyfourth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`536870913` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 536870913 =
      -((9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 536870913) =
    -((9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82)))
  have harg : (-((2 : ℝ) ^ k)) / 536870913 =
      -(((2 : ℝ) ^ k) / 536870913) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`536870913` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_exact_mul_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 536870913) *
        536870913 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 58))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_mul_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_below

/-- Negative denominator-`536870913`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_times_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 536870913)
        536870913 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199237963776 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 82)))) * 536870913) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_mul_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtyfourth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`536870913`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_times_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 536870913)
          536870913 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 536870913)
          536870913 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_times_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen hk,
    problem2_10_ieeeDouble_neg_two_pow_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteenths_times_fivehundredthirtysixmillioneighthundredseventythousandninehundredthirteen hk⟩

/-- Finite-normal range side condition for the denominator-`1073741825`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1073741825) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1073741825 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifth
      (by linarith : (1 / 1073741825 : ℝ) ≤ ((2 : ℝ) ^ k) / 1073741825)
  · have hdiv_le : ((2 : ℝ) ^ k) / 1073741825 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`1073741825` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/1073741825` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1073741825) =
      (9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199246352384 (((k : ℤ) - 30))
  let b : ℝ :=
    fmt.normalizedValue false (9007199246352384 + 1) (((k : ℤ) - 30))
  let x : ℝ := ((2 : ℝ) ^ k) / 1073741825
  have hm : fmt.normalizedMantissa 9007199246352384 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199246352384 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199246352384, (((k : ℤ) - 30)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199246352385 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 83)) * (2 : ℝ) ^ (83 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 83)) (83 : ℤ)
    have hexp : ((k : ℤ) - 83) + 83 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (8388608 * ((2 : ℝ) ^ (((k : ℤ) - 83)))) / 1073741825 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (1065353217 * ((2 : ℝ) ^ (((k : ℤ) - 83)))) / 1073741825 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 83)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (8388608 * ((2 : ℝ) ^ (((k : ℤ) - 83)))) / 1073741825 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (1065353217 * ((2 : ℝ) ^ (((k : ℤ) - 83)))) / 1073741825 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`1073741825`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1073741825 =
      (9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1073741825) =
    (9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83))
  exact
    problem2_10_ieeeDouble_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`1073741825`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_exact_mul_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1073741825) *
        1073741825 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_mul_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_below

/-- Positive denominator-`1073741825`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_times_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1073741825)
        1073741825 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83))) * 1073741825) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_mul_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`1073741825` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1073741825 =
      -((9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1073741825) =
    -((9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83)))
  have harg : (-((2 : ℝ) ^ k)) / 1073741825 =
      -(((2 : ℝ) ^ k) / 1073741825) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`1073741825` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_exact_mul_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1073741825) *
        1073741825 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_mul_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_below

/-- Negative denominator-`1073741825`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_times_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1073741825)
        1073741825 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199246352384 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 83)))) * 1073741825) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_mul_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`1073741825`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_times_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1073741825)
          1073741825 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1073741825)
          1073741825 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_times_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfifths_times_onebillionseventythreemillionsevenhundredfortyonethousandeighthundredtwentyfive hk⟩

/-- Finite-normal range side condition for the denominator-`2147483649`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2147483649) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2147483649 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninth
      (by linarith : (1 / 2147483649 : ℝ) ≤ ((2 : ℝ) ^ k) / 2147483649)
  · have hdiv_le : ((2 : ℝ) ^ k) / 2147483649 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`2147483649` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/2147483649` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2147483649) =
      (9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199250546688 (((k : ℤ) - 31))
  let b : ℝ :=
    fmt.normalizedValue false (9007199250546688 + 1) (((k : ℤ) - 31))
  let x : ℝ := ((2 : ℝ) ^ k) / 2147483649
  have hm : fmt.normalizedMantissa 9007199250546688 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199250546688 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199250546688, (((k : ℤ) - 31)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199250546689 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 84)) * (2 : ℝ) ^ (84 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 84)) (84 : ℤ)
    have hexp : ((k : ℤ) - 84) + 84 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4194304 * ((2 : ℝ) ^ (((k : ℤ) - 84)))) / 2147483649 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2143289345 * ((2 : ℝ) ^ (((k : ℤ) - 84)))) / 2147483649 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 84)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4194304 * ((2 : ℝ) ^ (((k : ℤ) - 84)))) / 2147483649 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2143289345 * ((2 : ℝ) ^ (((k : ℤ) - 84)))) / 2147483649 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`2147483649`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2147483649 =
      (9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2147483649) =
    (9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84))
  exact
    problem2_10_ieeeDouble_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`2147483649`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_exact_mul_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2147483649) *
        2147483649 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_mul_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_below

/-- Positive denominator-`2147483649`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_times_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2147483649)
        2147483649 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84))) * 2147483649) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_mul_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_onethousandtwentyfourth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`2147483649` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2147483649 =
      -((9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2147483649) =
    -((9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84)))
  have harg : (-((2 : ℝ) ^ k)) / 2147483649 =
      -(((2 : ℝ) ^ k) / 2147483649) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`2147483649` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_exact_mul_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2147483649) *
        2147483649 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 62))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_mul_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_below

/-- Negative denominator-`2147483649`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_times_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2147483649)
        2147483649 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199250546688 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 84)))) * 2147483649) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_mul_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_onethousandtwentyfourth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`2147483649`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_times_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2147483649)
          2147483649 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2147483649)
          2147483649 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_times_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine hk,
    problem2_10_ieeeDouble_neg_two_pow_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortyninths_times_twobilliononehundredfortysevenmillionfourhundredeightythreethousandsixhundredfortynine hk⟩

/-- Finite-normal range side condition for the denominator-`4294967297`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4294967297) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4294967297 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseventh
      (by linarith : (1 / 4294967297 : ℝ) ≤ ((2 : ℝ) ^ k) / 4294967297)
  · have hdiv_le : ((2 : ℝ) ^ k) / 4294967297 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`4294967297` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/4294967297` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4294967297) =
      (9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199252643840 (((k : ℤ) - 32))
  let b : ℝ :=
    fmt.normalizedValue false (9007199252643840 + 1) (((k : ℤ) - 32))
  let x : ℝ := ((2 : ℝ) ^ k) / 4294967297
  have hm : fmt.normalizedMantissa 9007199252643840 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199252643840 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199252643840, (((k : ℤ) - 32)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199252643841 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 85)) * (2 : ℝ) ^ (85 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 85)) (85 : ℤ)
    have hexp : ((k : ℤ) - 85) + 85 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (2097152 * ((2 : ℝ) ^ (((k : ℤ) - 85)))) / 4294967297 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (4292870145 * ((2 : ℝ) ^ (((k : ℤ) - 85)))) / 4294967297 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 85)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (2097152 * ((2 : ℝ) ^ (((k : ℤ) - 85)))) / 4294967297 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (4292870145 * ((2 : ℝ) ^ (((k : ℤ) - 85)))) / 4294967297 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`4294967297`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4294967297 =
      (9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4294967297) =
    (9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85))
  exact
    problem2_10_ieeeDouble_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`4294967297`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_exact_mul_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4294967297) *
        4294967297 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_mul_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_below

/-- Positive denominator-`4294967297`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_times_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4294967297)
        4294967297 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85))) * 4294967297) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_mul_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`4294967297` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4294967297 =
      -((9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4294967297) =
    -((9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85)))
  have harg : (-((2 : ℝ) ^ k)) / 4294967297 =
      -(((2 : ℝ) ^ k) / 4294967297) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`4294967297` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_exact_mul_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4294967297) *
        4294967297 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 64))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_mul_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_below

/-- Negative denominator-`4294967297`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_times_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4294967297)
        4294967297 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199252643840 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 85)))) * 4294967297) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_mul_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_fourthousandninetysixth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`4294967297`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_times_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4294967297)
          4294967297 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4294967297)
          4294967297 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_times_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetysevenths_times_fourbilliontwohundredninetyfourmillionninehundredsixtyseventhousandtwohundredninetyseven hk⟩

/-- Finite-normal range side condition for the denominator-`8589934593`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 8589934593) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 8589934593 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythird
      (by linarith : (1 / 8589934593 : ℝ) ≤ ((2 : ℝ) ^ k) / 8589934593)
  · have hdiv_le : ((2 : ℝ) ^ k) / 8589934593 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`8589934593` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/8589934593` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8589934593) =
      (9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199253692416 (((k : ℤ) - 33))
  let b : ℝ :=
    fmt.normalizedValue false (9007199253692416 + 1) (((k : ℤ) - 33))
  let x : ℝ := ((2 : ℝ) ^ k) / 8589934593
  have hm : fmt.normalizedMantissa 9007199253692416 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199253692416 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199253692416, (((k : ℤ) - 33)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199253692417 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 86)) * (2 : ℝ) ^ (86 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 86)) (86 : ℤ)
    have hexp : ((k : ℤ) - 86) + 86 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (1048576 * ((2 : ℝ) ^ (((k : ℤ) - 86)))) / 8589934593 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (8588886017 * ((2 : ℝ) ^ (((k : ℤ) - 86)))) / 8589934593 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 86)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (1048576 * ((2 : ℝ) ^ (((k : ℤ) - 86)))) / 8589934593 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (8588886017 * ((2 : ℝ) ^ (((k : ℤ) - 86)))) / 8589934593 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`8589934593`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8589934593 =
      (9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8589934593) =
    (9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86))
  exact
    problem2_10_ieeeDouble_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`8589934593`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_exact_mul_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8589934593) *
        8589934593 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_mul_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_below

/-- Positive denominator-`8589934593`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_times_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8589934593)
        8589934593 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86))) * 8589934593) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_mul_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`8589934593` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8589934593 =
      -((9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 8589934593) =
    -((9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86)))
  have harg : (-((2 : ℝ) ^ k)) / 8589934593 =
      -(((2 : ℝ) ^ k) / 8589934593) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`8589934593` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_exact_mul_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8589934593) *
        8589934593 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 66))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_mul_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_below

/-- Negative denominator-`8589934593`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_times_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8589934593)
        8589934593 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199253692416 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 86)))) * 8589934593) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_mul_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`8589934593`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_times_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8589934593)
          8589934593 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8589934593)
          8589934593 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_times_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree hk,
    problem2_10_ieeeDouble_neg_two_pow_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythirds_times_eightbillionfivehundredeightyninemillionninehundredthirtyfourthousandfivehundredninetythree hk⟩

/-- Finite-normal range side condition for the denominator-`17179869185`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 17179869185) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 17179869185 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifth
      (by linarith : (1 / 17179869185 : ℝ) ≤ ((2 : ℝ) ^ k) / 17179869185)
  · have hdiv_le : ((2 : ℝ) ^ k) / 17179869185 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`17179869185` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/17179869185` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 17179869185) =
      (9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254216704 (((k : ℤ) - 34))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254216704 + 1) (((k : ℤ) - 34))
  let x : ℝ := ((2 : ℝ) ^ k) / 17179869185
  have hm : fmt.normalizedMantissa 9007199254216704 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254216704 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254216704, (((k : ℤ) - 34)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254216705 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 87)) * (2 : ℝ) ^ (87 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 87)) (87 : ℤ)
    have hexp : ((k : ℤ) - 87) + 87 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (524288 * ((2 : ℝ) ^ (((k : ℤ) - 87)))) / 17179869185 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (17179344897 * ((2 : ℝ) ^ (((k : ℤ) - 87)))) / 17179869185 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 87)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (524288 * ((2 : ℝ) ^ (((k : ℤ) - 87)))) / 17179869185 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (17179344897 * ((2 : ℝ) ^ (((k : ℤ) - 87)))) / 17179869185 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`17179869185`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17179869185 =
      (9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 17179869185) =
    (9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87))
  exact
    problem2_10_ieeeDouble_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`17179869185`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_exact_mul_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17179869185) *
        17179869185 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_mul_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_below

/-- Positive denominator-`17179869185`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_times_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17179869185)
        17179869185 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87))) * 17179869185) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_mul_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`17179869185` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17179869185 =
      -((9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 17179869185) =
    -((9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87)))
  have harg : (-((2 : ℝ) ^ k)) / 17179869185 =
      -(((2 : ℝ) ^ k) / 17179869185) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`17179869185` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_exact_mul_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17179869185) *
        17179869185 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 68))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_mul_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_below

/-- Negative denominator-`17179869185`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_times_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17179869185)
        17179869185 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254216704 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 87)))) * 17179869185) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_mul_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtythreethousandfivehundredthirtysixth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`17179869185`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_times_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17179869185)
          17179869185 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17179869185)
          17179869185 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_times_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfifths_times_seventeenbilliononehundredseventyninemillioneighthundredsixtyninethousandonehundredeightyfive hk⟩

/-- Finite-normal range side condition for the denominator-`34359738369`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 34359738369) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 34359738369 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninth
      (by linarith : (1 / 34359738369 : ℝ) ≤ ((2 : ℝ) ^ k) / 34359738369)
  · have hdiv_le : ((2 : ℝ) ^ k) / 34359738369 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`34359738369` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/34359738369` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 34359738369) =
      (9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254478848 (((k : ℤ) - 35))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254478848 + 1) (((k : ℤ) - 35))
  let x : ℝ := ((2 : ℝ) ^ k) / 34359738369
  have hm : fmt.normalizedMantissa 9007199254478848 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254478848 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254478848, (((k : ℤ) - 35)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254478849 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 88)) * (2 : ℝ) ^ (88 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 88)) (88 : ℤ)
    have hexp : ((k : ℤ) - 88) + 88 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (262144 * ((2 : ℝ) ^ (((k : ℤ) - 88)))) / 34359738369 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (34359476225 * ((2 : ℝ) ^ (((k : ℤ) - 88)))) / 34359738369 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 88)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (262144 * ((2 : ℝ) ^ (((k : ℤ) - 88)))) / 34359738369 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (34359476225 * ((2 : ℝ) ^ (((k : ℤ) - 88)))) / 34359738369 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`34359738369`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 34359738369 =
      (9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 34359738369) =
    (9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88))
  exact
    problem2_10_ieeeDouble_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`34359738369`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_exact_mul_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 34359738369) *
        34359738369 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_mul_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_below

/-- Positive denominator-`34359738369`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_times_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 34359738369)
        34359738369 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88))) * 34359738369) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_mul_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`34359738369` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 34359738369 =
      -((9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 34359738369) =
    -((9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88)))
  have harg : (-((2 : ℝ) ^ k)) / 34359738369 =
      -(((2 : ℝ) ^ k) / 34359738369) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`34359738369` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_exact_mul_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 34359738369) *
        34359738369 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 70))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_mul_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_below

/-- Negative denominator-`34359738369`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_times_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 34359738369)
        34359738369 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254478848 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 88)))) * 34359738369) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_mul_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`34359738369`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_times_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 34359738369)
          34359738369 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 34359738369)
          34359738369 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_times_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine hk,
    problem2_10_ieeeDouble_neg_two_pow_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtyninths_times_thirtyfourbillionthreehundredfiftyninemillionsevenhundredthirtyeightthousandthreehundredsixtynine hk⟩

/-- Finite-normal range side condition for the denominator-`68719476737`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 68719476737) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 68719476737 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseventh
      (by linarith : (1 / 68719476737 : ℝ) ≤ ((2 : ℝ) ^ k) / 68719476737)
  · have hdiv_le : ((2 : ℝ) ^ k) / 68719476737 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`68719476737` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/68719476737` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 68719476737) =
      (9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254609920 (((k : ℤ) - 36))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254609920 + 1) (((k : ℤ) - 36))
  let x : ℝ := ((2 : ℝ) ^ k) / 68719476737
  have hm : fmt.normalizedMantissa 9007199254609920 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254609920 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254609920, (((k : ℤ) - 36)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254609921 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 89)) * (2 : ℝ) ^ (89 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 89)) (89 : ℤ)
    have hexp : ((k : ℤ) - 89) + 89 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (131072 * ((2 : ℝ) ^ (((k : ℤ) - 89)))) / 68719476737 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (68719345665 * ((2 : ℝ) ^ (((k : ℤ) - 89)))) / 68719476737 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 89)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (131072 * ((2 : ℝ) ^ (((k : ℤ) - 89)))) / 68719476737 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (68719345665 * ((2 : ℝ) ^ (((k : ℤ) - 89)))) / 68719476737 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`68719476737`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 68719476737 =
      (9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 68719476737) =
    (9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89))
  exact
    problem2_10_ieeeDouble_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`68719476737`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_exact_mul_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 68719476737) *
        68719476737 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_mul_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_below

/-- Positive denominator-`68719476737`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_times_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 68719476737)
        68719476737 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89))) * 68719476737) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_mul_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`68719476737` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 68719476737 =
      -((9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 68719476737) =
    -((9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89)))
  have harg : (-((2 : ℝ) ^ k)) / 68719476737 =
      -(((2 : ℝ) ^ k) / 68719476737) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`68719476737` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_exact_mul_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 68719476737) *
        68719476737 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 72))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_mul_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_below

/-- Negative denominator-`68719476737`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_times_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 68719476737)
        68719476737 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254609920 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 89)))) * 68719476737) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_mul_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`68719476737`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_times_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 68719476737)
          68719476737 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 68719476737)
          68719476737 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_times_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven hk,
    problem2_10_ieeeDouble_neg_two_pow_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtysevenths_times_sixtyeightbillionsevenhundrednineteenmillionfourhundredseventysixthousandsevenhundredthirtyseven hk⟩

/-- Finite-normal range side condition for the denominator-`137438953473`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 137438953473) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 137438953473 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythird
      (by linarith : (1 / 137438953473 : ℝ) ≤ ((2 : ℝ) ^ k) / 137438953473)
  · have hdiv_le : ((2 : ℝ) ^ k) / 137438953473 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`137438953473` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/137438953473` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 137438953473) =
      (9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254675456 (((k : ℤ) - 37))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254675456 + 1) (((k : ℤ) - 37))
  let x : ℝ := ((2 : ℝ) ^ k) / 137438953473
  have hm : fmt.normalizedMantissa 9007199254675456 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254675456 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254675456, (((k : ℤ) - 37)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254675457 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 90)) * (2 : ℝ) ^ (90 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 90)) (90 : ℤ)
    have hexp : ((k : ℤ) - 90) + 90 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (65536 * ((2 : ℝ) ^ (((k : ℤ) - 90)))) / 137438953473 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (137438887937 * ((2 : ℝ) ^ (((k : ℤ) - 90)))) / 137438953473 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 90)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (65536 * ((2 : ℝ) ^ (((k : ℤ) - 90)))) / 137438953473 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (137438887937 * ((2 : ℝ) ^ (((k : ℤ) - 90)))) / 137438953473 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`137438953473`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 137438953473 =
      (9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 137438953473) =
    (9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90))
  exact
    problem2_10_ieeeDouble_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`137438953473`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_exact_mul_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 137438953473) *
        137438953473 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_mul_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_below

/-- Positive denominator-`137438953473`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_times_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 137438953473)
        137438953473 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90))) * 137438953473) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_mul_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`137438953473` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 137438953473 =
      -((9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 137438953473) =
    -((9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90)))
  have harg : (-((2 : ℝ) ^ k)) / 137438953473 =
      -(((2 : ℝ) ^ k) / 137438953473) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`137438953473` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_exact_mul_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 137438953473) *
        137438953473 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 74))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_mul_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_below

/-- Negative denominator-`137438953473`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_times_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 137438953473)
        137438953473 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254675456 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 90)))) * 137438953473) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_mul_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`137438953473`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_times_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 137438953473)
          137438953473 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 137438953473)
          137438953473 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_times_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree hk,
    problem2_10_ieeeDouble_neg_two_pow_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythirds_times_onehundredthirtysevenbillionfourhundredthirtyeightmillionninehundredfiftythreethousandfourhundredseventythree hk⟩

/-- Finite-normal range side condition for the denominator-`274877906945`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 274877906945) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 274877906945 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifth
      (by linarith : (1 / 274877906945 : ℝ) ≤ ((2 : ℝ) ^ k) / 274877906945)
  · have hdiv_le : ((2 : ℝ) ^ k) / 274877906945 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`274877906945` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/274877906945` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 274877906945) =
      (9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254708224 (((k : ℤ) - 38))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254708224 + 1) (((k : ℤ) - 38))
  let x : ℝ := ((2 : ℝ) ^ k) / 274877906945
  have hm : fmt.normalizedMantissa 9007199254708224 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254708224 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254708224, (((k : ℤ) - 38)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254708225 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 91)) * (2 : ℝ) ^ (91 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 91)) (91 : ℤ)
    have hexp : ((k : ℤ) - 91) + 91 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (32768 * ((2 : ℝ) ^ (((k : ℤ) - 91)))) / 274877906945 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (274877874177 * ((2 : ℝ) ^ (((k : ℤ) - 91)))) / 274877906945 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 91)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (32768 * ((2 : ℝ) ^ (((k : ℤ) - 91)))) / 274877906945 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (274877874177 * ((2 : ℝ) ^ (((k : ℤ) - 91)))) / 274877906945 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`274877906945`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 274877906945 =
      (9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 274877906945) =
    (9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91))
  exact
    problem2_10_ieeeDouble_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`274877906945`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_exact_mul_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 274877906945) *
        274877906945 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_mul_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_below

/-- Positive denominator-`274877906945`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_times_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 274877906945)
        274877906945 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91))) * 274877906945) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_mul_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`274877906945` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 274877906945 =
      -((9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 274877906945) =
    -((9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91)))
  have harg : (-((2 : ℝ) ^ k)) / 274877906945 =
      -(((2 : ℝ) ^ k) / 274877906945) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`274877906945` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_exact_mul_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 274877906945) *
        274877906945 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 76))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_mul_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_below

/-- Negative denominator-`274877906945`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_times_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 274877906945)
        274877906945 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254708224 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 91)))) * 274877906945) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_mul_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`274877906945`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_times_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 274877906945)
          274877906945 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 274877906945)
          274877906945 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_times_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive hk,
    problem2_10_ieeeDouble_neg_two_pow_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfifths_times_twohundredseventyfourbillioneighthundredseventysevenmillionninehundredsixthousandninehundredfortyfive hk⟩

/-- Finite-normal range side condition for the denominator-`549755813889`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 549755813889) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 549755813889 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninth
      (by linarith : (1 / 549755813889 : ℝ) ≤ ((2 : ℝ) ^ k) / 549755813889)
  · have hdiv_le : ((2 : ℝ) ^ k) / 549755813889 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`549755813889` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/549755813889` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 549755813889) =
      (9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254724608 (((k : ℤ) - 39))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254724608 + 1) (((k : ℤ) - 39))
  let x : ℝ := ((2 : ℝ) ^ k) / 549755813889
  have hm : fmt.normalizedMantissa 9007199254724608 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254724608 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254724608, (((k : ℤ) - 39)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254724609 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 92)) * (2 : ℝ) ^ (92 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 92)) (92 : ℤ)
    have hexp : ((k : ℤ) - 92) + 92 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (16384 * ((2 : ℝ) ^ (((k : ℤ) - 92)))) / 549755813889 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (549755797505 * ((2 : ℝ) ^ (((k : ℤ) - 92)))) / 549755813889 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 92)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (16384 * ((2 : ℝ) ^ (((k : ℤ) - 92)))) / 549755813889 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (549755797505 * ((2 : ℝ) ^ (((k : ℤ) - 92)))) / 549755813889 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`549755813889`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 549755813889 =
      (9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 549755813889) =
    (9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92))
  exact
    problem2_10_ieeeDouble_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`549755813889`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_exact_mul_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 549755813889) *
        549755813889 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_mul_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_below

/-- Positive denominator-`549755813889`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_times_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 549755813889)
        549755813889 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92))) * 549755813889) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_mul_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`549755813889` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 549755813889 =
      -((9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 549755813889) =
    -((9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92)))
  have harg : (-((2 : ℝ) ^ k)) / 549755813889 =
      -(((2 : ℝ) ^ k) / 549755813889) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`549755813889` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_exact_mul_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 549755813889) *
        549755813889 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 78))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_mul_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_below

/-- Negative denominator-`549755813889`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_times_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 549755813889)
        549755813889 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254724608 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 92)))) * 549755813889) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_mul_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_sixtysevenmilliononehundredeightthousandsixtyfourth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`549755813889`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_times_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 549755813889)
          549755813889 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 549755813889)
          549755813889 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_times_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine hk,
    problem2_10_ieeeDouble_neg_two_pow_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightyninths_times_fivehundredfortyninebillionsevenhundredfiftyfivemillioneighthundredthirteenthousandeighthundredeightynine hk⟩

/-- Finite-normal range side condition for the denominator-`1099511627777`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1099511627777) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1099511627777 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_plus_one
      (by linarith : (1 / 1099511627777 : ℝ) ≤ ((2 : ℝ) ^ k) / 1099511627777)
  · have hdiv_le : ((2 : ℝ) ^ k) / 1099511627777 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`1099511627777` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/1099511627777` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1099511627777) =
      (9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254732800 (((k : ℤ) - 40))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254732800 + 1) (((k : ℤ) - 40))
  let x : ℝ := ((2 : ℝ) ^ k) / 1099511627777
  have hm : fmt.normalizedMantissa 9007199254732800 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254732800 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254732800, (((k : ℤ) - 40)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254732801 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 93)) * (2 : ℝ) ^ (93 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 93)) (93 : ℤ)
    have hexp : ((k : ℤ) - 93) + 93 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (8192 * ((2 : ℝ) ^ (((k : ℤ) - 93)))) / 1099511627777 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (1099511619585 * ((2 : ℝ) ^ (((k : ℤ) - 93)))) / 1099511627777 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 93)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (8192 * ((2 : ℝ) ^ (((k : ℤ) - 93)))) / 1099511627777 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (1099511619585 * ((2 : ℝ) ^ (((k : ℤ) - 93)))) / 1099511627777 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`1099511627777`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1099511627777 =
      (9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1099511627777) =
    (9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`1099511627777`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_plus_one_exact_mul_two_pow_forty_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1099511627777) *
        1099511627777 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_plus_one_denominator_mul_two_pow_forty_plus_one_below

/-- Positive denominator-`1099511627777`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_plus_one_denominator_times_two_pow_forty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1099511627777)
        1099511627777 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93))) * 1099511627777) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_plus_one_denominator_mul_two_pow_forty_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`1099511627777` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1099511627777 =
      -((9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1099511627777) =
    -((9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93)))
  have harg : (-((2 : ℝ) ^ k)) / 1099511627777 =
      -(((2 : ℝ) ^ k) / 1099511627777) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`1099511627777` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_plus_one_exact_mul_two_pow_forty_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1099511627777) *
        1099511627777 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 80))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_plus_one_denominator_mul_two_pow_forty_plus_one_below

/-- Negative denominator-`1099511627777`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_plus_one_denominator_times_two_pow_forty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1099511627777)
        1099511627777 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254732800 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 93)))) * 1099511627777) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_plus_one_denominator_mul_two_pow_forty_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_twohundredsixtyeightmillionfourhundredthirtyfivethousandfourhundredfiftysixth_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`1099511627777`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_plus_one_denominator_times_two_pow_forty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1099511627777)
          1099511627777 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1099511627777)
          1099511627777 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_plus_one_denominator_times_two_pow_forty_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_plus_one_denominator_times_two_pow_forty_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`2199023255553`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_one_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2199023255553) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2199023255553 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_one_plus_one
      (by linarith : (1 / 2199023255553 : ℝ) ≤ ((2 : ℝ) ^ k) / 2199023255553)
  · have hdiv_le : ((2 : ℝ) ^ k) / 2199023255553 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`2199023255553` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/2199023255553` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_one_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2199023255553) =
      (9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254736896 (((k : ℤ) - 41))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254736896 + 1) (((k : ℤ) - 41))
  let x : ℝ := ((2 : ℝ) ^ k) / 2199023255553
  have hm : fmt.normalizedMantissa 9007199254736896 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254736896 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254736896, (((k : ℤ) - 41)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254736897 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 94)) * (2 : ℝ) ^ (94 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 94)) (94 : ℤ)
    have hexp : ((k : ℤ) - 94) + 94 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4096 * ((2 : ℝ) ^ (((k : ℤ) - 94)))) / 2199023255553 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2199023251457 * ((2 : ℝ) ^ (((k : ℤ) - 94)))) / 2199023255553 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 94)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4096 * ((2 : ℝ) ^ (((k : ℤ) - 94)))) / 2199023255553 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2199023251457 * ((2 : ℝ) ^ (((k : ℤ) - 94)))) / 2199023255553 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_one_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`2199023255553`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2199023255553 =
      (9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2199023255553) =
    (9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_one_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`2199023255553`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_one_plus_one_exact_mul_two_pow_forty_one_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2199023255553) *
        2199023255553 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_one_plus_one_denominator_mul_two_pow_forty_one_plus_one_below

/-- Positive denominator-`2199023255553`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_one_plus_one_denominator_times_two_pow_forty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2199023255553)
        2199023255553 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94))) * 2199023255553) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_one_plus_one_denominator_mul_two_pow_forty_one_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`2199023255553` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2199023255553 =
      -((9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2199023255553) =
    -((9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94)))
  have harg : (-((2 : ℝ) ^ k)) / 2199023255553 =
      -(((2 : ℝ) ^ k) / 2199023255553) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_one_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`2199023255553` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_one_plus_one_exact_mul_two_pow_forty_one_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2199023255553) *
        2199023255553 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 82))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_one_plus_one_denominator_mul_two_pow_forty_one_plus_one_below

/-- Negative denominator-`2199023255553`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_one_plus_one_denominator_times_two_pow_forty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2199023255553)
        2199023255553 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254736896 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 94)))) * 2199023255553) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_one_plus_one_denominator_mul_two_pow_forty_one_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`2199023255553`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_one_plus_one_denominator_times_two_pow_forty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2199023255553)
          2199023255553 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2199023255553)
          2199023255553 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_one_plus_one_denominator_times_two_pow_forty_one_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_one_plus_one_denominator_times_two_pow_forty_one_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`4398046511105`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_two_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4398046511105) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4398046511105 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_two_plus_one
      (by linarith : (1 / 4398046511105 : ℝ) ≤ ((2 : ℝ) ^ k) / 4398046511105)
  · have hdiv_le : ((2 : ℝ) ^ k) / 4398046511105 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`4398046511105` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/4398046511105` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_two_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4398046511105) =
      (9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254738944 (((k : ℤ) - 42))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254738944 + 1) (((k : ℤ) - 42))
  let x : ℝ := ((2 : ℝ) ^ k) / 4398046511105
  have hm : fmt.normalizedMantissa 9007199254738944 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254738944 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254738944, (((k : ℤ) - 42)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254738945 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 95)) * (2 : ℝ) ^ (95 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 95)) (95 : ℤ)
    have hexp : ((k : ℤ) - 95) + 95 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (2048 * ((2 : ℝ) ^ (((k : ℤ) - 95)))) / 4398046511105 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (4398046509057 * ((2 : ℝ) ^ (((k : ℤ) - 95)))) / 4398046511105 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 95)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (2048 * ((2 : ℝ) ^ (((k : ℤ) - 95)))) / 4398046511105 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (4398046509057 * ((2 : ℝ) ^ (((k : ℤ) - 95)))) / 4398046511105 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_two_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`4398046511105`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4398046511105 =
      (9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4398046511105) =
    (9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_two_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`4398046511105`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_two_plus_one_exact_mul_two_pow_forty_two_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4398046511105) *
        4398046511105 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_two_plus_one_denominator_mul_two_pow_forty_two_plus_one_below

/-- Positive denominator-`4398046511105`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_two_plus_one_denominator_times_two_pow_forty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4398046511105)
        4398046511105 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95))) * 4398046511105) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_two_plus_one_denominator_mul_two_pow_forty_two_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_two_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`4398046511105` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4398046511105 =
      -((9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4398046511105) =
    -((9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95)))
  have harg : (-((2 : ℝ) ^ k)) / 4398046511105 =
      -(((2 : ℝ) ^ k) / 4398046511105) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_two_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`4398046511105` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_two_plus_one_exact_mul_two_pow_forty_two_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4398046511105) *
        4398046511105 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 84))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_two_plus_one_denominator_mul_two_pow_forty_two_plus_one_below

/-- Negative denominator-`4398046511105`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_two_plus_one_denominator_times_two_pow_forty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4398046511105)
        4398046511105 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254738944 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 95)))) * 4398046511105) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_two_plus_one_denominator_mul_two_pow_forty_two_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_two_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`4398046511105`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_two_plus_one_denominator_times_two_pow_forty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4398046511105)
          4398046511105 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4398046511105)
          4398046511105 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_two_plus_one_denominator_times_two_pow_forty_two_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_two_plus_one_denominator_times_two_pow_forty_two_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`8796093022209`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_three_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 8796093022209) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 8796093022209 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_three_plus_one
      (by linarith : (1 / 8796093022209 : ℝ) ≤ ((2 : ℝ) ^ k) / 8796093022209)
  · have hdiv_le : ((2 : ℝ) ^ k) / 8796093022209 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`8796093022209` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/8796093022209` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_three_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8796093022209) =
      (9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254739968 (((k : ℤ) - 43))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254739968 + 1) (((k : ℤ) - 43))
  let x : ℝ := ((2 : ℝ) ^ k) / 8796093022209
  have hm : fmt.normalizedMantissa 9007199254739968 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254739968 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254739968, (((k : ℤ) - 43)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254739969 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 96)) * (2 : ℝ) ^ (96 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 96)) (96 : ℤ)
    have hexp : ((k : ℤ) - 96) + 96 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (1024 * ((2 : ℝ) ^ (((k : ℤ) - 96)))) / 8796093022209 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (8796093021185 * ((2 : ℝ) ^ (((k : ℤ) - 96)))) / 8796093022209 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 96)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (1024 * ((2 : ℝ) ^ (((k : ℤ) - 96)))) / 8796093022209 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (8796093021185 * ((2 : ℝ) ^ (((k : ℤ) - 96)))) / 8796093022209 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_three_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`8796093022209`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8796093022209 =
      (9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 8796093022209) =
    (9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_three_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`8796093022209`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_three_plus_one_exact_mul_two_pow_forty_three_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8796093022209) *
        8796093022209 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_three_plus_one_denominator_mul_two_pow_forty_three_plus_one_below

/-- Positive denominator-`8796093022209`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_three_plus_one_denominator_times_two_pow_forty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8796093022209)
        8796093022209 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96))) * 8796093022209) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_three_plus_one_denominator_mul_two_pow_forty_three_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_four_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`8796093022209` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8796093022209 =
      -((9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 8796093022209) =
    -((9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96)))
  have harg : (-((2 : ℝ) ^ k)) / 8796093022209 =
      -(((2 : ℝ) ^ k) / 8796093022209) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_three_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`8796093022209` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_three_plus_one_exact_mul_two_pow_forty_three_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8796093022209) *
        8796093022209 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 86))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_three_plus_one_denominator_mul_two_pow_forty_three_plus_one_below

/-- Negative denominator-`8796093022209`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_three_plus_one_denominator_times_two_pow_forty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8796093022209)
        8796093022209 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254739968 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 96)))) * 8796093022209) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_three_plus_one_denominator_mul_two_pow_forty_three_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_four_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`8796093022209`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_three_plus_one_denominator_times_two_pow_forty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 8796093022209)
          8796093022209 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 8796093022209)
          8796093022209 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_three_plus_one_denominator_times_two_pow_forty_three_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_three_plus_one_denominator_times_two_pow_forty_three_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`17592186044417`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_four_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 17592186044417) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 17592186044417 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_four_plus_one
      (by linarith : (1 / 17592186044417 : ℝ) ≤ ((2 : ℝ) ^ k) / 17592186044417)
  · have hdiv_le : ((2 : ℝ) ^ k) / 17592186044417 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`17592186044417` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/17592186044417` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_four_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 17592186044417) =
      (9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740480 (((k : ℤ) - 44))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740480 + 1) (((k : ℤ) - 44))
  let x : ℝ := ((2 : ℝ) ^ k) / 17592186044417
  have hm : fmt.normalizedMantissa 9007199254740480 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740480 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740480, (((k : ℤ) - 44)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740481 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 97)) * (2 : ℝ) ^ (97 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 97)) (97 : ℤ)
    have hexp : ((k : ℤ) - 97) + 97 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (512 * ((2 : ℝ) ^ (((k : ℤ) - 97)))) / 17592186044417 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (17592186043905 * ((2 : ℝ) ^ (((k : ℤ) - 97)))) / 17592186044417 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 97)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (512 * ((2 : ℝ) ^ (((k : ℤ) - 97)))) / 17592186044417 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (17592186043905 * ((2 : ℝ) ^ (((k : ℤ) - 97)))) / 17592186044417 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_four_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`17592186044417`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17592186044417 =
      (9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 17592186044417) =
    (9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_four_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`17592186044417`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_four_plus_one_exact_mul_two_pow_forty_four_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17592186044417) *
        17592186044417 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_four_plus_one_denominator_mul_two_pow_forty_four_plus_one_below

/-- Positive denominator-`17592186044417`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_four_plus_one_denominator_times_two_pow_forty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17592186044417)
        17592186044417 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97))) * 17592186044417) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_four_plus_one_denominator_mul_two_pow_forty_four_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_six_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`17592186044417` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17592186044417 =
      -((9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 17592186044417) =
    -((9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97)))
  have harg : (-((2 : ℝ) ^ k)) / 17592186044417 =
      -(((2 : ℝ) ^ k) / 17592186044417) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_four_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`17592186044417` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_four_plus_one_exact_mul_two_pow_forty_four_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17592186044417) *
        17592186044417 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 88))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_four_plus_one_denominator_mul_two_pow_forty_four_plus_one_below

/-- Negative denominator-`17592186044417`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_four_plus_one_denominator_times_two_pow_forty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17592186044417)
        17592186044417 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740480 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 97)))) * 17592186044417) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_four_plus_one_denominator_mul_two_pow_forty_four_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_six_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`17592186044417`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_four_plus_one_denominator_times_two_pow_forty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 17592186044417)
          17592186044417 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 17592186044417)
          17592186044417 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_four_plus_one_denominator_times_two_pow_forty_four_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_four_plus_one_denominator_times_two_pow_forty_four_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`35184372088833`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_five_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 35184372088833) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 35184372088833 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_five_plus_one
      (by linarith : (1 / 35184372088833 : ℝ) ≤ ((2 : ℝ) ^ k) / 35184372088833)
  · have hdiv_le : ((2 : ℝ) ^ k) / 35184372088833 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`35184372088833` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/35184372088833` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_five_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 35184372088833) =
      (9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740736 (((k : ℤ) - 45))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740736 + 1) (((k : ℤ) - 45))
  let x : ℝ := ((2 : ℝ) ^ k) / 35184372088833
  have hm : fmt.normalizedMantissa 9007199254740736 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740736 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740736, (((k : ℤ) - 45)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740737 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 98)) * (2 : ℝ) ^ (98 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 98)) (98 : ℤ)
    have hexp : ((k : ℤ) - 98) + 98 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (256 * ((2 : ℝ) ^ (((k : ℤ) - 98)))) / 35184372088833 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (35184372088577 * ((2 : ℝ) ^ (((k : ℤ) - 98)))) / 35184372088833 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 98)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (256 * ((2 : ℝ) ^ (((k : ℤ) - 98)))) / 35184372088833 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (35184372088577 * ((2 : ℝ) ^ (((k : ℤ) - 98)))) / 35184372088833 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_five_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`35184372088833`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 35184372088833 =
      (9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 35184372088833) =
    (9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_five_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`35184372088833`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_five_plus_one_exact_mul_two_pow_forty_five_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 35184372088833) *
        35184372088833 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_five_plus_one_denominator_mul_two_pow_forty_five_plus_one_below

/-- Positive denominator-`35184372088833`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_five_plus_one_denominator_times_two_pow_forty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 35184372088833)
        35184372088833 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98))) * 35184372088833) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_five_plus_one_denominator_mul_two_pow_forty_five_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_thirty_eight_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`35184372088833` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 35184372088833 =
      -((9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 35184372088833) =
    -((9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98)))
  have harg : (-((2 : ℝ) ^ k)) / 35184372088833 =
      -(((2 : ℝ) ^ k) / 35184372088833) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_five_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`35184372088833` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_five_plus_one_exact_mul_two_pow_forty_five_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 35184372088833) *
        35184372088833 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 90))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_five_plus_one_denominator_mul_two_pow_forty_five_plus_one_below

/-- Negative denominator-`35184372088833`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_five_plus_one_denominator_times_two_pow_forty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 35184372088833)
        35184372088833 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740736 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 98)))) * 35184372088833) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_five_plus_one_denominator_mul_two_pow_forty_five_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_thirty_eight_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`35184372088833`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_five_plus_one_denominator_times_two_pow_forty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 35184372088833)
          35184372088833 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 35184372088833)
          35184372088833 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_five_plus_one_denominator_times_two_pow_forty_five_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_five_plus_one_denominator_times_two_pow_forty_five_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`70368744177665`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_six_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 70368744177665) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 70368744177665 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_six_plus_one
      (by linarith : (1 / 70368744177665 : ℝ) ≤ ((2 : ℝ) ^ k) / 70368744177665)
  · have hdiv_le : ((2 : ℝ) ^ k) / 70368744177665 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`70368744177665` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/70368744177665` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_six_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 70368744177665) =
      (9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740864 (((k : ℤ) - 46))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740864 + 1) (((k : ℤ) - 46))
  let x : ℝ := ((2 : ℝ) ^ k) / 70368744177665
  have hm : fmt.normalizedMantissa 9007199254740864 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740864 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740864, (((k : ℤ) - 46)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740865 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 99)) * (2 : ℝ) ^ (99 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 99)) (99 : ℤ)
    have hexp : ((k : ℤ) - 99) + 99 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (128 * ((2 : ℝ) ^ (((k : ℤ) - 99)))) / 70368744177665 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (70368744177537 * ((2 : ℝ) ^ (((k : ℤ) - 99)))) / 70368744177665 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 99)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (128 * ((2 : ℝ) ^ (((k : ℤ) - 99)))) / 70368744177665 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (70368744177537 * ((2 : ℝ) ^ (((k : ℤ) - 99)))) / 70368744177665 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_six_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`70368744177665`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 70368744177665 =
      (9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 70368744177665) =
    (9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_six_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`70368744177665`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_six_plus_one_exact_mul_two_pow_forty_six_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 70368744177665) *
        70368744177665 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_six_plus_one_denominator_mul_two_pow_forty_six_plus_one_below

/-- Positive denominator-`70368744177665`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_six_plus_one_denominator_times_two_pow_forty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 70368744177665)
        70368744177665 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99))) * 70368744177665) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_six_plus_one_denominator_mul_two_pow_forty_six_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`70368744177665` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 70368744177665 =
      -((9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 70368744177665) =
    -((9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99)))
  have harg : (-((2 : ℝ) ^ k)) / 70368744177665 =
      -(((2 : ℝ) ^ k) / 70368744177665) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_six_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`70368744177665` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_six_plus_one_exact_mul_two_pow_forty_six_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 70368744177665) *
        70368744177665 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 92))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_six_plus_one_denominator_mul_two_pow_forty_six_plus_one_below

/-- Negative denominator-`70368744177665`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_six_plus_one_denominator_times_two_pow_forty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 70368744177665)
        70368744177665 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740864 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 99)))) * 70368744177665) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_six_plus_one_denominator_mul_two_pow_forty_six_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`70368744177665`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_six_plus_one_denominator_times_two_pow_forty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 70368744177665)
          70368744177665 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 70368744177665)
          70368744177665 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_six_plus_one_denominator_times_two_pow_forty_six_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_six_plus_one_denominator_times_two_pow_forty_six_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`140737488355329`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_seven_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 140737488355329) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 140737488355329 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_seven_plus_one
      (by linarith : (1 / 140737488355329 : ℝ) ≤ ((2 : ℝ) ^ k) / 140737488355329)
  · have hdiv_le : ((2 : ℝ) ^ k) / 140737488355329 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`140737488355329` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/140737488355329` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_seven_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 140737488355329) =
      (9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740928 (((k : ℤ) - 47))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740928 + 1) (((k : ℤ) - 47))
  let x : ℝ := ((2 : ℝ) ^ k) / 140737488355329
  have hm : fmt.normalizedMantissa 9007199254740928 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740928 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740928, (((k : ℤ) - 47)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740929 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 100)) * (2 : ℝ) ^ (100 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 100)) (100 : ℤ)
    have hexp : ((k : ℤ) - 100) + 100 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (64 * ((2 : ℝ) ^ (((k : ℤ) - 100)))) / 140737488355329 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (140737488355265 * ((2 : ℝ) ^ (((k : ℤ) - 100)))) / 140737488355329 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 100)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (64 * ((2 : ℝ) ^ (((k : ℤ) - 100)))) / 140737488355329 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (140737488355265 * ((2 : ℝ) ^ (((k : ℤ) - 100)))) / 140737488355329 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_seven_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`140737488355329`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 140737488355329 =
      (9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 140737488355329) =
    (9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_seven_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`140737488355329`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_seven_plus_one_exact_mul_two_pow_forty_seven_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 140737488355329) *
        140737488355329 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_seven_plus_one_denominator_mul_two_pow_forty_seven_plus_one_below

/-- Positive denominator-`140737488355329`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_seven_plus_one_denominator_times_two_pow_forty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 140737488355329)
        140737488355329 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100))) * 140737488355329) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_seven_plus_one_denominator_mul_two_pow_forty_seven_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_two_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`140737488355329` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 140737488355329 =
      -((9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 140737488355329) =
    -((9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100)))
  have harg : (-((2 : ℝ) ^ k)) / 140737488355329 =
      -(((2 : ℝ) ^ k) / 140737488355329) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_seven_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`140737488355329` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_seven_plus_one_exact_mul_two_pow_forty_seven_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 140737488355329) *
        140737488355329 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 94))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_seven_plus_one_denominator_mul_two_pow_forty_seven_plus_one_below

/-- Negative denominator-`140737488355329`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_seven_plus_one_denominator_times_two_pow_forty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 140737488355329)
        140737488355329 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740928 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 100)))) * 140737488355329) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_seven_plus_one_denominator_mul_two_pow_forty_seven_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_two_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`140737488355329`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_seven_plus_one_denominator_times_two_pow_forty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 140737488355329)
          140737488355329 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 140737488355329)
          140737488355329 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_seven_plus_one_denominator_times_two_pow_forty_seven_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_seven_plus_one_denominator_times_two_pow_forty_seven_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`281474976710657`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_eight_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 281474976710657) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 281474976710657 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_eight_plus_one
      (by linarith : (1 / 281474976710657 : ℝ) ≤ ((2 : ℝ) ^ k) / 281474976710657)
  · have hdiv_le : ((2 : ℝ) ^ k) / 281474976710657 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`281474976710657` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/281474976710657` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_eight_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 281474976710657) =
      (9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740960 (((k : ℤ) - 48))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740960 + 1) (((k : ℤ) - 48))
  let x : ℝ := ((2 : ℝ) ^ k) / 281474976710657
  have hm : fmt.normalizedMantissa 9007199254740960 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740960 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740960, (((k : ℤ) - 48)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740961 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 101)) * (2 : ℝ) ^ (101 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 101)) (101 : ℤ)
    have hexp : ((k : ℤ) - 101) + 101 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (32 * ((2 : ℝ) ^ (((k : ℤ) - 101)))) / 281474976710657 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (281474976710625 * ((2 : ℝ) ^ (((k : ℤ) - 101)))) / 281474976710657 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 101)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (32 * ((2 : ℝ) ^ (((k : ℤ) - 101)))) / 281474976710657 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (281474976710625 * ((2 : ℝ) ^ (((k : ℤ) - 101)))) / 281474976710657 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_eight_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`281474976710657`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 281474976710657 =
      (9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 281474976710657) =
    (9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_eight_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`281474976710657`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_eight_plus_one_exact_mul_two_pow_forty_eight_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 281474976710657) *
        281474976710657 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_eight_plus_one_denominator_mul_two_pow_forty_eight_plus_one_below

/-- Positive denominator-`281474976710657`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_eight_plus_one_denominator_times_two_pow_forty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 281474976710657)
        281474976710657 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101))) * 281474976710657) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_eight_plus_one_denominator_mul_two_pow_forty_eight_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_four_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`281474976710657` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 281474976710657 =
      -((9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 281474976710657) =
    -((9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101)))
  have harg : (-((2 : ℝ) ^ k)) / 281474976710657 =
      -(((2 : ℝ) ^ k) / 281474976710657) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_eight_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`281474976710657` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_eight_plus_one_exact_mul_two_pow_forty_eight_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 281474976710657) *
        281474976710657 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 96))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_eight_plus_one_denominator_mul_two_pow_forty_eight_plus_one_below

/-- Negative denominator-`281474976710657`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_eight_plus_one_denominator_times_two_pow_forty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 281474976710657)
        281474976710657 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740960 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 101)))) * 281474976710657) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_eight_plus_one_denominator_mul_two_pow_forty_eight_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_four_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`281474976710657`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_eight_plus_one_denominator_times_two_pow_forty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 281474976710657)
          281474976710657 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 281474976710657)
          281474976710657 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_eight_plus_one_denominator_times_two_pow_forty_eight_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_eight_plus_one_denominator_times_two_pow_forty_eight_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`562949953421313`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_forty_nine_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 562949953421313) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 562949953421313 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_forty_nine_plus_one
      (by linarith : (1 / 562949953421313 : ℝ) ≤ ((2 : ℝ) ^ k) / 562949953421313)
  · have hdiv_le : ((2 : ℝ) ^ k) / 562949953421313 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`562949953421313` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/562949953421313` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_nine_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 562949953421313) =
      (9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740976 (((k : ℤ) - 49))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740976 + 1) (((k : ℤ) - 49))
  let x : ℝ := ((2 : ℝ) ^ k) / 562949953421313
  have hm : fmt.normalizedMantissa 9007199254740976 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740976 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740976, (((k : ℤ) - 49)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740977 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 102)) * (2 : ℝ) ^ (102 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 102)) (102 : ℤ)
    have hexp : ((k : ℤ) - 102) + 102 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (16 * ((2 : ℝ) ^ (((k : ℤ) - 102)))) / 562949953421313 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (562949953421297 * ((2 : ℝ) ^ (((k : ℤ) - 102)))) / 562949953421313 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 102)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (16 * ((2 : ℝ) ^ (((k : ℤ) - 102)))) / 562949953421313 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (562949953421297 * ((2 : ℝ) ^ (((k : ℤ) - 102)))) / 562949953421313 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_forty_nine_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`562949953421313`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 562949953421313 =
      (9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 562949953421313) =
    (9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_forty_nine_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`562949953421313`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_forty_nine_plus_one_exact_mul_two_pow_forty_nine_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 562949953421313) *
        562949953421313 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_nine_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_nine_plus_one_denominator_mul_two_pow_forty_nine_plus_one_below

/-- Positive denominator-`562949953421313`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_forty_nine_plus_one_denominator_times_two_pow_forty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 562949953421313)
        562949953421313 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_forty_nine_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102))) * 562949953421313) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_forty_nine_plus_one_denominator_mul_two_pow_forty_nine_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_six_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`562949953421313` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 562949953421313 =
      -((9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 562949953421313) =
    -((9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102)))
  have harg : (-((2 : ℝ) ^ k)) / 562949953421313 =
      -(((2 : ℝ) ^ k) / 562949953421313) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_forty_nine_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`562949953421313` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_nine_plus_one_exact_mul_two_pow_forty_nine_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 562949953421313) *
        562949953421313 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 98))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_nine_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_nine_plus_one_denominator_mul_two_pow_forty_nine_plus_one_below

/-- Negative denominator-`562949953421313`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_nine_plus_one_denominator_times_two_pow_forty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 562949953421313)
        562949953421313 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_forty_nine_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740976 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 102)))) * 562949953421313) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_forty_nine_plus_one_denominator_mul_two_pow_forty_nine_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_six_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`562949953421313`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_forty_nine_plus_one_denominator_times_two_pow_forty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 562949953421313)
          562949953421313 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 562949953421313)
          562949953421313 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_forty_nine_plus_one_denominator_times_two_pow_forty_nine_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_forty_nine_plus_one_denominator_times_two_pow_forty_nine_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`1125899906842625`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1125899906842625) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1125899906842625 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_plus_one
      (by linarith : (1 / 1125899906842625 : ℝ) ≤ ((2 : ℝ) ^ k) / 1125899906842625)
  · have hdiv_le : ((2 : ℝ) ^ k) / 1125899906842625 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`1125899906842625` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/1125899906842625` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1125899906842625) =
      (9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740984 (((k : ℤ) - 50))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740984 + 1) (((k : ℤ) - 50))
  let x : ℝ := ((2 : ℝ) ^ k) / 1125899906842625
  have hm : fmt.normalizedMantissa 9007199254740984 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740984 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740984, (((k : ℤ) - 50)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740985 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 103)) * (2 : ℝ) ^ (103 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 103)) (103 : ℤ)
    have hexp : ((k : ℤ) - 103) + 103 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (8 * ((2 : ℝ) ^ (((k : ℤ) - 103)))) / 1125899906842625 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (1125899906842617 * ((2 : ℝ) ^ (((k : ℤ) - 103)))) / 1125899906842625 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 103)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (8 * ((2 : ℝ) ^ (((k : ℤ) - 103)))) / 1125899906842625 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (1125899906842617 * ((2 : ℝ) ^ (((k : ℤ) - 103)))) / 1125899906842625 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`1125899906842625`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1125899906842625 =
      (9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1125899906842625) =
    (9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`1125899906842625`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_plus_one_exact_mul_two_pow_fifty_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1125899906842625) *
        1125899906842625 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_plus_one_denominator_mul_two_pow_fifty_plus_one_below

/-- Positive denominator-`1125899906842625`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_plus_one_denominator_times_two_pow_fifty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1125899906842625)
        1125899906842625 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103))) * 1125899906842625) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_plus_one_denominator_mul_two_pow_fifty_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_forty_eight_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`1125899906842625` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1125899906842625 =
      -((9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1125899906842625) =
    -((9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103)))
  have harg : (-((2 : ℝ) ^ k)) / 1125899906842625 =
      -(((2 : ℝ) ^ k) / 1125899906842625) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`1125899906842625` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_plus_one_exact_mul_two_pow_fifty_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1125899906842625) *
        1125899906842625 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 100))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_plus_one_denominator_mul_two_pow_fifty_plus_one_below

/-- Negative denominator-`1125899906842625`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_plus_one_denominator_times_two_pow_fifty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1125899906842625)
        1125899906842625 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740984 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 103)))) * 1125899906842625) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_plus_one_denominator_mul_two_pow_fifty_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_forty_eight_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`1125899906842625`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_plus_one_denominator_times_two_pow_fifty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1125899906842625)
          1125899906842625 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1125899906842625)
          1125899906842625 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_plus_one_denominator_times_two_pow_fifty_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_plus_one_denominator_times_two_pow_fifty_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`2251799813685249`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_one_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2251799813685249) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2251799813685249 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_one_plus_one
      (by linarith : (1 / 2251799813685249 : ℝ) ≤ ((2 : ℝ) ^ k) / 2251799813685249)
  · have hdiv_le : ((2 : ℝ) ^ k) / 2251799813685249 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`2251799813685249` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/2251799813685249` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_one_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2251799813685249) =
      (9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740988 (((k : ℤ) - 51))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740988 + 1) (((k : ℤ) - 51))
  let x : ℝ := ((2 : ℝ) ^ k) / 2251799813685249
  have hm : fmt.normalizedMantissa 9007199254740988 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740988 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740988, (((k : ℤ) - 51)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740989 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 104)) * (2 : ℝ) ^ (104 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 104)) (104 : ℤ)
    have hexp : ((k : ℤ) - 104) + 104 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (4 * ((2 : ℝ) ^ (((k : ℤ) - 104)))) / 2251799813685249 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (2251799813685245 * ((2 : ℝ) ^ (((k : ℤ) - 104)))) / 2251799813685249 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 104)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (4 * ((2 : ℝ) ^ (((k : ℤ) - 104)))) / 2251799813685249 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (2251799813685245 * ((2 : ℝ) ^ (((k : ℤ) - 104)))) / 2251799813685249 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_one_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`2251799813685249`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2251799813685249 =
      (9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2251799813685249) =
    (9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_one_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`2251799813685249`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_one_plus_one_exact_mul_two_pow_fifty_one_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2251799813685249) *
        2251799813685249 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_one_plus_one_denominator_mul_two_pow_fifty_one_plus_one_below

/-- Positive denominator-`2251799813685249`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_one_plus_one_denominator_times_two_pow_fifty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2251799813685249)
        2251799813685249 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104))) * 2251799813685249) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_one_plus_one_denominator_mul_two_pow_fifty_one_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`2251799813685249` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2251799813685249 =
      -((9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2251799813685249) =
    -((9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104)))
  have harg : (-((2 : ℝ) ^ k)) / 2251799813685249 =
      -(((2 : ℝ) ^ k) / 2251799813685249) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_one_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`2251799813685249` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_one_plus_one_exact_mul_two_pow_fifty_one_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2251799813685249) *
        2251799813685249 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 102))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_one_plus_one_denominator_mul_two_pow_fifty_one_plus_one_below

/-- Negative denominator-`2251799813685249`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_one_plus_one_denominator_times_two_pow_fifty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2251799813685249)
        2251799813685249 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740988 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 104)))) * 2251799813685249) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_one_plus_one_denominator_mul_two_pow_fifty_one_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_fifty_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`2251799813685249`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_one_plus_one_denominator_times_two_pow_fifty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2251799813685249)
          2251799813685249 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2251799813685249)
          2251799813685249 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_one_plus_one_denominator_times_two_pow_fifty_one_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_one_plus_one_denominator_times_two_pow_fifty_one_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`4503599627370497`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_two_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4503599627370497) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4503599627370497 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_two_plus_one
      (by linarith : (1 / 4503599627370497 : ℝ) ≤ ((2 : ℝ) ^ k) / 4503599627370497)
  · have hdiv_le : ((2 : ℝ) ^ k) / 4503599627370497 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`4503599627370497` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/4503599627370497` to the lower adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_two_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4503599627370497) =
      (9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false 9007199254740990 (((k : ℤ) - 52))
  let b : ℝ :=
    fmt.normalizedValue false (9007199254740990 + 1) (((k : ℤ) - 52))
  let x : ℝ := ((2 : ℝ) ^ k) / 4503599627370497
  have hm : fmt.normalizedMantissa 9007199254740990 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (9007199254740990 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 9007199254740990, (((k : ℤ) - 52)), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105)) := by
    simp [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hb_value :
      b = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105)) := by
    simp [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR]
    ring_nf
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 105)) * (2 : ℝ) ^ (105 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 105)) (105 : ℤ)
    have hexp : ((k : ℤ) - 105) + 105 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = (2 * ((2 : ℝ) ^ (((k : ℤ) - 105)))) / 4503599627370497 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x = (4503599627370495 * ((2 : ℝ) ^ (((k : ℤ) - 105)))) / 4503599627370497 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 105)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos : 0 < (2 * ((2 : ℝ) ^ (((k : ℤ) - 105)))) / 4503599627370497 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos : 0 < (4503599627370495 * ((2 : ℝ) ^ (((k : ℤ) - 105)))) / 4503599627370497 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_two_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`4503599627370497`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4503599627370497 =
      (9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4503599627370497) =
    (9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_two_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`4503599627370497`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_two_plus_one_exact_mul_two_pow_fifty_two_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4503599627370497) *
        4503599627370497 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_two_plus_one_denominator_mul_two_pow_fifty_two_plus_one_below

/-- Positive denominator-`4503599627370497`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_two_plus_one_denominator_times_two_pow_fifty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4503599627370497)
        4503599627370497 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105))) * 4503599627370497) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_two_plus_one_denominator_mul_two_pow_fifty_two_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_two_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`4503599627370497` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4503599627370497 =
      -((9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4503599627370497) =
    -((9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105)))
  have harg : (-((2 : ℝ) ^ k)) / 4503599627370497 =
      -(((2 : ℝ) ^ k) / 4503599627370497) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_two_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`4503599627370497` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_two_plus_one_exact_mul_two_pow_fifty_two_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4503599627370497) *
        4503599627370497 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 104))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_two_plus_one_denominator_mul_two_pow_fifty_two_plus_one_below

/-- Negative denominator-`4503599627370497`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_two_plus_one_denominator_times_two_pow_fifty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4503599627370497)
        4503599627370497 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740990 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 105)))) * 4503599627370497) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_two_plus_one_denominator_mul_two_pow_fifty_two_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_fifty_two_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`4503599627370497`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_two_plus_one_denominator_times_two_pow_fifty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4503599627370497)
          4503599627370497 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4503599627370497)
          4503599627370497 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_two_plus_one_denominator_times_two_pow_fifty_two_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_two_plus_one_denominator_times_two_pow_fifty_two_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`9007199254740993`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_three_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 9007199254740993) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 9007199254740993 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_three_plus_one
      (by linarith :
        (1 / 9007199254740993 : ℝ) ≤ ((2 : ℝ) ^ k) / 9007199254740993)
  · have hdiv_le : ((2 : ℝ) ^ k) / 9007199254740993 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`9007199254740993` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/9007199254740993` to the lower boundary-adjacent
endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_three_plus_one_denominator_rounds_to_lower
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9007199254740993) =
      (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 53))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 53) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 9007199254740993
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 53)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 53 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 106)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 106)) (1 : ℤ)
      have hexp : ((k : ℤ) - 106) + 1 = (k : ℤ) - 53 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 106)) * (2 : ℝ) ^ (106 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 106)) (106 : ℤ)
    have hexp : ((k : ℤ) - 106) + 106 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a = ((2 : ℝ) ^ (((k : ℤ) - 106))) / 9007199254740993 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 106)))) /
          9007199254740993 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 106)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 < ((2 : ℝ) ^ (((k : ℤ) - 106))) / 9007199254740993 :=
      div_pos hzpos (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 106)))) /
            9007199254740993 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_three_plus_one_finiteNormalRange hk)
  have hleftCloser : |x - a| < |x - b| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [abs_of_nonneg (le_of_lt hleft_pos), hxmb, abs_neg,
      abs_of_nonneg (le_of_lt hright_pos)]
    rw [hleft_dist, hright_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level wrapper for the first denominator-`9007199254740993`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9007199254740993 =
      (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9007199254740993) =
    (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_three_plus_one_denominator_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`9007199254740993`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_three_plus_one_exact_mul_two_pow_fifty_three_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9007199254740993) *
        9007199254740993 =
      (2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_three_plus_one_denominator_mul_two_pow_fifty_three_plus_one_below

/-- Positive denominator-`9007199254740993`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_three_plus_one_denominator_times_two_pow_fifty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9007199254740993)
        9007199254740993 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106))) * 9007199254740993) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_three_plus_one_denominator_mul_two_pow_fifty_three_plus_one_below]
  exact
    problem2_10_ieeeDouble_two_pow_minus_one_two_pow_fifty_four_ulp_rounds_to_two_pow
      (by omega : k ≤ 1023)

/-- Signed companion of the denominator-`9007199254740993` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9007199254740993 =
      -((9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 9007199254740993) =
    -((9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106)))
  have harg : (-((2 : ℝ) ^ k)) / 9007199254740993 =
      -(((2 : ℝ) ^ k) / 9007199254740993) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_three_plus_one_denominator_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`9007199254740993` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_three_plus_one_exact_mul_two_pow_fifty_three_plus_one_below
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9007199254740993) *
        9007199254740993 =
      -((2 : ℝ) ^ k - (2 : ℝ) ^ (((k : ℤ) - 106))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_three_plus_one_denominator_mul_two_pow_fifty_three_plus_one_below

/-- Negative denominator-`9007199254740993`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_three_plus_one_denominator_times_two_pow_fifty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9007199254740993)
        9007199254740993 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 106)))) * 9007199254740993) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_three_plus_one_denominator_mul_two_pow_fifty_three_plus_one_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_minus_one_two_pow_fifty_four_ulp_rounds_to_neg_two_pow
      (by omega : k ≤ 1023)

/-- Signed denominator-`9007199254740993`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_three_plus_one_denominator_times_two_pow_fifty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9007199254740993)
          9007199254740993 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9007199254740993)
          9007199254740993 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_three_plus_one_denominator_times_two_pow_fifty_three_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_three_plus_one_denominator_times_two_pow_fifty_three_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`18014398509481985`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_four_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 18014398509481985) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 18014398509481985 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_four_plus_one
      (by linarith :
        (1 / 18014398509481985 : ℝ) ≤
          ((2 : ℝ) ^ k) / 18014398509481985)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 18014398509481985 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`18014398509481985` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/18014398509481985` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_four_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 18014398509481985) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 54))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 54) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 18014398509481985
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 54)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 54 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 107)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 107)) (1 : ℤ)
      have hexp : ((k : ℤ) - 107) + 1 = (k : ℤ) - 54 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 107)) * (2 : ℝ) ^ (107 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 107)) (107 : ℤ)
    have hexp : ((k : ℤ) - 107) + 107 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (9007199254740993 * ((2 : ℝ) ^ (((k : ℤ) - 107)))) /
          18014398509481985 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 107)))) /
          18014398509481985 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 107)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (9007199254740993 * ((2 : ℝ) ^ (((k : ℤ) - 107)))) /
            18014398509481985 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 107)))) /
            18014398509481985 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_four_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`18014398509481985`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18014398509481985 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 18014398509481985) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_four_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`18014398509481985`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_four_plus_one_exact_mul_two_pow_fifty_four_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18014398509481985) *
        18014398509481985 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_four_plus_one_denominator_mul_two_pow_fifty_four_plus_one_above

/-- Positive denominator-`18014398509481985`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_four_plus_one_denominator_times_two_pow_fifty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18014398509481985)
        18014398509481985 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107))) *
        18014398509481985) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_four_plus_one_denominator_mul_two_pow_fifty_four_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`18014398509481985` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18014398509481985 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 18014398509481985) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107)))
  have harg : (-((2 : ℝ) ^ k)) / 18014398509481985 =
      -(((2 : ℝ) ^ k) / 18014398509481985) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_four_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`18014398509481985` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_four_plus_one_exact_mul_two_pow_fifty_four_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18014398509481985) *
        18014398509481985 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_four_plus_one_denominator_mul_two_pow_fifty_four_plus_one_above

/-- Negative denominator-`18014398509481985`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_four_plus_one_denominator_times_two_pow_fifty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18014398509481985)
        18014398509481985 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 107)))) *
        18014398509481985) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_four_plus_one_denominator_mul_two_pow_fifty_four_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`18014398509481985`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_four_plus_one_denominator_times_two_pow_fifty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18014398509481985)
          18014398509481985 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18014398509481985)
          18014398509481985 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_four_plus_one_denominator_times_two_pow_fifty_four_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_four_plus_one_denominator_times_two_pow_fifty_four_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`36028797018963969`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_five_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 36028797018963969) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 36028797018963969 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_five_plus_one
      (by linarith :
        (1 / 36028797018963969 : ℝ) ≤
          ((2 : ℝ) ^ k) / 36028797018963969)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 36028797018963969 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`36028797018963969` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/36028797018963969` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_five_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 36028797018963969) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 55))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 55) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 36028797018963969
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 55)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 55 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 108)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 108)) (1 : ℤ)
      have hexp : ((k : ℤ) - 108) + 1 = (k : ℤ) - 55 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 108)) * (2 : ℝ) ^ (108 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 108)) (108 : ℤ)
    have hexp : ((k : ℤ) - 108) + 108 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (27021597764222977 * ((2 : ℝ) ^ (((k : ℤ) - 108)))) /
          36028797018963969 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 108)))) /
          36028797018963969 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 108)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (27021597764222977 * ((2 : ℝ) ^ (((k : ℤ) - 108)))) /
            36028797018963969 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 108)))) /
            36028797018963969 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_five_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`36028797018963969`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36028797018963969 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 36028797018963969) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_five_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`36028797018963969`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_five_plus_one_exact_mul_two_pow_fifty_five_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36028797018963969) *
        36028797018963969 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_five_plus_one_denominator_mul_two_pow_fifty_five_plus_one_above

/-- Positive denominator-`36028797018963969`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_five_plus_one_denominator_times_two_pow_fifty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36028797018963969)
        36028797018963969 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108))) *
        36028797018963969) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_five_plus_one_denominator_mul_two_pow_fifty_five_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_eighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`36028797018963969` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36028797018963969 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 36028797018963969) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108)))
  have harg : (-((2 : ℝ) ^ k)) / 36028797018963969 =
      -(((2 : ℝ) ^ k) / 36028797018963969) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_five_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`36028797018963969` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_five_plus_one_exact_mul_two_pow_fifty_five_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36028797018963969) *
        36028797018963969 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_five_plus_one_denominator_mul_two_pow_fifty_five_plus_one_above

/-- Negative denominator-`36028797018963969`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_five_plus_one_denominator_times_two_pow_fifty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36028797018963969)
        36028797018963969 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 108)))) *
        36028797018963969) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_five_plus_one_denominator_mul_two_pow_fifty_five_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_eighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`36028797018963969`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_five_plus_one_denominator_times_two_pow_fifty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36028797018963969)
          36028797018963969 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36028797018963969)
          36028797018963969 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_five_plus_one_denominator_times_two_pow_fifty_five_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_five_plus_one_denominator_times_two_pow_fifty_five_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`72057594037927937`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_six_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 72057594037927937) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 72057594037927937 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_six_plus_one
      (by linarith :
        (1 / 72057594037927937 : ℝ) ≤
          ((2 : ℝ) ^ k) / 72057594037927937)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 72057594037927937 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`72057594037927937` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/72057594037927937` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_six_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 72057594037927937) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 56))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 56) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 72057594037927937
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 56)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 56 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 109)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 109)) (1 : ℤ)
      have hexp : ((k : ℤ) - 109) + 1 = (k : ℤ) - 56 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 109)) * (2 : ℝ) ^ (109 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 109)) (109 : ℤ)
    have hexp : ((k : ℤ) - 109) + 109 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (63050394783186945 * ((2 : ℝ) ^ (((k : ℤ) - 109)))) /
          72057594037927937 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 109)))) /
          72057594037927937 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 109)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (63050394783186945 * ((2 : ℝ) ^ (((k : ℤ) - 109)))) /
            72057594037927937 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 109)))) /
            72057594037927937 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_six_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`72057594037927937`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 72057594037927937 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 72057594037927937) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_six_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`72057594037927937`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_six_plus_one_exact_mul_two_pow_fifty_six_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 72057594037927937) *
        72057594037927937 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_six_plus_one_denominator_mul_two_pow_fifty_six_plus_one_above

/-- Positive denominator-`72057594037927937`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_six_plus_one_denominator_times_two_pow_fifty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 72057594037927937)
        72057594037927937 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109))) *
        72057594037927937) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_six_plus_one_denominator_mul_two_pow_fifty_six_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_sixteenth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`72057594037927937` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 72057594037927937 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 72057594037927937) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109)))
  have harg : (-((2 : ℝ) ^ k)) / 72057594037927937 =
      -(((2 : ℝ) ^ k) / 72057594037927937) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_six_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`72057594037927937` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_six_plus_one_exact_mul_two_pow_fifty_six_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 72057594037927937) *
        72057594037927937 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_six_plus_one_denominator_mul_two_pow_fifty_six_plus_one_above

/-- Negative denominator-`72057594037927937`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_six_plus_one_denominator_times_two_pow_fifty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 72057594037927937)
        72057594037927937 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 109)))) *
        72057594037927937) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_six_plus_one_denominator_mul_two_pow_fifty_six_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`72057594037927937`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_six_plus_one_denominator_times_two_pow_fifty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 72057594037927937)
          72057594037927937 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 72057594037927937)
          72057594037927937 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_six_plus_one_denominator_times_two_pow_fifty_six_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_six_plus_one_denominator_times_two_pow_fifty_six_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`144115188075855873`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_seven_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 144115188075855873) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 144115188075855873 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_seven_plus_one
      (by linarith :
        (1 / 144115188075855873 : ℝ) ≤
          ((2 : ℝ) ^ k) / 144115188075855873)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 144115188075855873 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`144115188075855873` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/144115188075855873` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_seven_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 144115188075855873) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 57))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 57) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 144115188075855873
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 57)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 57 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 110)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 110)) (1 : ℤ)
      have hexp : ((k : ℤ) - 110) + 1 = (k : ℤ) - 57 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 110)) * (2 : ℝ) ^ (110 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 110)) (110 : ℤ)
    have hexp : ((k : ℤ) - 110) + 110 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (135107988821114881 * ((2 : ℝ) ^ (((k : ℤ) - 110)))) /
          144115188075855873 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 110)))) /
          144115188075855873 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 110)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (135107988821114881 * ((2 : ℝ) ^ (((k : ℤ) - 110)))) /
            144115188075855873 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 110)))) /
            144115188075855873 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_seven_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`144115188075855873`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 144115188075855873 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 144115188075855873) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_seven_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`144115188075855873`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_seven_plus_one_exact_mul_two_pow_fifty_seven_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 144115188075855873) *
        144115188075855873 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_seven_plus_one_denominator_mul_two_pow_fifty_seven_plus_one_above

/-- Positive denominator-`144115188075855873`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_seven_plus_one_denominator_times_two_pow_fifty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 144115188075855873)
        144115188075855873 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110))) *
        144115188075855873) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_seven_plus_one_denominator_mul_two_pow_fifty_seven_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_thirtysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`144115188075855873` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 144115188075855873 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 144115188075855873) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110)))
  have harg : (-((2 : ℝ) ^ k)) / 144115188075855873 =
      -(((2 : ℝ) ^ k) / 144115188075855873) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_seven_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`144115188075855873` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_seven_plus_one_exact_mul_two_pow_fifty_seven_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 144115188075855873) *
        144115188075855873 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 57))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_seven_plus_one_denominator_mul_two_pow_fifty_seven_plus_one_above

/-- Negative denominator-`144115188075855873`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_seven_plus_one_denominator_times_two_pow_fifty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 144115188075855873)
        144115188075855873 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 110)))) *
        144115188075855873) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_seven_plus_one_denominator_mul_two_pow_fifty_seven_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`144115188075855873`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_seven_plus_one_denominator_times_two_pow_fifty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 144115188075855873)
          144115188075855873 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 144115188075855873)
          144115188075855873 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_seven_plus_one_denominator_times_two_pow_fifty_seven_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_seven_plus_one_denominator_times_two_pow_fifty_seven_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`288230376151711745`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_eight_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 288230376151711745) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 288230376151711745 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_eight_plus_one
      (by linarith :
        (1 / 288230376151711745 : ℝ) ≤
          ((2 : ℝ) ^ k) / 288230376151711745)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 288230376151711745 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`288230376151711745` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/288230376151711745` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_eight_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 288230376151711745) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 58))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 58) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 288230376151711745
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 58)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 58 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 111)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 111)) (1 : ℤ)
      have hexp : ((k : ℤ) - 111) + 1 = (k : ℤ) - 58 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 111)) * (2 : ℝ) ^ (111 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 111)) (111 : ℤ)
    have hexp : ((k : ℤ) - 111) + 111 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (279223176896970753 * ((2 : ℝ) ^ (((k : ℤ) - 111)))) /
          288230376151711745 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 111)))) /
          288230376151711745 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 111)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (279223176896970753 * ((2 : ℝ) ^ (((k : ℤ) - 111)))) /
            288230376151711745 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 111)))) /
            288230376151711745 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_eight_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`288230376151711745`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 288230376151711745 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 288230376151711745) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_eight_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`288230376151711745`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_eight_plus_one_exact_mul_two_pow_fifty_eight_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 288230376151711745) *
        288230376151711745 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_eight_plus_one_denominator_mul_two_pow_fifty_eight_plus_one_above

/-- Positive denominator-`288230376151711745`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_eight_plus_one_denominator_times_two_pow_fifty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 288230376151711745)
        288230376151711745 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111))) *
        288230376151711745) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_eight_plus_one_denominator_mul_two_pow_fifty_eight_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_sixtyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`288230376151711745` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 288230376151711745 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 288230376151711745) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111)))
  have harg : (-((2 : ℝ) ^ k)) / 288230376151711745 =
      -(((2 : ℝ) ^ k) / 288230376151711745) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_eight_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`288230376151711745` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_eight_plus_one_exact_mul_two_pow_fifty_eight_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 288230376151711745) *
        288230376151711745 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 58))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_eight_plus_one_denominator_mul_two_pow_fifty_eight_plus_one_above

/-- Negative denominator-`288230376151711745`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_eight_plus_one_denominator_times_two_pow_fifty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 288230376151711745)
        288230376151711745 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 111)))) *
        288230376151711745) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_eight_plus_one_denominator_mul_two_pow_fifty_eight_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`288230376151711745`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_eight_plus_one_denominator_times_two_pow_fifty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 288230376151711745)
          288230376151711745 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 288230376151711745)
          288230376151711745 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_eight_plus_one_denominator_times_two_pow_fifty_eight_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_eight_plus_one_denominator_times_two_pow_fifty_eight_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`576460752303423489`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_nine_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 576460752303423489) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 576460752303423489 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_fifty_nine_plus_one
      (by linarith :
        (1 / 576460752303423489 : ℝ) ≤
          ((2 : ℝ) ^ k) / 576460752303423489)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 576460752303423489 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`576460752303423489` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/576460752303423489` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_nine_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 576460752303423489) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 59))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 59) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 576460752303423489
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 59)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 59 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 112)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 112)) (1 : ℤ)
      have hexp : ((k : ℤ) - 112) + 1 = (k : ℤ) - 59 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 112)) * (2 : ℝ) ^ (112 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 112)) (112 : ℤ)
    have hexp : ((k : ℤ) - 112) + 112 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (567453553048682497 * ((2 : ℝ) ^ (((k : ℤ) - 112)))) /
          576460752303423489 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 112)))) /
          576460752303423489 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 112)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (567453553048682497 * ((2 : ℝ) ^ (((k : ℤ) - 112)))) /
            576460752303423489 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 112)))) /
            576460752303423489 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_fifty_nine_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`576460752303423489`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 576460752303423489 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 576460752303423489) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_fifty_nine_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`576460752303423489`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_nine_plus_one_exact_mul_two_pow_fifty_nine_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 576460752303423489) *
        576460752303423489 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_nine_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_nine_plus_one_denominator_mul_two_pow_fifty_nine_plus_one_above

/-- Positive denominator-`576460752303423489`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_fifty_nine_plus_one_denominator_times_two_pow_fifty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 576460752303423489)
        576460752303423489 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_fifty_nine_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112))) *
        576460752303423489) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_fifty_nine_plus_one_denominator_mul_two_pow_fifty_nine_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_onehundredtwentyeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`576460752303423489` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 576460752303423489 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 576460752303423489) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112)))
  have harg : (-((2 : ℝ) ^ k)) / 576460752303423489 =
      -(((2 : ℝ) ^ k) / 576460752303423489) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_fifty_nine_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`576460752303423489` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_nine_plus_one_exact_mul_two_pow_fifty_nine_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 576460752303423489) *
        576460752303423489 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 59))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_nine_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_nine_plus_one_denominator_mul_two_pow_fifty_nine_plus_one_above

/-- Negative denominator-`576460752303423489`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_nine_plus_one_denominator_times_two_pow_fifty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 576460752303423489)
        576460752303423489 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_fifty_nine_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 112)))) *
        576460752303423489) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_fifty_nine_plus_one_denominator_mul_two_pow_fifty_nine_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_onehundredtwentyeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`576460752303423489`, power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_fifty_nine_plus_one_denominator_times_two_pow_fifty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 576460752303423489)
          576460752303423489 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 576460752303423489)
          576460752303423489 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_fifty_nine_plus_one_denominator_times_two_pow_fifty_nine_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_fifty_nine_plus_one_denominator_times_two_pow_fifty_nine_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`1152921504606846977`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1152921504606846977) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1152921504606846977 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_plus_one
      (by linarith :
        (1 / 1152921504606846977 : ℝ) ≤
          ((2 : ℝ) ^ k) / 1152921504606846977)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 1152921504606846977 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`1152921504606846977` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/1152921504606846977` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1152921504606846977) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 60))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 60) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 1152921504606846977
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 60)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 60 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 113)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 113)) (1 : ℤ)
      have hexp : ((k : ℤ) - 113) + 1 = (k : ℤ) - 60 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 113)) * (2 : ℝ) ^ (113 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 113)) (113 : ℤ)
    have hexp : ((k : ℤ) - 113) + 113 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (1143914305352105985 * ((2 : ℝ) ^ (((k : ℤ) - 113)))) /
          1152921504606846977 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 113)))) /
          1152921504606846977 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 113)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (1143914305352105985 * ((2 : ℝ) ^ (((k : ℤ) - 113)))) /
            1152921504606846977 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 113)))) /
            1152921504606846977 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`1152921504606846977`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1152921504606846977 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1152921504606846977) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`1152921504606846977`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_plus_one_exact_mul_two_pow_sixty_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1152921504606846977) *
        1152921504606846977 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_plus_one_denominator_mul_two_pow_sixty_plus_one_above

/-- Positive denominator-`1152921504606846977`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_plus_one_denominator_times_two_pow_sixty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1152921504606846977)
        1152921504606846977 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113))) *
        1152921504606846977) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_plus_one_denominator_mul_two_pow_sixty_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`1152921504606846977` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1152921504606846977 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1152921504606846977) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113)))
  have harg : (-((2 : ℝ) ^ k)) / 1152921504606846977 =
      -(((2 : ℝ) ^ k) / 1152921504606846977) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`1152921504606846977` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_plus_one_exact_mul_two_pow_sixty_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1152921504606846977) *
        1152921504606846977 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 60))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_plus_one_denominator_mul_two_pow_sixty_plus_one_above

/-- Negative denominator-`1152921504606846977`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_plus_one_denominator_times_two_pow_sixty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1152921504606846977)
        1152921504606846977 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 113)))) *
        1152921504606846977) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_plus_one_denominator_mul_two_pow_sixty_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_twohundredfiftysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`1152921504606846977`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_plus_one_denominator_times_two_pow_sixty_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1152921504606846977)
          1152921504606846977 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1152921504606846977)
          1152921504606846977 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_plus_one_denominator_times_two_pow_sixty_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_plus_one_denominator_times_two_pow_sixty_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`2305843009213693953`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_one_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2305843009213693953) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2305843009213693953 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_one_plus_one
      (by linarith :
        (1 / 2305843009213693953 : ℝ) ≤
          ((2 : ℝ) ^ k) / 2305843009213693953)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 2305843009213693953 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`2305843009213693953` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/2305843009213693953` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_one_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2305843009213693953) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 61))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 61) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 2305843009213693953
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 61)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 61 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 114)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 114)) (1 : ℤ)
      have hexp : ((k : ℤ) - 114) + 1 = (k : ℤ) - 61 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 114)) * (2 : ℝ) ^ (114 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 114)) (114 : ℤ)
    have hexp : ((k : ℤ) - 114) + 114 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (2296835809958952961 * ((2 : ℝ) ^ (((k : ℤ) - 114)))) /
          2305843009213693953 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 114)))) /
          2305843009213693953 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 114)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (2296835809958952961 * ((2 : ℝ) ^ (((k : ℤ) - 114)))) /
            2305843009213693953 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 114)))) /
            2305843009213693953 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_one_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`2305843009213693953`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2305843009213693953 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2305843009213693953) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_one_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`2305843009213693953`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_one_plus_one_exact_mul_two_pow_sixty_one_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2305843009213693953) *
        2305843009213693953 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_one_plus_one_denominator_mul_two_pow_sixty_one_plus_one_above

/-- Positive denominator-`2305843009213693953`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_one_plus_one_denominator_times_two_pow_sixty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2305843009213693953)
        2305843009213693953 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114))) *
        2305843009213693953) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_one_plus_one_denominator_mul_two_pow_sixty_one_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwelfth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`2305843009213693953` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2305843009213693953 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2305843009213693953) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114)))
  have harg : (-((2 : ℝ) ^ k)) / 2305843009213693953 =
      -(((2 : ℝ) ^ k) / 2305843009213693953) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_one_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`2305843009213693953` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_one_plus_one_exact_mul_two_pow_sixty_one_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2305843009213693953) *
        2305843009213693953 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 61))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_one_plus_one_denominator_mul_two_pow_sixty_one_plus_one_above

/-- Negative denominator-`2305843009213693953`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_one_plus_one_denominator_times_two_pow_sixty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2305843009213693953)
        2305843009213693953 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 114)))) *
        2305843009213693953) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_one_plus_one_denominator_mul_two_pow_sixty_one_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_fivehundredtwelfth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`2305843009213693953`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_one_plus_one_denominator_times_two_pow_sixty_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2305843009213693953)
          2305843009213693953 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2305843009213693953)
          2305843009213693953 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_one_plus_one_denominator_times_two_pow_sixty_one_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_one_plus_one_denominator_times_two_pow_sixty_one_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`4611686018427387905`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_two_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4611686018427387905) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4611686018427387905 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_two_plus_one
      (by linarith :
        (1 / 4611686018427387905 : ℝ) ≤
          ((2 : ℝ) ^ k) / 4611686018427387905)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 4611686018427387905 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`4611686018427387905` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/4611686018427387905` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_two_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4611686018427387905) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 62))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 62) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 4611686018427387905
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 62)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 62 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 115)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 115)) (1 : ℤ)
      have hexp : ((k : ℤ) - 115) + 1 = (k : ℤ) - 62 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 115)) * (2 : ℝ) ^ (115 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 115)) (115 : ℤ)
    have hexp : ((k : ℤ) - 115) + 115 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (4602678819172646913 * ((2 : ℝ) ^ (((k : ℤ) - 115)))) /
          4611686018427387905 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 115)))) /
          4611686018427387905 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 115)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (4602678819172646913 * ((2 : ℝ) ^ (((k : ℤ) - 115)))) /
            4611686018427387905 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 115)))) /
            4611686018427387905 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_two_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`4611686018427387905`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4611686018427387905 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4611686018427387905) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_two_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`4611686018427387905`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_two_plus_one_exact_mul_two_pow_sixty_two_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4611686018427387905) *
        4611686018427387905 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_two_plus_one_denominator_mul_two_pow_sixty_two_plus_one_above

/-- Positive denominator-`4611686018427387905`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_two_plus_one_denominator_times_two_pow_sixty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4611686018427387905)
        4611686018427387905 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115))) *
        4611686018427387905) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_two_plus_one_denominator_mul_two_pow_sixty_two_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_onethousandtwentyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`4611686018427387905` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4611686018427387905 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4611686018427387905) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115)))
  have harg : (-((2 : ℝ) ^ k)) / 4611686018427387905 =
      -(((2 : ℝ) ^ k) / 4611686018427387905) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_two_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`4611686018427387905` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_two_plus_one_exact_mul_two_pow_sixty_two_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4611686018427387905) *
        4611686018427387905 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 62))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_two_plus_one_denominator_mul_two_pow_sixty_two_plus_one_above

/-- Negative denominator-`4611686018427387905`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_two_plus_one_denominator_times_two_pow_sixty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4611686018427387905)
        4611686018427387905 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 115)))) *
        4611686018427387905) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_two_plus_one_denominator_mul_two_pow_sixty_two_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_onethousandtwentyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`4611686018427387905`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_two_plus_one_denominator_times_two_pow_sixty_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4611686018427387905)
          4611686018427387905 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4611686018427387905)
          4611686018427387905 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_two_plus_one_denominator_times_two_pow_sixty_two_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_two_plus_one_denominator_times_two_pow_sixty_two_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`9223372036854775809`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_three_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 9223372036854775809) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 9223372036854775809 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_three_plus_one
      (by linarith :
        (1 / 9223372036854775809 : ℝ) ≤
          ((2 : ℝ) ^ k) / 9223372036854775809)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 9223372036854775809 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`9223372036854775809` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/9223372036854775809` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_three_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9223372036854775809) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 63))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 63) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 9223372036854775809
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 63)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 63 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 116)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 116)) (1 : ℤ)
      have hexp : ((k : ℤ) - 116) + 1 = (k : ℤ) - 63 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 116)) * (2 : ℝ) ^ (116 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 116)) (116 : ℤ)
    have hexp : ((k : ℤ) - 116) + 116 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (9214364837600034817 * ((2 : ℝ) ^ (((k : ℤ) - 116)))) /
          9223372036854775809 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 116)))) /
          9223372036854775809 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 116)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (9214364837600034817 * ((2 : ℝ) ^ (((k : ℤ) - 116)))) /
            9223372036854775809 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 116)))) /
            9223372036854775809 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_three_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`9223372036854775809`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9223372036854775809 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9223372036854775809) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_three_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`9223372036854775809`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_three_plus_one_exact_mul_two_pow_sixty_three_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9223372036854775809) *
        9223372036854775809 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_three_plus_one_denominator_mul_two_pow_sixty_three_plus_one_above

/-- Positive denominator-`9223372036854775809`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_three_plus_one_denominator_times_two_pow_sixty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9223372036854775809)
        9223372036854775809 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116))) *
        9223372036854775809) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_three_plus_one_denominator_mul_two_pow_sixty_three_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`9223372036854775809` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9223372036854775809 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 9223372036854775809) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116)))
  have harg : (-((2 : ℝ) ^ k)) / 9223372036854775809 =
      -(((2 : ℝ) ^ k) / 9223372036854775809) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_three_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`9223372036854775809` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_three_plus_one_exact_mul_two_pow_sixty_three_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9223372036854775809) *
        9223372036854775809 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 63))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_three_plus_one_denominator_mul_two_pow_sixty_three_plus_one_above

/-- Negative denominator-`9223372036854775809`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_three_plus_one_denominator_times_two_pow_sixty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9223372036854775809)
        9223372036854775809 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 116)))) *
        9223372036854775809) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_three_plus_one_denominator_mul_two_pow_sixty_three_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_twothousandfortyeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`9223372036854775809`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_three_plus_one_denominator_times_two_pow_sixty_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9223372036854775809)
          9223372036854775809 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9223372036854775809)
          9223372036854775809 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_three_plus_one_denominator_times_two_pow_sixty_three_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_three_plus_one_denominator_times_two_pow_sixty_three_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`18446744073709551617`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_four_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 18446744073709551617) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 18446744073709551617 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_four_plus_one
      (by linarith :
        (1 / 18446744073709551617 : ℝ) ≤
          ((2 : ℝ) ^ k) / 18446744073709551617)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 18446744073709551617 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`18446744073709551617` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/18446744073709551617` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_four_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 18446744073709551617) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 64))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 64) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 18446744073709551617
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 64)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 64 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 117)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 117)) (1 : ℤ)
      have hexp : ((k : ℤ) - 117) + 1 = (k : ℤ) - 64 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 117)) * (2 : ℝ) ^ (117 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 117)) (117 : ℤ)
    have hexp : ((k : ℤ) - 117) + 117 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (18437736874454810625 * ((2 : ℝ) ^ (((k : ℤ) - 117)))) /
          18446744073709551617 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 117)))) /
          18446744073709551617 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 117)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (18437736874454810625 * ((2 : ℝ) ^ (((k : ℤ) - 117)))) /
            18446744073709551617 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 117)))) /
            18446744073709551617 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_four_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`18446744073709551617`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18446744073709551617 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 18446744073709551617) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_four_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`18446744073709551617`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_four_plus_one_exact_mul_two_pow_sixty_four_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18446744073709551617) *
        18446744073709551617 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_four_plus_one_denominator_mul_two_pow_sixty_four_plus_one_above

/-- Positive denominator-`18446744073709551617`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_four_plus_one_denominator_times_two_pow_sixty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18446744073709551617)
        18446744073709551617 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117))) *
        18446744073709551617) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_four_plus_one_denominator_mul_two_pow_sixty_four_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_fourthousandninetysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`18446744073709551617` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18446744073709551617 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 18446744073709551617) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117)))
  have harg : (-((2 : ℝ) ^ k)) / 18446744073709551617 =
      -(((2 : ℝ) ^ k) / 18446744073709551617) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_four_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`18446744073709551617` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_four_plus_one_exact_mul_two_pow_sixty_four_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18446744073709551617) *
        18446744073709551617 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 64))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_four_plus_one_denominator_mul_two_pow_sixty_four_plus_one_above

/-- Negative denominator-`18446744073709551617`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_four_plus_one_denominator_times_two_pow_sixty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18446744073709551617)
        18446744073709551617 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 117)))) *
        18446744073709551617) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_four_plus_one_denominator_mul_two_pow_sixty_four_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_fourthousandninetysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`18446744073709551617`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_four_plus_one_denominator_times_two_pow_sixty_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18446744073709551617)
          18446744073709551617 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18446744073709551617)
          18446744073709551617 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_four_plus_one_denominator_times_two_pow_sixty_four_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_four_plus_one_denominator_times_two_pow_sixty_four_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`36893488147419103233`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_five_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 36893488147419103233) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 36893488147419103233 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_five_plus_one
      (by linarith :
        (1 / 36893488147419103233 : ℝ) ≤
          ((2 : ℝ) ^ k) / 36893488147419103233)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 36893488147419103233 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`36893488147419103233` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/36893488147419103233` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_five_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 36893488147419103233) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 65))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 65) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 36893488147419103233
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 65)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 65 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 118)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 118)) (1 : ℤ)
      have hexp : ((k : ℤ) - 118) + 1 = (k : ℤ) - 65 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 118)) * (2 : ℝ) ^ (118 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 118)) (118 : ℤ)
    have hexp : ((k : ℤ) - 118) + 118 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (36884480948164362241 * ((2 : ℝ) ^ (((k : ℤ) - 118)))) /
          36893488147419103233 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 118)))) /
          36893488147419103233 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 118)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (36884480948164362241 * ((2 : ℝ) ^ (((k : ℤ) - 118)))) /
            36893488147419103233 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 118)))) /
            36893488147419103233 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_five_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`36893488147419103233`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36893488147419103233 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 36893488147419103233) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_five_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`36893488147419103233`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_five_plus_one_exact_mul_two_pow_sixty_five_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36893488147419103233) *
        36893488147419103233 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_five_plus_one_denominator_mul_two_pow_sixty_five_plus_one_above

/-- Positive denominator-`36893488147419103233`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_five_plus_one_denominator_times_two_pow_sixty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36893488147419103233)
        36893488147419103233 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118))) *
        36893488147419103233) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_five_plus_one_denominator_mul_two_pow_sixty_five_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`36893488147419103233` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36893488147419103233 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 36893488147419103233) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118)))
  have harg : (-((2 : ℝ) ^ k)) / 36893488147419103233 =
      -(((2 : ℝ) ^ k) / 36893488147419103233) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_five_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`36893488147419103233` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_five_plus_one_exact_mul_two_pow_sixty_five_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36893488147419103233) *
        36893488147419103233 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 65))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_five_plus_one_denominator_mul_two_pow_sixty_five_plus_one_above

/-- Negative denominator-`36893488147419103233`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_five_plus_one_denominator_times_two_pow_sixty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36893488147419103233)
        36893488147419103233 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 118)))) *
        36893488147419103233) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_five_plus_one_denominator_mul_two_pow_sixty_five_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_eightthousandonehundredninetysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`36893488147419103233`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_five_plus_one_denominator_times_two_pow_sixty_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 36893488147419103233)
          36893488147419103233 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 36893488147419103233)
          36893488147419103233 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_five_plus_one_denominator_times_two_pow_sixty_five_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_five_plus_one_denominator_times_two_pow_sixty_five_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`73786976294838206465`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_six_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 73786976294838206465) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 73786976294838206465 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_six_plus_one
      (by linarith :
        (1 / 73786976294838206465 : ℝ) ≤
          ((2 : ℝ) ^ k) / 73786976294838206465)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 73786976294838206465 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`73786976294838206465` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/73786976294838206465` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_six_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 73786976294838206465) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 66))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 66) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 73786976294838206465
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 66)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 66 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 119)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 119)) (1 : ℤ)
      have hexp : ((k : ℤ) - 119) + 1 = (k : ℤ) - 66 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 119)) * (2 : ℝ) ^ (119 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 119)) (119 : ℤ)
    have hexp : ((k : ℤ) - 119) + 119 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (73777969095583465473 * ((2 : ℝ) ^ (((k : ℤ) - 119)))) /
          73786976294838206465 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 119)))) /
          73786976294838206465 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 119)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (73777969095583465473 * ((2 : ℝ) ^ (((k : ℤ) - 119)))) /
            73786976294838206465 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 119)))) /
            73786976294838206465 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_six_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`73786976294838206465`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 73786976294838206465 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 73786976294838206465) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_six_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`73786976294838206465`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_six_plus_one_exact_mul_two_pow_sixty_six_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 73786976294838206465) *
        73786976294838206465 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_six_plus_one_denominator_mul_two_pow_sixty_six_plus_one_above

/-- Positive denominator-`73786976294838206465`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_six_plus_one_denominator_times_two_pow_sixty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 73786976294838206465)
        73786976294838206465 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119))) *
        73786976294838206465) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_six_plus_one_denominator_mul_two_pow_sixty_six_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`73786976294838206465` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 73786976294838206465 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 73786976294838206465) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119)))
  have harg : (-((2 : ℝ) ^ k)) / 73786976294838206465 =
      -(((2 : ℝ) ^ k) / 73786976294838206465) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_six_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`73786976294838206465` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_six_plus_one_exact_mul_two_pow_sixty_six_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 73786976294838206465) *
        73786976294838206465 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 66))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_six_plus_one_denominator_mul_two_pow_sixty_six_plus_one_above

/-- Negative denominator-`73786976294838206465`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_six_plus_one_denominator_times_two_pow_sixty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 73786976294838206465)
        73786976294838206465 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 119)))) *
        73786976294838206465) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_six_plus_one_denominator_mul_two_pow_sixty_six_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenthousandthreehundredeightyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`73786976294838206465`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_six_plus_one_denominator_times_two_pow_sixty_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 73786976294838206465)
          73786976294838206465 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 73786976294838206465)
          73786976294838206465 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_six_plus_one_denominator_times_two_pow_sixty_six_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_six_plus_one_denominator_times_two_pow_sixty_six_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`147573952589676412929`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_seven_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 147573952589676412929) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 147573952589676412929 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_seven_plus_one
      (by linarith :
        (1 / 147573952589676412929 : ℝ) ≤
          ((2 : ℝ) ^ k) / 147573952589676412929)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 147573952589676412929 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`147573952589676412929` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/147573952589676412929` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_seven_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 147573952589676412929) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 67))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 67) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 147573952589676412929
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 67)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 67 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 120)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 120)) (1 : ℤ)
      have hexp : ((k : ℤ) - 120) + 1 = (k : ℤ) - 67 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 120)) * (2 : ℝ) ^ (120 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 120)) (120 : ℤ)
    have hexp : ((k : ℤ) - 120) + 120 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (147564945390421671937 * ((2 : ℝ) ^ (((k : ℤ) - 120)))) /
          147573952589676412929 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 120)))) /
          147573952589676412929 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 120)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (147564945390421671937 * ((2 : ℝ) ^ (((k : ℤ) - 120)))) /
            147573952589676412929 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 120)))) /
            147573952589676412929 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_seven_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`147573952589676412929`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 147573952589676412929 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 147573952589676412929) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_seven_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`147573952589676412929`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_seven_plus_one_exact_mul_two_pow_sixty_seven_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 147573952589676412929) *
        147573952589676412929 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_seven_plus_one_denominator_mul_two_pow_sixty_seven_plus_one_above

/-- Positive denominator-`147573952589676412929`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_seven_plus_one_denominator_times_two_pow_sixty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 147573952589676412929)
        147573952589676412929 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120))) *
        147573952589676412929) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_seven_plus_one_denominator_mul_two_pow_sixty_seven_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`147573952589676412929` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 147573952589676412929 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 147573952589676412929) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120)))
  have harg : (-((2 : ℝ) ^ k)) / 147573952589676412929 =
      -(((2 : ℝ) ^ k) / 147573952589676412929) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_seven_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`147573952589676412929` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_seven_plus_one_exact_mul_two_pow_sixty_seven_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 147573952589676412929) *
        147573952589676412929 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 67))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_seven_plus_one_denominator_mul_two_pow_sixty_seven_plus_one_above

/-- Negative denominator-`147573952589676412929`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_seven_plus_one_denominator_times_two_pow_sixty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 147573952589676412929)
        147573952589676412929 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 120)))) *
        147573952589676412929) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_seven_plus_one_denominator_mul_two_pow_sixty_seven_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtytwothousandsevenhundredsixtyeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`147573952589676412929`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_seven_plus_one_denominator_times_two_pow_sixty_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 147573952589676412929)
          147573952589676412929 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 147573952589676412929)
          147573952589676412929 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_seven_plus_one_denominator_times_two_pow_sixty_seven_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_seven_plus_one_denominator_times_two_pow_sixty_seven_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`295147905179352825857`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_eight_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 295147905179352825857) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 295147905179352825857 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_eight_plus_one
      (by linarith :
        (1 / 295147905179352825857 : ℝ) ≤
          ((2 : ℝ) ^ k) / 295147905179352825857)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 295147905179352825857 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`295147905179352825857` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/295147905179352825857` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_eight_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 295147905179352825857) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 68))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 68) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 295147905179352825857
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 68)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 68 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 121)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 121)) (1 : ℤ)
      have hexp : ((k : ℤ) - 121) + 1 = (k : ℤ) - 68 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 121)) * (2 : ℝ) ^ (121 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 121)) (121 : ℤ)
    have hexp : ((k : ℤ) - 121) + 121 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (295138897980098084865 * ((2 : ℝ) ^ (((k : ℤ) - 121)))) /
          295147905179352825857 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 121)))) /
          295147905179352825857 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 121)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (295138897980098084865 * ((2 : ℝ) ^ (((k : ℤ) - 121)))) /
            295147905179352825857 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 121)))) /
            295147905179352825857 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_eight_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`295147905179352825857`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 295147905179352825857 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 295147905179352825857) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_eight_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`295147905179352825857`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_eight_plus_one_exact_mul_two_pow_sixty_eight_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 295147905179352825857) *
        295147905179352825857 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_eight_plus_one_denominator_mul_two_pow_sixty_eight_plus_one_above

/-- Positive denominator-`295147905179352825857`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_eight_plus_one_denominator_times_two_pow_sixty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 295147905179352825857)
        295147905179352825857 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121))) *
        295147905179352825857) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_eight_plus_one_denominator_mul_two_pow_sixty_eight_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`295147905179352825857` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 295147905179352825857 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 295147905179352825857) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121)))
  have harg : (-((2 : ℝ) ^ k)) / 295147905179352825857 =
      -(((2 : ℝ) ^ k) / 295147905179352825857) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_eight_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`295147905179352825857` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_eight_plus_one_exact_mul_two_pow_sixty_eight_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 295147905179352825857) *
        295147905179352825857 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 68))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_eight_plus_one_denominator_mul_two_pow_sixty_eight_plus_one_above

/-- Negative denominator-`295147905179352825857`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_eight_plus_one_denominator_times_two_pow_sixty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 295147905179352825857)
        295147905179352825857 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 121)))) *
        295147905179352825857) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_eight_plus_one_denominator_mul_two_pow_sixty_eight_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtyfivethousandfivehundredthirtysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`295147905179352825857`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_eight_plus_one_denominator_times_two_pow_sixty_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 295147905179352825857)
          295147905179352825857 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 295147905179352825857)
          295147905179352825857 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_eight_plus_one_denominator_times_two_pow_sixty_eight_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_eight_plus_one_denominator_times_two_pow_sixty_eight_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`590295810358705651713`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_nine_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 590295810358705651713) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 590295810358705651713 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_sixty_nine_plus_one
      (by linarith :
        (1 / 590295810358705651713 : ℝ) ≤
          ((2 : ℝ) ^ k) / 590295810358705651713)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 590295810358705651713 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`590295810358705651713` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/590295810358705651713` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_nine_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 590295810358705651713) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 69))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 69) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 590295810358705651713
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 69)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 69 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 122)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 122)) (1 : ℤ)
      have hexp : ((k : ℤ) - 122) + 1 = (k : ℤ) - 69 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 122)) * (2 : ℝ) ^ (122 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 122)) (122 : ℤ)
    have hexp : ((k : ℤ) - 122) + 122 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (590286803159450910721 * ((2 : ℝ) ^ (((k : ℤ) - 122)))) /
          590295810358705651713 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 122)))) /
          590295810358705651713 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 122)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (590286803159450910721 * ((2 : ℝ) ^ (((k : ℤ) - 122)))) /
            590295810358705651713 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 122)))) /
            590295810358705651713 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_sixty_nine_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`590295810358705651713`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 590295810358705651713 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 590295810358705651713) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_sixty_nine_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`590295810358705651713`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_nine_plus_one_exact_mul_two_pow_sixty_nine_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 590295810358705651713) *
        590295810358705651713 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_nine_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_nine_plus_one_denominator_mul_two_pow_sixty_nine_plus_one_above

/-- Positive denominator-`590295810358705651713`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_sixty_nine_plus_one_denominator_times_two_pow_sixty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 590295810358705651713)
        590295810358705651713 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_sixty_nine_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122))) *
        590295810358705651713) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_sixty_nine_plus_one_denominator_mul_two_pow_sixty_nine_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`590295810358705651713` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 590295810358705651713 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 590295810358705651713) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122)))
  have harg : (-((2 : ℝ) ^ k)) / 590295810358705651713 =
      -(((2 : ℝ) ^ k) / 590295810358705651713) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_sixty_nine_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`590295810358705651713` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_nine_plus_one_exact_mul_two_pow_sixty_nine_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 590295810358705651713) *
        590295810358705651713 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 69))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_nine_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_nine_plus_one_denominator_mul_two_pow_sixty_nine_plus_one_above

/-- Negative denominator-`590295810358705651713`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_nine_plus_one_denominator_times_two_pow_sixty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 590295810358705651713)
        590295810358705651713 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_sixty_nine_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 122)))) *
        590295810358705651713) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_sixty_nine_plus_one_denominator_mul_two_pow_sixty_nine_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_onehundredthirtyonethousandseventysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`590295810358705651713`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_sixty_nine_plus_one_denominator_times_two_pow_sixty_nine_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 590295810358705651713)
          590295810358705651713 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 590295810358705651713)
          590295810358705651713 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_sixty_nine_plus_one_denominator_times_two_pow_sixty_nine_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_sixty_nine_plus_one_denominator_times_two_pow_sixty_nine_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`1180591620717411303425`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 1180591620717411303425) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 1180591620717411303425 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_plus_one
      (by linarith :
        (1 / 1180591620717411303425 : ℝ) ≤
          ((2 : ℝ) ^ k) / 1180591620717411303425)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 1180591620717411303425 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`1180591620717411303425` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/1180591620717411303425` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1180591620717411303425) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 70))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 70) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 1180591620717411303425
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 70)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 70 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 123)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 123)) (1 : ℤ)
      have hexp : ((k : ℤ) - 123) + 1 = (k : ℤ) - 70 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 123)) * (2 : ℝ) ^ (123 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 123)) (123 : ℤ)
    have hexp : ((k : ℤ) - 123) + 123 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (1180582613518156562433 * ((2 : ℝ) ^ (((k : ℤ) - 123)))) /
          1180591620717411303425 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 123)))) /
          1180591620717411303425 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 123)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (1180582613518156562433 * ((2 : ℝ) ^ (((k : ℤ) - 123)))) /
            1180591620717411303425 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 123)))) /
            1180591620717411303425 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`1180591620717411303425`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1180591620717411303425 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 1180591620717411303425) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`1180591620717411303425`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_plus_one_exact_mul_two_pow_seventy_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1180591620717411303425) *
        1180591620717411303425 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_plus_one_denominator_mul_two_pow_seventy_plus_one_above

/-- Positive denominator-`1180591620717411303425`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_plus_one_denominator_times_two_pow_seventy_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1180591620717411303425)
        1180591620717411303425 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123))) *
        1180591620717411303425) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_plus_one_denominator_mul_two_pow_seventy_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`1180591620717411303425` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1180591620717411303425 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 1180591620717411303425) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123)))
  have harg : (-((2 : ℝ) ^ k)) / 1180591620717411303425 =
      -(((2 : ℝ) ^ k) / 1180591620717411303425) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`1180591620717411303425` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_plus_one_exact_mul_two_pow_seventy_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1180591620717411303425) *
        1180591620717411303425 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 70))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_plus_one_denominator_mul_two_pow_seventy_plus_one_above

/-- Negative denominator-`1180591620717411303425`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_plus_one_denominator_times_two_pow_seventy_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1180591620717411303425)
        1180591620717411303425 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 123)))) *
        1180591620717411303425) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_plus_one_denominator_mul_two_pow_seventy_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_twohundredsixtytwothousandonehundredfortyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`1180591620717411303425`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_plus_one_denominator_times_two_pow_seventy_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 1180591620717411303425)
          1180591620717411303425 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 1180591620717411303425)
          1180591620717411303425 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_plus_one_denominator_times_two_pow_seventy_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_plus_one_denominator_times_two_pow_seventy_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`2361183241434822606849`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_one_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 2361183241434822606849) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 2361183241434822606849 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_one_plus_one
      (by linarith :
        (1 / 2361183241434822606849 : ℝ) ≤
          ((2 : ℝ) ^ k) / 2361183241434822606849)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 2361183241434822606849 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`2361183241434822606849` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/2361183241434822606849` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_one_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2361183241434822606849) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 71))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 71) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 2361183241434822606849
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 71)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 71 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 124)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 124)) (1 : ℤ)
      have hexp : ((k : ℤ) - 124) + 1 = (k : ℤ) - 71 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 124)) * (2 : ℝ) ^ (124 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 124)) (124 : ℤ)
    have hexp : ((k : ℤ) - 124) + 124 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (2361174234235567865857 * ((2 : ℝ) ^ (((k : ℤ) - 124)))) /
          2361183241434822606849 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 124)))) /
          2361183241434822606849 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 124)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (2361174234235567865857 * ((2 : ℝ) ^ (((k : ℤ) - 124)))) /
            2361183241434822606849 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 124)))) /
            2361183241434822606849 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_one_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`2361183241434822606849`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2361183241434822606849 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 2361183241434822606849) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_one_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`2361183241434822606849`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_one_plus_one_exact_mul_two_pow_seventy_one_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2361183241434822606849) *
        2361183241434822606849 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_one_plus_one_denominator_mul_two_pow_seventy_one_plus_one_above

/-- Positive denominator-`2361183241434822606849`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_one_plus_one_denominator_times_two_pow_seventy_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2361183241434822606849)
        2361183241434822606849 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124))) *
        2361183241434822606849) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_one_plus_one_denominator_mul_two_pow_seventy_one_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`2361183241434822606849` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2361183241434822606849 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 2361183241434822606849) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124)))
  have harg : (-((2 : ℝ) ^ k)) / 2361183241434822606849 =
      -(((2 : ℝ) ^ k) / 2361183241434822606849) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_one_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`2361183241434822606849` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_one_plus_one_exact_mul_two_pow_seventy_one_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2361183241434822606849) *
        2361183241434822606849 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 71))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_one_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_one_plus_one_denominator_mul_two_pow_seventy_one_plus_one_above

/-- Negative denominator-`2361183241434822606849`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_one_plus_one_denominator_times_two_pow_seventy_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2361183241434822606849)
        2361183241434822606849 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_one_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 124)))) *
        2361183241434822606849) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_one_plus_one_denominator_mul_two_pow_seventy_one_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_fivehundredtwentyfourthousandtwohundredeightyeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`2361183241434822606849`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_one_plus_one_denominator_times_two_pow_seventy_one_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 2361183241434822606849)
          2361183241434822606849 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 2361183241434822606849)
          2361183241434822606849 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_one_plus_one_denominator_times_two_pow_seventy_one_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_one_plus_one_denominator_times_two_pow_seventy_one_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`4722366482869645213697`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_two_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 4722366482869645213697) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 4722366482869645213697 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_two_plus_one
      (by linarith :
        (1 / 4722366482869645213697 : ℝ) ≤
          ((2 : ℝ) ^ k) / 4722366482869645213697)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 4722366482869645213697 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`4722366482869645213697` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/4722366482869645213697` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_two_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4722366482869645213697) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 72))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 72) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 4722366482869645213697
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 72)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 72 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 125)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 125)) (1 : ℤ)
      have hexp : ((k : ℤ) - 125) + 1 = (k : ℤ) - 72 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 125)) * (2 : ℝ) ^ (125 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 125)) (125 : ℤ)
    have hexp : ((k : ℤ) - 125) + 125 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (4722357475670390472705 * ((2 : ℝ) ^ (((k : ℤ) - 125)))) /
          4722366482869645213697 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 125)))) /
          4722366482869645213697 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 125)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (4722357475670390472705 * ((2 : ℝ) ^ (((k : ℤ) - 125)))) /
            4722366482869645213697 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 125)))) /
            4722366482869645213697 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_two_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`4722366482869645213697`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4722366482869645213697 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 4722366482869645213697) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_two_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`4722366482869645213697`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_two_plus_one_exact_mul_two_pow_seventy_two_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4722366482869645213697) *
        4722366482869645213697 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_two_plus_one_denominator_mul_two_pow_seventy_two_plus_one_above

/-- Positive denominator-`4722366482869645213697`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_two_plus_one_denominator_times_two_pow_seventy_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4722366482869645213697)
        4722366482869645213697 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125))) *
        4722366482869645213697) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_two_plus_one_denominator_mul_two_pow_seventy_two_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`4722366482869645213697` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4722366482869645213697 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 4722366482869645213697) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125)))
  have harg : (-((2 : ℝ) ^ k)) / 4722366482869645213697 =
      -(((2 : ℝ) ^ k) / 4722366482869645213697) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_two_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`4722366482869645213697` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_two_plus_one_exact_mul_two_pow_seventy_two_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4722366482869645213697) *
        4722366482869645213697 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 72))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_two_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_two_plus_one_denominator_mul_two_pow_seventy_two_plus_one_above

/-- Negative denominator-`4722366482869645213697`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_two_plus_one_denominator_times_two_pow_seventy_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4722366482869645213697)
        4722366482869645213697 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_two_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 125)))) *
        4722366482869645213697) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_two_plus_one_denominator_mul_two_pow_seventy_two_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_onemillionfortyeightthousandfivehundredseventysixth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`4722366482869645213697`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_two_plus_one_denominator_times_two_pow_seventy_two_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 4722366482869645213697)
          4722366482869645213697 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 4722366482869645213697)
          4722366482869645213697 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_two_plus_one_denominator_times_two_pow_seventy_two_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_two_plus_one_denominator_times_two_pow_seventy_two_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`9444732965739290427393`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_three_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 9444732965739290427393) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 9444732965739290427393 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_three_plus_one
      (by linarith :
        (1 / 9444732965739290427393 : ℝ) ≤
          ((2 : ℝ) ^ k) / 9444732965739290427393)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 9444732965739290427393 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`9444732965739290427393` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/9444732965739290427393` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_three_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9444732965739290427393) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 73))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 73) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 9444732965739290427393
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 73)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 73 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 126)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 126)) (1 : ℤ)
      have hexp : ((k : ℤ) - 126) + 1 = (k : ℤ) - 73 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 126)) * (2 : ℝ) ^ (126 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 126)) (126 : ℤ)
    have hexp : ((k : ℤ) - 126) + 126 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (9444723958540035686401 * ((2 : ℝ) ^ (((k : ℤ) - 126)))) /
          9444732965739290427393 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 126)))) /
          9444732965739290427393 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 126)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (9444723958540035686401 * ((2 : ℝ) ^ (((k : ℤ) - 126)))) /
            9444732965739290427393 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 126)))) /
            9444732965739290427393 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_three_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`9444732965739290427393`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9444732965739290427393 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 9444732965739290427393) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_three_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`9444732965739290427393`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_three_plus_one_exact_mul_two_pow_seventy_three_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9444732965739290427393) *
        9444732965739290427393 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_three_plus_one_denominator_mul_two_pow_seventy_three_plus_one_above

/-- Positive denominator-`9444732965739290427393`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_three_plus_one_denominator_times_two_pow_seventy_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9444732965739290427393)
        9444732965739290427393 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126))) *
        9444732965739290427393) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_three_plus_one_denominator_mul_two_pow_seventy_three_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`9444732965739290427393` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9444732965739290427393 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 9444732965739290427393) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126)))
  have harg : (-((2 : ℝ) ^ k)) / 9444732965739290427393 =
      -(((2 : ℝ) ^ k) / 9444732965739290427393) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_three_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`9444732965739290427393` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_three_plus_one_exact_mul_two_pow_seventy_three_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9444732965739290427393) *
        9444732965739290427393 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 73))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_three_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_three_plus_one_denominator_mul_two_pow_seventy_three_plus_one_above

/-- Negative denominator-`9444732965739290427393`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_three_plus_one_denominator_times_two_pow_seventy_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9444732965739290427393)
        9444732965739290427393 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_three_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 126)))) *
        9444732965739290427393) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_three_plus_one_denominator_mul_two_pow_seventy_three_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_twomillionninetyseventhousandonehundredfiftysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`9444732965739290427393`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_three_plus_one_denominator_times_two_pow_seventy_three_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 9444732965739290427393)
          9444732965739290427393 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 9444732965739290427393)
          9444732965739290427393 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_three_plus_one_denominator_times_two_pow_seventy_three_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_three_plus_one_denominator_times_two_pow_seventy_three_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`18889465931478580854785`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_four_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 18889465931478580854785) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 18889465931478580854785 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_four_plus_one
      (by linarith :
        (1 / 18889465931478580854785 : ℝ) ≤
          ((2 : ℝ) ^ k) / 18889465931478580854785)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 18889465931478580854785 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`18889465931478580854785` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/18889465931478580854785` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_four_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 18889465931478580854785) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 74))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 74) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 18889465931478580854785
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 74)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 74 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 127)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 127)) (1 : ℤ)
      have hexp : ((k : ℤ) - 127) + 1 = (k : ℤ) - 74 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 127)) * (2 : ℝ) ^ (127 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 127)) (127 : ℤ)
    have hexp : ((k : ℤ) - 127) + 127 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (18889456924279326113793 * ((2 : ℝ) ^ (((k : ℤ) - 127)))) /
          18889465931478580854785 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 127)))) /
          18889465931478580854785 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 127)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (18889456924279326113793 * ((2 : ℝ) ^ (((k : ℤ) - 127)))) /
            18889465931478580854785 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 127)))) /
            18889465931478580854785 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_four_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`18889465931478580854785`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18889465931478580854785 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 18889465931478580854785) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_four_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`18889465931478580854785`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_four_plus_one_exact_mul_two_pow_seventy_four_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18889465931478580854785) *
        18889465931478580854785 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_four_plus_one_denominator_mul_two_pow_seventy_four_plus_one_above

/-- Positive denominator-`18889465931478580854785`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_four_plus_one_denominator_times_two_pow_seventy_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18889465931478580854785)
        18889465931478580854785 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127))) *
        18889465931478580854785) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_four_plus_one_denominator_mul_two_pow_seventy_four_plus_one_above]
  exact problem2_10_ieeeDouble_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`18889465931478580854785` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18889465931478580854785 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 18889465931478580854785) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127)))
  have harg : (-((2 : ℝ) ^ k)) / 18889465931478580854785 =
      -(((2 : ℝ) ^ k) / 18889465931478580854785) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_four_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`18889465931478580854785` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_four_plus_one_exact_mul_two_pow_seventy_four_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18889465931478580854785) *
        18889465931478580854785 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 74))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_four_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_four_plus_one_denominator_mul_two_pow_seventy_four_plus_one_above

/-- Negative denominator-`18889465931478580854785`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_four_plus_one_denominator_times_two_pow_seventy_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18889465931478580854785)
        18889465931478580854785 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_four_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 127)))) *
        18889465931478580854785) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_four_plus_one_denominator_mul_two_pow_seventy_four_plus_one_above]
  exact problem2_10_ieeeDouble_negative_two_pow_plus_one_fourmilliononehundredninetyfourthousandthreehundredfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`18889465931478580854785`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_four_plus_one_denominator_times_two_pow_seventy_four_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 18889465931478580854785)
          18889465931478580854785 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 18889465931478580854785)
          18889465931478580854785 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_four_plus_one_denominator_times_two_pow_seventy_four_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_four_plus_one_denominator_times_two_pow_seventy_four_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`37778931862957161709569`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_five_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 37778931862957161709569) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 37778931862957161709569 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_five_plus_one
      (by linarith :
        (1 / 37778931862957161709569 : ℝ) ≤
          ((2 : ℝ) ^ k) / 37778931862957161709569)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 37778931862957161709569 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`37778931862957161709569` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/37778931862957161709569` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_five_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 37778931862957161709569) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 75))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 75) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 37778931862957161709569
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 75)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 75 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 128)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 128)) (1 : ℤ)
      have hexp : ((k : ℤ) - 128) + 1 = (k : ℤ) - 75 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 128)) * (2 : ℝ) ^ (128 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 128)) (128 : ℤ)
    have hexp : ((k : ℤ) - 128) + 128 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (37778922855757906968577 * ((2 : ℝ) ^ (((k : ℤ) - 128)))) /
          37778931862957161709569 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 128)))) /
          37778931862957161709569 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 128)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (37778922855757906968577 * ((2 : ℝ) ^ (((k : ℤ) - 128)))) /
            37778931862957161709569 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 128)))) /
            37778931862957161709569 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_five_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`37778931862957161709569`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 37778931862957161709569 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 37778931862957161709569) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_five_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`37778931862957161709569`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_five_plus_one_exact_mul_two_pow_seventy_five_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 37778931862957161709569) *
        37778931862957161709569 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_five_plus_one_denominator_mul_two_pow_seventy_five_plus_one_above

/-- Positive denominator-`37778931862957161709569`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_five_plus_one_denominator_times_two_pow_seventy_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 37778931862957161709569)
        37778931862957161709569 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128))) *
        37778931862957161709569) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_five_plus_one_denominator_mul_two_pow_seventy_five_plus_one_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`37778931862957161709569` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 37778931862957161709569 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 37778931862957161709569) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128)))
  have harg : (-((2 : ℝ) ^ k)) / 37778931862957161709569 =
      -(((2 : ℝ) ^ k) / 37778931862957161709569) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_five_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`37778931862957161709569` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_five_plus_one_exact_mul_two_pow_seventy_five_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 37778931862957161709569) *
        37778931862957161709569 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 75))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_five_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_five_plus_one_denominator_mul_two_pow_seventy_five_plus_one_above

/-- Negative denominator-`37778931862957161709569`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_five_plus_one_denominator_times_two_pow_seventy_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 37778931862957161709569)
        37778931862957161709569 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_five_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 128)))) *
        37778931862957161709569) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_five_plus_one_denominator_mul_two_pow_seventy_five_plus_one_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_eightmillionthreehundredeightyeightthousandsixhundredeighth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`37778931862957161709569`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_five_plus_one_denominator_times_two_pow_seventy_five_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 37778931862957161709569)
          37778931862957161709569 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 37778931862957161709569)
          37778931862957161709569 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_five_plus_one_denominator_times_two_pow_seventy_five_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_five_plus_one_denominator_times_two_pow_seventy_five_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`75557863725914323419137`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_six_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 75557863725914323419137) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 75557863725914323419137 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_six_plus_one
      (by linarith :
        (1 / 75557863725914323419137 : ℝ) ≤
          ((2 : ℝ) ^ k) / 75557863725914323419137)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 75557863725914323419137 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`75557863725914323419137` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/75557863725914323419137` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_six_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 75557863725914323419137) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 76))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 76) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 75557863725914323419137
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 76)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 76 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 129)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 129)) (1 : ℤ)
      have hexp : ((k : ℤ) - 129) + 1 = (k : ℤ) - 76 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 129)) * (2 : ℝ) ^ (129 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 129)) (129 : ℤ)
    have hexp : ((k : ℤ) - 129) + 129 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (75557854718715068678145 * ((2 : ℝ) ^ (((k : ℤ) - 129)))) /
          75557863725914323419137 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 129)))) /
          75557863725914323419137 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 129)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (75557854718715068678145 * ((2 : ℝ) ^ (((k : ℤ) - 129)))) /
            75557863725914323419137 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 129)))) /
            75557863725914323419137 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_six_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`75557863725914323419137`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 75557863725914323419137 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 75557863725914323419137) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_six_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`75557863725914323419137`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_six_plus_one_exact_mul_two_pow_seventy_six_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 75557863725914323419137) *
        75557863725914323419137 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_six_plus_one_denominator_mul_two_pow_seventy_six_plus_one_above

/-- Positive denominator-`75557863725914323419137`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_six_plus_one_denominator_times_two_pow_seventy_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 75557863725914323419137)
        75557863725914323419137 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129))) *
        75557863725914323419137) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_six_plus_one_denominator_mul_two_pow_seventy_six_plus_one_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`75557863725914323419137` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 75557863725914323419137 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 75557863725914323419137) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129)))
  have harg : (-((2 : ℝ) ^ k)) / 75557863725914323419137 =
      -(((2 : ℝ) ^ k) / 75557863725914323419137) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_six_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`75557863725914323419137` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_six_plus_one_exact_mul_two_pow_seventy_six_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 75557863725914323419137) *
        75557863725914323419137 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 76))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_six_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_six_plus_one_denominator_mul_two_pow_seventy_six_plus_one_above

/-- Negative denominator-`75557863725914323419137`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_six_plus_one_denominator_times_two_pow_seventy_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 75557863725914323419137)
        75557863725914323419137 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_six_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 129)))) *
        75557863725914323419137) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_six_plus_one_denominator_mul_two_pow_seventy_six_plus_one_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_sixteenmillionsevenhundredseventyseventhousandtwohundredsixteenth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`75557863725914323419137`, power-of-two numerator subfamily
of Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_six_plus_one_denominator_times_two_pow_seventy_six_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 75557863725914323419137)
          75557863725914323419137 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 75557863725914323419137)
          75557863725914323419137 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_six_plus_one_denominator_times_two_pow_seventy_six_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_six_plus_one_denominator_times_two_pow_seventy_six_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`151115727451828646838273`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_seven_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 151115727451828646838273) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 151115727451828646838273 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_seven_plus_one
      (by linarith :
        (1 / 151115727451828646838273 : ℝ) ≤
          ((2 : ℝ) ^ k) / 151115727451828646838273)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 151115727451828646838273 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`151115727451828646838273` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/151115727451828646838273` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_seven_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 151115727451828646838273) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 77))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 77) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 151115727451828646838273
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 77)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 77 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 130)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 130)) (1 : ℤ)
      have hexp : ((k : ℤ) - 130) + 1 = (k : ℤ) - 77 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 130)) * (2 : ℝ) ^ (130 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 130)) (130 : ℤ)
    have hexp : ((k : ℤ) - 130) + 130 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (151115718444629392097281 * ((2 : ℝ) ^ (((k : ℤ) - 130)))) /
          151115727451828646838273 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 130)))) /
          151115727451828646838273 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 130)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (151115718444629392097281 * ((2 : ℝ) ^ (((k : ℤ) - 130)))) /
            151115727451828646838273 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 130)))) /
            151115727451828646838273 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_seven_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`151115727451828646838273`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 151115727451828646838273 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 151115727451828646838273) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_seven_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`151115727451828646838273`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_seven_plus_one_exact_mul_two_pow_seventy_seven_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 151115727451828646838273) *
        151115727451828646838273 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_seven_plus_one_denominator_mul_two_pow_seventy_seven_plus_one_above

/-- Positive denominator-`151115727451828646838273`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_seven_plus_one_denominator_times_two_pow_seventy_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 151115727451828646838273)
        151115727451828646838273 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130))) *
        151115727451828646838273) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_seven_plus_one_denominator_mul_two_pow_seventy_seven_plus_one_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`151115727451828646838273` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 151115727451828646838273 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 151115727451828646838273) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130)))
  have harg : (-((2 : ℝ) ^ k)) / 151115727451828646838273 =
      -(((2 : ℝ) ^ k) / 151115727451828646838273) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_seven_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`151115727451828646838273` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_seven_plus_one_exact_mul_two_pow_seventy_seven_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 151115727451828646838273) *
        151115727451828646838273 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 77))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_seven_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_seven_plus_one_denominator_mul_two_pow_seventy_seven_plus_one_above

/-- Negative denominator-`151115727451828646838273`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_seven_plus_one_denominator_times_two_pow_seventy_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 151115727451828646838273)
        151115727451828646838273 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_seven_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 130)))) *
        151115727451828646838273) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_seven_plus_one_denominator_mul_two_pow_seventy_seven_plus_one_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_thirtythreemillionfivehundredfiftyfourthousandfourhundredthirtysecond_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`151115727451828646838273`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_seven_plus_one_denominator_times_two_pow_seventy_seven_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 151115727451828646838273)
          151115727451828646838273 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 151115727451828646838273)
          151115727451828646838273 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_seven_plus_one_denominator_times_two_pow_seventy_seven_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_seven_plus_one_denominator_times_two_pow_seventy_seven_plus_one hk⟩

/-- Finite-normal range side condition for the denominator-`302231454903657293676545`,
power-of-two numerator route. -/
theorem problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_eight_plus_one_finiteNormalRange
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteNormalRange (((2 : ℝ) ^ k) / 302231454903657293676545) := by
  rw [finiteNormalRange]
  have hxnonneg : 0 ≤ ((2 : ℝ) ^ k) / 302231454903657293676545 := by positivity
  rw [abs_of_nonneg hxnonneg]
  constructor
  · have hpow_ge_one : (1 : ℝ) ≤ (2 : ℝ) ^ k :=
      one_le_pow₀ (by norm_num : (1 : ℝ) ≤ 2)
    exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_two_pow_seventy_eight_plus_one
      (by linarith :
        (1 / 302231454903657293676545 : ℝ) ≤
          ((2 : ℝ) ^ k) / 302231454903657293676545)
  · have hdiv_le :
        ((2 : ℝ) ^ k) / 302231454903657293676545 ≤ (2 : ℝ) ^ k := by
      have hpow_nonneg : 0 ≤ (2 : ℝ) ^ k := by positivity
      linarith
    exact le_trans hdiv_le
      (problem2_10_ieeeDouble_two_pow_le_maxFiniteMagnitude
        (by omega : k ≤ 1023))

/-- Generic denominator-`302231454903657293676545` first-operation fact for the
power-of-two subfamily of Kahan's Problem 2.10 theorem: finite IEEE-double
round-to-even rounds `(2^k)/302231454903657293676545` to the upper
boundary-adjacent endpoint. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_eight_plus_one_denominator_rounds_to_upper
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 302231454903657293676545) =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131)) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ :=
    fmt.normalizedValue false fmt.maxNormalMantissa (((k : ℤ) - 78))
  let b : ℝ :=
    fmt.normalizedValue false fmt.minNormalMantissa ((((k : ℤ) - 78) + 1))
  let x : ℝ := ((2 : ℝ) ^ k) / 302231454903657293676545
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (((k : ℤ) - 78)), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (9007199254740991 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131)) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa]
    ring
  have hb_value :
      b = (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131)) := by
    have hshift :
        (2 : ℝ) ^ (((k : ℤ) - 78 + 1 - 53)) =
          (2 : ℝ) ^ (((k : ℤ) - 131)) * 2 := by
      have h :=
        zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 131)) (1 : ℤ)
      have hexp : ((k : ℤ) - 131) + 1 = (k : ℤ) - 78 + 1 - 53 := by
        ring
      rw [hexp] at h
      simpa [zpow_one] using h
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa]
    rw [hshift]
    ring
  have hpow :
      (2 : ℝ) ^ k =
        (2 : ℝ) ^ (((k : ℤ) - 131)) * (2 : ℝ) ^ (131 : ℤ) := by
    have h :=
      zpow_add₀ (by norm_num : (2 : ℝ) ≠ 0) (((k : ℤ) - 131)) (131 : ℤ)
    have hexp : ((k : ℤ) - 131) + 131 = (k : ℤ) := by ring
    rw [hexp] at h
    simpa [zpow_natCast] using h
  have hleft_dist :
      x - a =
        (302231445896458038935553 * ((2 : ℝ) ^ (((k : ℤ) - 131)))) /
          302231454903657293676545 := by
    simp [x, ha_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hright_dist :
      b - x =
        (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 131)))) /
          302231454903657293676545 := by
    simp [x, hb_value, hpow]
    norm_num [zpow_natCast]
    ring
  have hzpos : 0 < (2 : ℝ) ^ (((k : ℤ) - 131)) :=
    zpow_pos (by norm_num : (0 : ℝ) < 2) _
  have hleft_pos : 0 < x - a := by
    have hpos :
        0 <
          (302231445896458038935553 * ((2 : ℝ) ^ (((k : ℤ) - 131)))) /
            302231454903657293676545 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hright_pos : 0 < b - x := by
    have hpos :
        0 <
          (9007199254740992 * ((2 : ℝ) ^ (((k : ℤ) - 131)))) /
            302231454903657293676545 :=
      div_pos (mul_pos (by norm_num) hzpos) (by norm_num)
    linarith
  have hstrict : a < x ∧ x < b := by
    constructor <;> linarith
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) := by
    simpa [x, fmt] using
      fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
        (problem2_10_ieeeDouble_two_pow_div_two_pow_seventy_eight_plus_one_finiteNormalRange hk)
  have hrightCloser : |x - b| < |x - a| := by
    have hxmb : x - b = - (b - x) := by ring
    rw [hxmb, abs_neg, abs_of_nonneg (le_of_lt hright_pos),
      abs_of_nonneg (le_of_lt hleft_pos)]
    rw [hright_dist, hleft_dist]
    nlinarith [hzpos]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level wrapper for the first denominator-`302231454903657293676545`,
power-of-two Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 302231454903657293676545 =
      (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ k) / 302231454903657293676545) =
    (9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131))
  exact
    problem2_10_ieeeDouble_two_pow_two_pow_seventy_eight_plus_one_denominator_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`302231454903657293676545`,
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_eight_plus_one_exact_mul_two_pow_seventy_eight_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 302231454903657293676545) *
        302231454903657293676545 =
      (2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_eight_plus_one_denominator_mul_two_pow_seventy_eight_plus_one_above

/-- Positive denominator-`302231454903657293676545`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_two_pow_seventy_eight_plus_one_denominator_times_two_pow_seventy_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 302231454903657293676545)
        302231454903657293676545 =
      (2 : ℝ) ^ k := by
  rw [problem2_10_ieeeDouble_div_two_pow_two_pow_seventy_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      (((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131))) *
        302231454903657293676545) =
    (2 : ℝ) ^ k
  rw [
    problem2_10_ieeeDouble_rounded_two_pow_two_pow_seventy_eight_plus_one_denominator_mul_two_pow_seventy_eight_plus_one_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_two_pow hk

/-- Signed companion of the denominator-`302231454903657293676545` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 302231454903657293676545 =
      -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ k)) / 302231454903657293676545) =
    -((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131)))
  have harg : (-((2 : ℝ) ^ k)) / 302231454903657293676545 =
      -(((2 : ℝ) ^ k) / 302231454903657293676545) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [
      problem2_10_ieeeDouble_two_pow_two_pow_seventy_eight_plus_one_denominator_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result generic audit for the signed second
denominator-`302231454903657293676545` operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_eight_plus_one_exact_mul_two_pow_seventy_eight_plus_one_above
    {k : ℕ} (hk : k ≤ 1022) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 302231454903657293676545) *
        302231454903657293676545 =
      -((2 : ℝ) ^ k + (2 : ℝ) ^ (((k : ℤ) - 78))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_eight_plus_one hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_eight_plus_one_denominator_mul_two_pow_seventy_eight_plus_one_above

/-- Negative denominator-`302231454903657293676545`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_eight_plus_one_denominator_times_two_pow_seventy_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 302231454903657293676545)
        302231454903657293676545 =
      -((2 : ℝ) ^ k) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_two_pow_seventy_eight_plus_one hk]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((9007199254740992 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 131)))) *
        302231454903657293676545) =
    -((2 : ℝ) ^ k)
  rw [
    problem2_10_ieeeDouble_negative_rounded_two_pow_two_pow_seventy_eight_plus_one_denominator_mul_two_pow_seventy_eight_plus_one_above]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_one_sixtysevenmilliononehundredeightthousandeighthundredsixtyfourth_ulp_rounds_to_neg_two_pow hk

/-- Signed denominator-`302231454903657293676545`, power-of-two numerator
subfamily of Kahan's Problem 2.10 theorem in the finite IEEE-double
round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_two_pow_seventy_eight_plus_one_denominator_times_two_pow_seventy_eight_plus_one
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ k) 302231454903657293676545)
          302231454903657293676545 =
        (2 : ℝ) ^ k ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ k)) 302231454903657293676545)
          302231454903657293676545 =
        -((2 : ℝ) ^ k) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_two_pow_seventy_eight_plus_one_denominator_times_two_pow_seventy_eight_plus_one hk,
    problem2_10_ieeeDouble_neg_two_pow_two_pow_seventy_eight_plus_one_denominator_times_two_pow_seventy_eight_plus_one hk⟩

/-- Denominator-`18` first-operation fact for shifted power-of-two numerators:
`(2^(k+1))/18` reuses the denominator-`9` rounded quotient for `(2^k)/9`. -/
theorem problem2_10_ieeeDouble_div_two_pow_succ_eighteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 18 =
      (8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ (k + 1)) / 18) =
    (8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56))
  have harg : ((2 : ℝ) ^ (k + 1)) / 18 = ((2 : ℝ) ^ k) / 9 := by
    rw [pow_succ]
    ring
  rw [harg]
  exact problem2_10_ieeeDouble_two_pow_ninths_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`18`, shifted
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_succ_eighteen_exact_mul_eighteen_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 18) *
        18 =
      (2 : ℝ) ^ (k + 1) - (2 : ℝ) ^ (((k + 1 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_succ_eighteen hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_succ_eighteenths_mul_eighteen_midpoint

/-- Positive denominator-`18`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_succ_eighteenths_times_eighteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          ((2 : ℝ) ^ (k + 1)) 18)
        18 =
      (2 : ℝ) ^ (k + 1) := by
  rw [problem2_10_ieeeDouble_div_two_pow_succ_eighteen
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56))) * 18) =
    (2 : ℝ) ^ (k + 1)
  rw [problem2_10_ieeeDouble_rounded_two_pow_succ_eighteenths_mul_eighteen_midpoint]
  exact
    problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow
      (k := k + 1) (by omega : k + 1 ≤ 1023)

/-- Signed companion of the denominator-`18` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_succ_eighteen
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ (k + 1))) 18 =
      -((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ (k + 1))) / 18) =
    -((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)))
  have harg : (-((2 : ℝ) ^ (k + 1))) / 18 = -(((2 : ℝ) ^ k) / 9) := by
    rw [pow_succ]
    ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_ninths_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_succ_eighteen_exact_mul_eighteen_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 1))) 18) *
        18 =
      -((2 : ℝ) ^ (k + 1)) + (2 : ℝ) ^ (((k + 1 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_succ_eighteen hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_succ_eighteenths_mul_eighteen_midpoint

/-- Negative denominator-`18`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_succ_eighteenths_times_eighteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 1))) 18)
        18 =
      -((2 : ℝ) ^ (k + 1)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_succ_eighteen
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((8006399337547548 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 56)))) * 18) =
    -((2 : ℝ) ^ (k + 1))
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_succ_eighteenths_mul_eighteen_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow
      (k := k + 1) (by omega : k + 1 ≤ 1023)

/-- Signed denominator-`18`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_succ_eighteenths_times_eighteen
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            ((2 : ℝ) ^ (k + 1)) 18)
          18 =
        (2 : ℝ) ^ (k + 1) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            (-((2 : ℝ) ^ (k + 1))) 18)
          18 =
        -((2 : ℝ) ^ (k + 1)) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_succ_eighteenths_times_eighteen hk,
    problem2_10_ieeeDouble_neg_two_pow_succ_eighteenths_times_eighteen hk⟩

/-- Denominator-`6` first-operation fact for power-of-two numerators beyond
`1`: `(2^(k+1))/6` reuses the denominator-`3` rounded quotient for `(2^k)/3`. -/
theorem problem2_10_ieeeDouble_div_two_pow_succ_six
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 6 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ (k + 1)) / 6) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))
  have harg : ((2 : ℝ) ^ (k + 1)) / 6 = ((2 : ℝ) ^ k) / 3 := by
    rw [pow_succ]
    ring
  rw [harg]
  exact problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`6`, power-of-two
second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_succ_six_exact_mul_six_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 6) *
        6 =
      (2 : ℝ) ^ (k + 1) - (2 : ℝ) ^ (((k + 1 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_succ_six hk]
  exact problem2_10_ieeeDouble_rounded_two_pow_succ_sixths_mul_six_midpoint

/-- Positive denominator-`6`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_succ_sixths_times_six
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 6)
        6 =
      (2 : ℝ) ^ (k + 1) := by
  rw [problem2_10_ieeeDouble_div_two_pow_succ_six (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))) * 6) =
    (2 : ℝ) ^ (k + 1)
  rw [problem2_10_ieeeDouble_rounded_two_pow_succ_sixths_mul_six_midpoint]
  exact
    problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow
      (by omega : k + 1 ≤ 1023)

/-- Signed companion of the denominator-`6` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_succ_six
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ (k + 1))) 6 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ (k + 1))) / 6) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)))
  have harg : (-((2 : ℝ) ^ (k + 1))) / 6 = -(((2 : ℝ) ^ k) / 3) := by
    rw [pow_succ]
    ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_succ_six_exact_mul_six_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 1))) 6) *
        6 =
      -((2 : ℝ) ^ (k + 1)) + (2 : ℝ) ^ (((k + 1 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_succ_six hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_succ_sixths_mul_six_midpoint

/-- Negative denominator-`6`, power-of-two numerator subfamily of Kahan's
Problem 2.10 theorem in the finite IEEE-double round-to-even operation wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_succ_sixths_times_six
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 1))) 6)
        6 =
      -((2 : ℝ) ^ (k + 1)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_succ_six (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)))) * 6) =
    -((2 : ℝ) ^ (k + 1))
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_succ_sixths_mul_six_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow
      (by omega : k + 1 ≤ 1023)

/-- Signed denominator-`6`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_succ_sixths_times_six
    {k : ℕ} (hk : k ≤ 1022) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            ((2 : ℝ) ^ (k + 1)) 6)
          6 =
        (2 : ℝ) ^ (k + 1) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            (-((2 : ℝ) ^ (k + 1))) 6)
          6 =
        -((2 : ℝ) ^ (k + 1)) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_succ_sixths_times_six hk,
    problem2_10_ieeeDouble_neg_two_pow_succ_sixths_times_six hk⟩

/-- Denominator-`10` first-operation fact for shifted power-of-two numerators:
`(2^(k+1))/10` reuses the denominator-`5` rounded quotient for `(2^k)/5`. -/
theorem problem2_10_ieeeDouble_div_two_pow_succ_ten
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 10 =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ (k + 1)) / 10) =
    (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))
  have harg : ((2 : ℝ) ^ (k + 1)) / 10 = ((2 : ℝ) ^ k) / 5 := by
    rw [pow_succ]
    ring
  rw [harg]
  exact problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`10`, shifted
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_succ_ten_exact_mul_ten_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 10) *
        10 =
      (2 : ℝ) ^ (k + 1) + (2 : ℝ) ^ (((k + 1 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_succ_ten hk]
  exact problem2_10_ieeeDouble_rounded_two_pow_succ_tenths_mul_ten_above

/-- Positive denominator-`10`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_succ_tenths_times_ten
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 1)) 10)
        10 =
      (2 : ℝ) ^ (k + 1) := by
  rw [problem2_10_ieeeDouble_div_two_pow_succ_ten (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))) * 10) =
    (2 : ℝ) ^ (k + 1)
  rw [problem2_10_ieeeDouble_rounded_two_pow_succ_tenths_mul_ten_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow
      (k := k + 1) (by omega : k + 1 ≤ 1022)

/-- Signed companion of the denominator-`10` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_succ_ten
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ (k + 1))) 10 =
      -((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ (k + 1))) / 10) =
    -((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)))
  have harg : (-((2 : ℝ) ^ (k + 1))) / 10 = -(((2 : ℝ) ^ k) / 5) := by
    rw [pow_succ]
    ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_succ_ten_exact_mul_ten_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 1))) 10) *
        10 =
      -((2 : ℝ) ^ (k + 1) + (2 : ℝ) ^ (((k + 1 : ℤ) - 54))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_succ_ten hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_succ_tenths_mul_ten_below

/-- Negative denominator-`10`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_succ_tenths_times_ten
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 1))) 10)
        10 =
      -((2 : ℝ) ^ (k + 1)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_succ_ten (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)))) * 10) =
    -((2 : ℝ) ^ (k + 1))
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_succ_tenths_mul_ten_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow
      (k := k + 1) (by omega : k + 1 ≤ 1022)

/-- Signed denominator-`10`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_succ_tenths_times_ten
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            ((2 : ℝ) ^ (k + 1)) 10)
          10 =
        (2 : ℝ) ^ (k + 1) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            (-((2 : ℝ) ^ (k + 1))) 10)
          10 =
        -((2 : ℝ) ^ (k + 1)) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_succ_tenths_times_ten hk,
    problem2_10_ieeeDouble_neg_two_pow_succ_tenths_times_ten hk⟩

/-- Denominator-`12` first-operation fact for shifted power-of-two numerators:
`(2^(k+2))/12` reuses the denominator-`3` rounded quotient for `(2^k)/3`. -/
theorem problem2_10_ieeeDouble_div_two_pow_add_two_twelve
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 2)) 12 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ (k + 2)) / 12) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))
  have harg : ((2 : ℝ) ^ (k + 2)) / 12 = ((2 : ℝ) ^ k) / 3 := by
    rw [show k + 2 = k + 1 + 1 by omega, pow_succ, pow_succ]
    ring
  rw [harg]
  exact problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower hk

/-- Result-by-result generic audit for the denominator-`12`, shifted
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_add_two_twelve_exact_mul_twelve_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 2)) 12) *
        12 =
      (2 : ℝ) ^ (k + 2) - (2 : ℝ) ^ (((k + 2 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_add_two_twelve hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_add_two_twelfths_mul_twelve_midpoint

/-- Positive denominator-`12`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_add_two_twelfths_times_twelve
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 2)) 12)
        12 =
      (2 : ℝ) ^ (k + 2) := by
  rw [problem2_10_ieeeDouble_div_two_pow_add_two_twelve (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))) * 12) =
    (2 : ℝ) ^ (k + 2)
  rw [problem2_10_ieeeDouble_rounded_two_pow_add_two_twelfths_mul_twelve_midpoint]
  exact
    problem2_10_ieeeDouble_midpoint_below_two_pow_rounds_to_two_pow
      (k := k + 2) (by omega : k + 2 ≤ 1023)

/-- Signed companion of the denominator-`12` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_add_two_twelve
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ (k + 2))) 12 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ (k + 2))) / 12) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)))
  have harg : (-((2 : ℝ) ^ (k + 2))) / 12 = -(((2 : ℝ) ^ k) / 3) := by
    rw [show k + 2 = k + 1 + 1 by omega, pow_succ, pow_succ]
    ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_thirds_rounds_to_lower hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_add_two_twelve_exact_mul_twelve_midpoint
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 2))) 12) *
        12 =
      -((2 : ℝ) ^ (k + 2)) + (2 : ℝ) ^ (((k + 2 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_add_two_twelve hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_add_two_twelfths_mul_twelve_midpoint

/-- Negative denominator-`12`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_add_two_twelfths_times_twelve
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 2))) 12)
        12 =
      -((2 : ℝ) ^ (k + 2)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_add_two_twelve
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 54)))) * 12) =
    -((2 : ℝ) ^ (k + 2))
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_add_two_twelfths_mul_twelve_midpoint]
  exact
    problem2_10_ieeeDouble_negative_midpoint_above_two_pow_rounds_to_neg_two_pow
      (k := k + 2) (by omega : k + 2 ≤ 1023)

/-- Signed denominator-`12`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_add_two_twelfths_times_twelve
    {k : ℕ} (hk : k ≤ 1021) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            ((2 : ℝ) ^ (k + 2)) 12)
          12 =
        (2 : ℝ) ^ (k + 2) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            (-((2 : ℝ) ^ (k + 2))) 12)
          12 =
        -((2 : ℝ) ^ (k + 2)) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_add_two_twelfths_times_twelve hk,
    problem2_10_ieeeDouble_neg_two_pow_add_two_twelfths_times_twelve hk⟩

/-- Denominator-`20` first-operation fact for shifted power-of-two numerators:
`(2^(k+2))/20` reuses the denominator-`5` rounded quotient for `(2^k)/5`. -/
theorem problem2_10_ieeeDouble_div_two_pow_add_two_twenty
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 2)) 20 =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)) := by
  change ieeeDoubleFormat.finiteRoundToEven (((2 : ℝ) ^ (k + 2)) / 20) =
    (7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))
  have harg : ((2 : ℝ) ^ (k + 2)) / 20 = ((2 : ℝ) ^ k) / 5 := by
    rw [show k + 2 = k + 1 + 1 by omega, pow_succ, pow_succ]
    ring
  rw [harg]
  exact problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper hk

/-- Result-by-result generic audit for the denominator-`20`, shifted
power-of-two second operation. -/
theorem problem2_10_ieeeDouble_div_two_pow_add_two_twenty_exact_mul_twenty_above
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 2)) 20) *
        20 =
      (2 : ℝ) ^ (k + 2) + (2 : ℝ) ^ (((k + 2 : ℤ) - 54)) := by
  rw [problem2_10_ieeeDouble_div_two_pow_add_two_twenty hk]
  exact
    problem2_10_ieeeDouble_rounded_two_pow_add_two_twentieths_mul_twenty_above

/-- Positive denominator-`20`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_two_pow_add_two_twentieths_times_twenty
    {k : ℕ} (hk : k ≤ 1020) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div ((2 : ℝ) ^ (k + 2)) 20)
        20 =
      (2 : ℝ) ^ (k + 2) := by
  rw [problem2_10_ieeeDouble_div_two_pow_add_two_twenty
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      (((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))) * 20) =
    (2 : ℝ) ^ (k + 2)
  rw [problem2_10_ieeeDouble_rounded_two_pow_add_two_twentieths_mul_twenty_above]
  exact
    problem2_10_ieeeDouble_two_pow_plus_quarter_ulp_rounds_to_two_pow
      (k := k + 2) (by omega : k + 2 ≤ 1022)

/-- Signed companion of the denominator-`20` first operation. -/
theorem problem2_10_ieeeDouble_div_neg_two_pow_add_two_twenty
    {k : ℕ} (hk : k ≤ 1023) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-((2 : ℝ) ^ (k + 2))) 20 =
      -((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55))) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-((2 : ℝ) ^ (k + 2))) / 20) =
    -((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)))
  have harg : (-((2 : ℝ) ^ (k + 2))) / 20 = -(((2 : ℝ) ^ k) / 5) := by
    rw [show k + 2 = k + 1 + 1 by omega, pow_succ, pow_succ]
    ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_two_pow_fifths_rounds_to_upper hk]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_two_pow_add_two_twenty_exact_mul_twenty_below
    {k : ℕ} (hk : k ≤ 1023) :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 2))) 20) *
        20 =
      -((2 : ℝ) ^ (k + 2) + (2 : ℝ) ^ (((k + 2 : ℤ) - 54))) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_add_two_twenty hk]
  exact
    problem2_10_ieeeDouble_negative_rounded_two_pow_add_two_twentieths_mul_twenty_below

/-- Negative denominator-`20`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_neg_two_pow_add_two_twentieths_times_twenty
    {k : ℕ} (hk : k ≤ 1020) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
          (-((2 : ℝ) ^ (k + 2))) 20)
        20 =
      -((2 : ℝ) ^ (k + 2)) := by
  rw [problem2_10_ieeeDouble_div_neg_two_pow_add_two_twenty
    (by omega : k ≤ 1023)]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((7205759403792794 : ℝ) * (2 : ℝ) ^ (((k : ℤ) - 55)))) * 20) =
    -((2 : ℝ) ^ (k + 2))
  rw [problem2_10_ieeeDouble_negative_rounded_two_pow_add_two_twentieths_mul_twenty_below]
  exact
    problem2_10_ieeeDouble_negative_two_pow_plus_quarter_ulp_rounds_to_neg_two_pow
      (k := k + 2) (by omega : k + 2 ≤ 1022)

/-- Signed denominator-`20`, shifted power-of-two numerator subfamily of
Kahan's Problem 2.10 theorem in the finite IEEE-double round-to-even wrapper. -/
theorem problem2_10_ieeeDouble_signed_two_pow_add_two_twentieths_times_twenty
    {k : ℕ} (hk : k ≤ 1020) :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            ((2 : ℝ) ^ (k + 2)) 20)
          20 =
        (2 : ℝ) ^ (k + 2) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div
            (-((2 : ℝ) ^ (k + 2))) 20)
          20 =
        -((2 : ℝ) ^ (k + 2)) := by
  exact ⟨problem2_10_ieeeDouble_two_pow_add_two_twentieths_times_twenty hk,
    problem2_10_ieeeDouble_neg_two_pow_add_two_twentieths_times_twenty hk⟩

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 6 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (6 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (6 : ℝ) ≤ (2 : ℝ) ^ (3 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 6) hden

private theorem problem2_10_ieeeDouble_oneSixth_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (1 / 6 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 1 / 6)]
  constructor
  · exact problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_sixth
  · exact le_trans (by norm_num : (1 / 6 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

/-- First-operation audit for the missing denominator-`6`, numerator-`1`
case: finite IEEE-double round-to-even rounds `1/6` down to the nearest
double. -/
theorem problem2_10_ieeeDouble_oneSixth_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (1 / 6 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 (-2)
  let b : ℝ := fmt.normalizedValue false (6004799503160661 + 1) (-2)
  let x : ℝ := (1 / 6 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (-2 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_oneSixth_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 1`, `n = 6` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_one_six :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 6 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((1 : ℝ) / 6) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ)
  exact problem2_10_ieeeDouble_oneSixth_rounds_to_lower

/-- Result-by-result audit for the second `m = 1`, `n = 6` operation. -/
theorem problem2_10_ieeeDouble_div_one_six_exact_mul_six_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 6) * 6 =
      (1 : ℝ) - (2 : ℝ) ^ (-54 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_one_six]
  exact problem2_10_ieeeDouble_rounded_oneSixth_mul_six_midpoint

/-- Denominator-`6`, numerator-`1` finite-selector instance:
`fl((1/6) * 6) = 1` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_one_sixth_times_six :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 6) 6 =
      (1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_one_six]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ)) * 6) = (1 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_oneSixth_mul_six_midpoint]
  exact problem2_9_double_rounds_extended_midpoint_to_one

/-- Signed companion of the first denominator-`6`, numerator-`1` operation. -/
theorem problem2_10_ieeeDouble_div_neg_one_six :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 6 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-1 : ℝ) / 6) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ))
  have harg : ((-1 : ℝ) / 6) = -((1 : ℝ) / 6) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_oneSixth_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_one_six_exact_mul_six_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 6) * 6 =
      -(1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_one_six]
  exact problem2_10_ieeeDouble_negative_rounded_oneSixth_mul_six_midpoint

/-- Signed denominator-`6`, numerator-`-1` finite-selector instance. -/
theorem problem2_10_ieeeDouble_neg_one_sixth_times_six :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 6) 6 =
      (-1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_one_six]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-55 : ℤ))) * 6) = (-1 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_oneSixth_mul_six_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_one_rounds_to_neg_one

/-- Signed denominator-`6`, numerator-`1` finite-selector trace. -/
theorem problem2_10_ieeeDouble_signed_one_sixth_times_six :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 6) 6 =
        (1 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 6) 6 =
        (-1 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_one_sixth_times_six,
    problem2_10_ieeeDouble_neg_one_sixth_times_six⟩

private theorem problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_tenth :
    ieeeDoubleFormat.minNormalMagnitude ≤ (1 / 10 : ℝ) := by
  norm_num [ieeeDoubleFormat, minNormalMagnitude, betaR, zpow_neg]
  have hden : (10 : ℝ) ≤ (2 : ℝ) ^ (1022 : ℕ) := by
    calc
      (10 : ℝ) ≤ (2 : ℝ) ^ (4 : ℕ) := by norm_num
      _ ≤ (2 : ℝ) ^ (1022 : ℕ) :=
        pow_le_pow_right₀ (by norm_num : (1 : ℝ) ≤ 2) (by norm_num)
  simpa [one_div] using
    one_div_le_one_div_of_le (by norm_num : (0 : ℝ) < 10) hden

private theorem problem2_10_ieeeDouble_oneTenth_finiteNormalRange :
    ieeeDoubleFormat.finiteNormalRange (1 / 10 : ℝ) := by
  rw [finiteNormalRange]
  rw [abs_of_nonneg (by norm_num : (0 : ℝ) ≤ 1 / 10)]
  constructor
  · exact problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_tenth
  · exact le_trans (by norm_num : (1 / 10 : ℝ) ≤ 1)
      problem2_10_one_le_ieeeDoubleFormat_maxFiniteMagnitude

/-- First-operation audit for the next displayed denominator `10`:
finite IEEE-double round-to-even rounds `1/10` upward to the nearest double. -/
theorem problem2_10_ieeeDouble_oneTenth_rounds_to_upper :
    ieeeDoubleFormat.finiteRoundToEven (1 / 10 : ℝ) =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 7205759403792793 (-3)
  let b : ℝ := fmt.normalizedValue false (7205759403792793 + 1) (-3)
  let x : ℝ := (1 / 10 : ℝ)
  have hm : fmt.normalizedMantissa 7205759403792793 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (7205759403792793 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 7205759403792793, (-3 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (7205759403792793 : ℝ) * (2 : ℝ) ^ (-56 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_oneTenth_finiteNormalRange
  have hrightCloser : |x - b| < |x - a| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_right_closer
      hpolicy hadj hstrict hrightCloser
  simpa [x, fmt, hb_value] using hround

/-- Operation-level version of the first `m = 1`, `n = 10` Problem 2.10
step. -/
theorem problem2_10_ieeeDouble_div_one_ten :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 10 =
      (7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((1 : ℝ) / 10) =
    (7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ)
  exact problem2_10_ieeeDouble_oneTenth_rounds_to_upper

/-- Result-by-result audit for the second `m = 1`, `n = 10` operation. -/
theorem problem2_10_ieeeDouble_div_one_ten_exact_mul_ten_above_one :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 10) * 10 =
      (1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_one_ten]
  exact problem2_10_ieeeDouble_rounded_oneTenth_mul_ten_above_one

/-- Denominator-`10`, numerator-`1` finite-selector instance. -/
theorem problem2_10_ieeeDouble_one_tenth_times_ten :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 10) 10 =
      (1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_one_ten]
  change ieeeDoubleFormat.finiteRoundToEven
      (((7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ)) * 10) = (1 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_oneTenth_mul_ten_above_one]
  exact problem2_10_ieeeDouble_one_plus_two_pow_neg54_rounds_to_one

/-- Signed companion of the first denominator-`10`, numerator-`1` operation. -/
theorem problem2_10_ieeeDouble_div_neg_one_ten :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 10 =
      -((7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-1 : ℝ) / 10) =
    -((7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ))
  have harg : ((-1 : ℝ) / 10) = -((1 : ℝ) / 10) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_oneTenth_rounds_to_upper]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

theorem problem2_10_ieeeDouble_div_neg_one_ten_exact_mul_ten_below_neg_one :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 10) * 10 =
      -((1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ)) := by
  rw [problem2_10_ieeeDouble_div_neg_one_ten]
  exact problem2_10_ieeeDouble_negative_rounded_oneTenth_mul_ten_below_neg_one

/-- Signed denominator-`10`, numerator-`-1` finite-selector instance. -/
theorem problem2_10_ieeeDouble_neg_one_tenth_times_ten :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 10) 10 =
      (-1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_one_ten]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((7205759403792794 : ℝ) * (2 : ℝ) ^ (-56 : ℤ))) * 10) = (-1 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_oneTenth_mul_ten_below_neg_one]
  exact problem2_10_ieeeDouble_negative_one_plus_two_pow_neg54_rounds_to_neg_one

/-- Signed denominator-`10`, numerator-`1` finite-selector trace. -/
theorem problem2_10_ieeeDouble_signed_one_tenth_times_ten :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 10) 10 =
        (1 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 10) 10 =
        (-1 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_one_tenth_times_ten,
    problem2_10_ieeeDouble_neg_one_tenth_times_ten⟩

/-- Problem 2.10 example, first operation: finite IEEE-double round-to-even
rounds `1/3` down to the nearest double. -/
theorem problem2_10_ieeeDouble_oneThird_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (1 / 3 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 (-1)
  let b : ℝ := fmt.normalizedValue false 6004799503160662 (-1)
  let x : ℝ := (1 / 3 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (-1 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_oneThird_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first Problem 2.10 example step. -/
theorem problem2_10_ieeeDouble_div_one_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((1 : ℝ) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ)
  exact problem2_10_ieeeDouble_oneThird_rounds_to_lower

/-- Result-by-result audit of the second Problem 2.10 operation: after the
rounded division has stored the lower IEEE-double approximation to `1/3`, the
exact multiplication by `3` is the midpoint immediately below `1`. -/
theorem problem2_10_ieeeDouble_div_one_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 3) * 3 =
      (1 : ℝ) - (2 : ℝ) ^ (-54 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_one_three]
  exact problem2_10_ieeeDouble_rounded_oneThird_mul_three_midpoint

/-- Problem 2.10 displayed concrete instance:
`fl((1/3) * 3) = 1` in the finite round-to-even IEEE-double model. -/
theorem problem2_10_ieeeDouble_one_third_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 3) 3 =
      (1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_one_three]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ)) * 3) = (1 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_oneThird_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_one_rounds_to_one

/-- Problem 2.10 denominator-`3` extension, first operation: finite
IEEE-double round-to-even rounds `2/3` down to the nearest double. -/
theorem problem2_10_ieeeDouble_twoThirds_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (2 / 3 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 0
  let b : ℝ := fmt.normalizedValue false 6004799503160662 0
  let x : ℝ := (2 / 3 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (0 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_twoThirds_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 2`, `n = 3` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_two_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((2 : ℝ) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ)
  exact problem2_10_ieeeDouble_twoThirds_rounds_to_lower

/-- Result-by-result audit for the `m = 2`, `n = 3` second operation: after
the rounded division has stored the lower IEEE-double approximation to `2/3`,
the exact multiplication by `3` is the midpoint immediately below `2`. -/
theorem problem2_10_ieeeDouble_div_two_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 3) * 3 =
      (2 : ℝ) - (2 : ℝ) ^ (-53 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_two_three]
  exact problem2_10_ieeeDouble_rounded_twoThirds_mul_three_midpoint

/-- Denominator-`3`, numerator-`2` second rounding fact: the midpoint below
`2` ties to the even endpoint `2`. -/
theorem problem2_10_ieeeDouble_midpoint_below_two_rounds_to_two :
    ieeeDoubleFormat.finiteRoundToEven
        ((2 : ℝ) - (2 : ℝ) ^ (-53 : ℤ)) = (2 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa 1
  let b : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 2
  let x : ℝ := (2 : ℝ) - (2 : ℝ) ^ (-53 : ℤ)
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (1 : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (2 : ℝ) - (2 : ℝ) ^ (-52 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa, zpow_neg]
  have hb_value : b = (2 : ℝ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
        (by norm_num [x, zpow_neg] : (1 / 3 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (2 : ℝ))
        problem2_10_two_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleft :
      a = fmt.normalizedValue false fmt.maxNormalMantissa (1 : ℤ) := rfl
  have htie : |x - a| = |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hodd : ¬ evenMantissa fmt.maxNormalMantissa := by
    norm_num [fmt, ieeeDoubleFormat, maxNormalMantissa, evenMantissa]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_tie_odd
      hpolicy hadj hstrict hm hleft htie hodd
  simpa [x, fmt, hb_value] using hround

/-- Problem 2.10 denominator-`3`, numerator-`2` finite-selector instance:
`fl((2/3) * 3) = 2` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_two_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 3) 3 =
      (2 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_two_three]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ)) * 3) = (2 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_twoThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_two_rounds_to_two

/-- Signed companion of the first operation: finite IEEE-double round-to-even
rounds `-1/3` by oddness of the round-to-even selector. -/
theorem problem2_10_ieeeDouble_div_neg_one_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-1 : ℝ) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ))
  have harg : ((-1 : ℝ) / 3) = -((1 : ℝ) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_oneThird_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit of the signed second operation: after the rounded
division has stored the IEEE-double approximation to `-1/3`, the exact
multiplication by `3` is the midpoint immediately above `-1`. -/
theorem problem2_10_ieeeDouble_div_neg_one_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 3) * 3 =
      -(1 : ℝ) + (2 : ℝ) ^ (-54 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_one_three]
  exact problem2_10_ieeeDouble_negative_rounded_oneThird_mul_three_midpoint

/-- Signed displayed finite-selector instance:
`fl((-1/3) * 3) = -1` in the finite round-to-even IEEE-double model. -/
theorem problem2_10_ieeeDouble_neg_one_third_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 3) 3 =
      (-1 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_one_three]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-54 : ℤ))) * 3) = (-1 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_oneThird_mul_three_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_neg_one_rounds_to_neg_one

/-- Signed companion of the `m = 2`, `n = 3` first operation: finite
IEEE-double round-to-even rounds `-2/3` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_two_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-2 : ℝ) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ))
  have harg : ((-2 : ℝ) / 3) = -((2 : ℝ) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_twoThirds_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -2`, `n = 3` second operation:
after the rounded division has stored the IEEE-double approximation to `-2/3`,
the exact multiplication by `3` is the midpoint immediately above `-2`. -/
theorem problem2_10_ieeeDouble_div_neg_two_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 3) * 3 =
      -(2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_two_three]
  exact problem2_10_ieeeDouble_negative_rounded_twoThirds_mul_three_midpoint

/-- Signed `m = -2`, `n = 3` second rounding fact: the midpoint above `-2`
ties to the even endpoint `-2`. -/
theorem problem2_10_ieeeDouble_negative_midpoint_above_two_rounds_to_neg_two :
    ieeeDoubleFormat.finiteRoundToEven
        (-(2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ)) = (-2 : ℝ) := by
  have harg :
      (-(2 : ℝ) + (2 : ℝ) ^ (-53 : ℤ)) =
        -((2 : ℝ) - (2 : ℝ) ^ (-53 : ℤ)) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_midpoint_below_two_rounds_to_two]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Signed denominator-`3`, numerator-`-2` finite-selector instance:
`fl((-2/3) * 3) = -2` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_two_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 3) 3 =
      (-2 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_two_three]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-53 : ℤ))) * 3) = (-2 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_twoThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_two_rounds_to_neg_two

/-- Denominator-`3`, numerator-`4` first operation: finite IEEE-double
round-to-even rounds `4/3` down to the nearest double. -/
theorem problem2_10_ieeeDouble_fourThirds_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (4 / 3 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 1
  let b : ℝ := fmt.normalizedValue false 6004799503160662 1
  let x : ℝ := (4 / 3 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (1 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-52 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_fourThirds_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 4`, `n = 3` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_four_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (4 : ℝ) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((4 : ℝ) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ)
  exact problem2_10_ieeeDouble_fourThirds_rounds_to_lower

/-- Result-by-result audit for the `m = 4`, `n = 3` second operation: after
the rounded division has stored the lower IEEE-double approximation to `4/3`,
the exact multiplication by `3` is the midpoint immediately below `4`. -/
theorem problem2_10_ieeeDouble_div_four_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (4 : ℝ) 3) * 3 =
      (4 : ℝ) - (2 : ℝ) ^ (-52 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_four_three]
  exact problem2_10_ieeeDouble_rounded_fourThirds_mul_three_midpoint

/-- Denominator-`3`, numerator-`4` second rounding fact: the midpoint below
`4` ties to the even endpoint `4`. -/
theorem problem2_10_ieeeDouble_midpoint_below_four_rounds_to_four :
    ieeeDoubleFormat.finiteRoundToEven
        ((4 : ℝ) - (2 : ℝ) ^ (-52 : ℤ)) = (4 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa 2
  let b : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 3
  let x : ℝ := (4 : ℝ) - (2 : ℝ) ^ (-52 : ℤ)
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (2 : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (4 : ℝ) - (2 : ℝ) ^ (-51 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa, zpow_neg]
  have hb_value : b = (4 : ℝ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
        (by norm_num [x, zpow_neg] : (1 / 3 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (4 : ℝ))
        problem2_10_four_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleft :
      a = fmt.normalizedValue false fmt.maxNormalMantissa (2 : ℤ) := rfl
  have htie : |x - a| = |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hodd : ¬ evenMantissa fmt.maxNormalMantissa := by
    norm_num [fmt, ieeeDoubleFormat, maxNormalMantissa, evenMantissa]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_tie_odd
      hpolicy hadj hstrict hm hleft htie hodd
  simpa [x, fmt, hb_value] using hround

/-- Problem 2.10 denominator-`3`, numerator-`4` finite-selector instance:
`fl((4/3) * 3) = 4` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_four_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (4 : ℝ) 3) 3 =
      (4 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_four_three]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ)) * 3) = (4 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_fourThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_four_rounds_to_four

/-- Signed companion of the `m = 4`, `n = 3` first operation: finite
IEEE-double round-to-even rounds `-4/3` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_four_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-4 : ℝ) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-4 : ℝ) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ))
  have harg : ((-4 : ℝ) / 3) = -((4 : ℝ) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_fourThirds_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -4`, `n = 3` second operation:
after the rounded division has stored the IEEE-double approximation to `-4/3`,
the exact multiplication by `3` is the midpoint immediately above `-4`. -/
theorem problem2_10_ieeeDouble_div_neg_four_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-4 : ℝ) 3) * 3 =
      -(4 : ℝ) + (2 : ℝ) ^ (-52 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_four_three]
  exact problem2_10_ieeeDouble_negative_rounded_fourThirds_mul_three_midpoint

/-- Signed `m = -4`, `n = 3` second rounding fact: the midpoint above `-4`
ties to the even endpoint `-4`. -/
theorem problem2_10_ieeeDouble_negative_midpoint_above_four_rounds_to_neg_four :
    ieeeDoubleFormat.finiteRoundToEven
        (-(4 : ℝ) + (2 : ℝ) ^ (-52 : ℤ)) = (-4 : ℝ) := by
  have harg :
      (-(4 : ℝ) + (2 : ℝ) ^ (-52 : ℤ)) =
        -((4 : ℝ) - (2 : ℝ) ^ (-52 : ℤ)) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_midpoint_below_four_rounds_to_four]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Signed denominator-`3`, numerator-`-4` finite-selector instance:
`fl((-4/3) * 3) = -4` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_four_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-4 : ℝ) 3) 3 =
      (-4 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_four_three]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-52 : ℤ))) * 3) = (-4 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_fourThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_four_rounds_to_neg_four

/-- Denominator-`3`, numerator-`8` first operation: finite IEEE-double
round-to-even rounds `8/3` down to the nearest double. -/
theorem problem2_10_ieeeDouble_eightThirds_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (8 / 3 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 2
  let b : ℝ := fmt.normalizedValue false 6004799503160662 2
  let x : ℝ := (8 / 3 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (2 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-51 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_eightThirds_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 8`, `n = 3` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_eight_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (8 : ℝ) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((8 : ℝ) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ)
  exact problem2_10_ieeeDouble_eightThirds_rounds_to_lower

/-- Result-by-result audit for the `m = 8`, `n = 3` second operation: after
the rounded division has stored the lower IEEE-double approximation to `8/3`,
the exact multiplication by `3` is the midpoint immediately below `8`. -/
theorem problem2_10_ieeeDouble_div_eight_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (8 : ℝ) 3) * 3 =
      (8 : ℝ) - (2 : ℝ) ^ (-51 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_eight_three]
  exact problem2_10_ieeeDouble_rounded_eightThirds_mul_three_midpoint

/-- Denominator-`3`, numerator-`8` second rounding fact: the midpoint below
`8` ties to the even endpoint `8`. -/
theorem problem2_10_ieeeDouble_midpoint_below_eight_rounds_to_eight :
    ieeeDoubleFormat.finiteRoundToEven
        ((8 : ℝ) - (2 : ℝ) ^ (-51 : ℤ)) = (8 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa 3
  let b : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 4
  let x : ℝ := (8 : ℝ) - (2 : ℝ) ^ (-51 : ℤ)
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (3 : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (8 : ℝ) - (2 : ℝ) ^ (-50 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa, zpow_neg]
  have hb_value : b = (8 : ℝ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
        (by norm_num [x, zpow_neg] : (1 / 3 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (8 : ℝ))
        problem2_10_eight_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleft :
      a = fmt.normalizedValue false fmt.maxNormalMantissa (3 : ℤ) := rfl
  have htie : |x - a| = |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hodd : ¬ evenMantissa fmt.maxNormalMantissa := by
    norm_num [fmt, ieeeDoubleFormat, maxNormalMantissa, evenMantissa]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_tie_odd
      hpolicy hadj hstrict hm hleft htie hodd
  simpa [x, fmt, hb_value] using hround

/-- Problem 2.10 denominator-`3`, numerator-`8` finite-selector instance:
`fl((8/3) * 3) = 8` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_eight_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (8 : ℝ) 3) 3 =
      (8 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_eight_three]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ)) * 3) = (8 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_eightThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_eight_rounds_to_eight

/-- Signed companion of the `m = 8`, `n = 3` first operation: finite
IEEE-double round-to-even rounds `-8/3` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_eight_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-8 : ℝ) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-8 : ℝ) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ))
  have harg : ((-8 : ℝ) / 3) = -((8 : ℝ) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_eightThirds_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -8`, `n = 3` second operation:
after the rounded division has stored the IEEE-double approximation to `-8/3`,
the exact multiplication by `3` is the midpoint immediately above `-8`. -/
theorem problem2_10_ieeeDouble_div_neg_eight_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-8 : ℝ) 3) * 3 =
      -(8 : ℝ) + (2 : ℝ) ^ (-51 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_eight_three]
  exact problem2_10_ieeeDouble_negative_rounded_eightThirds_mul_three_midpoint

/-- Signed `m = -8`, `n = 3` second rounding fact: the midpoint above `-8`
ties to the even endpoint `-8`. -/
theorem problem2_10_ieeeDouble_negative_midpoint_above_eight_rounds_to_neg_eight :
    ieeeDoubleFormat.finiteRoundToEven
        (-(8 : ℝ) + (2 : ℝ) ^ (-51 : ℤ)) = (-8 : ℝ) := by
  have harg :
      (-(8 : ℝ) + (2 : ℝ) ^ (-51 : ℤ)) =
        -((8 : ℝ) - (2 : ℝ) ^ (-51 : ℤ)) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_midpoint_below_eight_rounds_to_eight]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Signed denominator-`3`, numerator-`-8` finite-selector instance:
`fl((-8/3) * 3) = -8` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_eight_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-8 : ℝ) 3) 3 =
      (-8 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_eight_three]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-51 : ℤ))) * 3) = (-8 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_eightThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_eight_rounds_to_neg_eight

/-- Denominator-`3`, numerator-`16` first operation: finite IEEE-double
round-to-even rounds `16/3` down to the nearest double. -/
theorem problem2_10_ieeeDouble_sixteenThirds_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (16 / 3 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 3
  let b : ℝ := fmt.normalizedValue false 6004799503160662 3
  let x : ℝ := (16 / 3 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (3 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-50 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_sixteenThirds_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 16`, `n = 3` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_sixteen_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (16 : ℝ) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((16 : ℝ) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ)
  exact problem2_10_ieeeDouble_sixteenThirds_rounds_to_lower

/-- Result-by-result audit for the `m = 16`, `n = 3` second operation: after
the rounded division has stored the lower IEEE-double approximation to `16/3`,
the exact multiplication by `3` is the midpoint immediately below `16`. -/
theorem problem2_10_ieeeDouble_div_sixteen_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (16 : ℝ) 3) * 3 =
      (16 : ℝ) - (2 : ℝ) ^ (-50 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_sixteen_three]
  exact problem2_10_ieeeDouble_rounded_sixteenThirds_mul_three_midpoint

/-- Denominator-`3`, numerator-`16` second rounding fact: the midpoint below
`16` ties to the even endpoint `16`. -/
theorem problem2_10_ieeeDouble_midpoint_below_sixteen_rounds_to_sixteen :
    ieeeDoubleFormat.finiteRoundToEven
        ((16 : ℝ) - (2 : ℝ) ^ (-50 : ℤ)) = (16 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa 4
  let b : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 5
  let x : ℝ := (16 : ℝ) - (2 : ℝ) ^ (-50 : ℤ)
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (4 : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (16 : ℝ) - (2 : ℝ) ^ (-49 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa, zpow_neg]
  have hb_value : b = (16 : ℝ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
        (by norm_num [x, zpow_neg] : (1 / 3 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (16 : ℝ))
        problem2_10_sixteen_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleft :
      a = fmt.normalizedValue false fmt.maxNormalMantissa (4 : ℤ) := rfl
  have htie : |x - a| = |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hodd : ¬ evenMantissa fmt.maxNormalMantissa := by
    norm_num [fmt, ieeeDoubleFormat, maxNormalMantissa, evenMantissa]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_tie_odd
      hpolicy hadj hstrict hm hleft htie hodd
  simpa [x, fmt, hb_value] using hround

/-- Problem 2.10 denominator-`3`, numerator-`16` finite-selector instance:
`fl((16/3) * 3) = 16` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_sixteen_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (16 : ℝ) 3) 3 =
      (16 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_sixteen_three]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ)) * 3) = (16 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_sixteenThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_sixteen_rounds_to_sixteen

/-- Signed companion of the `m = 16`, `n = 3` first operation: finite
IEEE-double round-to-even rounds `-16/3` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_sixteen_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-16 : ℝ) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-16 : ℝ) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ))
  have harg : ((-16 : ℝ) / 3) = -((16 : ℝ) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_sixteenThirds_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -16`, `n = 3` second operation:
after the rounded division has stored the IEEE-double approximation to `-16/3`,
the exact multiplication by `3` is the midpoint immediately above `-16`. -/
theorem problem2_10_ieeeDouble_div_neg_sixteen_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-16 : ℝ) 3) * 3 =
      -(16 : ℝ) + (2 : ℝ) ^ (-50 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_sixteen_three]
  exact problem2_10_ieeeDouble_negative_rounded_sixteenThirds_mul_three_midpoint

/-- Signed `m = -16`, `n = 3` second rounding fact: the midpoint above `-16`
ties to the even endpoint `-16`. -/
theorem problem2_10_ieeeDouble_negative_midpoint_above_sixteen_rounds_to_neg_sixteen :
    ieeeDoubleFormat.finiteRoundToEven
        (-(16 : ℝ) + (2 : ℝ) ^ (-50 : ℤ)) = (-16 : ℝ) := by
  have harg :
      (-(16 : ℝ) + (2 : ℝ) ^ (-50 : ℤ)) =
        -((16 : ℝ) - (2 : ℝ) ^ (-50 : ℤ)) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_midpoint_below_sixteen_rounds_to_sixteen]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Signed denominator-`3`, numerator-`-16` finite-selector instance:
`fl((-16/3) * 3) = -16` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_sixteen_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-16 : ℝ) 3) 3 =
      (-16 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_sixteen_three]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-50 : ℤ))) * 3) = (-16 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_sixteenThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_sixteen_rounds_to_neg_sixteen

/-- Denominator-`3`, numerator-`32` first operation: finite IEEE-double
round-to-even rounds `32/3` down to the nearest double. -/
theorem problem2_10_ieeeDouble_thirtytwoThirds_rounds_to_lower :
    ieeeDoubleFormat.finiteRoundToEven (32 / 3 : ℝ) =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false 6004799503160661 4
  let b : ℝ := fmt.normalizedValue false 6004799503160662 4
  let x : ℝ := (32 / 3 : ℝ)
  have hm : fmt.normalizedMantissa 6004799503160661 := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hmnext : fmt.normalizedMantissa (6004799503160661 + 1) := by
    norm_num [fmt, ieeeDoubleFormat, normalizedMantissa, mantissaInRange,
      minNormalMantissa]
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_sameExponentAdjacentNormalized
      ⟨false, 6004799503160661, (4 : ℤ), hm, hmnext,
        Or.inl ⟨rfl, rfl⟩⟩
  have ha_value :
      a = (6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hb_value :
      b = (6004799503160662 : ℝ) * (2 : ℝ) ^ (-49 : ℤ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      zpow_neg]
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange
      problem2_10_ieeeDouble_thirtytwoThirds_finiteNormalRange
  have hleftCloser : |x - a| < |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hround : fmt.finiteRoundToEven x = a :=
    fmt.sourceRoundToEvenEvidence_eq_left_of_realOrderAdjacent_strict_between_left_closer
      hpolicy hadj hstrict hleftCloser
  simpa [x, fmt, ha_value] using hround

/-- Operation-level version of the first `m = 32`, `n = 3` Problem 2.10 step. -/
theorem problem2_10_ieeeDouble_div_thirtytwo_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (32 : ℝ) 3 =
      (6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ) := by
  change ieeeDoubleFormat.finiteRoundToEven ((32 : ℝ) / 3) =
    (6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ)
  exact problem2_10_ieeeDouble_thirtytwoThirds_rounds_to_lower

/-- Result-by-result audit for the `m = 32`, `n = 3` second operation: after
the rounded division has stored the lower IEEE-double approximation to `32/3`,
the exact multiplication by `3` is the midpoint immediately below `32`. -/
theorem problem2_10_ieeeDouble_div_thirtytwo_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (32 : ℝ) 3) * 3 =
      (32 : ℝ) - (2 : ℝ) ^ (-49 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_thirtytwo_three]
  exact problem2_10_ieeeDouble_rounded_thirtytwoThirds_mul_three_midpoint

/-- Denominator-`3`, numerator-`32` second rounding fact: the midpoint below
`32` ties to the even endpoint `32`. -/
theorem problem2_10_ieeeDouble_midpoint_below_thirtytwo_rounds_to_thirtytwo :
    ieeeDoubleFormat.finiteRoundToEven
        ((32 : ℝ) - (2 : ℝ) ^ (-49 : ℤ)) = (32 : ℝ) := by
  let fmt := ieeeDoubleFormat
  let a : ℝ := fmt.normalizedValue false fmt.maxNormalMantissa 5
  let b : ℝ := fmt.normalizedValue false fmt.minNormalMantissa 6
  let x : ℝ := (32 : ℝ) - (2 : ℝ) ^ (-49 : ℤ)
  have hm : fmt.normalizedMantissa fmt.maxNormalMantissa :=
    fmt.maxNormalMantissa_normalized
  have hadj : fmt.realOrderAdjacentNormalized a b :=
    fmt.realOrderAdjacentNormalized_of_boundaryAdjacentNormalized
      ⟨false, (5 : ℤ), Or.inl ⟨rfl, rfl⟩⟩
  have ha_value : a = (32 : ℝ) - (2 : ℝ) ^ (-48 : ℤ) := by
    norm_num [a, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      maxNormalMantissa, zpow_neg]
  have hb_value : b = (32 : ℝ) := by
    norm_num [b, fmt, ieeeDoubleFormat, normalizedValue, signValue, betaR,
      minNormalMantissa, zpow_neg]
    rfl
  have hstrict : a < x ∧ x < b := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hxrange : fmt.finiteNormalRange x := by
    rw [finiteNormalRange]
    have hxnonneg : 0 ≤ x := by norm_num [x, zpow_neg]
    rw [abs_of_nonneg hxnonneg]
    constructor
    · exact le_trans problem2_10_ieeeDoubleFormat_minNormalMagnitude_le_one_third
        (by norm_num [x, zpow_neg] : (1 / 3 : ℝ) ≤ x)
    · exact le_trans (by norm_num [x, zpow_neg] : x ≤ (32 : ℝ))
        problem2_10_thirtytwo_le_ieeeDoubleFormat_maxFiniteMagnitude
  have hpolicy :
      fmt.sourceRoundToEvenEvidence x (fmt.finiteRoundToEven x) :=
    fmt.finiteRoundToEven_sourceRoundToEvenEvidence_of_finiteNormalRange hxrange
  have hleft :
      a = fmt.normalizedValue false fmt.maxNormalMantissa (5 : ℤ) := rfl
  have htie : |x - a| = |x - b| := by
    rw [ha_value, hb_value]
    norm_num [x, zpow_neg]
  have hodd : ¬ evenMantissa fmt.maxNormalMantissa := by
    norm_num [fmt, ieeeDoubleFormat, maxNormalMantissa, evenMantissa]
  have hround : fmt.finiteRoundToEven x = b :=
    fmt.sourceRoundToEvenEvidence_eq_right_of_realOrderAdjacent_strict_between_tie_odd
      hpolicy hadj hstrict hm hleft htie hodd
  simpa [x, fmt, hb_value] using hround

/-- Problem 2.10 denominator-`3`, numerator-`32` finite-selector instance:
`fl((32/3) * 3) = 32` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_thirtytwo_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (32 : ℝ) 3) 3 =
      (32 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_thirtytwo_three]
  change ieeeDoubleFormat.finiteRoundToEven
      (((6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ)) * 3) = (32 : ℝ)
  rw [problem2_10_ieeeDouble_rounded_thirtytwoThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_midpoint_below_thirtytwo_rounds_to_thirtytwo

/-- Signed companion of the `m = 32`, `n = 3` first operation: finite
IEEE-double round-to-even rounds `-32/3` by oddness of the selector. -/
theorem problem2_10_ieeeDouble_div_neg_thirtytwo_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-32 : ℝ) 3 =
      -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ)) := by
  change ieeeDoubleFormat.finiteRoundToEven ((-32 : ℝ) / 3) =
    -((6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ))
  have harg : ((-32 : ℝ) / 3) = -((32 : ℝ) / 3) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_thirtytwoThirds_rounds_to_lower]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Result-by-result audit for the signed `m = -32`, `n = 3` second operation:
after the rounded division has stored the IEEE-double approximation to `-32/3`,
the exact multiplication by `3` is the midpoint immediately above `-32`. -/
theorem problem2_10_ieeeDouble_div_neg_thirtytwo_three_exact_mul_three_midpoint :
    (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-32 : ℝ) 3) * 3 =
      -(32 : ℝ) + (2 : ℝ) ^ (-49 : ℤ) := by
  rw [problem2_10_ieeeDouble_div_neg_thirtytwo_three]
  exact problem2_10_ieeeDouble_negative_rounded_thirtytwoThirds_mul_three_midpoint

/-- Signed `m = -32`, `n = 3` second rounding fact: the midpoint above `-32`
ties to the even endpoint `-32`. -/
theorem problem2_10_ieeeDouble_negative_midpoint_above_thirtytwo_rounds_to_neg_thirtytwo :
    ieeeDoubleFormat.finiteRoundToEven
        (-(32 : ℝ) + (2 : ℝ) ^ (-49 : ℤ)) = (-32 : ℝ) := by
  have harg :
      (-(32 : ℝ) + (2 : ℝ) ^ (-49 : ℤ)) =
        -((32 : ℝ) - (2 : ℝ) ^ (-49 : ℤ)) := by ring
  rw [harg]
  rw [ieeeDoubleFormat.finiteRoundToEven_neg]
  · rw [problem2_10_ieeeDouble_midpoint_below_thirtytwo_rounds_to_thirtytwo]
  · norm_num [evenMantissa, ieeeDoubleFormat]
  · norm_num [ieeeDoubleFormat]

/-- Signed denominator-`3`, numerator-`-32` finite-selector instance:
`fl((-32/3) * 3) = -32` in the IEEE-double finite round-to-even model. -/
theorem problem2_10_ieeeDouble_neg_thirtytwo_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
        (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-32 : ℝ) 3) 3 =
      (-32 : ℝ) := by
  rw [problem2_10_ieeeDouble_div_neg_thirtytwo_three]
  change ieeeDoubleFormat.finiteRoundToEven
      ((-((6004799503160661 : ℝ) * (2 : ℝ) ^ (-49 : ℤ))) * 3) = (-32 : ℝ)
  rw [problem2_10_ieeeDouble_negative_rounded_thirtytwoThirds_mul_three_midpoint]
  exact problem2_10_ieeeDouble_negative_midpoint_above_thirtytwo_rounds_to_neg_thirtytwo

/-- Packaged signed audit for the displayed denominator `n = 3`: the finite
IEEE-double selector recovers both `m = 1` and `m = -1`. -/
theorem problem2_10_ieeeDouble_signed_one_third_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (1 : ℝ) 3) 3 =
        (1 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-1 : ℝ) 3) 3 =
        (-1 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_one_third_times_three,
    problem2_10_ieeeDouble_neg_one_third_times_three⟩

/-- Packaged signed audit for denominator `n = 3` and numerator magnitude `2`:
the finite IEEE-double selector recovers both `m = 2` and `m = -2`. -/
theorem problem2_10_ieeeDouble_signed_two_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (2 : ℝ) 3) 3 =
        (2 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-2 : ℝ) 3) 3 =
        (-2 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_two_thirds_times_three,
    problem2_10_ieeeDouble_neg_two_thirds_times_three⟩

/-- Packaged signed audit for denominator `n = 3` and numerator magnitude `4`:
the finite IEEE-double selector recovers both `m = 4` and `m = -4`. -/
theorem problem2_10_ieeeDouble_signed_four_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (4 : ℝ) 3) 3 =
        (4 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-4 : ℝ) 3) 3 =
        (-4 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_four_thirds_times_three,
    problem2_10_ieeeDouble_neg_four_thirds_times_three⟩

/-- Packaged signed audit for denominator `n = 3` and numerator magnitude `8`:
the finite IEEE-double selector recovers both `m = 8` and `m = -8`. -/
theorem problem2_10_ieeeDouble_signed_eight_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (8 : ℝ) 3) 3 =
        (8 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-8 : ℝ) 3) 3 =
        (-8 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_eight_thirds_times_three,
    problem2_10_ieeeDouble_neg_eight_thirds_times_three⟩

/-- Packaged signed audit for denominator `n = 3` and numerator magnitude `16`:
the finite IEEE-double selector recovers both `m = 16` and `m = -16`. -/
theorem problem2_10_ieeeDouble_signed_sixteen_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (16 : ℝ) 3) 3 =
        (16 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-16 : ℝ) 3) 3 =
        (-16 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_sixteen_thirds_times_three,
    problem2_10_ieeeDouble_neg_sixteen_thirds_times_three⟩

/-- Packaged signed audit for denominator `n = 3` and numerator magnitude `32`:
the finite IEEE-double selector recovers both `m = 32` and `m = -32`. -/
theorem problem2_10_ieeeDouble_signed_thirtytwo_thirds_times_three :
    ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (32 : ℝ) 3) 3 =
        (32 : ℝ) ∧
      ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.mul
          (ieeeDoubleFormat.finiteRoundToEvenOp BasicOp.div (-32 : ℝ) 3) 3 =
        (-32 : ℝ) := by
  exact ⟨problem2_10_ieeeDouble_thirtytwo_thirds_times_three,
    problem2_10_ieeeDouble_neg_thirtytwo_thirds_times_three⟩

end FloatingPointFormat
end NumStability

end
