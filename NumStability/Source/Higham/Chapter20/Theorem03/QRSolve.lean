import Mathlib.Algebra.BigOperators.Group.Finset.Basic
import Mathlib.Algebra.Order.BigOperators.Group.Finset
import Mathlib.Analysis.Matrix.Spectrum
import Mathlib.Data.Real.Basic
import Mathlib.LinearAlgebra.Matrix.Rank
import Mathlib.Tactic.FieldSimp
import Mathlib.Tactic.Linarith
import Mathlib.Tactic.Ring
import NumStability.Algorithms.DotProduct
import NumStability.Algorithms.LinearSystems.LeastSquares.AugmentedSystem
import NumStability.Algorithms.LinearSystems.LeastSquares.Basic
import NumStability.Algorithms.LinearSystems.LeastSquares.GramBasis
import NumStability.Algorithms.LinearSystems.LeastSquares.NormalEquations
import NumStability.Algorithms.LinearSystems.LeastSquares.QRSolve
import NumStability.Algorithms.LinearSystems.LeastSquares.RankGeometry
import NumStability.Algorithms.LinearSystems.LeastSquares.StoredQR
import NumStability.Algorithms.LinearSystems.QR.HouseholderApplySupport
import NumStability.Algorithms.LinearSystems.QR.HouseholderQR
import NumStability.Algorithms.LinearSystems.QR.HouseholderQRSupport
import NumStability.Algorithms.LinearSystems.QR.HouseholderSpec
import NumStability.Algorithms.LinearSystems.QR.HouseholderSpecSupport
import NumStability.Algorithms.LinearSystems.QR.QRSolve
import NumStability.Algorithms.LinearSystems.Triangular.BackSubstitution
import NumStability.Algorithms.LinearSystems.Triangular.DiagonalDominance
import NumStability.Algorithms.LinearSystems.Triangular.ForwardSubstitution
import NumStability.Algorithms.LinearSystems.Triangular.InverseBounds
import NumStability.Algorithms.RandomizedLinearAlgebra.LowRankApproximation.ColumnSketches.Core
import NumStability.Analysis.MatrixAlgebra
import NumStability.Analysis.MatrixNorms.Basic
import NumStability.Analysis.MatrixSpectral
import NumStability.Analysis.Perturbation.LeastSquares.AugmentedSystem
import NumStability.Analysis.Perturbation.LeastSquares.BackwardError
import NumStability.Analysis.Perturbation.LeastSquares.Basic
import NumStability.Analysis.Perturbation.LeastSquares.GramBasis
import NumStability.Analysis.Perturbation.LeastSquares.Normwise
import NumStability.Analysis.Perturbation.LeastSquares.Wedin
import NumStability.Analysis.PerturbationTheory
import NumStability.Analysis.Rounding
import NumStability.Analysis.SingularValues.Basic
import NumStability.Analysis.SingularValues.Realification
import NumStability.Analysis.VectorNorms.Basic
import NumStability.FloatingPoint.Model
import NumStability.Source.Higham.Chapter20.Lemma11.Support

namespace NumStability

open scoped BigOperators Matrix.Norms.Frobenius

/-!
# Higham Chapter 20 — QRSolve

Canonical source correspondence module extracted without change from LSQRSolve.
-/

private theorem lsResidualHigham_column_sum_eq_neg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (x : Fin n → ℝ)
    (j : Fin n) :
    ∑ i : Fin m, A i j * lsResidualHigham A b x i =
      -∑ i : Fin m, A i j * lsResidual A b x i := by
  rw [lsResidualHigham_eq_neg_lsResidual A b x]
  simp_rw [mul_neg]
  rw [← Finset.sum_neg_distrib]
/-- Higham, 2nd ed., Chapter 20, equation (20.3): using Higham's residual
    sign convention, the augmented system is equivalent to the rectangular
    normal equations. -/
theorem LSAugmentedNormalSystem.iff_rectLSNormalEquations {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (x : Fin n → ℝ) :
    LSAugmentedNormalSystem A b (lsResidualHigham A b x) x ↔
      RectLSNormalEquations A b x := by
  constructor
  · intro h
    apply RectLSNormalEquations.of_residual_orthogonal
    intro j
    have hbook := h.2 j
    rw [lsResidualHigham_column_sum_eq_neg A b x j] at hbook
    linarith
  · intro h
    constructor
    · intro i
      unfold lsResidualHigham
      ring
    · intro j
      have horth := h.residual_orthogonal j
      rw [lsResidualHigham_column_sum_eq_neg A b x j, horth]
      ring
/-- Combining (20.15) with the zero-`g` specialization recovers the exact
    normal-equation characterization from (20.3). -/
theorem LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (x : Fin n → ℝ) :
    LSAugmentedSystem A b (0 : Fin n → ℝ) (lsResidualHigham A b x) x ↔
      RectLSNormalEquations A b x := by
  rw [LSAugmentedSystem.iff_augmentedNormalSystem_zero_rhs]
  exact LSAugmentedNormalSystem.iff_rectLSNormalEquations A b x
/-- Equation (20.4) with `s = b + Deltab - (A + DeltaA)y` is equivalent to
    the normal equations for the perturbed least-squares problem. -/
theorem LSPerturbedAugmentedSystem.iff_rectLSNormalEquations {m n : ℕ}
    (A DeltaA : Fin m → Fin n → ℝ) (b Deltab : Fin m → ℝ)
    (y : Fin n → ℝ) :
    LSPerturbedAugmentedSystem A DeltaA b Deltab
        (lsResidualHigham (fun i j => A i j + DeltaA i j)
          (fun i => b i + Deltab i) y) y ↔
      RectLSNormalEquations (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y := by
  exact
    LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs
      (fun i j => A i j + DeltaA i j) (fun i => b i + Deltab i) y
/-- Instantiate Higham's inverse action (20.6) as the actual residual and
    solution differences between an exact least-squares augmented system and a
    perturbed one. -/
theorem lsAugmentedEq20_6_action_eq_differences_of_perturbed_systems {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (gramInv : Fin n → Fin n → ℝ)
    (DeltaA : Fin m → Fin n → ℝ) (b Deltab : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ)
    (hAplus : ∀ j i, Aplus j i = ∑ k : Fin n, gramInv j k * A i k)
    (hGramInv : IsInverse n (rectLSGram A) gramInv)
    (hGramInv_symm : ∀ j k : Fin n, gramInv j k = gramInv k j)
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y) :
    (∀ i : Fin m,
      lsAugmentedInverseActionTop A Aplus
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s) i = s i - r i) ∧
    (∀ j : Fin n,
      lsAugmentedInverseActionBottom Aplus gramInv
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s) j = y j - x j) := by
  let u : Fin m → ℝ := lsEq20_6RhsTop DeltaA Deltab y
  let v : Fin n → ℝ := lsEq20_6RhsBottom DeltaA s
  have hAction :
      LSAugmentedSystem A u v
        (lsAugmentedInverseActionTop A Aplus u v)
        (lsAugmentedInverseActionBottom Aplus gramInv u v) :=
    LSAugmentedSystem.of_eq20_6_inverse_action A Aplus gramInv u v
      hAplus hGramInv hGramInv_symm
  have hDiff :
      LSAugmentedSystem A u v
        (fun i => s i - r i) (fun j => y j - x j) := by
    simpa [u, v] using
      LSAugmentedSystem.difference_of_perturbed A DeltaA b Deltab r s x y
        hExact hPert
  have huniq :=
    LSAugmentedSystem.eq_of_gram_inverse A u v
      (lsAugmentedInverseActionTop A Aplus u v) (fun i => s i - r i)
      (lsAugmentedInverseActionBottom Aplus gramInv u v) (fun j => y j - x j)
      gramInv hGramInv hAction hDiff
  constructor
  · intro i
    simpa [u, v] using congrFun huniq.1 i
  · intro j
    simpa [u, v] using congrFun huniq.2 j
/-- First summand in the residual-side majorant from (20.7):
    `|I-AA^+| U`. -/
noncomputable def lsAugmentedEq20_7LeftMajorant {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (U : Fin m → ℝ) : Fin m → ℝ :=
  rectMatMulVec (absMatrixRect (lsAugmentedProjectionBlock A Aplus)) U
/-- Second summand in the residual-side majorant from (20.7):
    `|A^+|^T V`. -/
noncomputable def lsAugmentedEq20_7RightMajorant {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) (V : Fin n → ℝ) : Fin m → ℝ :=
  fun i => ∑ j : Fin n, |Aplus j i| * V j
/-- The residual-side majorant in Higham, 2nd ed., Chapter 20, equation
    (20.7): `|I-AA^+| U + |A^+|^T V`. -/
noncomputable def lsAugmentedEq20_7Majorant {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (U : Fin m → ℝ) (V : Fin n → ℝ) : Fin m → ℝ :=
  fun i =>
    rectMatMulVec (absMatrixRect (lsAugmentedProjectionBlock A Aplus)) U i +
      ∑ j : Fin n, |Aplus j i| * V j
/-- First summand in the solution-side majorant from (20.8):
    `|A^+| U`. -/
noncomputable def lsAugmentedEq20_8LeftMajorant {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) (U : Fin m → ℝ) : Fin n → ℝ :=
  rectMatMulVec (absMatrixRect Aplus) U
/-- Second summand in the solution-side majorant from (20.8):
    `|(A^T A)^{-1}| V`. -/
noncomputable def lsAugmentedEq20_8RightMajorant {n : ℕ}
    (gramInv : Fin n → Fin n → ℝ) (V : Fin n → ℝ) : Fin n → ℝ :=
  matMulVec n (absMatrix n gramInv) V
/-- The solution-side majorant in Higham, 2nd ed., Chapter 20, equation
    (20.8): `|A^+| U + |(A^T A)^{-1}| V`. -/
noncomputable def lsAugmentedEq20_8Majorant {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    (U : Fin m → ℝ) (V : Fin n → ℝ) : Fin n → ℝ :=
  fun j =>
    rectMatMulVec (absMatrixRect Aplus) U j +
      matMulVec n (absMatrix n gramInv) V j
private theorem rectMatMulVec_absMatrixRect_nonneg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) {x : Fin n → ℝ}
    (hx : ∀ j, 0 ≤ x j) :
    ∀ i : Fin m, 0 ≤ rectMatMulVec (absMatrixRect A) x i := by
  intro i
  unfold rectMatMulVec absMatrixRect
  exact Finset.sum_nonneg (by
    intro j _
    exact mul_nonneg (abs_nonneg (A i j)) (hx j))
private theorem matMulVec_absMatrix_nonneg {n : ℕ}
    (A : Fin n → Fin n → ℝ) {x : Fin n → ℝ}
    (hx : ∀ j, 0 ≤ x j) :
    ∀ i : Fin n, 0 ≤ matMulVec n (absMatrix n A) x i := by
  intro i
  unfold matMulVec absMatrix
  exact Finset.sum_nonneg (by
    intro j _
    exact mul_nonneg (abs_nonneg (A i j)) (hx j))
private theorem lsComponentwiseDataMajorant_nonneg {m n : ℕ}
    {E : Fin m → Fin n → ℝ} {f : Fin m → ℝ} {y : Fin n → ℝ}
    (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i) :
    ∀ i : Fin m, 0 ≤ lsComponentwiseDataMajorant E f y i := by
  intro i
  unfold lsComponentwiseDataMajorant rectMatMulVec absVec
  exact add_nonneg (hf i) (Finset.sum_nonneg (by
    intro j _
    exact mul_nonneg (hE i j) (abs_nonneg (y j))))
private theorem lsComponentwiseTransposeMajorant_nonneg {m n : ℕ}
    {E : Fin m → Fin n → ℝ} {s : Fin m → ℝ}
    (hE : ∀ i j, 0 ≤ E i j) :
    ∀ j : Fin n, 0 ≤ lsComponentwiseTransposeMajorant E s j := by
  intro j
  unfold lsComponentwiseTransposeMajorant
  exact Finset.sum_nonneg (by
    intro i _
    exact mul_nonneg (hE i j) (abs_nonneg (s i)))
private theorem lsAugmentedEq20_7LeftMajorant_nonneg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    {U : Fin m → ℝ} (hU : ∀ i, 0 ≤ U i) :
    ∀ i : Fin m, 0 ≤ lsAugmentedEq20_7LeftMajorant A Aplus U i := by
  intro i
  exact rectMatMulVec_absMatrixRect_nonneg
    (lsAugmentedProjectionBlock A Aplus) hU i
private theorem lsAugmentedEq20_7RightMajorant_nonneg {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) {V : Fin n → ℝ}
    (hV : ∀ j, 0 ≤ V j) :
    ∀ i : Fin m, 0 ≤ lsAugmentedEq20_7RightMajorant Aplus V i := by
  intro i
  unfold lsAugmentedEq20_7RightMajorant
  exact Finset.sum_nonneg (by
    intro j _
    exact mul_nonneg (abs_nonneg (Aplus j i)) (hV j))
private theorem lsAugmentedEq20_7Majorant_nonneg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    {U : Fin m → ℝ} {V : Fin n → ℝ}
    (hU : ∀ i, 0 ≤ U i) (hV : ∀ j, 0 ≤ V j) :
    ∀ i : Fin m, 0 ≤ lsAugmentedEq20_7Majorant A Aplus U V i := by
  intro i
  unfold lsAugmentedEq20_7Majorant
  exact add_nonneg
    (rectMatMulVec_absMatrixRect_nonneg
      (lsAugmentedProjectionBlock A Aplus) hU i)
    (by
      exact Finset.sum_nonneg (by
        intro j _
        exact mul_nonneg (abs_nonneg (Aplus j i)) (hV j)))
private theorem lsAugmentedEq20_8LeftMajorant_nonneg {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) {U : Fin m → ℝ}
    (hU : ∀ i, 0 ≤ U i) :
    ∀ j : Fin n, 0 ≤ lsAugmentedEq20_8LeftMajorant Aplus U j := by
  intro j
  exact rectMatMulVec_absMatrixRect_nonneg Aplus hU j
private theorem lsAugmentedEq20_8RightMajorant_nonneg {n : ℕ}
    (gramInv : Fin n → Fin n → ℝ) {V : Fin n → ℝ}
    (hV : ∀ j, 0 ≤ V j) :
    ∀ j : Fin n, 0 ≤ lsAugmentedEq20_8RightMajorant gramInv V j := by
  intro j
  exact matMulVec_absMatrix_nonneg gramInv hV j
private theorem lsAugmentedEq20_8Majorant_nonneg {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    {U : Fin m → ℝ} {V : Fin n → ℝ}
    (hU : ∀ i, 0 ≤ U i) (hV : ∀ j, 0 ≤ V j) :
    ∀ j : Fin n, 0 ≤ lsAugmentedEq20_8Majorant Aplus gramInv U V j := by
  intro j
  unfold lsAugmentedEq20_8Majorant
  exact add_nonneg
    (rectMatMulVec_absMatrixRect_nonneg Aplus hU j)
    (matMulVec_absMatrix_nonneg gramInv hV j)
private theorem lsEq20_6_rhsTop_abs_le {m n : ℕ}
    (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ)
    (E : Fin m → Fin n → ℝ) (f : Fin m → ℝ)
    (y : Fin n → ℝ) (eps : ℝ)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ∀ i : Fin m,
      |lsEq20_6RhsTop DeltaA Deltab y i| ≤
        eps * lsComponentwiseDataMajorant E f y i := by
  intro i
  have hDeltaAy :
      |rectMatMulVec DeltaA y i| ≤
        eps * rectMatMulVec E (absVec n y) i := by
    calc
      |rectMatMulVec DeltaA y i|
          ≤ ∑ j : Fin n, |DeltaA i j| * |y j| :=
            abs_rectMatMulVec_le DeltaA y i
      _ ≤ ∑ j : Fin n, (eps * E i j) * |y j| := by
            apply Finset.sum_le_sum
            intro j _
            exact mul_le_mul_of_nonneg_right (hDeltaA i j) (abs_nonneg (y j))
      _ = eps * rectMatMulVec E (absVec n y) i := by
            unfold rectMatMulVec absVec
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro j _
            ring
  unfold lsEq20_6RhsTop lsComponentwiseDataMajorant
  calc
    |Deltab i - rectMatMulVec DeltaA y i|
        ≤ |Deltab i| + |rectMatMulVec DeltaA y i| := by
          simpa [sub_eq_add_neg, abs_neg] using
            abs_add_le (Deltab i) (-(rectMatMulVec DeltaA y i))
    _ ≤ eps * f i + eps * rectMatMulVec E (absVec n y) i :=
        add_le_add (hDeltab i) hDeltaAy
    _ = eps * (f i + rectMatMulVec E (absVec n y) i) := by ring
private theorem lsEq20_6_rhsBottom_abs_le {m n : ℕ}
    (DeltaA : Fin m → Fin n → ℝ) (E : Fin m → Fin n → ℝ)
    (s : Fin m → ℝ) (eps : ℝ)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j) :
    ∀ j : Fin n,
      |lsEq20_6RhsBottom DeltaA s j| ≤
        eps * lsComponentwiseTransposeMajorant E s j := by
  intro j
  unfold lsEq20_6RhsBottom lsComponentwiseTransposeMajorant
  calc
    |-∑ i : Fin m, DeltaA i j * s i|
        = |∑ i : Fin m, DeltaA i j * s i| := by rw [abs_neg]
    _ ≤ ∑ i : Fin m, |DeltaA i j| * |s i| := by
        calc
          |∑ i : Fin m, DeltaA i j * s i|
              ≤ ∑ i : Fin m, |DeltaA i j * s i| :=
                Finset.abs_sum_le_sum_abs _ _
          _ = ∑ i : Fin m, |DeltaA i j| * |s i| := by
                apply Finset.sum_congr rfl
                intro i _
                exact abs_mul (DeltaA i j) (s i)
    _ ≤ ∑ i : Fin m, (eps * E i j) * |s i| := by
          apply Finset.sum_le_sum
          intro i _
          exact mul_le_mul_of_nonneg_right (hDeltaA i j) (abs_nonneg (s i))
    _ = eps * ∑ i : Fin m, E i j * |s i| := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro i _
          ring
/-- Absolute-value consequence of the top block in (20.6): if the two
    right-hand-side blocks are componentwise bounded by `eps * U` and
    `eps * V`, then the residual block is bounded by the source matrix
    majorant `eps * (|I-AA^+| U + |A^+|^T V)`. -/
theorem lsAugmentedInverseActionTop_abs_le_of_rhs_bounds {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (u : Fin m → ℝ) (v : Fin n → ℝ) (U : Fin m → ℝ) (V : Fin n → ℝ)
    (eps : ℝ)
    (hu : ∀ i, |u i| ≤ eps * U i)
    (hv : ∀ j, |v j| ≤ eps * V j) :
    ∀ i : Fin m,
      |lsAugmentedInverseActionTop A Aplus u v i| ≤
        eps * lsAugmentedEq20_7Majorant A Aplus U V i := by
  intro i
  let P := lsAugmentedProjectionBlock A Aplus
  have hproj_bound :
      |rectMatMulVec P u i| ≤
        eps * rectMatMulVec (absMatrixRect P) U i := by
    calc
      |rectMatMulVec P u i|
          ≤ ∑ k : Fin m, |P i k| * |u k| :=
            abs_rectMatMulVec_le P u i
      _ ≤ ∑ k : Fin m, |P i k| * (eps * U k) := by
            apply Finset.sum_le_sum
            intro k _
            exact mul_le_mul_of_nonneg_left (hu k) (abs_nonneg (P i k))
      _ = eps * rectMatMulVec (absMatrixRect P) U i := by
            unfold rectMatMulVec absMatrixRect
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro k _
            ring
  have hright_bound :
      |∑ j : Fin n, Aplus j i * v j| ≤
        eps * (∑ j : Fin n, |Aplus j i| * V j) := by
    calc
      |∑ j : Fin n, Aplus j i * v j|
          ≤ ∑ j : Fin n, |Aplus j i * v j| :=
            Finset.abs_sum_le_sum_abs _ _
      _ = ∑ j : Fin n, |Aplus j i| * |v j| := by
            apply Finset.sum_congr rfl
            intro j _
            exact abs_mul (Aplus j i) (v j)
      _ ≤ ∑ j : Fin n, |Aplus j i| * (eps * V j) := by
            apply Finset.sum_le_sum
            intro j _
            exact mul_le_mul_of_nonneg_left (hv j) (abs_nonneg (Aplus j i))
      _ = eps * ∑ j : Fin n, |Aplus j i| * V j := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro j _
            ring
  have htop_eq :
      lsAugmentedInverseActionTop A Aplus u v i =
        rectMatMulVec P u i + ∑ j : Fin n, Aplus j i * v j := by
    unfold P
    unfold lsAugmentedInverseActionTop
    rw [← congrFun (lsAugmentedProjectionBlock_mulVec A Aplus u) i]
  calc
    |lsAugmentedInverseActionTop A Aplus u v i|
        = |rectMatMulVec P u i + ∑ j : Fin n, Aplus j i * v j| := by
            rw [htop_eq]
    _ ≤ |rectMatMulVec P u i| + |∑ j : Fin n, Aplus j i * v j| :=
            abs_add_le _ _
    _ ≤ eps * rectMatMulVec (absMatrixRect P) U i +
          eps * (∑ j : Fin n, |Aplus j i| * V j) :=
            add_le_add hproj_bound hright_bound
    _ = eps * lsAugmentedEq20_7Majorant A Aplus U V i := by
            unfold lsAugmentedEq20_7Majorant P
            ring
/-- Absolute-value consequence of the bottom block in (20.6): if the two
    right-hand-side blocks are componentwise bounded by `eps * U` and
    `eps * V`, then the solution block is bounded by the source matrix
    majorant `eps * (|A^+| U + |(A^T A)^{-1}| V)`. -/
theorem lsAugmentedInverseActionBottom_abs_le_of_rhs_bounds {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    (u : Fin m → ℝ) (v : Fin n → ℝ) (U : Fin m → ℝ) (V : Fin n → ℝ)
    (eps : ℝ)
    (hu : ∀ i, |u i| ≤ eps * U i)
    (hv : ∀ j, |v j| ≤ eps * V j) :
    ∀ j : Fin n,
      |lsAugmentedInverseActionBottom Aplus gramInv u v j| ≤
        eps * lsAugmentedEq20_8Majorant Aplus gramInv U V j := by
  intro j
  have hleft_bound :
      |rectMatMulVec Aplus u j| ≤
        eps * rectMatMulVec (absMatrixRect Aplus) U j := by
    calc
      |rectMatMulVec Aplus u j|
          ≤ ∑ i : Fin m, |Aplus j i| * |u i| :=
            abs_rectMatMulVec_le Aplus u j
      _ ≤ ∑ i : Fin m, |Aplus j i| * (eps * U i) := by
            apply Finset.sum_le_sum
            intro i _
            exact mul_le_mul_of_nonneg_left (hu i) (abs_nonneg (Aplus j i))
      _ = eps * rectMatMulVec (absMatrixRect Aplus) U j := by
            unfold rectMatMulVec absMatrixRect
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
  have hright_bound :
      |matMulVec n gramInv v j| ≤
        eps * matMulVec n (absMatrix n gramInv) V j := by
    calc
      |matMulVec n gramInv v j|
          ≤ ∑ k : Fin n, |gramInv j k| * |v k| :=
            abs_matMulVec_le n gramInv v j
      _ ≤ ∑ k : Fin n, |gramInv j k| * (eps * V k) := by
            apply Finset.sum_le_sum
            intro k _
            exact mul_le_mul_of_nonneg_left (hv k) (abs_nonneg (gramInv j k))
      _ = eps * matMulVec n (absMatrix n gramInv) V j := by
            unfold matMulVec absMatrix
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro k _
            ring
  unfold lsAugmentedInverseActionBottom
  calc
    |rectMatMulVec Aplus u j - matMulVec n gramInv v j|
        ≤ |rectMatMulVec Aplus u j| + |matMulVec n gramInv v j| := by
          simpa [sub_eq_add_neg, abs_neg] using
            abs_add_le (rectMatMulVec Aplus u j) (-(matMulVec n gramInv v j))
    _ ≤ eps * rectMatMulVec (absMatrixRect Aplus) U j +
          eps * matMulVec n (absMatrix n gramInv) V j :=
          add_le_add hleft_bound hright_bound
    _ = eps * lsAugmentedEq20_8Majorant Aplus gramInv U V j := by
          unfold lsAugmentedEq20_8Majorant
          ring
/-- Higham, 2nd ed., Chapter 20, equation (20.7), exact componentwise
    residual-difference bound obtained from the block action (20.6) and the
    perturbation assumptions `|DeltaA| <= eps E`, `|Deltab| <= eps f`.

    This is the algebraic componentwise step only: the theorem does not supply
    the pseudo-inverse/Gram-inverse data required by (20.6), nor does it take an
    absolute norm to obtain (20.10). -/
theorem lsAugmentedEq20_7_abs_le_of_perturbation_bounds {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (y : Fin n → ℝ) (s : Fin m → ℝ) (eps : ℝ)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ∀ i : Fin m,
      |lsAugmentedInverseActionTop A Aplus
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s) i| ≤
        eps * lsAugmentedEq20_7Majorant A Aplus
          (lsComponentwiseDataMajorant E f y)
          (lsComponentwiseTransposeMajorant E s) i := by
  exact
    lsAugmentedInverseActionTop_abs_le_of_rhs_bounds A Aplus
      (lsEq20_6RhsTop DeltaA Deltab y)
      (lsEq20_6RhsBottom DeltaA s)
      (lsComponentwiseDataMajorant E f y)
      (lsComponentwiseTransposeMajorant E s)
      eps
      (lsEq20_6_rhsTop_abs_le DeltaA Deltab E f y eps hDeltaA hDeltab)
      (lsEq20_6_rhsBottom_abs_le DeltaA E s eps hDeltaA)
/-- Higham, 2nd ed., Chapter 20, equation (20.8), exact componentwise
    solution-difference bound obtained from the block action (20.6) and the
    perturbation assumptions `|DeltaA| <= eps E`, `|Deltab| <= eps f`.

    This is the algebraic componentwise step only: the theorem does not supply
    the pseudo-inverse/Gram-inverse data required by (20.6), nor does it take an
    absolute norm to obtain (20.9). -/
theorem lsAugmentedEq20_8_abs_le_of_perturbation_bounds {m n : ℕ}
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (y : Fin n → ℝ) (s : Fin m → ℝ) (eps : ℝ)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ∀ j : Fin n,
      |lsAugmentedInverseActionBottom Aplus gramInv
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s) j| ≤
        eps * lsAugmentedEq20_8Majorant Aplus gramInv
          (lsComponentwiseDataMajorant E f y)
          (lsComponentwiseTransposeMajorant E s) j := by
  exact
    lsAugmentedInverseActionBottom_abs_le_of_rhs_bounds Aplus gramInv
      (lsEq20_6RhsTop DeltaA Deltab y)
      (lsEq20_6RhsBottom DeltaA s)
      (lsComponentwiseDataMajorant E f y)
      (lsComponentwiseTransposeMajorant E s)
      eps
      (lsEq20_6_rhsTop_abs_le DeltaA Deltab E f y eps hDeltaA hDeltab)
      (lsEq20_6_rhsBottom_abs_le DeltaA E s eps hDeltaA)
/-- Higham, 2nd ed., Chapter 20, equations (20.5), (20.7), and (20.8):
    source componentwise residual and solution perturbation bounds for exact
    and perturbed augmented systems under full column rank of `A`.

    This theorem supplies the concrete Gram inverse and identifies the two
    blocks in (20.6) with the printed differences `s - r` and `y - x`. -/
theorem lsAugmentedEq20_7_8_abs_le_of_perturbed_systems_full_column_rank_of_componentwise_perturbation
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (hcomp : LSComponentwisePerturbation DeltaA E Deltab f eps) :
    (∀ i : Fin m,
      |s i - r i| ≤
        eps * lsAugmentedEq20_7Majorant A (lsAplusOfGramNonsingInv A)
          (lsComponentwiseDataMajorant E f y)
          (lsComponentwiseTransposeMajorant E s) i) ∧
    (∀ j : Fin n,
      |y j - x j| ≤
        eps * lsAugmentedEq20_8Majorant (lsAplusOfGramNonsingInv A)
          (lsGramNonsingInv A)
          (lsComponentwiseDataMajorant E f y)
          (lsComponentwiseTransposeMajorant E s) j) := by
  rcases hcomp with ⟨_heps, _hE, _hf, hDeltaA, hDeltab⟩
  have hDiffs :=
    lsAugmentedEq20_6_action_eq_differences_of_perturbed_systems
      A (lsAplusOfGramNonsingInv A) (lsGramNonsingInv A)
      DeltaA b Deltab r s x y
      (by intro j i; rfl)
      (lsGramNonsingInv_isInverse_of_det_ne_zero A
        (rectLSGram_det_ne_zero_of_rectMatMulVec_injective A hA))
      (by intro j k; exact lsGramNonsingInv_symmetric A j k)
      hExact hPert
  constructor
  · intro i
    have h :=
      lsAugmentedEq20_7_abs_le_of_perturbation_bounds
        A (lsAplusOfGramNonsingInv A) DeltaA E Deltab f y s eps
        hDeltaA hDeltab i
    simpa [hDiffs.1 i] using h
  · intro j
    have h :=
      lsAugmentedEq20_8_abs_le_of_perturbation_bounds
        (lsAplusOfGramNonsingInv A) (lsGramNonsingInv A)
        DeltaA E Deltab f y s eps hDeltaA hDeltab j
    simpa [hDiffs.2 j] using h
/-- Higham, 2nd ed., Chapter 20, equation (20.9), numerator form: applying
    any absolute norm to the source-shaped solution block in (20.6) gives the
    two-term bound from (20.9).

    This is still the supplied block-action algebra: the theorem assumes the
    pseudo-inverse/Gram-inverse data through the explicit block action and does
    not identify that block action with `y - x`. -/
theorem lsAugmentedEq20_9_numerator_bound_of_perturbation_bounds {m n : ℕ}
    (ν : CVec n → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (y : Fin n → ℝ) (s : Fin m → ℝ) (eps : ℝ)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex
        (lsAugmentedInverseActionBottom Aplus gramInv
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s))) ≤
      eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_8RightMajorant gramInv
              (lsComponentwiseTransposeMajorant E s)))) := by
  let U := lsComponentwiseDataMajorant E f y
  let V := lsComponentwiseTransposeMajorant E s
  let z :=
    lsAugmentedInverseActionBottom Aplus gramInv
      (lsEq20_6RhsTop DeltaA Deltab y)
      (lsEq20_6RhsBottom DeltaA s)
  let left := lsAugmentedEq20_8LeftMajorant Aplus U
  let right := lsAugmentedEq20_8RightMajorant gramInv V
  let major := lsAugmentedEq20_8Majorant Aplus gramInv U V
  have hU : ∀ i, 0 ≤ U i := by
    intro i
    exact lsComponentwiseDataMajorant_nonneg hE hf i
  have hV : ∀ j, 0 ≤ V j := by
    intro j
    exact lsComponentwiseTransposeMajorant_nonneg hE j
  have hmajor_nonneg : ∀ j, 0 ≤ eps * major j := by
    intro j
    exact mul_nonneg heps
      (lsAugmentedEq20_8Majorant_nonneg Aplus gramInv hU hV j)
  have hpoint : ∀ j, |z j| ≤ eps * major j := by
    intro j
    dsimp [z, major, U, V]
    exact
      lsAugmentedEq20_8_abs_le_of_perturbation_bounds Aplus gramInv
        DeltaA E Deltab f y s eps hDeltaA hDeltab j
  have hmono :
      ν (realVecToComplex z) ≤
        ν (realVecToComplex (fun j => eps * major j)) :=
    realVecToComplex_norm_le_of_abs_le hν habs hmajor_nonneg hpoint
  have hscale :
      ν (realVecToComplex (fun j => eps * major j)) =
        eps * ν (realVecToComplex major) :=
    realVecToComplex_norm_smul_nonneg hν eps heps major
  have htri :
      ν (realVecToComplex major) ≤
        ν (realVecToComplex left) + ν (realVecToComplex right) := by
    have h := realVecToComplex_norm_add_le hν left right
    simpa [major, left, right, lsAugmentedEq20_8Majorant,
      lsAugmentedEq20_8LeftMajorant, lsAugmentedEq20_8RightMajorant] using h
  have hbound :
      ν (realVecToComplex z) ≤
        eps * (ν (realVecToComplex left) + ν (realVecToComplex right)) := by
    calc
      ν (realVecToComplex z)
          ≤ ν (realVecToComplex (fun j => eps * major j)) := hmono
      _ = eps * ν (realVecToComplex major) := hscale
      _ ≤ eps * (ν (realVecToComplex left) + ν (realVecToComplex right)) :=
          mul_le_mul_of_nonneg_left htri heps
  simpa [z, U, V, left, right] using hbound
/-- Higham, 2nd ed., Chapter 20, equation (20.9), relative supplied-action
    form.  The denominator is an explicit reference vector norm, matching the
    `/ ‖x‖` shape of the source once the block action is connected to
    `y - x`. -/
theorem lsAugmentedEq20_9_relative_bound_of_perturbation_bounds {m n : ℕ}
    (ν : CVec n → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (x y : Fin n → ℝ) (s : Fin m → ℝ) (eps : ℝ)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < ν (realVecToComplex x))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex
        (lsAugmentedInverseActionBottom Aplus gramInv
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s))) / ν (realVecToComplex x) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_8RightMajorant gramInv
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex x) := by
  exact div_le_div_of_nonneg_right
    (lsAugmentedEq20_9_numerator_bound_of_perturbation_bounds
      ν hν habs Aplus gramInv DeltaA E Deltab f y s eps heps hE hf
      hDeltaA hDeltab)
    (le_of_lt hxnorm_pos)
/-- Higham, 2nd ed., Chapter 20, equation (20.9), source-signed relative
    form under an explicit equality connecting the supplied block action from
    (20.6) to the actual solution difference. -/
theorem lsAugmentedEq20_9_relative_bound_of_solution_difference {m n : ℕ}
    (ν : CVec n → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (Aplus : Fin n → Fin m → ℝ) (gramInv : Fin n → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (x y : Fin n → ℝ) (s : Fin m → ℝ) (eps : ℝ)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < ν (realVecToComplex x))
    (hDiff : ∀ j : Fin n,
      lsAugmentedInverseActionBottom Aplus gramInv
        (lsEq20_6RhsTop DeltaA Deltab y)
        (lsEq20_6RhsBottom DeltaA s) j = y j - x j)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun j => x j - y j)) / ν (realVecToComplex x) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_8RightMajorant gramInv
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex x) := by
  let z :=
    lsAugmentedInverseActionBottom Aplus gramInv
      (lsEq20_6RhsTop DeltaA Deltab y)
      (lsEq20_6RhsBottom DeltaA s)
  have hnorm :
      ν (realVecToComplex (fun j => x j - y j)) =
        ν (realVecToComplex z) := by
    have hvec : (fun j : Fin n => x j - y j) = fun j => -z j := by
      ext j
      dsimp [z]
      rw [hDiff j]
      ring
    rw [hvec]
    exact realVecToComplex_norm_neg hν z
  rw [hnorm]
  exact
    lsAugmentedEq20_9_relative_bound_of_perturbation_bounds
      ν hν habs Aplus gramInv DeltaA E Deltab f x y s eps
      heps hE hf hxnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.9), source-signed relative
    solution bound derived from exact and perturbed augmented systems.

    Compared with `lsAugmentedEq20_9_relative_bound_of_solution_difference`,
    this theorem no longer assumes that the (20.6) block action is `y - x`;
    it derives that identity from the two augmented systems and the supplied
    inverse data for `Aᵀ A`. -/
theorem lsAugmentedEq20_9_relative_bound_of_perturbed_systems {m n : ℕ}
    (ν : CVec n → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (gramInv : Fin n → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hAplus : ∀ j i, Aplus j i = ∑ k : Fin n, gramInv j k * A i k)
    (hGramInv : IsInverse n (rectLSGram A) gramInv)
    (hGramInv_symm : ∀ j k : Fin n, gramInv j k = gramInv k j)
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < ν (realVecToComplex x))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun j => x j - y j)) / ν (realVecToComplex x) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_8RightMajorant gramInv
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex x) := by
  have hDiffs :=
    lsAugmentedEq20_6_action_eq_differences_of_perturbed_systems
      A Aplus gramInv DeltaA b Deltab r s x y
      hAplus hGramInv hGramInv_symm hExact hPert
  exact
    lsAugmentedEq20_9_relative_bound_of_solution_difference
      ν hν habs Aplus gramInv DeltaA E Deltab f x y s eps
      heps hE hf hxnorm_pos hDiffs.2 hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.9), source-signed relative
    solution bound with the concrete Gram inverse supplied by
    `det(A^T A) ≠ 0`.  This removes the arbitrary inverse-table hypotheses
    from `lsAugmentedEq20_9_relative_bound_of_perturbed_systems`; the remaining
    source-level bridge is the equivalence between full column rank and this
    determinant condition. -/
theorem lsAugmentedEq20_9_relative_bound_of_perturbed_systems_nonsing_gram
    {m n : ℕ}
    (ν : CVec n → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hdet : Matrix.det (rectLSGram A : Matrix (Fin n) (Fin n) ℝ) ≠ 0)
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < ν (realVecToComplex x))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun j => x j - y j)) / ν (realVecToComplex x) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_8RightMajorant (lsGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex x) := by
  exact
    lsAugmentedEq20_9_relative_bound_of_perturbed_systems
      ν hν habs A (lsAplusOfGramNonsingInv A) (lsGramNonsingInv A)
      DeltaA E Deltab f b r s x y eps
      (by
        intro j i
        rfl)
      (lsGramNonsingInv_isInverse_of_det_ne_zero A hdet)
      (by
        intro j k
        exact lsGramNonsingInv_symmetric A j k)
      hExact hPert heps hE hf hxnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.9), source full-column-rank
    form of the relative solution bound.  The determinant condition required
    by the concrete Gram inverse is derived from injectivity of `x ↦ A x`. -/
theorem lsAugmentedEq20_9_relative_bound_of_perturbed_systems_full_column_rank
    {m n : ℕ}
    (ν : CVec n → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < ν (realVecToComplex x))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun j => x j - y j)) / ν (realVecToComplex x) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_8RightMajorant (lsGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex x) := by
  exact
    lsAugmentedEq20_9_relative_bound_of_perturbed_systems_nonsing_gram
      ν hν habs A DeltaA E Deltab f b r s x y eps
      (rectLSGram_det_ne_zero_of_rectMatMulVec_injective A hA)
      hExact hPert heps hE hf hxnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.10), numerator form: applying
    any absolute norm to the source-shaped residual block in (20.6) gives the
    two-term bound from (20.10).

    This is still the supplied block-action algebra: the theorem assumes the
    pseudo-inverse data through the explicit block action and does not identify
    that block action with `s - r`. -/
theorem lsAugmentedEq20_10_numerator_bound_of_perturbation_bounds {m n : ℕ}
    (ν : CVec m → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (y : Fin n → ℝ) (s : Fin m → ℝ) (eps : ℝ)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex
        (lsAugmentedInverseActionTop A Aplus
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s))) ≤
      eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_7RightMajorant Aplus
              (lsComponentwiseTransposeMajorant E s)))) := by
  let U := lsComponentwiseDataMajorant E f y
  let V := lsComponentwiseTransposeMajorant E s
  let z :=
    lsAugmentedInverseActionTop A Aplus
      (lsEq20_6RhsTop DeltaA Deltab y)
      (lsEq20_6RhsBottom DeltaA s)
  let left := lsAugmentedEq20_7LeftMajorant A Aplus U
  let right := lsAugmentedEq20_7RightMajorant Aplus V
  let major := lsAugmentedEq20_7Majorant A Aplus U V
  have hU : ∀ i, 0 ≤ U i := by
    intro i
    exact lsComponentwiseDataMajorant_nonneg hE hf i
  have hV : ∀ j, 0 ≤ V j := by
    intro j
    exact lsComponentwiseTransposeMajorant_nonneg hE j
  have hmajor_nonneg : ∀ i, 0 ≤ eps * major i := by
    intro i
    exact mul_nonneg heps
      (lsAugmentedEq20_7Majorant_nonneg A Aplus hU hV i)
  have hpoint : ∀ i, |z i| ≤ eps * major i := by
    intro i
    dsimp [z, major, U, V]
    exact
      lsAugmentedEq20_7_abs_le_of_perturbation_bounds A Aplus
        DeltaA E Deltab f y s eps hDeltaA hDeltab i
  have hmono :
      ν (realVecToComplex z) ≤
        ν (realVecToComplex (fun i => eps * major i)) :=
    realVecToComplex_norm_le_of_abs_le hν habs hmajor_nonneg hpoint
  have hscale :
      ν (realVecToComplex (fun i => eps * major i)) =
        eps * ν (realVecToComplex major) :=
    realVecToComplex_norm_smul_nonneg hν eps heps major
  have htri :
      ν (realVecToComplex major) ≤
        ν (realVecToComplex left) + ν (realVecToComplex right) := by
    have h := realVecToComplex_norm_add_le hν left right
    simpa [major, left, right, lsAugmentedEq20_7Majorant,
      lsAugmentedEq20_7LeftMajorant, lsAugmentedEq20_7RightMajorant] using h
  have hbound :
      ν (realVecToComplex z) ≤
        eps * (ν (realVecToComplex left) + ν (realVecToComplex right)) := by
    calc
      ν (realVecToComplex z)
          ≤ ν (realVecToComplex (fun i => eps * major i)) := hmono
      _ = eps * ν (realVecToComplex major) := hscale
      _ ≤ eps * (ν (realVecToComplex left) + ν (realVecToComplex right)) :=
          mul_le_mul_of_nonneg_left htri heps
  simpa [z, U, V, left, right] using hbound
/-- Higham, 2nd ed., Chapter 20, equation (20.10), relative supplied-action
    form.  The denominator is an explicit residual norm, matching the
    `/ ‖r‖` shape of the source once the block action is connected to
    `s - r`. -/
theorem lsAugmentedEq20_10_relative_bound_of_perturbation_bounds {m n : ℕ}
    (ν : CVec m → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (y : Fin n → ℝ) (r s : Fin m → ℝ) (eps : ℝ)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hrnorm_pos : 0 < ν (realVecToComplex r))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex
        (lsAugmentedInverseActionTop A Aplus
          (lsEq20_6RhsTop DeltaA Deltab y)
          (lsEq20_6RhsBottom DeltaA s))) / ν (realVecToComplex r) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_7RightMajorant Aplus
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex r) := by
  exact div_le_div_of_nonneg_right
    (lsAugmentedEq20_10_numerator_bound_of_perturbation_bounds
      ν hν habs A Aplus DeltaA E Deltab f y s eps heps hE hf
      hDeltaA hDeltab)
    (le_of_lt hrnorm_pos)
/-- Higham, 2nd ed., Chapter 20, equation (20.10), source-signed relative
    form under an explicit equality connecting the supplied block action from
    (20.6) to the actual residual difference. -/
theorem lsAugmentedEq20_10_relative_bound_of_residual_difference {m n : ℕ}
    (ν : CVec m → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f : Fin m → ℝ)
    (y : Fin n → ℝ) (r s : Fin m → ℝ) (eps : ℝ)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hrnorm_pos : 0 < ν (realVecToComplex r))
    (hDiff : ∀ i : Fin m,
      lsAugmentedInverseActionTop A Aplus
        (lsEq20_6RhsTop DeltaA Deltab y)
        (lsEq20_6RhsBottom DeltaA s) i = s i - r i)
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun i => r i - s i)) / ν (realVecToComplex r) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_7RightMajorant Aplus
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex r) := by
  let z :=
    lsAugmentedInverseActionTop A Aplus
      (lsEq20_6RhsTop DeltaA Deltab y)
      (lsEq20_6RhsBottom DeltaA s)
  have hnorm :
      ν (realVecToComplex (fun i => r i - s i)) =
        ν (realVecToComplex z) := by
    have hvec : (fun i : Fin m => r i - s i) = fun i => -z i := by
      ext i
      dsimp [z]
      rw [hDiff i]
      ring
    rw [hvec]
    exact realVecToComplex_norm_neg hν z
  rw [hnorm]
  exact
    lsAugmentedEq20_10_relative_bound_of_perturbation_bounds
      ν hν habs A Aplus DeltaA E Deltab f y r s eps
      heps hE hf hrnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.10), source-signed relative
    residual bound derived from exact and perturbed augmented systems.

    Compared with `lsAugmentedEq20_10_relative_bound_of_residual_difference`,
    this theorem no longer assumes that the (20.6) block action is `s - r`;
    it derives that identity from the two augmented systems and the supplied
    inverse data for `Aᵀ A`. -/
theorem lsAugmentedEq20_10_relative_bound_of_perturbed_systems {m n : ℕ}
    (ν : CVec m → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ) (Aplus : Fin n → Fin m → ℝ)
    (gramInv : Fin n → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hAplus : ∀ j i, Aplus j i = ∑ k : Fin n, gramInv j k * A i k)
    (hGramInv : IsInverse n (rectLSGram A) gramInv)
    (hGramInv_symm : ∀ j k : Fin n, gramInv j k = gramInv k j)
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hrnorm_pos : 0 < ν (realVecToComplex r))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun i => r i - s i)) / ν (realVecToComplex r) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A Aplus
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_7RightMajorant Aplus
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex r) := by
  have hDiffs :=
    lsAugmentedEq20_6_action_eq_differences_of_perturbed_systems
      A Aplus gramInv DeltaA b Deltab r s x y
      hAplus hGramInv hGramInv_symm hExact hPert
  exact
    lsAugmentedEq20_10_relative_bound_of_residual_difference
      ν hν habs A Aplus DeltaA E Deltab f y r s eps
      heps hE hf hrnorm_pos hDiffs.1 hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.10), source-signed relative
    residual bound with the concrete Gram inverse supplied by
    `det(A^T A) ≠ 0`.  This is the determinant-facing companion to
    `lsAugmentedEq20_9_relative_bound_of_perturbed_systems_nonsing_gram`. -/
theorem lsAugmentedEq20_10_relative_bound_of_perturbed_systems_nonsing_gram
    {m n : ℕ}
    (ν : CVec m → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hdet : Matrix.det (rectLSGram A : Matrix (Fin n) (Fin n) ℝ) ≠ 0)
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hrnorm_pos : 0 < ν (realVecToComplex r))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun i => r i - s i)) / ν (realVecToComplex r) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_7RightMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex r) := by
  exact
    lsAugmentedEq20_10_relative_bound_of_perturbed_systems
      ν hν habs A (lsAplusOfGramNonsingInv A) (lsGramNonsingInv A)
      DeltaA E Deltab f b r s x y eps
      (by
        intro j i
        rfl)
      (lsGramNonsingInv_isInverse_of_det_ne_zero A hdet)
      (by
        intro j k
        exact lsGramNonsingInv_symmetric A j k)
      hExact hPert heps hE hf hrnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equation (20.10), source full-column-rank
    form of the relative residual bound.  The determinant condition required
    by the concrete Gram inverse is derived from injectivity of `x ↦ A x`. -/
theorem lsAugmentedEq20_10_relative_bound_of_perturbed_systems_full_column_rank
    {m n : ℕ}
    (ν : CVec m → ℝ) (hν : IsComplexVectorNorm ν)
    (habs : IsAbsoluteComplexVectorNorm ν)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hrnorm_pos : 0 < ν (realVecToComplex r))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    ν (realVecToComplex (fun i => r i - s i)) / ν (realVecToComplex r) ≤
      (eps *
        (ν (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          ν (realVecToComplex
            (lsAugmentedEq20_7RightMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        ν (realVecToComplex r) := by
  exact
    lsAugmentedEq20_10_relative_bound_of_perturbed_systems_nonsing_gram
      ν hν habs A DeltaA E Deltab f b r s x y eps
      (rectLSGram_det_ne_zero_of_rectMatMulVec_injective A hA)
      hExact hPert heps hE hf hrnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, Theorem 20.2, paired source-facing
    augmented-system form.  The hypotheses keep the printed full-rank
    assumptions for both `A` and `A + DeltaA`; the proof uses full column rank
    of `A` to construct the inverse block in (20.6) and then combines the
    already proved relative bounds (20.9) and (20.10). -/
theorem lsAugmentedTheorem20_2_relative_bounds_full_column_rank
    {m n : ℕ}
    (νx : CVec n → ℝ) (hνx : IsComplexVectorNorm νx)
    (habsx : IsAbsoluteComplexVectorNorm νx)
    (νr : CVec m → ℝ) (hνr : IsComplexVectorNorm νr)
    (habsr : IsAbsoluteComplexVectorNorm νr)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (_hApert :
      Function.Injective (rectMatMulVec (fun i j => A i j + DeltaA i j)))
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < νx (realVecToComplex x))
    (hrnorm_pos : 0 < νr (realVecToComplex r))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    (νx (realVecToComplex (fun j => x j - y j)) / νx (realVecToComplex x) ≤
      (eps *
        (νx (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νx (realVecToComplex
            (lsAugmentedEq20_8RightMajorant (lsGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        νx (realVecToComplex x)) ∧
    (νr (realVecToComplex (fun i => r i - s i)) / νr (realVecToComplex r) ≤
      (eps *
        (νr (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νr (realVecToComplex
            (lsAugmentedEq20_7RightMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        νr (realVecToComplex r)) := by
  constructor
  · exact
      lsAugmentedEq20_9_relative_bound_of_perturbed_systems_full_column_rank
        νx hνx habsx A DeltaA E Deltab f b r s x y eps
        hA hExact hPert heps hE hf hxnorm_pos hDeltaA hDeltab
  · exact
      lsAugmentedEq20_10_relative_bound_of_perturbed_systems_full_column_rank
        νr hνr habsr A DeltaA E Deltab f b r s x y eps
        hA hExact hPert heps hE hf hrnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equations (20.5), (20.9), and (20.10):
    paired full-column-rank augmented-system perturbation bounds using the
    source componentwise perturbation predicate directly. -/
theorem lsAugmentedTheorem20_2_relative_bounds_full_column_rank_of_componentwise_perturbation
    {m n : ℕ}
    (νx : CVec n → ℝ) (hνx : IsComplexVectorNorm νx)
    (habsx : IsAbsoluteComplexVectorNorm νx)
    (νr : CVec m → ℝ) (hνr : IsComplexVectorNorm νr)
    (habsr : IsAbsoluteComplexVectorNorm νr)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (r s : Fin m → ℝ) (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (hApert :
      Function.Injective (rectMatMulVec (fun i j => A i j + DeltaA i j)))
    (hExact : LSAugmentedSystem A b (0 : Fin n → ℝ) r x)
    (hPert :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ) s y)
    (hcomp : LSComponentwisePerturbation DeltaA E Deltab f eps)
    (hxnorm_pos : 0 < νx (realVecToComplex x))
    (hrnorm_pos : 0 < νr (realVecToComplex r)) :
    (νx (realVecToComplex (fun j => x j - y j)) / νx (realVecToComplex x) ≤
      (eps *
        (νx (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νx (realVecToComplex
            (lsAugmentedEq20_8RightMajorant (lsGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        νx (realVecToComplex x)) ∧
    (νr (realVecToComplex (fun i => r i - s i)) / νr (realVecToComplex r) ≤
      (eps *
        (νr (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νr (realVecToComplex
            (lsAugmentedEq20_7RightMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E s))))) /
        νr (realVecToComplex r)) := by
  rcases hcomp with ⟨heps, hE, hf, hDeltaA, hDeltab⟩
  exact
    lsAugmentedTheorem20_2_relative_bounds_full_column_rank
      νx hνx habsx νr hνr habsr A DeltaA E Deltab f b r s x y eps
      hA hApert hExact hPert heps hE hf hxnorm_pos hrnorm_pos hDeltaA
      hDeltab
private theorem vecNorm2_pos_of_ne_zero_lsq {m : ℕ} {b : Fin m → ℝ}
    (hb : b ≠ 0) : 0 < vecNorm2 b := by
  have hbne : vecNorm2 b ≠ 0 := by
    intro hnorm
    apply hb
    ext i
    exact (vecNorm2_eq_zero_iff b).mp hnorm i
  exact lt_of_le_of_ne' (vecNorm2_nonneg b) hbne
private theorem vecNorm2Sq_pos_of_ne_zero_lsq {m : ℕ} {b : Fin m → ℝ}
    (hb : b ≠ 0) : 0 < vecNorm2Sq b := by
  have hbpos := vecNorm2_pos_of_ne_zero_lsq hb
  rw [← vecNorm2_sq]
  exact sq_pos_of_pos hbpos
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 limiting discussion:
    for a nonzero candidate vector `y`, the WKS scalar
    `mu = theta^2 ||y||_2^2 / (1 + theta^2 ||y||_2^2)` tends to one as
    `theta -> infinity`.  This is the scalar limit behind the source's
    `theta = infinity` matrix-only convention. -/
theorem lsNormwiseBackwardErrorMu_tendsto_one_atTop_of_y_ne_zero {n : ℕ}
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    Filter.Tendsto (fun theta : ℝ => lsNormwiseBackwardErrorMu theta y)
      Filter.atTop (nhds 1) := by
  have hySq_pos : 0 < vecNorm2Sq y := vecNorm2Sq_pos_of_ne_zero_lsq hy
  have hsq :
      Filter.Tendsto (fun theta : ℝ => theta ^ (2 : ℕ))
        Filter.atTop Filter.atTop :=
    Filter.tendsto_pow_atTop (α := ℝ) (by norm_num : (2 : ℕ) ≠ 0)
  have hprod :
      Filter.Tendsto (fun theta : ℝ => theta ^ (2 : ℕ) * vecNorm2Sq y)
        Filter.atTop Filter.atTop :=
    hsq.atTop_mul_const hySq_pos
  have hden :
      Filter.Tendsto
        (fun theta : ℝ => 1 + theta ^ (2 : ℕ) * vecNorm2Sq y)
        Filter.atTop Filter.atTop := by
    have hden' :
        Filter.Tendsto
          (fun theta : ℝ => theta ^ (2 : ℕ) * vecNorm2Sq y + 1)
          Filter.atTop Filter.atTop :=
      hprod.atTop_add tendsto_const_nhds
    simpa [add_comm] using hden'
  have hinv :
      Filter.Tendsto
        (fun theta : ℝ => (1 + theta ^ (2 : ℕ) * vecNorm2Sq y)⁻¹)
        Filter.atTop (nhds 0) :=
    hden.inv_tendsto_atTop
  have hlim :
      Filter.Tendsto
        (fun theta : ℝ => 1 - (1 + theta ^ (2 : ℕ) * vecNorm2Sq y)⁻¹)
        Filter.atTop (nhds 1) := by
    simpa using tendsto_const_nhds.sub hinv
  exact Filter.Tendsto.congr'
    (f₁ := fun theta : ℝ => 1 - (1 + theta ^ (2 : ℕ) * vecNorm2Sq y)⁻¹)
    (f₂ := fun theta : ℝ => lsNormwiseBackwardErrorMu theta y)
    (Filter.Eventually.of_forall fun theta => by
      simpa [one_div] using
        (lsNormwiseBackwardErrorMu_eq_one_sub_inv_den theta y).symm)
    hlim
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 limiting discussion:
    for nonzero `y`, the source scalar
    `phi = sqrt(mu) ||r||_2 / ||y||_2` tends to `||r||_2 / ||y||_2` as
    `theta -> infinity`.  This records the scalar part of the matrix-only
    WKS limit without asserting the still-open full formula (20.21). -/
theorem lsNormwiseBackwardErrorPhi_tendsto_ratio_atTop_of_y_ne_zero {m n : ℕ}
    (r : Fin m → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0) :
    Filter.Tendsto (fun theta : ℝ => lsNormwiseBackwardErrorPhi theta r y)
      Filter.atTop (nhds (vecNorm2 r / vecNorm2 y)) := by
  have hmu :
      Filter.Tendsto (fun theta : ℝ => lsNormwiseBackwardErrorMu theta y)
        Filter.atTop (nhds 1) :=
    lsNormwiseBackwardErrorMu_tendsto_one_atTop_of_y_ne_zero hy
  have hsqrt :
      Filter.Tendsto
        (fun theta : ℝ => Real.sqrt (lsNormwiseBackwardErrorMu theta y))
        Filter.atTop (nhds 1) := by
    simpa using hmu.sqrt
  have hmul :
      Filter.Tendsto
        (fun theta : ℝ =>
          Real.sqrt (lsNormwiseBackwardErrorMu theta y) * vecNorm2 r)
        Filter.atTop (nhds (1 * vecNorm2 r)) :=
    hsqrt.mul tendsto_const_nhds
  have hdiv :
      Filter.Tendsto
        (fun theta : ℝ =>
          Real.sqrt (lsNormwiseBackwardErrorMu theta y) * vecNorm2 r /
            vecNorm2 y)
        Filter.atTop (nhds ((1 * vecNorm2 r) / vecNorm2 y)) :=
    hmul.div_const (vecNorm2 y)
  simpa [lsNormwiseBackwardErrorPhi] using hdiv
/-- Positive-`y` simplification of the scalar branch
    `phi = sqrt(mu) ||r|| / ||y||` from (20.21). -/
theorem lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0) (r : Fin m → ℝ) :
    lsNormwiseBackwardErrorPhi theta r y =
      theta * vecNorm2 r /
        Real.sqrt (1 + theta ^ 2 * vecNorm2Sq y) := by
  let den : ℝ := 1 + theta ^ 2 * vecNorm2Sq y
  have hden_pos : 0 < den := by
    simpa [den] using lsNormwiseBackwardErrorMu_den_pos theta y
  have hsqrt_pos : 0 < Real.sqrt den := Real.sqrt_pos.mpr hden_pos
  have hy_norm_pos : 0 < vecNorm2 y := vecNorm2_pos_of_ne_zero_lsq hy
  have hsqrt_mu :
      Real.sqrt (lsNormwiseBackwardErrorMu theta y) =
        theta * vecNorm2 y / Real.sqrt den := by
    have hden_sq_pos : 0 < 1 + theta ^ 2 * vecNorm2 y ^ 2 := by
      simpa [den, ← vecNorm2_sq y] using hden_pos
    have hsq :
        lsNormwiseBackwardErrorMu theta y =
          (theta * vecNorm2 y / Real.sqrt den) ^ 2 := by
      unfold lsNormwiseBackwardErrorMu den
      rw [← vecNorm2_sq y, div_pow, mul_pow,
        Real.sq_sqrt (le_of_lt hden_sq_pos)]
    rw [hsq, Real.sqrt_sq_eq_abs]
    have hnonneg : 0 ≤ theta * vecNorm2 y / Real.sqrt den :=
      div_nonneg (mul_nonneg htheta (vecNorm2_nonneg y))
        (le_of_lt hsqrt_pos)
    exact abs_of_nonneg hnonneg
  unfold lsNormwiseBackwardErrorPhi
  rw [hsqrt_mu]
  calc
    (theta * vecNorm2 y / Real.sqrt den) * vecNorm2 r / vecNorm2 y =
        theta * vecNorm2 r / Real.sqrt den := by
          field_simp [ne_of_gt hy_norm_pos]
    _ = theta * vecNorm2 r /
        Real.sqrt (1 + theta ^ 2 * vecNorm2Sq y) := by
          simp [den]
/-- Exact-residual lower-bound branch for (20.21): if the source residual is
    represented as `DeltaA y - Delta b`, then the scalar branch `phi` is no
    larger than the weighted perturbation cost from (20.20). -/
theorem lsNormwiseBackwardErrorPhi_le_costF_of_residual_eq_deltaA_y_sub_deltab
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (r : Fin m → ℝ) (DeltaA : Fin m → Fin n → ℝ)
    (Deltab : Fin m → ℝ)
    (hr : r = fun i : Fin m => rectMatMulVec DeltaA y i - Deltab i) :
    lsNormwiseBackwardErrorPhi theta r y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  let w : Fin (n + 1) → ℝ :=
    Fin.append (fun j : Fin n => theta * y j) (fun _ : Fin 1 => -1)
  let W : Fin m → Fin (n + 1) → ℝ :=
    lsNormwiseBackwardErrorWeightedMatrix theta DeltaA Deltab
  have hWw :
      rectMatMulVec W w = fun i : Fin m => theta * r i := by
    rw [show W = lsNormwiseBackwardErrorWeightedMatrix theta DeltaA Deltab by rfl]
    rw [show w =
        Fin.append (fun j : Fin n => theta * y j) (fun _ : Fin 1 => -1) by rfl]
    rw [lsNormwiseBackwardErrorWeightedMatrix_mulVec_phi_witness]
    ext i
    rw [hr]
  have hw :
      vecNorm2 w = Real.sqrt (1 + theta ^ 2 * vecNorm2Sq y) := by
    simpa [w] using lsNormwiseBackwardErrorWeightedWitness_vecNorm2 theta y
  have hbound :=
    vecNorm2_rectMatMulVec_le_frobNormRect_mul W w
  have hscaled :
      theta * vecNorm2 r ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab *
          Real.sqrt (1 + theta ^ 2 * vecNorm2Sq y) := by
    simpa [W, w, hWw, hw, lsNormwiseBackwardErrorCostF, vecNorm2_smul,
      abs_of_nonneg htheta] using hbound
  have hsqrt_pos :
      0 < Real.sqrt (1 + theta ^ 2 * vecNorm2Sq y) := by
    exact Real.sqrt_pos.mpr (lsNormwiseBackwardErrorMu_den_pos theta y)
  rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den
    htheta hy r]
  exact (div_le_iff₀ hsqrt_pos).mpr hscaled
/-- Sign-oriented WKS perturbation-residual bound for the nonzero-`p`
    lower-bound route in (20.20)-(20.21).  The right projector block naturally
    contains `Delta b - Delta A y`; this theorem converts that orientation into
    the weighted-cost scalar `phi` bound. -/
theorem lsNormwiseBackwardErrorPhi_le_costF_of_residual_eq_deltab_sub_deltaA_y
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (r : Fin m → ℝ) (DeltaA : Fin m → Fin n → ℝ)
    (Deltab : Fin m → ℝ)
    (hr : r = fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i) :
    lsNormwiseBackwardErrorPhi theta r y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  let rneg : Fin m → ℝ := fun i => -r i
  have hrneg :
      rneg = fun i : Fin m => rectMatMulVec DeltaA y i - Deltab i := by
    ext i
    have hri := congrFun hr i
    calc
      rneg i = -(Deltab i - rectMatMulVec DeltaA y i) := by
        simp [rneg, hri]
      _ = rectMatMulVec DeltaA y i - Deltab i := by
        ring
  have hbase :
      lsNormwiseBackwardErrorPhi theta rneg y ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
    lsNormwiseBackwardErrorPhi_le_costF_of_residual_eq_deltaA_y_sub_deltab
      htheta hy rneg DeltaA Deltab hrneg
  have hphi :
      lsNormwiseBackwardErrorPhi theta rneg y =
        lsNormwiseBackwardErrorPhi theta r y := by
    unfold lsNormwiseBackwardErrorPhi
    have hnorm : vecNorm2 rneg = vecNorm2 r := by
      simpa [rneg] using vecNorm2_neg r
    rw [hnorm]
  simpa [hphi] using hbase
/-- Weighted-cost coupling for the right WKS projector term in the nonzero-`p`
    lower-bound route.  The scalar `phi` attached to the source residual turns
    the perturbation residual `Delta b - Delta A y` into a cost-controlled term
    with the source residual norm as the remaining factor. -/
theorem lsNormwiseBackwardErrorPhi_mul_deltab_sub_deltaA_y_vecNorm2_le_costF_mul_residual_vecNorm2
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (r : Fin m → ℝ) (DeltaA : Fin m → Fin n → ℝ)
    (Deltab : Fin m → ℝ) :
    lsNormwiseBackwardErrorPhi theta r y *
        vecNorm2 (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i) ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab * vecNorm2 r := by
  let q : Fin m → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
  have hqbound :
      lsNormwiseBackwardErrorPhi theta q y ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
    lsNormwiseBackwardErrorPhi_le_costF_of_residual_eq_deltab_sub_deltaA_y
      htheta hy q DeltaA Deltab rfl
  have hqmul :
      lsNormwiseBackwardErrorPhi theta q y * vecNorm2 r ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab * vecNorm2 r :=
    mul_le_mul_of_nonneg_right hqbound (vecNorm2_nonneg r)
  calc
    lsNormwiseBackwardErrorPhi theta r y *
        vecNorm2 (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i)
        = lsNormwiseBackwardErrorPhi theta q y * vecNorm2 r := by
          rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den htheta hy r]
          rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den htheta hy q]
          simp [q]
          ring
    _ ≤ lsNormwiseBackwardErrorCostF theta DeltaA Deltab * vecNorm2 r := hqmul
private theorem frobNormSqRect_rankOne_real {m n : ℕ} (c : ℝ)
    (r : Fin m → ℝ) (y : Fin n → ℝ) :
    frobNormSqRect (fun i j => c * r i * y j) =
      c ^ 2 * vecNorm2Sq r * vecNorm2Sq y := by
  unfold frobNormSqRect vecNorm2Sq
  calc
    (∑ i : Fin m, ∑ j : Fin n, (c * r i * y j) ^ 2)
        = ∑ i : Fin m, ∑ j : Fin n, (c ^ 2 * r i ^ 2) * y j ^ 2 := by
            apply Finset.sum_congr rfl
            intro i _
            apply Finset.sum_congr rfl
            intro j _
            ring
    _ = ∑ i : Fin m, (c ^ 2 * r i ^ 2) * ∑ j : Fin n, y j ^ 2 := by
            apply Finset.sum_congr rfl
            intro i _
            rw [Finset.mul_sum]
    _ = (∑ i : Fin m, c ^ 2 * r i ^ 2) * ∑ j : Fin n, y j ^ 2 := by
            rw [Finset.sum_mul]
    _ = c ^ 2 * (∑ i : Fin m, r i ^ 2) * ∑ j : Fin n, y j ^ 2 := by
            congr 1
            rw [Finset.mul_sum]
    _ = c ^ 2 * (∑ i : Fin m, r i ^ 2) * (∑ j : Fin n, y j ^ 2) := by
            ring
/-- Frobenius norm of the rank-one WKS perturbation. -/
theorem lsNormwiseBackwardErrorRankOneDeltaA_frobNormSq
    {m n : ℕ} {p : Fin m → ℝ} {u : Fin n → ℝ}
    (hp : vecNorm2Sq p ≠ 0) :
    frobNormSqRect (lsNormwiseBackwardErrorRankOneDeltaA p u) =
      vecNorm2Sq u / vecNorm2Sq p := by
  have h :=
    frobNormSqRect_rankOne_real (m := m) (n := n)
      (-(1 / vecNorm2Sq p)) p u
  calc
    frobNormSqRect (lsNormwiseBackwardErrorRankOneDeltaA p u)
        = (-(1 / vecNorm2Sq p)) ^ 2 * vecNorm2Sq p * vecNorm2Sq u := by
            change frobNormSqRect
                (fun i j => -((1 / vecNorm2Sq p) * p i * u j)) =
              (-(1 / vecNorm2Sq p)) ^ 2 * vecNorm2Sq p * vecNorm2Sq u
            simpa using h
    _ = vecNorm2Sq u / vecNorm2Sq p := by
            field_simp [hp]
/-- Exact-fit constructive witness for the `phi` branch of the WKS
    upper-bound route.  For finite nonnegative `theta` and nonzero candidate
    `y`, a rank-one perturbation makes `y` fit the perturbed data exactly and
    has weighted Frobenius cost equal to the scalar `phi` in (20.21). -/
theorem lsNormwiseBackwardErrorExactFitPhiWitness {m n : ℕ}
    {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
      LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
          lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let den : ℝ := 1 + theta ^ 2 * vecNorm2Sq y
  let DeltaA : Fin (m + 1) → Fin n → ℝ :=
    fun i j => (theta ^ 2 / den) * r i * y j
  let Deltab : Fin (m + 1) → ℝ :=
    fun i => -(1 / den) * r i
  refine ⟨DeltaA, Deltab, ?_, ?_⟩
  · unfold LSNormwiseBackwardErrorFeasible
    apply IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero
    ext i
    have hden_pos : 0 < den := by
      simpa [den] using lsNormwiseBackwardErrorMu_den_pos theta y
    have hden_ne : den ≠ 0 := ne_of_gt hden_pos
    unfold lsResidualHigham rectMatMulVec
    simp_rw [add_mul]
    rw [Finset.sum_add_distrib]
    dsimp [DeltaA, Deltab, r]
    change
      b i + -(1 / den) * lsResidualHigham A b y i -
          ((∑ j : Fin n, A i j * y j) +
            ∑ j : Fin n,
              theta ^ 2 / den * lsResidualHigham A b y i * y j * y j) = 0
    calc
      b i + -(1 / den) * lsResidualHigham A b y i -
          ((∑ j : Fin n, A i j * y j) +
            ∑ j : Fin n,
              theta ^ 2 / den * lsResidualHigham A b y i * y j * y j)
          = lsResidualHigham A b y i -
              (1 / den) * lsResidualHigham A b y i -
              (theta ^ 2 / den * lsResidualHigham A b y i) *
                vecNorm2Sq y := by
              unfold lsResidualHigham rectMatMulVec vecNorm2Sq
              rw [Finset.mul_sum]
              ring_nf
      _ = 0 := by
              field_simp [hden_ne]
              dsimp [den]
              ring
  · have hden_pos : 0 < den := by
      simpa [den] using lsNormwiseBackwardErrorMu_den_pos theta y
    have hden_nonneg : 0 ≤ den := le_of_lt hden_pos
    have hden_ne : den ≠ 0 := ne_of_gt hden_pos
    have hcost_sq :
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab ^ 2 =
          theta ^ 2 * vecNorm2Sq r / den := by
      rw [lsNormwiseBackwardErrorCostF_sq]
      have hA : frobNormSqRect DeltaA =
          (theta ^ 2 / den) ^ 2 * vecNorm2Sq r * vecNorm2Sq y := by
        simpa [DeltaA, mul_assoc] using
          frobNormSqRect_rankOne_real (m := m + 1) (n := n)
            (theta ^ 2 / den) r y
      have hb : vecNorm2Sq Deltab = (-(1 / den)) ^ 2 * vecNorm2Sq r := by
        simpa [Deltab] using vecNorm2Sq_smul (-(1 / den)) r
      rw [hA, hb]
      field_simp [hden_ne]
      dsimp [den]
      ring
    have hphi_sq :
        lsNormwiseBackwardErrorPhi theta r y ^ 2 =
          theta ^ 2 * vecNorm2Sq r / den := by
      rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den htheta hy r]
      rw [div_pow, mul_pow, Real.sq_sqrt hden_nonneg, ← vecNorm2_sq]
    have hcost_nonneg : 0 ≤ lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
      lsNormwiseBackwardErrorCostF_nonneg theta DeltaA Deltab
    have hphi_nonneg : 0 ≤ lsNormwiseBackwardErrorPhi theta r y :=
      lsNormwiseBackwardErrorPhi_nonneg theta r y
    have hsqeq :
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab ^ 2 =
          lsNormwiseBackwardErrorPhi theta r y ^ 2 := by
      rw [hcost_sq, hphi_sq]
    have habs := (sq_eq_sq_iff_eq_or_eq_neg.mp hsqeq)
    rcases habs with h | h
    · simpa [r] using h
    · have hphi_le_zero : lsNormwiseBackwardErrorPhi theta r y ≤ 0 := by
        nlinarith
      have hphi_zero : lsNormwiseBackwardErrorPhi theta r y = 0 :=
        le_antisymm hphi_le_zero hphi_nonneg
      have hcost_zero : lsNormwiseBackwardErrorCostF theta DeltaA Deltab = 0 := by
        nlinarith
      exact hcost_zero.trans hphi_zero.symm
/-- Constructive `phi`-branch upper bound for (20.21): the exact-fit witness
    shows that the infimum model `eta_F(y)` is no larger than the scalar
    `phi`.  The remaining WKS upper-bound work is the source-block
    `sigma_min` branch when it is smaller than `phi`. -/
theorem lsNormwiseBackwardErrorEtaF_le_phi_of_theta_nonneg {m n : ℕ}
    {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y := by
  rcases lsNormwiseBackwardErrorExactFitPhiWitness htheta A b hy with
    ⟨DeltaA, Deltab, hfeas, hcost⟩
  calc
    lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
      lsNormwiseBackwardErrorEtaF_le_costF_of_feasible theta A b y DeltaA Deltab hfeas
    _ = lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y := hcost
/-- The rank-one projector `P = ss^T/(s^Ts)` used in the proof of Lemma 20.6. -/
noncomputable def lsLemma20_6Projector {m : ℕ} (s : Fin m → ℝ) :
    Fin m → Fin m → ℝ :=
  fun i j => s i * s j / vecNorm2Sq s
/-- The complementary projector `I - P` from equation (20.22). -/
noncomputable def lsLemma20_6ProjectorComplement {m : ℕ} (s : Fin m → ℝ) :
    Fin m → Fin m → ℝ :=
  fun i j => idMatrix m i j - lsLemma20_6Projector s i j
/-- Higham, 2nd ed., Chapter 20, equation (20.21): source-facing model of
    `r r^+` in the nonzero-residual case, represented as the rank-one
    projector `r r^T / ||r||_2^2`.  This is a transparent alias for the
    rank-one projector already used in Lemma 20.6. -/
noncomputable abbrev lsResidualPseudoinverseProjector {m : ℕ}
    (r : Fin m → ℝ) : Fin m → Fin m → ℝ :=
  lsLemma20_6Projector r
/-- Higham, 2nd ed., Chapter 20, equation (20.21): source-facing model of
    `I - r r^+` in the nonzero-residual case. -/
noncomputable abbrev lsResidualComplementProjector {m : ℕ}
    (r : Fin m → ℝ) : Fin m → Fin m → ℝ :=
  lsLemma20_6ProjectorComplement r
/-- Entry form of the source projector `r r^+` from (20.21). -/
theorem lsResidualPseudoinverseProjector_apply {m : ℕ}
    (r : Fin m → ℝ) (i j : Fin m) :
    lsResidualPseudoinverseProjector r i j = r i * r j / vecNorm2Sq r := rfl
/-- Entry form of the source complement `I - r r^+` from (20.21). -/
theorem lsResidualComplementProjector_apply {m : ℕ}
    (r : Fin m → ℝ) (i j : Fin m) :
    lsResidualComplementProjector r i j =
      idMatrix m i j - r i * r j / vecNorm2Sq r := rfl
/-- The projector `P = ss^T/(s^Ts)` in Lemma 20.6 is symmetric. -/
theorem lsLemma20_6Projector_symmetric {m : ℕ} (s : Fin m → ℝ)
    (i j : Fin m) :
    lsLemma20_6Projector s i j = lsLemma20_6Projector s j i := by
  unfold lsLemma20_6Projector
  ring
/-- Nonzero `s` gives the nonzero denominator `s^Ts` required in the
    nonzero-residual branch of Lemma 20.6. -/
theorem lsLemma20_6Projector_den_ne_zero_of_ne_zero {m : ℕ} {s : Fin m → ℝ}
    (hs : s ≠ 0) :
    vecNorm2Sq s ≠ 0 := by
  intro hsq
  have hnorm : vecNorm2 s = 0 := by
    unfold vecNorm2
    rw [Real.sqrt_eq_zero (vecNorm2Sq_nonneg s)]
    exact hsq
  have hentries : ∀ i : Fin m, s i = 0 := (vecNorm2_eq_zero_iff s).mp hnorm
  apply hs
  ext i
  exact hentries i
/-- The projector `P = ss^T/(s^Ts)` fixes `s`, the identity used in the proof
    of Lemma 20.6 after defining `DeltaA = DeltaA1 + P (DeltaA2 - DeltaA1)`. -/
theorem lsLemma20_6Projector_apply_self {m : ℕ} (s : Fin m → ℝ)
    (hsq : vecNorm2Sq s ≠ 0) (i : Fin m) :
    ∑ j : Fin m, lsLemma20_6Projector s i j * s j = s i := by
  unfold lsLemma20_6Projector vecNorm2Sq
  let D : ℝ := ∑ k : Fin m, s k ^ 2
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  change (∑ j : Fin m, (s i * s j / D) * s j) = s i
  calc
    (∑ j : Fin m, (s i * s j / D) * s j)
        = (s i / D) * ∑ j : Fin m, s j ^ 2 := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro j _
          ring
    _ = (s i / D) * D := rfl
    _ = s i := by
      field_simp [hD]
/-- Since `P s = s`, the complementary action `(P - I)s` is zero.  This is the
    displayed cancellation in the proof of Lemma 20.6. -/
theorem lsLemma20_6Projector_sub_id_apply_self {m : ℕ} (s : Fin m → ℝ)
    (hsq : vecNorm2Sq s ≠ 0) (i : Fin m) :
    ∑ j : Fin m, (lsLemma20_6Projector s i j - idMatrix m i j) * s j = 0 := by
  have hid := congrFun (idMatrix_mulVec m s) i
  calc
    (∑ j : Fin m, (lsLemma20_6Projector s i j - idMatrix m i j) * s j)
        = (∑ j : Fin m, lsLemma20_6Projector s i j * s j) -
            (∑ j : Fin m, idMatrix m i j * s j) := by
          rw [← Finset.sum_sub_distrib]
          apply Finset.sum_congr rfl
          intro j _
          ring
    _ = s i - s i := by
      rw [lsLemma20_6Projector_apply_self s hsq i, hid]
    _ = 0 := by ring
/-- The complementary projector `I - s s^+` annihilates its source direction. -/
theorem lsLemma20_6ProjectorComplement_mulVec_self {m : ℕ} (s : Fin m → ℝ)
    (hsq : vecNorm2Sq s ≠ 0) :
    matMulVec m (lsLemma20_6ProjectorComplement s) s = 0 := by
  ext i
  have hrow := lsLemma20_6Projector_sub_id_apply_self s hsq i
  have hcomp :
      ∑ j : Fin m, (idMatrix m i j - lsLemma20_6Projector s i j) * s j = 0 := by
    calc
      (∑ j : Fin m, (idMatrix m i j - lsLemma20_6Projector s i j) * s j)
          = -∑ j : Fin m, (lsLemma20_6Projector s i j - idMatrix m i j) * s j := by
            rw [← Finset.sum_neg_distrib]
            apply Finset.sum_congr rfl
            intro j _
            ring
      _ = 0 := by
            rw [hrow]
            ring
  simpa [lsLemma20_6ProjectorComplement, matMulVec] using hcomp
/-- The two projector blocks in equation (20.22), here `P` and `I-P`, add to
    the identity. -/
theorem lsLemma20_6Projector_add_complement {m : ℕ} (s : Fin m → ℝ)
    (i j : Fin m) :
    lsLemma20_6Projector s i j + lsLemma20_6ProjectorComplement s i j =
      idMatrix m i j := by
  unfold lsLemma20_6ProjectorComplement
  ring
/-- The complementary projector `I-P` in equation (20.22) is symmetric. -/
theorem lsLemma20_6ProjectorComplement_symmetric {m : ℕ} (s : Fin m → ℝ)
    (i j : Fin m) :
    lsLemma20_6ProjectorComplement s i j = lsLemma20_6ProjectorComplement s j i := by
  unfold lsLemma20_6ProjectorComplement
  rw [lsLemma20_6Projector_symmetric]
  unfold idMatrix
  by_cases h : i = j
  · subst j
    simp
  · have hji : j ≠ i := Ne.symm h
    simp [h, hji]
/-- Idempotence `P^2 = P` for the rank-one projector in equation (20.22). -/
theorem lsLemma20_6Projector_idempotent {m : ℕ} (s : Fin m → ℝ)
    (hsq : vecNorm2Sq s ≠ 0) :
    matMul m (lsLemma20_6Projector s) (lsLemma20_6Projector s) =
      lsLemma20_6Projector s := by
  ext i j
  unfold matMul lsLemma20_6Projector vecNorm2Sq
  let D : ℝ := ∑ k : Fin m, s k ^ 2
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  change (∑ k : Fin m, (s i * s k / D) * (s k * s j / D)) = s i * s j / D
  calc
    (∑ k : Fin m, (s i * s k / D) * (s k * s j / D))
        = (s i * s j / (D * D)) * ∑ k : Fin m, s k ^ 2 := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro k _
          ring
    _ = (s i * s j / (D * D)) * D := rfl
    _ = s i * s j / D := by
      field_simp [hD]
/-- Idempotence `(I-P)^2 = I-P` for the complementary projector in
    equation (20.22). -/
theorem lsLemma20_6ProjectorComplement_idempotent {m : ℕ} (s : Fin m → ℝ)
    (hsq : vecNorm2Sq s ≠ 0) :
    matMul m (lsLemma20_6ProjectorComplement s) (lsLemma20_6ProjectorComplement s) =
      lsLemma20_6ProjectorComplement s := by
  ext i j
  have hP := congrFun (congrFun (lsLemma20_6Projector_idempotent s hsq) i) j
  have hIP := congrFun (congrFun (matMul_id_left m (lsLemma20_6Projector s)) i) j
  have hPI := congrFun (congrFun (matMul_id_right m (lsLemma20_6Projector s)) i) j
  have hII := congrFun (congrFun (matMul_id_left m (idMatrix m)) i) j
  unfold lsLemma20_6ProjectorComplement matMul at *
  calc
    (∑ k : Fin m,
        (idMatrix m i k - lsLemma20_6Projector s i k) *
          (idMatrix m k j - lsLemma20_6Projector s k j))
        = (∑ k : Fin m, idMatrix m i k * idMatrix m k j) -
            (∑ k : Fin m, idMatrix m i k * lsLemma20_6Projector s k j) -
            (∑ k : Fin m, lsLemma20_6Projector s i k * idMatrix m k j) +
            (∑ k : Fin m, lsLemma20_6Projector s i k *
              lsLemma20_6Projector s k j) := by
          simp_rw [sub_mul, mul_sub]
          rw [Finset.sum_sub_distrib]
          rw [Finset.sum_sub_distrib]
          rw [Finset.sum_sub_distrib]
          ring
    _ = idMatrix m i j - lsLemma20_6Projector s i j := by
          rw [hII, hIP, hPI, hP]
          ring
private theorem rectMatMulVec_add_matrix_lsq {m n : ℕ}
    (M N : Fin m → Fin n → ℝ) (x : Fin n → ℝ) :
    rectMatMulVec (fun i j => M i j + N i j) x =
      fun i => rectMatMulVec M x i + rectMatMulVec N x i := by
  ext i
  unfold rectMatMulVec
  rw [← Finset.sum_add_distrib]
  apply Finset.sum_congr rfl
  intro j _
  ring
/-- Rank-one projector action on an arbitrary vector.  This is the algebraic
    form of `P v = s (s^T v)/(s^T s)` used in the proof of Lemma 20.6. -/
theorem lsLemma20_6Projector_apply_vec {m : ℕ} (s v : Fin m → ℝ)
    (i : Fin m) :
    ∑ k : Fin m, lsLemma20_6Projector s i k * v k =
      (s i / vecNorm2Sq s) * ∑ k : Fin m, s k * v k := by
  unfold lsLemma20_6Projector vecNorm2Sq
  let D : ℝ := ∑ k : Fin m, s k ^ 2
  change (∑ k : Fin m, (s i * s k / D) * v k) =
    (s i / D) * ∑ k : Fin m, s k * v k
  rw [Finset.mul_sum]
  apply Finset.sum_congr rfl
  intro k _
  ring
/-- Column-entry form of the rank-one projector action used in Lemma 20.6. -/
theorem lsLemma20_6Projector_matMulRectLeft_entry {m n : ℕ}
    (s : Fin m → ℝ) (A : Fin m → Fin n → ℝ) (i : Fin m) (j : Fin n) :
    matMulRectLeft (lsLemma20_6Projector s) A i j =
      (s i / vecNorm2Sq s) * ∑ k : Fin m, s k * A k j := by
  simpa [matMulRectLeft] using
    (lsLemma20_6Projector_apply_vec s (fun k : Fin m => A k j) i)
/-- Squared Frobenius norm of the rank-one projected panel `P A`, where
    `P = ss^T/(s^Ts)` is the projector from equation (20.22). -/
theorem lsLemma20_6Projector_frobNormSqRect {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (A : Fin m → Fin n → ℝ) :
    frobNormSqRect (matMulRectLeft (lsLemma20_6Projector s) A) =
      ∑ j : Fin n, (∑ i : Fin m, s i * A i j) ^ 2 / vecNorm2Sq s := by
  unfold frobNormSqRect
  conv_lhs => rw [Finset.sum_comm]
  apply Finset.sum_congr rfl
  intro j _
  let D : ℝ := vecNorm2Sq s
  let dot : ℝ := ∑ k : Fin m, s k * A k j
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  have hcol :
      (∑ i : Fin m, matMulRectLeft (lsLemma20_6Projector s) A i j ^ 2) =
        dot ^ 2 / D := by
    calc
      (∑ i : Fin m, matMulRectLeft (lsLemma20_6Projector s) A i j ^ 2)
          = ∑ i : Fin m, ((s i / D) * dot) ^ 2 := by
            apply Finset.sum_congr rfl
            intro i _
            simpa [D, dot] using congrArg (fun x => x ^ 2)
              (lsLemma20_6Projector_matMulRectLeft_entry s A i j)
      _ = (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = (dot ^ 2 / (D * D)) * D := by
            simp [D, vecNorm2Sq]
      _ = dot ^ 2 / D := by
            field_simp [hD]
  simpa [D, dot] using hcol
/-- The rank-one projector from equation (20.22) is a Frobenius contraction. -/
theorem lsLemma20_6Projector_frobNormSqRect_le {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (A : Fin m → Fin n → ℝ) :
    frobNormSqRect (matMulRectLeft (lsLemma20_6Projector s) A) ≤
      frobNormSqRect A := by
  rw [lsLemma20_6Projector_frobNormSqRect s hsq A]
  unfold frobNormSqRect
  conv_rhs => rw [Finset.sum_comm]
  apply Finset.sum_le_sum
  intro j _
  have hDpos : 0 < vecNorm2Sq s :=
    lt_of_le_of_ne (vecNorm2Sq_nonneg s) (Ne.symm hsq)
  have hcs := vecInnerProduct_sq_le s (fun i : Fin m => A i j)
  have hdiv :
      (∑ i : Fin m, s i * A i j) ^ 2 / vecNorm2Sq s ≤
        vecNorm2Sq (fun i : Fin m => A i j) := by
    rw [div_le_iff₀ hDpos]
    simpa [mul_comm] using hcs
  simpa [vecNorm2Sq] using hdiv
/-- Complementary projector action on a vector:
    `(I-P)v = v - s(s^Tv)/(s^Ts)`. -/
theorem lsLemma20_6ProjectorComplement_apply_vec {m : ℕ}
    (s v : Fin m → ℝ) (i : Fin m) :
    ∑ k : Fin m, lsLemma20_6ProjectorComplement s i k * v k =
      v i - (s i / vecNorm2Sq s) * ∑ k : Fin m, s k * v k := by
  have hid := congrFun (idMatrix_mulVec m v) i
  calc
    (∑ k : Fin m, lsLemma20_6ProjectorComplement s i k * v k)
        = (∑ k : Fin m, idMatrix m i k * v k) -
            (∑ k : Fin m, lsLemma20_6Projector s i k * v k) := by
          unfold lsLemma20_6ProjectorComplement
          rw [← Finset.sum_sub_distrib]
          apply Finset.sum_congr rfl
          intro k _
          ring
    _ = v i - (s i / vecNorm2Sq s) * ∑ k : Fin m, s k * v k := by
          rw [hid, lsLemma20_6Projector_apply_vec]
/-- Exact squared-norm formula for the complement projector `I - s s^+`:
    applying the complement removes precisely the squared component in the
    source direction `s`. -/
theorem lsLemma20_6ProjectorComplement_vecNorm2Sq_eq_sub {m : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0) (v : Fin m → ℝ) :
    vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement s) v) =
      vecNorm2Sq v - (∑ k : Fin m, s k * v k) ^ 2 / vecNorm2Sq s := by
  let D : ℝ := vecNorm2Sq s
  let dot : ℝ := ∑ k : Fin m, s k * v k
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  have hmiddle :
      (∑ i : Fin m, 2 * (v i * ((s i / D) * dot))) =
        2 * (dot / D) * dot := by
    calc
      (∑ i : Fin m, 2 * (v i * ((s i / D) * dot)))
          = 2 * (dot / D) * ∑ i : Fin m, s i * v i := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = 2 * (dot / D) * dot := by
            simp [dot]
  have hlast :
      (∑ i : Fin m, ((s i / D) * dot) ^ 2) =
        (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro i _
    ring
  unfold vecNorm2Sq matMulVec
  calc
    (∑ i : Fin m,
        (∑ k : Fin m, lsLemma20_6ProjectorComplement s i k * v k) ^ 2)
        = ∑ i : Fin m, (v i - (s i / D) * dot) ^ 2 := by
          apply Finset.sum_congr rfl
          intro i _
          simpa [D, dot] using congrArg (fun x => x ^ 2)
            (lsLemma20_6ProjectorComplement_apply_vec s v i)
    _ = ∑ i : Fin m,
          (v i ^ 2 - 2 * (v i * ((s i / D) * dot)) +
            ((s i / D) * dot) ^ 2) := by
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = (∑ i : Fin m, v i ^ 2) -
          (∑ i : Fin m, 2 * (v i * ((s i / D) * dot))) +
          (∑ i : Fin m, ((s i / D) * dot) ^ 2) := by
          rw [Finset.sum_add_distrib, Finset.sum_sub_distrib]
    _ = (∑ i : Fin m, v i ^ 2) -
          2 * (dot / D) * dot + (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
          rw [hmiddle, hlast]
    _ = (∑ i : Fin m, v i ^ 2) -
          2 * (dot / D) * dot + (dot ^ 2 / (D * D)) * D := by
          simp [D, vecNorm2Sq]
    _ = (∑ i : Fin m, v i ^ 2) - dot ^ 2 / D := by
          field_simp [hD]
          ring
    _ = vecNorm2Sq v - (∑ k : Fin m, s k * v k) ^ 2 / vecNorm2Sq s := by
          simp [D, dot, vecNorm2Sq]
/-- Exact squared Frobenius norm of the complementary projected panel
    `(I-P)A`: columnwise it removes precisely the squared component in the
    source direction `s`. -/
theorem lsLemma20_6ProjectorComplement_frobNormSqRect_eq_sub {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (A : Fin m → Fin n → ℝ) :
    frobNormSqRect (matMulRectLeft (lsLemma20_6ProjectorComplement s) A) =
      frobNormSqRect A -
        ∑ j : Fin n, (∑ i : Fin m, s i * A i j) ^ 2 / vecNorm2Sq s := by
  unfold frobNormSqRect
  conv_lhs => rw [Finset.sum_comm]
  rw [show (∑ i : Fin m, ∑ j : Fin n, A i j ^ 2) =
      ∑ j : Fin n, ∑ i : Fin m, A i j ^ 2 by
        rw [Finset.sum_comm]]
  rw [← Finset.sum_sub_distrib]
  apply Finset.sum_congr rfl
  intro j _
  have hcol :=
    lsLemma20_6ProjectorComplement_vecNorm2Sq_eq_sub
      s hsq (fun i : Fin m => A i j)
  simpa [vecNorm2Sq, matMulVec, matMulRectLeft] using hcol
/-- Pythagorean Frobenius split for the rank-one projector direction `s`.
    The squared transpose action along `s` plus the scaled complementary
    Frobenius norm recovers the scaled total Frobenius norm. -/
theorem lsLemma20_6Projector_transpose_action_vecNorm2Sq_add_complement_frobNormSqRect
    {m n : ℕ} (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (A : Fin m → Fin n → ℝ) :
    vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, s i * A i j) +
        vecNorm2Sq s *
          frobNormSqRect (matMulRectLeft (lsLemma20_6ProjectorComplement s) A) =
      vecNorm2Sq s * frobNormSqRect A := by
  let R : ℝ := vecNorm2Sq s
  let z : Fin n → ℝ := fun j : Fin n => ∑ i : Fin m, s i * A i j
  have hR : R ≠ 0 := by
    simpa [R] using hsq
  have hsum :
      (∑ j : Fin n, (∑ i : Fin m, s i * A i j) ^ 2 / R) =
        vecNorm2Sq z / R := by
    unfold z vecNorm2Sq
    rw [Finset.sum_div]
  have hcomp :
      frobNormSqRect (matMulRectLeft (lsLemma20_6ProjectorComplement s) A) =
        frobNormSqRect A - vecNorm2Sq z / R := by
    have hbase :=
      lsLemma20_6ProjectorComplement_frobNormSqRect_eq_sub s hsq A
    simpa [R, z, hsum] using hbase
  calc
    vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, s i * A i j) +
        vecNorm2Sq s *
          frobNormSqRect (matMulRectLeft (lsLemma20_6ProjectorComplement s) A)
        = vecNorm2Sq z + R * (frobNormSqRect A - vecNorm2Sq z / R) := by
          simp [R, z, hcomp]
    _ = R * frobNormSqRect A := by
          field_simp [hR]
          ring
    _ = vecNorm2Sq s * frobNormSqRect A := by
          simp [R]
/-- The complementary projector `I-P` from equation (20.22) is a Frobenius
    contraction. -/
theorem lsLemma20_6ProjectorComplement_frobNormSqRect_le {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (A : Fin m → Fin n → ℝ) :
    frobNormSqRect (matMulRectLeft (lsLemma20_6ProjectorComplement s) A) ≤
      frobNormSqRect A := by
  unfold frobNormSqRect
  conv_lhs => rw [Finset.sum_comm]
  conv_rhs => rw [Finset.sum_comm]
  apply Finset.sum_le_sum
  intro j _
  let D : ℝ := vecNorm2Sq s
  let dot : ℝ := ∑ k : Fin m, s k * A k j
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  have hDpos : 0 < D := by
    simpa [D] using lt_of_le_of_ne (vecNorm2Sq_nonneg s) (Ne.symm hsq)
  have hcs := vecInnerProduct_sq_le s (fun i : Fin m => A i j)
  have hdiv_nonneg : 0 ≤ dot ^ 2 / D :=
    div_nonneg (sq_nonneg dot) hDpos.le
  have hmiddle :
      (∑ i : Fin m, 2 * (A i j * ((s i / D) * dot))) =
        2 * (dot / D) * dot := by
    calc
      (∑ i : Fin m, 2 * (A i j * ((s i / D) * dot)))
          = 2 * (dot / D) * ∑ i : Fin m, s i * A i j := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = 2 * (dot / D) * dot := by
            simp [dot]
  have hlast :
      (∑ i : Fin m, ((s i / D) * dot) ^ 2) =
        (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro i _
    ring
  have hcol :
      (∑ i : Fin m,
          matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j ^ 2) =
        (∑ i : Fin m, A i j ^ 2) - dot ^ 2 / D := by
    calc
      (∑ i : Fin m,
          matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j ^ 2)
          = ∑ i : Fin m, (A i j - (s i / D) * dot) ^ 2 := by
            apply Finset.sum_congr rfl
            intro i _
            have hentry :=
              lsLemma20_6ProjectorComplement_apply_vec
                s (fun k : Fin m => A k j) i
            simpa [matMulRectLeft, D, dot] using congrArg (fun x => x ^ 2) hentry
      _ = ∑ i : Fin m,
            (A i j ^ 2 - 2 * (A i j * ((s i / D) * dot)) +
              ((s i / D) * dot) ^ 2) := by
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = (∑ i : Fin m, A i j ^ 2) -
            (∑ i : Fin m, 2 * (A i j * ((s i / D) * dot))) +
            (∑ i : Fin m, ((s i / D) * dot) ^ 2) := by
            rw [Finset.sum_add_distrib, Finset.sum_sub_distrib]
      _ = (∑ i : Fin m, A i j ^ 2) -
            2 * (dot / D) * dot + (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
            rw [hmiddle, hlast]
      _ = (∑ i : Fin m, A i j ^ 2) -
            2 * (dot / D) * dot + (dot ^ 2 / (D * D)) * D := by
            simp [D, vecNorm2Sq]
      _ = (∑ i : Fin m, A i j ^ 2) - dot ^ 2 / D := by
            field_simp [hD]
            ring
  calc
    (∑ i : Fin m,
        matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j ^ 2)
        = (∑ i : Fin m, A i j ^ 2) - dot ^ 2 / D := hcol
    _ ≤ ∑ i : Fin m, A i j ^ 2 := by
          linarith
/-- The rank-one projector from equation (20.22) is a vector 2-norm
    contraction. -/
theorem lsLemma20_6Projector_vecNorm2Sq_le {m : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0) (v : Fin m → ℝ) :
    vecNorm2Sq (matMulVec m (lsLemma20_6Projector s) v) ≤ vecNorm2Sq v := by
  let D : ℝ := vecNorm2Sq s
  let dot : ℝ := ∑ k : Fin m, s k * v k
  have hDpos : 0 < D := by
    simpa [D] using lt_of_le_of_ne (vecNorm2Sq_nonneg s) (Ne.symm hsq)
  have hcs := vecInnerProduct_sq_le s v
  have hnorm :
      vecNorm2Sq (matMulVec m (lsLemma20_6Projector s) v) = dot ^ 2 / D := by
    unfold vecNorm2Sq matMulVec
    calc
      (∑ i : Fin m,
          (∑ k : Fin m, lsLemma20_6Projector s i k * v k) ^ 2)
          = ∑ i : Fin m, ((s i / D) * dot) ^ 2 := by
            apply Finset.sum_congr rfl
            intro i _
            simpa [D, dot] using congrArg (fun x => x ^ 2)
              (lsLemma20_6Projector_apply_vec s v i)
      _ = (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = (dot ^ 2 / (D * D)) * D := by
            simp [D, vecNorm2Sq]
      _ = dot ^ 2 / D := by
            have hD : D ≠ 0 := ne_of_gt hDpos
            field_simp [hD]
  rw [hnorm]
  rw [div_le_iff₀ hDpos]
  simpa [D, dot, mul_comm] using hcs
/-- Symmetric area identity for complement projections: the squared area of the
    parallelogram spanned by `r` and `p` can be measured by projecting either
    vector away from the other. -/
theorem lsLemma20_6ProjectorComplement_area_identity {m : ℕ}
    (r p : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0)
    (hpsq : vecNorm2Sq p ≠ 0) :
    vecNorm2Sq r *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p) =
      vecNorm2Sq p *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) := by
  let R : ℝ := vecNorm2Sq r
  let P : ℝ := vecNorm2Sq p
  let dot : ℝ := ∑ i : Fin m, r i * p i
  have hR : R ≠ 0 := by
    simpa [R] using hrsq
  have hP : P ≠ 0 := by
    simpa [P] using hpsq
  have hdot_comm : (∑ i : Fin m, p i * r i) = dot := by
    simp [dot, mul_comm]
  have hQr :
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p) =
        P - dot ^ 2 / R := by
    simpa [R, P, dot] using
      lsLemma20_6ProjectorComplement_vecNorm2Sq_eq_sub r hrsq p
  have hQp :
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) =
        R - dot ^ 2 / P := by
    simpa [R, P, dot, hdot_comm] using
      lsLemma20_6ProjectorComplement_vecNorm2Sq_eq_sub p hpsq r
  calc
    vecNorm2Sq r *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p)
        = R * (P - dot ^ 2 / R) := by
          simp [R, hQr]
    _ = R * P - dot ^ 2 := by
          field_simp [hR]
    _ = P * R - dot ^ 2 := by
          ring
    _ = P * (R - dot ^ 2 / P) := by
          field_simp [hP]
    _ = vecNorm2Sq p *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) := by
          simp [P, hQp]
/-- Area identity with the second vector written as the difference `p - r`.
    This is the projection form used by the WKS expanded-residual route, where
    `p - r` is the perturbation residual. -/
theorem lsLemma20_6ProjectorComplement_area_identity_sub {m : ℕ}
    (r p : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0)
    (hpsq : vecNorm2Sq p ≠ 0) :
    vecNorm2Sq r *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p) =
      vecNorm2Sq p *
        vecNorm2Sq
          (matMulVec m (lsLemma20_6ProjectorComplement p)
            (fun i : Fin m => p i - r i)) := by
  let Qp : Fin m → Fin m → ℝ := lsLemma20_6ProjectorComplement p
  have harea :=
    lsLemma20_6ProjectorComplement_area_identity r p hrsq hpsq
  have hsub :
      matMulVec m Qp (fun i : Fin m => p i - r i) =
        fun i : Fin m => -matMulVec m Qp r i := by
    ext i
    have hself := congrFun (lsLemma20_6ProjectorComplement_mulVec_self p hpsq) i
    calc
      matMulVec m Qp (fun i : Fin m => p i - r i) i
          = (∑ j : Fin m, Qp i j * p j) -
              (∑ j : Fin m, Qp i j * r j) := by
            unfold matMulVec
            rw [← Finset.sum_sub_distrib]
            apply Finset.sum_congr rfl
            intro j _
            ring
      _ = -matMulVec m Qp r i := by
            simpa [Qp, matMulVec, hself]
  have hnorm :
      vecNorm2Sq (matMulVec m Qp (fun i : Fin m => p i - r i)) =
        vecNorm2Sq (matMulVec m Qp r) := by
    rw [hsub]
    simpa using (vecNorm2Sq_smul (-1 : ℝ) (matMulVec m Qp r))
  calc
    vecNorm2Sq r *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p)
        = vecNorm2Sq p *
          vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) := harea
    _ = vecNorm2Sq p *
        vecNorm2Sq
          (matMulVec m (lsLemma20_6ProjectorComplement p)
            (fun i : Fin m => p i - r i)) := by
          rw [show lsLemma20_6ProjectorComplement p = Qp by rfl]
          rw [hnorm]
/-- The complementary projector `I-P` from equation (20.22) is a vector
    2-norm contraction. -/
theorem lsLemma20_6ProjectorComplement_vecNorm2Sq_le {m : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0) (v : Fin m → ℝ) :
    vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement s) v) ≤ vecNorm2Sq v := by
  let D : ℝ := vecNorm2Sq s
  let dot : ℝ := ∑ k : Fin m, s k * v k
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  have hDpos : 0 < D := by
    simpa [D] using lt_of_le_of_ne (vecNorm2Sq_nonneg s) (Ne.symm hsq)
  have hdiv_nonneg : 0 ≤ dot ^ 2 / D :=
    div_nonneg (sq_nonneg dot) hDpos.le
  have hmiddle :
      (∑ i : Fin m, 2 * (v i * ((s i / D) * dot))) =
        2 * (dot / D) * dot := by
    calc
      (∑ i : Fin m, 2 * (v i * ((s i / D) * dot)))
          = 2 * (dot / D) * ∑ i : Fin m, s i * v i := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = 2 * (dot / D) * dot := by
            simp [dot]
  have hlast :
      (∑ i : Fin m, ((s i / D) * dot) ^ 2) =
        (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro i _
    ring
  have hnorm :
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement s) v) =
        vecNorm2Sq v - dot ^ 2 / D := by
    unfold vecNorm2Sq matMulVec
    calc
      (∑ i : Fin m,
          (∑ k : Fin m, lsLemma20_6ProjectorComplement s i k * v k) ^ 2)
          = ∑ i : Fin m, (v i - (s i / D) * dot) ^ 2 := by
            apply Finset.sum_congr rfl
            intro i _
            simpa [D, dot] using congrArg (fun x => x ^ 2)
              (lsLemma20_6ProjectorComplement_apply_vec s v i)
      _ = ∑ i : Fin m,
            (v i ^ 2 - 2 * (v i * ((s i / D) * dot)) +
              ((s i / D) * dot) ^ 2) := by
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = (∑ i : Fin m, v i ^ 2) -
            (∑ i : Fin m, 2 * (v i * ((s i / D) * dot))) +
            (∑ i : Fin m, ((s i / D) * dot) ^ 2) := by
            rw [Finset.sum_add_distrib, Finset.sum_sub_distrib]
      _ = (∑ i : Fin m, v i ^ 2) -
            2 * (dot / D) * dot + (dot ^ 2 / (D * D)) * ∑ i : Fin m, s i ^ 2 := by
            rw [hmiddle, hlast]
      _ = (∑ i : Fin m, v i ^ 2) -
            2 * (dot / D) * dot + (dot ^ 2 / (D * D)) * D := by
            simp [D, vecNorm2Sq]
      _ = (∑ i : Fin m, v i ^ 2) - dot ^ 2 / D := by
            field_simp [hD]
            ring
  rw [hnorm]
  linarith
/-- Complement projectors commute with the perturbation residual
    `Delta b - Delta A y`.  This is the algebraic bridge used in the WKS
    nonzero-`p` route to apply the weighted residual bound after projecting
    both perturbation blocks away from the expanded residual direction. -/
theorem lsLemma20_6ProjectorComplement_deltab_sub_deltaA_y
    {m n : ℕ} (p : Fin m → ℝ)
    (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ)
    (y : Fin n → ℝ) :
    matMulVec m (lsLemma20_6ProjectorComplement p)
        (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i) =
      fun i : Fin m =>
        matMulVec m (lsLemma20_6ProjectorComplement p) Deltab i -
          rectMatMulVec
            (matMulRectLeft (lsLemma20_6ProjectorComplement p) DeltaA) y i := by
  let Q : Fin m → Fin m → ℝ := lsLemma20_6ProjectorComplement p
  ext i
  have hmul := congrFun (rectMatMulVec_matMulRectLeft Q DeltaA y) i
  calc
    matMulVec m (lsLemma20_6ProjectorComplement p)
        (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i) i =
        (∑ j : Fin m, Q i j * (Deltab j - rectMatMulVec DeltaA y j)) := by
          simp [Q, matMulVec]
    _ = (∑ j : Fin m, Q i j * Deltab j) -
          (∑ j : Fin m, Q i j * rectMatMulVec DeltaA y j) := by
          rw [← Finset.sum_sub_distrib]
          apply Finset.sum_congr rfl
          intro j _
          ring
    _ = matMulVec m Q Deltab i -
          rectMatMulVec (matMulRectLeft Q DeltaA) y i := by
          rw [hmul]
          rfl
    _ = matMulVec m (lsLemma20_6ProjectorComplement p) Deltab i -
          rectMatMulVec
            (matMulRectLeft (lsLemma20_6ProjectorComplement p) DeltaA) y i := by
          rfl
/-- Projected weighted perturbation-residual bound for the nonzero-`p` WKS
    lower-bound route.  After projecting both perturbation blocks by
    `I - p p^+`, the Cauchy--Schwarz witness `[theta y; -1]` gives the sharp
    squared estimate needed to couple the right projector term with the
    Frobenius slack left by the `Delta A^T p` block. -/
theorem lsNormwiseBackwardErrorProjectedPerturbationResidual_weighted_vecNorm2Sq_le
    {m n : ℕ} (theta : ℝ) (p : Fin m → ℝ)
    (hpsq : vecNorm2Sq p ≠ 0)
    (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ)
    (y : Fin n → ℝ) :
    theta ^ 2 *
        vecNorm2Sq
          (matMulVec m (lsLemma20_6ProjectorComplement p)
            (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i)) ≤
      (1 + theta ^ 2 * vecNorm2Sq y) *
        (frobNormSqRect
            (matMulRectLeft (lsLemma20_6ProjectorComplement p) DeltaA) +
          theta ^ 2 * vecNorm2Sq Deltab) := by
  let Q : Fin m → Fin m → ℝ := lsLemma20_6ProjectorComplement p
  let DeltaAQ : Fin m → Fin n → ℝ := matMulRectLeft Q DeltaA
  let DeltabQ : Fin m → ℝ := matMulVec m Q Deltab
  let qQ : Fin m → ℝ :=
    matMulVec m Q (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i)
  let w : Fin (n + 1) → ℝ :=
    Fin.append (fun j : Fin n => theta * y j) (fun _ : Fin 1 => -1)
  let W : Fin m → Fin (n + 1) → ℝ :=
    lsNormwiseBackwardErrorWeightedMatrix theta DeltaAQ DeltabQ
  have hres :
      qQ = fun i : Fin m => DeltabQ i - rectMatMulVec DeltaAQ y i := by
    simpa [Q, DeltaAQ, DeltabQ, qQ] using
      lsLemma20_6ProjectorComplement_deltab_sub_deltaA_y
        p DeltaA Deltab y
  have hneg :
      (fun i : Fin m => rectMatMulVec DeltaAQ y i - DeltabQ i) =
        fun i : Fin m => -qQ i := by
    ext i
    rw [hres]
    ring
  have hWw :
      rectMatMulVec W w =
        fun i : Fin m => theta * (rectMatMulVec DeltaAQ y i - DeltabQ i) := by
    simpa [W, w] using
      lsNormwiseBackwardErrorWeightedMatrix_mulVec_phi_witness
        theta DeltaAQ DeltabQ y
  have hleft :
      vecNorm2Sq (rectMatMulVec W w) = theta ^ 2 * vecNorm2Sq qQ := by
    calc
      vecNorm2Sq (rectMatMulVec W w) =
          vecNorm2Sq
            (fun i : Fin m =>
              theta * (rectMatMulVec DeltaAQ y i - DeltabQ i)) := by
            rw [hWw]
      _ = theta ^ 2 *
            vecNorm2Sq
              (fun i : Fin m => rectMatMulVec DeltaAQ y i - DeltabQ i) := by
            simpa using
              (vecNorm2Sq_smul theta
                (fun i : Fin m => rectMatMulVec DeltaAQ y i - DeltabQ i))
      _ = theta ^ 2 * vecNorm2Sq qQ := by
            have hnorm :
                vecNorm2Sq
                    (fun i : Fin m => rectMatMulVec DeltaAQ y i - DeltabQ i) =
                  vecNorm2Sq qQ := by
              rw [hneg]
              simpa using (vecNorm2Sq_smul (-1 : ℝ) qQ)
            rw [hnorm]
  have hw_sq :
      vecNorm2Sq w = 1 + theta ^ 2 * vecNorm2Sq y := by
    unfold w vecNorm2Sq
    rw [Fin.sum_univ_add]
    simp [Fin.append_left, Fin.append_right]
    rw [Finset.mul_sum]
    ring_nf
  have hcs :
      theta ^ 2 * vecNorm2Sq qQ ≤
        (frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq DeltabQ) *
          (1 + theta ^ 2 * vecNorm2Sq y) := by
    calc
      theta ^ 2 * vecNorm2Sq qQ =
          vecNorm2Sq (rectMatMulVec W w) := by
          rw [hleft]
      _ ≤ frobNormSqRect W * vecNorm2Sq w :=
          vecNorm2Sq_rectMatMulVec_le_frobNormSqRect_mul W w
      _ = (frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq DeltabQ) *
          (1 + theta ^ 2 * vecNorm2Sq y) := by
          rw [lsNormwiseBackwardErrorWeightedMatrix_frobNormSqRect, hw_sq]
  have hDeltabQ_le : vecNorm2Sq DeltabQ ≤ vecNorm2Sq Deltab := by
    simpa [Q, DeltabQ] using
      lsLemma20_6ProjectorComplement_vecNorm2Sq_le p hpsq Deltab
  have hcost_tail :
      theta ^ 2 * vecNorm2Sq DeltabQ ≤
        theta ^ 2 * vecNorm2Sq Deltab :=
    mul_le_mul_of_nonneg_left hDeltabQ_le (sq_nonneg theta)
  have hcost_le :
      frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq DeltabQ ≤
        frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq Deltab := by
    linarith
  have hden_nonneg : 0 ≤ 1 + theta ^ 2 * vecNorm2Sq y := by
    exact le_of_lt (lsNormwiseBackwardErrorMu_den_pos theta y)
  have hmul :
      (frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq DeltabQ) *
          (1 + theta ^ 2 * vecNorm2Sq y) ≤
        (frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq Deltab) *
          (1 + theta ^ 2 * vecNorm2Sq y) :=
    mul_le_mul_of_nonneg_right hcost_le hden_nonneg
  calc
    theta ^ 2 *
        vecNorm2Sq
          (matMulVec m (lsLemma20_6ProjectorComplement p)
            (fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i)) =
        theta ^ 2 * vecNorm2Sq qQ := by
          rfl
    _ ≤ (frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq DeltabQ) *
        (1 + theta ^ 2 * vecNorm2Sq y) := hcs
    _ ≤ (frobNormSqRect DeltaAQ + theta ^ 2 * vecNorm2Sq Deltab) *
        (1 + theta ^ 2 * vecNorm2Sq y) := hmul
    _ = (1 + theta ^ 2 * vecNorm2Sq y) *
        (frobNormSqRect
            (matMulRectLeft (lsLemma20_6ProjectorComplement p) DeltaA) +
          theta ^ 2 * vecNorm2Sq Deltab) := by
          simp [Q, DeltaAQ, mul_comm]
/-- Exact projector squared estimate for the nonzero-`p` WKS lower-bound
    route.  The left block `DeltaA^T p` consumes the rank-one direction in
    `DeltaA` by the Frobenius Pythagoras identity; the right block is controlled
    by the projected weighted perturbation-residual bound and the symmetric
    projector area identity. -/
theorem lsNormwiseBackwardErrorExactProjector_sq_bound
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (r p : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0)
    (hpsq : vecNorm2Sq p ≠ 0)
    (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ)
    (hp :
      p = fun i : Fin m => r i + Deltab i - rectMatMulVec DeltaA y i) :
    vecNorm2Sq
        (fun j : Fin n => -∑ i : Fin m, DeltaA i j * p i) +
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
        vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) ≤
      (lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ^ 2 *
        vecNorm2Sq p := by
  let Qp : Fin m → Fin m → ℝ := lsLemma20_6ProjectorComplement p
  let DeltaAQp : Fin m → Fin n → ℝ := matMulRectLeft Qp DeltaA
  let left : Fin n → ℝ := fun j : Fin n => -∑ i : Fin m, DeltaA i j * p i
  let trans : Fin n → ℝ := fun j : Fin n => ∑ i : Fin m, p i * DeltaA i j
  let P : ℝ := vecNorm2Sq p
  let F : ℝ := frobNormSqRect DeltaA
  let Fq : ℝ := frobNormSqRect DeltaAQp
  let B : ℝ := vecNorm2Sq Deltab
  let D : ℝ := 1 + theta ^ 2 * vecNorm2Sq y
  let C : ℝ := Fq + theta ^ 2 * B
  have hleft_neg : left = fun j : Fin n => -trans j := by
    ext j
    change -(∑ i : Fin m, DeltaA i j * p i) =
      -(∑ i : Fin m, p i * DeltaA i j)
    congr 1
    apply Finset.sum_congr rfl
    intro i _
    ring
  have hleft_sq : vecNorm2Sq left = vecNorm2Sq trans := by
    rw [hleft_neg]
    simpa using (vecNorm2Sq_smul (-1 : ℝ) trans)
  have hpyth :
      vecNorm2Sq left + P * Fq = P * F := by
    rw [hleft_sq]
    simpa [Qp, DeltaAQp, trans, P, F, Fq, mul_comm] using
      lsLemma20_6Projector_transpose_action_vecNorm2Sq_add_complement_frobNormSqRect
        p hpsq DeltaA
  have hdiff :
      (fun i : Fin m => p i - r i) =
        fun i : Fin m => Deltab i - rectMatMulVec DeltaA y i := by
    ext i
    have hpi := congrFun hp i
    linarith
  have hprojected :
      theta ^ 2 *
          vecNorm2Sq
            (matMulVec m Qp (fun i : Fin m => p i - r i)) ≤
        D * C := by
    have hbase :=
      lsNormwiseBackwardErrorProjectedPerturbationResidual_weighted_vecNorm2Sq_le
        theta p hpsq DeltaA Deltab y
    simpa [Qp, DeltaAQp, D, C, B, hdiff] using hbase
  have hDpos : 0 < D := by
    simpa [D] using lsNormwiseBackwardErrorMu_den_pos theta y
  have hP_nonneg : 0 ≤ P := by
    simpa [P] using vecNorm2Sq_nonneg p
  have hproj_div :
      theta ^ 2 *
          vecNorm2Sq (matMulVec m Qp (fun i : Fin m => p i - r i)) / D ≤
        C := by
    rw [div_le_iff₀ hDpos]
    simpa [mul_comm, mul_left_comm, mul_assoc] using hprojected
  have hphi_sq :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 =
        theta ^ 2 * vecNorm2Sq r / D := by
    rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den htheta hy r]
    rw [div_pow, mul_pow, Real.sq_sqrt (le_of_lt hDpos), ← vecNorm2_sq r]
  have harea :=
    lsLemma20_6ProjectorComplement_area_identity_sub r p hrsq hpsq
  have hright_eq :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) =
        P *
          (theta ^ 2 *
            vecNorm2Sq (matMulVec m Qp (fun i : Fin m => p i - r i)) / D) := by
    calc
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p)
          = (theta ^ 2 * vecNorm2Sq r / D) *
              vecNorm2Sq
                (matMulVec m (lsLemma20_6ProjectorComplement r) p) := by
              rw [hphi_sq]
      _ = (theta ^ 2 / D) *
            (vecNorm2Sq r *
              vecNorm2Sq
                (matMulVec m (lsLemma20_6ProjectorComplement r) p)) := by
              ring
      _ = (theta ^ 2 / D) *
            (vecNorm2Sq p *
              vecNorm2Sq (matMulVec m Qp (fun i : Fin m => p i - r i))) := by
              rw [harea]
      _ = P *
          (theta ^ 2 *
            vecNorm2Sq (matMulVec m Qp (fun i : Fin m => p i - r i)) / D) := by
          ring
  have hright_le :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) ≤
        P * C := by
    rw [hright_eq]
    exact mul_le_mul_of_nonneg_left hproj_div hP_nonneg
  calc
    vecNorm2Sq left +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p)
        ≤ vecNorm2Sq left + P * C := by
          exact add_le_add (le_refl (vecNorm2Sq left)) hright_le
    _ = vecNorm2Sq left + P * Fq + P * (theta ^ 2 * B) := by
          simp [C]
          ring
    _ = P * F + P * (theta ^ 2 * B) := by
          rw [hpyth]
    _ = P * (F + theta ^ 2 * B) := by
          ring
    _ = (lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ^ 2 *
          vecNorm2Sq p := by
          rw [lsNormwiseBackwardErrorCostF_sq]
          simp [P, F, B, mul_comm]
/-- The source projector `r r^+` from (20.21) is symmetric. -/
theorem lsResidualPseudoinverseProjector_symmetric {m : ℕ}
    (r : Fin m → ℝ) (i j : Fin m) :
    lsResidualPseudoinverseProjector r i j =
      lsResidualPseudoinverseProjector r j i := by
  simpa [lsResidualPseudoinverseProjector] using
    lsLemma20_6Projector_symmetric r i j
/-- The source complement `I - r r^+` from (20.21) is symmetric. -/
theorem lsResidualComplementProjector_symmetric {m : ℕ}
    (r : Fin m → ℝ) (i j : Fin m) :
    lsResidualComplementProjector r i j =
      lsResidualComplementProjector r j i := by
  simpa [lsResidualComplementProjector] using
    lsLemma20_6ProjectorComplement_symmetric r i j
/-- The source projector `r r^+` from (20.21) is idempotent when `r` is
    nonzero, expressed through the nonzero denominator `||r||_2^2`. -/
theorem lsResidualPseudoinverseProjector_idempotent {m : ℕ}
    (r : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) :
    matMul m (lsResidualPseudoinverseProjector r)
        (lsResidualPseudoinverseProjector r) =
      lsResidualPseudoinverseProjector r := by
  simpa [lsResidualPseudoinverseProjector] using
    lsLemma20_6Projector_idempotent r hrsq
/-- The source complement `I - r r^+` from (20.21) is idempotent when `r` is
    nonzero, expressed through the nonzero denominator `||r||_2^2`. -/
theorem lsResidualComplementProjector_idempotent {m : ℕ}
    (r : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) :
    matMul m (lsResidualComplementProjector r)
        (lsResidualComplementProjector r) =
      lsResidualComplementProjector r := by
  simpa [lsResidualComplementProjector] using
    lsLemma20_6ProjectorComplement_idempotent r hrsq
/-- The source projector `r r^+` fixes the residual vector `r`. -/
theorem lsResidualPseudoinverseProjector_mulVec_residual {m : ℕ}
    (r : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) :
    matMulVec m (lsResidualPseudoinverseProjector r) r = r := by
  ext i
  simpa [lsResidualPseudoinverseProjector, matMulVec] using
    lsLemma20_6Projector_apply_self r hrsq i
/-- The source complement `I - r r^+` annihilates the residual vector `r`. -/
theorem lsResidualComplementProjector_mulVec_residual {m : ℕ}
    (r : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) :
    matMulVec m (lsResidualComplementProjector r) r = 0 := by
  ext i
  have hrow := lsLemma20_6Projector_sub_id_apply_self r hrsq i
  have hcomp :
      ∑ j : Fin m, (idMatrix m i j - lsLemma20_6Projector r i j) * r j = 0 := by
    calc
      (∑ j : Fin m, (idMatrix m i j - lsLemma20_6Projector r i j) * r j)
          = -∑ j : Fin m, (lsLemma20_6Projector r i j - idMatrix m i j) * r j := by
            rw [← Finset.sum_neg_distrib]
            apply Finset.sum_congr rfl
            intro j _
            ring
      _ = 0 := by
            rw [hrow]
            ring
  simpa [lsResidualComplementProjector, lsLemma20_6ProjectorComplement, matMulVec]
    using hcomp
/-- Higham, 2nd ed., Chapter 20, equation (20.21): the displayed block matrix
    `[ A   phi (I - r r^+) ]` used in the alternative Walden--Karlson--Sun
    formula.  This definition records the exact block shape and projector
    algebra only; it does not assert the missing singular-value equality. -/
noncomputable def lsNormwiseBackwardErrorFormulaMatrix {m n : ℕ}
    (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) : Fin m → Fin (n + m) → ℝ :=
  fun i =>
    Fin.append (A i)
      (fun j : Fin m =>
        lsNormwiseBackwardErrorPhi theta r y *
          lsResidualComplementProjector r i j)
@[simp]
theorem lsNormwiseBackwardErrorFormulaMatrix_left {m n : ℕ}
    (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (i : Fin m) (j : Fin n) :
    lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.castAdd m j) =
      A i j := by
  simp [lsNormwiseBackwardErrorFormulaMatrix, Fin.append_left]
@[simp]
theorem lsNormwiseBackwardErrorFormulaMatrix_right {m n : ℕ}
    (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (i : Fin m) (j : Fin m) :
    lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.natAdd n j) =
      lsNormwiseBackwardErrorPhi theta r y *
        lsResidualComplementProjector r i j := by
  simp [lsNormwiseBackwardErrorFormulaMatrix, Fin.append_right]
/-- Row-Gram identity behind the equivalence between Higham's WKS minimum
    formula (20.21) and the Theorem 20.5 eigenvalue formula:
    `[A, phi(I-rr^+)] [A, phi(I-rr^+)]^T =
      (A A^T - mu rr^T / ||y||_2^2) + phi^2 I`. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_rowGram_eq_eigenMatrix_add_phi_sq_id
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (r : Fin m → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq r ≠ 0) (i k : Fin m) :
    (∑ q : Fin (n + m),
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i q *
          lsNormwiseBackwardErrorFormulaMatrix theta A r y k q) =
      lsNormwiseBackwardErrorEigenMatrix theta A r y i k +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * idMatrix m i k := by
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let P : Fin m → Fin m → ℝ := lsResidualComplementProjector r
  have hP_sym (a b : Fin m) : P a b = P b a := by
    change lsResidualComplementProjector r a b =
      lsResidualComplementProjector r b a
    exact lsResidualComplementProjector_symmetric r a b
  have hP_apply (a b : Fin m) :
      P a b = idMatrix m a b - r a * r b / vecNorm2Sq r := by
    change lsResidualComplementProjector r a b =
      idMatrix m a b - r a * r b / vecNorm2Sq r
    exact lsResidualComplementProjector_apply r a b
  have hproj :
      (∑ j : Fin m, P i j * P k j) = P i k := by
    calc
      (∑ j : Fin m, P i j * P k j)
          = ∑ j : Fin m, P i j * P j k := by
              apply Finset.sum_congr rfl
              intro j _
              rw [hP_sym k j]
      _ = P i k := by
              simpa [P, matMul] using
                congrFun
                  (congrFun
                    (lsResidualComplementProjector_idempotent r hrsq) i) k
  have hy_norm_ne : vecNorm2 y ≠ 0 :=
    ne_of_gt (vecNorm2_pos_of_ne_zero_lsq hy)
  have hy_sq_ne : vecNorm2Sq y ≠ 0 := by
    rw [← vecNorm2_sq y]
    exact pow_ne_zero 2 hy_norm_ne
  have hphi_sq :
      phi ^ 2 =
        lsNormwiseBackwardErrorMu theta y * vecNorm2Sq r / vecNorm2Sq y := by
    simpa [phi] using
      lsNormwiseBackwardErrorPhi_sq_eq_mu_mul_residual_sq_div_y_sq
        theta r y
  have hscalar :
      phi ^ 2 * (r i * r k / vecNorm2Sq r) =
        lsNormwiseBackwardErrorMu theta y * (r i * r k / vecNorm2Sq y) := by
    rw [hphi_sq]
    field_simp [hrsq, hy_sq_ne]
  calc
    (∑ q : Fin (n + m),
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i q *
          lsNormwiseBackwardErrorFormulaMatrix theta A r y k q)
        =
      (∑ j : Fin n, A i j * A k j) +
        phi ^ 2 * (∑ j : Fin m, P i j * P k j) := by
          rw [Fin.sum_univ_add]
          simp [phi, P, lsNormwiseBackwardErrorFormulaMatrix,
            Fin.append_left, Fin.append_right, Finset.mul_sum]
          ring_nf
    _ = (∑ j : Fin n, A i j * A k j) + phi ^ 2 * P i k := by
          rw [hproj]
    _ = (∑ j : Fin n, A i j * A k j) -
          lsNormwiseBackwardErrorMu theta y * (r i * r k / vecNorm2Sq y) +
        phi ^ 2 * idMatrix m i k := by
          calc
            (∑ j : Fin n, A i j * A k j) + phi ^ 2 * P i k
                =
              (∑ j : Fin n, A i j * A k j) +
                phi ^ 2 *
                  (idMatrix m i k - r i * r k / vecNorm2Sq r) := by
                  rw [hP_apply i k]
            _ =
              (∑ j : Fin n, A i j * A k j) +
                phi ^ 2 * idMatrix m i k -
                  phi ^ 2 * (r i * r k / vecNorm2Sq r) := by
                  ring
            _ =
              (∑ j : Fin n, A i j * A k j) +
                phi ^ 2 * idMatrix m i k -
                  lsNormwiseBackwardErrorMu theta y *
                    (r i * r k / vecNorm2Sq y) := by
                  rw [hscalar]
            _ =
              (∑ j : Fin n, A i j * A k j) -
                lsNormwiseBackwardErrorMu theta y * (r i * r k / vecNorm2Sq y) +
              phi ^ 2 * idMatrix m i k := by
                  ring
    _ =
      lsNormwiseBackwardErrorEigenMatrix theta A r y i k +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * idMatrix m i k := by
          simp [phi, lsNormwiseBackwardErrorEigenMatrix]
/-- Rayleigh lower bound for the WKS source block obtained from the row-Gram
    identity and `lambda_*`.  This is the next spectral bridge toward the
    equality between the (20.21) minimum RHS and the Theorem 20.5 eigenvalue
    RHS. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_transpose_vecNorm2Sq_ge_phi_sq_add_lambdaStar
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq r ≠ 0) (p : Fin (m + 1) → ℝ) :
    ((lsNormwiseBackwardErrorPhi theta r y) ^ 2 +
        lsNormwiseBackwardErrorLambdaStar theta A r y) *
        vecNorm2Sq p ≤
      vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) := by
  let B : Fin (m + 1) → Fin (n + (m + 1)) → ℝ :=
    lsNormwiseBackwardErrorFormulaMatrix theta A r y
  let M : Fin (m + 1) → Fin (m + 1) → ℝ :=
    lsNormwiseBackwardErrorEigenMatrix theta A r y
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let lambda : ℝ := lsNormwiseBackwardErrorLambdaStar theta A r y
  have hlambda :
      lambda * vecNorm2Sq p ≤ finiteQuadraticForm M p := by
    simpa [lambda, M] using
      lsNormwiseBackwardErrorLambdaStar_mul_vecNorm2Sq_le_eigenMatrix_quadraticForm
        theta A r y p
  have hrowEntry :
      (fun i k : Fin (m + 1) => ∑ q : Fin (n + (m + 1)), B i q * B k q) =
        fun i k : Fin (m + 1) => M i k + phi ^ 2 * finiteIdMatrix i k := by
    ext i k
    simpa [B, M, phi, finiteIdMatrix, idMatrix] using
      lsNormwiseBackwardErrorFormulaMatrix_rowGram_eq_eigenMatrix_add_phi_sq_id
        theta A r hy hrsq i k
  have hquad :
      finiteQuadraticForm
          (fun i k : Fin (m + 1) => ∑ q : Fin (n + (m + 1)), B i q * B k q) p =
        finiteQuadraticForm M p + phi ^ 2 * vecNorm2Sq p := by
    rw [hrowEntry, finiteQuadraticForm_add, finiteQuadraticForm_smul_finiteIdMatrix]
    simp [finiteVecNorm2Sq, vecNorm2Sq]
  have hgram :=
    finiteQuadraticForm_rowGram_transpose_eq_vecNorm2Sq_rectMatMulVec_finiteTranspose
      B p
  calc
    ((lsNormwiseBackwardErrorPhi theta r y) ^ 2 +
        lsNormwiseBackwardErrorLambdaStar theta A r y) *
        vecNorm2Sq p
        = lambda * vecNorm2Sq p + phi ^ 2 * vecNorm2Sq p := by
            simp [lambda, phi]
            ring
    _ ≤ finiteQuadraticForm M p + phi ^ 2 * vecNorm2Sq p := by
            nlinarith [hlambda]
    _ =
        finiteQuadraticForm
          (fun i k : Fin (m + 1) => ∑ q : Fin (n + (m + 1)), B i q * B k q) p := by
            rw [hquad]
    _ =
      vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) := by
            simpa [B] using hgram
/-- The appended projector block in (20.21) annihilates the residual direction:
    `[ A   phi(I - r r^+) ] [0; r] = 0` for nonzero residual `r`. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_right_residual_mulVec_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (hrsq : vecNorm2Sq r ≠ 0) :
    rectMatMulVec (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
        (Fin.append (0 : Fin n → ℝ) r) =
      0 := by
  ext i
  unfold rectMatMulVec
  rw [Fin.sum_univ_add]
  have hcomp := congrFun (lsResidualComplementProjector_mulVec_residual r hrsq) i
  have hcomp_sum :
      ∑ j : Fin m, lsResidualComplementProjector r i j * r j = 0 := by
    simpa [matMulVec] using hcomp
  calc
    (∑ j : Fin n,
          lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.castAdd m j) *
            Fin.append (0 : Fin n → ℝ) r (Fin.castAdd m j)) +
        ∑ j : Fin m,
          lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.natAdd n j) *
            Fin.append (0 : Fin n → ℝ) r (Fin.natAdd n j)
        =
        0 +
          ∑ j : Fin m,
            (lsNormwiseBackwardErrorPhi theta r y *
                lsResidualComplementProjector r i j) * r j := by
          simp [Fin.append_left, Fin.append_right]
    _ =
        lsNormwiseBackwardErrorPhi theta r y *
          ∑ j : Fin m, lsResidualComplementProjector r i j * r j := by
          rw [Finset.mul_sum]
          ring_nf
    _ = 0 := by
          rw [hcomp_sum]
          ring
/-- If the residual direction is orthogonal to each data column, then the
    `A`-panel of the source block `[A  phi(I-r r^+)]` has zero transposed
    action on that residual direction. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_left_residual_col_sum_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ)
    (horth : ∀ j : Fin n, ∑ i : Fin m, A i j * r i = 0)
    (j : Fin n) :
    ∑ i : Fin m,
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.castAdd m j) * r i =
      0 := by
  calc
    (∑ i : Fin m,
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.castAdd m j) * r i)
        = ∑ i : Fin m, A i j * r i := by
          apply Finset.sum_congr rfl
          intro i _
          simp [lsNormwiseBackwardErrorFormulaMatrix, Fin.append_left]
    _ = 0 := horth j
/-- The projector panel of `[A  phi(I-r r^+)]` has zero transposed action on
    the residual direction `r` when that direction is nonzero. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_right_residual_col_sum_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (hrsq : vecNorm2Sq r ≠ 0) (j : Fin m) :
    ∑ i : Fin m,
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.natAdd n j) * r i =
      0 := by
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  have hrow := congrFun (lsResidualComplementProjector_mulVec_residual r hrsq) j
  have hproj : ∑ i : Fin m, lsResidualComplementProjector r i j * r i = 0 := by
    calc
      (∑ i : Fin m, lsResidualComplementProjector r i j * r i)
          = ∑ i : Fin m, lsResidualComplementProjector r j i * r i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [lsResidualComplementProjector_symmetric]
      _ = 0 := by
            simpa [matMulVec] using hrow
  calc
    (∑ i : Fin m,
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i (Fin.natAdd n j) * r i)
        = ∑ i : Fin m, (phi * lsResidualComplementProjector r i j) * r i := by
          apply Finset.sum_congr rfl
          intro i _
          simp [phi, lsNormwiseBackwardErrorFormulaMatrix, Fin.append_right]
    _ = phi * ∑ i : Fin m, lsResidualComplementProjector r i j * r i := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = 0 := by
          rw [hproj]
          ring
/-- Left-null certificate for the source block in Higham, 2nd ed., Chapter 20,
    equation (20.21): if the supplied residual is orthogonal to the columns of
    `A` and is nonzero, then `r^T [A  phi(I-r r^+)] = 0`.  This is a source
    block dependency for the nonzero-residual WKS branch, not the missing
    singular-value minimization theorem. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_residual_leftNull_of_column_orthogonal
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ)
    (horth : ∀ j : Fin n, ∑ i : Fin m, A i j * r i = 0)
    (hrsq : vecNorm2Sq r ≠ 0) :
    ∀ k : Fin (n + m),
      ∑ i : Fin m, lsNormwiseBackwardErrorFormulaMatrix theta A r y i k * r i = 0 := by
  intro k
  refine Fin.addCases
    (motive := fun k : Fin (n + m) =>
      ∑ i : Fin m, lsNormwiseBackwardErrorFormulaMatrix theta A r y i k * r i = 0)
    ?left ?right k
  · intro j
    exact
      lsNormwiseBackwardErrorFormulaMatrix_left_residual_col_sum_eq_zero
        theta A r y horth j
  · intro j
    exact
      lsNormwiseBackwardErrorFormulaMatrix_right_residual_col_sum_eq_zero
        theta A r y hrsq j
/-- Matrix-vector action of the source block
    `[ A   phi(I - r r^+) ]` from equation (20.21), split across the two
    coordinate blocks of the input vector. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_mulVec {m n : ℕ}
    (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (z : Fin (n + m) → ℝ) :
    rectMatMulVec (lsNormwiseBackwardErrorFormulaMatrix theta A r y) z =
      fun i : Fin m =>
        rectMatMulVec A (fun j : Fin n => z (Fin.castAdd m j)) i +
          lsNormwiseBackwardErrorPhi theta r y *
            matMulVec m (lsResidualComplementProjector r)
              (fun j : Fin m => z (Fin.natAdd n j)) i := by
  ext i
  unfold rectMatMulVec matMulVec
  rw [Fin.sum_univ_add]
  have hright :
      (∑ j : Fin m,
          (lsNormwiseBackwardErrorPhi theta r y *
              lsResidualComplementProjector r i j) *
            z (Fin.natAdd n j)) =
        lsNormwiseBackwardErrorPhi theta r y *
          ∑ j : Fin m,
            lsResidualComplementProjector r i j * z (Fin.natAdd n j) := by
    rw [Finset.mul_sum]
    apply Finset.sum_congr rfl
    intro j _
    ring
  simp [lsNormwiseBackwardErrorFormulaMatrix, Fin.append_left,
    Fin.append_right, hright]
/-- The source complement `I - r r^+` from (20.21) has rectangular
    operator-2 norm at most one when the residual direction is nonzero. -/
theorem lsResidualComplementProjector_rectOpNorm2Le_one {m : ℕ}
    (r : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) :
    rectOpNorm2Le (lsResidualComplementProjector r) 1 := by
  intro x
  have hsq :
      vecNorm2Sq (rectMatMulVec (lsResidualComplementProjector r) x) ≤
        vecNorm2Sq x := by
    simpa [rectMatMulVec, matMulVec, lsResidualComplementProjector] using
    lsLemma20_6ProjectorComplement_vecNorm2Sq_le r hrsq x
  simpa [vecNorm2] using Real.sqrt_le_sqrt hsq
/-- Conservative operator-2 upper bound for the source block
    `[ A   phi(I - r r^+) ]` in (20.21).  This is a preparatory spectral
    bound, not the missing singular-value minimum formula. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_rectOpNorm2Le {m n : ℕ}
    (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) {cA : ℝ} (hcA : 0 ≤ cA)
    (hA : rectOpNorm2Le A cA) (hrsq : vecNorm2Sq r ≠ 0) :
    rectOpNorm2Le (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
      (cA + lsNormwiseBackwardErrorPhi theta r y) := by
  intro z
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let x : Fin n → ℝ := fun j => z (Fin.castAdd m j)
  let w : Fin m → ℝ := fun j => z (Fin.natAdd n j)
  have hphi : 0 ≤ phi := by
    simpa [phi] using lsNormwiseBackwardErrorPhi_nonneg theta r y
  have hC : rectOpNorm2Le (lsResidualComplementProjector r) 1 :=
    lsResidualComplementProjector_rectOpNorm2Le_one r hrsq
  have hleft :
      vecNorm2 (rectMatMulVec A x) ≤ cA * vecNorm2 z := by
    calc
      vecNorm2 (rectMatMulVec A x) ≤ cA * vecNorm2 x := hA x
      _ ≤ cA * vecNorm2 z :=
            mul_le_mul_of_nonneg_left
              (by simpa [x] using lsVecNorm2_left_le_of_sum_coords z) hcA
  have hCw :
      vecNorm2 (matMulVec m (lsResidualComplementProjector r) w) ≤
        vecNorm2 w := by
    simpa [rectMatMulVec, matMulVec] using hC w
  have hright :
      vecNorm2
          (fun i : Fin m =>
            phi * matMulVec m (lsResidualComplementProjector r) w i) ≤
        phi * vecNorm2 z := by
    calc
      vecNorm2
          (fun i : Fin m =>
            phi * matMulVec m (lsResidualComplementProjector r) w i)
          = phi * vecNorm2
              (matMulVec m (lsResidualComplementProjector r) w) := by
              rw [vecNorm2_smul, abs_of_nonneg hphi]
      _ ≤ phi * vecNorm2 w := mul_le_mul_of_nonneg_left hCw hphi
      _ ≤ phi * vecNorm2 z :=
            mul_le_mul_of_nonneg_left
              (by simpa [w] using lsVecNorm2_right_le_of_sum_coords z) hphi
  rw [lsNormwiseBackwardErrorFormulaMatrix_mulVec]
  change
    vecNorm2
        (fun i : Fin m =>
          rectMatMulVec A x i +
            phi * matMulVec m (lsResidualComplementProjector r) w i) ≤
      (cA + phi) * vecNorm2 z
  calc
    vecNorm2
        (fun i : Fin m =>
          rectMatMulVec A x i +
            phi * matMulVec m (lsResidualComplementProjector r) w i)
        ≤ vecNorm2 (rectMatMulVec A x) +
            vecNorm2
              (fun i : Fin m =>
                phi * matMulVec m (lsResidualComplementProjector r) w i) :=
          vecNorm2_add_le _ _
    _ ≤ cA * vecNorm2 z + phi * vecNorm2 z :=
          add_le_add hleft hright
    _ = (cA + phi) * vecNorm2 z := by ring
private theorem realVecToEuclidean_ne_zero_of_vecNorm2Sq_ne_zero {m : ℕ}
    {v : Fin m → ℝ} (hv : vecNorm2Sq v ≠ 0) :
    realVecToEuclidean v ≠ 0 := by
  intro hvzero
  have hnorm : vecNorm2 v = 0 := by
    have hnormE : ‖realVecToEuclidean v‖ = 0 := by
      simp [hvzero]
    simpa [realVecToEuclidean_norm] using hnormE
  have hsq : vecNorm2Sq v = 0 := by
    rw [← vecNorm2_sq, hnorm]
    norm_num
  exact hv hsq
private theorem complexMatrixRank_ne_card_of_euclideanLin_ker_nonzero
    {m n : ℕ} (A : CMatrix m n) {x : EuclideanSpace ℂ (Fin n)}
    (hxker : complexMatrixEuclideanLin A x = 0) (hxne : x ≠ 0) :
    complexMatrixRank A ≠ n := by
  intro hrank
  let T : EuclideanSpace ℂ (Fin n) →ₗ[ℂ] EuclideanSpace ℂ (Fin m) :=
    complexMatrixEuclideanLin A
  have hrange :
      Module.finrank ℂ (LinearMap.range T) = n := by
    simpa [T, complexMatrixRank_eq_finrank_range_euclideanLin A] using hrank
  have hdomain :
      Module.finrank ℂ (EuclideanSpace ℂ (Fin n)) = n :=
    finrank_euclideanSpace_fin (𝕜 := ℂ) (n := n)
  have hsum :
      Module.finrank ℂ (LinearMap.range T) +
          Module.finrank ℂ (LinearMap.ker T) =
        Module.finrank ℂ (EuclideanSpace ℂ (Fin n)) :=
    LinearMap.finrank_range_add_finrank_ker T
  have hker_finrank :
      Module.finrank ℂ (LinearMap.ker T) = 0 := by
    omega
  have hker_bot : LinearMap.ker T = ⊥ := by
    exact (Submodule.finrank_eq_zero).mp hker_finrank
  have hxmem : x ∈ LinearMap.ker T := by
    simpa [T, LinearMap.mem_ker] using hxker
  have hxzero : x = 0 := by
    have hxbot : x ∈ (⊥ : Submodule ℂ (EuclideanSpace ℂ (Fin n))) := by
      simpa [hker_bot] using hxmem
    simpa using hxbot
  exact hxne hxzero
private theorem complexMatrixRank_eq_card_of_euclideanLin_ker_eq_bot
    {m n : ℕ} (A : CMatrix m n)
    (hker : LinearMap.ker (complexMatrixEuclideanLin A) = ⊥) :
    complexMatrixRank A = n := by
  let T : EuclideanSpace ℂ (Fin n) →ₗ[ℂ] EuclideanSpace ℂ (Fin m) :=
    complexMatrixEuclideanLin A
  have hsum :
      Module.finrank ℂ (LinearMap.range T) +
          Module.finrank ℂ (LinearMap.ker T) =
        Module.finrank ℂ (EuclideanSpace ℂ (Fin n)) :=
    LinearMap.finrank_range_add_finrank_ker T
  have hker0 : Module.finrank ℂ (LinearMap.ker T) = 0 := by
    change Module.finrank ℂ (LinearMap.ker (complexMatrixEuclideanLin A)) = 0
    simp [hker]
  have hdomain : Module.finrank ℂ (EuclideanSpace ℂ (Fin n)) = n :=
    finrank_euclideanSpace_fin (𝕜 := ℂ) (n := n)
  have hrange : Module.finrank ℂ (LinearMap.range T) = n := by
    omega
  simpa [T, complexMatrixRank_eq_finrank_range_euclideanLin A] using hrange
private theorem complexMatrixEuclideanLin_ker_eq_bot_of_rank_eq_card
    {m n : ℕ} (A : CMatrix m n)
    (hrank : complexMatrixRank A = n) :
    LinearMap.ker (complexMatrixEuclideanLin A) = ⊥ := by
  rw [LinearMap.ker_eq_bot']
  intro z hz
  by_contra hz_ne
  exact complexMatrixRank_ne_card_of_euclideanLin_ker_nonzero A hz hz_ne hrank
/-- Full source column rank of the real rectangular least-squares matrix makes
    the real column-map kernel trivial. -/
theorem lsRealRectColRank_rectMatMulVec_eq_zero_of_colRank_eq_card {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (hrank : lsRealRectColRank A = n)
    {x : Fin n → ℝ} (hx : rectMatMulVec A x = 0) :
    x = 0 := by
  have hker_bot :
      LinearMap.ker (complexMatrixEuclideanLin (realRectToCMatrix A)) = ⊥ :=
    complexMatrixEuclideanLin_ker_eq_bot_of_rank_eq_card
      (realRectToCMatrix A) (by simpa [lsRealRectColRank] using hrank)
  have hxker :
      complexMatrixEuclideanLin (realRectToCMatrix A) (realVecToEuclidean x) = 0 := by
    apply norm_eq_zero.mp
    rw [realRectToCMatrix_euclideanLin_realVecToEuclidean_norm, hx]
    exact vecNorm2_zero
  have hxmem :
      realVecToEuclidean x ∈
        LinearMap.ker (complexMatrixEuclideanLin (realRectToCMatrix A)) := by
    simpa [LinearMap.mem_ker] using hxker
  have hxE_zero : realVecToEuclidean x = 0 := by
    have hxbot :
        realVecToEuclidean x ∈
          (⊥ : Submodule ℂ (EuclideanSpace ℂ (Fin n))) := by
      simpa [hker_bot] using hxmem
    simpa using hxbot
  have hre := congrArg euclideanReVec hxE_zero
  simpa using hre
/-- Full source column rank of the real rectangular least-squares matrix makes
    the real matrix-vector action injective. -/
theorem lsRealRectColRank_rectMatMulVec_injective_of_colRank_eq_card {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (hrank : lsRealRectColRank A = n) :
    Function.Injective (rectMatMulVec A) := by
  intro x y hxy
  have hdiff : rectMatMulVec A (fun j : Fin n => x j - y j) = 0 := by
    rw [rectMatMulVec_sub A x y]
    ext i
    exact sub_eq_zero.mpr (congrFun hxy i)
  have hzero :=
    lsRealRectColRank_rectMatMulVec_eq_zero_of_colRank_eq_card
      A hrank hdiff
  ext j
  exact sub_eq_zero.mp (congrFun hzero j)
/-- Higham, 2nd ed., Chapter 20, equations (20.18)-(20.19):
    source-rank version of the real right-Gram branch handoff.  Full source
    column rank supplies the real column-map injectivity used by the constructed
    right-Gram singular branches and left-nullspace tail. -/
theorem
    exists_lsScaledAugmentedMatrix_kappa2_bounds_of_rightGram_basis_of_colRank_eq_card
    {m n : ℕ} [Nonempty (Fin n)] (hmn : n ≤ m)
    {alpha : ℝ} {A : Fin m → Fin n → ℝ}
    (hrank : lsRealRectColRank A = n)
    (halpha :
      alpha = lsScaledAugmentedBranchSigmaMin
        (rectRightGramBasisSingularValue A) / Real.sqrt 2) :
    ∃ w : Fin (m - n) → Fin m → ℝ,
      (∀ k : Fin (m - n), vecNorm2Sq (w k) = 1) ∧
        (∀ k l : Fin (m - n),
          k ≠ l → (∑ r : Fin m, w k r * w l r) = 0) ∧
        (∀ k : Fin (m - n), ∀ j : Fin n,
          ∑ r : Fin m, A r j * w k r = 0) ∧
        Real.sqrt 2 *
              (lsScaledAugmentedBranchSigmaMax (rectRightGramBasisSingularValue A) /
                lsScaledAugmentedBranchSigmaMin (rectRightGramBasisSingularValue A)) ≤
            kappa2 (lsScaledAugmentedMatrix alpha A)
              (lsScaledAugmentedSourceBranchInverseCandidate hmn alpha
                (rectRightGramBasisSingularValue A)
                (fun a r => rectRightGramLeftSingularFromEigenbasis A r a)
                (fun a j => rectRightGramEigenbasis A j a) w) ∧
          kappa2 (lsScaledAugmentedMatrix alpha A)
              (lsScaledAugmentedSourceBranchInverseCandidate hmn alpha
                (rectRightGramBasisSingularValue A)
                (fun a r => rectRightGramLeftSingularFromEigenbasis A r a)
                (fun a j => rectRightGramEigenbasis A j a) w) ≤
            2 *
              (lsScaledAugmentedBranchSigmaMax (rectRightGramBasisSingularValue A) /
                lsScaledAugmentedBranchSigmaMin (rectRightGramBasisSingularValue A)) := by
  exact
    exists_lsScaledAugmentedMatrix_kappa2_bounds_of_rightGram_basis_of_rectMatMulVec_injective
      (m := m) (n := n) hmn
      (A := A) (alpha := alpha)
      (lsRealRectColRank_rectMatMulVec_injective_of_colRank_eq_card A hrank)
      halpha
/-- Higham, 2nd ed., Chapter 20, equations (20.18)-(20.19):
    source-rank right-Gram handoff with the bounds and scaling written using the
    ordered real right-Gram singular values.  The inverse candidate is still the
    constructed basis-indexed right-Gram candidate; the finite extrema are
    permutation-invariant under the basis-to-ordered reindexing. -/
theorem
    exists_lsScaledAugmentedMatrix_kappa2_bounds_of_rectSingularValue_rightGram_basis_of_colRank_eq_card
    {m n : ℕ} [Nonempty (Fin n)] (hmn : n ≤ m)
    {alpha : ℝ} {A : Fin m → Fin n → ℝ}
    (hrank : lsRealRectColRank A = n)
    (halpha :
      alpha = lsScaledAugmentedBranchSigmaMin (rectSingularValue A) /
        Real.sqrt 2) :
    ∃ w : Fin (m - n) → Fin m → ℝ,
      (∀ k : Fin (m - n), vecNorm2Sq (w k) = 1) ∧
        (∀ k l : Fin (m - n),
          k ≠ l → (∑ r : Fin m, w k r * w l r) = 0) ∧
        (∀ k : Fin (m - n), ∀ j : Fin n,
          ∑ r : Fin m, A r j * w k r = 0) ∧
        Real.sqrt 2 *
              (lsScaledAugmentedBranchSigmaMax (rectSingularValue A) /
                lsScaledAugmentedBranchSigmaMin (rectSingularValue A)) ≤
            kappa2 (lsScaledAugmentedMatrix alpha A)
              (lsScaledAugmentedSourceBranchInverseCandidate hmn alpha
                (rectRightGramBasisSingularValue A)
                (fun a r => rectRightGramLeftSingularFromEigenbasis A r a)
                (fun a j => rectRightGramEigenbasis A j a) w) ∧
          kappa2 (lsScaledAugmentedMatrix alpha A)
              (lsScaledAugmentedSourceBranchInverseCandidate hmn alpha
                (rectRightGramBasisSingularValue A)
                (fun a r => rectRightGramLeftSingularFromEigenbasis A r a)
                (fun a j => rectRightGramEigenbasis A j a) w) ≤
            2 *
              (lsScaledAugmentedBranchSigmaMax (rectSingularValue A) /
                lsScaledAugmentedBranchSigmaMin (rectSingularValue A)) := by
  have hmin :=
    lsScaledAugmentedBranchSigmaMin_rectRightGramBasis_eq_rectSingularValue A
  have hmax :=
    lsScaledAugmentedBranchSigmaMax_rectRightGramBasis_eq_rectSingularValue A
  have halpha_basis :
      alpha = lsScaledAugmentedBranchSigmaMin
          (rectRightGramBasisSingularValue A) / Real.sqrt 2 := by
    simpa [hmin] using halpha
  obtain ⟨w, hw, hnull, hATw, hbounds⟩ :=
    exists_lsScaledAugmentedMatrix_kappa2_bounds_of_rightGram_basis_of_colRank_eq_card
      (m := m) (n := n) hmn
      (A := A) (alpha := alpha) hrank halpha_basis
  refine ⟨w, hw, hnull, hATw, ?_⟩
  simpa [hmin, hmax] using hbounds
/-- A nonzero vector annihilating every column of a real rectangular matrix
    rules out full row rank of the row-side complexified transpose. -/
theorem lsRealRectRowRank_ne_card_of_leftNull {m n : ℕ}
    (A : Fin (m + 1) → Fin n → ℝ) (v : Fin (m + 1) → ℝ)
    (hv : vecNorm2Sq v ≠ 0)
    (hleft : ∀ k : Fin n, ∑ i : Fin (m + 1), A i k * v i = 0) :
    lsRealRectRowRank A ≠ m + 1 := by
  have hTzero : rectMatMulVec (finiteTranspose A) v = 0 := by
    ext k
    simpa [rectMatMulVec, finiteTranspose] using hleft k
  have hker :
      complexMatrixEuclideanLin (realRectToCMatrix (finiteTranspose A))
          (realVecToEuclidean v) = 0 := by
    apply norm_eq_zero.mp
    rw [realRectToCMatrix_euclideanLin_realVecToEuclidean_norm, hTzero]
    exact vecNorm2_zero
  have hvne : realVecToEuclidean v ≠ 0 :=
    realVecToEuclidean_ne_zero_of_vecNorm2Sq_ne_zero hv
  simpa [lsRealRectRowRank] using
    complexMatrixRank_ne_card_of_euclideanLin_ker_nonzero
      (realRectToCMatrix (finiteTranspose A)) hker hvne
/-- Full row rank eliminates every real left-null vector.  This is the converse
    kernel-trivial form paired with `lsRealRectRowRank_ne_card_of_leftNull`. -/
theorem lsRealRectRowRank_leftNull_eq_zero_of_rowRank_eq_card {m n : ℕ}
    (A : Fin (m + 1) → Fin n → ℝ)
    (hrank : lsRealRectRowRank A = m + 1)
    {v : Fin (m + 1) → ℝ}
    (hleft : ∀ k : Fin n, ∑ i : Fin (m + 1), A i k * v i = 0) :
    v = 0 := by
  by_contra hvne
  have hv : vecNorm2Sq v ≠ 0 :=
    ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq hvne)
  exact (lsRealRectRowRank_ne_card_of_leftNull A v hv hleft) hrank
/-- A nonzero left-null certificate forces the row-side `sigma_min` of a real
    rectangular matrix to vanish. -/
theorem lsRealRectSigmaMinRow_eq_zero_of_leftNull {m n : ℕ}
    (A : Fin (m + 1) → Fin n → ℝ) (v : Fin (m + 1) → ℝ)
    (hv : vecNorm2Sq v ≠ 0)
    (hleft : ∀ k : Fin n, ∑ i : Fin (m + 1), A i k * v i = 0) :
    lsRealRectSigmaMinRow A = 0 := by
  by_contra hne
  have hpos : 0 < lsRealRectSigmaMinRow A :=
    lt_of_le_of_ne' (lsRealRectSigmaMinRow_nonneg A) hne
  have hrank : lsRealRectRowRank A = m + 1 :=
    (lsRealRectSigmaMinRow_pos_iff_rowRank_eq_card A).mp hpos
  exact (lsRealRectRowRank_ne_card_of_leftNull A v hv hleft) hrank
/-- Row-side singular values of the source block
    `[ A   phi(I - r r^+) ]` from equation (20.21). -/
noncomputable def lsNormwiseBackwardErrorFormulaMatrixRowSingularValue
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (i : Fin m) : ℝ :=
  lsRealRectRowSingularValue
    (lsNormwiseBackwardErrorFormulaMatrix theta A r y) i
theorem lsNormwiseBackwardErrorFormulaMatrixRowSingularValue_nonneg
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) (i : Fin m) :
    0 ≤
      lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowSingularValue] using
    lsRealRectRowSingularValue_nonneg
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) i
/-- Row-side `sigma_min` for the nonempty-row source block in equation (20.21).
    This is a local singular-value API layer only; it does not assert the
    Walden--Karlson--Sun minimization formula. -/
noncomputable def lsNormwiseBackwardErrorFormulaMatrixSigmaMin
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) : ℝ :=
  lsRealRectSigmaMinRow
    (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
/-- Source-block row-side singular-value lower bound for (20.21). -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_mul_vecNorm2_le_transpose_mulVec
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p ≤
      vecNorm2
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectSigmaMinRow_mul_vecNorm2_le_transpose_mulVec
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) p
/-- The row-side `sigma_min` of the WKS source block
    `[A phi(I-r r^+)]` is attained by a nonzero real vector in the exact
    squared transpose-action form required by the constructive upper route. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_exists_transpose_attaining_vector_sq
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    ∃ p : Fin (m + 1) → ℝ, p ≠ 0 ∧
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) =
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectSigmaMinRow_exists_transpose_attaining_vector_sq
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
/-- Squared transpose-action expansion for the WKS source block
    `[A  phi(I-r r^+)]`.  This is the algebraic form needed to compare a
    candidate left singular vector with the rank-one source-residual witness
    cost: the two coordinate blocks of the transpose action contribute
    independently to the squared norm. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_transpose_vecNorm2Sq_eq
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (r : Fin m → ℝ) (y : Fin n → ℝ) (p : Fin m → ℝ) :
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) =
      vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec m (lsResidualComplementProjector r) p) := by
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let left : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let right : Fin m → ℝ :=
    fun k => phi * ∑ i : Fin m, lsResidualComplementProjector r i k * p i
  have haction :
      rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p =
        Fin.append left right := by
    ext q
    refine Fin.addCases
      (motive := fun q : Fin (n + m) =>
        rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p q =
          Fin.append left right q)
      ?left ?right q
    · intro j
      simp [left, rectMatMulVec, finiteTranspose,
        lsNormwiseBackwardErrorFormulaMatrix, Fin.append_left]
    · intro k
      calc
        rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p
            (Fin.natAdd n k)
            =
          ∑ i : Fin m,
            (phi * lsResidualComplementProjector r i k) * p i := by
            simp [phi, rectMatMulVec, finiteTranspose,
              lsNormwiseBackwardErrorFormulaMatrix, Fin.append_right]
        _ =
          phi * ∑ i : Fin m, lsResidualComplementProjector r i k * p i := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
        _ = Fin.append left right (Fin.natAdd n k) := by
            simp [right, Fin.append_right]
  have hprojected :
      (fun k : Fin m =>
        ∑ i : Fin m, lsResidualComplementProjector r i k * p i) =
        matMulVec m (lsResidualComplementProjector r) p := by
    ext k
    unfold matMulVec
    apply Finset.sum_congr rfl
    intro i _
    rw [lsResidualComplementProjector_symmetric]
  have hright_sq :
      vecNorm2Sq right =
        phi ^ 2 *
          vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) := by
    have hsmul :
        vecNorm2Sq
            (fun k : Fin m =>
              phi * matMulVec m (lsResidualComplementProjector r) p k) =
          phi ^ 2 *
            vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) := by
      simpa using
        (vecNorm2Sq_smul phi (matMulVec m (lsResidualComplementProjector r) p))
    calc
      vecNorm2Sq right =
          vecNorm2Sq
            (fun k : Fin m =>
              phi * matMulVec m (lsResidualComplementProjector r) p k) := by
            congr 1
            ext k
            exact congrArg (fun z : ℝ => phi * z) (congrFun hprojected k)
      _ =
          phi ^ 2 *
            vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) :=
            hsmul
  calc
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p)
        = vecNorm2Sq (Fin.append left right) := by
            rw [haction]
    _ = vecNorm2Sq left + vecNorm2Sq right :=
        lsVecNorm2Sq_append left right
    _ = vecNorm2Sq left +
        phi ^ 2 * vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) := by
        rw [hright_sq]
    _ =
      vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec m (lsResidualComplementProjector r) p) := by
        rfl
/-- Source-transpose certificate bridge for the scaled rank-one WKS witness.
    If a candidate vector has a source-block transpose action no larger than
    `sigma * ||p||_2`, and the scaled witness `Delta b` term is dominated by
    the source-block projector block, then the division-free squared rank-one
    witness cost is bounded by `sigma^2 ||p||_2^2`. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_sq_cost_le_of_source_transpose_sq_le
    {m n : ℕ} (theta sigma : ℝ) (A : Fin m → Fin n → ℝ)
    (r : Fin m → ℝ) (y : Fin n → ℝ) (p q : Fin m → ℝ)
    (hprojector :
      theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec m (lsResidualComplementProjector r) p))
    (hsource :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        sigma ^ 2 * vecNorm2Sq p) :
    vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) +
        theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
      sigma ^ 2 * vecNorm2Sq p := by
  let left : ℝ := vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i)
  let right : ℝ :=
    (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
      vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p)
  have hsplit :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) =
        left + right := by
    simpa [left, right] using
      lsNormwiseBackwardErrorFormulaMatrix_transpose_vecNorm2Sq_eq theta A r y p
  have hcost_to_split :
      left + theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤ left + right := by
    have h := add_le_add_left (by simpa [right] using hprojector) left
    simpa [add_comm, add_left_comm, add_assoc] using h
  have hsplit_le : left + right ≤ sigma ^ 2 * vecNorm2Sq p := by
    rw [← hsplit]
    exact hsource
  exact hcost_to_split.trans hsplit_le
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 ≤ lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectSigmaMinRow_nonneg
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_le_rowSingularValue
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) (i : Fin (m + 1)) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y ≤
      lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin,
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue] using
    lsRealRectSigmaMinRow_le_rowSingularValue
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) i
/-- Positive source-block `sigma_min` forces every row-side singular value of
    the (20.21) block to be positive. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowSingularValue_pos_of_sigmaMin_pos
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hσ : 0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y)
    (i : Fin (m + 1)) :
    0 < lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i := by
  exact lt_of_lt_of_le hσ
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_le_rowSingularValue
      theta A r y i)
/-- Source-block positivity form for the row-side `sigma_min` in (20.21). -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_forall_rowSingularValue_pos
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y ↔
      ∀ i : Fin (m + 1),
        0 < lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin,
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue] using
    lsRealRectSigmaMinRow_pos_iff_forall_rowSingularValue_pos
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
/-- Source-block zero form for the row-side `sigma_min` in (20.21). -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_iff_exists_rowSingularValue_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y = 0 ↔
      ∃ i : Fin (m + 1),
        lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i = 0 := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin,
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue] using
    lsRealRectSigmaMinRow_eq_zero_iff_exists_rowSingularValue_eq_zero
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
/-- Row rank of the source block `[A phi(I-r r^+)]` from (20.21). -/
noncomputable def lsNormwiseBackwardErrorFormulaMatrixRowRank
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) : ℕ :=
  lsRealRectRowRank
    (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
/-- The source-block row rank is the number of nonzero row-side singular
    values. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_nonzero_rowSingularValue
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y =
      Fintype.card {i : Fin m //
        lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i ≠ 0} := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank,
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue] using
    lsRealRectRowRank_eq_card_nonzero_rowSingularValue
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
private theorem
    realRectToCMatrix_finiteTranspose_formulaMatrix_left_panel_kernel
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    {z : EuclideanSpace ℂ (Fin (m + 1))}
    (hz :
      complexMatrixEuclideanLin
        (realRectToCMatrix
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))) z =
        0) :
    complexMatrixEuclideanLin (realRectToCMatrix (finiteTranspose A)) z = 0 := by
  apply norm_eq_zero.mp
  have hz_norm :
      ‖complexMatrixEuclideanLin
          (realRectToCMatrix
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))) z‖ =
        0 := by
    simp [hz]
  have hz_sq :
      ‖complexMatrixEuclideanLin
          (realRectToCMatrix
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))) z‖ ^ 2 =
        0 := by
    rw [hz_norm]
    norm_num
  have hnorm_formula := realRectToCMatrix_euclideanLin_norm_sq
    (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) z
  have hsum_zero :
      vecNorm2 (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          (euclideanReVec z)) ^ 2 +
        vecNorm2 (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          (euclideanImVec z)) ^ 2 = 0 := by
    simpa [hz_sq] using hnorm_formula.symm
  have hre_zero :
      vecNorm2 (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          (euclideanReVec z)) = 0 := by
    have hre_sq_zero :
        vecNorm2 (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
            (euclideanReVec z)) ^ 2 = 0 := by
      have hnonneg_re :
          0 ≤ vecNorm2 (rectMatMulVec
              (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
              (euclideanReVec z)) ^ 2 := sq_nonneg _
      have hnonneg_im :
          0 ≤ vecNorm2 (rectMatMulVec
              (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
              (euclideanImVec z)) ^ 2 := sq_nonneg _
      nlinarith
    exact sq_eq_zero_iff.mp hre_sq_zero
  have him_zero :
      vecNorm2 (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          (euclideanImVec z)) = 0 := by
    have him_sq_zero :
        vecNorm2 (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
            (euclideanImVec z)) ^ 2 = 0 := by
      have hnonneg_re :
          0 ≤ vecNorm2 (rectMatMulVec
              (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
              (euclideanReVec z)) ^ 2 := sq_nonneg _
      have hnonneg_im :
          0 ≤ vecNorm2 (rectMatMulVec
              (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
              (euclideanImVec z)) ^ 2 := sq_nonneg _
      nlinarith
    exact sq_eq_zero_iff.mp him_sq_zero
  have hre_formula_entries :
      ∀ j : Fin (n + (m + 1)),
        rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          (euclideanReVec z) j = 0 :=
    (vecNorm2_eq_zero_iff _).mp hre_zero
  have him_formula_entries :
      ∀ j : Fin (n + (m + 1)),
        rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          (euclideanImVec z) j = 0 :=
    (vecNorm2_eq_zero_iff _).mp him_zero
  have hre_A_zero : rectMatMulVec (finiteTranspose A) (euclideanReVec z) = 0 := by
    ext j
    have h := hre_formula_entries (Fin.castAdd (m + 1) j)
    simpa [rectMatMulVec, finiteTranspose, lsNormwiseBackwardErrorFormulaMatrix,
      Fin.append_left] using h
  have him_A_zero : rectMatMulVec (finiteTranspose A) (euclideanImVec z) = 0 := by
    ext j
    have h := him_formula_entries (Fin.castAdd (m + 1) j)
    simpa [rectMatMulVec, finiteTranspose, lsNormwiseBackwardErrorFormulaMatrix,
      Fin.append_left] using h
  have hnorm_A := realRectToCMatrix_euclideanLin_norm_sq (finiteTranspose A) z
  have hA_sq :
      ‖complexMatrixEuclideanLin (realRectToCMatrix (finiteTranspose A)) z‖ ^ 2 =
        0 := by
    rw [hnorm_A, hre_A_zero, him_A_zero]
    have hzero_norm : vecNorm2 (0 : Fin n → ℝ) = 0 := by
      simpa using (vecNorm2_zero (n := n))
    rw [hzero_norm]
    norm_num
  exact sq_eq_zero_iff.mp hA_sq
/-- Full row rank of the left `A` panel forces full row rank of the source
    block `[A  phi(I-r r^+)]` in (20.21).  Thus the appended projector panel
    cannot create a row-rank deficiency when the original data matrix already
    has full row rank. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hA : lsRealRectRowRank A = m + 1) :
    lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y = m + 1 := by
  let MA := realRectToCMatrix (finiteTranspose A)
  let MF := realRectToCMatrix
    (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
  have hkerA : LinearMap.ker (complexMatrixEuclideanLin MA) = ⊥ :=
    complexMatrixEuclideanLin_ker_eq_bot_of_rank_eq_card MA
      (by simpa [MA, lsRealRectRowRank] using hA)
  have hkerF : LinearMap.ker (complexMatrixEuclideanLin MF) = ⊥ := by
    rw [LinearMap.ker_eq_bot']
    intro z hz
    have hzA : complexMatrixEuclideanLin MA z = 0 := by
      simpa [MA, MF] using
        realRectToCMatrix_finiteTranspose_formulaMatrix_left_panel_kernel
          theta A r y (z := z) (by simpa [MF] using hz)
    exact (LinearMap.ker_eq_bot'.mp hkerA) z hzA
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank, MF] using
    complexMatrixRank_eq_card_of_euclideanLin_ker_eq_bot MF hkerF
/-- Positive source-block `sigma_min` gives full row rank for the (20.21)
    formula matrix. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_sigmaMin_pos
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hσ : 0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) :
    lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y = m + 1 := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank,
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectRowRank_eq_card_of_sigmaMinRow_pos
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) hσ
/-- Full row rank of the (20.21) formula matrix makes its row-side
    `sigma_min` positive. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_of_rowRank_eq_card
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrank : lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y = m + 1) :
    0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank,
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectSigmaMinRow_pos_of_rowRank_eq_card
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) hrank
/-- Source-block row-rank form for the row-side `sigma_min` in (20.21). -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y ↔
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y = m + 1 := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank,
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectSigmaMinRow_pos_iff_rowRank_eq_card
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
/-- Row-rank-deficient form for the zero row-side `sigma_min` branch in
    (20.21). -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_iff_rowRank_ne_card
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y = 0 ↔
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y ≠ m + 1 := by
  constructor
  · intro hσ hrank
    have hσpos :
        0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y :=
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
        theta A r y).mpr hrank
    exact (ne_of_gt hσpos) hσ
  · intro hrank
    have hnotpos :
        ¬ 0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y := by
      intro hσpos
      exact hrank
        ((lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
          theta A r y).mp hσpos)
    exact le_antisymm (le_of_not_gt hnotpos)
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y)
/-- A nonzero left-null vector for the source block in (20.21) rules out full
    row rank of `[A phi(I-r r^+)]`. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowRank_ne_card_of_leftNull
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrsq : vecNorm2Sq r ≠ 0)
    (hleft : ∀ k : Fin (n + (m + 1)),
      ∑ i : Fin (m + 1),
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i k * r i = 0) :
    lsNormwiseBackwardErrorFormulaMatrixRowRank theta A r y ≠ m + 1 := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank] using
    lsRealRectRowRank_ne_card_of_leftNull
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) r hrsq hleft
/-- A nonzero left-null vector for the source block in (20.21) forces its
    row-side `sigma_min` branch to vanish. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_of_leftNull
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrsq : vecNorm2Sq r ≠ 0)
    (hleft : ∀ k : Fin (n + (m + 1)),
      ∑ i : Fin (m + 1),
        lsNormwiseBackwardErrorFormulaMatrix theta A r y i k * r i = 0) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y = 0 := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin] using
    lsRealRectSigmaMinRow_eq_zero_of_leftNull
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y) r hrsq hleft
/-- Every row-side singular value of the source block in (20.21) is bounded by
    the conservative operator-2 certificate `cA + phi`. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowSingularValue_le
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ) (r : Fin m → ℝ)
    (y : Fin n → ℝ) {cA : ℝ} (hcA : 0 ≤ cA)
    (hA : rectOpNorm2Le A cA) (hrsq : vecNorm2Sq r ≠ 0) (i : Fin m) :
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue theta A r y i ≤
      cA + lsNormwiseBackwardErrorPhi theta r y := by
  have hphi : 0 ≤ lsNormwiseBackwardErrorPhi theta r y :=
    lsNormwiseBackwardErrorPhi_nonneg theta r y
  have hblock :
      rectOpNorm2Le (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
        (cA + lsNormwiseBackwardErrorPhi theta r y) :=
    lsNormwiseBackwardErrorFormulaMatrix_rectOpNorm2Le
      theta A r y hcA hA hrsq
  exact
    lsRealRectRowSingularValue_le_of_rectOpNorm2Le
      (lsNormwiseBackwardErrorFormulaMatrix theta A r y)
      (add_nonneg hcA hphi) hblock i
/-- The local row-side `sigma_min` of the source block in (20.21) inherits the
    same conservative upper bound.  This is not the printed equality for
    `eta_F(y)`. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) {cA : ℝ} (hcA : 0 ≤ cA)
    (hA : rectOpNorm2Le A cA) (hrsq : vecNorm2Sq r ≠ 0) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y ≤
      cA + lsNormwiseBackwardErrorPhi theta r y := by
  simpa [lsNormwiseBackwardErrorFormulaMatrixSigmaMin,
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue,
    lsRealRectSigmaMinRow] using
    lsNormwiseBackwardErrorFormulaMatrixRowSingularValue_le
      theta A r y hcA hA hrsq (Fin.last m)
/-- Full right-hand side of Higham, 2nd ed., Chapter 20, equation (20.21):
    `min {phi, sigma_min [A  phi(I - r r^+)]}` for a supplied residual `r`.
    This records the printed outer minimum separately from the row-side
    `sigma_min` wrapper; it is not the proof that the expression equals
    `eta_F(y)`. -/
noncomputable def lsNormwiseBackwardErrorFormulaValue
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) : ℝ :=
  min (lsNormwiseBackwardErrorPhi theta r y)
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y)
/-- Source-data form of the (20.21) right-hand side, with Higham's residual
    convention `r = b - A y`. -/
noncomputable def lsNormwiseBackwardErrorFormulaRHS
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ) : ℝ :=
  lsNormwiseBackwardErrorFormulaValue theta A (lsResidualHigham A b y) y
theorem lsNormwiseBackwardErrorFormulaValue_nonneg
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 ≤ lsNormwiseBackwardErrorFormulaValue theta A r y := by
  rw [lsNormwiseBackwardErrorFormulaValue]
  exact le_min (lsNormwiseBackwardErrorPhi_nonneg theta r y)
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y)
theorem lsNormwiseBackwardErrorFormulaValue_le_phi
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaValue theta A r y ≤
      lsNormwiseBackwardErrorPhi theta r y := by
  rw [lsNormwiseBackwardErrorFormulaValue]
  exact min_le_left _ _
theorem lsNormwiseBackwardErrorFormulaValue_le_sigmaMin
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaValue theta A r y ≤
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y := by
  rw [lsNormwiseBackwardErrorFormulaValue]
  exact min_le_right _ _
/-- Constructive upper-bound branch for (20.21): if the printed outer minimum
    selects the scalar `phi` term, then the exact-fit perturbation witness
    proves `eta_F(y) <=` the printed right-hand side.  The complementary
    source-block `sigma_min` branch remains the open WKS construction. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_phi_le_sigmaMin
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have heta :=
    lsNormwiseBackwardErrorEtaF_le_phi_of_theta_nonneg htheta A b hy
  have hrhs :
      lsNormwiseBackwardErrorFormulaRHS theta A b y =
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y := by
    unfold lsNormwiseBackwardErrorFormulaRHS lsNormwiseBackwardErrorFormulaValue
    exact min_eq_left hbranch
  rw [hrhs]
  exact heta
theorem lsNormwiseBackwardErrorFormulaRHS_nonneg
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 ≤ lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  simpa [lsNormwiseBackwardErrorFormulaRHS] using
    lsNormwiseBackwardErrorFormulaValue_nonneg theta A
      (lsResidualHigham A b y) y
/-- Upper square comparison for the WKS row-side `sigma_min`: the eigenvector
    for `lambda_*` in the Hermitian Theorem 20.5 matrix makes the shifted
    row-Gram action exact, while the row-side singular-value lower-bound API
    gives `sigma_min^2 <= phi^2 + lambda_*`. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_sq_le_phi_sq_add_lambdaStar
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq r ≠ 0) :
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 ≤
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 +
        lsNormwiseBackwardErrorLambdaStar theta A r y := by
  let B : Fin (m + 1) → Fin (n + (m + 1)) → ℝ :=
    lsNormwiseBackwardErrorFormulaMatrix theta A r y
  let M : Fin (m + 1) → Fin (m + 1) → ℝ :=
    lsNormwiseBackwardErrorEigenMatrix theta A r y
  let hM : IsSymmetricFiniteMatrix M :=
    lsNormwiseBackwardErrorEigenMatrix_isSymmetricFiniteMatrix theta A r y
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let lambda : ℝ := lsNormwiseBackwardErrorLambdaStar theta A r y
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  let e : Fin (Fintype.card (Fin (m + 1))) ≃ Fin (m + 1) :=
    Fintype.equivOfCardEq (Fintype.card_fin _)
  let a0 : Fin (Fintype.card (Fin (m + 1))) :=
    lsNormwiseBackwardErrorLambdaStarIndex m
  let a : Fin (m + 1) := e a0
  let p : Fin (m + 1) → ℝ :=
    ⇑((IsSymmetricFiniteMatrix.to_matrix_isHermitian M hM).eigenvectorBasis a)
  have hlambda_eig : finiteHermitianEigenvalues M hM a = lambda := by
    unfold finiteHermitianEigenvalues lambda lsNormwiseBackwardErrorLambdaStar
    simp only [Matrix.IsHermitian.eigenvalues]
    congr 1
    simp [e, a0, a]
  have hpnorm_sq : vecNorm2Sq p = 1 := by
    have h := finiteVecNorm2Sq_finiteHermitianEigenvector_eq_one M hM a
    simpa [p, finiteVecNorm2Sq, vecNorm2Sq] using h
  have hpnorm : vecNorm2 p = 1 := by
    unfold vecNorm2
    rw [hpnorm_sq]
    norm_num
  have hrowEntry :
      (fun i k : Fin (m + 1) => ∑ q : Fin (n + (m + 1)), B i q * B k q) =
        fun i k : Fin (m + 1) => M i k + phi ^ 2 * finiteIdMatrix i k := by
    ext i k
    simpa [B, M, phi, finiteIdMatrix, idMatrix] using
      lsNormwiseBackwardErrorFormulaMatrix_rowGram_eq_eigenMatrix_add_phi_sq_id
        theta A r hy hrsq i k
  have hquadM : finiteQuadraticForm M p = lambda * vecNorm2Sq p := by
    have h :=
      finiteQuadraticForm_finiteHermitianEigenvector_eq_eigenvalue_mul_norm_sq
        M hM a
    simpa [p, finiteVecNorm2Sq, vecNorm2Sq, hlambda_eig] using h
  have haction_sq :
      vecNorm2Sq (rectMatMulVec (finiteTranspose B) p) = phi ^ 2 + lambda := by
    have hgram :=
      finiteQuadraticForm_rowGram_transpose_eq_vecNorm2Sq_rectMatMulVec_finiteTranspose
        B p
    calc
      vecNorm2Sq (rectMatMulVec (finiteTranspose B) p)
          =
        finiteQuadraticForm
          (fun i k : Fin (m + 1) => ∑ q : Fin (n + (m + 1)), B i q * B k q) p := by
            rw [hgram]
      _ =
        finiteQuadraticForm
          (fun i k : Fin (m + 1) => M i k + phi ^ 2 * finiteIdMatrix i k) p := by
            rw [hrowEntry]
      _ = finiteQuadraticForm M p + phi ^ 2 * vecNorm2Sq p := by
            rw [finiteQuadraticForm_add, finiteQuadraticForm_smul_finiteIdMatrix]
            simp [finiteVecNorm2Sq, vecNorm2Sq]
      _ = lambda * vecNorm2Sq p + phi ^ 2 * vecNorm2Sq p := by
            rw [hquadM]
      _ = phi ^ 2 + lambda := by
            rw [hpnorm_sq]
            ring
  have hsigma_le_norm :
      sigma ≤ vecNorm2 (rectMatMulVec (finiteTranspose B) p) := by
    have h :=
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_mul_vecNorm2_le_transpose_mulVec
        theta A r y p
    simpa [sigma, B, hpnorm] using h
  have hsigma_nonneg : 0 ≤ sigma := by
    simpa [sigma] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y
  have hsquare :
      sigma ^ 2 ≤ (vecNorm2 (rectMatMulVec (finiteTranspose B) p)) ^ 2 := by
    exact
      (sq_le_sq₀ hsigma_nonneg
        (vecNorm2_nonneg (rectMatMulVec (finiteTranspose B) p))).mpr
        hsigma_le_norm
  rw [vecNorm2_sq, haction_sq] at hsquare
  simpa [sigma, phi, lambda] using hsquare
/-- Lower square comparison for the WKS row-side `sigma_min`: the Rayleigh
    lower bound applied to an exact row-side `sigma_min` attainer gives
    `phi^2 + lambda_* <= sigma_min^2`. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_sq_ge_phi_sq_add_lambdaStar
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq r ≠ 0) :
    (lsNormwiseBackwardErrorPhi theta r y) ^ 2 +
        lsNormwiseBackwardErrorLambdaStar theta A r y ≤
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 := by
  let B : Fin (m + 1) → Fin (n + (m + 1)) → ℝ :=
    lsNormwiseBackwardErrorFormulaMatrix theta A r y
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let lambda : ℝ := lsNormwiseBackwardErrorLambdaStar theta A r y
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  rcases
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_exists_transpose_attaining_vector_sq
      theta A r y with
    ⟨p, hp_ne, hp_action⟩
  have hp_norm_ne : vecNorm2 p ≠ 0 := by
    intro hnorm
    apply hp_ne
    ext i
    exact (vecNorm2_eq_zero_iff p).mp hnorm i
  have hp_sq_pos : 0 < vecNorm2Sq p := by
    rw [← vecNorm2_sq p]
    exact sq_pos_of_ne_zero hp_norm_ne
  have hlower :=
    lsNormwiseBackwardErrorFormulaMatrix_transpose_vecNorm2Sq_ge_phi_sq_add_lambdaStar
      theta A r hy hrsq p
  have hmul : (phi ^ 2 + lambda) * vecNorm2Sq p ≤ sigma ^ 2 * vecNorm2Sq p := by
    simpa [B, phi, lambda, sigma] using
      (hlower.trans (le_of_eq hp_action))
  exact le_of_mul_le_mul_right hmul hp_sq_pos
/-- Exact square bridge between the row-side source-block singular value in
    (20.21) and the shifted least eigenvalue in Theorem 20.5. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_sq_eq_phi_sq_add_lambdaStar
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq r ≠ 0) :
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 =
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 +
        lsNormwiseBackwardErrorLambdaStar theta A r y :=
  le_antisymm
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_sq_le_phi_sq_add_lambdaStar
      theta A r hy hrsq)
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_sq_ge_phi_sq_add_lambdaStar
      theta A r hy hrsq)
/-- The (20.21) minimum RHS agrees with the Theorem 20.5 eigenvalue RHS for a
    supplied nonzero residual and finite nonzero `y`.  This closes the
    finite-`theta` WKS spectral equivalence at the formula-value layer. -/
theorem lsNormwiseBackwardErrorFormulaValue_eq_eigenvalueFormulaValue
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq r ≠ 0) :
    lsNormwiseBackwardErrorFormulaValue theta A r y =
      lsNormwiseBackwardErrorEigenvalueFormulaValue theta A r y := by
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  let lambda : ℝ := lsNormwiseBackwardErrorLambdaStar theta A r y
  have hphi_nonneg : 0 ≤ phi := by
    simpa [phi] using lsNormwiseBackwardErrorPhi_nonneg theta r y
  have hsigma_nonneg : 0 ≤ sigma := by
    simpa [sigma] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y
  have hsq : sigma ^ 2 = phi ^ 2 + lambda := by
    simpa [sigma, phi, lambda] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_sq_eq_phi_sq_add_lambdaStar
        theta A r hy hrsq
  have hphi_sq_source :
      (vecNorm2Sq r / vecNorm2Sq y) * lsNormwiseBackwardErrorMu theta y =
        phi ^ 2 := by
    have h := lsNormwiseBackwardErrorPhi_sq_eq_mu_mul_residual_sq_div_y_sq
      theta r y
    rw [h]
    ring
  by_cases hlambda_nonneg : 0 ≤ lambda
  · have hphi_sq_le_sigma_sq : phi ^ 2 ≤ sigma ^ 2 := by
      rw [hsq]
      nlinarith
    have hphi_le_sigma : phi ≤ sigma :=
      (sq_le_sq₀ hphi_nonneg hsigma_nonneg).mp hphi_sq_le_sigma_sq
    have hformula : lsNormwiseBackwardErrorFormulaValue theta A r y = phi := by
      unfold lsNormwiseBackwardErrorFormulaValue
      exact min_eq_left (by simpa [phi, sigma] using hphi_le_sigma)
    have heigen :
        lsNormwiseBackwardErrorEigenvalueFormulaValue theta A r y = phi := by
      simpa [phi, lambda] using
        lsNormwiseBackwardErrorEigenvalueFormulaValue_eq_phi_of_lambdaStar_nonneg
          theta A r y (by simpa [lambda] using hlambda_nonneg)
    rw [hformula, heigen]
  · have hlambda_neg : lambda < 0 := lt_of_not_ge hlambda_nonneg
    have hsigma_sq_le_phi_sq : sigma ^ 2 ≤ phi ^ 2 := by
      rw [hsq]
      nlinarith
    have hsigma_le_phi : sigma ≤ phi :=
      (sq_le_sq₀ hsigma_nonneg hphi_nonneg).mp hsigma_sq_le_phi_sq
    have hformula :
        lsNormwiseBackwardErrorFormulaValue theta A r y = sigma := by
      unfold lsNormwiseBackwardErrorFormulaValue
      exact min_eq_right (by simpa [phi, sigma] using hsigma_le_phi)
    have heigen :
        lsNormwiseBackwardErrorEigenvalueFormulaValue theta A r y = sigma := by
      rw [lsNormwiseBackwardErrorEigenvalueFormulaValue_eq_sqrt_of_lambdaStar_neg
        theta A r y (by simpa [lambda] using hlambda_neg)]
      have hinside :
          (vecNorm2Sq r / vecNorm2Sq y) * lsNormwiseBackwardErrorMu theta y +
              lsNormwiseBackwardErrorLambdaStar theta A r y =
            sigma ^ 2 := by
        rw [hphi_sq_source]
        simpa [phi, lambda] using hsq.symm
      rw [hinside, Real.sqrt_sq_eq_abs, abs_of_nonneg hsigma_nonneg]
    rw [hformula, heigen]
/-- Source-data version of the WKS finite-`theta` spectral equivalence between
    the (20.21) minimum RHS and the Theorem 20.5 eigenvalue RHS, under the
    nonzero residual and nonzero `y` hypotheses required by the row-Gram shift. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_eigenvalueFormulaRHS_of_residual_sq_ne_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y =
      lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  simpa [lsNormwiseBackwardErrorFormulaRHS,
    lsNormwiseBackwardErrorEigenvalueFormulaRHS] using
    lsNormwiseBackwardErrorFormulaValue_eq_eigenvalueFormulaValue
      theta A (lsResidualHigham A b y) hy hrsq
/-- Positivity characterization for the printed right-hand side in (20.21):
    the outer minimum `min {phi, sigma_min}` is positive exactly when both
    displayed nonnegative branches are positive. -/
theorem lsNormwiseBackwardErrorFormulaValue_pos_iff
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 < lsNormwiseBackwardErrorFormulaValue theta A r y ↔
      0 < lsNormwiseBackwardErrorPhi theta r y ∧
        0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y := by
  simp [lsNormwiseBackwardErrorFormulaValue]
/-- Zero characterization for the printed right-hand side in (20.21): since
    both displayed branches are nonnegative, `min {phi, sigma_min}` vanishes
    exactly when at least one branch vanishes. -/
theorem lsNormwiseBackwardErrorFormulaValue_eq_zero_iff
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (r : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaValue theta A r y = 0 ↔
      lsNormwiseBackwardErrorPhi theta r y = 0 ∨
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y = 0 := by
  constructor
  · intro hzero
    by_cases hphi : lsNormwiseBackwardErrorPhi theta r y = 0
    · exact Or.inl hphi
    · by_cases hsigma :
          lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y = 0
      · exact Or.inr hsigma
      · exfalso
        have hphi_pos : 0 < lsNormwiseBackwardErrorPhi theta r y :=
          lt_of_le_of_ne' (lsNormwiseBackwardErrorPhi_nonneg theta r y) hphi
        have hsigma_pos :
            0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y :=
          lt_of_le_of_ne'
            (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y)
            hsigma
        have hpos : 0 < lsNormwiseBackwardErrorFormulaValue theta A r y :=
          (lsNormwiseBackwardErrorFormulaValue_pos_iff theta A r y).mpr
            ⟨hphi_pos, hsigma_pos⟩
        linarith
  · intro h
    rcases h with hphi | hsigma
    · unfold lsNormwiseBackwardErrorFormulaValue
      rw [hphi]
      exact min_eq_left
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y)
    · unfold lsNormwiseBackwardErrorFormulaValue
      rw [hsigma]
      exact min_eq_right (lsNormwiseBackwardErrorPhi_nonneg theta r y)
/-- Source-data positivity characterization for the printed right-hand side
    in (20.21): after substituting Higham's residual `r = b - A y`, the outer
    `min {phi, sigma_min}` is positive exactly when both displayed branches are
    positive. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      0 < lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y ∧
        0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y := by
  simpa [lsNormwiseBackwardErrorFormulaRHS] using
    (lsNormwiseBackwardErrorFormulaValue_pos_iff
      (theta := theta) (A := A) (r := lsResidualHigham A b y) (y := y))
/-- Source-data zero characterization for the printed right-hand side in
    (20.21): after substituting Higham's residual `r = b - A y`, the outer
    `min {phi, sigma_min}` vanishes exactly when one displayed branch
    vanishes.  This is branch anatomy only; it is not the missing
    Walden--Karlson--Sun equality proof with `eta_F(y)`. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y = 0 ∨
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y = 0 := by
  simpa [lsNormwiseBackwardErrorFormulaRHS] using
    (lsNormwiseBackwardErrorFormulaValue_eq_zero_iff
      (theta := theta) (A := A) (r := lsResidualHigham A b y) (y := y))
/-- Zero-residual branch of the printed right-hand side in (20.21): if
    `r = b - A y` vanishes, then the displayed `min {phi, sigma_min}` value is
    zero.  This is a source-faithful degenerate branch, not the spectral proof
    of the general Walden--Karlson--Sun formula. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_lsResidualHigham_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hres : lsResidualHigham A b y = 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 := by
  unfold lsNormwiseBackwardErrorFormulaRHS
  exact
    (lsNormwiseBackwardErrorFormulaValue_eq_zero_iff
      theta A (lsResidualHigham A b y) y).mpr
      (Or.inl
        (lsNormwiseBackwardErrorPhi_eq_zero_of_residual_eq_zero
          theta y hres))
/-- Exact zero-residual branch of (20.20)-(20.21): when `b - A y = 0`, the
    normwise backward-error infimum and the printed formula right-hand side both
    vanish, so they agree without invoking the missing spectral lower-bound and
    attaining-perturbation theorem. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_lsResidualHigham_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hres : lsResidualHigham A b y = 0) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hmin :
      IsLeastSquaresMinimizer A b y :=
    IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y = 0 :=
    lsNormwiseBackwardErrorEtaF_eq_zero_of_isLeastSquaresMinimizer
      theta A b y hmin
  have hrhs :
      lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 :=
    lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_lsResidualHigham_eq_zero
      theta A b y hres
  rw [heta, hrhs]
/-- Exact-residual branch of the WKS lower-bound side: if a perturbation makes
    the expanded residual `p = (b-Ay) + Delta b - Delta A y` vanish, then its
    weighted cost is at least the printed right-hand side in (20.21).  This
    closes the `p = 0` branch of the lower-bound route; the nonzero branch
    still requires the row-side spectral inequality for the source block. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_costF_of_source_residual_eq_deltaA_y_sub_deltab
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hres :
      lsResidualHigham A b y =
        fun i : Fin (m + 1) => rectMatMulVec DeltaA y i - Deltab i) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  calc
    lsNormwiseBackwardErrorFormulaRHS theta A b y =
        lsNormwiseBackwardErrorFormulaValue theta A (lsResidualHigham A b y) y := by
          rfl
    _ ≤ lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y :=
        lsNormwiseBackwardErrorFormulaValue_le_phi theta A
          (lsResidualHigham A b y) y
    _ ≤ lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
        lsNormwiseBackwardErrorPhi_le_costF_of_residual_eq_deltaA_y_sub_deltab
          htheta hy (lsResidualHigham A b y) DeltaA Deltab hres
/-- Lower-bound handoff for the alternative formula (20.21): once every
    feasible perturbation in (20.20) is known to have cost at least the printed
    right-hand side, the right-hand side is below the infimum model `eta_F`.
    The spectral proof of that lower bound remains the open Walden--Karlson--Sun
    step. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_forall_feasible_cost_ge
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hlower :
      ∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
          lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorEtaF theta A b y := by
  unfold lsNormwiseBackwardErrorEtaF
  apply le_csInf (lsNormwiseBackwardErrorValuesF.nonempty theta A b y)
  intro eta heta
  rcases heta with ⟨DeltaA, Deltab, hfeas, heta_eq⟩
  rw [heta_eq]
  exact hlower DeltaA Deltab hfeas
/-- Zero-branch lower-bound side of the alternative formula (20.21): whenever
    the printed right-hand side is zero, the inequality `RHS <= eta_F(y)` is
    immediate from nonnegativity of the (20.20) infimum model. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_formulaRHS_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrhs : lsNormwiseBackwardErrorFormulaRHS theta A b y = 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorEtaF theta A b y := by
  rw [hrhs]
  exact lsNormwiseBackwardErrorEtaF_nonneg theta A b y
/-- Attainment handoff for the alternative formula (20.21): a feasible
    perturbation whose weighted cost is the printed right-hand side gives the
    reverse inequality `eta_F(y) <=` that right-hand side. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_exists_feasible_cost_eq
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hatt :
      ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
          lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
            lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  rcases hatt with ⟨DeltaA, Deltab, hfeas, hcost⟩
  calc
    lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
      lsNormwiseBackwardErrorEtaF_le_costF_of_feasible
        theta A b y DeltaA Deltab hfeas
    _ = lsNormwiseBackwardErrorFormulaRHS theta A b y := hcost
/-- Upper-bound handoff for the alternative formula (20.21): it is enough to
    exhibit a feasible perturbation whose weighted cost is bounded by the
    printed right-hand side.  This is the inequality form of the WKS
    constructive side; it avoids requiring the future source proof to package
    an exact attaining perturbation when an upper-bound construction suffices. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_exists_feasible_cost_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hupper :
      ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
          lsNormwiseBackwardErrorCostF theta DeltaA Deltab ≤
            lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  rcases hupper with ⟨DeltaA, Deltab, hfeas, hcost⟩
  exact (lsNormwiseBackwardErrorEtaF_le_costF_of_feasible
    theta A b y DeltaA Deltab hfeas).trans hcost
/-- Certificate form of the missing equality in equation (20.21).  The theorem
    isolates the two obligations left for the Walden--Karlson--Sun proof:
    all feasible perturbations must be bounded below by the printed formula
    right-hand side, and one feasible perturbation must attain that value. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formula_certificate
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hlower :
      ∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
          lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab)
    (hatt :
      ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
          lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
            lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  exact le_antisymm
    (lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_exists_feasible_cost_eq
      theta A b y hatt)
    (lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_forall_feasible_cost_ge
      theta A b y hlower)
/-- Transposed version of `(P - I)s = 0` for the symmetric rank-one projector
    in Lemma 20.6. -/
theorem lsLemma20_6Projector_transpose_sub_id_apply_self {m : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0) (k : Fin m) :
    ∑ i : Fin m, (lsLemma20_6Projector s i k - idMatrix m i k) * s i = 0 := by
  have hrow := lsLemma20_6Projector_sub_id_apply_self s hsq k
  simpa [lsLemma20_6Projector_symmetric, idMatrix, eq_comm] using hrow
/-- Transposed action of the complementary projector on `s`: `(I-P)^T s = 0`. -/
theorem lsLemma20_6ProjectorComplement_transpose_apply_self {m : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0) (k : Fin m) :
    ∑ i : Fin m, lsLemma20_6ProjectorComplement s i k * s i = 0 := by
  have hrow := lsLemma20_6Projector_transpose_sub_id_apply_self s hsq k
  unfold lsLemma20_6ProjectorComplement
  calc
    (∑ i : Fin m, (idMatrix m i k - lsLemma20_6Projector s i k) * s i)
        = -∑ i : Fin m, (lsLemma20_6Projector s i k - idMatrix m i k) * s i := by
          rw [← Finset.sum_neg_distrib]
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = 0 := by
          rw [hrow]
          ring
/-- Transposed action of the source complement from (20.21):
    `r^T(I - r r^+) = 0`. -/
theorem lsResidualComplementProjector_transpose_mul_residual {m : ℕ}
    (r : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) (k : Fin m) :
    ∑ i : Fin m, lsResidualComplementProjector r i k * r i = 0 := by
  simpa [lsResidualComplementProjector] using
    lsLemma20_6ProjectorComplement_transpose_apply_self r hrsq k
/-- Since `(I-r r^+)^T r = 0`, adding the residual direction before applying
    the transposed source complement does not change the projected scalar. -/
theorem lsResidualComplementProjector_transpose_mul_residual_add_eq
    {m : ℕ} (r q : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0) (k : Fin m) :
    ∑ i : Fin m, lsResidualComplementProjector r i k * (r i + q i) =
      ∑ i : Fin m, lsResidualComplementProjector r i k * q i := by
  have hres :=
    lsResidualComplementProjector_transpose_mul_residual r hrsq k
  calc
    (∑ i : Fin m, lsResidualComplementProjector r i k * (r i + q i))
        = (∑ i : Fin m, lsResidualComplementProjector r i k * r i) +
            ∑ i : Fin m, lsResidualComplementProjector r i k * q i := by
          rw [← Finset.sum_add_distrib]
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = ∑ i : Fin m, lsResidualComplementProjector r i k * q i := by
          rw [hres]
          ring
/-- The `I-P` and `P` row-projected panels in equation (20.22) are orthogonal
    in the Frobenius inner product. -/
theorem lsLemma20_6ProjectorComplement_projector_frobInner_eq_zero {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (A B : Fin m → Fin n → ℝ) :
    ∑ i : Fin m, ∑ j : Fin n,
        matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j *
          matMulRectLeft (lsLemma20_6Projector s) B i j = 0 := by
  rw [Finset.sum_comm]
  apply Finset.sum_eq_zero
  intro j _
  let D : ℝ := vecNorm2Sq s
  let dotB : ℝ := ∑ k : Fin m, s k * B k j
  have hD : D ≠ 0 := by
    simpa [D] using hsq
  have hQs :
      ∑ i : Fin m,
          matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j * s i = 0 := by
    unfold matMulRectLeft
    calc
      (∑ i : Fin m,
          (∑ k : Fin m, lsLemma20_6ProjectorComplement s i k * A k j) * s i)
          = ∑ i : Fin m, ∑ k : Fin m,
              (lsLemma20_6ProjectorComplement s i k * A k j) * s i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [Finset.sum_mul]
      _ = ∑ k : Fin m, ∑ i : Fin m,
              (lsLemma20_6ProjectorComplement s i k * A k j) * s i := by
            rw [Finset.sum_comm]
      _ = ∑ k : Fin m, A k j *
              ∑ i : Fin m, lsLemma20_6ProjectorComplement s i k * s i := by
            apply Finset.sum_congr rfl
            intro k _
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = 0 := by
            simp [lsLemma20_6ProjectorComplement_transpose_apply_self s hsq]
  calc
    (∑ i : Fin m,
        matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j *
          matMulRectLeft (lsLemma20_6Projector s) B i j)
        = ∑ i : Fin m,
          matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j *
            ((s i / D) * dotB) := by
          apply Finset.sum_congr rfl
          intro i _
          simpa [D, dotB] using congrArg
            (fun x => matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j * x)
            (lsLemma20_6Projector_matMulRectLeft_entry s B i j)
    _ = (dotB / D) *
          ∑ i : Fin m,
            matMulRectLeft (lsLemma20_6ProjectorComplement s) A i j * s i := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = 0 := by
          rw [hQs]
          ring
/-- The vector images of `I-P` and `P` are orthogonal. -/
theorem lsLemma20_6ProjectorComplement_projector_vecInner_eq_zero {m : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (u v : Fin m → ℝ) :
    ∑ i : Fin m,
        matMulVec m (lsLemma20_6ProjectorComplement s) u i *
          matMulVec m (lsLemma20_6Projector s) v i = 0 := by
  let D : ℝ := vecNorm2Sq s
  let dotV : ℝ := ∑ k : Fin m, s k * v k
  have hQs :
      ∑ i : Fin m,
          matMulVec m (lsLemma20_6ProjectorComplement s) u i * s i = 0 := by
    unfold matMulVec
    calc
      (∑ i : Fin m,
          (∑ k : Fin m, lsLemma20_6ProjectorComplement s i k * u k) * s i)
          = ∑ i : Fin m, ∑ k : Fin m,
              (lsLemma20_6ProjectorComplement s i k * u k) * s i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [Finset.sum_mul]
      _ = ∑ k : Fin m, ∑ i : Fin m,
              (lsLemma20_6ProjectorComplement s i k * u k) * s i := by
            rw [Finset.sum_comm]
      _ = ∑ k : Fin m, u k *
              ∑ i : Fin m, lsLemma20_6ProjectorComplement s i k * s i := by
            apply Finset.sum_congr rfl
            intro k _
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = 0 := by
            simp [lsLemma20_6ProjectorComplement_transpose_apply_self s hsq]
  calc
    (∑ i : Fin m,
        matMulVec m (lsLemma20_6ProjectorComplement s) u i *
          matMulVec m (lsLemma20_6Projector s) v i)
        = ∑ i : Fin m,
          matMulVec m (lsLemma20_6ProjectorComplement s) u i *
            ((s i / D) * dotV) := by
          apply Finset.sum_congr rfl
          intro i _
          simpa [matMulVec, D, dotV] using congrArg
            (fun x => matMulVec m (lsLemma20_6ProjectorComplement s) u i * x)
            (lsLemma20_6Projector_apply_vec s v i)
    _ = (dotV / D) *
          ∑ i : Fin m,
            matMulVec m (lsLemma20_6ProjectorComplement s) u i * s i := by
          rw [Finset.mul_sum]
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = 0 := by
          rw [hQs]
          ring
/-- The perturbation `DeltaA1 + P(DeltaA2 - DeltaA1)` used in the nonzero
    branch of Higham, 2nd ed., Lemma 20.6. -/
noncomputable def lsLemma20_6Perturbation {m n : ℕ} (s : Fin m → ℝ)
    (DeltaA1 DeltaA2 : Fin m → Fin n → ℝ) : Fin m → Fin n → ℝ :=
  fun i j => DeltaA1 i j +
    matMulRectLeft (lsLemma20_6Projector s)
      (fun i j => DeltaA2 i j - DeltaA1 i j) i j
/-- The scalar `beta = 1 - s^T H y / s^T s` from the proof of Lemma 20.6,
    where `H = DeltaA2 - DeltaA1`. -/
noncomputable def lsLemma20_6Beta {m n : ℕ} (s : Fin m → ℝ)
    (DeltaA1 DeltaA2 : Fin m → Fin n → ℝ) (y : Fin n → ℝ) : ℝ :=
  1 - (∑ i : Fin m,
    s i * rectMatMulVec (fun i j => DeltaA2 i j - DeltaA1 i j) y i) /
      vecNorm2Sq s
/-- Equation (20.22)'s projector mixture: the constructed perturbation
    `DeltaA1 + P(DeltaA2 - DeltaA1)` is `(I-P)DeltaA1 + P DeltaA2`. -/
theorem lsLemma20_6Perturbation_eq_projector_mixture {m n : ℕ}
    (s : Fin m → ℝ) (DeltaA1 DeltaA2 : Fin m → Fin n → ℝ)
    (i : Fin m) (j : Fin n) :
    lsLemma20_6Perturbation s DeltaA1 DeltaA2 i j =
      matMulRectLeft (lsLemma20_6ProjectorComplement s) DeltaA1 i j +
        matMulRectLeft (lsLemma20_6Projector s) DeltaA2 i j := by
  unfold lsLemma20_6Perturbation lsLemma20_6ProjectorComplement
  have hid := congrFun (congrFun (matMulRectLeft_id DeltaA1) i) j
  unfold matMulRectLeft at hid
  calc
    DeltaA1 i j +
        matMulRectLeft (lsLemma20_6Projector s)
          (fun i j => DeltaA2 i j - DeltaA1 i j) i j
        = (∑ k : Fin m, idMatrix m i k * DeltaA1 k j) +
            (∑ k : Fin m, lsLemma20_6Projector s i k *
              (DeltaA2 k j - DeltaA1 k j)) := by
          rw [hid]
          rfl
    _ = (∑ k : Fin m,
            ((idMatrix m i k - lsLemma20_6Projector s i k) * DeltaA1 k j +
              lsLemma20_6Projector s i k * DeltaA2 k j)) := by
          rw [← Finset.sum_add_distrib]
          apply Finset.sum_congr rfl
          intro k _
          ring
    _ = (∑ k : Fin m,
            (idMatrix m i k - lsLemma20_6Projector s i k) * DeltaA1 k j) +
          (∑ k : Fin m, lsLemma20_6Projector s i k * DeltaA2 k j) := by
          rw [Finset.sum_add_distrib]
private theorem frobNormSqRect_add_eq_add_of_inner_eq_zero_lsq {m n : ℕ}
    (A B : Fin m → Fin n → ℝ)
    (hcross : ∑ i : Fin m, ∑ j : Fin n, A i j * B i j = 0) :
    frobNormSqRect (fun i j => A i j + B i j) =
      frobNormSqRect A + frobNormSqRect B := by
  have hexp : frobNormSqRect (fun i j => A i j + B i j) =
      frobNormSqRect A +
        2 * (∑ i : Fin m, ∑ j : Fin n, A i j * B i j) +
      frobNormSqRect B := by
    unfold frobNormSqRect
    simp_rw [show ∀ i : Fin m, ∀ j : Fin n, (A i j + B i j) ^ 2 =
        A i j ^ 2 + 2 * (A i j * B i j) + B i j ^ 2 from fun i j => by ring,
      Finset.sum_add_distrib]
    rw [show ∑ x : Fin m, ∑ x_1 : Fin n, 2 * (A x x_1 * B x x_1) =
        2 * ∑ x : Fin m, ∑ x_1 : Fin n, A x x_1 * B x x_1 from by
      rw [Finset.mul_sum]
      apply Finset.sum_congr rfl
      intro i _
      rw [Finset.mul_sum]]
  rw [hexp, hcross]
  ring
private theorem vecNorm2Sq_add_eq_add_of_inner_eq_zero_lsq {m : ℕ}
    (u v : Fin m → ℝ) (hcross : ∑ i : Fin m, u i * v i = 0) :
    vecNorm2Sq (fun i => u i + v i) = vecNorm2Sq u + vecNorm2Sq v := by
  have hexp : vecNorm2Sq (fun i => u i + v i) =
      vecNorm2Sq u + 2 * (∑ i : Fin m, u i * v i) + vecNorm2Sq v := by
    unfold vecNorm2Sq
    simp_rw [show ∀ i : Fin m, (u i + v i) ^ 2 =
        u i ^ 2 + 2 * (u i * v i) + v i ^ 2 from fun i => by ring,
      Finset.sum_add_distrib]
    rw [show ∑ i : Fin m, 2 * (u i * v i) =
        2 * ∑ i : Fin m, u i * v i from by rw [Finset.mul_sum]]
  rw [hexp, hcross]
  ring
/-- Squared Frobenius half of the norm consequence printed after equation
    (20.22) in Higham, 2nd ed., Lemma 20.6:
    for `DeltaA = (I-P)DeltaA1 + P DeltaA2`, the two orthogonal projector
    pieces give `||DeltaA||_F^2 <= ||DeltaA1||_F^2 + ||DeltaA2||_F^2`. -/
theorem lsLemma20_6Perturbation_frobNormSqRect_le {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (DeltaA1 DeltaA2 : Fin m → Fin n → ℝ) :
    frobNormSqRect (lsLemma20_6Perturbation s DeltaA1 DeltaA2) ≤
      frobNormSqRect DeltaA1 + frobNormSqRect DeltaA2 := by
  have hmix :
      lsLemma20_6Perturbation s DeltaA1 DeltaA2 =
        fun i j =>
          matMulRectLeft (lsLemma20_6ProjectorComplement s) DeltaA1 i j +
            matMulRectLeft (lsLemma20_6Projector s) DeltaA2 i j := by
    ext i j
    exact lsLemma20_6Perturbation_eq_projector_mixture s DeltaA1 DeltaA2 i j
  rw [hmix]
  have hpyth :
      frobNormSqRect
          (fun i j =>
            matMulRectLeft (lsLemma20_6ProjectorComplement s) DeltaA1 i j +
              matMulRectLeft (lsLemma20_6Projector s) DeltaA2 i j) =
        frobNormSqRect (matMulRectLeft (lsLemma20_6ProjectorComplement s) DeltaA1) +
          frobNormSqRect (matMulRectLeft (lsLemma20_6Projector s) DeltaA2) := by
    exact frobNormSqRect_add_eq_add_of_inner_eq_zero_lsq
      (matMulRectLeft (lsLemma20_6ProjectorComplement s) DeltaA1)
      (matMulRectLeft (lsLemma20_6Projector s) DeltaA2)
      (lsLemma20_6ProjectorComplement_projector_frobInner_eq_zero
        s hsq DeltaA1 DeltaA2)
  rw [hpyth]
  exact add_le_add
    (lsLemma20_6ProjectorComplement_frobNormSqRect_le s hsq DeltaA1)
    (lsLemma20_6Projector_frobNormSqRect_le s hsq DeltaA2)
/-- Frobenius-norm half of the stronger printed bound following equation
    (20.22) in Higham, 2nd ed., Lemma 20.6.  This proves the `p = F` case:
    `||DeltaA||_F <= (||DeltaA1||_F^2 + ||DeltaA2||_F^2)^{1/2}` for the
    constructed symmetric perturbation. -/
theorem lsLemma20_6Perturbation_norm_bound_two_frob {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (DeltaA1 DeltaA2 : Fin m → Fin n → ℝ) :
    frobNormRect (lsLemma20_6Perturbation s DeltaA1 DeltaA2) ≤
      Real.sqrt (frobNormRect DeltaA1 ^ 2 + frobNormRect DeltaA2 ^ 2) := by
  have hsq_bound :=
    lsLemma20_6Perturbation_frobNormSqRect_le s hsq DeltaA1 DeltaA2
  change Real.sqrt (frobNormSqRect (lsLemma20_6Perturbation s DeltaA1 DeltaA2)) ≤
    Real.sqrt (frobNormRect DeltaA1 ^ 2 + frobNormRect DeltaA2 ^ 2)
  exact Real.sqrt_le_sqrt (by
    simpa [frobNormRect_sq] using hsq_bound)
/-- Operator-2 half of the stronger printed bound following equation (20.22)
    in Higham, 2nd ed., Lemma 20.6, stated in the repository's predicate
    form for rectangular operator-2 upper bounds.  If `DeltaA1` and `DeltaA2`
    have operator-2 bounds `alpha` and `beta`, then the constructed
    perturbation has bound `(alpha^2 + beta^2)^{1/2}`. -/
theorem lsLemma20_6Perturbation_norm_bound_two_operator {m n : ℕ}
    (s : Fin m → ℝ) (hsq : vecNorm2Sq s ≠ 0)
    (DeltaA1 DeltaA2 : Fin m → Fin n → ℝ)
    {alpha beta : ℝ} (_halpha : 0 ≤ alpha) (_hbeta : 0 ≤ beta)
    (hDeltaA1 : rectOpNorm2Le DeltaA1 alpha)
    (hDeltaA2 : rectOpNorm2Le DeltaA2 beta) :
    rectOpNorm2Le (lsLemma20_6Perturbation s DeltaA1 DeltaA2)
      (Real.sqrt (alpha ^ 2 + beta ^ 2)) := by
  intro x
  let Q := lsLemma20_6ProjectorComplement s
  let P := lsLemma20_6Projector s
  let u := rectMatMulVec DeltaA1 x
  let v := rectMatMulVec DeltaA2 x
  have haction :
      rectMatMulVec (lsLemma20_6Perturbation s DeltaA1 DeltaA2) x =
        fun i : Fin m => matMulVec m Q u i + matMulVec m P v i := by
    ext i
    unfold rectMatMulVec
    have hQ := congrFun (rectMatMulVec_matMulRectLeft Q DeltaA1 x) i
    have hP := congrFun (rectMatMulVec_matMulRectLeft P DeltaA2 x) i
    calc
      (∑ j : Fin n, lsLemma20_6Perturbation s DeltaA1 DeltaA2 i j * x j)
          = ∑ j : Fin n,
              (matMulRectLeft Q DeltaA1 i j + matMulRectLeft P DeltaA2 i j) * x j := by
            apply Finset.sum_congr rfl
            intro j _
            rw [lsLemma20_6Perturbation_eq_projector_mixture]
      _ = (∑ j : Fin n, matMulRectLeft Q DeltaA1 i j * x j) +
            (∑ j : Fin n, matMulRectLeft P DeltaA2 i j * x j) := by
            rw [← Finset.sum_add_distrib]
            apply Finset.sum_congr rfl
            intro j _
            ring
      _ = matMulVec m Q u i + matMulVec m P v i := by
            simpa [u, v] using congrArg₂ (fun a b => a + b) hQ hP
  have hpyth :
      vecNorm2Sq (fun i : Fin m => matMulVec m Q u i + matMulVec m P v i) =
        vecNorm2Sq (matMulVec m Q u) + vecNorm2Sq (matMulVec m P v) := by
    exact vecNorm2Sq_add_eq_add_of_inner_eq_zero_lsq
      (matMulVec m Q u) (matMulVec m P v)
      (by
        simpa [Q, P] using
          lsLemma20_6ProjectorComplement_projector_vecInner_eq_zero s hsq u v)
  have hu_contract : vecNorm2Sq (matMulVec m Q u) ≤ vecNorm2Sq u := by
    simpa [Q] using lsLemma20_6ProjectorComplement_vecNorm2Sq_le s hsq u
  have hv_contract : vecNorm2Sq (matMulVec m P v) ≤ vecNorm2Sq v := by
    simpa [P] using lsLemma20_6Projector_vecNorm2Sq_le s hsq v
  have hu_bound : vecNorm2Sq u ≤ alpha ^ 2 * vecNorm2Sq x := by
    have hu := hDeltaA1 x
    have hu_sq : vecNorm2 u ^ 2 ≤ (alpha * vecNorm2 x) ^ 2 := by
      nlinarith [hu, vecNorm2_nonneg u,
        mul_nonneg _halpha (vecNorm2_nonneg x)]
    rw [← vecNorm2_sq u, ← vecNorm2_sq x]
    nlinarith [hu_sq]
  have hv_bound : vecNorm2Sq v ≤ beta ^ 2 * vecNorm2Sq x := by
    have hv := hDeltaA2 x
    have hv_sq : vecNorm2 v ^ 2 ≤ (beta * vecNorm2 x) ^ 2 := by
      nlinarith [hv, vecNorm2_nonneg v,
        mul_nonneg _hbeta (vecNorm2_nonneg x)]
    rw [← vecNorm2_sq v, ← vecNorm2_sq x]
    nlinarith [hv_sq]
  have hsq :
      vecNorm2Sq (rectMatMulVec (lsLemma20_6Perturbation s DeltaA1 DeltaA2) x) ≤
        (alpha ^ 2 + beta ^ 2) * vecNorm2Sq x := by
    rw [haction, hpyth]
    calc
      vecNorm2Sq (matMulVec m Q u) + vecNorm2Sq (matMulVec m P v)
          ≤ vecNorm2Sq u + vecNorm2Sq v :=
            add_le_add hu_contract hv_contract
      _ ≤ alpha ^ 2 * vecNorm2Sq x + beta ^ 2 * vecNorm2Sq x :=
            add_le_add hu_bound hv_bound
      _ = (alpha ^ 2 + beta ^ 2) * vecNorm2Sq x := by
            ring
  have hrad_nonneg : 0 ≤ alpha ^ 2 + beta ^ 2 :=
    add_nonneg (sq_nonneg alpha) (sq_nonneg beta)
  calc
    vecNorm2 (rectMatMulVec (lsLemma20_6Perturbation s DeltaA1 DeltaA2) x)
        = Real.sqrt
            (vecNorm2Sq (rectMatMulVec (lsLemma20_6Perturbation s DeltaA1 DeltaA2) x)) := rfl
    _ ≤ Real.sqrt ((alpha ^ 2 + beta ^ 2) * vecNorm2Sq x) :=
          Real.sqrt_le_sqrt hsq
    _ = Real.sqrt (alpha ^ 2 + beta ^ 2) * vecNorm2 x := by
          unfold vecNorm2
          rw [Real.sqrt_mul hrad_nonneg]
private theorem lsLemma20_6Perturbation_left_decomp {m n : ℕ}
    (A DeltaA1 DeltaA2 : Fin m → Fin n → ℝ) (s : Fin m → ℝ)
    (i : Fin m) (j : Fin n) :
    A i j + lsLemma20_6Perturbation s DeltaA1 DeltaA2 i j =
      (A i j + DeltaA2 i j) +
        matMulRectLeft
          (fun a b => lsLemma20_6Projector s a b - idMatrix m a b)
          (fun a b => DeltaA2 a b - DeltaA1 a b) i j := by
  unfold lsLemma20_6Perturbation
  have hid := congrFun (congrFun
    (matMulRectLeft_id (fun a b => DeltaA2 a b - DeltaA1 a b)) i) j
  unfold matMulRectLeft at hid
  calc
    A i j + (DeltaA1 i j +
        matMulRectLeft (lsLemma20_6Projector s)
          (fun i j => DeltaA2 i j - DeltaA1 i j) i j)
        = (A i j + DeltaA2 i j) +
            ((∑ k : Fin m, lsLemma20_6Projector s i k *
                (DeltaA2 k j - DeltaA1 k j)) -
              (DeltaA2 i j - DeltaA1 i j)) := by
          unfold matMulRectLeft
          ring
    _ = (A i j + DeltaA2 i j) +
            ((∑ k : Fin m, lsLemma20_6Projector s i k *
                (DeltaA2 k j - DeltaA1 k j)) -
              (∑ k : Fin m, idMatrix m i k *
                (DeltaA2 k j - DeltaA1 k j))) := by
          rw [hid]
    _ = (A i j + DeltaA2 i j) +
        matMulRectLeft
          (fun a b => lsLemma20_6Projector s a b - idMatrix m a b)
          (fun a b => DeltaA2 a b - DeltaA1 a b) i j := by
          unfold matMulRectLeft
          rw [← Finset.sum_sub_distrib]
          congr 1
          apply Finset.sum_congr rfl
          intro k _
          ring
private theorem lsLemma20_6Perturbation_transpose_action_self {m n : ℕ}
    (A DeltaA1 DeltaA2 : Fin m → Fin n → ℝ) (s : Fin m → ℝ)
    (hsq : vecNorm2Sq s ≠ 0)
    (hbottom : ∀ j : Fin n, ∑ i : Fin m, (A i j + DeltaA2 i j) * s i = 0)
    (j : Fin n) :
    ∑ i : Fin m, (A i j + lsLemma20_6Perturbation s DeltaA1 DeltaA2 i j) *
      s i = 0 := by
  let H : Fin m → Fin n → ℝ := fun a b => DeltaA2 a b - DeltaA1 a b
  have hcorr :
      ∑ i : Fin m,
        matMulRectLeft
          (fun a b => lsLemma20_6Projector s a b - idMatrix m a b) H i j * s i =
        0 := by
    unfold matMulRectLeft H
    calc
      (∑ i : Fin m,
          (∑ k : Fin m,
            (lsLemma20_6Projector s i k - idMatrix m i k) *
              (DeltaA2 k j - DeltaA1 k j)) * s i)
          = ∑ i : Fin m, ∑ k : Fin m,
              ((lsLemma20_6Projector s i k - idMatrix m i k) *
                (DeltaA2 k j - DeltaA1 k j)) * s i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [Finset.sum_mul]
      _ = ∑ k : Fin m, ∑ i : Fin m,
              ((lsLemma20_6Projector s i k - idMatrix m i k) *
                (DeltaA2 k j - DeltaA1 k j)) * s i := by
            rw [Finset.sum_comm]
      _ = ∑ k : Fin m,
              (DeltaA2 k j - DeltaA1 k j) *
                ∑ i : Fin m,
                  (lsLemma20_6Projector s i k - idMatrix m i k) * s i := by
            apply Finset.sum_congr rfl
            intro k _
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = 0 := by
            apply Finset.sum_eq_zero
            intro k _
            rw [lsLemma20_6Projector_transpose_sub_id_apply_self s hsq k]
            ring
  calc
    (∑ i : Fin m, (A i j + lsLemma20_6Perturbation s DeltaA1 DeltaA2 i j) * s i)
        = ∑ i : Fin m,
            ((A i j + DeltaA2 i j) +
              matMulRectLeft
                (fun a b => lsLemma20_6Projector s a b - idMatrix m a b) H i j) *
              s i := by
          apply Finset.sum_congr rfl
          intro i _
          rw [lsLemma20_6Perturbation_left_decomp]
    _ = (∑ i : Fin m, (A i j + DeltaA2 i j) * s i) +
          ∑ i : Fin m,
            matMulRectLeft
              (fun a b => lsLemma20_6Projector s a b - idMatrix m a b) H i j *
              s i := by
          rw [← Finset.sum_add_distrib]
          apply Finset.sum_congr rfl
          intro i _
          ring
    _ = 0 := by
          rw [hbottom j, hcorr]
          ring
/-- Nonzero-residual branch in the proof of Lemma 20.6: with
    `DeltaA = DeltaA1 + P(DeltaA2 - DeltaA1)` and
    `sbar = beta * s`, the symmetric perturbed augmented system holds. -/
theorem LSAsymmetricPerturbedAugmentedSystem.to_augmentedSystem_of_s_ne_zero
    {m n : ℕ} (A DeltaA1 DeltaA2 : Fin m → Fin n → ℝ)
    (b s : Fin m → ℝ) (y : Fin n → ℝ)
    (h : LSAsymmetricPerturbedAugmentedSystem A DeltaA1 DeltaA2 b s y)
    (hs : s ≠ 0) :
    let DeltaA := lsLemma20_6Perturbation s DeltaA1 DeltaA2
    let sbar := fun i : Fin m => lsLemma20_6Beta s DeltaA1 DeltaA2 y * s i
    LSAugmentedSystem (fun i j => A i j + DeltaA i j) b (0 : Fin n → ℝ)
      sbar y := by
  intro DeltaA sbar
  have hsq : vecNorm2Sq s ≠ 0 := lsLemma20_6Projector_den_ne_zero_of_ne_zero hs
  let H : Fin m → Fin n → ℝ := fun a b => DeltaA2 a b - DeltaA1 a b
  let v : Fin m → ℝ := rectMatMulVec H y
  constructor
  · intro i
    have htop := h.1 i
    have hsplit :
        rectMatMulVec (fun a b => A a b + DeltaA a b) y i =
          rectMatMulVec (fun a b => A a b + DeltaA1 a b) y i +
            ∑ k : Fin m, lsLemma20_6Projector s i k * v k := by
      have hmatrix := rectMatMulVec_add_matrix_lsq
        (fun a b => A a b + DeltaA1 a b)
        (matMulRectLeft (lsLemma20_6Projector s) H) y
      have hleft := congrFun hmatrix i
      have hright := congrFun
        (rectMatMulVec_matMulRectLeft (lsLemma20_6Projector s) H y) i
      simpa [DeltaA, lsLemma20_6Perturbation, H, v, hright, add_assoc] using hleft
    have hproj := lsLemma20_6Projector_apply_vec s v i
    rw [hsplit, hproj]
    unfold sbar lsLemma20_6Beta v H
    ring_nf at htop ⊢
    linarith
  · intro j
    have htrans := lsLemma20_6Perturbation_transpose_action_self
      A DeltaA1 DeltaA2 s hsq h.2 j
    calc
      (∑ i : Fin m, (A i j + DeltaA i j) * sbar i)
          = lsLemma20_6Beta s DeltaA1 DeltaA2 y *
              ∑ i : Fin m, (A i j + DeltaA i j) * s i := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            unfold sbar
            ring
      _ = 0 := by
            rw [htrans]
            ring
/-- Existence form of the constructive part of Higham, 2nd ed., Lemma 20.6:
    an asymmetric perturbed augmented system yields a symmetric perturbed
    augmented system for the same vector `y`. -/
theorem LSAsymmetricPerturbedAugmentedSystem.exists_augmentedSystem
    {m n : ℕ} (A DeltaA1 DeltaA2 : Fin m → Fin n → ℝ)
    (b s : Fin m → ℝ) (y : Fin n → ℝ)
    (h : LSAsymmetricPerturbedAugmentedSystem A DeltaA1 DeltaA2 b s y) :
    ∃ (DeltaA : Fin m → Fin n → ℝ) (sbar : Fin m → ℝ),
      LSAugmentedSystem (fun i j => A i j + DeltaA i j) b (0 : Fin n → ℝ) sbar y := by
  by_cases hs : s = 0
  · refine ⟨DeltaA1, (0 : Fin m → ℝ), ?_⟩
    exact LSAsymmetricPerturbedAugmentedSystem.to_augmentedSystem_of_s_eq_zero
      A DeltaA1 DeltaA2 b s y h hs
  · refine ⟨lsLemma20_6Perturbation s DeltaA1 DeltaA2,
      (fun i : Fin m => lsLemma20_6Beta s DeltaA1 DeltaA2 y * s i), ?_⟩
    simpa using
      LSAsymmetricPerturbedAugmentedSystem.to_augmentedSystem_of_s_ne_zero
        A DeltaA1 DeltaA2 b s y h hs
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 `Delta g` source majorant:
    the componentwise vector `|A^T| H |r_hat|`. -/
noncomputable def lsTheorem20_4DeltagMajorant {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (H : Fin m → Fin m → ℝ)
    (rhat : Fin m → ℝ) : Fin n → ℝ :=
  fun j => ∑ i : Fin m, |A i j| * matMulVec m H (fun t => |rhat t|) i
/-- The `Delta g` majorant is entrywise nonnegative when `H` is entrywise
    nonnegative. -/
theorem lsTheorem20_4DeltagMajorant_nonneg {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (H : Fin m → Fin m → ℝ)
    (rhat : Fin m → ℝ) (hH : ∀ i j, 0 ≤ H i j) :
    ∀ j : Fin n, 0 ≤ lsTheorem20_4DeltagMajorant A H rhat j := by
  intro j
  unfold lsTheorem20_4DeltagMajorant matMulVec
  apply Finset.sum_nonneg
  intro i _
  refine mul_nonneg (abs_nonneg _) ?_
  apply Finset.sum_nonneg
  intro t _
  exact mul_nonneg (hH i t) (abs_nonneg _)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 `Delta f` source majorant:
    the componentwise vector `H_1 |f| + H_2 |r_hat|`. -/
noncomputable def lsTheorem20_4DeltafMajorant {m : ℕ}
    (H1 H2 : Fin m → Fin m → ℝ)
    (f rhat : Fin m → ℝ) : Fin m → ℝ :=
  fun i =>
    matMulVec m H1 (fun t => |f t|) i +
      matMulVec m H2 (fun t => |rhat t|) i
/-- The Theorem 20.4 `Delta f` majorant is entrywise nonnegative when
    `H_1` and `H_2` are entrywise nonnegative. -/
theorem lsTheorem20_4DeltafMajorant_nonneg {m : ℕ}
    (H1 H2 : Fin m → Fin m → ℝ) (f rhat : Fin m → ℝ)
    (hH1 : ∀ i j, 0 ≤ H1 i j) (hH2 : ∀ i j, 0 ≤ H2 i j) :
    ∀ i : Fin m, 0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  intro i
  unfold lsTheorem20_4DeltafMajorant matMulVec
  exact add_nonneg
    (Finset.sum_nonneg (by
      intro t _
      exact mul_nonneg (hH1 i t) (abs_nonneg _)))
    (Finset.sum_nonneg (by
      intro t _
      exact mul_nonneg (hH2 i t) (abs_nonneg _)))
/-- Each visible `H_1 |f|` term is bounded by the full Theorem 20.4
    `Delta f` source majorant. -/
theorem lsTheorem20_4DeltafMajorant_f_term_le {m : ℕ}
    (H1 H2 : Fin m → Fin m → ℝ) (f rhat : Fin m → ℝ)
    (hH1 : ∀ i j, 0 ≤ H1 i j) (hH2 : ∀ i j, 0 ≤ H2 i j)
    (i j : Fin m) :
    H1 i j * |f j| ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  unfold lsTheorem20_4DeltafMajorant matMulVec
  have hH1sum :
      H1 i j * |f j| ≤ ∑ t : Fin m, H1 i t * |f t| := by
    exact Finset.single_le_sum
      (fun t _ => mul_nonneg (hH1 i t) (abs_nonneg _))
      (Finset.mem_univ j)
  have hH2sum : 0 ≤ ∑ t : Fin m, H2 i t * |rhat t| := by
    exact Finset.sum_nonneg (fun t _ =>
      mul_nonneg (hH2 i t) (abs_nonneg _))
  exact hH1sum.trans (le_add_of_nonneg_right hH2sum)
/-- Each visible `H_2 |r_hat|` term is bounded by the full Theorem 20.4
    `Delta f` source majorant. -/
theorem lsTheorem20_4DeltafMajorant_rhat_term_le {m : ℕ}
    (H1 H2 : Fin m → Fin m → ℝ) (f rhat : Fin m → ℝ)
    (hH1 : ∀ i j, 0 ≤ H1 i j) (hH2 : ∀ i j, 0 ≤ H2 i j)
    (i j : Fin m) :
    H2 i j * |rhat j| ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  unfold lsTheorem20_4DeltafMajorant matMulVec
  have hH1sum : 0 ≤ ∑ t : Fin m, H1 i t * |f t| := by
    exact Finset.sum_nonneg (fun t _ =>
      mul_nonneg (hH1 i t) (abs_nonneg _))
  have hH2sum :
      H2 i j * |rhat j| ≤ ∑ t : Fin m, H2 i t * |rhat t| := by
    exact Finset.single_le_sum
      (fun t _ => mul_nonneg (hH2 i t) (abs_nonneg _))
      (Finset.mem_univ j)
  exact hH2sum.trans (le_add_of_nonneg_left hH1sum)
/-- A pointwise scalar bound by one `H_1 |f|` component implies the full
    Theorem 20.4 `Delta f` source-majorant bound. -/
theorem lsTheorem20_4DeltafMajorant_dom_of_f_term_dom {m : ℕ}
    (H1 H2 : Fin m → Fin m → ℝ) (f rhat : Fin m → ℝ)
    (b c : ℝ) (hc : 0 ≤ c)
    (hH1 : ∀ i j, 0 ≤ H1 i j) (hH2 : ∀ i j, 0 ≤ H2 i j)
    (j0 : Fin m)
    (hdom : ∀ i : Fin m, b ≤ c * (H1 i j0 * |f j0|)) :
    ∀ i : Fin m, b ≤ c * lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  intro i
  have hterm :
      H1 i j0 * |f j0| ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
    lsTheorem20_4DeltafMajorant_f_term_le H1 H2 f rhat hH1 hH2 i j0
  exact (hdom i).trans (mul_le_mul_of_nonneg_left hterm hc)
/-- A pointwise scalar bound by one `H_2 |r_hat|` component implies the full
    Theorem 20.4 `Delta f` source-majorant bound. -/
theorem lsTheorem20_4DeltafMajorant_dom_of_rhat_term_dom {m : ℕ}
    (H1 H2 : Fin m → Fin m → ℝ) (f rhat : Fin m → ℝ)
    (b c : ℝ) (hc : 0 ≤ c)
    (hH1 : ∀ i j, 0 ≤ H1 i j) (hH2 : ∀ i j, 0 ≤ H2 i j)
    (j0 : Fin m)
    (hdom : ∀ i : Fin m, b ≤ c * (H2 i j0 * |rhat j0|)) :
    ∀ i : Fin m, b ≤ c * lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  intro i
  have hterm :
      H2 i j0 * |rhat j0| ≤
        lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
    lsTheorem20_4DeltafMajorant_rhat_term_le H1 H2 f rhat hH1 hH2 i j0
  exact (hdom i).trans (mul_le_mul_of_nonneg_left hterm hc)
/-- Uniform-column Frobenius-unit source matrix used to expose one max
    component of `|f|` in Theorem 20.4's `H_1 |f|` term. -/
noncomputable def lsTheorem20_4UniformColumnMajorant {m : ℕ}
    (j0 : Fin m) : Fin m → Fin m → ℝ :=
  fun _ j => if j = j0 then (Real.sqrt (m : ℝ))⁻¹ else 0
/-- One-hot Frobenius-unit source matrix, useful when a theorem needs an
    unused nonnegative `H_i` witness with Frobenius norm one. -/
noncomputable def lsTheorem20_4OneHotMajorant {m : ℕ}
    (row0 col0 : Fin m) : Fin m → Fin m → ℝ :=
  fun i j => if row0 = i ∧ col0 = j then 1 else 0
/-- The uniform-column Theorem 20.4 source matrix is entrywise nonnegative. -/
theorem lsTheorem20_4UniformColumnMajorant_nonneg {m : ℕ} (j0 : Fin m) :
    ∀ i j, 0 ≤ lsTheorem20_4UniformColumnMajorant j0 i j := by
  intro i j
  by_cases h : j = j0
  · simp [lsTheorem20_4UniformColumnMajorant, h,
      inv_nonneg.mpr (Real.sqrt_nonneg _)]
  · simp [lsTheorem20_4UniformColumnMajorant, h]
/-- The one-hot Theorem 20.4 source matrix is entrywise nonnegative. -/
theorem lsTheorem20_4OneHotMajorant_nonneg {m : ℕ}
    (row0 col0 : Fin m) :
    ∀ i j, 0 ≤ lsTheorem20_4OneHotMajorant row0 col0 i j := by
  intro i j
  by_cases h : row0 = i ∧ col0 = j
  · simp [lsTheorem20_4OneHotMajorant, h]
  · simp [lsTheorem20_4OneHotMajorant, h]
/-- The uniform-column Theorem 20.4 source matrix has Frobenius norm one. -/
theorem lsTheorem20_4UniformColumnMajorant_frobNorm {m : ℕ}
    (hm : 0 < m) (j0 : Fin m) :
    frobNorm (lsTheorem20_4UniformColumnMajorant j0) = 1 := by
  have hmR : 0 < (m : ℝ) := by exact_mod_cast hm
  have hsqrt_pos : 0 < Real.sqrt (m : ℝ) := Real.sqrt_pos.2 hmR
  have hs :
      ((Real.sqrt (m : ℝ))⁻¹) ^ 2 = (m : ℝ)⁻¹ := by
    rw [inv_pow]
    rw [sq, Real.mul_self_sqrt (le_of_lt hmR)]
  have hsq :
      frobNormSqRect (lsTheorem20_4UniformColumnMajorant j0) = 1 := by
    calc
      frobNormSqRect (lsTheorem20_4UniformColumnMajorant j0)
          = ∑ i : Fin m, ((Real.sqrt (m : ℝ))⁻¹) ^ 2 := by
              unfold frobNormSqRect lsTheorem20_4UniformColumnMajorant
              apply Finset.sum_congr rfl
              intro i _
              rw [Finset.sum_eq_single j0]
              · simp
              · intro j _ hj
                simp [hj]
              · intro hnot
                exact False.elim (hnot (Finset.mem_univ j0))
      _ = (m : ℝ) * ((Real.sqrt (m : ℝ))⁻¹) ^ 2 := by
              simp [Finset.card_univ]
      _ = (m : ℝ) * (m : ℝ)⁻¹ := by rw [hs]
      _ = 1 := by
              field_simp [ne_of_gt hmR]
  calc
    frobNorm (lsTheorem20_4UniformColumnMajorant j0)
        = frobNormRect (lsTheorem20_4UniformColumnMajorant j0) :=
            (frobNormRect_eq_frobNormFn
              (lsTheorem20_4UniformColumnMajorant j0)).symm
    _ = Real.sqrt (frobNormSqRect
          (lsTheorem20_4UniformColumnMajorant j0)) := rfl
    _ = 1 := by simp [hsq]
/-- The one-hot Theorem 20.4 source matrix has Frobenius norm one. -/
theorem lsTheorem20_4OneHotMajorant_frobNorm {m : ℕ}
    (row0 col0 : Fin m) :
    frobNorm (lsTheorem20_4OneHotMajorant row0 col0) = 1 := by
  calc
    frobNorm (lsTheorem20_4OneHotMajorant row0 col0)
        = frobNormRect (lsTheorem20_4OneHotMajorant row0 col0) :=
            (frobNormRect_eq_frobNormFn
              (lsTheorem20_4OneHotMajorant row0 col0)).symm
    _ = |(1 : ℝ)| := by
            simpa [lsTheorem20_4OneHotMajorant] using
              frobNormRect_single_left row0 col0 (1 : ℝ)
    _ = 1 := by norm_num
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 matrix-side source-shape QR
    handoff with the printed `Delta g` slot exposed.

    The triangular solve for `R^T h = g` used here has no right-hand-side
    perturbation, so `Delta g` is zero.  This theorem packages that zero
    perturbation against the source-shaped nonnegative majorant
    `|A^T| H_3 |r_hat|` for any supplied Frobenius-unit nonnegative `H_3`.
    The `Delta f` side is still the existing scalar RHS-transform bound, not
    Higham's final `H_1 |f| + H_2 |r_hat|` witness. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error_with_zero_deltag_bound
    {n k : ℕ} (fp : FPModel)
    (Q : Fin (n + k) → Fin (n + k) → ℝ)
    (A Rhat : Fin (n + k) → Fin n → ℝ)
    (f c_hat : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (cA cComp cF cG : ℝ)
    (H3 : Fin (n + k) → Fin (n + k) → ℝ)
    (hQR : StructuredHouseholderQRPanelHighamBackwardError (n + k) n
      A Q Rhat cA cComp)
    (hRhs : HouseholderQRRhsPanelExplicitBackwardError (n + k) n
      A f Q c_hat cF)
    (hcG : 0 ≤ cG)
    (hH3nonneg : ∀ i j, 0 ≤ H3 i j)
    (hH3norm : frobNorm H3 = 1)
    (hdiag : ∀ i : Fin n, Rhat (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H3w : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ cA ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        cComp * matMulRect (n + k) (n + k) n G
          (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤ cF) ∧
      (∀ j, |Deltag j| ≤
        cG * lsTheorem20_4DeltagMajorant A H3w rhat j) ∧
      (∀ i j, 0 ≤ H3w i j) ∧
      frobNorm H3w = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error
      fp Q A Rhat f c_hat g cA cComp cF hQR hRhs hdiag hγ with
    ⟨DeltaA, G, Deltaf, DeltaR1, DeltaR2, hDeltaA, hGnonneg, hGnorm,
      hDeltaAcomp, hDeltaf, hDeltaR1, hDeltaR2, hsys⟩
  let Deltag : Fin n → ℝ := fun _ => 0
  have hDeltag : ∀ j : Fin n,
      |Deltag j| ≤ cG * lsTheorem20_4DeltagMajorant A H3 rhat j := by
    intro j
    have hmaj := lsTheorem20_4DeltagMajorant_nonneg A H3 rhat hH3nonneg j
    simpa [Deltag] using mul_nonneg hcG hmaj
  refine ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag,
    hH3nonneg, hH3norm, hDeltaR1, hDeltaR2, ?_⟩
  simpa [R, cBot, h, rhat, Deltag] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 matrix-side source-shape QR
    handoff with both printed RHS majorants exposed.

    This theorem is deliberately conditional on the remaining RHS-transform
    domination lemma: the existing QR RHS analysis supplies a scalar component
    bound `cF`, and the hypothesis `hDeltafDom` states that this scalar bound is
    dominated by the printed source-shaped majorant
    `cFsrc * (H_1 |f| + H_2 |r_hat|)`.  Thus the theorem packages the final
    source shape without hiding the still-open proof of that domination. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error_with_source_rhs_bounds
    {n k : ℕ} (fp : FPModel)
    (Q : Fin (n + k) → Fin (n + k) → ℝ)
    (A Rhat : Fin (n + k) → Fin n → ℝ)
    (f c_hat : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (cA cComp cF cFsrc cG : ℝ)
    (H1 H2 H3 : Fin (n + k) → Fin (n + k) → ℝ)
    (hQR : StructuredHouseholderQRPanelHighamBackwardError (n + k) n
      A Q Rhat cA cComp)
    (hRhs : HouseholderQRRhsPanelExplicitBackwardError (n + k) n
      A f Q c_hat cF)
    (hcG : 0 ≤ cG)
    (hH1nonneg : ∀ i j, 0 ≤ H1 i j)
    (hH2nonneg : ∀ i j, 0 ≤ H2 i j)
    (hH3nonneg : ∀ i j, 0 ≤ H3 i j)
    (hH1norm : frobNorm H1 = 1)
    (hH2norm : frobNorm H2 = 1)
    (hH3norm : frobNorm H3 = 1)
    (hDeltafDom :
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        cF ≤ cFsrc * lsTheorem20_4DeltafMajorant H1 H2 f rhat i)
    (hdiag : ∀ i : Fin n, Rhat (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3w : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ cA ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        cComp * matMulRect (n + k) (n + k) n G
          (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        cFsrc * lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        cG * lsTheorem20_4DeltagMajorant A H3w rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3w i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3w = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error_with_zero_deltag_bound
      fp Q A Rhat f c_hat g cA cComp cF cG H3
      hQR hRhs hcG hH3nonneg hH3norm hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3w, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3wNonneg,
      hH3wNorm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤ cFsrc * lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i) (hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3w, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3wNonneg, hH1norm, hH2norm, hH3wNorm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [R, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with source-shaped matrix perturbation and an explicit zero
    `Delta g` package.

    The concrete rounded `fl_householderQRPanel_R` path supplies `DeltaA` and
    `G` in Higham's matrix-side componentwise form.  Since the repository's
    forward-substitution backward-error theorem perturbs the matrix in
    `R^T h = g` rather than the right-hand side, this wrapper exposes
    `Delta g = 0` and bounds it by the printed-style nonnegative majorant
    `|A^T| H_3 |r_hat|` with a Frobenius-unit one-hot `H_3`.  The remaining
    Theorem 20.4 gap is the printed `Delta f` witness
    `H_1 |f| + H_2 |r_hat|`. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i,
        |Deltaf i| ≤ householderQRRhsPanelBackwardBound fp (n + k) n A f) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  let row0 : Fin (n + k) := ⟨0, by omega⟩
  let H3 : Fin (n + k) → Fin (n + k) → ℝ :=
    fun i j => if row0 = i ∧ row0 = j then 1 else 0
  have hH3nonneg : ∀ i j, 0 ≤ H3 i j := by
    intro i j
    by_cases hij : row0 = i ∧ row0 = j
    · have hij_eq : i = j := hij.1.symm.trans hij.2
      simp [H3, hij, hij_eq]
    · simp [H3, hij]
  have hH3norm : frobNorm H3 = 1 := by
    calc
      frobNorm H3 = frobNormRect H3 := (frobNormRect_eq_frobNormFn H3).symm
      _ = |(1 : ℝ)| := by
          simpa [H3] using frobNormRect_single_left row0 row0 (1 : ℝ)
      _ = 1 := by norm_num
  have hn_le_rows : n ≤ n + k := Nat.le_add_right n k
  have hsteps : 0 < Nat.min (n + k) n := by
    simpa [Nat.min_eq_right hn_le_rows] using hn
  have hQR :
      StructuredHouseholderQRPanelHighamBackwardError (n + k) n A Q Rhat
        (gamma fp (n * K) * frobNorm A)
        ((n + k : ℝ) * gamma fp (n * K)) := by
    have hraw :=
      fl_householderQRPanel_R_higham_backward_error_gammaHigham_of_global_gammaValid
        fp (n + k) n A hsteps
        (by simpa [K, Nat.min_eq_right hn_le_rows] using hvalid)
    simpa [Q, Rhat, K, Nat.min_eq_right hn_le_rows] using hraw
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  have hRhs :
      HouseholderQRRhsPanelExplicitBackwardError (n + k) n A f Q c_hat
        (householderQRRhsPanelBackwardBound fp (n + k) n A f) := by
    simpa [Q, c_hat] using
      fl_householderQRPanel_rhs_explicit_backward_error fp (n + k) n A f
        hready
  have hcG :
      0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) * gamma fp (n * K) := by
    exact mul_nonneg
      (mul_nonneg (Real.sqrt_nonneg _) (by positivity))
      (gamma_nonneg fp (by simpa [K] using hvalid))
  have hbase :=
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error_with_zero_deltag_bound
      fp Q A Rhat f c_hat g
      (gamma fp (n * K) * frobNorm A)
      ((n + k : ℝ) * gamma fp (n * K))
      (householderQRRhsPanelBackwardBound fp (n + k) n A f)
      (Real.sqrt (n + k : ℝ) * (n : ℝ) * gamma fp (n * K))
      H3 hQR hRhs hcG hH3nonneg hH3norm
      (by simpa [Rhat] using hdiag) hγ
  simpa [Q, Rhat, c_hat, K, H3] using hbase
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with source-shaped matrix perturbation, an explicit zero `Delta g`
    package, and the sharpened fixed-`Q` RHS perturbation bound.

    This is the sqrt-budget analogue of
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound`.
    The only changed component is the `Delta f` radius, which is now
    `householderQRRhsPanelSqrtBackwardBound`. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound_sqrt
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i,
        |Deltaf i| ≤
          householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  let row0 : Fin (n + k) := ⟨0, by omega⟩
  let H3 : Fin (n + k) → Fin (n + k) → ℝ :=
    fun i j => if row0 = i ∧ row0 = j then 1 else 0
  have hH3nonneg : ∀ i j, 0 ≤ H3 i j := by
    intro i j
    by_cases hij : row0 = i ∧ row0 = j
    · have hij_eq : i = j := hij.1.symm.trans hij.2
      simp [H3, hij, hij_eq]
    · simp [H3, hij]
  have hH3norm : frobNorm H3 = 1 := by
    calc
      frobNorm H3 = frobNormRect H3 := (frobNormRect_eq_frobNormFn H3).symm
      _ = |(1 : ℝ)| := by
          simpa [H3] using frobNormRect_single_left row0 row0 (1 : ℝ)
      _ = 1 := by norm_num
  have hn_le_rows : n ≤ n + k := Nat.le_add_right n k
  have hsteps : 0 < Nat.min (n + k) n := by
    simpa [Nat.min_eq_right hn_le_rows] using hn
  have hQR :
      StructuredHouseholderQRPanelHighamBackwardError (n + k) n A Q Rhat
        (gamma fp (n * K) * frobNorm A)
        ((n + k : ℝ) * gamma fp (n * K)) := by
    have hraw :=
      fl_householderQRPanel_R_higham_backward_error_gammaHigham_of_global_gammaValid
        fp (n + k) n A hsteps
        (by simpa [K, Nat.min_eq_right hn_le_rows] using hvalid)
    simpa [Q, Rhat, K, Nat.min_eq_right hn_le_rows] using hraw
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  have hRhs :
      HouseholderQRRhsPanelExplicitBackwardError (n + k) n A f Q c_hat
        (householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f) := by
    simpa [Q, c_hat] using
      fl_householderQRPanel_rhs_explicit_backward_error_sqrt
        fp (n + k) n A f hready
  have hcG :
      0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) * gamma fp (n * K) := by
    exact mul_nonneg
      (mul_nonneg (Real.sqrt_nonneg _) (by positivity))
      (gamma_nonneg fp (by simpa [K] using hvalid))
  have hbase :=
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error_with_zero_deltag_bound
      fp Q A Rhat f c_hat g
      (gamma fp (n * K) * frobNorm A)
      ((n + k : ℝ) * gamma fp (n * K))
      (householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f)
      (Real.sqrt (n + k : ℝ) * (n : ℝ) * gamma fp (n * K))
      H3 hQR hRhs hcG hH3nonneg hH3norm
      (by simpa [Rhat] using hdiag) hγ
  simpa [Q, Rhat, c_hat, K, H3] using hbase
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with source-shaped matrix perturbation, an explicit zero `Delta g`
    package, and the residual-sharpened fixed-`Q` RHS perturbation bound.

    This is the residual-sharpened analogue of
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound_sqrt`.
    The only changed component is the `Delta f` radius, which is now
    `householderQRRhsPanelSqrtResidualBackwardBound`. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound_sqrt_residual
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i,
        |Deltaf i| ≤
          householderQRRhsPanelSqrtResidualBackwardBound fp
            (n + k) n A f) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  let row0 : Fin (n + k) := ⟨0, by omega⟩
  let H3 : Fin (n + k) → Fin (n + k) → ℝ :=
    fun i j => if row0 = i ∧ row0 = j then 1 else 0
  have hH3nonneg : ∀ i j, 0 ≤ H3 i j := by
    intro i j
    by_cases hij : row0 = i ∧ row0 = j
    · have hij_eq : i = j := hij.1.symm.trans hij.2
      simp [H3, hij, hij_eq]
    · simp [H3, hij]
  have hH3norm : frobNorm H3 = 1 := by
    calc
      frobNorm H3 = frobNormRect H3 := (frobNormRect_eq_frobNormFn H3).symm
      _ = |(1 : ℝ)| := by
          simpa [H3] using frobNormRect_single_left row0 row0 (1 : ℝ)
      _ = 1 := by norm_num
  have hn_le_rows : n ≤ n + k := Nat.le_add_right n k
  have hsteps : 0 < Nat.min (n + k) n := by
    simpa [Nat.min_eq_right hn_le_rows] using hn
  have hQR :
      StructuredHouseholderQRPanelHighamBackwardError (n + k) n A Q Rhat
        (gamma fp (n * K) * frobNorm A)
        ((n + k : ℝ) * gamma fp (n * K)) := by
    have hraw :=
      fl_householderQRPanel_R_higham_backward_error_gammaHigham_of_global_gammaValid
        fp (n + k) n A hsteps
        (by simpa [K, Nat.min_eq_right hn_le_rows] using hvalid)
    simpa [Q, Rhat, K, Nat.min_eq_right hn_le_rows] using hraw
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  have hRhs :
      HouseholderQRRhsPanelExplicitBackwardError (n + k) n A f Q c_hat
        (householderQRRhsPanelSqrtResidualBackwardBound fp
          (n + k) n A f) := by
    simpa [Q, c_hat] using
      fl_householderQRPanel_rhs_explicit_backward_error_sqrt_residual
        fp (n + k) n A f hready
  have hcG :
      0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) * gamma fp (n * K) := by
    exact mul_nonneg
      (mul_nonneg (Real.sqrt_nonneg _) (by positivity))
      (gamma_nonneg fp (by simpa [K] using hvalid))
  have hbase :=
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_forwardSub_fl_backSub_of_higham_qr_and_rhs_explicit_backward_error_with_zero_deltag_bound
      fp Q A Rhat f c_hat g
      (gamma fp (n * K) * frobNorm A)
      ((n + k : ℝ) * gamma fp (n * K))
      (householderQRRhsPanelSqrtResidualBackwardBound fp (n + k) n A f)
      (Real.sqrt (n + k : ℝ) * (n : ℝ) * gamma fp (n * K))
      H3 hQR hRhs hcG hH3nonneg hH3norm
      (by simpa [Rhat] using hdiag) hγ
  simpa [Q, Rhat, c_hat, K, H3] using hbase
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with all printed perturbation slots exposed, conditional on the
    remaining `Delta f` source-majorant domination.

    The concrete rounded path supplies the matrix-side `G |A|` witness, the
    original-coordinate `Delta f`, the zero `Delta g` package, and the
    triangular-solve perturbations.  The hypothesis `hDeltafDom` is the still
    open source-facing RHS-transform estimate converting the local scalar
    `householderQRRhsPanelBackwardBound` into
    `sqrt(m) * n * gamma * (H_1 |f| + H_2 |r_hat|)`. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_source_rhs_bounds
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (H1 H2 : Fin (n + k) → Fin (n + k) → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n)
    (hH1nonneg : ∀ i j, 0 ≤ H1 i j)
    (hH2nonneg : ∀ i j, 0 ≤ H2 i j)
    (hH1norm : frobNorm H1 = 1)
    (hH2norm : frobNorm H2 = 1)
    (hDeltafDom :
      let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i) (hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Concrete Householder QR RHS-bound bridge for Theorem 20.4.  It reduces
    the open `Delta f` source-majorant domination to domination by one
    visible `H_1 |f|` component. -/
theorem householderQRRhsPanelBackwardBound_le_lsTheorem20_4DeltafMajorant_of_f_term_dom
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (H1 H2 : Fin (n + k) → Fin (n + k) → ℝ)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hH1nonneg : ∀ i j, 0 ≤ H1 i j)
    (hH2nonneg : ∀ i j, 0 ≤ H2 i j)
    (j0 : Fin (n + k))
    (hdom : ∀ i : Fin (n + k),
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          (H1 i j0 * |f j0|)) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∀ i : Fin (n + k),
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  have hc :
      0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) *
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) := by
    exact mul_nonneg
      (mul_nonneg (Real.sqrt_nonneg _) (Nat.cast_nonneg n))
      (gamma_nonneg fp hvalid)
  exact
    lsTheorem20_4DeltafMajorant_dom_of_f_term_dom H1 H2 f rhat
      (householderQRRhsPanelBackwardBound fp (n + k) n A f)
      (Real.sqrt (n + k : ℝ) * (n : ℝ) *
        gamma fp (n * householderConstructApplyGammaIndex (n + k)))
      hc hH1nonneg hH2nonneg j0 hdom
/-- Concrete Householder QR RHS-bound bridge for Theorem 20.4.  It reduces
    the open `Delta f` source-majorant domination to domination by one
    visible `H_2 |r_hat|` component. -/
theorem householderQRRhsPanelBackwardBound_le_lsTheorem20_4DeltafMajorant_of_rhat_term_dom
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (H1 H2 : Fin (n + k) → Fin (n + k) → ℝ)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hH1nonneg : ∀ i j, 0 ≤ H1 i j)
    (hH2nonneg : ∀ i j, 0 ≤ H2 i j)
    (j0 : Fin (n + k))
    (hdom :
      let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
            (H2 i j0 * |rhat j0|)) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∀ i : Fin (n + k),
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  have hc :
      0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) *
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) := by
    exact mul_nonneg
      (mul_nonneg (Real.sqrt_nonneg _) (Nat.cast_nonneg n))
      (gamma_nonneg fp hvalid)
  have hdom' : ∀ i : Fin (n + k),
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          (H2 i j0 * |rhat j0|) := by
    simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hdom
  exact
    lsTheorem20_4DeltafMajorant_dom_of_rhat_term_dom H1 H2 f rhat
      (householderQRRhsPanelBackwardBound fp (n + k) n A f)
      (Real.sqrt (n + k : ℝ) * (n : ℝ) *
        gamma fp (n * householderConstructApplyGammaIndex (n + k)))
      hc hH1nonneg hH2nonneg j0 hdom'
/-- Concrete uniform-column `H_1` source witness for Theorem 20.4's
    `Delta f` bound.

    If the scalar RHS-transform perturbation is bounded by
    `n * gamma * ‖f‖∞`, and `j0` is a visible max component of `f`, then the
    uniform-column `H_1` and a one-hot `H_2` are nonnegative Frobenius-unit
    witnesses and satisfy the full printed `hDeltafDom` majorant shape. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (j0 : Fin (n + k))
    (hmax : infNormVec f ≤ |f j0|)
    (hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          infNormVec f) :
    let H1 : Fin (n + k) → Fin (n + k) → ℝ :=
      lsTheorem20_4UniformColumnMajorant j0
    let H2 : Fin (n + k) → Fin (n + k) → ℝ :=
      lsTheorem20_4OneHotMajorant j0 j0
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  let H1 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4UniformColumnMajorant j0
  let H2 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4OneHotMajorant j0 j0
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  have hH1nonneg : ∀ i j, 0 ≤ H1 i j := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_nonneg j0
  have hH2nonneg : ∀ i j, 0 ≤ H2 i j := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_nonneg j0 j0
  have hH1norm : frobNorm H1 = 1 := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_frobNorm hm j0
  have hH2norm : frobNorm H2 = 1 := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_frobNorm j0 j0
  have hDeltafDom :
      let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    apply
      householderQRRhsPanelBackwardBound_le_lsTheorem20_4DeltafMajorant_of_f_term_dom
        fp A f g H1 H2 hvalid hH1nonneg hH2nonneg j0
    intro i
    have hngamma :
        0 ≤ (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) := by
      exact mul_nonneg (Nat.cast_nonneg n) (gamma_nonneg fp hvalid)
    have hscale :
        (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
            infNormVec f ≤
          (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
            |f j0| := by
      exact mul_le_mul_of_nonneg_left hmax hngamma
    have hbase :
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
            |f j0| :=
      hInfDom.trans hscale
    have hmR : 0 < (n + k : ℝ) := by exact_mod_cast hm
    have hsqrt_pos : 0 < Real.sqrt (n + k : ℝ) := Real.sqrt_pos.2 hmR
    have hsimpl :
        (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
            |f j0| =
          (Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
            (H1 i j0 * |f j0|) := by
      simp [H1, lsTheorem20_4UniformColumnMajorant]
      field_simp [ne_of_gt hsqrt_pos]
    simpa [hsimpl] using hbase
  exact ⟨hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
/-- Concrete uniform-column `H_1` source witness for Theorem 20.4's
    `Delta f` bound, with the max component chosen internally.

    The only remaining RHS-side hypothesis is the scalar infinity-norm
    domination of the concrete Householder RHS perturbation.  This theorem
    supplies the finite max component required by
    `householderQRRhsPanelBackwardBound_uniform_f_source_witness`, constructs
    `H_1,H_2`, and returns the full printed `hDeltafDom` majorant shape. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_infNorm_dom
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          infNormVec f) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  obtain ⟨j0, hmax_eq⟩ := infNormVec_exists_abs_eq hm f
  let H1 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4UniformColumnMajorant j0
  let H2 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4OneHotMajorant j0 j0
  have hmax : infNormVec f ≤ |f j0| := le_of_eq hmax_eq
  have hw :=
    householderQRRhsPanelBackwardBound_uniform_f_source_witness
      fp A f g hn hvalid j0 hmax hInfDom
  refine ⟨H1, H2, ?_⟩
  simpa [H1, H2] using hw
/-- Uniform-column `H_1` source witness for any nonnegative scalar
    infinity-norm domination of the concrete Householder QR RHS recursion.

    This is the coefficient-parametric version of the printed-constant witness:
    if the implementation-backed RHS perturbation is bounded by
    `eta * infNormVec f`, then the same max-component construction yields the
    full Theorem 20.4 source-majorant shape with coefficient
    `sqrt(n+k) * eta`. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n) (eta : ℝ) (heta : 0 ≤ eta)
    (hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        eta * infNormVec f) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * eta) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  obtain ⟨j0, hmax_eq⟩ := infNormVec_exists_abs_eq hm f
  let H1 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4UniformColumnMajorant j0
  let H2 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4OneHotMajorant j0 j0
  have hmax : infNormVec f ≤ |f j0| := le_of_eq hmax_eq
  have hH1nonneg : ∀ i j, 0 ≤ H1 i j := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_nonneg j0
  have hH2nonneg : ∀ i j, 0 ≤ H2 i j := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_nonneg j0 j0
  have hH1norm : frobNorm H1 = 1 := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_frobNorm hm j0
  have hH2norm : frobNorm H2 = 1 := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_frobNorm j0 j0
  have hDeltafDom :
      let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * eta) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    have hc : 0 ≤ Real.sqrt (n + k : ℝ) * eta := by
      exact mul_nonneg (Real.sqrt_nonneg _) heta
    apply
      lsTheorem20_4DeltafMajorant_dom_of_f_term_dom H1 H2 f rhat
        (householderQRRhsPanelBackwardBound fp (n + k) n A f)
        (Real.sqrt (n + k : ℝ) * eta) hc hH1nonneg hH2nonneg j0
    intro i
    have hscale : eta * infNormVec f ≤ eta * |f j0| := by
      exact mul_le_mul_of_nonneg_left hmax heta
    have hbase :
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          eta * |f j0| :=
      hInfDom.trans hscale
    have hmR : 0 < (n + k : ℝ) := by exact_mod_cast hm
    have hsqrt_pos : 0 < Real.sqrt (n + k : ℝ) := Real.sqrt_pos.2 hmR
    have hsimpl :
        eta * |f j0| =
          (Real.sqrt (n + k : ℝ) * eta) * (H1 i j0 * |f j0|) := by
      simp [H1, lsTheorem20_4UniformColumnMajorant]
      field_simp [ne_of_gt hsqrt_pos]
    simpa [hsimpl] using hbase
  exact ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using the recursive RHS growth coefficient.

    This removes the abstract scalar `hInfDom` obligation by applying
    `householderQRRhsPanelBackwardBound_le_growthCoeff`.  The coefficient is
    the conservative implementation growth coefficient, not Higham's printed
    `n * gamma` constant. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_growthCoeff
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid : gammaValid fp (11 * (n + k) + 23))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelGrowthCoeff fp (n + k) n) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ householderQRRhsPanelGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelGrowthCoeff_nonneg fp (n + k) n hvalid
  have hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        householderQRRhsPanelGrowthCoeff fp (n + k) n * infNormVec f :=
    householderQRRhsPanelBackwardBound_le_growthCoeff fp
      (n + k) n A f hvalid hready
  simpa using
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn (householderQRRhsPanelGrowthCoeff fp (n + k) n) heta hInfDom
/-- Uniform-column `H_1` source witness for any nonnegative scalar
    infinity-norm domination of the sharpened Householder QR RHS recursion.

    This is the sqrt-bound analogue of
    `householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom`.
    If the sharpened implementation-backed RHS perturbation is bounded by
    `eta * infNormVec f`, the same max-component construction yields the
    Theorem 20.4 source-majorant shape with coefficient `sqrt(n+k) * eta`. -/
theorem householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n) (eta : ℝ) (heta : 0 ≤ eta)
    (hInfDom :
      householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
        eta * infNormVec f) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * eta) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  obtain ⟨j0, hmax_eq⟩ := infNormVec_exists_abs_eq hm f
  let H1 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4UniformColumnMajorant j0
  let H2 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4OneHotMajorant j0 j0
  have hmax : infNormVec f ≤ |f j0| := le_of_eq hmax_eq
  have hH1nonneg : ∀ i j, 0 ≤ H1 i j := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_nonneg j0
  have hH2nonneg : ∀ i j, 0 ≤ H2 i j := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_nonneg j0 j0
  have hH1norm : frobNorm H1 = 1 := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_frobNorm hm j0
  have hH2norm : frobNorm H2 = 1 := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_frobNorm j0 j0
  have hDeltafDom :
      let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) * eta) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    have hc : 0 ≤ Real.sqrt (n + k : ℝ) * eta := by
      exact mul_nonneg (Real.sqrt_nonneg _) heta
    apply
      lsTheorem20_4DeltafMajorant_dom_of_f_term_dom H1 H2 f rhat
        (householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f)
        (Real.sqrt (n + k : ℝ) * eta) hc hH1nonneg hH2nonneg j0
    intro i
    have hscale : eta * infNormVec f ≤ eta * |f j0| := by
      exact mul_le_mul_of_nonneg_left hmax heta
    have hbase :
        householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
          eta * |f j0| :=
      hInfDom.trans hscale
    have hmR : 0 < (n + k : ℝ) := by exact_mod_cast hm
    have hsqrt_pos : 0 < Real.sqrt (n + k : ℝ) := Real.sqrt_pos.2 hmR
    have hsimpl :
        eta * |f j0| =
          (Real.sqrt (n + k : ℝ) * eta) * (H1 i j0 * |f j0|) := by
      simp [H1, lsTheorem20_4UniformColumnMajorant]
      field_simp [ne_of_gt hsqrt_pos]
    simpa [hsimpl] using hbase
  exact ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using the sharpened recursive RHS growth
    coefficient.

    This removes the abstract scalar `hInfDom` obligation by applying
    `householderQRRhsPanelSqrtBackwardBound_le_sqrtGrowthCoeff`.  The resulting
    coefficient is conservative and implementation-derived; it is a sharpened
    bound for the current fixed-`Q` RHS transport path, not Higham's printed
    `n * gamma` constant. -/
theorem householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_sqrtGrowthCoeff
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid : gammaValid fp (11 * (n + k) + 23))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelSqrtGrowthCoeff_nonneg fp (n + k) n hvalid
  have hInfDom :
      householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
        householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n * infNormVec f :=
    householderQRRhsPanelSqrtBackwardBound_le_sqrtGrowthCoeff fp
      (n + k) n A f hvalid hready
  simpa using
    householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn (householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n)
      heta hInfDom
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using the sharper recursive RHS growth coefficient.

    Compared with
    `householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_sqrtGrowthCoeff`,
    this consumes
    `householderQRRhsPanelSqrtBackwardBound_le_sqrtGrowthCoeffSharp`, whose
    coefficient also sharpens the intermediate rounded-vector growth estimate
    for each one-reflector RHS step. -/
theorem householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_sqrtGrowthCoeffSharp
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid : gammaValid fp (11 * (n + k) + 23))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n :=
    householderQRRhsPanelSqrtGrowthCoeffSharp_nonneg fp (n + k) n hvalid
  have hInfDom :
      householderQRRhsPanelSqrtBackwardBound fp (n + k) n A f ≤
        householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n *
          infNormVec f :=
    householderQRRhsPanelSqrtBackwardBound_le_sqrtGrowthCoeffSharp fp
      (n + k) n A f hvalid hready
  simpa using
    householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn
      (householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n)
      heta hInfDom
/-- Generic uniform-column `H_1` source witness for Theorem 20.4's `Delta f`
    source-majorant shape from any scalar RHS budget dominated by
    `eta * ||f||∞`.

    This packages the max-component construction independently of the concrete
    QR RHS budget, so sharper implementation budgets can reuse the same source
    witness argument. -/
theorem lsTheorem20_4DeltafBound_uniform_f_source_witness_of_scalar_infNorm_dom
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n) (beta eta : ℝ) (heta : 0 ≤ eta)
    (hInfDom : beta ≤ eta * infNormVec f) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        beta ≤
          (Real.sqrt (n + k : ℝ) * eta) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  obtain ⟨j0, hmax_eq⟩ := infNormVec_exists_abs_eq hm f
  let H1 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4UniformColumnMajorant j0
  let H2 : Fin (n + k) → Fin (n + k) → ℝ :=
    lsTheorem20_4OneHotMajorant j0 j0
  have hmax : infNormVec f ≤ |f j0| := le_of_eq hmax_eq
  have hH1nonneg : ∀ i j, 0 ≤ H1 i j := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_nonneg j0
  have hH2nonneg : ∀ i j, 0 ≤ H2 i j := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_nonneg j0 j0
  have hH1norm : frobNorm H1 = 1 := by
    simpa [H1] using lsTheorem20_4UniformColumnMajorant_frobNorm hm j0
  have hH2norm : frobNorm H2 = 1 := by
    simpa [H2] using lsTheorem20_4OneHotMajorant_frobNorm j0 j0
  have hDeltafDom :
      let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        beta ≤
          (Real.sqrt (n + k : ℝ) * eta) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    have hc : 0 ≤ Real.sqrt (n + k : ℝ) * eta := by
      exact mul_nonneg (Real.sqrt_nonneg _) heta
    apply
      lsTheorem20_4DeltafMajorant_dom_of_f_term_dom H1 H2 f rhat
        beta (Real.sqrt (n + k : ℝ) * eta) hc hH1nonneg hH2nonneg j0
    intro i
    have hscale : eta * infNormVec f ≤ eta * |f j0| := by
      exact mul_le_mul_of_nonneg_left hmax heta
    have hbase : beta ≤ eta * |f j0| :=
      hInfDom.trans hscale
    have hmR : 0 < (n + k : ℝ) := by exact_mod_cast hm
    have hsqrt_pos : 0 < Real.sqrt (n + k : ℝ) := Real.sqrt_pos.2 hmR
    have hsimpl :
        eta * |f j0| =
          (Real.sqrt (n + k : ℝ) * eta) * (H1 i j0 * |f j0|) := by
      simp [H1, lsTheorem20_4UniformColumnMajorant]
      field_simp [ne_of_gt hsqrt_pos]
    simpa [hsimpl] using hbase
  exact ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using the residual-sharpened recursive RHS growth
    coefficient. -/
theorem householderQRRhsPanelSqrtResidualBackwardBound_uniform_f_source_witness_of_sqrtResidualGrowthCoeff
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid : gammaValid fp (11 * (n + k) + 23))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelSqrtResidualBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelSqrtResidualGrowthCoeff_nonneg fp (n + k) n hvalid
  have hInfDom :
      householderQRRhsPanelSqrtResidualBackwardBound fp (n + k) n A f ≤
        householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n *
          infNormVec f :=
    householderQRRhsPanelSqrtResidualBackwardBound_le_growthCoeff fp
      (n + k) n A f hvalid hready
  simpa using
    lsTheorem20_4DeltafBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn
      (householderQRRhsPanelSqrtResidualBackwardBound fp (n + k) n A f)
      (householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n)
      heta hInfDom
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using the nonrecursive closed RHS growth
    coefficient.

    The coefficient is a conservative derived implementation coefficient, not
    Higham's printed `n * gamma` constant. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_closedGrowth
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid : gammaValid fp (11 * (n + k) + 23))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelClosedGrowthCoeff fp (n + k) n) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ householderQRRhsPanelClosedGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelClosedGrowthCoeff_nonneg fp (n + k) n hvalid
  have hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        householderQRRhsPanelClosedGrowthCoeff fp (n + k) n * infNormVec f :=
    householderQRRhsPanelBackwardBound_le_closedGrowth fp
      (n + k) n A f (Nat.le_add_right n k) hvalid hready
  simpa using
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn (householderQRRhsPanelClosedGrowthCoeff fp (n + k) n)
      heta hInfDom
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using a gamma-only nonrecursive closed RHS growth
    coefficient.

    This replaces the one-step construction/application coefficient in the
    public bound by `gamma fp (householderConstructApplyGammaIndex (n+k))`.
    The result is still a conservative implementation coefficient, not Higham's
    printed `n * gamma` constant. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_gammaClosedGrowth
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid : gammaValid fp (householderConstructApplyGammaIndex (n + k)))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelGammaClosedGrowthCoeff_nonneg fp (n + k) n hvalid
  have hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n *
          infNormVec f :=
    householderQRRhsPanelBackwardBound_le_gammaClosedGrowth fp
      (n + k) n A f (Nat.le_add_right n k) hvalid hready
  simpa using
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn
      (householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n)
      heta hInfDom
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using a single accumulated `gamma` index.

    The index is the conservative
    `householderQRRhsPanelGammaClosedGrowthIndex (n+k) n`, obtained by
    absorbing the gamma-only closed RHS growth coefficient into one Higham
    `gamma` term.  This still does not prove the printed `n * gamma`
    constant. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_gammaIndex
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            gamma fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n)) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  have heta :
      0 ≤ gamma fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n) :=
    gamma_nonneg fp hvalid
  have hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        gamma fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n) *
          infNormVec f :=
    householderQRRhsPanelBackwardBound_le_gammaClosedGrowthIndex fp
      (n + k) n A f (Nat.le_add_right n k) hvalid hready
  simpa using
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_scalar_infNorm_dom
      fp A f g hn
      (gamma fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n))
      heta hInfDom
/-- Implementation-backed uniform-column `H_1` source witness for Theorem
    20.4's `Delta f` bound using an explicit dimension-only factor times the
    printed panel gamma radius.

    The coefficient is
    `2 * householderQRRhsPanelGammaClosedGrowthFactor (n+k) n *
      gamma fp (n * householderConstructApplyGammaIndex (n+k))`.  This exposes
    the remaining gap to Higham's printed `n * gamma` constant as a visible
    factor, instead of hiding it inside the conservative accumulated index. -/
theorem householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_gammaFactor
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hhalf :
      ((householderQRRhsPanelGammaClosedGrowthIndex (n + k) n : ℝ) *
        fp.u ≤ 1 / 2))
    (hready : HouseholderQRPanelReady fp (n + k) n A) :
    ∃ H1 H2 : Fin (n + k) → Fin (n + k) → ℝ,
      (∀ i j, 0 ≤ H1 i j) ∧
      (∀ i j, 0 ≤ H2 i j) ∧
      frobNorm H1 = 1 ∧
      frobNorm H2 = 1 ∧
      (let Q : Fin (n + k) → Fin (n + k) → ℝ :=
        fl_householderQRPanel_Q fp (n + k) n A
      let Rhat : Fin (n + k) → Fin n → ℝ :=
        fl_householderQRPanel_R fp (n + k) n A
      let R : Fin n → Fin n → ℝ :=
        fun i j => Rhat (Fin.castAdd k i) j
      let c_hat : Fin (n + k) → ℝ :=
        fl_householderQRPanel_rhs fp (n + k) n A f
      let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
      let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
      let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
      ∀ i : Fin (n + k),
        householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
          (Real.sqrt (n + k : ℝ) *
            ((2 : ℝ) *
              (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
              gamma fp (n * householderConstructApplyGammaIndex (n + k)))) *
            lsTheorem20_4DeltafMajorant H1 H2 f rhat i) := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let idx : ℕ := householderQRRhsPanelGammaClosedGrowthIndex (n + k) n
  have hvalid : gammaValid fp idx := by
    unfold gammaValid
    exact lt_of_le_of_lt (by simpa [idx] using hhalf) (by norm_num)
  rcases
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_gammaIndex
      fp A f g hn (by simpa [idx] using hvalid) hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  refine ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, ?_⟩
  dsimp only at hDeltafDom ⊢
  intro i
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  have hgamma_le :
      gamma fp idx ≤
        (2 : ℝ) *
          (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) := by
    simpa [idx] using
      householderQRRhsPanelGammaClosedGrowthIndex_gamma_le_factor_printedGamma
        fp (n + k) n hm hhalf
  have hscale :
      Real.sqrt (n + k : ℝ) * gamma fp idx ≤
        Real.sqrt (n + k : ℝ) *
          ((2 : ℝ) *
            (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) := by
    exact mul_le_mul_of_nonneg_left hgamma_le (Real.sqrt_nonneg _)
  have hmaj_nonneg :
      0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
    lsTheorem20_4DeltafMajorant_nonneg H1 H2 f rhat
      hH1nonneg hH2nonneg i
  exact le_trans
    (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat, idx] using hDeltafDom i)
    (by
      simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using
        mul_le_mul_of_nonneg_right hscale hmaj_nonneg)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with the uniform `Delta f` source witnesses chosen internally.

    Compared with
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_source_rhs_bounds`,
    this theorem removes the explicit `H1,H2,hDeltafDom` inputs.  The only
    remaining RHS-side source obligation is the scalar infinity-norm bound
    `hInfDom` for the concrete Householder RHS recursion. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_uniform_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n)
    (hInfDom :
      householderQRRhsPanelBackwardBound fp (n + k) n A f ≤
        (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          infNormVec f) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  rcases
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_infNorm_dom
      fp A f g hn hvalid hInfDom with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  simpa using
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_source_rhs_bounds
      fp A f g H1 H2 hn hvalid hdiag hγ
      hH1nonneg hH2nonneg hH1norm hH2norm hDeltafDom
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses.

    Unlike the printed-constant wrapper above, this theorem does not assume a
    separate RHS infinity-norm domination hypothesis.  It uses the verified
    recursive Householder RHS growth coefficient in the `Delta f` source bound,
    while keeping the printed matrix-side and `Delta g` coefficients. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_growth_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  rcases
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_growthCoeff
      fp A f g hn hbase_valid hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i)
      (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and
    the sharpened fixed-`Q` RHS growth coefficient.

    Compared with
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_growth_source_rhs_bound`,
    this theorem uses the `householderQRRhsPanelSqrtBackwardBound` path and
    the corresponding `householderQRRhsPanelSqrtGrowthCoeff` in the source-side
    `Delta f` coefficient.  The matrix-side and `Delta g` coefficients remain
    the printed Theorem 20.4 shapes. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sqrt_growth_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  rcases
    householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_sqrtGrowthCoeff
      fp A f g hn hbase_valid hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound_sqrt
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i)
      (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and
    the sharper fixed-`Q` RHS growth coefficient.

    Compared with
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sqrt_growth_source_rhs_bound`,
    this theorem replaces `householderQRRhsPanelSqrtGrowthCoeff` by
    `householderQRRhsPanelSqrtGrowthCoeffSharp`, whose recurrence also uses
    the `sqrt(m+1)` bound for intermediate rounded-vector growth.  The
    matrix-side and `Delta g` coefficients remain the printed Theorem 20.4
    shapes. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sharp_sqrt_growth_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  rcases
    householderQRRhsPanelSqrtBackwardBound_uniform_f_source_witness_of_sqrtGrowthCoeffSharp
      fp A f g hn hbase_valid hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound_sqrt
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i)
      (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and
    the residual-sharpened fixed-`Q` RHS growth coefficient.

    Compared with
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sharp_sqrt_growth_source_rhs_bound`,
    this theorem replaces `householderQRRhsPanelSqrtGrowthCoeffSharp` by
    `householderQRRhsPanelSqrtResidualGrowthCoeff`, whose recurrence also uses
    the `sqrt(m+1)` bound for the one-reflector rounded RHS residual. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sqrt_residual_growth_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  rcases
    householderQRRhsPanelSqrtResidualBackwardBound_uniform_f_source_witness_of_sqrtResidualGrowthCoeff
      fp A f g hn hbase_valid hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound_sqrt_residual
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i)
      (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Explicit conservative `γ̃` coefficient for the concrete Householder QR
    augmented-system solve in Higham, Chapter 20, Theorem 20.4.

    Higham states Theorem 20.4 with a tilde gamma `γ̃_m`, hiding a modest
    implementation-dependent constant.  This repository-facing coefficient
    makes that hidden constant explicit for the current model by dominating both
    the matrix-side QR gamma and the sharpened fixed-`Q` RHS growth coefficient. -/
noncomputable def lsTheorem20_4ConcreteGammaTilde (fp : FPModel)
    (m n : ℕ) : ℝ :=
  gamma fp (n * householderConstructApplyGammaIndex m) +
    householderQRRhsPanelSqrtGrowthCoeff fp m n
/-- The global Householder gamma-validity assumption used in the concrete
    Theorem 20.4 handoff also validates the fixed-`Q` RHS coefficient base. -/
theorem lsTheorem20_4ConcreteGammaTilde_base_gammaValid
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    gammaValid fp (11 * m + 23) := by
  let K : ℕ := householderConstructApplyGammaIndex m
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * m + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  exact gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
    simpa [K] using hvalid)
/-- Higham, Chapter 20, Theorem 20.4 concrete `γ̃` is nonnegative under the
    same gamma-validity hypothesis as the Householder QR handoff. -/
theorem lsTheorem20_4ConcreteGammaTilde_nonneg
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    0 ≤ lsTheorem20_4ConcreteGammaTilde fp m n := by
  have hbase_valid :
      gammaValid fp (11 * m + 23) :=
    lsTheorem20_4ConcreteGammaTilde_base_gammaValid fp hn hvalid
  exact add_nonneg (gamma_nonneg fp hvalid)
    (householderQRRhsPanelSqrtGrowthCoeff_nonneg fp m n hbase_valid)
/-- The matrix-side QR gamma is dominated by the explicit concrete `γ̃` used in
    the Chapter 20, Theorem 20.4 Householder QR handoff. -/
theorem gamma_le_lsTheorem20_4ConcreteGammaTilde
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    gamma fp (n * householderConstructApplyGammaIndex m) ≤
      lsTheorem20_4ConcreteGammaTilde fp m n := by
  have hbase_valid :
      gammaValid fp (11 * m + 23) :=
    lsTheorem20_4ConcreteGammaTilde_base_gammaValid fp hn hvalid
  dsimp [lsTheorem20_4ConcreteGammaTilde]
  exact le_add_of_nonneg_right
    (householderQRRhsPanelSqrtGrowthCoeff_nonneg fp m n hbase_valid)
/-- The fixed-`Q` RHS growth coefficient is dominated by the explicit concrete
    `γ̃` used in the Chapter 20, Theorem 20.4 Householder QR handoff. -/
theorem householderQRRhsPanelSqrtGrowthCoeff_le_lsTheorem20_4ConcreteGammaTilde
    (fp : FPModel) {m n : ℕ}
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    householderQRRhsPanelSqrtGrowthCoeff fp m n ≤
      lsTheorem20_4ConcreteGammaTilde fp m n := by
  dsimp [lsTheorem20_4ConcreteGammaTilde]
  exact le_add_of_nonneg_left (gamma_nonneg fp hvalid)
/-- Sharper explicit conservative `γ̃` coefficient for the concrete
    Householder QR augmented-system solve in Higham, Chapter 20, Theorem 20.4.

    This is the same presentation idea as `lsTheorem20_4ConcreteGammaTilde`,
    but it uses `householderQRRhsPanelSqrtGrowthCoeffSharp`, whose recurrence
    also improves intermediate rounded-vector growth. -/
noncomputable def lsTheorem20_4ConcreteGammaTildeSharp (fp : FPModel)
    (m n : ℕ) : ℝ :=
  gamma fp (n * householderConstructApplyGammaIndex m) +
    householderQRRhsPanelSqrtGrowthCoeffSharp fp m n
/-- The global Householder gamma-validity assumption used in the concrete
    Theorem 20.4 handoff also validates the sharp fixed-`Q` RHS coefficient base. -/
theorem lsTheorem20_4ConcreteGammaTildeSharp_base_gammaValid
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    gammaValid fp (11 * m + 23) :=
  lsTheorem20_4ConcreteGammaTilde_base_gammaValid fp hn hvalid
/-- Higham, Chapter 20, Theorem 20.4 sharp concrete `γ̃` is nonnegative under
    the same gamma-validity hypothesis as the Householder QR handoff. -/
theorem lsTheorem20_4ConcreteGammaTildeSharp_nonneg
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    0 ≤ lsTheorem20_4ConcreteGammaTildeSharp fp m n := by
  have hbase_valid :
      gammaValid fp (11 * m + 23) :=
    lsTheorem20_4ConcreteGammaTildeSharp_base_gammaValid fp hn hvalid
  exact add_nonneg (gamma_nonneg fp hvalid)
    (householderQRRhsPanelSqrtGrowthCoeffSharp_nonneg fp m n hbase_valid)
/-- The matrix-side QR gamma is dominated by the sharp explicit concrete `γ̃`
    used in the Chapter 20, Theorem 20.4 Householder QR handoff. -/
theorem gamma_le_lsTheorem20_4ConcreteGammaTildeSharp
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    gamma fp (n * householderConstructApplyGammaIndex m) ≤
      lsTheorem20_4ConcreteGammaTildeSharp fp m n := by
  have hbase_valid :
      gammaValid fp (11 * m + 23) :=
    lsTheorem20_4ConcreteGammaTildeSharp_base_gammaValid fp hn hvalid
  dsimp [lsTheorem20_4ConcreteGammaTildeSharp]
  exact le_add_of_nonneg_right
    (householderQRRhsPanelSqrtGrowthCoeffSharp_nonneg fp m n hbase_valid)
/-- The sharp fixed-`Q` RHS growth coefficient is dominated by the sharp
    explicit concrete `γ̃` used in the Chapter 20, Theorem 20.4 handoff. -/
theorem householderQRRhsPanelSqrtGrowthCoeffSharp_le_lsTheorem20_4ConcreteGammaTildeSharp
    (fp : FPModel) {m n : ℕ}
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    householderQRRhsPanelSqrtGrowthCoeffSharp fp m n ≤
      lsTheorem20_4ConcreteGammaTildeSharp fp m n := by
  dsimp [lsTheorem20_4ConcreteGammaTildeSharp]
  exact le_add_of_nonneg_left (gamma_nonneg fp hvalid)
/-- Residual-sharpened explicit conservative `γ̃` coefficient for the concrete
    Householder QR augmented-system solve in Higham, Chapter 20,
    Theorem 20.4.

    This uses `householderQRRhsPanelSqrtResidualGrowthCoeff`, whose recurrence
    sharpens both intermediate rounded-vector growth and the one-reflector RHS
    residual estimate. -/
noncomputable def lsTheorem20_4ConcreteGammaTildeSqrtResidual
    (fp : FPModel) (m n : ℕ) : ℝ :=
  gamma fp (n * householderConstructApplyGammaIndex m) +
    householderQRRhsPanelSqrtResidualGrowthCoeff fp m n
/-- The global Householder gamma-validity assumption used in the concrete
    Theorem 20.4 handoff also validates the residual-sharpened fixed-`Q` RHS
    coefficient base. -/
theorem lsTheorem20_4ConcreteGammaTildeSqrtResidual_base_gammaValid
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    gammaValid fp (11 * m + 23) :=
  lsTheorem20_4ConcreteGammaTilde_base_gammaValid fp hn hvalid
/-- Higham, Chapter 20, Theorem 20.4 residual-sharpened concrete `γ̃` is
    nonnegative under the same gamma-validity hypothesis as the Householder QR
    handoff. -/
theorem lsTheorem20_4ConcreteGammaTildeSqrtResidual_nonneg
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    0 ≤ lsTheorem20_4ConcreteGammaTildeSqrtResidual fp m n := by
  have hbase_valid :
      gammaValid fp (11 * m + 23) :=
    lsTheorem20_4ConcreteGammaTildeSqrtResidual_base_gammaValid fp
      hn hvalid
  exact add_nonneg (gamma_nonneg fp hvalid)
    (householderQRRhsPanelSqrtResidualGrowthCoeff_nonneg fp m n hbase_valid)
/-- The matrix-side QR gamma is dominated by the residual-sharpened explicit
    concrete `γ̃` used in the Chapter 20, Theorem 20.4 Householder QR handoff. -/
theorem gamma_le_lsTheorem20_4ConcreteGammaTildeSqrtResidual
    (fp : FPModel) {m n : ℕ} (hn : 0 < n)
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    gamma fp (n * householderConstructApplyGammaIndex m) ≤
      lsTheorem20_4ConcreteGammaTildeSqrtResidual fp m n := by
  have hbase_valid :
      gammaValid fp (11 * m + 23) :=
    lsTheorem20_4ConcreteGammaTildeSqrtResidual_base_gammaValid fp
      hn hvalid
  dsimp [lsTheorem20_4ConcreteGammaTildeSqrtResidual]
  exact le_add_of_nonneg_right
    (householderQRRhsPanelSqrtResidualGrowthCoeff_nonneg fp m n hbase_valid)
/-- The residual-sharpened fixed-`Q` RHS growth coefficient is dominated by
    the residual-sharpened explicit concrete `γ̃` used in the Chapter 20,
    Theorem 20.4 handoff. -/
theorem householderQRRhsPanelSqrtResidualGrowthCoeff_le_lsTheorem20_4ConcreteGammaTildeSqrtResidual
    (fp : FPModel) {m n : ℕ}
    (hvalid : gammaValid fp (n * householderConstructApplyGammaIndex m)) :
    householderQRRhsPanelSqrtResidualGrowthCoeff fp m n ≤
      lsTheorem20_4ConcreteGammaTildeSqrtResidual fp m n := by
  dsimp [lsTheorem20_4ConcreteGammaTildeSqrtResidual]
  exact le_add_of_nonneg_left (gamma_nonneg fp hvalid)
/-- Computed-QR nonbreakdown domain for the concrete Chapter 20,
    Theorem 20.4 Householder path.

    Higham's printed theorem assumes source full column rank.  The verified
    implementation-backed triangular solves also require the computed top
    square `R` diagonal to be nonzero; this predicate names that finite-
    precision domain condition instead of leaving it as an anonymous `hdiag`. -/
def lsTheorem20_4ComputedQRNonbreakdown (fp : FPModel) {n k : ℕ}
    (A : Fin (n + k) → Fin n → ℝ) : Prop :=
  ∀ i : Fin n,
    fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0
/-- Source-facing domain for the concrete Chapter 20, Theorem 20.4
    Householder path: the printed full-column-rank hypothesis plus the
    computed nonbreakdown side condition consumed by triangular solves. -/
def lsTheorem20_4FullRankComputedQRDomain (fp : FPModel) {n k : ℕ}
    (A : Fin (n + k) → Fin n → ℝ) : Prop :=
  lsRealRectColRank A = n ∧ lsTheorem20_4ComputedQRNonbreakdown fp A
theorem lsTheorem20_4FullRankComputedQRDomain.fullRank (fp : FPModel)
    {n k : ℕ} {A : Fin (n + k) → Fin n → ℝ}
    (h : lsTheorem20_4FullRankComputedQRDomain fp A) :
    lsRealRectColRank A = n :=
  h.1
theorem lsTheorem20_4FullRankComputedQRDomain.computedQRNonbreakdown
    (fp : FPModel) {n k : ℕ} {A : Fin (n + k) → Fin n → ℝ}
    (h : lsTheorem20_4FullRankComputedQRDomain fp A) :
    lsTheorem20_4ComputedQRNonbreakdown fp A :=
  h.2
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with an explicit conservative `γ̃` coefficient.

    This is the source-shaped closure corresponding to Higham's printed
    `mn γ̃_m`, `sqrt(m)n γ̃_m` bounds.  The `γ̃` is not identified with the
    sharper printed-looking `gamma fp (n * householderConstructApplyGammaIndex
    m)`; it is the explicit conservative coefficient
    `lsTheorem20_4ConcreteGammaTilde`, which is large enough to cover the
    verified fixed-`Q` RHS path. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTilde
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTilde fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let gammaTilde : ℝ :=
    lsTheorem20_4ConcreteGammaTilde fp (n + k) n
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hgamma_nonneg :
      0 ≤ gamma fp (n * householderConstructApplyGammaIndex (n + k)) :=
    gamma_nonneg fp hvalid
  have hsqrtCoeff_nonneg :
      0 ≤ householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelSqrtGrowthCoeff_nonneg fp (n + k) n hbase_valid
  have hgamma_le_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        gammaTilde := by
    dsimp [gammaTilde, lsTheorem20_4ConcreteGammaTilde]
    exact le_add_of_nonneg_right hsqrtCoeff_nonneg
  have hsqrtCoeff_le_tilde :
      householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n ≤ gammaTilde := by
    dsimp [gammaTilde, lsTheorem20_4ConcreteGammaTilde]
    exact le_add_of_nonneg_left hgamma_nonneg
  have htilde_nonneg : 0 ≤ gammaTilde := by
    exact le_trans hgamma_nonneg hgamma_le_tilde
  have htilde_le_n_tilde : gammaTilde ≤ (n : ℝ) * gammaTilde := by
    have hn1 : (1 : ℝ) ≤ (n : ℝ) := by
      exact_mod_cast Nat.succ_le_of_lt hn
    calc
      gammaTilde = (1 : ℝ) * gammaTilde := by ring
      _ ≤ (n : ℝ) * gammaTilde :=
          mul_le_mul_of_nonneg_right hn1 htilde_nonneg
  have hgamma_le_n_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        (n : ℝ) * gammaTilde :=
    le_trans hgamma_le_tilde htilde_le_n_tilde
  have hsqrtCoeff_le_n_tilde :
      householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n ≤
        (n : ℝ) * gammaTilde :=
    le_trans hsqrtCoeff_le_tilde htilde_le_n_tilde
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sqrt_growth_source_rhs_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH1nonneg,
      hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm, hDeltaR1,
      hDeltaR2, hsys⟩
  have hDeltaAcomp_tilde : ∀ i j,
      |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j := by
    intro i j
    have hmaj_nonneg :
        0 ≤ matMulRect (n + k) (n + k) n G
          (fun a b => |A a b|) i j := by
      unfold matMulRect
      exact Finset.sum_nonneg (by
        intro t _
        exact mul_nonneg (hGnonneg i t) (abs_nonneg _))
    have hcoeff :
        (n + k : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      calc
        (n + k : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          (n + k : ℝ) * ((n : ℝ) * gammaTilde) :=
            mul_le_mul_of_nonneg_left hgamma_le_n_tilde (by positivity)
        _ = (n + k : ℝ) * (n : ℝ) * gammaTilde := by ring
    exact le_trans (hDeltaAcomp i j)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  have hDeltaf_tilde : ∀ i,
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
      lsTheorem20_4DeltafMajorant_nonneg H1 H2 f rhat
        hH1nonneg hH2nonneg i
    have hcoeff :
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      calc
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtGrowthCoeff fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * ((n : ℝ) * gammaTilde) :=
            mul_le_mul_of_nonneg_left hsqrtCoeff_le_n_tilde
              (Real.sqrt_nonneg _)
        _ = Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by ring
    exact le_trans (hDeltaf i)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  have hDeltag_tilde : ∀ j,
      |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j := by
    intro j
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltagMajorant A H3 rhat j :=
      lsTheorem20_4DeltagMajorant_nonneg A H3 rhat hH3nonneg j
    have hcoeff :
        Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      have hleft_nonneg : 0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) :=
        mul_nonneg (Real.sqrt_nonneg _) (Nat.cast_nonneg n)
      exact mul_le_mul_of_nonneg_left hgamma_le_tilde hleft_nonneg
    exact le_trans (hDeltag j)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp_tilde, hDeltaf_tilde,
    hDeltag_tilde, hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm,
    hH3norm, hDeltaR1, hDeltaR2, ?_⟩
  simpa [gammaTilde, Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with the normwise matrix perturbation also stated using the
    explicit conservative `γ̃` coefficient.

    This is a presentation wrapper around the concrete `γ̃` theorem above.  It
    weakens the first Frobenius bound from the matrix-side QR gamma to `γ̃`, so
    all displayed perturbation budgets in the source-facing handoff use the same
    conservative coefficient. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTilde_unified_frob_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTilde fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ gammaTilde * frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let gammaTilde : ℝ :=
    lsTheorem20_4ConcreteGammaTilde fp (n + k) n
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  have hgamma_le_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        gammaTilde := by
    simpa [gammaTilde] using
      (gamma_le_lsTheorem20_4ConcreteGammaTilde fp
        (m := n + k) (n := n) hn hvalid)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTilde
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
      hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag,
      hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
      hDeltaR1, hDeltaR2, hsys⟩
  have hDeltaA_tilde :
      frobNorm DeltaA ≤ gammaTilde * frobNorm A := by
    exact le_trans hDeltaA
      (mul_le_mul_of_nonneg_right hgamma_le_tilde (frobNorm_nonneg A))
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA_tilde, hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [gammaTilde, Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with all displayed perturbation budgets stated using the sharper
    explicit conservative `γ̃` coefficient.

    This is the sharp-coefficient analogue of
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTilde_unified_frob_bound`.
    It uses `lsTheorem20_4ConcreteGammaTildeSharp`, whose RHS component is
    `householderQRRhsPanelSqrtGrowthCoeffSharp`. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTildeSharp_unified_frob_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTildeSharp fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ gammaTilde * frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let gammaTilde : ℝ :=
    lsTheorem20_4ConcreteGammaTildeSharp fp (n + k) n
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    lsTheorem20_4ConcreteGammaTildeSharp_base_gammaValid fp
      (m := n + k) (n := n) hn hvalid
  have hgamma_nonneg :
      0 ≤ gamma fp (n * householderConstructApplyGammaIndex (n + k)) :=
    gamma_nonneg fp hvalid
  have hsharpCoeff_nonneg :
      0 ≤ householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n :=
    householderQRRhsPanelSqrtGrowthCoeffSharp_nonneg fp (n + k) n
      hbase_valid
  have hgamma_le_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        gammaTilde := by
    simpa [gammaTilde] using
      (gamma_le_lsTheorem20_4ConcreteGammaTildeSharp fp
        (m := n + k) (n := n) hn hvalid)
  have hsharpCoeff_le_tilde :
      householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n ≤
        gammaTilde := by
    simpa [gammaTilde] using
      (householderQRRhsPanelSqrtGrowthCoeffSharp_le_lsTheorem20_4ConcreteGammaTildeSharp
        fp (m := n + k) (n := n) hvalid)
  have htilde_nonneg : 0 ≤ gammaTilde := by
    exact le_trans hgamma_nonneg hgamma_le_tilde
  have htilde_le_n_tilde : gammaTilde ≤ (n : ℝ) * gammaTilde := by
    have hn1 : (1 : ℝ) ≤ (n : ℝ) := by
      exact_mod_cast Nat.succ_le_of_lt hn
    calc
      gammaTilde = (1 : ℝ) * gammaTilde := by ring
      _ ≤ (n : ℝ) * gammaTilde :=
          mul_le_mul_of_nonneg_right hn1 htilde_nonneg
  have hgamma_le_n_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        (n : ℝ) * gammaTilde :=
    le_trans hgamma_le_tilde htilde_le_n_tilde
  have hsharpCoeff_le_n_tilde :
      householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n ≤
        (n : ℝ) * gammaTilde :=
    le_trans hsharpCoeff_le_tilde htilde_le_n_tilde
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sharp_sqrt_growth_source_rhs_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH1nonneg,
      hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm, hDeltaR1,
      hDeltaR2, hsys⟩
  have hDeltaA_tilde :
      frobNorm DeltaA ≤ gammaTilde * frobNorm A := by
    exact le_trans hDeltaA
      (mul_le_mul_of_nonneg_right hgamma_le_tilde (frobNorm_nonneg A))
  have hDeltaAcomp_tilde : ∀ i j,
      |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j := by
    intro i j
    have hmaj_nonneg :
        0 ≤ matMulRect (n + k) (n + k) n G
          (fun a b => |A a b|) i j := by
      unfold matMulRect
      exact Finset.sum_nonneg (by
        intro t _
        exact mul_nonneg (hGnonneg i t) (abs_nonneg _))
    have hcoeff :
        (n + k : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      calc
        (n + k : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          (n + k : ℝ) * ((n : ℝ) * gammaTilde) :=
            mul_le_mul_of_nonneg_left hgamma_le_n_tilde (by positivity)
        _ = (n + k : ℝ) * (n : ℝ) * gammaTilde := by ring
    exact le_trans (hDeltaAcomp i j)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  have hDeltaf_tilde : ∀ i,
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
      lsTheorem20_4DeltafMajorant_nonneg H1 H2 f rhat
        hH1nonneg hH2nonneg i
    have hcoeff :
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      calc
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtGrowthCoeffSharp fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * ((n : ℝ) * gammaTilde) :=
            mul_le_mul_of_nonneg_left hsharpCoeff_le_n_tilde
              (Real.sqrt_nonneg _)
        _ = Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by ring
    exact le_trans (hDeltaf i)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  have hDeltag_tilde : ∀ j,
      |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j := by
    intro j
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltagMajorant A H3 rhat j :=
      lsTheorem20_4DeltagMajorant_nonneg A H3 rhat hH3nonneg j
    have hcoeff :
        Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      have hleft_nonneg : 0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) :=
        mul_nonneg (Real.sqrt_nonneg _) (Nat.cast_nonneg n)
      exact mul_le_mul_of_nonneg_left hgamma_le_tilde hleft_nonneg
    exact le_trans (hDeltag j)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA_tilde, hGnonneg, hGnorm, hDeltaAcomp_tilde, hDeltaf_tilde,
    hDeltag_tilde, hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm,
    hH3norm, hDeltaR1, hDeltaR2, ?_⟩
  simpa [gammaTilde, Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with all displayed perturbation budgets stated using the
    residual-sharpened explicit conservative `γ̃` coefficient.

    This is the residual-sharpened analogue of
    `exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTildeSharp_unified_frob_bound`.
    It uses `lsTheorem20_4ConcreteGammaTildeSqrtResidual`, whose RHS component
    is `householderQRRhsPanelSqrtResidualGrowthCoeff`. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTildeSqrtResidual_unified_frob_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ gammaTilde * frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let gammaTilde : ℝ :=
    lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  have hgamma_le_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        gammaTilde := by
    simpa [gammaTilde] using
      (gamma_le_lsTheorem20_4ConcreteGammaTildeSqrtResidual fp
        (m := n + k) (n := n) hn hvalid)
  have hresCoeff_le_tilde :
      householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n ≤
        gammaTilde := by
    simpa [gammaTilde] using
      (householderQRRhsPanelSqrtResidualGrowthCoeff_le_lsTheorem20_4ConcreteGammaTildeSqrtResidual
        fp (m := n + k) (n := n) hvalid)
  have htilde_nonneg : 0 ≤ gammaTilde := by
    simpa [gammaTilde] using
      (lsTheorem20_4ConcreteGammaTildeSqrtResidual_nonneg fp
        (m := n + k) (n := n) hn hvalid)
  have htilde_le_n_tilde : gammaTilde ≤ (n : ℝ) * gammaTilde := by
    have hn1 : (1 : ℝ) ≤ (n : ℝ) := by
      exact_mod_cast Nat.succ_le_of_lt hn
    calc
      gammaTilde = (1 : ℝ) * gammaTilde := by ring
      _ ≤ (n : ℝ) * gammaTilde :=
          mul_le_mul_of_nonneg_right hn1 htilde_nonneg
  have hgamma_le_n_tilde :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
        (n : ℝ) * gammaTilde :=
    le_trans hgamma_le_tilde htilde_le_n_tilde
  have hresCoeff_le_n_tilde :
      householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n ≤
        (n : ℝ) * gammaTilde :=
    le_trans hresCoeff_le_tilde htilde_le_n_tilde
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_sqrt_residual_growth_source_rhs_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH1nonneg,
      hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm, hDeltaR1,
      hDeltaR2, hsys⟩
  have hDeltaA_tilde :
      frobNorm DeltaA ≤ gammaTilde * frobNorm A := by
    exact le_trans hDeltaA
      (mul_le_mul_of_nonneg_right hgamma_le_tilde (frobNorm_nonneg A))
  have hDeltaAcomp_tilde : ∀ i j,
      |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j := by
    intro i j
    have hmaj_nonneg :
        0 ≤ matMulRect (n + k) (n + k) n G
          (fun a b => |A a b|) i j := by
      unfold matMulRect
      exact Finset.sum_nonneg (by
        intro t _
        exact mul_nonneg (hGnonneg i t) (abs_nonneg _))
    have hcoeff :
        (n + k : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      calc
        (n + k : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          (n + k : ℝ) * ((n : ℝ) * gammaTilde) :=
            mul_le_mul_of_nonneg_left hgamma_le_n_tilde (by positivity)
        _ = (n + k : ℝ) * (n : ℝ) * gammaTilde := by ring
    exact le_trans (hDeltaAcomp i j)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  have hDeltaf_tilde : ∀ i,
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
      lsTheorem20_4DeltafMajorant_nonneg H1 H2 f rhat
        hH1nonneg hH2nonneg i
    have hcoeff :
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      calc
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * ((n : ℝ) * gammaTilde) :=
            mul_le_mul_of_nonneg_left hresCoeff_le_n_tilde
              (Real.sqrt_nonneg _)
        _ = Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by ring
    exact le_trans (hDeltaf i)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  have hDeltag_tilde : ∀ j,
      |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j := by
    intro j
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltagMajorant A H3 rhat j :=
      lsTheorem20_4DeltagMajorant_nonneg A H3 rhat hH3nonneg j
    have hcoeff :
        Real.sqrt (n + k : ℝ) * (n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)) ≤
          Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde := by
      have hleft_nonneg : 0 ≤ Real.sqrt (n + k : ℝ) * (n : ℝ) :=
        mul_nonneg (Real.sqrt_nonneg _) (Nat.cast_nonneg n)
      exact mul_le_mul_of_nonneg_left hgamma_le_tilde hleft_nonneg
    exact le_trans (hDeltag j)
      (mul_le_mul_of_nonneg_right hcoeff hmaj_nonneg)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA_tilde, hGnonneg, hGnorm, hDeltaAcomp_tilde, hDeltaf_tilde,
    hDeltag_tilde, hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm,
    hH3norm, hDeltaR1, hDeltaR2, ?_⟩
  simpa [gammaTilde, Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    residual-sharpened handoff with no separate triangular-solve
    gamma-validity input.

The global Householder QR validity hypothesis already implies `gammaValid fp n`,
so callers only supply the single source-facing validity condition for the
residual-sharpened `γ̃` theorem. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTildeSqrtResidual_unified_frob_bound_of_global_gammaValid
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ gammaTilde * frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  have hγ : gammaValid fp n :=
    gammaValid_n_of_householderConstructApplyGammaValid fp (n + k) n hvalid
  exact
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTildeSqrtResidual_unified_frob_bound
      fp A f g hn hvalid hdiag hγ
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 source-domain wrapper.

    This is the same residual-sharpened concrete `γ̃` handoff as
    `..._of_global_gammaValid`, but its solve-side domain is named in the
    source-facing form: full column rank of `A` together with computed QR
    nonbreakdown for the top square `R`.  The full-rank field records the
    printed hypothesis; the computed nonbreakdown field is the finite-precision
    triangular-solve side condition still not derivable from full rank alone by
    the current verified zero-aware QR API. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_theorem20_4_source_fullRank_computed_nonbreakdown
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdomain : lsTheorem20_4FullRankComputedQRDomain fp A) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤ gammaTilde * frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  exact
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_concrete_gammaTildeSqrtResidual_unified_frob_bound_of_global_gammaValid
      fp A f g hn hvalid
      (lsTheorem20_4FullRankComputedQRDomain.computedQRNonbreakdown fp hdomain)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 with the two actual matrix
    perturbations exposed.  If `DeltaA` is the common Householder-QR panel and
    `DeltaR_i` is the perturbation from triangular solve `i`, then the matrices
    occurring in the exact asymmetric augmented system are

      `DeltaA_i = DeltaA + Q [DeltaR_i; 0]`,  `i = 1,2`.

    The componentwise estimates below are the strongest direct consequence of
    the current verified ingredients: the printed `G_i |A|` majorant for the
    common panel plus the exact absolute lifted triangular-solve remainder.
    In particular, they do not misstate the common-panel estimate as a bound
    on either total perturbation. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_theorem20_4_source_fullRank_computed_nonbreakdown_total_perturbations
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdomain : lsTheorem20_4FullRankComputedQRDomain fp A) :
    let gammaTilde : ℝ :=
      lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA DeltaA1 DeltaA2 : Fin (n + k) → Fin n → ℝ,
    ∃ G1 G2 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      DeltaA1 = (fun i j =>
        DeltaA i j + matMulRectLeft Q (lsQRTallBlock DeltaR1) i j) ∧
      DeltaA2 = (fun i j =>
        DeltaA i j + matMulRectLeft Q (lsQRTallBlock DeltaR2) i j) ∧
      frobNorm DeltaA ≤ gammaTilde * frobNorm A ∧
      (∀ i j, 0 ≤ G1 i j) ∧
      (∀ i j, 0 ≤ G2 i j) ∧
      frobNorm G1 = 1 ∧
      frobNorm G2 = 1 ∧
      (∀ i j, |DeltaA1 i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
            matMulRect (n + k) (n + k) n G1
              (fun a b => |A a b|) i j +
          |matMulRectLeft Q (lsQRTallBlock DeltaR1) i j|) ∧
      (∀ i j, |DeltaA2 i j| ≤
        ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
            matMulRect (n + k) (n + k) n G2
              (fun a b => |A a b|) i j +
          |matMulRectLeft Q (lsQRTallBlock DeltaR2) i j|) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) * gammaTilde) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA1 i j)
        (fun i j => A i j + DeltaA2 i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let gammaTilde : ℝ :=
    lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_theorem20_4_source_fullRank_computed_nonbreakdown
      fp A f g hn hvalid hdomain with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
      hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag,
      hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
      hDeltaR1, hDeltaR2, hsys⟩
  let DeltaA1 : Fin (n + k) → Fin n → ℝ := fun i j =>
    DeltaA i j + matMulRectLeft Q (lsQRTallBlock DeltaR1) i j
  let DeltaA2 : Fin (n + k) → Fin n → ℝ := fun i j =>
    DeltaA i j + matMulRectLeft Q (lsQRTallBlock DeltaR2) i j
  have htotal1 : ∀ i j, |DeltaA1 i j| ≤
      ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j +
        |matMulRectLeft Q (lsQRTallBlock DeltaR1) i j| := by
    intro i j
    calc
      |DeltaA1 i j| ≤ |DeltaA i j| +
          |matMulRectLeft Q (lsQRTallBlock DeltaR1) i j| := by
            simpa [DeltaA1] using
              (abs_add_le (DeltaA i j)
                (matMulRectLeft Q (lsQRTallBlock DeltaR1) i j))
      _ ≤ ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
            matMulRect (n + k) (n + k) n G
              (fun a b => |A a b|) i j +
          |matMulRectLeft Q (lsQRTallBlock DeltaR1) i j| :=
        add_le_add (by simpa [gammaTilde] using hDeltaAcomp i j) le_rfl
  have htotal2 : ∀ i j, |DeltaA2 i j| ≤
      ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j +
        |matMulRectLeft Q (lsQRTallBlock DeltaR2) i j| := by
    intro i j
    calc
      |DeltaA2 i j| ≤ |DeltaA i j| +
          |matMulRectLeft Q (lsQRTallBlock DeltaR2) i j| := by
            simpa [DeltaA2] using
              (abs_add_le (DeltaA i j)
                (matMulRectLeft Q (lsQRTallBlock DeltaR2) i j))
      _ ≤ ((n + k : ℝ) * (n : ℝ) * gammaTilde) *
            matMulRect (n + k) (n + k) n G
              (fun a b => |A a b|) i j +
          |matMulRectLeft Q (lsQRTallBlock DeltaR2) i j| :=
        add_le_add (by simpa [gammaTilde] using hDeltaAcomp i j) le_rfl
  refine ⟨DeltaA, DeltaA1, DeltaA2, G, G, Deltaf, Deltag,
    H1, H2, H3, DeltaR1, DeltaR2, rfl, rfl, hDeltaA,
    hGnonneg, hGnonneg, hGnorm, hGnorm, htotal1, htotal2,
    hDeltaf, hDeltag, hH1nonneg, hH2nonneg, hH3nonneg,
    hH1norm, hH2norm, hH3norm, hDeltaR1, hDeltaR2, ?_⟩
  simpa [DeltaA1, DeltaA2, add_assoc, gammaTilde, Q, Rhat, R,
    c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and a
    nonrecursive closed RHS growth coefficient.

    This removes the recursive RHS coefficient from the `Delta f` source bound
    by applying `householderQRRhsPanelGrowthCoeff_le_closedGrowth`.  The
    resulting coefficient is conservative and implementation-derived; it is not
    the printed `n * gamma` constant. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_closed_growth_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelClosedGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp (le_trans hbase_le_K hK_le_nK) (by
      simpa [K] using hvalid)
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  rcases
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_closedGrowth
      fp A f g hn hbase_valid hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelClosedGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i)
      (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and a
    gamma-only nonrecursive closed RHS growth coefficient.

    This removes the public dependency on `householderConstructApplyBound` in
    the closed RHS coefficient by applying the proved gamma cap for one
    Householder construction/application step.  The coefficient remains
    conservative and implementation-derived; it is not Higham's printed
    `n * gamma` constant. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_closed_growth_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0)
    (hγ : gammaValid fp n) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  have hK_le_nK : K ≤ n * K := by
    have hn1 : 1 ≤ n := Nat.succ_le_of_lt hn
    simpa using Nat.mul_le_mul_right K hn1
  have hbase_le_K : 11 * (n + k) + 23 ≤ K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hK_valid : gammaValid fp K :=
    gammaValid_mono fp hK_le_nK (by simpa [K] using hvalid)
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    gammaValid_mono fp hbase_le_K hK_valid
  have hready : HouseholderQRPanelReady fp (n + k) n A :=
    HouseholderQRPanelReady_of_global_gammaValid fp (n + k) n (n + k) A
      (le_refl (n + k)) hbase_valid
  rcases
    householderQRRhsPanelBackwardBound_uniform_f_source_witness_of_gammaClosedGrowth
      fp A f g hn hK_valid hready with
    ⟨H1, H2, hH1nonneg, hH2nonneg, hH1norm, hH2norm, hDeltafDom⟩
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_zero_deltag_bound
      fp A f g hn hvalid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H3, DeltaR1, DeltaR2, hDeltaA,
      hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag, hH3nonneg,
      hH3norm, hDeltaR1, hDeltaR2, hsys⟩
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    exact le_trans (hDeltaf i)
      (by simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hDeltafDom i)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and
    a single accumulated `gamma` RHS index.

    The RHS coefficient uses
    `householderQRRhsPanelGammaClosedGrowthIndex (n+k) n`, obtained by
    absorbing the gamma-only closed RHS growth coefficient into one Higham
    `gamma` term.  This is still conservative and does not prove Higham's
    printed `n * gamma` RHS constant. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_index_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hvalid :
      gammaValid fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          gamma fp (householderQRRhsPanelGammaClosedGrowthIndex (n + k) n)) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let K : ℕ := householderConstructApplyGammaIndex (n + k)
  let idx : ℕ := householderQRRhsPanelGammaClosedGrowthIndex (n + k) n
  let I : ℕ := n * (n + k) ^ 2 * (1 + 2 * (n + k) ^ 2) ^ n
  have hm_pos : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  have hm_sq_pos : 0 < (n + k) ^ 2 := Nat.pow_pos hm_pos
  have hpow_pos : 0 < (1 + 2 * (n + k) ^ 2) ^ n :=
    Nat.pow_pos (by omega)
  have hfactor_pos :
      0 < (n + k) ^ 2 * (1 + 2 * (n + k) ^ 2) ^ n :=
    Nat.mul_pos hm_sq_pos hpow_pos
  have hn_le_I : n ≤ I := by
    dsimp [I]
    simpa [Nat.mul_assoc] using Nat.le_mul_of_pos_right n hfactor_pos
  have hidx_eq : idx = I * K := by
    dsimp [idx, I, K, householderQRRhsPanelGammaClosedGrowthIndex]
  have hnK_le_idx : n * K ≤ idx := by
    rw [hidx_eq]
    exact Nat.mul_le_mul_right K hn_le_I
  have hmatrix_valid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k)) :=
    gammaValid_mono fp
      (by simpa [K] using hnK_le_idx)
      (by simpa [idx] using hvalid)
  have hK_pos : 0 < K := by
    dsimp [K, householderConstructApplyGammaIndex]
    omega
  have hn_le_nK : n ≤ n * K := Nat.le_mul_of_pos_right n hK_pos
  have hγ : gammaValid fp n :=
    gammaValid_mono fp
      (le_trans hn_le_nK (by simpa [K] using hnK_le_idx))
      (by simpa [idx] using hvalid)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_closed_growth_source_rhs_bound
      fp A f g hn hmatrix_valid hdiag hγ with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
      hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag,
      hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
      hDeltaR1, hDeltaR2, hsys⟩
  have hcoeff :
      householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n ≤
        gamma fp idx := by
    simpa [idx] using
      householderQRRhsPanelGammaClosedGrowthCoeff_le_gammaIndex
        fp (n + k) n hvalid
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) * gamma fp idx) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
      lsTheorem20_4DeltafMajorant_nonneg H1 H2 f rhat
        hH1nonneg hH2nonneg i
    have hscale :
        Real.sqrt (n + k : ℝ) *
            householderQRRhsPanelGammaClosedGrowthCoeff fp (n + k) n ≤
          Real.sqrt (n + k : ℝ) * gamma fp idx :=
      mul_le_mul_of_nonneg_left hcoeff (Real.sqrt_nonneg _)
    exact le_trans (hDeltaf i)
      (mul_le_mul_of_nonneg_right hscale hmaj_nonneg)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 concrete Householder QR
    handoff with implementation-backed uniform `Delta f` source witnesses and
    an explicit dimension-only factor times the printed panel gamma radius.

    The RHS coefficient is still conservative:
    `2 * householderQRRhsPanelGammaClosedGrowthFactor (n+k) n *
      gamma fp (n * householderConstructApplyGammaIndex (n+k))`.  This exposes
    the remaining gap to Higham's printed `n * gamma` RHS coefficient as a
    visible factor rather than a hidden accumulated index. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_factor_source_rhs_bound
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hhalf :
      ((householderQRRhsPanelGammaClosedGrowthIndex (n + k) n : ℝ) *
        fp.u ≤ 1 / 2))
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          ((2 : ℝ) *
            (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)))) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x := by
  let Q : Fin (n + k) → Fin (n + k) → ℝ :=
    fl_householderQRPanel_Q fp (n + k) n A
  let Rhat : Fin (n + k) → Fin n → ℝ :=
    fl_householderQRPanel_R fp (n + k) n A
  let R : Fin n → Fin n → ℝ :=
    fun i j => Rhat (Fin.castAdd k i) j
  let c_hat : Fin (n + k) → ℝ :=
    fl_householderQRPanel_rhs fp (n + k) n A f
  let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
  let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
  let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
  let idx : ℕ := householderQRRhsPanelGammaClosedGrowthIndex (n + k) n
  have hvalid : gammaValid fp idx := by
    unfold gammaValid
    exact lt_of_le_of_lt (by simpa [idx] using hhalf) (by norm_num)
  rcases
    LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_index_source_rhs_bound
      fp A f g hn (by simpa [idx] using hvalid) hdiag with
    ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
      hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltaf, hDeltag,
      hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
      hDeltaR1, hDeltaR2, hsys⟩
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  have hgamma_le :
      gamma fp idx ≤
        (2 : ℝ) *
          (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) := by
    simpa [idx] using
      householderQRRhsPanelGammaClosedGrowthIndex_gamma_le_factor_printedGamma
        fp (n + k) n hm hhalf
  have hscale :
      Real.sqrt (n + k : ℝ) * gamma fp idx ≤
        Real.sqrt (n + k : ℝ) *
          ((2 : ℝ) *
            (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k))) := by
    exact mul_le_mul_of_nonneg_left hgamma_le (Real.sqrt_nonneg _)
  have hDeltafSrc : ∀ i : Fin (n + k),
      |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          ((2 : ℝ) *
            (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)))) *
          lsTheorem20_4DeltafMajorant H1 H2 f rhat i := by
    intro i
    have hmaj_nonneg :
        0 ≤ lsTheorem20_4DeltafMajorant H1 H2 f rhat i :=
      lsTheorem20_4DeltafMajorant_nonneg H1 H2 f rhat
        hH1nonneg hH2nonneg i
    exact le_trans (hDeltaf i)
      (mul_le_mul_of_nonneg_right hscale hmaj_nonneg)
  refine ⟨DeltaA, G, Deltaf, Deltag, H1, H2, H3, DeltaR1, DeltaR2,
    hDeltaA, hGnonneg, hGnorm, hDeltaAcomp, hDeltafSrc, hDeltag,
    hH1nonneg, hH2nonneg, hH3nonneg, hH1norm, hH2norm, hH3norm,
    hDeltaR1, hDeltaR2, ?_⟩
  simpa [Q, Rhat, R, c_hat, cBot, h, rhat] using hsys
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4 conservative gamma-factor
    source package with the half-radius guard discharged by a named
    unit-roundoff smallness threshold.

This is the source-facing form of the implementation-backed `Delta f` route
with the explicit gamma-factor coefficient.  It still deliberately keeps the
extra factor `2 * householderQRRhsPanelGammaClosedGrowthFactor (n+k) n`; the
printed `n * gamma` coefficient remains a separate sharpening problem. -/
theorem LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_factor_source_rhs_bound_unit_roundoff_smallnessThreshold
    {n k : ℕ} (fp : FPModel)
    (A : Fin (n + k) → Fin n → ℝ)
    (f : Fin (n + k) → ℝ) (g : Fin n → ℝ)
    (hn : 0 < n)
    (hu : fp.u < theorem20_4GammaFactorRhsSmallnessThreshold n k)
    (hdiag : ∀ i : Fin n,
      fl_householderQRPanel_R fp (n + k) n A (Fin.castAdd k i) i ≠ 0) :
    let Q : Fin (n + k) → Fin (n + k) → ℝ :=
      fl_householderQRPanel_Q fp (n + k) n A
    let Rhat : Fin (n + k) → Fin n → ℝ :=
      fl_householderQRPanel_R fp (n + k) n A
    let R : Fin n → Fin n → ℝ :=
      fun i j => Rhat (Fin.castAdd k i) j
    let c_hat : Fin (n + k) → ℝ :=
      fl_householderQRPanel_rhs fp (n + k) n A f
    let cTop : Fin n → ℝ := fun i => c_hat (Fin.castAdd k i)
    let cBot : Fin k → ℝ := fun i => c_hat (Fin.natAdd n i)
    let h : Fin n → ℝ := fl_forwardSub fp n (matTranspose R) g
    let x : Fin n → ℝ := fl_backSub fp n R (fun i : Fin n => cTop i - h i)
    let rhat : Fin (n + k) → ℝ := matMulVec (n + k) Q (Fin.append h cBot)
    ∃ DeltaA : Fin (n + k) → Fin n → ℝ,
    ∃ G : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ Deltaf : Fin (n + k) → ℝ,
    ∃ Deltag : Fin n → ℝ,
    ∃ H1w H2w H3 : Fin (n + k) → Fin (n + k) → ℝ,
    ∃ DeltaR1 DeltaR2 : Fin n → Fin n → ℝ,
      frobNorm DeltaA ≤
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) *
          frobNorm A ∧
      (∀ i j, 0 ≤ G i j) ∧
      frobNorm G = 1 ∧
      (∀ i j, |DeltaA i j| ≤
        ((n + k : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          matMulRect (n + k) (n + k) n G
            (fun a b => |A a b|) i j) ∧
      (∀ i, |Deltaf i| ≤
        (Real.sqrt (n + k : ℝ) *
          ((2 : ℝ) *
            (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
            gamma fp (n * householderConstructApplyGammaIndex (n + k)))) *
          lsTheorem20_4DeltafMajorant H1w H2w f rhat i) ∧
      (∀ j, |Deltag j| ≤
        (Real.sqrt (n + k : ℝ) * (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) *
          lsTheorem20_4DeltagMajorant A H3 rhat j) ∧
      (∀ i j, 0 ≤ H1w i j) ∧
      (∀ i j, 0 ≤ H2w i j) ∧
      (∀ i j, 0 ≤ H3 i j) ∧
      frobNorm H1w = 1 ∧
      frobNorm H2w = 1 ∧
      frobNorm H3 = 1 ∧
      (∀ i j, |DeltaR1 i j| ≤ gamma fp n * |R i j|) ∧
      (∀ i j, |DeltaR2 i j| ≤ gamma fp n * |R i j|) ∧
      LSAsymmetricAugmentedSystem
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR1) i j)
        (fun i j => A i j + DeltaA i j +
          matMulRectLeft Q (lsQRTallBlock DeltaR2) i j)
        (fun i => f i + Deltaf i) (fun j => g j + Deltag j)
        rhat x :=
  LSAsymmetricAugmentedSystem.exists_exact_qr_solution_of_fl_householderQRPanel_higham_matrix_solve_components_with_gamma_factor_source_rhs_bound
    fp A f g hn
    (theorem20_4GammaFactorRhs_half_of_unit_roundoff_lt_smallnessThreshold
      fp hn hu)
    hdiag
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4, source-facing coefficient
    obstruction for the printed `Delta f` route.

    Under positive unit roundoff and a nonempty panel, the conservative
    implementation-backed RHS coefficient exposed by
    `...with_gamma_factor_source_rhs_bound` is strictly larger than the printed
    coefficient `sqrt(n+k) * n * gamma`.  Thus the remaining Theorem 20.4 gap
    cannot be closed by converting the current conservative coefficient to the
    printed one; the RHS arithmetic/model itself must be sharpened. -/
theorem theorem20_4_printed_deltaf_coefficient_lt_conservative_gammaFactor_coefficient
    (fp : FPModel) {n k : ℕ} (hn : 0 < n) (hu : 0 < fp.u)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k))) :
    Real.sqrt (n + k : ℝ) *
        ((n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) <
      Real.sqrt (n + k : ℝ) *
        ((2 : ℝ) *
          (householderQRRhsPanelGammaClosedGrowthFactor (n + k) n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) := by
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  have hsqrt_pos : 0 < Real.sqrt (n + k : ℝ) := by
    exact Real.sqrt_pos.2 (by exact_mod_cast hm)
  exact mul_lt_mul_of_pos_left
    (by
      simpa [mul_assoc] using
        householderQRRhsPanelGammaClosedGrowthFactor_printedCoeff_lt_factorCoeff
          fp (m := n + k) (p := n) hm hn hu hvalid)
    hsqrt_pos
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4, residual-sharpened coefficient
    obstruction for the printed `Delta f` route in every nonempty panel.

    The residual-sharpened unified coefficient is
    `gamma + householderQRRhsPanelSqrtResidualGrowthCoeff`.  The residual-growth
    addend is strictly positive under positive unit roundoff for any nonempty
    panel, so the current implementation-backed `gammaTilde` coefficient is
    still strictly larger than Higham's printed `gamma` coefficient after the
    common `sqrt(n+k)*n` source factor is restored. -/
theorem theorem20_4_printed_deltaf_coefficient_lt_concrete_gammaTildeSqrtResidual
    (fp : FPModel) {n k : ℕ} (hn : 0 < n) (hu : 0 < fp.u)
    (hvalid :
      gammaValid fp (n * householderConstructApplyGammaIndex (n + k))) :
    Real.sqrt (n + k : ℝ) *
        ((n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k))) <
      Real.sqrt (n + k : ℝ) *
        ((n : ℝ) *
          lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n) := by
  have hm : 0 < n + k := Nat.lt_of_lt_of_le hn (Nat.le_add_right n k)
  have hm_real : 0 < (n + k : ℝ) := by
    exact_mod_cast hm
  have hn_real : 0 < (n : ℝ) := by
    exact_mod_cast hn
  have hsqrt_pos : 0 < Real.sqrt (n + k : ℝ) :=
    Real.sqrt_pos.2 hm_real
  have hbase_valid : gammaValid fp (11 * (n + k) + 23) :=
    lsTheorem20_4ConcreteGammaTildeSqrtResidual_base_gammaValid fp
      (m := n + k) (n := n) hn hvalid
  have htail_pos :
      0 <
        householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n :=
    householderQRRhsPanelSqrtResidualGrowthCoeff_pos fp hm hn hu
      hbase_valid
  have htilde_gt_gamma :
      gamma fp (n * householderConstructApplyGammaIndex (n + k)) <
        lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n := by
    have hadd :
        gamma fp (n * householderConstructApplyGammaIndex (n + k)) <
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) +
            householderQRRhsPanelSqrtResidualGrowthCoeff fp (n + k) n :=
      lt_add_of_pos_right _ htail_pos
    simpa [lsTheorem20_4ConcreteGammaTildeSqrtResidual] using hadd
  have hinner :
      (n : ℝ) *
          gamma fp (n * householderConstructApplyGammaIndex (n + k)) <
        (n : ℝ) *
          lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (n + k) n :=
    mul_lt_mul_of_pos_left htilde_gt_gamma hn_real
  exact mul_lt_mul_of_pos_left hinner hsqrt_pos
/-- Higham, 2nd ed., Chapter 20, Theorem 20.4, residual-sharpened coefficient
    obstruction for the printed `Delta f` route in the one-column panel case.

    The latest residual-sharpened unified coefficient is
    `gamma + householderQRRhsPanelSqrtResidualGrowthCoeff`.  For one panel
    reflector the residual-growth term is already positive under positive unit
    roundoff, so the current implementation-backed `gammaTilde` coefficient is
    still strictly larger than Higham's printed `1 * gamma` coefficient. -/
theorem theorem20_4_printed_deltaf_coefficient_lt_concrete_gammaTildeSqrtResidual_one_column
    (fp : FPModel) {k : ℕ} (hu : 0 < fp.u)
    (hvalid :
      gammaValid fp (1 * householderConstructApplyGammaIndex (k + 1))) :
    Real.sqrt (k + 1 : ℝ) *
        ((1 : ℝ) *
          gamma fp (1 * householderConstructApplyGammaIndex (k + 1))) <
      Real.sqrt (k + 1 : ℝ) *
        lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (k + 1) 1 := by
  have hm : 0 < k + 1 := Nat.succ_pos k
  have hm_real : 0 < (k + 1 : ℝ) := by
    exact_mod_cast hm
  have hm_real_nat : 0 < ((k + 1 : ℕ) : ℝ) := by
    exact_mod_cast hm
  have hbase_valid : gammaValid fp (11 * (k + 1) + 23) :=
    gammaValid_mono fp (by
      dsimp [householderConstructApplyGammaIndex]
      omega) hvalid
  have hCpos : 0 < householderConstructApplyBound fp (k + 1) := by
    unfold householderConstructApplyBound
    have hu_sq : 0 < fp.u ^ 2 := sq_pos_of_pos hu
    have hprod : 0 < ((k + 1 : ℕ) : ℝ) * fp.u ^ 2 :=
      mul_pos hm_real_nat hu_sq
    have hsqrt : 0 < Real.sqrt (((k + 1 : ℕ) : ℝ) * fp.u ^ 2) :=
      Real.sqrt_pos.2 hprod
    have hgamma_nonneg : 0 ≤ gamma fp (11 * (k + 1) + 23) :=
      gamma_nonneg fp hbase_valid
    exact add_pos_of_pos_of_nonneg hsqrt
      (mul_nonneg (by norm_num) hgamma_nonneg)
  have hsqrt_pos : 0 < Real.sqrt (k + 1 : ℝ) :=
    Real.sqrt_pos.2 hm_real
  have htail_pos :
      0 <
        householderQRRhsPanelSqrtResidualGrowthCoeff fp (k + 1) 1 := by
    have htail_zero :
        householderQRRhsPanelSqrtResidualGrowthCoeff fp k 0 = 0 := by
      cases k <;> simp [householderQRRhsPanelSqrtResidualGrowthCoeff]
    simpa [householderQRRhsPanelSqrtResidualGrowthCoeff, htail_zero] using
      mul_pos hsqrt_pos (mul_pos hsqrt_pos hCpos)
  have hinner :
      (1 : ℝ) *
          gamma fp (1 * householderConstructApplyGammaIndex (k + 1)) <
        lsTheorem20_4ConcreteGammaTildeSqrtResidual fp (k + 1) 1 := by
    have hadd :
        gamma fp (1 * householderConstructApplyGammaIndex (k + 1)) <
          gamma fp (1 * householderConstructApplyGammaIndex (k + 1)) +
            householderQRRhsPanelSqrtResidualGrowthCoeff fp (k + 1) 1 :=
      lt_add_of_pos_right _ htail_pos
    simpa [lsTheorem20_4ConcreteGammaTildeSqrtResidual] using hadd
  exact mul_lt_mul_of_pos_left hinner hsqrt_pos
/-- Exact least-squares minimizers make Higham's signed residual `b - A*x`
    orthogonal to every column of `A`. -/
theorem IsLeastSquaresMinimizer.higham_residual_orthogonal {m n : ℕ}
    {A : Fin m → Fin n → ℝ} {b : Fin m → ℝ} {x : Fin n → ℝ}
    {s : Fin m → ℝ}
    (hmin : IsLeastSquaresMinimizer A b x)
    (hs : s = lsResidualHigham A b x) :
    ∀ j : Fin n, ∑ i : Fin m, A i j * s i = 0 := by
  intro j
  have hNE : RectLSNormalEquations A b x :=
    IsLeastSquaresMinimizer.rectLSNormalEquations hmin
  have horth := hNE.residual_orthogonal j
  have hneg :
      ∑ i : Fin m, A i j * s i =
        -∑ i : Fin m, A i j * lsResidual A b x i := by
    rw [hs]
    exact lsResidualHigham_column_sum_eq_neg A b x j
  rw [hneg, horth, neg_zero]
/-- Higham, 2nd ed., Chapter 20, Wedin perturbation setup:
    an exact minimizer of the perturbed least-squares problem makes the
    perturbed Higham residual `s = (b + Delta b) - B*y` orthogonal to every
    column of `B`. -/
theorem IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
    {m n : ℕ} {B : Fin m → Fin n → ℝ}
    {b Deltab s : Fin m → ℝ} {y : Fin n → ℝ}
    (hmin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    ∀ j : Fin n, ∑ i : Fin m, B i j * s i = 0 := by
  exact
    IsLeastSquaresMinimizer.higham_residual_orthogonal
      (A := B) (b := fun i => b i + Deltab i) (x := y)
      hmin (by
        ext i
        rw [hs]
        rfl)
/-- Higham, 2nd ed., Chapter 20, Wedin perturbation setup:
    exact least-squares optimality makes the source Higham residual orthogonal
    to the data columns, hence a symmetric range projector `A*Aplus`
    annihilates it. -/
theorem IsLeastSquaresMinimizer.wedin_rangeProjection_higham_residual_eq_zero
    {m k : ℕ} {A : Fin m → Fin (k + 1) → ℝ}
    {Aplus : Fin (k + 1) → Fin m → ℝ}
    {b r : Fin m → ℝ} {x : Fin (k + 1) → ℝ}
    (hmin : IsLeastSquaresMinimizer A b x)
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hr : r = fun i => b i - rectMatMulVec A x i) :
    rectMatMulVec (rectMatMul A Aplus) r = 0 := by
  have horth :
      ∀ j : Fin (k + 1), ∑ i : Fin m, A i j * r i = 0 :=
    IsLeastSquaresMinimizer.higham_residual_orthogonal
      (A := A) (b := b) (x := x) (s := r) hmin
      (by simpa [lsResidualHigham] using hr)
  exact
    wedinTheorem20_1_rangeProjection_perturbed_residual_eq_zero_of_column_orthogonal
      A Aplus r hSymA horth
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.1):
    source-strength solution-side Wedin bound with perturbed least-squares
    optimality as the caller-facing hypothesis.

This is the minimizer-facing version of
`wedinTheorem20_1_solutionRelativeRHS_le_of_residual_definitions_projector_bound_column_orthogonal`.
It discharges residual column orthogonality from exact optimality.  The
source-strength projector estimate remains explicit until the full
Lemma 20.12 equality/min surface is formalized. -/
theorem IsLeastSquaresMinimizer.wedin_solutionRelativeRHS_le_of_projector_bound_geometry
    {m k : ℕ} (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {Aplus_norm delta eta DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hAplus_pos : 0 < Aplus_norm)
    (hA_norm_pos : 0 < A_norm)
    (hx_norm_pos : 0 < vecNorm2 x)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (heta : eta = Aplus_norm * delta)
    (hsmall : eta < 1)
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hPBIPA :
      rectOpNorm2Le
        (rectMatMul
          (rectMatMul B Bplus)
          (fun i j => idMatrix m i j - rectMatMul A Aplus i j))
        (delta * Aplus_norm))
    (hBplus : rectOpNorm2Le Bplus (Aplus_norm / (1 - eta)))
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget :
      Deltab_norm ≤ eps * (A_norm * vecNorm2 x + vecNorm2 r))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun j => y j - x j) / vecNorm2 x ≤
      wedinTheorem20_1SolutionRelativeRHS
        kappa eps A_norm (vecNorm2 x) (vecNorm2 r) := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_solutionRelativeRHS_le_of_residual_definitions_projector_bound_column_orthogonal
      A B Aplus Bplus DeltaA b Deltab r s x y hAplus_pos hA_norm_pos
      hx_norm_pos hkappa hdelta heta hsmall hleftB hSymB hPBIPA hBplus
      hDeltaA hDeltab hDeltaA_norm_budget hDeltab_norm_budget
      hrangeA_residual hB hr hs horth_s
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.1):
    minimizer-facing printed solution-side Wedin bound, conditional only on
    the exact cross-projection norm equality from the open Stewart--Sun/CS
    route.

This is the caller-facing exact-LS version of
`wedinTheorem20_1_solutionRelativeRHS_le_of_residual_definitions_crossProjection_eq_column_orthogonal`.
The perturbed residual column orthogonality is discharged from exact optimality,
while the exact cross-projection equality remains visible. -/
theorem IsLeastSquaresMinimizer.wedin_solutionRelativeRHS_le_of_crossProjection_eq_geometry
    {m k : ℕ} (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {Aplus_norm delta eta DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hAplus_pos : 0 < Aplus_norm)
    (hA_norm_pos : 0 < A_norm)
    (hx_norm_pos : 0 < vecNorm2 x)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (heta : eta = Aplus_norm * delta)
    (hsmall : eta < 1)
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hDelta : rectOpNorm2Le (fun i j => B i j - A i j) delta)
    (hAplus : rectOpNorm2Le Aplus Aplus_norm)
    (hBplus : rectOpNorm2Le Bplus (Aplus_norm / (1 - eta)))
    (hEq :
      complexMatrixOp2
          (realRectToCMatrix
            (rectMatMul
              (rectMatMul B Bplus)
              (fun i j => idMatrix m i j - rectMatMul A Aplus i j))) =
        complexMatrixOp2
          (realRectToCMatrix
            (rectMatMul
              (rectMatMul A Aplus)
              (fun i j => idMatrix m i j - rectMatMul B Bplus i j))))
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget :
      Deltab_norm ≤ eps * (A_norm * vecNorm2 x + vecNorm2 r))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun j => y j - x j) / vecNorm2 x ≤
      wedinTheorem20_1SolutionRelativeRHS
        kappa eps A_norm (vecNorm2 x) (vecNorm2 r) := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_solutionRelativeRHS_le_of_residual_definitions_crossProjection_eq_column_orthogonal
      A B Aplus Bplus DeltaA b Deltab r s x y hAplus_pos hA_norm_pos
      hx_norm_pos hkappa hdelta heta hsmall hleftB hSymA hSymB hDelta
      hAplus hBplus hEq hDeltaA hDeltab hDeltaA_norm_budget
      hDeltab_norm_budget hrangeA_residual hB hr hs horth_s
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.1), conservative
    solution-side Wedin bound with perturbed least-squares optimality as the
    caller-facing hypothesis.

This is the minimizer-facing version of
`wedinTheorem20_1_solutionRelativeRHSConservative_le_of_residual_definitions_column_orthogonal`.
It discharges residual column orthogonality from exact optimality.  The
conclusion is conservative, not the printed (20.1) RHS, because it still uses
the one-sided Lemma 20.12 `Bplus` route. -/
theorem IsLeastSquaresMinimizer.wedin_solutionRelativeRHSConservative_le_of_Bplus_bound_geometry
    {m k : ℕ} (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {Aplus_norm delta eta DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hAplus_pos : 0 < Aplus_norm)
    (hA_norm_pos : 0 < A_norm)
    (hx_norm_pos : 0 < vecNorm2 x)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (heta : eta = Aplus_norm * delta)
    (hsmall : eta < 1)
    (hleftA : rectMatMul Aplus A = idMatrix (k + 1))
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hDelta : rectOpNorm2Le (fun i j => B i j - A i j) delta)
    (hBplus : rectOpNorm2Le Bplus (Aplus_norm / (1 - eta)))
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget :
      Deltab_norm ≤ eps * (A_norm * vecNorm2 x + vecNorm2 r))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun j => y j - x j) / vecNorm2 x ≤
      wedinTheorem20_1SolutionRelativeRHSConservative
        kappa eps A_norm (vecNorm2 x) (vecNorm2 r) := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_solutionRelativeRHSConservative_le_of_residual_definitions_column_orthogonal
      A B Aplus Bplus DeltaA b Deltab r s x y hAplus_pos hA_norm_pos
      hx_norm_pos hkappa hdelta heta hsmall hleftA hleftB hSymA hSymB
      hDelta hBplus hDeltaA hDeltab hDeltaA_norm_budget
      hDeltab_norm_budget hrangeA_residual hB hr hs horth_s
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.2):
    residual-side Wedin bound with perturbed least-squares optimality as the
    caller-facing hypothesis.

This composes the `IsLeastSquaresMinimizer` normal-equation bridge with the
current strongest residual-side Wedin wrapper.  The source-strength projector
estimate `||P_B(I-P_A)|| <= ||Delta A|| ||Aplus||` remains explicit, so the
full Wedin theorem is still open until the Lemma 20.12 equality/min surface is
formalized. -/
theorem IsLeastSquaresMinimizer.wedin_residualRelativeRHS_le_of_projector_bound_geometry
    {m k : ℕ} (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {delta Aplus_norm DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hb_norm_pos : 0 < vecNorm2 b)
    (hA_norm_nonneg : 0 ≤ A_norm)
    (heps_nonneg : 0 ≤ eps)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (hAplus : rectOpNorm2Le Aplus Aplus_norm)
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget : Deltab_norm ≤ eps * vecNorm2 b)
    (hleftA : rectMatMul Aplus A = idMatrix (k + 1))
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hPBIPA :
      rectOpNorm2Le
        (rectMatMul
          (rectMatMul B Bplus)
          (fun i j => idMatrix m i j - rectMatMul A Aplus i j))
        (delta * Aplus_norm))
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun i => r i - s i) / vecNorm2 b ≤
      wedinTheorem20_1ResidualRelativeRHS kappa eps := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_residualRelativeRHS_le_of_residual_definitions_projector_bound_geometry_column_orthogonal
      A B Aplus Bplus DeltaA b Deltab r s x y hb_norm_pos hA_norm_nonneg
      heps_nonneg hkappa hdelta hAplus hDeltaA hDeltab hDeltaA_norm_budget
      hDeltab_norm_budget hleftA hleftB hSymA hSymB hrangeA_residual
      hPBIPA hB hr hs horth_s
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.2):
    minimizer-facing printed residual-side Wedin bound, conditional only on
    the exact cross-projection norm equality from the open Stewart--Sun/CS
    route.

This discharges perturbed residual column orthogonality from exact optimality
and derives the source-strength projector estimate through the
`complexMatrixOp2` equality wrapper in the perturbation module. -/
theorem IsLeastSquaresMinimizer.wedin_residualRelativeRHS_le_of_crossProjection_eq_geometry
    {m k : ℕ} (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {delta Aplus_norm DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hb_norm_pos : 0 < vecNorm2 b)
    (hA_norm_nonneg : 0 ≤ A_norm)
    (heps_nonneg : 0 ≤ eps)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (hAplus : rectOpNorm2Le Aplus Aplus_norm)
    (hDelta : rectOpNorm2Le (fun i j => B i j - A i j) delta)
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget : Deltab_norm ≤ eps * vecNorm2 b)
    (hleftA : rectMatMul Aplus A = idMatrix (k + 1))
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hEq :
      complexMatrixOp2
          (realRectToCMatrix
            (rectMatMul
              (rectMatMul B Bplus)
              (fun i j => idMatrix m i j - rectMatMul A Aplus i j))) =
        complexMatrixOp2
          (realRectToCMatrix
            (rectMatMul
              (rectMatMul A Aplus)
              (fun i j => idMatrix m i j - rectMatMul B Bplus i j))))
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun i => r i - s i) / vecNorm2 b ≤
      wedinTheorem20_1ResidualRelativeRHS kappa eps := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_residualRelativeRHS_le_of_residual_definitions_crossProjection_eq_geometry_column_orthogonal
      A B Aplus Bplus DeltaA b Deltab r s x y hb_norm_pos hA_norm_nonneg
      heps_nonneg hkappa hdelta hAplus hDelta hDeltaA hDeltab
      hDeltaA_norm_budget hDeltab_norm_budget hleftA hleftB hSymA hSymB
      hrangeA_residual hEq hB hr hs horth_s
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.2):
    minimizer-facing residual-side Wedin bound using the proved Lemma 20.12
    source `min` surface.

This is the exact-LS caller-facing version of
`wedinTheorem20_1_residualRelativeRHS_le_of_residual_definitions_min_surface_geometry_column_orthogonal`.
The perturbed residual column orthogonality is discharged from exact
least-squares optimality; the Lemma 20.11 and Lemma 20.12 radius/projection
steps are handled by the imported Wedin wrapper. -/
theorem IsLeastSquaresMinimizer.wedin_residualRelativeRHS_le_of_min_surface_geometry
    {m k : ℕ} (hm : 0 < m) (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {delta Aplus_norm DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hb_norm_pos : 0 < vecNorm2 b)
    (hAplus_pos : 0 < Aplus_norm)
    (hA_norm_nonneg : 0 ≤ A_norm)
    (heps_nonneg : 0 ≤ eps)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (hsmall : kappa * eps < 1)
    (hAplus : rectOpNorm2Le Aplus Aplus_norm)
    (hDelta : rectOpNorm2Le (fun i j => B i j - A i j) delta)
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget : Deltab_norm ≤ eps * vecNorm2 b)
    (hleftA : rectMatMul Aplus A = idMatrix (k + 1))
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun i => r i - s i) / vecNorm2 b ≤
      wedinTheorem20_1ResidualRelativeRHS kappa eps := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_residualRelativeRHS_le_of_residual_definitions_min_surface_geometry_column_orthogonal
      hm A B Aplus Bplus DeltaA b Deltab r s x y hb_norm_pos
      hAplus_pos hA_norm_nonneg heps_nonneg hkappa hdelta hsmall
      hAplus hDelta hDeltaA hDeltab hDeltaA_norm_budget
      hDeltab_norm_budget hleftA hleftB hSymA hSymB hrangeA_residual
      hB hr hs horth_s
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.2):
    minimizer-facing residual-side Wedin bound using exact optimality for both
    the source and perturbed least-squares problems.

The source minimizer supplies the `P_A r = 0` residual-projection condition;
the perturbed minimizer supplies residual column orthogonality.  Lemmas 20.11
and 20.12 still discharge the Wedin pseudoinverse and projector estimates
through the min-surface wrapper. -/
theorem
    IsLeastSquaresMinimizer.wedin_residualRelativeRHS_le_of_min_surface_geometry_source_minimizer
    {m k : ℕ} (hm : 0 < m) (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {delta Aplus_norm DeltaA_norm Deltab_norm kappa eps A_norm : ℝ}
    (hExactMin : IsLeastSquaresMinimizer A b x)
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hb_norm_pos : 0 < vecNorm2 b)
    (hAplus_pos : 0 < Aplus_norm)
    (hA_norm_nonneg : 0 ≤ A_norm)
    (heps_nonneg : 0 ≤ eps)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (hsmall : kappa * eps < 1)
    (hAplus : rectOpNorm2Le Aplus Aplus_norm)
    (hDelta : rectOpNorm2Le (fun i j => B i j - A i j) delta)
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget : Deltab_norm ≤ eps * vecNorm2 b)
    (hleftA : rectMatMul Aplus A = idMatrix (k + 1))
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun i => r i - s i) / vecNorm2 b ≤
      wedinTheorem20_1ResidualRelativeRHS kappa eps := by
  have hrangeA_residual :
      rectMatMulVec (rectMatMul A Aplus) r = 0 :=
    IsLeastSquaresMinimizer.wedin_rangeProjection_higham_residual_eq_zero
      (A := A) (Aplus := Aplus) (b := b) (r := r) (x := x)
      hExactMin hSymA hr
  exact
    IsLeastSquaresMinimizer.wedin_residualRelativeRHS_le_of_min_surface_geometry
      hm A B Aplus Bplus DeltaA b Deltab r s x y hPertMin hb_norm_pos
      hAplus_pos hA_norm_nonneg heps_nonneg hkappa hdelta hsmall
      hAplus hDelta hDeltaA hDeltab hDeltaA_norm_budget
      hDeltab_norm_budget hleftA hleftB hSymA hSymB hrangeA_residual
      hB hr hs
/-- Higham, 2nd ed., Chapter 20, Theorem 20.1, equation (20.2), conservative
    residual-side Wedin bound with perturbed least-squares optimality as the
    caller-facing hypothesis.

This is the minimizer-facing version of
`wedinTheorem20_1_residualRelativeRHSConservative_le_of_residual_definitions_Bplus_bound_geometry_column_orthogonal`.
It discharges residual column orthogonality from exact optimality and uses the
available one-sided Lemma 20.12 plus a Lemma 20.11-style `Bplus` radius.  The
conclusion is conservative, not the printed `(1+2*kappa)*eps` RHS. -/
theorem IsLeastSquaresMinimizer.wedin_residualRelativeRHSConservative_le_of_Bplus_bound_geometry
    {m k : ℕ} (A B : Fin m → Fin (k + 1) → ℝ)
    (Aplus Bplus : Fin (k + 1) → Fin m → ℝ)
    (DeltaA : Fin m → Fin (k + 1) → ℝ) (b Deltab r s : Fin m → ℝ)
    (x y : Fin (k + 1) → ℝ)
    {delta Aplus_norm DeltaA_norm Deltab_norm kappa eps A_norm eta : ℝ}
    (hPertMin : IsLeastSquaresMinimizer B (fun i => b i + Deltab i) y)
    (hb_norm_pos : 0 < vecNorm2 b)
    (hA_norm_nonneg : 0 ≤ A_norm)
    (heps_nonneg : 0 ≤ eps)
    (hkappa : kappa = Aplus_norm * A_norm)
    (hdelta : delta = eps * A_norm)
    (heta : eta = kappa * eps)
    (hsmall : kappa * eps < 1)
    (hAplus : rectOpNorm2Le Aplus Aplus_norm)
    (hBplus : rectOpNorm2Le Bplus (Aplus_norm / (1 - eta)))
    (hDelta : rectOpNorm2Le (fun i j => B i j - A i j) delta)
    (hDeltaA : rectOpNorm2Le DeltaA DeltaA_norm)
    (hDeltab : vecNorm2 Deltab ≤ Deltab_norm)
    (hDeltaA_norm_budget : DeltaA_norm ≤ eps * A_norm)
    (hDeltab_norm_budget : Deltab_norm ≤ eps * vecNorm2 b)
    (hleftA : rectMatMul Aplus A = idMatrix (k + 1))
    (hleftB : rectMatMul Bplus B = idMatrix (k + 1))
    (hSymA : IsSymmetricFiniteMatrix (rectMatMul A Aplus))
    (hSymB : IsSymmetricFiniteMatrix (rectMatMul B Bplus))
    (hrangeA_residual : rectMatMulVec (rectMatMul A Aplus) r = 0)
    (hB : B = fun i j => A i j + DeltaA i j)
    (hr : r = fun i => b i - rectMatMulVec A x i)
    (hs : s = fun i => (b i + Deltab i) - rectMatMulVec B y i) :
    vecNorm2 (fun i => r i - s i) / vecNorm2 b ≤
      wedinTheorem20_1ResidualRelativeRHSConservative kappa eps := by
  have horth_s :
      ∀ j : Fin (k + 1), ∑ i : Fin m, B i j * s i = 0 :=
    IsLeastSquaresMinimizer.wedin_perturbed_residual_column_orthogonal
      (B := B) (b := b) (Deltab := Deltab) (s := s) (y := y)
      hPertMin hs
  exact
    wedinTheorem20_1_residualRelativeRHSConservative_le_of_residual_definitions_Bplus_bound_geometry_column_orthogonal
      A B Aplus Bplus DeltaA b Deltab r s x y hb_norm_pos hA_norm_nonneg
      heps_nonneg hkappa hdelta heta hsmall hAplus hBplus hDelta hDeltaA
      hDeltab hDeltaA_norm_budget hDeltab_norm_budget hleftA hleftB
      hSymA hSymB hrangeA_residual hB hr hs horth_s
/-- Higham-signed residual-orthogonality form of normwise backward-error
    feasibility.  This rewrites the consistency characterization using the
    chapter's residual convention `r = b - A y`. -/
theorem LSNormwiseBackwardErrorFeasible.iff_perturbed_higham_residual_orthogonal
    {m n : ℕ} (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ) :
    LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ↔
      ∀ j : Fin n,
        ∑ i : Fin m, (A i j + DeltaA i j) *
          lsResidualHigham
            (fun i j => A i j + DeltaA i j)
            (fun i => b i + Deltab i) y i = 0 := by
  constructor
  · intro h j
    have horth :
        ∑ i : Fin m, (A i j + DeltaA i j) *
          lsResidual
            (fun i j => A i j + DeltaA i j)
            (fun i => b i + Deltab i) y i = 0 :=
      (LSNormwiseBackwardErrorFeasible.iff_perturbed_residual_orthogonal
        A b y DeltaA Deltab).mp h j
    calc
      ∑ i : Fin m, (A i j + DeltaA i j) *
          lsResidualHigham
            (fun i j => A i j + DeltaA i j)
            (fun i => b i + Deltab i) y i
          = -∑ i : Fin m, (A i j + DeltaA i j) *
              lsResidual
                (fun i j => A i j + DeltaA i j)
                (fun i => b i + Deltab i) y i := by
            exact lsResidualHigham_column_sum_eq_neg
              (fun i j => A i j + DeltaA i j)
              (fun i => b i + Deltab i) y j
      _ = 0 := by rw [horth, neg_zero]
  · intro h
    apply
      (LSNormwiseBackwardErrorFeasible.iff_perturbed_residual_orthogonal
        A b y DeltaA Deltab).mpr
    intro j
    have hneg :
        ∑ i : Fin m, (A i j + DeltaA i j) *
          lsResidualHigham
            (fun i j => A i j + DeltaA i j)
            (fun i => b i + Deltab i) y i =
          -∑ i : Fin m, (A i j + DeltaA i j) *
            lsResidual
              (fun i j => A i j + DeltaA i j)
              (fun i => b i + Deltab i) y i :=
      lsResidualHigham_column_sum_eq_neg
        (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y j
    have hsum :
        -∑ i : Fin m, (A i j + DeltaA i j) *
          lsResidual
            (fun i j => A i j + DeltaA i j)
            (fun i => b i + Deltab i) y i = 0 := by
      simpa [hneg] using h j
    exact neg_eq_zero.mp hsum
/-- Source-data expansion of the WKS feasibility condition: a perturbation pair
    is feasible for (20.20) exactly when the columns of `A + Delta A` are
    orthogonal to `(b - A y) + Delta b - Delta A y`. -/
theorem LSNormwiseBackwardErrorFeasible.iff_source_higham_residual_orthogonal
    {m n : ℕ} (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ) :
    LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ↔
      ∀ j : Fin n,
        ∑ i : Fin m, (A i j + DeltaA i j) *
          (lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i) = 0 := by
  constructor
  · intro h j
    have horth :=
      (LSNormwiseBackwardErrorFeasible.iff_perturbed_higham_residual_orthogonal
        A b y DeltaA Deltab).mp h j
    simpa [lsResidualHigham_perturbed_eq] using horth
  · intro h
    apply
      (LSNormwiseBackwardErrorFeasible.iff_perturbed_higham_residual_orthogonal
        A b y DeltaA Deltab).mpr
    intro j
    have hj := h j
    simpa [lsResidualHigham_perturbed_eq] using hj
/-- Constructive source-residual feasibility witness for the WKS
    source-block route.  Given any nonzero target expanded residual `p`, the
    rank-one perturbation with left action `-Aᵀp` and the corresponding
    `Delta b` make `y` an exact least-squares minimizer for the perturbed
    problem. -/
theorem LSNormwiseBackwardErrorFeasible.rankOne_source_residual_witness
    {m n : ℕ} (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (y : Fin n → ℝ)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0) :
    let DeltaA : Fin m → Fin n → ℝ :=
      lsNormwiseBackwardErrorRankOneDeltaA p
        (fun j => ∑ i : Fin m, A i j * p i)
    let Deltab : Fin m → ℝ :=
      fun i => p i - lsResidualHigham A b y i + rectMatMulVec DeltaA y i
    LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab := by
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let DeltaA : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankOneDeltaA p u
  let Deltab : Fin m → ℝ :=
    fun i => p i - lsResidualHigham A b y i + rectMatMulVec DeltaA y i
  dsimp only
  apply
    (LSNormwiseBackwardErrorFeasible.iff_source_higham_residual_orthogonal
      A b y DeltaA Deltab).mpr
  intro j
  have hp_expand :
      (fun i : Fin m =>
        lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) =
        p := by
    ext i
    dsimp [Deltab]
    ring
  have hDelta :
      (fun j : Fin n => ∑ i : Fin m, DeltaA i j * p i) =
        fun j => -u j := by
    simpa [DeltaA] using
      (lsNormwiseBackwardErrorRankOneDeltaA_transpose_mul
        (p := p) (u := u) hp)
  have hDelta_j : ∑ i : Fin m, DeltaA i j * p i = -u j := by
    exact congrFun hDelta j
  calc
    ∑ i : Fin m, (A i j + DeltaA i j) *
        (lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i)
        = ∑ i : Fin m, (A i j + DeltaA i j) * p i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [congrFun hp_expand i]
    _ = (∑ i : Fin m, A i j * p i) +
          ∑ i : Fin m, DeltaA i j * p i := by
            simp [add_mul, Finset.sum_add_distrib]
    _ = u j + (-u j) := by
            simp [u, hDelta_j]
    _ = 0 := by ring
/-- Bundled rank-one WKS source-residual witness.  This packages the
    constructive perturbation with the exact expanded residual, left
    transpose-action, and Frobenius component needed by the later
    source-block `sigma_min` upper-bound cost proof. -/
theorem LSNormwiseBackwardErrorFeasible.exists_rankOne_source_residual_witness
    {m n : ℕ} (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ) (y : Fin n → ℝ)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0) :
    ∃ (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ),
      LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
        (fun i : Fin m =>
          lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) =
          p ∧
        (fun j : Fin n => ∑ i : Fin m, DeltaA i j * p i) =
          (fun j => -(∑ i : Fin m, A i j * p i)) ∧
        frobNormSqRect DeltaA =
          vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) /
            vecNorm2Sq p := by
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let DeltaA : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankOneDeltaA p u
  let Deltab : Fin m → ℝ :=
    fun i => p i - lsResidualHigham A b y i + rectMatMulVec DeltaA y i
  refine ⟨DeltaA, Deltab, ?_, ?_, ?_, ?_⟩
  · simpa [DeltaA, Deltab, u] using
      (LSNormwiseBackwardErrorFeasible.rankOne_source_residual_witness
        A b y p hp)
  · ext i
    dsimp [Deltab]
    ring
  · simpa [DeltaA, u] using
      (lsNormwiseBackwardErrorRankOneDeltaA_transpose_mul
        (p := p) (u := u) hp)
  · simpa [DeltaA, u] using
      (lsNormwiseBackwardErrorRankOneDeltaA_frobNormSq
        (p := p) (u := u) hp)
/-- Rank-one WKS source-residual upper bound.  Any nonzero expanded residual
    candidate `p` gives an explicit feasible perturbation, hence an explicit
    upper bound for `eta_F`; a later `sigma_min` branch proof must choose `p`
    from the source block so this square-root expression is bounded by the
    printed row-side singular value. -/
theorem lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (b : Fin m → ℝ) (y : Fin n → ℝ)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0) :
    let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
    let DeltaA : Fin m → Fin n → ℝ :=
      lsNormwiseBackwardErrorRankOneDeltaA p u
    let Deltab : Fin m → ℝ :=
      fun i => p i - lsResidualHigham A b y i + rectMatMulVec DeltaA y i
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 * vecNorm2Sq Deltab) := by
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let DeltaA : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankOneDeltaA p u
  let Deltab : Fin m → ℝ :=
    fun i => p i - lsResidualHigham A b y i + rectMatMulVec DeltaA y i
  dsimp only
  have hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab := by
    simpa [DeltaA, Deltab, u] using
      (LSNormwiseBackwardErrorFeasible.rankOne_source_residual_witness
        A b y p hp)
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
    lsNormwiseBackwardErrorEtaF_le_costF_of_feasible
      theta A b y DeltaA Deltab hfeas
  have hcost :
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
        Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
          theta ^ 2 * vecNorm2Sq Deltab) := by
    rw [lsNormwiseBackwardErrorCostF_eq_sqrt_sq_sum]
    rw [lsNormwiseBackwardErrorRankOneDeltaA_frobNormSq (p := p) (u := u) hp]
  exact hcost ▸ heta
/-- Expanded rank-one WKS source-residual upper bound.  This rewrites the
    `Delta b` component of the rank-one witness cost using the explicit
    matrix-vector action of the rank-one `DeltaA`, preparing the later
    source-block singular-vector certificate. -/
theorem lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness_expanded
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (b : Fin m → ℝ) (y : Fin n → ℝ)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0) :
    let r : Fin m → ℝ := lsResidualHigham A b y
    let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin m =>
              p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) := by
  let r : Fin m → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let DeltaA : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankOneDeltaA p u
  let Deltab : Fin m → ℝ :=
    fun i => p i - r i + rectMatMulVec DeltaA y i
  have hbase :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
          theta ^ 2 * vecNorm2Sq Deltab) := by
    simpa [r, u, DeltaA, Deltab] using
      (lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness
        theta A b y p hp)
  have hDeltab :
      Deltab =
        fun i : Fin m =>
          p i - r i -
            ((1 / vecNorm2Sq p) * p i * (∑ j : Fin n, u j * y j)) := by
    ext i
    have hmul :
        rectMatMulVec DeltaA y i =
          -((1 / vecNorm2Sq p) * p i * (∑ j : Fin n, u j * y j)) := by
      simpa [DeltaA] using
        congrFun
          (lsNormwiseBackwardErrorRankOneDeltaA_mulVec
            (p := p) (u := u) (y := y)) i
    dsimp [Deltab]
    rw [hmul]
    ring
  simpa [r, u, hDeltab] using hbase
/-- Scaled expanded rank-one WKS source-residual upper bound.  Scaling the
    expanded residual candidate by a nonzero scalar preserves the rank-one
    Frobenius component and leaves the source-action term invariant, while
    exposing the free scalar in the `Delta b` part of the witness cost. -/
theorem lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness_scaled
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (b : Fin m → ℝ) (y : Fin n → ℝ)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (c : ℝ) (hc : c ≠ 0) :
    let r : Fin m → ℝ := lsResidualHigham A b y
    let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin m =>
              c * p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) := by
  let r : Fin m → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let q : Fin m → ℝ := fun i => c * p i
  let uq : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * q i
  have hc_sq : c ^ 2 ≠ 0 := pow_ne_zero 2 hc
  have hqnorm : vecNorm2Sq q = c ^ 2 * vecNorm2Sq p := by
    simpa [q] using (vecNorm2Sq_smul c p)
  have hq : vecNorm2Sq q ≠ 0 := by
    rw [hqnorm]
    exact mul_ne_zero hc_sq hp
  have huq : uq = fun j => c * u j := by
    ext j
    dsimp [uq, u, q]
    calc
      ∑ i : Fin m, A i j * (c * p i)
          = ∑ i : Fin m, c * (A i j * p i) := by
              apply Finset.sum_congr rfl
              intro i _
              ring
      _ = c * ∑ i : Fin m, A i j * p i := by
              rw [Finset.mul_sum]
  have huqnorm : vecNorm2Sq uq = c ^ 2 * vecNorm2Sq u := by
    rw [huq]
    exact vecNorm2Sq_smul c u
  have hratio :
      vecNorm2Sq uq / vecNorm2Sq q = vecNorm2Sq u / vecNorm2Sq p := by
    rw [huqnorm, hqnorm]
    field_simp [hp, hc_sq]
  have hdot :
      (∑ j : Fin n, uq j * y j) = c * (∑ j : Fin n, u j * y j) := by
    rw [huq]
    calc
      ∑ j : Fin n, (c * u j) * y j
          = ∑ j : Fin n, c * (u j * y j) := by
              apply Finset.sum_congr rfl
              intro j _
              ring
      _ = c * ∑ j : Fin n, u j * y j := by
              rw [Finset.mul_sum]
  have hproj :
      (fun i : Fin m =>
        (1 / vecNorm2Sq q) * q i * (∑ j : Fin n, uq j * y j)) =
        fun i : Fin m =>
          (1 / vecNorm2Sq p) * p i * (∑ j : Fin n, u j * y j) := by
    ext i
    rw [hdot]
    dsimp [q]
    rw [hqnorm]
    field_simp [hp, hc, hc_sq]
  have hDeltab :
      (fun i : Fin m =>
        q i - r i -
          ((1 / vecNorm2Sq q) * q i * (∑ j : Fin n, uq j * y j))) =
        fun i : Fin m =>
          c * p i - r i -
            ((1 / vecNorm2Sq p) * p i * (∑ j : Fin n, u j * y j)) := by
    ext i
    rw [congrFun hproj i]
  have hbase :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        Real.sqrt (vecNorm2Sq uq / vecNorm2Sq q +
          theta ^ 2 *
            vecNorm2Sq
              (fun i : Fin m =>
                q i - r i -
                  ((1 / vecNorm2Sq q) * q i *
                    (∑ j : Fin n, uq j * y j)))) := by
    simpa [r, q, uq] using
      (lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness_expanded
        theta A b y q hq)
  rw [hratio, hDeltab] at hbase
  simpa [r, u] using hbase
/-- The free scalar in the scaled rank-one WKS witness only changes the
    component parallel to `p`.  After applying the complementary projector
    `I-p p^+`, the explicit right-hand-side perturbation term is always the
    negative projection of the source residual `r`. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_q_projected_eq_neg
    {m : ℕ} (p r : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (t c : ℝ) :
    matMulVec m (lsLemma20_6ProjectorComplement p)
        (fun i : Fin m =>
          c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)) =
      fun i : Fin m =>
        -matMulVec m (lsLemma20_6ProjectorComplement p) r i := by
  let Qp : Fin m → Fin m → ℝ := lsLemma20_6ProjectorComplement p
  ext i
  have hself := congrFun (lsLemma20_6ProjectorComplement_mulVec_self p hp) i
  have hpzero : ∑ j : Fin m, Qp i j * p j = 0 := by
    simpa [Qp, matMulVec] using hself
  calc
    matMulVec m (lsLemma20_6ProjectorComplement p)
        (fun i : Fin m =>
          c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)) i
        =
      ∑ j : Fin m,
        Qp i j *
          (c * p j - r j - ((1 / vecNorm2Sq p) * p j * t)) := by
          rfl
    _ =
      c * (∑ j : Fin m, Qp i j * p j) -
        (∑ j : Fin m, Qp i j * r j) -
          ((1 / vecNorm2Sq p) * t) *
            (∑ j : Fin m, Qp i j * p j) := by
          calc
            ∑ j : Fin m,
                Qp i j *
                  (c * p j - r j - ((1 / vecNorm2Sq p) * p j * t))
                =
              ∑ j : Fin m,
                (c * (Qp i j * p j) -
                  Qp i j * r j -
                    ((1 / vecNorm2Sq p) * t) * (Qp i j * p j)) := by
                apply Finset.sum_congr rfl
                intro j _
                ring
            _ =
              c * (∑ j : Fin m, Qp i j * p j) -
                (∑ j : Fin m, Qp i j * r j) -
                  ((1 / vecNorm2Sq p) * t) *
                    (∑ j : Fin m, Qp i j * p j) := by
                rw [Finset.sum_sub_distrib, Finset.sum_sub_distrib,
                  Finset.mul_sum, Finset.mul_sum]
    _ = -matMulVec m (lsLemma20_6ProjectorComplement p) r i := by
          rw [hpzero]
          simp [Qp, matMulVec]
/-- Any scalar choice in the scaled rank-one WKS witness has squared norm at
    least the squared norm of the source residual projected away from `p`.
    Thus the remaining upper-bound route must account for this unavoidable
    orthogonal component. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_q_projector_lower_bound
    {m : ℕ} (p r : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (t c : ℝ) :
    vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) ≤
      vecNorm2Sq
        (fun i : Fin m =>
          c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)) := by
  let q : Fin m → ℝ :=
    fun i : Fin m => c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)
  let Qr : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  have hproj :
      matMulVec m (lsLemma20_6ProjectorComplement p) q =
        fun i : Fin m => -Qr i := by
    simpa [q, Qr] using
      lsNormwiseBackwardErrorRankOne_scaled_q_projected_eq_neg p r hp t c
  have hnorm :
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) q) =
        vecNorm2Sq Qr := by
    rw [hproj]
    simpa [Qr] using (vecNorm2Sq_smul (-1 : ℝ) Qr)
  have hcontract :
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) q) ≤
        vecNorm2Sq q :=
    lsLemma20_6ProjectorComplement_vecNorm2Sq_le p hp q
  rw [hnorm] at hcontract
  simpa [q, Qr] using hcontract
/-- Optimal scalar choice for the scaled rank-one WKS witness.  Choosing
    `c = (p^T r + t) / ||p||_2^2` makes the explicit `Delta b` component
    exactly the negative complement projection of `r` away from `p`, hence
    realizes the lower bound from
    `lsNormwiseBackwardErrorRankOne_scaled_q_projector_lower_bound`. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_q_optimal_vecNorm2Sq
    {m : ℕ} (p r : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (t : ℝ) :
    vecNorm2Sq
        (fun i : Fin m =>
          (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p) * p i -
            r i - ((1 / vecNorm2Sq p) * p i * t)) =
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) := by
  let dot : ℝ := ∑ k : Fin m, p k * r k
  let q : Fin m → ℝ :=
    fun i : Fin m =>
      ((dot + t) / vecNorm2Sq p) * p i -
        r i - ((1 / vecNorm2Sq p) * p i * t)
  let Qr : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  have hq : q = fun i : Fin m => -Qr i := by
    ext i
    have hentry :
        Qr i = r i - (p i / vecNorm2Sq p) * dot := by
      simpa [Qr, dot, matMulVec] using
        (lsLemma20_6ProjectorComplement_apply_vec p r i)
    dsimp [q]
    rw [hentry]
    field_simp [hp]
    ring
  calc
    vecNorm2Sq
        (fun i : Fin m =>
          (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p) * p i -
            r i - ((1 / vecNorm2Sq p) * p i * t))
        = vecNorm2Sq q := by
          rfl
    _ = vecNorm2Sq (fun i : Fin m => -Qr i) := by
          rw [hq]
    _ = vecNorm2Sq Qr := by
          simpa using (vecNorm2Sq_smul (-1 : ℝ) Qr)
    _ = vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) := by
          rfl
/-- Vector form of the optimal scalar choice for the scaled rank-one WKS
    witness.  The optimized explicit `Delta b` component is the negative
    complement projection of the source residual away from `p`. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_q_optimal_eq_neg_projector
    {m : ℕ} (p r : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (t : ℝ) :
    (fun i : Fin m =>
      (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p) * p i -
        r i - ((1 / vecNorm2Sq p) * p i * t)) =
      fun i : Fin m =>
        -matMulVec m (lsLemma20_6ProjectorComplement p) r i := by
  let dot : ℝ := ∑ k : Fin m, p k * r k
  let Qr : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  ext i
  have hentry :
      Qr i = r i - (p i / vecNorm2Sq p) * dot := by
    simpa [Qr, dot, matMulVec] using
      (lsLemma20_6ProjectorComplement_apply_vec p r i)
  change ((dot + t) / vecNorm2Sq p) * p i - r i -
      ((1 / vecNorm2Sq p) * p i * t) = -Qr i
  rw [hentry]
  field_simp [hp]
  ring
/-- Orthogonality of the optimized scaled rank-one WKS `Delta b` component:
    after the optimal scalar is chosen, the remaining component is orthogonal
    to the source direction `p`. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_q_optimal_dot_self_eq_zero
    {m : ℕ} (p r : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (t : ℝ) :
    ∑ i : Fin m,
      (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p * p i -
        r i - (1 / vecNorm2Sq p * p i * t)) * p i = 0 := by
  let Qp : Fin m → Fin m → ℝ := lsLemma20_6ProjectorComplement p
  let Qr : Fin m → ℝ := matMulVec m Qp r
  have hq :
      (fun i : Fin m =>
        (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p) * p i -
          r i - ((1 / vecNorm2Sq p) * p i * t)) =
        fun i : Fin m => -Qr i := by
    simpa [Qr, Qp] using
      lsNormwiseBackwardErrorRankOne_scaled_q_optimal_eq_neg_projector
        p r hp t
  have hQr_dot : ∑ i : Fin m, Qr i * p i = 0 := by
    unfold Qr Qp matMulVec
    calc
      ∑ i : Fin m,
          (∑ j : Fin m, lsLemma20_6ProjectorComplement p i j * r j) * p i
          =
        ∑ j : Fin m, r j *
          ∑ i : Fin m, lsLemma20_6ProjectorComplement p i j * p i := by
            calc
              ∑ i : Fin m,
                  (∑ j : Fin m,
                    lsLemma20_6ProjectorComplement p i j * r j) * p i
                  =
                ∑ i : Fin m, ∑ j : Fin m,
                  (lsLemma20_6ProjectorComplement p i j * r j) * p i := by
                    apply Finset.sum_congr rfl
                    intro i _
                    rw [Finset.sum_mul]
              _ =
                ∑ j : Fin m, ∑ i : Fin m,
                  (lsLemma20_6ProjectorComplement p i j * r j) * p i := by
                    rw [Finset.sum_comm]
              _ =
                ∑ j : Fin m, r j *
                  ∑ i : Fin m, lsLemma20_6ProjectorComplement p i j * p i := by
                    apply Finset.sum_congr rfl
                    intro j _
                    rw [Finset.mul_sum]
                    apply Finset.sum_congr rfl
                    intro i _
                    ring
      _ = 0 := by
            apply Finset.sum_eq_zero
            intro j _
            rw [lsLemma20_6ProjectorComplement_transpose_apply_self p hp j]
            ring
  calc
    ∑ i : Fin m,
      (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p * p i -
        r i - (1 / vecNorm2Sq p * p i * t)) * p i
        = ∑ i : Fin m, (fun i : Fin m => -Qr i) i * p i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [congrFun hq i]
    _ = -∑ i : Fin m, Qr i * p i := by
            rw [← Finset.sum_neg_distrib]
            apply Finset.sum_congr rfl
            intro i _
            ring
    _ = 0 := by rw [hQr_dot, neg_zero]
/-- Frobenius norm of the extra rank-two outer-product perturbation. -/
theorem lsNormwiseBackwardErrorRankTwoExtraDeltaA_frobNormSq
    {m n : ℕ} (beta : ℝ) (q : Fin m → ℝ)
    {y : Fin n → ℝ} (hy : vecNorm2Sq y ≠ 0) :
    frobNormSqRect (lsNormwiseBackwardErrorRankTwoExtraDeltaA beta q y) =
      beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y := by
  have h :=
    frobNormSqRect_rankOne_real (m := m) (n := n)
      (beta / vecNorm2Sq y) q y
  calc
    frobNormSqRect (lsNormwiseBackwardErrorRankTwoExtraDeltaA beta q y)
        = (beta / vecNorm2Sq y) ^ 2 * vecNorm2Sq q * vecNorm2Sq y := by
            simpa [lsNormwiseBackwardErrorRankTwoExtraDeltaA] using h
    _ = beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y := by
            field_simp [hy]
/-- Frobenius square of the combined rank-one plus extra outer-product WKS
    matrix perturbation.  Orthogonality of the extra left vector to `p`
    kills the cross term. -/
theorem lsNormwiseBackwardErrorRankTwoDeltaA_frobNormSq
    {m n : ℕ} {p : Fin m → ℝ} {u : Fin n → ℝ}
    {beta : ℝ} {q : Fin m → ℝ} {y : Fin n → ℝ}
    (hp : vecNorm2Sq p ≠ 0) (hy : vecNorm2Sq y ≠ 0)
    (hq_orth : ∑ i : Fin m, q i * p i = 0) :
    frobNormSqRect
        (fun i j =>
          lsNormwiseBackwardErrorRankOneDeltaA p u i j +
            lsNormwiseBackwardErrorRankTwoExtraDeltaA beta q y i j) =
      vecNorm2Sq u / vecNorm2Sq p +
        beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y := by
  let DeltaA1 : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankOneDeltaA p u
  let DeltaA2 : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankTwoExtraDeltaA beta q y
  have hpq : ∑ i : Fin m, p i * q i = 0 := by
    calc
      ∑ i : Fin m, p i * q i = ∑ i : Fin m, q i * p i := by
        apply Finset.sum_congr rfl
        intro i _
        ring
      _ = 0 := hq_orth
  have hinner :
      (∑ i : Fin m, ∑ j : Fin n, DeltaA1 i j * DeltaA2 i j) = 0 := by
    dsimp [DeltaA1, DeltaA2, lsNormwiseBackwardErrorRankOneDeltaA,
      lsNormwiseBackwardErrorRankTwoExtraDeltaA]
    calc
      ∑ i : Fin m, ∑ j : Fin n,
          (-((1 / vecNorm2Sq p) * p i * u j)) *
            (beta / vecNorm2Sq y * q i * y j)
          =
        ∑ i : Fin m,
          (-(1 / vecNorm2Sq p) * (beta / vecNorm2Sq y) * (p i * q i)) *
            ∑ j : Fin n, u j * y j := by
            apply Finset.sum_congr rfl
            intro i _
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro j _
            ring
      _ =
        (-(1 / vecNorm2Sq p) * (beta / vecNorm2Sq y)) *
          (∑ i : Fin m, p i * q i) *
            ∑ j : Fin n, u j * y j := by
          calc
            ∑ i : Fin m,
                (-(1 / vecNorm2Sq p) * (beta / vecNorm2Sq y) *
                  (p i * q i)) *
                  ∑ j : Fin n, u j * y j
                =
              (∑ i : Fin m,
                (-(1 / vecNorm2Sq p) * (beta / vecNorm2Sq y) *
                  (p i * q i))) *
                  ∑ j : Fin n, u j * y j := by
                rw [Finset.sum_mul]
            _ =
              (-(1 / vecNorm2Sq p) * (beta / vecNorm2Sq y)) *
                (∑ i : Fin m, p i * q i) *
                  ∑ j : Fin n, u j * y j := by
                congr 1
                rw [Finset.mul_sum]
      _ = 0 := by
          rw [hpq]
          ring
  have hsum :
      frobNormSqRect (fun i j => DeltaA1 i j + DeltaA2 i j) =
        frobNormSqRect DeltaA1 + frobNormSqRect DeltaA2 :=
    frobNormSqRect_add_of_inner_eq_zero DeltaA1 DeltaA2 hinner
  have h1 :
      frobNormSqRect DeltaA1 = vecNorm2Sq u / vecNorm2Sq p := by
    simpa [DeltaA1] using
      (lsNormwiseBackwardErrorRankOneDeltaA_frobNormSq
        (p := p) (u := u) hp)
  have h2 :
      frobNormSqRect DeltaA2 =
        beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y := by
    simpa [DeltaA2] using
      (lsNormwiseBackwardErrorRankTwoExtraDeltaA_frobNormSq
        (m := m) (n := n) beta q (y := y) hy)
  simpa [DeltaA1, DeltaA2, h1, h2] using hsum
/-- Scalar optimization for the rank-two WKS upper witness.  After the
    rank-one source-residual witness leaves a residual component `q`
    orthogonal to the chosen direction, adding a second outer-product
    perturbation in the `y` direction with the displayed coefficient lowers the
    combined Frobenius/RHS cost by exactly the missing
    `1 + theta^2 ||y||_2^2` denominator. -/
theorem lsNormwiseBackwardErrorRankTwo_extra_weighted_cost_eq
    {m n : ℕ} (theta : ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (q : Fin m → ℝ) :
    let Y : ℝ := vecNorm2Sq y
    let D : ℝ := 1 + theta ^ 2 * Y
    let beta : ℝ := -(theta ^ 2 * Y) / D
    beta ^ 2 * vecNorm2Sq q / Y +
        theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i) =
      theta ^ 2 * vecNorm2Sq q / D := by
  let Y : ℝ := vecNorm2Sq y
  let D : ℝ := 1 + theta ^ 2 * Y
  let beta : ℝ := -(theta ^ 2 * Y) / D
  have hYpos : 0 < Y := by
    simpa [Y] using vecNorm2Sq_pos_of_ne_zero_lsq hy
  have hDpos : 0 < D := by
    have htheta_sq : 0 ≤ theta ^ 2 := sq_nonneg theta
    have hprod : 0 ≤ theta ^ 2 * Y := mul_nonneg htheta_sq (le_of_lt hYpos)
    dsimp [D]
    linarith
  have hbeta : 1 + beta = 1 / D := by
    dsimp [beta, D]
    field_simp [ne_of_gt hDpos]
    ring
  have hvec :
      (fun i : Fin m => q i + beta * q i) =
        fun i : Fin m => (1 + beta) * q i := by
    ext i
    ring
  have hYne : Y ≠ 0 := ne_of_gt hYpos
  have hDne : D ≠ 0 := ne_of_gt hDpos
  have hDne' : 1 + theta ^ 2 * Y ≠ 0 := by
    simpa [D] using hDne
  have hcoeff :
      beta ^ 2 / Y + theta ^ 2 * (1 / D) ^ 2 = theta ^ 2 / D := by
    dsimp [beta, D]
    field_simp [hYne, hDne']
    ring
  change
    beta ^ 2 * vecNorm2Sq q / Y +
        theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i) =
      theta ^ 2 * vecNorm2Sq q / D
  rw [hvec, vecNorm2Sq_smul, hbeta]
  calc
    beta ^ 2 * vecNorm2Sq q / Y +
        theta ^ 2 * ((1 / D) ^ 2 * vecNorm2Sq q)
        =
      (beta ^ 2 / Y + theta ^ 2 * (1 / D) ^ 2) * vecNorm2Sq q := by
        ring
    _ = (theta ^ 2 / D) * vecNorm2Sq q := by rw [hcoeff]
    _ = theta ^ 2 * vecNorm2Sq q / D := by ring
/-- Projector form of the rank-two WKS upper-witness scalar optimization.
    The extra outer-product component converts the unavoidable
    `p`-orthogonal residual component into the source-block projector term
    weighted by `phi^2`.  This is the algebraic denominator missing from the
    scalar-only rank-one route. -/
theorem lsNormwiseBackwardErrorRankTwo_extra_cost_eq_phi_projector
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (r p : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0)
    (hpsq : vecNorm2Sq p ≠ 0) :
    theta ^ 2 *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) /
        (1 + theta ^ 2 * vecNorm2Sq y) =
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
        vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) /
        vecNorm2Sq p := by
  let R : ℝ := vecNorm2Sq r
  let P : ℝ := vecNorm2Sq p
  let Z : ℝ :=
    vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p)
  let Q : ℝ :=
    vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r)
  let D : ℝ := 1 + theta ^ 2 * vecNorm2Sq y
  have hRpos : 0 < R := by
    exact lt_of_le_of_ne (by simpa [R] using vecNorm2Sq_nonneg r)
      (by simpa [R] using Ne.symm hrsq)
  have hPpos : 0 < P := by
    exact lt_of_le_of_ne (by simpa [P] using vecNorm2Sq_nonneg p)
      (by simpa [P] using Ne.symm hpsq)
  have hDpos : 0 < D := by
    simpa [D] using lsNormwiseBackwardErrorMu_den_pos theta y
  have harea : R * Z = P * Q := by
    simpa [R, P, Z, Q, lsResidualComplementProjector] using
      lsLemma20_6ProjectorComplement_area_identity r p hrsq hpsq
  have hQ_eq : Q = R * Z / P := by
    have hPne : P ≠ 0 := ne_of_gt hPpos
    calc
      Q = P * Q / P := by field_simp [hPne]
      _ = R * Z / P := by rw [← harea]
  have hphi_sq :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 = theta ^ 2 * R / D := by
    rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den
      htheta hy r]
    rw [div_pow, mul_pow, Real.sq_sqrt (le_of_lt hDpos), vecNorm2_sq r]
  have hPne : P ≠ 0 := ne_of_gt hPpos
  have hDne : D ≠ 0 := ne_of_gt hDpos
  calc
    theta ^ 2 *
        vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement p) r) /
        (1 + theta ^ 2 * vecNorm2Sq y)
        = theta ^ 2 * Q / D := by rfl
    _ = theta ^ 2 * (R * Z / P) / D := by rw [hQ_eq]
    _ = (theta ^ 2 * R / D) * Z / P := by
          field_simp [hPne, hDne]
    _ =
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
        vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) /
        vecNorm2Sq p := by
          rw [hphi_sq]
/-- Source-block transpose equality for the optimized rank-two WKS witness.
    The rank-two correction converts the scalar-only residual-complement cost
    into exactly the right projector block of
    `[A, phi(I-r r^+)]^T p`, so the total optimized cost multiplied by
    `||p||_2^2` is the squared source-block transpose action. -/
theorem lsNormwiseBackwardErrorRankTwo_scaled_source_cost_eq_formulaMatrix_transpose
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (A : Fin m → Fin n → ℝ) (r p : Fin m → ℝ)
    (hrsq : vecNorm2Sq r ≠ 0) (hpsq : vecNorm2Sq p ≠ 0) :
    let q : Fin m → ℝ :=
      matMulVec m (lsLemma20_6ProjectorComplement p) r
    let beta : ℝ :=
      -(theta ^ 2 * vecNorm2Sq y) /
        (1 + theta ^ 2 * vecNorm2Sq y)
    vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) +
        (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
          vecNorm2Sq p =
      vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          p) := by
  let q : Fin m → ℝ :=
    matMulVec m (lsLemma20_6ProjectorComplement p) r
  let beta : ℝ :=
    -(theta ^ 2 * vecNorm2Sq y) /
      (1 + theta ^ 2 * vecNorm2Sq y)
  let P : ℝ := vecNorm2Sq p
  let left : ℝ := vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i)
  let Z : ℝ := vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p)
  let right : ℝ := (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * Z
  have hPpos : 0 < P := by
    exact lt_of_le_of_ne (by simpa [P] using vecNorm2Sq_nonneg p)
      (by simpa [P] using Ne.symm hpsq)
  have hPne : P ≠ 0 := ne_of_gt hPpos
  have hweighted :
      beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i) =
        theta ^ 2 * vecNorm2Sq q /
          (1 + theta ^ 2 * vecNorm2Sq y) := by
    simpa [q, beta] using
      (lsNormwiseBackwardErrorRankTwo_extra_weighted_cost_eq
        (m := m) (n := n) theta hy q)
  have hprojector :
      theta ^ 2 * vecNorm2Sq q /
          (1 + theta ^ 2 * vecNorm2Sq y) =
        right / P := by
    simpa [q, P, Z, right, lsResidualComplementProjector] using
      (lsNormwiseBackwardErrorRankTwo_extra_cost_eq_phi_projector
        (m := m) (n := n) htheta hy r p hrsq hpsq)
  have hterm :
      (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
          P =
        right := by
    calc
      (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
          P
          =
        (theta ^ 2 * vecNorm2Sq q /
          (1 + theta ^ 2 * vecNorm2Sq y)) * P := by
            rw [hweighted]
      _ = (right / P) * P := by
            rw [hprojector]
      _ = right := by
            field_simp [hPne]
  have hsplit :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) =
        left + right := by
    simpa [left, right, Z] using
      lsNormwiseBackwardErrorFormulaMatrix_transpose_vecNorm2Sq_eq
        theta A r y p
  change
    left +
        (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
          P =
      vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          p)
  calc
    left +
        (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
          P
        = left + right := by rw [hterm]
    _ =
      vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
          p) := by
            rw [hsplit]
/-- Source-transpose certificate bridge for the optimized rank-two WKS cost.
    Once the source block transpose action is bounded by `sigma * ||p||_2`,
    the optimized rank-two cost is bounded by `sigma^2 ||p||_2^2`. -/
theorem lsNormwiseBackwardErrorRankTwo_scaled_source_cost_le_of_source_transpose_sq_le
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (sigma : ℝ) (A : Fin m → Fin n → ℝ) (r p : Fin m → ℝ)
    (hrsq : vecNorm2Sq r ≠ 0) (hpsq : vecNorm2Sq p ≠ 0)
    (hsource :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
            p) ≤
        sigma ^ 2 * vecNorm2Sq p) :
    let q : Fin m → ℝ :=
      matMulVec m (lsLemma20_6ProjectorComplement p) r
    let beta : ℝ :=
      -(theta ^ 2 * vecNorm2Sq y) /
        (1 + theta ^ 2 * vecNorm2Sq y)
    vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) +
        (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
          vecNorm2Sq p ≤
      sigma ^ 2 * vecNorm2Sq p := by
  let q : Fin m → ℝ :=
    matMulVec m (lsLemma20_6ProjectorComplement p) r
  let beta : ℝ :=
    -(theta ^ 2 * vecNorm2Sq y) /
      (1 + theta ^ 2 * vecNorm2Sq y)
  have heq :
      vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) +
          (beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
            theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) *
            vecNorm2Sq p =
        vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
            p) := by
    simpa [q, beta] using
      (lsNormwiseBackwardErrorRankTwo_scaled_source_cost_eq_formulaMatrix_transpose
        (m := m) (n := n) htheta hy A r p hrsq hpsq)
  dsimp only
  rw [heq]
  exact hsource
/-- Square-root form of the rank-two WKS source-transpose certificate.  This
    is the direct cost surface needed by the future constructive upper witness:
    the optimized rank-two cost is at most any nonnegative `sigma` whose
    squared source-block transpose certificate holds. -/
theorem lsNormwiseBackwardErrorRankTwo_scaled_cost_sqrt_le_of_source_transpose_sq_le
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    {sigma : ℝ} (hsigma : 0 ≤ sigma)
    (A : Fin m → Fin n → ℝ) (r p : Fin m → ℝ)
    (hrsq : vecNorm2Sq r ≠ 0) (hpsq : vecNorm2Sq p ≠ 0)
    (hsource :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y))
            p) ≤
        sigma ^ 2 * vecNorm2Sq p) :
    let q : Fin m → ℝ :=
      matMulVec m (lsLemma20_6ProjectorComplement p) r
    let beta : ℝ :=
      -(theta ^ 2 * vecNorm2Sq y) /
        (1 + theta ^ 2 * vecNorm2Sq y)
    Real.sqrt
        (vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) /
            vecNorm2Sq p +
          beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
          theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)) ≤
      sigma := by
  let q : Fin m → ℝ :=
    matMulVec m (lsLemma20_6ProjectorComplement p) r
  let beta : ℝ :=
    -(theta ^ 2 * vecNorm2Sq y) /
      (1 + theta ^ 2 * vecNorm2Sq y)
  let P : ℝ := vecNorm2Sq p
  let left : ℝ := vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i)
  let extra : ℝ :=
    beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
      theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i)
  have hPpos : 0 < P := by
    exact lt_of_le_of_ne (by simpa [P] using vecNorm2Sq_nonneg p)
      (by simpa [P] using Ne.symm hpsq)
  have hcost_sq :
      left + extra * P ≤ sigma ^ 2 * P := by
    simpa [q, beta, P, left, extra] using
      (lsNormwiseBackwardErrorRankTwo_scaled_source_cost_le_of_source_transpose_sq_le
        (m := m) (n := n) htheta hy sigma A r p hrsq hpsq hsource)
  have hinside_le :
      left / P + extra ≤ sigma ^ 2 := by
    have hmul :
        (left / P + extra) * P ≤ sigma ^ 2 * P := by
      calc
        (left / P + extra) * P = left + extra * P := by
          field_simp [ne_of_gt hPpos]
        _ ≤ sigma ^ 2 * P := hcost_sq
    exact le_of_mul_le_mul_right hmul hPpos
  have htarget :
      vecNorm2Sq (fun j : Fin n => ∑ i : Fin m, A i j * p i) /
          vecNorm2Sq p +
        beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
        theta ^ 2 * vecNorm2Sq (fun i : Fin m => q i + beta * q i) =
        left / P + extra := by
    simp [left, P, extra]
    ring
  dsimp only
  rw [htarget]
  calc
    Real.sqrt (left / P + extra) ≤ Real.sqrt (sigma ^ 2) :=
      Real.sqrt_le_sqrt hinside_le
    _ = sigma := by
      rw [Real.sqrt_sq_eq_abs, abs_of_nonneg hsigma]
/-- Feasible optimized rank-two WKS source-residual witness.  The first
    rank-one perturbation realizes the source-block transpose action on `p`;
    the second outer product absorbs the `p`-orthogonal residual component in
    the `y` direction without changing that transpose action. -/
theorem LSNormwiseBackwardErrorFeasible.exists_rankTwo_source_residual_witness
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (b : Fin m → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0) :
    let r : Fin m → ℝ := lsResidualHigham A b y
    let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
    let q : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
    let beta : ℝ :=
      -(theta ^ 2 * vecNorm2Sq y) /
        (1 + theta ^ 2 * vecNorm2Sq y)
    ∃ (DeltaA : Fin m → Fin n → ℝ) (Deltab : Fin m → ℝ),
      LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
          Real.sqrt
            (vecNorm2Sq u / vecNorm2Sq p +
              beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
                theta ^ 2 *
                  vecNorm2Sq (fun i : Fin m => q i + beta * q i)) := by
  let r : Fin m → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let t : ℝ := ∑ j : Fin n, u j * y j
  let c : ℝ := ((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p
  let q : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  let qneg : Fin m → ℝ := fun i => -q i
  let beta : ℝ :=
    -(theta ^ 2 * vecNorm2Sq y) /
      (1 + theta ^ 2 * vecNorm2Sq y)
  let DeltaA1 : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankOneDeltaA p u
  let DeltaA2 : Fin m → Fin n → ℝ :=
    lsNormwiseBackwardErrorRankTwoExtraDeltaA beta qneg y
  let DeltaA : Fin m → Fin n → ℝ := fun i j => DeltaA1 i j + DeltaA2 i j
  let Deltab : Fin m → ℝ := fun i => qneg i + beta * qneg i
  have hy_sq : vecNorm2Sq y ≠ 0 :=
    ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq hy)
  have hqneg_opt :
      qneg =
        fun i : Fin m =>
          c * p i - r i - ((1 / vecNorm2Sq p) * p i * t) := by
    have hopt :
        (fun i : Fin m =>
          (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p) * p i -
            r i - ((1 / vecNorm2Sq p) * p i * t)) =
          fun i : Fin m =>
            -matMulVec m (lsLemma20_6ProjectorComplement p) r i := by
      exact
        lsNormwiseBackwardErrorRankOne_scaled_q_optimal_eq_neg_projector
          p r hp t
    ext i
    simpa [qneg, q, c] using (congrFun hopt i).symm
  have hqneg_orth : ∑ i : Fin m, qneg i * p i = 0 := by
    have horth :
        ∑ i : Fin m,
          (((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p * p i -
            r i - (1 / vecNorm2Sq p * p i * t)) * p i = 0 := by
      simpa [c] using
        (lsNormwiseBackwardErrorRankOne_scaled_q_optimal_dot_self_eq_zero
          p r hp t)
    calc
      ∑ i : Fin m, qneg i * p i =
          ∑ i : Fin m,
            (c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)) * p i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [congrFun hqneg_opt i]
      _ = 0 := by
            simpa [c] using horth
  have hDeltaA1_y :
      rectMatMulVec DeltaA1 y =
        fun i : Fin m => -((1 / vecNorm2Sq p) * p i * t) := by
    simpa [DeltaA1, t] using
      (lsNormwiseBackwardErrorRankOneDeltaA_mulVec
        (p := p) (u := u) (y := y))
  have hDeltaA2_y :
      rectMatMulVec DeltaA2 y = fun i : Fin m => beta * qneg i := by
    simpa [DeltaA2] using
      (lsNormwiseBackwardErrorRankTwoExtraDeltaA_mulVec
        (m := m) (n := n) (beta := beta) qneg (y := y) hy_sq)
  have hDeltaA_y :
      rectMatMulVec DeltaA y =
        fun i : Fin m =>
          -((1 / vecNorm2Sq p) * p i * t) + beta * qneg i := by
    ext i
    unfold rectMatMulVec DeltaA
    calc
      ∑ j : Fin n, (DeltaA1 i j + DeltaA2 i j) * y j
          = ∑ j : Fin n, (DeltaA1 i j * y j + DeltaA2 i j * y j) := by
              apply Finset.sum_congr rfl
              intro j _
              ring
      _ = ∑ j : Fin n, DeltaA1 i j * y j +
            ∑ j : Fin n, DeltaA2 i j * y j := by
              rw [Finset.sum_add_distrib]
      _ = -((1 / vecNorm2Sq p) * p i * t) + beta * qneg i := by
              change rectMatMulVec DeltaA1 y i +
                  rectMatMulVec DeltaA2 y i =
                -((1 / vecNorm2Sq p) * p i * t) + beta * qneg i
              rw [congrFun hDeltaA1_y i, congrFun hDeltaA2_y i]
  have hDeltaA1_tp :
      (fun j : Fin n => ∑ i : Fin m, DeltaA1 i j * p i) =
        fun j => -u j := by
    simpa [DeltaA1] using
      (lsNormwiseBackwardErrorRankOneDeltaA_transpose_mul
        (p := p) (u := u) hp)
  have hDeltaA2_tp :
      (fun j : Fin n => ∑ i : Fin m, DeltaA2 i j * p i) = 0 := by
    simpa [DeltaA2] using
      (lsNormwiseBackwardErrorRankTwoExtraDeltaA_transpose_mul_eq_zero
        (m := m) (n := n) beta qneg p y hqneg_orth)
  refine ⟨DeltaA, Deltab, ?_, ?_⟩
  · apply
      (LSNormwiseBackwardErrorFeasible.iff_source_higham_residual_orthogonal
        A b y DeltaA Deltab).mpr
    intro j
    have hres_expand :
        (fun i : Fin m =>
          r i + Deltab i - rectMatMulVec DeltaA y i) =
          fun i : Fin m => c * p i := by
      ext i
      have hAy_i :
          rectMatMulVec DeltaA y i =
            -((1 / vecNorm2Sq p) * p i * t) + beta * qneg i :=
        congrFun hDeltaA_y i
      have hq_i :
          qneg i = c * p i - r i - ((1 / vecNorm2Sq p) * p i * t) :=
        congrFun hqneg_opt i
      calc
        r i + Deltab i - rectMatMulVec DeltaA y i
            =
          r i + (qneg i + beta * qneg i) -
            (-((1 / vecNorm2Sq p) * p i * t) + beta * qneg i) := by
            rw [hAy_i]
        _ = r i + qneg i + ((1 / vecNorm2Sq p) * p i * t) := by
            ring
        _ = c * p i := by
            rw [hq_i]
            ring
    have hDeltaA1_j : ∑ i : Fin m, DeltaA1 i j * p i = -u j :=
      congrFun hDeltaA1_tp j
    have hDeltaA2_j : ∑ i : Fin m, DeltaA2 i j * p i = 0 := by
      simpa using congrFun hDeltaA2_tp j
    calc
      ∑ i : Fin m, (A i j + DeltaA i j) *
          (lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i)
          =
        ∑ i : Fin m, (A i j + DeltaA i j) * (c * p i) := by
          apply Finset.sum_congr rfl
          intro i _
          rw [congrFun hres_expand i]
      _ =
        c * ((∑ i : Fin m, A i j * p i) +
          ∑ i : Fin m, DeltaA1 i j * p i +
            ∑ i : Fin m, DeltaA2 i j * p i) := by
          dsimp [DeltaA]
          calc
            ∑ i : Fin m, (A i j + (DeltaA1 i j + DeltaA2 i j)) *
                (c * p i)
                =
              ∑ i : Fin m,
                c * (A i j * p i + DeltaA1 i j * p i +
                  DeltaA2 i j * p i) := by
                apply Finset.sum_congr rfl
                intro i _
                ring
            _ =
              c * ((∑ i : Fin m, A i j * p i) +
                ∑ i : Fin m, DeltaA1 i j * p i +
                  ∑ i : Fin m, DeltaA2 i j * p i) := by
                rw [← Finset.mul_sum]
                congr 1
                rw [Finset.sum_add_distrib, Finset.sum_add_distrib]
      _ = 0 := by
          rw [hDeltaA1_j, hDeltaA2_j]
          simp [u]
  · have hqneg_norm : vecNorm2Sq qneg = vecNorm2Sq q := by
      simpa [qneg] using (vecNorm2Sq_smul (-1 : ℝ) q)
    have hDeltab_norm :
        vecNorm2Sq Deltab =
          vecNorm2Sq (fun i : Fin m => q i + beta * q i) := by
      have hvec :
          Deltab = fun i : Fin m => -(q i + beta * q i) := by
        ext i
        dsimp [Deltab, qneg]
        ring
      rw [hvec]
      simpa using
        (vecNorm2Sq_smul (-1 : ℝ)
          (fun i : Fin m => q i + beta * q i))
    have hDeltaA_sq :
        frobNormSqRect DeltaA =
          vecNorm2Sq u / vecNorm2Sq p +
            beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y := by
      have hraw :
          frobNormSqRect DeltaA =
            vecNorm2Sq u / vecNorm2Sq p +
              beta ^ 2 * vecNorm2Sq qneg / vecNorm2Sq y := by
        simpa [DeltaA, DeltaA1, DeltaA2] using
          (lsNormwiseBackwardErrorRankTwoDeltaA_frobNormSq
            (p := p) (u := u) (beta := beta) (q := qneg) (y := y)
            hp hy_sq hqneg_orth)
      simpa [hqneg_norm] using hraw
    rw [lsNormwiseBackwardErrorCostF_eq_sqrt_sq_sum]
    rw [hDeltaA_sq, hDeltab_norm]
/-- Optimized rank-two WKS source-residual upper bound.  This is the concrete
    feasible perturbation side before the source-block singular-vector
    certificate is consumed. -/
theorem lsNormwiseBackwardErrorEtaF_le_rankTwo_source_residual_witness
    {m n : ℕ} (theta : ℝ) (A : Fin m → Fin n → ℝ)
    (b : Fin m → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0)
    (p : Fin m → ℝ) (hp : vecNorm2Sq p ≠ 0) :
    let r : Fin m → ℝ := lsResidualHigham A b y
    let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
    let q : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
    let beta : ℝ :=
      -(theta ^ 2 * vecNorm2Sq y) /
        (1 + theta ^ 2 * vecNorm2Sq y)
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      Real.sqrt
        (vecNorm2Sq u / vecNorm2Sq p +
          beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
            theta ^ 2 *
              vecNorm2Sq (fun i : Fin m => q i + beta * q i)) := by
  let r : Fin m → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin m, A i j * p i
  let q : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  let beta : ℝ :=
    -(theta ^ 2 * vecNorm2Sq y) /
      (1 + theta ^ 2 * vecNorm2Sq y)
  rcases
    (LSNormwiseBackwardErrorFeasible.exists_rankTwo_source_residual_witness
      theta A b hy p hp) with
    ⟨DeltaA, Deltab, hfeas, hcost⟩
  dsimp only
  calc
    lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorCostF theta DeltaA Deltab :=
      lsNormwiseBackwardErrorEtaF_le_costF_of_feasible
        theta A b y DeltaA Deltab hfeas
    _ =
        Real.sqrt
          (vecNorm2Sq u / vecNorm2Sq p +
            beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
              theta ^ 2 *
                vecNorm2Sq (fun i : Fin m => q i + beta * q i)) := by
        simpa [r, u, q, beta] using hcost
/-- Source-transpose certificate version of the optimized rank-two WKS upper
    construction.  The source-block singular-vector certificate now feeds an
    actual feasible perturbation pair rather than a supplied upper inequality. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankTwo_scaled_source_transpose_cert
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hsource :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let q : Fin (m + 1) → ℝ :=
    matMulVec (m + 1) (lsLemma20_6ProjectorComplement p) r
  let beta : ℝ :=
    -(theta ^ 2 * vecNorm2Sq y) /
      (1 + theta ^ 2 * vecNorm2Sq y)
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  have hsigma : 0 ≤ sigma := by
    simpa [sigma, r] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        Real.sqrt
          (vecNorm2Sq u / vecNorm2Sq p +
            beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
              theta ^ 2 *
                vecNorm2Sq (fun i : Fin (m + 1) => q i + beta * q i)) := by
    simpa [r, u, q, beta] using
      (lsNormwiseBackwardErrorEtaF_le_rankTwo_source_residual_witness
        theta A b hy p hp)
  have hcost :
      Real.sqrt
          (vecNorm2Sq u / vecNorm2Sq p +
            beta ^ 2 * vecNorm2Sq q / vecNorm2Sq y +
              theta ^ 2 *
                vecNorm2Sq (fun i : Fin (m + 1) => q i + beta * q i)) ≤
        sigma := by
    simpa [r, u, q, beta, sigma] using
      (lsNormwiseBackwardErrorRankTwo_scaled_cost_sqrt_le_of_source_transpose_sq_le
        (m := m + 1) (n := n) htheta hy (sigma := sigma) hsigma
        A r p hrsq hp (by simpa [r, sigma] using hsource))
  have hrhs :
      lsNormwiseBackwardErrorFormulaRHS theta A b y = sigma := by
    unfold lsNormwiseBackwardErrorFormulaRHS lsNormwiseBackwardErrorFormulaValue
    exact min_eq_right (by simpa [r, sigma] using hbranch)
  rw [hrhs]
  exact heta.trans hcost
/-- Source-block branch of the WKS upper inequality using the optimized
    rank-two feasible witness.  The local row-side `sigma_min` API supplies an
    attaining source vector, and the rank-two construction consumes its
    transpose-action equality directly. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_sigmaMin_le_phi_rankTwo
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  rcases
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_exists_transpose_attaining_vector_sq
      theta A r y with
    ⟨p, hp, hsource_eq⟩
  have hpsq : vecNorm2Sq p ≠ 0 :=
    ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq hp)
  have hsource_le :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p := by
    simpa [r] using (le_of_eq hsource_eq)
  exact
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankTwo_scaled_source_transpose_cert
      htheta A b hy p hpsq hrsq hbranch hsource_le
/-- Route-elimination check for the scaled rank-one WKS source-transpose
    handoff.  In the nondegenerate case where `(I-r r^+)p` is nonzero, the
    current right-projector comparison required by the rank-one route is
    impossible for every scalar: the best scalar still leaves the projected
    residual component, while `phi^2` contains the extra
    `1 + theta^2 ||y||_2^2` denominator. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_projector_comparison_not_of_projected_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (r p : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0)
    (hpsq : vecNorm2Sq p ≠ 0)
    (hprojected :
      vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p) ≠ 0)
    (t c : ℝ) :
    ¬ theta ^ 2 *
        vecNorm2Sq
          (fun i : Fin m =>
            c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)) *
          vecNorm2Sq p ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec m (lsLemma20_6ProjectorComplement r) p) := by
  intro hcomparison
  let q : Fin m → ℝ :=
    fun i : Fin m => c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)
  let Qrp : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement r) p
  let Qpr : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  let R : ℝ := vecNorm2Sq r
  let P : ℝ := vecNorm2Sq p
  let Z : ℝ := vecNorm2Sq Qrp
  let D : ℝ := 1 + theta ^ 2 * vecNorm2Sq y
  have htheta_nonneg : 0 ≤ theta := le_of_lt htheta
  have htheta_sq_pos : 0 < theta ^ 2 := sq_pos_of_ne_zero (ne_of_gt htheta)
  have hRpos : 0 < R := by
    exact lt_of_le_of_ne (by simpa [R] using vecNorm2Sq_nonneg r) (by simpa [R] using Ne.symm hrsq)
  have hPpos : 0 < P := by
    exact lt_of_le_of_ne (by simpa [P] using vecNorm2Sq_nonneg p) (by simpa [P] using Ne.symm hpsq)
  have hZpos : 0 < Z := by
    exact lt_of_le_of_ne (by simpa [Z, Qrp] using vecNorm2Sq_nonneg Qrp)
      (by simpa [Z, Qrp] using Ne.symm hprojected)
  have hy_sq_pos : 0 < vecNorm2Sq y := vecNorm2Sq_pos_of_ne_zero_lsq hy
  have hDpos : 0 < D := by
    dsimp [D]
    nlinarith [mul_pos htheta_sq_pos hy_sq_pos]
  have hDgt_one : 1 < D := by
    dsimp [D]
    nlinarith [mul_pos htheta_sq_pos hy_sq_pos]
  have hphi_sq :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 = theta ^ 2 * R / D := by
    rw [lsNormwiseBackwardErrorPhi_eq_theta_mul_norm_div_sqrt_den
      htheta_nonneg hy r]
    rw [div_pow, mul_pow, Real.sq_sqrt (le_of_lt hDpos), vecNorm2_sq r]
  have hq_lower :
      vecNorm2Sq Qpr ≤ vecNorm2Sq q := by
    simpa [q, Qpr] using
      lsNormwiseBackwardErrorRankOne_scaled_q_projector_lower_bound
        p r hpsq t c
  have harea :
      R * Z = P * vecNorm2Sq Qpr := by
    simpa [R, P, Z, Qrp, Qpr] using
      lsLemma20_6ProjectorComplement_area_identity r p hrsq hpsq
  have hRZ_le_Pq : R * Z ≤ P * vecNorm2Sq q := by
    rw [harea]
    exact mul_le_mul_of_nonneg_left hq_lower (le_of_lt hPpos)
  have hleft_ge :
      theta ^ 2 * R * Z ≤ theta ^ 2 * vecNorm2Sq q * P := by
    have hmul :=
      mul_le_mul_of_nonneg_left hRZ_le_Pq (le_of_lt htheta_sq_pos)
    nlinarith [hmul]
  have hcomparison' :
      theta ^ 2 * vecNorm2Sq q * P ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * Z := by
    simpa [q, P, Z, Qrp] using hcomparison
  have hApos : 0 < theta ^ 2 * R * Z := by
    exact mul_pos (mul_pos htheta_sq_pos hRpos) hZpos
  have hrhs_eq :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * Z =
        theta ^ 2 * R * Z / D := by
    rw [hphi_sq]
    field_simp [ne_of_gt hDpos]
  have hrhs_lt :
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * Z <
        theta ^ 2 * R * Z := by
    rw [hrhs_eq]
    rw [div_lt_iff₀ hDpos]
    nlinarith [hApos, hDgt_one]
  exact not_lt_of_ge (hleft_ge.trans hcomparison') hrhs_lt
/-- Source-block attainer route-elimination check for the WKS `sigma_min`
    branch in (20.21).  If an exact row-side `sigma_min` attainer for
    `[A phi(I-r r^+)]` has nonzero component away from the residual direction,
    then no scalar choice in the current scaled rank-one witness can satisfy
    the projector comparison required by the source-transpose handoff.

    This is not the full WKS construction; it records, at the source-block
    attainer surface, why the scalar-only rank-one route cannot close the
    nondegenerate projected branch. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_attainer_projector_comparison_not_of_projected_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hatt :
      ∃ p : Fin (m + 1) → ℝ, p ≠ 0 ∧
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p ∧
        vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y)) p) ≠ 0) :
    ∃ p : Fin (m + 1) → ℝ, p ≠ 0 ∧
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y)) p) =
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
            (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p ∧
      ∀ c : ℝ,
        let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
        let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
        let q : Fin (m + 1) → ℝ :=
          fun i =>
            c * p i - r i -
              ((1 / vecNorm2Sq p) * p i *
                (∑ j : Fin n, u j * y j))
        ¬ theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
          (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
            vecNorm2Sq
              (matMulVec (m + 1) (lsResidualComplementProjector r) p) := by
  rcases hatt with ⟨p, hp, hsource, hprojected⟩
  refine ⟨p, hp, hsource, ?_⟩
  intro c
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let t : ℝ := ∑ j : Fin n, u j * y j
  have hpsq : vecNorm2Sq p ≠ 0 :=
    ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq hp)
  have hnot :
      ¬ theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)) *
          vecNorm2Sq p ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec (m + 1) (lsLemma20_6ProjectorComplement r) p) :=
    lsNormwiseBackwardErrorRankOne_scaled_projector_comparison_not_of_projected_ne_zero
      (m := m + 1) (n := n) htheta hy r p hrsq hpsq
      (by simpa [r, lsResidualComplementProjector] using hprojected) t c
  simpa [r, u, t, lsResidualComplementProjector] using hnot
/-- Degenerate source-projector branch for the scaled rank-one WKS
    source-transpose handoff.  If the source candidate `p` has zero component
    away from the residual direction `r`, then the optimal scalar choice in the
    explicit `Delta b` witness makes the required right-projector comparison
    vanish on both sides. -/
theorem lsNormwiseBackwardErrorRankOne_scaled_projector_comparison_of_projected_eq_zero
    {m n : ℕ} (theta : ℝ) (y : Fin n → ℝ)
    (r p : Fin m → ℝ) (hrsq : vecNorm2Sq r ≠ 0)
    (hpsq : vecNorm2Sq p ≠ 0)
    (hprojected :
      vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) = 0)
    (t : ℝ) :
    let c : ℝ := ((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p
    let q : Fin m → ℝ :=
      fun i : Fin m =>
        c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)
    theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
      (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
        vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) := by
  let c : ℝ := ((∑ k : Fin m, p k * r k) + t) / vecNorm2Sq p
  let q : Fin m → ℝ :=
    fun i : Fin m =>
      c * p i - r i - ((1 / vecNorm2Sq p) * p i * t)
  let Qrp : Fin m → ℝ := matMulVec m (lsResidualComplementProjector r) p
  let Qpr : Fin m → ℝ := matMulVec m (lsLemma20_6ProjectorComplement p) r
  have hPpos : 0 < vecNorm2Sq p :=
    lt_of_le_of_ne (vecNorm2Sq_nonneg p) (Ne.symm hpsq)
  have harea :
      vecNorm2Sq r * vecNorm2Sq Qrp =
        vecNorm2Sq p * vecNorm2Sq Qpr := by
    simpa [Qrp, Qpr, lsResidualComplementProjector] using
      lsLemma20_6ProjectorComplement_area_identity r p hrsq hpsq
  have hmul_zero : vecNorm2Sq p * vecNorm2Sq Qpr = 0 := by
    rw [← harea]
    simp [Qrp, hprojected]
  have hQpr_zero : vecNorm2Sq Qpr = 0 := by
    exact (mul_eq_zero.mp hmul_zero).resolve_left (ne_of_gt hPpos)
  have hopt : vecNorm2Sq q = vecNorm2Sq Qpr := by
    simpa [q, c, Qpr] using
      lsNormwiseBackwardErrorRankOne_scaled_q_optimal_vecNorm2Sq p r hpsq t
  have hq_zero : vecNorm2Sq q = 0 := by
    rw [hopt]
    exact hQpr_zero
  calc
    theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p = 0 := by
      rw [hq_zero]
      ring
    _ ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq (matMulVec m (lsResidualComplementProjector r) p) := by
      rw [hprojected]
      simp
/-- Source-block `sigma_min` branch handoff for the rank-one WKS witness.
    If a nonzero expanded residual `p` makes the explicit rank-one witness cost
    no larger than the row-side source-block `sigma_min`, and that branch is
    selected by the printed outer minimum, then the constructive upper
    inequality `eta_F(y) <=` the right-hand side of (20.21) follows.  The
    remaining WKS work is to prove the displayed expanded-cost certificate from
    a genuine source-block singular-vector construction. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_expanded_cost_le_sigmaMin
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hcost :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
          theta ^ 2 *
            vecNorm2Sq
              (fun i : Fin (m + 1) =>
                p i - r i -
                  ((1 / vecNorm2Sq p) * p i *
                    (∑ j : Fin n, u j * y j)))) := by
    simpa [r, u] using
      (lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness_expanded
        theta A b y p hp)
  have hcost' :
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) ≤ sigma := by
    simpa [r, u, sigma] using hcost
  have hrhs :
      lsNormwiseBackwardErrorFormulaRHS theta A b y = sigma := by
    unfold lsNormwiseBackwardErrorFormulaRHS lsNormwiseBackwardErrorFormulaValue
    exact min_eq_right (by simpa [r, sigma] using hbranch)
  rw [hrhs]
  exact heta.trans hcost'
/-- Scaled source-block `sigma_min` branch handoff for the rank-one WKS
    witness.  This is the same constructive upper-bound route as
    `lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_expanded_cost_le_sigmaMin`,
    but keeps the nonzero scalar freedom in the expanded residual witness
    visible for the remaining singular-vector cost certificate. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_cost_le_sigmaMin
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (c : ℝ) (hc : c ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hcost :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              c * p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
          theta ^ 2 *
            vecNorm2Sq
              (fun i : Fin (m + 1) =>
                c * p i - r i -
                  ((1 / vecNorm2Sq p) * p i *
                    (∑ j : Fin n, u j * y j)))) := by
    simpa [r, u] using
      (lsNormwiseBackwardErrorEtaF_le_rankOne_source_residual_witness_scaled
        theta A b y p hp c hc)
  have hcost' :
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              c * p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) ≤ sigma := by
    simpa [r, u, sigma] using hcost
  have hrhs :
      lsNormwiseBackwardErrorFormulaRHS theta A b y = sigma := by
    unfold lsNormwiseBackwardErrorFormulaRHS lsNormwiseBackwardErrorFormulaValue
    exact min_eq_right (by simpa [r, sigma] using hbranch)
  rw [hrhs]
  exact heta.trans hcost'
/-- Source-block `sigma_min` branch handoff from a division-free squared-cost
    certificate for the scaled rank-one WKS witness.  This is the form expected
    from the remaining singular-vector algebra: prove the explicit scaled
    witness cost squared is at most `sigma_min^2 * ||p||_2^2`, and the
    constructive upper inequality `eta_F(y) <=` the right-hand side of (20.21)
    follows when the printed minimum selects the source-block branch. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_sq_cost_le_sigmaMin
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (c : ℝ) (hc : c ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hcost_sq :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      let q : Fin (m + 1) → ℝ :=
        fun i =>
          c * p i - r i -
            ((1 / vecNorm2Sq p) * p i *
              (∑ j : Fin n, u j * y j))
      vecNorm2Sq u + theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let q : Fin (m + 1) → ℝ :=
    fun i =>
      c * p i - r i -
        ((1 / vecNorm2Sq p) * p i *
          (∑ j : Fin n, u j * y j))
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  have hsigma : 0 ≤ sigma := by
    simpa [sigma, r] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y
  have hcost :
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 * vecNorm2Sq q) ≤ sigma := by
    exact
      lsNormwiseBackwardErrorRankOne_scaled_cost_sqrt_le_of_sq_le
        theta sigma p hp u q hsigma (by
          simpa [r, u, q, sigma] using hcost_sq)
  exact
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_cost_le_sigmaMin
      theta A b y p hp c hc hbranch (by
        simpa [r, u, q, sigma] using hcost)
/-- Source-transpose certificate version of the scaled rank-one WKS
    `sigma_min` handoff.  This replaces the raw squared witness-cost
    certificate by two source-block obligations: an upper certificate for
    `||[A phi(I-r r^+)]^T p||_2^2`, supplied by the future singular-vector
    construction, and a right-projector comparison for the explicit scaled
    `Delta b` component. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_cert
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (c : ℝ) (hc : c ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hprojector :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      let q : Fin (m + 1) → ℝ :=
        fun i =>
          c * p i - r i -
            ((1 / vecNorm2Sq p) * p i *
              (∑ j : Fin n, u j * y j))
      theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1) (lsResidualComplementProjector r) p))
    (hsource :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let q : Fin (m + 1) → ℝ :=
    fun i =>
      c * p i - r i -
        ((1 / vecNorm2Sq p) * p i *
          (∑ j : Fin n, u j * y j))
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  have hcost_sq :
      vecNorm2Sq u + theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
        sigma ^ 2 * vecNorm2Sq p := by
    exact
      lsNormwiseBackwardErrorRankOne_scaled_sq_cost_le_of_source_transpose_sq_le
        theta sigma A r y p q
        (by simpa [r, u, q] using hprojector)
        (by simpa [r, sigma] using hsource)
  exact
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_sq_cost_le_sigmaMin
      theta A b y p hp c hc hbranch (by
        simpa [r, u, q, sigma] using hcost_sq)
/-- Degenerate source-projector specialization of the scaled rank-one
    source-transpose WKS handoff.  If the `sigma_min` candidate `p` has zero
    component away from the residual direction, then the optimal scalar choice
    supplies the required right-projector comparison, so only the
    source-transpose certificate and the nonzero scaling side condition remain
    as hypotheses. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_projected_eq_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) (hp : p ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hc :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      (((∑ k : Fin (m + 1), p k * r k) +
          (∑ j : Fin n, u j * y j)) / vecNorm2Sq p) ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hprojected :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) = 0)
    (hsource :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  let t : ℝ := ∑ j : Fin n, u j * y j
  let c : ℝ := ((∑ k : Fin (m + 1), p k * r k) + t) / vecNorm2Sq p
  have hpsq : vecNorm2Sq p ≠ 0 :=
    ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq hp)
  have hc' : c ≠ 0 := by
    simpa [r, u, t, c] using hc
  have hprojector :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      let q : Fin (m + 1) → ℝ :=
        fun i =>
          c * p i - r i -
            ((1 / vecNorm2Sq p) * p i *
              (∑ j : Fin n, u j * y j))
      theta ^ 2 * vecNorm2Sq q * vecNorm2Sq p ≤
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1) (lsResidualComplementProjector r) p) := by
    simpa [r, u, t, c] using
      lsNormwiseBackwardErrorRankOne_scaled_projector_comparison_of_projected_eq_zero
        theta y r p hrsq hpsq (by simpa [r] using hprojected) t
  exact
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_cert
      theta A b y p hpsq c hc' hbranch hprojector (by
        simpa [r] using hsource)
/-- Source-dot nonzero version of the degenerate source-projector WKS handoff.
    The optimal scale side condition from
    `lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_projected_eq_zero`
    is discharged by the source-shaped identity
    `c = (p^T b)/||p||_2^2`. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_projected_eq_zero_of_dot_b_ne_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (p : Fin (m + 1) → ℝ) (hp : p ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hdotb : (∑ i : Fin (m + 1), p i * b i) ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hprojected :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) = 0)
    (hsource :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
  have hpsq : vecNorm2Sq p ≠ 0 :=
    ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq hp)
  have hc :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      (((∑ k : Fin (m + 1), p k * r k) +
          (∑ j : Fin n, u j * y j)) / vecNorm2Sq p) ≠ 0 := by
    have hscale :
        (((∑ k : Fin (m + 1), p k * r k) +
            (∑ j : Fin n, u j * y j)) / vecNorm2Sq p) =
          (∑ i : Fin (m + 1), p i * b i) / vecNorm2Sq p := by
      simpa [r, u] using
        lsNormwiseBackwardErrorRankOne_scaled_optimal_scale_eq_dot_b_div
          A b y p
    have hdiv :
        (∑ i : Fin (m + 1), p i * b i) / vecNorm2Sq p ≠ 0 :=
      div_ne_zero hdotb hpsq
    simpa [r, u, hscale] using hdiv
  exact
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_projected_eq_zero
      theta A b y p hp hrsq hc hbranch hprojected hsource
/-- Source-block-attainer version of the degenerate WKS `sigma_min` upper
    branch.  The row-side sigma-min attainer for `[A phi(I-r r^+)]` supplies
    the source-transpose certificate internally; the remaining assumptions say
    that such an exact attainer lies in the residual direction and has
    nonzero source dot product with `b`.

    This closes the source-transpose side of the degenerate subcase but still
    leaves the genuine nondegenerate WKS construction, eigenvalue branch
    formula, and `theta = infinity` case open. -/
theorem lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hprojected :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y)) p) = 0)
    (hdotb :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        (∑ i : Fin (m + 1), p i * b i) ≠ 0) :
    lsNormwiseBackwardErrorEtaF theta A b y ≤
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  rcases
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_exists_transpose_attaining_vector_sq
      theta A r y with
    ⟨p, hp, hsource_eq⟩
  have hsource_le :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p := by
    simpa [r] using (le_of_eq hsource_eq)
  exact
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_projected_eq_zero_of_dot_b_ne_zero
      theta A b y p hp hrsq
      (hdotb p hp (by simpa [r] using hsource_eq))
      hbranch
      (by
        simpa [r] using hprojected p hp (by simpa [r] using hsource_eq))
      hsource_le
/-- Left block of the transposed WKS source matrix on a feasible perturbed
    residual.  Under (20.20) feasibility, the `A^T` part of
    `[A phi(I-r r^+)]^T p` is exactly cancelled by `DeltaA^T p`, where
    `p = (b - A y) + Delta b - Delta A y`. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_left
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (j : Fin n) :
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i)
        (Fin.castAdd (m + 1) j) =
      -∑ i : Fin (m + 1), DeltaA i j *
        (lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) := by
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  have horth :
      ∑ i : Fin (m + 1), (A i j + DeltaA i j) * p i = 0 :=
    (LSNormwiseBackwardErrorFeasible.iff_source_higham_residual_orthogonal
      A b y DeltaA Deltab).mp hfeas j
  have hsplit :
      (∑ i : Fin (m + 1), A i j * p i) +
        ∑ i : Fin (m + 1), DeltaA i j * p i = 0 := by
    simpa [p, add_mul, Finset.sum_add_distrib] using horth
  have hleft :
      ∑ i : Fin (m + 1), A i j * p i =
        -∑ i : Fin (m + 1), DeltaA i j * p i := by
    linarith
  calc
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        p (Fin.castAdd (m + 1) j)
        = ∑ i : Fin (m + 1), A i j * p i := by
          simp [rectMatMulVec, finiteTranspose,
            lsNormwiseBackwardErrorFormulaMatrix, Fin.append_left]
    _ = -∑ i : Fin (m + 1), DeltaA i j * p i := hleft
/-- Right projector block of the transposed WKS source matrix on a source-form
    perturbed residual.  Since `(I-r r^+)^T r = 0`, the right block sees only
    the perturbation residual `Delta b - Delta A y`. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (k : Fin (m + 1)) :
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i)
        (Fin.natAdd n k) =
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector (lsResidualHigham A b y) i k *
            (Deltab i - rectMatMulVec DeltaA y i) := by
  have hres :
      ∑ i : Fin (m + 1),
        lsResidualComplementProjector (lsResidualHigham A b y) i k *
          lsResidualHigham A b y i = 0 :=
    lsResidualComplementProjector_transpose_mul_residual
      (lsResidualHigham A b y) hrsq k
  calc
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i)
        (Fin.natAdd n k)
        = ∑ i : Fin (m + 1),
            (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
                lsResidualComplementProjector (lsResidualHigham A b y) i k) *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i) := by
          simp [rectMatMulVec, finiteTranspose,
            lsNormwiseBackwardErrorFormulaMatrix, Fin.append_right]
    _ = lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        (∑ i : Fin (m + 1),
            lsResidualComplementProjector (lsResidualHigham A b y) i k *
              lsResidualHigham A b y i +
          ∑ i : Fin (m + 1),
            lsResidualComplementProjector (lsResidualHigham A b y) i k *
              (Deltab i - rectMatMulVec DeltaA y i)) := by
          calc
            (∑ i : Fin (m + 1),
                lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
                    lsResidualComplementProjector (lsResidualHigham A b y) i k *
                  (lsResidualHigham A b y i + Deltab i -
                    rectMatMulVec DeltaA y i))
                =
              ∑ i : Fin (m + 1),
                lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
                  (lsResidualComplementProjector (lsResidualHigham A b y) i k *
                    (lsResidualHigham A b y i + Deltab i -
                      rectMatMulVec DeltaA y i)) := by
                apply Finset.sum_congr rfl
                intro i _
                ring
            _ =
              lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
                ∑ i : Fin (m + 1),
                  lsResidualComplementProjector (lsResidualHigham A b y) i k *
                    (lsResidualHigham A b y i + Deltab i -
                      rectMatMulVec DeltaA y i) := by
                rw [Finset.mul_sum]
            _ = lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
                (∑ i : Fin (m + 1),
                    lsResidualComplementProjector (lsResidualHigham A b y) i k *
                      lsResidualHigham A b y i +
                  ∑ i : Fin (m + 1),
                    lsResidualComplementProjector (lsResidualHigham A b y) i k *
                      (Deltab i - rectMatMulVec DeltaA y i)) := by
                congr 1
                rw [← Finset.sum_add_distrib]
                apply Finset.sum_congr rfl
                intro i _
                ring
    _ = lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector (lsResidualHigham A b y) i k *
            (Deltab i - rectMatMulVec DeltaA y i) := by
          rw [hres]
          ring
/-- Equivalent expanded-residual form of the right projector block in the
    transposed WKS source matrix.  The complement projector annihilates the
    exact residual direction, so the right block may be written with
    `p = r + Delta b - Delta A y`; this is the form needed for the sharper
    coupled lower-bound route. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right_expanded
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (k : Fin (m + 1)) :
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i)
        (Fin.natAdd n k) =
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector (lsResidualHigham A b y) i k *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i) := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let q : Fin (m + 1) → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
  have hright :=
    lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right
      theta A b y DeltaA Deltab hrsq k
  have hsum :
      ∑ i : Fin (m + 1),
          lsResidualComplementProjector r i k * q i =
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector r i k * (r i + q i) :=
    (lsResidualComplementProjector_transpose_mul_residual_add_eq
      r q (by simpa [r] using hrsq) k).symm
  calc
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i)
        (Fin.natAdd n k)
        =
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector r i k * q i := by
          simpa [r, q] using hright
    _ =
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector r i k * (r i + q i) := by
          rw [hsum]
    _ =
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector (lsResidualHigham A b y) i k *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i) := by
          congr 1
          apply Finset.sum_congr rfl
          intro i _
          simp [r, q, sub_eq_add_neg, add_assoc]
/-- Norm bound for the right projector block in the transposed WKS source
    matrix.  This closes the projector-contraction part of the nonzero-`p`
    lower-bound route for (20.21); the coupled weighted-cost estimate remains
    a separate open step. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right_vecNorm2_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    vecNorm2
        (fun k : Fin (m + 1) =>
          lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
            ∑ i : Fin (m + 1),
              lsResidualComplementProjector (lsResidualHigham A b y) i k *
                (Deltab i - rectMatMulVec DeltaA y i)) ≤
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        vecNorm2
          (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let q : Fin (m + 1) → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  have hphi : 0 ≤ phi := by
    simpa [phi, r] using lsNormwiseBackwardErrorPhi_nonneg theta r y
  have hC : rectOpNorm2Le (lsResidualComplementProjector r) 1 :=
    lsResidualComplementProjector_rectOpNorm2Le_one r (by simpa [r] using hrsq)
  have hprojected :
      (fun k : Fin (m + 1) =>
        ∑ i : Fin (m + 1), lsResidualComplementProjector r i k * q i) =
        matMulVec (m + 1) (lsResidualComplementProjector r) q := by
    ext k
    unfold matMulVec
    apply Finset.sum_congr rfl
    intro i _
    rw [lsResidualComplementProjector_symmetric]
  have hCq :
      vecNorm2 (matMulVec (m + 1) (lsResidualComplementProjector r) q) ≤
        vecNorm2 q := by
    simpa [rectMatMulVec, matMulVec] using hC q
  calc
    vecNorm2
        (fun k : Fin (m + 1) =>
          lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
            ∑ i : Fin (m + 1),
              lsResidualComplementProjector (lsResidualHigham A b y) i k *
                (Deltab i - rectMatMulVec DeltaA y i))
        = vecNorm2
            (fun k : Fin (m + 1) =>
              phi * matMulVec (m + 1) (lsResidualComplementProjector r) q k) := by
          congr 1
          ext k
          have hk := congrFun hprojected k
          exact congrArg (fun z : ℝ => phi * z) hk
    _ = phi * vecNorm2
          (matMulVec (m + 1) (lsResidualComplementProjector r) q) := by
          rw [vecNorm2_smul, abs_of_nonneg hphi]
    _ ≤ phi * vecNorm2 q :=
          mul_le_mul_of_nonneg_left hCq hphi
    _ = lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        vecNorm2
          (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
          rfl
/-- Vector form of the transposed WKS source-block action on the expanded
    perturbed residual.  This combines the left feasibility cancellation with
    the right projector cancellation and is the squared-norm input for the
    row-side spectral lower-bound route behind (20.21). -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_eq
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i) =
      Fin.append
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i))
        (fun k : Fin (m + 1) =>
          lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
            ∑ i : Fin (m + 1),
              lsResidualComplementProjector (lsResidualHigham A b y) i k *
                (Deltab i - rectMatMulVec DeltaA y i)) := by
  ext q
  refine Fin.addCases
    (motive := fun q : Fin (n + (m + 1)) =>
      rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          (fun i => lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i) q =
        Fin.append
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i))
          (fun k : Fin (m + 1) =>
            lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
              ∑ i : Fin (m + 1),
                lsResidualComplementProjector (lsResidualHigham A b y) i k *
                  (Deltab i - rectMatMulVec DeltaA y i)) q)
    ?left ?right q
  · intro j
    simpa [Fin.append_left] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_left
        theta A b y DeltaA Deltab hfeas j
  · intro k
    simpa [Fin.append_right] using
      lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right
        theta A b y DeltaA Deltab hrsq k
/-- Expanded-residual vector form of the transposed WKS source-block action.
    The right projector block is written against
    `p = (b-Ay) + Delta b - Delta A y`, rather than only against the
    perturbation residual; this is the exact form needed before the sharp
    weighted-cost inequality. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_eq_expanded
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    rectMatMulVec
        (finiteTranspose
          (lsNormwiseBackwardErrorFormulaMatrix theta A
            (lsResidualHigham A b y) y))
        (fun i => lsResidualHigham A b y i + Deltab i -
          rectMatMulVec DeltaA y i) =
      Fin.append
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i))
        (fun k : Fin (m + 1) =>
          lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
            ∑ i : Fin (m + 1),
              lsResidualComplementProjector (lsResidualHigham A b y) i k *
                (lsResidualHigham A b y i + Deltab i -
                  rectMatMulVec DeltaA y i)) := by
  ext q
  refine Fin.addCases
    (motive := fun q : Fin (n + (m + 1)) =>
      rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          (fun i => lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i) q =
        Fin.append
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i))
          (fun k : Fin (m + 1) =>
            lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
              ∑ i : Fin (m + 1),
                lsResidualComplementProjector (lsResidualHigham A b y) i k *
                  (lsResidualHigham A b y i + Deltab i -
                    rectMatMulVec DeltaA y i)) q)
    ?left ?right q
  · intro j
    simpa [Fin.append_left] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_left
        theta A b y DeltaA Deltab hfeas j
  · intro k
    simpa [Fin.append_right] using
      lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right_expanded
        theta A b y DeltaA Deltab hrsq k
/-- Squared-norm split for the transposed WKS source-block action on the
    expanded perturbed residual.  The left summand is the `DeltaA^T p` part
    forced by (20.20) feasibility; the right summand is the projected
    `Delta b - Delta A y` part. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2Sq_eq
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          (fun i => lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i)) =
      vecNorm2Sq
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i)) +
        vecNorm2Sq
          (fun k : Fin (m + 1) =>
            lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
              ∑ i : Fin (m + 1),
                lsResidualComplementProjector (lsResidualHigham A b y) i k *
                  (Deltab i - rectMatMulVec DeltaA y i)) := by
  rw [
    LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_eq
      theta A b y DeltaA Deltab hfeas hrsq]
  exact
    lsVecNorm2Sq_append
      (fun j : Fin n =>
        -∑ i : Fin (m + 1), DeltaA i j *
          (lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i))
      (fun k : Fin (m + 1) =>
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
          ∑ i : Fin (m + 1),
            lsResidualComplementProjector (lsResidualHigham A b y) i k *
              (Deltab i - rectMatMulVec DeltaA y i))
/-- Exact expanded-residual squared-norm split for the transposed WKS
    source-block action.  This keeps the right block as
    `phi * (I-r r^+)p`, preserving the slack needed in the sharp
    weighted-cost lower-bound proof. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2Sq_eq_expanded
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          (fun i => lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i)) =
      vecNorm2Sq
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i)) +
        (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y))
              (fun i : Fin (m + 1) =>
                lsResidualHigham A b y i + Deltab i -
                  rectMatMulVec DeltaA y i)) := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let left : Fin n → ℝ :=
    fun j => -∑ i : Fin (m + 1), DeltaA i j * p i
  let right : Fin (m + 1) → ℝ :=
    fun k => phi * ∑ i : Fin (m + 1),
      lsResidualComplementProjector r i k * p i
  have hsplit :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y))
            p) =
        vecNorm2Sq left + vecNorm2Sq right := by
    rw [
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_eq_expanded
        theta A b y DeltaA Deltab hfeas hrsq]
    exact lsVecNorm2Sq_append left right
  have hprojected :
      (fun k : Fin (m + 1) =>
        ∑ i : Fin (m + 1), lsResidualComplementProjector r i k * p i) =
        matMulVec (m + 1) (lsResidualComplementProjector r) p := by
    ext k
    unfold matMulVec
    apply Finset.sum_congr rfl
    intro i _
    rw [lsResidualComplementProjector_symmetric]
  have hright_sq :
      vecNorm2Sq right =
        phi ^ 2 *
          vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) := by
    have hsmul :
        vecNorm2Sq
            (fun k : Fin (m + 1) =>
              phi * matMulVec (m + 1) (lsResidualComplementProjector r) p k) =
          phi ^ 2 *
            vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) := by
      simpa using
        (vecNorm2Sq_smul phi
          (matMulVec (m + 1) (lsResidualComplementProjector r) p))
    calc
      vecNorm2Sq right =
          vecNorm2Sq
            (fun k : Fin (m + 1) =>
              phi * matMulVec (m + 1) (lsResidualComplementProjector r) p k) := by
            congr 1
            ext k
            exact congrArg (fun z : ℝ => phi * z) (congrFun hprojected k)
      _ = phi ^ 2 *
            vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) :=
            hsmul
  calc
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          p) =
        vecNorm2Sq left + vecNorm2Sq right := hsplit
    _ =
        vecNorm2Sq left +
          phi ^ 2 *
            vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) := by
          rw [hright_sq]
    _ =
        vecNorm2Sq
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i)) +
          (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
            vecNorm2Sq
              (matMulVec (m + 1)
                (lsResidualComplementProjector (lsResidualHigham A b y))
                (fun i : Fin (m + 1) =>
                  lsResidualHigham A b y i + Deltab i -
                    rectMatMulVec DeltaA y i)) := by
          rfl
/-- Squared upper bound for the transposed WKS source-block action on the
    expanded feasible residual.  This keeps the two coordinate blocks coupled in
    squared form, avoiding the triangle-inequality loss used by the additive
    bridge and preparing the sharp weighted-cost lower-bound step for (20.21). -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2Sq_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          (fun i => lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i)) ≤
      frobNormSqRect DeltaA *
        vecNorm2Sq
          (fun i : Fin (m + 1) =>
            lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i) +
        (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let q : Fin (m + 1) → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  let left : Fin n → ℝ :=
    fun j => -∑ i : Fin (m + 1), DeltaA i j * p i
  let right : Fin (m + 1) → ℝ :=
    fun k => phi *
      ∑ i : Fin (m + 1), lsResidualComplementProjector r i k * q i
  have hsplit :
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y))
            p) =
        vecNorm2Sq left + vecNorm2Sq right := by
    simpa [r, p, q, phi, left, right] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2Sq_eq
        theta A b y DeltaA Deltab hfeas hrsq
  have hleft_eq :
      left = fun j : Fin n => -rectMatMulVec (finiteTranspose DeltaA) p j := by
    ext j
    simp [left, rectMatMulVec, finiteTranspose]
  have hleft_sq :
      vecNorm2Sq left ≤ frobNormSqRect DeltaA * vecNorm2Sq p := by
    have hbase :
        vecNorm2Sq (rectMatMulVec (finiteTranspose DeltaA) p) ≤
          frobNormSqRect (finiteTranspose DeltaA) * vecNorm2Sq p :=
      vecNorm2Sq_rectMatMulVec_le_frobNormSqRect_mul (finiteTranspose DeltaA) p
    have hneg :
        vecNorm2Sq (fun j : Fin n =>
            -rectMatMulVec (finiteTranspose DeltaA) p j) =
          vecNorm2Sq (rectMatMulVec (finiteTranspose DeltaA) p) := by
      simpa using
        (vecNorm2Sq_smul (-1 : ℝ)
          (rectMatMulVec (finiteTranspose DeltaA) p))
    calc
      vecNorm2Sq left =
          vecNorm2Sq (fun j : Fin n =>
            -rectMatMulVec (finiteTranspose DeltaA) p j) := by
            rw [hleft_eq]
      _ = vecNorm2Sq (rectMatMulVec (finiteTranspose DeltaA) p) := hneg
      _ ≤ frobNormSqRect (finiteTranspose DeltaA) * vecNorm2Sq p := hbase
      _ = frobNormSqRect DeltaA * vecNorm2Sq p := by
            rw [frobNormSqRect_finiteTranspose]
  have hprojected :
      (fun k : Fin (m + 1) =>
        ∑ i : Fin (m + 1), lsResidualComplementProjector r i k * q i) =
        matMulVec (m + 1) (lsResidualComplementProjector r) q := by
    ext k
    unfold matMulVec
    apply Finset.sum_congr rfl
    intro i _
    rw [lsResidualComplementProjector_symmetric]
  have hproj_sq :
      vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) q) ≤
        vecNorm2Sq q := by
    simpa [r, lsResidualComplementProjector] using
      lsLemma20_6ProjectorComplement_vecNorm2Sq_le r
        (by simpa [r] using hrsq) q
  have hright_eq :
      right = fun k : Fin (m + 1) =>
        phi * matMulVec (m + 1) (lsResidualComplementProjector r) q k := by
    ext k
    exact congrArg (fun z : ℝ => phi * z) (congrFun hprojected k)
  have hright_sq :
      vecNorm2Sq right ≤ phi ^ 2 * vecNorm2Sq q := by
    have hsmul :
        vecNorm2Sq
            (fun k : Fin (m + 1) =>
              phi * matMulVec (m + 1) (lsResidualComplementProjector r) q k) =
          phi ^ 2 *
            vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) q) := by
      simpa using
        (vecNorm2Sq_smul phi
          (matMulVec (m + 1) (lsResidualComplementProjector r) q))
    calc
      vecNorm2Sq right =
          vecNorm2Sq
            (fun k : Fin (m + 1) =>
              phi * matMulVec (m + 1) (lsResidualComplementProjector r) q k) := by
            rw [hright_eq]
      _ = phi ^ 2 *
            vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) q) :=
            hsmul
      _ ≤ phi ^ 2 * vecNorm2Sq q :=
            mul_le_mul_of_nonneg_left hproj_sq (sq_nonneg phi)
  calc
    vecNorm2Sq
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          p) =
        vecNorm2Sq left + vecNorm2Sq right := hsplit
    _ ≤ frobNormSqRect DeltaA * vecNorm2Sq p + phi ^ 2 * vecNorm2Sq q :=
        add_le_add hleft_sq hright_sq
/-- Row-side spectral lower bound for the WKS source block on the expanded
    feasible residual. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrixSigmaMin_mul_source_residual_vecNorm2_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y *
      vecNorm2
        (fun i : Fin (m + 1) =>
          lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) ≤
      vecNorm2
        (Fin.append
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i))
          (fun k : Fin (m + 1) =>
            lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
              ∑ i : Fin (m + 1),
                lsResidualComplementProjector (lsResidualHigham A b y) i k *
                  (Deltab i - rectMatMulVec DeltaA y i))) := by
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  have hspec :=
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_mul_vecNorm2_le_transpose_mulVec
      theta A (lsResidualHigham A b y) y p
  have haction :=
    LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_eq
      theta A b y DeltaA Deltab hfeas hrsq
  change
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y * vecNorm2 p ≤
      vecNorm2
        (Fin.append
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i))
          (fun k : Fin (m + 1) =>
            lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
              ∑ i : Fin (m + 1),
                lsResidualComplementProjector (lsResidualHigham A b y) i k *
                  (Deltab i - rectMatMulVec DeltaA y i)))
  rw [← haction]
  exact hspec
/-- Squared spectral bridge for the nonzero-`p` WKS lower-bound route.  The
    row-side singular-value action of `[A  phi(I-r r^+)]` on the expanded
    feasible residual is bounded by the squared perturbation blocks, preserving
    the coupling needed for the sharp weighted-cost step in (20.21). -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrixSigmaMin_mul_source_residual_vecNorm2_sq_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y *
      vecNorm2
        (fun i : Fin (m + 1) =>
          lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i)) ^ 2 ≤
      frobNormSqRect DeltaA *
        vecNorm2Sq
          (fun i : Fin (m + 1) =>
            lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i) +
        (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let q : Fin (m + 1) → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
  let B : Fin (m + 1) → Fin (n + (m + 1)) → ℝ :=
    lsNormwiseBackwardErrorFormulaMatrix theta A r y
  have hspec :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p ≤
        vecNorm2 (rectMatMulVec (finiteTranspose B) p) :=
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_mul_vecNorm2_le_transpose_mulVec
      theta A r y p
  have hspec_sq :
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p) ^ 2 ≤
        vecNorm2Sq (rectMatMulVec (finiteTranspose B) p) := by
    have hleft_nonneg :
        0 ≤ lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p :=
      mul_nonneg
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y)
        (vecNorm2_nonneg p)
    have hright_nonneg :
        0 ≤ vecNorm2 (rectMatMulVec (finiteTranspose B) p) :=
      vecNorm2_nonneg _
    have habs :
        |lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p| ≤
          |vecNorm2 (rectMatMulVec (finiteTranspose B) p)| := by
      simpa [abs_of_nonneg hleft_nonneg, abs_of_nonneg hright_nonneg] using hspec
    have hsquare :=
      (sq_le_sq).mpr habs
    simpa [vecNorm2_sq] using hsquare
  have hupper :
      vecNorm2Sq (rectMatMulVec (finiteTranspose B) p) ≤
        frobNormSqRect DeltaA * vecNorm2Sq p +
          (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * vecNorm2Sq q := by
    simpa [r, p, q, B] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2Sq_le
        theta A b y DeltaA Deltab hfeas hrsq
  exact hspec_sq.trans hupper
/-- Exact squared spectral bridge for the nonzero-`p` WKS lower-bound route.
    This version keeps the right projector term as
    `phi * (I-r r^+)p` and the left term as the exact `DeltaA^T p` block,
    avoiding the Frobenius and projector-contraction relaxations. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrixSigmaMin_mul_source_residual_vecNorm2_sq_le_exact
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y *
      vecNorm2
        (fun i : Fin (m + 1) =>
          lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i)) ^ 2 ≤
      vecNorm2Sq
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i)) +
        (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y))
              (fun i : Fin (m + 1) =>
                lsResidualHigham A b y i + Deltab i -
                  rectMatMulVec DeltaA y i)) := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let B : Fin (m + 1) → Fin (n + (m + 1)) → ℝ :=
    lsNormwiseBackwardErrorFormulaMatrix theta A r y
  have hspec :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p ≤
        vecNorm2 (rectMatMulVec (finiteTranspose B) p) :=
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_mul_vecNorm2_le_transpose_mulVec
      theta A r y p
  have hspec_sq :
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p) ^ 2 ≤
        vecNorm2Sq (rectMatMulVec (finiteTranspose B) p) := by
    have hleft_nonneg :
        0 ≤ lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p :=
      mul_nonneg
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y)
        (vecNorm2_nonneg p)
    have hright_nonneg :
        0 ≤ vecNorm2 (rectMatMulVec (finiteTranspose B) p) :=
      vecNorm2_nonneg _
    have habs :
        |lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y * vecNorm2 p| ≤
          |vecNorm2 (rectMatMulVec (finiteTranspose B) p)| := by
      simpa [abs_of_nonneg hleft_nonneg, abs_of_nonneg hright_nonneg] using hspec
    have hsquare :=
      (sq_le_sq).mpr habs
    simpa [vecNorm2_sq] using hsquare
  have hsplit :
      vecNorm2Sq (rectMatMulVec (finiteTranspose B) p) =
        vecNorm2Sq
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j *
              (lsResidualHigham A b y i + Deltab i -
                rectMatMulVec DeltaA y i)) +
          (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
            vecNorm2Sq
              (matMulVec (m + 1)
                (lsResidualComplementProjector (lsResidualHigham A b y))
                (fun i : Fin (m + 1) =>
                  lsResidualHigham A b y i + Deltab i -
                    rectMatMulVec DeltaA y i)) := by
    simpa [r, p, B] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2Sq_eq_expanded
        theta A b y DeltaA Deltab hfeas hrsq
  calc
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y * vecNorm2 p) ^ 2
        ≤ vecNorm2Sq (rectMatMulVec (finiteTranspose B) p) := by
          simpa [r, p, B] using hspec_sq
    _ =
      vecNorm2Sq
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j *
            (lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i)) +
        (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y))
              (fun i : Fin (m + 1) =>
                lsResidualHigham A b y i + Deltab i -
                  rectMatMulVec DeltaA y i)) := hsplit
/-- Corrected conditional handoff for the nonzero-expanded-residual WKS
    lower-bound route.  Once the exact projector squared estimate for
    `DeltaA^T p` and `phi * (I-r r^+)p` is proved, the printed right-hand side
    in (20.21) is bounded by the weighted perturbation cost from (20.20). -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_costF_of_exact_projector_sq_bound
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hpne :
      (fun i : Fin (m + 1) =>
        lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) ≠ 0)
    (hexact :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let p : Fin (m + 1) → ℝ :=
        fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
      vecNorm2Sq
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j * p i) +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1) (lsResidualComplementProjector r) p) ≤
        (lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  let cost : ℝ := lsNormwiseBackwardErrorCostF theta DeltaA Deltab
  have hspectral :
      (sigma * vecNorm2 p) ^ 2 ≤
        vecNorm2Sq
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j * p i) +
          (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
            vecNorm2Sq
              (matMulVec (m + 1) (lsResidualComplementProjector r) p) := by
    simpa [r, p, sigma] using
      LSNormwiseBackwardErrorFeasible.formulaMatrixSigmaMin_mul_source_residual_vecNorm2_sq_le_exact
        theta A b y DeltaA Deltab hfeas hrsq
  have hexact' :
      vecNorm2Sq
          (fun j : Fin n =>
            -∑ i : Fin (m + 1), DeltaA i j * p i) +
          (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
            vecNorm2Sq
              (matMulVec (m + 1) (lsResidualComplementProjector r) p) ≤
        cost ^ 2 * vecNorm2Sq p := by
    simpa [r, p, cost] using hexact
  have hsq :
      (sigma * vecNorm2 p) ^ 2 ≤ (cost * vecNorm2 p) ^ 2 := by
    have hmain := hspectral.trans hexact'
    have hcost :
        cost ^ 2 * vecNorm2Sq p = (cost * vecNorm2 p) ^ 2 := by
      rw [← vecNorm2_sq p]
      ring
    simpa [hcost] using hmain
  have hp_pos : 0 < vecNorm2 p := by
    exact vecNorm2_pos_of_ne_zero_lsq (by simpa [p] using hpne)
  have hsigma_nonneg : 0 ≤ sigma := by
    simpa [sigma, r] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y
  have hleft_nonneg : 0 ≤ sigma * vecNorm2 p :=
    mul_nonneg hsigma_nonneg (vecNorm2_nonneg p)
  have hcost_nonneg : 0 ≤ cost := by
    simpa [cost] using lsNormwiseBackwardErrorCostF_nonneg theta DeltaA Deltab
  have hright_nonneg : 0 ≤ cost * vecNorm2 p :=
    mul_nonneg hcost_nonneg (vecNorm2_nonneg p)
  have hprod : sigma * vecNorm2 p ≤ cost * vecNorm2 p := by
    have habs := (sq_le_sq).mp hsq
    simpa [abs_of_nonneg hleft_nonneg, abs_of_nonneg hright_nonneg] using habs
  have hsigma_le_cost : sigma ≤ cost :=
    le_of_mul_le_mul_right hprod hp_pos
  have hrhs_le_sigma :
      lsNormwiseBackwardErrorFormulaRHS theta A b y ≤ sigma := by
    simpa [lsNormwiseBackwardErrorFormulaRHS, r, sigma] using
      lsNormwiseBackwardErrorFormulaValue_le_sigmaMin theta A r y
  exact hrhs_le_sigma.trans hsigma_le_cost
/-- Nonzero-expanded-residual WKS lower-bound branch.  For finite
    nonnegative `theta` and `y ≠ 0`, every feasible perturbation whose
    perturbed residual is nonzero has weighted Frobenius cost at least the
    printed right-hand side in (20.21). -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_costF_of_nonzero_expanded_residual
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hpne :
      (fun i : Fin (m + 1) =>
        lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  have hpsq : vecNorm2Sq p ≠ 0 := by
    exact ne_of_gt (vecNorm2Sq_pos_of_ne_zero_lsq (by simpa [p] using hpne))
  have hexact :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let p : Fin (m + 1) → ℝ :=
        fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
      vecNorm2Sq
        (fun j : Fin n =>
          -∑ i : Fin (m + 1), DeltaA i j * p i) +
        (lsNormwiseBackwardErrorPhi theta r y) ^ 2 *
          vecNorm2Sq
            (matMulVec (m + 1) (lsResidualComplementProjector r) p) ≤
        (lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ^ 2 *
          vecNorm2Sq p := by
    simpa [r, p] using
      lsNormwiseBackwardErrorExactProjector_sq_bound
        htheta hy r p hrsq hpsq DeltaA Deltab rfl
  exact
    lsNormwiseBackwardErrorFormulaRHS_le_costF_of_exact_projector_sq_bound
      theta A b y DeltaA Deltab hfeas hrsq hpne hexact
/-- Finite-`theta` pointwise WKS lower-bound packaging.  Combining the
    exact-residual `p = 0` branch with the nonzero-expanded-residual branch,
    every feasible perturbation has weighted Frobenius cost at least the
    printed right-hand side in (20.21), under the finite nonnegative `theta`,
    `y ≠ 0`, and nonzero source-residual assumptions. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_costF_of_feasible_finite_theta
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  by_cases hpzero : p = 0
  · have hres :
        lsResidualHigham A b y =
          fun i : Fin (m + 1) => rectMatMulVec DeltaA y i - Deltab i := by
      ext i
      have hp_i :
          lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i = 0 := by
        simpa [p] using congrFun hpzero i
      linarith
    exact
      lsNormwiseBackwardErrorFormulaRHS_le_costF_of_source_residual_eq_deltaA_y_sub_deltab
        htheta A b hy DeltaA Deltab hres
  · exact
      lsNormwiseBackwardErrorFormulaRHS_le_costF_of_nonzero_expanded_residual
        htheta A b hy DeltaA Deltab hfeas hrsq (by simpa [p] using hpzero)
/-- Finite-`theta` lower-bound side of the WKS formula (20.21).  The pointwise
    feasible perturbation lower bound transfers to the infimum model
    `eta_F(y)` from (20.20). -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_finite_theta
    {m n : ℕ} {theta : ℝ} (htheta : 0 ≤ theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorEtaF theta A b y := by
  apply
    lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_forall_feasible_cost_ge
      theta A b y
  intro DeltaA Deltab hfeas
  exact
    lsNormwiseBackwardErrorFormulaRHS_le_costF_of_feasible_finite_theta
      htheta A b hy DeltaA Deltab hfeas hrsq
/-- Conditional handoff for the nonzero-expanded-residual WKS lower-bound route.
    Once the sharp coupled squared estimate converts the two perturbation blocks
    from `formulaMatrixSigmaMin_mul_source_residual_vecNorm2_sq_le` into
    `cost_F^2 * ||p||_2^2`, the printed right-hand side in (20.21) is bounded by
    the weighted perturbation cost from (20.20). -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_costF_of_coupled_sq_bound
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0)
    (hpne :
      (fun i : Fin (m + 1) =>
        lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) ≠ 0)
    (hcoupled :
      let p : Fin (m + 1) → ℝ :=
        fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
      let q : Fin (m + 1) → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
      frobNormSqRect DeltaA * vecNorm2Sq p +
        (lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) ^ 2 *
          vecNorm2Sq q ≤
        (lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ^ 2 * vecNorm2Sq p) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let q : Fin (m + 1) → ℝ := fun i => Deltab i - rectMatMulVec DeltaA y i
  let sigma : ℝ := lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y
  let cost : ℝ := lsNormwiseBackwardErrorCostF theta DeltaA Deltab
  have hspectral :
      (sigma * vecNorm2 p) ^ 2 ≤
        frobNormSqRect DeltaA * vecNorm2Sq p +
          (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * vecNorm2Sq q := by
    simpa [r, p, q, sigma] using
      LSNormwiseBackwardErrorFeasible.formulaMatrixSigmaMin_mul_source_residual_vecNorm2_sq_le
        theta A b y DeltaA Deltab hfeas hrsq
  have hcoupled' :
      frobNormSqRect DeltaA * vecNorm2Sq p +
          (lsNormwiseBackwardErrorPhi theta r y) ^ 2 * vecNorm2Sq q ≤
        cost ^ 2 * vecNorm2Sq p := by
    simpa [r, p, q, cost] using hcoupled
  have hsq :
      (sigma * vecNorm2 p) ^ 2 ≤ (cost * vecNorm2 p) ^ 2 := by
    have hmain := hspectral.trans hcoupled'
    have hcost :
        cost ^ 2 * vecNorm2Sq p = (cost * vecNorm2 p) ^ 2 := by
      rw [← vecNorm2_sq p]
      ring
    simpa [hcost] using hmain
  have hp_pos : 0 < vecNorm2 p := by
    exact vecNorm2_pos_of_ne_zero_lsq (by simpa [p] using hpne)
  have hsigma_nonneg : 0 ≤ sigma := by
    simpa [sigma, r] using
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin_nonneg theta A r y
  have hleft_nonneg : 0 ≤ sigma * vecNorm2 p :=
    mul_nonneg hsigma_nonneg (vecNorm2_nonneg p)
  have hcost_nonneg : 0 ≤ cost := by
    simpa [cost] using lsNormwiseBackwardErrorCostF_nonneg theta DeltaA Deltab
  have hright_nonneg : 0 ≤ cost * vecNorm2 p :=
    mul_nonneg hcost_nonneg (vecNorm2_nonneg p)
  have hprod : sigma * vecNorm2 p ≤ cost * vecNorm2 p := by
    have habs := (sq_le_sq).mp hsq
    simpa [abs_of_nonneg hleft_nonneg, abs_of_nonneg hright_nonneg] using habs
  have hsigma_le_cost : sigma ≤ cost :=
    le_of_mul_le_mul_right hprod hp_pos
  have hrhs_le_sigma :
      lsNormwiseBackwardErrorFormulaRHS theta A b y ≤ sigma := by
    simpa [lsNormwiseBackwardErrorFormulaRHS, r, sigma] using
      lsNormwiseBackwardErrorFormulaValue_le_sigmaMin theta A r y
  exact hrhs_le_sigma.trans hsigma_le_cost
/-- Upper bound for the full transposed WKS source-block action on the
    expanded feasible residual.  This combines the left Frobenius estimate for
    `DeltaA^T p` with the right projector-contraction estimate, exposing the
    remaining coupled weighted-cost step for the nonzero-`p` lower-bound route
    behind (20.21). -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2_le
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    vecNorm2
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          (fun i => lsResidualHigham A b y i + Deltab i -
            rectMatMulVec DeltaA y i)) ≤
      frobNormRect DeltaA *
        vecNorm2
          (fun i : Fin (m + 1) =>
            lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i) +
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
          vecNorm2
            (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  let left : Fin n → ℝ :=
    fun j => -∑ i : Fin (m + 1), DeltaA i j * p i
  let right : Fin (m + 1) → ℝ :=
    fun k =>
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
        ∑ i : Fin (m + 1),
          lsResidualComplementProjector (lsResidualHigham A b y) i k *
            (Deltab i - rectMatMulVec DeltaA y i)
  have haction :
      rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          p =
        Fin.append left right := by
    simpa [p, left, right] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_eq
        theta A b y DeltaA Deltab hfeas hrsq
  have hleft :
      vecNorm2 left ≤
        frobNormRect DeltaA * vecNorm2 p := by
    simpa [p, left] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_left_vecNorm2_le
        A b y DeltaA Deltab
  have hright :
      vecNorm2 right ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
          vecNorm2
            (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
    simpa [right] using
      lsNormwiseBackwardErrorFormulaMatrix_transpose_source_residual_right_vecNorm2_le
        theta A b y DeltaA Deltab hrsq
  calc
    vecNorm2
        (rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y))
          p)
        = vecNorm2 (Fin.append left right) := by rw [haction]
    _ ≤ vecNorm2 left + vecNorm2 right :=
        lsVecNorm2_append_le_add left right
    _ ≤ frobNormRect DeltaA * vecNorm2 p +
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
          vecNorm2
            (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) :=
        add_le_add hleft hright
/-- Spectral-to-additive bridge for the nonzero-`p` WKS lower-bound route.
    Combining the row-side `sigma_min` action of the source block
    `[A phi(I-r r^+)]` with the full transposed-action estimate gives the
    remaining inequality before the sharp coupled weighted-cost step.  This is
    intentionally not the final (20.21) lower-bound theorem. -/
theorem LSNormwiseBackwardErrorFeasible.formulaMatrixSigmaMin_mul_source_residual_vecNorm2_le_add
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (DeltaA : Fin (m + 1) → Fin n → ℝ)
    (Deltab : Fin (m + 1) → ℝ)
    (hfeas : LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y *
      vecNorm2
        (fun i : Fin (m + 1) =>
          lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i) ≤
      frobNormRect DeltaA *
        vecNorm2
          (fun i : Fin (m + 1) =>
            lsResidualHigham A b y i + Deltab i -
              rectMatMulVec DeltaA y i) +
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
          vecNorm2
            (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
  let p : Fin (m + 1) → ℝ :=
    fun i => lsResidualHigham A b y i + Deltab i - rectMatMulVec DeltaA y i
  have hspec :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y * vecNorm2 p ≤
        vecNorm2
          (rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y)) p) :=
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_mul_vecNorm2_le_transpose_mulVec
      theta A (lsResidualHigham A b y) y p
  have hupper :
      vecNorm2
          (rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y)) p) ≤
        frobNormRect DeltaA * vecNorm2 p +
          lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y *
            vecNorm2
              (fun i : Fin (m + 1) => Deltab i - rectMatMulVec DeltaA y i) := by
    simpa [p] using
      LSNormwiseBackwardErrorFeasible.formulaMatrix_transpose_source_residual_vecNorm2_le
        theta A b y DeltaA Deltab hfeas hrsq
  exact hspec.trans hupper
/-- Exact-minimizer specialization of the nonzero-residual left-null
    certificate for (20.21).  If `y` is a least-squares minimizer, then
    normal-equation orthogonality of Higham's residual `b - A y` to the data
    columns makes a nonzero residual annihilate the full WKS source block
    `[A  phi(I-r r^+)]` from the left. -/
theorem lsNormwiseBackwardErrorFormulaMatrix_residual_leftNull_of_isLeastSquaresMinimizer
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer A b y)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    ∀ k : Fin (n + (m + 1)),
      ∑ i : Fin (m + 1),
        lsNormwiseBackwardErrorFormulaMatrix theta A (lsResidualHigham A b y) y i k *
          lsResidualHigham A b y i = 0 := by
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  have hNE : RectLSNormalEquations A b y :=
    hmin.rectLSNormalEquations
  have horth : ∀ j : Fin n, ∑ i : Fin (m + 1), A i j * r i = 0 := by
    intro j
    dsimp [r]
    rw [lsResidualHigham_column_sum_eq_neg A b y j]
    rw [hNE.residual_orthogonal j]
    ring
  simpa [r] using
    lsNormwiseBackwardErrorFormulaMatrix_residual_leftNull_of_column_orthogonal
      theta A r y horth hrsq
/-- Exact-minimizer, nonzero-residual specialization of the (20.21)
    source-block row-side singular-value branch: the residual left-null
    certificate forces `sigma_min [A phi(I-r r^+)] = 0`. -/
theorem lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_of_isLeastSquaresMinimizer
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer A b y)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
      (lsResidualHigham A b y) y = 0 := by
  exact
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_of_leftNull
      theta A (lsResidualHigham A b y) y hrsq
      (lsNormwiseBackwardErrorFormulaMatrix_residual_leftNull_of_isLeastSquaresMinimizer
        theta A b y hmin hrsq)
/-- Exact-minimizer, nonzero-residual branch of the printed right-hand side in
    (20.21): the source block has zero row-side `sigma_min`, so the displayed
    outer minimum is zero. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer_nonzero_residual
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer A b y)
    (hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 := by
  unfold lsNormwiseBackwardErrorFormulaRHS
  exact
    (lsNormwiseBackwardErrorFormulaValue_eq_zero_iff
      theta A (lsResidualHigham A b y) y).mpr
      (Or.inr
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_of_isLeastSquaresMinimizer
          theta A b y hmin hrsq))
/-- Exact least-squares minimizers close the printed right-hand side in
    (20.21): the zero-residual branch kills `phi`, while the nonzero-residual
    branch gives a left-null vector for the source block and hence zero
    row-side `sigma_min`. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 := by
  by_cases hrsq : vecNorm2Sq (lsResidualHigham A b y) = 0
  · have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact
      lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_lsResidualHigham_eq_zero
        theta A b y hres
  · exact
      lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer_nonzero_residual
        theta A b y hmin hrsq
/-- Exact-minimizer lower-bound side of (20.21): on this zero branch the
    printed right-hand side is zero, so it is below `eta_F(y)`.  The equality
    branch is proved separately. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_isLeastSquaresMinimizer
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorEtaF theta A b y := by
  exact
    lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_formulaRHS_eq_zero
      theta A b y
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
        theta A b y hmin)
/-- Exact least-squares minimizers close both residual branches of (20.20)-
    (20.21): `eta_F(y)` and the printed WKS right-hand side are both zero,
    hence equal.  This does not prove the general WKS lower-bound/attainment
    theorem for non-minimizers. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_isLeastSquaresMinimizer
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y = 0 :=
    lsNormwiseBackwardErrorEtaF_eq_zero_of_isLeastSquaresMinimizer
      theta A b y hmin
  have hrhs :
      lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 :=
    lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
      theta A b y hmin
  rw [heta, hrhs]
/-- Positive finite-`theta` zero-error consistency for (20.20)-(20.21): if the
    normwise backward-error infimum vanishes, the printed WKS right-hand side
    also vanishes.  The converse still requires the open spectral formula for
    non-minimizer degeneracies. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_etaF_eq_zero_of_positive_theta
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    (y : Fin n → ℝ)
    (heta : lsNormwiseBackwardErrorEtaF theta A b y = 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 := by
  have hmin :
      IsLeastSquaresMinimizer A b y :=
    (lsNormwiseBackwardErrorEtaF_eq_zero_iff_isLeastSquaresMinimizer_of_positive_theta
      htheta A b y).mp heta
  exact
    lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
      theta A b y hmin
/-- Positive finite-`theta` zero-error equality branch for (20.20)-(20.21):
    if the normwise backward-error infimum vanishes, then `y` is already an
    exact least-squares minimizer, so the printed WKS right-hand side also
    agrees with `eta_F(y)`.  This is only the zero-error branch, not the
    general Walden--Karlson--Sun spectral formula for non-minimizers. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_etaF_eq_zero_of_positive_theta
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    (y : Fin n → ℝ)
    (heta : lsNormwiseBackwardErrorEtaF theta A b y = 0) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hmin :
      IsLeastSquaresMinimizer A b y :=
    (lsNormwiseBackwardErrorEtaF_eq_zero_iff_isLeastSquaresMinimizer_of_positive_theta
      htheta A b y).mp heta
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_isLeastSquaresMinimizer
      theta A b y hmin
/-- The source scalar `mu` in Theorem 20.5 is positive for positive finite
    `theta` and a nonzero candidate vector `y`. -/
theorem lsNormwiseBackwardErrorMu_pos_of_theta_pos_of_y_ne_zero
    {n : ℕ} {theta : ℝ} (htheta : 0 < theta) {y : Fin n → ℝ}
    (hy : y ≠ 0) :
    0 < lsNormwiseBackwardErrorMu theta y := by
  unfold lsNormwiseBackwardErrorMu
  exact div_pos
    (mul_pos (sq_pos_of_pos htheta) (vecNorm2Sq_pos_of_ne_zero_lsq hy))
    (lsNormwiseBackwardErrorMu_den_pos theta y)
/-- Nondegenerate scalar branch of (20.21): for positive finite `theta` and
    nonzero `y`, the displayed `phi` scalar vanishes exactly when the residual
    vector vanishes. -/
theorem lsNormwiseBackwardErrorPhi_eq_zero_iff_residual_eq_zero_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    {r : Fin m → ℝ} {y : Fin n → ℝ} (hy : y ≠ 0) :
    lsNormwiseBackwardErrorPhi theta r y = 0 ↔ r = 0 := by
  constructor
  · intro hphi
    have hmu_pos :
        0 < lsNormwiseBackwardErrorMu theta y :=
      lsNormwiseBackwardErrorMu_pos_of_theta_pos_of_y_ne_zero htheta hy
    have hsqrt_pos :
        0 < Real.sqrt (lsNormwiseBackwardErrorMu theta y) :=
      Real.sqrt_pos.2 hmu_pos
    have hy_norm_pos : 0 < vecNorm2 y := vecNorm2_pos_of_ne_zero_lsq hy
    have hy_norm_ne : vecNorm2 y ≠ 0 := ne_of_gt hy_norm_pos
    unfold lsNormwiseBackwardErrorPhi at hphi
    field_simp [hy_norm_ne] at hphi
    have hphi_zero :
        Real.sqrt (lsNormwiseBackwardErrorMu theta y) * vecNorm2 r = 0 := by
      simpa using hphi
    have hr_norm : vecNorm2 r = 0 :=
      (mul_eq_zero.mp hphi_zero).resolve_left (ne_of_gt hsqrt_pos)
    ext i
    exact (vecNorm2_eq_zero_iff r).mp hr_norm i
  · intro hr
    exact lsNormwiseBackwardErrorPhi_eq_zero_of_residual_eq_zero theta y hr
/-- Nondegenerate converse-zero branch for (20.20)-(20.21): if the printed
    right-hand side is zero, `theta > 0`, `y ≠ 0`, and the row-side
    `sigma_min [A phi(I-r r^+)]` branch is positive, then the zero must come
    from `phi = 0`; hence the residual is zero and `eta_F(y)` equals the
    printed right-hand side.  The remaining converse case is the degenerate
    `sigma_min = 0` branch. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaRHS_eq_zero_of_sigmaMin_pos
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrhs : lsNormwiseBackwardErrorFormulaRHS theta A b y = 0)
    (hsigma :
      0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hbranches :
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y = 0 ∨
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y = 0 :=
    (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff theta A b y).mp hrhs
  have hphi :
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y = 0 := by
    rcases hbranches with hphi | hsigma_zero
    · exact hphi
    · exact False.elim ((ne_of_gt hsigma) hsigma_zero)
  have hres :
      lsResidualHigham A b y = 0 :=
    (lsNormwiseBackwardErrorPhi_eq_zero_iff_residual_eq_zero_of_theta_pos_of_y_ne_zero
      htheta hy).mp hphi
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_lsResidualHigham_eq_zero
      theta A b y hres
/-- Non-minimizer scalar branch of (20.21): for positive finite `theta` and
    nonzero `y`, the displayed `phi` scalar is positive whenever `y` is not an
    exact least-squares minimizer. -/
theorem lsNormwiseBackwardErrorPhi_pos_of_not_isLeastSquaresMinimizer_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    0 < lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y := by
  have hnonneg :
      0 ≤ lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y :=
    lsNormwiseBackwardErrorPhi_nonneg theta (lsResidualHigham A b y) y
  have hne :
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y ≠ 0 := by
    intro hphi
    have hres :
        lsResidualHigham A b y = 0 :=
      (lsNormwiseBackwardErrorPhi_eq_zero_iff_residual_eq_zero_of_theta_pos_of_y_ne_zero
        htheta hy).mp hphi
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  exact lt_of_le_of_ne hnonneg (Ne.symm hne)
/-- On the positive finite-weight, non-minimizer branch, the only real
    left-null vector of the WKS formula matrix is zero.  The complementary
    projector first forces such a vector to be parallel to the residual;
    failure of the normal equations then kills that remaining direction. -/
private theorem
    finiteTranspose_formulaMatrix_real_kernel_eq_zero_of_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    {p : Fin (m + 1) → ℝ}
    (hp :
      rectMatMulVec
          (finiteTranspose
            (lsNormwiseBackwardErrorFormulaMatrix theta A
              (lsResidualHigham A b y) y)) p =
        0) :
    p = 0 := by
  classical
  let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
  let phi : ℝ := lsNormwiseBackwardErrorPhi theta r y
  have hphi : phi ≠ 0 := by
    exact ne_of_gt (by
      simpa [phi, r] using
        lsNormwiseBackwardErrorPhi_pos_of_not_isLeastSquaresMinimizer_of_theta_pos_of_y_ne_zero
          htheta A b hy hnot)
  have hA (j : Fin n) : ∑ i : Fin (m + 1), A i j * p i = 0 := by
    have hj := congrFun hp (Fin.castAdd (m + 1) j)
    simpa [rectMatMulVec, finiteTranspose,
      lsNormwiseBackwardErrorFormulaMatrix, Fin.append_left, r] using hj
  have hright (j : Fin (m + 1)) :
      phi *
          (∑ i : Fin (m + 1),
            lsResidualComplementProjector r i j * p i) =
        0 := by
    have hj := congrFun hp (Fin.natAdd n j)
    have hj' :
        (∑ i : Fin (m + 1),
            (phi * lsResidualComplementProjector r i j) * p i) =
          0 := by
      simpa [rectMatMulVec, finiteTranspose,
        lsNormwiseBackwardErrorFormulaMatrix, Fin.append_right, phi, r] using hj
    calc
      phi *
          (∑ i : Fin (m + 1),
            lsResidualComplementProjector r i j * p i) =
          ∑ i : Fin (m + 1),
            (phi * lsResidualComplementProjector r i j) * p i := by
              rw [Finset.mul_sum]
              apply Finset.sum_congr rfl
              intro i _
              ring
      _ = 0 := hj'
  have hproj (j : Fin (m + 1)) :
      ∑ i : Fin (m + 1),
          lsResidualComplementProjector r j i * p i =
        0 := by
    have hcol :
        ∑ i : Fin (m + 1),
            lsResidualComplementProjector r i j * p i =
          0 :=
      (mul_eq_zero.mp (hright j)).resolve_left hphi
    calc
      (∑ i : Fin (m + 1),
          lsResidualComplementProjector r j i * p i) =
          ∑ i : Fin (m + 1),
            lsResidualComplementProjector r i j * p i := by
              apply Finset.sum_congr rfl
              intro i _
              rw [lsResidualComplementProjector_symmetric]
      _ = 0 := hcol
  let c : ℝ :=
    (∑ k : Fin (m + 1), r k * p k) / vecNorm2Sq r
  have hspan (i : Fin (m + 1)) : p i = c * r i := by
    have happ := lsLemma20_6ProjectorComplement_apply_vec r p i
    rw [hproj i] at happ
    have hi :
        p i =
          (r i / vecNorm2Sq r) *
            ∑ k : Fin (m + 1), r k * p k :=
      sub_eq_zero.mp happ.symm
    rw [hi]
    simp only [c]
    ring
  have hnotorth :
      ¬ ∀ j : Fin n, ∑ i : Fin (m + 1), A i j * r i = 0 := by
    intro horth
    apply hnot
    have hnormal : RectLSNormalEquations A b y :=
      RectLSNormalEquations.of_residual_orthogonal (by
        intro j
        have hsign := lsResidualHigham_column_sum_eq_neg A b y j
        have hj := horth j
        simp only [r] at hj
        rw [hj] at hsign
        linarith)
    exact (RectLSNormalEquations.iff_isLeastSquaresMinimizer A b y).mp hnormal
  rcases not_forall.mp hnotorth with ⟨j, hj⟩
  have hcmul : c * (∑ i : Fin (m + 1), A i j * r i) = 0 := by
    calc
      c * (∑ i : Fin (m + 1), A i j * r i) =
          ∑ i : Fin (m + 1), A i j * (c * r i) := by
            rw [Finset.mul_sum]
            apply Finset.sum_congr rfl
            intro i _
            ring
      _ = ∑ i : Fin (m + 1), A i j * p i := by
            apply Finset.sum_congr rfl
            intro i _
            rw [hspan]
      _ = 0 := hA j
  have hc : c = 0 := (mul_eq_zero.mp hcmul).resolve_right hj
  ext i
  rw [hspan i, hc]
  simp
/-- Positive `theta`, a nonzero candidate, and failure of least-squares
    optimality imply the full row-rank condition required by the finite WKS
    formula.  No rank assumption on the original data matrix is needed. -/
theorem lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_positive_theta_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y =
      m + 1 := by
  let MF := realRectToCMatrix
    (finiteTranspose
      (lsNormwiseBackwardErrorFormulaMatrix theta A
        (lsResidualHigham A b y) y))
  have hker : LinearMap.ker (complexMatrixEuclideanLin MF) = ⊥ := by
    rw [LinearMap.ker_eq_bot']
    intro z hz
    have hre :
        rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y))
            (euclideanReVec z) =
          0 := by
      ext j
      have hj :
          (WithLp.ofLp (complexMatrixEuclideanLin MF z)) j = 0 := by
        rw [hz]
        rfl
      have hjre := congrArg Complex.re hj
      rw [realRectToCMatrix_euclideanLin_ofLp] at hjre
      rw [realRectToCMatrix_vecMul_re] at hjre
      simpa [MF] using hjre
    have him :
        rectMatMulVec
            (finiteTranspose
              (lsNormwiseBackwardErrorFormulaMatrix theta A
                (lsResidualHigham A b y) y))
            (euclideanImVec z) =
          0 := by
      ext j
      have hj :
          (WithLp.ofLp (complexMatrixEuclideanLin MF z)) j = 0 := by
        rw [hz]
        rfl
      have hjim := congrArg Complex.im hj
      rw [realRectToCMatrix_euclideanLin_ofLp] at hjim
      rw [realRectToCMatrix_vecMul_im] at hjim
      simpa [MF] using hjim
    have hre_zero : euclideanReVec z = 0 :=
      finiteTranspose_formulaMatrix_real_kernel_eq_zero_of_not_isLeastSquaresMinimizer
        htheta A b hy hnot hre
    have him_zero : euclideanImVec z = 0 :=
      finiteTranspose_formulaMatrix_real_kernel_eq_zero_of_not_isLeastSquaresMinimizer
        htheta A b hy hnot him
    apply norm_eq_zero.mp
    have hnorm_sq := euclidean_norm_sq_re_im z
    rw [hre_zero, him_zero] at hnorm_sq
    have hzero_norm : vecNorm2 (0 : Fin (m + 1) → ℝ) = 0 := by
      simpa using (vecNorm2_zero (n := m + 1))
    rw [hzero_norm] at hnorm_sq
    exact sq_eq_zero_iff.mp (by simpa using hnorm_sq)
  simpa [lsNormwiseBackwardErrorFormulaMatrixRowRank, MF] using
    complexMatrixRank_eq_card_of_euclideanLin_ker_eq_bot MF hker
/-- Non-minimizer zero-case classification for (20.20)-(20.21): after excluding
    the exact-minimizer branch, a zero printed RHS is equivalent to the
    degenerate row-side `sigma_min [A phi(I-r r^+)] = 0` branch. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_sigmaMin_eq_zero_of_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y = 0 := by
  constructor
  · intro hrhs
    have hbranches :
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y = 0 ∨
          lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
            (lsResidualHigham A b y) y = 0 :=
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff theta A b y).mp hrhs
    rcases hbranches with hphi | hsigma
    · have hres :
          lsResidualHigham A b y = 0 :=
        (lsNormwiseBackwardErrorPhi_eq_zero_iff_residual_eq_zero_of_theta_pos_of_y_ne_zero
          htheta hy).mp hphi
      exact False.elim (hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres))
    · exact hsigma
  · intro hsigma
    exact
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff theta A b y).mpr
        (Or.inr hsigma)
/-- Non-minimizer positive-case classification for (20.20)-(20.21): for
    positive finite `theta` and nonzero `y`, once `y` is not an exact
    least-squares minimizer, positivity of the printed RHS is equivalent to
    positivity of the row-side `sigma_min [A phi(I-r r^+)]` branch. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_sigmaMin_pos_of_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y := by
  have hphi_pos :
      0 < lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y :=
    lsNormwiseBackwardErrorPhi_pos_of_not_isLeastSquaresMinimizer_of_theta_pos_of_y_ne_zero
      htheta A b hy hnot
  constructor
  · intro hrhs
    exact ((lsNormwiseBackwardErrorFormulaRHS_pos_iff theta A b y).mp hrhs).2
  · intro hsigma
    exact
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff theta A b y).mpr
        ⟨hphi_pos, hsigma⟩
/-- Global finite-positive-`theta` zero-branch decomposition for (20.20)-(20.21):
    when `y` is nonzero, the printed WKS right-hand side is zero exactly when
    either `y` is already an exact least-squares minimizer or the row-side
    `sigma_min [A phi(I-r r^+)]` branch is degenerate.  This isolates the
    remaining non-minimizer obstruction; it is not the general WKS spectral
    equality proof. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_isLeastSquaresMinimizer_or_sigmaMin_eq_zero_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      IsLeastSquaresMinimizer A b y ∨
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y = 0 := by
  constructor
  · intro hrhs
    by_cases hmin : IsLeastSquaresMinimizer A b y
    · exact Or.inl hmin
    · exact Or.inr
        ((lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_sigmaMin_eq_zero_of_not_isLeastSquaresMinimizer
          htheta A b hy hmin).mp hrhs)
  · intro h
    rcases h with hmin | hsigma
    · exact
        lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
          theta A b y hmin
    · exact
        (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff theta A b y).mpr
          (Or.inr hsigma)
/-- Global finite-positive-`theta` positive-branch decomposition for
    (20.20)-(20.21): when `y` is nonzero, the printed WKS right-hand side is
    positive exactly when `y` is not an exact least-squares minimizer and the
    row-side `sigma_min [A phi(I-r r^+)]` branch is positive. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_sigmaMin_pos_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      ¬ IsLeastSquaresMinimizer A b y ∧
        0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y := by
  constructor
  · intro hrhs
    refine ⟨?_, ((lsNormwiseBackwardErrorFormulaRHS_pos_iff theta A b y).mp hrhs).2⟩
    intro hmin
    exact (ne_of_gt hrhs)
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
        theta A b y hmin)
  · intro h
    exact
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff_sigmaMin_pos_of_not_isLeastSquaresMinimizer
        htheta A b hy h.1).mpr h.2
/-- Nondegenerate finite-positive-`theta` zero bridge for (20.20)-(20.21):
    if the row-side `sigma_min [A phi(I-r r^+)]` branch is positive, then
    the printed WKS right-hand side vanishes exactly when `eta_F(y)` vanishes.
    This avoids the open degenerate `sigma_min = 0` branch and does not prove
    the general WKS spectral equality. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_of_theta_pos_of_y_ne_zero_of_sigmaMin_pos
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hsigma :
      0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorEtaF theta A b y = 0 := by
  constructor
  · intro hrhs
    have hsplit :
        IsLeastSquaresMinimizer A b y ∨
          lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
            (lsResidualHigham A b y) y = 0 :=
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_isLeastSquaresMinimizer_or_sigmaMin_eq_zero_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs
    have hmin : IsLeastSquaresMinimizer A b y := by
      rcases hsplit with hmin | hsigma_zero
      · exact hmin
      · exact False.elim ((ne_of_gt hsigma) hsigma_zero)
    exact
      (lsNormwiseBackwardErrorEtaF_eq_zero_iff_isLeastSquaresMinimizer_of_positive_theta
        htheta A b y).mpr hmin
  · intro heta
    have hmin :
        IsLeastSquaresMinimizer A b y :=
      (lsNormwiseBackwardErrorEtaF_eq_zero_iff_isLeastSquaresMinimizer_of_positive_theta
        htheta A b y).mp heta
    exact
      lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
        theta A b y hmin
/-- Nondegenerate finite-positive-`theta` positive bridge for (20.20)-(20.21):
    if the row-side `sigma_min [A phi(I-r r^+)]` branch is positive, then
    positivity of the printed WKS right-hand side is equivalent to positivity
    of `eta_F(y)`.  This is only the nondegenerate branch. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_etaF_pos_of_theta_pos_of_y_ne_zero_of_sigmaMin_pos
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hsigma :
      0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      0 < lsNormwiseBackwardErrorEtaF theta A b y := by
  constructor
  · intro hrhs
    have hnot :
        ¬ IsLeastSquaresMinimizer A b y :=
      ((lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_sigmaMin_pos_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs).1
    exact
      (lsNormwiseBackwardErrorEtaF_pos_iff_not_isLeastSquaresMinimizer_of_positive_theta
        htheta A b y).mpr hnot
  · intro heta_pos
    have hnot :
        ¬ IsLeastSquaresMinimizer A b y :=
      (lsNormwiseBackwardErrorEtaF_pos_iff_not_isLeastSquaresMinimizer_of_positive_theta
        htheta A b y).mp heta_pos
    exact
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_sigmaMin_pos_of_theta_pos_of_y_ne_zero
        htheta A b hy).mpr ⟨hnot, hsigma⟩
/-- Source-facing full-row-rank version of the nondegenerate zero bridge for
    (20.20)-(20.21): if the row-side block `[A phi(I-r r^+)]` has full row
    rank, then its `sigma_min` branch is positive, so the printed WKS right-hand
    side vanishes exactly when `eta_F(y)` vanishes.  This is still only the
    nondegenerate branch, not the full WKS spectral formula. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_of_theta_pos_of_y_ne_zero_of_formulaMatrixRowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorEtaF theta A b y = 0 := by
  have hsigma :
      0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y :=
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
      theta A (lsResidualHigham A b y) y).mpr hrank
  exact
    lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_of_theta_pos_of_y_ne_zero_of_sigmaMin_pos
      htheta A b hy hsigma
/-- Source-facing full-row-rank version of the nondegenerate positivity bridge
    for (20.20)-(20.21): if `[A phi(I-r r^+)]` has full row rank, then
    positivity of the printed WKS right-hand side is equivalent to positivity of
    `eta_F(y)`.  This leaves the degenerate row-rank-deficient branch open for
    the later spectral lower/attainment theorem. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_etaF_pos_of_theta_pos_of_y_ne_zero_of_formulaMatrixRowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      0 < lsNormwiseBackwardErrorEtaF theta A b y := by
  have hsigma :
      0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y :=
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
      theta A (lsResidualHigham A b y) y).mpr hrank
  exact
    lsNormwiseBackwardErrorFormulaRHS_pos_iff_etaF_pos_of_theta_pos_of_y_ne_zero_of_sigmaMin_pos
      htheta A b hy hsigma
/-- Source-facing full-row-rank zero-equality branch for (20.20)-(20.21): if
    `[A phi(I-r r^+)]` has full row rank and the printed WKS right-hand side is
    zero, then `eta_F(y)` equals that right-hand side.  This is only the zero
    branch of the nondegenerate case, not the full positive-branch WKS equality. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaRHS_eq_zero_of_formulaMatrixRowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrhs : lsNormwiseBackwardErrorFormulaRHS theta A b y = 0)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have heta :
      lsNormwiseBackwardErrorEtaF theta A b y = 0 :=
    (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_of_theta_pos_of_y_ne_zero_of_formulaMatrixRowRank_eq_card
      htheta A b hy hrank).mp hrhs
  rw [heta, hrhs]
/-- Full-left-panel version of the nondegenerate zero bridge for
    (20.20)-(20.21): if the original data matrix `A` has full row rank, then
    the source block `[A phi(I-r r^+)]` has full row rank, so printed-RHS zero
    is equivalent to zero normwise backward error.  This is still a branch
    result, not the full WKS spectral formula. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_of_theta_pos_of_y_ne_zero_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hA : lsRealRectRowRank A = m + 1) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorEtaF theta A b y = 0 := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_of_theta_pos_of_y_ne_zero_of_formulaMatrixRowRank_eq_card
      htheta A b hy hrank
/-- Full-left-panel version of the nondegenerate positivity bridge for
    (20.20)-(20.21): if the original data matrix `A` has full row rank, then
    the printed WKS right-hand side is positive exactly when `eta_F(y)` is
    positive.  This packages the full-row-rank data-matrix branch only. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_etaF_pos_of_theta_pos_of_y_ne_zero_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hA : lsRealRectRowRank A = m + 1) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      0 < lsNormwiseBackwardErrorEtaF theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorFormulaRHS_pos_iff_etaF_pos_of_theta_pos_of_y_ne_zero_of_formulaMatrixRowRank_eq_card
      htheta A b hy hrank
/-- Full-left-panel zero-equality branch for (20.20)-(20.21): if the original
    data matrix `A` has full row rank and the printed WKS right-hand side is
    zero, then `eta_F(y)` equals that right-hand side.  This packages the
    source-data nondegenerate zero branch only. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaRHS_eq_zero_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrhs : lsNormwiseBackwardErrorFormulaRHS theta A b y = 0)
    (hA : lsRealRectRowRank A = m + 1) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaRHS_eq_zero_of_formulaMatrixRowRank_eq_card
      htheta A b hy hrhs hrank
/-- Row-rank-deficient source-block branch for the printed WKS right-hand side:
    if `[A phi(I-r r^+)]` is not full row rank, then the row-side
    `sigma_min` branch is zero, so the printed RHS in (20.21) is zero. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_formulaMatrixRowRank_ne_card
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y ≠ m + 1) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 := by
  have hsigma :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
        (lsResidualHigham A b y) y = 0 :=
    (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_iff_rowRank_ne_card
      theta A (lsResidualHigham A b y) y).mpr hrank
  exact
    (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff theta A b y).mpr
      (Or.inr hsigma)
/-- Row-rank-deficient lower-bound side of (20.21): if the source block
    `[A phi(I-r r^+)]` is row-rank deficient, the printed RHS is zero and hence
    below the nonnegative infimum model `eta_F(y)`. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_formulaMatrixRowRank_ne_card
    {m n : ℕ} (theta : ℝ) (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) (y : Fin n → ℝ)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y ≠ m + 1) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorEtaF theta A b y := by
  exact
    lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_formulaRHS_eq_zero
      theta A b y
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_formulaMatrixRowRank_ne_card
        theta A b y hrank)
/-- Non-minimizer row-rank-deficient zero classification for (20.20)-(20.21):
    after excluding the exact-minimizer branch, the printed RHS vanishes exactly
    when `[A phi(I-r r^+)]` is not full row rank. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_formulaMatrixRowRank_ne_card_of_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y ≠ m + 1 := by
  exact
    (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_sigmaMin_eq_zero_of_not_isLeastSquaresMinimizer
      htheta A b hy hnot).trans
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_iff_rowRank_ne_card
        theta A (lsResidualHigham A b y) y)
/-- Non-minimizer full-row-rank positivity classification for (20.20)-(20.21):
    after excluding the exact-minimizer branch, the printed RHS is positive
    exactly when `[A phi(I-r r^+)]` has full row rank. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_formulaMatrixRowRank_eq_card_of_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 := by
  exact
    (lsNormwiseBackwardErrorFormulaRHS_pos_iff_sigmaMin_pos_of_not_isLeastSquaresMinimizer
      htheta A b hy hnot).trans
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
        theta A (lsResidualHigham A b y) y)
/-- Global finite-positive-`theta` row-rank zero-branch decomposition for
    (20.20)-(20.21): when `y` is nonzero, the printed RHS is zero exactly when
    `y` is an exact least-squares minimizer or the source block
    `[A phi(I-r r^+)]` is row-rank deficient. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_isLeastSquaresMinimizer_or_formulaMatrixRowRank_ne_card_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      IsLeastSquaresMinimizer A b y ∨
        lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
          (lsResidualHigham A b y) y ≠ m + 1 := by
  constructor
  · intro hrhs
    have hsplit :
        IsLeastSquaresMinimizer A b y ∨
          lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
            (lsResidualHigham A b y) y = 0 :=
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_isLeastSquaresMinimizer_or_sigmaMin_eq_zero_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs
    rcases hsplit with hmin | hsigma
    · exact Or.inl hmin
    · exact Or.inr
        ((lsNormwiseBackwardErrorFormulaMatrixSigmaMin_eq_zero_iff_rowRank_ne_card
          theta A (lsResidualHigham A b y) y).mp hsigma)
  · intro h
    rcases h with hmin | hrank
    · exact
        lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
          theta A b y hmin
    · exact
        lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_formulaMatrixRowRank_ne_card
          theta A b y hrank
/-- Global finite-positive-`theta` row-rank positivity decomposition for
    (20.20)-(20.21): when `y` is nonzero, the printed RHS is positive exactly
    when `y` is not an exact least-squares minimizer and
    `[A phi(I-r r^+)]` has full row rank. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      ¬ IsLeastSquaresMinimizer A b y ∧
        lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
          (lsResidualHigham A b y) y = m + 1 := by
  constructor
  · intro hrhs
    have hsplit :
        ¬ IsLeastSquaresMinimizer A b y ∧
          0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
            (lsResidualHigham A b y) y :=
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_sigmaMin_pos_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs
    exact ⟨hsplit.1,
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
        theta A (lsResidualHigham A b y) y).mp hsplit.2⟩
  · intro h
    have hsigma :
        0 < lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y :=
      (lsNormwiseBackwardErrorFormulaMatrixSigmaMin_pos_iff_rowRank_eq_card
        theta A (lsResidualHigham A b y) y).mpr h.2
    exact
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_sigmaMin_pos_of_theta_pos_of_y_ne_zero
        htheta A b hy).mpr ⟨h.1, hsigma⟩
/-- Finite-positive-`theta` zero split for (20.20)-(20.21) in `eta_F`/rank
    form: when `y` is nonzero, the printed WKS right-hand side vanishes exactly
    when `eta_F(y)` vanishes or the source block `[A phi(I-r r^+)]` is row-rank
    deficient. -/
theorem lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_etaF_eq_zero_or_formulaMatrixRowRank_ne_card_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y = 0 ↔
      lsNormwiseBackwardErrorEtaF theta A b y = 0 ∨
        lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
          (lsResidualHigham A b y) y ≠ m + 1 := by
  constructor
  · intro hrhs
    have hsplit :
        IsLeastSquaresMinimizer A b y ∨
          lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
            (lsResidualHigham A b y) y ≠ m + 1 :=
      (lsNormwiseBackwardErrorFormulaRHS_eq_zero_iff_isLeastSquaresMinimizer_or_formulaMatrixRowRank_ne_card_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs
    rcases hsplit with hmin | hrank
    · exact Or.inl
        ((lsNormwiseBackwardErrorEtaF_eq_zero_iff_isLeastSquaresMinimizer_of_positive_theta
          htheta A b y).mpr hmin)
    · exact Or.inr hrank
  · intro h
    rcases h with heta | hrank
    · have hmin :
          IsLeastSquaresMinimizer A b y :=
        (lsNormwiseBackwardErrorEtaF_eq_zero_iff_isLeastSquaresMinimizer_of_positive_theta
          htheta A b y).mp heta
      exact
        lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_isLeastSquaresMinimizer
          theta A b y hmin
    · exact
        lsNormwiseBackwardErrorFormulaRHS_eq_zero_of_formulaMatrixRowRank_ne_card
          theta A b y hrank
/-- Finite-positive-`theta` positivity split for (20.20)-(20.21) in
    `eta_F`/rank form: when `y` is nonzero, the printed WKS right-hand side is
    positive exactly when `eta_F(y)` is positive and `[A phi(I-r r^+)]` has full
    row rank. -/
theorem lsNormwiseBackwardErrorFormulaRHS_pos_iff_etaF_pos_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    0 < lsNormwiseBackwardErrorFormulaRHS theta A b y ↔
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
        lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
          (lsResidualHigham A b y) y = m + 1 := by
  constructor
  · intro hrhs
    have hsplit :
        ¬ IsLeastSquaresMinimizer A b y ∧
          lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
            (lsResidualHigham A b y) y = m + 1 :=
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs
    exact ⟨
      (lsNormwiseBackwardErrorEtaF_pos_iff_not_isLeastSquaresMinimizer_of_positive_theta
        htheta A b y).mpr hsplit.1,
      hsplit.2⟩
  · intro h
    have hnot :
        ¬ IsLeastSquaresMinimizer A b y :=
      (lsNormwiseBackwardErrorEtaF_pos_iff_not_isLeastSquaresMinimizer_of_positive_theta
        htheta A b y).mp h.1
    exact
      (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
        htheta A b hy).mpr ⟨hnot, h.2⟩
/-- Positive-branch inequality-certificate form of the
    Walden--Karlson--Sun formula (20.21): for positive finite `theta`,
    nonzero `y`, and full row rank of the source block
    `[A phi(I-r r^+)]`, the final equality follows from the two inequality
    obligations `formulaRHS <= eta_F(y)` and `eta_F(y) <= formulaRHS`.

    This is the route surface for the remaining WKS proof split.  It replaces
    the stronger exact-attainment certificate with the upper inequality that
    the constructive side actually needs to prove. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_inequality_certificate
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hlower :
      ∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
          lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab)
    (hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hlower_eta :
      lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
        lsNormwiseBackwardErrorEtaF theta A b y :=
    lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_forall_feasible_cost_ge
      theta A b y hlower
  have heq :
      lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    le_antisymm hupper hlower_eta
  have hrhs_pos :
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
      htheta A b hy).mpr ⟨hnot, hrank⟩
  have heta_pos :
      0 < lsNormwiseBackwardErrorEtaF theta A b y := by
    rw [heq]
    exact hrhs_pos
  exact ⟨heq, heta_pos, hrhs_pos⟩
/-- Positive finite-`theta` lower-bound side of the WKS formula (20.21) with
    the source nonzero-residual condition derived from the branch assumption
    that `y` is not already an exact least-squares minimizer. -/
theorem lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_positive_theta_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
      lsNormwiseBackwardErrorEtaF theta A b y := by
  have hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0 := by
    intro hrsq_zero
    have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq_zero]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  exact
    lsNormwiseBackwardErrorFormulaRHS_le_etaF_of_finite_theta
      (le_of_lt htheta) A b hy hrsq
/-- Positive-branch upper-certificate form of the Walden--Karlson--Sun formula
    (20.21): for positive finite `theta`, nonzero `y`, a non-minimizer
    candidate, and full row rank of `[A phi(I-r r^+)]`, the previously proved
    finite lower-bound side leaves only the constructive upper inequality
    `eta_F(y) <= RHS` as an explicit certificate. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0 := by
    intro hrsq_zero
    have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq_zero]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  have hlower :
      ∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
          lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab := by
    intro DeltaA Deltab hfeas
    exact
      lsNormwiseBackwardErrorFormulaRHS_le_costF_of_feasible_finite_theta
        (le_of_lt htheta) A b hy DeltaA Deltab hfeas hrsq
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_inequality_certificate
      htheta A b hy hnot hrank hlower hupper
/-- Scalar-branch positive finite-`theta` WKS handoff.  If the printed outer
    minimum in (20.21) selects the scalar `phi` branch, the exact-fit
    perturbation witness gives the upper inequality; the finite-positive
    lower-bound side then gives equality and positivity.  The source-block
    `sigma_min` branch, eigenvalue branch formula, and `theta = infinity` case
    remain separate obligations. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_phi_le_sigmaMin
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hbranch :
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_phi_le_sigmaMin
      (le_of_lt htheta) A b hy hbranch
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
      htheta A b hy hnot hrank hupper
/-- Source-block positive finite-`theta` WKS handoff using the optimized
    rank-two feasible witness.  If the printed outer minimum selects the
    row-side `sigma_min` branch, the local sigma-min attainer and rank-two
    construction give the upper inequality; the finite-positive lower-bound
    side then gives equality and positivity. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_sigmaMin_le_phi_rankTwo
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0 := by
    intro hrsq_zero
    have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq_zero]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  have hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_sigmaMin_le_phi_rankTwo
      (le_of_lt htheta) A b hy hrsq hbranch
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
      htheta A b hy hnot hrank hupper
/-- Finite-positive full-row-rank WKS equality.  For positive finite `theta`,
    nonzero non-minimizer `y`, and full row rank of the source block
    `[A phi(I-r r^+)]`, the printed minimum in (20.21) is closed by splitting
    into the scalar `phi` branch and the optimized rank-two `sigma_min` branch.

    This still leaves the eigenvalue reformulation of the branch value and the
    `theta = infinity` limiting case as separate selected rows. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  let sigma : ℝ :=
    lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
      (lsResidualHigham A b y) y
  let phi : ℝ :=
    lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y
  rcases le_total sigma phi with hsigma | hphi
  · exact
      lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_sigmaMin_le_phi_rankTwo
        htheta A b hy hnot hrank (by simpa [sigma, phi] using hsigma)
  · exact
      lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_phi_le_sigmaMin
        htheta A b hy hnot hrank (by simpa [sigma, phi] using hphi)
/-- Source-generality finite-positive WKS equality.  The full row rank of the
    formula matrix is a consequence of positive `theta`, nonzero `y`, and
    failure of the normal equations, rather than an extra assumption. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
      htheta A b hy hnot
      (lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_positive_theta_not_isLeastSquaresMinimizer
        htheta A b hy hnot)
/-- Source-left-panel version of the finite-positive WKS equality.  Full row
    rank of the original data matrix `A` supplies the full-row-rank hypothesis
    for the source block `[A phi(I-r r^+)]`. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hA : lsRealRectRowRank A = m + 1) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
      htheta A b hy hnot hrank
/-- Transfer form for the finite-positive WKS theorem: once the remaining
    spectral equivalence between the printed SVD/minimum RHS (20.21) and the
    Theorem 20.5 eigenvalue RHS is supplied, the already proved finite-positive
    equality immediately yields the eigenvalue formulation of `eta_F(y)`. -/
theorem lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_formulaRHS_eq_eigenvalueFormulaRHS_of_formulaMatrixRowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hspectral :
      lsNormwiseBackwardErrorFormulaRHS theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  have hclosed :=
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
      htheta A b hy hnot hrank
  exact ⟨hclosed.1.trans hspectral, hclosed.2.1, by
    rw [← hspectral]
    exact hclosed.2.2⟩
/-- Source-left-panel transfer form for the finite-positive WKS theorem.  A
    later proof of the spectral equivalence between the (20.21) SVD/minimum RHS
    and the Theorem 20.5 eigenvalue RHS can reuse the closed left-panel
    row-rank equality without reopening the perturbation construction. -/
theorem lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_formulaRHS_eq_eigenvalueFormulaRHS_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hA : lsRealRectRowRank A = m + 1)
    (hspectral :
      lsNormwiseBackwardErrorFormulaRHS theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  have hclosed :=
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_left_panel_rowRank_eq_card
      htheta A b hy hnot hA
  exact ⟨hclosed.1.trans hspectral, hclosed.2.1, by
    rw [← hspectral]
    exact hclosed.2.2⟩
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 with (20.21):
    finite-positive WKS equality in the eigenvalue formulation, under the
    source-block full-row-rank hypothesis already used for the minimum formula.
    The nonzero-residual condition required by the spectral bridge follows
    from the non-minimizer hypothesis. -/
theorem lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  have hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0 := by
    intro hrsq_zero
    have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq_zero]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  exact
    lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_formulaRHS_eq_eigenvalueFormulaRHS_of_formulaMatrixRowRank_eq_card
      htheta A b hy hnot hrank
      (lsNormwiseBackwardErrorFormulaRHS_eq_eigenvalueFormulaRHS_of_residual_sq_ne_zero
        theta A b hy hrsq)
/-- Source-generality finite-positive WKS equality in the eigenvalue form.
    The formula-matrix row-rank condition is discharged from the printed
    assumptions instead of being imposed on `A` or on the block matrix. -/
theorem lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  exact
    lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
      htheta A b hy hnot
      (lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_positive_theta_not_isLeastSquaresMinimizer
        htheta A b hy hnot)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 at its printed finite source
    assumptions.  Exact minimizers use the zero-error branch (including the
    zero-residual Gram-matrix argument above); non-minimizers use the derived
    WKS formula-matrix row rank. -/
theorem theorem20_5_wks_finite_formula_and_eigenvalue
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  by_cases hmin : IsLeastSquaresMinimizer A b y
  · have hformula :=
      lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_isLeastSquaresMinimizer
        theta A b y hmin
    refine ⟨hformula, ?_⟩
    by_cases hrsq : vecNorm2Sq (lsResidualHigham A b y) = 0
    · have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
        simp [vecNorm2, hrsq]
      have hres : lsResidualHigham A b y = 0 := by
        ext i
        exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
      rw [lsNormwiseBackwardErrorEtaF_eq_zero_of_isLeastSquaresMinimizer
        theta A b y hmin]
      exact
        (lsNormwiseBackwardErrorEigenvalueFormulaRHS_eq_zero_of_residual_eq_zero
          theta A b y hres).symm
    · exact hformula.trans
        (lsNormwiseBackwardErrorFormulaRHS_eq_eigenvalueFormulaRHS_of_residual_sq_ne_zero
          theta A b hy hrsq)
  · exact
      ⟨(lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer
          htheta A b hy hmin).1,
        (lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer
          htheta A b hy hmin).1⟩
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 with (20.21):
    source-left-panel finite-positive WKS equality in the eigenvalue
    formulation.  Full row rank of `A` supplies the source-block rank
    hypothesis, and the new spectral bridge supplies the eigenvalue RHS. -/
theorem lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hA : lsRealRectRowRank A = m + 1) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorEtaF_eq_eigenvalueFormulaRHS_and_pos_of_positive_theta_not_isLeastSquaresMinimizer_of_formulaMatrixRowRank_eq_card
      htheta A b hy hnot hrank
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 limiting discussion:
    under the printed nonzero-candidate hypothesis, the
    eigenvalue right-hand side has the same `theta -> +∞` limit as the
    finite-weight backward-error model.  This version records the limit as the
    supremum of nonnegative finite-weight values; the following theorem rewrites
    it to the matrix-only infimum using the compactness equality.  The legacy
    suffix is retained for lookup compatibility; no row-rank hypothesis remains. -/
theorem lsNormwiseBackwardErrorEigenvalueFormulaRHS_tendsto_nonneg_iSup_atTop_of_left_panel_rowRank_eq_card
    {m n : ℕ} (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0) :
    Filter.Tendsto
      (fun theta : ℝ =>
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y)
      Filter.atTop
      (nhds (⨆ theta : {theta : ℝ // 0 ≤ theta},
        lsNormwiseBackwardErrorEtaF theta.1 A b y)) := by
  exact Filter.Tendsto.congr'
    (f₁ := fun theta : ℝ => lsNormwiseBackwardErrorEtaF theta A b y)
    (f₂ := fun theta : ℝ =>
      lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y)
    (by
      filter_upwards [Filter.eventually_gt_atTop (0 : ℝ)] with theta htheta
      exact
        (theorem20_5_wks_finite_formula_and_eigenvalue
          htheta A b hy).2)
    (lsNormwiseBackwardErrorEtaF_tendsto_nonneg_iSup_atTop A b y)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 limiting discussion:
    under the printed nonzero-candidate hypothesis, the printed eigenvalue
    right-hand side tends to the matrix-only `Delta b = 0` infimum as
    `theta -> +∞`.  The legacy suffix is retained for lookup compatibility. -/
theorem lsNormwiseBackwardErrorEigenvalueFormulaRHS_tendsto_matrixOnlyEtaF_atTop_of_left_panel_rowRank_eq_card
    {m n : ℕ} (A : Fin (m + 1) → Fin n → ℝ)
    (b : Fin (m + 1) → ℝ) {y : Fin n → ℝ} (hy : y ≠ 0) :
    Filter.Tendsto
      (fun theta : ℝ =>
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y)
      Filter.atTop
      (nhds (lsNormwiseBackwardErrorMatrixOnlyEtaF A b y)) := by
  rw [lsNormwiseBackwardErrorMatrixOnlyEtaF_eq_nonneg_iSup A b y]
  exact
    lsNormwiseBackwardErrorEigenvalueFormulaRHS_tendsto_nonneg_iSup_atTop_of_left_panel_rowRank_eq_card
      A b hy
/-- Higham, 2nd ed., Chapter 20, Theorem 20.5 source-facing WKS package:
    under positive finite `theta` and the printed nonzero-candidate hypothesis,
    the infimum model equals both displayed finite right-hand sides, and the
    eigenvalue right-hand side tends to the matrix-only `Delta b = 0` model as
    `theta -> +∞`.  The legacy suffix is retained for lookup compatibility;
    neither a non-minimizer nor a row-rank hypothesis remains. -/
theorem theorem20_5_wks_formula_eigenvalue_and_matrixOnly_limit_of_left_panel_rowRank_eq_card
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    (lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y) ∧
    (lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y) ∧
    Filter.Tendsto
      (fun theta : ℝ =>
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y)
      Filter.atTop
      (nhds (lsNormwiseBackwardErrorMatrixOnlyEtaF A b y)) := by
  have hfinite :=
    theorem20_5_wks_finite_formula_and_eigenvalue htheta A b hy
  exact
    ⟨hfinite.1, hfinite.2,
      lsNormwiseBackwardErrorEigenvalueFormulaRHS_tendsto_matrixOnlyEtaF_atTop_of_left_panel_rowRank_eq_card
        A b hy⟩
/-- Clean source-facing name for the complete Theorem 20.5 package. -/
theorem theorem20_5_wks_formula_eigenvalue_and_matrixOnly_limit
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0) :
    (lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y) ∧
    (lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y) ∧
    Filter.Tendsto
      (fun theta : ℝ =>
        lsNormwiseBackwardErrorEigenvalueFormulaRHS theta A b y)
      Filter.atTop
      (nhds (lsNormwiseBackwardErrorMatrixOnlyEtaF A b y)) := by
  exact
    theorem20_5_wks_formula_eigenvalue_and_matrixOnly_limit_of_left_panel_rowRank_eq_card
      htheta A b hy
/-- Positive finite-`theta` WKS branch from the concrete rank-one
    source-block certificate.  This replaces the generic upper-inequality
    hypothesis by the explicit expanded rank-one witness cost bound against
    the source-block `sigma_min`; proving that bound from an actual
    singular-vector construction remains the open WKS dependency. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_rankOne_expanded_cost_le_sigmaMin
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hcost :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_expanded_cost_le_sigmaMin
      theta A b y p hp hbranch hcost
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
      htheta A b hy hnot hrank hupper
/-- Positive finite-`theta` WKS branch from the scaled concrete rank-one
    source-block certificate.  This keeps the scalar degree of freedom in the
    expanded residual witness visible while using the already proved
    finite-positive lower-bound side. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_rankOne_scaled_cost_le_sigmaMin
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (p : Fin (m + 1) → ℝ) (hp : vecNorm2Sq p ≠ 0)
    (c : ℝ) (hc : c ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hcost :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      let u : Fin n → ℝ := fun j => ∑ i : Fin (m + 1), A i j * p i
      Real.sqrt (vecNorm2Sq u / vecNorm2Sq p +
        theta ^ 2 *
          vecNorm2Sq
            (fun i : Fin (m + 1) =>
              c * p i - r i -
                ((1 / vecNorm2Sq p) * p i *
                  (∑ j : Fin n, u j * y j)))) ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_cost_le_sigmaMin
      theta A b y p hp c hc hbranch hcost
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
      htheta A b hy hnot hrank hupper
/-- Degenerate source-projector positive-branch WKS handoff with source-shaped
    nonzero scale.  In the finite-positive branch, if a source-block candidate
    `p` is parallel to the residual direction, has `p^T b != 0`, and satisfies
    the source-transpose certificate, then the degenerate upper-bound theorem
    combines with the already proved lower-bound side to give equality and
    positivity in (20.21).  The nondegenerate source-block construction,
    eigenvalue branch formula, and `theta = infinity` case remain open. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_rankOne_scaled_source_transpose_projected_eq_zero_of_dot_b_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (p : Fin (m + 1) → ℝ) (hp : p ≠ 0)
    (hdotb : (∑ i : Fin (m + 1), p i * b i) ≠ 0)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hprojected :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq (matMulVec (m + 1) (lsResidualComplementProjector r) p) = 0)
    (hsource :
      let r : Fin (m + 1) → ℝ := lsResidualHigham A b y
      vecNorm2Sq
          (rectMatMulVec
            (finiteTranspose (lsNormwiseBackwardErrorFormulaMatrix theta A r y)) p) ≤
        (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A r y) ^ 2 *
          vecNorm2Sq p) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0 := by
    intro hrsq_zero
    have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq_zero]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  have hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_rankOne_scaled_source_transpose_projected_eq_zero_of_dot_b_ne_zero
      theta A b y p hp hrsq hdotb hbranch hprojected hsource
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
      htheta A b hy hnot hrank hupper
/-- Source-block-attainer version of the degenerate projected-residual WKS
    equality branch.  The row-side sigma-min attainer for
    `[A phi(I-r r^+)]` supplies the source-transpose certificate internally;
    the visible side conditions are now the degenerate projected component and
    nonzero source dot product for exact attainers.

    This is still a degenerate subcase of the (20.21) positive branch.  It does
    not solve the nondegenerate obstruction, the eigenvalue branch formula, or
    the `theta = infinity` limiting case. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hbranch :
      lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y)
    (hprojected :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y)) p) = 0)
    (hdotb :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        (∑ i : Fin (m + 1), p i * b i) ≠ 0) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrsq : vecNorm2Sq (lsResidualHigham A b y) ≠ 0 := by
    intro hrsq_zero
    have hnorm : vecNorm2 (lsResidualHigham A b y) = 0 := by
      simp [vecNorm2, hrsq_zero]
    have hres : lsResidualHigham A b y = 0 := by
      ext i
      exact (vecNorm2_eq_zero_iff (lsResidualHigham A b y)).mp hnorm i
    exact hnot (IsLeastSquaresMinimizer.of_lsResidualHigham_eq_zero hres)
  have hupper :
      lsNormwiseBackwardErrorEtaF theta A b y ≤
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_le_formulaRHS_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
      theta A b y hrsq hbranch hprojected hdotb
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_upper_certificate
      htheta A b hy hnot hrank hupper
/-- Nondegenerate branch-eliminated WKS attainer handoff.  If the source block
    `[A phi(I-r r^+)]` has full row rank, then the zero-RHS branch is already
    closed; on the positive branch, either the scalar `phi` side of the printed
    minimum applies directly or the row-side `sigma_min` side is closed by the
    degenerate source-block attainer hypotheses.

    This removes the visible `phi`/`sigma_min` branch-selection hypothesis from
    the degenerate attainer route.  It still does not prove the genuinely
    nondegenerate WKS construction, because the projected-attainer and source
    dot-product conditions remain explicit. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaMatrixRowRank_eq_card_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hprojected :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y)) p) = 0)
    (hdotb :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        (∑ i : Fin (m + 1), p i * b i) ≠ 0) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  by_cases hrhs_zero : lsNormwiseBackwardErrorFormulaRHS theta A b y = 0
  · exact
      lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaRHS_eq_zero_of_formulaMatrixRowRank_eq_card
        htheta A b hy hrhs_zero hrank
  · have hrhs_nonneg :
        0 ≤ lsNormwiseBackwardErrorFormulaRHS theta A b y :=
      lsNormwiseBackwardErrorFormulaRHS_nonneg theta A b y
    have hrhs_pos :
        0 < lsNormwiseBackwardErrorFormulaRHS theta A b y :=
      lt_of_le_of_ne hrhs_nonneg (Ne.symm hrhs_zero)
    have hnot :
        ¬ IsLeastSquaresMinimizer A b y :=
      ((lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs_pos).1
    by_cases hphi_branch :
      lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y ≤
        lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
          (lsResidualHigham A b y) y
    · exact
        (lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_phi_le_sigmaMin
          htheta A b hy hnot hrank hphi_branch).1
    · have hsigma_branch :
          lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y ≤
            lsNormwiseBackwardErrorPhi theta (lsResidualHigham A b y) y :=
        le_of_lt (not_le.mp hphi_branch)
      exact
        (lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
          htheta A b hy hnot hrank hsigma_branch hprojected hdotb).1
/-- Full-left-panel branch-eliminated WKS attainer handoff.  Full row rank of
    the original data matrix supplies full row rank of the WKS source block,
    so the branch-eliminated degenerate attainer theorem can be applied without
    exposing the source-block row-rank hypothesis. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_left_panel_rowRank_eq_card_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hA : lsRealRectRowRank A = m + 1)
    (hprojected :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        vecNorm2Sq
            (matMulVec (m + 1)
              (lsResidualComplementProjector (lsResidualHigham A b y)) p) = 0)
    (hdotb :
      ∀ p : Fin (m + 1) → ℝ, p ≠ 0 →
        vecNorm2Sq
            (rectMatMulVec
              (finiteTranspose
                (lsNormwiseBackwardErrorFormulaMatrix theta A
                  (lsResidualHigham A b y) y)) p) =
          (lsNormwiseBackwardErrorFormulaMatrixSigmaMin theta A
              (lsResidualHigham A b y) y) ^ 2 * vecNorm2Sq p →
        (∑ i : Fin (m + 1), p i * b i) ≠ 0) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaMatrixRowRank_eq_card_of_formulaMatrixSigmaMin_attainer_projected_eq_zero_of_dot_b_ne_zero
      htheta A b hy hrank hprojected hdotb
/-- Positive-branch certificate form of the Walden--Karlson--Sun formula
    (20.21): for positive finite `theta`, nonzero `y`, and full row rank of
    the source block `[A phi(I-r r^+)]`, a supplied WKS lower-bound certificate
    together with a supplied attaining perturbation proves the printed formula
    and also records that both sides are positive.

    The theorem deliberately leaves the two WKS proof obligations visible as
    hypotheses: all feasible perturbations are bounded below by the printed
    right-hand side, and one feasible perturbation attains it. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_formula_certificate
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hlower :
      ∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
          lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab)
    (hatt :
      ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
          lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
            lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have heq :
      lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formula_certificate
      theta A b y hlower hatt
  have hrhs_pos :
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y :=
    (lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
      htheta A b hy).mpr ⟨hnot, hrank⟩
  have heta_pos :
      0 < lsNormwiseBackwardErrorEtaF theta A b y := by
    rw [heq]
    exact hrhs_pos
  exact ⟨heq, heta_pos, hrhs_pos⟩
/-- Full-left-panel version of the positive-branch WKS certificate handoff:
    if the original data matrix `A` has full row rank, the WKS source block
    `[A phi(I-r r^+)]` has full row rank, so the same lower-bound and
    attainment certificates prove the positive equality in (20.21). -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_left_panel_positive_formula_certificate
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hA : lsRealRectRowRank A = m + 1)
    (hnot : ¬ IsLeastSquaresMinimizer A b y)
    (hlower :
      ∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
          lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab)
    (hatt :
      ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
        LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
          lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
            lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
        lsNormwiseBackwardErrorFormulaRHS theta A b y ∧
      0 < lsNormwiseBackwardErrorEtaF theta A b y ∧
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_formula_certificate
      htheta A b hy hnot hrank hlower hatt
/-- Nondegenerate branchwise certificate form of the Walden--Karlson--Sun
    formula (20.21): if the WKS source block `[A phi(I-r r^+)]` has full row
    rank, the zero-RHS branch is handled by the existing exact-minimizer route,
    and only the positive-RHS branch needs supplied lower-bound and attainment
    certificates. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaMatrixRowRank_eq_card_of_positive_formula_certificate
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1)
    (hcert :
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y →
        (∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
          LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
            lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
              lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ∧
        ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
          LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
              lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  by_cases hrhs_zero : lsNormwiseBackwardErrorFormulaRHS theta A b y = 0
  · exact
      lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaRHS_eq_zero_of_formulaMatrixRowRank_eq_card
        htheta A b hy hrhs_zero hrank
  · have hrhs_nonneg :
        0 ≤ lsNormwiseBackwardErrorFormulaRHS theta A b y :=
      lsNormwiseBackwardErrorFormulaRHS_nonneg theta A b y
    have hrhs_pos :
        0 < lsNormwiseBackwardErrorFormulaRHS theta A b y :=
      lt_of_le_of_ne hrhs_nonneg (Ne.symm hrhs_zero)
    have hnot :
        ¬ IsLeastSquaresMinimizer A b y :=
      ((lsNormwiseBackwardErrorFormulaRHS_pos_iff_not_isLeastSquaresMinimizer_and_formulaMatrixRowRank_eq_card_of_theta_pos_of_y_ne_zero
        htheta A b hy).mp hrhs_pos).1
    rcases hcert hrhs_pos with ⟨hlower, hatt⟩
    exact
      (lsNormwiseBackwardErrorEtaF_eq_formulaRHS_and_pos_of_positive_formula_certificate
        htheta A b hy hnot hrank hlower hatt).1
/-- Full-left-panel branchwise certificate form of the Walden--Karlson--Sun
    formula (20.21): full row rank of the original `A` panel supplies full row
    rank of the WKS source block, so only the positive-RHS branch keeps the
    spectral lower-bound and attainment certificates visible. -/
theorem lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_left_panel_rowRank_eq_card_of_positive_formula_certificate
    {m n : ℕ} {theta : ℝ} (htheta : 0 < theta)
    (A : Fin (m + 1) → Fin n → ℝ) (b : Fin (m + 1) → ℝ)
    {y : Fin n → ℝ} (hy : y ≠ 0)
    (hA : lsRealRectRowRank A = m + 1)
    (hcert :
      0 < lsNormwiseBackwardErrorFormulaRHS theta A b y →
        (∀ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
          LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab →
            lsNormwiseBackwardErrorFormulaRHS theta A b y ≤
              lsNormwiseBackwardErrorCostF theta DeltaA Deltab) ∧
        ∃ (DeltaA : Fin (m + 1) → Fin n → ℝ) (Deltab : Fin (m + 1) → ℝ),
          LSNormwiseBackwardErrorFeasible A b y DeltaA Deltab ∧
            lsNormwiseBackwardErrorCostF theta DeltaA Deltab =
              lsNormwiseBackwardErrorFormulaRHS theta A b y) :
    lsNormwiseBackwardErrorEtaF theta A b y =
      lsNormwiseBackwardErrorFormulaRHS theta A b y := by
  have hrank :
      lsNormwiseBackwardErrorFormulaMatrixRowRank theta A
        (lsResidualHigham A b y) y = m + 1 :=
    lsNormwiseBackwardErrorFormulaMatrixRowRank_eq_card_of_left_panel_rowRank_eq_card
      theta A (lsResidualHigham A b y) y hA
  exact
    lsNormwiseBackwardErrorEtaF_eq_formulaRHS_of_formulaMatrixRowRank_eq_card_of_positive_formula_certificate
      htheta A b hy hrank hcert
/-- Any zero-right-hand-side augmented least-squares system gives an exact
    least-squares minimizer, even if the residual vector is supplied abstractly. -/
theorem LSAugmentedSystem.isLeastSquaresMinimizer_of_zero_rhs {m n : ℕ}
    (A : Fin m → Fin n → ℝ) (b r : Fin m → ℝ) (x : Fin n → ℝ)
    (h : LSAugmentedSystem A b (0 : Fin n → ℝ) r x) :
    IsLeastSquaresMinimizer A b x := by
  have hr : r = lsResidualHigham A b x := by
    ext i
    have htop := h.1 i
    unfold lsResidualHigham
    linarith
  have haug : LSAugmentedSystem A b (0 : Fin n → ℝ) (lsResidualHigham A b x) x := by
    simpa [← hr] using h
  exact (RectLSNormalEquations.iff_isLeastSquaresMinimizer A b x).mp
    ((LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs A b x).mp haug)
/-- Least-squares-minimizer form of the constructive part of Lemma 20.6. -/
theorem LSAsymmetricPerturbedAugmentedSystem.exists_isLeastSquaresMinimizer
    {m n : ℕ} (A DeltaA1 DeltaA2 : Fin m → Fin n → ℝ)
    (b s : Fin m → ℝ) (y : Fin n → ℝ)
    (h : LSAsymmetricPerturbedAugmentedSystem A DeltaA1 DeltaA2 b s y) :
    ∃ DeltaA : Fin m → Fin n → ℝ,
      IsLeastSquaresMinimizer (fun i j => A i j + DeltaA i j) b y := by
  rcases LSAsymmetricPerturbedAugmentedSystem.exists_augmentedSystem
      A DeltaA1 DeltaA2 b s y h with ⟨DeltaA, sbar, haug⟩
  exact ⟨DeltaA,
    LSAugmentedSystem.isLeastSquaresMinimizer_of_zero_rhs
      (fun i j => A i j + DeltaA i j) b sbar y haug⟩
/-- Exact minimizers of the perturbed least-squares problem satisfy Higham's
    perturbed augmented system (20.4). -/
theorem LSPerturbedAugmentedSystem.of_isLeastSquaresMinimizer {m n : ℕ}
    (A DeltaA : Fin m → Fin n → ℝ) (b Deltab : Fin m → ℝ)
    (y : Fin n → ℝ)
    (hmin : IsLeastSquaresMinimizer (fun i j => A i j + DeltaA i j)
      (fun i => b i + Deltab i) y) :
    LSPerturbedAugmentedSystem A DeltaA b Deltab
      (lsResidualHigham (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y) y := by
  exact
    (LSPerturbedAugmentedSystem.iff_rectLSNormalEquations
      A DeltaA b Deltab y).mpr
      (IsLeastSquaresMinimizer.rectLSNormalEquations hmin)
/-- Higham, 2nd ed., Chapter 20, Theorem 20.2, source-facing least-squares
    minimizer form.  The residuals are the printed `r = b - A*x` and
    `s = b + Delta b - (A + Delta A)*y`; normal-equation orthogonality bridges
    exact minimizers to the augmented systems used in (20.3)-(20.4). -/
theorem theorem20_2_relative_bounds_of_minimizers_full_column_rank
    {m n : ℕ}
    (νx : CVec n → ℝ) (hνx : IsComplexVectorNorm νx)
    (habsx : IsAbsoluteComplexVectorNorm νx)
    (νr : CVec m → ℝ) (hνr : IsComplexVectorNorm νr)
    (habsr : IsAbsoluteComplexVectorNorm νr)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (hApert :
      Function.Injective (rectMatMulVec (fun i j => A i j + DeltaA i j)))
    (hExactMin : IsLeastSquaresMinimizer A b x)
    (hPertMin :
      IsLeastSquaresMinimizer (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y)
    (heps : 0 ≤ eps) (hE : ∀ i j, 0 ≤ E i j) (hf : ∀ i, 0 ≤ f i)
    (hxnorm_pos : 0 < νx (realVecToComplex x))
    (hrnorm_pos : 0 < νr (realVecToComplex (lsResidualHigham A b x)))
    (hDeltaA : ∀ i j, |DeltaA i j| ≤ eps * E i j)
    (hDeltab : ∀ i, |Deltab i| ≤ eps * f i) :
    (νx (realVecToComplex (fun j => x j - y j)) / νx (realVecToComplex x) ≤
      (eps *
        (νx (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νx (realVecToComplex
            (lsAugmentedEq20_8RightMajorant (lsGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E
                (lsResidualHigham (fun i j => A i j + DeltaA i j)
                  (fun i => b i + Deltab i) y)))))) /
        νx (realVecToComplex x)) ∧
    (νr (realVecToComplex
        (fun i =>
          lsResidualHigham A b x i -
            lsResidualHigham (fun i j => A i j + DeltaA i j)
              (fun i => b i + Deltab i) y i)) /
        νr (realVecToComplex (lsResidualHigham A b x)) ≤
      (eps *
        (νr (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νr (realVecToComplex
            (lsAugmentedEq20_7RightMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E
                (lsResidualHigham (fun i j => A i j + DeltaA i j)
                  (fun i => b i + Deltab i) y)))))) /
        νr (realVecToComplex (lsResidualHigham A b x))) := by
  have hExactAug :
      LSAugmentedSystem A b (0 : Fin n → ℝ)
        (lsResidualHigham A b x) x :=
    (LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs A b x).mpr
      (IsLeastSquaresMinimizer.rectLSNormalEquations hExactMin)
  have hPertAug :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ)
        (lsResidualHigham (fun i j => A i j + DeltaA i j)
          (fun i => b i + Deltab i) y) y :=
    (LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs
      (fun i j => A i j + DeltaA i j) (fun i => b i + Deltab i) y).mpr
      (IsLeastSquaresMinimizer.rectLSNormalEquations hPertMin)
  exact
    lsAugmentedTheorem20_2_relative_bounds_full_column_rank
      νx hνx habsx νr hνr habsr A DeltaA E Deltab f b
      (lsResidualHigham A b x)
      (lsResidualHigham (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y)
      x y eps hA hApert hExactAug hPertAug heps hE hf hxnorm_pos
      hrnorm_pos hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, Theorem 20.2 with equation (20.5) packaged
    as the source componentwise perturbation predicate. -/
theorem theorem20_2_relative_bounds_of_minimizers_full_column_rank_of_componentwise_perturbation
    {m n : ℕ}
    (νx : CVec n → ℝ) (hνx : IsComplexVectorNorm νx)
    (habsx : IsAbsoluteComplexVectorNorm νx)
    (νr : CVec m → ℝ) (hνr : IsComplexVectorNorm νr)
    (habsr : IsAbsoluteComplexVectorNorm νr)
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (hApert :
      Function.Injective (rectMatMulVec (fun i j => A i j + DeltaA i j)))
    (hExactMin : IsLeastSquaresMinimizer A b x)
    (hPertMin :
      IsLeastSquaresMinimizer (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y)
    (hcomp : LSComponentwisePerturbation DeltaA E Deltab f eps)
    (hxnorm_pos : 0 < νx (realVecToComplex x))
    (hrnorm_pos : 0 < νr (realVecToComplex (lsResidualHigham A b x))) :
    (νx (realVecToComplex (fun j => x j - y j)) / νx (realVecToComplex x) ≤
      (eps *
        (νx (realVecToComplex
            (lsAugmentedEq20_8LeftMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νx (realVecToComplex
            (lsAugmentedEq20_8RightMajorant (lsGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E
                (lsResidualHigham (fun i j => A i j + DeltaA i j)
                  (fun i => b i + Deltab i) y)))))) /
        νx (realVecToComplex x)) ∧
    (νr (realVecToComplex
        (fun i =>
          lsResidualHigham A b x i -
            lsResidualHigham (fun i j => A i j + DeltaA i j)
              (fun i => b i + Deltab i) y i)) /
        νr (realVecToComplex (lsResidualHigham A b x)) ≤
      (eps *
        (νr (realVecToComplex
            (lsAugmentedEq20_7LeftMajorant A (lsAplusOfGramNonsingInv A)
              (lsComponentwiseDataMajorant E f y))) +
          νr (realVecToComplex
            (lsAugmentedEq20_7RightMajorant (lsAplusOfGramNonsingInv A)
              (lsComponentwiseTransposeMajorant E
                (lsResidualHigham (fun i j => A i j + DeltaA i j)
                  (fun i => b i + Deltab i) y)))))) /
        νr (realVecToComplex (lsResidualHigham A b x))) := by
  rcases hcomp with ⟨heps, hE, hf, hDeltaA, hDeltab⟩
  exact
    theorem20_2_relative_bounds_of_minimizers_full_column_rank
      νx hνx habsx νr hνr habsr A DeltaA E Deltab f b x y eps
      hA hApert hExactMin hPertMin heps hE hf hxnorm_pos hrnorm_pos
      hDeltaA hDeltab
/-- Higham, 2nd ed., Chapter 20, equations (20.5), (20.7), and (20.8):
    source-facing least-squares minimizer form of the componentwise pointwise
    residual and solution perturbation bounds.

    The residuals are the printed `r = b - A*x` and
    `s = b + Delta b - (A + Delta A)*y`; normal-equation orthogonality bridges
    exact minimizers to the augmented systems used in (20.3)-(20.4). -/
theorem theorem20_2_eq20_7_8_abs_le_of_minimizers_full_column_rank_of_componentwise_perturbation
    {m n : ℕ}
    (A : Fin m → Fin n → ℝ)
    (DeltaA E : Fin m → Fin n → ℝ) (Deltab f b : Fin m → ℝ)
    (x y : Fin n → ℝ) (eps : ℝ)
    (hA : Function.Injective (rectMatMulVec A))
    (_hApert :
      Function.Injective (rectMatMulVec (fun i j => A i j + DeltaA i j)))
    (hExactMin : IsLeastSquaresMinimizer A b x)
    (hPertMin :
      IsLeastSquaresMinimizer (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y)
    (hcomp : LSComponentwisePerturbation DeltaA E Deltab f eps) :
    (∀ i : Fin m,
      |lsResidualHigham (fun i j => A i j + DeltaA i j)
          (fun i => b i + Deltab i) y i - lsResidualHigham A b x i| ≤
        eps * lsAugmentedEq20_7Majorant A (lsAplusOfGramNonsingInv A)
          (lsComponentwiseDataMajorant E f y)
          (lsComponentwiseTransposeMajorant E
            (lsResidualHigham (fun i j => A i j + DeltaA i j)
              (fun i => b i + Deltab i) y)) i) ∧
    (∀ j : Fin n,
      |y j - x j| ≤
        eps * lsAugmentedEq20_8Majorant (lsAplusOfGramNonsingInv A)
          (lsGramNonsingInv A)
          (lsComponentwiseDataMajorant E f y)
          (lsComponentwiseTransposeMajorant E
            (lsResidualHigham (fun i j => A i j + DeltaA i j)
              (fun i => b i + Deltab i) y)) j) := by
  have hExactAug :
      LSAugmentedSystem A b (0 : Fin n → ℝ)
        (lsResidualHigham A b x) x :=
    (LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs A b x).mpr
      (IsLeastSquaresMinimizer.rectLSNormalEquations hExactMin)
  have hPertAug :
      LSAugmentedSystem (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) (0 : Fin n → ℝ)
        (lsResidualHigham (fun i j => A i j + DeltaA i j)
          (fun i => b i + Deltab i) y) y :=
    (LSAugmentedSystem.iff_rectLSNormalEquations_zero_rhs
      (fun i j => A i j + DeltaA i j) (fun i => b i + Deltab i) y).mpr
      (IsLeastSquaresMinimizer.rectLSNormalEquations hPertMin)
  exact
    lsAugmentedEq20_7_8_abs_le_of_perturbed_systems_full_column_rank_of_componentwise_perturbation
      A DeltaA E Deltab f b (lsResidualHigham A b x)
      (lsResidualHigham (fun i j => A i j + DeltaA i j)
        (fun i => b i + Deltab i) y)
      x y eps hA hExactAug hPertAug hcomp
/-- The first exact Householder QR counterexample state used to rule out
    deriving diagonal dominance from the unpivoted trailing Householder
    recurrence itself. -/
def exactHouseholderQRDiagDominanceCounterexampleA1 :
    Fin 2 → Fin 2 → ℝ :=
  fun i j =>
    if i.val = 0 ∧ j.val = 0 then -1
    else if i.val = 0 ∧ j.val = 1 then -2
    else if i.val = 1 ∧ j.val = 1 then 1
    else 0
/-- The final exact Householder QR counterexample state.  It is upper
    triangular with nonzero diagonal but it is not diagonally dominant. -/
def exactHouseholderQRDiagDominanceCounterexampleA2 :
    Fin 2 → Fin 2 → ℝ :=
  fun i j =>
    if i.val = 0 ∧ j.val = 0 then -1
    else if i.val = 0 ∧ j.val = 1 then -2
    else if i.val = 1 ∧ j.val = 1 then -1
    else 0
/-- Exact two-step unpivoted trailing Householder sequence for the diagonal
    dominance counterexample. -/
def exactHouseholderQRDiagDominanceCounterexampleSeq :
    ℕ → Fin 2 → Fin 2 → ℝ
  | 0 => diagDominanceCounterexample2
  | 1 => exactHouseholderQRDiagDominanceCounterexampleA1
  | _ => exactHouseholderQRDiagDominanceCounterexampleA2
/-- Source-style signed Householder scalars for the two-step counterexample. -/
def exactHouseholderQRDiagDominanceCounterexampleAlpha : ℕ → ℝ
  | 0 => -1
  | _ => -1
/-- Exact arithmetic model used to lift the diagonal-dominance counterexample
    to the stored-panel floating-point recurrence surface. -/
noncomputable def exactHouseholderQRDiagDominanceCounterexampleFP : FPModel :=
  FPModel.exactWithUnitRoundoff 0 (by norm_num)
/-- The counterexample sequence really follows the exact unpivoted trailing
    Householder recurrence. -/
theorem exactHouseholderQRDiagDominanceCounterexample_step :
    ∀ k (hk : k < 2),
      exactHouseholderQRDiagDominanceCounterexampleSeq (k + 1) =
        matMulRectLeft
          (householder 2
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i =>
                exactHouseholderQRDiagDominanceCounterexampleSeq k i ⟨k, hk⟩)
              (exactHouseholderQRDiagDominanceCounterexampleAlpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i =>
                  exactHouseholderQRDiagDominanceCounterexampleSeq k i ⟨k, hk⟩)
                (exactHouseholderQRDiagDominanceCounterexampleAlpha k))))
          (exactHouseholderQRDiagDominanceCounterexampleSeq k) := by
  intro k hk
  interval_cases k
  · ext i j
    fin_cases i <;> fin_cases j <;>
      norm_num [exactHouseholderQRDiagDominanceCounterexampleSeq,
        exactHouseholderQRDiagDominanceCounterexampleA1,
        diagDominanceCounterexample2,
        exactHouseholderQRDiagDominanceCounterexampleAlpha,
        householderTrailingActiveVector, householderActiveVector,
        householderTrailingPart, householderBetaSpec, householder,
        matMulRectLeft, idMatrix]
  · ext i j
    fin_cases i <;> fin_cases j <;>
      norm_num [exactHouseholderQRDiagDominanceCounterexampleSeq,
        exactHouseholderQRDiagDominanceCounterexampleA1,
        exactHouseholderQRDiagDominanceCounterexampleA2,
        exactHouseholderQRDiagDominanceCounterexampleAlpha,
        householderTrailingActiveVector, householderActiveVector,
        householderTrailingPart, householderBetaSpec, householder,
        matMulRectLeft, idMatrix]
/-- The counterexample scalars have the squared trailing-norm magnitude
    required by the exact Householder recurrence. -/
theorem exactHouseholderQRDiagDominanceCounterexample_alpha_sq :
    ∀ k (hk : k < 2),
      exactHouseholderQRDiagDominanceCounterexampleAlpha k *
          exactHouseholderQRDiagDominanceCounterexampleAlpha k =
        householderTrailingNorm2Sq 2
          ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
          (fun i =>
            exactHouseholderQRDiagDominanceCounterexampleSeq k i ⟨k, hk⟩) := by
  intro k hk
  interval_cases k
  · norm_num [exactHouseholderQRDiagDominanceCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleSeq,
      diagDominanceCounterexample2, householderTrailingNorm2Sq,
      vecNorm2Sq, householderTrailingPart]
    rfl
  · norm_num [exactHouseholderQRDiagDominanceCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleSeq,
      exactHouseholderQRDiagDominanceCounterexampleA1,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
    rfl
/-- The exact Householder denominators in the counterexample are nonzero. -/
theorem exactHouseholderQRDiagDominanceCounterexample_den_ne_zero :
    ∀ k (hk : k < 2),
      (∑ i : Fin 2,
        householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun a =>
              exactHouseholderQRDiagDominanceCounterexampleSeq k a ⟨k, hk⟩)
            (exactHouseholderQRDiagDominanceCounterexampleAlpha k) i *
          householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun a =>
              exactHouseholderQRDiagDominanceCounterexampleSeq k a ⟨k, hk⟩)
            (exactHouseholderQRDiagDominanceCounterexampleAlpha k) i) ≠ 0 := by
  intro k hk
  interval_cases k
  · norm_num [exactHouseholderQRDiagDominanceCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleSeq,
      diagDominanceCounterexample2, householderTrailingActiveVector,
      householderActiveVector, householderTrailingPart]
  · norm_num [exactHouseholderQRDiagDominanceCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleSeq,
      exactHouseholderQRDiagDominanceCounterexampleA1,
      householderTrailingActiveVector, householderActiveVector,
      householderTrailingPart]
/-- The same counterexample follows the stored-panel recurrence under exact
    arithmetic.  Completed columns are preserved and the newly completed
    pivot column is explicitly zeroed below the pivot, matching the existing
    upper-triangular witness. -/
theorem exactHouseholderQRDiagDominanceCounterexample_stored_step :
    ∀ k (hk : k < 2),
      exactHouseholderQRDiagDominanceCounterexampleSeq (k + 1) =
        fl_householderStoredPanelStep
          exactHouseholderQRDiagDominanceCounterexampleFP 2 2 k
          (householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i =>
              exactHouseholderQRDiagDominanceCounterexampleSeq k i ⟨k, hk⟩)
            (exactHouseholderQRDiagDominanceCounterexampleAlpha k))
          (householderBetaSpec 2
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i =>
                exactHouseholderQRDiagDominanceCounterexampleSeq k i ⟨k, hk⟩)
              (exactHouseholderQRDiagDominanceCounterexampleAlpha k)))
          (exactHouseholderQRDiagDominanceCounterexampleSeq k) := by
  intro k hk
  interval_cases k
  · ext i j
    fin_cases i <;> fin_cases j <;>
      simp [exactHouseholderQRDiagDominanceCounterexampleFP,
        FPModel.exactWithUnitRoundoff,
        exactHouseholderQRDiagDominanceCounterexampleSeq,
        exactHouseholderQRDiagDominanceCounterexampleA1,
        diagDominanceCounterexample2,
        exactHouseholderQRDiagDominanceCounterexampleAlpha,
        fl_householderStoredPanelStep, fl_householderApplyCompactPanel,
        fl_householderApplyCompact, fl_dotProduct, Fin.foldl_succ,
        householderTrailingActiveVector, householderActiveVector,
        householderTrailingPart, householderBetaSpec]
    all_goals ring_nf
  · ext i j
    fin_cases i <;> fin_cases j <;>
      simp [exactHouseholderQRDiagDominanceCounterexampleFP,
        FPModel.exactWithUnitRoundoff,
        exactHouseholderQRDiagDominanceCounterexampleSeq,
        exactHouseholderQRDiagDominanceCounterexampleA1,
        exactHouseholderQRDiagDominanceCounterexampleA2,
        exactHouseholderQRDiagDominanceCounterexampleAlpha,
        fl_householderStoredPanelStep, fl_householderApplyCompactPanel,
        fl_householderApplyCompact, fl_dotProduct, Fin.foldl_succ,
        householderTrailingActiveVector, householderActiveVector,
        householderTrailingPart, householderBetaSpec]
    all_goals ring_nf
/-- The counterexample scalars are exactly the signed Householder choices used
    by the stored-panel least-squares route. -/
theorem exactHouseholderQRDiagDominanceCounterexample_signed_alpha_def :
    ∀ k (hk : k < 2),
      exactHouseholderQRDiagDominanceCounterexampleAlpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i =>
                exactHouseholderQRDiagDominanceCounterexampleSeq k i ⟨k, hk⟩)))
          (exactHouseholderQRDiagDominanceCounterexampleSeq k
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩) := by
  intro k hk
  interval_cases k
  · norm_num [exactHouseholderQRDiagDominanceCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleSeq,
      diagDominanceCounterexample2, signedHouseholderAlpha,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
  · norm_num [exactHouseholderQRDiagDominanceCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleSeq,
      exactHouseholderQRDiagDominanceCounterexampleA1, signedHouseholderAlpha,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
/-- The actual exact unpivoted trailing Householder QR recurrence can output an
    upper triangular factor that is not diagonally dominant.

    This rules out the remaining tempting shortcut in the rectangular
    QR/preconditioner bottleneck: diagonal dominance cannot be recovered merely
    from using the exact no-pivot Householder loop.  A positive route must
    prove a separate off-diagonal-control hypothesis or keep diagonal
    dominance visible as a domain assumption. -/
theorem exactHouseholderQRDiagDominanceCounterexample_not_diagDominant :
    ¬ IsDiagDominantUpper 2
      (exactHouseholderQRDiagDominanceCounterexampleSeq 2) := by
  intro hDD
  have hbad := hDD.2.2 ⟨0, by norm_num⟩ ⟨1, by norm_num⟩ (by norm_num)
  norm_num [exactHouseholderQRDiagDominanceCounterexampleSeq,
    exactHouseholderQRDiagDominanceCounterexampleA2] at hbad
/-- Universal-form route elimination: exact unpivoted trailing Householder
    recurrence, valid Householder squared norms, and nonzero denominators do
    not imply the triangular diagonal-dominance hypothesis used by the current
    QR least-squares solve route. -/
theorem not_forall_exact_trailing_householder_sequence_implies_diagDominant :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          matMulRectLeft
            (householder 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun i => A_hat k i ⟨k, hk⟩) (alpha k))))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k * alpha k =
          householderTrailingNorm2Sq 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i => A_hat k i ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      IsDiagDominantUpper 2 (fun i j => A_hat 2 i j)) := by
  intro h
  exact exactHouseholderQRDiagDominanceCounterexample_not_diagDominant
    (h exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_step
      exactHouseholderQRDiagDominanceCounterexample_alpha_sq
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero)
/-- Universal-form route elimination on the stored-panel actual-unit surface:
    the stored recurrence, signed-alpha choice, nonzero source denominators,
    and a small declared unit roundoff do not imply the local diagonal
    dominance hypothesis used by the active QR/preconditioner branch. -/
theorem not_forall_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_implies_diagDominant :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      IsDiagDominantUpper 2 (fun i j => A_hat 2 i j)) := by
  intro h
  exact exactHouseholderQRDiagDominanceCounterexample_not_diagDominant
    (h exactHouseholderQRDiagDominanceCounterexampleFP
      exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_stored_step
      exactHouseholderQRDiagDominanceCounterexample_signed_alpha_def
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff]))
/-- At the initial diagonal-dominance counterexample state, the displayed first
    column is not the active max-pivot column: the second column has larger
    active trailing norm. -/
theorem exactHouseholderQRDiagDominanceCounterexample_not_activeMaxPivotChoice :
    ¬ ((⟨0, by norm_num⟩ : Fin 2) =
        householderActiveMaxPivotColumn
          (⟨0, by norm_num⟩ : Fin 2)
          (⟨0, by norm_num⟩ : Fin 2)
          (exactHouseholderQRDiagDominanceCounterexampleSeq 0)) := by
  intro hchoice
  have hmax :=
    householderActiveMaxPivotColumn_pivot_max
      (p := (⟨0, by norm_num⟩ : Fin 2))
      (k := (⟨0, by norm_num⟩ : Fin 2))
      (A := exactHouseholderQRDiagDominanceCounterexampleSeq 0)
      (l := (⟨1, by norm_num⟩ : Fin 2)) (by norm_num)
  rw [← hchoice] at hmax
  norm_num [exactHouseholderQRDiagDominanceCounterexampleSeq,
    diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
    householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq] at hmax
/-- Universal-form route elimination on the stored-panel actual-unit surface:
    the stored recurrence, signed-alpha choice, nonzero source denominators,
    and a small declared unit roundoff do not imply that the displayed pivot is
    the finite active max-pivot choice. -/
theorem not_forall_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_implies_activeMaxPivotChoice :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k))) := by
  intro h
  have hpivot :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_stored_step
      exactHouseholderQRDiagDominanceCounterexample_signed_alpha_def
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
  exact exactHouseholderQRDiagDominanceCounterexample_not_activeMaxPivotChoice
    (by
      simpa using hpivot 0 (by norm_num))
/-- Stronger universal-form route elimination for the finite-max route: the
    exact unpivoted trailing Householder recurrence cannot generically produce
    diagonal dominance together with any additional property `P` of the final
    triangular block.  In particular, a future proof cannot obtain the pair
    "local diagonal dominance plus scalar finite-max smallness" from this
    recurrence without adding a genuinely stronger invariant or visible domain
    assumptions. -/
theorem not_forall_exact_trailing_householder_sequence_implies_diagDominant_and_property
    (P : (Fin 2 → Fin 2 → ℝ) → Prop) :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          matMulRectLeft
            (householder 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun i => A_hat k i ⟨k, hk⟩) (alpha k))))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k * alpha k =
          householderTrailingNorm2Sq 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i => A_hat k i ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      IsDiagDominantUpper 2 (fun i j => A_hat 2 i j) ∧ P (fun i j => A_hat 2 i j)) := by
  intro h
  exact exactHouseholderQRDiagDominanceCounterexample_not_diagDominant
    (h exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_step
      exactHouseholderQRDiagDominanceCounterexample_alpha_sq
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero).1
/-- First stage for the active-pivot strengthening of the row-budget
    diagonal-lower-bound counterexample.  The first column has the largest
    active trailing norm, so the active max-pivot field holds at stage zero. -/
def activeMaxPivotRowBudgetDiagCounterexampleA0 :
    Fin 2 → Fin 2 → ℝ :=
  fun i j =>
    if i.val = 0 ∧ j.val = 0 then 2
    else if i.val = 0 ∧ j.val = 1 then 1
    else if i.val = 1 ∧ j.val = 1 then 1
    else 0
/-- Sequence used to show that active max-pivot information still does not
    imply the row-budget diagonal lower-bound field. -/
def activeMaxPivotRowBudgetDiagCounterexampleSeq :
    ℕ → Fin 2 → Fin 2 → ℝ
  | 0 => activeMaxPivotRowBudgetDiagCounterexampleA0
  | _ => diagDominanceCounterexample2
/-- Strengthened route elimination for the Cox--Higham row-budget split:
    upper-triangular nonsingular leading blocks, positive active-block mass,
    finite active max-pivot information, and an off-diagonal row-growth budget
    still do not imply the matching diagonal lower-bound field.

    This prevents the active-pivot route from silently deriving the residual
    `StoredQRDisplayedRowBudgetControl.diag_lower` field from pivot maximality
    plus row-growth bounds.  A positive theorem must prove a genuine diagonal
    lower-bound/off-diagonal-control invariant or keep that field visible. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_offdiagBudget_implies_rowBudget_diag :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (rowBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k) →
      (∀ k (hk : k < 2), ∀ i : Fin (k + 1), i.val < k →
        rowBudget k ≤
          |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i i|)) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let rowBudget : ℕ → ℝ := fun _ => 2
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hdiag := h A_hat rowBudget hupper hdetLead hactive hpivotMax hoffdiagBudget
  have hbad :=
    hdiag 1 (by norm_num) (⟨0, by norm_num⟩ : Fin (1 + 1)) (by norm_num)
  norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
    qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
    diagDominanceCounterexample2] at hbad
/-- Active-block budget strengthening of the row-budget diagonal
    route-elimination theorem.

    Even if the same budget bounds the active trailing block at each displayed
    stage, active max-pivoting and strict-upper row-growth bounds still do not
    imply the row-budget diagonal lower-bound field.  The witness uses the same
    two displayed stages as
    `not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_offdiagBudget_implies_rowBudget_diag`:
    stage zero satisfies the active max-pivot condition, while stage one has
    strict upper entry `2` above diagonal `1`. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_rowBudget_diag :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (rowBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ rowBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ rowBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k) →
      (∀ k (hk : k < 2), ∀ i : Fin (k + 1), i.val < k →
        rowBudget k ≤
          |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i i|)) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let rowBudget : ℕ → ℝ := fun _ => 2
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ rowBudget k := by
    intro k _hk
    norm_num [rowBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ rowBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, rowBudget,
            activeMaxPivotRowBudgetDiagCounterexampleSeq,
            diagDominanceCounterexample2]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hdiag := h A_hat rowBudget hupper hdetLead hactive hpivotMax
    hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  have hbad :=
    hdiag 1 (by norm_num) (⟨0, by norm_num⟩ : Fin (1 + 1)) (by norm_num)
  norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
    qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
    diagDominanceCounterexample2] at hbad
/-- Meta route elimination for the Cox--Higham row-budget split.

    Adding an arbitrary auxiliary side property that happens to hold of the
    displayed-stage sequence and row budget still cannot turn active-block
    budgets, active max-pivoting, and strict-upper row-growth bounds into the
    missing diagonal lower-bound field.  This is a guard against repeatedly
    trying to close the QR bottleneck by appending unrelated scalar/product
    hypotheses instead of proving a genuine diagonal lower-bound invariant. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_property_implies_rowBudget_diag :
    ¬ (∀
      (P : (ℕ → Fin 2 → Fin 2 → ℝ) → (ℕ → ℝ) → Prop)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (rowBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ rowBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ rowBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k) →
      P A_hat rowBudget →
      (∀ k (hk : k < 2), ∀ i : Fin (k + 1), i.val < k →
        rowBudget k ≤
          |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i i|)) := by
  intro h
  exact
    not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_rowBudget_diag
      (by
        intro A_hat rowBudget hupper hdetLead hactive hpivotMax
          hbudget_nonneg hactiveBlockBudget hoffdiagBudget
        exact h (fun _ _ => True) A_hat rowBudget hupper hdetLead hactive
          hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget trivial)
/-- Explicit row-budget control certificate for displayed QR leading blocks.

    This packages the two source-facing obligations left by the Cox--Higham
    row-growth route: a row budget controls every displayed strict upper entry,
    and that same row budget is no larger than the displayed diagonal on rows
    that actually have strict upper entries.  The final row of a `(k+1) ×
    (k+1)` leading block has no such strict upper entry, so no diagonal
    comparison is required there. -/
structure StoredQRDisplayedRowBudgetControl
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ) : Prop where
  offdiag_le : ∀ k (hk : k < n),
    ∀ i j : Fin (k + 1), i.val < j.val →
      |qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i j| ≤
      rowBudget k hk i
  diag_lower : ∀ k (hk : k < n),
    ∀ i : Fin (k + 1), i.val < k →
      rowBudget k hk i ≤
      |qrLeadingBlock (A_hat k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|
/-- Uniform row-max contraction gives the displayed row-budget certificate.

This is the source-facing shape of the stronger computed-loop invariant needed
by the rectangular QR/preconditioner route.  If every finite strict-upper row
maximum in a displayed leading block is at most a scalar `ρ ≤ 1` times the
corresponding displayed diagonal, then the canonical row-max budget supplies
both fields of `StoredQRDisplayedRowBudgetControl`. -/
theorem StoredQRDisplayedRowBudgetControl.of_rowMaxBudget_le_diag_factor
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    {ρ : ℝ} (hρ : ρ ≤ 1)
    (hrowMaxContract : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤
        ρ *
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k hk i => qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) := by
  refine ⟨?_, ?_⟩
  · intro k hk i j hij
    exact qrLeadingStrictUpperRowMaxBudget_entry_le hmn A_hat hk hij
  · intro k hk i hi
    have hdiag_nonneg :
        0 ≤
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| :=
      abs_nonneg _
    have hfactor :
        ρ *
            |qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| ≤
          1 *
            |qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| :=
      mul_le_mul_of_nonneg_right hρ hdiag_nonneg
    exact (hrowMaxContract k hk i hi).trans (by simpa using hfactor)
/-- A nonpositive finite row-max/diagonal defect budget supplies the displayed
    row-budget certificate.

This is the scalar-budget version of the route-1 contraction branch: instead of
passing a family of inequalities, the caller proves the single finite condition
`storedQRRowMaxDiagDefectBudget <= 0`. -/
theorem StoredQRDisplayedRowBudgetControl.of_rowMaxDiagDefectBudget_nonpos
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (hdefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k hk i => qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) := by
  refine
    StoredQRDisplayedRowBudgetControl.of_rowMaxBudget_le_diag_factor
      hmn A_hat (ρ := 1) (by norm_num) ?_
  intro k hk i hi
  have hle :=
    storedQRRowMaxDiagDefect_le_budget hmn A_hat hk i hi
  have hnonpos :
      qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i -
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| ≤ 0 :=
    hle.trans hdefect
  nlinarith
/-- Local diagonal dominance supplies the canonical row-max displayed
    row-budget certificate.

This is a small reuse wrapper around
`storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant` and
`StoredQRDisplayedRowBudgetControl.of_rowMaxDiagDefectBudget_nonpos`. -/
theorem StoredQRDisplayedRowBudgetControl.of_diagDominant
    {m n : ℕ} (hmn : n ≤ m)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k hk i => qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) :=
  StoredQRDisplayedRowBudgetControl.of_rowMaxDiagDefectBudget_nonpos
    hmn A_hat
    (storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant hmn A_hat hDD)
/-- Stage-budget/row-max comparison route elimination for the active-pivot
    Cox--Higham split.

    Even with upper triangular nonsingular displayed blocks, positive active
    mass, active max-pivoting, active-block budget control, and displayed
    strict-upper budget control, the uniform stage budget need not be bounded
    by the actual displayed row maximum.  The witness uses the same active-pivot
    sequence as the row-budget diagonal counterexample and chooses the harmless
    oversized budget `3`; at displayed stage one the strict-upper row maximum is
    `2`, so the comparison `stageBudget <= rowMax` fails. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageBudget_le_rowMax :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      (∀ k (hk : k < 2), ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2) A_hat k hk i)) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowBudgetDiagCounterexampleSeq,
            diagDominanceCounterexample2]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hcompare := h A_hat stageBudget hupper hdetLead hactive hpivotMax
    hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  have hbad :=
    hcompare 1 (by norm_num) (⟨0, by norm_num⟩ : Fin (1 + 1)) (by norm_num)
  norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
    qrLeadingStrictUpperRowMaxBudget, qrLeadingBlock, qrLeadingRow,
    qrLeadingColumn, diagDominanceCounterexample2] at hbad
  rcases hbad with ⟨j, hjpos, hle⟩
  fin_cases j
  · norm_num at hjpos
  · norm_num at hle
/-- Scalar-comparison route elimination for the active-pivot Cox--Higham
    split.

The finite comparison defect package is exactly equivalent to the displayed
stage-budget/row-max comparison, so the active-pivot budget counterexample also
rules out deriving `storedQRStageRowMaxComparisonDefectBudget <= 0` from the
current active-pivot active/off-diagonal budget surface. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  exact
    not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageBudget_le_rowMax
      (by
        intro A_hat stageBudget hupper hdetLead hactive hpivotMax
          hbudget_nonneg hactiveBlockBudget hoffdiagBudget
        exact
          storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
            (by omega : 2 ≤ 2) A_hat stageBudget
            (h A_hat stageBudget hupper hdetLead hactive hpivotMax
              hbudget_nonneg hactiveBlockBudget hoffdiagBudget))
/-- The active-pivot row-budget counterexample has positive scalar
    row-max/diagonal defect.

At displayed stage one, the leading strict-upper row maximum is `2` while the
displayed diagonal magnitude is `1`, so the canonical finite row-max defect is
positive even though the same sequence satisfies the active-pivot and
active/off-diagonal budget fields used by the route-elimination theorems
above. -/
theorem activeMaxPivotRowBudgetDiagCounterexample_rowMaxDiagDefectBudget_pos :
    0 < storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2)
      activeMaxPivotRowBudgetDiagCounterexampleSeq := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRRowMaxDiagDefect_le_budget
      (by omega : 2 ≤ 2)
      activeMaxPivotRowBudgetDiagCounterexampleSeq
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
          activeMaxPivotRowBudgetDiagCounterexampleSeq 1 (by norm_num) i -
        |qrLeadingBlock
          (activeMaxPivotRowBudgetDiagCounterexampleSeq 1)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le (by norm_num : 1 < 2)
            (by omega : 2 ≤ 2)))
          (by norm_num : 1 < 2) i i| = 1 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock (activeMaxPivotRowBudgetDiagCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        diagDominanceCounterexample2, qrLeadingBlock, qrLeadingRow,
        qrLeadingColumn]
    rw [hsup]
    norm_num [i, activeMaxPivotRowBudgetDiagCounterexampleSeq,
      diagDominanceCounterexample2, qrLeadingBlock, qrLeadingRow,
      qrLeadingColumn]
    rfl
  have hbudget :
      (1 : ℝ) ≤ storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2)
        activeMaxPivotRowBudgetDiagCounterexampleSeq := by
    simpa [hentry] using hle
  linarith
/-- Active-pivot budget route elimination for the scalar row-max defect.

Even upper triangular nonsingular displayed blocks, positive active-block mass,
active max-pivoting, nonnegative active-block budgets, active-block budget
control, and displayed strict-upper budget control do not imply
`storedQRRowMaxDiagDefectBudget <= 0`.  The witness is the same active-pivot
sequence used for the row-budget diagonal and stage-budget/row-max comparison
obstructions; at displayed stage one the actual row maximum is larger than the
matching displayed diagonal. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_rowMaxDiagDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (rowBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ rowBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ rowBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let rowBudget : ℕ → ℝ := fun _ => 2
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ rowBudget k := by
    intro k _hk
    norm_num [rowBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ rowBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, rowBudget,
            activeMaxPivotRowBudgetDiagCounterexampleSeq,
            diagDominanceCounterexample2]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos := h A_hat rowBudget hupper hdetLead hactive hpivotMax
    hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  exact not_lt_of_ge hnonpos
    activeMaxPivotRowBudgetDiagCounterexample_rowMaxDiagDefectBudget_pos
/-- Product compact smallness plus upper-triangular nonsingularity still does
    not imply the scalar row-max defect condition.

The witness satisfies the same leading-block triangular and nonsingularity
fields as the active-pivot row-max counterexample, and a sufficiently small
scalar compact budget makes the displayed product-smallness inequality true at
both stages.  The row-max defect is nevertheless positive, so the row-max
condition cannot be recovered from product smallness alone. -/
theorem not_forall_upper_tri_det_ne_zero_product_budget_implies_rowMaxDiagDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 => |diagDominanceCounterexample2 k k|) = 1 := by
        apply Finset.inf'_eq_of_forall
        intro k _
        fin_cases k <;> norm_num [diagDominanceCounterexample2]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hnonpos := h A_hat B hupper hdetLead (by norm_num [B]) hproduct
  exact not_lt_of_ge hnonpos
    activeMaxPivotRowBudgetDiagCounterexample_rowMaxDiagDefectBudget_pos
/-- The active-pivot row-budget counterexample also has positive scalar
    stage-diagonal lower-bound defect for the constant stage budget `2`. -/
theorem activeMaxPivotRowBudgetDiagCounterexample_stageDiagLowerDefectBudget_pos :
    0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
      activeMaxPivotRowBudgetDiagCounterexampleSeq (fun _ => (2 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageDiagLowerDefect_le_budget
      (by omega : 2 ≤ 2)
      activeMaxPivotRowBudgetDiagCounterexampleSeq (fun _ => (2 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (fun _ => (2 : ℝ)) 1 -
        |qrLeadingBlock
          (activeMaxPivotRowBudgetDiagCounterexampleSeq 1)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le (by norm_num : 1 < 2)
            (by omega : 2 ≤ 2)))
          (by norm_num : 1 < 2) i i| = 1 := by
    norm_num [i, activeMaxPivotRowBudgetDiagCounterexampleSeq,
      diagDominanceCounterexample2, qrLeadingBlock, qrLeadingRow,
      qrLeadingColumn]
    rfl
  have hbudget :
      (1 : ℝ) ≤ storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        activeMaxPivotRowBudgetDiagCounterexampleSeq (fun _ => (2 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- Product compact smallness plus upper-triangular nonsingularity does not
    imply the scalar stage-diagonal lower-bound condition.

The active-pivot row-budget witness satisfies the displayed compact-product
inequality for `B = 1 / 16`, but its constant-budget stage-diagonal defect is
positive.  Thus product smallness cannot replace the diagonal lower-bound
invariant used by the active/prefix Cox--Higham route. -/
theorem not_forall_upper_tri_det_ne_zero_product_budget_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat (fun _ => (2 : ℝ)) ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 => |diagDominanceCounterexample2 k k|) = 1 := by
        apply Finset.inf'_eq_of_forall
        intro k _
        fin_cases k <;> norm_num [diagDominanceCounterexample2]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hnonpos := h A_hat B hupper hdetLead (by norm_num [B]) hproduct
  exact not_lt_of_ge hnonpos
    activeMaxPivotRowBudgetDiagCounterexample_stageDiagLowerDefectBudget_pos
/-- Adding product compact smallness to the active-pivot budget surface still
    does not imply the scalar stage-diagonal lower-bound condition.

The same active-pivot witness satisfies upper-triangular nonsingular displayed
blocks, positive active-block mass, active max-pivoting, active/off-diagonal
budget control, and the compact-product inequality for `B = 1 / 16`.  With the
constant stage budget `2`, however, the displayed diagonal magnitude at stage
one is only `1`, so the finite stage-diagonal defect is positive. -/
theorem not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 2
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 => |diagDominanceCounterexample2 k k|) = 1 := by
        apply Finset.inf'_eq_of_forall
        intro k _
        fin_cases k <;> norm_num [diagDominanceCounterexample2]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowBudgetDiagCounterexampleSeq,
            diagDominanceCounterexample2]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos := h A_hat stageBudget B hupper hdetLead (by norm_num [B])
    hproduct hactive hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  have hpos :
      0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowBudgetDiagCounterexample_stageDiagLowerDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Active-pivot budget route elimination for the scalar stage-diagonal defect.

If the active-pivot budget surface alone implied the nonpositive
stage-diagonal lower-bound defect, it would also imply that conclusion after
adding the product compact-smallness hypotheses.  The preceding product-small
counterexample rules out that stronger universal statement, so the scalar
stage-diagonal condition remains an independent invariant. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  exact
    not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageDiagLowerDefectBudget_nonpos
      (by
        intro A_hat stageBudget B hupper hdetLead _hB _hproduct hactive
          hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
        exact h A_hat stageBudget hupper hdetLead hactive hpivotMax
          hbudget_nonneg hactiveBlockBudget hoffdiagBudget)
/-- Adding product compact smallness to the active-pivot budget surface still
    does not imply the stage-budget/row-max comparison.

The same active-pivot witness satisfies upper-triangular nonsingular displayed
blocks, positive active-block mass, active max-pivoting, active/off-diagonal
budget control, and the compact-product inequality for `B = 1 / 16`.  With the
oversized stage budget `3`, however, the displayed row maximum at stage one is
only `2`, so `stageBudget <= rowMax` fails. -/
theorem not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageBudget_le_rowMax :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      (∀ k (hk : k < 2), ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2) A_hat k hk i)) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 => |diagDominanceCounterexample2 k k|) = 1 := by
        apply Finset.inf'_eq_of_forall
        intro k _
        fin_cases k <;> norm_num [diagDominanceCounterexample2]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowBudgetDiagCounterexampleSeq,
            diagDominanceCounterexample2]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hcompare := h A_hat stageBudget B hupper hdetLead (by norm_num [B])
    hproduct hactive hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  have hbad :=
    hcompare 1 (by norm_num) (⟨0, by norm_num⟩ : Fin (1 + 1)) (by norm_num)
  norm_num [A_hat, stageBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
    qrLeadingStrictUpperRowMaxBudget, qrLeadingBlock, qrLeadingRow,
    qrLeadingColumn, diagDominanceCounterexample2] at hbad
  rcases hbad with ⟨j, hjpos, hle⟩
  fin_cases j
  · norm_num at hjpos
  · norm_num at hle
/-- Product-smallness does not rescue the scalar comparison-defect route.

If compact-product smallness plus the active-pivot budget surface implied
`storedQRStageRowMaxComparisonDefectBudget <= 0`, then the finite-max extractor
would imply the already-refuted displayed comparison theorem. -/
theorem not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  exact
    not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageBudget_le_rowMax
      (by
        intro A_hat stageBudget B hupper hdetLead hB hproduct hactive
          hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
        exact
          storedQRStageBudget_le_rowMax_of_stageRowMaxComparisonDefectBudget_nonpos
            (by omega : 2 ≤ 2) A_hat stageBudget
            (h A_hat stageBudget B hupper hdetLead hB hproduct hactive
              hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget))
/-- A diagonally safe active-pivot witness for separating the scalar comparison
    defect from the scalar row-max defect.

    The displayed stage-one block has strict-upper row maximum `2` and matching
    diagonal magnitude `3`, so the row-max/diagonal defect is nonpositive.
    Taking the stage budget to be `3`, however, still makes the
    stage-budget/row-max comparison defect positive. -/
def activeMaxPivotRowMaxComparisonCounterexampleA0 :
    Fin 2 → Fin 2 → ℝ :=
  fun i j =>
    if i.val = 0 ∧ j.val = 0 then 3
    else if i.val = 0 ∧ j.val = 1 then 2
    else if i.val = 1 ∧ j.val = 1 then 1
    else 0
/-- Constant sequence built from
    `activeMaxPivotRowMaxComparisonCounterexampleA0`. -/
def activeMaxPivotRowMaxComparisonCounterexampleSeq :
    ℕ → Fin 2 → Fin 2 → ℝ :=
  fun _ => activeMaxPivotRowMaxComparisonCounterexampleA0
theorem activeMaxPivotRowMaxComparisonCounterexample_diagDominant :
    ∀ k (hk : k < 2),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (activeMaxPivotRowMaxComparisonCounterexampleSeq k)
          (Nat.succ_le_iff.mpr hk) hk) := by
  intro k hk
  interval_cases k
  · refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      fin_cases j
      omega
    · intro i
      fin_cases i
      norm_num [activeMaxPivotRowMaxComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i
      fin_cases j
      omega
  · refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      · fin_cases j
        · norm_num at hji
        · norm_num at hji
      · fin_cases j
        · norm_num [activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
        · norm_num at hji
    · intro i
      fin_cases i <;>
        norm_num [activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i <;> fin_cases j <;>
        first
        | omega
        | norm_num [activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
theorem activeMaxPivotRowMaxComparisonCounterexample_rowMaxDiagDefectBudget_nonpos :
    storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleSeq ≤ 0 := by
  exact
    storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant
      (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleSeq
      activeMaxPivotRowMaxComparisonCounterexample_diagDominant
theorem activeMaxPivotRowMaxComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos :
    0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleSeq (fun _ => (3 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget
      (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleSeq
      (fun _ => (3 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (3 : ℝ) -
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
            activeMaxPivotRowMaxComparisonCounterexampleSeq 1
            (by norm_num) i = 1 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock
                (activeMaxPivotRowMaxComparisonCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, activeMaxPivotRowMaxComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    rw [hsup]
    norm_num
  have hbudget :
      (1 : ℝ) ≤
        storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
          activeMaxPivotRowMaxComparisonCounterexampleSeq (fun _ => (3 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- Local diagonal dominance alone does not imply the scalar
    stage-budget/row-max comparison defect.

The witness is diagonally dominant at every displayed leading block, but the
stage budget `3` is still larger than the strict-upper row maximum `2` at
stage one.  Thus diagonal dominance may remove the separate row-max defect
field, but it cannot remove the comparison scalar. -/
theorem not_forall_diagDominant_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hDD : ∀ k (hk : k < 2),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk) := by
    intro k hk
    simpa [A_hat] using
      activeMaxPivotRowMaxComparisonCounterexample_diagDominant k hk
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0 := h A_hat stageBudget hDD
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
theorem activeMaxPivotRowMaxComparisonCounterexample_productBudget :
    ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (activeMaxPivotRowMaxComparisonCounterexampleSeq k)
              (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * ((1 : ℝ) / 16) ^ 2) <
      1 := by
  intro k hk
  interval_cases k
  · have hblock :
        (qrLeadingBlock
          (activeMaxPivotRowMaxComparisonCounterexampleSeq 0)
          (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          (fun _ _ => (3 : ℝ)) := by
      ext i j
      fin_cases i
      fin_cases j
      norm_num [activeMaxPivotRowMaxComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    rw [hblock]
    norm_num [diagDominantUpperInvBudgetExpr]
  · have hblock :
        (qrLeadingBlock
          (activeMaxPivotRowMaxComparisonCounterexampleSeq 1)
          (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          activeMaxPivotRowMaxComparisonCounterexampleA0 := by
      ext i j
      fin_cases i <;> fin_cases j <;>
        norm_num [activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
    have hdiagInf :
        Finset.inf' Finset.univ
            ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
            (fun k : Fin 2 =>
              |activeMaxPivotRowMaxComparisonCounterexampleA0 k k|) = 1 := by
      apply le_antisymm
      · have hle :=
          Finset.inf'_le
            (s := Finset.univ)
            (f := fun k : Fin 2 =>
              |activeMaxPivotRowMaxComparisonCounterexampleA0 k k|)
            (b := (⟨1, by norm_num⟩ : Fin 2)) (Finset.mem_univ _)
        simp [activeMaxPivotRowMaxComparisonCounterexampleA0] at hle ⊢
      · apply Finset.le_inf'
        intro k _
        fin_cases k <;>
          norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]
    rw [hblock]
    dsimp [diagDominantUpperInvBudgetExpr]
    rw [hdiagInf]
    norm_num
/-- Adding compact-product smallness to local diagonal dominance still does not
    imply the scalar stage-budget/row-max comparison defect. -/
theorem not_forall_diagDominant_product_budget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ)
        (B : ℝ),
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  let B : ℝ := (1 : ℝ) / 16
  have hDD : ∀ k (hk : k < 2),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk) := by
    intro k hk
    simpa [A_hat] using
      activeMaxPivotRowMaxComparisonCounterexample_diagDominant k hk
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    simpa [A_hat, B] using
      activeMaxPivotRowMaxComparisonCounterexample_productBudget k hk
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0 := h A_hat stageBudget B hDD (by norm_num [B]) hproduct
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Adding active max-pivoting and the active/off-diagonal budget surface to
    diagonal dominance and compact-product smallness still does not imply the
    scalar stage-budget/row-max comparison defect.

This is the route-elimination statement matching the active finite-max
diagonal-dominant QR branch: the concrete witness satisfies all of these
visible domain/budget fields, but the scalar comparison defect remains
positive. -/
theorem not_forall_diagDominant_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ)
        (B : ℝ),
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  let B : ℝ := (1 : ℝ) / 16
  have hDD : ∀ k (hk : k < 2),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk) := by
    intro k hk
    simpa [A_hat] using
      activeMaxPivotRowMaxComparisonCounterexample_diagDominant k hk
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    simpa [A_hat, B] using
      activeMaxPivotRowMaxComparisonCounterexample_productBudget k hk
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget,
          activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0 :=
    h A_hat stageBudget B hDD (by norm_num [B]) hproduct hactive hpivotMax
      hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- The same diagonally safe witness has positive scalar stage-diagonal defect
    when the displayed stage budget is allowed to overshoot the diagonal.

This separates the row-max scalar defect from the downstream stage-diagonal
scalar condition: nonpositive row-max defect alone does not stop the stage
budget from being larger than the displayed diagonal. -/
theorem activeMaxPivotRowMaxComparisonCounterexample_stageDiagLowerDefectBudget_pos :
    0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleSeq (fun _ => (4 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageDiagLowerDefect_le_budget
      (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleSeq
      (fun _ => (4 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (4 : ℝ) -
          |qrLeadingBlock
            (activeMaxPivotRowMaxComparisonCounterexampleSeq 1)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
            (by norm_num : 1 < 2) i i| = 1 := by
    norm_num [i, activeMaxPivotRowMaxComparisonCounterexampleSeq,
      activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
      qrLeadingRow, qrLeadingColumn]
    rfl
  have hbudget :
      (1 : ℝ) ≤
        storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
          activeMaxPivotRowMaxComparisonCounterexampleSeq (fun _ => (4 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- The scalar row-max defect alone does not imply the downstream scalar
    stage-diagonal condition.

The witness has nonpositive row-max defect, but choosing the stage budget
larger than the displayed diagonal makes the stage-diagonal finite defect
positive.  Thus the row-max bridge genuinely needs the separate
stage-budget/row-max comparison (or an equivalent stronger loop invariant). -/
theorem not_forall_rowMaxDiagDefectBudget_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 4
  have hnonpos :=
    h A_hat stageBudget
      activeMaxPivotRowMaxComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
  have hpos :
      0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageDiagLowerDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Product compact-smallness and active-pivot budget data still do not make
    the scalar stage-diagonal defect follow from a granted scalar row-max
    defect.

The diagonally safe row-max witness satisfies the active-pivot, active-budget,
displayed off-diagonal budget, and compact-product side conditions.  With the
larger uniform stage budget `4`, however, the stage-diagonal scalar defect is
positive. -/
theorem not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_rowMaxDiagDefect_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 4
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hji
        · norm_num at hji
      · fin_cases j
        · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
        · norm_num at hji
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (3 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            activeMaxPivotRowMaxComparisonCounterexampleA0 := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      rw [hblock]
      rw [Matrix.det_fin_two]
      norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (3 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            activeMaxPivotRowMaxComparisonCounterexampleA0 := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 =>
                |activeMaxPivotRowMaxComparisonCounterexampleA0 k k|) = 1 := by
        apply le_antisymm
        · have hle :=
            Finset.inf'_le
              (s := Finset.univ)
              (f := fun k : Fin 2 =>
                |activeMaxPivotRowMaxComparisonCounterexampleA0 k k|)
              (b := (⟨1, by norm_num⟩ : Fin 2)) (Finset.mem_univ _)
          simp [activeMaxPivotRowMaxComparisonCounterexampleA0] at hle ⊢
        · apply Finset.le_inf'
          intro k _
          fin_cases k <;>
            norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget,
          activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos := h A_hat stageBudget B hupper hdetLead (by norm_num [B])
    hproduct hactive hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
    activeMaxPivotRowMaxComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
  have hpos :
      0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageDiagLowerDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Even granting the row-max scalar defect, the active-pivot budget surface
    alone does not imply the scalar stage-diagonal condition. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_rowMaxDiagDefect_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  exact
    not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_rowMaxDiagDefect_implies_stageDiagLowerDefectBudget_nonpos
      (by
        intro A_hat stageBudget B hupper hdetLead _hB _hproduct hactive
          hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget hrowDefect
        exact h A_hat stageBudget hupper hdetLead hactive hpivotMax
          hbudget_nonneg hactiveBlockBudget hoffdiagBudget hrowDefect)
/-- Even after granting the scalar row-max defect, the active-pivot budget
    surface does not imply the scalar stage-budget/row-max comparison defect.

    The witness is diagonally safe for the row-max scalar condition but uses an
    oversized stage budget.  Thus the two scalar row-max dependencies exposed
    by the active/prefix QR theorem surface are genuinely independent unless a
    stronger loop invariant proves the comparison field. -/
theorem not_forall_leadingBlock_upper_det_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_rowMaxDiagDefect_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hji
        · norm_num at hji
      · fin_cases j
        · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
        · norm_num at hji
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (3 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            activeMaxPivotRowMaxComparisonCounterexampleA0 := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      rw [hblock]
      rw [Matrix.det_fin_two]
      norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget,
          activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos := h A_hat stageBudget hupper hdetLead hactive hpivotMax
    hbudget_nonneg hactiveBlockBudget hoffdiagBudget
    activeMaxPivotRowMaxComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Product compact-smallness still does not make the scalar comparison defect
    follow from the row-max scalar defect and active-pivot budget surface. -/
theorem not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_rowMaxDiagDefect_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (stageBudget : ℕ → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ stageBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowMaxComparisonCounterexampleSeq
  let stageBudget : ℕ → ℝ := fun _ => 3
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hji
        · norm_num at hji
      · fin_cases j
        · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
        · norm_num at hji
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (3 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            activeMaxPivotRowMaxComparisonCounterexampleA0 := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      rw [hblock]
      rw [Matrix.det_fin_two]
      norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (3 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            activeMaxPivotRowMaxComparisonCounterexampleA0 := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 =>
                |activeMaxPivotRowMaxComparisonCounterexampleA0 k k|) = 1 := by
        apply le_antisymm
        · have hle :=
            Finset.inf'_le
              (s := Finset.univ)
              (f := fun k : Fin 2 =>
                |activeMaxPivotRowMaxComparisonCounterexampleA0 k k|)
              (b := (⟨1, by norm_num⟩ : Fin 2)) (Finset.mem_univ _)
          simp [activeMaxPivotRowMaxComparisonCounterexampleA0] at hle ⊢
        · apply Finset.le_inf'
          intro k _
          fin_cases k <;>
            norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowMaxComparisonCounterexampleA0)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowMaxComparisonCounterexampleA0]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ stageBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, stageBudget,
          activeMaxPivotRowMaxComparisonCounterexampleSeq,
          activeMaxPivotRowMaxComparisonCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          stageBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, stageBudget,
            activeMaxPivotRowMaxComparisonCounterexampleSeq,
            activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos := h A_hat stageBudget B hupper hdetLead (by norm_num [B])
    hproduct hactive hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
    activeMaxPivotRowMaxComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget := by
    simpa [A_hat, stageBudget] using
      activeMaxPivotRowMaxComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Adding product compact smallness to the active-pivot budget surface still
    does not imply the nonpositive scalar row-max defect.

The same active-pivot witness satisfies upper-triangular nonsingular displayed
blocks, positive active-block mass, active max-pivoting, active/off-diagonal
budget control, and the compact-product inequality for `B = 1 / 16`.  At
displayed stage one the strict-upper row maximum is nevertheless `2` while the
displayed diagonal magnitude is `1`, so the finite row-max defect is positive. -/
theorem not_forall_leadingBlock_upper_det_product_activeBlockPos_activeMaxPivot_activeBlockBudget_offdiagBudget_implies_rowMaxDiagDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (rowBudget : ℕ → ℝ) (B : ℝ),
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
        qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      0 < B →
      (∀ k (hk : k < 2),
        2 *
            diagDominantUpperInvBudgetExpr (k + 1)
              (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
              ⟨k, Nat.lt_succ_self k⟩ *
          ((2 : ℝ) * B ^ 2) <
        1) →
      (∀ k (hk : k < 2),
        0 < householderActiveBlockNorm2Sq
          ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k)) →
      (∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
          householderTrailingColumnNorm2Sq
            (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩) →
      (∀ k (_hk : k < 2), 0 ≤ rowBudget k) →
      (∀ k (_hk : k < 2), ∀ r l : Fin 2, k ≤ r.val → k ≤ l.val →
        |A_hat k r l| ≤ rowBudget k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0) := by
  intro h
  let A_hat := activeMaxPivotRowBudgetDiagCounterexampleSeq
  let rowBudget : ℕ → ℝ := fun _ => 2
  let B : ℝ := (1 : ℝ) / 16
  have hupper : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), j.val < i.val →
      qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j = 0 := by
    intro k hk i j hji
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn] using
        diagDominanceCounterexample2_upper
          (qrLeadingRow 2 1 (Nat.succ_le_iff.mpr hk) i)
          (qrLeadingColumn 2 1 hk j)
          (by simpa [qrLeadingRow, qrLeadingColumn] using hji)
  have hdetLead : ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [show
        (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          qrLeadingBlock activeMaxPivotRowBudgetDiagCounterexampleA0
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      rw [hblock, Matrix.det_fin_one]
      norm_num
    · have hblock :
          (qrLeadingBlock diagDominanceCounterexample2
              (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      rw [show
        (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          qrLeadingBlock diagDominanceCounterexample2
            (Nat.succ_le_iff.mpr hk) hk by
        simp [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq]]
      simpa [hblock] using diagDominanceCounterexample2_det_ne_zero
  have hproduct : ∀ k (hk : k < 2),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((2 : ℝ) * B ^ 2) <
      1 := by
    intro k hk
    interval_cases k
    · have hblock :
          (qrLeadingBlock (A_hat 0) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
            (fun _ _ => (2 : ℝ)) := by
        ext i j
        fin_cases i
        fin_cases j
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
      rw [hblock]
      norm_num [diagDominantUpperInvBudgetExpr, B]
    · have hblock :
          (qrLeadingBlock (A_hat 1) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
            (diagDominanceCounterexample2 : Matrix (Fin 2) (Fin 2) ℝ) := by
        ext i j
        fin_cases i <;> fin_cases j <;>
          norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
      have hdiagInf :
          Finset.inf' Finset.univ
              ⟨(⟨1, by norm_num⟩ : Fin 2), Finset.mem_univ _⟩
              (fun k : Fin 2 => |diagDominanceCounterexample2 k k|) = 1 := by
        apply Finset.inf'_eq_of_forall
        intro k _
        fin_cases k <;> norm_num [diagDominanceCounterexample2]
      rw [hblock]
      dsimp [diagDominantUpperInvBudgetExpr]
      rw [hdiagInf]
      norm_num [B]
  have hactive : ∀ k (hk : k < 2),
      0 < householderActiveBlockNorm2Sq ⟨k, hk⟩ ⟨k, hk⟩ (A_hat k) := by
    intro k hk
    interval_cases k
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨0, by norm_num⟩ : Fin 2))
          (k := (⟨0, by norm_num⟩ : Fin 2))
          (A := activeMaxPivotRowBudgetDiagCounterexampleA0)
          ⟨⟨0, by norm_num⟩, by norm_num,
            ⟨0, by norm_num⟩, by norm_num,
            by norm_num [activeMaxPivotRowBudgetDiagCounterexampleA0]⟩
    · simpa [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq] using
        householderActiveBlockNorm2Sq_pos_of_exists_active_entry_ne
          (p := (⟨1, by norm_num⟩ : Fin 2))
          (k := (⟨1, by norm_num⟩ : Fin 2))
          (A := diagDominanceCounterexample2)
          ⟨⟨1, by norm_num⟩, by norm_num,
            ⟨1, by norm_num⟩, by norm_num,
            by norm_num [diagDominanceCounterexample2]⟩
  have hpivotMax : ∀ k (hk : k < 2), ∀ l : Fin 2, k ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) l ≤
        householderTrailingColumnNorm2Sq
          (m := 2) (n := 2) ⟨k, hk⟩ (A_hat k) ⟨k, hk⟩ := by
    intro k hk l hl
    interval_cases k
    · fin_cases l <;>
        norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0,
          householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
          householderTrailingPart, vecNorm2Sq]
    · fin_cases l
      · norm_num at hl
      · norm_num [A_hat, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          diagDominanceCounterexample2, householderTrailingColumnNorm2Sq,
          householderTrailingNorm2Sq, householderTrailingPart, vecNorm2Sq]
  have hbudget_nonneg : ∀ k (_hk : k < 2), 0 ≤ rowBudget k := by
    intro k _hk
    norm_num [rowBudget]
  have hactiveBlockBudget : ∀ k (hk : k < 2), ∀ r l : Fin 2,
      k ≤ r.val → k ≤ l.val → |A_hat k r l| ≤ rowBudget k := by
    intro k hk r l hr hl
    interval_cases k
    · fin_cases r <;> fin_cases l <;>
        norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
          activeMaxPivotRowBudgetDiagCounterexampleA0]
    · fin_cases r
      · norm_num at hr
      · fin_cases l
        · norm_num at hl
        · norm_num [A_hat, rowBudget,
            activeMaxPivotRowBudgetDiagCounterexampleSeq,
            diagDominanceCounterexample2]
  have hoffdiagBudget : ∀ k (hk : k < 2), ∀ i j : Fin (k + 1),
      i.val < j.val →
        |qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk i j| ≤
          rowBudget k := by
    intro k hk i j hij
    interval_cases k
    · fin_cases i
      fin_cases j
      omega
    · fin_cases i
      · fin_cases j
        · norm_num at hij
        · norm_num [A_hat, rowBudget, activeMaxPivotRowBudgetDiagCounterexampleSeq,
            qrLeadingBlock, qrLeadingRow, qrLeadingColumn,
            diagDominanceCounterexample2]
      · fin_cases j
        · norm_num at hij
        · norm_num at hij
  have hnonpos := h A_hat rowBudget B hupper hdetLead (by norm_num [B])
    hproduct hactive hpivotMax hbudget_nonneg hactiveBlockBudget hoffdiagBudget
  exact not_lt_of_ge hnonpos
    activeMaxPivotRowBudgetDiagCounterexample_rowMaxDiagDefectBudget_pos
/-- The exact no-pivot Householder counterexample has positive scalar
    row-max/diagonal defect.

At displayed stage one, the leading block row has strict-upper maximum `2`
while the diagonal magnitude is `1`.  Hence the canonical finite defect budget
is at least `1`. -/
theorem exactHouseholderQRDiagDominanceCounterexample_rowMaxDiagDefectBudget_pos :
    0 < storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleSeq := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRRowMaxDiagDefect_le_budget
      (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleSeq
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
          exactHouseholderQRDiagDominanceCounterexampleSeq 1 (by norm_num) i -
        |qrLeadingBlock
          (exactHouseholderQRDiagDominanceCounterexampleSeq 1)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le (by norm_num : 1 < 2)
            (by omega : 2 ≤ 2)))
          (by norm_num : 1 < 2) i i| = 1 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock (exactHouseholderQRDiagDominanceCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, exactHouseholderQRDiagDominanceCounterexampleSeq,
        exactHouseholderQRDiagDominanceCounterexampleA1,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    rw [hsup]
    norm_num [i, exactHouseholderQRDiagDominanceCounterexampleSeq,
      exactHouseholderQRDiagDominanceCounterexampleA1,
      qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    rfl
  have hbudget :
      (1 : ℝ) ≤ storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2)
        exactHouseholderQRDiagDominanceCounterexampleSeq := by
    simpa [hentry] using hle
  linarith
/-- Universal-form route elimination for the scalar route-1 defect shortcut:
    exact unpivoted trailing Householder recurrence, valid Householder squared
    norms, and nonzero denominators do not imply
    `storedQRRowMaxDiagDefectBudget <= 0`.

Thus the scalar defect condition introduced for the row-max handoff is a real
source/domain invariant, not a consequence of the ordinary exact no-pivot QR
loop. -/
theorem not_forall_exact_trailing_householder_sequence_implies_rowMaxDiagDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          matMulRectLeft
            (householder 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun i => A_hat k i ⟨k, hk⟩) (alpha k))))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k * alpha k =
          householderTrailingNorm2Sq 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i => A_hat k i ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0) := by
  intro h
  have hnonpos :=
    h exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_step
      exactHouseholderQRDiagDominanceCounterexample_alpha_sq
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
  exact not_lt_of_ge hnonpos
    exactHouseholderQRDiagDominanceCounterexample_rowMaxDiagDefectBudget_pos
/-- The exact no-pivot Householder counterexample also has positive scalar
    stage-diagonal lower-bound defect for the constant stage budget `2`.

At displayed stage one, the active displayed row has diagonal magnitude `1`,
so the defect `2 - |diag|` is already `1`. -/
theorem exactHouseholderQRDiagDominanceCounterexample_stageDiagLowerDefectBudget_pos :
    0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleSeq (fun _ => (2 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageDiagLowerDefect_le_budget
      (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleSeq (fun _ => (2 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (fun _ => (2 : ℝ)) 1 -
        |qrLeadingBlock
          (exactHouseholderQRDiagDominanceCounterexampleSeq 1)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le (by norm_num : 1 < 2)
            (by omega : 2 ≤ 2)))
          (by norm_num : 1 < 2) i i| = 1 := by
    norm_num [i, exactHouseholderQRDiagDominanceCounterexampleSeq,
      exactHouseholderQRDiagDominanceCounterexampleA1,
      qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    rfl
  have hbudget :
      (1 : ℝ) ≤ storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        exactHouseholderQRDiagDominanceCounterexampleSeq (fun _ => (2 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- Universal-form route elimination for the stage-diagonal scalar shortcut:
    exact unpivoted trailing Householder recurrence, valid Householder squared
    norms, and nonzero denominators do not imply
    `storedQRStageDiagLowerDefectBudget <= 0` even for the constant stage
    budget `2`.

Thus the stage-diagonal scalar condition in the active/prefix route is a real
computed-loop/domain invariant, not a generic consequence of the ordinary exact
no-pivot QR recurrence. -/
theorem not_forall_exact_trailing_householder_sequence_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          matMulRectLeft
            (householder 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun i => A_hat k i ⟨k, hk⟩) (alpha k))))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k * alpha k =
          householderTrailingNorm2Sq 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i => A_hat k i ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat (fun _ => (2 : ℝ)) ≤ 0) := by
  intro h
  have hnonpos :=
    h exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_step
      exactHouseholderQRDiagDominanceCounterexample_alpha_sq
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
  exact not_lt_of_ge hnonpos
    exactHouseholderQRDiagDominanceCounterexample_stageDiagLowerDefectBudget_pos
/-- The exact no-pivot Householder counterexample has positive scalar
    stage-budget/row-max comparison defect for the constant stage budget `3`.

At displayed stage one, the strict-upper row maximum is `2`, so the comparison
defect `3 - rowMax` is already positive. -/
theorem exactHouseholderQRDiagDominanceCounterexample_stageRowMaxComparisonDefectBudget_pos :
    0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleSeq (fun _ => (3 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget
      (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleSeq (fun _ => (3 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (fun _ => (3 : ℝ)) 1 -
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
            exactHouseholderQRDiagDominanceCounterexampleSeq 1
            (by norm_num) i = 1 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock
                (exactHouseholderQRDiagDominanceCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, exactHouseholderQRDiagDominanceCounterexampleSeq,
        exactHouseholderQRDiagDominanceCounterexampleA1,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    rw [hsup]
    norm_num
  have hbudget :
      (1 : ℝ) ≤
        storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
          exactHouseholderQRDiagDominanceCounterexampleSeq (fun _ => (3 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- Universal-form route elimination for the scalar comparison shortcut:
    exact unpivoted trailing Householder recurrence, valid Householder squared
    norms, nonzero denominators, and nonnegative stage budgets do not imply
    `storedQRStageRowMaxComparisonDefectBudget <= 0`.

Thus the comparison scalar in the active diagonal-dominant route is not a
generic consequence of the ordinary exact no-pivot QR recurrence. -/
theorem not_forall_exact_trailing_householder_sequence_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (A_hat : ℕ → Fin 2 → Fin 2 → ℝ) (alpha : ℕ → ℝ)
        (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          matMulRectLeft
            (householder 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun i => A_hat k i ⟨k, hk⟩) (alpha k))))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k * alpha k =
          householderTrailingNorm2Sq 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i => A_hat k i ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hbudgetNonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hnonpos :=
    h exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha stageBudget
      exactHouseholderQRDiagDominanceCounterexample_step
      exactHouseholderQRDiagDominanceCounterexample_alpha_sq
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
      hbudgetNonneg
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        exactHouseholderQRDiagDominanceCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      exactHouseholderQRDiagDominanceCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Universal-form route elimination on the stored-panel actual-unit surface:
    the stored recurrence, signed-alpha choice, nonzero source denominators,
    and a small declared unit roundoff do not imply the scalar stage-diagonal
    lower-bound condition, even for the constant stage budget `2`. -/
theorem not_forall_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat (fun _ => (2 : ℝ)) ≤ 0) := by
  intro h
  have hnonpos :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha
      exactHouseholderQRDiagDominanceCounterexample_stored_step
      exactHouseholderQRDiagDominanceCounterexample_signed_alpha_def
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
  exact not_lt_of_ge hnonpos
    exactHouseholderQRDiagDominanceCounterexample_stageDiagLowerDefectBudget_pos
/-- Universal-form route elimination on the stored-panel actual-unit surface:
    the stored recurrence, signed-alpha choice, nonzero source denominators,
    nonnegative stage budgets, and a small declared unit roundoff do not imply
    the scalar stage-budget/row-max comparison condition. -/
theorem not_forall_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (_hk : k < 2), 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hbudgetNonneg : ∀ k (_hk : k < 2), 0 ≤ stageBudget k := by
    intro k _hk
    norm_num [stageBudget]
  have hnonpos :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      exactHouseholderQRDiagDominanceCounterexampleSeq
      exactHouseholderQRDiagDominanceCounterexampleAlpha stageBudget
      exactHouseholderQRDiagDominanceCounterexample_stored_step
      exactHouseholderQRDiagDominanceCounterexample_signed_alpha_def
      exactHouseholderQRDiagDominanceCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      hbudgetNonneg
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        exactHouseholderQRDiagDominanceCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      exactHouseholderQRDiagDominanceCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- First stored-recursive state for the diagonally-dominant scalar-comparison
    counterexample.

Starting from `[[3, 2], [0, 1]]`, the first exact stored Householder step
flips the first row.  The leading `2 x 2` block remains diagonally dominant,
but its strict-upper row maximum is still only `2`. -/
def storedDiagDominantComparisonCounterexampleA1 :
    Fin 2 → Fin 2 → ℝ :=
  fun i j =>
    if i.val = 0 ∧ j.val = 0 then -3
    else if i.val = 0 ∧ j.val = 1 then -2
    else if i.val = 1 ∧ j.val = 1 then 1
    else 0
/-- Final stored-recursive state for the diagonally-dominant scalar-comparison
    counterexample. -/
def storedDiagDominantComparisonCounterexampleA2 :
    Fin 2 → Fin 2 → ℝ :=
  fun i j =>
    if i.val = 0 ∧ j.val = 0 then -3
    else if i.val = 0 ∧ j.val = 1 then -2
    else if i.val = 1 ∧ j.val = 1 then -1
    else 0
/-- Exact stored-panel sequence for the diagonally-dominant scalar-comparison
    counterexample. -/
def storedDiagDominantComparisonCounterexampleSeq :
    ℕ → Fin 2 → Fin 2 → ℝ
  | 0 => activeMaxPivotRowMaxComparisonCounterexampleA0
  | 1 => storedDiagDominantComparisonCounterexampleA1
  | _ => storedDiagDominantComparisonCounterexampleA2
/-- Signed Householder scalars for the diagonally-dominant scalar-comparison
    stored-panel counterexample. -/
def storedDiagDominantComparisonCounterexampleAlpha : ℕ → ℝ
  | 0 => -3
  | _ => -1
/-- Zero right-hand-side sequence paired with the diagonally-dominant
    scalar-comparison stored-panel witness. -/
def storedDiagDominantComparisonCounterexampleRhsSeq :
    ℕ → Fin 2 → ℝ := fun _ _ => 0
/-- The diagonally-dominant scalar-comparison witness follows the stored-panel
    recurrence under exact arithmetic. -/
theorem storedDiagDominantComparisonCounterexample_stored_step :
    ∀ k (hk : k < 2),
      storedDiagDominantComparisonCounterexampleSeq (k + 1) =
        fl_householderStoredPanelStep
          exactHouseholderQRDiagDominanceCounterexampleFP 2 2 k
          (householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i =>
              storedDiagDominantComparisonCounterexampleSeq k i ⟨k, hk⟩)
            (storedDiagDominantComparisonCounterexampleAlpha k))
          (householderBetaSpec 2
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i =>
                storedDiagDominantComparisonCounterexampleSeq k i ⟨k, hk⟩)
              (storedDiagDominantComparisonCounterexampleAlpha k)))
          (storedDiagDominantComparisonCounterexampleSeq k) := by
  intro k hk
  interval_cases k
  · ext i j
    fin_cases i <;> fin_cases j <;>
      simp [exactHouseholderQRDiagDominanceCounterexampleFP,
        FPModel.exactWithUnitRoundoff,
        storedDiagDominantComparisonCounterexampleSeq,
        storedDiagDominantComparisonCounterexampleA1,
        activeMaxPivotRowMaxComparisonCounterexampleA0,
        storedDiagDominantComparisonCounterexampleAlpha,
        fl_householderStoredPanelStep, fl_householderApplyCompactPanel,
        fl_householderApplyCompact, fl_dotProduct, Fin.foldl_succ,
        householderTrailingActiveVector, householderActiveVector,
        householderTrailingPart, householderBetaSpec]
    all_goals ring_nf
  · ext i j
    fin_cases i <;> fin_cases j <;>
      simp [exactHouseholderQRDiagDominanceCounterexampleFP,
        FPModel.exactWithUnitRoundoff,
        storedDiagDominantComparisonCounterexampleSeq,
        storedDiagDominantComparisonCounterexampleA1,
        storedDiagDominantComparisonCounterexampleA2,
        storedDiagDominantComparisonCounterexampleAlpha,
        fl_householderStoredPanelStep, fl_householderApplyCompactPanel,
        fl_householderApplyCompact, fl_dotProduct, Fin.foldl_succ,
        householderTrailingActiveVector, householderActiveVector,
        householderTrailingPart, householderBetaSpec]
    all_goals ring_nf
/-- The zero right-hand side follows the stored RHS recurrence for the
    diagonally-dominant scalar-comparison witness under exact arithmetic. -/
theorem storedDiagDominantComparisonCounterexample_rhs_step :
    ∀ k (hk : k < 2),
      storedDiagDominantComparisonCounterexampleRhsSeq (k + 1) =
        fl_householderStoredRhsStep
          exactHouseholderQRDiagDominanceCounterexampleFP 2 k
          (householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun i =>
              storedDiagDominantComparisonCounterexampleSeq k i ⟨k, hk⟩)
            (storedDiagDominantComparisonCounterexampleAlpha k))
          (householderBetaSpec 2
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i =>
                storedDiagDominantComparisonCounterexampleSeq k i ⟨k, hk⟩)
              (storedDiagDominantComparisonCounterexampleAlpha k)))
          (storedDiagDominantComparisonCounterexampleRhsSeq k) := by
  intro k hk
  interval_cases k <;>
    ext i <;>
    fin_cases i <;>
    simp [storedDiagDominantComparisonCounterexampleRhsSeq,
      exactHouseholderQRDiagDominanceCounterexampleFP,
      FPModel.exactWithUnitRoundoff,
      storedDiagDominantComparisonCounterexampleSeq,
      storedDiagDominantComparisonCounterexampleA1,
      activeMaxPivotRowMaxComparisonCounterexampleA0,
      storedDiagDominantComparisonCounterexampleAlpha,
      fl_householderStoredRhsStep, fl_householderApplyCompact,
      fl_dotProduct, Fin.foldl_succ, householderTrailingActiveVector,
      householderActiveVector, householderTrailingPart, householderBetaSpec]
/-- The stored diagonally-dominant scalar-comparison witness uses the signed
    Householder alpha definition. -/
theorem storedDiagDominantComparisonCounterexample_signed_alpha_def :
    ∀ k (hk : k < 2),
      storedDiagDominantComparisonCounterexampleAlpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i =>
                storedDiagDominantComparisonCounterexampleSeq k i ⟨k, hk⟩)))
          (storedDiagDominantComparisonCounterexampleSeq k
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩) := by
  intro k hk
  interval_cases k
  · have hsqrt9 : Real.sqrt (9 : ℝ) = 3 := by
      rw [Real.sqrt_eq_iff_mul_self_eq_of_pos (by norm_num : (0 : ℝ) < 3)]
      norm_num
    norm_num [storedDiagDominantComparisonCounterexampleAlpha,
      storedDiagDominantComparisonCounterexampleSeq,
      activeMaxPivotRowMaxComparisonCounterexampleA0, signedHouseholderAlpha,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
    exact hsqrt9.symm
  · norm_num [storedDiagDominantComparisonCounterexampleAlpha,
      storedDiagDominantComparisonCounterexampleSeq,
      storedDiagDominantComparisonCounterexampleA1, signedHouseholderAlpha,
      householderTrailingNorm2Sq, vecNorm2Sq, householderTrailingPart]
/-- The exact Householder source denominators in the stored diagonally-dominant
    scalar-comparison witness are nonzero. -/
theorem storedDiagDominantComparisonCounterexample_den_ne_zero :
    ∀ k (hk : k < 2),
      (∑ i : Fin 2,
        householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun a =>
              storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
            (storedDiagDominantComparisonCounterexampleAlpha k) i *
          householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun a =>
              storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
            (storedDiagDominantComparisonCounterexampleAlpha k) i) ≠ 0 := by
  intro k hk
  interval_cases k
  · norm_num [storedDiagDominantComparisonCounterexampleAlpha,
      storedDiagDominantComparisonCounterexampleSeq,
      activeMaxPivotRowMaxComparisonCounterexampleA0,
      householderTrailingActiveVector, householderActiveVector,
      householderTrailingPart]
  · norm_num [storedDiagDominantComparisonCounterexampleAlpha,
      storedDiagDominantComparisonCounterexampleSeq,
      storedDiagDominantComparisonCounterexampleA1,
      householderTrailingActiveVector, householderActiveVector,
      householderTrailingPart]
/-- The stored scalar-comparison counterexample is diagonally dominant at every
    displayed leading block consumed by the active branch. -/
theorem storedDiagDominantComparisonCounterexample_diagDominant :
    ∀ k (hk : k < 2),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
          (Nat.succ_le_iff.mpr hk) hk) := by
  intro k hk
  interval_cases k
  · refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      fin_cases j
      omega
    · intro i
      fin_cases i
      norm_num [storedDiagDominantComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i
      fin_cases j
      omega
  · refine ⟨?_, ?_, ?_⟩
    · intro i j hji
      fin_cases i
      · fin_cases j
        · norm_num at hji
        · norm_num at hji
      · fin_cases j
        · norm_num [storedDiagDominantComparisonCounterexampleSeq,
            storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
        · norm_num at hji
    · intro i
      fin_cases i <;>
        norm_num [storedDiagDominantComparisonCounterexampleSeq,
          storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
    · intro i j hij
      fin_cases i <;> fin_cases j <;>
        first
        | omega
        | norm_num [storedDiagDominantComparisonCounterexampleSeq,
            storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
            qrLeadingRow, qrLeadingColumn]
/-- The stored diagonally-dominant scalar-comparison witness also satisfies the
    finite active max-pivot policy.  This keeps the later route elimination on
    the active-pivot branch, rather than only on an unpivoted stored surface. -/
theorem storedDiagDominantComparisonCounterexample_activeMaxPivotChoice :
    ∀ k (hk : k < 2),
      (⟨k, hk⟩ : Fin 2) =
        householderActiveMaxPivotColumn
          (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
          (⟨k, hk⟩ : Fin 2)
          (storedDiagDominantComparisonCounterexampleSeq k) := by
  intro k hk
  interval_cases k
  · let q : Fin 2 :=
      householderActiveMaxPivotColumn
        (⟨0, by norm_num⟩ : Fin 2) (⟨0, by norm_num⟩ : Fin 2)
        (storedDiagDominantComparisonCounterexampleSeq 0)
    have hmax :=
      householderActiveMaxPivotColumn_pivot_max
        (p := (⟨0, by norm_num⟩ : Fin 2))
        (k := (⟨0, by norm_num⟩ : Fin 2))
        (A := storedDiagDominantComparisonCounterexampleSeq 0)
        (l := (⟨0, by norm_num⟩ : Fin 2)) (by norm_num)
    change
      householderTrailingColumnNorm2Sq
          (⟨0, by norm_num⟩ : Fin 2)
          (storedDiagDominantComparisonCounterexampleSeq 0)
          (⟨0, by norm_num⟩ : Fin 2) ≤
        householderTrailingColumnNorm2Sq
          (⟨0, by norm_num⟩ : Fin 2)
          (storedDiagDominantComparisonCounterexampleSeq 0) q at hmax
    have hq_ne_one : q.val ≠ 1 := by
      intro hq
      have hqeq : q = (⟨1, by norm_num⟩ : Fin 2) := Fin.ext hq
      rw [hqeq] at hmax
      norm_num [storedDiagDominantComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0,
        householderTrailingColumnNorm2Sq, householderTrailingNorm2Sq,
        householderTrailingPart, vecNorm2Sq] at hmax
    apply Fin.ext
    have hq_lt : q.val < 2 := q.isLt
    interval_cases q.val
    · rfl
    · exact False.elim (hq_ne_one rfl)
  · let q : Fin 2 :=
      householderActiveMaxPivotColumn
        (⟨1, by norm_num⟩ : Fin 2) (⟨1, by norm_num⟩ : Fin 2)
        (storedDiagDominantComparisonCounterexampleSeq 1)
    have hge :=
      householderActiveMaxPivotColumn_ge
        (p := (⟨1, by norm_num⟩ : Fin 2))
        (k := (⟨1, by norm_num⟩ : Fin 2))
        (A := storedDiagDominantComparisonCounterexampleSeq 1)
    apply Fin.ext
    have hgeq : 1 ≤ q.val := by
      simpa [q] using hge
    exact Nat.le_antisymm hgeq (Nat.le_of_lt_succ q.isLt)
/-- Even on the stored-recursive diagonally-dominant surface, an oversized
    uniform stage budget can violate the scalar diagonal lower-bound defect. -/
theorem storedDiagDominantComparisonCounterexample_stageDiagLowerDefectBudget_pos :
    0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq (fun _ => (4 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageDiagLowerDefect_le_budget
      (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq
      (fun _ => (4 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (4 : ℝ) -
          |qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq 1)
            (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
            (by norm_num : 1 < 2) i i| = 1 := by
    norm_num [i, storedDiagDominantComparisonCounterexampleSeq,
      storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
      qrLeadingRow, qrLeadingColumn]
    rfl
  have hbudget :
      (1 : ℝ) ≤
        storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
          storedDiagDominantComparisonCounterexampleSeq (fun _ => (4 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- Even on the stored-recursive diagonally-dominant surface, the scalar
    stage-budget/row-max comparison can remain positive. -/
theorem storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos :
    0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq (fun _ => (3 : ℝ)) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget
      (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq
      (fun _ => (3 : ℝ))
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      (3 : ℝ) -
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
            storedDiagDominantComparisonCounterexampleSeq 1
            (by norm_num) i = 1 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock
                (storedDiagDominantComparisonCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, storedDiagDominantComparisonCounterexampleSeq,
        storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    rw [hsup]
    norm_num
  have hbudget :
      (1 : ℝ) ≤
        storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
          storedDiagDominantComparisonCounterexampleSeq (fun _ => (3 : ℝ)) := by
    simpa [hentry] using hle
  linarith
/-- Stored-recurrence actual-unit route elimination with diagonal dominance:
    even adding local diagonal dominance to the stored-panel actual-unit data
    does not force the scalar stage-diagonal lower-bound defect. -/
theorem not_forall_diagDominant_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageDiagLowerDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 4
  have hnonpos :
      storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleAlpha stageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      (by intro k; norm_num [stageBudget])
  have hpos :
      0 < storedQRStageDiagLowerDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      storedDiagDominantComparisonCounterexample_stageDiagLowerDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Stored-recurrence actual-unit route elimination with diagonal dominance:
    even adding local diagonal dominance to the stored-panel actual-unit data
    does not force the scalar stage-budget/row-max comparison defect. -/
theorem not_forall_diagDominant_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleAlpha stageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      (by intro k; norm_num [stageBudget])
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Active-pivot stored-recurrence route elimination with diagonal dominance:
    even after adding the finite active max-pivot policy to the stored-panel
    actual-unit data, the scalar stage-budget/row-max comparison defect is not
    forced. -/
theorem not_forall_diagDominant_activeMaxPivot_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleAlpha stageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      (by intro k; norm_num [stageBudget])
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Source-shaped off-diagonal control supplies a displayed row-budget
    certificate by choosing each row budget to be the finite maximum of the
    displayed strict-upper entries in that row.

This is a safe-direction bridge: it does not derive off-diagonal domination
from Cox--Higham row-growth estimates.  Instead, once the stronger source
off-diagonal-control field is available, it packages it into the row-budget
certificate expected by the row-growth route. -/
theorem StoredQRDisplayedRowBudgetControl.of_sourceOffDiagonalControl_rowMaxBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hoff : StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k hk i => qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i) := by
  refine ⟨?_, ?_⟩
  · intro k hk i j hij
    exact qrLeadingStrictUpperRowMaxBudget_entry_le hmn A_hat hk hij
  · intro k hk i hi
    exact
      qrLeadingStrictUpperRowMaxBudget_le_diag_of_offdiag
        hmn A_hat hk hi
        (fun j hij =>
          hoff.leadingBlock_offdiag_le_diag k hk i j hij)
/-- Build the displayed row-budget control certificate from signed-stage
    Cox--Higham row-growth budgets.

The signed-stage row-growth theorem supplies the strict-upper-entry budget
field.  The diagonal lower-bound field remains the honest source/domain
obligation, restricted to rows that can actually have strict upper entries.
The pivot-column zeroing field is derived from the same norm-square
nonbreakdown budget used elsewhere in the stored-QR source-control route. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_budgets_factor_of_normSqBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ) (c : ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (entryBudget :
      ∀ k (_hk : k < n), ∀ i j : Fin (k + 1), i.val < j.val → ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        entryBudget k hk i j hij 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * entryBudget k hk i j hij t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ entryBudget k hk i j hij (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * entryBudget k hk i j hij t)
    (hrowBudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ hij : i.val < j.val,
      entryBudget k hk i j hij (qrLeadingOffdiagStop j) ≤
        rowBudget k hk i)
    (hdiagLower : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        rowBudget k hk i ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|) :
    StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget := by
  refine ⟨?_, hdiagLower⟩
  have hStepStored : ∀ k (_hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (storedQRSignedStageVector hmn A_hat alpha k)
          (storedQRSignedStageBeta hmn A_hat alpha k)
          (A_hat k) := by
    intro k hk
    simpa [storedQRSignedStageVector, storedQRSignedStageBeta, hk] using
      hStepA k hk
  exact
    fl_householderStoredPanel_sequence_leadingBlock_offdiag_budget_of_signed_stage_budgets_factor
      hmn fp A_hat alpha c rowBudget entryBudget hm hStepStored hinit
      (storedQRSignedStage_pivot_zeroing_field_of_normSqBudget
        hmn fp A_hat alpha hAlphaDef hbudgetNormSq)
      hbudget hexact hrowBudget
/-- Build the displayed row-budget control certificate from one monotone
    signed-stage Cox--Higham budget sequence.

This specializes the row-budget package to `rowBudget k i = stageBudget k`.
The stop-time inequality for displayed upper entries supplies the terminal
budget domination field from monotonicity, so the remaining source/domain field
is exactly the offdiag-row diagonal lower bound
`stageBudget k <= |(S_k)_{ii}|`. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_factor_of_normSqBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ) (c : ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hinit : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |A_hat 0
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          (qrLeadingColumn n k hk j)| ≤
        stageBudget 0)
    (hbudget : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        c * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
          ≤ stageBudget (t + 1))
    (hexact : ∀ k (hk : k < n), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      ∀ t : ℕ, t < qrLeadingOffdiagStop j →
        |matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a (qrLeadingColumn n k hk j))
          (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)| ≤
          c * stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hdiagLower : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget k) := by
  refine
    StoredQRDisplayedRowBudgetControl.of_signed_stage_budgets_factor_of_normSqBudget
      hmn fp A_hat alpha c
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget k)
      (fun _k (_hk : _k < n) (_i _j : Fin (_k + 1))
        (_hij : _i.val < _j.val) t => stageBudget t)
      hm hStepA hAlphaDef hbudgetNormSq ?_ ?_ ?_ ?_ hdiagLower
  · intro k hk i j hij
    simpa using hinit k hk i j hij
  · intro k hk i j hij t ht
    simpa using hbudget k hk i j hij t ht
  · intro k hk i j hij t ht
    simpa using hexact k hk i j hij t ht
  · intro k hk i j hij
    simpa using hBudget_mono (qrLeadingOffdiagStop j) k (qrLeadingOffdiagStop_le j)
/-- Source-control handoff from the packaged displayed row-budget certificate.

    This is a theorem-statement correction for the Cox--Higham route: the
    diagonal lower-bound/nonbreakdown field is deliberately kept as a named
    source/domain certificate instead of being hidden among scalar arguments. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudget_product_of_offdiag_rows
      hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hrowControl.offdiag_le hrowControl.diag_lower
      hproduct
/-- The diagonally-dominant scalar-comparison witness has zero compact-update
    relative budget in exact arithmetic with a zero RHS trace. -/
theorem storedDiagDominantComparisonCounterexample_compactSequenceRelativeBudget_eq_zero :
    storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha = 0 := by
  norm_num [storedQRCompactSequenceRelativeBudget,
    storedQRCompactStepRelativeBudget,
    householderCompactPanelRelativeBudget,
    householderCompactRelativeBudget,
    householderCompactNormBudget,
    householderCompactComponentBudget,
    householderAbsDotBudget,
    storedDiagDominantComparisonCounterexampleSeq,
    activeMaxPivotRowMaxComparisonCounterexampleA0,
    storedDiagDominantComparisonCounterexampleA1,
    storedDiagDominantComparisonCounterexampleAlpha,
    exactHouseholderQRDiagDominanceCounterexampleFP,
    FPModel.exactWithUnitRoundoff,
    gamma,
    storedDiagDominantComparisonCounterexampleRhsSeq,
    storedQRSignedStageVector,
    storedQRSignedStageBeta,
    householderTrailingActiveVector,
    householderActiveVector,
    householderTrailingPart,
    householderBetaSpec]
  simp [vecNorm2_zero]
/-- The diagonally-dominant scalar-comparison witness satisfies the global
    compact-product smallness condition in the exact/zero-RHS regime. -/
theorem storedDiagDominantComparisonCounterexample_compactSequenceProductBudget_lt_one :
    storedQRCompactSequenceProductBudget (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha < 1 := by
  exact
    storedQRCompactSequenceProductBudget_lt_one_of_relativeBudget_eq_zero
      (by omega : 2 ≤ 2)
      exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexample_compactSequenceRelativeBudget_eq_zero
/-- Active-pivot stored-recurrence route elimination with diagonal dominance
    and compact-product smallness: even after adding the finite active
    max-pivot policy, stored RHS recurrence, and global compact-product
    inequality, the scalar stage-budget/row-max comparison defect is not
    forced. -/
theorem not_forall_diagDominant_activeMaxPivot_product_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      storedQRCompactSequenceProductBudget (by omega : 2 ≤ 2)
        fp A_hat b_hat alpha < 1 →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha stageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_compactSequenceProductBudget_lt_one
      (by intro k; norm_num [stageBudget])
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Source-control handoff from the packaged displayed row-budget certificate
    with the compact-product side condition supplied by the finite global
    product budget.

This is the row-budget-control analogue of the active/prefix global-product
route: the caller supplies one scalar condition
`storedQRCompactSequenceProductBudget < 1`, and the finite-maximum adapter
turns it into the per-pivot compact-product inequalities consumed by the
source-control theorem. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  classical
  refine
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product
      hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
      hbudgetNormSq hdetLead hrowControl ?_
  intro k hk
  let kk : Fin n := ⟨k, hk⟩
  have hle :
      storedQRCompactSequenceProductExpr hmn fp A_hat b_hat alpha kk ≤
        storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha :=
    storedQRCompactSequenceProductExpr_le_budget hmn fp A_hat b_hat alpha kk
  exact lt_of_le_of_lt (by
    simpa [storedQRCompactSequenceProductExpr, kk] using hle) hglobalProduct
/-- Source-control handoff from a uniform row-max-to-diagonal contraction.

This is the route-1 invariant form for the rectangular QR/preconditioner
bottleneck.  The computed loop may supply a scalar contraction `ρ ≤ 1` saying
that every displayed strict-upper row maximum is bounded by `ρ` times the
matching displayed diagonal.  The finite row-max bridge turns that invariant
into the packaged row-budget certificate, and the existing finite global
compact-product adapter supplies the compact-product side condition. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (ρ : ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hρ : ρ ≤ 1)
    (hrowMaxContract : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤
        ρ *
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
      hmn fp A_hat b_hat alpha
      (fun k hk i => qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i)
      hm hStepA hAlphaDef hbudgetNormSq hdetLead
      (StoredQRDisplayedRowBudgetControl.of_rowMaxBudget_le_diag_factor
        hmn A_hat hρ hrowMaxContract)
      hglobalProduct
/-- Stored trailing Householder QR source control from a scalar finite
    row-max/diagonal defect budget.

This is the scalar-budget sibling of
`..._rowMaxBudgetFactor_globalProduct`: the route-1 contraction field is
packaged as the single check `storedQRRowMaxDiagDefectBudget <= 0`, then the
existing row-max global-product handoff is reused with `ρ = 1`. -/
theorem StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hdefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    StoredQRSourceOffDiagonalControl hmn fp A_hat b_hat alpha := by
  exact
    StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
      hmn fp A_hat b_hat alpha
      (fun k hk i => qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i)
      hm hStepA hAlphaDef hbudgetNormSq hdetLead
      (StoredQRDisplayedRowBudgetControl.of_rowMaxDiagDefectBudget_nonpos
        hmn A_hat hdefect)
      hglobalProduct
/-- The diagonally-dominant scalar-comparison witness also satisfies the
    canonical finite-max compact-product scalar, because its exact/zero-RHS
    relative compact budget is zero. -/
theorem storedDiagDominantComparisonCounterexample_finiteMaxSmallness :
    2 * storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2)
          storedDiagDominantComparisonCounterexampleSeq *
        ((2 : ℝ) *
          (storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
            exactHouseholderQRDiagDominanceCounterexampleFP
            storedDiagDominantComparisonCounterexampleSeq
            storedDiagDominantComparisonCounterexampleRhsSeq
            storedDiagDominantComparisonCounterexampleAlpha *
            storedQRPivotColumnNormBudget (by omega : 2 ≤ 2)
              storedDiagDominantComparisonCounterexampleSeq) ^ 2) <
      1 := by
  rw [storedDiagDominantComparisonCounterexample_compactSequenceRelativeBudget_eq_zero]
  norm_num
/-- Active-pivot stored-recurrence route elimination with diagonal dominance
    and canonical finite-max compact-product smallness: even the finite-max
    scalar used to derive the active branch's global product field does not
    force the scalar stage-budget/row-max comparison defect. -/
theorem not_forall_diagDominant_activeMaxPivot_finiteMaxSmallness_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      2 * storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2) A_hat *
          ((2 : ℝ) *
            (storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
              fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget (by omega : 2 ≤ 2) A_hat) ^ 2) <
        1 →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  let stageBudget : ℕ → ℝ := fun _ => 3
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha stageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_finiteMaxSmallness
      (by intro k; norm_num [stageBudget])
  have hpos :
      0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq stageBudget := by
    simpa [stageBudget] using
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos
  exact not_lt_of_ge hnonpos hpos
/-- Nonconstant stage budget for the signed global-compact-budget
    scalar-comparison counterexample.

The first stage budget is zero so the Cox--Higham recurrence can grow into the
oversized stage-one budget `3`; later budgets are set to `10`, enough for the
second displayed recurrence in dimension two. -/
def storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget :
    ℕ → ℝ
  | 0 => 0
  | 1 => 3
  | _ => 10
/-- The nonconstant signed-stage counterexample budget is nonnegative. -/
theorem storedDiagDominantComparisonCounterexample_globalBudgetStageBudget_nonneg :
    ∀ k : ℕ,
      0 ≤ storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget k := by
  intro k
  rcases k with _ | k
  · norm_num [storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget]
  rcases k with _ | k
  · norm_num [storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget]
  norm_num [storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget]
/-- The signed finite global compact-update budget vanishes for the exact
    diagonally-dominant scalar-comparison witness. -/
theorem storedDiagDominantComparisonCounterexample_globalCompactBudget_eq_zero :
    ∀ t (ht : t < 2),
      storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
        exactHouseholderQRDiagDominanceCounterexampleFP
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleAlpha t ht = 0 := by
  intro t ht
  interval_cases t <;>
    norm_num [storedQRSignedStageGlobalCompactBudget,
      householderCompactComponentBudget, householderAbsDotBudget,
      storedDiagDominantComparisonCounterexampleSeq,
      activeMaxPivotRowMaxComparisonCounterexampleA0,
      storedDiagDominantComparisonCounterexampleA1,
      storedDiagDominantComparisonCounterexampleAlpha,
      exactHouseholderQRDiagDominanceCounterexampleFP,
      FPModel.exactWithUnitRoundoff, gamma,
      storedQRSignedStageVector, storedQRSignedStageBeta,
      householderTrailingActiveVector, householderActiveVector,
      householderTrailingPart, householderBetaSpec]
/-- In the two-row counterexample, the unified Cox--Higham active-row growth
    factor is bounded by `3`. -/
theorem storedDiagDominantComparisonCounterexample_coxHighamActiveRowGrowthFactor_two_le_three :
    coxHighamActiveRowGrowthFactor 2 ≤ 3 := by
  unfold coxHighamActiveRowGrowthFactor
  have hsqrt2_le_two : Real.sqrt (2 : ℝ) ≤ 2 := by
    nlinarith [Real.sq_sqrt (by norm_num : (0 : ℝ) ≤ 2),
      Real.sqrt_nonneg (2 : ℝ)]
  have hsqrt_nat_le_two : Real.sqrt ((2 : ℕ) : ℝ) ≤ 2 := by
    simpa using hsqrt2_le_two
  exact max_le (by linarith) (by linarith)
/-- The exact diagonally-dominant scalar-comparison witness satisfies the
    signed-stage global compact-budget recurrence with the nonconstant budget
    `0, 3, 10, ...`. -/
theorem storedDiagDominantComparisonCounterexample_globalCompactBudget_recurrence :
    ∀ t (ht : t < 2),
      coxHighamActiveRowGrowthFactor 2 *
            storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget t +
          storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
            exactHouseholderQRDiagDominanceCounterexampleFP
            storedDiagDominantComparisonCounterexampleSeq
            storedDiagDominantComparisonCounterexampleAlpha t ht ≤
        storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget
          (t + 1) := by
  intro t ht
  rw [storedDiagDominantComparisonCounterexample_globalCompactBudget_eq_zero t ht]
  interval_cases t
  · norm_num [storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget]
  · have hfactor : coxHighamActiveRowGrowthFactor 2 ≤ 3 :=
      storedDiagDominantComparisonCounterexample_coxHighamActiveRowGrowthFactor_two_le_three
    norm_num [storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget]
    nlinarith
/-- The signed-stage global-budget witness still has a positive scalar
    stage-budget/row-max comparison defect. -/
theorem storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_globalBudgetStageBudget :
    0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget
      (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget 1 -
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
            storedDiagDominantComparisonCounterexampleSeq 1
            (by norm_num) i = 1 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock
                (storedDiagDominantComparisonCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, storedDiagDominantComparisonCounterexampleSeq,
        storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    rw [hsup]
    norm_num [storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget]
    rfl
  have hbudget :
      (1 : ℝ) ≤
        storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget := by
    simpa [hentry] using hle
  linarith
/-- The exact diagonally-dominant scalar-comparison witness also satisfies the
    row-max/diagonal finite defect scalar. -/
theorem storedDiagDominantComparisonCounterexample_rowMaxDiagDefectBudget_nonpos :
    storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq ≤ 0 :=
  storedQRRowMaxDiagDefectBudget_nonpos_of_diagDominant
    (by omega : 2 ≤ 2)
    storedDiagDominantComparisonCounterexampleSeq
    storedDiagDominantComparisonCounterexample_diagDominant
/-- The exact diagonally-dominant scalar-comparison witness has nonsingular
    displayed leading blocks at every active stage. -/
theorem storedDiagDominantComparisonCounterexample_leadingBlock_det_ne_zero :
    ∀ k (hk : k < 2),
      Matrix.det
        (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
          (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
  intro k hk
  interval_cases k
  · have hblock :
        (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq 0)
            (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (0 + 1)) (Fin (0 + 1)) ℝ) =
          (fun _ _ => (3 : ℝ)) := by
      ext i j
      fin_cases i
      fin_cases j
      norm_num [storedDiagDominantComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    rw [hblock, Matrix.det_fin_one]
    norm_num
  · have hblock :
        (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq 1)
            (Nat.succ_le_iff.mpr hk) hk :
          Matrix (Fin (1 + 1)) (Fin (1 + 1)) ℝ) =
          storedDiagDominantComparisonCounterexampleA1 := by
      ext i j
      fin_cases i <;> fin_cases j <;>
        norm_num [storedDiagDominantComparisonCounterexampleSeq,
          storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
          qrLeadingRow, qrLeadingColumn]
    rw [hblock]
    rw [Matrix.det_fin_two]
    norm_num [storedDiagDominantComparisonCounterexampleA1]
/-- Active-pivot stored-recurrence route elimination with diagonal dominance,
    signed-stage global compact-budget recurrence, and canonical finite-max
    compact-product smallness: even these strengthened stage-budget fields do
    not force the scalar stage-budget/row-max comparison defect. -/
theorem not_forall_diagDominant_activeMaxPivot_globalCompactBudget_finiteMaxSmallness_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      (∀ t (ht : t < 2),
        coxHighamActiveRowGrowthFactor 2 * stageBudget t +
            storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
              fp A_hat alpha t ht ≤
          stageBudget (t + 1)) →
      2 * storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2) A_hat *
          ((2 : ℝ) *
            (storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
              fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget (by omega : 2 ≤ 2) A_hat) ^ 2) <
        1 →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_globalCompactBudget_recurrence
      storedDiagDominantComparisonCounterexample_finiteMaxSmallness
      storedDiagDominantComparisonCounterexample_globalBudgetStageBudget_nonneg
  exact
    not_lt_of_ge hnonpos
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_globalBudgetStageBudget
/-- Row-max-granted active-pivot stored-recurrence route elimination with
    diagonal dominance, signed-stage global compact-budget recurrence, and
    canonical finite-max compact-product smallness: the row-max/diagonal defect
    scalar can hold at the same witness, so the missing comparison scalar still
    cannot be inferred from these stronger signed-stage fields. -/
theorem not_forall_diagDominant_activeMaxPivot_globalCompactBudget_finiteMaxSmallness_rowMaxDiagDefect_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      (∀ t (ht : t < 2),
        coxHighamActiveRowGrowthFactor 2 * stageBudget t +
            storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
              fp A_hat alpha t ht ≤
          stageBudget (t + 1)) →
      2 * storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2) A_hat *
          ((2 : ℝ) *
            (storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
              fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget (by omega : 2 ≤ 2) A_hat) ^ 2) <
        1 →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
      storedDiagDominantComparisonCounterexample_globalCompactBudget_recurrence
      storedDiagDominantComparisonCounterexample_finiteMaxSmallness
      storedDiagDominantComparisonCounterexample_globalBudgetStageBudget_nonneg
  exact
    not_lt_of_ge hnonpos
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_globalBudgetStageBudget
/-- Determinant- and row-max-granted active-pivot stored-recurrence route
    elimination with diagonal dominance, signed-stage global compact-budget
    recurrence, and canonical finite-max compact-product smallness: even
    nonsingular displayed leading blocks plus the row-max/diagonal defect scalar
    do not force the missing scalar stage-budget/row-max comparison defect. -/
theorem not_forall_diagDominant_activeMaxPivot_leadingBlock_det_ne_zero_globalCompactBudget_finiteMaxSmallness_rowMaxDiagDefect_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha : ℕ → ℝ)
      (stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      (∀ t (ht : t < 2),
        coxHighamActiveRowGrowthFactor 2 * stageBudget t +
            storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
              fp A_hat alpha t ht ≤
          stageBudget (t + 1)) →
      2 * storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2) A_hat *
          ((2 : ℝ) *
            (storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
              fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget (by omega : 2 ≤ 2) A_hat) ^ 2) <
        1 →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_leadingBlock_det_ne_zero
      storedDiagDominantComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
      storedDiagDominantComparisonCounterexample_globalCompactBudget_recurrence
      storedDiagDominantComparisonCounterexample_finiteMaxSmallness
      storedDiagDominantComparisonCounterexample_globalBudgetStageBudget_nonneg
  exact
    not_lt_of_ge hnonpos
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_globalBudgetStageBudget
/-- Final-surface stage budget for the diagonally-dominant
    scalar-comparison counterexample.

Unlike the smaller global-budget witness `0, 3, 10, ...`, this budget starts at
`3` so the final active/prefix route's initial row-budget fields also hold. -/
def storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget :
    ℕ → ℝ
  | 0 => 3
  | 1 => 10
  | _ => 30
/-- The final-surface counterexample budget is nonnegative. -/
theorem storedDiagDominantComparisonCounterexample_finalSurfaceStageBudget_nonneg :
    ∀ k : ℕ,
      0 ≤ storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget k := by
  intro k
  rcases k with _ | k
  · norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
  rcases k with _ | k
  · norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
  norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
/-- The final-surface counterexample budget is monotone. -/
theorem storedDiagDominantComparisonCounterexample_finalSurfaceStageBudget_mono :
    ∀ a b : ℕ, a ≤ b →
      storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget a ≤
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget b := by
  intro a b hab
  rcases a with _ | a
  · rcases b with _ | b
    · norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
    rcases b with _ | b
    · norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
    norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
  rcases a with _ | a
  · rcases b with _ | b
    · omega
    rcases b with _ | b
    · norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
    norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
  rcases b with _ | b
  · omega
  rcases b with _ | b
  · omega
  norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
/-- The final-surface counterexample budget bounds the initial full active
    block. -/
theorem storedDiagDominantComparisonCounterexample_finalSurface_initBlock :
    ∀ r : Fin 2, ∀ l : Fin 2,
      |storedDiagDominantComparisonCounterexampleSeq 0 r l| ≤
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget 0 := by
  intro r l
  fin_cases r <;> fin_cases l <;>
    norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget,
      storedDiagDominantComparisonCounterexampleSeq,
      activeMaxPivotRowMaxComparisonCounterexampleA0]
/-- The final-surface counterexample budget bounds every initial displayed
    strict-upper entry used by the active/prefix route. -/
theorem storedDiagDominantComparisonCounterexample_finalSurface_init :
    ∀ k (hk : k < 2), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
      |storedDiagDominantComparisonCounterexampleSeq 0
          (qrLeadingRow 2 k (Nat.succ_le_iff.mpr
            (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) i)
          (qrLeadingColumn 2 k hk j)| ≤
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget 0 := by
  intro k hk i j hij
  interval_cases k
  · fin_cases i
    fin_cases j
    · norm_num at hij
  · fin_cases i <;> fin_cases j <;>
      norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget,
        storedDiagDominantComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0,
        qrLeadingRow, qrLeadingColumn]
/-- The final-surface counterexample budget satisfies the signed-stage global
    compact-budget recurrence. -/
theorem storedDiagDominantComparisonCounterexample_finalSurface_globalCompactBudget_recurrence :
    ∀ t (ht : t < 2),
      coxHighamActiveRowGrowthFactor 2 *
            storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget t +
          storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
            exactHouseholderQRDiagDominanceCounterexampleFP
            storedDiagDominantComparisonCounterexampleSeq
            storedDiagDominantComparisonCounterexampleAlpha t ht ≤
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget
          (t + 1) := by
  intro t ht
  rw [storedDiagDominantComparisonCounterexample_globalCompactBudget_eq_zero t ht]
  interval_cases t
  · have hfactor : coxHighamActiveRowGrowthFactor 2 ≤ 3 :=
      storedDiagDominantComparisonCounterexample_coxHighamActiveRowGrowthFactor_two_le_three
    norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
    nlinarith
  · have hfactor : coxHighamActiveRowGrowthFactor 2 ≤ 3 :=
      storedDiagDominantComparisonCounterexample_coxHighamActiveRowGrowthFactor_two_le_three
    norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
    nlinarith
/-- The final-surface budget still has a positive scalar stage-budget/row-max
    comparison defect. -/
theorem storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_finalSurfaceStageBudget :
    0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget
      (by omega : 2 ≤ 2)
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hentry :
      storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget 1 -
          qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
            storedDiagDominantComparisonCounterexampleSeq 1
            (by norm_num) i = 8 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock
                (storedDiagDominantComparisonCounterexampleSeq 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      norm_num [i, storedDiagDominantComparisonCounterexampleSeq,
        storedDiagDominantComparisonCounterexampleA1, qrLeadingBlock,
        qrLeadingRow, qrLeadingColumn]
    rw [hsup]
    norm_num [storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget]
    rfl
  have hbudget :
      (8 : ℝ) ≤
        storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget := by
    simpa [hentry] using hle
  linarith
/-- The exact diagonally-dominant scalar-comparison witness has zero compact
    component budget at every active stage. -/
theorem storedDiagDominantComparisonCounterexample_compactComponentBudget_eq_zero :
    ∀ k (hk : k < 2),
      householderCompactComponentBudget
        exactHouseholderQRDiagDominanceCounterexampleFP 2
        (householderTrailingActiveVector 2
          ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
          (fun a => storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
          (storedDiagDominantComparisonCounterexampleAlpha k))
        (householderBetaSpec 2
          (householderTrailingActiveVector 2
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
            (fun a => storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
            (storedDiagDominantComparisonCounterexampleAlpha k)))
        (fun a => storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
        ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ = 0 := by
  intro k hk
  interval_cases k <;>
    norm_num [householderCompactComponentBudget, householderAbsDotBudget,
      gamma, exactHouseholderQRDiagDominanceCounterexampleFP,
      FPModel.exactWithUnitRoundoff,
      storedDiagDominantComparisonCounterexampleSeq,
      activeMaxPivotRowMaxComparisonCounterexampleA0,
      storedDiagDominantComparisonCounterexampleA1,
      storedDiagDominantComparisonCounterexampleAlpha,
      householderTrailingActiveVector, householderActiveVector,
      householderTrailingPart, householderBetaSpec]
/-- Exact local `κ∞` budget for the diagonally-dominant
    scalar-comparison witness. -/
noncomputable def storedDiagDominantComparisonCounterexampleKappaBudget
    (k : ℕ) : ℝ :=
  if hk : k < 2 then
    kappaInf (k + 1) (Nat.succ_pos k)
      (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
        (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)
      (nonsingInv (k + 1)
        (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk))
  else 0
/-- Witness-side `K` budget for the local `κ∞`/self-norm branch, chosen as
    the exact displayed expression plus one. -/
noncomputable def storedDiagDominantComparisonCounterexampleKappaNormSqBudget
    (k : ℕ) : ℝ :=
  if hk : k < 2 then
    ((k + 1 : ℕ) : ℝ) *
        (storedDiagDominantComparisonCounterexampleKappaBudget k /
          infNorm
            (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
              (Nat.succ_le_iff.mpr
                (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ^ 2 +
      1
  else 1
/-- The witness `κ∞` budget bounds its exact local condition expression. -/
theorem storedDiagDominantComparisonCounterexample_kappaBudget_le :
    ∀ k (hk : k < 2),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ≤
        storedDiagDominantComparisonCounterexampleKappaBudget k := by
  intro k hk
  unfold storedDiagDominantComparisonCounterexampleKappaBudget
  rw [dif_pos hk]
/-- The witness local `K` budget is strictly positive. -/
theorem storedDiagDominantComparisonCounterexample_kappaNormSqBudget_pos :
    ∀ k (_hk : k < 2),
      0 < storedDiagDominantComparisonCounterexampleKappaNormSqBudget k := by
  intro k hk
  unfold storedDiagDominantComparisonCounterexampleKappaNormSqBudget
  rw [dif_pos hk]
  have hnonneg :
      0 ≤
        ((k + 1 : ℕ) : ℝ) *
          (storedDiagDominantComparisonCounterexampleKappaBudget k /
            infNorm
              (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ^ 2 := by
    exact mul_nonneg (Nat.cast_nonneg _) (sq_nonneg _)
  linarith
/-- The witness local `K` budget bounds the standard `κ∞`/self-norm squared
    expression. -/
theorem storedDiagDominantComparisonCounterexample_kappaNormSqBudget :
    ∀ k (hk : k < 2),
      ((k + 1 : ℕ) : ℝ) *
          (storedDiagDominantComparisonCounterexampleKappaBudget k /
            infNorm
              (qrLeadingBlock (storedDiagDominantComparisonCounterexampleSeq k)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ^ 2 ≤
        storedDiagDominantComparisonCounterexampleKappaNormSqBudget k := by
  intro k hk
  unfold storedDiagDominantComparisonCounterexampleKappaNormSqBudget
  rw [dif_pos hk]
  linarith
/-- The exact witness satisfies the local `κ∞`/self-norm dual compact-budget
    inequality because the compact component budget is zero. -/
theorem storedDiagDominantComparisonCounterexample_dualBudget :
    ∀ k (hk : k < 2),
      (2 : ℝ) *
          (householderCompactComponentBudget
            exactHouseholderQRDiagDominanceCounterexampleFP 2
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
              (storedDiagDominantComparisonCounterexampleAlpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun a => storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
                (storedDiagDominantComparisonCounterexampleAlpha k)))
            (fun a => storedDiagDominantComparisonCounterexampleSeq k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩) ^ 2 <
        1 / storedDiagDominantComparisonCounterexampleKappaNormSqBudget k := by
  intro k hk
  rw [storedDiagDominantComparisonCounterexample_compactComponentBudget_eq_zero k hk]
  simp
  exact storedDiagDominantComparisonCounterexample_kappaNormSqBudget_pos k hk
/-- Conditioning- and row-max-granted active-pivot stored-recurrence route
    elimination with diagonal dominance, signed-stage global compact-budget
    recurrence, and canonical finite-max compact-product smallness: even local
    `κ∞`/self-norm and dual compact-budget data do not force the missing scalar
    stage-budget/row-max comparison defect. -/
theorem not_forall_diagDominant_activeMaxPivot_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_globalCompactBudget_finiteMaxSmallness_rowMaxDiagDefect_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonnegStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha κ K stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (_hk : k < 2), 0 < K k) →
      (∀ k (hk : k < 2),
        kappaInf (k + 1) (Nat.succ_pos k)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)
            (nonsingInv (k + 1)
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ≤
          κ k) →
      (∀ k (hk : k < 2),
        ((k + 1 : ℕ) : ℝ) *
            (κ k /
              infNorm
                (qrLeadingBlock (A_hat k)
                  (Nat.succ_le_iff.mpr
                    (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ^ 2 ≤
          K k) →
      (∀ k (hk : k < 2),
        (2 : ℝ) *
            (householderCompactComponentBudget fp 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩) ^ 2 <
          1 / K k) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      (∀ t (ht : t < 2),
        coxHighamActiveRowGrowthFactor 2 * stageBudget t +
            storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
              fp A_hat alpha t ht ≤
          stageBudget (t + 1)) →
      2 * storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2) A_hat *
          ((2 : ℝ) *
            (storedQRCompactSequenceRelativeBudget (by omega : 2 ≤ 2)
              fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget (by omega : 2 ≤ 2) A_hat) ^ 2) <
        1 →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexampleKappaBudget
      storedDiagDominantComparisonCounterexampleKappaNormSqBudget
      storedDiagDominantComparisonCounterexampleGlobalBudgetStageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_leadingBlock_det_ne_zero
      storedDiagDominantComparisonCounterexample_kappaNormSqBudget_pos
      storedDiagDominantComparisonCounterexample_kappaBudget_le
      storedDiagDominantComparisonCounterexample_kappaNormSqBudget
      storedDiagDominantComparisonCounterexample_dualBudget
      storedDiagDominantComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
      storedDiagDominantComparisonCounterexample_globalCompactBudget_recurrence
      storedDiagDominantComparisonCounterexample_finiteMaxSmallness
      storedDiagDominantComparisonCounterexample_globalBudgetStageBudget_nonneg
  exact
    not_lt_of_ge hnonpos
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_globalBudgetStageBudget
/-- Final-surface route elimination for the scalar stage-budget/row-max
    comparison defect.

Even after granting the final active/prefix route's initial row-budget fields,
monotone stage budget, global compact-product smallness, local determinant,
local `κ∞`/dual-budget data, active pivoting, diagonal dominance, row-max defect
control, and signed-stage global compact-budget recurrence, the comparison
scalar is still independent. -/
theorem not_forall_diagDominant_activeMaxPivot_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_init_globalProduct_globalCompactBudget_rowMaxDiagDefect_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonneg_monoStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha κ K stageBudget : ℕ → ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (_hk : k < 2), 0 < K k) →
      (∀ k (hk : k < 2),
        kappaInf (k + 1) (Nat.succ_pos k)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)
            (nonsingInv (k + 1)
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ≤
          κ k) →
      (∀ k (hk : k < 2),
        ((k + 1 : ℕ) : ℝ) *
            (κ k /
              infNorm
                (qrLeadingBlock (A_hat k)
                  (Nat.succ_le_iff.mpr
                    (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ^ 2 ≤
          K k) →
      (∀ k (hk : k < 2),
        (2 : ℝ) *
            (householderCompactComponentBudget fp 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩) ^ 2 <
          1 / K k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow 2 k (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) i)
            (qrLeadingColumn 2 k hk j)| ≤
          stageBudget 0) →
      (∀ r : Fin 2, ∀ l : Fin 2, |A_hat 0 r l| ≤ stageBudget 0) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      (∀ t (ht : t < 2),
        coxHighamActiveRowGrowthFactor 2 * stageBudget t +
            storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
              fp A_hat alpha t ht ≤
          stageBudget (t + 1)) →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      (∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b) →
      storedQRCompactSequenceProductBudget (by omega : 2 ≤ 2)
        fp A_hat b_hat alpha < 1 →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexampleKappaBudget
      storedDiagDominantComparisonCounterexampleKappaNormSqBudget
      storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_leadingBlock_det_ne_zero
      storedDiagDominantComparisonCounterexample_kappaNormSqBudget_pos
      storedDiagDominantComparisonCounterexample_kappaBudget_le
      storedDiagDominantComparisonCounterexample_kappaNormSqBudget
      storedDiagDominantComparisonCounterexample_dualBudget
      storedDiagDominantComparisonCounterexample_finalSurface_init
      storedDiagDominantComparisonCounterexample_finalSurface_initBlock
      storedDiagDominantComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
      storedDiagDominantComparisonCounterexample_finalSurface_globalCompactBudget_recurrence
      storedDiagDominantComparisonCounterexample_finalSurfaceStageBudget_nonneg
      storedDiagDominantComparisonCounterexample_finalSurfaceStageBudget_mono
      storedDiagDominantComparisonCounterexample_compactSequenceProductBudget_lt_one
  exact
    not_lt_of_ge hnonpos
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_finalSurfaceStageBudget
/-- The final-surface scalar-comparison witness also satisfies the canonical
    source-denominator rational-gamma finite-max scalar with `Ucap = 0`.

    Under exact arithmetic, the rational-gamma and Householder-factor cap terms
    collapse to zero, so the displayed source-denominator smallness inequality
    is independent of the finite maximum budgets. -/
theorem storedDiagDominantComparisonCounterexample_finalSurface_sourceDenURationalGammaCanonicalSmallness :
    (let Dcap := storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2)
          storedDiagDominantComparisonCounterexampleSeq
     let Ncap := storedQRPivotColumnNormBudget (by omega : 2 ≤ 2)
          storedDiagDominantComparisonCounterexampleSeq
     let Ucap : ℝ := 0
     let Gcap := ((2 : ℝ) * Ucap) / (1 - (2 : ℝ) * Ucap)
     let Fcap :=
        Ucap * (1 + Gcap) * (1 + Ucap) +
          Ucap * (1 + Gcap) +
          Gcap +
          Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
     2 * Dcap *
          ((2 : ℝ) *
            ((((2 : ℝ) * ((2 : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                Ncap) ^ 2)) <
        1) := by
  ring_nf
  norm_num
/-- Final-surface source-denominator route elimination.

    Even granting the final active/pivot/conditioning/global-budget surface and
    the canonical source-denominator rational-gamma finite-max scalar
    smallness, the scalar stage-budget/row-max comparison defect is not
    automatic.  The exact `2 x 2` witness satisfies those source-denominator
    cap fields with `Ucap = 0`, while its final-surface stage-one budget is
    still larger than the displayed strict-upper row maximum. -/
theorem not_forall_diagDominant_activeMaxPivot_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_init_sourceDenURationalGammaCanonical_globalCompactBudget_rowMaxDiagDefect_signedAlphaDef_stored_trailing_sequence_actualU_sourceDen_nonneg_monoStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀
      (fp : FPModel)
      (A_hat : ℕ → Fin 2 → Fin 2 → ℝ)
      (b_hat : ℕ → Fin 2 → ℝ)
      (alpha κ K stageBudget : ℕ → ℝ)
      (Ucap : ℝ),
      (∀ k (hk : k < 2),
        A_hat (k + 1) =
          fl_householderStoredPanelStep fp 2 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (A_hat k)) →
      (∀ k (hk : k < 2),
        b_hat (k + 1) =
          fl_householderStoredRhsStep fp 2 k
            (householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun i => A_hat k i ⟨k, hk⟩) (alpha k))
            (householderBetaSpec 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩) (alpha k)))
            (b_hat k)) →
      (∀ k (hk : k < 2),
        alpha k =
          signedHouseholderAlpha
            (Real.sqrt
              (householderTrailingNorm2Sq 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun i => A_hat k i ⟨k, hk⟩)))
            (A_hat k ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ ⟨k, hk⟩)) →
      (∀ k (hk : k < 2),
        (∑ i : Fin 2,
          householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i *
            householderTrailingActiveVector 2
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k) i) ≠ 0) →
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ k (hk : k < 2),
        (⟨k, hk⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨k, hk⟩ : Fin 2)
            (A_hat k)) →
      (∀ k (hk : k < 2),
        Matrix.det
          (qrLeadingBlock (A_hat k) (Nat.succ_le_iff.mpr hk) hk :
            Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0) →
      (∀ k (_hk : k < 2), 0 < K k) →
      (∀ k (hk : k < 2),
        kappaInf (k + 1) (Nat.succ_pos k)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)
            (nonsingInv (k + 1)
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ≤
          κ k) →
      (∀ k (hk : k < 2),
        ((k + 1 : ℕ) : ℝ) *
            (κ k /
              infNorm
                (qrLeadingBlock (A_hat k)
                  (Nat.succ_le_iff.mpr
                    (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) hk)) ^ 2 ≤
          K k) →
      (∀ k (hk : k < 2),
        (2 : ℝ) *
            (householderCompactComponentBudget fp 2
              (householderTrailingActiveVector 2
                ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec 2
                (householderTrailingActiveVector 2
                  ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk (by omega : 2 ≤ 2)⟩) ^ 2 <
          1 / K k) →
      (∀ k (hk : k < 2), ∀ i j : Fin (k + 1), ∀ _hij : i.val < j.val,
        |A_hat 0
            (qrLeadingRow 2 k (Nat.succ_le_iff.mpr
              (lt_of_lt_of_le hk (by omega : 2 ≤ 2))) i)
            (qrLeadingColumn 2 k hk j)| ≤
          stageBudget 0) →
      (∀ r : Fin 2, ∀ l : Fin 2, |A_hat 0 r l| ≤ stageBudget 0) →
      storedQRRowMaxDiagDefectBudget (by omega : 2 ≤ 2) A_hat ≤ 0 →
      (∀ t (ht : t < 2),
        coxHighamActiveRowGrowthFactor 2 * stageBudget t +
            storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
              fp A_hat alpha t ht ≤
          stageBudget (t + 1)) →
      (∀ k : ℕ, 0 ≤ stageBudget k) →
      (∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b) →
      0 ≤ Ucap →
      fp.u ≤ Ucap →
      (2 : ℝ) * Ucap < 1 →
      (let Dcap := storedQRDiagDominantInvFactorBudget (by omega : 2 ≤ 2) A_hat
       let Ncap := storedQRPivotColumnNormBudget (by omega : 2 ≤ 2) A_hat
       let Gcap := ((2 : ℝ) * Ucap) / (1 - (2 : ℝ) * Ucap)
       let Fcap :=
          Ucap * (1 + Gcap) * (1 + Ucap) +
            Ucap * (1 + Gcap) +
            Gcap +
            Ucap * (1 + Gcap) * (1 + Ucap) ^ 2
       2 * Dcap *
            ((2 : ℝ) *
              ((((2 : ℝ) * ((2 : ℝ) + 1) * (Ucap + 2 * Fcap)) *
                  Ncap) ^ 2)) <
          1) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        A_hat stageBudget ≤ 0) := by
  intro h
  have hnonpos :
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        storedDiagDominantComparisonCounterexampleSeq
        storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget ≤ 0 :=
    h exactHouseholderQRDiagDominanceCounterexampleFP
      storedDiagDominantComparisonCounterexampleSeq
      storedDiagDominantComparisonCounterexampleRhsSeq
      storedDiagDominantComparisonCounterexampleAlpha
      storedDiagDominantComparisonCounterexampleKappaBudget
      storedDiagDominantComparisonCounterexampleKappaNormSqBudget
      storedDiagDominantComparisonCounterexampleFinalSurfaceStageBudget
      (0 : ℝ)
      storedDiagDominantComparisonCounterexample_stored_step
      storedDiagDominantComparisonCounterexample_rhs_step
      storedDiagDominantComparisonCounterexample_signed_alpha_def
      storedDiagDominantComparisonCounterexample_den_ne_zero
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      storedDiagDominantComparisonCounterexample_diagDominant
      storedDiagDominantComparisonCounterexample_activeMaxPivotChoice
      storedDiagDominantComparisonCounterexample_leadingBlock_det_ne_zero
      storedDiagDominantComparisonCounterexample_kappaNormSqBudget_pos
      storedDiagDominantComparisonCounterexample_kappaBudget_le
      storedDiagDominantComparisonCounterexample_kappaNormSqBudget
      storedDiagDominantComparisonCounterexample_dualBudget
      storedDiagDominantComparisonCounterexample_finalSurface_init
      storedDiagDominantComparisonCounterexample_finalSurface_initBlock
      storedDiagDominantComparisonCounterexample_rowMaxDiagDefectBudget_nonpos
      storedDiagDominantComparisonCounterexample_finalSurface_globalCompactBudget_recurrence
      storedDiagDominantComparisonCounterexample_finalSurfaceStageBudget_nonneg
      storedDiagDominantComparisonCounterexample_finalSurfaceStageBudget_mono
      (by norm_num)
      (by
        norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
          FPModel.exactWithUnitRoundoff])
      (by norm_num)
      storedDiagDominantComparisonCounterexample_finalSurface_sourceDenURationalGammaCanonicalSmallness
  exact
    not_lt_of_ge hnonpos
      storedDiagDominantComparisonCounterexample_stageRowMaxComparisonDefectBudget_pos_finalSurfaceStageBudget
/-- Build the displayed row-budget control certificate from one monotone
    signed-stage budget sequence and a finite global compact-step budget.

This is the packaged-certificate version of the active/prefix Cox--Higham
route.  The finite global compact budget supplies the one-step row recurrences,
the active/prefix lemmas supply the exact same-reflector bounds, and the
remaining honest domain field is still the offdiag-row diagonal lower bound. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_of_normSqBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget k) := by
  classical
  have hcompleted : ∀ t (_ht : t < n), ∀ j : Fin n, j.val < t →
      ∀ i : Fin m,
        matMulVec m
          (householder m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t))
          (fun a => A_hat t a j) i = A_hat t i j :=
    storedQRSignedStage_completed_column_preservation
      hmn fp A_hat alpha hStepA
  have hbudgets :=
    storedQRSignedStage_active_prefix_budgets_of_globalCompactBudget
      hmn fp A_hat alpha stageBudget hglobalBudget
  have hactiveBlock :
      ∀ t (_ht : t < n), ∀ row : Fin m, t ≤ row.val →
        ∀ l : Fin n, t ≤ l.val → |A_hat t row l| ≤ stageBudget t :=
    storedQRSignedStage_active_block_bound_of_signed_stage_budget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef hbudgetNormSq
      hinitBlock hbudgets.1 hBudget_nonneg hpivotMax hcompleted
  have hprefixRows :
      ∀ t (_ht : t < n), ∀ row : Fin m, row.val < t →
        ∀ l : Fin n, t ≤ l.val → |A_hat t row l| ≤ stageBudget t :=
    storedQRSignedStage_prefix_row_bound_of_active_block_and_prefix_budget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef hbudgetNormSq
      hBudget_nonneg hpivotMax hactiveBlock hbudgets.2
  refine
    StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_factor_of_normSqBudget
      hmn fp A_hat alpha (coxHighamActiveRowGrowthFactor m) stageBudget
      hm hStepA hAlphaDef hbudgetNormSq ?_ ?_ ?_ hBudget_mono
      hBudget_diag
  · intro k hk i j _hij
    exact hinitBlock
      (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
      (qrLeadingColumn n k hk j)
  · intro k hk i j _hij t ht
    have ht_le_k : t ≤ k := by
      exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
    have htn : t < n := lt_of_le_of_lt ht_le_k hk
    have hcomp :
        householderCompactComponentBudget fp m
            (storedQRSignedStageVector hmn A_hat alpha t)
            (storedQRSignedStageBeta hmn A_hat alpha t)
            (fun a => A_hat t a (qrLeadingColumn n k hk j))
            (qrLeadingRow m k
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) ≤
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t htn :=
      storedQRSignedStage_compact_component_le_globalBudget
        hmn fp A_hat alpha t htn
        (qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i)
        (qrLeadingColumn n k hk j)
    have hstep :
        coxHighamActiveRowGrowthFactor m * stageBudget t +
            householderCompactComponentBudget fp m
              (storedQRSignedStageVector hmn A_hat alpha t)
              (storedQRSignedStageBeta hmn A_hat alpha t)
              (fun a => A_hat t a (qrLeadingColumn n k hk j))
              (qrLeadingRow m k
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i) ≤
          coxHighamActiveRowGrowthFactor m * stageBudget t +
            storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t htn := by
      simpa [add_comm] using
        add_le_add_left hcomp (coxHighamActiveRowGrowthFactor m * stageBudget t)
    exact hstep.trans (hglobalBudget t htn)
  · intro k hk i j _hij t ht
    let row : Fin m :=
      qrLeadingRow m k (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) i
    let col : Fin n := qrLeadingColumn n k hk j
    have ht_le_k : t ≤ k := by
      exact le_trans (Nat.le_of_lt ht) (qrLeadingOffdiagStop_le j)
    have htn : t < n := lt_of_le_of_lt ht_le_k hk
    have hcol : t ≤ col.val := by
      by_cases hjk : j.val < k
      · have htj : t ≤ j.val := by
          exact Nat.lt_succ_iff.mp
            (by simpa [col, qrLeadingOffdiagStop, hjk] using ht)
        simpa [col, qrLeadingColumn] using htj
      · have hle : j.val ≤ k := Nat.le_of_lt_succ j.isLt
        have heq : j.val = k := le_antisymm hle (le_of_not_gt hjk)
        have htk : t < k := by
          simpa [col, qrLeadingOffdiagStop, hjk, heq] using ht
        have htj : t ≤ j.val := by omega
        simpa [col, qrLeadingColumn] using htj
    have hnorm :
        0 <
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le htn hmn⟩
            (A_hat t) ⟨t, htn⟩ := by
      have hleft_nonneg :
          0 ≤
            (m : ℝ) *
              (householderCompactComponentBudget fp m
                (householderTrailingActiveVector m
                  ⟨t, lt_of_lt_of_le htn hmn⟩
                  (fun a => A_hat t a ⟨t, htn⟩) (alpha t))
                (householderBetaSpec m
                  (householderTrailingActiveVector m
                    ⟨t, lt_of_lt_of_le htn hmn⟩
                    (fun a => A_hat t a ⟨t, htn⟩) (alpha t)))
                (fun a => A_hat t a ⟨t, htn⟩)
                ⟨t, lt_of_lt_of_le htn hmn⟩) ^ 2 := by
        exact mul_nonneg (Nat.cast_nonneg m) (sq_nonneg _)
      have hpos := lt_of_le_of_lt hleft_nonneg (hbudgetNormSq t htn)
      simpa [householderTrailingColumnNorm2Sq] using hpos
    exact
      storedQRSignedStage_exact_same_reflector_bound_of_prefix_or_active_stage_bounds
        hmn A_hat alpha t htn row col (stageBudget t) (hAlphaDef t htn)
        hcol (hBudget_nonneg t)
        (fun hprefix => hprefixRows t htn row hprefix col hcol)
        hnorm
        (fun l hl => hpivotMax t htn l hl)
        (fun l hl => by
          by_cases hprefix : row.val < t
          · exact hprefixRows t htn row hprefix l hl
          · have hactive : t ≤ row.val := le_of_not_gt hprefix
            exact hactiveBlock t htn row hactive l hl)
        (fun r hr => hactiveBlock t htn r hr col hcol)
/-- Build the displayed row-budget control certificate from the same global
    compact-step recurrence, deriving norm-square nonbreakdown from the local
    `κ∞`/dual-budget route.

This is the row-budget-certificate counterpart of the solver-level
`..._kappaInf_selfNorm_dualBudget...` wrappers: the raw norm-square margin is
not a hidden hypothesis, but follows from the existing repository inverse
budget theorem. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_kappaInf_dualBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) l ≤
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget k) := by
  have hbudgetNormSq :
      ∀ k (hk : k < n),
        (m : ℝ) *
            (householderCompactComponentBudget fp m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
          householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_of_normSqBudget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef hbudgetNormSq
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hBudget_diag
      hpivotMax
/-- Active-max-pivot variant of
    `StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_of_normSqBudget`.

Instead of assuming the raw column-pivot maximality inequality, this constructor
uses the repository's finite active max-pivot selector.  This is the
Cox--Higham source-facing pivot-policy field: if the displayed active column is
chosen by `householderActiveMaxPivotColumn`, then the needed pivot-max
inequality follows from `householderActiveMaxPivotColumn_pivot_max`. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_activeMaxPivot_of_normSqBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t)) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget k) := by
  classical
  have hpivotMax : ∀ t (ht : t < n), ∀ l : Fin n, t ≤ l.val →
      householderTrailingColumnNorm2Sq
          (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
          (A_hat t) l ≤
        householderTrailingColumnNorm2Sq
          (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
          (A_hat t) ⟨t, ht⟩ := by
    intro t ht l hl
    have hmax :=
      householderActiveMaxPivotColumn_pivot_max
        ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t) l hl
    have hnormEq :
        householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t)
            (householderActiveMaxPivotColumn
              ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t)) =
          householderTrailingColumnNorm2Sq
            (m := m) (n := n) ⟨t, lt_of_lt_of_le ht hmn⟩
            (A_hat t) ⟨t, ht⟩ := by
      rw [← hpivotChoice t ht]
    exact hmax.trans_eq hnormEq
  exact
    StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_of_normSqBudget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef hbudgetNormSq
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hBudget_diag
      hpivotMax
/-- Active-max-pivot variant of the `κ∞`/dual-budget row-budget constructor.

This removes the raw pivot-max inequality from the source-facing
`κ∞`/dual-budget route by deriving it from the finite active max-pivot
algorithmic policy. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_activeMaxPivot_kappaInf_dualBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget_nonneg : ∀ t : ℕ, 0 ≤ stageBudget t)
    (hBudget_mono : ∀ a b : ℕ, a ≤ b → stageBudget a ≤ stageBudget b)
    (hBudget_diag : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t)) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget k) := by
  have hbudgetNormSq :
      ∀ k (hk : k < n),
        (m : ℝ) *
            (householderCompactComponentBudget fp m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
          householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_activeMaxPivot_of_normSqBudget
      hmn fp A_hat alpha stageBudget hm hStepA hAlphaDef hbudgetNormSq
      hinitBlock hglobalBudget hBudget_nonneg hBudget_mono hBudget_diag
      hpivotChoice
/-- Horizon-budget active-max-pivot variant of the `κ∞`/dual-budget displayed
    row-budget constructor.

The finite global compact-step recurrence implies monotonicity on the QR
horizon.  This theorem clamps the supplied stage budget after `n`, uses the
finite stage-diagonal defect to obtain the displayed diagonal lower-bound
field on active stages, and derives the packaged
`StoredQRDisplayedRowBudgetControl` certificate without asking callers for a
separate global monotonicity proof. -/
theorem StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_activeMaxPivot_kappaInf_dualBudget_stageDiagDefect_horizonBudget
    {m n : ℕ} (hmn : n ≤ m)
    (fp : FPModel)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t)) :
    StoredQRDisplayedRowBudgetControl hmn A_hat
      (fun k (_hk : k < n) (_i : Fin (k + 1)) =>
        qrStageHorizonBudget n stageBudget k) := by
  classical
  let stageBudget' : ℕ → ℝ := qrStageHorizonBudget n stageBudget
  have hBudget_mono_on_stages :
      ∀ a b : ℕ, a ≤ b → b ≤ n → stageBudget a ≤ stageBudget b :=
    storedQRSignedStageBudget_mono_on_stages_of_globalCompactBudget_of_initial_nonneg
      hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
  have hBudget'_mono :
      ∀ a b : ℕ, a ≤ b → stageBudget' a ≤ stageBudget' b := by
    simpa [stageBudget'] using
      qrStageHorizonBudget_mono_of_mono_on_stages
        n stageBudget hBudget_mono_on_stages
  have hBudget'_nonneg : ∀ t : ℕ, 0 ≤ stageBudget' t := by
    have hnonneg_on :
        ∀ t : ℕ, t ≤ n → 0 ≤ stageBudget t :=
      storedQRSignedStageBudget_nonneg_on_stages_of_globalCompactBudget
        hmn fp A_hat alpha stageBudget hm hglobalBudget hBudget0_nonneg
    intro t
    by_cases ht : t ≤ n
    · simpa [stageBudget', qrStageHorizonBudget, ht] using hnonneg_on t ht
    · simpa [stageBudget', qrStageHorizonBudget, ht] using hnonneg_on n le_rfl
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget' t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget' (t + 1) := by
    intro t ht
    have ht_le : t ≤ n := Nat.le_of_lt ht
    have hsucc_le : t + 1 ≤ n := Nat.succ_le_iff.mpr ht
    simpa [stageBudget', qrStageHorizonBudget, ht_le, hsucc_le] using
      hglobalBudget t ht
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget' 0 := by
    intro r l
    have h0 : 0 ≤ n := Nat.zero_le n
    simpa [stageBudget', qrStageHorizonBudget, h0] using hinitBlock r l
  have hBudget_diag' : ∀ k (hk : k < n),
      ∀ i : Fin (k + 1), i.val < k →
        stageBudget' k ≤
        |qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i| := by
    intro k hk i hi
    have hk_le : k ≤ n := Nat.le_of_lt hk
    simpa [stageBudget', qrStageHorizonBudget, hk_le] using
      storedQRStageBudget_le_diag_of_stageDiagLowerDefectBudget_nonpos
        hmn A_hat stageBudget hstageDiagDefect k hk i hi
  simpa [stageBudget'] using
    StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_activeMaxPivot_kappaInf_dualBudget
      hmn fp A_hat alpha κ K stageBudget' hm hStepA hAlphaDef
      hdetLead hK hκ hκbudget hbudgetDual hinitBlock' hglobalBudget'
      hBudget'_nonneg hBudget'_mono hBudget_diag' hpivotChoice
/-- Solver-facing QR certificate from the packaged displayed row-budget
    control certificate.

    This is the solver-level companion to
    `StoredQRDisplayedRowBudgetControl`: the Cox--Higham row-growth route keeps
    its diagonal lower-bound/nonbreakdown field explicit and named. -/
theorem LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    let R : Fin n → Fin n → ℝ :=
      fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
    LSQRSolveBackwardError n (rectLSGram A) (rectLSRhs A b)
      (fl_backSub fp n R
        (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
      (qrSolveFinalGramBudget fp A R cStep)
      (qrSolveFinalRhsBudget fp A b R cStep) := by
  exact
    LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product
        hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hrowControl hproduct)
/-- Solver-facing row-budget-control QR certificate with one finite global
    compact-product budget.

This is the local `LSQRSolveBackwardError` companion to
`StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct`.
It removes the per-pivot compact-product family from the solver theorem
surface and consumes the scalar finite-maximum condition
`storedQRCompactSequenceProductBudget < 1`.  The packaged row-budget
certificate, determinant field, and norm-square nonbreakdown margin remain
visible. -/
theorem LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    let R : Fin n → Fin n → ℝ :=
      fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
    LSQRSolveBackwardError n (rectLSGram A) (rectLSRhs A b)
      (fl_backSub fp n R
        (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
      (qrSolveFinalGramBudget fp A R cStep)
      (qrSolveFinalRhsBudget fp A b R cStep) := by
  exact
    LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
        hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hrowControl hglobalProduct)
/-- Source-facing exact-minimizer certificate from the packaged displayed
    row-budget control certificate and per-pivot compact-product smallness.

This is the rectangular-data counterpart of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product`.
It exposes the Cox--Higham row-growth package directly and returns the
perturbed least-squares minimizer conclusion rather than only the solver-facing
normal-equation backward-error certificate. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hproduct : ∀ k (hk : k < n),
      2 *
          diagDominantUpperInvBudgetExpr (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
            ⟨k, Nat.lt_succ_self k⟩ *
        ((m : ℝ) *
          (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
            vecNorm2 (fun i : Fin m => A_hat k i ⟨k, hk⟩)) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_product
        hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hrowControl hproduct)
/-- Source-facing exact-minimizer certificate from the packaged displayed
    row-budget control certificate and one finite global compact-product
    budget.

This is the rectangular-data counterpart of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct`.
It keeps the row-budget-control package, determinant nonbreakdown, norm-square
nonbreakdown, and scalar global product condition visible on the final
perturbed least-squares minimizer route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
        hmn fp A_hat b_hat alpha rowBudget hm hStepA hAlphaDef
        hbudgetNormSq hdetLead hrowControl hglobalProduct)
/-- Solver-facing QR certificate from a uniform row-max-to-diagonal
    contraction and one finite global compact-product budget.

This is the solver-level companion to
`StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct`.
It exposes the route-1 computed-loop invariant directly: the caller supplies a
scalar `ρ ≤ 1` bounding each displayed strict-upper row maximum by `ρ` times
the matching displayed diagonal. -/
theorem LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (ρ : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hρ : ρ ≤ 1)
    (hrowMaxContract : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤
        ρ *
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    let R : Fin n → Fin n → ℝ :=
      fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
    LSQRSolveBackwardError n (rectLSGram A) (rectLSRhs A b)
      (fl_backSub fp n R
        (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
      (qrSolveFinalGramBudget fp A R cStep)
      (qrSolveFinalRhsBudget fp A b R cStep) := by
  exact
    LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct
        hmn fp A_hat b_hat alpha ρ hm hStepA hAlphaDef hbudgetNormSq
        hdetLead hρ hrowMaxContract hglobalProduct)
/-- Solver-facing QR certificate from the scalar finite row-max/diagonal defect
    budget and one finite global compact-product budget.

Compared with the row-max-factor theorem, the family of contraction inequalities
is replaced by the single finite check
`storedQRRowMaxDiagDefectBudget <= 0`.  The determinant, norm-square
nonbreakdown, and global compact-product fields remain visible. -/
theorem LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hdefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    let R : Fin n → Fin n → ℝ :=
      fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
    LSQRSolveBackwardError n (rectLSGram A) (rectLSRhs A b)
      (fl_backSub fp n R
        (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
      (qrSolveFinalGramBudget fp A R cStep)
      (qrSolveFinalRhsBudget fp A b R cStep) := by
  exact
    LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
        hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
        hdetLead hdefect hglobalProduct)
/-- Solver-facing scalar row-max-defect/global-product QR certificate with the
    operation-validity guard displayed as `(m : ℝ) * fp.u < 1`.

This is the actual-unit-roundoff sibling of the row-max-defect theorem above.
It derives the `gammaValid fp m` and `gammaValid fp n` obligations internally
and leaves the determinant, norm-square nonbreakdown, scalar row-defect, and
global compact-product hypotheses visible. -/
theorem LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hdefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    let R : Fin n → Fin n → ℝ :=
      fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
    LSQRSolveBackwardError n (rectLSGram A) (rectLSRhs A b)
      (fl_backSub fp n R
        (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
      (qrSolveFinalGramBudget fp A R cStep)
      (qrSolveFinalRhsBudget fp A b R cStep) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hbudgetNormSq hdetLead hdefect hglobalProduct
/-- Source-facing exact-minimizer certificate from a uniform
    row-max-to-diagonal contraction and one finite global compact-product
    budget.

This is the rectangular-data counterpart of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct`.
It exposes the route-1 row-max invariant directly at the perturbed
least-squares minimizer surface. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (ρ : ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hρ : ρ ≤ 1)
    (hrowMaxContract : ∀ k (hk : k < n), ∀ i : Fin (k + 1), i.val < k →
      qrLeadingStrictUpperRowMaxBudget hmn A_hat k hk i ≤
        ρ *
          |qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk i i|)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxBudgetFactor_globalProduct
        hmn fp A_hat b_hat alpha ρ hm hStepA hAlphaDef hbudgetNormSq
        hdetLead hρ hrowMaxContract hglobalProduct)
/-- Source-facing exact-minimizer certificate from the scalar finite
    row-max/diagonal defect budget and one finite global compact-product
    budget.

Compared with the row-max-factor theorem, the row-growth condition is the
single finite scalar check `storedQRRowMaxDiagDefectBudget <= 0`; the conclusion
is still the direct rectangular perturbed least-squares minimizer statement. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hdefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_sourceOffDiagonalControl
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef
      (StoredQRSourceOffDiagonalControl.of_stored_trailing_sequence_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
        hmn fp A_hat b_hat alpha hm hStepA hAlphaDef hbudgetNormSq
        hdetLead hdefect hglobalProduct)
/-- Source-facing scalar row-max-defect/global-product exact-minimizer
    certificate with the operation-validity guard displayed as
    `(m : ℝ) * fp.u < 1`.

This is the actual-unit-roundoff sibling of the row-max-defect direct minimizer
theorem: it derives the `gammaValid fp m` and `gammaValid fp n` obligations
internally before applying the direct rectangular route. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hbudgetNormSq : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        householderTrailingNorm2Sq m
          ⟨k, lt_of_lt_of_le hk hmn⟩
          (fun a => A_hat k a ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hdefect : storedQRRowMaxDiagDefectBudget hmn A_hat ≤ 0)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowMaxDiagDefect_globalProduct
      fp hmn A b A_hat b_hat alpha hm hγ hInitA hInitb hStepA hStepb
      hAlphaDef hbudgetNormSq hdetLead hdefect hglobalProduct
/-- Solver-facing row-budget-control finite-global-product QR certificate with
    norm-square nonbreakdown derived from local `κ∞`/dual-budget data.

This is the `κ∞`/dual-budget sibling of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct`.
It removes the raw norm-square margin from the local solver surface while
keeping the packaged row-budget certificate, local determinant/conditioning
fields, dual compact-budget field, and scalar global product condition visible. -/
theorem LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    let R : Fin n → Fin n → ℝ :=
      fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j
    LSQRSolveBackwardError n (rectLSGram A) (rectLSRhs A b)
      (fl_backSub fp n R
        (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩))
      (qrSolveFinalGramBudget fp A R cStep)
      (qrSolveFinalRhsBudget fp A b R cStep) := by
  have hbudgetNormSq :
      ∀ k (hk : k < n),
        (m : ℝ) *
            (householderCompactComponentBudget fp m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
          householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
      fp hmn A b A_hat b_hat alpha rowBudget hm hγ hInitA hInitb hStepA
      hStepb hAlphaDef hbudgetNormSq hdetLead hrowControl hglobalProduct
/-- Source-facing exact-minimizer certificate from row-budget control whose
    norm-square nonbreakdown is derived from local `κ∞`/dual-budget data.

This is the rectangular-data counterpart of
`LSQRSolveBackwardError.of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct`.
It removes the raw norm-square margin from the final minimizer surface while
keeping the determinant, local conditioning, dual compact-budget, packaged
row-budget-control, and finite global compact-product fields visible. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (hm : gammaValid fp m)
    (hγ : gammaValid fp n)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hbudgetNormSq :
      ∀ k (hk : k < n),
        (m : ℝ) *
            (householderCompactComponentBudget fp m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
              (householderBetaSpec m
                (householderTrailingActiveVector m
                  ⟨k, lt_of_lt_of_le hk hmn⟩
                  (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
              (fun a => A_hat k a ⟨k, hk⟩)
              ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
          householderTrailingNorm2Sq m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) :=
    storedQRSignedStage_normSqBudget_of_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget
      hmn fp A_hat alpha κ K hStepA hdetLead hK hκ hκbudget hbudgetDual
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_normSqBudget_rowBudgetControl_globalProduct
      fp hmn A b A_hat b_hat alpha rowBudget hm hγ hInitA hInitb hStepA
      hStepb hAlphaDef hbudgetNormSq hdetLead hrowControl hglobalProduct
/-- Actual-unit-roundoff sibling of the `κ∞`/dual-budget row-budget-control
    exact-minimizer theorem. -/
theorem exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct_of_actualUnitRoundoff_no_gammaValid
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hγ : gammaValid fp n :=
    gammaValid_mono fp hmn hm
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct
      fp hmn A b A_hat b_hat alpha κ K rowBudget hm hγ hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual
      hrowControl hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, row-budget-control compact
    wrapper.

This source-facing wrapper uses the packaged displayed row-budget certificate
instead of the active-pivot/stage-budget surfaces.  The row-budget certificate
contains exactly the displayed strict-upper-entry and diagonal-lower control
needed by the source off-diagonal route; determinant/conditioning data, dual
compact-budget data, and the compact global-product condition remain visible. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_rowBudgetControl_dualBudget_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (rowBudget : ∀ k, k < n → Fin (k + 1) → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hrowControl : StoredQRDisplayedRowBudgetControl hmn A_hat rowBudget)
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct_of_actualUnitRoundoff_no_gammaValid
      fp hmn A b A_hat b_hat alpha κ K rowBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual
      hrowControl hglobalProduct
/-- The first concrete stored-Householder matrix state agrees with the
    diagonally dominant scalar-comparison counterexample. -/
theorem storedDiagDominantComparisonCounterexample_concreteStoredMatrixSeq_one :
    storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
      (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0 1 =
    storedDiagDominantComparisonCounterexampleSeq 1 := by
  rw [storedHouseholderQRMatrixSeq_succ_of_lt
    exactHouseholderQRDiagDominanceCounterexampleFP (by omega : 2 ≤ 2)
    activeMaxPivotRowMaxComparisonCounterexampleA0 0 (by norm_num)]
  have hstep :=
    storedDiagDominantComparisonCounterexample_stored_step 0
      (by norm_num : 0 < 2)
  rw [hstep]
  have hA0 :
      storedDiagDominantComparisonCounterexampleSeq 0 =
        activeMaxPivotRowMaxComparisonCounterexampleA0 := rfl
  have halpha0 :
      storedHouseholderQRAlphaSeq exactHouseholderQRDiagDominanceCounterexampleFP
        (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0 0 =
      storedDiagDominantComparisonCounterexampleAlpha 0 := by
    rw [storedHouseholderQRAlphaSeq_eq_signed
      exactHouseholderQRDiagDominanceCounterexampleFP (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleA0 0 (by norm_num)]
    symm
    simpa [hA0] using
      storedDiagDominantComparisonCounterexample_signed_alpha_def 0
        (by norm_num : 0 < 2)
  simp [hA0, halpha0]
/-- The canonical Frobenius-starting budget does not remove the scalar
    stage-budget/row-max comparison obstruction.

For the exact `2 x 2` diagonally dominant active-pivot witness, stage one has
strict-upper row maximum `2`, while the canonical Frobenius-starting stage
budget is at least `3`.  Hence the finite comparison defect is positive. -/
theorem storedDiagDominantComparisonCounterexample_concreteFrobStageBudget_stageRowMaxComparisonDefectBudget_pos :
    0 < storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
      (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
        (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
      (storedHouseholderQRFrobStageBudget exactHouseholderQRDiagDominanceCounterexampleFP
        (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0) := by
  let i : Fin (1 + 1) := ⟨0, by norm_num⟩
  have hle :=
    storedQRStageRowMaxComparisonDefect_le_budget
      (by omega : 2 ≤ 2)
      (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
        (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
      (storedHouseholderQRFrobStageBudget exactHouseholderQRDiagDominanceCounterexampleFP
        (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
      (k := 1) (hk := by norm_num) i (by norm_num)
  have hrow :
      qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
        (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
          (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
        1 (by norm_num) i = 2 := by
    rw [qrLeadingStrictUpperRowMaxBudget]
    rw [dif_pos (by norm_num : i.val < 1)]
    have hsup :
        (Finset.univ.filter (fun j : Fin (1 + 1) => i.val < j.val)).sup'
            (by
              refine ⟨(⟨1, by norm_num⟩ : Fin (1 + 1)), ?_⟩
              simp [i])
            (fun j =>
              |qrLeadingBlock
                ((storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
                  (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0) 1)
                (Nat.succ_le_iff.mpr
                  (lt_of_lt_of_le (by norm_num : 1 < 2) (by omega : 2 ≤ 2)))
                (by norm_num : 1 < 2) i j|) = 2 := by
      apply Finset.sup'_eq_of_forall
      intro j hj
      have hj_one : j = (⟨1, by norm_num⟩ : Fin (1 + 1)) := by
        have hjpos : i.val < j.val := (Finset.mem_filter.mp hj).2
        fin_cases j
        · norm_num [i] at hjpos
        · rfl
      subst hj_one
      rw [storedDiagDominantComparisonCounterexample_concreteStoredMatrixSeq_one]
      norm_num [i, storedDiagDominantComparisonCounterexampleSeq,
        storedDiagDominantComparisonCounterexampleA1,
        qrLeadingBlock, qrLeadingRow, qrLeadingColumn]
    rw [hsup]
  have hstage :
      (3 : ℝ) ≤ storedHouseholderQRFrobStageBudget
        exactHouseholderQRDiagDominanceCounterexampleFP (by omega : 2 ≤ 2)
        activeMaxPivotRowMaxComparisonCounterexampleA0 1 := by
    rw [storedHouseholderQRFrobStageBudget_succ_of_lt
      exactHouseholderQRDiagDominanceCounterexampleFP (by omega : 2 ≤ 2)
      activeMaxPivotRowMaxComparisonCounterexampleA0 0 (by norm_num)]
    simp only [storedHouseholderQRFrobStageBudget_zero]
    have hfrob_ge :
        (3 : ℝ) ≤ frobNormRect activeMaxPivotRowMaxComparisonCounterexampleA0 := by
      let r0 : Fin 2 := ⟨0, by norm_num⟩
      let c0 : Fin 2 := ⟨0, by norm_num⟩
      have hentry :=
        abs_entry_le_frobNormRect activeMaxPivotRowMaxComparisonCounterexampleA0 r0 c0
      norm_num [r0, c0, activeMaxPivotRowMaxComparisonCounterexampleA0] at hentry
      exact hentry
    have hfrob_nonneg :
        0 ≤ frobNormRect activeMaxPivotRowMaxComparisonCounterexampleA0 :=
      frobNormRect_nonneg _
    have hfactor : (1 : ℝ) ≤ coxHighamActiveRowGrowthFactor 2 :=
      one_le_coxHighamActiveRowGrowthFactor 2
    have hmul :
        frobNormRect activeMaxPivotRowMaxComparisonCounterexampleA0 ≤
          coxHighamActiveRowGrowthFactor 2 *
            frobNormRect activeMaxPivotRowMaxComparisonCounterexampleA0 := by
      nlinarith
    have hterm_nonneg :
        0 ≤ storedQRSignedStageGlobalCompactBudget (by omega : 2 ≤ 2)
          exactHouseholderQRDiagDominanceCounterexampleFP
          (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
            (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
          (storedHouseholderQRAlphaSeq exactHouseholderQRDiagDominanceCounterexampleFP
            (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
          0 (by norm_num) :=
      storedQRSignedStageGlobalCompactBudget_nonneg (by omega : 2 ≤ 2)
        exactHouseholderQRDiagDominanceCounterexampleFP
        (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
          (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
        (storedHouseholderQRAlphaSeq exactHouseholderQRDiagDominanceCounterexampleFP
          (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
        0 (by norm_num)
        (by
          norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
            FPModel.exactWithUnitRoundoff, gammaValid])
    linarith
  have hterm :
      (1 : ℝ) ≤
        storedHouseholderQRFrobStageBudget exactHouseholderQRDiagDominanceCounterexampleFP
          (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0 1 -
        qrLeadingStrictUpperRowMaxBudget (by omega : 2 ≤ 2)
          (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
            (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
          1 (by norm_num) i := by
    rw [hrow]
    linarith
  have hbudget :
      (1 : ℝ) ≤ storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
          (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0)
        (storedHouseholderQRFrobStageBudget exactHouseholderQRDiagDominanceCounterexampleFP
          (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0) :=
    hterm.trans hle
  linarith
/-- Route elimination for the current canonical Frobenius-stage Theorem 20.3
    surface.

Local diagonal dominance, the concrete active-max-pivot choice, and a small
declared unit roundoff do not force the scalar comparison defect needed by the
stored-lower Frobenius-budget wrapper.  Thus that remaining hypothesis is an
independent domain/algorithm invariant unless a different budget or pivoted
stored-loop model is supplied. -/
theorem not_forall_diagDominant_activeMaxPivot_concreteStoredFrobStageBudget_implies_stageRowMaxComparisonDefectBudget_nonpos :
    ¬ (∀ (fp : FPModel) (A : Fin 2 → Fin 2 → ℝ),
      (2 : ℝ) * fp.u < 1 →
      (∀ k (hk : k < 2),
        IsDiagDominantUpper (k + 1)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp (by omega : 2 ≤ 2) A k)
            (Nat.succ_le_iff.mpr hk) hk)) →
      (∀ t (ht : t < 2),
        (⟨t, ht⟩ : Fin 2) =
          householderActiveMaxPivotColumn
            (⟨t, lt_of_lt_of_le ht (by omega : 2 ≤ 2)⟩ : Fin 2)
            (⟨t, ht⟩ : Fin 2)
            (storedHouseholderQRMatrixSeq fp (by omega : 2 ≤ 2) A t)) →
      storedQRStageRowMaxComparisonDefectBudget (by omega : 2 ≤ 2)
        (storedHouseholderQRMatrixSeq fp (by omega : 2 ≤ 2) A)
        (storedHouseholderQRFrobStageBudget fp (by omega : 2 ≤ 2) A) ≤ 0) := by
  intro h
  have hDD : ∀ k (hk : k < 2),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock
          (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
            (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0 k)
          (Nat.succ_le_iff.mpr hk) hk) := by
    intro k hk
    interval_cases k
    · simpa [storedDiagDominantComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0] using
        storedDiagDominantComparisonCounterexample_diagDominant 0
          (by norm_num : 0 < 2)
    · rw [storedDiagDominantComparisonCounterexample_concreteStoredMatrixSeq_one]
      exact storedDiagDominantComparisonCounterexample_diagDominant 1
        (by norm_num : 1 < 2)
  have hpivot : ∀ t (ht : t < 2),
      (⟨t, ht⟩ : Fin 2) =
        householderActiveMaxPivotColumn
          (⟨t, lt_of_lt_of_le ht (by omega : 2 ≤ 2)⟩ : Fin 2)
          (⟨t, ht⟩ : Fin 2)
          (storedHouseholderQRMatrixSeq exactHouseholderQRDiagDominanceCounterexampleFP
            (by omega : 2 ≤ 2) activeMaxPivotRowMaxComparisonCounterexampleA0 t) := by
    intro t ht
    interval_cases t
    · simpa [storedDiagDominantComparisonCounterexampleSeq,
        activeMaxPivotRowMaxComparisonCounterexampleA0] using
        storedDiagDominantComparisonCounterexample_activeMaxPivotChoice 0
          (by norm_num : 0 < 2)
    · rw [storedDiagDominantComparisonCounterexample_concreteStoredMatrixSeq_one]
      exact storedDiagDominantComparisonCounterexample_activeMaxPivotChoice 1
        (by norm_num : 1 < 2)
  have hnonpos := h exactHouseholderQRDiagDominanceCounterexampleFP
    activeMaxPivotRowMaxComparisonCounterexampleA0
    (by
      norm_num [exactHouseholderQRDiagDominanceCounterexampleFP,
        FPModel.exactWithUnitRoundoff])
    hDD hpivot
  exact not_lt_of_ge hnonpos
    storedDiagDominantComparisonCounterexample_concreteFrobStageBudget_stageRowMaxComparisonDefectBudget_pos
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, active-pivot horizon-budget
    compact wrapper.

This wrapper routes the stage-diagonal branch through the packaged
`StoredQRDisplayedRowBudgetControl` constructor.  The finite global compact
recurrence supplies the monotone horizon-budget extension internally, and the
full initial-block bound supplies the displayed strict-upper initial field.
Compared with
`theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_actualUnitRoundoff`,
it removes the separate per-entry initial displayed-upper hypothesis, global
stage-budget monotonicity hypothesis, and all-stage nonnegativity hypothesis
from the public surface; the recurrence propagates nonnegativity from the
initial budget. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  classical
  let stageBudget' : ℕ → ℝ := qrStageHorizonBudget n stageBudget
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hrowControl :
      StoredQRDisplayedRowBudgetControl hmn A_hat
        (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget' k) := by
    simpa [stageBudget'] using
      StoredQRDisplayedRowBudgetControl.of_signed_stage_uniformBudget_globalCompactBudget_activeMaxPivot_kappaInf_dualBudget_stageDiagDefect_horizonBudget
        hmn fp A_hat alpha κ K stageBudget hm hStepA hAlphaDef
        hdetLead hK hκ hκbudget hbudgetDual hinitBlock hglobalBudget
        hBudget0_nonneg hstageDiagDefect hpivotChoice
  exact
    exists_perturbed_ls_minimizer_of_stored_trailing_householder_sequence_topBlock_fl_backSub_gamma_bound_explicitCompactBudget_of_signed_alpha_leadingBlock_det_ne_zero_kappaInf_selfNorm_dualBudget_rowBudgetControl_globalProduct_of_actualUnitRoundoff_no_gammaValid
      fp hmn A b A_hat b_hat alpha κ K
      (fun k (_hk : k < n) (_i : Fin (k + 1)) => stageBudget' k)
      huSmall hInitA hInitb hStepA hStepb hAlphaDef hdetLead hK hκ
      hκbudget hbudgetDual hrowControl hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, active-pivot
    horizon-budget compact wrapper with finite-max product smallness.

This strengthens
`theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_actualUnitRoundoff`
by deriving the compact global-product hypothesis from the canonical
diagonal-dominant finite-max smallness condition.  The displayed row-budget
certificate is still obtained from the stage-diagonal horizon-budget route, so
the remaining visible finite check on that side is the scalar
`storedQRStageDiagLowerDefectBudget <= 0`. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm : gammaValid fp m :=
    gammaValid_of_u_le_cap fp m fp.u (le_rfl : fp.u ≤ fp.u) huSmall
  have hglobalProduct :
      storedQRCompactSequenceProductBudget hmn fp A_hat b_hat alpha < 1 :=
    storedQRCompactSequenceProductBudget_lt_one_of_diagDominant_finite_max_smallness
      hmn fp A_hat b_hat alpha hm (fun k => hDD k.val k.isLt) hsmall
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_actualUnitRoundoff
      fp hmn A b A_hat b_hat alpha κ K stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hK hκ hκbudget hbudgetDual
      hinitBlock hglobalBudget hBudget0_nonneg hstageDiagDefect
      hpivotChoice hglobalProduct
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    active-pivot stage-diagonal/dual-budget wrapper.

This specializes
`theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_finiteMaxSmallness_actualUnitRoundoff`
to `storedHouseholderQRMatrixSeq`, `storedHouseholderQRRhsSeq`, and
`storedHouseholderQRAlphaSeq`.  The stored-loop recurrence, initial equations,
and signed-`alpha` definition are discharged definitionally; the determinant,
conditioning, dual-budget, active-pivot, stage-diagonal, initial-budget, and
finite-max smallness hypotheses remain visible. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let A_hat : ℕ → Fin m → Fin n → ℝ := storedHouseholderQRMatrixSeq fp hmn A
  let b_hat : ℕ → Fin m → ℝ := storedHouseholderQRRhsSeq fp hmn A b
  let alpha : ℕ → ℝ := storedHouseholderQRAlphaSeq fp hmn A
  have hInitA : A_hat 0 = A := by
    simp [A_hat]
  have hInitb : b_hat 0 = b := by
    simp [b_hat]
  have hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k) := by
    intro k hk
    simpa [A_hat, alpha] using
      storedHouseholderQRMatrixSeq_succ_of_lt fp hmn A k hk
  have hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k) := by
    intro k hk
    simpa [A_hat, b_hat, alpha] using
      storedHouseholderQRRhsSeq_succ_of_lt fp hmn A b k hk
  have hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩) := by
    intro k hk
    simpa [A_hat, alpha] using
      storedHouseholderQRAlphaSeq_eq_signed fp hmn A k hk
  have hdetLead' : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    simpa [A_hat] using hdetLead k hk
  have hDD' : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk) := by
    intro k hk
    simpa [A_hat] using hDD k hk
  have hκ' : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k := by
    intro k hk
    simpa [A_hat] using hκ k hk
  have hκbudget' : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k := by
    intro k hk
    simpa [A_hat] using hκbudget k hk
  have hbudgetDual' : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k := by
    intro k hk
    simpa [A_hat, alpha] using hbudgetDual k hk
  have hinitBlock' : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0 := by
    intro r l
    simpa [A_hat] using hinitBlock r l
  have hglobalBudget' : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    simpa [A_hat, alpha] using hglobalBudget t ht
  have hstageDiagDefect' :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 := by
    simpa [A_hat] using hstageDiagDefect
  have hpivotChoice' : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t) := by
    intro t ht
    simpa [A_hat] using hpivotChoice t ht
  have hsmall' :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1 := by
    simpa [A_hat, b_hat, alpha] using hsmall
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
      fp hmn A b A_hat b_hat alpha κ K stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead' hDD' hK hκ' hκbudget'
      hbudgetDual' hinitBlock' hglobalBudget' hBudget0_nonneg
      hstageDiagDefect' hpivotChoice' hsmall'
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    active-pivot scalar-comparison/dual-budget wrapper.

This specializes the finite-max scalar-comparison route to the concrete stored
Householder loop.  The scalar comparison defect is converted to the
stage-diagonal defect using local diagonal dominance, then the concrete
stage-diagonal/dual-budget wrapper supplies the final perturbed least-squares
minimizer statement. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn
          (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0 :=
    storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageRowMaxComparisonDefectBudget_nonpos
      hmn (storedHouseholderQRMatrixSeq fp hmn A) stageBudget hDD
      hcomparison
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
      fp hmn A b κ K stageBudget huSmall hdetLead hDD hK hκ hκbudget
      hbudgetDual hinitBlock hglobalBudget hBudget0_nonneg hstageDiagDefect
      hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    scalar-comparison/dual-budget wrapper with nonempty source dimensions.

For a nonempty least-squares problem, the initial block bound
`|A r l| <= stageBudget 0` already forces `0 <= stageBudget 0`; this wrapper
therefore removes the separate initial-budget nonnegativity hypothesis from
the concrete scalar-comparison route. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hm_pos : 0 < m := lt_of_lt_of_le hn hmn
  let r0 : Fin m := ⟨0, hm_pos⟩
  let l0 : Fin n := ⟨0, hn⟩
  have hBudget0_nonneg : 0 ≤ stageBudget 0 :=
    le_trans (abs_nonneg (A r0 l0)) (hinitBlock r0 l0)
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
      fp hmn A b κ K stageBudget huSmall hdetLead hDD hK hκ hκbudget
      hbudgetDual hinitBlock hglobalBudget hBudget0_nonneg hcomparison
      hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    scalar-comparison/dual-budget wrapper with nonempty dimensions and
    determinant hypotheses derived from diagonal dominance.

This removes two proof-artifact side conditions from the concrete
scalar-comparison route: initial-budget nonnegativity follows from the
nonempty initial-block bound, and leading-block determinant nonzeroness follows
from `IsDiagDominantUpper`. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty_det_from_diagDominant
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0 := by
    intro k hk
    exact
      det_ne_zero_of_diagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
        (hDD k hk)
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty
      fp hmn hn A b κ K stageBudget huSmall hdetLead hDD hK hκ
      hκbudget hbudgetDual hinitBlock hglobalBudget hcomparison hpivotChoice
      hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    scalar-comparison/dual-budget wrapper with a Frobenius initial budget.

This is the determinant-derived nonempty concrete scalar-comparison route with
the full entrywise initial block bound replaced by the normwise source-facing
assumption `frobNormRect A <= stageBudget 0`. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty_det_from_diagDominant_frobInitial
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ) (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitFrob : frobNormRect A ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1))
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A) stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A r l| ≤ stageBudget 0 := by
    intro r l
    exact (abs_entry_le_frobNormRect A r l).trans hinitFrob
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty_det_from_diagDominant
      fp hmn hn A b κ K stageBudget huSmall hDD hK hκ hκbudget
      hbudgetDual hinitBlock hglobalBudget hcomparison hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, concrete stored-loop
    scalar-comparison/dual-budget wrapper using the canonical Frobenius stage
    budget.

This specializes the determinant-derived Frobenius-initial scalar-comparison
route to `storedHouseholderQRFrobStageBudget`, removing the public
Frobenius-initial and compact-stage recurrence hypotheses.  The remaining
visible obligations are the conditioning/dual-budget data, active-pivot
policy, scalar comparison defect for the canonical budget, local diagonal
dominance, and finite-max smallness. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_frobStageBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m) (hn : 0 < n)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (κ K : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (storedHouseholderQRMatrixSeq fp hmn A k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a =>
                storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
              (storedHouseholderQRAlphaSeq fp hmn A k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a =>
                  storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
                (storedHouseholderQRAlphaSeq fp hmn A k)))
            (fun a =>
              storedHouseholderQRMatrixSeq fp hmn A k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn
        (storedHouseholderQRMatrixSeq fp hmn A)
        (storedHouseholderQRFrobStageBudget fp hmn A) ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩
          (storedHouseholderQRMatrixSeq fp hmn A t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn
            (storedHouseholderQRMatrixSeq fp hmn A) *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp
                (storedHouseholderQRMatrixSeq fp hmn A)
                (storedHouseholderQRRhsSeq fp hmn A b)
                (storedHouseholderQRAlphaSeq fp hmn A) *
              storedQRPivotColumnNormBudget hmn
                (storedHouseholderQRMatrixSeq fp hmn A)) ^ 2) <
        1) :
    let A_hat := storedHouseholderQRMatrixSeq fp hmn A
    let b_hat := storedHouseholderQRRhsSeq fp hmn A b
    let alpha := storedHouseholderQRAlphaSeq fp hmn A
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  let stageBudget : ℕ → ℝ := storedHouseholderQRFrobStageBudget fp hmn A
  have hinitFrob : frobNormRect A ≤ stageBudget 0 := by
    simp [stageBudget]
  have hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        stageBudget (t + 1) := by
    intro t ht
    change
      coxHighamActiveRowGrowthFactor m *
            storedHouseholderQRFrobStageBudget fp hmn A t +
          storedQRSignedStageGlobalCompactBudget hmn fp
            (storedHouseholderQRMatrixSeq fp hmn A)
            (storedHouseholderQRAlphaSeq fp hmn A) t ht ≤
        storedHouseholderQRFrobStageBudget fp hmn A (t + 1)
    rw [storedHouseholderQRFrobStageBudget_succ_of_lt fp hmn A t ht]
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_concreteStored_horizonBudget_finiteMaxSmallness_actualUnitRoundoff_of_nonempty_det_from_diagDominant_frobInitial
      fp hmn hn A b κ K stageBudget huSmall hDD hK hκ hκbudget
      hbudgetDual hinitFrob hglobalBudget hcomparison hpivotChoice hsmall
/-- Higham, 2nd ed., Chapter 20, Theorem 20.3, active-pivot
    horizon-budget compact wrapper with scalar comparison and finite-max
    product smallness.

This eliminates the stage-diagonal defect hypothesis from the finite-max
stage-diagonal wrapper by deriving it from local diagonal dominance plus the
finite scalar stage-budget/row-max comparison defect.  The compact
global-product field is again derived from the canonical finite-max smallness
condition. -/
theorem theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_diagDominant_stageRowMaxComparison_dualBudget_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
    {m n : ℕ} (fp : FPModel) (hmn : n ≤ m)
    (A : Fin m → Fin n → ℝ) (b : Fin m → ℝ)
    (A_hat : ℕ → Fin m → Fin n → ℝ)
    (b_hat : ℕ → Fin m → ℝ)
    (alpha κ K : ℕ → ℝ)
    (stageBudget : ℕ → ℝ)
    (huSmall : (m : ℝ) * fp.u < 1)
    (hInitA : A_hat 0 = A)
    (hInitb : b_hat 0 = b)
    (hStepA : ∀ k (hk : k < n),
      A_hat (k + 1) =
        fl_householderStoredPanelStep fp m n k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (A_hat k))
    (hStepb : ∀ k (hk : k < n),
      b_hat (k + 1) =
        fl_householderStoredRhsStep fp m k
          (householderTrailingActiveVector m
            ⟨k, lt_of_lt_of_le hk hmn⟩
            (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
          (householderBetaSpec m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
          (b_hat k))
    (hAlphaDef : ∀ k (hk : k < n),
      alpha k =
        signedHouseholderAlpha
          (Real.sqrt
            (householderTrailingNorm2Sq m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩)))
          (A_hat k ⟨k, lt_of_lt_of_le hk hmn⟩ ⟨k, hk⟩))
    (hdetLead : ∀ k (hk : k < n),
      Matrix.det
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk :
          Matrix (Fin (k + 1)) (Fin (k + 1)) ℝ) ≠ 0)
    (hDD : ∀ k (hk : k < n),
      IsDiagDominantUpper (k + 1)
        (qrLeadingBlock (A_hat k)
          (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk))
    (hK : ∀ k (_hk : k < n), 0 < K k)
    (hκ : ∀ k (hk : k < n),
      kappaInf (k + 1) (Nat.succ_pos k)
          (qrLeadingBlock (A_hat k)
            (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)
          (nonsingInv (k + 1)
            (qrLeadingBlock (A_hat k)
              (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ≤
        κ k)
    (hκbudget : ∀ k (hk : k < n),
      ((k + 1 : ℕ) : ℝ) *
          (κ k /
            infNorm
              (qrLeadingBlock (A_hat k)
                (Nat.succ_le_iff.mpr (lt_of_lt_of_le hk hmn)) hk)) ^ 2 ≤
        K k)
    (hbudgetDual : ∀ k (hk : k < n),
      (m : ℝ) *
          (householderCompactComponentBudget fp m
            (householderTrailingActiveVector m
              ⟨k, lt_of_lt_of_le hk hmn⟩
              (fun a => A_hat k a ⟨k, hk⟩) (alpha k))
            (householderBetaSpec m
              (householderTrailingActiveVector m
                ⟨k, lt_of_lt_of_le hk hmn⟩
                (fun a => A_hat k a ⟨k, hk⟩) (alpha k)))
            (fun a => A_hat k a ⟨k, hk⟩)
            ⟨k, lt_of_lt_of_le hk hmn⟩) ^ 2 <
        1 / K k)
    (hinitBlock : ∀ r : Fin m, ∀ l : Fin n,
      |A_hat 0 r l| ≤ stageBudget 0)
    (hglobalBudget : ∀ t (ht : t < n),
      coxHighamActiveRowGrowthFactor m * stageBudget t +
          storedQRSignedStageGlobalCompactBudget hmn fp A_hat alpha t ht ≤
        stageBudget (t + 1))
    (hBudget0_nonneg : 0 ≤ stageBudget 0)
    (hcomparison :
      storedQRStageRowMaxComparisonDefectBudget hmn A_hat stageBudget ≤ 0)
    (hpivotChoice : ∀ t (ht : t < n),
      ⟨t, ht⟩ =
        householderActiveMaxPivotColumn
          ⟨t, lt_of_lt_of_le ht hmn⟩ ⟨t, ht⟩ (A_hat t))
    (hsmall :
      2 * storedQRDiagDominantInvFactorBudget hmn A_hat *
          ((m : ℝ) *
            (storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha *
              storedQRPivotColumnNormBudget hmn A_hat) ^ 2) <
        1) :
    let cStep := storedQRCompactSequenceRelativeBudget hmn fp A_hat b_hat alpha
    ∃ (ΔA' : Fin m → Fin n → ℝ) (Δb' : Fin m → ℝ),
      frobNorm ΔA' ≤
        ((1 + cStep) ^ n - 1) * frobNormRect A +
          gamma fp n *
            frobNormRect (rectTopBlock (m := m)
              (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)) ∧
      vecNorm2 Δb' ≤ ((1 + cStep) ^ n - 1) * vecNorm2 b ∧
      IsLeastSquaresMinimizer
        (fun i j => A i j + ΔA' i j) (fun i => b i + Δb' i)
        (fl_backSub fp n
          (fun i j => A_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩ j)
          (fun i => b_hat n ⟨i.val, lt_of_lt_of_le i.isLt hmn⟩)) := by
  have hstageDiagDefect :
      storedQRStageDiagLowerDefectBudget hmn A_hat stageBudget ≤ 0 :=
    storedQRStageDiagLowerDefectBudget_nonpos_of_diagDominant_stageRowMaxComparisonDefectBudget_nonpos
      hmn A_hat stageBudget hDD hcomparison
  exact
    theorem20_3_householder_qr_ls_backward_error_compactBudget_of_activePivot_stageDiag_dualBudget_horizonBudget_finiteMaxSmallness_actualUnitRoundoff
      fp hmn A b A_hat b_hat alpha κ K stageBudget huSmall hInitA hInitb
      hStepA hStepb hAlphaDef hdetLead hDD hK hκ hκbudget hbudgetDual
      hinitBlock hglobalBudget hBudget0_nonneg hstageDiagDefect
      hpivotChoice hsmall

end NumStability
