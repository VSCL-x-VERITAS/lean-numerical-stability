import NumStability.Algorithms.LinearSystems.Underdetermined.QR.ModifiedGramSchmidt.Rounded.Core

/-!
# Higham21MGSRounded (compatibility module)

Import-only module retained so existing imports of
`NumStability.Algorithms.Underdetermined.Higham21MGSRounded`
keep resolving. Every declaration moved unchanged, as a whole owner, to the
canonical destination imported above; the destination preserves this
module's complete original import surface, so consumers reaching
identifiers transitively through this path still see the same surface.
-/
