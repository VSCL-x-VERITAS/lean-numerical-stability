import Mathlib.Data.Complex.BigOperators
import NumStability.Algorithms.ComplexBackwardError
import NumStability.Algorithms.HighamChapter10
import NumStability.Analysis.ComplexArithmetic
import NumStability.Analysis.MatrixAlgebra
import NumStability.Source.Higham.Chapter10.Equation30.ComplexPositiveDefinite.NoPivotLU.SourceBounds
import NumStability.Source.Higham.Chapter10.Equation30.ComplexPositiveDefinite.SourceClosure

/-!
# Ch10ComplexPositiveDefiniteSourceClosure (compatibility module)

Import-only module retained so existing imports of `NumStability.Algorithms.Ch10ComplexPositiveDefiniteSourceClosure` keep
resolving. Every declaration moved unchanged to the routed canonical
destination(s). The module's own original imports are re-stated so
consumers reaching an identifier transitively through this path still
see the same surface.
-/
