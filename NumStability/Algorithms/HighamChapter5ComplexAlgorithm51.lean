-- NumStability/Algorithms/HighamChapter5ComplexAlgorithm51.lean
--
-- Import-only compatibility wrapper retained by reorganization wave R03
-- (phase branch B0005, projection P0005). This historical path is preserved,
-- not deleted and not Git-renamed, so every existing `import` keeps resolving.
-- All of its declarations moved unchanged to the canonical module(s) below.

import Mathlib.Tactic
import NumStability.Analysis.ComplexArithmetic
import NumStability.Source.Higham.Chapter05.Algorithm01.ComplexHorner.Basic
import NumStability.Source.Higham.Chapter05.Algorithm01.ComplexHorner.ErrorBounds.Theorems

/-!
# HighamChapter5ComplexAlgorithm51 (compatibility wrapper)

Declaration-free import-only wrapper. Canonical module(s):

* `NumStability.Source.Higham.Chapter05.Algorithm01.ComplexHorner.ErrorBounds.Theorems`

Retained by wave R03 so historical imports continue to resolve unchanged.
-/
