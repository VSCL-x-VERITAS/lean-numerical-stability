# R01 delivery — stationary iteration and semiconvergence

R01 routes every declaration from the 16 frozen B0001 residual owners to the exact
canonical modules frozen by P0001. Historical paths remain declaration-free import-only
wrappers or Source aggregates; no historical file was deleted or Git-renamed.

Base: C0000 `b1b18772d80185ec08f49c818919558645c330a1`

Branch: `codex/reorg-completion-2026-08-r01-stationary-semiconvergence`

Active control: `daf5c92355e26c07ab0d219c20cd6ce6782b98f3`

Projection: P0001

## Authority and activation

B0001 and P0001 were read from the clean active-control checkout rather than merged into
the worker. B0001 is active, names `codex-local`, the exact branch, C0000, the exact base
SHA, and P0001. P0001 is active and every selector, projection, checker, baseline,
inventory, overlap-review, declaration-route, module-route, private-normalization,
private-closure, and test-plan hash matches its active record.

The planned-control Lean CI run `31480013380` (job `93742561898`) and active-control run
`31480852798` (job `93745203356`) both completed successfully. B0002/P0002 have zero
path, direct-import, transitive-reachability, signature-edge, or body-edge overlap with
R01. `NumStability/Analysis/MatrixAlgebra.lean` remains read-only at blob
`06ddc3d5d2a3c19c6aff3c088f932f3e3074d279`.

## Exact delivery

| quantity | value |
| --- | ---: |
| historical owners | 16 |
| selected declarations | 243 |
| public declarations | 233 |
| private declarations | 10 |
| theorems | 215 |
| definitions | 28 |
| reusable declarations | 142 |
| Chapter 17 source declarations | 101 |
| canonical declaration leaves | 20 (9 reusable, 11 source) |
| declaration-free canonical aggregates | 4 |
| production paths | 40 (16 modified, 24 added) |
| tests | 41 (20 canonical-only, 16 old-only, 5 focused) |
| delivery artifacts | 17 |
| total changed paths | 98 (16 modified, 82 added) |

The five stationary-iteration mixed owners, five semiconvergence mixed owners, and six
Chapter 17 equation umbrellas are now declaration-free. Their 243 declarations occur
exactly once at the 20 frozen destinations. All 233 public declarations retain their
exact name, namespace, kind, visibility, type, and proof/body behavior.

The ten private declarations remain private and use only the exact P0001-approved
module-encoded names. `PRIVATE_CLOSURE.tsv` is the byte-identical 42-row authority
closure: ten private seeds plus 32 public reverse dependents. The focused private test
constructs all ten internal `Lean.Name` values, including the numeric private component,
and requires each name to exist in the compiled environment.

## Import and compatibility contract

All 16 old paths remain importable. The 20 canonical-only tests each import exactly one
canonical leaf; the 16 old-only tests each import exactly one historical path. The five
focused tests protect all 233 public names, private normalization, Equation22 typed
consumers, StationaryIterationSeries, and the three protected roots.

All nine reusable leaves and both reusable family aggregates have zero direct or
transitive Source imports and zero reachability to a historical facade. All 20 canonical
leaves have zero reachability to an R01 owner. The complete import graph is acyclic and
all four new aggregates are declaration-free, sorted, unique, and exact.

## Integration boundary

No shared consumer was edited. `INTEGRATOR_REQUESTS.md`,
`INTEGRATOR_POSTIMAGES.tsv`, and `INTEGRATOR_REQUEST.patch` specify the exact five-path
C0000 request, including every preimage and minimal postimage. The 7,033-byte patch has
SHA-256 `E5A8F2C07CFE899B3F6B9C486989A92FF94CE99E16EF104278D52BADD0B8FA8C`;
forward replay and reverse restoration are exact.

The worker-view layout gate is intentionally red only because test-root wiring, family
discovery, tier classification, compatibility registration, and legacy-ratchet cleanup
are integrator-owned. `CHECK_REQUEST_REPLAY.py --full` documents and applies seven
routine acceptance-wiring paths only in a disposable C0000 clone. That complete state is
green for layout, compatibility, provenance, and strict-source. The formal `Rxxxx`
registry record remains the integrator's responsibility.

## Verification

Every required Lean command ran under `Local\lean-reorganization-2026-08`. The final
41-target matrix, isolated canonical and old-path subsets, focused subset, full production
build, combined production/test build, and `lake test` all exit 0. Exact commands, job
counts, timings, exit codes, the one integrator-only red gate, and all static checks are
recorded in `GATE_RESULTS.tsv`.

The final candidate is format 2 with 56,903 declarations, 266,387 signature edges,
382,872 body/proof edges, 649,259 typed rows, and 424,082 union edges. P0001 replay is
green for all 243 declarations, 693 signature edges, 1,341 body/proof edges, 1,422 union
edges, and ten private normalizations. `PROJECTION.md` records exact artifact sizes,
hashes, deterministic replay, and the sole-placeholder argument proof.

## Changed paths

`CHANGED_PATHS.md` is the exact status-aware 98-path ledger. The worker contains only
authorized production code, R01 tests, and R01 delivery evidence; it contains no shared
consumer, MatrixAlgebra, phase registry, control, or formal request-record edit.
