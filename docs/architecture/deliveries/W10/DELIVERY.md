# W10 delivery — norm estimation and Chapter 15

W10 reorganizes the 27 frozen B0012 owners into reusable norm-estimation mathematics under
`NumStability/Algorithms/NormEstimation/` and exact book correspondence under
`NumStability/Source/Higham/Chapter15/`.

Base: C0007 `9eb534a06db267203c2b9b88227edd44fc64f5db`

Branch: `codex/reorg-2026-08-w10-norm-estimation-ch15`

Projection: P0013
Selector SHA-256: `444AA9109E4990AD47E281D550EA7A80057A8DBC493D8AF1693760EE7434BBB0`

## Exact delivery

| quantity | value |
| --- | ---: |
| owners | 27 |
| selected declarations | 1,029 |
| retained at owners | 134 |
| relocated | 895 |
| reusable declarations | 494 |
| Chapter 15 source declarations | 401 |
| canonical modules | 96 (95 declaration owners plus 1 Source wrapper) |
| owner modules | 27 (13 declaration-bearing, 14 import-only) |
| tests | 135 (96 canonical-only, 27 old-only, 12 focused) |
| delivery paths | 274 (27 modified, 247 added) |
| evidence artifacts | 16 |

The frozen selected-induced private reverse-closure floor remains exactly
**132 = 80 private + 52 public**. Actual retention is 134: the floor plus the
two full-graph Chapter15CondEst re-entry hazards explicitly anticipated by the
B0012 overlap review.
All 43 authorized production destination prefixes are populated.

## Repaired evidence contract

The delivery carries four executable auditors:

- `CHECK_SCOPE.py` proves the exact 274-path B0012 diff and destination vacancy.
- `CHECK_PROJECTION.py` hash-pins active P0013 and replays its exact 73-argument vector,
  changing only the candidate path.
- `CHECK_STATIC.py` derives every route's physical module from the full candidate, checks
  the 132-member closure, 134-member retention ledger, all 135 test ledgers, and the
  production import graph.
- `CHECK_INTEGRATOR_PATCH.py` replays the exact 14-path C0007 aggregate/control request,
  preserves accepted-consumer and historical-root imports, and validates the complete
  integration import/tier/layout postimage.

`DECLARATION_ROUTES.tsv` now records the actual physical candidate module for every one of
the 1,029 selected declarations. The previous coarse-prefix values in 796 rows were not
physical modules and have been replaced deterministically. Four additional reviewed
boundary rows differ from the rejected packet: the two full-graph re-entry declarations
remain historical, while `cond_norm_identity` and `oneNorm_eq_infNorm_transpose'` route to
the reusable finite-index leaf. The 132 pinned private-closure rows remain unchanged.

Canonical-only tests contain exactly 895 distinct checks, one for every relocated selected
name and no additive replacement declaration. Old-only tests contain exactly all
949 selected public names. `TEST_MATRIX.tsv` records the actual imports and check counts.

## Integration boundary

W10 does not edit integrator-owned consumers, aggregates, tiers, layout controls, phase
records, or test roots. `INTEGRATOR_REQUESTS.md` specifies the exact 14-path R0011 union.
`Algorithms.CondEstimation` is an honest reusable-in-place retained core: its worker
postimage imports only reusable modules. The two generic identities formerly misrouted to
Source now live in the existing reusable finite-index leaf, while a declaration-free
Source correspondence wrapper preserves discovery. Both reusable-to-Source and
canonical-to-historical reachability are zero before and after R0011.

The accepted W06 `Higham16NormEstimator` and W09 Dixon surfaces remain present. Historical
imports and all public selected declarations remain available through the 27 owners.
R0011 classifies `CondEstimation` reusable and the other 26 owners as reviewed mixed debt
while owner-to-owner imports remain.

## Final gate artifacts

The one final full candidate has SHA-256
`749CC6B1888CF99BA5A44AC74A55A51A1DC23FD2EC7BE11D533F0E34BE128E61`
for TSV, `86422CC6BFB76B0D3FF7CF0515232E4DAEB8487473964B628C5252F757C03653`
for JSON, and `74EC53E847199D2012E1BC344F8BD7E0118AE6B5065EA35BB7F5D6531C63C9E3`
for Markdown. `CHECK_PROJECTION.py` pins their exact byte sizes and hashes.

The final deterministic zero-context R0011 patch is 34,111 bytes with SHA-256
`6AA9A9070BDA882DB9AA482D695F74CB15C9379BB8147B52F89BDB05CD4AA97C`.
`GATE_RESULTS.tsv` records the exact production/combined builds, complete 135-module
matrix, `lake test`, projection/static/scope checks, forward/reverse request replay, full
disposable integration overlay, and generated-artifact audit.

## Changed paths

The exact ledger is `CHANGED_PATHS.md`: 123 production paths (27 owners plus 96 canonical
modules), 135 tests, and 16 evidence artifacts. No integrator-owned path is part of the
worker diff.
