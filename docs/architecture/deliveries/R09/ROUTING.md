# R09 semantic routing

R09 covers 72 historical owners, all of them `NumStability.Algorithms.TestMatrices.Higham28*` modules that were unclassified at C0006. Routing was performed from declarations and
imports, not from filenames.

The 570 declarations (405 public and 165 private) split into 432 whole-owner routes and 138 split-owner
routes. They land in 41 destinations: 6 reusable, 35 source leaves,
distributed over the reviewed slices gallery=20, ginibre=12, spectral=9.
`DECLARATION_ROUTES.tsv` is the exact reviewed declaration ledger; `REALIZED_IMPORTS.tsv` is the exact 607-row post-move import contract for 109
modules.

All 68 declaration-bearing historical owners retain their public import surface as import-only compatibility wrappers. The other 4
historical owners had no format-2 declarations and remain byte-identical to C0006.
`RETENTION.tsv` records this owner-by-owner result, including base blobs and postimage hashes.

The destination DAG has 72 edges and no cycle. 7 multi-owner destinations required a pinned
concatenation order so that defining parts precede their consumers; `MATERIALIZATION.json` binds the exact resulting bytes.

## Disclosed deviations from a pure verbatim relocation

1. **Instance visibility.** 0 anonymous `local instance` declarations are restated as `private local instance` in their destinations so
   that the compiler-generated auto names cannot collide across the destinations that each need the instance. The instances were file-scoped and never visible to
   importers, so restating them is behaviour-preserving, but it is a real visibility change and not a byte-for-byte transplant.
2. **One proof repair.** NumStability.Source.Higham.Chapter28.Section02.RealGinibre.FiniteExpectation.ClosedFormAsymptotics::sqrt_two_mul_sqrt_one_half: dropped a tactic step made redundant by the destination's ambient environment.
   No statement changed; only a tactic step that the destination's richer ambient environment made redundant was dropped.
3. **Anonymous instance placement.** 11 compiler-named instances in 4 owners could not be matched to a ledger route
   name by name. They were placed by the documented replication / dominant-destination rule recorded in the materialization report, and their `source_ordinal`
   in `DECLARATION_ROUTES.tsv` is derived positionally against the remaining anonymous declaration heads of the owner, sorted by name:

   - `NumStability.Algorithms.TestMatrices.Higham28Ginibre`: `instMeasurableSpaceRSqMat_2`
   - `NumStability.Algorithms.TestMatrices.Higham28GinibreRoots`: `instBorelSpaceForallFinComplex_numStability`, `instBorelSpaceGinibreRawMatrix`, `instMeasurableSpaceForallFinComplex_numStability`, `instMeasurableSpaceGinibreRawMatrix`, `instOpensMeasurableSpaceForallFinComplex_numStability`, `instOpensMeasurableSpaceGinibreRawMatrix`, `instStandardBorelSpaceForallFinComplex_numStability`, `instStandardBorelSpaceGinibreRawMatrix`
   - `NumStability.Algorithms.TestMatrices.Higham28Probability`: `instMeasurableSpaceRSqMat_1`
   - `NumStability.Algorithms.TestMatrices.Higham28Stewart`: `instMeasurableSpaceRSqMat`

4. **Exact-duplicate instance dedup.** 0 merged destinations received textually identical single-line `local instance` declarations from
   more than one contributing owner; the duplicates were collapsed. Every collapse is recorded in the materialization report.

## Reviewed judgment calls

The consolidated route ledger records 27 reviewed judgment calls for this wave (of 33 across R09 and R10). They are reproduced verbatim below so that
the integrator reviews the same text the router did.

- [ginibre] COUNT DISCREPANCY vs prose: ground truth has 325 declarations (prose spec says 324). Higham28GinibreSignedIncidence carries 15 public declarations (prose counted 14) — the extra one is covered by the whole-owner move to S9 (OneRootMomentReduction), so S9's total is 48, not the prose's 47. No routing ambiguity.
- [ginibre] CROSS-FAMILY PIN (verbatim from spec): realGinibreMeasure, realGinibreMeasure_univ, expectedRealEigenvalueCount, and RealGinibreExpectedCountLimit are still DEFINED in the out-of-scope facade NumStability.Algorithms.TestMatrices.Higham28Probability. Every source destination S1-S10 references at least one. Preferred: schedule Higham28Probability/Higham28Moments owners earlier in R09 and import their destination; fallback (verified safe, no cycle in its 107-module closure): S1-S10 transitionally import NumStability.Algorithms.TestMatrices.Higham28Probability, retargeted when the probability family moves. R1/R2 unaffected (Mathlib-only).
- [ginibre] External sibling families already canonical: destinations must import NumStability.Analysis.TestMatrices.Gaussian.GaussianOrthogonal / GaussianDirection and NumStability.Analysis.TestMatrices.Orthogonal.OrthogonalSphere directly (NOT the Higham28GaussianOrthogonal / Higham28GaussianDirection wrappers).
- [ginibre] Anonymous/compiler-generated instances: the prose did not name them; they were resolved against ground truth and routed with their owner's block: instMeasurableSpaceRSqMat_2 (Higham28Ginibre) -> S2 per the prose's explicit anonymous-instance split row; the eight inst*GinibreRawMatrix/inst*ForallFinComplex_numStability instances (Roots) -> S2 per prose '#1-#8'; instMeasurableSpaceRSqMat_3/_4/_5, instMeasurableSpaceGinibreRawMatrix_1, instStandardBorelSpaceGinibreIncidence{Coordinates,Nuisance}, instDecidable_numStability, and the three private inst* in TraceDensity follow their owner's whole-owner destination.
- [ginibre] Merged destinations will contain duplicate local MeasurableSpace/Borel/StandardBorel instances from different owners (e.g. S1 gets two identical anonymous local MeasurableSpace (RSqMat n); S2 carries Higham28Ginibre's next to Roots' eight). Spec recommends deduping identical local instances at merge (cosmetic; duplicates also compile).
- [ginibre] Private global instances: Higham28GinibreGaussianBridge.matrixVolume_isAddHaarMeasure and five Higham28GinibreOrthogonalFiber instances are `private instance` WITHOUT `local` — globally resolving. Rename-on-relocate is safe (instance resolution is name-independent); IsAddHaarMeasure overlap between S7 and S9 already exists today. No action beyond noting.
- [ginibre] Mixed-flavor placements (deliberate per spec): Higham28GinibreMultiplicity -> S7 (its one theorem is the multiplicity link consumed only by the coarea chain); Higham28GinibreParity -> S1 (pure closed-form recurrence residue); Higham28GinibreOrthogonalFiber -> S9 (with its sole consumer SignedIncidence). Wrappers preserve every historical surface.
- [ginibre] R2 opens the new prefix NumStability/Analysis/Polynomials/. Spec fallback if the integrator declines a new Analysis subtree: merge R2's three Polynomial.* declarations into S8 and route the two Roots declarations (card_filter_lt_card_filter_of_mem, card_filter_le_card_filter_of_imp) to S2 instead (S2/S7 must NOT import S8 — cycle with S8->S7). Primary design (R2 standalone) is what this table encodes.
- [ginibre] Wrapper imports were materialized as: current file imports (read from the worktree) plus the spec's appended destination modules ('original imports preserved per R07 mechanics'). The five zero-declaration owners (Atlas, PlaneIncidence, SignedGaussian, SignedIncidenceAlgebra, SignedKernel) keep their current imports unchanged.
- [ginibre] Privates relocate with rename `_private.<dest>.0.NumStability.<name>` per B0010 normalization; anonymous local/private instances regenerate names at the destination (route rows use the current elaborated names verbatim).
- [spectral] Anonymous-name mapping: ground-truth 'NumStability.instMeasurableSpaceRSqMat_1' matched to the prose row '[anonymous local instance MeasurableSpace (RSqMat n), line 29]' -> routed to Section02.RealGinibre.ProbabilityLaw.ProductLaw (D7) as the ORIGINAL instance declaration; prose warns 'auto-name suffix may change on re-elaboration - record in private-normalization ledger'.
- [spectral] LOCAL-INSTANCE SPLIT (Higham28Probability): all 15 decls elaborate against the file-local anonymous 'local instance : MeasurableSpace (RSqMat n) := MeasurableSpace.pi'. The 3-way split gives the original instance declaration to D7 and re-declares it in D8 and D9 (+2 anonymous instance declarations; compiled terms of the moved defs re-bind to their module's instance constant). Recommended resolution: accept, recorded as instance-copy rows in the private-normalization ledger; 19 existing Higham28* modules already carry per-file copies of this exact local instance, so no name collision and no behavior change. Fallback if the integrator rejects declaration-graph growth: relocate_whole OP -> D7 only, with a ch28-ledger organization note that the p. 517 uniform-Perron and Theorem-28.1 ambient-law surfaces are temporarily co-located under RealGinibre.ProbabilityLaw.
- [spectral] D9 TIER: IsNormalizedOrthogonalHaarLaw's docstring names Stewart/Theorem 28.1 but its statements are locator-free generic mathematics; routed reusable under the existing Analysis.Probability.Haar prefix. If the integrator prefers strict co-location with Theorem 28.1 instead, the vacant alternative is NumStability.Source.Higham.Chapter28.Section03.Theorem01.StewartHaar.AmbientLaw - a one-line change to the D9 rows.
- [spectral] DECLARATION NAMES with locators (higham9_sineMatrix_isOrthogonal, higham9_12_sineMatrix references, ch28gf_* upstream): out of scope - noncanonical_name polices module names only; all routes are preserve_name_and_signature.
- [spectral] D6 semantics: pascalMoment_integral/pascal_circleAverage are arguably reusable circle-Fourier facts, but the bundle's headline theorems cite p. 519 verbatim and the ledger row lists them as the printed-row evidence; kept together as one source leaf to avoid splitting the 8 shared privates.
- [spectral] Umbrella Higham28.lean: 0 declarations (absent from ground-truth owners map, consistent); no retirement action beyond tier row 'compatibility'; wrapper preserves its current 18-import sequence byte-for-byte, nothing appended.
- [spectral] Cross-slice ledger note from spec: also append the D9 module to the declaration-free NumStability.Analysis.Probability.Haar aggregate; DAG constraint D4 before D3; D7/D8 additionally import existing Section02.RealGinibre.ProbabilityLaw.Probability, D8 also Analysis.Conditioning.LinearSystems.PerronFrobenius.
- [gallery] NumStability.instMeasurableSpaceRSqMat is the compiler-generated name for the anonymous local instance the prose lists as '<anonymous local instance MeasurableSpace (RSqMat n), line 32>'; routed to StewartMeasurability per the prose's explicit per-declaration route.
- [gallery] P1. H28Stewart local anonymous instance (line 32, `local instance (n) : MeasurableSpace (RSqMat n) := MeasurableSpace.pi`): file-scoped, never visible to importers, so restating it is behavior-preserving; restate in StewartMeasurability (required) and in SingleHouseholderRankTwo/HaarConclusion only if their proofs fail without it. Verify at compile that the measurability block has no reference to the randsvd privates.
- [gallery] P2. Source/Higham/Chapter28/Equation02/RatioDiscrepancy.lean:8 imports the historical owner H28HilbertAsymptotic (ledger: deliberate 'until the broader Hilbert family moves'). Recommended: retarget that import to Equation02.DeterminantAsymptotics (+ Analysis.TestMatrices.Hilbert.HilbertAsymptotic if needed) in R09; leaving it also compiles via the wrapper but perpetuates a source-tier module importing a compatibility module.
- [gallery] P3. Pascal oscillation/total-positivity remnants: earlier wave placed that family's defs in Analysis.TestMatrices.Pascal (reusable), but the remaining capstones close the printed p.520 ledger row and compoundMatrix_pascal_pos calls pascalMatrix_isStrictlyTotallyPositive directly; a reusable placement of downstream machinery would need reusable->source imports. Resolution adopted: route all four remnant owners to Source Section04/Pascal leaves (TotalPositivity <- OscillationEigenbasis <- OscillationSpectrum <- SignChangeCount), dependency-safe and law-faithful.
- [gallery] P4. H28GaussianQRHaar mixed content: a.e. linear independence of Gaussian columns is generically reusable, but the facade precedent sends this owner to Source only and the ledger cites the whole module for the printed p.517 row; routed whole-owner to QRFactorHaar. Acceptable alternative: split ae_linearIndependent_*/gaussianColumnMatrixMeasure(OfScale)_det_ne_zero_ae + privates span_range_ne_top..., measurableSet_linearIndependent_fin_cons, det_gsColumnsToMatrix_ne_zero... into a new reusable Analysis.TestMatrices.Gaussian.ColumnIndependence (name checked vacant).
- [gallery] P5. Cross-slice imports for new destinations: DeterminantAsymptotics needs hilbertRNat/Cholesky-diag content currently supplied via H28Asymptotics (another slice's owner; canonical home Analysis.TestMatrices.Hilbert.Asymptotics) — import the canonical module, not the owner; ConditionLogRate may need Source...Section04.Pascal.PascalCondition and, if required, OptimalPerturbationRate; OscillationEigenbasis/OscillationSpectrum import existing Analysis.TestMatrices.Pascal.{PascalOscillation,PascalOscillationCore,PascalDualFlag} plus new TotalPositivity; StewartMeasurability imports Analysis.TestMatrices.RandomSVD.Stewart + QR modules; HaarFiberMeasure imports StewartMeasurability + Analysis.TestMatrices.Orthogonal.{OrthogonalCoordinates,OrthogonalFibers}; GroupLawRecursion imports StewartMeasurability + HaarFiberMeasure + Analysis.TestMatrices.RandomSVD.{StewartHaar,StewartRecursion}; RawFiberConclusion imports GroupLawRecursion + HaarConclusion + Source...StewartHaar.StewartRawFiber; QRFactorHaar imports the Ginibre-measure canonical home (via H28GinibreMeasure's destinations, Ginibre slice) + Analysis.TestMatrices.Orthogonal.OrthogonalHaar + QR.GramSchmidt. Suggested build order: TotalPositivity, SingularizingPerturbation, IdentityCubeRoot, TriangularInvolution, SineEigenpairs, DeterminantAsymptotics, StewartMeasurability, SingleHouseholderRankTwo, ConditionLogRate, OptimalPerturbationRate, OscillationEigenbasis, OscillationSpectrum, SignChangeCount, CharacteristicPolynomial, SecondDifferenceCondition, HaarConclusion, HaarFiberMeasure, GroupLawRecursion, RawFiberConclusion, QRFactorHaar.
- [gallery] P6. Counts: 26 unclassified owners confirmed (H28GaussianAbsoluteMoment and H28HilbertRatioDiscrepancy are already exact 'compatibility' and excluded); decl-bearing Hilbert owners are HilbertAsymptotic + HilbertCondition (RatioDiscrepancy and ShiftedHilbert are import-only); decl-bearing Gaussian owner is GaussianQRHaar only. Exact totals: 147 declarations (106 public incl. 7 instances and 3 defs, 41 private incl. 3 private defs), vs the assignment's ~144 estimate.
- [gallery] Ground truth lists 18 decl-bearing owners; the prose's 26 owners include 8 import-only owners (H28GaussianDirection, H28GaussianOrthogonal, H28OrthogonalCoordinates, H28OrthogonalHaar, H28OrthogonalSphere, H28PascalDualFlag, H28ShiftedHilbert, H28StewartHaar) which get wrapper entries only; all 26 owners are reclassified exact tier -> compatibility per the spec.
- [gallery] H28OrthogonalCoordinates wrapper MUST persist verbatim: 7 external consumers import NumStability.Algorithms.TestMatrices.Higham28OrthogonalCoordinates (Ch15DixonProbability.lean:5, Ch15DixonClosure.lean:7, NormEstimation/TwoNorm/Dixon/Probability/DixonProbability.lean:14, .../Probability/DixonCompletion.lean:17, .../PowerBounds/DixonCompletion.lean:18, .../Algebra/DixonCompletion.lean:15, Source/Higham/Chapter15/Theorem06/Dixon/Basic.lean:19); the wrapper stays so they compile with zero edits.
- [gallery] Private declarations relocate with normalization rename _private.<owner>.0.NumStability.<n> -> _private.<destination>.0.NumStability.<n>.

