interface:
  display_name: "Formalization Faithfulness Audit"
  short_description: "Audit Lean statements against source texts"
  default_prompt: "Use $formalization-faithfulness-audit to audit TASK-ID against its configured source."
