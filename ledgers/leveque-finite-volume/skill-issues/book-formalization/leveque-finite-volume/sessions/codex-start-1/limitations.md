# Limitations — session codex-start-1

Record skill, harness, prompt, gate, tool, or agent-process limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Limitation | Consequence | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| BF-LEV-S01-LIMITATION-001 | 2026-08-31T22:59:29Z | faithfulness kit package verification | The Windows checkout stores `SHA256SUMS` and all 42 controlled text files with CRLF, while the manifest names LF-byte hashes; the prescribed GNU `sha256sum -c` therefore first treats carriage returns as filename characters and then every raw working-tree file differs. | The bundled package cannot be verified or installed byte-for-byte directly from this checkout. | Confirmed all 42 contents match after CRLF-to-LF normalization, reconstructed a temporary LF tree, obtained 42/42 raw `OK`, and installed only that verified tree. | mitigated | Workflow maintainer: make archive/checksum verification newline-portable or distribute canonical archive bytes; coordinator: retain the 42/42 transcript. |
| BF-LEV-S01-LIMITATION-002 | 2026-09-01T08:27:00Z | theorem-level semantic audit targeting | A theorem that only unfolds an abstract algorithm-contract predicate can pass a field-level comparison while its blind or round-trip review correctly observes that no source-relevant behavior was established; conversely, over-operationalizing qualitative prose can invent unsupported numerical details. | Three superseded dimensional-splitting targets were rejected and preserved: arbitrary endomorphisms plus self-labeling predicates were `not-faithful-weaker`; invented adjacency/upwind/coupled-update details were `not-faithful-different`; and a narrowed schedule target passed direct review but was rejected on round trip as `not-faithful-weaker` because it assumed and repeated direction coverage, left geometry and one-dimensional solves semantically opaque, admitted empty directions, and concluded a definitional fold equality. | Reopened the immutable-gate target with a genuine coordinate-box finite-volume partition, nonempty exhaustive duplicate-free coordinate family, direction-indexed constant-preserving one-dimensional solvers, admissible fractions, a constructed schedule, and inductive sequential execution. Fresh isolated blind, direct, and round-trip reviews all accepted the repaired target; adjudication resolved the equivalent-versus-stronger classification difference as `faithful-equivalent`, with both implications `yes`. Neighboring effectiveness and fully multidimensional-method prose remain excluded. | mitigated | Workflow maintainer: require operational semantics only for the exact gated claim, prohibit invented adjacent details, and reject hypotheses that merely restate the source conclusion; coordinator: retain every rejected history and require fresh blind, direct, round-trip, and adjudicator evidence when classifications differ. |
| BF-LEV-S01-LIMITATION-003 | 2026-09-01T10:05:00Z | independent audit-agent scheduling | The collaboration runtime permits only four simultaneously active agents including the coordinator, and an organization agent may legitimately occupy a nested analysis lane. It cannot always create a distinct fresh thread for every task/role pair at the desired moment. | Some same-role audits must run serially on an existing role-isolated agent thread, delaying direct and round-trip judging; cross-role reuse would invalidate independence. | Reuse only within the identical role, never expose other-role outputs, reset each task from its complete current packet, hash-validate every returned artifact, and record the runtime limitation and exact agent ID. Malformed outputs are still retried with a new stateless agent. | mitigated | Runtime maintainer: expose a larger independent-agent pool or releasable completed handles; coordinator: preserve strict information boundaries and provenance. |
| BF-LEV-S01-LIMITATION-004 | 2026-09-01T10:00:17Z | faithfulness preparation on Windows | `prepare_audit.py --force` inherited the Windows legacy text codec and failed while reading a generated dossier containing a byte not defined by that code page. | The controlled-input refresh aborted after archiving the prior bundle, before it could prepare the repaired dimensional-splitting packet. | Reran the unchanged command with Python's explicit `-X utf8` mode; preparation and prepared-phase validation then completed successfully. | mitigated | Workflow maintainer: open generated text explicitly as UTF-8 or enable UTF-8 mode in the launcher; coordinator: use `python -X utf8` for all remaining audit scripts. |
| BF-LEV-S01-LIMITATION-005 | 2026-09-01T10:24:00Z | faithfulness direct-judge hash instruction | A clean direct judge populated `dossier_sha256` with the declaration-dossier hash, while `validate_agent_output.py` requires the enclosing `direct_review_packet.md` hash. Both are called dossiers in nearby workflow surfaces. | A substantively accepted 103-dependency judgment failed machine validation and could not be archived as the current role output. | Deleted the invalid artifact rather than editing agent provenance, then launched a new stateless direct judge with the exact required packet hash explicitly named. | mitigated | Workflow maintainer: name the exact hashed file in the direct prompt/schema field description; coordinator: precompute and state the validator-expected hash for every remaining direct role. |
| BF-LEV-S01-LIMITATION-006 | 2026-09-01T11:19:00Z | heterogeneous-cell semantic target | A generic predicate asserting that some value is an “appropriate average” can be definitionally true without modeling the source, while a fixed normalized Bochner average can impose an unsupported universal arithmetic rule even when grid and assignment structure are present. | Four superseded targets were rejected and preserved: the generic predicate was `not-faithful-different`; a normalized integral without the physical grid/model was `not-faithful-weaker`; a genuine partition plus an unconstrained appropriateness predicate was `not-faithful-weaker`; and a partition with distinct wrapped normalized Bochner means was accepted directly but rejected on round trip as `not-faithful-weaker` because its universal mean remained unconnected to the physical model. | The fifth target uses a genuine nonempty measurable finite-volume partition, an actual model and material field, and a model-indexed operational averaging rule tied to each cell region. The rule is cell-local and preserves constants on positive finite cells; the theorem constructs the corresponding assigned properties and transports an explicit distinct-average premise to distinct properties. Fresh blind/direct/round-trip reviews all accepted it; primary-evidence adjudication resolved the classification as `faithful-equivalent`. All four rejected bundles remain in audit history. | mitigated | Workflow maintainer: qualitative discretization targets must represent the source's physical/model roles and problem-dependent averaging freedom, not merely a convenient formula or self-labeling predicate; coordinator: retain the complete accepted audit and adjudication record. |
| BF-LEV-S01-LIMITATION-007 | 2026-09-01T12:29:49Z | Riemann-interface operational target | A theorem can reproduce the neighboring-state indexing and conservative flux-difference stencil while still failing to represent a hyperbolic Riemann solve, finite-volume cell-average semantics, or the causal link from solution information to a usable numerical flux and time-step update. | The superseded exact-flux-subcase target was accepted directly but rejected on round trip as `not-faithful-different`: it constructed only piecewise initial data, treated `solve` and `Information` as arbitrary, assumed exact readout equality to an arbitrary two-state flux for all inputs, and fixed a positive-scale update without equation, cell-integral/volume, or time-step structure. | The repaired target uses positive-width adjacent cells, normalized interval-integral averages, a differentiable real-hyperbolic conservation law, problem-indexed solutions certified by the Riemann trace and integral conservation law, information and numerical flux derived from those solutions, constant-state consistency, and a volume-scaled positive-time update. Fresh blind, direct, and round-trip reviews accepted it as `faithful-stronger`; Lean implies source `yes` and source implies Lean `no`. The rejected bundle remains in audit history. | mitigated | Workflow maintainer: procedural source claims must retain the semantic relation between input problem, certified solver output, extracted numerical information, and downstream update; coordinator: retain both rejected and accepted evidence and the exact stronger-classification rationale. |
| BF-LEV-S01-LIMITATION-008 | 2026-09-01T12:31:27Z | finite-volume numerical-flux target | A correct telescoping flux-difference identity is not by itself a faithful model of the source's finite-volume update principle if the states are arbitrary sequences, the edge flux is global and ungrounded, exact physical-flux equality is assumed, and the scale has no time-step or cell-volume meaning. | The superseded exact-flux finite-prefix target was accepted directly but rejected on round trip as `not-faithful-different`: it lacked genuine cells, normalized averages, time advancement, local neighboring-interface flux construction, and a relation to the conservation-law flux; it also fixed a natural half-line and exact update formula not selected by the scoped prose. | The repaired target uses a genuine measurable cell partition with positive finite volumes, normalized integrals, nonempty oriented interfaces shared by distinct cells, strictly local two-neighbor numerical fluxes, physical conservation-law interface fluxes, a positive `dist < tolerance` approximation relation, a positive time interval, volume-scaled updates, and arbitrary finite-cell-set boundary cancellation. Fresh blind, direct, and round-trip reviews accepted it as `faithful-stronger`; Lean implies source `yes` and source implies Lean `no`. The rejected bundle remains in audit history. | mitigated | Workflow maintainer: conservation identities must be audited together with the physical/grid semantics of their quantities; coordinator: retain both rejected and accepted evidence and the exact stronger-classification rationale. |
