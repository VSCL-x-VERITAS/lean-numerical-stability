# Issues — session claude-chapter01-b

Record skill, harness, prompt, gate, tool, or agent-process issues encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Symptom | Evidence | Impact | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| S-CLAUDE-C01B-I001 | 2026-08-28, initialization organization preflight | `tools/architecture/check_layout.py` (Lean repo layout contract) | The repository layout contract exits non-zero with `new noncanonical modules` naming ~100 `NumStability.HDP.Contracts.*` and `NumStability.HDP.ContractSignatures.*` modules, none of which appear in the reviewed baseline `docs/architecture/layout-exceptions.json` (`HDP.Contracts` occurs 0 times in that file). | Verified pre-existing, not introduced by this session: a detached `git worktree` at pristine `HEAD` (`d602405cd`) reproduces a non-zero exit with an overlapping HDP module list (there: `NumStability.All misses 77 canonical descendant(s)`), 2319 modules / 386 unclassified vs. the working tree's 2359 / 309. | The organization loop cannot close while this debt is unrecorded; `unclassified_modules=309` in `gates/ch01.json` is confirmed accurate against a current scan, so the gate counter is not itself wrong. Ratcheting the baseline is a reviewed operation (`--write-baseline`) and was deliberately NOT performed, since doing so would mask the debt rather than resolve it. | open | Coordinator/operator: decide whether the HDP contract-module naming is canonical (then ratchet the baseline in a reviewed change) or should be renamed under the migration lane; this spans chapters 1, 2 and 5 and is outside a single chapter-01 increment. |
| S-CLAUDE-C01B-I002 | 2026-08-29, during the cross-lane classification increment | shared `main` worktree, concurrent writer | The Lean worktree is being written by another agent while this session runs. The tracked module count moved 2359 -> 2375 -> 2376 mid-session, and `NumStability/HDP/Scalar/BerryEsseen.lean` appeared (file mtime 00:49) one minute before a scan. HDP modules are tiered by ~134 *exact* manifest entries rather than a prefix rule, so every newly added HDP module reopens `unclassified_modules` until a manifest entry is added. | `check_layout.py` reported `new unclassified modules: NumStability.HDP.Scalar.BerryEsseen` on a rescan that had reported 0 minutes earlier; `git status` shows the file untracked; `NumStability`/`NumStability.All` still report it as a missing canonical descendant, i.e. the other session has not yet wired it into the aggregates. | The organization counters are only valid as of a scan instant. `unclassified_modules=0` was true at the scan backing this gate update, but a concurrent writer can reopen it at any time. Also note `tier_assignments` raises a hard `LayoutError` for "tier entries without files", so if the concurrent session renames or deletes a module I have classified, the layout scan will hard-error until the manifest entry follows. | open (coordination) | Consider a prefix rule for `NumStability.HDP` so new HDP modules are classified by construction; and re-run the organization preflight immediately before trusting the counter whenever another agent is active in the worktree. |
