# Limitations — session claude-chapter01-b

Record skill, harness, prompt, gate, tool, or agent-process limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Limitation | Consequence | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| S-CLAUDE-C01B-L001 | 2026-08-28, after closing the formalization and semantic loops | organization loop (`unclassified_modules`) vs. chapter-scoped formalization work | The chapter-01 gate's organization loop is repository-wide by design (completion-contract: "these counters describe the Lean repository rather than one source row"), so it cannot close while any module anywhere in the worktree is unclassified. A current scan shows all 309 unclassified modules are in the Higham numerical-stability lane (`NumStability.Algorithms` 255, `NumStability.Analysis` 54); **zero are HDP modules**. | Chapter 1's own formalization (45/45) and semantic-equivalence (48/48) loops are closed and every HDP-lane organization counter is zero, yet the chapter gate remains `ACTIVE` on debt that belongs to a different book's lane with a separately recorded owner. | No HDP Lean artifact is implicated; the debt is in `NumStability.Algorithms.*` / `NumStability.Analysis.*`. | Not resolvable safely from this invocation, and measured rather than assumed: applying the repository's own frozen classification proposal in full (`docs/architecture/lane-proposals/claude-classification/classification/modules.tsv`, 386 rows) would still leave **106** of the 309 unclassified, and would mark **44** modules `mixed` against a reviewed baseline that names exactly **9** — converting a currently-passing counter into a failure. The lane's own `apply_tier_proposal.py` additionally refuses by construction to write the shared manifest ("refusing to overwrite the shared tiers manifest"), i.e. the step is review-gated by design. A non-destructive proposed manifest was generated to a scratch path for review only; production `docs/architecture/tiers.json` was not modified. | open | Operator/lane owner: decide whether the Higham-lane classification (203 proposed + 106 unproposed, plus the 44-vs-9 `mixed` baseline question) is completed by that lane, or whether the chapter gate's organization loop should be scoped per-book. Chapter 1 has no further local organization work. |
| S-CLAUDE-C01B-L002 | 2026-08-29, after classifying the Higham lane | repository layout contract (`check_layout.py`) | Two failures remain in the layout contract that this increment did not address and did not cause. | (a) `new noncanonical modules`: ~110 `NumStability.HDP.Contracts.*` / `ContractSignatures.*` modules whose `C_01_hthm_...` naming violates the canonical-name rule and are absent from the reviewed baseline. Verified pre-existing by reproducing a non-zero exit on a detached worktree at pristine `HEAD` (`d602405cd`) before any edit of mine. (b) `NumStability`/`NumStability.All` miss canonical descendant `NumStability.HDP.Scalar.BerryEsseen`, a module another session created mid-run and has not yet wired into the aggregates. | Not applicable — neither is a Lean defect. | (a) needs a decision on whether the HDP contract-module naming is canonical (then ratchet the baseline) or should be renamed under the migration lane; it spans chapters 1, 2 and 5. (b) resolves itself when the concurrent session finishes wiring its module in. Neither blocks the chapter-01 gate, whose organization loop reads only `unclassified_modules`, `duplicate_wrappers`, `placeholder_findings` and `canonical_placement_pending`. | open | Lane owner / operator. |
