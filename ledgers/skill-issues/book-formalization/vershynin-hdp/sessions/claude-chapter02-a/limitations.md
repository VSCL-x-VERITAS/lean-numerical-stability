# Limitations — session claude-chapter02-a

Record skill, harness, prompt, gate, tool, or agent-process limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Limitation | Consequence | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| BF-VHDP-C02-A-L001 | 2026-08-28 | `tools/architecture/check_layout.py` `PLACEHOLDER_RE` | The forbidden-placeholder regex `\b(?:sorry|admit)\b|^\s*(?:axiom|constant)\b` matches any indented line beginning with the word `constant`, including ordinary `structure` field names. `constant` is not a Lean 4 command (it was replaced by `opaque`), so the `constant` alternative can now only fire on identifiers. | The checker reports `proof placeholders or axiom/constant commands: NumStability/HDP/Concentration/MetricMeasure.lean` for a file that contains no placeholder at all; a genuine placeholder scan of the HDP tree is clean. | The four matches are the `constant : NNReal` fields of `LipschitzCertificate` and `LipschitzMapData` and the two `constant := K` structure-instance assignments. Independent evidence that the tree is clean: `grep -rnE '\b(sorry|admit|unsafe|native_decide)\b|^ *axiom ' NumStability/HDP/` returns nothing. | Treated as a checker false positive, not a hygiene finding; the gate's `placeholder_findings` counter is set from the direct scan plus `#print axioms`, and this discrepancy is reported rather than silently absorbed. | open | Coordinator: report the false positive; a regex fix to `check_layout.py` belongs to a separately authorized tooling run, not to a chapter formalization pass. |
| BF-VHDP-C02-A-L002 | 2026-08-28 | faithfulness kit blind-translation role isolation, as run from Claude Code | The kit requires the blind translator to receive `the complete packet inline` with tools explicitly prohibited. The Agent tool available here cannot revoke a subagent's tools, so the prohibition is enforced by instruction only, and the inlined packet condensed the metadata blocks of the pure structure-projection dependencies D055-D060 and D066-D069 (their `Definition body` blocks were replaced by the descriptor `structure projection`); D001, the target types, and all mathematically load-bearing dependency bodies were supplied verbatim. | The requirement is stated in `.faithfulness-audit/skill/formalization-faithfulness-audit/SKILL.md` under `### Blind translator` and in `START_HERE.md` under `Non-negotiable audit boundaries`. | The blind pass for HDP-02-THM-2.8.1 is a genuine independent translation of the target type and of every semantically meaningful dependency, but its dependency coverage for the ten listed type-class projections rests on condensed metadata rather than the verbatim body text, and tool abstinence is not mechanically guaranteed. | Recorded rather than suppressed. The condensation touches only Lean type-class plumbing at distance 2 that cannot change the proposition's mathematical content; the affected IDs are named here so a reviewer can re-run those records verbatim. | open | Coordinator: report the deviation with the row. A future run on a harness that can spawn a genuinely tool-free agent should re-run the blind pass verbatim; the recorded packet SHA-256 makes that re-run detectable as a change. |
