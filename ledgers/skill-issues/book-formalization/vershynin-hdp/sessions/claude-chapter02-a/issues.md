# Issues — session claude-chapter02-a

Record skill, harness, prompt, gate, tool, or agent-process issues encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Symptom | Evidence | Impact | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| BF-VHDP-C02-A-I001 | 2026-08-28 | `lean-numerical-stability` build graph / `lakefile.toml` | The whole `NumStability/HDP` tree (77 modules) is unreachable from `NumStability.lean` and `NumStability/All.lean`, so the default `lake build` target never type-checked any HDP module. | `grep -c HDP NumStability.lean` = 0; `grep -n HDP NumStability/All.lean` empty; `find .lake/build/lib -path '*HDP*' -name '*.olean'` returned 0 before this session; `tools/architecture/check_layout.py` reports `NumStability misses 77 canonical descendant(s)` and `NumStability.All misses 77 canonical descendant(s)`. | All pre-existing Chapter 1/2/5 HDP work was unverified by the repository's own build target. Explicitly building all 77 modules in this session produced 0 errors, so the content was in fact sound, but nothing enforced that. | open | Coordinator: add canonical HDP aggregators, wire `NumStability.HDP` into `NumStability/All.lean`, and classify the 77 modules in `docs/architecture/tiers.json` as part of this chapter's organization loop. |
| BF-VHDP-C02-A-I002 | 2026-08-28 | `NumStability/HDP/Contracts` and `NumStability/HDP/ContractSignatures` module naming | All 69 HDP source-correspondence modules are named `C_NN_h<kind>_h<sec>_d<num>` (e.g. `C_02_hthm_h2_d6_d2`), which fails the repository's canonical UpperCamelCase module-name rule `^[A-Z][A-Za-z0-9']*$`. The canonical precedent for a numbered source lane is `NumStability.Source.Higham.Chapter02.Problem11`. | `tools/architecture/check_layout.py` reports `new noncanonical modules:` listing all 69 modules; `UPPER_CAMEL_RE` at `tools/architecture/check_layout.py:31`. The repository already carries 330 reviewed legacy naming exceptions for comparable debt. | Cosmetic/navigational, not mathematical: the modules compile, are now classified and reachable. Renaming them is a relocation touching the Chapter 1, 2 and 5 source aliases at once and would invalidate paths recorded in contracts, gates, ledgers and cached faithfulness evidence. | open | Deferred to a `book-formalization-migration` run with an explicit rename ledger. Proposed destination: `NumStability.HDP.Chapter02.Theorem06_3`-style locators mirroring `NumStability.Source.Higham`, with compatibility re-exports for the existing `hdp_02_*` declaration names, which are stable and must not move. |
| BF-VHDP-C02-A-I003 | 2026-08-28 | this session's own orchestration of the blind-translation role | The first blind-translation agent for HDP-02-THM-2.8.1 was launched with a packet in which the orchestrator had itself paraphrased the `meaning` of dependencies D002-D069 instead of supplying their types and bodies. That supplies the translator with the orchestrator's own reading of the dependencies, which is part of what the blind pass is supposed to produce independently. | The contaminated launch was detected before the agent returned and the agent was stopped without its output being written to `agent_outputs/`; a fresh stateless agent was launched with the verbatim target types and dependency bodies. | No contaminated artifact entered the audit record. Had it not been caught, the blind pass would have partly restated the orchestrator's interpretation and the blind/direct independence would have been illusory. | closed | Coordinator: fixed in-session by discarding the run and relaunching a fresh agent, per the kit rule that malformed or contaminated role output is replaced by a new run and never coached. Residual condensation is tracked as BF-VHDP-C02-A-L002. |
| BF-VHDP-C02-A-I004 | 2026-08-28 | `.faithfulness-audit/scripts/record_agent_run.py` | The script writes a hardcoded `"runtime": "Codex internal agent"` into every `agent_runs.json` entry, with no flag to override it. This session's audit roles ran as Claude Code internal agents, so the recorded runtime is wrong for every row audited here. | `audits/vershynin-hdp/chapter02/*/faithfulness/agent_outputs/agent_runs.json` each contain `"runtime": "Codex internal agent"` although the model recorded alongside is `claude-opus-5`; the script exposes only `--model`, `--reasoning-effort` and `--agent-id`. | The kit requires that provenance be recorded and never inferred. The model, effort and agent id are accurate; the `runtime` field is not, and a later reviewer reading it alone would attribute these runs to the wrong harness. | open | Coordinator: report the mismatch with the chapter and treat `model` + `agent_id` as the authoritative provenance. A `--runtime` option belongs to a separately authorized kit-maintenance run; the kit's own files must not be edited from inside an audit. |
| BF-VHDP-C02-A-I005 | 2026-08-28 | shared `lean-numerical-stability` worktree and shared collaboration checkout | A second session (Chapter 1, Codex) wrote to the same single worktree while this Chapter 2 session was running, with no isolated write scope between them. The worktree was clean at this session's first `git status`; by the end it also carried Chapter 1 edits this session never made. | Files changed by the other writer, with mtimes between 21:48 and 22:16 local while this session's own writes are 22:56 onward: `NumStability/HDP/Contracts/C_01_hprop_h1_d2_d4.lean` and its ContractSignatures twin (the type of `hdp_01_hprop_h1_d2_d4` changed - `[IsProbabilityMeasure]` added, the `Integrable` hypothesis dropped, and the `mu.real` half of the conclusion removed), a new `NumStabilityTest/HDP/Chapter01/Markov.lean` wired into `NumStabilityTest.lean`, a new `docs/vershynin-hdp/chapter01/` directory, and `books/vershynin-hdp/book.json` flipped from lifecycle `paused` to `active` in the collaboration checkout. A `codex` process (PIDs 600, 2039) was live throughout. | No damage to Chapter 2: the full default-target build ran after those edits and completed with 0 errors (5985 jobs), `hdp_01_hcor_h1_d2_d5` (the Chapter 1 result this chapter's blockers point at) still exists, and no file under `NumStability/` changed after this session's final build. But the coordinator's own rule that each collaborator gets an isolated write scope was not in force, so a Chapter 1 edit to a shared Chapter 1 declaration could have silently invalidated a Chapter 2 blocker's reuse recommendation, and the two sessions' `git status` views are entangled - neither can commit without staging the other's work. | open | Operator/coordinator: give each concurrent unit its own worktree (or serialize the two sessions) before the next pass, and stage commits per-path. This session deliberately left everything uncommitted rather than commit another session's in-flight Chapter 1 edits. |
