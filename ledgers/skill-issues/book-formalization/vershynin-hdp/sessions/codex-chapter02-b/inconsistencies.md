# Inconsistencies — session codex-chapter02-b

Record skill, harness, prompt, gate, tool, or agent-process inconsistencies encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Surfaces compared | Inconsistency | Evidence | Risk | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| VHDP-S-C02B-I001 | 2026-08-28 | Fresh generated prompt/profile validation vs. persisted `gates/ch02.json` and Section 2.8 audit tasks | The gate still named the superseded module-audit v2 epoch/hash and prior Claude session, while three audit tasks targeted window-scale aliases superseded by printed-form `*_hpsi1` declarations. | Fresh validation passed profile SHA-256 `b366ee13ce5c1890555f7dda961eda9c2bf9ce6e8ef71de6a17c3f5585196cab` and module-audit SHA-256 `481184f947b961d8532149c9095e679377269aec43c0132c0eb96472367f6aa9`; `rg` resolved the new declarations in `Bernstein.lean`; the gate itself described the old audit targets as superseded. | Reusing the stale bindings would attach faithfulness evidence to a different proposition and report obsolete readiness provenance. | addressed | Coordinator updated only the mutable gate/session binding and the three task targets, then will force-prepare the batch so prior evidence is archived rather than reused. |
| VHDP-S-C02B-I002 | 2026-08-28 | Generated prompt / completion contract vs. `organization_preflight.py` implementation | The prompt requires a current repository scan and forbids trusting inherited counters, but the preflight executable only compares the four integer values already stored in sibling gate JSON files; it does not scan Lean modules, tiers, placeholders, wrappers, or placement. | Direct inspection of `skills/book-formalization/scripts/organization_preflight.py` shows only JSON counter reads and equality comparison. A separate fresh `source_architecture.py` scan found 2359 modules and `check_layout.py` was launched to establish current evidence. | A passing preflight can certify agreement among stale counters rather than current organization state. | open | Skill maintainer: make preflight consume a current scan artifact or invoke the configured scans; until then every run must pair it with independent repository scans. |
| VHDP-S-C02B-I003 | 2026-08-28 | Per-declaration semantic audit intent vs. target-file hashing in the faithfulness kit | A proof change for Exercise 2.8.6 in the shared `Bernstein.lean` file invalidated the completed Exercise 2.8.5 audit even though the audited declaration, elaborated type, dependencies, blind packet, and source contract were unchanged. A later documentation/import cleanup in shared `GraphDegreeLaw.lean` similarly invalidated nine Chapter 2 audit manifests whose audited declarations and generated role packets were unchanged. | `validate_audit.py HDP-02-EX-2.8.5 --phase complete` changed from pass to `target: SHA-256 mismatch` immediately after removing the unrelated `hVar` parameter from later declarations in the same file; the Exercise 2.8.5 theorem text was untouched. In the later incident, all nine tasks were force-prepared, their regenerated blind/direct packet hashes compared byte-for-byte with the immediately preceding archives, and only exact matching cached role artifacts were restored before role validation, re-finalization, and complete validation. | Whole-file binding forces avoidable reruns, temporarily regresses honest gate coverage, and discourages reusable modules containing multiple independently audited declarations. | open | Faithfulness-kit maintainer: bind freshness primarily to the proof-free declaration/dossier and dependency hashes, while retaining the file hash as provenance; coordinator may restore cached role evidence only when regenerated packet hashes match exactly. |
| VHDP-S-C02B-I004 | 2026-08-28 | Blind-role prompt contract vs. first Exercise 2.8.6 transport invocation | The blind prompt requires the orchestrator-supplied dossier SHA-256, but the first transport invocation inlined only the prompt, schema, and blind packet; the packet does not itself contain that hash. | The isolated child returned a complete translation but explicitly reported that no dossier hash was supplied and used the all-zero sentinel. The prepared blind packet SHA-256 is `fdd8ee8cf87c2a5ae4c3db4b420558a0d5988ac36049e8788145266ecf439dd8`. | Accepting or coordinator-editing that result would break hash binding and provenance. | addressed | Coordinator rejected the result unchanged and re-invoked the same still-source-isolated blind role with only the exact dossier SHA as new orchestrator metadata; no source or repository context was added. Future transport instructions must always include this value explicitly. |
| VHDP-S-C02B-I005 | 2026-08-28 | Direct-judge prompt/schema requirements vs. generated `direct_review_packet.md` for HDP-02-THM-2.8.1 | The direct prompt requires every configured semantic check and the schema requires a nonempty `semantic_checklist`, but the generated direct packet ends after dependency D093 and contains neither the checklist IDs/text nor a pointer to them; `audit-task.json` also contains no checklist-profile field. | The fresh direct judge stopped before judgment and reported the missing authorized input. The installed `.faithfulness-audit/audit.config.json` separately selects `checks/core.json`. | A judge must either guess hidden checklist content, breach its allowed-input boundary, or pause for coordinator authorization. | open | Faithfulness-kit maintainer: embed the configured checklist in every role packet or add its exact hash-bound path to task metadata. Coordinator explicitly authorized only `audit.config.json` field `semantic_check_files` and the resolved `checks/core.json` for this run. |
| VHDP-S-C02B-I006 | 2026-08-29 | Blind-role no-tool isolation protocol vs. available nested-agent transport | Two fork-none transport invocations were instructed to load only the blind prompt, schema, and packet, then spawn a further no-filesystem child with those contents inline. Neither reached a visible child invocation after repeated multi-minute live checks; no artifact from either stalled turn was accepted. | Agent-tree checks repeatedly showed only the transport and no descendant. The second transport was interrupted, then re-invoked in the same fork-none context to judge solely from the already-loaded authorized material with all further tools forbidden; it returned a complete 93-dependency artifact bound to packet SHA-256 `ef29da9e1a96481aeac787ba3a8acd1b54967e299c77f75b267b5503703b74e0`. | The fallback preserves source blindness and input isolation by instruction but has weaker mechanical enforcement than a child runtime with no filesystem/tools at all. | open | Agent/runtime maintainer: provide a reliable inline large-packet child handoff or a first-class no-tool role invocation. Coordinator must record the fallback provenance explicitly and must not describe this run as mechanically filesystem-disabled. |
| VHDP-S-C02B-I007 | 2026-08-29 | Completed direct-judge response vs. requested artifact transport for HDP-02-THM-2.8.4 | The fresh direct judge completed a schema-shaped JSON judgment, but its follow-up repository transport wrote the literal text `PLACEHOLDER` to `direct_judge.json` and could not replace it. | The placeholder failed JSON validation. The coordinator located the one exact assistant response for task `HDP-02-THM-2.8.4` and role `direct-judge` in the immutable session log, transferred it byte-for-byte to the explicit artifact path, and then `validate_agent_output.py` passed; the restored artifact SHA-256 is `469561a7aa132fb8d629112df69be239f583d810afd7a570b2e95d29ac86d55f`. | A failed transport can obscure a completed independent judgment or tempt manual reconstruction, weakening reproducibility. | addressed | Agent/runtime maintainer: support structured artifact return or reliable exact-output persistence. Coordinator accepted only the exact logged response and recorded its original agent provenance. |
| VHDP-S-C02B-I008 | 2026-08-29 | Required fresh semantic roles for HDP-02-THM-2.8.2 vs. persistent agent-thread quota | After the repaired blind and direct roles completed, the runtime rejected a new root round-trip child and also rejected a fork-none grandchild requested through a transport-only worker, both with `agent thread limit reached`; completed and interrupted root children continued to occupy the quota. | Agent-tree output showed the root plus two interrupted workers and one completed direct worker. The unrelated `/root/thm281_transport` context had not seen the current target's Lean packet or direct output; it was re-invoked with access restricted to the current pinned source, source contract, blind artifact, checklist, and schema, and returned a valid source-only round-trip artifact SHA-256 `8b4220e7fcd90f93e8f99b2ab6f0ba39e00fd268a68c6c8cd4d88f6696a88d09`. The same runtime limit required reusing the unrelated `/root/ex286_blind_helper` context for restricted-input adjudication; its valid artifact SHA-256 is `010c922d649bbbb01099778b852a8b91c282850123948a9795c99f636c198c65`. | Both roles remained independent of prohibited current-target evidence under their role-specific input restrictions, but their process contexts were reused rather than mechanically fresh. | open | Agent/runtime maintainer: release completed/interrupted thread slots or provide first-class isolated role invocations. Coordinator must identify reused contexts in provenance and must not describe them as fresh fork-none agents. |
| VHDP-S-C02B-I009 | 2026-08-29 | Required fresh semantic roles for repaired HDP-02-COR-2.8.3 vs. persistent agent-thread quota | The runtime still exposed no fresh thread after completion of the prior audit, so every corollary role had to reuse an existing context under new role-specific input restrictions. | The blind role reused `/root/ex286_blind_helper` with only packet SHA-256 `a68c1bb15a193f6c1e0cc2f8e9fa260afc0cc226b8e3888400898fe1e9cfb754`, producing artifact `8e19239f8f1f5860301edb8bdec30fbbd334aed7434039e54c4008bff3567ae1`; the direct role reused `/root/thm282_direct`, producing `3742ccadbe46924d6399c5375e24a0bc830cde0a7c67d62da4ad816a49d0a3f6`; the source-only round trip reused `/root/thm281_transport`, producing `0b24a25c10660f272fa0597926ebeb3d16327eb78d97e2be26ae91d542dd9517`; adjudication reused `/root/ex286_blind_helper`, producing `a4fac3f205f4a487c1b6713d518b1d667bfbcfb35e133f5feae5c5832f4c3cb1`. Each artifact passed role validation and the complete audit passes. | Role-specific prohibited evidence remained excluded by instruction and recorded provenance, but none of the four roles had a mechanically fresh process context. | open | Agent/runtime maintainer: release completed/interrupted slots or provide first-class stateless role invocations. Coordinator must preserve the reused-context provenance and avoid claiming fresh fork-none isolation. |
| VHDP-S-C02B-I010 | 2026-08-29 | Equation (2.23)/(2.24) semantic-loop requirements vs. persistent role contexts and schema-sensitive transport | The Section 2.8 display audits again required persistent-context reuse, and several first responses failed purely mechanical requirements before any artifact could be accepted. | Equation (2.23) required a corrected round-trip authorization including the source after the first prompt omitted it, and its blind role retried a task-ID/schema mismatch. Equation (2.24) required a blind hash-typo correction and an adjudicator trigger/schema correction. Only validated artifacts were retained. Current Equation (2.23) source contract and decision SHA-256 values are `894db76e160bbf698100d690e83940794a961375f621ae59dc3aea91bc97e7d2` and `6166559dff235c47636eb7eb75247b643536b10c67afe4f1c1755bdfa3e92f59`; Equation (2.24) values are `32732704f6273db3503f2f15b16554abc73378a4b2befa3cae6bbfc4004b56d1` and `d161ce5aaa22d8e7f2f135814661a35a59a31271f31f38fb4e49d53ac701883a`. | Incorrect task IDs, hashes, missing authorized inputs, or malformed triggers would break reproducibility or silently bind a semantic judgment to the wrong evidence. | addressed | Coordinator rejected every malformed response unchanged, retried with only the missing/corrected coordinator metadata, recorded persistent-context provenance, and accepted only schema- and hash-valid artifacts. First-class structured role output remains the maintainer-level remedy. |
| VHDP-S-C02B-I011 | 2026-08-29 | Source-order Section 2.1 semantic loop vs. persistent role quota and blind dependency enumeration | Equation (2.1) had to reuse the three established contexts under strict role allowlists; its first blind response also appended nonexistent dependency D074 even though the packet ended at D073. | Source extraction reused `/root/ex286_blind_helper`, blind translation `/root/thm282_direct`, direct judgment `/root/thm281_transport`, and round trip `/root/ex286_blind_helper`. The invalid blind artifact was rejected; a packet-only retry covered exactly D001-D073. Validated artifact SHA-256 values are source contract `3a83daa4ee4465081d587a17ba2ad244a1314f29c5309cb0ab3f9f255d9cbac6`, blind `870754cd49891937fd9ed4ef7b000c1026fa478ef42eb52a93ce2bbf26c566ae`, direct `1e59cc95c1434b0d27a7836e4e770ce7b9f6e40bd347de1c43b1e8dc98f066d4`, round trip `3b60a6fa477bb1bef1953e551b5eed7078b5f1692c7bd9d3e395546266743c39`, and decision `4b1d0090f61bf349235058602bfcf4bdba44326e36ac78dbed3c321737b1f9af`. | Persistent contexts are weaker mechanical isolation than fresh stateless roles, and off-by-one dependency invention would make the artifact fail exact dossier coverage. | addressed | Coordinator recorded role reuse, rejected the extra dependency, and finalized only after all role validators and complete audit validation passed. Runtime should provide fresh stateless role slots and structured output persistence. |
| VHDP-S-C02B-I012 | 2026-08-29 | Section 2.1 normalization-identity semantic loop vs. persistent role quota | The normalization-identity audit also had to reuse the three established contexts under strict, role-specific allowlists because no fresh role slot was available. | Blind translation reused `/root/thm282_direct`, source extraction and round trip reused `/root/ex286_blind_helper`, and direct judgment reused `/root/thm281_transport`; `agent_runs.json` records the weaker process isolation. Validated SHA-256 values are source contract `57aaf34ae74095e10d40591e6c20f6704498b499b1198b57853739effc4ebe03`, blind `35ad4b712514f68e09cd6f62b0c7d63b54fb5d31d38d72fa09088d42b5cbcddf`, direct `699e474f66870ee51a07d445f903472500d1b595b87b0746a008890e6565a613`, round trip `a85b87eb2ea70e9f08e5ae716b4a0a3e4e1bfcf309186afeda512cf88720cc9e`, and decision `daaf3902b556719146297deaa64842d8bb8dd8d41fa5270f63cdf245548600d5`. | Role-specific prohibited current-target evidence remained excluded by instruction, but the contexts were not mechanically fresh/stateless. | addressed | Coordinator recorded exact reuse provenance and accepted only schema-, hash-, and complete-audit-valid artifacts. Runtime should release completed threads or provide first-class isolated structured roles. |
| VHDP-S-C02B-I013 | 2026-08-29 | Incremental proving alongside Proposition 2.1.2 audit vs. faithfulness kit whole-import-file binding | Adding the later Equation (2.3) declaration to `GaussianTails.lean` after preparing the Proposition 2.1.2 audit invalidated that audit's prepared manifest even though the audited proposition type and all semantic dependencies used by it were unchanged. | `validate_audit.py HDP-02-PROP-2.1.2 --phase prepared` failed only with `local import 3: SHA-256 mismatch ... GaussianTails.lean`. The in-flight blind role was interrupted and the completed direct result was rejected without transfer; the audit was force-prepared again after the full 3667-job build. | A harmless sibling declaration addition forces all source and judge roles to rerun and can waste independent-agent work during the required prove/audit overlap. | addressed | Coordinator archived/reprepared before accepting any role artifact. Faithfulness-kit maintainer should implement the declaration/dependency-level freshness remedy already identified in VHDP-S-C02B-I003. |
| VHDP-S-C02B-I014 | 2026-08-29 | Gaussian-atom semantic repair loop vs. persistent role quota and artifact transport | The runtime again offered only the three persistent role contexts, and completed schema-valid JSON had to be recovered byte-for-byte from immutable session logs rather than through a first-class structured artifact channel. The first round-trip result also correctly rejected the initial `Measure.real` target because the blind packet could not exclude `ENNReal.toReal ∞ = 0`; the target was repaired and all target-dependent roles rerun. | After strengthening the wrapper to literal `standardNormalLaw {0} = 0`, the unchanged source contract was revalidated at SHA-256 `396b7b5ce4fcc3281b4f09575f57f040e7922ef9397c9fffd44bd7b873c6f45f`; strict-allowlist persistent roles produced validated blind/direct/round-trip artifacts `a96a49494f7ec5fd1c94b363be597e9e2aa67ad72c7bf540e4804e5b9c426118`, `81625273481132c78a573bf3f4e4d4a6a7e78ca54266fcb8e008bc87f6e3b6f4`, and `76f1d2ef3d698c2ce23f37d1a4d282c83c2d206449fdd219ce3af8f5f0716104`; complete decision `8a4b063e9031c2195852ed8d857b42d0bdb30c51c474a2ca57baa9a8b44bd241` is accepted. | Reused contexts and manual log transport are weaker mechanical isolation/persistence than fresh stateless roles with structured output, although the successful semantic disagreement demonstrates that the role separation remained useful. | addressed | Coordinator preserved strict role allowlists, rejected the ambiguous target rather than adjudicating it, and recorded exact hashes/provenance. Runtime maintainer should release completed threads and provide first-class structured role output. |
| VHDP-S-C02B-I015 | 2026-08-29 | Section 2.1 asymptotic-notation semantic loop vs. persistent role quota and machine-generated adjudication triggers | The notation audit reused the three persistent contexts under strict evidence allowlists. Its first adjudicator prompt summarized four substantive disagreements, but the finalizer required seven exact, schema-sensitive trigger strings computed from the two judgments. | Validated source/blind/direct/round-trip artifacts have SHA-256 values `eb905ccb6c16b30c207712447ee58a469c098e3105060e6369ed7bd71a2903d6`, `22d548f053b2d7640e9bd3db3006e75d579184498b6ce7a361fc0a3500515a22`, `4f172a397bcc4c0ef5fed3c27611761c6c65dafc5055f11df89e20d9b882e132`, and `0b342cf0947abd99c6118bdf5c2e2b047e54d39d31b5242baf007a463ae60792`. The first adjudicator artifact was rejected by `finalize_audit.py`; the same restricted adjudicator reissued the exact seven-trigger artifact `0d2995f9f1546bd1ad7b0378d160fb4bd3db3f1067fd6c51d1814c2c65fefe23`, resolving the blind-name opacity and source-silent type specialization as faithful-equivalent. Complete decision `4560a25fe1541a92eee5c58ceb434a65dc2df4c1797ba3097bee93bbeed1fd2d` passes. | A semantically correct adjudication cannot finalize unless the orchestrator supplies the finalizer's exact reason vocabulary; persistent contexts remain weaker than mechanically fresh stateless roles. | addressed | Coordinator accepted only the exact-trigger, schema-valid artifact and recorded persistent-role provenance. Faithfulness kit should expose computed triggers as a structured adjudicator input, and runtime should provide fresh isolated role slots. |
| VHDP-S-C02B-I016 | 2026-08-29 | Central-binomial semantic loop vs. persistent role quota, large blind-packet transport, and checklist/schema retries | The 136-KB blind packet could not be dynamically attached inline, the first otherwise complete blind artifact had a forbidden top-level `task_id`, a different-context retry used the wrong schema, and the first direct artifact omitted C11-C12 after the coordinator mistakenly requested only C01-C10. No unused agent context was available for the prescribed fresh retries. | The still-source-blind role made a mechanical schema-only reissue through the sole hash-named packet; the direct role reissued the same judgment with C11-C12 from its unchanged authorized evidence. Validated source/blind/direct/round-trip hashes are `23641de2a92ae31aba6f0211f1ec1fd81fcf657186310a4f0ac70019d724e5cd`, `c8888d4e85c02d1e301568ea1434713b0b80c093a529ac9af3969925cad34872`, `df0d19daa0027b5846d0194ce81beb607d1fef6d08b9cf4b7e89f5ba4e2f9767`, and `b8750378f68d50fff12d9f0d599bc502be96d4f340248dfe29403669f1478b42`. Exact nine-trigger adjudication `7154fe9fc987bd6303a3e2ef1c2fab8a89c3a76a2f6e7d4ff451af7fd7a2834c` resolved inherited `Z_N`, asymptotic scope, and the zero-trial boundary; complete decision `80f7b8db2089e70ebc39b4097036832cdf1e34f07b1582237aaabf98581c3d8e` passes. | Hash-only file transport and persistent retry contexts are weaker mechanical isolation than fresh inline stateless roles, while incorrect checklist-range metadata causes avoidable schema failures. | addressed | Coordinator rejected malformed artifacts unchanged, transferred only exact logged responses, preserved strict role allowlists, and finalized only validated evidence. Runtime should provide fresh stateless structured roles with dynamic packet attachment; the kit should expose the configured checklist range in each role packet. |
| VHDP-S-C02B-I017 | 2026-08-29 | Exercise 2.1.4 semantic loop vs. persistent role quota and blind-packet attachment limits | All four semantic roles again reused the three persistent contexts. The 29-KB blind packet was supplied as a sole hash-named read-only file because the collaboration interface has no dynamic inline attachment channel; this is weaker mechanical isolation than the prescribed inline stateless role. | Strict allowlists produced validated source/blind/direct/round-trip hashes `536e390214170a351d40249602a9d9cb89a59ea95da285af23c49b634ec02fbf`, `87a24e8f4cf186c7d6d83ad44ed22411a3df07d9d8c121b7ac3f70e3a0457089`, `35be4488bc47432c7fb8520e9494648be158b72a483d23bdb262d8e51fbf89ab`, and `60edcced2b40811dde651b890bede586b388f41aec1bb9ef7db8ceec132dcb70`. Exact ten-trigger adjudication `176e5a9037f3af3cd7774eed8ef3bff427ad21937e431790d063866529a3ea01` resolved the blind artifact's open-versus-closed Gaussian-tail uncertainty from atomlessness and finite probability mass; complete decision `f3bb312ee2d0de1f2a44ab528bc4eef9e15569d3ee7a1729658bcf650ddfb5b1` passes. | Persistent contexts and filesystem packet transport reduce mechanical assurance even though role-specific prohibited evidence remained excluded and the semantic disagreement was surfaced and adjudicated. | addressed | Coordinator recorded exact provenance, accepted only schema/hash-valid outputs, and finalized only after complete validation. Runtime should provide fresh stateless structured roles and a first-class inline packet attachment mechanism. |
| VHDP-S-C02B-I018 | 2026-08-30 | Direct Lean error filter vs. full Lake JSON build | The standard direct-check pipeline `lake env lean ... | rg 'error:'` returned no matching diagnostic for an unknown identifier (`abs_add`) in the new combined Stein theorem, while the full `lake build` JSON invocation reported it at line 4987. | The first full target build failed after 196 seconds with `Unknown identifier abs_add`; replacing it by Mathlib's `abs_add_le` made both the widened direct filter and the complete 3440-job build pass. | Treating the empty narrow-filter output as sufficient validation would have recorded a non-building declaration. | addressed | This run now requires the full target build after material theorem increments and widens diagnostic filters to include `Unknown identifier`; skill maintainer should make the canonical direct command propagate/print all Lean failures independently of logger format. |
| VHDP-S-C02B-I019 | 2026-08-30 | Definition-row gate target vs. faithfulness-kit preparation | The gate named `hdp_02_hdef_h2_d2_d1`, a `noncomputable def`, as the sole source-facing declaration for Definition 2.2.1, but `prepare_audit.py` extracts only `theorem` or `lemma` headers and rejected the task before dossier generation. | `prepare_batch_audit.py vershynin-hdp-chapter02-section-2-2` returned `cannot find theorem or lemma source declaration for NumStability.HDP.Contract.hdp_02_hdef_h2_d2_d1`; existing audited definition rows target proposition-valued `*_spec` or theorem wrappers. | The queue described the row as locally auditable although its only named declaration was outside the audit kit's accepted target kinds. | addressed | Added the non-invasive theorem wrapper `hdp_02_hdef_h2_d2_d1_spec`, preserving the existing data definition and exposing exactly the two source point masses plus the immediately following affine Bernoulli equivalence; the audit task now targets that theorem. The kit should either document theorem-only targets at setup or support proposition-bearing definitions. |
| VHDP-S-C02B-I020 | 2026-08-30 | Inherited Bernoulli-shift target vs. direct semantic judgment | The gate treated `affineBernoulliIsRademacherIff` as ready for auditing, but that theorem quantifies only over the canonical family `PMF.bernoulli p`; the printed sentence characterizes an arbitrary random variable/law by whether its affine image is Rademacher. | The first fresh direct judge classified the inherited target `not-faithful-weaker`, with source-implies-Lean yes and Lean-implies-source no, because the target assumed family membership before stating the equivalence. | Merely auditing and promoting the inherited theorem would silently certify a specialization as the book's full iff characterization. | addressed | Preserved the specialized theorem, added the arbitrary-real-law theorem `affineLawIsRademacherIff` using the inverse affine pushforward, changed the audit target, force-prepared target-dependent evidence, and required fresh blind/direct/round-trip judgments before promotion. |
| VHDP-S-C02B-I021 | 2026-08-30 | First Bernoulli-shift repair vs. source-only round-trip judgment | The first repair generalized from a parameterized Bernoulli family to arbitrary `PMF ℝ`, but a PMF still represents only a discrete law, whereas the printed random-variable characterization has no discreteness assumption. | The repaired direct judge accepted the law-level target, but the source-only round-trip judge independently classified it `not-faithful-weaker`, with source-implies-translation yes and translation-implies-source no, citing the explicit PMF restriction. | Accepting the first repair would still omit nondiscrete source instances even though the equivalence is valid there by invertibility of `x ↦ 2x - 1`. | addressed | Preserved both reusable PMF theorems, added `affineMeasureIsRademacherIff` for arbitrary `Measure ℝ`, changed the audit target again, archived/reprepared target-dependent evidence, and reran every target-dependent semantic role. |
| VHDP-S-C02B-I022 | 2026-08-30 | Organization preflight counters vs. current repository layout scan | `organization_preflight.py` initially passed by comparing inherited zero counters across two gates, while fresh repository scans found noncanonical modules. Every material layout increment in this run has been followed by a new scan and explicit tier classification; the preflight continues to fail because Chapter 1 still records a stale zero rather than this Chapter's current scan-bound placement count. | The latest `tools/architecture/check_layout.py` run inventories 2,415 Lean modules, zero unclassified modules, 9 mixed modules, 117 modules missing docs, 383 legacy naming exceptions, 21 declaration-bearing umbrellas, zero unsorted aggregate imports, and exactly 136 new noncanonical placements against the reviewed baseline. Gate `ch02.json` now records `{unclassified_modules: 0, duplicate_wrappers: 0, placeholder_findings: 0, canonical_placement_pending: 136}`, while sibling `ch01.json` still records four inherited zeros; the required preflight therefore fails closed and explicitly reports the mismatch. | Reporting organization completeness from inherited gate zeros would contradict current repository evidence; changing another chapter's PASS gate or rewriting the reviewed repository ratchet would materially expand this unit's scope. | open | Keep exact scan evidence and do not describe organization as closed. Migration/layout owner must coordinate the repository-wide 136-placement/ratchet decision and reconcile sibling gates; this run classified and documented the new reusable `GraphDegreeMean` and `LowerChernoffBoundary` modules plus the source-facing expected-degree, zero-threshold, fixed-vertex degree-Chernoff, finite union-bound, and equation (2.10) contracts, updated their umbrellas, and introduced no unclassified module, duplicate import, proof placeholder, or unsorted aggregate import. |
| VHDP-S-C02B-I023 | 2026-08-30 | Round-trip role authorization vs. installed checklist locator | The first arbitrary-measure round-trip instruction authorized `.faithfulness-audit/checklists/core.json`, but the installed kit stores the configured checklist at `.faithfulness-audit/checks/core.json`. | The fresh role stopped after the failed read and requested the correct locator; `rg --files .faithfulness-audit` resolved `checks/core.json`. The coordinator authorized that single corrected path before the role performed its review, and the resulting artifact validated. | An incorrect allowlisted locator can either stall an isolated role or tempt it to search outside its authorized evidence boundary. | addressed | Corrected the locator without broadening any other evidence access and recorded the correction in `agent_runs.json`. Future role prompts must resolve the configured checklist path before spawning. |
| VHDP-S-C02B-I024 | 2026-08-30 | Required prove/audit overlap vs. whole-file target freshness for Theorem 2.2.2 | Adding the next source-row normalization lemmas to `Hoeffding.lean` while the theorem audit roles were running invalidated the prepared target-file hash, although the audited declaration and its dependencies were unchanged. | `split_batch_source_contract.py` rejected the source contract with `target: SHA-256 mismatch` immediately after the sibling WLOG lemmas were added. Removing only that uncommitted sibling increment restored target SHA-256 `b2516642cbc7b90008d4cbdcbf7008aca1307b4bc624d17bcc1813e8078ccb22`, after which splitting and both prepared-role validations passed. | The mandated proving/audit concurrency conflicts with file-granular freshness and can waste independent audits or force unrelated theorem work to pause. | addressed | Reverted only the not-yet-recorded sibling increment, preserved the repaired audited theorem, and deferred the WLOG addition until this audit is finalized. This is another concrete instance of the open kit-level issue VHDP-S-C02B-I003. |
| VHDP-S-C02B-I025 | 2026-08-30 | Initial WLOG-normalization wrapper vs. fresh direct semantic judgment | The first wrapper encoded only the positive coefficient-energy scaling lemma, adding `0 < ∑ i, a i ^ 2`, while the selected source proof step occurs inside Theorem 2.2.2's universal coefficient domain. | The validated fresh direct artifact SHA-256 `5c4c1ed6f600db658a8c2dba4ff042a694e9bee95d8a3f60c9885ab9ca2908a1` classified the target `not-faithful-weaker`, with Lean-implies-source `no`, because it omitted any zero-vector disposition. | Promoting the positive branch alone would certify only a restricted normalization calculation rather than the complete without-loss-of-generality reduction. | addressed | Preserved the positive-energy reusable lemmas, repaired the source wrapper to expose the exhaustive zero-energy/trivial-event versus positive-energy/unit-normalization dichotomy, rebuilt both modules, archived the rejected evidence through forced re-preparation, and required fresh target-dependent judgments. |
| VHDP-S-C02B-I026 | 2026-08-30 | Generated repaired-target direct packet vs. direct-judge source-comparison contract | `direct_review_packet.md` again contained the Lean dossier and dependencies but no authoritative source passage, source digest, or inherited source context, so the fresh direct judge could not perform the requested comparison. | The first repaired-target direct response returned the all-zero source-hash sentinel, classified the result `undetermined`, and explicitly reported `missing-authoritative-source-evidence`; no artifact was persisted or accepted. This repeats the open kit defect in VHDP-S-C02B-I005. | Treating the generated packet as self-contained would convert a missing-input stop into a spurious semantic verdict or tempt the judge to search outside its evidence boundary. | addressed | Coordinator authorized only the pinned Chapter 2 PDF, current source locator, and the enclosing Theorem 2.2.2 context, then retried the same otherwise fresh judge; the kit should embed or hash-link this evidence in every direct packet. |
| VHDP-S-C02B-I027 | 2026-08-30 | Equation (2.5) source contract vs. inherited/repaired inequality-only wrapper and initial direct judgment | The source contract marks both the exponential-event probability equality and the following Markov inequality as undebatable parts of display (2.5), but the first `hdp_02_heq_h2_d5` alias exposed only the final inequality. Its direct judge accepted this as faithful-stronger because the equality is derivable, while the blind translation correctly contained only the inequality. | Initial validated artifacts are archived under the first prepared generation: source contract SHA-256 `3f22d3dbe19159a5d05e7d5c702b179d92ae993f6e0e69a4a7c4a3112fa4ce07`, blind `8c1f5df09426bc97d470996fc3f66d5b37d35490d376c62241d32de4ab19820a`, and direct `358ebcd39a3bcfc9771eacec09108943e4822da5107a3561c5063e7c343aeb80`. | Treating a derivable omitted conjunct as source-faithful would understate the displayed contract and make later users reconstruct a source-level equality from proof internals. | addressed | Strengthened `hdp_02_heq_h2_d5` to return the exact probability equality together with the one-sided Markov bound, proved equality by strict monotonicity of `exp` at `λ > 0`, rebuilt 3329 jobs, force-prepared fresh packets, and required fresh target-dependent roles. |
| VHDP-S-C02B-I028 | 2026-08-30 | Repaired Equation (2.5) blind-role response vs. artifact persistence | The fresh blind role returned one schema-valid JSON object, but its first apply-patch persistence produced a file with extra trailing data; the artifact hash matched the corrupted file and `validate_agent_output.py` rejected it at line 1 column 15282. | The invalid file SHA-256 was `f62d58fc46bcc96bd02c7144eb40d48dd98b56bf64609117726a252d1c2a6b34`. The same still-source-blind agent replaced the entire file rather than appending, then role validation passed with SHA-256 `3592ffd4c8d1fd0ef946381f85cf664f9cc43654fdd66bcedc11a725c03a19b7`. | A transport defect can make correct independent semantic work unusable or tempt manual reconstruction. | addressed | Rejected the malformed artifact unchanged, required whole-file exact replacement by the originating role, and accepted only the validated final artifact. Runtime should provide first-class structured output persistence. |
| VHDP-S-C02B-I029 | 2026-08-30 | Equation (2.5) adjudicator response vs. faithfulness-kit artifact naming contract | The adjudicator first persisted its schema-valid result as `adjudication.json`, while the installed validator and finalizer require the exact filename `adjudicator.json`. | The role validator/finalizer reported the required adjudicator artifact missing. The originating restricted adjudicator deleted only its incorrectly named file and persisted the identical JSON as `agent_outputs/adjudicator.json`; validation then passed at SHA-256 `2beb037e58be7805a5bfdab8039616466b4b555e4bd345b890ae1cab624830fb`. | A near-synonymous filename can strand a completed independent judgment or invite coordinator-side reconstruction. | addressed | Accepted only the originating agent's correctly named, schema-valid artifact and removed the unusable duplicate. Role prompts should state the canonical output path explicitly, or the runtime should provide schema-bound structured persistence. |
| VHDP-S-C02B-I030 | 2026-08-30 | Required proving/audit overlap for consecutive Section 2.2 rows vs. whole-import-file freshness for Equation (2.6) | Adding the later exact cosh-MGF sibling theorem to `Hoeffding.lean` after all Equation (2.6) roles completed invalidated Equation (2.6)'s prepared local-import hash even though its audited wrapper, theorem type, blind packet, dependencies, and role artifacts were unchanged. | `finalize_audit.py HDP-02-EQ-2.6` failed only with `local import 2: SHA-256 mismatch ... Hoeffding.lean`. Temporarily removing exactly the not-yet-gated cosh sibling restored prepared file SHA-256 `b2516642cbc7b90008d4cbdcbf7008aca1307b4bc624d17bcc1813e8078ccb22`; prepared validation, no-adjudication check, finalization, and complete validation then passed. The exact cosh increment was reapplied afterward and rebuilt. | File-granular freshness makes the mandated overlap between proving the next row and auditing the current row unsafe, wasting completed role work or forcing temporary sibling rollback. | addressed | Preserved every independent Equation (2.6) artifact unchanged, finalized only at its original prepared hash, then reapplied and rebuilt the next proof increment. This is another concrete instance of open kit issue VHDP-S-C02B-I003; declaration/dependency-level freshness remains the required maintainer remedy. |
| VHDP-S-C02B-I031 | 2026-08-30 | Fresh blind-role contract vs. collaboration transport for the 92-KB Exercise 2.5.7 dossier | The first fork-none blind agent completed with an empty response before any packet payload could be delivered. The collaboration interface exposes no dynamic file attachment to a no-tool agent, and a single message containing the full 92-KB generated packet is impractical in this channel. | The empty `/root/ex257_blind_translation` result was discarded and never persisted or recorded. A new fork-none agent `/root/ex257_blind_translation_retry` received the full elaborated target, all local semantic definitions, and one meaning-bearing record for every dependency D001-D154 in four source-free messages, bound by the prepared dossier SHA-256 `830c70cd1322b8939a4d8cbb478f2b1b82aea37338e2d2f12ccd0bb12e01e010`; standard external-frontier boilerplate was serialized compactly rather than byte-for-byte. | Source blindness and complete dependency enumeration are preserved, but transport is weaker than a single mechanically attached byte-identical dossier and the child cannot independently recompute the packet hash. | open | Accept only a schema-valid D001-D154 artifact and record the exact retry provenance; runtime maintainer should provide first-class large-packet attachment to a no-tool stateless role. |
| VHDP-S-C02B-I032 | 2026-08-30 | Exercise 2.5.7 role semantics vs. exact JSON-schema persistence | The fresh blind and direct roles each returned a complete semantic judgment but their first responses failed mechanical schema requirements: the blind dependency entries lacked the required `reuse_sha256` shape, while the direct response used flat implication fields, `check_id`, and omitted required dossier/dependency fields. | Neither first response was accepted as an artifact. Each same originating role made a schema-only reissue using only its canonical role schema and role-specific dependency-reuse ledger. The corrected blind artifact validates at SHA-256 `f5792a029102e3808596c161ec25ea5343e802d41277ad4cec2179f70c688618`; the corrected direct artifact validates at SHA-256 `5395feebd009ed89c23c8b1f16ad646150bd217d79ab613b67c4344d6bb4098b`. | Semantically complete independent work can be lost or manually reconstructed if the runtime does not enforce the exact output schema at generation time. | addressed | Rejected both malformed drafts unchanged, recovered only exact corrected responses from immutable session logs, recorded their originating-agent provenance, and accepted them only after role validation. Runtime should provide schema-constrained structured output. |
| VHDP-S-C02B-I033 | 2026-08-30 | Required source-facing repair module vs. repository naming ratchet | The new correctly tiered Chapter 2 source contract module follows the repository's existing `C_02_*` locator naming convention, which is intentionally noncanonical under the current Mathlib-style name checker and therefore increments the review-pending placement queue. | After adding `NumStability.HDP.Contracts.C_02_hrem_h2_d3_d4`, the first layout scan reported exactly one unclassified module. Adding the explicit `source` tier entry removed that defect; the next full scan reported 2,416 modules, zero unclassified, 9 mixed, 117 missing docs, 384 legacy naming exceptions, 21 declaration-bearing umbrellas, zero unsorted aggregate imports, and 137 new noncanonical placements. | Leaving the module unclassified would violate organization initialization; pretending its locator name is canonical would undercount current migration debt. | open | The module is explicitly classified as source-tier and the gate records 137 review-pending placements. Repository migration owner must reconcile the source-locator naming policy and repository-wide ratchet together with VHDP-S-C02B-I022. |
| VHDP-S-C02B-I034 | 2026-08-30 | Round-trip role authorization vs. verified kit filesystem layout | The first fresh Remark 2.3.4 round-trip instruction named nonexistent `.faithfulness-audit/kit/...` aliases instead of the verified installation's direct `prompts/`, `schemas/`, and `checks/` paths. | The isolated role stopped before reviewing evidence and reported all missing paths. A current repository listing resolved `.faithfulness-audit/prompts/roundtrip_judge.md`, `.faithfulness-audit/schemas/roundtrip_judge.schema.json`, and `.faithfulness-audit/checks/core.json`; there is no separate `semantic-checklist.md` or `core/source-contract.md` file. | Incorrect allowlisted framework paths can stall a source-isolated role or cause unauthorized evidence discovery. | addressed | Coordinator corrected only the three canonical installed paths, identified the task-local `source_contract.json` as the source contract, preserved the original source/task-only prohibition, and instructed the same fresh role to continue without inspecting Lean, direct judgment, or history. Future role launchers must resolve paths against the verified installed kit before dispatch. |
| VHDP-S-C02B-I035 | 2026-08-31 | Incremental Section 2.5 proving vs. completed Equation (2.14)/(2.15) file-level audit bindings | Adding the later exact-gauge square-moment declarations to shared `SubGaussian.lean` invalidated both just-completed equation audits although neither audited declaration nor its theorem type was edited. | Immediately before the sibling increment, complete validation passed for both rows with decision SHA-256 values `e7edadf366bbb672b4fe2a0edff5001dbd64e3242ae717ea6daabb4e2bff7591` and `4fc4d173981059294a22cad4b3be8f5cef6bf0eec96fdbbfc73e63dfd385d145`. Immediately afterward, `validate_audit.py HDP-02-EQ-2.14 --phase complete` failed only with `target: SHA-256 mismatch for .../SubGaussian.lean`. | The required prove/audit overlap makes a semantically sealed declaration temporarily unverifiable after unrelated same-module progress and forces packet-hash comparison or redundant roles. | open | This is another concrete instance of VHDP-S-C02B-I003. Finish the current shared-module material increment, force-prepare affected audits, reuse source-only evidence, and restore target-dependent evidence only when regenerated packet hashes match exactly; the kit maintainer should bind freshness at declaration/dependency granularity. |
| VHDP-S-C02B-I036 | 2026-08-31 | Blind-role no-tool contract vs. collaboration attachment support for the new Section 2.5 dossiers | The installed blind prompt requires the prepared packet inline and forbids tools, but the collaboration runtime has no file-attachment primitive for fork-none agents; the two current prepared packets are 38,863 and 42,783 bytes. | Fresh fork-none roles were given no inherited conversation and were authorized to read exactly one hash-pinned `blind_review_packet.md` plus the fixed prompt/schema/checklist, with explicit prohibitions on source, task metadata, other repository evidence, web, and other agent outputs. Packet SHA-256 values are `9864a3687758f9488378b7f9129a6f73660a91bd4626833b471dd5bd4f7ba419` and `a8b9ea7257940cbe66729259b30cc8ec9e981f7e8db212cd626651da56f33809`. | Source blindness and evidence minimization remain enforceable, but the runtime transport does not literally satisfy the prompt's inline/no-tool mechanism. | open | Accept only artifacts whose dossier hashes, dependency inventories, schemas, and fresh-agent provenance validate; runtime/kit maintainers should provide a first-class immutable packet attachment to a stateless no-tool role. |
| VHDP-S-C02B-I037 | 2026-08-31 | Blind-role no-tool contract vs. transport of the 61,073-byte psi-two minimality packet | The same attachment limitation recurred for the fresh minimality audit: the packet could not be supplied inline to a fork-none role without exceeding the practical collaboration message channel. | The role inherited no conversation and was authorized to read exactly the prepared `blind_review_packet.md` bound to SHA-256 `d1098521bc061b57bae1d276332a3789d9d3ea0a1967bda0672ebd70b6e51b66`; it remained prohibited from reading the source, task metadata, direct packet, repository code, other role outputs, or the web. Its artifact validates at SHA-256 `51e7f75897ad1cfaf8f85bab69bb5c3b256810a0236b83bd182b7b639dc4401e`, and the complete audit validates. | The semantic blindness boundary is explicit and hash-bound, but the runtime did not literally enforce the prompt's inline/no-tool transport mechanism. | open | Preserve the exact constrained-read provenance and accept only schema-, hash-, and complete-audit-valid evidence; runtime/kit maintainers should provide an immutable attachment channel for stateless no-tool roles. |
| VHDP-S-C02B-I038 | 2026-08-31 | Blind-role no-tool contract vs. transport of the 40,603-byte Exercise 2.5.9 packet | The collaboration runtime again supplied no inline file-attachment primitive for the fresh fork-none blind role, so the prepared packet could not be delivered under the prompt's literal no-tool mechanism. | The role inherited no conversation and was authorized to read exactly the prepared `blind_review_packet.md` bound to SHA-256 `d2137e4c118e25765158d78fa3b7348bc3a6d25f35933d29a03ed112b34bde7b`; it remained prohibited from reading the source, source contract, task metadata, direct packet, repository code, other role outputs, or the web. Its D001-D081 artifact validates at SHA-256 `8db83c3d7543cab6f7def76c23eba00f1da88bed42f1f58a7ef7dda4f876c4cb`. | The role-specific semantic blindness boundary is explicit and hash-bound, but process isolation is weaker than an immutable inline packet delivered to a stateless no-tool runtime. | open | Preserve the exact constrained-read provenance and accept only schema-, hash-, and complete-audit-valid evidence; runtime/kit maintainers should provide immutable large-packet attachment to stateless roles. |
| VHDP-S-C02B-I039 | 2026-08-31 | Repaired Exercise 2.5.9 blind-role contract vs. packet transport and installed prompt locator | The refreshed 43,833-byte packet again required a constrained filesystem read because no inline attachment primitive exists; additionally, the coordinator's first instruction named nonexistent `prompts/blind_translator.md` while the verified kit installs `prompts/blind_translation.md`. | The fresh role stopped at the missing prompt before review. The coordinator corrected only that locator and preserved the single-packet allowlist. Packet SHA-256 `f7e056b3a8c7fcf5e7b3f97e57f06ef5227a80ed10afa9c3b9222f446601ac02` and the resulting exact D001-D088 artifact SHA-256 `209b111a803502d33054507a5e494646e9b43bdeb8ef8ade04b7025d431e5538` both validate; the role never inspected source, Lean files, other role outputs, history, or web. | The semantic blindness boundary remains explicit and hash-bound, but literal no-tool transport is unavailable, and an incorrect framework locator can stall an otherwise isolated role. | addressed | Recorded the constrained-read provenance and the one-path correction in `agent_runs.json`; future launches must use the verified installed locator, while runtime/kit maintainers should provide immutable large-packet attachment to stateless roles. |
| VHDP-S-C02B-I040 | 2026-08-31 | Equation (2.18) blind-role no-tool contract vs. transport of the 16,665-byte prepared packet | The collaboration runtime still has no immutable inline attachment primitive for a fresh fork-none role, so the blind packet required one explicitly constrained filesystem read instead of the installed prompt's literal no-tool transport. | The fresh role was authorized to read exactly `blind_review_packet.md`, `prompts/blind_translation.md`, its schema, and the core checklist, with the packet bound to SHA-256 `94cca1c912cfb0f4d82c5ddcafca0a3c4e253474fe71b4cf299967e279f6ffac`; it returned exact dependency coverage D001-D032 and a schema-valid artifact SHA-256 `e4333bdf975c2a93dde4a331a3c372b29d415cd30f79e8f55367e7e1a085f737` without inspecting the source, Lean files, other role outputs, history, or web. | Source blindness and evidence minimization remain explicit and hash-bound, but process isolation is weaker than a stateless no-tool runtime receiving an immutable attachment. | open | Preserve the exact constrained-read provenance in `agent_runs.json` and accept only hash- and schema-valid role evidence; runtime/kit maintainers should provide immutable packet attachment for stateless blind roles. |
| VHDP-S-C02B-I041 | 2026-08-31 | Proposition 2.6.1 blind-role no-tool contract vs. transport of the 58,440-byte D001-D112 packet | The collaboration runtime again could not attach the prepared packet immutably to a fresh fork-none role, so the role required one explicitly constrained filesystem read rather than literal inline/no-tool transport. | The fresh role was authorized to read only the packet bound to SHA-256 `e78597b688c2aad759d85ce4a5a8414572c0afc53d628ec75c2e2db31e880f0d` plus the fixed blind prompt/schema/checklist. It returned exact unique ordered coverage D001-D112 and a schema-valid artifact SHA-256 `a90b3ede9e14046b1eb2006228f1d0e43524365804725927e9f12467e2a29121` without inspecting source, Lean files, task metadata, other role outputs, history, or web. | The blindness boundary is explicit and hash-bound, but mechanical process isolation remains weaker than an immutable attachment to a stateless no-tool runtime. | open | Preserve the constrained-read provenance in `agent_runs.json` and accept only hash-, dependency-, and schema-valid evidence; runtime/kit maintainers should provide first-class immutable packet attachment. |
| VHDP-S-C02B-I042 | 2026-08-31 | Material Proposition 2.6.1 repair vs. file-granular audit freshness and gate semantic counters | A current complete-audit scan after the exact-gauge repair found 46 stale Chapter 2 audit manifests: 30 mention changed shared `SubGaussian.lean`, 9 mention changed `Hoeffding.lean` without `SubGaussian.lean`, and 7 mention other previously changed shared files. The gate still reports the recorded-role semantic counters as closed because it does not validate current manifest hashes. | Every task with a decision was checked using `validate_audit.py --phase complete`; failures are hash mismatches, including Equation (2.18) and otherwise untouched declarations whose packets may remain byte-identical. This is a new concrete instance of open kit defect VHDP-S-C02B-I003. | Recorded-role counts can appear closed while completed decisions are stale at whole-file granularity; blindly decrementing gate counts would also be inaccurate until packet equality and exact evidence reuse are checked task by task. | open | Finish the current repaired Proposition 2.6.1 audit, then force-prepare affected completed tasks, compare regenerated role packets exactly, restore only byte-identical role evidence, rerun target-dependent roles when packets changed, and re-finalize. The kit maintainer should bind freshness at declaration/dependency granularity and make gate semantic closure consume current validation results. |
| VHDP-S-C02B-I043 | 2026-08-31 | Repaired Proposition 2.6.1 blind-role no-tool contract vs. transport of the 44,714-byte D001-D081 packet | The collaboration runtime still could not attach the repaired prepared packet immutably to a fresh fork-none role, so the role required one explicitly constrained filesystem read rather than literal inline/no-tool transport. | The fresh role was authorized to read only the packet bound to SHA-256 `3e1b0787e78c8f4e88e20971943b08cdc47d3ee596bcb08c02afa739ccc8f26a` plus the fixed blind prompt/schema/checklist. It returned exact unique ordered coverage D001-D081 and a schema-valid artifact SHA-256 `c2aaf834bd831a8eb662897430f9a46933d808080c7c5120a806590fb44cca94` without inspecting source, Lean files, task metadata, other role outputs, history, or web. | The source-blind boundary remains explicit and hash-bound, but mechanical process isolation is weaker than immutable packet attachment to a stateless no-tool runtime. | open | Preserve the constrained-read provenance in `agent_runs.json` and accept only hash-, dependency-, and schema-valid evidence; runtime/kit maintainers should provide first-class immutable packet attachment. |
| VHDP-S-C02B-I044 | 2026-08-31 | Exercise 2.5.7 packet refresh vs. dependency-reuse invalidation and large blind-packet transport | Forced preparation after the shared `SubGaussian.lean` repair expanded the blind packet to 87,827 bytes / D001-D154 and the direct packet to 448,238 bytes because prior dependency-review reuse metadata no longer applied, although the source locator remained byte-identical. Two fresh blind roles correctly refused to fabricate coverage when a single ordinary read and a higher-output-budget read were truncated by the runtime. | Current packet SHA-256 values are blind `4f9956966df6c02b5729a77e9a58822b83810db49aa839fd570ca0c85aeaee5f` and direct `0959a7887d7a3336106e023d5058f36c8a909cd7a5cee7dc31140d39505a6236`. A third fresh role consumed the packet through one slowly streamed open read, polling only that same process, received every chunk without truncation, covered D001-D154 exactly once, and produced schema-valid blind artifact SHA-256 `c48b9ec8d048ba768ef1f681c720b8c42a4f22227542416638f18a594ea7c9b2`. | The semantic audit remained source-blind and complete, but large-packet transport required a runtime-specific streaming workaround and two discarded stateless attempts. | open | Preserve the streaming provenance in `agent_runs.json`; the kit/runtime should provide immutable attachments or chunk-safe one-read packet delivery and dependency-review reuse should survive semantically unchanged owner-module relocation. |
| VHDP-S-C02B-I045 | 2026-08-31 | Exercise 2.5.7 adjudicator semantics vs. exact finalizer trigger strings | The first fresh adjudicator resolved all six computed issues correctly but paraphrased five trigger labels; schema validation passed, while the stricter finalizer rejected the artifact because its trigger list was not byte-identical to the computed reasons. | Rejected artifact SHA-256 was `45493302f065c275cb699851ddf499f7f9a20e9870cca6613d69a73af9d49d9e`. A new stateless adjudicator was given the literal finalizer strings, did not read the rejected artifact, reproduced all six strings exactly, and produced valid artifact SHA-256 `e23c24a93406fa577aebe535ca5a1446f9318bd7a90453f390cb92af5112ae94`; complete audit validation now passes with decision SHA-256 `4732d7cd2146fa37aa948acf107df926f828792319432ef174c96e051121224b`. | The first artifact was semantically adequate but unusable for completion, causing one extra role run; the schema validator alone did not expose the protocol-exactness requirement. | addressed | Retained only the exact-trigger artifact in the current audit and recorded both attempts in provenance. The adjudicator schema/validator or prompt should enforce literal equality to computed trigger reasons before the finalizer stage. |
| VHDP-S-C02B-I046 | 2026-08-31 | Resolution of the current stale-audit instance recorded in VHDP-S-C02B-I042 | All 46 stale Chapter 2 manifests were force-prepared against current repository hashes. Forty-five regenerated blind packets, direct packets, and source locators were byte-identical to their archived complete generations, so only exact role evidence was restored, revalidated, provenance-recorded, and re-finalized. Exercise 2.5.7 alone changed packets because dependency-review reuse metadata was lost; only its unchanged source contract was reused and all target-dependent roles plus exact-trigger adjudication were rerun. | A repository-wide current scan now reports 76 complete audit tasks valid, 0 invalid, and 0 audit tasks without a decision. Proposition 2.6.1 remains accepted faithful-equivalent at decision SHA-256 `516ba7c38a6bc911ef32a7a9dbc7be1d5423bd6ddcb1ac7acf0938fffc5692ed`; refreshed Exercise 2.5.7 is accepted faithful-equivalent at `4732d7cd2146fa37aa948acf107df926f828792319432ef174c96e051121224b`. | The concrete false-closed semantic-freshness state is repaired and the gate again has current evidence for every recorded semantic row. The underlying whole-file binding/gate-integration defect described by I042 remains a process risk for future shared-file changes. | addressed | Keep I042 as the open design defect; rerun the same complete-audit scan after any future shared-module change and never reuse role evidence unless all prepared semantic packets and the source locator are byte-identical. |
| VHDP-S-C02B-I047 | 2026-08-31 | Equation (2.19) reusable foundation increment vs. file-granular Chapter 2 audit freshness | Appending `centered_eLpNorm_two_le` to the shared `Preliminaries.lean` module made 67 previously complete Chapter 2 decisions fail current-manifest validation although their audited declarations were unchanged. All 67 tasks were force-prepared; every regenerated blind packet, direct packet, and source locator was byte-identical to its immediately archived generation, so only exact role evidence was restored with `apply_patch`, revalidated, re-finalized, and checked complete. | The post-refresh Chapter 2 scan reports 77 complete audit tasks valid, 0 invalid, and 0 audit tasks without a decision, including the newly adjudicated Equation (2.19) decision SHA-256 `6d1c51522dc598ec03cbb1c9038f69b2bf386d89446d32afdabea35cbc4b2aa6`. | A harmless reusable sibling theorem caused a temporary false-closed semantic loop and 67 mechanical dossier regenerations. Exact packet matching prevented stale or semantically changed evidence from being reused. | addressed | Keep the underlying declaration-vs-file freshness defects I003/I042 open; require a current complete-audit scan after every shared-module increment and restore cached roles only under byte-identical packet and locator hashes. |
| VHDP-S-C02B-I048 | 2026-08-31 | Inherited Lemma 2.6.8 alias vs. source-level absolute-constant and intrinsic-norm contract | The inherited `hdp_02_hlem_h2_d6_d8` placed its existential constant after the probability-space, random-variable, property-kind, and arbitrary witness-scale binders, and bounded the centered gauge by that witness rather than by the intrinsic `PsiTwoNorm` of `X`. The first target-dependent blind role was interrupted before any artifact was accepted when the concurrent source-only extraction made the uniformity requirement explicit. | The rejected prepared generation is archived under the task history. The repaired exact alias quantifies one `C` before all instance data and states both centered sub-Gaussianity and `PsiTwoNorm(centered X) <= ofReal C * PsiTwoNorm X`; targeted and full builds pass, all four fresh roles validate, and the accepted faithful-equivalent decision has SHA-256 `687966b96163e20152a9d149fb0f20bcd01e4167983692071a1b63ffd705edef`. | Treating the inherited compatibility theorem as the source row would have certified a formally provable but semantically weaker and incorrectly scoped statement. | addressed | Preserve the old theorem only as compatibility API; use `hdp_02_hlem_h2_d6_d8_exact` and its frozen wrapper as the audited source contract, and keep running source-contract checks before accepting target-dependent evidence. |
| VHDP-S-C02B-I049 | 2026-08-31 | Equation (2.20) blind dependency opacity vs. exact finalizer adjudication trigger | The source-blind translation correctly reconstructed the triangle inequality but left dependency D019's totalized Bochner integral semantics unclear; both source-aware judges independently resolved the point, yet the finalizer conservatively required a separate adjudicator for the literal uncertainty trigger. | The adjudicator established that the inherited sub-Gaussian premise supplies integrability, so the totalized Lean integral denotes the source expectation in this setting. Its valid artifact has SHA-256 `a9c1b2d01a1bd255488fd2ec31a28f6165995ebea461922c0a74a470b66f80d6`, and the faithful-equivalent decision has SHA-256 `bea4d4280de921f4787b207f066fce96570fd0ae9440bd79917b32612143531c`. | One opaque dependency entry caused a semantically unnecessary extra role run, although the conservative protocol prevented uncertainty from being silently discarded. | addressed | Preserve the adjudication; improve blind dependency dossiers so integrability consequences needed to interpret totalized operations are explicit, or continue treating such opacity as a mandatory conservative adjudication. |
| VHDP-S-C02B-I050 | 2026-08-31 | Initial constant-psi-two audit locator vs. the hash-pinned Chapter 2 source | The coordinator initially described Equation (2.17) as the definition of the psi-two norm. The source role correctly found that Definition 2.5.6 and Equation (2.13) define the norm, while Equation (2.17) is the bounded-random-variable estimate explicitly recalled by the Lemma 2.6.8 proof. | Direct extraction of PDF page 16/printed page 27 shows the norm definition as `(2.13)` and `||X||_{psi_2} <= C ||X||_infinity` as `(2.17)`. The source, blind, and direct in-flight roles were interrupted before any output was accepted; the task anchor was corrected and the generation is being force-prepared. | An incorrect anchor description could misdirect source extraction and bind otherwise valid semantic artifacts to false locator metadata. | addressed | Accept no artifact from the initial generation. Rerun all roles against the corrected, freshly prepared packets and retain the archived failed generation only as provenance. |
| VHDP-S-C02B-I051 | 2026-08-31 | First constant-psi-two round-trip role isolation vs. collaboration status metadata | The first fresh source-only round-trip role called the collaboration status API only to identify its runtime metadata, but that response unexpectedly exposed completed blind/direct role summaries including the direct classification. The role immediately self-disqualified and wrote no artifact. | The role reported the breach before reviewing or persisting a judgment; `roundtrip_judge.json` remained absent. It was interrupted and replaced by a new fork-none role explicitly prohibited from calling collaboration status or inspecting any direct-role evidence. | Accepting the contaminated role would violate the round-trip judge's required independence from the direct judgment. | addressed | Use only the replacement role's schema-valid artifact and provenance. Runtime should expose an agent's own metadata without revealing sibling outputs, or role prompts should explicitly forbid status calls. |
| VHDP-S-C02B-I052 | 2026-08-31 | Exercise 2.6.9 blind artifact transport vs. required task-local output path | The fresh blind role initially inferred a nonexistent generic `faithfulness/outputs` convention and wrote its completed JSON one directory too high as `faithfulness/blind_translation.json`, despite the kit's required `faithfulness/agent_outputs/blind_translation.json` path. It stopped before cleanup when the coordinator's correction arrived. | The coordinator authorized deletion of only the role-created unaccepted wrong-path file and byte-for-byte relocation via `apply_patch` to the required path. The wrong-path file is absent, the installed role validator passes, exact D001-D082 coverage is present, and the accepted artifact SHA-256 is `5547edfa7497af8102e42b29618c1c9ac8e19630f1dbf79b9cd2c4b75569208d`. | A valid judgment at an untracked path would be omitted from manifest validation or tempt manual reconstruction; uncontrolled cleanup could also remove unrelated evidence. | addressed | Retain only the validated artifact under `agent_outputs`; future role launches should give the absolute required output path explicitly rather than saying only the filename. |
| VHDP-S-C02B-I053 | 2026-08-31 | Gaussian-square-tail blind semantic responses vs. fixed blind-translation schema | The first fresh blind role covered dependencies D001-D051 and reconstructed the target, but encoded the translation, restrictions, and ambiguity arrays as structured objects although the installed schema requires arrays of strings. The first retry fixed those arrays but encoded `dependency_coverage` as an object keyed by ID rather than an array. | Neither malformed response was persisted or consumed; both agent runs were recorded with their rejection reasons. A third fork-none, source-blind role received the same packet hash, complete D001-D051 semantics, and a literal schema skeleton; its exact response validates at SHA-256 `c18389bc9bb855f7f328ea5708d2e1f46361b838f59acc2802014b022def1846`. | Semantically useful independent work remains unusable when output shape is not schema-constrained, and coordinator-side repair would compromise role provenance. | addressed | Retain only the third role's schema-valid response; runtime/kit should enforce the JSON schema during generation rather than relying on post hoc retry. |
| VHDP-S-C02B-I054 | 2026-08-31 | Exercise 2.7.4 source wording vs. persisted gate `next_foundation` | The gate proposed the positive endpoint `lambda = 1` divergence of an exponential law, but property (c) already includes its positive endpoint after choosing its own parameter; the exercise asks whether its nonnegative-lambda bound can be extended to the symmetric interval `|lambda| <= 1/K3`. | Hash-pinned PDF page 22 / printed page 33 says “extended for all lambda such that |lambda| <= 1/K3,” while Proposition 2.7.1(c) on PDF page 21 / printed page 32 states only `0 <= lambda <= 1/K3`. For any constant-zero variable and negative lambda, the left side is `1` and `exp(K3*lambda) < 1`, giving the direct counterexample. | Following the inherited note would formalize a different endpoint-divergence claim rather than the printed exercise's negative-lambda obstruction. | addressed | Replace the row's foundation note with the constant-zero negative-lambda witness and audit that exact source-facing statement. |
| VHDP-S-C02B-I055 | 2026-08-31 | Newly created Section 2.7 audit locators vs. the hash-pinned source | Three prepared task locators grouped Definition 2.5.6 with PDF pages 13--14 / printed pages 24--25, but the source-only batch agent found Definition 2.5.6 at PDF page 16 / printed page 27. | The first shared batch locator had SHA-256 `96999ab9c93dd8971b343042a9dbb8df6ab0d66f4db2c3e00054f6ef1ac2564e`; batch source artifact `3e39d619b3c4737f1beebc1f4a6d89d80928155a0af9cf2c3a44f3fd92e95975` explicitly reported the mismatch while preserving the source statements. The corrected generation has locator SHA-256 `344c2f15f875394c1b35c8732c25ff4482a74b2364f89e81c7bee4a9d3ea6fc2` and fresh batch contract SHA-256 `856d0400f37d309d6e394f4e5d704d0cd26bb8d59f2cdc4932f9f89bb4eb46e9`; every task-local source contract validates. | Leaving the bad auxiliary locator would weaken provenance even though the source agent found the correct definition and the primary statement pages were right. | addressed | The three locators were corrected, the whole five-task group was force-prepared with the first generation archived, and a fresh fork-none source batch was validated before target-dependent evidence. |
| VHDP-S-C02B-I056 | 2026-08-31 | Definition 2.7.5 blind-role large-packet transport vs. complete dependency coverage | The first fresh retry-generation blind agent's single constrained read of the 51,648-byte packet was truncated across D048--D063; a later response covered the packet but used the non-schema status token `received and interpreted`. Neither response was persisted or consumed. A final packet-only retry covered D001--D102 exactly; after correcting its reported schema version and moving two otherwise unchanged arrays to the installed schema's required root locations, that same role persisted a validator-passing artifact. | Packet SHA-256 is `d2a3297b0c13f58b3b59e1bd74104f496545e3b4f72b78aac17986b46acd7e81`. Accepted blind artifact SHA-256 is `0406779c5b1bf89062e81ea8dcdb514a2085f53dfe6d954fbe9be22e27daf0fa`; `validate_agent_output.py HDP-02-DEF-2.7.5 blind-translation` passes. | Repeated transport/schema failures delayed the semantic loop and show that natural-language schema instructions are not equivalent to enforced structured output. | addressed | Retain only the exact-coverage validator-passing artifact and recorded retry provenance; use schema-constrained structured role output when available. |
| VHDP-S-C02B-I057 | 2026-08-31 | Remark 2.7.14 blind response vs. installed exact dependency validator and schema | The first response altered dependency names/order; later retries used object-shaped arrays, omitted the temporary packet path, or omitted required `name` and `effect_on_target` fields. None was accepted. A final retry copied D001--D079 exactly and returned the required shape. | The first rejected response SHA-256 was `229b8234918a3c8c6157e46f0ca2700c68f5ffcd52235e14370c9083252db983`. The accepted blind artifact is SHA-256 `34bf3457eb1136f59c05f85d7a9c74d1d694b4aaba787e6839cb6714e888087e`, bound to blind packet `55d83969fda77a84a61cc97af1efee2b844dfaf33ecf472cb0bb88f228a0b52c`. After the shared-module refresh regenerated a byte-identical packet, the artifact was restored with `apply_patch` and the current role validator passed. | Repeated mechanically malformed responses delayed the semantic loop and could have tempted coordinator-side repair that would compromise role provenance. | addressed | Retain only the validator-passing final response and its recorded agent provenance; use schema-constrained structured role output when the runtime provides it. |
| VHDP-S-C02B-I058 | 2026-08-31 | Equation (2.22) blind-role responses vs. fixed blind-translation schema | Three source-blind attempts reconstructed the target but were unusable: the first used invalid dependency-status tokens, the second failed to receive the intended opaque packet path, and the third omitted required `name` and `effect_on_target` fields from coverage records. A final packet-only retry covered D001--D059 exactly but initially reported schema version `1.0` and nested two required root arrays under `translation`; the coordinator performed only the schema-preserving relocation after the role supplied the corrected version token. | Current blind packet SHA-256 is `860297e2bb0060600fbc79183d6c154382e0b98cd36e26527231abc4269bd846`. Accepted artifact SHA-256 is `15f43bf3f263d29be705a701415539022e65159d6d7093f3fce2c045e55b4961`; `validate_agent_output.py HDP-02-EQ-2.22 blind-translation` passes with exact dependency coverage. | The mathematical role output is preserved, but coordinator-side schema relocation weakens transport provenance compared with a natively schema-valid response. | addressed | Preserve the explicit relocation note and recorded agent provenance; require first-class schema-constrained role output in future tooling. |
| VHDP-S-C02B-I059 | 2026-08-31 | Exact Theorems 2.6.2/2.6.3 shared-module increment vs. current Chapter 2 semantic counters | A repository-wide complete validation scan after the two intrinsic psi-two theorem repairs found 40 previously decided tasks stale solely because their manifests bind the whole changed `SubGaussian.lean` file, while the gate still reports all 79 recorded semantic rows closed. | The current scan executed `validate_audit.py --phase complete` for every decided Chapter 2 task and reports `valid=48 invalid=40 undecided=8`; each invalid result names only a target or local-import SHA-256 mismatch for `NumStability/HDP/Scalar/SubGaussian.lean`. The freshly regenerated 2.6.2 and Remark 2.7.14 packets were byte-identical to their archived generations and exact artifacts revalidate after `apply_patch` restoration. | Gate semantic counters are temporarily false-closed at file granularity; bulk evidence reuse before exact packet comparison would risk certifying changed targets. | addressing | Finish the current shared-module increments, force-prepare all 40 stale decided tasks, compare blind/direct packets and source locators to their immediately archived generations, restore only byte-identical role evidence, rerun changed roles, re-finalize, and repeat the complete scan before trusting semantic closure. |
