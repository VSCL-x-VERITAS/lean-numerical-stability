# Limitations — session claude-continue-2

Record skill, harness, prompt, gate, tool, or agent-process limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Limitation | Consequence | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| VHDP-S-CC2-L001 | 2026-08-28 | formalization-faithfulness-audit kit: `scripts/validate_audit.py`, `scripts/finalize_audit.py`, prepared-bundle manifest | The prepared-bundle integrity check is **file-level**: it pins the SHA-256 of the target's whole `.lean` file and of every local import. Any edit anywhere in those files invalidates the bundle, even a pure *addition* of unrelated declarations that leaves the audited declaration's type and every semantic dependency byte-identical. There is no statement-level or declaration-level revalidation path, and no way to certify "the target type and its dependency closure are unchanged, so the recorded role outputs remain valid". | Consequence for this session: the HDP-02-THM-2.8.2 bundle was prepared earlier, then this session added `psiOneGauge*` and Lemma 2.7.6/2.7.7 to `NumStability/HDP/Scalar/SubExponential.lean` and `psiOneGaugeToLinearMGF*` to `NumStability/HDP/Scalar/IndependentSums/Bernstein.lean`. `finalize_audit.py --check-adjudication` then refused with SHA-256 mismatches on the target file and two local imports, although `hdp_02_hthm_h2_d8_d2` itself was never touched (verified by reading the alias back) and `NumStability/HDP/Scalar/Preliminaries.lean` was already modified before this session began. Three fully validated role outputs (blind translation, direct judgment, round-trip judgment, all hash-checked against their own input packets) are therefore bound to a superseded file hash. | Workaround adopted: sequence the work so a module is not audited while it is still being extended. The Lean work for the printed psi-1 forms of Theorems 2.8.1/2.8.2 and Corollary 2.8.3 is being completed first; the Section 2.8 batch will then be re-prepared with `prepare_batch_audit.py --force` (which archives rather than deletes prior artifacts) and re-run once against a stable target, using `apply_dependency_reuse.py` for the declaration meanings whose semantic hashes are unchanged. | open | Recommend the kit distinguish *controlled* inputs (the target declaration's elaborated type plus the semantic hashes of its dependency closure, which it already computes and stores) from *incidental* ones (whole-file hashes), and offer a revalidation mode that keeps role outputs when only the latter changed. Reported as a limitation, not an issue: the current behaviour is conservative and safe, just costly. |
