# Issues — session claude-continue-2

Record skill, harness, prompt, gate, tool, or agent-process issues encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Symptom | Evidence | Impact | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| VHDP-S-CC2-001 | 2026-08-28 | formalization-faithfulness-audit kit, roundtrip-judge role | Fresh round-trip judge for HDP-02-THM-2.8.2 returned JSON with a duplicated `reasoning` key inside semantic check C06 (`"reasoning="...", "reasoning":"..."`), which is invalid JSON and also violates the schema's `additionalProperties: false`. | Role output could not be parsed or written; `validate_agent_output.py` would reject it. Raw output preserved in the session task transcript. | One wasted round-trip role run (~66k subagent tokens, ~5 min). No audit artifact was corrupted because the orchestrator writes only validated JSON. | open | Per kit METHODOLOGY, retried with a **new** stateless agent rather than coaching the invalid role; added an explicit "emit strictly valid RFC 8259 JSON, no duplicate keys" instruction to the role prompt. Recommend the kit's role prompts carry that instruction by default. |
| VHDP-S-CC2-002 | 2026-08-28 | formalization-faithfulness-audit kit: `prompts/direct_judge.md`, `prompts/roundtrip_judge.md`, `schemas/*_judge.schema.json`, `scripts/validate_agent_output.py` | The judge schemas expose `accepted` as a free boolean, and the role prompts never state any constraint on it, but `validate_agent_output.py:95` rejects any output where `accepted != (classification in {faithful-equivalent, faithful-stronger})`. A judge that wants to record "the implication holds, so the classification is faithful-stronger, but the statement-level divergence should not be waved through" naturally sets `accepted=false` and is then rejected wholesale. | Direct judge for HDP-02-THM-2.8.2 returned `classification=faithful-stronger`, `accepted=false`, `requires_adjudication=true`; `validate_agent_output.py HDP-02-THM-2.8.2 direct-judge` failed with `direct-judge: accepted flag contradicts classification`. The output was otherwise complete and its `dossier_sha256` matched `direct_review_packet.md` exactly. Preserved at `audits/vershynin-hdp/chapter02/HDP-02-THM-2.8.2/faithfulness/history/rejected-role-outputs/direct_judge.invalid-accepted-flag.2026-08-28.json`. | One discarded direct-judge run (~76k subagent tokens, ~7.5 min). `finalize_audit.py:243` already *derives* the decision-level `accepted`, so the judge-level field carries no independent information and is pure redundancy that can only disagree. | open | Reran with a fresh stateless agent whose prompt states the derived-field rule explicitly and routes statement-level objections to `requires_adjudication` plus `findings`. Recommend the kit either drop `accepted` from the judge schemas (deriving it as `finalize_audit.py` already does) or state the constraint in the two judge prompts. |
| VHDP-S-CC2-003 | 2026-08-28 | book-formalization organization dispatch and Chapter 2 gate | The run did not invoke `book-formalization-migration`, created no Chapter 2 organization report, and accepted inherited all-zero organization counters even though the Chapter 1 gate in the same Lean worktree recorded 309 unclassified modules and one placeholder. | Transcript attribution names only `book-formalization`; `gates/ch02.json` formerly stored `0/0/0/0` at `2026-08-28T00:00:00Z`, while `gates/ch01.json` stored `309/0/1/0`; the current layout checker reports the latter repository-wide debt. | The gate displayed the organization loop as closed on stale evidence and hid actionable repository debt. | mitigated | Corrected Chapter 2 to `309/0/1/0`, added `docs/vershynin-hdp/chapter02/organization.md`, and hardened the coordinator with explicit migration dispatch plus cross-gate organization preflight. Full organization debt remains actionable. |
| VHDP-S-CC2-004 | 2026-08-28 | book-formalization termination and durability workflow | Claude voluntarily ended the main formalization response with an `ACTIVE` gate containing 119 actionable rows and no typed nonlocal blocker; all Chapter 2 work was also left uncommitted. | The persisted gate and report both state `ACTIVE`, 0 formalized / 119 remaining / denominator 119 / 0.00%, with 11 skipped and zero deferred; the transcript then ended the turn and later acknowledged that no migration skill ran. | Violated the continuation contract and exposed a large local-only work product to data loss. | mitigated | Added a skill-scoped Claude Stop guard that returns `ACTIVE` and inconsistent organization state as continuation feedback, plus a collision-safe authorized checkpoint-branch procedure. Current work is being checkpointed without force-pushing or altering `main`. |
| VHDP-S-CC2-005 | 2026-08-28 | remote durability checkpoint | The shared worktree contained 1,355 uncommitted Chapter 1/2 implementation, audit, gate, ledger, and organization files, while the coordinator hardening was also local-only. | After fetching `origin`, isolated worktrees were created from `3d76b31d41d50136dd446cafa8cd16c1342aad30` and `d602405cdd2a25915e5ba09dfd542886f4f9e2fa`. The coordinator candidate was pushed at `086aae30f9a28e6f606c2af1f4530499c3539a35` on `checkpoint/formalization-workflow/guardrails/codex-20260828T204244Z`; the complete Lean snapshot was pushed at `cdf80e2543fdadfd076ff20e68d5ca9dd9ad6132` on `checkpoint/vershynin-hdp/ch01-ch02/codex-20260828T204244Z`. | Removes the single-machine data-loss risk without merging incomplete work or changing either local `main` branch/index. | closed | Both pushes used explicit refspecs, unique previously absent branch names, and no force. Direct `main` and release tags remain untouched. |
| VHDP-S-CC2-006 | 2026-08-28 | Claude Stop-hook deployment | Correction to VHDP-S-CC2-004: shared skill frontmatter cannot own the hook because Codex's skill validator rejects Claude's `hooks` key. | Codex quick validation initially failed on that key; after moving registration to `/home/mymel/.claude/settings.json`, the shared skill validated and both providers still resolved the identical canonical `SKILL.md`. Running the installed guard on the real Claude transcript returned `decision=block` with the current `ACTIVE` gate. | Avoids a provider-incompatible or stale fork while retaining enforcement on this host. | closed | The guard is transcript-scoped but registered in Claude's user Stop hooks; it exits silently unless a generated `/book-formalization` prompt is present. This row supersedes only the phrase "skill-scoped" in VHDP-S-CC2-004. |
