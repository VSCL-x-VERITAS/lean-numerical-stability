# Limitations — session codex-continue-1

Record skill, harness, prompt, gate, tool, or agent-process limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Limitation | Consequence | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| BF-VHDP-CONT1-L001 | 2026-08-28 | faithfulness kit `prepare_audit.py` declaration extraction | Preparation rejects source-facing `def` targets because `declaration_source` recognizes only `theorem` and `lemma`. | Definition rows cannot be audited directly even though the HDP gate requires per-row faithfulness evidence for Lean-bearing definitions. | Add a theorem-level specification wrapper that exposes the definition's mathematical contract, then audit that theorem without changing it during the audit. | open | Use the wrapper route for Chapter 1 definition rows; address generic `def` extraction only in a separately authorized skill-maintenance run. |
| BF-VHDP-CONT1-L002 | 2026-08-28 | faithfulness kit prepared-artifact import hashing | A canonical-placement edit removed an unrelated Chapter 1 alias from `Scalar/Preliminaries.lean` after the moments packets were prepared; `finalize_audit.py --check-adjudication` then rejected both audits because the whole imported producer hash changed even though both audited declarations and their dependencies were unchanged. | Independent judge work tied to the old dossier hashes cannot be finalized, so the two complete review sets must be discarded and rerun. | Freeze every local import for the duration of an audit; after any import-file change, run `prepare_audit.py --force` and obtain fresh source, blind, direct, round-trip, and any required adjudication outputs before gate closure. | open | Apply the forced-refresh route now; schedule unrelated producer organization only between completed audit epochs. |
| BF-VHDP-CONT1-L003 | 2026-08-28 | collaboration transport for the faithfulness blind role | The collaboration API accepts only a text task and cannot attach the generated 30 KB anonymized dossier without manually duplicating it into the prompt; a path-only handoff conflicts with the kit's literal prohibition on blind-role filesystem tools. | The isolated blind agent needs a narrowly scoped read of the anonymized packet, so the transport boundary cannot satisfy both the no-filesystem wording and practical exact-packet delivery. | Start a fresh no-history agent, reveal only the anonymized packet path and its hash, permit exactly one read-only call for that file, prohibit every other tool/source/metadata access, and discard malformed responses rather than coaching the same agent. | open | Treat this as an explicit runtime limitation; add attachment/inline-file transport before claiming literal no-tool blind isolation. |
| BF-VHDP-CONT1-L004 | 2026-08-28 | faithfulness kit explicit-adjudication trigger handling | The source contract retained a material quantifier-scope ambiguity and an independently requested adjudicator resolved it, but `finalize_audit.py` rejected the schema-valid adjudicator as “unexpected” because both judges had over-resolved the ambiguity and set `requires_adjudication=false`. | The kit documents explicit request and unresolved semantic items as adjudication triggers, yet its finalizer accepts only mechanically inferred judge disagreement/uncertainty triggers. | Discard the over-resolving current judge artifact and obtain a fresh isolated judgment that preserves the source-contract ambiguity as an unclear implication and requests adjudication; then validate the existing fresh adjudicator against the mechanically visible trigger. | open | Extend the finalizer with an explicit-trigger manifest/CLI field in a separately authorized skill-maintenance run. |
| BF-VHDP-CONT1-L005 | 2026-08-28 | faithfulness blind packet provenance transport | The prepared `blind_review_packet.md` does not itself print the `dossier_sha256` that `blind_translation.schema.json` requires, although the hash is present in `manifest.json`; the isolated blind role correctly refused to invent it. | A packet-only role cannot populate a mandatory provenance field without a second orchestrator message, even when the one-read packet transport workaround in L003 is followed. | Supply only the manifest-pinned 64-hex packet/dossier hash in a separate message, revealing no source or task metadata, and record the split transport in the agent run. | open | Add the dossier SHA directly to generated blind review packets in a separately authorized faithfulness-kit maintenance run. |
| BF-VHDP-CONT1-L006 | 2026-08-28 | faithfulness direct packet size | The generated tail-CLT direct packet is larger than the collaboration execution output window, so a literal full-file read can be truncated even when it is the role's sole authorized read. | A fresh direct judge may lose a contiguous dependency block and the schema footer, making complete hash-bound coverage impossible from that raw transport alone. | Use a deterministic bounded projection containing the full source contract, target header, exact dependency inventory/interpretations, checklist, schema, and packet hash; if truncation is discovered only after reading, allow schema/completeness transport without evidence reread and record it in `agent_runs.json`. | open | Update the faithfulness kit's generated judge packet or role launcher to emit a bounded complete projection before future 100-plus-dependency audits. |
