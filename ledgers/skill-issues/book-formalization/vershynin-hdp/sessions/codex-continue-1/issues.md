# Issues — session codex-continue-1

Record skill, harness, prompt, gate, tool, or agent-process issues encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Observed at | Component | Symptom | Evidence | Impact | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| BF-VHDP-CONT1-I001 | 2026-08-28 | faithfulness blind-agent schema compliance | Two fresh Cauchy--Schwarz blind roles returned incompatible legacy-shaped objects instead of the configured `blind_translation.schema.json` shape. | Agent `/root/cauchy_blind_translation` omitted required outer/structured fields; replacement `/root/cauchy_blind_translation_retry` used wrong constants (`1.0`, `blind_translator`), status `covered`, and unconfigured translation keys despite an exact-schema instruction. | Neither response could enter the hash-bound audit and neither was repaired or coached in place. | mitigated | Discarded both malformed responses; third fresh no-history role `/root/cauchy_blind_translation_retry2` used the exact field/value vocabulary and its transported artifact passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I002 | 2026-08-28 | faithfulness blind-agent required-field compliance | The fresh Hölder blind role's first response omitted `dossier_sha256`, `restrictions_and_vacuity_risks`, and `ambiguities` even though its substantive dependency ledger covered every prepared dependency. | The first response could not validate against `blind_translation.schema.json`, delaying the independent judge phase. | The role needed a schema-only postprocessing turn, without new evidence or filesystem access, to restore the three required top-level fields while preserving its blind translation. | mitigated | The corrected artifact records the pinned dossier hash `02f524dcba5f9a016353507e278a4c2cebed05669dd834fb35a3c62f571d14b6`, covers D001--D052, and passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I003 | 2026-08-28 | faithfulness blind-agent schema compliance | The first fresh distribution-determination blind role returned a legacy-shaped object with upper-case role, no schema version, string-valued `translation`, and unconfigured dependency fields/status vocabulary. | Although the prose translation was useful, the response could not enter the hash-bound audit or pass `blind_translation.schema.json`. | Discard the malformed response without coaching the same role and launch a fresh no-history replacement with the exact configured key/value vocabulary. | mitigated | Replacement `/root/distribution_blind_translation_retry` covered D001--D012 against the same one-read anonymized packet and its transported artifact passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I004 | 2026-08-28 | faithfulness blind-agent schema compliance | The fresh tail/CDF blind role returned the correct dependency ledger and proposition under an unconfigured `structured_translation` key instead of the required `translation` object. | Agent `/root/tail_blind_translation` covered D001--D026 and preserved strict `X > t` versus inclusive `X ≤ t`, but its first response could not pass `blind_translation.schema.json`. | A schema-only postprocessing turn was required before independent judges could consume the hash-bound translation; no new dossier or semantic evidence was supplied. | mitigated | The corrected artifact retained dossier SHA `5be611db408759e0c6af00218dce951690208fa9222fdaea27defa1f367d01f9`, covered D001--D026, and passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I005 | 2026-08-28 | faithfulness direct-review packet completeness | The prepared tail/CDF `direct_review_packet.md` omits both the packet SHA required by `direct_judge.schema.json` and the configured meanings of checks C01--C12 requested by the direct-judge prompt. | Fresh agent `/root/tail_direct_judge` reported a zero-hash sentinel, marked essential checks unclear, and requested adjudication rather than inventing absent provenance; packet SHA-256 `9ee4efe296e82639196c44811fc3499cdab67d44fb40b4b71781289d07d42aad`. | Its output could not be validated or used; a fresh direct role needed the missing prepared packet SHA and `checks/core.json` explicitly. | mitigated | Discarded the first output; fresh no-history replacement `/root/tail_direct_judge_retry` received the exact direct-packet SHA and configured checklist, preserved independent semantics, and passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I006 | 2026-08-28 | faithfulness blind-agent dependency-name compliance | The first fresh indicator-expectation blind role returned all 25 dependency records in order but shortened several fully qualified dependency names, so the framework rejected the artifact with `dependency names/order mismatch`. | `validate_agent_output.py HDP-01-LEM-1.2.1-INDICATOR-EXPECTATION blind-translation` rejected names such as `IsProbabilityMeasure`, `Measure`, and `Measure.real` where the prepared inventory requires their exact qualified forms. | The otherwise substantive translation cannot enter the audit and must not be repaired by the orchestrator or the same role. | mitigated | Discarded the malformed artifact and launched fresh no-history replacement `/root/indicator_expectation_blind_retry` with the same pinned dossier hash and the exact prepared dependency-name sequence. |
| BF-VHDP-CONT1-I007 | 2026-08-28 | faithfulness blind-agent schema compliance | The first fresh Fubini blind role returned a legacy `blind_translation.v1` object with `mathematical_translation`, `dependency_id`, `coverage`, and `uncertainties` instead of the configured framework schema. | Agent `/root/fubini_blind_translation` did preserve the iterated-ENNReal-integral semantics and covered D001--D025, but its output cannot pass `blind_translation.schema.json`. | The response cannot enter the audit and must not be repaired by the orchestrator or continued agent. | mitigated | Discarded the malformed response and launched fresh no-history replacement `/root/fubini_blind_translation_retry` with the same pinned packet hash plus explicit configured top-level, translation, and dependency-record key vocabularies. |
| BF-VHDP-CONT1-I008 | 2026-08-28 | faithfulness blind-agent nested-type/status compliance | The first fresh Markov-decomposition blind role used objects for `translation.binders` and `translation.mathematical_definitions`; a fresh replacement corrected those types but used unconfigured dependency status `covered` instead of `understood`/`unclear`. | Agents `/root/decomposition_blind_translation` and `/root/decomposition_blind_translation_retry` otherwise returned the configured outer vocabulary, exact D001--D020 coverage, and the correct complementary-threshold identity. | Neither response can validate or enter the judge phase and neither may be edited into conformance. | mitigated | Discarded both malformed responses and launched third no-history replacement `/root/decomposition_blind_translation_retry2` with the same pinned packet hash and explicit exact status vocabulary. |
| BF-VHDP-CONT1-I009 | 2026-08-28 | faithfulness blind-agent dependency-order and schema compliance | The first schema-shaped Fubini blind replacement returned all 25 exact dependency names but transposed D018 `Real.measurableSpace` and D019 `Real.measureSpace`; the next fresh retry fixed the order but reverted to a legacy outer schema. | `validate_agent_output.py HDP-01-LEM-1.2.1-FUBINI blind-translation` rejected the first artifact with `dependency names/order mismatch`, and `/root/fubini_blind_translation_retry2` returned unconfigured `informal_statement`/`blind_name`/`uncertainties` fields. | Neither artifact could enter the audit, and the orchestrator may not reorder or reshape independent blind evidence by hand. | mitigated | Discarded both responses; fresh no-history replacement `/root/fubini_blind_translation_retry3` used the exact configured outer, translation, dependency, and status vocabulary, preserved D001--D025 order, and passed validation. |
| BF-VHDP-CONT1-I010 | 2026-08-28 | Exercise 1.2.3 audit task locator | The initially prepared task metadata cited PDF page 3 / printed page 7, but the fresh source-only role found Exercise 1.2.3 under its exact number and displayed formula on hashed PDF page 4 / printed page 8. | The role's source evidence showed page 3 contains Lemma 1.2.1 and Exercise 1.2.2; the exact Exercise 1.2.3 heading, formula, finiteness sentence, and hint occur on page 4. | The first prepared epoch cannot be sealed even though the semantic anchor was unambiguous; all role outputs tied to it must be discarded. | mitigated | Corrected `audit-task.json`, interrupted the still-running blind role, and forced a fresh preparation before launching replacement no-history source and blind roles. |
| BF-VHDP-CONT1-I011 | 2026-08-28 | Exercise 1.2.3 blind-agent schema compliance | The first blind role in the corrected locator epoch returned a legacy object: no schema version/role, a string-valued `translation`, dependency keys `dependency_id` with status `used`, and no restrictions/ambiguities arrays. | Agent `/root/exercise123_blind_retry` did identify the extended moment identity and covered D001--D054 in order, but its response cannot satisfy `blind_translation.schema.json`. | The independent evidence cannot enter the audit and must not be reshaped by the orchestrator or coached in place. | mitigated | Discarded the response and launched fresh no-history `/root/exercise123_blind_retry2` with the same one-read packet/hash and the exact configured top-level, translation, dependency-record, and status vocabulary. |
| BF-VHDP-CONT1-I012 | 2026-08-28 | Equation (1.5) blind-agent required-field compliance | The first fresh Equation (1.5) blind role returned a schema-shaped translation and exact D001--D048 ledger but omitted mandatory top-level `schema_version`, `role`, and `dossier_sha256`. | Agent `/root/eq15_blind` preserved the finite-measure, L2, pairwise-independence, identical-distribution, indexing, and 1/N variance identity semantics, but the object cannot validate. | The response cannot enter the audit and must not be repaired by the orchestrator or the same role. | mitigated | Discarded it and launched fresh no-history `/root/eq15_blind_retry` with the same one-read packet/hash and an explicit requirement not to omit any of the seven mandatory top-level keys. |
| BF-VHDP-CONT1-I013 | 2026-08-28 | Theorem 1.3.1 blind-agent nested-type compliance | The first fresh strong-law blind role returned objects for the configured string-array fields `translation.binders`, `hypotheses`, `conclusions`, `mathematical_definitions`, `restrictions_and_vacuity_risks`, and `ambiguities`. | `validate_agent_output.py HDP-01-THM-1.3.1 blind-translation` rejected every nested object while the role otherwise covered D001--D045 and preserved the almost-sure sample-average limit. | The malformed independent evidence cannot enter the audit and must not be reshaped by the orchestrator. | mitigated | Discarded it and launched fresh no-history `/root/slln_blind_retry` with the same one-read packet/hash and explicit string-array constraints. |
| BF-VHDP-CONT1-I014 | 2026-08-28 | Equation (1.5) direct-agent schema compliance | The first direct judge used `dependency_reviews` instead of `dependency_coverage`, string implication fields instead of configured verdict/reasoning objects, classification `lean_stronger`, and unconfigured finding keys. | Agent `/root/eq15_direct` covered D001--D048 and identified the pairwise-independence and finite-measure scope changes, but its response cannot satisfy `direct_judge.schema.json`. | The semantic judgment cannot enter the audit and must not be reshaped by the orchestrator or continued in place. | mitigated | Discarded it and launched a fresh no-history direct replacement with the exact outer, implication, classification, and finding vocabularies. |
| BF-VHDP-CONT1-I015 | 2026-08-28 | Equation (1.5) round-trip-agent finding schema compliance | The first round-trip judge used unconfigured finding keys `id` and `reasoning` and severity value `material`, omitting required `category` and `impact`. | Agent `/root/eq15_roundtrip` otherwise completed C01--C12, found Lean/translation-to-source yes and reverse unclear, and requested adjudication. | The response cannot pass `roundtrip_judge.schema.json` and cannot enter finalization. | mitigated | Discarded it and launched a fresh no-history replacement with the same two pinned artifacts and exact finding schema. |
| BF-VHDP-CONT1-I016 | 2026-08-28 | Sample-mean variance-limit round-trip classification vocabulary | The first fresh round-trip judge returned classification `translation_stronger`, which is not one of the five configured classifications. | The role otherwise completed C01--C12, judged translation-to-source yes and reverse no, and identified only the finite-measure and pairwise-independence generalizations. | The artifact cannot validate and must not be relabeled by the orchestrator. | mitigated | Discarded it and launched a fresh no-history replacement explicitly constrained to `faithful-stronger` or another configured value. |
| BF-VHDP-CONT1-I017 | 2026-08-28 | Sample-mean variance-limit direct dependency-status vocabulary | The first fresh direct judge used unconfigured dependency status `match` for most of D001--D054. | Agent `/root/sample_limit_direct` otherwise produced schema-shaped evidence, C01--C12, implication yes/no, and classification `faithful-stronger`. | The response cannot satisfy the dependency coverage schema and cannot enter the audit. | mitigated | Discarded it and launched a fresh no-history replacement requiring only `pass`, `fail`, `unclear`, or `not-applicable`. |
| BF-VHDP-CONT1-I018 | 2026-08-28 | Sample-mean variance-limit direct-agent legacy schema regression | The first replacement reverted to legacy keys: `dependency_id`/`dependency_name`/`rationale`, `check_id`/`criterion`, `source_implies_formal`, classification `formalization_stronger`, and unconfigured finding fields/severity. | Agent `/root/sample_limit_direct_retry` preserved the same substantive N+1, finite-measure, and pairwise-independence analysis, but none of the major nested collections conforms to `direct_judge.schema.json`. | The response cannot enter the audit and must be discarded rather than mechanically reshaped. | mitigated | Launched a third fresh no-history role with a literal minimal JSON skeleton and exact per-record key constraints. |
| BF-VHDP-CONT1-I019 | 2026-08-28 | Variance-sum round-trip isolation/protocol compliance | The fresh role paused to request the round-trip schema field names even though its initial isolated prompt already supplied the exact top-level, checklist, implication, classification, and finding vocabularies. | Agent `/root/variance_sum_roundtrip` reported reading the two pinned artifacts and then requested in-place schema coaching before producing evidence. | Continuing the same role would violate the no-coaching isolation rule and blur the provenance of its judgment. | mitigated | Interrupted and discarded the run; launch a fresh no-history replacement with a literal schema skeleton. |
| BF-VHDP-CONT1-I020 | 2026-08-28 | Variance-sum direct-agent schema compliance | The first direct judge used top-level `checklist` rather than `semantic_checklist`, reduced checklist entries to `evidence`, used status/reason implication objects, classification `lean_stronger`, and omitted required `accepted`, `requires_adjudication`, and `rationale`. | Agent `/root/variance_sum_direct` otherwise covered D001--D037 and identified empty-family, finite-measure, pairwise-independence, and L2 scope changes. | The output cannot satisfy `direct_judge.schema.json` and cannot enter the audit. | mitigated | Discarded it and launched a fresh no-history replacement with a literal full outer/nested schema skeleton. |
| BF-VHDP-CONT1-I021 | 2026-08-28 | Strong-law direct-review evidence transport | The prepared `direct_review_packet.md` contains the target declaration and dependency dossier but no readable source contract; a fresh packet-only direct role therefore marked every source comparison unclear and correctly refused to infer the hashed source statement. | Agent `/root/slln_direct_judge` returned a schema-shaped but unusable `undetermined` judgment whose critical finding is the missing source evidence. | A direct semantic comparison cannot be made from the generated packet alone, and transporting that output would create avoidable adjudication noise rather than an independent source comparison. | mitigated | Discarded the packet-only run and launched a fresh no-history replacement allowed one combined read of the pinned source-contract JSON and direct-review packet, with no other access. |
| BF-VHDP-CONT1-I022 | 2026-08-28 | Chebyshev blind-agent schema vocabulary | Both fresh Chebyshev blind roles used the obsolete keys `dependency_ledger`, `interpretation`, `effect_on_translation`, and `informal_summary` instead of the installed schema's `dependency_coverage`, `meaning`, `effect_on_target`, and `proposition_plain_english`. | `validate_agent_output.py HDP-01-COR-1.2.5 blind-translation` reported all four vocabulary mismatches; the Exercise role returned the same obsolete shape. | Neither first response could enter the hash-bound judge phase despite complete dependency order and substantive translations. | mitigated | The same isolated roles performed schema-only transformations without rereading any evidence; both corrected artifacts retained their dossier hashes and passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I023 | 2026-08-28 | Equation (1.6) round-trip classification vocabulary | The fresh round-trip judge returned classification `equivalent` instead of configured `faithful-equivalent`, while every checklist item, both implications, acceptance, and adjudication flags were otherwise schema-shaped and decisive. | The first `/root/eq16_roundtrip_judge` response used the sole unconfigured value in its classification field. | The artifact could not enter finalization until the vocabulary mismatch was corrected. | mitigated | The same isolated role changed only the classification token, reread no evidence, and the transported artifact passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I024 | 2026-08-28 | Normalized-sum source/blind schema vocabulary | The fresh source role used obsolete `locator`/`excerpt`, string-valued semantic facets and ambiguities; the fresh blind role used a zero dossier hash and dependency status `covered`. | Initial `/root/zn_source_contract` and `/root/zn_blind_translation` responses failed the installed schemas despite preserving the source statement and all D001--D043 semantics. | Neither initial response could enter the judge phase without schema-only correction. | mitigated | The same isolated roles reread no evidence and changed only schema structure, the orchestrator-supplied prepared dossier hash, and configured status tokens; both transported artifacts passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I025 | 2026-08-28 | Normalized-sum direct dependency identity and findings schema | The first direct judge diverged from the prepared inventory at D030--D043, inventing generic instance names, and encoded findings as strings. | `/root/zn_direct_judge` used names such as `Type`, `real-arithmetic-instance-1`, and `numeric-literal-instance` where the manifest requires `instHDiv` through `Real.normedAddCommGroup`. | The response could not be transported as independent hash-bound dependency coverage; correcting names would require substantive dossier interpretation rather than schema-only reshaping. | mitigated | Discarded the response and launched fresh no-history replacement `/root/zn_direct_judge_retry` with the exact prepared D001--D043 name sequence and findings schema. |
| BF-VHDP-CONT1-I026 | 2026-08-28 | Equation (1.8) direct checklist schema | The fresh direct judge used an unconfigured `runtime_id` and encoded C01--C12 under `checks` with `criterion`/`verdict` instead of the installed semantic-checklist evidence fields. | `/root/eq18_direct_judge` otherwise returned exact D001--D034 coverage, bidirectional `yes` implications, and a decisive `faithful-equivalent` classification. | The first response could not satisfy `direct_judge.schema.json`. | mitigated | The same isolated role reread no evidence and performed a schema-only C01--C12 transport into `semantic_checklist`, preserving all judgments and removing only the runtime field. |
| BF-VHDP-CONT1-I027 | 2026-08-28 | Bernoulli/binomial source and blind role schema regression | Both fresh source roles returned rich but obsolete top-level source-contract shapes, and both fresh blind roles initially omitted required schema metadata, dependency meanings/effects, and the installed translation-field names. | `/root/bernoulli_source_contract`, `/root/binomial_source_contract`, `/root/bernoulli_blind_translation`, and `/root/binomial_blind_translation` all preserved the intended mathematics and dependency order but could not validate in their first response. | Four independent artifacts required schema-only/schema-completion turns before judge consumption. | mitigated | Each isolated role reread no evidence and transported its existing content into the installed schemas; all four resulting artifacts passed `validate_agent_output.py`. |
| BF-VHDP-CONT1-I028 | 2026-08-28 | Bernoulli round-trip reader availability | The first fresh Bernoulli round-trip role attempted to use `jq` in its sole allowed read call, but `jq` was unavailable, so it read neither pinned artifact and produced no judgment. | `/root/bernoulli_roundtrip_judge` reported `/bin/bash: jq: command not found` and correctly made no further tool call. | The run yielded no semantic evidence and could not enter the audit. | mitigated | Discarded the empty run and launched fresh no-history replacement `/root/bernoulli_roundtrip_judge_retry` with an explicit `sed`-only one-read command. |
| BF-VHDP-CONT1-I029 | 2026-08-28 | Corrected Bernoulli blind-packet read bound | The fresh corrected-epoch blind role was instructed to read only lines 1--1200, but the regenerated packet extended past that bound and contained dependencies D057--D059 below it. | `validate_agent_output.py HDP-01-DEF-BERNOULLI blind-translation` expected D001--D059 but `/root/blind_translation_v2a` could cover only D001--D056 from its permitted excerpt. | The response is incomplete independent evidence and cannot enter the audit or be extended by the same role. | mitigated | Discarded the artifact and launched a fresh no-history replacement with a one-read bound covering the complete prepared packet. |
| BF-VHDP-CONT1-I030 | 2026-08-28 | Corrected binomial round-trip role token | The fresh round-trip judge returned the sole obsolete token `round-trip-judge` instead of the installed schema constant `roundtrip-judge`; all C01--C12 judgments and both implications were otherwise complete and decisive. | `/root/roundtrip_judge_v2b` returned a faithful-equivalent yes/yes result with the wrong top-level role constant. | The artifact could not validate until the vocabulary-only mismatch was corrected. | mitigated | The same isolated role was asked to change only the role token, without tools or rereading evidence. |
| BF-VHDP-CONT1-I031 | 2026-08-28 | Corrected Bernoulli direct dependency identities | The first corrected-epoch direct judge omitted prepared D017 `Monoid.toNatPow`, shifted subsequent IDs, and inserted unprepared `NormedAddCommGroup` at D021. | `/root/direct_judge_v2a` returned strong semantic C01--C12 evidence but its D017--D059 dependency identities diverged from the regenerated manifest. | The response cannot be transported as hash-bound dependency coverage and the identities cannot be repaired without re-reading the dossier. | mitigated | Discarded the response and launched a fresh no-history direct replacement with the literal prepared D001--D059 name sequence. |
| BF-VHDP-CONT1-I032 | 2026-08-28 | Convergence-definition judge schema compliance | The fresh direct and round-trip roles independently rejected the generic every-threshold definition for dropping the inherited normalized-sum and standard-normal context, but both initially returned legacy judge schemas. | `/root/direct_judge_v3` used `checklist`, `verdict`, and legacy dependency/finding fields; `/root/roundtrip_judge_v3` used comparison prose rather than the installed semantic-checklist and implication records. | The agreeing substantive failure correctly triggers a source-specific wrapper repair, but the first JSON objects cannot enter the hash-bound audit. | mitigated | Both isolated roles received schema-only correction instructions without tools or evidence rereads; their original failure judgments remain unchanged while the formal target is repaired in a fresh audit epoch. |
| BF-VHDP-CONT1-I033 | 2026-08-28 | Repaired convergence direct implication vocabulary | The repaired-epoch direct judge returned exact D001--D100 coverage and a decisive faithful-equivalent judgment, but used implication verdict token `pass` instead of the installed `yes`/`no`/`unclear` vocabulary. | `/root/convergence_direct_v4` marked both implication directions `pass` while its classification, acceptance, and rationale explicitly say equivalence. | The otherwise complete direct artifact cannot validate until the two vocabulary tokens are normalized by the isolated role. | mitigated | Requested a schema-only change from `pass` to `yes` in both implication records, with no tool call or evidence reread. |
| BF-VHDP-CONT1-I034 | 2026-08-28 | Exercise 1.3.3 round-trip classification vocabulary | The first round-trip result used legacy classification `accepted`, omitted the two required booleans, and used finding severity `warning`. | `validate_agent_output.py` rejected `/root/exercise133_roundtrip` despite its complete C01--C12 analysis and decisive translation-to-source yes / reverse no judgment. | The artifact could not enter finalization in its first serialization. | mitigated | The same isolated role reread no evidence and changed only the classification to `faithful-stronger`, added `accepted: true` / `requires_adjudication: false`, and normalized severity to `minor`; validation then passed. |
| BF-VHDP-CONT1-I035 | 2026-08-28 | Repaired convergence direct dependency identity | A packet-level check after the schema-only implication correction showed that the direct role had omitted prepared D006, shifted the following identifiers, and therefore did not actually cover the exact manifest sequence claimed by I033. | The prepared packet names D006 `NumStability.HDP.Contract.hdp_01_hdef_hconvergence_hin_hdistribution_normalized_sum_pointwise_cdf._proof_1`, while `/root/convergence_direct_v4` placed `AddCommMonoidWithOne.toAddMonoidWithOne` at D006 and continued with a different sequence. | The corrected response remains inadmissible as hash-bound dependency coverage; implication-token repair alone was insufficient. | open | Discard `/root/convergence_direct_v4`; fresh one-read replacement `/root/convergence_direct_v5` must cover the exact prepared sequence before transport. |
| BF-VHDP-CONT1-I036 | 2026-08-28 | Direct-judge source-contract locator | The first fresh convergence dependency-identity retry was given a nonexistent module-side source-contract path, and its complete packet output exceeded the direct-call return budget. | `/root/convergence_direct_v5` correctly returned `source-contract-unreadable` for `/books/vershynin-hdp/module/source-contracts/chapter-01/...`; the validated artifact actually resides under the audit task's `faithfulness/agent_outputs/source_contract.json`. | The role had no admissible source evidence and could not judge; the simultaneously launched Exercise role was interrupted before relying on the same bad locator. | mitigated | Discarded both attempted runs and launched fresh roles with the validated audit-local source-contract paths and explicit read bounds exceeding the complete 2,945-line and 2,031-line packets. |
| BF-VHDP-CONT1-I037 | 2026-08-28 | Direct-judge evidence transport cap | Even with the corrected source locator and a complete `sed` bound, the convergence packet produced 22,290 tokens and the execution layer truncated the role's sole read before D064. | `/root/convergence_direct_v6` identified the truncation and issued no judgment rather than fabricating the missing D002--D064 identities or schema. | A raw full-packet read cannot supply an independently complete 100-dependency review under the agent-output cap. | mitigated | Discarded the empty run; fresh role `/root/convergence_direct_v7` receives in one read a deterministic bounded projection of every exact inventory record plus the full source contract, dossier hash, and target dossier header. |
| BF-VHDP-CONT1-I038 | 2026-08-28 | Exercise/direct judge legacy schema | The fresh Exercise 1.3.3 direct role covered exact D001--D070 and reached the expected faithful-stronger yes/no judgment, but first used legacy dependency/checklist/implication/recommendation keys. | `/root/exercise133_direct_v2` returned `dependency_id`, `checklist`, singular `implication`, and a nested recommendation despite the current-schema prompt. | Its semantic result could not be validated in the first serialization. | mitigated | The same isolated role used no tools and deterministically rendered the existing result into the current schema; transported direct and round-trip artifacts both passed validation and finalized decision `fbcb9e7b8f3ec7bdbb3f2563c71ae9b6d6bf0774c2458c0a957f1965cdd6b4d5`. |
| BF-VHDP-CONT1-I039 | 2026-08-28 | Convergence audit transitive-import epoch invalidation | The repaired convergence audit was prepared before Exercise 1.3.3 added reusable theorems to the imported `LimitTheorems.lean`, so finalization correctly rejected the old manifest's local-import hash even though the audited target dossier and dependency inventories were unchanged. | `validate_audit.py` reported a SHA mismatch for local import 1; forced manifest `85e6b6390a0b6cef9766efc0faf702ae41a6bad3469bc22961a7aea13d998e46` retained direct packet `e1755db315895e2d4bbaae95a005b9a9a47fbe0bad0534235352e115b7423dca`, blind dossier `2200b4d52e6437b7d4e195df19ab96f992391689ec2481f1ebcfd18d81cd4ecf`, and byte-identical dependency inventories. | The old manifest could not be sealed; repeating semantic judgments over byte-identical role evidence would add no independence. | mitigated | Forced a new epoch, hash-checked the judge-facing evidence against history, reused the just-completed hash-bound role artifacts with provenance, and finalized validated decision `7af81f245fe80985e971e3712a831f36c58a85432d88fc2885def0f9a762905c`. |
| BF-VHDP-CONT1-I040 | 2026-08-28 | Tail-CLT judge packet transport | The first tail direct/round-trip prompts named nonexistent combined packet paths, and the corrected direct role's one-call read of the real source contract plus `direct_review_packet.md` exceeded the execution output budget and omitted D003--D067 and the schema footer. | The roles reported the missing paths without inspecting evidence; after corrected paths, the direct role refused to fabricate coverage from its truncated view. Direct packet SHA-256 `3b711f41430e8c290d505c761026dc9556e015d6562a608d4aa75d7be57ed13d`. | The tail audit required a provenance-recorded schema/completeness continuation before the already-isolated direct role could serialize all 101 dependency records. | mitigated | Supplied only the omitted prepared dependency identities, their standard declaration effects, and the installed output schema; prohibited rereading or further tools. The successful round-trip role required only a provenance-hash correction from dossier hash to blind-output hash. |
| BF-VHDP-CONT1-I041 | 2026-08-28 | Equation (1.7) direct-role bounded projection | The first direct-role projection command invoked unavailable `python` rather than installed `python3`, so the command failed before reading source or target evidence. | Agent `/root/eq17_direct_judge` produced no judgment from the failed call. | The same isolated role needed a corrected one-read command to obtain its authorized evidence. | mitigated | Supplied the identical bounded projection command with only the executable changed to `python3`; the role then completed its evidence review, and the failed empty call is recorded in `agent_runs.json`. |
| BF-VHDP-CONT1-I042 | 2026-08-28 | Equation (1.7) judge schema/classification consistency | The direct role serialized dependency and checklist records with legacy `review`/`verdict` fields, while the round-trip role initially paired implication verdicts yes/no with validator-incompatible classification `not-faithful-different`. | Neither first serialization could enter the hash-bound audit despite decisive substantive reviews. | Canonical validation required schema-only transport of the direct coverage and a classification reconciliation by the isolated round-trip role. | mitigated | The orchestrator transported the direct role's all-pass coverage into the manifest-ordered reviewed/reused schema without rereading evidence; the round-trip role reread no evidence and corrected yes/no to `faithful-stronger` while retaining an explicit adjudication request. Fresh adjudication accepted the strengthening, and complete validation passed. |
| BF-VHDP-CONT1-I043 | 2026-08-28 | repository-wide organization validator versus incremental Chapter 1 organization loop | `tools/architecture/check_layout.py` reports the pre-existing placeholder in `NumStability/HDP/Concentration/MetricMeasure.lean`, the existing untracked HDP module wave as new noncanonical modules, and the existing unsorted `NumStability.HDP.Scalar` umbrella; it now also lists the new source-facing Theorem 1.3.4 contract even though `tiers.json` classifies it as `source`. | Current validator output: 2359 modules, 309 unclassified, 9 mixed, 117 missing module docs, 361 legacy naming exceptions, 21 declaration-bearing umbrellas, and 1 unsorted aggregate import. | The full ratchet cannot pass in this dirty collaboration worktree, while the Chapter 1 increment itself has reusable foundations in `Scalar/PoissonLimit` and a single source wrapper in `Contracts/C_01_hthm_h1_d3_d4`. | open | Preserve the shared baseline and unrelated work; report the stable gate organization counters and treat global baseline resealing or cleanup as out of scope for this theorem increment. |
| BF-VHDP-CONT1-I044 | 2026-08-28 | Theorem 1.3.4 direct-judge evidence transport cap | Three fresh direct roles consumed their one permitted raw-packet read without receiving the complete 84-dependency dossier: the first omitted D003--D046, the second reported a wrapper-cap truncation despite a high nested command budget, and the third used both outer and nested budgets but still reported 5,669 truncated tokens. | The preserved first failed judgment is `faithfulness/agent_outputs/direct_judge_attempt1_truncated.json` (SHA-256 `5b3640b56a0a871f32068223d2cd0ea84ab1445680905d9cc06ace050ee6b187`); subsequent roles issued no admissible semantic verdict. | No truncated role may guess the missing dependency frontier or enter finalization. | mitigated | Launch a fresh no-history direct role whose sole read deterministically projects the complete source contract, target declaration, and exact D001--D084 names, roles, ordinary types, and one-level bodies while removing only redundant fully-explicit syntax, import metadata, and whitespace. |
| BF-VHDP-CONT1-I045 | 2026-08-28 | Theorem 1.3.4 blind dependency-name serialization | The blind role covered D001--D084 in order but serialized D045 under the elaborator-facing alias `Fin.instFintype` rather than the inventory's exact name `Fin.fintype`. | The first transported JSON failed `validate_agent_output.py HDP-01-THM-1.3.4 blind-translation` with `dependency names/order mismatch`. | The independent mathematical translation was complete, but the artifact could not enter the judge phase with the alias mismatch. | mitigated | The same isolated role reread no semantic evidence and changed only D045's name token; artifact SHA-256 `b3b7d62a7fd8d71970818719ef3c08927d90d1d2a3f1a60453f4302e229e1f8d` then passed validation. |
| BF-VHDP-CONT1-I046 | 2026-08-28 | Theorem 1.3.4 source-contract endpoint ambiguity | The source-only contract preserved uncertainty about whether the book's Poisson parameter convention includes `λ = 0`, causing both otherwise-agreeing judges to return `undetermined` and request adjudication. | Pinned PDF page 6 states that sums of nonnegative Bernoulli parameters tend to `λ < ∞`, imposes no strict positivity condition, and immediately defines `P{Z=k}=e^{-λ}λ^k/k!` for natural `k`; the fresh adjudicator therefore resolved `λ≥0`, including the degenerate zero-rate law. | The cautious source-contract ambiguity delayed an otherwise decisive faithful-equivalent classification but did not identify a book discrepancy. | mitigated | Preserve the source contract as immutable role evidence; accept the primary-source adjudication that both implication directions hold and record the endpoint resolution in the final audit decision. |
| BF-VHDP-CONT1-I047 | 2026-08-28 | Theorem 1.3.4 adjudicator trigger completeness | The fresh adjudicator decisively resolved the sole semantic endpoint issue but serialized two prose trigger summaries instead of the framework's ten mechanically computed adjudication reasons. | `finalize_audit.py HDP-01-THM-1.3.4 --check-adjudication` listed ten exact reasons; finalization rejected the first adjudicator artifact with `adjudicator trigger list differs from computed reasons`. | The accepted faithful-equivalent verdict could not be sealed until every computed trigger was copied verbatim and paired with an explicit resolution record. | mitigated | The same isolated adjudicator reread no semantic evidence, replaced only the trigger/completeness mapping, retained its verdict, and produced validated artifact SHA-256 `d2741158efb781fbc75fd6e751fd899c99b27008549083e12468c372aa792e9f`. |
