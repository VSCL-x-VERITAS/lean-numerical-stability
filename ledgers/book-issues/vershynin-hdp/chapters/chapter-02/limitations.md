# Limitations — chapter 2

Record book, source, or source-to-Lean limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Source row | Limitation | Scope/consequence | Lean artifact | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| VHDP-C02-L001 | Theorem 2.8.1 and Theorem 2.8.2 (printed pp. 36-37) | The printed theorems put the maximum `max_i ||X_i||_{psi_1}` in the linear branch of the tail bound. The first Lean rendering instead took an arbitrary uniform upper bound `M` with `K i <= M`, which yields a strictly weaker large-deviation branch because `t / M` can be made arbitrarily small by enlarging `M`. | Chapter 2 Section 2.8: the Lean statement is weaker than the printed statement in the sub-exponential regime, so the row could not close as `faithful-equivalent`. | `NumStability.HDP.Contract.hdp_02_hthm_h2_d8_d1`, `NumStability.HDP.Contract.hdp_02_hthm_h2_d8_d2` (pre-fix revision). | Found by the blind-translation role of the faithfulness audit for HDP-02-THM-2.8.1 before the row was closed; the translator recorded it under `restrictions_and_vacuity_risks` as "M is only required to be an upper bound for the K i and to be positive; it need not be the least such bound (e.g. max_i K_i), so the t/M term can be made arbitrarily weak by taking M huge." | open | Coordinator: restate the source-facing aliases with the exact maximum over the (necessarily nonempty) index set, keep the uniform-bound form as the reusable lemma, and re-run the audit against the corrected statement. **Update 2026-08-28 (session claude-continue-2):** addressed in Lean. `bernsteinTailPsiOne`, `bernsteinWeightedTailPsiOne` and `bernsteinAverageTailPsiOne` now state the printed 2.8.1 / 2.8.2 / 2.8.3 over `PsiOneGauge` with the exact maximum and the printed denominators, so neither the arbitrary-uniform-bound weakening (L001) nor the missing printed objects (L002) remains in the row's faithful rendering. The window-scale results are retained, separately named, as the strictly sharper reusable lemmas. Status stays `open` pending the faithfulness audit of the new declarations. |
| VHDP-C02-L002 | Theorem 2.8.2 (printed p. 37), inherited by Theorem 2.8.1, Corollary 2.8.3 | The printed statements normalize both tail branches by the single derived scalar `K = max_i ||X_i||_{psi_1}`, giving denominators `K^2 ||a||_2^2` and `K ||a||_inf`. The current Lean statements instead universally quantify over a per-index MGF-window family `K i` satisfying a hard-constant local bound `E exp(lam X_i) <= exp(K_i^2 lam^2)` for `|lam| <= 1/K_i`, with denominators `sum_i (a_i K_i)^2` and `max_i (|a_i| K_i)`. | Chapter 2 Section 2.8: the Lean proposition is strictly *stronger* (its denominators are pointwise no larger), and it implies the printed bound after rescaling `K_i := M * max_i ||X_i||_{psi_1}` using Proposition 2.7.1(e). But none of the printed statement's characteristic objects — `||X_i||_{psi_1}`, `K = max_i ||X_i||_{psi_1}`, `||a||_2`, `||a||_inf` — occurs in it, so the row cannot close as `faithful-equivalent`, and the psi_1-to-MGF bridge is left outside the formal statement. | `NumStability.HDP.Contract.hdp_02_hthm_h2_d8_d1`, `hdp_02_hthm_h2_d8_d2`, `hdp_02_hcor_h2_d8_d3` (current revision). | The independent round-trip judge for HDP-02-THM-2.8.2 classified the statement `faithful-stronger` with `accepted=false` and `requires_adjudication=true`, failing semantic checks C02, C04, C05, C06, C08, C09, all tracing to this one substitution. | open | Step 1 done in this session: the `psi_1` gauge now exists as `NumStability.HDP.Scalar.SubExponential.PsiOneGauge` (display (2.21) / property (d)), with `psiOneGauge_finite_iff`, `psiOneOrliczGauge_eq_psiOneGauge`, and Lemma 2.7.6 `psiOneGauge_sq_eq_psiTwoGauge_sq`. Step 2 (the open `next_foundation`): prove the quantitative Proposition 2.7.1 (d) => (e) bridge `PsiOneGauge mu X <= ENNReal.ofReal K -> E exp(lam X) <= exp(C K^2 lam^2)` for `|lam| <= c/K` with explicit absolute `C, c`, then state the printed `K`-uniform forms of 2.8.1/2.8.2/2.8.3 over the gauge as separately named source-facing aliases, keeping the current per-index results as the reusable strengthened lemmas. **Update 2026-08-28 (session claude-continue-2):** addressed in Lean. `bernsteinTailPsiOne`, `bernsteinWeightedTailPsiOne` and `bernsteinAverageTailPsiOne` now state the printed 2.8.1 / 2.8.2 / 2.8.3 over `PsiOneGauge` with the exact maximum and the printed denominators, so neither the arbitrary-uniform-bound weakening (L001) nor the missing printed objects (L002) remains in the row's faithful rendering. The window-scale results are retained, separately named, as the strictly sharper reusable lemmas. Status stays `open` pending the faithfulness audit of the new declarations. |
| VHDP-C02-L003 | Proposition 2.5.2 (printed pp. 24-25) | The inherited source alias assumes `E X = 0` before every parameter conversion and chooses its comparison constant after fixing the probability space and random variable. The source first asserts the equivalence of properties (i)-(iv) for arbitrary random variables, restricts centering only to the addition of property (v), and states one absolute comparison constant. | The inherited Lean target was a centered, pointwise-in-`X` specialization: it omitted the uncentered four-property theorem and did not express uniformity of the absolute constant. | `NumStability.HDP.Contract.hdp_02_hprop_h2_d5_d2`, `NumStability.HDP.Scalar.SubGaussian.subGaussianCharacterization_absolute`, and the preserved centered theorem `subGaussianCharacterization`. | The first complete audit rejected the inherited target as `not-faithful-weaker`, decision SHA-256 `1fd16f95791268a66d12ddbbcb8d9e06211cd173df619daca0bf938f937cc700`. The repaired target's current complete audit is accepted `faithful-equivalent`, implications yes/yes, decision SHA-256 `08175ff5a4e5cbbb9f25d70eb280852495d1d961968c9bad88e97e7da7dab1dc`. | addressed | Repair complete: an uncentered conversion path covers targets (i)-(iv), one constant is quantified outside every probability-space and random-variable binder, centering scopes only the five-property clause, and the accepted fresh audit promoted the row. |
| VHDP-C02-L004 | Remark 2.5.3 (printed p. 26) | The inherited target states only separate existence-level Iffs for replacing threshold `2` by `A > 1` in the tail and one-point square-MGF predicates. Read in its immediate context, the remark says Proposition 2.5.2 remains valid, including full equivalence with moment/local-square-MGF properties, the centered linear-MGF extension, and absolute-factor comparison of scale witnesses. | The qualitative Iffs could not recover quantitative witness bounds or the full characterization, so they were strictly weaker than the selected source row even though their individual threshold formulas were correct. | `NumStability.HDP.Contract.hdp_02_hrem_h2_d5_d3`, `NumStability.HDP.Scalar.SubGaussian.subGaussianThresholdCharacterization_absolute`, and the preserved qualitative theorem `subGaussianThresholdRemark`. | The first complete audit rejected the inherited target as `not-faithful-weaker`, decision SHA-256 `65532b4655aeab32aefa7ea9e577228ddf31e3823ba4b26aa9271254e7e94ad2`. The repaired target's complete audit is accepted `faithful-equivalent`, implications yes/yes, decision SHA-256 `5dd42fba0a972db000d6705a6df7a8b181900152d2725d4c129c5d0b37ce9c89`. | addressed | Repair complete: both rescalers expose scale bounds depending only on fixed threshold constants, the A-parametrized property interface covers all five properties, one comparison constant precedes every probability-space and random-variable binder, and the accepted fresh audit promoted the row. |
| VHDP-C02-L005 | Definition 2.5.6 (printed p. 27) | The inherited source alias states only that the extended-real gauge is finite iff a positive property-(iv) witness exists. The selected definition also names the class satisfying any of Proposition 2.5.2(i)-(iv) as sub-gaussian and identifies the `ψ₂` norm itself with the displayed positive-scale infimum. | The target's dependencies contained the infimum and square-point formulas, but its top-level statement omitted the four-property class designation and direct norm/infimum identification; it also totalized the gauge for arbitrary nonmeasurable functions without marking that extension. | Repaired `NumStability.HDP.Contract.hdp_02_hdef_h2_d5_d6`; new `NumStability.HDP.Scalar.SubGaussian.IsSubGaussian`, `PsiTwoNorm`, `isSubGaussian_iff_property`, and `isSubGaussian_iff_psiTwoNorm_finite`; preserved `PsiTwoGauge` and `psiTwoGauge_finite_iff`. | The first complete audit was adjudicated `not-faithful-different`, implications no/no, accepted=false: source `037c1ee6972c5a6c38222dbdbab8788fe2901348d8c425287ea9b46d9459b086`, blind `3de4a310ab6042a62776506e09792426590aeba10f2bcb67642c33ed9c117461`, direct `8a628b56ba3e614a1e7a7d53a01bacfe11ab28676b6a465316079943a869aa4e`, round trip `01cd4a0709dc3bf77b34d4b8c072bb3a9bf1da811687c1ccad42e6766d52e011`, adjudication `e2e23cd6711dc89b387fa723b8a04d596a45c960a0c05b9bb5378774f7ef72a5`, decision `2a98803fb2176b172721f31bb3436c2e934fcf4899f54e1edd1e173bb2910ba5`. The repaired audit is accepted `faithful-equivalent`, implications yes/yes, decision SHA-256 `02e88b6c9d03f1975112169f4e2c87842142f183fd07939499daed54c0a22068`. | addressed | Repair complete: the source-facing wrapper restricts to measurable random variables, characterizes the named class by every non-linear property kind, and conditionally exposes a finite named `ψ₂` norm equal to the displayed infimum; the extended gauge remains reusable. |
| VHDP-C02-L006 | Example 2.5.8(a) (printed p. 27) | The inherited Gaussian wrapper states only that the standard and scaled centered Gaussian `ψ₂` gauges are finite. The source asserts one absolute constant `C`, the standard bound `||X||_{ψ₂} <= C`, and the uniform homogeneous bound `||X||_{ψ₂} <= C σ` for `X ~ N(0,σ²)`. | Pointwise qualitative finiteness cannot recover a common constant or linear dependence on `σ`, so the source-facing row is strictly weaker even though its canonical Gaussian-law representation is valid. | Repaired `NumStability.HDP.Contract.hdp_02_hexample_h2_d5_d8a`; new `NumStability.HDP.Scalar.SubGaussian.psiTwoGauge_le_of_squarePoint` and `gaussianPsiTwoGauge_le_two_mul`; preserved `gaussianPsiTwoGauge_finite`. | The first complete audit is adjudicated `not-faithful-weaker`, Lean-implies-source=`no`, source-implies-Lean=`yes`, accepted=`false`: source `de36de7142ab5beaba2636e11da54e738294b5fafde72001a51e674958d42046`, blind `aec9cdc059660bbeada71533220ccf020b91d675cbe114c99b11c19d0585ff1f`, direct `009155d586e838a9dea37750a0032ecb322bc03d78c088a1894bbe4ae1cf3cfc`, round trip `807e0f0b3853d7945b965caf37a6bb10066f2543139f1951dfdf59b08fb318c7`, adjudication `49dfa5fdb895d96ffe4244aa2b7533212844d00446c0a70162825b3cf737a1e5`, decision `561aa482de7518732e40d11916bf1b70202b699dc5a9b9dba8f282a03d825cd9`. The repaired complete audit is adjudicated accepted `faithful-equivalent`, implications yes/yes, with source `de36de7142ab5beaba2636e11da54e738294b5fafde72001a51e674958d42046`, blind `2ff3b54d5060fbb19bff2009fb6fa8c4961d7580b8e8d285d436ef520caf3245`, direct `f1beaed861da8088b50f99c76ebcadcac205c9c6f3a43620309c7ba77e170f10`, round trip `f551ad4cec58ea59a2239ed383c7e3684c1b3ebcc399180a7bbad38f52380dac`, adjudication `325bdd0186d85ad084a2a92eae71612d05d940cca09aa2dbc1a107eebe8d7fb4`, and decision `07a2d0e2021ba92dfafbdba8890fa69c94dae37260d0274d39cb31beafef856a`. | addressed | Repair and semantic re-audit complete: the wrapper exhibits `C=2` before both bounds, handles `σ=0` exactly, and the accepted hash-bound audit supports promotion to PROVED. The rejected audit remains preserved in history. |
