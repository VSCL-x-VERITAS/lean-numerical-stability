# Limitations — chapter 2

Record book, source, or source-to-Lean limitations encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Source row | Limitation | Scope/consequence | Lean artifact | Workaround | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| VHDP-C02-L001 | Theorem 2.8.1 and Theorem 2.8.2 (printed pp. 36-37) | The printed theorems put the maximum `max_i ||X_i||_{psi_1}` in the linear branch of the tail bound. The first Lean rendering instead took an arbitrary uniform upper bound `M` with `K i <= M`, which yields a strictly weaker large-deviation branch because `t / M` can be made arbitrarily small by enlarging `M`. | Chapter 2 Section 2.8: the Lean statement is weaker than the printed statement in the sub-exponential regime, so the row could not close as `faithful-equivalent`. | `NumStability.HDP.Contract.hdp_02_hthm_h2_d8_d1`, `NumStability.HDP.Contract.hdp_02_hthm_h2_d8_d2` (pre-fix revision). | Found by the blind-translation role of the faithfulness audit for HDP-02-THM-2.8.1 before the row was closed; the translator recorded it under `restrictions_and_vacuity_risks` as "M is only required to be an upper bound for the K i and to be positive; it need not be the least such bound (e.g. max_i K_i), so the t/M term can be made arbitrarily weak by taking M huge." | open | Coordinator: restate the source-facing aliases with the exact maximum over the (necessarily nonempty) index set, keep the uniform-bound form as the reusable lemma, and re-run the audit against the corrected statement. **Update 2026-08-28 (session claude-continue-2):** addressed in Lean. `bernsteinTailPsiOne`, `bernsteinWeightedTailPsiOne` and `bernsteinAverageTailPsiOne` now state the printed 2.8.1 / 2.8.2 / 2.8.3 over `PsiOneGauge` with the exact maximum and the printed denominators, so neither the arbitrary-uniform-bound weakening (L001) nor the missing printed objects (L002) remains in the row's faithful rendering. The window-scale results are retained, separately named, as the strictly sharper reusable lemmas. Status stays `open` pending the faithfulness audit of the new declarations. |
| VHDP-C02-L002 | Theorem 2.8.2 (printed p. 37), inherited by Theorem 2.8.1, Corollary 2.8.3 | The printed statements normalize both tail branches by the single derived scalar `K = max_i ||X_i||_{psi_1}`, giving denominators `K^2 ||a||_2^2` and `K ||a||_inf`. The current Lean statements instead universally quantify over a per-index MGF-window family `K i` satisfying a hard-constant local bound `E exp(lam X_i) <= exp(K_i^2 lam^2)` for `|lam| <= 1/K_i`, with denominators `sum_i (a_i K_i)^2` and `max_i (|a_i| K_i)`. | Chapter 2 Section 2.8: the Lean proposition is strictly *stronger* (its denominators are pointwise no larger), and it implies the printed bound after rescaling `K_i := M * max_i ||X_i||_{psi_1}` using Proposition 2.7.1(e). But none of the printed statement's characteristic objects — `||X_i||_{psi_1}`, `K = max_i ||X_i||_{psi_1}`, `||a||_2`, `||a||_inf` — occurs in it, so the row cannot close as `faithful-equivalent`, and the psi_1-to-MGF bridge is left outside the formal statement. | `NumStability.HDP.Contract.hdp_02_hthm_h2_d8_d1`, `hdp_02_hthm_h2_d8_d2`, `hdp_02_hcor_h2_d8_d3` (current revision). | The independent round-trip judge for HDP-02-THM-2.8.2 classified the statement `faithful-stronger` with `accepted=false` and `requires_adjudication=true`, failing semantic checks C02, C04, C05, C06, C08, C09, all tracing to this one substitution. | open | Step 1 done in this session: the `psi_1` gauge now exists as `NumStability.HDP.Scalar.SubExponential.PsiOneGauge` (display (2.21) / property (d)), with `psiOneGauge_finite_iff`, `psiOneOrliczGauge_eq_psiOneGauge`, and Lemma 2.7.6 `psiOneGauge_sq_eq_psiTwoGauge_sq`. Step 2 (the open `next_foundation`): prove the quantitative Proposition 2.7.1 (d) => (e) bridge `PsiOneGauge mu X <= ENNReal.ofReal K -> E exp(lam X) <= exp(C K^2 lam^2)` for `|lam| <= c/K` with explicit absolute `C, c`, then state the printed `K`-uniform forms of 2.8.1/2.8.2/2.8.3 over the gauge as separately named source-facing aliases, keeping the current per-index results as the reusable strengthened lemmas. **Update 2026-08-28 (session claude-continue-2):** addressed in Lean. `bernsteinTailPsiOne`, `bernsteinWeightedTailPsiOne` and `bernsteinAverageTailPsiOne` now state the printed 2.8.1 / 2.8.2 / 2.8.3 over `PsiOneGauge` with the exact maximum and the printed denominators, so neither the arbitrary-uniform-bound weakening (L001) nor the missing printed objects (L002) remains in the row's faithful rendering. The window-scale results are retained, separately named, as the strictly sharper reusable lemmas. Status stays `open` pending the faithfulness audit of the new declarations. |
