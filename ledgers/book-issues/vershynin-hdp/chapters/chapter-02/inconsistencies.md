# Inconsistencies — chapter 2

Record book, source, or source-to-Lean inconsistencies encountered in this scope. Use stable IDs; never delete history—mark superseded or closed entries.

| ID | Source row | Surfaces compared | Inconsistency | Lean impact | Evidence | Status | Owner/next action |
|---|---|---|---|---|---|---|---|
| VHDP-C02-N021 | Equations (2.14), (2.16), and the following square-moment display (printed p. 27) | Displayed quotients by `||X||_{psi_2}` vs. Definition 2.5.6 and the zero random variable | The zero random variable has `psi_2` norm zero, but the source displays `t^2 / ||X||_{psi_2}^2`, `X^2 / ||X||_{psi_2}^2`, and `lambda^2 ||X||_{psi_2}^2` without specifying a convention for the zero-norm quotient in the first two formulas. | The Lean wrappers preserve the universal finite-gauge domain, prove that zero gauge implies `X = 0` almost everywhere, and use total real division in that branch. Equation (2.14) becomes the conservative bound `P{|X| >= t} <= 2`; the square-moment integrand is exactly one almost everywhere; Equation (2.16) has both sides one after centering. | Pinned printed page 27 and the completed Equation (2.14) adjudication; direct proofs `psiTwoGaugeToTail`, `psiTwoGauge_squareMoment`, and `psiTwoGaugeToMGF` compile, including explicit zero-gauge branches. | open | Preserve the source-wide domain and record the totalized zero-gauge completion in each hash-bound audit; do not silently add a positive-norm hypothesis. |
| VHDP-C02-N001 | Theorem 2.6.3 / preceding body text (printed p. 29) | Rendered PDF p. 29 body text vs. the theorem it introduces | The text reads "let us apply Theorem 2.6.3 for a_i X_i instead of X_i. We obtain the following." and then states Theorem 2.6.3. The derivation is self-referential; the theorem being applied is Theorem 2.6.2. | None on the Lean statements: 2.6.2 and 2.6.3 are formalized as separate printed rows and 2.6.3 is derived from 2.6.2, which is the mathematically intended direction. | Rendered page image of PDF page 18 (printed page 29) read directly in this session; both theorem statements confirmed two-sided with absolute-value bars. | open | Record only; no Lean change required. Printed cross-reference defect. |
| VHDP-C02-N002 | Definition 2.7.5 (printed p. 33) | Definition 2.7.5 prose vs. its own display (2.21), and vs. the parallel Definition 2.5.6 | Definition 2.7.5 defines the sub-exponential norm as "the smallest K3 in property 3", but the display (2.21) it then gives, inf{t>0 : E exp(|X|/t) <= 2}, is the smallest K4 of property (d), not the smallest K3 of property (c). The properties of Proposition 2.7.1 are also lettered a-e, so "property 3" mislabels them; the parallel Definition 2.5.6 correctly says "the smallest K4 in property iv". | The Lean sub-exponential gauge must follow the display (2.21) / property (d), matching the psi_2 gauge convention, not the prose's "K3 in property 3". | Rendered PDF text of printed p. 33 (extraction and structural grep both read in this session); compared against printed p. 27 Definition 2.5.6. | superseded | The original policy choice was rejected by independent faithfulness adjudication; preserve this historical entry and use VHDP-C02-N024's two-gauge treatment. |
| VHDP-C02-N003 | Theorem 2.9.1 (printed p. 39) | Theorem 2.9.1 hypotheses vs. its conclusion and vector notation | The theorem fixes X_1, ..., X_N independent and sums c_i^2 over i = 1..N, but declares f : R^n -> R, quantifies the bounded-difference condition over x in R^n, and sets X = (X_1, ..., X_n). The dimension is written both N and n. | The Lean statement must fix a single index count; no mathematical ambiguity remains once N = n is imposed. | Rendered PDF text of printed p. 39 read in this session, including footnotes 10 and 11. | open | Record only; formalize with one index type. Printed notational defect in a Notes-section cited theorem. |
| VHDP-C02-N004 | Section 2.9 body after Theorem 2.9.2 (printed p. 39) | The body text's own definition of u vs. its large-deviation condition | The text sets u := K t / sigma^2 and then writes "in the large deviations regime, say where u >> K t / sigma^2 >= 2", i.e. u >> u. The intended condition is u >= 2. | None: the row is an asymptotic commentary on Bennett's inequality, not a formalizable bound as printed. | Rendered PDF text of printed p. 39 read in this session. | open | Record only; the row is accounted for as commentary on a cited theorem. |
| VHDP-C02-N005 | Proposition 2.7.1 footnote 7 (printed p. 31) | Proposition 2.7.1 footnote 7 vs. Proposition 2.5.2 footnote 4 and vs. Proposition 2.7.1's own property list | Proposition 2.7.1 states five properties (a)-(e) but its footnote 7 asserts constant-comparability only "for any two properties i, j = 1, 2, 3, 4", omitting (e). The parallel footnote 4 of Proposition 2.5.2 covers "i, j = 1, ..., 5", including its mean-zero property (v). The two parallel results therefore make different claims about the same structure. | The Lean equivalence for Proposition 2.7.1 should state the (a)-(d) constant comparability exactly as printed and treat the (e) direction constants separately, rather than silently extending footnote 7 to five properties. | Rendered PDF text of printed pp. 24 and 31-32 read in this session; both footnotes quoted verbatim from the extraction. | open | Record and preserve the printed scope; do not strengthen footnote 7 when formalizing. |
| VHDP-C02-N006 | Theorems 2.6.2 and 2.6.3 (printed p. 29) | Two distinct printed theorems carrying one name | Theorem 2.6.2 and Theorem 2.6.3 are both titled "General Hoeffding's inequality" although they are different statements (unweighted with per-variable psi_2 norms; weighted with K = max_i ||X_i||_{psi_2}). | Both need separately named Lean declarations and separate gate rows so the weighted and unweighted forms cannot be conflated. | Rendered page image of PDF page 18 (printed page 29) read in this session. | open | Record only; keep `hdp_02_hthm_h2_d6_d2` and `hdp_02_hthm_h2_d6_d3` as distinct source aliases. |
| VHDP-C02-N007 | Theorem 2.8.4 / Exercise 2.8.6 (printed p. 38) | Universal domain `t >= 0` vs. the displayed rational exponent | If every `X_i` has zero second moment and `t = 0`, then the displayed denominator `sigma^2 + Kt/3` is zero while the numerator `t^2/2` is also zero, so the printed exponent contains `0/0`; the source states no boundary convention or separate degenerate case. | The Lean statement keeps the printed `t >= 0` domain and uses standard total real division, making the degenerate exponent zero and the harmless bound `P{|sum X_i| >= 0} <= 2`. Faithfulness adjudication must treat this as an explicit conservative resolution of source-silent notation, not silently add a positive-variance premise. | Printed Theorem 2.8.4 and Exercise 2.8.6 on the pinned PDF; independently raised by the Exercise 2.8.6 round-trip judge under semantic check C11 and `boundary-division-convention`. | open | Record the source ambiguity; retain the source-wide domain and the zero-variance Lean branch unless adjudication finds a genuine mismatch. |
| VHDP-C02-N008 | Theorem 2.8.1 (printed p. 36) | Universal sub-exponential family and `t >= 0` domain vs. both displayed normalization denominators | If every `X_i` has zero `psi_1` norm, then both `sum_i ||X_i||_{psi_1}^2` and `max_i ||X_i||_{psi_1}` vanish; at `t = 0` both printed branches contain `0/0`, and for positive `t` they contain division by zero. The source states no extended-real or limiting convention and no separate degenerate case. | The repaired Lean statement does not add a source-absent positive-energy premise. Standard total real division makes both branches zero and yields the harmless bound by `probability <= 1 <= 2`; exact faithfulness still needs adjudication of the source-silent notation. | Printed Theorem 2.8.1 on the pinned PDF; independently raised by the fresh direct judge after the quantifier-scope repair under semantic check C11 and `degenerate-zero-denominators`. | open | Preserve the universal source domain and explicit zero-energy Lean branch; classify the source-boundary convention through the hash-bound adjudication rather than silently narrowing the theorem. |
| VHDP-C02-N009 | Theorem 2.8.2 (printed p. 37) | Arbitrary weight vector and unrestricted psi-one maximum vs. the two displayed normalization denominators | The source permits every `a in R^N` and does not require `K = max_i ||X_i||_{psi_1}` to be positive. Thus `a = 0` or an all-zero-gauge family makes one or both denominators zero; at `t = 0` the display contains `0/0`, and for positive `t` it contains positive-real division by zero, with no stated convention. | The repaired Lean theorem keeps the printed universal domain and handles zero gauge or zero weight energy using standard total real division and `probability <= 1 <= 2`. Its nondegenerate core is equivalent to the source, but the complete audit cannot classify the source-silent boundary convention as bidirectionally faithful. | Pinned Theorem 2.8.2 source and fresh artifacts: direct `6077010b84949fb6bcdcc904bf2d5d879c1c796f029b34c94a8b10922ca3cf5f`, round-trip `8b4220e7fcd90f93e8f99b2ab6f0ba39e00fd268a68c6c8cd4d88f6696a88d09`, adjudication `010c922d649bbbb01099778b852a8b91c282850123948a9795c99f636c198c65`; after exact packet restoration for the shared-file normalized-regimes foundation, current final decision `d3b8b7ba945f8bfe0908329e217eecb243d08ab223aefb57d1ec17a27f03414b`. | open | Preserve the universal wrapper and explicit degenerate branches; resolve through a module-level source convention or a separately audited corrected/nondegenerate contract, not by silently adding positivity hypotheses. |
| VHDP-C02-N010 | Corollary 2.8.3 (printed p. 37) | Universal sub-exponential family and `t >= 0` domain vs. the two displayed `K` denominators | The source does not exclude an all-zero family, so `K = max_i ||X_i||_{psi_1}` may vanish. It nevertheless displays `t^2/K^2` and `t/K` for every `t >= 0`; at `t = 0` this gives `0/0`, and for positive `t` it gives positive-real division by zero, with no convention. | The repaired Lean corollary keeps the source-wide domain and uses total real division plus `probability <= 1 <= 2` in the zero-gauge branch. Nonempty indexing makes the separate `1/N` average normalization unproblematic. The exact `K > 0` core is equivalent, but the complete audit cannot classify the source-silent boundary globally. | Pinned Corollary 2.8.3 source and validated artifacts: direct `3742ccadbe46924d6399c5375e24a0bc830cde0a7c67d62da4ad816a49d0a3f6`, round-trip `0b24a25c10660f272fa0597926ebeb3d16327eb78d97e2be26ae91d542dd9517`, adjudication `a4fac3f205f4a487c1b6713d518b1d667bfbcfb35e133f5feae5c5832f4c3cb1`; after exact packet restoration for the shared-file normalized-regimes foundation, current final decision `44af861de642e41d244270d8d5784265b42c9d143fa662beef58ff7a1d0c8f1a`. | open | Preserve the universal wrapper and explicit zero-gauge branch; resolve through a module-level source convention or a separately audited corrected/nondegenerate contract, not by adding a silent `K > 0` premise. |
| VHDP-C02-N011 | Unnumbered normalized two-regime display after Corollary 2.8.3 (printed p. 37) | Literal large-deviation branch and footnote 9 vs. the preceding weighted Bernstein theorem | Specializing Theorem 2.8.2 at `a_i = 1/sqrt N` gives rate `c_0 min(t^2/K^2, t sqrt N/K)`, so the large branch has a positive coefficient proportional to `c_0/K`. The display instead prints `2 exp(-t sqrt N)` with coefficient 1. Footnote 9 says only that constants `c, C` may depend on K; changing the regime threshold `C` cannot replace the asymptotic large-tail coefficient by 1 for arbitrary K. | The exact normalized `min` theorem is proved as `bernsteinNormalizedTailPsiOne`. The corrected split `bernsteinNormalizedTwoRegimePsiOne_corrected` and wrapper `hdp_02_body_h2_d8_hnormalized_hregimes_corrected` retain a positive K-dependent coefficient in both branches and explicitly require `K > 0`; they are not presented as literal aliases for the printed display. | Direct reading of the pinned printed p. 37 display and footnote 9; algebraic specialization of the already proved weighted Bernstein theorem; both new declarations compile and the full `NumStability.HDP` build passes with 3651 jobs. | open | Create a separate hash-bound discrepancy/adjudication contract for the literal row. Do not silently identify the corrected large branch `2 exp(-c t sqrt N)` with the printed `2 exp(-t sqrt N)`. |
| VHDP-C02-N012 | Equation (2.23) in the proof of Theorem 2.8.1 (printed p. 36) | Ordinary expectation notation in the display vs. the chapter's explicitly possible infinite sub-exponential MGFs | Equation (2.23) is stated for every positive `λ` before the small-`λ` restriction (2.24), while Remark 2.7.9 explains that a sub-exponential MGF can be infinite for large positive `λ`. The display does not state whether its expectations and product are extended-valued or whether finiteness is tacitly assumed. | `hdp_02_heq_h2_d23` uses `ENNReal` probabilities and nonnegative extended integrals, so the inequality remains meaningful when one or more coordinate MGFs are infinite; it retains the enclosing theorem's nonempty, centered, sub-exponential, independent, and `t ≥ 0` context. | The first hash-bound direct and round-trip judgments independently identified the crossing finite-MGF domain. The repaired declaration compiles using extended Markov and `lintegral_prod_eq_prod_lintegral_of_indepFun`; the source-boundary interpretation remains subject to the refreshed audit/adjudication. | open | Preserve the explicit extended-valued wrapper and record any adjudicator policy; do not silently narrow (2.23) to a finite-MGF premise. |
| VHDP-C02-N013 | Theorem 2.2.2 (printed p. 15) | Universal coefficient vector vs. the displayed denominator and normalization proof | The theorem permits arbitrary `a ∈ ℝ^N`, but its bound divides by `2‖a‖₂²` and the proof immediately says to assume `‖a‖₂ = 1` without treating `a = 0`. Thus the printed zero-vector formula has a zero denominator and the stated normalization step is unavailable. | The source-facing theorem keeps arbitrary coefficients. For zero square energy, standard total real division makes the exponent zero and the conclusion becomes the valid bound `P{0 ≥ t} ≤ 1`; for positive energy the existing optimized proof applies. The isolated WLOG wrapper exposes the exhaustive zero-energy/trivial-event branch or positive-energy/unit-normalization branch, rather than adding a source-absent nonzero premise. | Pinned Theorem 2.2.2 statement/proof; the fresh source contract records the ambiguity. The theorem's completed audit accepts the totalized zero-vector boundary completion; the first WLOG direct judgment rejected a positive-energy-only wrapper, prompting the explicit exhaustive repair. The refreshed WLOG audit then accepted the exhaustive contract as `faithful-stronger` with Lean-to-source implication `yes` and source-to-Lean implication `no`. | open | Preserve the universal coefficient domain and explicit zero branch, plus the accepted exhaustive WLOG contract; retain this source-silent division convention as an open book issue rather than silently narrowing the theorem. |
| VHDP-C02-N014 | Theorem 2.2.5 (printed p. 17) | Arbitrary coefficient vector and positive threshold vs. the displayed squared-norm denominator | The theorem permits `a ∈ ℝ^N` without a nonzero hypothesis but divides by `2‖a‖₂²`. At `a = 0` the printed real quotient has zero denominator, although `t > 0` makes the two-sided tail event empty. | The source wrapper removes the inherited positive-energy premise and handles zero energy explicitly. Lean's total real division makes the right side `2`, while the event probability is exactly zero; the nondegenerate branch reuses the optimized theorem. | Pinned Theorem 2.2.5; both direct and round-trip judges left the boundary implications unclear. Exact-trigger adjudication SHA-256 `70a0992f1abe1cd0417a6c52ade7b1559f6d94557a079de274a6e5b8a07b79cc` resolved the source normalization context as supplying the trivial branch and accepted `faithful-equivalent`; decision SHA-256 `4f53d0dee8223fd3d496fc3e4b40619c1ac6139adc7ce5d9bef907a6183694e7`. | open | Preserve the universal coefficient domain and explicit zero-energy branch; do not silently add `‖a‖₂ > 0`. |
| VHDP-C02-N015 | Theorem 2.2.6 (printed p. 17) | Arbitrary bounding intervals vs. the displayed total-width denominator | The theorem assumes only `X_i ∈ [m_i,M_i]` and does not require positive total squared width, yet divides by `∑(M_i-m_i)²`. If every interval is degenerate, the printed quotient has zero denominator while the positive-threshold centered tail is trivial. | The source wrapper omits the inherited positive-width premise. It applies the reusable theorem when total width is positive and uses the universal probability bound when total width is zero, where Lean's total division reduces the displayed right side to one. | Pinned Theorem 2.2.6; direct and round-trip judgments triggered adjudication of the degenerate denominator, almost-sure interval semantics, dependencies, and implication directions. Exact-trigger adjudication SHA-256 `3b21619e423082bef80d963a81818f5cafe3808545ba661e0cf4cf43bc7f5ac6` accepted `faithful-equivalent` with yes/yes implications and no uncertainty; decision SHA-256 `c980fedd413647978d73974b975d471e5f3650bc45cc427ebabf48d71aff900e`. | open | Preserve the source-wide interval domain and explicit zero-width branch; do not silently add a positive-width premise. |
| VHDP-C02-N016 | Exercise 2.2.9(a) and footnote 2 (printed p. 18) | Opening definition of epsilon-accuracy vs. the footnote's probability event | The exercise defines an epsilon-accurate estimate by membership in the open interval `(mu-epsilon, mu+epsilon)`, but footnote 2 restates the guarantee with the closed event `|mu-hat-mu| <= epsilon`. These differ when the error has boundary mass. | The Lean theorem controls the complementary closed failure event `|mu-hat-mu| >= epsilon`, exactly matching the opening strict-accuracy convention and therefore implying the weaker closed-success footnote convention. | Completed Exercise 2.2.9(a) audit decision SHA-256 `767e09a6e8ae0feca7d5519f1af6f9fa8105bdd2a51fc05bf4a4e30b9052f022`; both judges identified the boundary mismatch and accepted the Lean target as faithful-stronger. | open | Preserve the opening paragraph's strict accuracy convention in Lean; do not use the footnote to weaken the failure event without an explicit source-policy choice. |
| VHDP-C02-N017 | Exercise 2.2.9(b) (printed p. 18) | Literal raw-product big-O sample size vs. positive integer sampling at degenerate parameters | The displayed rate `N = O(log(delta^{-1}) sigma^2/epsilon^2)` vanishes when `sigma = 0` and tends to zero as `delta` tends to one, although any data-based median-of-means construction uses positive integer block sizes and a positive number of blocks. The source gives no `max(1, ·)`, rounding convention, or explicit asymptotic regime. | The Lean wrapper chooses explicit positive integer block size `n` and odd block count `2k+1`, with thresholds `n >= 4 sigma^2/epsilon^2` and `2k+1 >= 8 log(1/delta)`. This realizes the intended nondegenerate asymptotic rate and remains valid at the boundary, but it is not a literal uniform equality to the raw product. | Completed Exercise 2.2.9(b) adjudication SHA-256 `fd6b4be3b5e186e1b74efa918c3d3ca3310d5ea2dca6b3b7cd71df2615a54f15` and decision SHA-256 `69cc27ba6c76c4829c401d781d381b90a6fec0a5d67bff288de59a6fd8ece847`; adjudication accepted faithful-stronger and retained this as a source limitation. | open | Preserve explicit integer thresholds and report the source's missing max-one/rounding convention; do not claim a literal uniform raw-product bound at `sigma=0` or low confidence. |
| VHDP-C02-N018 | Exercise 2.3.3 and display (2.8) (printed p. 19) | Source notation `Pois(lambda)` vs. the Lean parameter type `NNReal` | The selected source passage does not state whether its Poisson parameter convention permits `lambda = 0`. The Lean wrapper includes rate zero; under `t > lambda` that case is the point mass at zero and the asserted upper-tail event is empty. | No proof defect: the zero-rate case is discharged explicitly from the Poisson point mass at zero. Semantic classification depends on whether the book's implicit distribution convention regards this as a trivial boundary completion or a genuine domain extension. | Fresh direct judgments for Exercise 2.3.3 and display (2.8), SHA-256 `78dda2d358da086b14b8cc253658108cf35b6b15caa46eb19bfdd72a73e7edcd` and `729f4c513a8c0e8f53ebcedb3bd306ab28e61791db5723296143611c52b1195e`, accept every formula component but require adjudication solely on the zero-rate convention. | open | Preserve the explicit zero-rate Lean branch; resolve both same-source-group audits through independent adjudication rather than silently imposing `lambda > 0`. |
| VHDP-C02-N019 | Exercise 2.3.2 (printed p. 19) | Printed domain `t < mu` vs. the displayed factor `(e mu / t)^t` | The exercise states only `t < mu`, but at `t = 0` the displayed quotient divides by zero, and at negative `t` the usual real-valued interpretation of a noninteger power of `e mu / t` is not generally available. The standard Chernoff lower-tail formula has the meaningful domain `0 < t < mu`. | `hdp_02_hex_h2_d3_d2_source` proves the standard domain `0 < t < mu`. The separate reusable theorem `poissonBinomial_zeroThresholdBound` and wrapper `hdp_02_hex_h2_d3_d2_zero` prove the exact endpoint `P{S_N ≤ 0} ≤ exp(-mu)` from independence and `1-p ≤ exp(-p)`; all new targets and the 3678-job Contracts build pass. | The full validated audit records source contract `c5f674bc2010fe6c051ae631a289ab695f0bb28aad97b2020d8fd49655a3c99b`, blind translation `341815c29c9b00885985cee7bd9cb24f92c9a837e3cec95907003c169f545422`, direct judgment `3f131d3635675bfdd7ca6f5ea2251a4f6f6381ca1848cc21956f6aae50cc78fa`, round-trip judgment `76bb84742298c18c23af04fd87f312c576872ce466bff74a433df39e344679ff`, adjudication `297900865f45e2d013731c10148cf615e24dd7c2ea3be41c39c4a9fb51c02070`, and decision `0066f4ef1bec8097144ba9e61fccf610d24e7f446283551f467626aab484259f`. The accepted=false verdict is `undetermined` with implications unclear/yes. | hard-blocked | Local mathematics now covers every well-defined nonnegative threshold case. Resume only after an operator-approved module policy selects the standard positive domain, specifies negative-base real-power semantics, or authorizes distinct correction/discrepancy treatment; do not claim the printed formula literally covers negative `t`. |
| VHDP-C02-N020 | Example 2.5.8(b)-(c), Equation (2.17), and Exercise 2.5.9 | Gate locators vs. pinned rendered source | These adjacent gate rows were assigned across the page-27/page-28 boundary, but all four occur on PDF page 16 / printed page 27; printed page 28 starts Exercise 2.5.10. | Faithfulness tasks for these rows must bind to PDF page 16 / printed page 27. Their mathematics and existing Lean declarations are unaffected. | Direct `pdftotext -f 16 -l 16 -layout` and `-f 17 -l 17` checks against source SHA-256 `ecc53be86c091b5df118c4464b6d81672e0261df960a3f32ceea4900107d090a` in session `codex-chapter02-b`. | closed | Correct the four gate locators to printed page 27 and use verified PDF/printed locators in audit tasks; do not edit the sealed unit index. |
| VHDP-C02-N022 | Exercise 2.5.9 (printed p. 27) | Plural distribution-family names vs. their unstated parameters and endpoints | The exercise says only that “Poisson, exponential, Pareto and Cauchy distributions are not sub-gaussian.” It prints no intensity, rate, scale, shape, or location parameters, no universal/existential quantifiers, and no nondegeneracy convention. In particular, allowing Poisson intensity zero would make a universal claim false because that law is degenerate and sub-gaussian. | The first source wrapper proved every positive Poisson rate but selected Exp(1), Pareto(1,1), and Cauchy(0,1), creating an asymmetric scope that the complete audit classified `undetermined`. A uniformly all-nondegenerate-parameters theorem is an actionable faithful strengthening, but its universal binders must be reported as added strength rather than source syntax. | Pinned Exercise 2.5.9 and Definition 2.5.6; complete first audit decision SHA-256 `01f23e49dd1eba09d8104975420782f97877da50a74ec6839fc298b7046a318e`, with all law/operator semantics resolved and both implications unclear solely from parameter scope. | open | Preserve the source ambiguity in the contract. Strengthen the reusable mathematics uniformly over conventional nondegenerate parameter ranges, rerun target-dependent semantic roles, and require any accepted classification to identify the universal scope as nonvacuous added strength. |
| VHDP-C02-N023 | Section 2.7 Gaussian-square-tail display and footnote 6 (printed p. 31) | Suppressed polynomial pre-factor vs. Proposition 2.1.2 | Footnote 6 says the display ignored a pre-factor `1/t`, but Proposition 2.1.2 gives a one-sided Gaussian-tail factor `1/u`; substituting the display's `u = sqrt(t)` and doubling for symmetry yields a leading factor proportional to `t^(-1/2)`, not `t^(-1)`. | `NumStability.HDP.Scalar.GaussianSquareTail.standardNormal_squareTail_bounds` and source wrapper `NumStability.HDP.Contract.hdp_02_hbody_h2_d7_hgaussian_hsquare_htail` state the exact two-sided Mills bounds with `1/sqrt(t)` and the lower correction `1/(sqrt(t))^3`. | Fresh source-batch contract SHA-256 `f2a9053f59fc0eb571aebbe0d71ed6fcd407d5fc1f2036d41cdaf01bba7e8ffd`, extracted independently from the pinned Chapter 2 PDF and Proposition 2.1.2. | open | Preserve the footnote as a source discrepancy and use exact Mills bounds in Lean; do not formalize the literal `1/t` pre-factor. |
| VHDP-C02-N024 | Definition 2.7.5 and Equation (2.21) (printed p. 33) | Literal property-(c) prose vs. property-(d) display and completed semantic adjudication | The source calls `||X||_{psi_1}` both the smallest `K3` satisfying Proposition 2.7.1(c) and, “in other words,” the property-(d) infimum in (2.21), although Proposition 2.7.1 supplies only absolute-factor equivalence between those numerical parameters and does not establish equality or attainment of the property-(c) infimum. | Keep two reusable gauges. `PsiOnePropertyThreeGauge` formalizes the literal property-(c) infimum for Definition 2.7.5, while `PsiOneGauge` formalizes the separately audited display (2.21). Do not identify them by definitional equality; the class-membership equivalence follows through the uniform characterization. | The first property-(d) Definition wrapper was adjudicated `not-faithful-weaker` (adjudicator SHA-256 `73953bc20e57de5f2feb52985ede1a8b5936476b4925fb2be568f089b1f5a481`). Fresh repaired source contract SHA-256 `71c1bfadec4255560afb3d13798c01e9c8ce132ada1fcc5587e8088843f5f700` independently preserves both clauses. The repaired declarations pass the 6,136-job full build and standard-axiom inspection. | open | Complete the fresh repaired-target faithfulness loop. Retain Definition 2.7.5 and Equation (2.21) as distinct rows and report the source discrepancy rather than choosing one as a silent correction. |
| VHDP-C02-N025 | Remark 2.7.14 (printed p. 36) | Displayed hierarchy and cited earlier results vs. the remark's explanatory attribution | The displayed hierarchy is `L^infinity subset L_{psi_2} subset L^p`. The remark then says the first inclusion follows from Proposition 2.5.2(ii) and the second from (2.17), but those references support the inclusions in the opposite order: (2.17) is the bounded-to-psi-two estimate and Proposition 2.5.2(ii) is the psi-two-to-moment implication. | Formalize the two containments in their displayed directions and do not encode the reversed attribution as proof dependencies. | Pinned Remark 2.7.14 and its cited Proposition 2.5.2(ii)/Equation (2.17); the fresh source-only round-trip judgment independently classified the target faithful-equivalent and recorded `source-cross-reference-order`. | open | Record only; preserve the mathematical hierarchy and treat the swapped citation order as an editorial source defect. |
| VHDP-C02-N026 | Theorems 2.6.2 and 2.6.3 (printed p. 29) | Universal centered sub-gaussian families and weights vs. the displayed psi-two denominators | Neither theorem assumes positive total scale. In Theorem 2.6.2 an all-zero-gauge family makes `sum_i ||X_i||_{psi_2}^2 = 0`; in Theorem 2.6.3 either that condition or `a = 0` makes `K^2 ||a||_2^2 = 0`. The printed real quotients then divide by zero, including `0/0` at `t = 0`, without stating a convention. | The exact Lean aliases retain every printed family, weight, and `t >= 0`. They use total real division and explicit zero-scale branches; zero intrinsic gauge forces the corresponding variables to vanish almost surely, while zero weights make the sum identically zero. | Pinned Theorems 2.6.2/2.6.3; completed 2.6.2 direct/round-trip judgments and exact-trigger adjudication SHA-256 `dae8e55765031999f7b2f896933f214dd4a02d1589db829ee2a18ab33a53e849`, which accepted the totalized boundary as proposition-level faithful-equivalent. | open | Preserve the universal domains and document the boundary convention in both audits; do not silently add a positive-energy or nonzero-weight hypothesis. |
