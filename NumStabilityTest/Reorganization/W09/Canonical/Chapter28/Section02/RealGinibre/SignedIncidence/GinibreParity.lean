import NumStability.Source.Higham.Chapter28.Section02.RealGinibre.SignedIncidence.GinibreParity

/-!
# GinibreParity canonical-only test (S_GIN_SIGNED, source)

Imports exactly one canonical module, so no sibling import can supply the
declarations checked below. They moved here from
`NumStability.Algorithms.TestMatrices.Higham28GinibreParity`
during wave W09 and must resolve from S_GIN_SIGNED alone.
-/
#check @NumStability.realGinibreParityIncrement
