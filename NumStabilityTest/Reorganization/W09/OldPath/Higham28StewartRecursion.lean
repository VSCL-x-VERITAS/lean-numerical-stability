import NumStability.Algorithms.TestMatrices.Higham28StewartRecursion

/-!
# Higham28StewartRecursion old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.split_succ
#check @NumStability.finCastEquiv_val
#check @NumStability.tail_input_apply
