import NumStability.Algorithms.TestMatrices.Higham28GinibreMultiplicity

/-!
# Higham28GinibreMultiplicity old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.ginibre_normalized_eigenpair
#check @NumStability.ginibreRegularFiberMultiplicity
#check @NumStability.exists_regular_incidence_preimage_of_root
