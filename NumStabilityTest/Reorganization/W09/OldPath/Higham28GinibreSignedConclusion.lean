import NumStability.Algorithms.TestMatrices.Higham28GinibreSignedConclusion

/-!
# Higham28GinibreSignedConclusion old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.ginibreCorollary31Factor_product_shift_two
#check @NumStability.two_mul_ginibreCorollary31Factor_product_div_pi
