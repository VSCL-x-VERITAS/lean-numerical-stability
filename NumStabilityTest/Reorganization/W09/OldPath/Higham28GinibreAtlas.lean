import NumStability.Algorithms.TestMatrices.Higham28GinibreAtlas

/-!
# Higham28GinibreAtlas old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.ginibreSwapIndex
#check @NumStability.ginibreCoordinateSwap
#check @NumStability.ginibreMatrixCoordinates
