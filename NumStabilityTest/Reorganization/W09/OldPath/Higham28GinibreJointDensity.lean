import NumStability.Algorithms.TestMatrices.Higham28GinibreJointDensity

/-!
# Higham28GinibreJointDensity old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.integrable_standardGaussianVectorDensity
#check @NumStability.integral_standardGaussianVectorDensity_eq_one
