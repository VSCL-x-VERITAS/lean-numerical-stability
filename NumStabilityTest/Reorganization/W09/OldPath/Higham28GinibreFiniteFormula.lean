import NumStability.Algorithms.TestMatrices.Higham28GinibreFiniteFormula

/-!
# Higham28GinibreFiniteFormula old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.ch28gf_charProdVal
#check @NumStability.ch28gf_integrable_hw
#check @NumStability.ch28gf_abs_le_one_add_sq
