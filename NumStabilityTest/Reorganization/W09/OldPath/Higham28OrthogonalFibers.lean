import NumStability.Algorithms.TestMatrices.Higham28OrthogonalFibers

/-!
# Higham28OrthogonalFibers old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.stewartFirstSection
#check @NumStability.stewartSphereSection
#check @NumStability.orthogonalTailExtract
