import NumStability.Algorithms.TestMatrices.Higham28GinibreTraceDensity

/-!
# Higham28GinibreTraceDensity old-path-only test

Imports only the historical path. Every declaration checked below moved to a
canonical module during wave W09, so this compiles only if the compatibility
module still re-exports it under its original name.
-/
#check @NumStability.ginibreMatrixSq
#check @NumStability.ginibreShiftShear
#check @NumStability.ginibreUnshiftShear
