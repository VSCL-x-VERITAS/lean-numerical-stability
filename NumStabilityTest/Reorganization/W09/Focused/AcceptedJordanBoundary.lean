import NumStability.Analysis.LinearOperators.Jordan.NormalForm.PrimaryDecomposition

/-!
# AcceptedJordanBoundary: accepted dependency boundary

B0009 requires the reusable companion material to import the accepted canonical `Jordan.NormalForm.PrimaryDecomposition` directly -- never the historical Jordan implementation and never a compatibility facade. This test imports exactly that pair.
-/
