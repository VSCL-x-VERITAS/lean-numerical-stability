import NumStability.Algorithms.LinearSystems.LeastSquares.RowSorting

/-!
# R11 protected consumer — `NumStability.Algorithms.LinearSystems.LeastSquares.RowSorting`

Integrator-owned shared consumer, imported unedited. R11 does not modify it.
This test compiles it against the R11 worker tree, proving the historical
import it still uses continues to resolve through the retained compatibility
wrapper. Its R0003 postimage (the retargeted canonical import) is validated
separately in a disposable checkout, because R0003 is integrator-owned.
-/
